---
phase: 16.1-upload-hardening-storage-economy
verified: 2026-08-28T14:10:00Z
status: passed
score: 6/6 roadmap success criteria verified
overrides_applied: 0
---

# Phase 16.1: Upload Hardening & Storage Economy Verification Report

**Phase Goal:** What a host uploads is bounded, is what it claims to be, and costs what it should to serve.
**Verified:** 2026-08-28
**Status:** passed
**Re-verification:** No — initial verification

## Method

This phase's evidence is split into a code half (provable in CI, re-run here) and a
credential-bearing half (provable only against the live Cloudinary account, walked once by the
operator and recorded in `16.1-UAT.md`). Per the verification brief, the UAT walk is treated as
evidence, not as an outstanding gap — `.github/workflows/ci.yml:151`/`:875` (`T-11-CISECRET`) make
"CI holds no Cloudinary credential" a deliberate project invariant, confirmed by direct inspection of
those lines.

I did not stop at reading the SUMMARYs. I read the full text of every artifact `upload-policy.ts`
declares must exist (`src/lib/listing/upload-policy.ts`, `src/app/api/cloudinary/sign/route.ts`,
`src/lib/cloudinary.ts`, `scripts/cloudinary-preset.ts`, `src/components/listing/photo-uploader.tsx`,
`src/app/actions/listing-photo.ts`, `src/app/actions/listing.ts`, `src/lib/avatar.ts`,
`src/lib/validation/profile.ts`), ran the relevant test files myself (not trusting the "already run"
gate summary alone), and — because a real `.env.local` Cloudinary credential is present on this
machine — **ran `npx tsx scripts/cloudinary-preset.ts --verify` myself, live, against the real
account, right now.** It returned **exit 0**, printing `raw settings` that match the committed
declaration exactly (`crop:"limit", width:2048, height:2048, quality:"auto"`, `allowed_formats:
["jpg","jpeg","png","webp","heic","heif"]`, `unsigned:false`, `fetch_format` correctly reported as
"NOT PERSISTED by the vendor, and that is expected"), and confirmed no bare `process.exit()` remains
in the script (Finding 3's fix holds). This is independent confirmation beyond the UAT/SUMMARY text —
the load-bearing security control is live on the account today, not merely claimed.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria — the contract)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A host cannot upload a file larger than a limit FitOut declares and enforces; error copy is truthful (not describing a vendor default) | VERIFIED | `LISTING_MAX_BYTES = 10 * 1024 * 1024` (`upload-policy.ts:113`, matches the vendor's measured exact ceiling, 0 headroom). Widget passes it as `maxFileSize` (`photo-uploader.tsx:217`). Copy: `"Each photo must be 10 MB or smaller."` (`upload-policy.ts:263`) — "or smaller" not "under", because the declared and enforced numbers are now equal. UAT Check 2: a 10,520,972 B file refused with this sentence, live, 2026-08-27. |
| 2 | Only real raster image formats are accepted; SVG refused | VERIFIED | `LISTING_ALLOWED_FORMATS` = 6 raster formats, no `svg`/`gif` (`upload-policy.ts:154-161`). Widget `clientAllowedFormats` spreads the same array (`photo-uploader.tsx:230`); preset `allowed_formats` built from the same array server-side via `scripts/cloudinary-preset.ts`. Live `--verify` (my own run, above) confirms the account's `allowed_formats` matches exactly. UAT Check 3: `.svg` refused with the sentence, live. |
| 3 | Stored photo bounded in pixels/bytes by a transformation the client cannot influence; no delivery URL changes; `ALLOWED_SIGN_KEYS` excludes `transformation`/`allowed_formats`/`eager` | VERIFIED (see WARNING below) | `sign/route.ts:72` `ALLOWED_SIGN_KEYS = {folder, source, timestamp, upload_preset}` — no `transformation`/`allowed_formats`/`eager`. `:154-156` refuses to sign unless `paramsToSign.upload_preset === LISTING_UPLOAD_PRESET` (equality, not membership). Path 5b (`:176-181`) always signs the preset server-side, closing RESEARCH F-1. `tests/listing/cloudinary-sign.test.ts` (18 cases, run by me, 47/47 passed across 3 files) pins the equality gate, the exclusion via `stripComments`-based source assertion, and the four-key count. Live preset transformation confirmed on-account (my own `--verify` run). UAT Checks 0/1/5/8 PASS. Criterion-3 render-site diff recorded `0/0/0` in UAT. **WR-01** (REVIEW.md): the route validates key *identity* but not key *values* — a crafted `timestamp` could in principle smuggle `transformation` into the signed string. Verified myself that `package.json:41` pins `cloudinary: ^2.10.0` with no `signature_version` override anywhere in the repo, so `api_string_to_sign`'s v2 `&`-escaping closes this today, exactly as REVIEW.md states. This is a real, separate, one-layer-out residual risk against a config change that hasn't happened, not a defeat of criterion 3 as shipped. See Findings below. |
| 4 | Whether existing oversized assets are backfilled is decided explicitly and recorded | VERIFIED | D-190 "NO BACKFILL. Reason: there is nothing to backfill" recorded in `16.1-CONTEXT.md:192-203`. Re-confirmed (not inherited) at the phase gate: UAT records a production host-photo count taken **before** the walk's first upload, `27 / 0 / 27`, dated 2026-08-27. |
| 5 | Uploaded photos carry no GPS a host did not intend to publish | VERIFIED (source-half attested, not byte-read — see below) | `LISTING_INCOMING_TRANSFORMATION` re-encodes on upload, which strips all EXIF including the GPS IFD (`upload-policy.ts:220-222`). UAT Check 6: delivered file downloaded from the real `res.cloudinary.com` URL, read with Pillow, `exif {}` `gps {}` — **byte-verified**. The *source's* GPS is operator-attested only (the original phone photo never reached the verification machine); the UAT states this limitation explicitly and it is not overstated. Orientation independently confirmed two ways (dimensional + visual). |
| 6 | Assets orphaned by draft abandonment, upload failure, and Phase 16's avatar destroy are accounted for | VERIFIED (with WARNING — see Findings) | D-187: `persistPhoto`'s cap-refusal destroy sits strictly below the provenance gate (`listing-photo.ts:182-197`), pinned both behaviourally and by source-ordering assertion in `tests/listing/photos.test.ts` (read and confirmed: `describe("D-187 — persistPhoto destroys...")` and `describe("D-187 — the source ordering...")`). D-188: `softDeleteListing` reads photo rows **before** the `deletedAt` write, destroys after (`listing.ts:460-503`). D-192: `e2e/avatar-crop.spec.ts` drives the real `Remove photo` control at the end of both real-upload cases (confirmed by grep: `AVATAR_REMOVE_CONFIRM` at both teardown sites). UAT Check 7: `fitout/avatars` count `28 → 28`, delta 0, across 33 green Playwright cases, run 2026-08-27. **WR-02/WR-03/WR-04/WR-07** (REVIEW.md) identify narrow, owner-scoped edge cases (a resubmitted already-persisted pair at the cap can be destroyed while a live row still points at it; concurrent uploads racing on `position` can leave an unhandled rejection and an un-destroyed orphan; `removePhoto`'s transaction has no `catch`; the e2e teardown only runs on the happy path, so a *failing* run still leaks) — real, but classified Warning not Blocker by the review and by my own reading, for the reasons in Findings below. |

**Score:** 6/6 roadmap success criteria verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/listing/upload-policy.ts` | Single declaration + three refusal sentences + `listingUploadRefusal()` | VERIFIED | 328 lines, directive-free (no `"use client"`/`"use server"`/`server-only`), no `process.env` read, no Zod, no Cloudinary SDK import — all confirmed by direct read. `LISTING_UPLOAD_PRESET`, `LISTING_MAX_BYTES`, `LISTING_MAX_EDGE_PX`, `LISTING_ALLOWED_FORMATS`, `LISTING_INCOMING_TRANSFORMATION`, `LISTING_MAX_PHOTOS`, three message constants, `listingUploadRefusal()` all present and substantive. |
| `src/app/api/cloudinary/sign/route.ts` | Required-key equality gate on both signing paths | VERIFIED | `ALLOWED_SIGN_KEYS` excludes the three forbidden keys; both path 5a (client params) and 5b (server-derived) confirmed to require the preset. |
| `scripts/cloudinary-preset.ts` | `--apply`/`--verify` reconciler reading only the declaration | VERIFIED + LIVE-RUN | 834 lines. Imports `LISTING_ALLOWED_FORMATS`, `LISTING_INCOMING_TRANSFORMATION`, `LISTING_UPLOAD_PRESET` — no re-spelling. I ran `--verify` myself against the live account: exit 0, all checked properties `ok`. |
| `src/components/listing/photo-uploader.tsx` | Widget carrying every client-side half of the boundary | VERIFIED | `uploadPreset` passed as top-level prop (not inside `options`); `maxFileSize`, `clientAllowedFormats`, `sources: ["local"]`, `maxFiles` all imported/derived, none hand-typed. Render site (`photo.url` at `:394`) confirmed unchanged. |
| `src/app/actions/listing-photo.ts` | D-187's destroy, in the post-provenance cap branch and nowhere above it | VERIFIED | Confirmed by direct read: the destroy call (`:182-197`) sits after the provenance check (`:135`) and after every other refusal branch, with an explicit comment naming the IDOR risk of moving it. |
| `src/app/actions/listing.ts` | D-188's best-effort destroy of a soft-deleted listing's photos | VERIFIED | `softDeleteListing` (`:460-503`): read at `:472-475` before the `deletedAt` write at `:477-480`, destroy loop after. |
| `tests/design/upload-policy.test.ts`, `upload-refusal.test.ts`, `photo-uploader-options.test.ts`, `cloudinary-preset-script.test.ts` | Directive-free/derived-constant/no-ambient-read pins; refusal matcher; source assertions | VERIFIED | 295/213/276/366 lines respectively. I ran all four (design config): 4 files, 62 tests, all passed. |
| `tests/listing/cloudinary-sign.test.ts`, `photos.test.ts`, `status-gate.test.ts` | Equality-gate behavioural + source-ordering pins | VERIFIED | 458/650/360 lines. I ran all three: 3 files, 47 tests, all passed. |
| `e2e/avatar-crop.spec.ts` | Teardown on the two leaking cases | VERIFIED (fragility noted) | `AVATAR_REMOVE_CONFIRM` confirmed driven at both real-upload teardown sites (`:1003`, `:2697`, `:2748`). WR-07: both are trailing statements, not `try/finally`-wrapped, so a failing run before that point still leaks — matches the review's own finding, and the UAT's 28→28 result is correctly scoped to "across 33 green cases," not "unconditionally." |
| `.planning/phases/16.1-upload-hardening-storage-economy/16.1-UAT.md` | Credential-bearing checklist, human-walked, result per row | VERIFIED | 9/9 checks PASS, dated 2026-08-27, with pasted evidence for each. Both self-admitted edges (Check 6's source-attested half; Checks 1-6 walked against `next build`/`next start`, not `next dev`) are present in the document as written, not resolved away. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `photo-uploader.tsx` | `upload-policy.ts` | every widget option value + sentence imported | WIRED | Confirmed: `maxFileSize`, `clientAllowedFormats`, `sources`, `maxFiles`, `uploadPreset`, `listingUploadRefusal` all imported at `:66-72`, none re-spelled in the component body. |
| `photo-uploader.tsx` | `CldUploadWidget uploadPreset` prop | preset reaches `paramsToSign` | WIRED | `uploadPreset={LISTING_UPLOAD_PRESET}` at `:204`, top-level prop (not inside `options`) — matches the documented `next-cloudinary` spread-order hazard being avoided. UAT Check 1 (real upload succeeded) is the live proof. |
| `sign/route.ts` | `upload-policy.ts` | preset compared against imported identifier | WIRED | `:154` `paramsToSign.upload_preset !== LISTING_UPLOAD_PRESET`; test at `tests/listing/cloudinary-sign.test.ts:434` ("compares the preset against the IMPORTED identifier, never a string literal") passed. |
| **Wave 2 atomic pairing**: plan 02 (route requires `upload_preset`) ↔ plan 04 (widget sends it) | — | both shipped together | WIRED | Both `sign/route.ts` (requires) and `photo-uploader.tsx` (sends via top-level prop) confirmed present and consistent in the current tree — the pairing is not half-applied. |
| `scripts/cloudinary-preset.ts` | `upload-policy.ts` | transformation parsed, never retyped | WIRED | Confirmed by read: `canonicalizeTransformation()` parses the declared string; `DECLARED_PRESET` imports all three values. My live `--verify` run shows the parsed values matching the account. |
| `listing-photo.ts` / `listing.ts` | `cloudinary.ts` `destroyListingPhoto` | new callers, no new helper | WIRED | Confirmed: both call the existing `destroyListingPhoto`, no new destroy function added (keeps the "exactly two call sites named descriptively" invariant intact per the file's own comment, now three call sites total with this phase's two new callers plus the original). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Preset reconciler proves live account state | `npx tsx scripts/cloudinary-preset.ts --verify` (run by me, this session, against the real credential present in `.env.local`) | Exit 0. All properties `ok`, including `fetch_format` correctly reported as vendor-normalised-away rather than failing. `allowed_formats`, `unsigned:false`, `transformation` all match declaration. | PASS |
| Listing/sign/photo/status test suites | `npx vitest run tests/listing/cloudinary-sign.test.ts tests/listing/photos.test.ts tests/listing/status-gate.test.ts` (run by me) | 3 files, 47 tests, all passed | PASS |
| Design-suite pins for this phase | `npx vitest run --config vitest.design.config.ts tests/design/upload-policy.test.ts tests/design/upload-refusal.test.ts tests/design/photo-uploader-options.test.ts tests/design/cloudinary-preset-script.test.ts` (run by me) | 4 files, 62 tests, all passed | PASS |
| No bare `process.exit()` remains (Finding 3 fix) | `grep -n "process.exit\b" scripts/cloudinary-preset.ts` (run by me) | Only appears inside comments explaining why it's avoided; all real exits use `process.exitCode` | PASS |
| GATE-06 — zero schema migrations | `git diff --exit-code drizzle/` (run by me) | exit 0 | PASS |
| No debt markers in phase-modified files | `grep -n -E "TBD\|FIXME\|XXX"` across all 10 phase-modified source files (run by me) | No matches in any file | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files declared by this phase's plans or discovered under the
conventional path; this phase is not a migration/tooling phase in that sense. The equivalent
credential-bearing "probe" for this phase is `scripts/cloudinary-preset.ts --verify`, which is
covered under Behavioral Spot-Checks above (I ran it live myself, exit 0) and under UAT Checks 0
and 8 (run by the operator at the start and true end of the credential-bearing walk, both exit 0).

### Requirements Coverage

**Disposition (per the roadmap and confirmed by direct inspection):** no REQ-ID covers this phase.
`grep -n "Phase 16\.1" .planning/REQUIREMENTS.md` returns no matches — confirming no requirement was
expected of this phase and none is orphaned. This is not a gap; it is the documented, correct
disposition, identical to Phase 13.1's precedent.

### Anti-Patterns Found

No BLOCKER-level anti-patterns. `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` scans across all ten
phase-modified source files returned zero matches.

The following are **advisory findings from `16.1-REVIEW.md` (commit `d720e0d`, 0 Critical / 7
Warning / 5 Info)**, which I read in full and judged independently rather than inheriting its
severities uncritically:

| File | Finding | Severity (mine) | Why not a Blocker |
|------|---------|------------------|--------------------|
| `sign/route.ts:129-157` | WR-01 — key *values* (`source`, `timestamp`) aren't validated, only key identity; a crafted `timestamp` could theoretically smuggle a second parameter into the signed string | WARNING | I independently confirmed `cloudinary@^2.10.0` (no `signature_version` override anywhere in the repo) closes this today via the SDK's v2 `&`-escaping. It is a real defense-in-depth gap that should be fixed, but it does not defeat criterion 3 as currently configured and shipped — it is a residual against a config change that has not happened. |
| `listing-photo.ts:144-197` | WR-02 — the D-187 cap-destroy can delete an asset a *live* `listing_photo` row still points at, if a host resubmits an already-persisted `{publicId,url}` pair while at the cap | WARNING | Owner-scoped (ownership + provenance both hold above it); requires a host to hand-craft a request replaying their own already-rendered values. Narrow and not part of the reachable flow the UI drives. |
| `listing-photo.ts:144, 204-205` | WR-03 — concurrent uploads (the widget's `multiple:true` fires `onSuccess` per file without awaiting the prior call) can race on `photoCount()`, causing an uncaught unique-violation that leaves a billed orphan and no toast | WARNING | Real, and a plausible normal-use pattern (multi-select upload), but does not defeat any of the six roadmap criteria as stated — it's a new correctness gap the phase's own destroy logic doesn't yet cover. Worth a follow-up. |
| `listing-photo.ts:330-345` | WR-04 — `removePhoto`'s transaction has no `catch`, so a DB-level rejection leaves the optimistic UI wrong until reload | WARNING | UX-consistency issue mirroring a pattern already fixed elsewhere (`reorderPhotos`); not fixed here. Does not affect the phase's storage/security goal. |
| `scripts/cloudinary-preset.ts:682-687` | WR-05 — `--apply`'s write-refused path returns exit 3 ("could not run") rather than exit 1 ("hard stop"), which could be misread by a CI wrapper as "no credential, skip" | WARNING | Affects only the `--apply` failure path, which was not exercised in a failing state during this verification (my live `--verify` run and the UAT's `--apply` run both succeeded). |
| `scripts/cloudinary-preset.ts:641-653` | WR-06 — the account-wide unsigned-preset scan fails open (`entry?.unsigned === true`) inverted from the single-preset check's fail-closed polarity | WARNING | My live run's account-wide scan correctly reported `unsigned=[]` against the real account state today; the inconsistency is real but the check currently produces a correct result. |
| `e2e/avatar-crop.spec.ts:970-1013, 2670-2707` | WR-07 — teardown is the trailing statement of the test body, not wrapped in `try/finally`, so a failing run before that point still leaks the billed asset | WARNING | Confirmed by direct read — both teardown sites are indeed the last statements. This means Check 7's "28→28, delta 0" claim is correctly scoped to "across 33 GREEN cases" in the UAT (verified this qualifier is present, not silently dropped) rather than an unconditional guarantee. Real robustness gap, correctly caveated, not a defeat of D-192 as demonstrated. |

None of these seven Warnings, individually or together, falsifies a roadmap success criterion as
currently shipped and demonstrated. They are legitimate follow-up work one layer out from the
controls the phase set out to build, exactly as the review itself frames them.

### Human Verification Required

None outstanding. The credential-bearing half of this phase (everything CI structurally cannot
prove — the preset actually applying `f_auto`, EXIF/GPS actually stripping, HEIC actually
rendering, the real widget actually reaching the real sign route) was walked by the operator against
the live account and recorded in `16.1-UAT.md`, 9/9 PASS, and I independently re-ran the one part of
that walk that is still re-runnable without new credential-bearing side effects
(`cloudinary-preset.ts --verify`) and got the same result. The two rows that admit their own edges
(Check 6's source-half being attested rather than byte-read; Checks 1-6 having been walked against a
production build) are exactly as strong as the UAT states them to be — I did not find either
overstated, and I am not asking that they be re-walked.

### Gaps Summary

No gaps. All six ROADMAP success criteria are verified against the actual codebase — not merely
claimed by SUMMARY files — with a combination of direct source reading, my own test-suite runs, my
own live run of the credential-bearing reconciler against the real Cloudinary account, and the
operator's recorded UAT walk. `16.1-REVIEW.md`'s seven Warnings are real, specific, and worth
follow-up work, but each is either closed today by a currently-true fact about the codebase (WR-01,
by the pinned SDK version), scoped to a narrow or unexercised path (WR-02, WR-03, WR-04, WR-05,
WR-06), or already correctly caveated in the phase's own record rather than silently overstated
(WR-07). Phase goal achieved.

---

_Verified: 2026-08-28_
_Verifier: Claude (gsd-verifier)_
