---
phase: 16.1-upload-hardening-storage-economy
plan: 06
subsystem: api
tags: [cloudinary, avatar, comments, e2e, teardown, deferred-items, D-191, D-192, D-187]

# Dependency graph
requires:
  - phase: 16.1-upload-hardening-storage-economy (plan 02)
    provides: "src/lib/cloudinary.ts in its post-16.1-02 state — signListingUpload deleted, the CDN-invalidation token count at exactly 2, which is the constraint the :19 comment edit had to respect"
  - phase: 16.1-upload-hardening-storage-economy (plan 05)
    provides: "D-187's declined sweep as a settled fact, and the explicit hand-off of the three stale avatar-path comments this plan corrects"
  - phase: 16-image-crop-framing
    provides: "e2e/avatar-crop.spec.ts's CR-03 case (the shipped teardown model), e2e/helpers/avatar-session.ts (the fixture-refusing signup helper), deferred-items.md D4"
provides:
  - "No file under src/ or e2e/ promises a Phase 16.1 orphan audit or a Phase 16.1 pixel guard — all seven Phase-16.1 source references now describe a decision"
  - "D-191 recorded at all three of its sites WITH the condition that reverses it: the day any server-side code decodes uploaded image bytes"
  - "D-187 recorded at both avatar-path sites, including WHICH kind of leftover each one leaves"
  - "e2e/avatar-crop.spec.ts leaves no Cloudinary asset behind — all three real-upload cases now remove for real, through the product's own path"
  - "Phase 16's deferred item D4 DISCHARGED, with the two measured corrections the fix required"
affects: [16.1-07 UAT]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A deferral closed as UNNECESSARY carries the condition that would reverse it, in the source, in the cloudinary-provenance.ts:19-21 mould"
    - "Test teardown drives the SHIPPED control rather than the credentialed helper — no forged response, no secret in the test process"
    - "An identifier is named DESCRIPTIVELY rather than quoted when a machine count depends on its absence (cloudinary.ts:125-128's resolution, applied twice more)"

key-files:
  created: []
  modified:
    - src/lib/avatar.ts
    - src/lib/validation/profile.ts
    - src/lib/cloudinary.ts
    - src/app/actions/avatar.ts
    - e2e/avatar-crop.spec.ts
    - .planning/phases/16-image-crop-framing/deferred-items.md

key-decisions:
  - "The two D-187 comments say the leftover they leave is a TRUE orphan, NOT abandoned-but-referenced — the plan's action text had it the wrong way round for these two sites, because removeAvatarAction nulls the row BEFORE the destroy. The referenced-not-orphaned fact is true of the e2e assets and is recorded there instead"
  - "The e2e comment rewrites were committed with Task 2's teardown, not with Task 1 — they say a teardown exists, and no commit should contain a comment that is false at that commit"
  - "The credentialed avatar destroy helper is named descriptively, never quoted, in both teardown comments — Task 2's own acceptance criterion asserts that identifier appears nowhere under e2e/"

patterns-established:
  - "A comment describing work that was CONSIDERED AND REJECTED is worse than no comment; the correction states the decision, the reason, and the condition that would reverse it"
  - "When a plan's own instruction would install a false sentence, write the true one and record the correction — that is the defect the plan exists to remove"

requirements-completed: []

# Metrics
duration: 16min
completed: 2026-08-26
---

# Phase 16.1 Plan 06: Seven Truthful Comments and a Suite That Stops Billing Us Summary

**Every one of the seven source comments that pointed at Phase 16.1 now describes a DECISION rather than a promise — three record D-191's pixel guard as closed-as-unnecessary with the server-side decoder that would reverse it, two record D-187's declined sweep and which kind of leftover they actually leave, and two record that the e2e cases they sit on now clean up after themselves — while `e2e/avatar-crop.spec.ts` stops adding two permanently-billed `fitout/avatars/*` assets per local run by pressing the same `Remove photo` control a real person would, with no `afterAll`, no fixture, no seed and no credential in the test process.**

## Performance

- **Duration:** 16 min
- **Started:** 2026-08-26T18:45:29Z
- **Completed:** 2026-08-26T19:01:55Z
- **Tasks:** 2
- **Files modified:** 6 (0 created, 6 modified)

## Accomplishments

- **Five comments that would have become FALSE the day this phase ships are gone, and the two that were merely imprecise are sharper.** `grep -rn "is Phase 16.1 scope\|Phase 16.1's orphan audit\|Phase 16.1's audit\|Phase 16.1's sweep" src/ e2e/` returns **0**. The four surviving `Phase 16.1` mentions all read *"considered by Phase 16.1 and DECLINED"* — a decision a reader can act on rather than a phase to go looking through.
- **D-191's reversal condition is in the source at all three sites, not only in CONTEXT.md.** `src/lib/avatar.ts`, `src/lib/cloudinary.ts` and `src/lib/validation/profile.ts` each state, in that file's own voice, that the pixel-dimension guard is closed as UNNECESSARY rather than as done — the server is handed a `File`/`Buffer` and never decodes it, so a 40MP image has no decoder of ours to detonate against, and D-171's retained 400×400 `c_fill` already bounds storage — and each carries the condition that voids the decision: **the day any server-side code decodes uploaded image bytes, for dimensions, a blurhash or a thumbnail.** The shape is copied from `cloudinary-provenance.ts:19-21`'s CNAME clause, which is the repo's shipped model for exactly this.
- **`src/lib/cloudinary.ts:19` was rewritten without disturbing plan 16-12's count.** `grep -c "invalidate" src/lib/cloudinary.ts` still returns exactly **2** — the two destroy call sites, and nothing in the ten new comment lines quotes the token. The file's own `:125-128` records why that matters; this edit is the first test of that constraint since it was written.
- **Both D-187 sites now say what was decided AND which kind of leftover they leave** — see Deviations, because the second half is a correction to the plan's own instruction. `removeAvatarAction` nulls the row *before* the destroy, so a failed destroy leaves a **TRUE orphan**: bytes with no row, of the very kind a diff sweep could have caught, tolerated by decision rather than by oversight. The *abandoned-but-referenced* fact is true of the e2e-leaked assets and is recorded there, where it explains why teardown was the only mechanism that could reach them.
- **The suite stops bleeding.** Three cases in `avatar-crop.spec.ts` perform a real Cloudinary upload; the third already pressed `Remove photo` and cleaned up. The other two — `CROP-01 / Delta-3` and `the removal confirm: `Keep photo` holds focus` — now do the same, modelled line-for-line on the third, including the `dialogOf` scoping that is load-bearing because `AVATAR_REMOVE_CONFIRM` and `AVATAR_REMOVE_LABEL` are the same string and the page control is still mounted at that instant. `grep -c "afterAll"` is still **0** and `grep -rc "destroyAvatar" e2e/` is still **0**: no hook, no fixture, no seed, no `CLOUDINARY_API_SECRET` anywhere near the test process.
- **The measurement that made the shape non-obvious is now in the file, so nobody re-derives it.** Both teardown comments record that `overwrite: true` + `public_id: userId` cannot make repeat runs idempotent, because `e2e/helpers/avatar-session.ts:56` mints a fresh randomised `e2e.avatar.<Date.now()>.<random>@example.com` every run — new signup, new `user.id`, new `public_id`, and `overwrite: true` overwrites nothing. Pinning the test user is the *expensive* option against a helper whose header refuses seeds and fixtures on the record.
- **Phase 16's D4 is DISCHARGED with the two corrections the fix required**, both measured: it was **two** cases per run and not one, and its "cheapest correct handling" line pointed at an audit that was **declined** (D-187) and that could not have swept these assets in any case — all 28 `fitout/avatars` assets have a matching `user.avatar_public_id` row, so a diff sees nothing to delete.

## Task Commits

Each task was committed atomically:

1. **Task 1: seven comments that describe what was actually decided (the five `src/` sites)** — `8514bac` (docs)
2. **Task 2: the two leaking e2e cases end by pressing the button a real person would (+ the two e2e comment sites, + D4 discharged)** — `ea0d9a8` (test)

## Files Created/Modified

- `src/lib/avatar.ts` (**modified**, +10/−1) — the `WHAT THIS FILE DELIBERATELY DOES NOT DO` bullet on `AVATAR_MIN_SOURCE_PX` now closes D-191 as unnecessary and states the reversal condition. No export, constant or string touched.
- `src/lib/validation/profile.ts` (**modified**, +6/−2) — `avatarFileSchema`'s docblock paragraph on the missing pixel refine; the D-164 / D-166 pointer becomes a D-191 record with the same reversal condition. The refusal messages and every `.refine` are byte-unchanged (D-183 stands).
- `src/lib/cloudinary.ts` (**modified**, +9/−1) — the header's `Deleting it opens that hole` sentence; D-191's argument and reversal condition in the file's own voice, written around the CDN-invalidation token rather than through it.
- `src/app/actions/avatar.ts` (**modified**, +18/−3) — `removeAvatarAction`'s order-is-behaviour docblock and the destroy `catch`'s inline comment. D-169's tolerance statement is intact; only the sweep promise changed, and both now name the kind of leftover.
- `e2e/avatar-crop.spec.ts` (**modified**, +96/−8) — the Delta-3 describe header and the `Keep photo` case's real-upload paragraph rewritten; a teardown block appended to each of those two cases. No case renamed, added or dropped; no existing assertion touched; no restructure.
- `.planning/phases/16-image-crop-framing/deferred-items.md` (**modified**, +45/−3) — D4 gains a `> **DISCHARGED 2026-08-26 …**` block in the file's own D1/D10 convention, and its superseded "cheapest correct handling" / "suggested owner" lines are marked as such. No other entry touched.

## Decisions Made

- **The two D-187 sites say TRUE orphan, not abandoned-but-referenced.** The plan's action text asked for the opposite; it is wrong for these two specific sites and right for the e2e ones. See Deviations — writing it as instructed would have installed a new false comment in the plan whose entire purpose is removing false comments.
- **The e2e comment rewrites shipped in Task 2's commit rather than Task 1's.** Both rewritten paragraphs say a teardown exists at the foot of the case. Committing them a commit early would have produced exactly one commit in which a comment describes something not in the tree. The final tree is identical either way; every Task 1 acceptance grep was evaluated against the working tree at Task 1 verification time and passed.
- **The credentialed avatar destroy helper is named descriptively in both teardown comments, never quoted.** Task 2's action text asks the comments to explain why a direct call was rejected; Task 2's acceptance criterion asserts that identifier appears **nowhere** under `e2e/`. Both are satisfiable at once by `src/lib/cloudinary.ts:125-128`'s own resolution, which the comments cite so the spelling reads as a choice rather than as coyness.
- **The Delta-3 teardown's interaction with the live `page.route("**/*")` handler is stated in the comment.** The handler is still installed when the teardown's action POST goes out; `gate` is already resolved by the `release()` above, so it passes straight through, and `heldActions` was asserted before the teardown runs. Recorded because a future reader moving that assertion would otherwise have to work it out.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The plan's instruction for the two D-187 comments would have installed a factually false sentence**

- **Found during:** Task 1
- **Issue:** The plan's action text says of `src/app/actions/avatar.ts:127` and `:188`: *"an avatar orphan produced here is abandoned-but-referenced (the `user` row still carries the `avatar_public_id`), so a diff sweep would never have found it in the first place."* That is false for these two sites. `removeAvatarAction`'s **step 2 nulls both columns and step 3 then destroys the asset** — the ordering the very docblock being edited exists to explain. So when the destroy fails, the row no longer carries `avatar_public_id` and what is left is bytes with **no** row: a true orphan, and precisely the kind a diff sweep *could* have found. The abandoned-but-referenced measurement (28 assets / 28 rows, RESEARCH § I-3) is about the e2e-leaked assets, which come from `uploadAvatarAction` on throwaway accounts whose rows survive.
- **Fix:** Both sites state the truth instead: the leftover is a TRUE orphan, one of the few a sweep could have caught, tolerated by decision rather than oversight — and D-187 declined the sweep anyway, for its own stated reason. The docblock also names the OTHER kind explicitly and points at `e2e/avatar-crop.spec.ts` and D-192, so the distinction is recorded rather than lost. The referenced-not-orphaned fact appears at both e2e sites and in D4's discharge block, where it is true and where it is the load-bearing argument for teardown.
- **Files modified:** `src/app/actions/avatar.ts`
- **Verification:** Re-read `removeAvatarAction` end to end against the edit; the `db.update(...).set({ avatarUrl: null, avatarPublicId: null })` at step 2 precedes the `if (publicId)` destroy at step 3, unchanged by this plan. Plan must_have truth #6 (the 28-asset measurement) is still asserted, at the sites where it holds.
- **Committed in:** `8514bac`

**2. [Rule 3 - Blocking] `destroyAvatar` cannot be both explained by name and absent from `e2e/`**

- **Found during:** Task 2
- **Issue:** Task 2's action text requires the teardown comments to say *"the teardown drives the SHIPPED removal control rather than calling `destroyAvatar` directly"*; Task 2's acceptance criterion requires `grep -c "destroyAvatar" e2e/` to return `0`. Written literally the comments made it 2 and the criterion red.
- **Fix:** Both comments name it descriptively — *"the credentialed avatar destroy helper in `src/lib/cloudinary.ts`"* — and one of them states in a parenthesis why it is spelled that way, citing `src/lib/cloudinary.ts:125-128`, which is the repo's own precedent for a comment that must not disturb a count. The rejection is explained in full; only the token is avoided, exactly as the plan requires at `cloudinary.ts:19` for the same class of reason.
- **Files modified:** `e2e/avatar-crop.spec.ts`
- **Verification:** `grep -rc "destroyAvatar" e2e/` → 0 files with a match. Both comments still state the credential argument and the no-forgery argument in full.
- **Committed in:** `ea0d9a8`

**3. [Rule 2 - Missing critical functionality] The e2e comment edits moved from Task 1's commit to Task 2's**

- **Found during:** Task 1 (before commit)
- **Issue:** Task 1 rewrites the two e2e comments to say *"this case now cleans up after itself by driving the shipped removal control"*; Task 2 adds the teardown they describe. Committed as sequenced, `8514bac` would have contained two comments that were false **at that commit** — the exact defect this plan exists to remove, introduced by the plan's own task ordering.
- **Fix:** Task 1's commit stages the four `src/` files; the e2e comment rewrites were held in the working tree and committed alongside the teardown in `ea0d9a8`. Nothing was dropped or re-worded, and every Task 1 acceptance grep runs over the working tree, where both edits were present and green at Task 1 verification time.
- **Files modified:** none beyond the plan's set — this is a staging decision, not a content change.
- **Verification:** `git diff HEAD~2 HEAD --stat` shows exactly the plan's six files; `git show 8514bac --stat` shows four files, `git show ea0d9a8 --stat` shows two.
- **Committed in:** `8514bac` / `ea0d9a8`

---

**Total deviations:** 3 auto-fixed (1 × Rule 1, 1 × Rule 2, 1 × Rule 3)
**Impact on plan:** No scope change. The file set is exactly the plan's six files, no code, identifier or string changed anywhere under `src/`, and every acceptance criterion in both tasks passes as literally written. Deviation 1 is the only content correction, and it makes a comment true that the plan would have made false.

## Verification Results

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | exit 0 (run after each task) |
| `npm run lint` | exit 0 (25 pre-existing warnings in unrelated files, 0 errors) |
| `npx eslint e2e/avatar-crop.spec.ts` | exit 0 |
| `npx vitest run tests/use-server-exports.test.ts` | exit 0, 4 passed — Task 1's named gate |
| `npm test` | exit 0, **185 files passed / 2 skipped, 2140 tests passed / 5 skipped** (run after each task; unchanged, as expected for a comment-and-e2e plan) |
| `npm run build` | exit 0 |
| `npx playwright test e2e/avatar-crop.spec.ts --list` | exit 0, **33 tests in 1 file** — byte-identical case-name list before and after, verified by diffing the two listings with line numbers stripped |
| `git diff HEAD~2 -U0 -- src/` non-comment lines | **0** — comment-only, as required |
| `git diff --exit-code HEAD~2 HEAD -- drizzle/` | exit 0 — **GATE-06 intact** |
| `git diff --exit-code HEAD~2 HEAD -- upload-policy.ts sign/route.ts photo-uploader.tsx listing-photo.ts listing.ts` | exit 0 — **no earlier plan's work re-opened** |

Grep criteria, all as the plan words them:

```
Task 1
  "is Phase 16.1 scope" in src/+e2e/ ................. 0   (0 required)
  "Phase 16.1's orphan audit" in src/+e2e/ ........... 0   (0 required)
  "Phase 16.1's audit" in src/+e2e/ .................. 0   (0 required)
  "Phase 16.1's sweep" in src/+e2e/ .................. 0   (0 required)
  invalidate in src/lib/cloudinary.ts ................ 2   (exactly 2 required — 16-12's count)
  D-191 in src/lib/avatar.ts ......................... 2   (>= 1 required)
  D-191 in src/lib/cloudinary.ts ..................... 2   (>= 1 required)
  D-191 in src/lib/validation/profile.ts ............. 2   (>= 1 required)
  decode* in src/lib/avatar.ts ....................... 7   (>= 1 in the edited block)
  decode* in src/lib/cloudinary.ts ................... 3   (>= 1 in the edited block)
  decode* in src/lib/validation/profile.ts ........... 4   (>= 1 in the edited block)

Task 2
  AVATAR_REMOVE_CONFIRM in avatar-crop.spec.ts ....... 8   (>= 3 required; 4 before)
  afterAll in avatar-crop.spec.ts .................... 0   (unchanged from 0 — no hook introduced)
  destroyAvatar in e2e/ .............................. 0   (0 required)
  DISCHARGED in deferred-items.md .................... 2   (>= 1 required)
```

The four `Phase 16.1` mentions that remain in `src/` and `e2e/` are the corrected ones, and all four read as a decision:

```
src/app/actions/avatar.ts:129   "…was considered by Phase 16.1 and DECLINED (D-187)."
src/app/actions/avatar.ts:201   "…the Admin-API diff reaper was considered by Phase 16.1 and declined…"
e2e/avatar-crop.spec.ts:862     "…Phase 16.1 DECLINED it (D-187) and closed the sources of new leftovers instead…"
e2e/avatar-crop.spec.ts:2573    "…Phase 16.1 declined it (D-187), and it would never have reached these assets anyway…"
```

## Success Criteria

- [x] All seven Phase-16.1 source references describe a decision, not a promise; the five that would have become false are gone
- [x] D-191's reversal condition — a server-side decoder — appears in all three of its sites
- [x] The two D-187 sites say the sweep was declined and why, and distinguish the referenced kind of leftover from the orphaned kind (see Deviation 1 — corrected relative to the plan's wording, so that both sentences are true)
- [x] Both leaking e2e cases end by driving `Remove photo`, with no `afterAll`, no fixture, no seed and no credential
- [x] Phase 16's deferred item D4 is discharged with what actually closed it

## Issues Encountered

None beyond the three recorded under Deviations. No auth gate, no package install, no architectural decision, no blocker. No dependency was added — `T-16.1-SC` holds trivially, and it is the whole of D-191 that nothing was.

`npm test` prints its standing `[test-db] LEAKED WRITES` banner (2 `public.audit` rows from `recordAudit`'s module-level db singleton, `action=guest-email` and `action=notify`). Pre-existing, contained, documented, unrelated to this plan — no file this plan touches writes an audit row — and the suite exits 0. The same condition plans 16.1-02 and 16.1-05 recorded.

## Known Stubs

None. This plan adds no code path: every `src/` change is a comment, and the two e2e additions are teardown against shipped controls that already have a case asserting them.

⚠ Not a stub, restated once more so the next plan does not mistake it for one: **the Cloudinary preset `fitout_listing_v1` still does not exist on the account** — `scripts/cloudinary-preset.ts --apply` is 16.1-07's operator step. Nothing here depends on it; the avatar path uses `upload_stream` with an inline transformation and no preset at all.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change. No credential entered any process that did not already hold one.

Register dispositions delivered by this plan:

- **T-16.1-20** (decompression bomb against a server-side decoder) — *accepted, conditionally, and the condition is now in three source comments.* `uploadAvatarAction` does `arrayBuffer()` → `Buffer` → `upload_stream` and never decodes; storage is bounded by D-171's retained 400×400 `c_fill`. The acceptance is void the day any server-side code decodes uploaded bytes, and each of the three sites says so in as many words.
- **T-16.1-21** (our own tooling adding two permanently-billed abandoned assets per local run, invisible to any diff sweep because both are referenced by a `user` row) — *mitigated.* D-192's teardown through the shipped removal control, which deletes the asset AND nulls the row. The mitigation is local-only because the bleed is local-only: no CI job holds a Cloudinary credential and this spec is not among the specs CI runs.
- **T-16.1-22** (a credential entering the e2e process to enable a direct destroy teardown) — *avoided, and machine-asserted.* `grep -rc "destroyAvatar" e2e/` is 0 and no e2e helper reads `CLOUDINARY_API_SECRET`.
- **T-16.1-02** (the arbitrary-delete IDOR) — untouched, owned by 16.1-05. This plan adds no destroy call site under `src/`; the e2e teardown deletes only the throwaway account's OWN avatar through the shipped, session-scoped, ownership-free-by-construction action (`removeAvatarAction` takes no arguments at all).
- **T-16.1-01** (client `transformation` past the ceiling) — untouched, owned by 16.1-02. No signing path and no upload parameter changed.
- **T-16.1-SC** (supply chain) — *holds trivially.* No `npm install`, no image-header parser, no `sharp`, no decoder of any kind.

## User Setup Required

None from this plan.

**One instruction for 16.1-07's operator step, recorded here rather than faked as a command:** Task 2's teardown is credential-bearing to *verify*, not to run in CI. Before the UAT, note the `fitout/avatars` asset count on the account (28 as of 2026-08-26); run `npx playwright test e2e/avatar-crop.spec.ts` locally with Cloudinary credentials present; confirm the count is **unchanged** afterwards. Before this plan the same run raised it by 2. CI structurally cannot perform this check (`T-11-CISECRET`, `ci.yml:151` / `:757` / `:875`) and this spec is one of the eleven excluded from the CI e2e set.

## Next Phase Readiness

- **16.1-07 (UAT)** — the check above is the one new thing to fold into the walkthrough. Note also that the two teardowns each drive `removeAvatarAction` against the live account, so a UAT run of this spec now *deletes* two avatars it just created; that is the intended behaviour and is worth knowing before the walkthrough rather than after.
- **Anyone editing `src/lib/cloudinary.ts`** — the header is now longer, and every added line was written around the CDN-invalidation token on purpose. `:125-128` explains the rule; `grep -c "invalidate"` must stay at 2.
- **Anyone who adds a server-side image decoder** — three comments (`src/lib/avatar.ts`, `src/lib/cloudinary.ts`, `src/lib/validation/profile.ts`) name that act as the condition that voids D-191 and makes a pixel-dimension guard necessary. They are the alarm; they are not a reminder to be tidied away.
- **Anyone adding a case to `e2e/avatar-crop.spec.ts` that performs a real upload** — all three existing real-upload cases now end by pressing `Remove photo`. A fourth that does not would silently restore the bleed, and nothing in CI would see it.

---
*Phase: 16.1-upload-hardening-storage-economy*
*Completed: 2026-08-26*

## Self-Check: PASSED

All 6 modified files and the SUMMARY exist on disk; both task commits (`8514bac`, `ea0d9a8`) are present in `git log`.
