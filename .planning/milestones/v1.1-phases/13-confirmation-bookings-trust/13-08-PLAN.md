---
phase: 13-confirmation-bookings-trust
plan: 08
type: execute
wave: 3
depends_on: ["13-02", "13-05"]
files_modified:
  - src/app/(app)/bookings/[id]/group/page.tsx
  - src/app/(public)/invite/[token]/page.tsx
  - src/components/group/invite-card.tsx
  - src/components/group/headcount-meter.tsx
  - src/components/group/attendee-roster.tsx
  - src/components/group/share-link-box.tsx
  - src/components/group/rsvp-form.tsx
  - src/components/group/rsvp-confirmation.tsx
  - src/components/group/top-up-nudge.tsx
  - tests/design/card-pattern-coverage.test.ts
  - tests/group/group-surface-shell.test.tsx
autonomous: true
requirements: [TRUST-02, STATE-08]
must_haves:
  truths:
    - "The two group surfaces read as the same product as the rest of the booker path — same patterns, same type roles, same 44px targets (D-79)"
    - "An organiser contacting support from the group page quotes the same booking reference as everywhere else (D-78, TRUST-02)"
    - "No net-new group capability ships: no new filter, no new roster action, no new invite mechanism (D-79)"
    - "The invite route keeps its no-index/no-referrer metadata and its textually identical 404 (the 08-06 oracle)"
  artifacts:
    - path: "src/app/(app)/bookings/[id]/group/page.tsx"
      provides: "PageHeader + PanelCard + RowCard adoption and the booking reference, with zero capability added (D-79)"
    - path: "src/app/(public)/invite/[token]/page.tsx"
      provides: "the design-system pass with its security metadata and 404 parity preserved"
  key_links:
    - from: "src/app/(app)/bookings/[id]/group/page.tsx"
      to: "src/components/booking/booking-reference.tsx"
      via: "the same FIT- string an organiser quotes to support (TRUST-02, D-78)"
      pattern: "BookingReference"
    - from: "src/components/group/attendee-roster.tsx"
      to: "src/components/patterns/row-card.tsx"
      via: "roster rows adopt the declared pattern instead of re-deciding padding and radius (D-79, DS-11)"
      pattern: "RowCard"
---

<objective>
Give `/invite/[token]` and `/bookings/[id]/group` the design-system pass — and nothing else.

Purpose: 13-CONTEXT D-79 is precise about the boundary. These two surfaces carry **zero dedicated
REQ-IDs** (the roadmap's own scope note), so they inherit this phase's tokens, patterns, state families
and trust decisions, and they gain no capability. Anything beyond that is D-136 territory and needs its
own phase. The reason this is written as a plan rather than left to judgement is that a design pass on
a surface with no requirement of its own is exactly where scope creep is invisible.

Output: two surfaces composed from the declared pattern layer, the reference rendered on the group
page, and a proof that the invite route's security properties survived the restyle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The group page — patterns, targets, and the reference</name>
  <read_first>
    - src/app/(app)/bookings/[id]/group/page.tsx (the whole file, post-13-01). Read the owner-scoped
      group reads carefully: they are organizer-scoped INSIDE their own SQL, and
      `bookings/[id]/page.tsx:565-577` states the rule — *"a foreign roster must be UNREADABLE, not
      merely unrendered."* Never filter a roster in JS after reading it.
    - src/components/patterns/panel-card.tsx, row-card.tsx, empty-state.tsx, page-header.tsx,
      responsive-dialog.tsx — the five declared patterns this surface adopts. Read each one's props and
      its padding rule.
    - src/components/group/headcount-meter.tsx, attendee-roster.tsx, share-link-box.tsx,
      invite-card.tsx, top-up-nudge.tsx, remove-attendee-button.tsx, regenerate-link-button.tsx.
    - 13-UI-SPEC.md § The Group Surfaces — the change table, including the ⚠ Never row.
    - 13-UI-SPEC.md § Visual Hierarchy — the group page's focal point is the HEADCOUNT; the STATE-08
      alerts (already built in 13-05) are `tone="muted"` panels above it, not accent surfaces.
    - tests/design/card-pattern-coverage.test.ts:205/:329/:343 and
      src/lib/design/measurements.ts — `ROW_CARD_HEIGHT`, `ROW_CARD_THUMB`.
  </read_first>
  <files>src/app/(app)/bookings/[id]/group/page.tsx, src/components/group/headcount-meter.tsx, src/components/group/attendee-roster.tsx, src/components/group/share-link-box.tsx, src/components/group/top-up-nudge.tsx, tests/design/card-pattern-coverage.test.ts</files>
  <action>
    Adopt the declared patterns: `PageHeader` for the title block, `PanelCard` for the invite card, the
    share box and the headcount meter, `RowCard` for roster rows, and the Phase-11 `EmptyState` at zero
    attendees. Any confirm overlay (remove attendee, regenerate link) is `ResponsiveDialog` — the app's
    ONE overlay primitive. No second overlay mechanism, no grab handle, no fetched `sheet` block.

    Type comes from the four named roles only; zero arbitrary sizes. Every control is at least 44px
    (`size="touch"`). Colour is neutral throughout: `--success` is reserved to the confirmed BOOKING
    badge and there is no alarm colour anywhere in the group flows — the existing grep tripwire in
    `rsvp-confirmation.tsx` stays untripped.

    Render `<BookingReference/>` on the group page (TRUST-02): an organiser contacting support quotes the
    same string as the booker on the detail page. The value is server-computed with
    `bookingReference(bookingId)` in the RSC and passed as a prop.

    Re-measure `CARD_SURFACES` / `EXPECTED_SURFACES` and move them in the SAME commit as the adoption.
    Add **zero** `ALLOWED_RAW_CARD` rows. Note the trap the gate's own header records: a listed file that
    is deleted or moved must lose its row in the same commit, and `attendee-roster.tsx` is one of the
    already-listed files.

    **Add no capability.** No new filter, no new roster action, no new invite mechanism, no attendee name
    on anything printable. Do not change the organizer-scoped SQL, the headcount arithmetic, the RSVP
    state machine, or `getHeadcount`. If a change appears to require touching one of those, it has left
    D-79's boundary and is a scope alarm to raise.

    Write `tests/group/group-surface-shell.test.tsx`: every card container on the group page renders
    `data-testid` of `panel-card` or `row-card`; the booking reference renders; the STATE-08 alert slot
    from 13-05 is present.
  </action>
  <verify>
    <automated>npx vitest run tests/group/group-surface-shell.test.tsx && npx vitest run --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/components/ui/card"' 'src/app/(app)/bookings/[id]/group/page.tsx'` returns 0.
    - `npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts` passes;
      any `CARD_SURFACES` row added is paired with an `EXPECTED_SURFACES` bump in the same commit; zero
      `ALLOWED_RAW_CARD` rows added.
    - `npx vitest run --config vitest.design.config.ts tests/design/sheet-absent.test.ts` passes — no
      second overlay primitive was fetched.
    - `git diff 'src/app/(app)/bookings/[id]/group/page.tsx'` contains no changed line matching
      `getHeadcount`, `getOwnedGroupByBooking`, or any SQL string — the organizer-scoped reads are
      untouched.
    - `npx vitest run tests/group/` passes in full — the RSVP state machine is unaffected.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The group page composes the declared patterns, renders the reference, adds no capability, and its owner-scoped reads are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The invite route — restyle without touching its security properties</name>
  <read_first>
    - src/app/(public)/invite/[token]/page.tsx and src/app/(public)/invite/[token]/not-found.tsx — read
      both in full, including the `metadata` export. `robots: { index: false, follow: false }` and
      `referrer: "no-referrer"` are load-bearing.
    - tests/design/invite-notfound-parity.test.ts — the 08-06 oracle: the invite 404 must stay
      **textually identical** to the inactive state, because distinguishable copy is an ownership
      oracle. Read it; do not edit it.
    - tests/design/og-routes.test.ts and the invite OG image route — it reads no `params` and performs
      zero DB reads. That property must survive.
    - src/components/group/invite-card.tsx, rsvp-form.tsx, rsvp-confirmation.tsx.
    - 13-UI-SPEC.md § The Group Surfaces → the "Security, preserved" row.
  </read_first>
  <files>src/app/(public)/invite/[token]/page.tsx, src/components/group/invite-card.tsx, src/components/group/rsvp-form.tsx, src/components/group/rsvp-confirmation.tsx</files>
  <action>
    Adopt `PanelCard` for the invite card, the four named type roles, 44px targets and the neutral
    palette on `/invite/[token]` and its three components. `EmptyState` and `ErrorState` where the
    surface has an empty or failed shape.

    Preserve, verbatim and provably:
    - the route's `metadata` export including `robots: { index: false, follow: false }` and
      `referrer: "no-referrer"`;
    - the 404's text, character for character, so it stays identical to the inactive state;
    - the OG image route's zero-`params`, zero-DB-read property;
    - the RSVP submission path, its server action, and its refusal handling.

    The RSVP result and the refusal sentence each stay ONE named `role="status"` region (the inventory
    bookkeeping is plan 13-14's; here, make sure each has a non-empty accessible name and that the
    card's STATIC content is not wrapped in a region — a freshly navigated page is not a change).

    Add no capability: no new invite mechanism, no new RSVP option, no attendee name anywhere new.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/invite-notfound-parity.test.ts tests/design/og-routes.test.ts && npx vitest run tests/group/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/invite-notfound-parity.test.ts`
      passes and `git diff --stat tests/design/invite-notfound-parity.test.ts` shows zero lines changed.
    - `grep -c 'index: false' 'src/app/(public)/invite/[token]/page.tsx'` and
      `grep -c 'no-referrer'` both return at least 1.
    - `npx vitest run --config vitest.design.config.ts tests/design/og-routes.test.ts` passes.
    - `grep -c 'aria-live="assertive"' src/components/group/*.tsx` returns 0.
    - `npx vitest run tests/group/` passes in full.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The invite surface carries the design system while its no-index metadata, its 404 parity and its DB-free OG route are proved intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public invite token → group data | `/invite/[token]` is a PUBLIC route reached by anyone holding the token |
| organiser → attendee roster | A roster belongs to one group; a foreign roster must be unreadable, not merely unrendered |
| 404 text → ownership oracle | Distinguishable copy for "missing" vs "not yours" leaks existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-INVITEORACLE | Information Disclosure | `/invite/[token]` 404 | mitigate | `invite-notfound-parity.test.ts` stays green UNMODIFIED, and its zero-line diff is an acceptance criterion. The 08-06 oracle is not reopened by a restyle |
| T-13-08-INVITEINDEX | Information Disclosure | invite route metadata | mitigate | `robots: { index: false, follow: false }` and `referrer: "no-referrer"` are asserted present by grep after the restyle; the OG route's zero-DB-read property is asserted by `og-routes.test.ts` |
| T-13-08-ROSTERSCOPE | Elevation of Privilege | group roster reads | mitigate | The organizer-scoped SQL is untouched — asserted by a diff containing no changed line matching `getHeadcount` / `getOwnedGroupByBooking` / any SQL string. Filtering in JS after reading is never introduced |
| T-13-08-SCOPECREEP | Tampering | D-79's boundary | mitigate | The plan enumerates what may not be added and asserts the RSVP state machine and headcount arithmetic are untouched by running `tests/group/` in full. A change that requires touching them is a scope alarm to raise, not to absorb |
| T-13-08-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed** and no shadcn block is fetched; `sheet-absent.test.ts` stays green |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `git diff --stat tests/design/invite-notfound-parity.test.ts src/lib/group/` shows zero changes to both.
- `git diff --stat drizzle/ package.json components.json` shows zero changes to all three.
</verification>

<success_criteria>
- Both group surfaces render only declared patterns; zero new `ALLOWED_RAW_CARD` rows.
- The booking reference renders on `/bookings/[id]/group`.
- The invite route's metadata, 404 parity and DB-free OG route are proved intact.
- `tests/group/` passes in full — no capability changed.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-08-SUMMARY.md` when done.
</output>
