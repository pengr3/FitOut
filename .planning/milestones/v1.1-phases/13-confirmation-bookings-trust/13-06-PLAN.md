---
phase: 13-confirmation-bookings-trust
plan: 06
type: execute
wave: 2
depends_on: ["13-01", "13-03"]
files_modified:
  - src/app/(app)/bookings/[id]/cancel/page.tsx
  - src/lib/email.ts
  - tests/booking/cancellation-copy.test.tsx
  - tests/booking/cancellation-policy.test.ts
autonomous: true
requirements: [TRUST-01, TRUST-03]
must_haves:
  truths:
    - "A booker's screen and their email state the SAME refund window, because both read the same verified owner (D-92)"
    - "The cancel review page carries the same shell and the same panels as the pages either side of it"
    - "No email send trigger is added, moved or removed — only a copy constant changes (D-78, D-92)"
    - "Every percentage, hour figure and rung on the cancel page still derives from LADDER, never hand-typed (TRUST-03)"
  artifacts:
    - path: "src/app/(app)/bookings/[id]/cancel/page.tsx"
      provides: "BOOKING_SHELL + PanelCard adoption, the verified refund window, and the live region removed from static content (D-93)"
    - path: "src/lib/email.ts"
      provides: "the corrected refund-window copy constant at the single call site D-92 names"
  key_links:
    - from: "src/lib/email.ts"
      to: "src/lib/booking/refund-window.ts"
      via: "the refund sentence read from the verified owner instead of the unsourced literal (D-83, D-92)"
      pattern: "refund-window"
    - from: "src/app/(app)/bookings/[id]/cancel/page.tsx"
      to: "src/components/booking/cancellation-policy-disclosure.tsx"
      via: "LADDER-derived rungs and server-computed rungBoundaries, unchanged (TRUST-03)"
      pattern: "CancellationPolicyDisclosure"
---

<objective>
Close the cancel review page's three named items, and stop the screen and the email disagreeing about
when a booker's money comes back.

Purpose: 13-UI-SPEC § The Cancel Review Page scopes this surface to **exactly three items and nothing
else**, because `/bookings/[id]/cancel` sits inside this phase's boundary but carries no requirement of
its own. The third item is the one that matters: the unsourced *"Refunds usually land back on your
original payment method within a few days"* appears at three call sites, and 13-CONTEXT D-92 rules that
the `src/lib/email.ts` occurrence is corrected **here**, because it is a copy constant and not a send
trigger — D-78's hands-off rule for Phase 15 protects triggers, not strings. A booker's screen and
their inbox disagreeing about their own money is the exact failure this phase exists to remove.

Output: one surface matching its neighbours, one live region removed from static content, and one
copy constant reading from the verified owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The verified refund window replaces the unsourced string on the cancel page and in the email</name>
  <read_first>
    - src/app/(app)/bookings/[id]/cancel/page.tsx — the whole file. In particular :206-260 (the
      server-side composition shape: `tierOrDefault` → `quoteRefund` → `rungBoundaries(tier, startsAt)`
      → index math, all in the RSC), :262-273 (the third-party fallback idiom), and :356 (the unsourced
      sentence).
    - src/lib/email.ts:330-345 — the refund email's body and the same unsourced sentence at :341. Read
      enough of the surrounding module to be certain which function is the SEND and which is the COPY.
    - src/lib/booking/refund-window.ts (13-03) — the three verified sentences and the rail-free fallback.
    - 13-CONTEXT.md D-78, D-83 and D-92 — the trigger/constant distinction, stated three times.
    - 13-UI-SPEC.md § The Cancel Review Page — the three-item scope and the "change it here" decision,
      with the note that the PM may move it to Phase 15.
  </read_first>
  <files>src/app/(app)/bookings/[id]/cancel/page.tsx, src/lib/email.ts</files>
  <action>
    On the cancel page, replace the sentence at :356 with the verified window sentence selected from
    `refund-window.ts`. The cancel page knows the booking's rail only when the payment succeeded
    (`booking.payment_method` is populated on a confirmed booking, unlike a reversed one), so pass that
    rail when present and fall back to the all-rails sentence when it is null. Do not hand-type a number
    and do not call the probe from this page — a booker-initiated cancel has a real
    `payment_method` column and does not need one.

    In `src/lib/email.ts`, replace the literal at :341 with the same sentence, derived from the same
    module. This is a **copy constant only**: do not add, move, remove, rename, reorder or re-trigger a
    single `send(...)` call, and do not change the subject line, the recipient, the call site, or the
    conditions under which the function is invoked. `git diff` must show the change confined to the body
    string and its import.

    Record in a comment beside the change that D-92 placed this here deliberately, that D-78 bans moving
    or adding a send TRIGGER (which this is not), and that Phase 15's SC#3 — *"not a single send trigger
    has moved"* — therefore survives.

    If `tests/booking/cancellation-copy.test.ts(x)` or `tests/booking/cancellation-policy.test.ts` pins
    either old string, move the pin in the SAME commit.
  </action>
  <verify>
    <automated>npx vitest run tests/booking/cancellation-copy.test.tsx tests/booking/cancellation-policy.test.ts tests/booking/notify-emission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'within a few days' src/` returns ZERO hits across the whole tree (`page.tsx:522` was
      closed by 13-04; these were the other two).
    - `npx vitest run tests/booking/notify-emission.test.ts` passes unchanged — the send-trigger
      inventory is provably untouched.
    - `git diff src/lib/email.ts` touches only the body string, one import, and the comment; it contains
      zero added or removed lines matching `send(` or `export const`.
    - `grep -oiE '[0-9]+ *(day|hour)' src/lib/email.ts 'src/app/(app)/bookings/[id]/cancel/page.tsx'`
      returns nothing — every number comes from `refund-window.ts`.
    - `npx vitest run tests/booking/cancellation-policy.test.ts` passes: the disclosure and `quoteRefund`
      still agree at every rung boundary.
  </acceptance_criteria>
  <done>Screen and email read the same verified window from one owner; the send-trigger inventory is unchanged and proved so.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt the shell and the panel pattern, and remove the live region from static content</name>
  <read_first>
    - src/app/(app)/bookings/[id]/cancel/page.tsx:171 and :277 — the two container returns 13-01 already
      switched to `BOOKING_SHELL`; the remaining work is the card layer inside them.
    - src/components/patterns/panel-card.tsx:84-148 — props and the padding rule; and the trap recorded
      in `card-pattern-coverage.test.ts`'s header: nesting `PanelCard` inside a container that already
      supplies `bg-card ring-1 rounded-xl` pays the block padding twice (112px vs 80px).
    - src/components/booking/price-breakdown.tsx:34-45 — `PriceBreakdown` is deliberately a bare `<div>`
      for that reason. **Do not "finish the adoption" by wrapping it.**
    - tests/design/card-pattern-coverage.test.ts:205 (`CARD_SURFACES`), :329 (`EXPECTED_SURFACES = 12`),
      :343 (`ALLOWED_RAW_CARD` — this file already HAS a row; if the raw `<Card>` goes away the stale row
      stays green, but if the file is ever deleted or moved the row must go in the same commit).
    - 13-UI-SPEC.md § Live Regions — `bookings/[id]/cancel/page.tsx`'s `role="status" aria-live="polite"`
      on static content is REMOVED: *"A live region announces a CHANGE. A freshly navigated page is not
      a change — it is a page."*
  </read_first>
  <files>src/app/(app)/bookings/[id]/cancel/page.tsx, tests/design/cancel-page-shell.test.tsx</files>
  <action>
    Replace the page's raw `<Card>` containers with `PanelCard`, so the surface matches the detail page
    and the receipt either side of it. Add no padding of your own. Do not wrap `PriceBreakdown` or
    `RefundBreakdown` — both are bare `div`s deliberately.

    Remove the `role="status" aria-live="polite"` from the page's static content. Add no focus move and
    no replacement region: a fresh navigation already moves the reading position, and this page's
    content does not change while the booker watches it. (The `live-regions.ts` inventory bookkeeping is
    plan 13-14's; removing the attribute here is what makes that bookkeeping honest.)

    Change nothing else on this surface. Specifically: the cancellation ladder, `quoteRefund`, the
    `rungBoundaries` composition, `CancellationPolicyDisclosure`, the destination form, the confirm
    action and the `isApiRefundable` call at :262 are all untouched. The booking's tier remains the
    SNAPSHOT `booking.cancellation_policy` (PROJECT D-67), never the listing's current tier — a host
    re-tiering must not rewrite an existing booking's terms.

    Add a small design assertion (`tests/design/cancel-page-shell.test.tsx` or an added `it()` in an
    existing suite, your choice — state which and why) that every card container on
    `/bookings/[id]/cancel` renders `data-testid` of `panel-card` or `row-card`.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts && npx vitest run tests/booking/cancellation-copy.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-live' 'src/app/(app)/bookings/[id]/cancel/page.tsx'` returns 0.
    - `grep -c 'from "@/components/ui/card"' 'src/app/(app)/bookings/[id]/cancel/page.tsx'` returns 0.
    - `npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts` passes;
      any added `CARD_SURFACES` row is paired with an `EXPECTED_SURFACES` bump in the same commit, and
      zero `ALLOWED_RAW_CARD` rows are added.
    - `git diff 'src/app/(app)/bookings/[id]/cancel/page.tsx'` contains no changed line matching
      `quoteRefund`, `rungBoundaries`, `LADDER`, `isApiRefundable` or `cancellation_policy`.
    - `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The cancel page uses the shell and the panel pattern, carries no live region on static content, and its refund arithmetic is provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen copy ↔ email copy | Two channels stating the same money fact; divergence is a disclosure defect on a money path |
| copy constant → send trigger | `src/lib/email.ts` is Phase 15's shell; the boundary between changing a string and moving a trigger must stay legible |
| disclosure ↔ enforcement | A cancellation disclosure that disagrees with `quoteRefund` becomes a refund dispute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-COPYDIVERGE | Repudiation | refund-window copy in two channels | mitigate | Both read `refund-window.ts`; a repo-wide grep for the superseded string returns zero and every duration in either file is asserted to come from the module |
| T-13-06-TRIGGERMOVE | Tampering | `src/lib/email.ts` | mitigate | The diff is confined to a body string, one import and a comment; `notify-emission.test.ts` passing unchanged is the proof that the send inventory did not move (D-78, Phase 15 SC#3) |
| T-13-06-LADDERDRIFT | Repudiation | cancellation disclosure | mitigate | Zero changed lines matching `quoteRefund` / `rungBoundaries` / `LADDER`, and `cancellation-policy.test.ts` still asserts the disclosure and the quote agree at every rung boundary |
| T-13-06-STALEREGION | Denial of Service | assistive-technology surface | mitigate | The static-content region is removed rather than renamed: an empty announcement on every navigation is noise that trains a user to ignore the mechanism |
| T-13-06-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `grep -rn 'within a few days' src/` returns zero.
- `git diff --stat drizzle/` shows zero changes.
</verification>

<success_criteria>
- The superseded refund-window string is gone from the entire `src/` tree.
- `src/lib/email.ts`'s diff touches no send trigger and `notify-emission.test.ts` is green unchanged.
- The cancel page renders only `panel-card` / `row-card` containers and carries no live region.
- `cancellation-policy.test.ts` still proves disclosure and enforcement agree.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-06-SUMMARY.md` when done.
</output>
