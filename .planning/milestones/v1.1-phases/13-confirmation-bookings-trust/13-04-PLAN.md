---
phase: 13-confirmation-bookings-trust
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - src/components/booking/payment-reversed-state.tsx
  - src/app/(app)/bookings/[id]/page.tsx
  - src/lib/design/selector-contract.ts
  - tests/design/reversed-copy.test.ts
  - tests/booking/reversed-state.test.tsx
autonomous: true
requirements: [STATE-05, STATE-06, TRUST-02]
must_haves:
  truths:
    - "A booker whose payment was reversed reads the exact amount that left their account, and the sentence agrees with their bank app (D-69)"
    - "The automatic-refund branch and the manual-return branch say opposite things, because they ARE opposite facts (D-83)"
    - "The manual-return branch never uses the word 'refunded' and never says 'cannot be reversed' (D-82, D-83)"
    - "A reversed booking still renders its money statement with NO query string on the URL (D-87)"
    - "Back to availability remains the single coral primary; the support control is above it in reading order, not louder in colour (D-72, D-83)"
  artifacts:
    - path: "src/components/booking/payment-reversed-state.tsx"
      provides: "the corrected reversed landing — two money truths, one layout, data-testid=payment-state-reversed (D-69, D-83)"
      contains: "payment-state-reversed"
    - path: "tests/design/reversed-copy.test.ts"
      provides: "two grep tripwires in the two-piece idiom: the not-completed sentence and the manual branch's refund token"
    - path: "src/app/(app)/bookings/[id]/page.tsx"
      provides: "the D-87 discriminator — reversed is reached from DB state plus the D-84 probe, never from ?paid=1"
  key_links:
    - from: "src/app/(app)/bookings/[id]/page.tsx"
      to: "src/lib/payments/checkout-probe.ts"
      via: "probeCheckoutSession on the cancelled branch: session paid ⇒ reversed, session expired ⇒ swept unpaid hold (D-84, D-87)"
      pattern: "probeCheckoutSession"
    - from: "src/components/booking/payment-reversed-state.tsx"
      to: "src/lib/booking/refund-window.ts"
      via: "the only permitted window sentences, selected by the probed rail (D-83)"
      pattern: "refund-window"
    - from: "src/components/booking/payment-reversed-state.tsx"
      to: "src/components/booking/money-statement.tsx"
      via: "the single owner of STATE-06's above-the-fold sentence (D-73)"
      pattern: "MoneyStatement"
---

<objective>
Correct the phase's sharpest surface: the reversed-payment landing, whose shipped copy is wrong in two
directions at once and whose only route to the screen is a query parameter this phase is about to
consume.

Purpose: 13-RESEARCH's primary recommendation is to plan this first — *"it is the only one where a
locked decision collides with a data gap, and the only one where the shipped copy is actively
contradicted by what the code does."* The file carries three defects: a nested `<main>` (closed by
13-01), the sentence *"you haven't been charged"* which is STATE-05's language for a DIFFERENT state
and which contradicts the booker's own bank statement, and an upstream gate on `paid === '1'` that
makes the whole state vanish the moment D-60 strips the param. D-60's safety argument — *"everything
the moment states is repeated on the ordinary detail page"* — is currently FALSE for this one state,
and 13-UI-SPEC states plainly that consuming the param is only safe once it is true. **Order the work
accordingly: this plan runs before the confirmation moment.**

Output: a two-branch reversed state whose copy survives the bank-app test, a probe-driven
discriminator that needs no query string, and two grep tripwires that cannot disarm themselves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-RESEARCH.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two grep tripwires FIRST, and watch them fail (D-83)</name>
  <read_first>
    - tests/design/price-surface.test.ts:256-261 (the `Forbidden` type — `pieces` split mid-word,
      `why` mandatory), :273-283 (a model row), :317-345 (the two-pass scanner: raw lines PLUS a
      whitespace-collapsed copy, because a formatter wraps JSX prose mid-sentence), :660-666 (the
      guard-the-guard fixture built FROM the two-piece encoding), :741-746 (the failure message that
      states the rule), :749-760 (the both-sides shape — assert the ban AND assert the required string
      still renders, because *"a file with no fee line at all satisfies the ban perfectly"*).
    - src/components/booking/payment-reversed-state.tsx — the shipped file, so the watched red is real.
    - 13-UI-SPEC.md § The Reversed State → "Two grep tripwires" — the exact two rows and their scope.
    - 13-CONTEXT.md D-82 — *"not reversed automatically"*, never *"cannot be reversed"*; the money is
      not stuck, it sits on the FitOut platform wallet and a human can move it.
  </read_first>
  <files>tests/design/reversed-copy.test.ts</files>
  <action>
    Create `tests/design/reversed-copy.test.ts` by extracting `price-surface.test.ts`'s two-piece idiom
    verbatim in shape — the `Forbidden` type with mandatory `why`, the two-pass raw+collapsed scanner,
    the guard-the-guard fixture built from the encoding, and the failure message that states the rule.

    Ban half, scoped to `src/components/booking/payment-reversed-state.tsx`:
    1. The not-completed sentence (*"you haven't been charged"*) must not appear AT ALL, comments
       included. `why`: it is STATE-05's language for the not-completed state and it contradicts the
       booker's own bank app — on the QRPh path it is the exact inverse of the truth in two directions.
    2. In the manual-return branch's strings: zero occurrences of the token `refund` in any case, and
       zero occurrences of `cannot be reversed` / `can't be reversed`. `why`: the money has NOT been sent
       back — a human has to send it — and D-82's accurate phrasing is *"not reversed automatically"*.

    Positive half, without which the ban is satisfiable by deleting the copy: the file must still render
    a money statement, must still carry the booking reference, and must still contain the exact amount
    prop. Assert each.

    Encode every banned phrase in two pieces joined at runtime, in the test fixture as well as the rows,
    so this file never spells a prohibited phrase contiguously either.

    Run it now. It MUST fail on row 1 against the shipped file. Record the failure output verbatim in
    the SUMMARY before writing a line of Task 2.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/reversed-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Before Task 2: `npx vitest run --config vitest.design.config.ts tests/design/reversed-copy.test.ts`
      FAILS, and the failure names `src/components/booking/payment-reversed-state.tsx` with a line number
      and the `why` string. The verbatim output is pasted into the SUMMARY.
    - `grep -ci "haven't been charged" tests/design/reversed-copy.test.ts` returns 0 — the test does not
      spell its own prohibition.
    - The scanner runs two passes: `grep -c 'replace(/\\s+/g' tests/design/reversed-copy.test.ts`
      returns at least 1.
    - After Task 2 the same command passes.
  </acceptance_criteria>
  <done>Two tripwires exist in the two-piece idiom, with a positive half, and have been watched failing against the shipped defect.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the reversed state's copy across two money truths (D-69, D-72, D-82, D-83)</name>
  <read_first>
    - src/components/booking/payment-reversed-state.tsx — the whole file after 13-01's container fix.
      Its structure is right (calm, never `--destructive`, single coral recovery CTA); its copy is not.
    - src/components/booking/hold-expired-state.tsx:50-92 — the calm-recovery idiom: muted icon at
      `size-8 text-muted-foreground`, `max-w-prose`, one coral primary, `role="status"`. Copy the shape.
    - 13-UI-SPEC.md § The Reversed State — the three-branch copy table (automatic/card, automatic/rail
      unknown, manual), the layout sketch under "How the coral primary and the unmissable support path
      coexist", and the falsifiable claim that in the manual branch the support control's
      `boundingBox().y` is LESS than the coral primary's at 320×568.
    - 13-UI-SPEC.md § Copywriting Contract → "The money statement" — the four reversed rows, verbatim.
    - 13-UI-SPEC.md § The Support Path → the two-state table, and the warning that the closed-guard copy
      must NOT promise an email on the reversal path: `handleGoneSlot` sends NO notification, only the
      confirm path does.
    - 13-UI-SPEC.md § Live Regions — `payment-reversed-state.tsx`'s `role="status"` is REMOVED here: a
      fresh render is not a change, and the `<h1>` is already the first thing. No focus move either.
    - src/components/booking/money-statement.tsx, support-path.tsx, booking-reference.tsx (13-02) and
      src/lib/booking/refund-window.ts (13-03) — the pieces this file composes.
  </read_first>
  <files>src/components/booking/payment-reversed-state.tsx, tests/booking/reversed-state.test.tsx</files>
  <action>
    Rewrite the component's props and copy. It becomes a Server Component taking finished, server-composed
    values: `listingId`, `reference`, `amountLabel` (the formatted `booking.quotedTotalCents` — the
    charge and the refund are the same figure by design, and `booking.refundCents` is NULL on this path),
    a `branch` discriminator of `"auto"` or `"manual"`, and an optional `railLabel`. It performs no
    money arithmetic and receives no `number` money prop.

    Render, in this order: the muted `Undo2Icon`, the shipped `<h1>` (*"We couldn't complete this
    booking"*, preserved byte-for-byte), the one-line status meaning, then `<MoneyStatement/>` as the
    FIRST element after the heading block — nothing may render between the `<h1>` and the panel.

    The money sentences come from 13-UI-SPEC § Copywriting Contract verbatim, selected by branch:
    - `auto` with a known rail: *"We've refunded {amount} in full."* plus the window sentence from
      `refund-window.ts` for that rail.
    - `auto` with the rail unknown (probe fell back): the same L1 plus the all-rails sentence.
    - `manual`: *"You were charged {amount}, and it's coming back to you."* plus the by-hand sentence
      carrying the reference. **The word "refunded" does not appear anywhere in this branch.**

    Place `<SupportPath/>` INSIDE the money panel, as a full-width `variant="outline" size="touch"`
    control — the first control on the page, adjacent to the sentence that explains why it exists. Guard
    closed (today) it renders nothing and the sentence stands alone; the closed-guard copy must not
    promise an email, because the reversal path sends none. The coral `Back to availability` and the
    secondary `Search other spaces` stay exactly as shipped, BELOW a `Separator`, in the recovery block
    (D-72). One accent fill in the viewport.

    Render `<BookingReference/>` on this state (TRUST-02: every status). Carry
    `data-testid="payment-state-reversed"`.

    Remove the `role="status" aria-live="polite"` from the container (13-UI-SPEC § Live Regions). Add no
    focus move. Use zero `--destructive` tokens — a reversed payment is not an error the booker caused,
    and DS-10 reserves `attention` for genuine failure needing a human, which is the operator, not them.

    Carry the grep-tripwire header in the shape `price-breakdown.tsx:28-33` uses, naming both banned
    phrases WITHOUT spelling either contiguously.

    Write `tests/booking/reversed-state.test.tsx` (RTL, modelled on
    `tests/booking/hold-expired-state.test.tsx`): render both branches; assert the amount renders in
    both; assert the automatic branch's window sentence matches the rail; assert the manual branch
    carries the reference and the by-hand sentence; assert both branches render exactly one
    `payment-state-reversed` container and one `money-statement`; assert `Back to availability` is
    present in both.
  </action>
  <verify>
    <automated>npx vitest run tests/booking/reversed-state.test.tsx && npx vitest run --config vitest.design.config.ts tests/design/reversed-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/reversed-copy.test.ts` now PASSES
      (both halves) — the Task 1 red is closed by this task, and both outputs are in the SUMMARY.
    - `npx vitest run tests/booking/reversed-state.test.tsx` passes for both branches.
    - `grep -ciE 'destructive' src/components/booking/payment-reversed-state.tsx` returns 0.
    - `grep -c 'aria-live' src/components/booking/payment-reversed-state.tsx` returns 0.
    - `grep -oiE '[0-9]+ *(day|hour)' src/components/booking/payment-reversed-state.tsx` returns nothing
      — every number comes from `refund-window.ts`, never hand-typed here.
    - `grep -c 'variant="brand"' src/components/booking/payment-reversed-state.tsx` returns exactly 1.
    - `npx vitest run --config vitest.design.config.ts` is green overall, including
      `card-pattern-coverage.test.ts` with zero new `ALLOWED_RAW_CARD` rows.
  </acceptance_criteria>
  <done>Two money truths, one layout, one coral; the reference and the support control are present; the tripwires are green from the demanding side.</done>
</task>

<task type="auto">
  <name>Task 3: Reach the reversed state from DB state plus the probe, with no query string (D-87)</name>
  <read_first>
    - src/app/(app)/bookings/[id]/page.tsx:195-197 — the shipped gate
      `if (bk.status === "cancelled" && paid === "1")`. This is defect 3.
    - src/app/(app)/bookings/[id]/page.tsx:441-460 — the `cancelled` branch, including the header's own
      "KNOWN EDGE" note at :450-454 admitting that a D-58 gone-slot reversal read without `?paid=1` on a
      request-mode booking reads as a lapse. That edge closes here.
    - src/app/(app)/bookings/[id]/page.tsx:500-534 — the generic cancelled render, including the
      unsourced *"within a few days"* string at :522 which D-83 supersedes.
    - src/lib/payments/checkout-probe.ts (13-03) — `probeCheckoutSession` and the four-way discriminator.
    - src/lib/db/schema.ts — `booking.checkoutSessionId`; confirm the column name before selecting it.
    - 13-RESEARCH.md § Code Examples → Example 3 — the verified statement that there is NO row-level
      signal separating a reversed booking from a swept unpaid hold.
    - 13-CONTEXT.md D-87 and PROJECT D-57 — the param is a UX signal only and never proof of payment;
      consuming it must not move any branch off DB status.
  </read_first>
  <files>src/app/(app)/bookings/[id]/page.tsx, src/lib/design/selector-contract.ts</files>
  <action>
    Add `booking.checkoutSessionId` to the page's select if it is not already there.

    Replace the `status === "cancelled" && paid === "1"` gate. The new discriminator, inside the
    `cancelled` branch and BEFORE the lapsed-approval check, is: call `probeCheckoutSession(
    bk.checkoutSessionId)`; when it reports the session `paid`, this is a reversal — render
    `<PaymentReversedState/>` with the branch chosen by `isApiRefundable(probe.sourceType)` (rail in
    `REFUNDABLE_RAILS` ⇒ `auto`; `qrph`, `dob_ubp`, unrecognised or a null probe result ⇒ `manual`) and
    the rail label passed through when known. When the probe reports `expired`, this is a swept unpaid
    hold and NOT a reversal — fall through to the existing branches unchanged.

    ⚠ The `manual` branch is also where a NULL probe lands, and that is deliberate and must be recorded:
    a fallback that guesses `auto` would tell a booker their money was refunded when nobody refunded it.
    Failing to `manual` is the honest direction, and it matches `isApiRefundable`'s own fail-closed rule.

    Compose the props server-side: `formatMoney(bk.quotedTotalCents, currency)` for the amount (the
    charge and the full refund are the same figure), `bookingReference(bk.id)` for the reference. The
    component receives finished strings; the RSC does the formatting (GATE-05 / D-130).

    Then supersede the unsourced string at `page.tsx:522`: the generic cancelled branch's *"within a few
    days"* sentence is replaced by the verified window from `refund-window.ts`. Do not hand-type a
    number. (The other two call sites — `cancel/page.tsx:356` and `src/lib/email.ts:341` — belong to
    plan 13-12.)

    Add the `payment-state-reversed` row to `SELECTOR_IDS` and `SELECTOR_CONTRACT`, `owner: "13-04"`,
    with the `why` 13-UI-SPEC gives: STATE-05's distinctness is asserted between three concrete
    containers and D-87's no-query-string assertion needs this one by name.

    Do NOT touch the `pending` branch in this plan — plan 13-05 owns it, and touching it here would put
    D-70 and D-89's interaction in two places.
  </action>
  <verify>
    <automated>npx playwright test e2e/shell.spec.ts -g "one \`main\` landmark" && npx vitest run --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'paid === "1"' 'src/app/(app)/bookings/[id]/page.tsx'` returns exactly 1 — only the
      `pending` branch's remaining use, which plan 13-05 owns. The cancelled branch no longer reads the
      param.
    - Behavioural proof, using 13-01's seed helper: load `/bookings/{reversedId}` with **no query
      string** against a seeded reversed row and assert `payment-state-reversed` renders with its money
      statement. Add this as a case in `e2e/shell.spec.ts`'s seeded describe or a scratch spec, and
      record the observed output in the SUMMARY.
    - `grep -c 'within a few days' 'src/app/(app)/bookings/[id]/page.tsx'` returns 0.
    - `npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts` passes with
      `Declared-but-absent: [none]`.
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0.
    - The page still renders when the probe is unavailable: run the check with no PayMongo key in the
      environment and confirm the reversed row renders the `manual` branch rather than throwing.
  </acceptance_criteria>
  <done>The reversed state is reached from DB status plus the probe, renders with no query string, fails to the honest branch when the probe is unavailable, and the page's unsourced refund window is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → render branch | `?paid=1` is forgeable; the DB status is not (PROJECT D-57) |
| PayMongo probe → booker-facing money copy | A third-party read decides which of two opposite sentences a booker sees about their own money |
| screen copy → the booker's bank app | If our screen and their statement disagree, our screen is wrong (13-CONTEXT § Specific Ideas) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-PARAMTRUST | Spoofing | `page.tsx` cancelled branch | mitigate | The branch reads DB status plus the probe; `?paid=1` no longer gates it. PROJECT D-57 stands: the webhook remains the sole confirm authority and no branch moves off DB status |
| T-13-04-FALSEREFUND | Repudiation | reversed copy branch selection | mitigate | A null or failed probe falls to the `manual` branch, never `auto`. Guessing `auto` would tell a booker money was refunded when nobody refunded it — the direction of the failure is chosen deliberately and recorded in the code |
| T-13-04-MONEYCROSS | Tampering | `payment-reversed-state.tsx` | mitigate | The component takes formatted strings; `formatMoney` runs in the RSC. GATE-05's AST boundary test and `price-surface.test.ts` are the gates |
| T-13-04-PROBEFAIL | Denial of Service | probe on the render path | mitigate | `probeCheckoutSession` is timeout-bounded and never throws (13-03). A degraded provider cannot fail the one page that exists to explain a failure |
| T-13-04-COPYDRIFT | Repudiation | refund-window copy | mitigate | Every number comes from `refund-window.ts`; `reversed-copy.test.ts`'s two tripwires plus a grep for hand-typed durations enforce it, and both have been watched failing |
| T-13-04-IDOR | Elevation of Privilege | `/bookings/[id]` | accept | Unchanged in this plan: the shipped owner gate at `page.tsx:148-182` (`bookerId === session.userId`, else the same bare 404 for missing and foreign rows) already covers this route and is not modified here (T-04-CONFIRMIDOR) |
| T-13-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `npx vitest run --config vitest.design.config.ts tests/design/reversed-copy.test.ts` green from the demanding side, with the Task-1 red recorded verbatim.
- `git diff --stat drizzle/ src/lib/payments/refund-rail.ts tests/design/site-contacts.test.ts` shows zero changes to all three.

<human-check>
On a seeded reversed booking in the `manual` branch at 320×568 in both themes: the money statement is fully
inside the initial viewport with no scrolling, and the support control (guard-open build) sits ABOVE the
coral `Back to availability`. Deferred to the phase-close verification plan (13-15) if the guard is still
closed, since the control does not render while `SUPPORT_EMAIL` is null.
</human-check>
</verification>

<success_criteria>
- The reversed state renders two opposite money truths from one layout, with the amount, the window (auto only), the reference and the support slot.
- The word "refunded" appears nowhere in the manual branch; "cannot be reversed" appears nowhere at all.
- The state renders from `/bookings/{id}` with no query string.
- A null probe lands on `manual`, never `auto`.
- `payment-state-reversed` is declared in the selector contract and rendered in `src/`.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-04-SUMMARY.md` when done.
</output>
