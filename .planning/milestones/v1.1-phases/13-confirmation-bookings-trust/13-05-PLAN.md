---
phase: 13-confirmation-bookings-trust
plan: 05
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - src/components/booking/cancel-confirm.tsx
  - src/components/group/remove-attendee-button.tsx
  - src/components/group/regenerate-link-button.tsx
  - src/components/group/attendee-roster.tsx
  - src/components/group/share-link-box.tsx
  - tests/design/status-vocab.test.ts
  - tests/group/state08-alerts.test.tsx
autonomous: true
requirements: [STATE-08]
must_haves:
  truths:
    - "A refund amount, a reduced headcount and a voided invite each land as an in-page alert the booker can re-read after a refresh, never as a timed toast (STATE-08, D-93)"
    - "Exactly one region announces each outcome — there is no toast-plus-alert pair (GATE-03 rule 6)"
    - "The toasts that legitimately remain carry no fact the booker must retain"
  artifacts:
    - path: "tests/design/status-vocab.test.ts"
      provides: "an AST scan over the Phase-13 file set failing any toast literal carrying a must-read fact, with share-link-box.tsx allow-listed and its reason in the row"
    - path: "src/components/group/attendee-roster.tsx"
      provides: "the in-page alert slot above the roster (PanelCard tone=muted + role=status + a non-empty accessible name)"
  key_links:
    - from: "src/components/group/remove-attendee-button.tsx"
      to: "src/components/group/attendee-roster.tsx"
      via: "the removal outcome rendered as one named in-page alert instead of toast.success (STATE-08, D-93)"
      pattern: "role=\"status\""
    - from: "src/components/booking/cancel-confirm.tsx"
      to: "src/components/booking/money-statement.tsx"
      via: "the refund fact leaves the toast and lands on the destination's cancelled branch as a money statement (D-73, D-93, PROJECT D-79 wording preserved)"
      pattern: "Booking cancelled."
---

<objective>
Close STATE-08's three named examples: a refund amount, a reduced headcount and a voided invite must
each be something the booker can still read after a refresh.

Purpose: STATE-08 names exactly these three cases, and all three ship today as `toast.success` calls.
A toast is dismissible, timed, unaddressable and gone on refresh; those three facts are none of those
things. 13-CONTEXT D-93 records these as corrections inside this phase's domain rather than scope
creep — but records them so the phase's size is honest.

Output: three corrected outcomes, one in-page alert pattern reused (not invented), and an AST scan
that makes the rule mechanical instead of a review comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the toast AST scan, and watch it fail on the three shipped calls</name>
  <read_first>
    - tests/design/status-vocab.test.ts — read the whole file: the module-level single scan, the
      `it()`-blocks-only-assert discipline, and the scan-reaches-what-it-claims positive control at
      :455-471. Extend this file; do not create a fourth status test.
    - tests/design/price-surface.test.ts:256-283 and :317-345 — the two-piece `Forbidden` idiom and the
      raw+collapsed two-pass scanner. The banned tokens here are short (`refund`, `spot`, `invite`), so
      the two-piece encoding matters more, not less.
    - 13-UI-SPEC.md § STATE-08 — the correction table, the "no toast + alert pair" rule, the list of
      toasts that legitimately stay, and the falsifiable statement naming the six banned tokens and the
      one allow-listed file.
    - src/components/group/share-link-box.tsx:61-72 — the copy toasts that stay, and the reason.
  </read_first>
  <files>tests/design/status-vocab.test.ts</files>
  <action>
    Extend `tests/design/status-vocab.test.ts` with an AST scan over `src/app/(app)/bookings/**`,
    `src/components/booking/**` and `src/components/group/**`. Walk each file's TypeScript AST, collect
    every call expression whose callee is `toast` or a `toast.*` member, and read its string-literal and
    template-literal arguments. Fail when any of those literals contains: `refund`, `₱`, `spot`,
    `coming`, `no longer works`, or `invite`.

    Allow-list exactly one file — `src/components/group/share-link-box.tsx` — as a row carrying its own
    reason (its copy toasts announce plumbing, not a fact the booker must retain). A row without a
    reason is not a row.

    Encode every banned token with the two-piece idiom so this test cannot disarm its own scan, and
    include the guard-the-guard fixture built from the encoding.

    Add the positive control this file's existing describe already models: assert the scanner actually
    visited the three target directories and found a non-zero number of `toast` calls, so a green result
    can never mean "the scan reached nothing".

    Run it now. It MUST fail naming `cancel-confirm.tsx`, `remove-attendee-button.tsx` and
    `regenerate-link-button.tsx`. Record the verbatim failure in the SUMMARY before Task 2.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/status-vocab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Before Task 2 the command FAILS and the failure names all three files with line numbers. The
      verbatim output is in the SUMMARY.
    - The positive control asserts a non-zero `toast` call count across the three directories.
    - `grep -c 'no longer works' tests/design/status-vocab.test.ts` returns 0 — the test does not spell
      its own prohibition contiguously.
    - After Task 2, the same command passes.
  </acceptance_criteria>
  <done>An AST scan exists that would have caught all three shipped violations, has a positive control, and has been watched failing.</done>
</task>

<task type="auto">
  <name>Task 2: Move the three must-read outcomes out of toasts and into named in-page alerts</name>
  <read_first>
    - src/components/booking/cancel-confirm.tsx — the current `toast.success` carrying the refund
      sentence, and where it navigates to afterwards.
    - src/components/group/remove-attendee-button.tsx and src/components/group/regenerate-link-button.tsx
      — the two `toast.success` calls and how each component signals completion to its parent today.
    - src/components/group/attendee-roster.tsx and src/components/group/share-link-box.tsx — where the
      alert must render (above the roster, above the share box) and what state each parent already holds.
    - src/components/patterns/panel-card.tsx:84-148 — `tone="muted"` is Phase 11's declared in-page
      advisory surface. Not a new pattern, not `attention` tone (DS-10 reserves that for genuine failure
      needing a human), not `--destructive`.
    - 13-UI-SPEC.md § Copywriting Contract → "STATE-08 alerts" — the three exact strings.
    - src/lib/design/live-regions.ts rules 5 and 6 — a region needs a name and a reason, and exactly one
      region announces one outcome.
  </read_first>
  <files>src/components/booking/cancel-confirm.tsx, src/components/group/remove-attendee-button.tsx, src/components/group/regenerate-link-button.tsx, src/components/group/attendee-roster.tsx, src/components/group/share-link-box.tsx, tests/group/state08-alerts.test.tsx</files>
  <action>
    **Cancel confirmation.** Reduce the toast to exactly `Booking cancelled.` The refund fact leaves the
    toast entirely; it lands on the destination — the detail page's cancelled branch — as the
    `<MoneyStatement/>` plan 13-10 mounts there, with PROJECT D-79's wording preserved verbatim
    (*"{₱X} refund on its way"* / *"No refund — cancelled inside the no-refund window"*). Do not
    re-word D-79's sentences and do not add a second refund sentence here.

    **Attendee removed.** Delete the `toast.success` and render an in-page alert above the roster:
    *"{Name} removed — {N} coming, {M} spots free."* The counts arrive from the same server response the
    action already returns; do not compute them in the button.

    **Link regenerated.** Delete the `toast.success` and render an in-page alert above the share box:
    *"The old invite link no longer works. Copy the new one below and share it again."*

    Both alerts are `PanelCard tone="muted"` with `role="status"` and a NON-EMPTY accessible name. There
    is no toast-and-alert pair for either outcome — two regions announcing one outcome is GATE-03 rule
    6's defect, and the alert is the single announcement.

    Leave `share-link-box.tsx`'s own copy toasts (*"Link copied"* and the copy-failure sentence)
    untouched: they announce plumbing.

    Write `tests/group/state08-alerts.test.tsx` (RTL): after a simulated removal, exactly one
    `role="status"` element renders with the expected accessible name and text, and no toast is
    dispatched; the same for the regenerate outcome. Use the project's existing toast-spy idiom if one
    exists in `tests/`; otherwise stub the `sonner` module and assert zero calls.
  </action>
  <verify>
    <automated>npx vitest run tests/group/state08-alerts.test.tsx && npx vitest run --config vitest.design.config.ts tests/design/status-vocab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/status-vocab.test.ts` PASSES, closing
      the Task-1 red; both outputs are in the SUMMARY.
    - `npx vitest run tests/group/state08-alerts.test.tsx` passes, asserting exactly one `role="status"`
      per outcome and zero toast calls for both.
    - `grep -c 'toast' src/components/group/remove-attendee-button.tsx` and the same for
      `regenerate-link-button.tsx` both return 0 for `toast.success`; any remaining `toast.error` for a
      server refusal is allowed and named in the SUMMARY.
    - `grep -c 'Booking cancelled.' src/components/booking/cancel-confirm.tsx` returns 1 and no refund
      wording remains in that file.
    - `npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts` passes
      with zero new `ALLOWED_RAW_CARD` rows; if a `CARD_SURFACES` row is added, `EXPECTED_SURFACES` is
      bumped in the same commit.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Three must-read facts are in-page alerts with names; the AST scan is green; no outcome is announced twice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action result → announcement | The headcount and refund figures originate server-side and must not be recomputed in a client component |
| announcement → assistive technology | Two regions for one outcome is a correctness defect, not a style choice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-LOSTFACT | Repudiation | toast-carried refund amount | mitigate | The refund fact moves to a persistent in-page money statement on the destination; a booker who dismissed or missed the toast can still read it after a refresh, which is STATE-08's whole point |
| T-13-05-DOUBLEANNOUNCE | Denial of Service | assistive-technology surface | mitigate | Exactly one `role="status"` per outcome, with a non-empty accessible name (live-regions rules 5 and 6). `tests/group/state08-alerts.test.tsx` asserts the count, not just the presence |
| T-13-05-COUNTCROSS | Tampering | `remove-attendee-button.tsx` | mitigate | The headcount in the alert comes from the server response the action already returns; the client performs no arithmetic on it |
| T-13-05-SCANVACUITY | Repudiation | `status-vocab.test.ts` | mitigate | The two-piece encoding prevents the scan matching its own prohibition, the guard-the-guard fixture is built from the encoding, and a positive control asserts the scan visited a non-zero number of `toast` calls |
| T-13-05-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `git diff --stat src/components/group/share-link-box.tsx` shows only the alert slot, not its copy toasts.
</verification>

<success_criteria>
- Zero `toast.*` literals in the Phase-13 file set carry `refund`, `₱`, `spot`, `coming`, `no longer works` or `invite`, except the allow-listed rows.
- The attendee-removed and link-regenerated outcomes each render exactly one named in-page alert and no toast.
- The cancel toast reads `Booking cancelled.` and nothing more.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-05-SUMMARY.md` when done.
</output>
