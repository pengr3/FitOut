---
phase: 13-confirmation-bookings-trust
plan: 16
type: execute
wave: 10
depends_on: ["13-11", "13-12", "13-13", "13-14", "13-15"]
files_modified:
  - .planning/phases/13-confirmation-bookings-trust/13-16-SUMMARY.md
autonomous: false
requirements: [BFLOW-08, TRUST-01, TRUST-05, STATE-05]
user_setup:
  - service: github-actions
    why: "GATE-VRT baselines can only be minted inside the pinned Playwright Linux image via a workflow_dispatch, and a GITHUB_TOKEN push triggers no workflow run — so a human must dispatch, then force a follow-up run that actually COMPARES"
    dashboard_config:
      - task: "Dispatch the `baselines` workflow on the phase branch, confirm the resulting commit added only *-visual-linux.png, then push an empty commit (or re-dispatch `ci`) so a comparison run exists"
        location: "GitHub → Actions → baselines / ci"
  - service: paymongo
    why: "The real hosted-checkout return cannot be minted by any automated harness; BFLOW-08's decay must be walked once against a live sk_test_ payment"
    env_vars:
      - name: PAYMONGO_SECRET_KEY
        source: "PayMongo Dashboard → Developers → API keys (TEST key only)"
    dashboard_config:
      - task: "Expose the local webhook endpoint through ngrok/cloudflared and register that URL, per memory/paymongo-local-webhook-uat.md"
        location: "PayMongo Dashboard → Developers → Webhooks"
  - service: support-inbox
    why: "D-64's SUPPORT_EMAIL is null and the support path ships code-complete behind the guard. A monitored inbox is an operational prerequisite, not a code task"
    env_vars:
      - name: SUPPORT_EMAIL
        source: "A real, monitored mailbox the business will actually read — set at src/lib/site.ts:70, the only line that changes"
must_haves:
  truths:
    - "A real hosted-checkout return renders the confirmation moment, the URL decays, and a reload shows the ordinary page — walked once against a live sk_test_ payment (BFLOW-08)"
    - "The receipt prints legibly on paper in BOTH themes, with no solid flood and a readable reference and total (TRUST-05)"
    - "The QRPh manual-return copy matches what actually happens to the money, and never says 'refunded' (STATE-05, D-82, D-83)"
    - "TRUST-01 and STATE-05 close as PARTIAL — code-complete, address-pending — stated plainly rather than marked done (D-64)"
  artifacts:
    - path: ".planning/phases/13-confirmation-bookings-trust/13-16-SUMMARY.md"
      provides: "the walked outcomes, the comparison run id, and the named human_needed items carried to phase close"
  key_links:
    - from: "13-16-SUMMARY.md"
      to: "src/lib/site.ts"
      via: "the SUPPORT_EMAIL human_needed item, recorded with the one line that changes when the inbox exists (D-64, 11-CONTEXT D-26)"
      pattern: "SUPPORT_EMAIL"
---

<objective>
Walk the four things no harness in this repository can prove, mint the visual baselines, and record
what closes PARTIAL.

Purpose: `13-VALIDATION.md` § Manual-Only Verifications names four behaviours and gives the reason each
is manual. Three of them are about money and paper — a real hosted-checkout return, a printed page's
legibility, and whether the QRPh manual-return copy matches what actually happens to the funds. The
fourth is the standing re-verification item D-81 leaves open. Separately, GATE-VRT's baselines can only
be minted by an operator: 12-14 and 12-15 both recorded that a `GITHUB_TOKEN` push triggers no workflow
run, so **the deliverable is the follow-up COMPARISON run's id, not the generation run's**. And D-64
requires that TRUST-01 and STATE-05 be stated as PARTIAL at phase close rather than marked done.

Output: four walked verifications, one comparison run id, and the phase's `human_needed` record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-VALIDATION.md
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Mint the visual baselines, then force a run that COMPARES</name>
  <read_first>
    - .planning/phases/12-booker-path-search-listing-checkout/12-14-SUMMARY.md and 12-15-SUMMARY.md — the
      recorded operational facts, including the two-dispatch cycle and the reason the generation run
      proves nothing about the files it wrote.
    - .github/workflows/baselines.yml and .github/workflows/ci.yml, plus scripts/verify-workflows.mjs.
    - src/lib/design/visual-baselines.ts after plan 13-15 — the eleven new rows and the renamed alias.
  </read_first>
  <files>(no source file changes — this task drives GitHub Actions and reports a run id)</files>
  <action>
    OPERATOR ACTION. An agent cannot dispatch a workflow, and the pinned Playwright Linux image is the
    only place a baseline may be minted (D-29). Present the steps below, wait, and record what comes
    back verbatim. Do not generate a baseline locally under any circumstance — a locally-minted PNG
    encodes this machine's font rendering as the reference and is the exact failure D-27 exists to
    prevent.
  </action>
  <what-built>
    Plan 13-15 declared eleven new baseline surfaces, extended `e2e/helpers/visual-drive.ts` with a
    deterministic drive for each (frozen clocks asserted before capture), and extended the theme-swap
    smoke to cover all of them including the receipt's print capture. The rows are RED in CI today and
    that is the designed intermediate state: baselines are generated only inside the pinned Playwright
    Linux image, which an agent cannot dispatch.
  </what-built>
  <how-to-verify>
    1. Push the phase branch.
    2. GitHub → Actions → **baselines** → Run workflow on that branch.
    3. When it finishes, open the commit it created and confirm it added **only** files matching
       `*-visual-linux.png`. Any other file in that commit is a finding, not a nuisance.
    4. Confirm the two theme captures of each new surface differ byte-wise (D-135). A `court.png` equal
       to its `grove.png` means that surface ignored the tokens.
    5. **Then push an empty commit, or re-dispatch `ci`.** A `GITHUB_TOKEN` push triggers no workflow
       run, so the generation run has never compared against what it wrote.
    6. Report the **comparison** run's id and its result. That id is the deliverable; the generation
       run's id is not.
    7. If the comparison run is red, report the failure text verbatim rather than regenerating. A
       `A snapshot doesn't exist at …` on a surface that was supposed to be captured is a different
       finding from a pixel diff.
  </how-to-verify>
  <acceptance_criteria>
    - A comparison run id is reported, with its pass/fail result and the counts from its output.
    - The generation commit is confirmed to contain only `*-visual-linux.png` files.
    - Every new surface's two theme captures are confirmed to differ byte-wise.
    - Any red is reported verbatim and triaged as either "missing snapshot" or "pixel diff", not
      resolved by regenerating.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
  </verify>
  <done>A comparison run id is recorded with its result, the generation commit is confirmed PNG-only, and every new surface's two theme captures are confirmed to differ byte-wise.</done>
  <resume-signal>Reply with the comparison run id and its result, or describe what went wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk the four manual-only verifications</name>
  <read_first>
    - .planning/phases/13-confirmation-bookings-trust/13-VALIDATION.md § Manual-Only Verifications — the
      four rows, each with the reason it is manual and its test instructions.
    - The project memory notes for this machine: the local env / UAT seed, and the PayMongo local
      webhook recipe including the te-XOR-li signature gotcha that 400'd every confirm webhook.
    - 13-RESEARCH.md § Addendum — the QRPh re-probe method and its verbatim `HTTP 400`.
  </read_first>
  <files>(no source file changes — this task walks four behaviours and reports observations)</files>
  <action>
    OPERATOR WALKTHROUGH. Each of the four below is manual because `13-VALIDATION.md` records a reason
    no harness in this repository can satisfy: a real hosted-checkout return cannot be minted, a printed
    page's legibility is not something `emulateMedia` can prove, the manual return is an operator action
    outside the app, and third-party behaviour can change under us. Present the steps, wait, and record
    what was OBSERVED rather than "passed".
  </action>
  <what-built>
    The full phase: the confirmation moment and its decay, the three payment states with the reversed
    state's two money truths, the trust block, the copyable reference, the receipt route with its print
    contract, and the STATE-08 alert corrections. Everything an automated harness can assert has been
    asserted; these four are the ones it cannot.
  </what-built>
  <how-to-verify>
    **A — BFLOW-08's real return.** Book a slot on the local stack, pay it in PayMongo test mode on the
    hosted page, and let the webhook confirm it (tunnel per the recorded recipe). Confirm: the
    confirmation moment renders on the return; the URL bar then reads `/bookings/{id}` with no query; a
    reload shows the ordinary detail page with no moment; Back leaves for the PayMongo page rather than
    returning into the moment.

    **B — TRUST-05's printed page, both themes.** Open `/bookings/{id}/receipt` and use the browser's own
    print dialog to Save as PDF, once per theme, with Background graphics OFF (the default). Confirm: no
    solid flood; the reference and the total are readable; the status appears as a WORD and not as a
    pill; no button or link appears on the page; the itemisation and the Total are not split across a
    page break.

    **C — STATE-05 / D-82 / D-83's manual-return copy.** Trigger a gone-slot reversal on a QRPh test
    payment. Confirm: the `needs_attention` audit row appears (`action: "auto_refund_manual"`); the
    booker-facing copy never uses the word *"refunded"*; it says the amount is flagged to be returned by
    hand and carries the reference; it never says *"cannot be reversed"*. Then read the same screen
    beside a bank/e-wallet statement and confirm the two do not contradict each other.

    **D — D-81's standing re-verification.** Re-run the `/v1/refunds` probe against a QRPh test payment
    with a FRESH `Idempotency-Key`, TEST key asserted before the call. Report the verbatim response.
    ⚠ If it ever returns 2xx that is a **finding**, not a fix to apply silently — it changes D-82's
    policy split, and `REFUNDABLE_RAILS` is not widened on the strength of a walkthrough.
  </how-to-verify>
  <acceptance_criteria>
    - Each of A–D is reported as walked, with what was observed rather than "passed".
    - B is reported per theme, with the two PDFs described.
    - C reports whether the audit row appeared and quotes the copy that rendered.
    - D reports the verbatim HTTP status and body, and states explicitly whether it matches the
      2026-07-23 and 2026-08-20 probes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/confirmation-decay.spec.ts e2e/receipt-print.spec.ts</automated>
  </verify>
  <done>All four walks are reported as observations, with B reported per theme and D quoting the verbatim HTTP status and body.</done>
  <resume-signal>Reply with the four outcomes, or describe what failed and where.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the human_needed items and the PARTIAL closes</name>
  <read_first>
    - 13-CONTEXT.md D-64 — TRUST-01 and STATE-05 are **partially satisfied at phase close**:
      code-complete, address-pending. *"State this plainly in the phase SUMMARY rather than marking them
      done."* The convention is 11-CONTEXT D-26's `human_needed`.
    - src/lib/site.ts:70 — `export const SUPPORT_EMAIL: string | null = null;` and the D-26 block above
      it. This is the ONLY line that changes when the inbox exists; the gate flips to its demanding
      branch automatically and both requirements close with no other edit.
    - 13-UI-SPEC.md § Open Questions — the five product calls flagged for the PM, each decided so the
      phase could proceed and each reversible.
    - The SUMMARYs of plans 13-01 … 13-15 — every recorded skip, prediction, deviation and finding.
  </read_first>
  <files>.planning/phases/13-confirmation-bookings-trust/13-16-SUMMARY.md</files>
  <action>
    Write the phase-close record into this plan's SUMMARY. It must carry, each stated plainly rather
    than implied:

    1. **The `human_needed` items**, named: `SUPPORT_EMAIL` (a monitored inbox — D-64/D-26, with
       `src/lib/site.ts:70` named as the one line that changes), and any operator item the checkpoints
       above left open.
    2. **TRUST-01 and STATE-05 close as PARTIAL** — code-complete, address-pending. Do not mark them
       done. Say which half is complete and what the remaining half is.
    3. **The five PM-facing open questions** from 13-UI-SPEC, restated as decisions taken and reversible:
       the request-mode copy corrected by D-90; the full address on a `showExactAddress = false` listing
       (a host-facing promise the PM owns, reversible at one named boundary — `bookedListingAddress()`);
       the `src/lib/email.ts` copy constant changed here rather than in Phase 15; the pending-state
       support-escalation threshold; and the parked BIR-receipt business question.
    4. **The findings this phase hands forward**, each with its measurement: Assumption A1's
       `tabular-nums` result from 13-13 Task 3 (a failure is a Phase-17 finding, not a Phase-13 defect);
       D-81's re-probe outcome from Task 2D; and any skipped-with-reason assertion from 13-15 Task 2.
    5. **The invariants proved held**: `drizzle/` still ends at `0025_audit_resolved_by.sql` (GATE-06 /
       D-80); `REFUNDABLE_RAILS` still excludes `qrph` (D-81); `tests/design/site-contacts.test.ts` is
       green UNMODIFIED (D-64); zero packages installed; no email send trigger added, moved or removed
       (D-78); no file under `src/components/ui/**` hand-edited.

    Run the closing commands and paste their output rather than describing it:
    `npm test && npm run test:design && npm run build`, `npx tsc --noEmit`, and
    `git diff --stat drizzle/ package.json components.json src/components/ui/`.
  </action>
  <verify>
    <automated>npm test && npm run test:design && npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY names every `human_needed` item with the exact file and line that changes when it is
      resolved.
    - TRUST-01 and STATE-05 are recorded as PARTIAL with both halves described; neither is marked done.
    - All five PM-facing open questions are restated with their decision and their reversal cost.
    - `git diff --stat drizzle/ package.json components.json src/components/ui/` output is pasted and
      shows zero changes to all four.
    - `git diff --stat tests/design/site-contacts.test.ts` output is pasted and shows zero lines changed.
    - `npm test && npm run test:design && npm run build` output is pasted and all three exit 0.
  </acceptance_criteria>
  <done>The phase's partial closes, human_needed items, forwarded findings and held invariants are recorded with evidence rather than assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator dispatch → committed baselines | The one job with `contents: write` must never be granted a real secret |
| live PayMongo test key → local walkthrough | A real key on a developer machine, exercising a real charge path |
| "verified" in a SUMMARY → what was actually walked | A phase recorded as done on the strength of an unwalked checkpoint is the anti-pattern this project has already caught once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16-BASELINEUNVERIFIED | Repudiation | GATE-VRT baselines | mitigate | The deliverable is the FOLLOW-UP comparison run's id, not the generation run's — a `GITHUB_TOKEN` push triggers no run, so the generation run never compares against what it wrote (recorded in 12-14/12-15). The checkpoint asks for the comparison id explicitly |
| T-13-16-CISECRET | Information Disclosure | `baselines.yml` | mitigate | No PayMongo secret is added to any workflow. A surface needing a real secret has left GATE-01's scope and the fix is the inventory, never a credential in the job with `contents: write` (D-35) |
| T-13-16-FAKESUPPORT | Spoofing | `SUPPORT_EMAIL` | mitigate | The constant stays `null` and is carried as a named `human_needed` item. Setting it to a placeholder to make a surface pass is forbidden in three separate sentences of D-64, and `site-contacts.test.ts` is proved green UNMODIFIED |
| T-13-16-QRPHDRIFT | Tampering | `REFUNDABLE_RAILS` | mitigate | Task 2D re-runs the probe and reports verbatim. A 2xx is escalated as a FINDING that changes D-82's policy split; the array is never widened on the strength of a walkthrough |
| T-13-16-FALSEDONE | Repudiation | phase completion record | mitigate | TRUST-01 and STATE-05 are recorded PARTIAL by instruction; every closing command's output is pasted rather than described; the four manual walks report what was OBSERVED rather than "passed" |
| T-13-16-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed**, and the closing record pastes a `git diff --stat package.json` showing zero changes |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green, output pasted.
- `npx tsc --noEmit` exits 0, output pasted.
- The GATE-VRT comparison run id is recorded with its result.
- All four manual-only verifications are recorded with observations.
</verification>

<success_criteria>
- A comparison run id exists and is recorded; the generation commit contained only PNGs; both theme captures differ per surface.
- The four manual-only verifications are walked and reported as observations.
- TRUST-01 and STATE-05 are recorded PARTIAL with the one line that closes them named.
- Every phase invariant is proved held with pasted command output.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-16-SUMMARY.md` when done.
</output>
