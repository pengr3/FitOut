---
phase: 13-confirmation-bookings-trust
plan: 15
type: execute
wave: 9
depends_on: ["13-10", "13-11", "13-12", "13-13", "13-14"]
files_modified:
  - tests/design/phase13-surface-gates.test.ts
  - e2e/overflow-320.spec.ts
  - src/lib/design/visual-baselines.ts
  - e2e/helpers/visual-drive.ts
  - e2e/visual/surfaces.spec.ts
  - e2e/visual/theme-swap.spec.ts
autonomous: true
requirements: [STATE-05, STATE-06, TRUST-01, TRUST-05]
must_haves:
  truths:
    - "`--destructive` renders NOWHERE in this phase, and that is asserted rather than reviewed (13-UI-SPEC § Color)"
    - "Exactly one element in the viewport carries an accent fill on every Phase-13 surface, at 320/768/1280, in both themes"
    - "Every surface this phase touched holds at 320px with nothing wrapping or overflowing, every control is at least 44px, and focus is visible through the one DS-05 recipe (GATE-RESP, GATE-A11Y)"
    - "The eleven new baseline surfaces are declared, driven and theme-swap-checked, with the count alias renamed to the measured number (GATE-VRT, D-135)"
  artifacts:
    - path: "tests/design/phase13-surface-gates.test.ts"
      provides: "the destructive-token ban and the accent-inventory pin over the Phase-13 file set, in the two-piece idiom"
    - path: "src/lib/design/visual-baselines.ts"
      provides: "the eleven Phase-13 rows and the renamed count alias"
    - path: "e2e/helpers/visual-drive.ts"
      provides: "the per-surface drives for the payment states, the moment and the receipt — including the frozen clocks the poller and the countdown require"
  key_links:
    - from: "e2e/helpers/visual-drive.ts"
      to: "e2e/helpers/seed-payment-states.ts"
      via: "the seeded pending / not-completed / reversed fixtures the baselines photograph"
      pattern: "seed-payment-states"
    - from: "e2e/visual/theme-swap.spec.ts"
      to: "src/lib/design/visual-baselines.ts"
      via: "court.png !== grove.png for every new surface, print included (D-135)"
      pattern: "theme-swap"
---

<objective>
Hold the five hard gates on every surface this phase touched, and declare the fixtures that make the
visual gate able to see them.

Purpose: ROADMAP § Cross-Cutting Constraints makes GATE-RESP, GATE-A11Y, GATE-STATES, GATE-VRT and
GATE-NOREG exit criteria on every surface-touching phase. Two of this phase's own colour rules are
findings rather than defaults and are worth asserting: `--destructive` renders NOWHERE (a reversed
payment, a failed checkout, a settling webhook and a cancelled booking are not errors the booker
caused, and colour-as-alarm on a money surface tells them they did something wrong at the exact moment
they did not), and the accent reserved-for list stays at ten (there is no new KIND of accent use in the
whole phase). 13-UI-SPEC § Visual Baselines names the payment states as this phase's fixtures and
`e2e/helpers/seed-payment-states.ts` as the item that unblocks them.

Output: two source gates, a widened 320px sweep, and eleven declared, driven, deterministic baseline
surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The destructive-token ban and the accent-inventory pin</name>
  <read_first>
    - 13-UI-SPEC.md § Color → "`--destructive` renders nowhere in this phase, and that is the contract"
      and "The accent reserved-for list — Phase 13 appends ZERO entries." Read the falsifiable statement
      in each: a source scan over `src/app/(app)/bookings/**`, `src/components/booking/**` and
      `src/components/group/**` for `text-destructive`, `bg-destructive`, `variant="destructive"` and
      `--destructive`; and `ACCENT_USES` staying at 10.
    - src/components/group/rsvp-confirmation.tsx and src/components/booking/top-up-nudge.tsx — the
      shipped grep-tripwire idiom this extends.
    - tests/design/price-surface.test.ts:256-283, :317-345, :660-666 — the two-piece `Forbidden` type,
      the two-pass scanner, and the guard-the-guard fixture.
    - tests/design/status-vocab.test.ts:455-471 — the scan-reaches-what-it-claims positive control.
    - src/lib/design/ — locate the module holding `ACCENT_USES` and read its count assertion; the pin
      must be asserted, not merely observed.
    - 13-UI-SPEC.md § Copywriting Contract → "Destructive confirmation": *"A plan that introduces a
      `variant="destructive"` control here has misread the phase."*
  </read_first>
  <files>tests/design/phase13-surface-gates.test.ts</files>
  <action>
    Create `tests/design/phase13-surface-gates.test.ts` with two describes.

    **The destructive ban.** Scan the three directories for `text-destructive`, `bg-destructive`,
    `variant="destructive"` and `--destructive`, written in the two-piece idiom so the test cannot trip
    its own scan, and with a positive control asserting the scan visited a non-zero number of files. Each
    row's `why` carries the reasoning: DS-10 reserves `attention` for a genuine failure needing a human,
    and the booker is not that human — the operator alert `handleGoneSlot` already raises is.

    **The accent pin.** Assert `ACCENT_USES` still holds exactly ten entries and that no Phase-13 entry
    was appended. The `why` records the finding: every coral on these surfaces is already accent item 1
    (`<Button variant="brand">`) — `Back to availability`, `Try paying again`, `Find another space`,
    `Invite people`, `Pay now` — so there is no new KIND of accent use in the whole phase, and that is a
    finding rather than an omission.

    Also assert `CONTRAST_PAIRS` and `EXCLUDED_PAIRS` are unchanged in count, since 13-UI-SPEC § Color
    states this phase renders **zero undeclared pairings** — a new pair would mean a surface reached for
    an ink nobody measured.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/phase13-surface-gates.test.ts tests/design/contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/phase13-surface-gates.test.ts` passes,
      with the positive control asserting a non-zero file count.
    - Watched red, recorded verbatim in the SUMMARY: temporarily add `variant="destructive"` to a button
      in `src/components/booking/not-completed-state.tsx` and re-run — the scan must go RED naming the
      file, the line and the `why`. Remove and re-run green.
    - `grep -c 'text-destructive' tests/design/phase13-surface-gates.test.ts` returns 0 — the test does
      not spell its own prohibition contiguously.
    - `npx vitest run --config vitest.design.config.ts tests/design/contrast.test.ts` passes in both
      themes at the declared bar.
  </acceptance_criteria>
  <done>Two colour contracts are mechanical: no alarm colour on a money surface, and no eleventh accent use.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the 320px and target-size sweep to every surface this phase touched</name>
  <read_first>
    - e2e/overflow-320.spec.ts — the shipped sweep: the surfaces it visits, how it asserts
      `scrollWidth <= clientWidth`, and how it drives both themes. Extend it; do not fork it.
    - e2e/helpers/overflow.ts and e2e/helpers/theme.ts — the shared assertion and theme drive.
    - e2e/helpers/seed-payment-states.ts (13-01) — the fixtures for the states that cannot be reached by
      URL alone.
    - 13-UI-SPEC.md § Acceptance Criteria items 22, 30 and the § The Reversed State falsifiable claim:
      in the MANUAL branch at 320×568 in both themes, the support control's bounding box is fully inside
      the initial viewport and its `boundingBox().y` is LESS than the coral primary's.
    - ROADMAP § Cross-Cutting Constraints → the five hard gates table, GATE-RESP and GATE-A11Y rows.
  </read_first>
  <files>e2e/overflow-320.spec.ts</files>
  <action>
    Extend `e2e/overflow-320.spec.ts` with every surface this phase touched: the confirmation moment,
    the confirmed detail with no query, all three payment states (both reversed branches), the receipt,
    `/bookings/[id]/cancel`, `/bookings/[id]/group`, `/invite/[token]` and `bookings/[id]/not-found`.
    Both themes.

    For each, assert: `document.documentElement.scrollWidth <= clientWidth` at 320px; every interactive
    control's bounding box is at least 44px on its smaller axis; and focus is visible on the first
    tab-stop (assert a computed outline/ring rather than merely that focus moved — the one DS-05 recipe
    is the only mechanism, so a surface with no visible ring is a defect, not a variant).

    Add the STATE-06 above-the-fold assertion here rather than inventing a fourth spec:
    `money-statement`'s `boundingBox()` satisfies `y + height <= viewport.height` with zero scrolling, at
    320×568 and 1280×800, both themes, on all three payment states.

    Add the reversed-manual ordering assertion: the support control's `boundingBox().y` is less than the
    coral primary's. **When `SUPPORT_EMAIL` is null the control does not render** — write the assertion
    so it SKIPS with an explicit, named reason in that case rather than passing vacuously, and record
    the skip in the SUMMARY as a `human_needed`-linked item. A silently-passing assertion over an absent
    element is the vacuity this repository has now recorded nine times.

    Freeze or drive the clock on every surface that ticks (the poller, the hold countdown). A sweep run
    against a live poller is a flake.
  </action>
  <verify>
    <automated>npx playwright test e2e/overflow-320.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/overflow-320.spec.ts` passes for every added surface in both themes.
    - The STATE-06 above-the-fold assertion runs on all three payment states at both viewport sizes.
    - The reversed-manual ordering assertion either RUNS or SKIPS with an explicit named reason; it never
      passes over an absent element. The outcome is recorded in the SUMMARY.
    - `npx playwright test --list` shows the added cases and no existing test id changed.
    - Every added surface drives a frozen or controlled clock where it ticks.
  </acceptance_criteria>
  <done>Every Phase-13 surface holds at 320px with 44px targets and a visible focus ring, the money statement is proved above the fold, and no assertion can pass over an element that is not there.</done>
</task>

<task type="auto">
  <name>Task 3: Declare, drive and theme-swap-check the eleven new baseline surfaces</name>
  <read_first>
    - src/lib/design/visual-baselines.ts — the whole inventory shape, the BLOCKED-row convention with its
      argued reasons, and the count alias at :1083-1087 (`BaselineCountIsFiftyThree`) plus its header
      note: *"A gate whose name says 27 while its constraint says 53 is a gate…"*
    - e2e/helpers/visual-drive.ts — the per-surface drive both visual specs share, and why it exists:
      several surfaces are STATES rather than URLs and a spec cannot import a spec. Read the frozen-clock
      precedent it already carries.
    - e2e/visual/freeze.css and e2e/visual/surfaces.spec.ts and e2e/visual/theme-swap.spec.ts.
    - 13-UI-SPEC.md § Visual Baselines — the eleven-row table with widths, themes and the ⚠ notes:
      **Playwright clock freeze required** on pending settlement (the poller refreshes every 2500ms) and
      on not-completed (it renders a countdown); the reversed MANUAL branch at 320 is where the support
      control's above-the-fold claim is proved; the receipt in PRINT media via `emulateMedia`.
    - ROADMAP § Cross-Cutting Constraints → GATE-VRT and D-135: baselines are generated ONLY in the
      pinned Linux image, and a theme-swap smoke where `court.png === grove.png` means that surface
      ignored the tokens.
    - The 12-14 and 12-15 SUMMARYs (`.planning/phases/12-*/12-14-SUMMARY.md`, `12-15-SUMMARY.md`) — the
      recorded operational facts: a `GITHUB_TOKEN` push triggers no workflow run, so the generation run
      never compares against what it wrote; the deliverable is the FOLLOW-UP comparison run.
  </read_first>
  <files>src/lib/design/visual-baselines.ts, e2e/helpers/visual-drive.ts, e2e/visual/surfaces.spec.ts, e2e/visual/theme-swap.spec.ts</files>
  <action>
    Add the eleven Phase-13 rows to `src/lib/design/visual-baselines.ts` at the widths and themes
    13-UI-SPEC's table specifies, each with the reason field the inventory already demands. Rename the
    count alias from `BaselineCountIsFiftyThree` to the MEASURED new number and move the matching literal
    in whatever test pins it, in the SAME commit. Measure; do not derive the number from this plan.

    Extend `e2e/helpers/visual-drive.ts` with a drive per surface. Four of them are STATES rather than
    URLs and need the seeded fixtures from `e2e/helpers/seed-payment-states.ts`: pending settlement
    (`?paid=1` on a seeded pending row), not-completed (a live hold with an active session), reversed
    automatic, and reversed manual. The receipt's print capture drives `emulateMedia({ media: "print" })`
    before the screenshot.

    **Determinism is the acceptance criterion, not a nicety.** Every ticking surface gets a frozen or
    driven clock, asserted BEFORE any pixel is captured, exactly as 12-14's hold-countdown drive does
    (*"the hold's deadline is re-frozen to the fixture's own instant and the page clock driven to
    exactly 14:52 remaining, asserted before any pixel"*). A baseline captured on a live poller or a
    live countdown is a guaranteed flake and must not be committed.

    Extend the theme-swap smoke to cover all eleven, print included, asserting `court.png !== grove.png`.
    Add **no** new theme-swap exclusion — `global-error` remains the only one in the repo.

    ⚠ **Baselines are NOT generated by this plan.** They can only be minted in the pinned Playwright
    Linux image via the `baselines` workflow dispatch, which is an operator action. Expect the new rows
    to be RED in CI until that dispatch — **that is the correct intermediate state** and must be stated
    in the SUMMARY as a prediction, in the same shape 12-15's SUMMARY records its own. Plan 13-16 carries
    the dispatch as a blocking human checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with the renamed count alias; watched red recorded: revert the alias to
      its old number and confirm the type-level assertion fails naming it.
    - `npx playwright test --list` shows every new visual case registered, and the count of registered
      visual cases matches the inventory's new number.
    - Every drive for a ticking surface asserts its frozen clock state BEFORE capture — verifiable by
      reading the drive, and stated per surface in the SUMMARY.
    - The theme-swap spec covers all eleven surfaces including the print capture, with zero added
      exclusions (`global-error` remains the only one).
    - The SUMMARY records the PREDICTION explicitly: the new rows fail in CI with a
      `A snapshot doesn't exist at …` shape until the operator dispatch in 13-16, and that is the
      designed intermediate state — not a defect to work around by writing baselines locally.
  </acceptance_criteria>
  <done>Eleven surfaces are declared, driven deterministically and theme-swap-checked; the count alias is compile-enforced at the measured number; the operator dispatch is named as the remaining step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a passing gate → a real guarantee | A scan over zero files, or an assertion over an absent element, is green and means nothing |
| local machine → committed baselines | Baselines generated outside the pinned image encode the author's font rendering as truth |
| colour → meaning on a money surface | Alarm colour tells a booker they did something wrong at the exact moment they did not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15-VACUOUSSCAN | Repudiation | `phase13-surface-gates.test.ts` | mitigate | Two-piece encoding plus a positive control asserting a non-zero visited-file count, plus a watched red introduced into a real component. This repository has recorded nine scan-of-nothing vacuities; the control is the answer to all of them |
| T-13-15-VACUOUSSKIP | Repudiation | reversed-manual ordering assertion | mitigate | While `SUPPORT_EMAIL` is null the control does not render, so the assertion SKIPS with an explicit named reason and the skip is recorded and linked to the `human_needed` item — it never passes over an absent element |
| T-13-15-FLAKYBASELINE | Denial of Service | visual baselines | mitigate | Every ticking surface is captured against a frozen or driven clock, asserted before capture (the 12-14 precedent). A baseline taken on a live poller is a guaranteed flake |
| T-13-15-LOCALBASELINE | Tampering | `*-visual-linux.png` | mitigate | This plan generates NO baselines. They are minted only in the pinned Linux image via the operator dispatch (D-29), and the red-until-dispatch state is recorded as a prediction rather than resolved locally |
| T-13-15-ALARMCOLOUR | Repudiation | Phase-13 surfaces | mitigate | Zero `destructive` tokens asserted across three directories, with the reasoning in the `why` so the next author knows it is a contract and not an oversight |
| T-13-15-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `npx playwright test e2e/overflow-320.spec.ts` green for every added surface in both themes.
- `npx tsc --noEmit` exits 0 with the renamed baseline count alias.
- `git diff --stat drizzle/ package.json` shows zero changes.
</verification>

<success_criteria>
- Zero `destructive` tokens under the three Phase-13 directories, asserted with a positive control and a watched red.
- `ACCENT_USES` is pinned at 10; `CONTRAST_PAIRS` / `EXCLUDED_PAIRS` unchanged.
- Every Phase-13 surface passes 320px, 44px and visible-focus in both themes; the money statement is proved above the fold on all three payment states.
- Eleven baseline surfaces declared and driven deterministically; the count alias carries the measured number; the CI-red-until-dispatch state is recorded as a prediction.
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-15-SUMMARY.md` when done.
</output>
