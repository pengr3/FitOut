---
phase: 13-confirmation-bookings-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/design/measurements.ts
  - src/components/booking/pending-payment-state.tsx
  - src/components/booking/payment-reversed-state.tsx
  - src/components/booking/expired-approval-state.tsx
  - src/app/(app)/bookings/[id]/page.tsx
  - src/app/(app)/bookings/[id]/loading.tsx
  - src/app/(app)/bookings/[id]/cancel/page.tsx
  - src/app/(app)/bookings/[id]/cancel/loading.tsx
  - src/app/(app)/bookings/[id]/group/page.tsx
  - src/app/(app)/bookings/[id]/group/loading.tsx
  - e2e/helpers/seed-payment-states.ts
  - e2e/shell.spec.ts
autonomous: true
requirements: [STATE-05, BFLOW-08]
must_haves:
  truths:
    - "A booker on a pending, reversed or lapsed-approval booking reaches a page with exactly one `main` landmark (D-88.1)"
    - "The booking shell measure is one constant, not fourteen hand-typed copies"
    - "A test can seed a pending booking, a pending booking with a live hold, a reversed booking and a lapsed-approval booking — none of which any helper could seed before"
  artifacts:
    - path: "src/lib/design/measurements.ts"
      provides: "BOOKING_SHELL and CONFIRMATION_MOMENT_MIN_H, the two constants 13-UI-SPEC § Inventories this phase extends declares"
      contains: "BOOKING_SHELL"
    - path: "e2e/helpers/seed-payment-states.ts"
      provides: "the payment-state seed helper every later payment-state test depends on (13-VALIDATION § Wave 0, the single highest-leverage item)"
    - path: "e2e/shell.spec.ts"
      provides: "the one-main-landmark assertion extended to the pending / reversed / lapsed branches (D-88.1)"
  key_links:
    - from: "src/components/booking/payment-reversed-state.tsx"
      to: "src/lib/design/measurements.ts"
      via: "BOOKING_SHELL import replacing the hand-typed container string, and `main` becoming `div` (D-88.1)"
      pattern: "BOOKING_SHELL"
    - from: "e2e/shell.spec.ts"
      to: "e2e/helpers/seed-payment-states.ts"
      via: "seeded pending / reversed / lapsed rows driving getByRole(\"main\") counts"
      pattern: "seed-payment-states"
---

<objective>
Lay the two pieces of shared ground every other Phase-13 plan stands on, and close the live
nested-`<main>` defect while doing it.

Purpose: `13-VALIDATION.md` § Wave 0 names `e2e/helpers/seed-payment-states.ts` as *"the single
highest-leverage Wave 0 item — today only `confirmed` rows are seeded anywhere"*. Until it exists,
none of D-70's, D-71's, D-83's or D-87's behaviour can be tested at all. In the same pass, 13-RESEARCH
Pitfall 7 records a **verified live defect**: three components this phase rewrites each open a `<main>`
inside `(app)/layout.tsx`'s, and `e2e/shell.spec.ts` seeds only a `confirmed` booking so none of the
three is covered. Fixing the landmark before the rewrites means the rewrites cannot propagate it.

Output: two declared measurement constants, fourteen hand-typed container strings collapsed to one
import, three `<main>` landmarks converted to `div`, a reusable payment-state seed helper, and an
extended e2e assertion that would have caught the defect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-confirmation-bookings-trust/13-CONTEXT.md
@.planning/phases/13-confirmation-bookings-trust/13-PATTERNS.md
@.planning/phases/13-confirmation-bookings-trust/13-UI-SPEC.md
@.planning/phases/13-confirmation-bookings-trust/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare BOOKING_SHELL and CONFIRMATION_MOMENT_MIN_H, and collapse every hand-typed copy</name>
  <read_first>
    - src/lib/design/measurements.ts — the whole file. `HEADER_HEIGHT` (`h-14 sm:h-16` = 56/64px) is the
      value `CONFIRMATION_MOMENT_MIN_H` is derived from; `RESULT_GRID_GAP` (:209) is the precedent for a
      layout string promoted out of call sites, and its header carries the argument to restate.
    - 13-UI-SPEC.md § Spacing Scale → "Phase 13 declares exactly ONE exception" and
      "One duplication this phase ends, mechanically" — the two constants, their exact values, and the
      `svh`-not-`dvh`-not-`vh` reasoning that must be recorded in the header.
    - src/app/(app)/bookings/[id]/page.tsx:53-61 — the ONE-`main`-landmark-per-document header block.
  </read_first>
  <files>src/lib/design/measurements.ts, src/app/(app)/bookings/[id]/page.tsx, src/app/(app)/bookings/[id]/loading.tsx, src/app/(app)/bookings/[id]/cancel/page.tsx, src/app/(app)/bookings/[id]/cancel/loading.tsx, src/app/(app)/bookings/[id]/group/page.tsx, src/app/(app)/bookings/[id]/group/loading.tsx</files>
  <action>
    Add two exported constants to `src/lib/design/measurements.ts`, each with the header discipline every
    other constant in that file already carries (the value, where it is used, and why it is not a
    call-site literal):

    - `BOOKING_SHELL = "mx-auto w-full max-w-2xl px-4 py-8 sm:py-12"`. Record that every value in it is
      already a declared ladder step, that nothing about the rendering changes, and that it exists
      because `page.tsx`'s own header instructed the next author to "copy the container from
      `loading.tsx`" — an instruction is not a mechanism.
    - `CONFIRMATION_MOMENT_MIN_H = "min-h-[calc(100svh-3.5rem)] sm:min-h-[calc(100svh-4rem)]"`, placed
      immediately beside `HEADER_HEIGHT`, whose two values (`h-14 sm:h-16` = 3.5rem/4rem) it is derived
      from. Record the unit argument verbatim in substance: `svh` not `vh` (which includes the retracted
      iOS URL bar and overshoots) and not `dvh` (which re-resolves as the bar retracts, so a `min-height`
      in `dvh` grows mid-scroll and reflows the page under the reader's thumb). Phase 11's
      "`dvh`, never `vh`" rule was written for a `max-height` on a sheet; a `min-height` promise needs
      the smallest viewport. State this so it does not read as a violation. It is declared here and
      consumed by plan 13-08; declaring it now is deliberate so its derivation lives beside its source.

    Then replace every hand-typed occurrence of the container string with `{BOOKING_SHELL}`. Locate them
    first — do not work from a remembered count. Run
    `grep -rn 'max-w-2xl px-4 py-8' src/ | grep -v -c '^$'` and convert every hit in the files listed
    above (the three component files are Task 2's). Import from `@/lib/design/measurements` following the
    import ordering already in each file. The rendered class string must be byte-identical to what it
    replaced — this task changes zero pixels, which is the property that makes it safe to do first.

    Do NOT add `CONFIRMATION_MOMENT_MIN_H` to any call site in this plan.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const BOOKING_SHELL' src/lib/design/measurements.ts` returns 1 and
      `grep -c 'export const CONFIRMATION_MOMENT_MIN_H' src/lib/design/measurements.ts` returns 1.
    - `grep -rn 'max-w-2xl px-4 py-8' src/ | grep -v '^src/lib/design/measurements.ts' | grep -v '^#'`
      returns ONLY the three files Task 2 owns (`pending-payment-state.tsx`,
      `payment-reversed-state.tsx`, `expired-approval-state.tsx`) — every other call site now imports the
      constant.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run --config vitest.design.config.ts` passes with no new failures and no changed pins.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Two constants declared with their derivations recorded; every non-component call site imports `BOOKING_SHELL`; the rendered class string is unchanged; the design suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the three nested `main` landmarks to `div` (D-88.1)</name>
  <read_first>
    - src/components/booking/pending-payment-state.tsx (76 L, the `<main>` is at :52) — read the whole
      file. Its poll mechanics are FROZEN by D-71 and this task must not touch a line of them.
    - src/components/booking/payment-reversed-state.tsx (46 L, `<main>` at :18) — read the whole file.
      Only the container element and its class string change here; the copy defect is plan 13-04's.
    - src/components/booking/expired-approval-state.tsx (160 L, `<main>` at :100).
    - src/app/(app)/bookings/[id]/loading.tsx:14-15 — the precedent comment: *"It renders as a `<div>`:
      `(app)/layout.tsx` already wraps `{children}` in `<main>`, and a second `<main>` inside the first
      is a landmark this file has no reason to add."*
    - src/app/(app)/bookings/[id]/page.tsx:53-61 — the ONE-`main`-per-document header, the source of the
      rule and of the 20 Aug 2026 fix that missed these three.
  </read_first>
  <files>src/components/booking/pending-payment-state.tsx, src/components/booking/payment-reversed-state.tsx, src/components/booking/expired-approval-state.tsx</files>
  <action>
    In each of the three files, change the outer `<main …>` / `</main>` to `<div …>` / `</div>` and
    replace the hand-typed container class with `{BOOKING_SHELL}` imported from
    `@/lib/design/measurements`. Carry a one-line comment in each file naming `(app)/layout.tsx`'s
    `main` as the reason, in the shape `bookings/[id]/loading.tsx:14-15` already uses — this is the third
    place the rule has had to be restated, so state it, do not assume the next author reads the sibling.

    Change nothing else. Specifically: `pending-payment-state.tsx`'s `POLL_INTERVAL_MS`,
    `MAX_ATTEMPTS`, the ref-held router, the interval-only `setState`, the `Loader2Icon` and the
    `aria-live="polite"` region are all untouched here (D-71 freezes the mechanics; plan 13-05 owns the
    copy). `payment-reversed-state.tsx`'s copy strings are untouched here (plan 13-04 owns them).
    `expired-approval-state.tsx` changes ONLY its container — it carries no other Phase-13 work.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/booking/hold-expired-state.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<main' src/components/booking/pending-payment-state.tsx
      src/components/booking/payment-reversed-state.tsx
      src/components/booking/expired-approval-state.tsx` reports 0 for all three.
    - `grep -rn 'BOOKING_SHELL' src/components/booking/` lists all three files.
    - `git diff --stat src/components/booking/pending-payment-state.tsx` shows the change confined to the
      container line, its closing tag, the import and the comment — `POLL_INTERVAL_MS`, `MAX_ATTEMPTS`
      and the effect body are byte-identical (`git diff` shows no line containing `setInterval`,
      `MAX_ATTEMPTS` or `routerRef`).
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Zero `<main>` elements remain in the three state components; each imports `BOOKING_SHELL`; the poller's mechanics and the reversed component's copy are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build the payment-state seed helper and prove the landmark fix</name>
  <read_first>
    - e2e/shell.spec.ts:501-600 — the shipped `one main landmark on the booking routes` describe. Read
      the seed INSERT at :544-558 (the full column list), the route/resolved-marker table at :572-590,
      the teardown ordering trap at :511-519 (`booking_group.booking_id` is ON DELETE RESTRICT, so group
      rows go BEFORE `teardown()`), and the matcher rule at :460-467 — `getByRole("main")` is
      buffer-immune, a CSS `locator("main")` is not.
    - e2e/helpers/booker-seed.ts — `seedBookableListing`, `SeededListing`, `signUpBooker`, `placeHold`,
      `readExpiresAt` and the `sql` handle each seeded listing carries.
    - src/app/(app)/bookings/[id]/page.tsx:186-204 and :441-460 — the exact status/column conditions each
      branch reads, so a seeded row lands on the branch it is meant to exercise. In particular the
      lapsed-approval shape is `status='cancelled'` + `cancelled_by IS NULL` + `booking_mode='request'`
      + `payment_id IS NULL`.
    - src/lib/db/schema.ts — the `booking` table columns the INSERT must name, including
      `checkout_session_id` and `expires_at`.
  </read_first>
  <files>e2e/helpers/seed-payment-states.ts, e2e/shell.spec.ts</files>
  <action>
    Create `e2e/helpers/seed-payment-states.ts`: one helper module that, given a `SeededListing` and a
    booker id, inserts a booking row in each of the four shapes later plans need, returning the ids and a
    teardown that respects the `booking_group` ordering trap. The four shapes, each named after the
    branch it drives rather than after its status:

    - `confirmed` with frozen money columns (`space_price_cents`, `service_fee_cents`,
      `quoted_total_cents`, `currency`, `payment_id`, `payment_method`) — the receipt and detail fixture.
    - `pending` with a LIVE hold (`expires_at` in the future) and a non-null `checkout_session_id` — the
      D-70 not-completed and D-71 pending fixtures.
    - `pending` with an EXPIRED hold (`expires_at` in the past) — the fixture that proves the
      not-completed state never steals `hold-expired-state.tsx`'s landing (D-70 ⚠).
    - `cancelled`-reversed: `status='cancelled'`, `cancelled_by IS NULL`, `payment_id IS NULL`,
      `refund_cents IS NULL`, a non-null `checkout_session_id`, `booking_mode='instant'`, and the frozen
      `quoted_total_cents` — the D-83/D-87 fixture. Record in the header WHY every one of those columns
      is NULL: the confirm UPDATE is the only statement that writes `payment_id`/`payment_method`, and a
      reversal is by definition the branch where that UPDATE claimed zero rows (13-RESEARCH Pitfall 1).
    - `cancelled`-lapsed-approval: `cancelled_by IS NULL`, `booking_mode='request'`, `payment_id IS NULL`
      — distinct from the reversed row only by `booking_mode`, which is itself the finding worth
      recording in the header.

    Take the INSERT column list verbatim from `shell.spec.ts:544-558` and extend it; do not invent a
    shorter one. Parameterise ids with `randomUUID()` prefixed per shape so a failure names its fixture.

    Then extend the existing `one main landmark on the booking routes` describe in `e2e/shell.spec.ts` —
    extend it, do not fork it. Add three rows to the route/resolved-marker table: the pending booking
    loaded with `?paid=1` (resolved marker: the shipped `Payment received` heading), the reversed booking
    (resolved marker: the shipped `We couldn't complete this booking` heading), and the lapsed-approval
    booking (resolved marker: `ExpiredApprovalState`'s shipped heading — read it from the component, do
    not retype it from memory). Every marker must be RESOLVED-only so the landmark count is never
    measured against a skeleton, and every landmark query must be `getByRole("main")`.

    Do not change the assertion itself, the widths it runs at, or the existing `confirmed` row.
  </action>
  <verify>
    <automated>npx playwright test e2e/shell.spec.ts -g "one \`main\` landmark"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/helpers/seed-payment-states.ts` exists and exports a seeder for all five shapes plus a
      teardown; `npx tsc --noEmit` exits 0.
    - `npx playwright test e2e/shell.spec.ts -g "one \`main\` landmark"` passes with the three new routes
      present in the run output (the spec names each route it visits).
    - Watched red, recorded verbatim in the SUMMARY: revert ONE of the three `div`s from Task 2 back to
      `<main>` and re-run — the spec must fail naming that route and reporting `2` landmarks. Restore and
      re-run green. A spec that cannot be made to fail here has not proved the fix.
    - `npx playwright test --list` shows the booking-landmark describe grew by exactly the number of new
      routes and no existing test id changed.
  </acceptance_criteria>
  <done>The four payment-state fixtures are seedable from one helper; the landmark assertion covers the pending, reversed and lapsed branches; the fix has been watched failing and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e seed → database | The helper writes booking rows directly with raw SQL, bypassing every application-level guard |
| test fixture → production build | A fixture that leaks into `src/` becomes shipped behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-SEED | Tampering | `e2e/helpers/seed-payment-states.ts` | mitigate | Every value is interpolated through the `postgres` tagged-template `sql` handle (never string concatenation), exactly as `shell.spec.ts:544-558` already does; ids are `randomUUID()`-prefixed per shape so a fixture can never collide with a real row |
| T-13-01-TEARDOWN | Denial of Service | test database | mitigate | The helper's teardown deletes `booking_group` rows before `teardown()`, honouring the `ON DELETE RESTRICT` trap recorded at `shell.spec.ts:511-519`; without it the whole seed becomes undeletable and every later payment-state spec inherits a poisoned database |
| T-13-01-LANDMARK | Information Disclosure | assistive-technology surface | mitigate | Two `main` landmarks make a screen reader's document outline ambiguous on exactly the three branches that explain a money failure; the fix is proved by a watched red, not asserted |
| T-13-01-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** 13-RESEARCH § Package Legitimacy Audit records that this phase installs nothing; any proposed install is a scope alarm to raise, not to absorb |
</threat_model>

<verification>
- `npm test && npm run test:design && npm run build` all green.
- `npx playwright test e2e/shell.spec.ts` green, including the three new routes.
- `git diff --stat` shows zero changes under `drizzle/` and zero changes to `package.json`.
</verification>

<success_criteria>
- `BOOKING_SHELL` and `CONFIRMATION_MOMENT_MIN_H` are declared in `measurements.ts` with their derivations recorded.
- Zero `<main>` elements remain under `src/components/booking/`.
- `e2e/helpers/seed-payment-states.ts` seeds all five booking shapes and is imported by `e2e/shell.spec.ts`.
- The landmark fix has been watched failing (one `div` reverted) and passing.
- `drizzle/` still ends at `0025_audit_resolved_by.sql` (D-80 / GATE-06).
</success_criteria>

<output>
Create `.planning/phases/13-confirmation-bookings-trust/13-01-SUMMARY.md` when done.
</output>
