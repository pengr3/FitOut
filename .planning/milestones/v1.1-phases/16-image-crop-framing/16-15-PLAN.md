---
phase: 16-image-crop-framing
plan: 15
type: execute
wave: 9
depends_on: [16-06, 16-11, 16-14]
files_modified:
  - e2e/overflow-320.spec.ts
  - src/lib/design/visual-baselines.ts
  - e2e/visual/surfaces.spec.ts
autonomous: false
requirements: [CROP-01, CROP-02, CROP-03]
must_haves:
  truths:
    - "GATE-RESP holds for the surfaces this phase touched: `/profile` with the crop dialog OPEN survives 320px with nothing wrapping or overflowing, measured on a row of the existing table rather than in a second spec"
    - "GATE-VRT's inventory moves honestly: the crop dialog and the cover preview enter `SURFACE_IDS` / `VISUAL_SURFACES` / `VISUAL_BASELINES` court-only, and the count alias moves together with its twin literal in `e2e/visual/surfaces.spec.ts` — the file the docblock says nothing will remind you about"
    - "No row is added to `e2e/visual/theme-swap.spec.ts`: the token-contract set is a FIXED FOUR under D-138 and this phase makes no claim that the four cannot reach a token family (Delta-16)"
  artifacts:
    - path: "e2e/overflow-320.spec.ts"
      provides: "A second /profile row measuring the crop-dialog-open state, via the existing `open` seam"
      contains: "open:"
    - path: "src/lib/design/visual-baselines.ts"
      provides: "Two new court-only surfaces and their rows; the count alias moved"
      contains: "avatar-crop-dialog"
  key_links:
    - from: "src/lib/design/visual-baselines.ts"
      to: "e2e/visual/surfaces.spec.ts"
      via: "the duplicated baseline-count literal, deliberately a second literal in a second file"
      pattern: "EXPECTED_BASELINE_COUNT"
---

<objective>
Close the two cross-cutting gates this phase's new surfaces owe, and hand the one that cannot run on this
machine to CI.

Purpose: GATE-RESP and GATE-VRT are exit criteria on every surface-touching phase (D-131/D-134). Phase 16
added two surfaces — a bottom-sheet crop dialog and a wizard preview block — and neither has been measured
at 320px or pinned by a reference image.

Output: one new row on the 320px table, two new VRT surfaces with their inventory moves, and a blocking CI
dispatch.

**Two measured corrections to the upstream documents, made here rather than inherited.**

1. **The duplicated baseline-count literal is NOT in `.github/workflows/baselines.yml`.** RESEARCH R6 and
   `16-VALIDATION.md` both say it is. Measured: `grep -n "74" .github/workflows/baselines.yml` returns
   nothing, and `visual-baselines.ts`'s own docblock (just above the alias) names the real twin —
   *"`e2e/visual/surfaces.spec.ts` carries its own `EXPECTED_BASELINE_COUNT` literal — deliberately a SECOND
   literal in a SECOND file, so that an edit to one without the other fails loudly — but it is a `const`,
   not a type, so `tsc` reads it as a number and says nothing."* Confirmed: `e2e/visual/surfaces.spec.ts:273`
   is `const EXPECTED_BASELINE_COUNT = 74;`. **That is the file to move with the alias.** The failure it
   produces if you do not is worse than a compile error and arrives later: the dispatch shoots every surface,
   the inventory test fails on the stale count, the Playwright step exits non-zero, and the stage/commit steps
   never run — a dispatch that shoots everything and commits nothing.

2. **`profile`'s existing baselines are NOT invalidated by this phase, because there are none.** The brief
   says `profile` is already a VRT surface and its baselines are therefore invalid after the avatar-field
   restyle. Half true: `profile` IS in `SURFACE_IDS` and has two `VISUAL_BASELINES` rows — and both are
   **blocked**, on "a drive and a seeded user", named at the surface. STATE.md records the 15-11 dispatch's
   outcome verbatim: *"both `profile` rows correctly produced nothing."* So there is no committed `profile`
   image for this phase to invalidate. Say this in the SUMMARY; it changes what the dispatch is expected to
   produce.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-crop-framing/16-RESEARCH.md
@.planning/phases/16-image-crop-framing/16-PATTERNS.md
@.planning/phases/16-image-crop-framing/16-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md

<interfaces>
The GATE-RESP seam already exists and was built for exactly this shape — `e2e/overflow-320.spec.ts:259-270`:

  /** An interaction to perform AFTER `goto` and BEFORE the measurement, for a row whose subject only
   *  exists once a booker has done something. Added by plan 12-10 for exactly one row and deliberately
   *  not generalised further … */
  readonly open?: (page: Page) => Promise<void>;

used at `:340` for the booking sheet. The existing `/profile` row is at `:509-525` and its `tell` is
`'[data-testid="panel-card"]:has-text("Private account info")'` — deliberately narrowed, because
`/profile` redirects an anonymous visitor to `/login`, and since plan 15-07 `/login` also renders
`[data-testid="panel-card"]`, so the plain hook would have been satisfied by the exact failure the `tell`
mechanism exists to catch. The new row needs its own `tell` naming the DIALOG for the same reason.

The table's header count, which must move with the row — `:273`:
  "SEVENTEEN ROUTES AND THREE ROUTE STATES — 20 rows, 40 cases at two themes each."

The VRT inventory, all in `src/lib/design/visual-baselines.ts`:
  SURFACE_IDS        `:151`   — the closed union
  VISUAL_SURFACES    `:254`   — `{ kind, url, hook, hookWhy, blocked }` per id
  VISUAL_BASELINES   `:1118`  — one row per (surface, width, height, theme) with a `why`
  BaselineCountIsSeventyFour  `:2232` — `extends 74`
and its twin: `e2e/visual/surfaces.spec.ts:273` `const EXPECTED_BASELINE_COUNT = 74;` used at `:304`.

Court only (D-138). One shot per width is the rule everywhere.
`e2e/visual/theme-swap.spec.ts` is FIXED at four surfaces and gains nothing (Delta-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-RESP — a second /profile row measuring the crop dialog open at 320px</name>
  <files>e2e/overflow-320.spec.ts</files>
  <read_first>
    - `e2e/overflow-320.spec.ts` in full — the `RouteRow` type at `:245-270` including the `open` seam and its docblock, the header count at `:273`, the booking-sheet row using `open` at `:340`, the `/profile` row at `:509-525` with its narrowed-`tell` argument, and `signUpAndReachProfile` at `:200-239`
    - `e2e/helpers/avatar-session.ts` from plan 16-13
    - `src/components/profile/avatar-field.tsx` and `src/components/profile/image-crop-dialog.tsx` — what has to be on screen for the row to be measuring the dialog and not the page behind it
    - `.planning/ROADMAP.md` § Cross-Cutting Constraints — GATE-RESP's wording
  </read_first>
  <action>
    Add ONE row to `e2e/overflow-320.spec.ts`'s table: `/profile` with the crop dialog OPEN. Use the existing
    `open` seam rather than writing a second spec — the seam's own docblock says it exists for "a row whose
    subject only exists once a booker has done something", which is precisely this.

    The row's `path` reuses the existing `/profile` resolver. Its `open` signs in (already done by `path`),
    sets a file on the hidden input using a committed fixture, and waits for the dialog. Its `tell` must name
    the DIALOG — not `[data-testid="panel-card"]` — for the same reason the existing `/profile` row narrowed
    its own: a `tell` that the page behind the overlay also satisfies would report this state as covered
    while measuring the page the table already covers, twice. The pattern's shipped
    `data-testid="responsive-dialog"` scoped to the dialog's accessible name is the right hook.

    Move the header count at `:273` with the row: 20 rows -> 21, 40 cases -> 42, and update the sentence's
    route/state arithmetic so it stays a MEASURED claim rather than a stale one. The header's own text says a
    stale measured count in a gate's header is the defect class Phase 15 exists to repair — do not leave it
    stale here.

    Add nothing else to this file. Do NOT refactor `signUpAndReachProfile` into the shared helper plan 16-13
    created — that spec passes, and this phase has no requirement that needs it changed.

    Also confirm, and record, that the WIZARD photos step is already a row on this table (the cover preview
    renders there). If it is, the preview is covered by an existing row and nothing more is owed; if it is
    NOT, say so explicitly in the SUMMARY and raise it rather than silently adding a route this phase did not
    plan for.
  </action>
  <verify>
    <automated>npx playwright test e2e/overflow-320.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium` exits 0, including the new row at both themes.
    - `grep -c "21 rows, 42 cases" e2e/overflow-320.spec.ts` returns `1` (or the equivalent corrected sentence — paste the new header line in the SUMMARY).
    - The new row's `tell` names the dialog and NOT a bare `[data-testid="panel-card"]`.
    - Watched red first: point the new row's `tell` at the bare panel-card selector, observe that it passes even with the dialog closed, restore the narrowed `tell`. Transcribe both outcomes — this is the vacuity probe the file's own header demands.
    - `git diff --exit-code e2e/helpers/avatar-session.ts` exits 0.
    - The SUMMARY states whether the wizard photos step is already a row on this table.
  </acceptance_criteria>
  <done>The narrowest surface this phase ships is measured at 320px in the state that only exists after an interaction.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-VRT inventory — two court-only surfaces, and the count moved with its twin</name>
  <files>src/lib/design/visual-baselines.ts, e2e/visual/surfaces.spec.ts</files>
  <read_first>
    - `src/lib/design/visual-baselines.ts` — `SURFACE_IDS` at `:151` with its Phase-11/12/13/15 comment blocks, `VISUAL_SURFACES` at `:254` (read at least three entries in full, including `profile` at `:1040` and its `blocked` sentence), `VISUAL_BASELINES` at `:1118` with the subtotal arithmetic in the docblock above it, the `profile` rows at `:2002-2020`, and the alias plus its docblock at `:2225-2239`
    - `e2e/visual/surfaces.spec.ts` — `EXPECTED_BASELINE_COUNT` at `:273`, its use at `:304`, and the `drive.interact` mechanism at `:414-416` that lets a surface be captured in an interacted state
    - `16-UI-SPEC.md` Delta-16 — court-only, and NO row in the theme-swap spec
    - `.planning/ROADMAP.md` § Cross-Cutting Constraints — GATE-VRT and the D-138 fixed-four rule
  </read_first>
  <action>
    Add TWO surfaces, court only, one shot per width:
      `avatar-crop-dialog`     `/profile` with the dialog open   widths 320 and 1280
      `wizard-cover-preview`   the wizard photos step with >=1 photo   widths 320 and 1280
    That is FOUR new `VISUAL_BASELINES` rows. Each row carries a `why` in the file's voice, naming what that
    width pins that the other does not — for the dialog, 320 is the bottom sheet at the `dvh`-capped stage and
    1280 is the centred 384px box; for the preview, 320 is where the two `w-32` frames plus the `gap-2` gutter
    have to fit and 1280 is where they are `w-40`.

    For each new `VISUAL_SURFACES` entry, supply `kind`, `url`, `hook`, `hookWhy` and `blocked` in the file's
    established style. **Decide `blocked` by reading, not by guessing**, and take exactly one branch per
    surface:
      - If `e2e/visual/surfaces.spec.ts`'s `drive.interact` mechanism plus a seeded session can actually reach
        the state, set `blocked: null` and supply the drive.
      - Otherwise set `blocked` to a SENTENCE naming exactly what is missing, mirroring the shape `profile`'s
        two rows already use ("a drive and a seeded user"). A blocked row is this repo's stated convention —
        *an inventory somebody can work from, never a gap dressed as coverage* — not a failure.
    Record which branch each surface took, and why, in the SUMMARY.

    Then move BOTH count literals **in the same commit**:
      - `src/lib/design/visual-baselines.ts`: rename `BaselineCountIsSeventyFour` -> `BaselineCountIsSeventyEight`
        and move `extends 74` -> `extends 78`. Update the subtotal arithmetic in the docblock above
        `VISUAL_BASELINES` with a Phase-16 block, in the same tabular style the Phase-12 block uses.
      - `e2e/visual/surfaces.spec.ts:273`: `const EXPECTED_BASELINE_COUNT = 74;` -> `78`.
    The alias's own docblock warns that this second literal is a `const` and not a type, so `tsc` reads it as
    a number and says nothing — *"if you add rows here, that file is the second place to look and nothing
    will reminded you."* This task is that reminder; add a line to the docblock recording that plan 16-15
    moved both together, so the next reader sees the pairing honoured rather than rediscovered.

    Add NOTHING to `e2e/visual/theme-swap.spec.ts` and do not touch `EXPECTED_COMPARED_SURFACES` or
    `ThemeContractSurfaceCountIsFour`. The token-contract set is fixed at four by D-138, and a fifth surface
    is a claim that the four cannot reach a token family — argued in prose as an amendment, never a row
    appended. This phase makes no such claim (Delta-16).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:design && node scripts/verify-workflows.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BaselineCountIsSeventyEight" src/lib/design/visual-baselines.ts` returns `1`; `grep -c "SeventyFour"` returns `0`.
    - `grep -c "EXPECTED_BASELINE_COUNT = 78" e2e/visual/surfaces.spec.ts` returns `1`.
    - Both literals moved in ONE commit: `git show --stat HEAD` lists both files.
    - `grep -c "avatar-crop-dialog\|wizard-cover-preview" src/lib/design/visual-baselines.ts` returns at least `6` (2 ids + 2 surfaces + 4 rows, counted across the file).
    - `git diff --exit-code e2e/visual/theme-swap.spec.ts` exits 0.
    - `npx tsc --noEmit` exits 0 — the alias is a compile gate and this is what fires if the arithmetic is wrong.
    - `npm run test:design` exits 0.
    - The SUMMARY records each new surface's `blocked` disposition and the reason, and repeats this plan's two measured corrections.
  </acceptance_criteria>
  <done>Four court-only rows exist for the two new surfaces, and the count moved in both of the two files that carry it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CI baselines dispatch — the comparison run is the deliverable</name>
  <files>.planning/phases/16-image-crop-framing/16-15-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md` row `15-11-02` — the shape this checkpoint copies, including that the deliverable is the FOLLOW-UP comparison run's id and not the generation run's
    - `.planning/STATE.md` § Operator Next Steps — the 15-11 dispatch's recorded outcome, including that a `GITHUB_TOKEN` push triggers no workflow run
    - `.github/workflows/baselines.yml` — the `workflow_dispatch` trigger and the generate job
    - `playwright.config.ts` — `RUN_VISUAL_PROJECT` is constructed only when `process.platform === "linux"`, and `updateSnapshots: "none"` is unconditional
  </read_first>
  <action>
    Everything automatable is already done by Tasks 1 and 2. This task is the operator step that cannot run
    on this machine: dispatch the `baselines` workflow on `dev`, then cause a follow-up run that actually
    COMPARES, and record BOTH run ids plus the per-row outcome of the four new baselines. The deliverable is
    the comparison run's id and its `gate-visual` status — not the generation run's — because a
    `GITHUB_TOKEN` push triggers no workflow run. Handle it exactly as `15-11-02` was handled.
  </action>
  <what-built>
    Four new court-only VRT rows for `avatar-crop-dialog` and `wizard-cover-preview`, with
    `BaselineCountIsSeventyEight` and `e2e/visual/surfaces.spec.ts`'s `EXPECTED_BASELINE_COUNT` both moved to
    78 in the same commit. Everything automatable is done: the inventory compiles, `npm run test:design`
    is green, and `e2e/overflow-320.spec.ts` covers the 320px floor for the dialog-open state.

    What CANNOT be automated here, and why: `playwright.config.ts` constructs the `visual` project ONLY when
    `process.platform === "linux"`, and this machine is Windows — so the project does not exist locally.
    `updateSnapshots: "none"` is unconditional, so nothing on any machine may write a baseline outside the
    pinned Linux image. Baselines are generated ONLY by the `baselines` workflow, which is
    `workflow_dispatch`-only. This is the same handling `15-11-02` received.
  </what-built>
  <how-to-verify>
    1. Push `dev`.
    2. Dispatch the `baselines` workflow (Actions -> `baselines` -> Run workflow, on `dev`). Note the run id.
    3. When it finishes, confirm the resulting commit added ONLY `*-visual-linux.png` files — no `-win32`,
       no `-darwin`. `git ls-files 'e2e/visual/surfaces.spec.ts-snapshots/*-visual-linux.png' | wc -l`
       gives the new total; record it.
    4. Confirm which of the four new rows actually produced an image and which produced nothing. A row whose
       surface is `blocked` correctly produces nothing — that is the same outcome `profile`'s two rows had in
       the 15-11 dispatch and it is not a failure. Record the per-row outcome.
    5. **A `GITHUB_TOKEN` push triggers no workflow run**, so the generation run is NOT the deliverable. Push
       an empty commit or re-dispatch so that a `ci` run actually COMPARES, and record THAT run's id and the
       status of its `gate-visual` job.
    6. Confirm `gate-visual` is green. If it is red on a surface this phase did not touch, say so rather than
       accepting it silently — the 15-11 dispatch cleared a pre-existing `gate-visual` red as a side effect
       and re-minted ten surfaces nobody read the diff for, which STATE.md records as a thing to watch.
  </how-to-verify>
  <acceptance_criteria>
    - The generation run's id and the FOLLOW-UP comparison run's id are both recorded in the SUMMARY, and the comparison run's `gate-visual` job is green.
    - The new baseline file count is recorded, and every file added matches `*-visual-linux.png`.
    - Each of the four new rows has a recorded outcome: an image, or "blocked — produced nothing" with the blocking reason.
    - If any surface outside this phase's scope was re-minted, it is named in the SUMMARY rather than accepted quietly.
  </acceptance_criteria>
  <verify>
    <automated>git ls-files 'e2e/visual/surfaces.spec.ts-snapshots/*-visual-linux.png' | wc -l</automated>
  </verify>
  <done>The follow-up comparison run's id is recorded, its `gate-visual` job is green, only `*-visual-linux.png` files were added, and each of the four new rows has a named outcome (an image, or blocked with its reason).</done>
  <resume-signal>Type "approved" with the comparison run id, or describe what went red</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine -> committed reference images | a baseline shot off the pinned Linux image is unreproducible for everyone else |
| a gate's own header count -> reality | a stale measured count in a gate reads as coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-55 | Tampering | committed VRT baselines | mitigate | Generation is `workflow_dispatch`-only inside the pinned Linux image; `.gitignore` plus `tests/design/gitignore-baselines.test.ts` keep `*-win32.png`/`*-darwin.png` out of the index; the checkpoint confirms only `-visual-linux` files landed |
| T-16-56 | Repudiation | a dispatch that shoots everything and commits nothing | mitigate | Both count literals move in one commit; the checkpoint's step 3 confirms the commit's contents rather than trusting the run's exit code |
| T-16-57 | Spoofing | a 320px row that measures the page behind the overlay | mitigate | The new row's `tell` names the dialog, and the vacuity probe (pointing it at the bare panel-card selector and watching it pass with the dialog closed) is a required, transcribed step |
| T-16-58 | Repudiation | accepting an unrelated `gate-visual` red as cleared | mitigate | The checkpoint requires naming any surface re-minted outside this phase's scope, which is exactly what went unread after the 15-11 dispatch |
</threat_model>

<verification>
- `npx playwright test e2e/overflow-320.spec.ts --project=chromium` exits 0
- `npx tsc --noEmit` exits 0
- `npm run test:design` exits 0
- `node scripts/verify-workflows.mjs` exits 0
- `git diff --exit-code e2e/visual/theme-swap.spec.ts src/lib/design/accent-uses.ts` exits 0
- The CI comparison run's `gate-visual` job is green, with its id recorded
</verification>

<success_criteria>
- `/profile` with the crop dialog open is a measured row on the 320px table, with a `tell` that cannot pass vacuously
- Two new court-only surfaces exist with four rows, and both baseline-count literals moved together
- Nothing was added to the fixed-four theme-swap set
- The CI comparison run is green and its id is on the record, with each new row's outcome named
</success_criteria>

<output>
Create `.planning/phases/16-image-crop-framing/16-15-SUMMARY.md` when done
</output>
