---
phase: 16-image-crop-framing
plan: 11
type: execute
wave: 5
depends_on: [16-10]
files_modified:
  - src/app/(app)/profile/profile-form.tsx
  - src/lib/design/live-regions.ts
  - tests/design/profile-pass.test.tsx
  - tests/design/avatar-copy.test.tsx
autonomous: true
requirements: [CROP-01]
must_haves:
  truths:
    - "`/profile` renders the new avatar field, and the form around it is byte-unchanged: the RHF wiring, `updateProfile`, both `PanelCard`s and the truthful-save machine at `:58-76` are untouched (14-CONTEXT D-150 named this file the reference model — restyle around it, never through it)"
    - "The file input still carries no `name` and is still unregistered with RHF, so the values `updateProfile` receives did not change"
    - "Every avatar sentence the page renders is byte-identical to the exported literal it comes from, including Delta-14's changed helper, and `Uploading…` is gone from the tree (D-176)"
    - "Three of `profile-pass.test.tsx`'s four Phase-16 assertions move here, each watched red first; the fourth (`REMOVAL_WORDS`) belongs to CROP-03 and moves in plan 16-12 when `Remove photo` actually arrives"
  artifacts:
    - path: "src/app/(app)/profile/profile-form.tsx"
      provides: "The avatar row replaced by <AvatarField />; nothing else changed"
      contains: "AvatarField"
    - path: "tests/design/avatar-copy.test.tsx"
      provides: "The exported-literal copy contract, asserted against the rendered field"
      contains: "AVATAR_HELPER"
    - path: "tests/design/profile-pass.test.tsx"
      provides: "The AUTHUI-02 design gate, re-scoped to where the avatar controls now live"
      contains: "avatar-field"
  key_links:
    - from: "src/app/(app)/profile/profile-form.tsx"
      to: "src/components/profile/avatar-field.tsx"
      via: "one element replacing the inline block at :119-157"
      pattern: "<AvatarField"
---

<objective>
Put the new field on the shipped page, and move the three gates that go red because of it.

Purpose: `profile-form.tsx:119-122` carries a comment reserving this exact seam — *"Crop and avatar
teardown are Phase 16 (CROP-01 / CROP-03); this row is left able to hold a second control and is given
none now, because a destructive affordance shipped ahead of the action behind it is a button that lies."*
This plan discharges the first half of it.

Output: a one-element swap, a live-region re-key, three moved assertions and the copy gate.

**The file this touches is a reference model, and the header says so.** `profile-form.tsx:16-19`:
*"⚠ THE SAVE-STATE MACHINE IS NOT PART OF THAT CHANGE, and it is the reason to read before editing.
`saved` is set from the ACTUAL `updateProfile` result and cleared at the top of the next submit; there is
no timer on the save path, no optimistic flag, and nothing derived from 'probably worked'. 14-CONTEXT
D-150 copied this file as the reference truthful-save model. Restyle around it."*

**A second red that the gate's own comments do not anticipate (R10).** `profile-pass.test.tsx` predicts
the `REMOVAL_WORDS` red at `:1063` and says so in its message. It does NOT predict that extracting
`AvatarField` into its own file empties `form.scan.fileInputs` and the `"Upload avatar"` `ariaLabels` at
`:1044-1057`, because those are scanned against `const FORM = "src/app/(app)/profile/profile-form.tsx"`
(`:353`) specifically. Nor does it predict that `PINNED_COPY[FORM]` breaks on `Uploading…` and the helper
string. Budget both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-crop-framing/16-CONTEXT.md
@.planning/phases/16-image-crop-framing/16-RESEARCH.md
@.planning/phases/16-image-crop-framing/16-PATTERNS.md
@.planning/phases/16-image-crop-framing/16-UI-SPEC.md
@.planning/phases/14-host-tooling/14-CONTEXT.md

<interfaces>
What must stay byte-unchanged in `profile-form.tsx` (16-PATTERNS § M1, measured):
  :16-19    the header's save-machine warning
  :58-76    `saved`, `formError`, `useForm`, `onSubmit` — the truthful-save machine
  :99-103   the comment explaining the file input carries NO `name` and is not registered with RHF
  :114-118  and :219-223   the two `PanelCard` calls
  :272-300  the submit and the two regions below the panels — `formError` (`role="alert"`) and
            `saved` (`role="status" aria-label="Save state"`), and the NEUTRAL submit
            `<Button type="submit">{isSubmitting ? "Saving…" : "Save profile"}</Button>` that Delta-9 reasons from

Imports to REMOVE when the block leaves (`:27`, `:41`, `:21`): `uploadAvatarAction`,
`Avatar`/`AvatarFallback`/`AvatarImage`, and `useRef`. `useState` STAYS — the save machine uses it.

The live-region re-key, and its cause (16-PATTERNS § Measured corrections #2): `live-regions.ts`'s `at`
field is a **1-based ORDINAL among regions of the same KIND in the same FILE, in source order**
(`:227-229`, `:540`) — not a line number. So:
  - `photo-uploader.tsx`'s row does NOT move. Plan 16-06 mounting the preview above its `role="status"`
    shifted a line number, and no key reads a line number.
  - `profile-form.tsx`'s rows DO move, for a different reason: this extraction REMOVES
    `profile-avatar-error` (`alert#1`) from that file, so **`profile-form-error` becomes `alert#1`**.
    `profile-save-result` (`status#1`) is untouched. That is one row deleted and one row re-keyed.
  - `LIVE_REGION_FILES` stays at **28** — `profile-form.tsx` keeps two regions and remains a member.

The four `profile-pass.test.tsx` sites, with measured lines (16-PATTERNS § M11):
  :1044-1057  `form.scan.fileInputs` length 1 in FORM + `ariaLabels` contains "Upload avatar" in FORM   -> THIS PLAN
  :1063-1071  `form.scan.texts.filter(REMOVAL_WORDS)` is []                                            -> plan 16-12
  :1145-1156  the RENDERED ProfileForm: `getByLabelText("Upload avatar")` exists (THIS PLAN) and no
              rendered control name matches REMOVAL_WORDS (plan 16-12)                                  -> SPLIT
  :387-424    `PINNED_COPY[FORM]` pins "Upload photo", "Uploading…", "JPG or PNG, up to 5 MB. Optional.",
              "Your avatar" byte-for-byte                                                               -> THIS PLAN
  :988-1016   case 8 bans `variant="destructive"` IN `profile-form.tsx` — stays GREEN and must, because the
              removal confirm lives inside `avatar-field.tsx` (a new file). That is an argument for the file
              layout, not merely a consequence of it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the avatar block with one element, and prove nothing else moved</name>
  <files>src/app/(app)/profile/profile-form.tsx</files>
  <read_first>
    - `src/app/(app)/profile/profile-form.tsx` in full — every range named in `<interfaces>`
    - `src/components/profile/avatar-field.tsx` from plan 16-10 — its prop surface
    - `.planning/phases/14-host-tooling/14-CONTEXT.md` D-150 — the truthful-save model this file is the reference for
    - `16-PATTERNS.md` § M1 — the excerpts, the seam comment, and the import list to remove
  </read_first>
  <action>
    Replace `profile-form.tsx`'s avatar block (the `flex items-center gap-4` row and everything inside it)
    with a single `<AvatarField … />`, passing whatever props plan 16-10 defined — the initial avatar URL and
    the initials the fallback renders.

    Keep the `{/* Avatar (optional — D-09) */}` comment. **Rewrite the seam comment at `:119-122` rather than
    deleting it**: it currently says the row is left able to hold a second control and is given none *"because
    a destructive affordance shipped ahead of the action behind it is a button that lies."* Half of that is
    now discharged and half is not — CROP-01 has landed, CROP-03 arrives in plan 16-12 — so the replacement
    sentence must say exactly that, naming the plan. Deleting a comment that recorded a decision loses the
    decision.

    Remove `uploadAvatarAction`, `Avatar`/`AvatarFallback`/`AvatarImage` and `useRef` from the imports. Keep
    `useState`.

    Change NOTHING else. The `git diff` for this file must contain exactly three kinds of hunk: the import
    removals, the block replacement, and the seam comment rewrite. If a fourth hunk appears — a reformat, a
    "while I'm here", a lint autofix reaching outside the edit — revert it.

    Preserve the property `:99-103` records: the file input carries no `name` and is not registered with RHF,
    so the values `updateProfile` receives are unchanged. That property now lives inside `AvatarField`, and
    the comment explaining it must survive somewhere — either restated at the call site or already present in
    `avatar-field.tsx`'s header. Check, do not assume.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/profile/ && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git diff src/app/\(app\)/profile/profile-form.tsx` contains exactly three kinds of hunk (imports, block, seam comment) — paste the diff stat in the SUMMARY.
    - `grep -c "useState" src/app/\(app\)/profile/profile-form.tsx` is unchanged from before the edit; `grep -c "useRef\|uploadAvatarAction\|AvatarImage"` returns `0`.
    - `grep -c "Saving…" src/app/\(app\)/profile/profile-form.tsx` returns `1` and `grep -c "Save profile"` returns `1` — the submit is untouched.
    - `grep -c "setSaved(true)" src/app/\(app\)/profile/profile-form.tsx` returns `1` — the truthful-save machine is intact.
    - `npx vitest run tests/profile/` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>`/profile` renders the new field and the diff proves the form around it did not move.</done>
</task>

<task type="auto">
  <name>Task 2: Re-key the live regions and move three profile-pass assertions, each watched red first</name>
  <files>src/lib/design/live-regions.ts, tests/design/profile-pass.test.tsx</files>
  <read_first>
    - `src/lib/design/live-regions.ts` — the `profile-avatar-error` row at `:1382-1400`, the `profile-form-error` and `profile-save-result` rows, `LIVE_REGION_FILES`, and the `at`-is-an-ordinal definition at `:227-229` / `:540`
    - `tests/design/live-regions.test.tsx` SCAN 2 at `:702-766` — declared-but-absent and present-but-undeclared reported together
    - `tests/design/profile-pass.test.tsx` — the header at `:15-30` (the four-step mechanism), `FORM` at `:353`, `PINNED_COPY` at `:387-424`, `REMOVAL_WORDS` at `:445`, case 8 at `:988-1016`, case 10 at `:1044-1071`, and the rendered assertions at `:1145-1156`
    - `16-PATTERNS.md` § M8 and § M11
  </read_first>
  <action>
    Run both design gates FIRST and observe them red. Transcribe each failure into the SUMMARY separately —
    there are four distinct reds here and a single "tests failed" line hides three of them.

    (a) `src/lib/design/live-regions.ts`: DELETE the `profile-avatar-error` row keyed on
        `src/app/(app)/profile/profile-form.tsx` (the element left that file) — the equivalent row keyed on
        `avatar-field.tsx` already exists from plan 16-10. Then re-key `profile-form-error` from `alert#2` to
        `alert#1`, because it is now the first alert in source order in that file. Leave `profile-save-result`
        (`status#1`) alone. `LIVE_REGION_FILES` stays at 28 and the alias stays
        `DeclaredFileCountIsTwentyEight` — `profile-form.tsx` still owns two regions.
        Add one sentence to the re-keyed row's `why` recording that its ordinal moved because a sibling left
        the file, not because it became more important — the module's existing rows use exactly that voice
        (`:1382-1400`'s "⚠ ITS ORDINAL IS 1 BECAUSE OF WHERE IT SITS, NOT BECAUSE IT MATTERS MORE").

    (b) `tests/design/profile-pass.test.tsx` — move exactly THREE things and leave the fourth:
        1. `:1044-1057` — the `fileInputs` length and the `"Upload avatar"` `ariaLabels` assertion. Re-scope
           them from `FORM` to `src/components/profile/avatar-field.tsx`, adding that path to whatever file
           set the scan walks. Do NOT weaken them to "somewhere in the tree": the claim is that there is
           exactly ONE file input and exactly ONE `Upload avatar` label, and that claim is only worth
           anything if it names the file.
        2. `:1145-1156` — the RENDERED half. `getByLabelText("Upload avatar")` must still resolve, because
           `AvatarField` renders inside `ProfileForm`. If it already passes, say so and change nothing; if the
           extraction broke the render (a missing mock, a client-boundary import), fix the TEST's setup, not
           the component. **Leave the `REMOVAL_WORDS` half of that block alone** — nothing removal-shaped
           exists yet.
        3. `:387-424` `PINNED_COPY[FORM]` — DELETE `"Uploading…"` (Delta-14 retired it: nothing uploads before
           the confirm, so it describes a state that no longer exists) and REPLACE
           `"JPG or PNG, up to 5 MB. Optional."` with `"JPG, PNG, or WebP, up to 5 MB. Optional."`. If
           `"Upload photo"` and `"Your avatar"` now render from `avatar-field.tsx` rather than `profile-form.tsx`,
           move those pins to a `PINNED_COPY` entry for the new file rather than dropping them — a pin
           dropped is a copy contract silently retired.
        Each edit carries a comment naming the plan that caused it and the Delta or decision behind it, in
        the file's own voice.

    Leave `:1063-1071` (`REMOVAL_WORDS`) and case 8 (`:988-1016`, `variant="destructive"` banned in
    `profile-form.tsx`) exactly as they are. Both are plan 16-12's, and case 8 must stay GREEN forever —
    keeping the removal confirm inside `avatar-field.tsx` is what makes that true.
  </action>
  <verify>
    <automated>npx vitest run tests/design/live-regions.test.tsx tests/design/profile-pass.test.tsx --config vitest.design.config.ts && npm run test:design</automated>
  </verify>
  <acceptance_criteria>
    - Four distinct reds are transcribed separately in the SUMMARY: the declared-but-absent live region, the `fileInputs`/`ariaLabels` emptying, the rendered-label assertion (if it failed), and the `PINNED_COPY` mismatch.
    - `grep -c "profile-avatar-error" src/lib/design/live-regions.ts` returns `1` — the `avatar-field.tsx` row only; the `profile-form.tsx` one is gone (rename the id if it collides).
    - `grep -c "DeclaredFileCountIsTwentyEight" src/lib/design/live-regions.ts` returns `1` — the count did NOT move.
    - `grep -c "Uploading…" tests/design/profile-pass.test.tsx` returns `0`.
    - `grep -c "JPG, PNG, or WebP, up to 5 MB. Optional." tests/design/profile-pass.test.tsx` returns at least `1`.
    - `grep -c "REMOVAL_WORDS" tests/design/profile-pass.test.tsx` is unchanged from before this task — that assertion did not move.
    - `npx vitest run tests/design/profile-pass.test.tsx --config vitest.design.config.ts` exits 0.
    - Task 1's extraction and these inventory moves are in ONE commit.
  </acceptance_criteria>
  <done>Both gates are green again for the right reason, the fourth assertion is still waiting for CROP-03, and every move was watched failing.</done>
</task>

<task type="auto">
  <name>Task 3: tests/design/avatar-copy.test.tsx — the exported-literal contract</name>
  <files>tests/design/avatar-copy.test.tsx</files>
  <read_first>
    - `tests/design/profile-pass.test.tsx` lines `:15-30` (the four-step mechanism: file->pattern by AST, pattern->attribute by a REAL render, attribute->contract by a closed union, and the copy written as literals so a change has to move a test file) and `:33-56` (how it renders the real `ProfileForm` under the design config — one `vi.mock("next/navigation")` supplying a router with three no-ops)
    - `src/lib/avatar.ts` — the literals under contract
    - `src/components/profile/avatar-field.tsx` — what actually renders
    - `src/components/listing/photo-uploader.tsx` lines `:76-81` — the hoisted-literal convention and its stated reason (so the gate can resolve it to a string)
    - `16-UI-SPEC.md` § Copywriting Contract — the full literal list, the "Reused verbatim" table and Delta-14
  </read_first>
  <action>
    Create `tests/design/avatar-copy.test.tsx` (design config) with `// @vitest-environment jsdom` on line 1.

    Header: state the mechanism in `profile-pass.test.tsx`'s numbered voice — (1) every `AVATAR_*` literal is
    read from `@/lib/avatar`, (2) `AvatarField` is RENDERED in jsdom and the sentences are counted off the
    produced DOM rather than off anybody's source, (3) each rendered sentence is compared to the export with
    `toBe`, so a copy change has to move a test file and be seen in the diff. State what would still pass
    without it: a component that inlined a string identical to the export today and drifted tomorrow.

    Assert, at minimum:
    - the helper renders `AVATAR_HELPER` byte-for-byte, and that literal NAMES WebP (Delta-14 changed it; the
      shipped `JPG or PNG, up to 5 MB. Optional.` must appear nowhere in `src/`);
    - the primary control reads `AVATAR_UPLOAD_LABEL` with no avatar and `AVATAR_CHANGE_LABEL` with one;
    - `Uploading…` renders nowhere and appears nowhere under `src/` (a source scan plus a render scan — the
      retirement is a claim about the whole tree, not about one component);
    - each of the four refusal sentences renders byte-identically to its export when its guard fires;
    - `AVATAR_CROP_CONFIRM_BUSY` is byte-identical to the `Saving…` the profile submit renders, including the
      ellipsis CHARACTER — same route, same word, and Delta-14 calls that out specifically;
    - the shipped strings the UI-SPEC's "Reused verbatim" table pins are NOT re-declared in `src/lib/avatar.ts`:
      `Only image files are allowed.`, `Image must be 5 MB or smaller.` and
      `Could not upload your photo. Please try again.` each have exactly one home, and this file asserts the
      count rather than trusting it.
  </action>
  <verify>
    <automated>npx vitest run tests/design/avatar-copy.test.tsx --config vitest.design.config.ts && npm run test:design</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/design/avatar-copy.test.tsx --config vitest.design.config.ts` exits 0.
    - `grep -rc "JPG or PNG, up to 5 MB. Optional." src/` returns `0` — the shipped helper is gone from the tree.
    - `grep -rc "Uploading…" src/` returns `0`.
    - Every copy assertion uses `toBe` against an imported constant; `grep -c "toContain(" tests/design/avatar-copy.test.tsx` returns `0` for the copy assertions.
    - Watched red first: change one character of `AVATAR_HELPER` in `src/lib/avatar.ts`, observe the render comparison fail, restore it. Transcribe the failure.
    - `npm run test:design` exits 0.
  </acceptance_criteria>
  <done>Every avatar sentence on the page is provably the exported literal, and the retired one is provably gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a shipped, UAT-passed surface -> a phase-16 edit | `/profile` carries the truthful-save model other phases copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-37 | Tampering | the truthful-save machine (`profile-form.tsx:58-76`) | mitigate | The diff is constrained to three hunk kinds and the machine's key lines are grep-asserted; `tests/profile/` and `npm run build` are in the verify command |
| T-16-38 | Tampering | RHF field values reaching `updateProfile` | mitigate | The file input still carries no `name` and is still unregistered; the comment recording why must survive the extraction, and the task checks rather than assumes |
| T-16-39 | Elevation | a design gate re-scoped into vacuity | mitigate | The `fileInputs` / `ariaLabels` assertions are re-scoped to the NEW file by name, never weakened to "somewhere in the tree"; a pin that would otherwise be dropped is moved rather than deleted |
| T-16-40 | Repudiation | a copy contract silently retired | mitigate | `avatar-copy.test.tsx` asserts the retirement of `Uploading…` over the whole `src/` tree, not just over one render |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `npm run test:design` exits 0
- `npx vitest run tests/profile/` exits 0
- `npx playwright test e2e/overflow-320.spec.ts --project=chromium` exits 0 — `/profile` is a row on that table and its `tell` is pinned to the private-account panel
- `git diff --exit-code src/components/profile/avatar-field.tsx src/components/profile/image-crop-dialog.tsx` exits 0 — this plan changes neither
</verification>

<success_criteria>
- `/profile` renders `<AvatarField />` and the surrounding form is byte-unchanged
- The live-region rows reflect where the alert actually lives, with the count unmoved at 28
- Three `profile-pass.test.tsx` assertions moved, each watched red; `REMOVAL_WORDS` and case 8 untouched
- Every avatar sentence is provably its exported literal, and `Uploading…` is gone from `src/`
</success_criteria>

<output>
Create `.planning/phases/16-image-crop-framing/16-11-SUMMARY.md` when done
</output>
