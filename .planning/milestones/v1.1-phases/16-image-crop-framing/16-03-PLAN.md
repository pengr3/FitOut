---
phase: 16-image-crop-framing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/patterns/responsive-dialog.tsx
  - tests/design/responsive-dialog-autofocus.test.tsx
autonomous: true
requirements: [CROP-03]
must_haves:
  truths:
    - "An adopter can decide where focus lands when the overlay opens, so 'focus lands on the safe action, never on Remove photo' becomes mechanical rather than a DOM-order accident (D-168's binding mitigation)"
    - "Omitting the new prop changes nothing: all five existing adopter files are byte-unchanged and the default is proven inert by a rendering assertion, not by a diff alone"
    - "The app still has ONE overlay mechanism, one focus trap and one escape behaviour — no `alert-dialog`, no second primitive (D-167)"
  artifacts:
    - path: "src/components/patterns/responsive-dialog.tsx"
      provides: "ResponsiveDialogProps + onOpenAutoFocus, forwarded verbatim to DialogContent"
      contains: "onOpenAutoFocus"
    - path: "tests/design/responsive-dialog-autofocus.test.tsx"
      provides: "The guard-the-guard: the default is inert AND the handler actually moves focus"
      contains: "onOpenAutoFocus"
  key_links:
    - from: "src/components/patterns/responsive-dialog.tsx"
      to: "DialogContent"
      via: "verbatim prop forwarding, exactly as onCloseAutoFocus"
      pattern: "onOpenAutoFocus=\\{onOpenAutoFocus\\}"
---

<objective>
Add the single additive prop this phase needs on a shared pattern, and prove the addition changed
nothing for the five files that do not use it.

Purpose: D-168 traded `alert-dialog` away and made "default focus lands on `Keep photo`" a BINDING
mitigation. Under `ResponsiveDialog` that mitigation is currently unimplementable — Radix autofocuses
the first tabbable element, `DialogFooter` is `flex-col-reverse … sm:flex-row`, so DOM order and visual
order are inverted on mobile and no DOM order satisfies both the focus requirement and the stacking
order (Delta-5b). The pattern forwards `onCloseAutoFocus` and nothing else.

Output: `onOpenAutoFocus?: (event: Event) => void` on `ResponsiveDialogProps`, and a design spec that
asserts BOTH halves.

Explicitly NOT added: `dismissLocked` (RESEARCH §D13 — Delta-3's single `onOpenChange` guard is the
contract and the briefly-inert `×` is an accepted, recorded trade; shipping an unused branch on a
shared pattern is worse). It stays a named reversal in 16-UI-SPEC Open Question 16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-crop-framing/16-CONTEXT.md
@.planning/phases/16-image-crop-framing/16-RESEARCH.md
@.planning/phases/16-image-crop-framing/16-PATTERNS.md
@.planning/phases/16-image-crop-framing/16-UI-SPEC.md

<interfaces>
Current `ResponsiveDialogProps` — ten props, no `className`, no width (`responsive-dialog.tsx:138-201`,
quoted in full at `16-PATTERNS.md` § M2):

  open?, onOpenChange?, trigger?, title (REQUIRED), hideTitle?, description?,
  children?, footer?, closeLabel?, onCloseAutoFocus?

The five adopter FILES that must stay byte-unchanged (six call sites; re-verified 2026-08-25):

  src/app/dev/theme/page.tsx                     (1)
  src/components/availability/blocks-editor.tsx  (2 — :207 and :327)
  src/components/booking/booking-sticky-bar.tsx  (1)
  src/components/host/request-row.tsx            (1)
  src/components/patterns/site-chrome.tsx        (1)

Downstream consumers of the new prop (do not build them here):
  - plan 16-12's removal confirm passes a handler that `preventDefault()`s and focuses `Keep photo`
  - `ImageCropDialog` (plan 16-09) does NOT use it — it uses `onCloseAutoFocus`, which already exists (Delta-5c)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onOpenAutoFocus to ResponsiveDialogProps and forward it verbatim</name>
  <files>src/components/patterns/responsive-dialog.tsx</files>
  <read_first>
    - `src/components/patterns/responsive-dialog.tsx` in full — especially `:113-117` (the AC#28 source scan for `/\[\d+vh\]/`), `:121-136` (`SHEET_PRESENTATION`), `:138-201` (the prop type), `:185-199` (the `onCloseAutoFocus` docblock this one twins), `:203-214` (the destructure) and `:227-260` (the DOM)
    - `16-UI-SPEC.md` Delta-5b and Delta-5c — why the prop is required rather than convenient, and which overlay uses which hook
    - `16-RESEARCH.md` §D13 — the exact current prop table, the six adopters, and why `dismissLocked` is NOT added
    - `src/components/ui/dialog.tsx` around `:62` and `:73` — `showCloseButton` default and the `DialogContent` class string
  </read_first>
  <action>
    In `src/components/patterns/responsive-dialog.tsx`, make exactly three edits:

    (1) Add `onOpenAutoFocus?: (event: Event) => void;` to `ResponsiveDialogProps`, positioned immediately
        ABOVE `onCloseAutoFocus` so the pair reads as a pair, with a docblock that twins `:185-199`'s shape:
        Radix autofocuses the first tabbable element in `DialogContent`; `DialogFooter` inverts DOM and
        visual order below `sm:` (`ui/dialog.tsx:119` `flex-col-reverse … sm:flex-row`), so an adopter whose
        footer holds a destructive action and a safe one cannot satisfy both the stacking order and
        "focus never lands on the destructive action" through DOM order alone. State the same boundary the
        twin states: every adopter whose default focus is already correct should leave this undefined —
        Radix's own behaviour is right for them, and taking the decision over unconditionally would be a
        claim this pattern has no basis for. Name D-168 as the decision that made it binding.

    (2) Add `onOpenAutoFocus` to the destructure at `:203-214`, in the same relative position.

    (3) Add `onOpenAutoFocus={onOpenAutoFocus}` to the `DialogContent` attribute list, immediately above the
        existing `onCloseAutoFocus={onCloseAutoFocus}`. Verbatim forwarding — no wrapper, no default value,
        no `?? undefined`, no conditional spread.

    Do NOT add a class to `SHEET_PRESENTATION`. Do NOT add `dismissLocked`. Do NOT write a `vh` value or
    a `[NNvh]` string anywhere in this file, including in a comment — `:113-117` records that AC#28 scans
    this file's source with `/\[\d+vh\]/` and requires no match, so a comment about the crop stage's
    `40dvh` cap must not be written here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:design && git diff --exit-code src/app/dev/theme/page.tsx src/components/availability/blocks-editor.tsx src/components/booking/booking-sticky-bar.tsx src/components/host/request-row.tsx src/components/patterns/site-chrome.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onOpenAutoFocus" src/components/patterns/responsive-dialog.tsx` returns exactly `3` (type, destructure, forward).
    - `grep -c "dismissLocked" src/components/patterns/responsive-dialog.tsx` returns `0`.
    - `grep -cE "\[[0-9]+vh\]" src/components/patterns/responsive-dialog.tsx` returns `0`.
    - `git diff --exit-code` over the five adopter files exits 0.
    - `npm run test:design` exits 0 (includes `sheet-absent.test.ts`, whose three-name deny-list must not have moved).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The prop exists, is forwarded verbatim, defaults `undefined`, and no adopter file changed.</done>
</task>

<task type="auto">
  <name>Task 2: tests/design/responsive-dialog-autofocus.test.tsx — both halves of the guard</name>
  <files>tests/design/responsive-dialog-autofocus.test.tsx</files>
  <read_first>
    - `tests/design/theme-provider.test.tsx` lines `:1-25` — the design-config spec that renders React, and whose header is exactly the "provably a map, not a constant dressed up as one" argument this file needs
    - `tests/host/request-refusal.test.tsx` lines `:1-64` and `:96-108` — the jsdom idioms: pragma on line 1, `await screen.findByRole("dialog")` for a portal, the `textOf` helper
    - `src/components/patterns/responsive-dialog.tsx` as edited in Task 1
    - `16-RESEARCH.md` §D13 → "The assertion that makes 'byte-unchanged' a fact rather than a hope" — the four proofs in decreasing order of value
  </read_first>
  <action>
    Create `tests/design/responsive-dialog-autofocus.test.tsx` with `// @vitest-environment jsdom` on line 1,
    above the header comment.

    Header: state that this file proves an ADDITIVE prop is additive, and state what would still pass
    without it — a `git diff` over the five adopter files stays clean even if the pattern's DEFAULT changed,
    because none of them mention the prop. That is the whole reason half one exists.

    Half one — THE DEFAULT IS INERT. Render a `ResponsiveDialog` with `open`, a `title`, a `footer`
    containing two named buttons, and NO `onOpenAutoFocus`. Assert `document.activeElement` is where Radix
    puts it with no handler — capture that element's accessible name and assert it with `toBe`, not
    `toBeTruthy`. Add the comment that makes it a map rather than a constant: if the pattern ever defaulted
    the prop to a handler, this assertion is the only thing in the repository that would notice.

    Half two — THE HANDLER ACTUALLY MOVES FOCUS. Render the same tree with an `onOpenAutoFocus` that calls
    `event.preventDefault()` and focuses the SECOND footer button by ref. Assert `document.activeElement`
    is that button, by accessible name, with `toBe`. This is Delta-5b's mitigation itself, asserted on the
    primitive rather than on the not-yet-existing removal confirm.

    Half three — THE ADOPTER CENSUS. Read the five adopter files from disk and assert that none of them
    contains the string `onOpenAutoFocus`. This is the standing version of Task 1's `git diff`: a diff is
    true once, at commit time; this is true on every run.

    Add the `ResizeObserver` stub verbatim from `tests/availability/availability-calendar.test.tsx:75-83`
    (including the `??` so it never clobbers a real implementation) if Radix needs it. Do NOT stub
    `getBoundingClientRect` — this repo has never done so and two specs record the refusal in prose.
  </action>
  <verify>
    <automated>npx vitest run tests/design/responsive-dialog-autofocus.test.tsx --config vitest.design.config.ts && npm run test:design && npx playwright test e2e/mobile-booker-path.spec.ts e2e/photo-lightbox.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/design/responsive-dialog-autofocus.test.tsx --config vitest.design.config.ts` exits 0 with at least 3 assertions (default-inert, handler-moves-focus, adopter census).
    - The default-inert assertion uses `toBe` against a named element, not `toBeTruthy`.
    - `npx playwright test e2e/mobile-booker-path.spec.ts e2e/photo-lightbox.spec.ts --project=chromium` exits 0 — two adopters still work in a real browser.
    - `npm run test:design` exits 0.
    - Record in the SUMMARY the accessible name Radix focuses by default with no handler, as measured — not as predicted.
  </acceptance_criteria>
  <done>Both directions are asserted: omitting the prop changes nothing, and supplying it changes exactly what Delta-5b needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared pattern -> six call sites | one edit to `responsive-dialog.tsx` reaches every overlay in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | `patterns/responsive-dialog.tsx` | mitigate | Additive prop only, default `undefined`, forwarded verbatim; proven by `git diff --exit-code` over the five adopter files PLUS a standing rendering assertion that the default is inert |
| T-16-08 | Elevation | focus placement on a destructive confirm | mitigate | The prop is the mechanism D-168's binding mitigation needs; half two of the spec asserts it actually moves focus, so the mitigation cannot ship as a no-op |
| T-16-09 | Spoofing | a second focus trap entering the app | accept | `alert-dialog` is not added and `tests/design/sheet-absent.test.ts`'s three-name deny-list is untouched; the residual risk is that a later phase adds one, which that gate already covers |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run test:design` exits 0
- `git diff --exit-code` over the five adopter files exits 0
- `npx playwright test e2e/mobile-booker-path.spec.ts e2e/photo-lightbox.spec.ts --project=chromium` exits 0
- `grep -c "alert-dialog" src/components/ui/ -r` returns `0` — `ui/alert-dialog.tsx` still does not exist
</verification>

<success_criteria>
- `onOpenAutoFocus` exists on `ResponsiveDialogProps`, is forwarded verbatim, and defaults `undefined`
- The five adopter files are byte-unchanged and a standing spec proves the default is inert
- The positive half proves the handler moves focus to a named button
- `dismissLocked` was not added; `SHEET_PRESENTATION` was not touched
</success_criteria>

<output>
Create `.planning/phases/16-image-crop-framing/16-03-SUMMARY.md` when done
</output>
