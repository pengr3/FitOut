---
phase: 16-image-crop-framing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/slider.tsx
  - tests/design/leak.test.ts
autonomous: true
requirements: [CROP-01]
must_haves:
  truths:
    - "The zoom control's primitive exists in the repo and is inside the leak gate's scanned set, not beside it (D-167 names `slider` as the one net-new shadcn block; `alert-dialog` is NOT added)"
    - "The emitted block ships zero `bg-white`, zero `ring-ring/50` and a thumb hit area of 44px, so the phase's first commit is green rather than red"
    - "`package.json` gains nothing from this plan — `@radix-ui/react-slider` is already a dependency of the installed `radix-ui@1.4.3`"
  artifacts:
    - path: "src/components/ui/slider.tsx"
      provides: "The vendored Radix Slider block, token-mapped to this repo's gates"
      contains: "SliderPrimitive"
    - path: "tests/design/leak.test.ts"
      provides: "The vendored-primitive census, moved 31 -> 32 with the new file named"
      contains: "src/components/ui/slider.tsx"
  key_links:
    - from: "src/components/ui/slider.tsx"
      to: "tests/design/leak.test.ts"
      via: "the vendored-primitive census counts the file"
      pattern: "toHaveLength\\(32\\)"
---

<objective>
Land the one shadcn block this phase adds, and land it already inside the gates rather than beside them.

Purpose: `react-easy-crop` gives no zoom control. The zoom row (999.2 § 2c) needs a continuous range,
and nothing in the 31 installed primitives does one. The emitted registry block ships three things this
repo rejects on arrival (RESEARCH §D14 / R11), so the add and the corrections are ONE task — a two-task
sequence would put a red commit on `dev`.

Output: `src/components/ui/slider.tsx` (vendored, token-mapped) and `tests/design/leak.test.ts` moved
31 -> 32 with the new file named beside the number, exactly as `ui/collapsible.tsx` got in commit `26ddcf8`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-image-crop-framing/16-CONTEXT.md
@.planning/phases/16-image-crop-framing/16-RESEARCH.md
@.planning/phases/16-image-crop-framing/16-PATTERNS.md
@.planning/phases/16-image-crop-framing/16-UI-SPEC.md

<interfaces>
The exact block `npx shadcn add slider` emits was fetched from the registry on 2026-08-25 and is
quoted verbatim in `16-RESEARCH.md` §D14. Read it there rather than re-fetching. The four properties
that matter, and their dispositions:

  bg-white              (thumb)  -> bg-background                      leak gate, no vendored exemption
  ring-ring/50 / ring-3 (thumb)  -> the canonical DS-05 focus recipe   focus-recipe.test.ts:115
  after:-inset-2        (thumb)  -> after:-inset-4                     12px + 32px = 44px (Delta-12)
  bg-primary            (range)  -> KEEP                               --primary is a neutral (Delta-9)

The canonical focus recipe, byte-for-byte (`tests/design/focus-recipe.test.ts:110-112`):
`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`

Consumer contract (plan 16-09 will call it): the crop dialog passes `value={[zoom]}` as an ARRAY,
`min={1}`, `max={maxZoom}`, `step={0.01}`, `aria-label="Zoom"`. A scalar `value` makes the block compute
`_values = [min, max]` and render TWO thumbs at the extremes — do not "simplify" that away.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the slider block and map its three rejected values in the same edit</name>
  <files>src/components/ui/slider.tsx</files>
  <read_first>
    - `16-RESEARCH.md` §D14 — the verbatim emitted block and the four-row disposition table
    - `16-PATTERNS.md` § `src/components/ui/slider.tsx` — what this repo does to a vendored block on arrival (commit `26ddcf8`, the four landing conditions)
    - `src/components/ui/collapsible.tsx` — the most recently vendored block; observe the CLI's own formatting (no semicolons, double quotes) is left untouched
    - `src/components/ui/button.tsx` — lines around `:74` (the canonical focus recipe in situ) and `:104`/`:112-115` (`sm` = `h-7`, `touch` = `h-11`)
    - `config/design-leak-patterns.mjs` — the `white-black-class` rule (`:190`) and `LEAK_SCAN_PREFIXES` (`:218`)
    - `tests/design/focus-recipe.test.ts` — `CANONICAL_RECIPE` (`:110-112`) and `HALF_ALPHA_RING` (`:115`)
  </read_first>
  <action>
    Run `npx shadcn add slider` (official registry; `components.json` `registries` stays `{}`). Then, in the
    SAME edit, before committing, apply exactly four changes to `src/components/ui/slider.tsx`:

    (1) On `SliderPrimitive.Thumb`, replace `bg-white` with `bg-background`.
    (2) On `SliderPrimitive.Thumb`, delete `ring-ring/50`, delete all three `ring-3` utilities
        (`hover:ring-3`, `focus-visible:ring-3`, `active:ring-3`) and the bare `focus-visible:outline-hidden`,
        and put the canonical recipe in their place verbatim:
        `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`.
        Keep `border border-ring`, `transition-[color,box-shadow]`, `select-none`,
        `disabled:pointer-events-none` and `disabled:opacity-50`.
    (3) On `SliderPrimitive.Thumb`, change `after:-inset-2` to `after:-inset-4` so the `size-3` (12px) thumb
        carries a 44px hit area. `-inset-4` is 16px, already on the 4px grid — introduce no arbitrary value.
    (4) Leave `bg-primary` on `SliderPrimitive.Range`, `touch-none` on the Root and
        `data-disabled:opacity-50` on the Root exactly as emitted. All three are correct and are load-bearing:
        `touch-none` is the slider's half of Delta-4, and `data-disabled:opacity-50` gives rule F8's disabled
        row its treatment for free.

    Do NOT reformat the emitted file (no semicolons added, double quotes kept), do NOT add a `dark:`
    utility, do NOT add `alert-dialog`, and do NOT touch `src/lib/design/selector-contract.ts` (Delta-18:
    this phase declares zero new selector ids).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/design/focus-recipe.test.ts tests/design/dark-scope.test.ts --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/slider.tsx` exists and exports `Slider`.
    - `grep -c "bg-white\|bg-black\|text-black" src/components/ui/slider.tsx` returns `0`.
    - `grep -c "ring-ring/" src/components/ui/slider.tsx` returns `0`.
    - `grep -c "after:-inset-4" src/components/ui/slider.tsx` returns `1`; `grep -c "after:-inset-2"` returns `0`.
    - `grep -c "focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background" src/components/ui/slider.tsx` returns `1`.
    - `grep -c "dark:" src/components/ui/slider.tsx` returns `0`, so `tests/design/dark-scope.test.ts`'s pins (13 files / 44 occurrences) do not move.
    - `git show --stat HEAD -- package.json package-lock.json` prints no file rows for this plan's commit.
    - `node -p "Object.keys(require('./node_modules/radix-ui/package.json').dependencies).filter(d=>d.includes('slider'))"` prints `[ '@radix-ui/react-slider' ]`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The slider primitive is present, carries zero leak-gate findings, zero half-alpha rings and a 44px thumb hit area, and added no npm dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Move the vendored-primitive census 31 -> 32, watched red first</name>
  <files>tests/design/leak.test.ts</files>
  <read_first>
    - `tests/design/leak.test.ts` lines `:319-341` — the `it()` title, the length assertion and the `collapsible` precedent at `:339`
    - `16-PATTERNS.md` § M10 — the three-part move and the reason comment shape
    - commit `26ddcf8`'s message (via `git log -1 --format=%B 26ddcf8`) — the four landing conditions this repo states for a vendored block
  </read_first>
  <action>
    FIRST run the leak suite and OBSERVE it red on the length assertion (31 vs the 32 files now on disk).
    Paste the observed failure line into the plan's SUMMARY — a count moved without its red watched is a
    number, not a gate.

    Then make the same three-part move `collapsible` got:
    (a) the `it()` title `visits all 31 vendored primitives` -> `visits all 32 vendored primitives`;
    (b) `expect(VENDORED_PRIMITIVES).toHaveLength(31)` -> `toHaveLength(32)`;
    (c) a new `expect(VENDORED_PRIMITIVES).toContain("src/components/ui/slider.tsx")` beside it, with its
        own reason comment in the file's voice: a count is satisfied by any thirty-second file, and the
        claim is that THIS one is inside the scanned set — a primitive that leaks nothing is
        indistinguishable, to a violations list, from one the walker never opened.

    Do not touch the `toEqual([])` violations assertion. Do not add the file to any other inventory:
    `selector-contract.ts` is untouched (Delta-18) and `accent-uses.ts` stays at ten (Delta-9).
  </action>
  <verify>
    <automated>npx vitest run tests/design/leak.test.ts --config vitest.design.config.ts && npm run test:design</automated>
  </verify>
  <acceptance_criteria>
    - The observed RED output (the `toHaveLength(31)` failure naming 32 received) is transcribed verbatim in the plan SUMMARY.
    - `grep -c "toHaveLength(32)" tests/design/leak.test.ts` returns `1`.
    - `grep -c 'toContain("src/components/ui/slider.tsx")' tests/design/leak.test.ts` returns `1`.
    - `grep -c "visits all 32 vendored primitives" tests/design/leak.test.ts` returns `1`.
    - `ls src/components/ui | wc -l` returns `32`.
    - `npm run test:design` exits 0.
    - Task 1's file edit and this inventory move are in ONE commit (`git show --stat HEAD` lists both `src/components/ui/slider.tsx` and `tests/design/leak.test.ts`).
  </acceptance_criteria>
  <done>The census reads 32, names the slider, and was watched failing at 31 before it moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry -> repo | third-party code enters the tree from `ui.shadcn.com` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `npx shadcn add slider` | mitigate | Official shadcn registry only; `components.json` `registries` stays `{}`; the emitted file is read in full before commit and its four Tailwind values are dispositioned explicitly (RESEARCH §D14) |
| T-16-02 | Tampering | npm dependency graph | mitigate | `git show --stat HEAD -- package.json package-lock.json` must print no rows; `@radix-ui/react-slider` is already transitively installed under `radix-ui@1.4.3` (measured), so this add is provably dependency-free |
| T-16-03 | Elevation | design gates | mitigate | The census move is watched red first, so the gate is proven to fire rather than assumed to |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run test:design` exits 0
- `git show --stat HEAD -- package.json package-lock.json` prints no rows
- `ls src/components/ui | wc -l` = 32
- `ls drizzle/*.sql | tail -1` still prints `drizzle/0025_audit_resolved_by.sql` (GATE-06)
</verification>

<success_criteria>
- `src/components/ui/slider.tsx` exists, is inside the leak gate's scanned set and produces zero findings
- The thumb carries the canonical DS-05 focus recipe and a 44px hit area
- `tests/design/leak.test.ts` reads 32 and names the new file, with its red transcribed
- Zero npm dependencies added; `alert-dialog` not added
</success_criteria>

<output>
Create `.planning/phases/16-image-crop-framing/16-01-SUMMARY.md` when done
</output>
