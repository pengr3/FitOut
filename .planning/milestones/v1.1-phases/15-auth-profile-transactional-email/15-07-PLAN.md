---
phase: 15-auth-profile-transactional-email
plan: 07
type: execute
wave: 2
depends_on: [15-06]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/reset-password/page.tsx
  - tests/design/card-pattern-coverage.test.ts
  - tests/design/brand-recipe.test.ts
autonomous: true
requirements: [AUTHUI-01, AUTHUI-03]

must_haves:
  truths:
    - "Each auth document renders exactly one h1, at the heading type role, in every branch — including forgot's post-submit and reset's missing-token states."
    - "Each auth viewport carries exactly one accent-filled element — the submit button — and forgot's post-submit branch carries zero (15-CONTEXT D-162: coral on the primary action only)."
    - "No raw Card survives on any auth page; the four allow-list rows are gone and the four pages are declared panel-card adopters."
    - "Two live regions announcing content that is present at first paint are demoted; no new live role is introduced."
    - "Every shipped sentence, label, accessible name and input placeholder is byte-identical; the two auth e2e specs pass unmodified."
    - "The anti-enumeration copy and branching are untouched — the uniform forgot sentence and the one generic login refusal (GATE-NOREG #2)."
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "PanelCard titleAs=h1, brand+touch submit, demoted ResetNotice"
      contains: "PanelCard"
    - path: "src/app/(auth)/reset-password/page.tsx"
      provides: "PanelCard titleAs=h1, brand+touch submit, demoted missing-token notice"
      contains: "PanelCard"
  key_links:
    - from: "the four auth pages"
      to: "src/components/patterns/panel-card.tsx"
      via: "PanelCard title/description/titleAs=h1, no padding and no space-y at the call site"
      pattern: "titleAs=\"h1\""
    - from: "tests/design/card-pattern-coverage.test.ts"
      to: "the four auth pages"
      via: "four ALLOWED_RAW_CARD rows deleted, four CARD_SURFACES adopters added, EXPECTED_SURFACES moved"
      pattern: "EXPECTED_SURFACES"
---

<objective>
Convert the four auth pages from raw `<Card>` to `PanelCard` with the document's `<h1>`, put the one
coral on the one primary action, demote the two live regions that announce content present at first
paint, and move the two pinned inventories that those conversions justify — each in the same commit as
its rows.

Purpose: AUTHUI-01 wants the auth screens to read as the same product as the app; AUTHUI-03 wants them
to hold the same five gates. Today the four pages render `CardTitle` — a `<div>` — and **no `<h1>` at
all**. They are also the four remaining `ALLOWED_RAW_CARD` rows, each excused with a sentence that says
"Phase 15 owns this". This is that phase.

Output: four converted pages and two inventory files whose numbers moved with the rows that justify
them, never ahead of them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-06-SUMMARY.md

<interfaces>
`PanelCard` (widened by plan 15-06):
  { title?, titleAs?: "h1" | "h2" | "h3" (default "h2"), description?, footer?, sticky?, tone?, children? }
  Its own CardContent already carries `space-y-4 p-4 sm:p-6`.
  ⚠ ADD NO PADDING AND NO `space-y-4` AT THE CALL SITE. `cancel-page-shell.test.tsx:222-227`'s failure
  message states the rule: this tree's Card puts block padding on Card itself, so a child that asks
  again pays it twice.

Per-screen h1 / description, all shipped copy, byte-for-byte (15-UI-SPEC § One composition, four screens):
  login   h1 "Welcome back"                  desc "Log in to your FitOut account."
  signup  h1 "Create your FitOut account"    desc "Book a space or list one of your own."
  forgot  h1 "Reset your password"           desc "Enter your email and we'll send you a reset link."
  reset   h1 "Set a new password"            desc "Choose a strong password you don't reuse."

Primary actions, all shipped labels, now `variant="brand" size="touch"` and full-width:
  "Log in" · "Sign up to book" / "Sign up to host" · "Send reset link" · "Set new password"
In-flight labels shipped and unchanged: "Logging in…", "Creating account…", "Sending…", "Updating…"

Inventories this plan moves (measured 2026-08-24 against `dev` HEAD):
  tests/design/card-pattern-coverage.test.ts
    ALLOWED_RAW_CARD  — a Record<string,string> at :445; the four auth rows are at :447/:449/:451/:453,
                        under a `// ── Phase 15 …` banner. 9 rows → 5.
    CARD_SURFACES     — the declared adopter array at :205. +4 rows.
    EXPECTED_SURFACES — a PLAIN CONST at :431, currently `16`. 16 → 18 → 20 across this plan's two tasks.
                        (It reaches 21 in plan 15-08 when `profile-form.tsx` joins.)
  tests/design/brand-recipe.test.ts
    repo-wide adoption total — `expect(total).toBe(24)` at :817. 24 → 26 → 28 across the two tasks.
    scoped map total          — `expect(total).toBe(19)` at :664. BYTE-IDENTICAL, do not touch.
    EXPECTED_SURVIVING_ACCENT_LINES at :143 — BYTE-IDENTICAL, do not touch.
    `ADOPTION_TREES` is `["src/app/", "src/components/"]`, so `(auth)` pages are inside it and outside
    the scoped trees — which is exactly why the repo-wide number moves and the scoped one does not.

Copy that a gate has already declared and that must NOT move:
  `tests/design/site-contacts.test.ts` declares three rows keyed `file — you@example.com` for the
  login, signup and forgot pages, each excused as "the `placeholder` on the email INPUT". Keep all
  three placeholders byte-identical. A stale declaration is asserted red.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login and forgot-password adopt the pattern</name>

  <read_first>
    - src/app/(auth)/login/page.tsx (the whole 205-line file — the `ResetNotice` at :50-61 with its D-14/D-15 comment above it, the Card block at :108-114, the `Forgot password?` link at :148, the submit at :172-178, the "or" divider at :182)
    - src/app/(auth)/forgot-password/page.tsx (the whole 114-line file — especially the post-submit branch that replaces the form with the uniform sentence)
    - src/components/patterns/panel-card.tsx lines 185-205 (what the pattern already renders, so you add nothing it supplies)
    - src/app/(app)/bookings/[id]/group/page.tsx lines 159-186 (the shipped raw-container → PanelCard swap to copy)
    - tests/design/card-pattern-coverage.test.ts lines 195-215, 425-460 (CARD_SURFACES' row shape, EXPECTED_SURFACES, the four auth ALLOWED_RAW_CARD rows and the Phase-13 block that shows how a deletion is left behind as an explanatory comment)
    - tests/design/brand-recipe.test.ts lines 110-150 and 800-845 (ADOPTION_TREES, EXPECTED_SURVIVING_ACCENT_LINES, the repo-wide total assertion)
    - e2e/password-reset.spec.ts (the accessible names and sentences these two pages must keep)
  </read_first>

  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/forgot-password/page.tsx, tests/design/card-pattern-coverage.test.ts, tests/design/brand-recipe.test.ts</files>

  <action>
    **login/page.tsx.** Replace the `Card` / `CardHeader` / `CardTitle` / `CardDescription` /
    `CardContent` block with a single `PanelCard` carrying `title="Welcome back"`, `titleAs="h1"` and
    `description="Log in to your FitOut account."`. Everything that was inside `CardContent` becomes
    `PanelCard`'s children, byte-for-byte in content. Delete the now-unused `card` imports. Add no
    padding class and no `space-y-4` at the call site.

    Give the submit button `variant="brand"` and `size="touch"`, keeping it full-width and keeping the
    in-flight label pattern (`{isSubmitting ? "Logging in…" : "Log in"}`) exactly as written. Change the
    `Forgot password?` link from `text-xs` to `text-sm` — 14px, matching the card's other links. Leave
    the `"or"` divider at `text-xs`: it is chrome, not a control.

    Demote `ResetNotice`: drop **only** `role="status"`. Its class list and its sentence
    (`Password updated — please sign in.`) stay byte-identical, and the D-14/D-15 comment above it
    explaining the neutral tint is preserved. The rule is Phase 14's, verbatim: a live region announces
    a CHANGE, and a freshly rendered page is not a change — it is a page. `e2e/password-reset.spec.ts`
    asserts that sentence by text, not by role, so it passes unmodified.

    Leave untouched: `safeCallbackPath` and every call to it, the `Continue with Google` outline button
    and its `callbackURL`, the generic refusal sentence and its `role="alert"`, the email input's
    `placeholder="you@example.com"`, and every accessible name.

    **forgot-password/page.tsx.** The same `PanelCard` conversion with `title="Reset your password"`,
    `titleAs="h1"`, `description="Enter your email and we'll send you a reset link."`. Submit gains
    `variant="brand" size="touch"`. The post-submit branch keeps its uniform sentence **byte-identical**
    and keeps its `role="status"`, but that region must resolve a non-empty accessible name — give it
    `aria-label="Reset request result"` (plan 15-09 declares this row; the markup lands here). The
    post-submit branch replaces the form, so that branch renders **zero** accent-filled elements —
    verify that is what the code actually does rather than assuming it. Keep the
    `placeholder="you@example.com"` byte-identical.

    ⚠ **GATE-NOREG #2.** The uniform sentence is shown regardless of whether the account exists. Do not
    introduce any branch on account existence, do not "improve" the sentence, and do not add a second
    state that is reachable only in one of the two cases.

    **The inventories, in this same commit.** Delete the `login` and `forgot-password` rows from
    `ALLOWED_RAW_CARD` (9 → 7), leaving behind an explanatory comment in the Phase-13 block's style
    naming which rows left and why. Add both pages to `CARD_SURFACES` as `panel-card` adopters with a
    real `why` sentence each. Move `EXPECTED_SURFACES` 16 → 18. Move `brand-recipe.test.ts`'s repo-wide
    total 24 → 26 and append the reason to that test's running changelog. **Observe the red first** —
    run each gate with the rows added and the number not yet moved, and quote the failure verbatim in
    the changelog. Never move a number ahead of its rows. Do not touch the scoped 19 or
    `EXPECTED_SURVIVING_ACCENT_LINES`.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design &amp;&amp; npx playwright test e2e/password-reset.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'PanelCard' 'src/app/(auth)/login/page.tsx'` returns at least 2 (import + call site) and `grep -c '<Card' 'src/app/(auth)/login/page.tsx'` returns 0
    - Same two conditions hold for `src/app/(auth)/forgot-password/page.tsx`
    - `grep -c 'titleAs="h1"' 'src/app/(auth)/login/page.tsx'` returns 1 and likewise for forgot-password
    - `grep -c 'variant="brand"' 'src/app/(auth)/login/page.tsx'` returns 1, and the same call carries `size="touch"`
    - `grep -c 'role="status"' 'src/app/(auth)/login/page.tsx'` returns 0
    - `grep -c 'Password updated — please sign in.' 'src/app/(auth)/login/page.tsx'` returns 1
    - `grep -c 'aria-label="Reset request result"' 'src/app/(auth)/forgot-password/page.tsx'` returns 1
    - `grep -c 'you@example.com' 'src/app/(auth)/login/page.tsx' 'src/app/(auth)/forgot-password/page.tsx'` returns 1 for each file
    - `grep -c 'text-xs' 'src/app/(auth)/login/page.tsx'` returns 1 — only the "or" divider keeps it
    - `tests/design/card-pattern-coverage.test.ts` has `EXPECTED_SURFACES = 18` and `ALLOWED_RAW_CARD` has 7 rows
    - `tests/design/brand-recipe.test.ts` asserts the repo-wide total as 26; `grep -c 'toBe(19)' tests/design/brand-recipe.test.ts` returns 1 and `EXPECTED_SURVIVING_ACCENT_LINES` is unchanged in `git diff`
    - Both tests' changelogs quote the observed red verbatim
    - `npm run test:design` exits 0
    - `npx playwright test e2e/password-reset.spec.ts` exits 0 with the spec unedited
  </acceptance_criteria>

  <done>Two of four auth cards are the pattern, carry the document's h1 and one coral, and both inventories moved with their rows after their red was watched.</done>
</task>

<task type="auto">
  <name>Task 2: Signup and reset-password adopt the pattern</name>

  <read_first>
    - src/app/(auth)/signup/page.tsx (the whole 220-line file — the intent radio pair and its shipped token recipe, the four fields, the two submit labels)
    - src/app/(auth)/reset-password/page.tsx (the whole 146-line file — the hidden token input, the Suspense fallback line, the missing-token notice and its `role="alert"`, the `/login?reset=1` redirect)
    - src/app/(auth)/login/page.tsx (as converted in Task 1 — the shape to match, so the four screens really are one composition)
    - tests/design/card-pattern-coverage.test.ts and tests/design/brand-recipe.test.ts (as edited in Task 1)
    - e2e/login-persistence.spec.ts (the signup flow's accessible names — `First name`, `Book a space`, `/sign up to book/i`)
  </read_first>

  <files>src/app/(auth)/signup/page.tsx, src/app/(auth)/reset-password/page.tsx, tests/design/card-pattern-coverage.test.ts, tests/design/brand-recipe.test.ts</files>

  <action>
    **signup/page.tsx.** The same `PanelCard` conversion: `title="Create your FitOut account"`,
    `titleAs="h1"`, `description="Book a space or list one of your own."`. Submit gains
    `variant="brand" size="touch"` and keeps both shipped labels (`Sign up to book` / `Sign up to host`)
    and the in-flight `Creating account…`.

    The intent radio pair keeps its **shipped token recipe byte-identical** — the selected/idle class
    pairs are declared pairings and the D-21 control fill, deliberately not the accent. Do not restyle
    it, do not give it a variant, do not make it coral. Measure its 320px fit (15-UI-SPEC § Measurements
    Owed M1): each button is roughly 136px wide inside the 288px card content box; if `Book a space` /
    `Host a space` wrap at 14px, the fix is stacking the pair below the `sm:` breakpoint, **never**
    shrinking the type. Record the measurement in the summary.

    ⚠ **GATE-NOREG #5.** `intent` still posts to the `signup` server action; `canBook` and `canHost`
    stay server-assigned. Do not move that mapping into the client, and do not add a client-side default.

    Keep the `placeholder="you@example.com"` byte-identical.

    **reset-password/page.tsx.** The same conversion: `title="Set a new password"`, `titleAs="h1"`,
    `description="Choose a strong password you don't reuse."`. Submit gains `variant="brand"
    size="touch"` and keeps `Set new password` / `Updating…`.

    Demote the missing-token notice: drop **only** `role="alert"`. It is present at first paint, which
    is the same defect shape as `ResetNotice`. It stays a plain paragraph in `text-destructive` with its
    shipped sentence and its `Request a new link` route out, byte-identical. The submit-refusal line
    keeps its `role="alert"` — that one announces a change.

    Keep the shipped Suspense fallback line (`text-sm text-muted-foreground`) as the token-read loading
    state. Verify — do not assume — that the hidden token input is `type="hidden"` and therefore not
    tabbable.

    ⚠ **GATE-NOREG #4.** The hidden token input, `resetSchema`, the `newPassword` mapping and the
    `/login?reset=1` redirect are unchanged, and `revokeSessionsOnPasswordReset` is not touched.

    **The inventories, in this same commit.** Delete the `signup` and `reset-password` rows from
    `ALLOWED_RAW_CARD` (7 → 5). Add both pages to `CARD_SURFACES`. Move `EXPECTED_SURFACES` 18 → 20 and
    `brand-recipe.test.ts`'s repo-wide total 26 → 28, each with the red observed first and quoted
    verbatim in the owning test's changelog. `EXPECTED_SURFACES` reaches 21 in plan 15-08 when
    `profile-form.tsx` joins — leave it at 20 here, because moving it ahead of that row is exactly the
    thing this repository's inventory procedure forbids.

    Finally, verify the "one composition" claim across all four converted pages: same layout file, same
    ground, same column width, same wordmark, same card container, same `<h1>` type role, exactly one
    coral, the same in-flight submit-label pattern, field labels above inputs, refusal line between the
    fields and the submit.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design &amp;&amp; npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '<Card' 'src/app/(auth)/signup/page.tsx' 'src/app/(auth)/reset-password/page.tsx'` returns 0 for each
    - `grep -c 'titleAs="h1"' 'src/app/(auth)/signup/page.tsx'` returns 1 and likewise for reset-password
    - `grep -c 'variant="brand"' 'src/app/(auth)/signup/page.tsx'` returns 1 and likewise for reset-password; each carries `size="touch"`
    - `grep -c 'role="alert"' 'src/app/(auth)/reset-password/page.tsx'` returns 1 — the submit refusal keeps it, the missing-token notice does not
    - `grep -c 'reset=1' 'src/app/(auth)/reset-password/page.tsx'` is unchanged from its pre-task value
    - `grep -c 'type="hidden"' 'src/app/(auth)/reset-password/page.tsx'` returns 1
    - `grep -c 'you@example.com' 'src/app/(auth)/signup/page.tsx'` returns 1
    - `grep -c 'border-primary bg-primary' 'src/app/(auth)/signup/page.tsx'` is unchanged — the intent pair's recipe is byte-identical
    - `tests/design/card-pattern-coverage.test.ts` has `EXPECTED_SURFACES = 20` and `ALLOWED_RAW_CARD` has 5 rows
    - `tests/design/brand-recipe.test.ts` asserts the repo-wide total as 28; `grep -c 'toBe(19)' tests/design/brand-recipe.test.ts` returns 1
    - `git diff --exit-code e2e/password-reset.spec.ts e2e/login-persistence.spec.ts` exits 0
    - `npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts` exits 0
    - `npm run test:design` exits 0
    - The summary records the M1 measurement (the signup intent pair at 320px) as an observation, not a prediction
  </acceptance_criteria>

  <done>All four auth cards are the pattern; the allow-list is down to five rows; both pinned totals moved with their rows; the two e2e specs pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated submitter → the auth server actions | The pages are the untrusted-input entry to login, signup and reset |
| a reset link's `token` → the reset form | Token-bearing, single-use, and the reason the hidden input's shape matters |
| a `callbackURL` / `redirect` parameter → navigation | The open-redirect surface `safeCallbackPath` guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-22 | Information disclosure | Account enumeration via a differential auth response | mitigate | The uniform forgot sentence and the one generic login refusal stay byte-identical; no state redesign introduces a branch on account existence (GATE-NOREG #2). Asserted by the two e2e specs passing unmodified |
| T-15-23 | Tampering | Open redirect via a reset callback | mitigate | `safeCallbackPath` and its call sites are not edited; the module and its tests are untouched (GATE-NOREG #3) |
| T-15-24 | Elevation of privilege | Client-side capability assignment on signup | mitigate | `intent` still posts to the `signup` server action; `canBook`/`canHost` stay server-assigned (GATE-NOREG #5) |
| T-15-25 | Information disclosure | Reset token exposed by a tabbable or visible input | mitigate | The hidden token input is verified `type="hidden"` rather than assumed; `resetSchema` and the `newPassword` mapping unchanged (GATE-NOREG #4) |
| T-15-26 | Repudiation | A screen-reader user missing a refusal, or hearing a page as a change | mitigate | Two demotions of first-paint content; the one genuine status region gains a non-empty accessible name; the two `role="alert"` refusals keep theirs |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:design` green — `card-pattern-coverage`, `brand-recipe`, `type-scale`, `contrast`,
  `focus-recipe`, `elevation-z`, `accent-uses` and `site-contacts` all pass.
- `npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts` green, both unedited.
- `git diff --exit-code src/lib/design/accent-uses.ts src/lib/design/contrast-pairs.ts src/lib/design/status-tones.ts src/lib/design/selector-contract.ts` exits 0 — no new accent kind, pairing, tone or testid.
- `grep -c 'variant="destructive"' 'src/app/(auth)/'*/page.tsx` returns 0 for every file.
- `git diff --name-only` at plan end lists only the six files in `files_modified`.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never concurrently.
</verification>

<success_criteria>
- Four `PanelCard` call sites, four `h1`s, four corals, zero raw Cards.
- `ALLOWED_RAW_CARD` 9 → 5 with the deletions explained where the rows were.
- `EXPECTED_SURFACES` 16 → 20 and the repo-wide brand total 24 → 28, each moved in the commit that
  justified it, each red observed and quoted.
- The scoped brand map (19), `EXPECTED_SURVIVING_ACCENT_LINES` (8) and `ACCENT_USES` (10) are untouched.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-07-SUMMARY.md` when done.
</output>
