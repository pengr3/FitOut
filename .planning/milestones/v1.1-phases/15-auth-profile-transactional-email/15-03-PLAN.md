---
phase: 15-auth-profile-transactional-email
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - src/lib/email.ts
autonomous: true
requirements: [EMAIL-01]

must_haves:
  truths:
    - "The private send() carries both an HTML part and a plain-text part to Resend."
    - "Eighteen of the nineteen exported senders compose renderEmail; none builds bare markup."
    - "Subjects, recipients, argument lists, parameter names and call sites are byte-identical — not a single send trigger moved."
    - "The WR-02 dev-fallback guard's statements are unchanged: production-without-key still fails loudly without logging the token-bearing link."
    - "The sandbox sender literal stays the single occurrence in src/ and stays reachable by one env change (15-CONTEXT D-160)."
    - "The verification and reset emails gain a CTA label and nothing else — no expiry line, no 'if you didn't request this', no landing change (AUTHFB-01/02 stay in the backlog)."
  artifacts:
    - path: "src/lib/email.ts"
      provides: "19 senders composing one shell behind an unchanged send() signature contract"
      contains: "renderEmail"
  key_links:
    - from: "src/lib/email.ts"
      to: "src/lib/email-shell.ts"
      via: "renderEmail(content) composed by every sender"
      pattern: "renderEmail\\("
    - from: "src/lib/email.ts send()"
      to: "resend.emails.send"
      via: "the options object gains text alongside html"
      pattern: "resend\\.emails\\.send\\(\\{[^}]*text"
---

<objective>
Convert the transport and eighteen of the nineteen exported senders in `src/lib/email.ts` to compose
the one shell. The nineteenth — the ops digest — is plan 15-04's, because its two contracts (the
`meta`-absent type and the pre-escaped table slot) need their own reasoning.

Purpose: EMAIL-01 says all existing sends render through ONE shared branded shell **without any send
trigger moving**. The proof of the second half is that `tests/booking/notify-emission.test.ts`'s
trigger-graph assertions and the auth flow tests pass unmodified — so this plan's discipline is
subtractive: the third argument to `send()` changes shape, and nothing else in the file does.

Output: an `email.ts` whose per-field `escapeHtml` calls have moved to the choke point, whose bodies are
`EmailContent` values, and whose signatures a caller cannot tell apart from yesterday's.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-01-SUMMARY.md

<interfaces>
From `src/lib/email-shell.ts` (plan 15-01):
  export type EmailContent = {
    preheader?: string; heading: string; paragraphs: string[];
    cta?: { label: string; href: string };
    tableHtml?: string; tableText?: string;
  }
  export function escapeHtml(s: string): string
  export function renderEmail(content: EmailContent, theme?: ThemeName): { html: string; text: string }

The shipped `send()` this plan edits (`src/lib/email.ts:38-56`):
  async function send(to: string, subject: string, html: string)
  → becomes: async function send(to: string, subject: string, html: string, text: string)
  The `if (!resend)` block's statements stay byte-identical (it still logs `html`).
  The transport call gains `text` in its options object.

The 18 senders this plan converts, with the heading and CTA label 15-UI-SPEC pins for each:
   1 sendVerificationEmail          "Verify your email"                  / "Verify email"     (label change)
   2 sendResetPassword              "Reset your password"                / "Reset password"   (label change)
   3 sendBookingConfirmed           "Booking confirmed"                  / "View your booking"
   4 sendRequestReceived            "Request sent"                       / "View your request"
   5 sendRequestApproved            "Your request was approved"          / "Pay now"
   6 sendRequestDeclined            "This request expired" | "This request wasn't available" / "Find another space"
   7 sendNewRequestToHost           "New booking request"                / "Review request"
   8 sendBookingCancelledByBooker   "A booking was cancelled"            / "View the booking"
   9 sendBookingCancelledByHost     "The host cancelled this booking"    / "View the booking"
  10 sendHostCancellationRecord     "You cancelled this booking"         / "View your bookings"
  11 sendRefundIssued               "Your refund is on its way"          / "View the booking"
  12 sendReminderPreExpiry          "Your approved booking is waiting"   / "Pay now"
  13 sendReminderPreSession         "Your session is coming up"          / "View the booking"
  14 sendReminderPreSla             "A request is waiting for your answer" / "Review request"
  15 sendGroupRsvpReceived          "New RSVP for your group booking"    / "View your group"
  16 sendGroupRsvpConfirmed         "You're confirmed for the group"     / "View the details"
  17 sendGroupCancelled             "This group booking was cancelled"   / "View the details"
  18 sendGuestRsvpEmail             "Your RSVP is confirmed" | "This group booking was cancelled" / "View the details"  (2 send() calls)

  Row 19 `sendOpsAlertDigest` is NOT in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The transport gains a plain-text part, and the two auth sends adopt the shell</name>

  <read_first>
    - src/lib/email.ts lines 1-70 (the header contract, `send()` at :38-56 including the WR-02 comment block, the two auth senders at :58-66)
    - src/lib/email-shell.ts (what `renderEmail` returns and which fields it escapes)
    - tests/auth/email-escaping.test.ts (the whole file — it must pass with ZERO edits; read it so you can predict the assertions rather than discover them)
    - tests/auth/email-dev-fallback.test.ts (the WR-02 guard's own test)
    - tests/helpers/mocks.ts (the `CapturedEmail` type already stores `text`; the `extractLink` first-URL regex)
    - .planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md § Pitfall 1 and § "The escaping test that must pass with zero edits"
  </read_first>

  <files>src/lib/email.ts</files>

  <action>
    Add a fourth parameter `text: string` to the private `send`. Inside it, the `if (!resend)` block's
    statements stay **byte-identical** — the production branch still calls `console.error` with the same
    sentence and returns before logging anything, the dev branch still logs the `[email:dev] to=` line
    followed by the `html` body, and the entire WR-02 comment block above them survives verbatim. This
    is asserted at AST level by 15-UI-SPEC AC#17, so treat the block as read-only. The only other edit
    in the function is adding `text` to the options object passed to `resend.emails.send` alongside
    `from`, `to`, `subject` and `html`. That combination is type-valid: `CreateEmailOptions`'s
    `RequireAtLeastOne` union member that requires `html` leaves `text` present and optional; the
    `text?: never` visible on a casual grep belongs to the stored-template branch this repo does not use.

    Import `renderEmail` from `@/lib/email-shell` alongside the `escapeHtml` import plan 15-01 added.

    Convert `sendVerificationEmail`. Its signature `(to: string, url: string)` and its subject
    `"Verify your FitOut email"` are byte-identical. Its body becomes a `renderEmail` call with heading
    `Verify your email`, **`paragraphs: []`**, and a CTA whose label is `Verify email` and whose href is
    the **raw** `url` — the shell escapes it. Destructure the returned `html` and `text` and pass both
    to `send`. Delete the local `escapeHtml(url)` line; escaping is now structural.

    `paragraphs: []` is deliberate and is the AUTHFB-01 boundary in code. The shipped body is
    `Verify: <a href="...">...</a>` — a raw URL as the visible label, which a shell button cannot
    carry. Replacing it with a labelled button is the phase's one email copy change. Adding a sentence
    of explanation, an expiry line, or an "if you didn't request this" line would be AUTHFB-01, which
    stays in the backlog by standing choice. If the shell makes that work look near-free, propose a
    roadmap amendment in the summary — do not absorb it.

    Convert `sendResetPassword` identically: signature and subject `"Reset your FitOut password"`
    unchanged, heading `Reset your password`, `paragraphs: []`, CTA label `Reset password`, href the raw
    `url`.

    Before committing, run the three auth tests that depend on the first-URL property
    (`login-reachable-after-reset`, `reset-revokes-sessions`, `stale-session-selfheal`) and confirm they
    pass. If any fails with a token or session error while `email-escaping.test.ts` stays green, the
    shell is emitting an absolute URL before the CTA href — fix the shell's construction, not the test
    helper. Hardening `tests/helpers/mocks.ts`'s `extractLink` is a recorded fallback, not the default:
    it repairs the probe rather than the property.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/auth/ &amp;&amp; git diff --exit-code tests/auth/email-escaping.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE 'async function send\(' src/lib/email.ts` shows four parameters including `text: string`
    - `grep -c 'resend.emails.send' src/lib/email.ts` returns 1 and that call's options object contains `text`
    - `grep -c 'RESEND_API_KEY missing in production' src/lib/email.ts` returns 1 — the WR-02 sentence survives
    - `grep -c 'email:dev' src/lib/email.ts` returns 1 — the dev-fallback log line survives
    - `grep -c 'onboarding@resend.dev' src/lib/email.ts` returns 1 — the sandbox sender is still singular and still the `EMAIL_FROM` fallback
    - `grep -c 'Verify your FitOut email' src/lib/email.ts` returns 1 and `grep -c 'Reset your FitOut password' src/lib/email.ts` returns 1 — both subjects byte-identical
    - `git diff --exit-code tests/auth/email-escaping.test.ts` exits 0
    - `npx vitest run tests/auth/` exits 0 — including `login-reachable-after-reset`, `reset-revokes-sessions` and `stale-session-selfheal`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Resend receives both parts, the two auth sends wear the shell, the WR-02 guard is untouched, and the three tests that extract a reset token from the first URL in the body still find the right one.</done>
</task>

<task type="auto">
  <name>Task 2: The eight booking and request sends adopt the shell (rows 3-10)</name>

  <read_first>
    - src/lib/email.ts lines 95-360 (the eight senders in this task, each with its docblock)
    - src/lib/email-shell.ts (which fields the renderer escapes, so you can delete the right lines)
    - .planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md § `src/lib/email.ts` — the adopter shape that must survive the conversion
    - tests/booking/notify-emission.test.ts (the trigger-graph assertions that must pass unmodified — read them before touching a sender)
  </read_first>

  <files>src/lib/email.ts</files>

  <action>
    Convert rows 3-10 — `sendBookingConfirmed`, `sendRequestReceived`, `sendRequestApproved`,
    `sendRequestDeclined`, `sendNewRequestToHost`, `sendBookingCancelledByBooker`,
    `sendBookingCancelledByHost`, `sendHostCancellationRecord` — using the heading and CTA label this
    plan's `<interfaces>` block pins for each. Every one of these is the same diff, mechanically.

    For each sender: delete the per-field `escapeHtml(...)` locals; build an `EmailContent` whose
    `heading` is the string that is today inside `<p><strong>…</strong></p>`, whose `paragraphs` are the
    remaining body sentences **byte-identical** with the **raw** (unescaped) interpolations, and whose
    `cta` is the shipped anchor's label and its **raw** href; call `renderEmail`; pass the returned
    `html` and `text` to `send`.

    What must NOT move, in any of the eight: the exported `const` arrow form, the function name, the
    parameter list and every parameter name, the `to` argument, and the subject template literal —
    which today interpolates the **raw** value (for example the raw space title, not the escaped local).
    Keep that. Deleting the escaped local without noticing the subject used the raw one is the mistake
    this paragraph exists to prevent.

    `sendRequestDeclined` branches its heading between the expiry and unavailability wordings; keep both
    branches and both sentences byte-identical, and let the branch choose the `heading` rather than
    building two whole markup strings. `sendRequestDeclined`'s CTA href is the one composed from
    `APP_URL` rather than a caller-supplied link — that constant and its fallback stay exactly as they
    are.

    Every docblock above these senders stays. Several carry contracts (the CR-02 no-deadline rule, the
    D-96/D-99 pre-composed-label rule) that this conversion does not touch and must not appear to.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/booking/ tests/notifications/ &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'renderEmail(' src/lib/email.ts` returns at least 10 (rows 1-10 converted)
    - `grep -c '<p><strong>' src/lib/email.ts` has decreased by exactly 8 from its value at the start of this task
    - Each of the eight senders' parameter lists is unchanged: `git diff src/lib/email.ts` shows no edit inside any `export const send…= (` parameter list
    - `git diff --exit-code tests/booking/notify-emission.test.ts` exits 0 — trigger assertions unmodified
    - `npx vitest run tests/booking/ tests/notifications/` exits 0
    - `npm run test:design` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Eight sends compose the shell; their subjects, recipients, arguments and triggers are byte-identical and the trigger-graph test proves it without being edited.</done>
</task>

<task type="auto">
  <name>Task 3: The refund, reminder, group and guest sends adopt the shell (rows 11-18)</name>

  <read_first>
    - src/lib/email.ts lines 340-590 (`sendRefundIssued` and its docblock, the three reminders, the three group sends, `sendGuestRsvpEmail` at :558-586)
    - src/lib/booking/refund-window.ts (read `ALL_RAILS_REFUND_WINDOW`'s literal value — the conversion depends on what characters it contains)
    - src/lib/email.ts lines 14-16 (the D-92 copy-constant import note that must survive)
    - .planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md § The 19 adopters (the `ALL_RAILS_REFUND_WINDOW` sentence stays in the same paragraph as its neighbours)
  </read_first>

  <files>src/lib/email.ts</files>

  <action>
    Convert rows 11-18 — `sendRefundIssued`, `sendReminderPreExpiry`, `sendReminderPreSession`,
    `sendReminderPreSla`, `sendGroupRsvpReceived`, `sendGroupRsvpConfirmed`, `sendGroupCancelled` and
    `sendGuestRsvpEmail` — by the same mechanical diff as Task 2, using the headings and CTA labels this
    plan's `<interfaces>` block pins.

    `sendGuestRsvpEmail` is the one row with two `send()` calls: it branches on `kind` between the
    confirmed and cancelled variants. Keep the branch, keep both subjects and both body sentences
    byte-identical, keep the `Promise<{ sent: true }>` return and the `await`s, and build two
    `EmailContent` values rather than one — the second is not a variant of the first at the string
    level and pretending otherwise would flatten copy the spec pins.

    `sendRefundIssued` needs one measurement before it is converted, and the measurement must be
    recorded in the summary. Today it interpolates `ALL_RAILS_REFUND_WINDOW` **unescaped, deliberately**,
    and its docblock explains the D-78/D-92 boundary. Under the shell that sentence becomes a
    `paragraphs` entry, which the renderer escapes. Read the constant's actual value and check whether
    it contains any of the five HTML-significant characters. If it contains none, the rendering is
    byte-identical and the conversion is safe — say so explicitly in the summary with the value quoted.
    If it contains one, stop and record the finding rather than working around it: the sentence stating
    a refund window on a money path is a disclosure contract, and a silently entity-encoded character in
    it is a change to that contract. Keep the `import { ALL_RAILS_REFUND_WINDOW }` line and its D-92
    comment exactly where they are — the import's asserted-unmoved status is part of the contract.

    After the last conversion, verify no bare markup survives: a scan of `src/lib/email.ts` outside
    `renderOpsAlertDigest` must find no `<p><strong>` string-concatenation form. Row 19 is plan 15-04's
    and its `<table>`/`<tr>` builders are expected to still be here.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/booking/ tests/notifications/ tests/auth/ tests/ops/ &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'renderEmail(' src/lib/email.ts` returns at least 18 (rows 1-18, with `sendGuestRsvpEmail` contributing two)
    - `grep -c '<p><strong>' src/lib/email.ts` returns exactly 1 — the only survivor is `renderOpsAlertDigest`'s, which plan 15-04 owns
    - `grep -c 'ALL_RAILS_REFUND_WINDOW' src/lib/email.ts` returns at least 2 (the import and the interpolation) and the D-92 comment above the import is unchanged
    - The summary quotes `ALL_RAILS_REFUND_WINDOW`'s literal value and states whether it contains an HTML-significant character
    - `grep -c 'sent: true' src/lib/email.ts` returns 1 — `sendGuestRsvpEmail`'s return contract is unchanged
    - `git diff --exit-code tests/booking/notify-emission.test.ts tests/auth/email-escaping.test.ts` exits 0
    - `npx vitest run tests/booking/ tests/notifications/ tests/auth/ tests/ops/` exits 0
    - `npm run test:design` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Eighteen of nineteen sends compose one shell. The only bare markup left in the file is the ops digest's, and the money-path refund sentence was measured rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → sender arguments | Listing titles, booker labels, references and URLs all originate outside this module and are untrusted for HTML purposes |
| `send()` → Resend | The one external egress; content that enters it is forwarded, archived and indexed |
| environment → `send()`'s fallback branch | A missing `RESEND_API_KEY` in production must fail loudly without writing the token-bearing link to logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Nineteen scattered escape sites reduced to one | mitigate | Per-field `escapeHtml` deleted only where the choke point now covers it; `tests/auth/email-escaping.test.ts` passes with zero edits as the proof |
| T-15-04 | Information disclosure | Reset token extracted from the wrong URL | mitigate | Task 1 runs `login-reachable-after-reset`, `reset-revokes-sessions` and `stale-session-selfheal` explicitly, and names the diagnostic signature of the failure |
| T-15-09 | Information disclosure | Credential leakage into production logs (WR-02) | mitigate | The `if (!resend)` block is treated as read-only; its statements and comment survive verbatim; `email-dev-fallback.test.ts` passes unedited |
| T-15-10 | Repudiation | A send trigger moving under cover of a large refactor | mitigate | No consumer module is edited; `notify-emission.test.ts`'s trigger-graph assertions pass unmodified and are asserted byte-unchanged by `git diff --exit-code` |
| T-15-11 | Information disclosure | A money-path disclosure sentence changing silently | mitigate | `ALL_RAILS_REFUND_WINDOW`'s value is measured against the five escaped characters and the result recorded, rather than assumed identical |
| T-15-12 | Spoofing | Sender identity hard-coded beyond an env change's reach | mitigate | The sandbox literal stays the single occurrence in `src/` and stays the `EMAIL_FROM` fallback (15-CONTEXT D-160) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/auth/ tests/booking/ tests/notifications/ tests/ops/` green (Docker up).
- `npm run test:design` green.
- `git diff --exit-code tests/auth/email-escaping.test.ts tests/auth/email-dev-fallback.test.ts tests/booking/notify-emission.test.ts` exits 0.
- `git diff --name-only` at plan end lists only `src/lib/email.ts`.
- No consumer module (`src/lib/auth.ts`, `src/inngest/functions/notify.ts`, `guest-email.ts`,
  `ops-alert-digest.ts`) appears in the diff.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never concurrently.
</verification>

<success_criteria>
- Eighteen senders compose `renderEmail`; one bare-markup builder survives and it belongs to plan 15-04.
- Zero consumer modules edited; zero triggers, subjects, recipients or argument lists moved.
- The WR-02 guard and the escaping test are provably untouched.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-03-SUMMARY.md` when done.
</output>
