---
phase: 15-auth-profile-transactional-email
plan: 11
type: execute
wave: 5
depends_on: [15-09, 15-10]
files_modified:
  - src/lib/design/visual-baselines.ts
autonomous: false
requirements: [AUTHUI-03]

must_haves:
  truths:
    - "auth-login's surface row and both of its baseline rows are EDITED, not left standing — its hook selects an element the new composition does not render and its three prose claims became false."
    - "Four new surfaces and eight new rows are declared, court-only, with the reason each exists."
    - "The baseline count alias moved 66 → 74 in the same commit as the rows, with the compile red observed first and quoted verbatim."
    - "auth-login stays one of the four theme-contract surfaces, stated deliberately with the reason, rather than discovered."
    - "Regeneration is an operator step: baselines.yml is workflow_dispatch-only and the Playwright visual project is not constructed off Linux — the plan routes it to CI rather than pretending to run it."
    - "Any row that cannot be shot in-phase is declared blocked with what it needs — an inventory somebody can work from, never coverage."
  artifacts:
    - path: "src/lib/design/visual-baselines.ts"
      provides: "SURFACE_IDS 37 → 41, VISUAL_BASELINES 66 → 74, auth-login rewritten, alias renamed"
      contains: "BaselineCountIsSeventyFour"
  key_links:
    - from: "src/lib/design/visual-baselines.ts auth-login row"
      to: "the auth composition"
      via: "hook moves from the departed site-header to the panel-card the card now renders"
      pattern: "panel-card"
    - from: "src/lib/design/visual-baselines.ts"
      to: ".github/workflows/baselines.yml"
      via: "a workflow_dispatch run in the pinned Linux image is the only thing that may write a PNG"
      pattern: "blocked"
---

<objective>
Bring the visual-baseline inventory in line with the composition this phase built, and route the actual
capture to the only thing in the repository allowed to do it.

Purpose: this is an **edit-and-add** operation, not an add-only one, and the difference is the whole
risk. `auth-login` is **already** a declared surface with two committed PNGs, and it is **already** one
of only four `THEME_SWAP_SURFACES` — the fixed set `e2e/visual/theme-swap.spec.ts` pins by count *and*
by members. Its hook is `[data-testid="site-header"]`, and plan 15-06 removed the header from the
`(auth)` layout, so that selector now names an element the page does not render and the shot would time
out. Its `hookWhy` and both of its rows' `why` strings describe a header-led composition that no longer
exists. 15-UI-SPEC's inventory table says 37 → 42 and 66 → 76 and then correctly hedges *"measure the
file's real counts before moving the aliases"*; the measured truth is **37 → 41 and 66 → 74, plus one
edited row pair**.

Output: one inventory file edited, and one operator dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-06-SUMMARY.md

<interfaces>
Measured against `dev` HEAD 2026-08-24 — use these, not 15-UI-SPEC's prose arithmetic:

  SURFACE_IDS.length          37   (`visual-baselines.ts:151-216`; no count alias exists for it)
  VISUAL_BASELINES.length     66   (alias `BaselineCountIsSixtySix` at :1923-1925)
  `auth-login` in SURFACE_IDS YES  (row at :303-312) — its 2 baseline rows are at :1109-1123
  `auth-login` committed PNGs 2    (`auth-login-{320,1280}-court-visual-linux.png`)
  `auth-login` current hook   '[data-testid="site-header"]'
  THEME_SWAP_SURFACES         4    (`search-results`, `auth-login`, `terms`, `root-not-found`),
                                   pinned by `ThemeContractSurfaceCountIsFour` at :1964 AND by members
                                   in `e2e/visual/theme-swap.spec.ts`
  THEME_SWAP_EXCLUSIONS       1    (pinned by `ThemeSwapExclusionCountIsOne` at :1928-1930)

Target arithmetic:
  SURFACE_IDS       37 → 41   (+4: `auth-signup`, `auth-forgot`, `auth-reset`, `profile`)
  VISUAL_BASELINES  66 → 74   (+8 = 4 new surfaces × 2 widths) PLUS 2 EDITED `auth-login` rows
  alias             `BaselineCountIsSixtySix` → `BaselineCountIsSeventyFour`, 66 → 74
  THEME_SWAP_*      unchanged — state the `auth-login` decision deliberately

The five rows 15-UI-SPEC § Visual Baselines declares (court only — PROJECT D-138):
  auth-login   /login                          hook [data-testid="panel-card"]  320/1280  no determinism needed
  auth-signup  /signup                         hook [data-testid="panel-card"]  320/1280  no determinism needed
  auth-forgot  /forgot-password                hook [data-testid="panel-card"]  320/1280  no determinism needed
  auth-reset   /reset-password?token=<literal> hook [data-testid="panel-card"]  320/1280  a fixed literal token in the URL
  profile      /profile                        hook [data-testid="panel-card"]  320/1280  ⚠ fixture required, clock NOT

⚠ `e2e/helpers/visual-drive.ts`'s `DRIVES` map keys only six surfaces; anything absent falls through to
a plain `goto` with no session. `/profile` redirects an unauthenticated visitor, so a default-driven
`profile` row would photograph `/login` — permanent, silent and green, the exact failure the Phase-14
host block records. The four auth rows are anonymous and the default drive is correct for them.

⚠ `.github/workflows/baselines.yml` is `workflow_dispatch`-only and is documented as the ONLY thing in
this repository that may write a visual-regression baseline. `playwright.config.ts:39` constructs the
`visual` project ONLY on Linux, so on this Windows machine the command does not exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit two rows, add eight, move one alias</name>

  <read_first>
    - src/lib/design/visual-baselines.ts lines 140-230 (`SURFACE_IDS`, the Phase-14 banner block whose honesty convention to copy, and the `as const satisfies Record<…>` totality note)
    - src/lib/design/visual-baselines.ts lines 296-320 (the `auth-login` surface row with its hook and two-sentence `hookWhy`)
    - src/lib/design/visual-baselines.ts lines 1100-1130 (the two `auth-login` baseline rows and their `why` strings, both of which name the header)
    - src/lib/design/visual-baselines.ts lines 1900-1970 (the count alias with its OBSERVED-RED procedure docblock, `ThemeSwapExclusionCountIsOne`, and `ThemeContractSurfaceCountIsFour`)
    - src/lib/design/visual-baselines.ts lines 2020-2045 (`THEME_SWAP_SURFACES` and its four members)
    - e2e/visual/theme-swap.spec.ts (it pins the MEMBERS, not just the count — read it before deciding anything about `auth-login`)
    - e2e/helpers/visual-drive.ts lines 870-900 (the `DRIVES` map and what a missing key falls through to)
    - src/app/(auth)/layout.tsx as rewritten by plan 15-06 (so the new hook names something the page actually renders)
  </read_first>

  <files>src/lib/design/visual-baselines.ts</files>

  <action>
    **Edit `auth-login`'s surface row.** Change `hook` from the departed site-header selector to
    `[data-testid="panel-card"]`. Rewrite `hookWhy` — both of its sentences are now false. The first
    claims the header is the composition this baseline exists to pin; the composition is now the card on
    the quiet ground. The second names a real property — that the hook fails loudly if the anonymous
    session read ever starts reaching the database, which keeps this surface in the DB-free scope. Decide
    deliberately whether the new hook preserves that claim; if it does not, **the row must stop making
    it** rather than carry a claim nothing checks. Say which you did, and why, in the row's prose.

    **Edit `auth-login`'s two baseline rows.** Both `why` strings name the header. Rewrite them for the
    new composition — the wordmark above one card on the quiet ground at the 320px floor, and the same
    composition at desktop width. The row *count* for `auth-login` stays 2.

    **Add four surface ids** — `auth-signup`, `auth-forgot`, `auth-reset`, `profile` — under a Phase-15
    banner comment in the existing block style. `auth-login` is already there; adding it again is a
    compile error and adding a fifth id is the arithmetic mistake this plan exists to prevent. Copy the
    Phase-14 block's honesty convention in spirit: state at the top of the block what these rows are and
    are not, rather than leaving it to be inferred from eight `blocked` strings.

    **Add eight baseline rows** — the four new surfaces at 320 and 1280, all `theme: "court"` (PROJECT
    D-138). The three new auth surfaces need **no determinism work at all**: anonymous, static forms, no
    clock, no seed. Declare them `blocked: null`. `auth-reset`'s URL carries a fixed literal token
    string; the form does not validate it client-side beyond non-emptiness, so the literal is a fixture
    and not a real token.

    **The `profile` rows need a decision, and it must be an honest one.** They need a seeded user with a
    fixed literal `createdAt` (the member-since line is derived from `createdAt`, not from now, so the
    row is date-independent by construction — the Phase-14 `[14-16]` rule is satisfied) and no avatar, so
    the fallback initials are deterministic from the fixed name. They ALSO need a `DRIVES` entry, because
    a surface absent from that map falls through to a plain unauthenticated `goto` and `/profile`
    redirects — which would photograph the login page twice, permanently, silently and green. If the
    fixture and the drive do not ship in this phase, declare both `profile` rows **`blocked`** with a
    string naming both needs. Do not declare them `blocked: null` and hope. An inventory somebody can
    work from is the deliverable; a baseline captured against a live account is not.

    **Move the count alias.** Rename `BaselineCountIsSixtySix` to `BaselineCountIsSeventyFour` and move
    the constraint 66 → 74. **Observe the red first**: with the eight rows added and the number still at
    66, run `npx tsc --noEmit` and append the whole of stdout verbatim to the alias's own docblock,
    following the procedure that docblock already records from plans 13-15 and 14-16. Note again the
    gate's standing weakness — the error names only the alias's own line, not the eight rows — which is
    why the arithmetic is spelled out in prose beside it.

    **State the theme-contract decision deliberately.** `auth-login` **stays** one of the four
    `THEME_SWAP_SURFACES`. Its new composition — quiet ground, card surface, the foreground ink and one
    brand-filled button — is if anything a better two-theme drift subject than the header-led one it
    replaces. Write that reason into the file next to the set. Removing it would need a
    `THEME_SWAP_EXCLUSIONS` row, and `ThemeSwapExclusionCountIsOne` pins that list at one — larger and
    worse. `ThemeContractSurfaceCountIsFour` is unchanged.

    **Record the stale PNGs.** `auth-login-320-court-visual-linux.png` and
    `auth-login-1280-court-visual-linux.png` are committed and are stale from the moment plan 15-06
    landed. Do not delete them and do not attempt to regenerate them here — that is Task 2's dispatch,
    and it is the only path. Note in the block that the two are stale and awaiting the dispatch.

    Touch nothing else in the file. `THEME_SWAP_EXCLUSIONS`, `ThemeSwapExclusionCountIsOne` and every
    pre-existing row stay byte-identical.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'BaselineCountIsSeventyFour' src/lib/design/visual-baselines.ts` returns 1 and `grep -c 'BaselineCountIsSixtySix' src/lib/design/visual-baselines.ts` returns 0
    - `grep -c 'extends 74' src/lib/design/visual-baselines.ts` returns 1
    - `SURFACE_IDS` contains `auth-signup`, `auth-forgot`, `auth-reset` and `profile`, and contains `auth-login` exactly once
    - The `auth-login` surface row's `hook` is `[data-testid="panel-card"]` and its `hookWhy` names the card composition, not the header
    - Both `auth-login` baseline rows' `why` strings name the card composition, not the header, and `auth-login` still has exactly 2 rows
    - `grep -c 'ThemeContractSurfaceCountIsFour' src/lib/design/visual-baselines.ts` returns 1 and `THEME_SWAP_SURFACES` still lists `auth-login`
    - `git diff src/lib/design/visual-baselines.ts` shows no change to `THEME_SWAP_EXCLUSIONS` or `ThemeSwapExclusionCountIsOne`
    - The alias docblock quotes the observed `tsc` red verbatim, dated and attributed to this plan
    - The `profile` rows are either `blocked: null` with a shipped fixture AND a `DRIVES` entry, or `blocked` with a string naming both the seeded-user need and the drive need
    - `npx tsc --noEmit` exits 0 and `npm run test:design` exits 0
    - `git diff --name-only` lists only `src/lib/design/visual-baselines.ts`
  </acceptance_criteria>

  <done>The inventory describes the composition that actually ships, the count moved with its rows after its red was watched, and the one row that cannot be shot honestly says what it needs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Dispatch the baseline generation in CI</name>

  <read_first>
    - .github/workflows/baselines.yml (the whole file — the `workflow_dispatch` trigger and the note that this is the only thing that may write a baseline)
    - src/lib/design/visual-baselines.ts as edited in Task 1 (which rows are `blocked: null` and therefore expected to produce a PNG)
    - .planning/STATE.md § Operator Next Steps (the Phase-12 baseline dispatch note — the same procedure, including the follow-up-run trap)
  </read_first>

  <files>e2e/visual/surfaces.spec.ts-snapshots/</files>

  <action>
    Pause for the operator. Baseline regeneration genuinely cannot be done from this machine and that is
    structural rather than a shortfall: `.github/workflows/baselines.yml` is `workflow_dispatch`-only and
    is the only thing in this repository permitted to write a baseline, and `playwright.config.ts`
    constructs the `visual` Playwright project only on Linux.

    Present the `how-to-verify` steps below and wait for the resume signal. Do not attempt
    `npx playwright test --project=visual` locally — the project does not exist here and a failure to
    construct it is not evidence of anything. Do not hand-commit a screenshot taken in any other
    environment, and do not delete a stale PNG by hand.

    When the operator responds, record in the summary: the generation run's id, the FOLLOW-UP comparison
    run's id and its result, the list of PNGs added or replaced, and — for any `blocked: null` row that
    came back without a PNG — which row and what the run reported. A missing PNG for a row declared
    shootable is a finding about that row, and it belongs in the summary rather than being retried
    silently.
  </action>

  <what-built>
    `visual-baselines.ts` now declares 41 surfaces and 74 rows. `auth-login`'s hook and all three of its
    prose claims were rewritten for the composition plan 15-06 built; four surfaces and eight rows were
    added, court-only. Two committed PNGs (`auth-login-{320,1280}-court-visual-linux.png`) are stale
    because the header they pin has left the layout.
  </what-built>

  <how-to-verify>
    1. Push `dev`.
    2. Dispatch the `baselines` workflow.
    3. Confirm the resulting commit added or replaced **only** `*-visual-linux.png` files — nothing else.
    4. Confirm the two stale `auth-login` PNGs were **replaced**, not merely joined by new siblings. A
       run that added eight files and left the two old ones untouched means the `auth-login` hook edit
       did not take, and the surface is still being shot against a selector the page does not render.
    5. Confirm one PNG exists per `blocked: null` row — eight new plus the two replacements. Any row
       declared `blocked` should produce nothing, and that is correct.
    6. **Then push an empty commit or re-dispatch so a `ci` run actually COMPARES.** A `GITHUB_TOKEN`
       push triggers no workflow run, so the deliverable is the FOLLOW-UP comparison run's id, not the
       generation run's. The comparison happens in `gate-visual`.
    7. Report the comparison run's id and its result.
  </how-to-verify>

  <resume-signal>
    Paste the comparison run's id and its outcome, or describe what the dispatch produced. If any row
    came back without a PNG, say which — that is a finding about the row, not about the dispatch.
  </resume-signal>

  <verify>
    <automated>git ls-files 'e2e/visual/surfaces.spec.ts-snapshots/*-visual-linux.png' | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - The `baselines` workflow ran by dispatch and its commit touched only `*-visual-linux.png` files
    - `git ls-files 'e2e/visual/surfaces.spec.ts-snapshots/auth-*-court-visual-linux.png'` lists one file per `blocked: null` auth row at each of the two widths
    - The two `auth-login` PNGs' blob hashes differ from their pre-dispatch values — they were replaced, not left standing
    - A FOLLOW-UP `ci` run exists whose `gate-visual` job actually compared, and its id is recorded in the summary
    - Any row declared `blocked` produced no PNG, and that is stated rather than treated as a failure
  </acceptance_criteria>

  <done>The declared inventory and the committed images agree, and the agreement was proved by a comparison run rather than by a generation run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the declared baseline inventory → the committed PNGs | A row whose hook names a missing element times out; a row driven without a session photographs the login page |
| this machine → the baseline artifacts | Nothing here may write one; the pinned Linux image via `workflow_dispatch` is the only path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-34 | Repudiation | A surface silently photographed as `/login` because it has no drive entry | mitigate | The `profile` row is either given a fixture and a `DRIVES` entry or declared `blocked` with both needs named; the Phase-14 precedent for this exact failure is cited in the block |
| T-15-35 | Repudiation | A stale baseline pinning a composition that no longer ships | mitigate | `auth-login`'s hook and all three prose claims are rewritten in Task 1; Task 2's checklist requires the two PNGs to be REPLACED, verified by blob hash, not merely joined |
| T-15-36 | Repudiation | A pinned count moved ahead of the rows that justify it | mitigate | The alias red is observed first and quoted verbatim in its own docblock, following the file's recorded procedure |
| T-15-37 | Tampering | A baseline captured outside the pinned image | mitigate | Regeneration is routed through `workflow_dispatch` only; the checkpoint forbids hand-committed screenshots and hand-deleted PNGs |
| T-15-38 | Repudiation | A generation run mistaken for a comparison run | mitigate | The checkpoint requires a FOLLOW-UP `ci` run whose `gate-visual` job actually compared, and its id as the deliverable |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:design` green.
- `git diff --name-only` for Task 1 lists only `src/lib/design/visual-baselines.ts`.
- `git diff --exit-code e2e/visual/theme-swap.spec.ts` exits 0 — the theme-contract spec is not edited.
- The dispatch's follow-up comparison run id is recorded in the summary.

⚠ The Playwright `visual` project is not constructed on this machine. Nothing local will report a
baseline mismatch, in either direction. Treat green here as silence, not as evidence.
</verification>

<success_criteria>
- 41 surfaces, 74 rows, one alias renamed with its red quoted.
- `auth-login` edited rather than duplicated, and deliberately kept in the four theme-contract surfaces
  with the reason written down.
- Every `blocked: null` row has a PNG from the pinned image; every `blocked` row says what it needs.
- The deliverable is a comparison run's id, not a generation run's.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-11-SUMMARY.md` when done.
</output>
