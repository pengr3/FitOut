---
phase: 15-auth-profile-transactional-email
plan: 05
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - scripts/send-email-previews.ts
  - package.json
  - .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md
autonomous: false
requirements: [EMAIL-03]

must_haves:
  truths:
    - "One command delivers one of each of the nineteen sends to the account-owner inbox, without moving a single call site (15-CONTEXT D-160 restricts delivery to that inbox)."
    - "The harness is a harness, not a dispatcher: it lives outside src/app and reinstates no fire-and-forget call site (D-83)."
    - "A checklist artifact exists with one row per send per available client, and the PM — not the phase — states each outcome."
    - "Outlook desktop reads BLOCKED — client access, honestly, and EMAIL-03 is not ticked complete until the PM opens it or explicitly accepts the gap (15-CONTEXT D-163)."
    - "Apple Mail is walked in dark mode; the assertion is legibility under client auto-inversion, not authored colours."
  artifacts:
    - path: "scripts/send-email-previews.ts"
      provides: "dev-only batch delivery of all 19 sends from the shared fixture module"
      min_lines: 60
    - path: ".planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md"
      provides: "the PM-initialed walk record, screenshots referenced by absolute path outside the repo"
      min_lines: 50
  key_links:
    - from: "scripts/send-email-previews.ts"
      to: "tests/helpers/email-fixtures.ts"
      via: "the same 19 argument lists the injection probe reads — built once"
      pattern: "email-fixtures"
    - from: "scripts/send-email-previews.ts"
      to: "src/lib/email.ts"
      via: "direct import of the exported senders through the @/ alias"
      pattern: "from \"@/lib/email\""
---

<objective>
Produce the two things EMAIL-03 needs from the phase — a way to put one of each send into the
account-owner inbox, and a checklist honest enough that a gap in it reads as a gap — and then hand the
walk to the PM.

Purpose: EMAIL-03 is a manual UAT by construction. Real mail clients cannot be driven from CI, and
15-CONTEXT D-163 records what is available (Gmail web, Gmail Android, Apple Mail) and what is not
(Outlook desktop). The phase produces the sends and the checklist; the PM states the outcome. This is
14-UAT-LOG's convention: **a walk is discharged when the operator states the outcome, never when the
automated half is green** — the automated half is precisely the part that could not answer these
questions.

Output: `scripts/send-email-previews.ts`, one `package.json` script line, `15-UAT-EMAIL.md`, and a
blocking checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-CONTEXT.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/14-host-tooling/14-UAT-LOG.md

<interfaces>
From `tests/helpers/email-fixtures.ts` (plan 15-04): one argument list per exported sender,
totality-typed. The harness reads the same module.

From `scripts/ops-alerts.ts` lines 13-18 — the alias-import permission, already probed first-hand:
  `npx tsx` on a file importing `@/…` resolves the alias cleanly; tsx auto-discovers tsconfig's
  `paths: { "@/*": ["./src/*"] }` from the repo root under `npm run`. Do not "fix" these imports
  back to relative paths. Unlike `ops-alerts.ts`, this harness needs **no** database client.

From `package.json` scripts (the alphabetical block to extend):
  "db:seed", "db:test:setup", "db:studio", "design:tokens", "ops:alerts", "ops:alerts:resolve", …

Environment, measured 2026-08-24:
  `RESEND_API_KEY` is present and uncommented in `.env.local` — the dev server SENDS rather than logs.
  `EMAIL_FROM` is absent, so `FROM` falls back to the sandbox literal, which is what 15-CONTEXT D-160
  intends. No tunnel is needed for this walk (the PayMongo webhook tunnel plays no part).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The dev-only preview harness</name>

  <read_first>
    - scripts/ops-alerts.ts (the whole header and the CLI shape — the analog; note the alias-import note at :13-18 and that this harness skips its `postgres` connection block entirely)
    - tests/helpers/email-fixtures.ts (the 19 argument lists this script reads)
    - src/lib/email.ts lines 84-93 (the D-83 boundary the script's own header must cite)
    - package.json lines 5-25 (the scripts block, for where the new line goes)
  </read_first>

  <files>scripts/send-email-previews.ts, package.json</files>

  <action>
    Create `scripts/send-email-previews.ts`, run with `tsx`, importing the exported senders from
    `@/lib/email` and the argument lists from the shared fixture module plan 15-04 created. It touches
    no database — skip `ops-alerts.ts`'s standalone `postgres` connection block entirely.

    Behaviour: take the destination address as the first CLI argument, refuse to run without it rather
    than defaulting to a hard-coded inbox, and then call each of the nineteen senders once with its
    fixture arguments, substituting the supplied address for the recipient. Print one line per send
    naming the sender and the subject so the operator has an ordered list to match against the inbox.
    Space the calls with a small delay so provider rate limits do not silently drop a send, and print a
    final count. `sendGuestRsvpEmail` produces two messages (its two `kind` variants) — send both and
    say so in the output, so an operator counting twenty messages against nineteen senders is not
    confused.

    Write the header in `ops-alerts.ts`'s register and make three things explicit in it:
    (1) **This is a harness, not a dispatcher.** It lives outside `src/app`, moves no product call site,
    and reinstates no `void sendXxx(...)` — cite D-83 by name, the way `ops-alerts.ts` cites its own
    boundary. 15-UI-SPEC sanctions this shape explicitly.
    (2) **It sends real mail.** `RESEND_API_KEY` is already live in `.env.local`, so the first run
    delivers — and under 15-CONTEXT D-160's test mode, delivery only ever reaches the account-owner
    inbox. Say which inbox that is in operational terms, not as a hard-coded default.
    (3) **The fixtures are shared with `tests/auth/email-injection.test.ts`.** One owner, two readers;
    do not fork them.

    Add exactly one line to `package.json`'s scripts block, in the same alphabetical neighbourhood as
    the `ops:*` entries: an `email:previews` entry invoking `tsx scripts/send-email-previews.ts`. Change
    nothing else in `package.json` — the `build` chain, the two test scripts and `postinstall` stay
    byte-identical.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts['email:previews']) process.exit(1); if(!/lint.*test:design.*next build/.test(p.scripts.build)) process.exit(1);"</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/send-email-previews.ts` exists and imports from `@/lib/email` and from the shared fixture module
    - `grep -c 'postgres' scripts/send-email-previews.ts` returns 0 — no database client
    - `grep -c 'D-83' scripts/send-email-previews.ts` returns at least 1 — the boundary is cited in the header
    - `grep -cE '\bvoid send' scripts/send-email-previews.ts` returns 0
    - The script exits non-zero with a usage message when invoked with no destination argument
    - `node -e "console.log(require('./package.json').scripts['email:previews'])"` prints `tsx scripts/send-email-previews.ts`
    - `git diff package.json` shows exactly one added line
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>One command puts one of each send into a named inbox, from outside the product's dispatch graph.</done>
</task>

<task type="auto">
  <name>Task 2: The walk checklist artifact</name>

  <read_first>
    - .planning/phases/14-host-tooling/14-UAT-LOG.md (the whole file — the discharge rule at :3-5, the header block shape at :7-16, the verdict table shape at :33-38, and the convention that screenshots live OUTSIDE the repo with the absolute path quoted)
    - .planning/phases/15-auth-profile-transactional-email/15-CONTEXT.md § D-163
    - .planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md § EMAIL-03 — the real-client walk, and § Measurements Owed M3 and M4
  </read_first>

  <files>.planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md</files>

  <action>
    Create `15-UAT-EMAIL.md` in the phase directory, following `14-UAT-LOG.md`'s conventions.

    Open with the discharge rule copied in framing from `14-UAT-LOG.md:3-5`: only what a human actually
    observed is recorded here, and a walk is discharged when the operator states the outcome, never when
    the automated half is green.

    Header block: operator, environment (the `RESEND_API_KEY` already live in `.env.local`; the dev
    server restarted; the `npm run email:previews` invocation with the destination address), the
    evidence-prepared line, and the statement that screenshots live **outside the repository** in the
    session scratchpad with the absolute path quoted — they are evidence, not artifacts.

    The main table: one row per send × client. Columns: send name, client, *renders correctly*,
    *preheader shows*, *CTA tappable*, *plain-text part present*. Clients are **Gmail web**, **Gmail
    Android** and **Apple Mail**, plus a fourth **Outlook desktop** column whose every cell reads
    `BLOCKED — client access (D-163)`. Do not leave that column out and do not leave it empty: the
    honest convention is the one Phase 14 used for its nine ungenerated baselines — an inventory
    somebody can work from, never a gap dressed as coverage. State next to it that EMAIL-03 is **not**
    marked complete until either Outlook is opened or the PM explicitly accepts the gap at phase close.

    A dark-mode section: the Apple Mail walk under client auto-inversion. The assertion is
    **legibility end to end** — wordmark, heading, body, CTA label and footer all readable, and the CTA
    still reading as a button. Exact colours under inversion are best-effort and no baseline is taken of
    a darkened rendering; the phase authors no `prefers-color-scheme` block, because DSFUT-02 defers
    dark mode product-wide and an email dark theme ahead of the app would be a second identity.

    Two measurement rows the walk settles, from 15-UI-SPEC § Measurements Owed. **M3**: the largest
    rendered email is the ops digest at its row cap; measure its raw HTML byte size and judge it against
    the **Gmail Android** threshold, not the ~102KB desktop one — Android clips lower, and clipping
    hides the footer and therefore the plain-text link and the support slot, which is exactly the part
    EMAIL-01 is graded on. If it exceeds, the fix is trimming the shell's whitespace, never trimming the
    table's content. **M4**: the CTA button's rendered height in Gmail web against the 44px floor;
    the declared padding computes to 48px but client line-height quirks can shrink it.

    Close with a verdict table in `14-UAT-LOG.md`'s shape — Walk | Requirement | PM verdict | What
    changes — with the verdict cells left empty for the PM to fill.

    Leave every observation cell empty. The phase does not self-certify a row.
  </action>

  <verify>
    <automated>test -f .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md &amp;&amp; grep -c 'BLOCKED — client access' .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md` exists
    - It contains the discharge sentence about the operator stating the outcome
    - It contains a table with the four column headings *renders correctly*, *preheader shows*, *CTA tappable*, *plain-text part present*
    - `grep -c 'BLOCKED — client access' 15-UAT-EMAIL.md` returns at least 1 and the Outlook column is present rather than omitted
    - It contains sections for the Apple Mail dark walk, M3 (Gmail Android byte threshold) and M4 (44px CTA floor)
    - It contains a verdict table with the columns Walk | Requirement | PM verdict | What changes
    - Every observation cell is empty — `grep -c '✓\|PASS' 15-UAT-EMAIL.md` returns 0
  </acceptance_criteria>

  <done>A checklist exists that the PM can work through, in which the one unavailable client is visible as an unavailable client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The real-client walk (EMAIL-03)</name>

  <read_first>
    - .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md (the checklist the operator is about to work through)
    - .planning/phases/15-auth-profile-transactional-email/15-CONTEXT.md § D-160 and § D-163
  </read_first>

  <files>.planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md</files>

  <action>
    Pause for the operator. Present the `how-to-verify` steps below, then wait for the resume signal.

    Do not fill in a single observation cell on the operator's behalf. Do not run the walk yourself, and
    do not substitute a web client, a rendering service or a screenshot API for a client the PM does not
    have — that is the "absence dressed as coverage" this phase's whole convention exists to forbid.

    When the operator responds, transcribe **only** what they stated into `15-UAT-EMAIL.md`: the
    observation cells, the M3 and M4 numbers, the dark-mode verdict, the screenshot absolute paths and
    the verdict table. If they report a defect, record it verbatim and stop — a fix is a plan, not a
    checkpoint note. If they accept the Outlook gap, record that acceptance explicitly with the date; if
    they do not, leave EMAIL-03 unticked and say so in the summary.
  </action>

  <what-built>
    All nineteen sends now render through one branded shell with a preheader, a text wordmark, a
    600px single-column table body, one CTA and a plain-text twin. `scripts/send-email-previews.ts`
    delivers one of each to a named inbox. `15-UAT-EMAIL.md` is the empty checklist.
  </what-built>

  <how-to-verify>
    1. Confirm `RESEND_API_KEY` is set and uncommented in `.env.local` (it already is), then restart the
       dev server. No tunnel is needed.
    2. Run `npm run email:previews -- pengr.clmc.3@gmail.com`. Expect twenty messages for nineteen
       senders — `sendGuestRsvpEmail` sends both of its variants. The script prints the ordered list.
    3. Open that inbox in **Gmail web**. For each message record: renders correctly · preheader shows in
       the list preview · CTA is tappable · the plain-text part is present (inspect the raw source once
       for the client, not once per message).
    4. Open the same inbox in the **Gmail Android** app and repeat. Watch specifically for
       `[Message clipped] View entire message` on the ops digest — that is M3, and clipping hides the
       footer and the plain-text link.
    5. Open the same inbox in **Apple Mail** (the account added to Mail.app) and repeat, then walk it
       again in **dark mode**: wordmark, heading, body, CTA label and footer all legible under the
       client's auto-inversion, and the CTA still reads as a button.
    6. Measure M4 once: the CTA button's rendered height in Gmail web against the 44px floor.
    7. **Outlook desktop is declared blocked on client access (D-163).** Leave that column reading
       `BLOCKED — client access (D-163)`. Do not substitute a web-Outlook or a rendering service for it —
       an absence dressed as coverage is worse than the absence.
    8. Save one screenshot per client per representative send to the session scratchpad (outside the
       repository) and quote the absolute path in the log.
    9. State the outcomes so they can be transcribed into `15-UAT-EMAIL.md`.
  </how-to-verify>

  <resume-signal>
    Type "approved" once the walk is done, or describe what rendered wrong and in which client. Also
    state explicitly whether you accept the Outlook-desktop gap at phase close — EMAIL-03 stays unticked
    until you do or until Outlook is opened.
  </resume-signal>

  <verify>
    <automated>grep -qE '^\s*\|.*(PM|initial)' .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md &amp;&amp; grep -c 'BLOCKED — client access' .planning/phases/15-auth-profile-transactional-email/15-UAT-EMAIL.md</automated>
  </verify>

  <acceptance_criteria>
    - `15-UAT-EMAIL.md` carries a filled observation cell for every send × available-client row
    - The Outlook column still reads `BLOCKED — client access (D-163)` in every cell
    - The verdict table carries a PM verdict for each walk
    - The M3 measurement (ops-digest HTML byte size, judged against the Gmail Android threshold) is recorded as a number
    - The M4 measurement (CTA rendered height in Gmail web) is recorded as a number
    - The dark-mode section carries a legibility verdict
    - Screenshot absolute paths are quoted and point outside the repository
    - The PM has stated whether the Outlook gap is accepted at phase close, and the statement is recorded with its date
  </acceptance_criteria>

  <done>EMAIL-03's three available clients are walked and recorded by the operator; the fourth is recorded as blocked, not as covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → Resend → a real inbox | The script delivers real mail on first run; the key is already live |
| harness → the product's dispatch graph | A script that reinstated a fire-and-forget call site would be a second dispatcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Repudiation | A second dispatcher entering through a preview script | mitigate | The harness lives outside `src/app`, imports the senders directly, reinstates no `void sendXxx(...)`, and cites D-83 in its own header; asserted by a grep in this plan's acceptance criteria |
| T-15-16 | Information disclosure | Real mail delivered to an unintended recipient | mitigate | The destination is a required CLI argument with no hard-coded default; 15-CONTEXT D-160's test mode restricts delivery to the account-owner inbox regardless |
| T-15-17 | Repudiation | An unavailable client's coverage being self-certified | mitigate | The Outlook column is present and reads BLOCKED; the checklist ships with every observation cell empty; the checkpoint forbids substituting a rendering service; the requirement is not ticked until the PM states the outcome |
| T-15-05 | Spoofing | Phishing-shaped rendering in a real client | mitigate | The walk checks the CTA reads as a button and the sender is the sandbox address; no images, no external resources, no tracking pixel exist to check |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` green — nothing in this plan touches the build-blocking suite, and the `build` script
  itself is asserted byte-identical.
- `git diff package.json` shows exactly one added line.
- `15-UAT-EMAIL.md` exists, ships empty, and is filled only from what the operator stated.
</verification>

<success_criteria>
- One command, one inbox, twenty messages, an ordered printed list to match them against.
- A checklist in which the blocked client is visible as blocked.
- EMAIL-03 discharged by the PM's statement, or explicitly left open with the reason recorded.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-05-SUMMARY.md` when done.
</output>
