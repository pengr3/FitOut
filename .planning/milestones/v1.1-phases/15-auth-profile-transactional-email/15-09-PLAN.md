---
phase: 15-auth-profile-transactional-email
plan: 09
type: execute
wave: 4
depends_on: [15-07, 15-08]
files_modified:
  - src/lib/design/live-regions.ts
  - tests/design/auth-composition.test.tsx
autonomous: true
requirements: [AUTHUI-01, AUTHUI-03]

must_haves:
  truths:
    - "Every live region on the five Phase-15 surfaces is declared with what a screen reader hears, when, and which rule justifies its shape."
    - "The declared-file count moved from 21 to 26 in the same commit as the seven rows that justify it, with the compile red observed first."
    - "LIVE_REGION_EXCLUSIONS stays empty and no Phase-15 surface carries an assertive live region."
    - "A build-blocking gate proves all four auth routes compose one composition: one main landmark, the wordmark above one PanelCard, exactly one h1, exactly one accent-filled control."
    - "The gate reads page → pattern by AST, pattern → attribute by DOM, and attribute → contract by the declared selector union."
  artifacts:
    - path: "src/lib/design/live-regions.ts"
      provides: "LIVE_REGION_FILES 21 → 26 and seven new LIVE_REGIONS rows"
      contains: "DeclaredFileCountIsTwentySix"
    - path: "tests/design/auth-composition.test.tsx"
      provides: "the AUTHUI-01/03 composition gate, jsdom + AST"
      min_lines: 180
  key_links:
    - from: "src/lib/design/live-regions.ts"
      to: "the four auth pages and profile-form.tsx"
      via: "five declared files, seven declared rows, the count alias renamed"
      pattern: "DeclaredFileCountIsTwentySix"
    - from: "tests/design/auth-composition.test.tsx"
      to: "src/lib/design/selector-contract.ts"
      via: "the rendered testid is pinned against the closed SELECTOR_IDS union"
      pattern: "SELECTOR_IDS"
---

<objective>
Declare the Phase-15 live regions in the inventory that makes them a contract, and write the gate that
makes "one composition, four screens" falsifiable inside `npm run build`.

Purpose: `LIVE_REGION_FILES` is a closed union of 21 files with a compile-pinned count. The auth and
profile surfaces carry live regions the inventory has never seen — SCAN 4's derived closure is scoped to
the Phase-14 host surfaces, so nothing today audits them. And 15-UI-SPEC's central AUTHUI-01 claim —
that all four screens are one composition — is prose until something renders them and counts.

Output: the extended inventory and one jsdom + AST gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-07-SUMMARY.md
@.planning/phases/15-auth-profile-transactional-email/15-08-SUMMARY.md

<interfaces>
From `src/lib/design/live-regions.ts`:
  export const LIVE_REGION_FILES = [ … ] as const          // :340-367, grouped by `// ─── name ───` banners
  export type LiveRegionRow = {
    readonly file: LiveRegionFile;
    readonly kind: "status" | "alert" | "timer" | "loading" | "threshold";
    readonly at: number;      // 1-based ordinal among SAME-KIND regions in the SAME file
    readonly announces: string;  // WHAT a screen reader hears, and WHEN — a sentence, not a noun
    readonly why: string;        // WHICH of the seven rules, and why this shape
  }                                                        // :497-525
  export const LIVE_REGION_EXCLUSIONS = []                 // :459 — stays empty
  export type DeclaredFileCountIsTwentyOne = Assert<…extends 21…>   // :1408-1410
  Route groups and dynamic segments are ordinary characters in these paths (:337-338 says so).
  A row whose `why` is "so it announces" is rejected by the type's own docblock.

The seven rows 15-UI-SPEC § Live Regions declares (id · file · kind · announces · name):
  login-form-error       (auth)/login/page.tsx            alert   the generic anti-enumeration refusal, once per failed submit   content
  signup-form-error      (auth)/signup/page.tsx           alert   the server action's refusal sentence                          content
  forgot-request-result  (auth)/forgot-password/page.tsx  status  the uniform sentence, once, when the form is replaced         aria-label="Reset request result"
  reset-form-error       (auth)/reset-password/page.tsx   alert   the invalid/expired-link sentence, once per failed submit     content
  profile-save-result    (app)/profile/profile-form.tsx   status  "Profile saved." once per successful save                     aria-label="Save state"
  profile-form-error     (app)/profile/profile-form.tsx   alert   the server's refusal sentence                                 content
  profile-avatar-error   (app)/profile/profile-form.tsx   alert   the upload refusal                                            content

Two regions were REMOVED rather than declared (plan 15-07): `ResetNotice` on login and the reset page's
missing-token notice. Both are static-on-arrival content, and a live region announces a CHANGE.

From `src/lib/design/selector-contract.ts`: `SELECTOR_ATTRIBUTE` is `"data-testid"`; `SELECTOR_IDS`
already declares `panel-card`. 15-UI-SPEC pins `SELECTOR_IDS` at **+0** for this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the seven Phase-15 live regions</name>

  <read_first>
    - src/lib/design/live-regions.ts (the module header, `LIVE_REGION_FILES` at :330-370, `LIVE_REGION_EXCLUSIONS` at :459, `LiveRegionRow` at :497-525, a filled row such as `calendar-day-loading` at :598-613 to copy the register from, and the count alias with its docblock at :1401-1410)
    - src/app/(auth)/login/page.tsx, signup/page.tsx, forgot-password/page.tsx, reset-password/page.tsx (as converted by plan 15-07 — read the ACTUAL regions and their source order, because `at` is a 1-based ordinal among same-kind regions in the same file)
    - src/app/(app)/profile/profile-form.tsx (as converted by plan 15-08 — three regions, two kinds, so two of them share an ordinal sequence)
    - tests/design/live-regions.test.tsx lines 1-160 (SCANs 1-3 audit the declared set; SCAN 4's derived closure is Phase-14-scoped and does not reach these files — know that before you decide what the extension does and does not buy)
  </read_first>

  <files>src/lib/design/live-regions.ts</files>

  <action>
    Add a new grouping block to `LIVE_REGION_FILES` in the file's existing banner style — a
    `// ─── the auth surfaces + profile (plan 15-09) ───` header followed by the five file paths: the
    four `src/app/(auth)/*/page.tsx` files and `src/app/(app)/profile/profile-form.tsx`. Write the paths
    exactly as they appear on disk, route-group parentheses and all.

    Add the seven `LIVE_REGIONS` rows this plan's `<interfaces>` block names. For each row:
    - `at` is the 1-based ordinal among same-kind regions **in the same file**, read off the converted
      source rather than assumed. `profile-form.tsx` has one `status` and two `alert` regions, so the
      two alerts are ordinals 1 and 2 in source order — read which is which.
    - `announces` quotes the literal sentence a screen reader hears and names the moment it is heard, in
      the register of `calendar-day-loading` at :598-613. Not a noun, not "the error".
    - `why` cites the numbered rule from the module's own list. A row whose `why` is "so it announces"
      is rejected by the type's docblock.

    Rename the count alias from `DeclaredFileCountIsTwentyOne` to `DeclaredFileCountIsTwentySix` and
    move the constraint 21 → 26. **Observe the red first**: with the five files added and the number
    still at 21, run `npx tsc --noEmit` and quote the whole of stdout verbatim into the alias's own
    docblock, following the procedure that docblock already records from plan 14-14. Re-measure and
    update the header's stated count in the same commit.

    Add a short note in the new block recording the two REMOVALS — `ResetNotice` and the reset page's
    missing-token notice — with the rule: a live region announces a CHANGE; a freshly rendered page is
    not a change, it is a page. A reader looking for those two regions should find out here why they are
    absent rather than concluding the inventory missed them.

    `LIVE_REGION_EXCLUSIONS` stays `[]`. No Phase-15 surface carries `aria-live="assertive"`.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/design/live-regions.test.tsx --config vitest.design.config.ts &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'DeclaredFileCountIsTwentySix' src/lib/design/live-regions.ts` returns 1 and `grep -c 'DeclaredFileCountIsTwentyOne' src/lib/design/live-regions.ts` returns 0
    - `grep -c 'extends 26' src/lib/design/live-regions.ts` returns 1
    - `LIVE_REGION_FILES` contains all five Phase-15 paths, and `grep -c 'LIVE_REGION_EXCLUSIONS = \[\]' src/lib/design/live-regions.ts` returns 1
    - The seven new rows exist, each with a non-empty `announces` quoting a literal sentence and a `why` citing a numbered rule
    - The alias docblock quotes the observed `tsc` red verbatim, dated and attributed to this plan
    - `grep -c 'aria-live="assertive"' src/app/\(auth\)/*/page.tsx 'src/app/(app)/profile/profile-form.tsx'` returns 0 for every file
    - `npx vitest run tests/design/live-regions.test.tsx --config vitest.design.config.ts` exits 0
    - `npm run test:design` exits 0 and `npx tsc --noEmit` exits 0
    - `git diff --name-only` lists only `src/lib/design/live-regions.ts`
  </acceptance_criteria>

  <done>Seven regions are contracts rather than markup, the count moved with its rows after its red was watched, and the two deliberate removals are recorded where somebody would look for them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The AUTHUI-01/03 composition gate</name>

  <behavior>
    - Each of the four auth pages composes `PanelCard` and renders zero raw `Card` — proved by AST, with aliased imports resolved so `import { Card as Box }` is still a raw container.
    - Each page passes `titleAs="h1"`, so each auth document carries exactly one `h1`.
    - `(auth)/layout.tsx` renders exactly one `main`, one wordmark reading the shared class constant, and keeps `SiteFooter`; it renders no `PublicHeader`.
    - Rendering `PanelCard` produces the declared `panel-card` testid, and that id is a member of `SELECTOR_IDS`.
    - Each page carries exactly one `variant="brand"` control and that control carries `size="touch"`.
    - Neither `ResetNotice` nor the reset page's missing-token notice carries a live-region role.
    - A deliberately violating module fixture is caught by every scanner the file claims.
  </behavior>

  <read_first>
    - tests/design/cancel-page-shell.test.tsx (the whole file — the four-link chain in its header at :42-47, the `(path, text)` scanner signature at :94-102, the guard-the-guard block at :202-217, the violating fixture at :174-195 and its case (6) at :281-294, and the disarmed-tripwire idiom at :153-157)
    - src/app/(auth)/layout.tsx and the four converted `(auth)/*/page.tsx` files (assert against what they actually render)
    - src/components/patterns/panel-card.tsx (what it renders and which testid it carries)
    - src/lib/design/selector-contract.ts (`SELECTOR_ATTRIBUTE`, `SELECTOR_IDS`)
    - vitest.design.config.ts (confirm the jsdom environment is selected per-file by a pragma, as `cancel-page-shell.test.tsx` does)
  </read_first>

  <files>tests/design/auth-composition.test.tsx</files>

  <action>
    Create `tests/design/auth-composition.test.tsx` modelled on `tests/design/cancel-page-shell.test.tsx`
    — the same job on the same kind of surface. Open with the `// @vitest-environment jsdom` pragma and
    import `describe`/`it`/`expect` from vitest, `readFileSync`/`resolve` from node, `typescript` for the
    AST, `render`/`screen` from `@testing-library/react`, `PanelCard` from the pattern layer, and
    `SELECTOR_ATTRIBUTE`/`SELECTOR_IDS` from the selector contract.

    Follow the analog's four-link chain and say so in the header: **page → pattern (AST) → attribute
    (DOM) → contract (union)**. A source scan alone proves only that a page composes a pattern; it
    cannot see what that pattern renders.

    Write the scanner with the `(path, text)` signature, never `(path)`, so the positive-control fixture
    runs the same code path the real assertions do. Resolve aliased imports through
    `propertyName ?? name`, so renaming an import cannot launder a raw container.

    **Guard-the-guard, asserted first**, for all five files: each source read is longer than a floor, and
    each scan resolved more JSX elements than a floor. Almost every assertion below is an absence, and an
    empty parse satisfies all of them.

    Then, per auth page: exactly one `PanelCard` element, zero raw `Card`/`CardHeader`/`CardTitle`/
    `CardDescription`/`CardContent` elements, exactly one `titleAs="h1"` attribute, exactly one
    `variant="brand"` attribute and a `size="touch"` on the same element. Collect the four `title` and
    `description` string literals and assert them against the byte-for-byte table in 15-UI-SPEC — write
    those eight strings into the test as its own expectation, so a copy change has to move this file too.

    Then, for the layout: exactly one `main` element, zero `PublicHeader` references, at least one
    `SiteFooter` reference, and exactly one reference to the shared wordmark class constant. Assert the
    wordmark is a link to `/` and carries no `data-testid`.

    Then the DOM link: render `PanelCard` and read the `data-testid` attribute off the result; assert its
    value is a member of `SELECTOR_IDS`. This is the link a source scan cannot make.

    Then the live-region demotions: assert `login/page.tsx` contains no `role="status"` and
    `reset-password/page.tsx` contains exactly one `role="alert"`. Spell the politeness attribute through
    a join, the disarmed-tripwire idiom `cancel-page-shell.test.tsx:153-157` records, so this file's own
    source cannot be read as a violation by a future whole-tree scan.

    Finish with the **positive-control fixture**: a violating module string — a raw `Card` imported under
    an alias, a `PanelCard` without `titleAs`, two `variant="brand"` controls, a `role="status"` on
    static content — fed to the same scanner, asserting each defect is reported. Never write it to disk.

    Use the AST rather than a text scan throughout. The converted pages carry comments that EXPLAIN the
    adoption, and a text scan would report the comment describing the fix as the defect — the exact
    reason `cancel-page-shell.test.tsx:30-36` gives.

    Apply the mutation-verification discipline: for each assertion group, mutate the corresponding
    source, record the RED verbatim in the header, revert, and confirm `git diff --exit-code src/` clean.
  </action>

  <verify>
    <automated>npx vitest run tests/design/auth-composition.test.tsx --config vitest.design.config.ts &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `tests/design/auth-composition.test.tsx` exists and begins with the `@vitest-environment jsdom` pragma
    - It imports `SELECTOR_IDS` from `@/lib/design/selector-contract` and renders `PanelCard` to read its testid off the DOM
    - Its scanner is declared with a `(path: string, text: string)` signature
    - It contains a guard-the-guard block asserting length and JSX-element floors, positioned before the first absence assertion
    - It contains a violating fixture string that is never written to disk, and a case that feeds it to the scanner
    - `grep -c 'aria-live' tests/design/auth-composition.test.tsx` returns 0 — the attribute is spelled through a join
    - `npx vitest run tests/design/auth-composition.test.tsx --config vitest.design.config.ts` exits 0
    - `npm run test:design` exits 0
    - The header records at least four mutations with their RED output quoted verbatim
    - `git diff --exit-code src/` exits 0 after the mutation walk
  </acceptance_criteria>

  <done>"One composition, four screens" is a command that exits non-zero when it stops being true, and every assertion in it has been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the declared inventory → the surfaces it claims to audit | A file joins by being written into the union; a file nobody wrote in scans identically to a file somebody audited |
| a gate's assertions → the tree they run over | An absence asserted over a tree the walker never entered is not an assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-26 | Repudiation | A screen-reader user missing a refusal, or hearing a page as a change | mitigate | Seven declared rows with the sentence and the moment stated; the two demotions recorded with the rule; `LIVE_REGION_EXCLUSIONS` stays empty |
| T-15-07 | Tampering | A vacuous gate over an empty parse | mitigate | Guard-the-guard asserted first over all five files; a positive-control violating fixture; mutation walk with RED quoted |
| T-15-19 | Information disclosure | An undeclared or duplicated testid making a container query resolve on the wrong element | mitigate | The rendered attribute is pinned against the closed `SELECTOR_IDS` union; the auth wordmark is asserted to carry no testid |
| T-15-31 | Tampering | A copy change to an anti-enumeration or auth sentence slipping through | mitigate | The eight `title`/`description` literals are written into the gate, so a copy change must move this file too and be seen |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:design` green.
- `git diff --exit-code src/` exits 0 after both mutation walks.
- `git diff --name-only` at plan end lists only the two files in `files_modified`.
- `grep -c 'aria-live="assertive"'` over the five Phase-15 surfaces returns 0.
</verification>

<success_criteria>
- `LIVE_REGION_FILES` at 26, seven rows, exclusions still empty, the alias red quoted verbatim.
- A build-blocking gate that reads page → pattern → attribute → contract across all four auth routes.
- Both new artifacts carry a guard-the-guard block and a positive control.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-09-SUMMARY.md` when done.
</output>
