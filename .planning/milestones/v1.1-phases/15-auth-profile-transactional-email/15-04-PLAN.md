---
phase: 15-auth-profile-transactional-email
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - src/lib/email.ts
  - tests/helpers/email-fixtures.ts
  - tests/auth/email-injection.test.ts
  - tests/ops/alert-digest.test.ts
  - tests/design/site-contacts.test.ts
autonomous: true
requirements: [EMAIL-01, EMAIL-02]

must_haves:
  truths:
    - "All nineteen exported senders compose one shell; zero build bare markup."
    - "The ops digest keeps its two contracts: OpsDigestRow still has no meta field, and its per-field escaping survives because its output enters the one pre-escaped slot."
    - "One adversarial payload driven through every string parameter of all nineteen senders appears nowhere raw in any rendered HTML, preheader div included."
    - "The digest's PII guarantee is now read at the point the string is built, not only through the transport."
    - "The email footer's guarded support site is declared in the contacts inventory with its reason (15-CONTEXT D-161) — the gate is extended, never weakened."
    - "The sandbox sender remains the single address-shaped literal declared for src/lib/email.ts (15-CONTEXT D-160)."
  artifacts:
    - path: "tests/helpers/email-fixtures.ts"
      provides: "one argument list per exported sender, totality-typed so a missing sender is a compile error"
      min_lines: 80
    - path: "tests/auth/email-injection.test.ts"
      provides: "the all-sender injection probe over every string parameter"
      min_lines: 70
  key_links:
    - from: "src/lib/email.ts renderOpsAlertDigest"
      to: "src/lib/email-shell.ts"
      via: "the table enters through the pre-escaped tableHtml slot with a matching tableText"
      pattern: "tableHtml"
    - from: "tests/auth/email-injection.test.ts"
      to: "tests/helpers/email-fixtures.ts"
      via: "the shared 19 argument lists, also read by the preview harness"
      pattern: "email-fixtures"
    - from: "tests/design/site-contacts.test.ts"
      to: "src/lib/email-shell.ts"
      via: "the declared guarded-site inventory gains the shell's footer with a reason"
      pattern: "email-shell"
---

<objective>
Finish EMAIL-01: bring the nineteenth sender under the shell without breaking either of its two
contracts, then widen the escaping guarantee from two hand-picked sends to every string parameter of
all nineteen, and declare the shell's guarded support site in the contacts inventory.

Purpose: the ops digest is the one send whose body is a data table and whose type deliberately has no
`meta` field, so that re-exporting booking ids, transfer ids and masked last-4s across the email trust
boundary is a **type error at the renderer** rather than something a reviewer has to notice. Folding it
into the shell is the single place where either contract could be lost. Separately, 15-UI-SPEC AC#16
asks for one injection probe through every string parameter of all nineteen senders — the guarantee the
current two-send escaping test only samples.

Output: a fully converted `email.ts`, a shared sender-fixture module the preview harness will also read,
the all-sender probe, a direct-read PII assertion, and one more declared row in the contacts gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-03-SUMMARY.md

<interfaces>
From `src/lib/email.ts` (unchanged by this plan except where stated):
  export type OpsDigestRow = {
    id: string; action: string; actorId: string;
    createdAt: Date; ageHours: number; aging: boolean;
  }                                    // NO `meta` field — the absence IS the contract (D-J3Z-02 / D-72)
  export function renderOpsAlertDigest(rows: OpsDigestRow[], opts: { truncated: boolean; limit: number }): string
    → return type becomes { html: string; text: string }
  export const sendOpsAlertDigest = (to, rows, opts) => …   // signature and subject byte-identical

From `tests/helpers/mocks.ts`:
  type CapturedEmail = { from: string; to: string | string[]; subject: string; html?: string; text?: string }
  mockResend.last() / mockResend.sent()          // `text` is ALREADY captured — no helper edit needed
  mockResend.extractLink(...)                    // returns the FIRST https?:// match in html + " " + text

From `tests/design/site-contacts.test.ts` (the shape this plan extends):
  const FOOTER = "src/components/patterns/site-footer.tsx"
  describe("the guard itself — one conditional, no else-branch, in either state")
    → three assertions, currently scoped to FOOTER alone:
      exactly one SUPPORT_EMAIL conditional · no else-branch · no address-shaped literal in the file
  const EXCLUDED_ADDRESSES: Readonly<Record<string, string>>   // keyed `file — literal`, reason > 40 chars
  The null branch already asserts "every `mailto:` in src/ sits inside a verified SUPPORT_EMAIL guard"
  over ALL of `src/**`, so the shell's guarded mailto is already in scope — this plan adds the
  STRUCTURAL trio for it, which is currently footer-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The ops digest wears the shell, and its PII guarantee becomes a direct read</name>

  <read_first>
    - src/lib/email.ts lines 588-690 (the whole ops-digest block: the `meta`-absence contract header, `OpsDigestRow`, `renderOpsAlertDigest`, `sendOpsAlertDigest`)
    - src/lib/email-shell.ts (the `tableHtml` / `tableText` slot's contract and the fact the renderer does NOT escape it)
    - tests/ops/alert-digest.test.ts (the whole file — especially the haystack idiom around :113-118 and the PII sentinel assertions around :191-201)
    - .planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md § Pitfall 10
    - .planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md § the two corrections table (zero test files import `renderOpsAlertDigest` today)
  </read_first>

  <files>src/lib/email.ts, tests/ops/alert-digest.test.ts</files>

  <action>
    Change `renderOpsAlertDigest`'s return type from `string` to `{ html: string; text: string }` and
    have it compose `renderEmail`. It stays **exported and pure**. `OpsDigestRow` is untouched — it must
    not gain a `meta` field, and its absence stays the type-level enforcement of the D-72 column rule.
    The two references to `renderOpsAlertDigest` in the repository are both in this file (the
    declaration and `sendOpsAlertDigest`'s call), so no other module needs an edit.

    Split its current output at the natural seam:
    - `heading` becomes `FitOut ops — unresolved money alerts`.
    - `paragraphs` becomes the runbook sentences and, when `opts.truncated`, the honest
      `{limit}+ unresolved — showing the {limit} newest.` line. Keep every command string
      byte-identical.
    - `tableHtml` becomes the `<table>` block — the header row plus the mapped `<tr>` rows — built
      exactly as it is today, with its per-field `escapeHtml` calls **intact**. This is the one
      pre-escaped slot and the renderer does not escape it, so removing those calls would create the
      injection sink the choke point exists to close. Colour the cell borders from the rule token via
      the shell rather than by typing a value here; if that means the table needs a border colour
      passed in, pass the already-resolved value from the shell's palette rather than a literal.
    - `tableText` becomes a plain-text projection of the same rows: one line per row carrying the same
      five fields in the same order, with the `— AGING` marker as plain text exactly as today. Derive it
      from the same `rows` array in the same map, so the two cannot drift.
    - The digest renders **no CTA**. Do not pass one. Its focal point is the table.

    One deliberate formatting change, recorded rather than slipped in: the runbook paragraphs today wrap
    three command strings in `<code>` elements and pre-escape an angle-bracketed placeholder. Under the
    shell, paragraphs are escaped at the choke point, so a `<code>` element inside one would render as
    visible tag text. Drop the `<code>` wrappers and carry the command strings as plain text, with the
    placeholder written as its raw angle-bracketed form in the content (the renderer escapes it back for
    HTML, and `text/plain` shows it correctly). The command strings themselves stay byte-identical, and
    an operator email that reads its commands as plain text is the same house style as the `— AGING`
    marker. Record this in the summary as a stated change, not as an incidental one.

    Update `renderOpsAlertDigest`'s docblock. Its current sentence — "the PII assertion reads this body
    DIRECTLY rather than only through the transport" — is measurably **false today**: zero test files
    import it, and `tests/ops/alert-digest.test.ts` asserts the sentinel through `mockResend.sent()`.
    Make it true in this same commit by adding a direct-read case to `tests/ops/alert-digest.test.ts`
    that imports `renderOpsAlertDigest`, renders fixture rows, concatenates the returned `html` and
    `text`, and asserts the same PII sentinels absent. Leave the existing transport-level assertions in
    place and unmodified — the two are complementary, and the transport one is what proves the send
    actually carries what the renderer built.

    `sendOpsAlertDigest` destructures the two parts and passes both to `send`. Its signature, its
    subject template literal and its "never called with an empty rows" docblock are byte-identical.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/ops/ &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'meta' src/lib/email.ts` shows no `meta` field added to `OpsDigestRow` — the type still lists exactly `id`, `action`, `actorId`, `createdAt`, `ageHours`, `aging`
    - `grep -c '<p><strong>' src/lib/email.ts` returns 0 — no bare markup builder survives anywhere in the file
    - `grep -c 'renderEmail(' src/lib/email.ts` returns at least 19
    - `renderOpsAlertDigest` is still `export function` and still takes `(rows, opts)`
    - `grep -c 'escapeHtml' src/lib/email.ts` is at least 5 — the digest's per-field calls survive
    - `tests/ops/alert-digest.test.ts` contains an import of `renderOpsAlertDigest` from `@/lib/email`
    - `npx vitest run tests/ops/` exits 0, and the pre-existing transport-level PII assertions appear unmodified in `git diff`
    - `npm run test:design` exits 0
    - `npx tsc --noEmit` exits 0
    - The summary records the `<code>`-wrapper removal as a stated formatting change with its reason
  </acceptance_criteria>

  <done>Nineteen of nineteen sends compose one shell; the digest's type-level PII contract and its per-field escaping both survived, and the docblock's claim about a direct read is true for the first time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The shared sender fixtures and the all-sender injection probe</name>

  <behavior>
    - One adversarial payload substituted into every string parameter of all 19 exported senders appears nowhere raw in any rendered HTML.
    - The escaped forms of the payload's characters DO appear in the HTML.
    - The payload appearing raw in the `text` part is expected and is not asserted against.
    - A sender missing from the fixture module is a compile error, not a silently unprobed sender.
    - The probe inspects the whole HTML document, the leading preheader div explicitly included.
  </behavior>

  <read_first>
    - tests/auth/email-escaping.test.ts (the whole file — the capture pattern to widen, and the file that must stay unedited)
    - tests/ops/alert-digest.test.ts lines 100-130 (the whole-body haystack idiom that concatenates subject + html + text)
    - tests/helpers/mocks.ts (the `CapturedEmail` type and `mockResend`'s surface)
    - src/lib/email.ts (the 19 exported sender signatures — read the parameter lists you are about to fixture)
    - src/lib/design/visual-baselines.ts lines 216-230 (the `as const satisfies Record<…>` totality idiom to copy so a missing sender is a compile error)
    - .planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md § Pitfall 2 and § Open Question 2
  </read_first>

  <files>tests/helpers/email-fixtures.ts, tests/auth/email-injection.test.ts</files>

  <action>
    Create `tests/helpers/email-fixtures.ts`: one entry per exported sender, giving a plausible argument
    list for each, plus a per-entry marker of which arguments are strings the probe may substitute into.
    Type it with the `as const satisfies Record<…>` totality idiom `visual-baselines.ts` uses, keyed
    against a union derived from the sender names, so **adding a sender without a fixture is a compile
    error**. `sendGuestRsvpEmail` takes an object and branches on `kind`, so give it both variants;
    `sendOpsAlertDigest` takes rows plus options, so give it a small fixed row set with a literal
    `createdAt` instant — never `new Date()`, per the Phase-14 rule that geometry and fixtures never
    seed from the clock.

    This module is deliberately shared: plan 15-05's `scripts/send-email-previews.ts` reads the same 19
    argument lists to deliver one of each send for the EMAIL-03 walk. Build it once. Its header should
    say so, so nobody builds a second copy.

    Create `tests/auth/email-injection.test.ts` in the **main** vitest config (it needs `mockResend`,
    which only `tests/setup.ts` installs, and therefore Docker). Define one payload carrying all five
    HTML-significant characters plus a script tag and an attribute-breaking quote sequence. For each of
    the 19 senders, drive the payload through every string parameter the fixture marks — one call per
    substitution, or one call with the payload in every string slot at once, whichever the sender's
    contract allows; where a parameter is a URL, keep it URL-shaped with the payload appended so the
    call remains realistic.

    For every captured send, assert the raw payload appears nowhere in `html`. Assert explicitly over
    the whole document string, and add one assertion that specifically slices the first ~300 characters
    of the HTML — the preheader region — and checks the payload is absent there too. The derived
    preheader is a fifth escape site that 15-UI-SPEC's escaping sentence does not name, and a probe that
    only inspects the body would pass over exactly the sink nobody looks at.

    Assert the escaped forms are present, so the probe cannot pass by rendering nothing. Assert a
    positive control: at least one captured send exists per sender and its `html` exceeds a length
    floor. Do **not** assert the payload absent from `text` — plain text takes raw strings by design and
    an assertion forbidding it would be wrong.

    Do not edit `tests/auth/email-escaping.test.ts`. Do not edit `tests/helpers/mocks.ts` — it already
    captures `text`.

    Apply the mutation-verification discipline from `tests/ops/alert-digest.test.ts:25-60`: delete the
    preheader's escape call in the shell, watch the probe go red, record the RED verbatim in the test's
    header, revert, and confirm `git diff --exit-code src/` is clean.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/auth/ &amp;&amp; git diff --exit-code tests/auth/email-escaping.test.ts tests/helpers/mocks.ts</automated>
  </verify>

  <acceptance_criteria>
    - `tests/helpers/email-fixtures.ts` exists and contains `satisfies Record<`
    - The fixture module declares 19 entries; removing one fails `npx tsc --noEmit`
    - `grep -c 'new Date()' tests/helpers/email-fixtures.ts` returns 0 — fixtures use absolute instants
    - `tests/auth/email-injection.test.ts` exists and imports from `../helpers/email-fixtures`
    - The probe contains an assertion scoped to the leading region of the HTML document (the preheader)
    - `npx vitest run tests/auth/email-injection.test.ts` exits 0
    - `git diff --exit-code tests/auth/email-escaping.test.ts tests/helpers/mocks.ts` exits 0
    - The test header records the preheader-escape mutation with its RED output quoted verbatim
    - `git diff --exit-code src/` exits 0 after the mutation walk
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>The escaping guarantee is proved over the whole surface rather than sampled at two sends, and the fifth escape site is inside the probe's reach.</done>
</task>

<task type="auto">
  <name>Task 3: Declare the shell's guarded support site in the contacts inventory</name>

  <read_first>
    - tests/design/site-contacts.test.ts lines 1-200 (the header, the `SITE_MODULE`/`FOOTER`/`EMAIL_MODULE` constants, the scan's stated width over all of `src/`)
    - tests/design/site-contacts.test.ts lines 205-240 (`EXCLUDED_ADDRESSES` and its three auth-placeholder rows)
    - tests/design/site-contacts.test.ts lines 526-565 (the three structural guard assertions, currently scoped to `FOOTER`)
    - src/lib/email-shell.ts (the footer's guard as actually written — one conditional, no else)
    - src/lib/site.ts (the D-26/D-64 block that both sites implement)
  </read_first>

  <files>tests/design/site-contacts.test.ts</files>

  <action>
    Extend the structural guard block so it audits a declared **inventory** of guarded sites rather than
    the single `FOOTER` constant. Replace the bare `FOOTER` scoping in the "the guard itself — one
    conditional, no else-branch, in either state" describe with a small declared record mapping file
    path to reason, seeded with two rows: the existing footer (carrying its shipped reason, written out
    rather than implied) and `src/lib/email-shell.ts` with a reason over 40 characters explaining that
    the email footer implements the same D-26/D-64 slot on the other surface, that one line in
    `src/lib/site.ts` lights both up, and that 15-CONTEXT D-161 re-confirmed the null state for this
    phase. Run all three existing assertions — exactly one `SUPPORT_EMAIL` conditional, no else-branch,
    no address-shaped literal in the file — over every row.

    Weaken nothing. The null branch's four whole-tree assertions, the non-null branch's exactly-one-
    footer-`mailto:` assertion, `EXCLUDED_ADDRESSES` and its stale-row check, the `MIN_FILES` floor and
    the Resend-sender row all stay byte-identical in behaviour. The non-null branch's `mailto:` count
    assertion stays **scoped to the app footer** — the shell's guarded mailto is a second site, not a
    second footer entry.

    Add a guard-the-guard case for the new row: assert the scan actually reached
    `src/lib/email-shell.ts` (a file count or a positive marker), because a structural assertion over a
    file the walker never opened reports a clean result forever — the exact hole this file's own header
    at (d) records.

    Record the observed red: temporarily add an else-branch to the shell's support guard, run the gate,
    quote the failure verbatim in the test's running changelog, revert, and confirm
    `git diff --exit-code src/` is clean.
  </action>

  <verify>
    <automated>npx vitest run tests/design/site-contacts.test.ts --config vitest.design.config.ts &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'email-shell' tests/design/site-contacts.test.ts` returns at least 1
    - The declared guarded-site inventory has exactly 2 rows and each reason exceeds 40 characters
    - The three structural assertions iterate the inventory rather than naming `FOOTER` directly
    - `grep -c 'you@example.com' tests/design/site-contacts.test.ts` is unchanged from its pre-task value — the three auth placeholder rows were not touched
    - `grep -c 'onboarding@resend.dev' tests/design/site-contacts.test.ts` returns at least 1 — the sender row survives
    - `npx vitest run tests/design/site-contacts.test.ts --config vitest.design.config.ts` exits 0
    - `npm run test:design` exits 0
    - The test's changelog records the else-branch mutation with its RED output quoted verbatim
    - `git diff --exit-code src/` exits 0 after the mutation walk
  </acceptance_criteria>

  <done>The email footer's support slot is a declared guarded site with a reason, audited by the same three assertions that protect the app footer — extended, never weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit table free-text columns → email HTML | `action` and `actorId` are written from ~20 `recordAudit` call sites and are untrusted for HTML purposes |
| `OpsDigestRow` type → the email trust boundary | The absent `meta` column is the compile-time enforcement of a column-level PII rule (D-72) |
| `tableHtml` slot → the renderer | The one place the choke point deliberately does not escape |
| `SUPPORT_EMAIL` → two rendered footers | One constant controls an affordance on two surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | `tableHtml` used as a general-purpose raw slot | mitigate | The digest keeps its per-field `escapeHtml`; the slot's contract is stated in the type's comment; the all-sender probe drives the payload through the digest's `action` and `actorId` too |
| T-15-13 | Information disclosure | Booking ids / transfer ids / masked last-4s crossing into email | mitigate | `OpsDigestRow` gains no `meta` field, so re-export is a type error; a direct-read PII assertion is added so the guarantee is pinned where the string is built |
| T-15-02 | Tampering | The derived preheader as an unnamed injection sink | mitigate | The probe asserts the raw payload absent from the leading region of the document, and the mutation walk proves that assertion fires |
| T-15-14 | Tampering | Escaping regression on a sender nobody sampled | mitigate | The probe covers every string parameter of all 19 senders; the fixture module's totality typing makes a missing sender a compile error |
| T-15-06 | Information disclosure | A fabricated or unguarded support address | mitigate | The structural trio (one conditional, no else, no address literal) now runs over the shell as well as the footer, with a guard-the-guard proving the walker reached it |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/auth/ tests/booking/ tests/notifications/ tests/ops/` green (Docker up).
- `npm run test:design` green.
- `git diff --exit-code tests/auth/email-escaping.test.ts tests/auth/email-dev-fallback.test.ts tests/booking/notify-emission.test.ts tests/helpers/mocks.ts` exits 0.
- `grep -c '<p><strong>' src/lib/email.ts` returns 0.
- No consumer module appears in `git diff --name-only`.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never concurrently.
</verification>

<success_criteria>
- Nineteen of nineteen senders compose one shell.
- The digest's two contracts are intact and one of them is now proved by a direct read.
- One payload through every string parameter of every sender is absent from every rendered HTML,
  including the preheader.
- The contacts gate audits two guarded sites and was watched red on the new one.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-04-SUMMARY.md` when done.
</output>
