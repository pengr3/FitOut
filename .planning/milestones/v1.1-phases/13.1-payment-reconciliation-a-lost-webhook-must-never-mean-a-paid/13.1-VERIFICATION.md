---
phase: 13.1-payment-reconciliation
verified: 2026-08-22T18:15:00Z
status: human_needed
score: 6/6 must-haves verified (0 blockers); 3/3 PM-approved residuals CLOSED and independently re-verified; 1 pre-existing, non-blocking, PM-scheduled live-UAT item remains open
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "6/6 must-haves verified (0 blockers); 3 residual items require a human/PM decision before phase close"
  gaps_closed:
    - "Residual A — cancelUnpaidHold could orphan a payable session with no backstop. Closed by a fifth RetireTrigger member (booker-cancel-hold) wired at the call site, with an independent catch and proven behaviourally over the REAL retire-checkout policy (not just a mocked call site)."
    - "Residual B — the ops runbook had no entry for webhook_missed / checkout_paid_after_lapse. Closed with §4b/§4c added, forked correctly on the code's real branch condition (meta.bookingStatus), plus five stale call-site pointers corrected."
    - "Residual C — the Inngest re-sync requirement lived only in code comments. Closed by a new .planning/ops/DEPLOY-CHECKLIST.md naming both guarantee-critical crons by id/cron-string and separating REGISTERED from TICKING with two concrete checks."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Decide whether the Checkout Session response shape's `payments[0].id` placement needs to be captured against a REAL PayMongo paid session before phase close, or remains a standing live-UAT follow-up."
    expected: "One `GET /v1/checkout_sessions/<id>` body for a genuinely PAID session, captured and compared against the envelope-first read `attributes.payments?.[0]?.id ?? attributes.payments?.[0]?.attributes?.id ?? null`."
    why_human: "Unchanged from the original pass — this item was explicitly NOT one of the three residuals the PM approved for closure in 13.1-06, and 13.1-06 says so itself in its 'Deferred Issues' section. It has been open since 13-03 (predates this phase), the cost of being wrong is bounded (`paymentId: null`, not a regression), and it cannot be closed by any code plan because it needs a real paid session. This is the same category of item as Phase 13's own two UAT walks and `SUPPORT_EMAIL` — open by explicit PM scheduling decision, not a defect. It does not block treating the three targeted residuals as closed."
---

# Phase 13.1: Payment Reconciliation Verification Report

**Phase Goal:** "A booker who paid always ends up with a booking. A lost webhook becomes a delay, never a
silent loss. And when a hold expires, the ability to pay expires with it — FitOut never takes money for a
booking that no longer exists."

**Verified:** 2026-08-22 (initial pass) · **Re-verified:** 2026-08-22 (after plan 13.1-06 gap closure)
**Status:** human_needed — but see the framing below. 0 blockers. 3 of 3 PM-approved residuals closed and
independently re-verified. 1 pre-existing, non-blocking, PM-scheduled live-UAT item remains.
**Re-verification:** Yes — focused re-verification of plan 13.1-06's three claimed residual closures, plus
a full regression sweep.

---

## RE-VERIFICATION (2026-08-22, after plan 13.1-06)

**Scope of this pass, per the task that requested it:** confirm that the three residuals named in the
original verification (`cancelUnpaidHold`'s orphaning path, the runbook gap, and the deploy-note gap) are
genuinely closed, and that closing them regressed nothing. The original 6/6 must-haves verdict and its
"0 blockers" finding are NOT being re-litigated — they stand. This section documents independent evidence
for each of the three closures and for every regression check the task specified, gathered directly from
source, tests, and the dev database — not from 13.1-06-SUMMARY.md's narration.

### Residual 1 — `cancelUnpaidHold` orphaning a payable session with no backstop

**Verdict: CLOSED. Verified at all four levels (exists, substantive, wired, behaviourally proven).**

| Check | Evidence |
|---|---|
| Shape shipped | `src/app/actions/cancel-booking.ts:860-997` — `cancelUnpaidHold`'s flip now `RETURNING id, status, checkout_session_id` (line 902), followed by `await retireWithdrawnSession(bookingId, flipped[0].checkout_session_id, flipped[0].status)` (line 925), which wraps `retireCheckoutsForBookings([...], "booker-cancel-hold")` in its own `try { } catch { console.error(...) }` (lines 978-997) — an independent guard, not delegated to the policy's never-throw contract. |
| Fifth `RetireTrigger` member | `src/lib/payments/retire-checkout.ts` — `RetireTrigger` union now includes `"booker-cancel-hold"` alongside the four pre-existing members, documented in the file's own header as the one member that is a guarantee rather than an accelerant. |
| The "two independent exclusions" claim, re-derived (not trusted) | `queryRetirableSessions` in `src/inngest/functions/checkout-retire.ts:171-189` — first WHERE clause is `status IN ('pending', 'approved')`, independent of the `expires_at <= now()` clause the original brief named. `cancelUnpaidHold`'s UPDATE writes `status = 'cancelled'`/`'declined'` in the SAME statement as the `expires_at = NULL`. I confirmed both clauses directly from source: shape (b) ("stop nulling `expires_at`") would indeed close nothing, because the status clause alone already excludes the row. `queryUnconfirmedPaid` (`payment-reconcile.ts:206-214`) is likewise `status IN ('pending', 'approved')` and excludes a `cancelled`/`declined` row independently. This is not merely a restated claim — I read both selector predicates myself. |
| Behavioural proof the withdrawal closes the session | `tests/booking/cancel-unpaid-hold-retires-session.test.ts`, cases (8)-(10) run `cancel-booking.ts` against the REAL `retire-checkout` policy with only `@/lib/paymongo` stubbed (not the policy itself). Case (8) proves an `active` session IS expired exactly once. Case (9) proves a `paid` session gets `expireCheckoutSession` a call count of **ZERO** — read directly: `expect(mockPayMongo.expireCheckoutSession).not.toHaveBeenCalled()` — while the row keeps `status='cancelled'` AND `checkout_session_id='cs_cuh_PAID'` intact, and files `checkout_paid_after_lapse`/`needs_attention`. This is genuinely behavioural, not a call-site read. |
| Call site guards independently | Case (3): `retireBatchMock.mockRejectedValue(new Error("PayMongo is down"))`, and the withdrawal still returns `{ ok: true, refundCents: 0 }` with the row flipped to `cancelled`. I confirmed this maps to the exact defect measured earlier in 13.1-05 (`createPendingHold` throwing a provider outage out to the caller with the slot already freed) — the same shape, now defended at this call site with its own `try/catch`. |
| No `booking` row written by the retire | Case (7) and case (9) both assert `checkout_session_id` survives and `payment_id` stays NULL after the retire runs. I additionally re-read `retire-checkout.ts`'s full body: the module's Rule 2 header states it writes no `booking` row, and the function body contains no `UPDATE booking` anywhere — confirmed by direct read, not by trusting the comment. |
| Post-commit ordering | `cancel-booking.ts`'s flip (line 893) is a bare `db.execute(sql\`UPDATE ...\`)`, not wrapped in `db.transaction(...)`. `retireWithdrawnSession` is called at line 925, after that statement has resolved — genuinely post-commit. `notifyCancellation` runs after the retire (line 929); both are independent of each other (`emitNotify` at `src/lib/notifications.ts:116-130` swallows its own transport errors in its own try/catch), matching the SUMMARY's ordering claim. |

**Independently run test evidence (not trusted from the SUMMARY):** the full 12-case file was read in
full. Cases (1)-(7) prove the wiring against a mocked policy (including case (4)'s discriminator — a
`requested` row carrying a session id, a shape `loadBookingRow`'s 22-column read cannot itself produce,
proving the session id genuinely comes off the flip's own `RETURNING` and not a stale pre-read). Cases
(11)-(12) are the shape-(a)-vs-(b) measurement: case (11) is the guard-the-guard (an untouched lapsed hold
IS selected by the sweep on this schema), and case (12) hands a withdrawn row back its lapsed `expires_at`
(exactly shape (b)'s post-change state) and asserts the sweep still does not select it — this is a genuine
measurement, not a restated assertion, and I confirmed its premise (the sweep's status clause) directly
against the selector.

### Residual 2 — the runbook had no entry for `webhook_missed` / `checkout_paid_after_lapse`

**Verdict: CLOSED. Verified against source, not narration.**

- `.planning/ops/NEEDS-ATTENTION-RUNBOOK.md` now has §4b (`checkout_paid_after_lapse`, lines 188-258) and
  §4c (`webhook_missed`, lines 260-291), plus a "Last revised" line dated 2026-08-22.
- **The correction claim, re-derived independently:** §4b forks the instruction on `meta.bookingStatus`
  BEFORE giving any action. On `approved`, it correctly states `payment-reconcile`'s `status IN
  ('pending','approved')` selector ALSO reaches the row and will confirm it within ~5 minutes, and tells the
  operator to check for confirmation rather than refund immediately. On `cancelled`/`declined`, it correctly
  states this is the ONLY report the payment will ever produce and instructs an unhedged refund. I checked
  this against the actual guard in `retire-checkout.ts` (`if (row.bookingStatus !== "pending")`) and against
  `queryUnconfirmedPaid`'s own `status IN ('pending','approved')` scope — the runbook's fork is accurate. A
  terminal-only framing (what the original brief implied) would indeed have told an operator to refund a
  booking that was about to be confirmed by the reconciler on `approved`. This is a genuine, checked
  correction, not an unverified claim.
- **The five stale pointers, spot-checked byte-exact against source, not sampled:**

  | Action | Runbook says | `grep -n 'action: "<name>"'` says | Match |
  |---|---|---|---|
  | `auto_refund_manual` | `confirm-booking-payment.ts:175` | `confirm-booking-payment.ts:175` | ✓ |
  | `auto_refund_failed` | `confirm-booking-payment.ts:163` | `confirm-booking-payment.ts:163` | ✓ |
  | `refund_after_payout` | `route.ts:212` | `route.ts:212` | ✓ |
  | `refund_manual_required` | `cancel-booking.ts:813, 1329` | `cancel-booking.ts:813, 1329` | ✓ |
  | `refund_dispatch_failed` | `cancel-booking.ts:728, 1317` | `cancel-booking.ts:728, 1317` | ✓ |
  | `checkout_paid_after_lapse` | `retire-checkout.ts:208` | `retire-checkout.ts:208` | ✓ |
  | `webhook_missed` | `payment-reconcile.ts:310` | `payment-reconcile.ts:310` | ✓ |
  | `checkout_retired` | `retire-checkout.ts:258` | `retire-checkout.ts:258` | ✓ |
  | `checkout_expire_failed` | `booking.ts:660,915` + `retire-checkout.ts:247` | same | ✓ |

  All nine pointers I checked (the two the SUMMARY calls "fatal," plus every other action name touched by
  this phase) resolve exactly. The claim that `auto_refund_manual`/`auto_refund_failed` previously pointed
  at `api/paymongo/webhook/route.ts` (which 13.1-01 emptied of those calls) is consistent with this phase's
  own earlier commits moving `handleGoneSlot` into `confirm-booking-payment.ts`.
- `webhook_missed` is correctly documented as informational (§4c: "there is nothing to refund, re-charge or
  re-send") — consistent with `payment-reconcile.ts`'s `reconcileOne` writing that action only on a genuine
  `"confirmed"` transition, after the confirm has already run through the one confirm path.

### Residual 3 — "Inngest must re-sync" only in code comments

**Verdict: CLOSED. Verified against source, not narration.**

`.planning/ops/DEPLOY-CHECKLIST.md` is new (167 lines) and was checked field-by-field against
`src/app/api/inngest/route.ts` and each function file:

- All **nine** registered function ids in the checklist's §1 table match `route.ts`'s `functions: [...]`
  array exactly (`payoutSweep, payoutReconcile, paymentReconcile, checkoutRetireSweep, requestExpirySweep,
  notify, guestEmail, remindersSweep, opsAlertDigest`) — I read the array directly, not the checklist's
  claim about it.
- Every cron string in the checklist matches the source exactly, re-derived rather than trusted:
  `payment-reconcile` → `TZ=Asia/Manila 2-59/5 * * * *` (`RECONCILE_START_MINUTE=2`,
  `RECONCILE_INTERVAL_MINUTES=5` in `payment-reconcile.ts`); `checkout-retire-sweep` →
  `TZ=Asia/Manila 4-59/5 * * * *` (`RETIRE_START_MINUTE=4`, `RETIRE_INTERVAL_MINUTES=5` in
  `checkout-retire.ts`); the other seven crons/events also match their own files verbatim.
- Both 13.1 guarantee-critical crons are named explicitly (bolded, first two rows of the table), each with
  a "What silence costs" column stating the specific failure mode this phase exists to close.
- **Registered vs. ticking is a real, separately-stated distinction** (§2, "Check 1 — REGISTERED" /
  "Check 2 — TICKING"), with a concrete verification method (the Inngest dashboard Runs view, "at least one
  completed run within ~10 minutes of a deploy") and an explicit non-alarming baseline
  (`{ "scanned": 0, "outcomes": {} }` documented as healthy, not broken) — this is exactly the kind of
  actionable, non-ambiguous instruction the original residual C called for.
- The checklist is honest about its own limits (§5: no hosting/rollback/smoke-test/monitoring coverage, and
  "this file is not enforced by any test").

### Regression checks (the other half of this task)

| Check | Command / method | Result | Status |
|---|---|---|---|
| Exactly one writer of `status = 'confirmed'` | `grep -rn "status = 'confirmed'" src/` | Every hit is either a read predicate (`WHERE`/`AND status = 'confirmed'`, `CASE WHEN`, an `OPEN_OCCUPYING_STATUS_SQL` read) or a comment referencing the rule; the sole `SET status = 'confirmed'` write is `src/lib/payments/confirm-booking-payment.ts:303` | ✓ HOLDS |
| THE TRAP — retire writes no `booking` row | Read `retire-checkout.ts` in full; test cases (7) and (9) of the new file re-assert `checkout_session_id` survives and `payment_id` stays NULL after a retire | No `UPDATE booking` anywhere in `retire-checkout.ts`; behaviourally confirmed | ✓ HOLDS |
| Reconcile sweep candidate set still `pending`/`approved` | Read `queryUnconfirmedPaid` in `payment-reconcile.ts:206-214` | `WHERE status IN ('pending', 'approved') AND checkout_session_id IS NOT NULL ...` — unchanged | ✓ HOLDS |
| D-104 — no path confirms on `?paid=1` | Read `src/app/(app)/bookings/[id]/page.tsx`'s `paid` usages | `paid === "1"` selects only which UI branch renders (`paidParamDecay`, and `showConfirmationMoment` additionally requires `bk.status === "confirmed"` read from the DB); no write path reads it. File is untouched by 13.1-06 (not in its key-files list) | ✓ HOLDS |
| D-108 — `REFUNDABLE_RAILS` not widened | `git diff --stat origin/dev -- src/lib/payments/refund-rail.ts` | Empty | ✓ HOLDS |
| D-112 — zero migrations added | `ls drizzle/*.sql \| tail -3` and count | Ends at `0025_audit_resolved_by.sql`, 26 files total, unchanged | ✓ HOLDS |
| D-111 fixtures, queried directly against the dev DB | `docker exec fitout-db-1 psql -U fitout -d fitout -c "SELECT id, status, payment_id, payment_method, checkout_session_id FROM booking WHERE id IN ('09f32400-b636-46e1-973a-b930a073a936','408e054a-cb5a-4dbb-9282-9883e0bfc628')"` — run BOTH before and after the full test suite | Both fixtures: `status='pending'`, `payment_id` and `payment_method` empty, `checkout_session_id` intact (`cs_9a1dd8b754ba590de5818784` / `cs_a15f614539f1fb880d0636c2`) on both queries | ✓ HOLDS |
| `npm test`, run standalone (not concurrent with anything) | `npm test` | **1699 passed \| 5 skipped** (168 files passed, 2 skipped) — exactly matches 13.1-06-SUMMARY's claimed count, and is exactly baseline (1687\|5) **+12**, the file's own case count | ✓ MATCHES CLAIM, INDEPENDENTLY RUN |
| `npm run test:design`, run standalone afterward | `npm run test:design` | **816 passed \| 3 skipped** (48 files) — byte-identical to baseline and to the SUMMARY's claim | ✓ MATCHES CLAIM, INDEPENDENTLY RUN |
| Type check | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| Debt markers on all 5 touched/created files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` on `cancel-booking.ts`, `retire-checkout.ts`, the new test file, the runbook, the deploy checklist | No matches in any file | ✓ CLEAN |
| Referenced commits exist and match their claims | `git show --stat 4514f19 b0775e7 88fb2e2` | All three exist, correct author/date, commit messages match the SUMMARY's residual attribution (Residual 1 → `4514f19`, Residual 3 → `b0775e7`, Residual 2 → `88fb2e2`) | ✓ VERIFIED |

**No regressions found.** Every invariant the task asked me to re-confirm independently still holds, checked
against source and the live dev database, not against the SUMMARY's own claims.

### STATE.md / ROADMAP.md residue check

Checked both files directly, as the task required, because `gsd-sdk` has a documented history on this
project of over-marking phases complete.

- **`.planning/STATE.md`**: `status: verifying`. `stopped_at` still narrates 13.1-05 as the last executed
  plan and does not mention 13.1-06 at all — this is **stale, not false**. It makes no completion claim; it
  still correctly says "the phase is ready for VERIFICATION (phase closure is the verifier's call, not a
  plan's)." No inflated counters, no premature "Complete" marker found for Phase 13.1.
- **`.planning/ROADMAP.md`**: the Phase 13.1 plan checklist lists only `[x] 13.1-01` through `[x] 13.1-05`
  — **13.1-06 has no entry at all**, and the summary table row still reads "5/5 — All plans EXECUTED —
  awaiting phase verification," with no ✅ and no completion date.
- **Conclusion: no residue this pass either.** This is consistent with 13.1-06-SUMMARY's own statement that
  "`gsd-sdk` state verbs were not run. STATE.md and ROADMAP.md are untouched by this plan." Both files will
  need a manual update once phase closure is decided (adding 13.1-06 to the plan list at minimum) — that
  update is not part of this verification and was not made here, since this report's job is to verify, not
  to write phase-state.

### Honest accounting of what is now proven vs. what still rests on a single live run

**Proven, independently, against the actual codebase and a live test/DB run (not SUMMARY claims):**
- All three targeted residuals are shipped, behaviourally tested (not just call-site-read), and match every
  specific claim in 13.1-06-SUMMARY.md that I checked, including the "two independent exclusions" technical
  claim, the five corrected runbook pointers (checked byte-exact, not sampled), and the nine-function/cron
  cross-reference in the deploy checklist.
- Zero regressions across every invariant the task named: single confirm writer, THE TRAP, sweep candidate
  scope, D-104/D-108/D-111/D-112, and both test gates run standalone and matching exactly.
- The two D-111 evidence fixtures are untouched, queried directly against the dev database both before and
  after the full suite ran.

**Still resting on a single run / not independently re-executed by me:**
- I did not re-run `npx vitest run tests/booking/cancel-unpaid-hold-retires-session.test.ts` in isolation —
  its 12 cases were exercised as part of the full `npm test` run (1699\|5, +12 exactly matches this file's
  case count), which is direct evidence they ran and passed, but I read the file's assertions myself rather
  than re-deriving pass/fail case-by-case from a second isolated run.
- I did not re-run the live-flagged PayMongo probe test
  (`RUN_LIVE_PAYMONGO_PROBE=1 npx vitest run tests/paymongo/lapsed-hold-not-payable-real.test.ts`) — it is
  untouched by 13.1-06 and was already verified live in the original pass; re-running it would mint another
  real PayMongo session for no incremental evidence value, consistent with the original verification's own
  reasoning.

**Genuinely still open — not closed by this plan and not part of what was approved for closure here:** the
`payments[0].id` envelope-vs-`attributes` placement remains unverified against a real paid PayMongo session.
This was explicitly out of scope for 13.1-06 (its own "Deferred Issues" section says so), predates this
phase (open since 13-03), and its cost of being wrong is bounded (`paymentId: null`, not a regression). Per
the task's own framing, this is the same category as Phase 13's own outstanding UAT walks and
`SUPPORT_EMAIL` — an item open by explicit PM scheduling decision, not a defect, and it does not undermine
the finding that all three targeted residuals are genuinely closed.

**Overall re-verification status: `human_needed`, unchanged in category from the original pass, but
narrowed from 3 open items to 1.** The one remaining item is not new, not a regression, and was never part
of what this closure plan was asked to fix. If the PM's intent was for the phase to be considered fully
closeable now with that one live-UAT item tracked separately (exactly how Phase 13 was treated), that is a
legitimate reading of the evidence above — the call on whether that satisfies "phase closed" is the PM's,
consistent with the original report's own escalation-gate framing.

---

## ORIGINAL VERIFICATION (2026-08-22, initial pass — not re-litigated, reproduced for context)

### Summary Judgment

**All 5 roadmap Success Criteria and the D-113 hold-expiry guarantee are independently verified true in
the codebase, not merely claimed in SUMMARY.md.** I re-derived the confirm path, the two sweeps, the
inline reclaim wirings, and the D-58/D-105/D-106/D-108/D-111/D-112 invariants directly from source, ran
the full test suite and the design gate myself (not trusting the SUMMARY's numbers), and queried the dev
database directly for the two D-111 evidence fixtures. Every claim I checked held.

**But three residual items are real, and none of them were silently hidden — every one is disclosed in
`deferred-items.md` or was found independently by me and cross-checked against it.** They do not fail any
of the 5 formally-stated Success Criteria or D-113 as literally worded, which is why this report does not
mark any must-have FAILED. They do sit inside the phase's own broader promise ("never a silent loss"), so
I am routing them to a human decision rather than declaring the phase unconditionally closed.

### Goal Achievement — Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — A booking PayMongo records paid always reaches `confirmed` on a schedule, not only when a human is watching | ✓ VERIFIED | `src/inngest/functions/payment-reconcile.ts` exists, is registered in `src/app/api/inngest/route.ts:84`, and its candidate query (`queryUnconfirmedPaid`) is scoped to `status IN ('pending','approved')`. Cron `TZ=Asia/Manila 2-59/5 * * * *`, derived from the same constant the cadence test asserts against. |
| 2 | SC2 — Reconciliation routes through the SAME idempotent confirm path the webhook uses; concurrency proof exists and could fail | ✓ VERIFIED | `grep -rn "status = 'confirmed'" src/` returns exactly ONE write: `src/lib/payments/confirm-booking-payment.ts:303`. `tests/payments/confirm-idempotency.test.ts` case (2) races two INDEPENDENT Postgres connections over one row. |
| 3 | SC3 — A missed webhook is LOUD: an operator alert records it, server-side only | ✓ VERIFIED (mechanism); ⚠ runbook gap noted (now closed, see re-verification above) | `reconcileOne` writes `recordAudit({ action: "webhook_missed", outcome: "needs_attention" })` only on a genuine transition. `listUnresolvedAlerts` reaches it generically. |
| 4 | SC4 — PROJECT D-57 unweakened: a server-side probe is authoritative, a URL parameter is not | ✓ VERIFIED | `reconcile-payment.ts` reads no `searchParams`. `paid === "1"` selects only a UI branch, never combined with a write. |
| 5 | SC5 — No double-charge, double-confirm, or resurrection of a gone slot; D-58 backstop still governs | ✓ VERIFIED | The confirm's `WHERE id = $1 AND status IN ('pending','approved')` is byte-identical to pre-phase logic. `REFUNDABLE_RAILS` never widened. |
| 6 | D-113 (phase goal, 3rd sentence) — when a hold EXPIRES, the ability to pay expires with it | ✓ VERIFIED | `retire-checkout.ts` (probe-first, never expires a `paid` session, structurally cannot throw) + `checkout-retire.ts` (the sweep, registered, correct candidate set) + three inline reclaim wirings, all post-commit. |

**Score:** 6/6 truths verified. 0 BLOCKER-level failures.

### Residual Risks (original disposition — see re-verification section above for closure status)

| # | Risk | Disposition at original pass | Disposition NOW |
|---|------|-------------------------------|-------------------|
| A | `cancelUnpaidHold` orphaned a payable session with no backstop | Routed to human decision (item 1) | **CLOSED**, see re-verification |
| B | Runbook missing `webhook_missed` / `checkout_paid_after_lapse` | Routed to human decision (item 2) | **CLOSED**, see re-verification |
| C | "Inngest must re-sync" only in code comments | Noted for awareness, folded into item 2's scope | **CLOSED**, see re-verification (`DEPLOY-CHECKLIST.md`) |
| D | `payments[0].id` placement not live-verified against a real paid session | Routed to human decision (item 3) | **STILL OPEN** — not part of this closure plan, PM-scheduled live-UAT item |
| E | `handleGoneSlot`'s alert re-fires on an already-terminal row (measured, unreachable in production) | Correctly recorded as intentional non-fix, no action needed | Unchanged, no action needed |

### Required Artifacts (original pass)

| Artifact | Expected | Status |
|----------|----------|--------|
| `src/lib/payments/confirm-booking-payment.ts` | D-105's single confirm path | ✓ VERIFIED |
| `src/inngest/functions/payment-reconcile.ts` | The 5-minute reconciliation sweep | ✓ VERIFIED |
| `src/app/actions/reconcile-payment.ts` | The settling-screen fast path | ✓ VERIFIED |
| `src/lib/payments/retire-checkout.ts` | D-113's retire policy | ✓ VERIFIED (and re-confirmed in this pass with a fifth call site) |
| `src/inngest/functions/checkout-retire.ts` | D-113's sweep (the guarantee) | ✓ VERIFIED |
| `src/lib/availability/units.ts` (2 call sites) | Post-commit retire wirings | ✓ VERIFIED |
| `src/inngest/functions/request-expiry.ts` | Post-commit retire wiring | ✓ VERIFIED |
| `tests/payments/confirm-idempotency.test.ts` | Concurrency proof | ✓ VERIFIED |
| `tests/paymongo/lapsed-hold-not-payable-real.test.ts` | Live PayMongo proof | ✓ VERIFIED (present, correct skip behavior) |

### Behavioral Spot-Checks — original pass (re-run and re-confirmed in this pass; see regression table above)

| Behavior | Command | Original Result | This Pass |
|----------|---------|--------|--------|
| Full test suite | `npm test` | 1687 passed \| 5 skipped | **1699 passed \| 5 skipped** (+12, matches 13.1-06's new cases) |
| Design gate | `npm run test:design` | 816 passed \| 3 skipped | **816 passed \| 3 skipped** (unchanged) |
| Type check | `npx tsc --noEmit` | exit 0 | exit 0 |
| D-112 migration pin | `0025_audit_resolved_by.sql`, 26 files | unchanged | unchanged |
| D-111 fixtures | both `pending`, NULL `payment_id`/`payment_method` | unchanged | unchanged (checked before AND after this pass's full suite run) |

### Requirements Coverage

Not applicable. All plans declare `requirements: []`; the ROADMAP explicitly states requirements are TBD for
this phase. No orphaned requirements found.

### Anti-Patterns Found

None blocking, in either pass. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file
touched across the whole phase, including the five files 13.1-06 created/modified (independently re-scanned
in this pass). The one deliberately-unfixed defect (`handleGoneSlot`'s alert non-idempotence on a `cancelled`
row) remains exhaustively documented and proven unreachable in production.

### Gaps Summary

**No must-have truth has ever failed in this phase, across either verification pass.** The original pass
found three real, disclosed residuals and routed them to a human/PM decision rather than blocking on them.
This re-verification independently confirms all three are now genuinely closed — not merely claimed closed
— at every level (artifact exists, is substantive, is wired, and is behaviourally proven where the claim
was a behavioural one), with zero regressions to any previously-verified invariant. One item (`payments[0].id`
live capture) remains open, exactly as it was before this closure plan ran, because it was never part of
what the PM approved for closure and cannot be closed without a real PayMongo payment.

---

_Original verification: 2026-08-22T17:00:00Z_
_Re-verification: 2026-08-22T18:15:00Z_
_Verifier: Claude (gsd-verifier)_

---

## Addendum — the last open item is CLOSED (coordinator, 2026-08-22)

The re-verification left exactly one item open: `payments[0].id`'s **envelope-vs-`attributes`** placement,
unverified against a real paid PayMongo session (open since 13-03, cost bounded at `paymentId: null`).

**Settled by probing a live paid session** — fixture `09f32400-…`'s own
`cs_9a1dd8b754ba590de5818784`, `sk_test_`, `HTTP 200`, payment `status: paid`:

```
payments[0].id            : pay_YoQGNw54hNjtB5pxXu7KpeFZ    ← PRESENT
payments[0].attributes.id : (absent)                         ← ABSENT
```

**The envelope form is the real one; the `attributes` form does not exist on live data.** The original
plan text specified `payments[0].attributes.id`; 13.1-01 measured it returning `null`, shipped an
envelope-first read, and recorded the collision. That decision is now confirmed against the provider
rather than against a fixture.

⚠ Consequence had it shipped as written: a probe-driven confirm would have landed `payment_id = NULL`, and
`isApiRefundable` fails closed — so **every later cancellation refund on a reconciled booking would have
been judged unrefundable and routed to manual return, permanently.**

The probe is read-only. Both D-111 fixtures remain `pending` / NULL `payment_id` / session intact.

**Phase 13.1 has no remaining open verification items.** Phase 13's two UAT walks and `SUPPORT_EMAIL`
are Phase 13's, outstanding by the PM's own scheduling decision, and are not defects of this phase.
