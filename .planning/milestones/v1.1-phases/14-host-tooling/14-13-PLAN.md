---
phase: 14-host-tooling
plan: 13
type: execute
wave: 6
depends_on: ["14-01", "14-12"]
files_modified:
  - src/components/availability/blocks-editor.tsx
  - src/app/(host)/host/listings/[id]/availability/page.tsx
  - src/app/(host)/host/listings/[id]/availability/loading.tsx
  - tests/design/card-pattern-coverage.test.ts
  - tests/design/empty-state-adoption.test.ts
  - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
autonomous: true
requirements: [HFLOW-04]
must_haves:
  truths:
    - "Both availability editors have now LEFT the raw-card allow-list and both rows are deleted; the exemptions the pattern layer held open for this phase are spent (D-155)"
    - "The blocked-dates absence is a genuine empty LIST and takes the shared empty-state pattern; the hours guidance box was an advisory about a form that is fully present and correctly did not"
    - "The add-block overlay moves to the app's ONE overlay primitive, which is the second half of the deferred pair — and no sheet block is fetched"
    - "The availability page carries the shared panel shell and the shared header pattern, and its two bare advisory paragraphs become the declared advisory surface — copy byte-identical, with the hours-lock sentence still composed from the shared constant"
    - "The host listing tile STAYS as it is and the pattern layer's spec is corrected instead: a management tile carrying four controls cannot become one whole-card anchor without nested-interactive problems, and no fourth container is added"
    - "The pattern count stays at three (DS-11) and no allow-list row is added for anything"
  artifacts:
    - path: "src/components/availability/blocks-editor.tsx"
      provides: "the empty-state adoption, the panel-pattern list, and the add-block overlay on the shared responsive primitive"
    - path: "src/app/(host)/host/listings/[id]/availability/page.tsx"
      provides: "the shared shell, the shared header pattern, and the two advisories on the declared advisory surface"
    - path: "tests/design/empty-state-adoption.test.ts"
      provides: "the adopter and call-site counts re-measured in the same commit as the adoption"
    - path: ".planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md"
      provides: "the corrected replaces-list, discharging the deferred fork with the reason it was decided that way"
  key_links:
    - from: "src/components/availability/blocks-editor.tsx"
      to: "src/components/patterns/responsive-dialog.tsx"
      via: "the add-block overlay ported off the vendored dialog onto the one overlay primitive"
      pattern: "ResponsiveDialog"
    - from: "src/app/(host)/host/listings/[id]/availability/page.tsx"
      to: "HOURS_LOCKED_MESSAGE"
      via: "the lock notice still composed from the shared constant, never retyped"
      pattern: "HOURS_LOCKED"
---

<objective>
Finish the availability route: the blocks editor stops opening raw boxes and raw overlays, the page gets the
shared shell and header, and the pattern layer's one incorrect line about the host listing tile is corrected
rather than obeyed.

Purpose: D-155 spends the second of the two exemptions the pattern layer held open for this phase, and an
allow-list row for a file with no raw card left is not harmless — it goes on quietly licensing the next box
somebody adds. The page's two bare advisory paragraphs are the last place on this route where an advisory has
no declared surface. And the deferred fork about the host listing tile is decided here, in writing, so a
later phase finds a decision rather than an open question.

Output: two editors out of the allow-list, a route wearing the shared shell and header, one overlay primitive
across the whole route, and one corrected sentence in an earlier phase's spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-host-tooling/14-CONTEXT.md
@.planning/phases/14-host-tooling/14-UI-SPEC.md
@.planning/phases/14-host-tooling/14-RESEARCH.md
@.planning/phases/14-host-tooling/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The blocks editor adopts the empty state, the panel and the one overlay primitive</name>
  <read_first>
    - `src/components/availability/blocks-editor.tsx` — the whole file (457 lines). Specifically :48 (the raw
      card import — DELETED), :50-57 (the direct vendored-dialog imports — REPLACED), :116-124 (the
      no-blocked-dates box — a genuine empty LIST), :126-147 (the block list — a dense date list), :174 and
      onward (the add-block overlay), :319 (an alert-role validation message — read it, do not change it
      here; its census and pin are plan 14-14's).
    - `src/components/patterns/empty-state.tsx:81-115` (the discriminated props; `actions` is REQUIRED and a
      caller with nothing to offer passes null on purpose) and :116-153 (the render).
    - `src/components/patterns/panel-card.tsx:48-84`.
    - `src/components/patterns/responsive-dialog.tsx:150-201` — the props; the title is required for the
      accessible-name rule, the footer renders through the vendored footer, and the close-auto-focus handler
      is left undefined when the trigger is stable.
    - `src/components/booking/booking-sticky-bar.tsx:187-202` — the only real call site to copy the API from.
    - `src/components/host/request-row.tsx` as 14-03 left it — the first host-side adopter, and the second
      half of the deferred overlay pair.
    - ⚠ `tests/design/empty-state-adoption.test.ts:447` (the adopter count), :448 (the call-site count), both
      asserted at :784-785, and the file's own not-covered footer at :218-222, which says in as many words
      that a Phase-14 host surface is EXPECTED to extend the adopter list in its own commit.
    - ⚠ `tests/design/card-pattern-coverage.test.ts` as 14-12 left it — the second allow-list row is deleted
      here and the surface count re-measured again.
    - `tests/design/sheet-absent.test.ts` — it stays green; no sheet block is fetched.
    - 14-UI-SPEC § The Availability Page's Design-System Pass — the per-site table.
  </read_first>
  <files>src/components/availability/blocks-editor.tsx, tests/design/card-pattern-coverage.test.ts, tests/design/empty-state-adoption.test.ts</files>
  <action>
    Replace the no-blocked-dates box with the shared empty-state pattern — copy byte-identical, a calendar-off
    glyph, and null actions, because the add-block control is already adjacent and above. This IS a genuine
    empty list, unlike the hours editor's guidance box, which was an advisory about a form that is fully
    present.

    Replace the block list's raw card with the declared panel pattern, preserving the dividing rule and the
    list's zero inner padding. NOT a row card per block: a dense date list is a panel's contents, and one
    panel per list is what the pattern is for.

    Port the add-block overlay onto the shared responsive primitive, preserving the shipped confirm content
    and copy byte-for-byte. Delete the direct vendored-dialog imports and the raw card import.

    Write the file header the way the request-row header is written: the CONTAINERS changed and nothing else
    about this file moved.

    ⚠ In the SAME commit: delete this file's row from the raw-card allow-list and re-measure the declared
    surface count in both places it is asserted; and extend the empty-state adopter list and re-measure both
    the adopter count and the call-site count. Every added row carries a reason longer than the gate's
    minimum. Add no allow-list row for anything. The pattern count stays at three.

    ⚠ Confirm — and state in the SUMMARY — that the host listing tile KEEPS its allow-list row with its
    measured reason. That row is not spent by this phase.

    Do not change the block CRUD, the reason mapper or the validation message.
  </action>
  <verify>
    <automated>npm run test:design -- card-pattern-coverage empty-state-adoption sheet-absent leak && npx vitest run tests/availability && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/components/ui/card"\|from "@/components/ui/dialog"' src/components/availability/blocks-editor.tsx`
      returns 0.
    - `grep -n 'blocks-editor' tests/design/card-pattern-coverage.test.ts` shows it in the declared adopters
      and NOT in the allow-list; a grep of the allow-list for either availability editor returns nothing.
    - `grep -n 'listing-card' tests/design/card-pattern-coverage.test.ts` still shows the host tile's
      allow-list row, with its reason unchanged.
    - `npm run test:design -- card-pattern-coverage empty-state-adoption` passes with all four counts
      re-measured in this commit. Report old and new values for each in the SUMMARY.
    - `npm run test:design -- sheet-absent` passes with its file unedited; `git diff components.json` empty.
    - `npx vitest run tests/availability` passes — the block CRUD and the shared schema are untouched.
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>Both availability editors are out of the allow-list, and the whole route uses one overlay primitive and the three declared containers.</done>
</task>

<task type="auto">
  <name>Task 2: The availability page's shell, header and two advisories</name>
  <read_first>
    - `src/app/(host)/host/listings/[id]/availability/page.tsx` — the whole file (200 lines). Specifically
      :100-101 (the hours-lock state), :105-109 (the initial windows normalisation), :119-134 (the city
      label, the offset label and the assembled lock notice), :137 (the hand-typed shell with its own
      vertical rhythm), :140 (the toaster, mounted ONCE at the shared ancestor — leave it exactly where it
      is), :142-176 (the hand-rolled heading, the lede, the drop-in note at :155-160 and the lock notice at
      :168-175), :178-186 and :188-197 (the two sections and their second-level headings).
    - `src/app/(host)/host/listings/[id]/availability/loading.tsx` — the whole file.
    - `src/app/(host)/host/requests/page.tsx:132-137` — the shell-plus-header shape to copy.
    - `src/components/patterns/page-header.tsx` and `panel-card.tsx`.
    - `src/lib/design/measurements.ts` — `HOST_PANEL_SHELL`.
    - `HOURS_LOCKED_MESSAGE` — find its declaration. ⚠ GATE-NOREG 7: the notice is still COMPOSED from the
      shared constant, still names weekdays and a date and never a booker, and still offers the way out.
    - 14-UI-SPEC § The Availability Page's Design-System Pass — the page shell, page heading and advisory
      rows — and § Copywriting → Availability.
  </read_first>
  <files>src/app/(host)/host/listings/[id]/availability/page.tsx, src/app/(host)/host/listings/[id]/availability/loading.tsx</files>
  <action>
    Replace the hand-typed shell on the page and its plate with the shared panel-shell constant plus the
    section rhythm.

    Replace the hand-rolled heading and lede with the shared header pattern carrying the shipped title and
    the shipped lede, at the same heading size every other host page uses.

    Turn the two bare advisory paragraphs — the drop-in note and the hours-lock notice — into the declared
    advisory surface at the muted tone, copy byte-identical. The lock notice is still assembled from the
    shared constant and its per-listing extension; do not retype a sentence, a weekday, a date or a duration.

    Leave the toaster mounted exactly where it is — once, at the shared ancestor.

    Leave both section headings and both sections where they are. Do not reorder the strip, the hours editor
    or the blocks editor.
  </action>
  <verify>
    <automated>npm run test:design -- card-pattern-coverage loading-coverage type-scale && npx vitest run tests/availability && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'HOST_PANEL_SHELL' 'src/app/(host)/host/listings/[id]/availability/page.tsx' 'src/app/(host)/host/listings/[id]/availability/loading.tsx'`
      returns one hit in each, and a grep for the old hand-typed shell string returns nothing in either.
    - `grep -c 'PageHeader' 'src/app/(host)/host/listings/[id]/availability/page.tsx'` is at least 1 and no
      hand-rolled top-level heading element remains.
    - `grep -c 'Toaster' 'src/app/(host)/host/listings/[id]/availability/page.tsx'` returns 1, on the same
      element as before.
    - `grep -c 'HOURS_LOCKED' 'src/app/(host)/host/listings/[id]/availability/page.tsx'` is unchanged from
      before this task, and no weekday name, date format or duration is typed into the file.
    - `npm run test:design -- loading-coverage` still pins 29 / 21 / 8 with zero edits.
    - `npx vitest run tests/availability` passes, including the hours-lock coverage (GATE-NOREG 7).
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>The route reads as the same product as the booker side, and every advisory on it sits on the declared advisory surface.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the pattern layer's replaces-list rather than obey it</name>
  <read_first>
    - `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md` — find the result-card
      pattern's "Replaces" list and the line naming the host listing tile.
    - `.planning/phases/11-quality-gates-pattern-layer-app-shell/deferred-items.md:74` — the deferred fork
      this discharges: either drop the tile from the replaces list, or make it one whole-card anchor and move
      its three controls off the card.
    - `src/components/listing/listing-card.tsx` — enough to confirm it is a MANAGEMENT tile carrying four
      controls, and that it keeps its own local confirm overlay (it is NOT named for conversion in this
      phase).
    - `tests/design/card-pattern-coverage.test.ts` — the host tile's allow-list row and its measured reason.
    - 14-CONTEXT § Claude's Discretion — the decision and its argument.
    - 14-UI-SPEC Open Question 7 — the same decision, recorded.
  </read_first>
  <files>.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md</files>
  <action>
    Drop the host listing tile from the result-card pattern's replaces list and record, in one or two
    sentences beside the change, WHY: it is a management tile carrying four controls, and a tile that is one
    whole-card anchor cannot hold interactive children without nested-interactive accessibility problems.
    State that this is a SPEC CORRECTION, not a container swap, and that no fourth container is added — the
    pattern count still says three, and the allow-list already carries the tile with its measured reason.

    Do not modify `src/components/listing/listing-card.tsx`. Do not modify its allow-list row. Do not convert
    its local confirm overlay — it is not named for conversion in this phase.

    Also record, in the same edit or in this plan's SUMMARY, that the deferred fork at the pattern layer's
    item is now CLOSED by option (a), so a later phase finds a decision rather than an open question.
  </action>
  <verify>
    <automated>npm run test:design -- card-pattern-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `git diff src/components/listing/listing-card.tsx` is EMPTY.
    - `grep -n 'listing-card' .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md` no
      longer shows the tile inside the result-card replaces list, and the correction's reason is present in
      the file.
    - `npm run test:design -- card-pattern-coverage` passes; the pattern count is still three and the tile's
      allow-list row is unchanged.
    - The SUMMARY states that the pattern layer's deferred fork is closed by option (a), with the reason.
  </acceptance_criteria>
  <done>The deferred fork is closed in writing, and the tile that a future phase might have swapped is left exactly as it is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host session → the availability route | The page's own session and host-capability re-check and its owner-scoped listing read are not opened by a restyle |
| absence → failure | An unblocked calendar and an unset week are normal states of a working listing |
| allow-list row → future boxes | A row for a file with no raw card left goes on licensing the next hand-rolled box |
| overlay primitive → small viewports | A confirm the host cannot reach on a phone is a functional failure, not a styling one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13-ALLOWLIST | Tampering | the raw-card allow-list | mitigate | The second row is deleted in the same commit as the swap and both counts re-measured; a grep proves neither availability editor remains in the list |
| T-14-13-UNREACHABLE | Denial of service | the add-block confirm | mitigate | Ported to the shared responsive primitive, which owns the small-viewport presentation; `sheet-absent` stays green with zero edits |
| T-14-13-FALSEALARM | Denial of service | the empty blocks list | mitigate | The shared empty state with null actions and no retry; the adopter and call-site counts move as part of the work |
| T-14-13-LOCKCOPY | Repudiation | the hours-lock notice | mitigate | Still composed from the shared constant; no weekday, date or duration is typed into the page, and `tests/availability` re-runs the lock coverage (GATE-NOREG 7) |
| T-14-13-CRUD | Tampering | block create and delete | mitigate | The CRUD paths and the reason mapper are not opened; `tests/availability` passes unedited |
| T-14-13-TILE | Tampering | the host listing tile | accept | Deliberately unchanged; a whole-card anchor holding four controls is a nested-interactive defect, so the spec is corrected instead. `git diff` on the tile is empty |
| T-14-13-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm run test:design` green; `card-pattern-coverage`, `empty-state-adoption`, `sheet-absent`, `leak`,
  `loading-coverage` and `type-scale` all pass.
- `npx vitest run tests/availability tests/listing` — zero failures.
- `npx tsc --noEmit` exit 0.
- `git diff --stat drizzle/` empty; `git diff components.json` empty.
- If an end-to-end run is used, name at most three spec files; never bare.
</verification>

<success_criteria>
- Both availability editors are out of the raw-card allow-list and both rows are deleted.
- The blocked-dates absence is the shared empty state and the add-block overlay is the shared primitive.
- The route's page and plate share one shell and one header, and both advisories sit on the declared surface.
- The pattern layer's replaces-list is corrected and the deferred fork is closed in writing.
</success_criteria>

<output>
Create `.planning/phases/14-host-tooling/14-13-SUMMARY.md` when done.
</output>
