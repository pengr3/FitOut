---
phase: 14-host-tooling
plan: 16
type: execute
wave: 9
depends_on: ["14-08", "14-11", "14-13", "14-15"]
files_modified:
  - e2e/host-headings.spec.ts
  - e2e/overflow-320.spec.ts
  - tests/design/host-tone-census.test.ts
  - src/lib/design/visual-baselines.ts
autonomous: true
requirements: [HFLOW-01, HFLOW-02, HFLOW-03, HFLOW-04, HFLOW-05]
must_haves:
  truths:
    - "Exactly ONE first-level heading renders per document on all five surfaces, in every state and both occupancy modes, and all five compute the same size"
    - "Every Phase-14 surface holds the 320px floor: no horizontal overflow, every control at or above the target floor, every primary action at or above the touch floor, and visible focus from the one canonical recipe"
    - "The alarm token gains ZERO new occurrences on the host and availability trees — and that claim is written as a PINNED PER-FILE MAP, because the measured baseline is eight spellings across four files and a zero-count scan would go red on its first run"
    - "Nine visual baselines are DECLARED with per-surface reasons and marked blocked; the visual project does not exist on this machine and a baseline captured against a live clock is the failure the config's own note exists to prevent"
    - "This phase adds no route, so the loading-coverage pins do not move; and `drizzle/` is unchanged, which is an executable check and not an assurance (PROJECT D-136)"
    - "Earnings and payouts carried tokens only and nothing else, which the string-literal freeze from plan 14-01 proves at phase close (D-156)"
  artifacts:
    - path: "e2e/host-headings.spec.ts"
      provides: "one first-level heading per document across the five surfaces, every state, both modes, with size equality"
    - path: "e2e/overflow-320.spec.ts"
      provides: "the five Phase-14 surfaces added to the route sweep"
    - path: "tests/design/host-tone-census.test.ts"
      provides: "the alarm-token census as a pinned per-file map over the three named trees"
    - path: "src/lib/design/visual-baselines.ts"
      provides: "nine declared court-only rows with per-surface blocking reasons"
  key_links:
    - from: "e2e/overflow-320.spec.ts"
      to: "the five Phase-14 routes"
      via: "the existing route table extended rather than a second sweep authored"
      pattern: "ROUTES"
    - from: "src/lib/design/visual-baselines.ts"
      to: "scripts/seed-baseline-fixtures.ts"
      via: "each blocked row names the fixture it waits on, so the inventory is something to work from"
      pattern: "blocked"
---

<objective>
Close the phase's five hard gates across all five surfaces at once: one heading per document, the 320px
floor, an honest alarm-token census, and a declared baseline inventory that says what is blocked and why.

Purpose: each of the five surfaces was restyled by its own plan, and each proved its own claims. What no
individual plan can prove is the CROSS-SURFACE ones — that all five headings are the same size, that all five
hold the 320px floor, and that the phase as a whole added no alarm colour to a set of calm workflow surfaces.
The baseline inventory is the fifth gate's honest form: the visual project is Linux-only and does not exist
on this machine, so declaring nine rows with per-surface reasons is a thing somebody can work from, and
minting a baseline against a live clock is the failure the config's own note exists to prevent.

Output: two end-to-end sweeps, one census gate, and nine declared rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-host-tooling/14-CONTEXT.md
@.planning/phases/14-host-tooling/14-UI-SPEC.md
@.planning/phases/14-host-tooling/14-RESEARCH.md
@.planning/phases/14-host-tooling/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One heading per document, and the 320px floor on all five surfaces</name>
  <read_first>
    - `e2e/overflow-320.spec.ts` — the whole file. Its route-row shape at :340-360 (name, path, skip reason,
      tell), its target floor at :554, its control-collection and accessibility-exception handling at
      :560-600, and the host route it already carries at :348. ⚠ EXTEND its route table rather than
      duplicating the sweep.
    - `e2e/mode-switch.spec.ts:16-37` — the unique-email helper and the host sign-up drive.
    - `e2e/shell.spec.ts:168-182` — the host sign-up helper, and :385-405, the host header geometry the shell
      spec already pins per width. That geometry is UNTOUCHED by this phase.
    - `src/components/patterns/page-header.tsx` — it guarantees exactly one first-level heading.
    - ⚠ `tests/listing/wizard-occupancy.test.tsx:156-158` — its heading helper THROWS on more than one
      first-level heading, so its ten passing cases are already a partial proof for the wizard. The
      end-to-end spec covers the other four surfaces and the wizard's rendered states.
    - 14-UI-SPEC § Typography rules 1 and 4, and acceptance criteria 35 and 36.
    - ⚠ 14-RESEARCH § The DB-contention flake pattern — six rules. Name the spec files; cap at three
      DB-seeding files per invocation; a failure in an untouched spec is a flake until it fails alone; the
      standing red in the availability spec is not this phase's.
  </read_first>
  <files>e2e/host-headings.spec.ts, e2e/overflow-320.spec.ts</files>
  <action>
    Author the headings spec in the chromium project, structured as a route table the way the overflow sweep
    is, so a later surface is a row rather than a copy. Its header states what silent failure it catches: two
    first-level headings on one document give a screen-reader user two answers to "what page is this", and
    two DIFFERENT first-level sizes across one product is drift that no single-surface test can see.

    For each of the five surfaces, and for each state a surface has (the dashboard's four, the inbox's two,
    the bookings table's two tabs times its two states, the wizard's every step in both occupancy modes, and
    the availability route), assert:
    - exactly ONE first-level heading resolves, and
    - its computed size is recorded.

    Then assert all recorded sizes are EQUAL to each other. Report the full set on failure so a red names the
    outlier rather than just failing.

    Separately, extend the existing overflow sweep's route table with the five Phase-14 routes, using its own
    row shape and its own skip-reason convention. It already asserts the horizontal-overflow rule, the
    control target floor and the primary touch floor; adding rows is the correct extension and authoring a
    second sweep is not.

    Where a surface needs seeded data to reach a state, reuse the existing sign-up and seeding helpers.
    Do not author a third sign-up path.
  </action>
  <verify>
    <automated>npx playwright test e2e/host-headings.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/host-headings.spec.ts --project=chromium` passes. Run it ALONE.
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium` passes with the five new rows. Run it
      ALONE — it is a seeding spec.
    - The size-equality assertion is observed FAILING against a deliberate regression: temporarily restore one
      surface's oversized heading, run the headings spec alone, observe the failure naming the outlier,
      revert. Record the message in the SUMMARY.
    - The one-heading assertion is observed failing against a deliberately added second first-level heading,
      then reverted.
    - `npx vitest run tests/listing/wizard-occupancy.test.tsx` still passes with that file unedited — its
      helper throwing on multiple first-level headings is the wizard's independent second reading.
    - Any failure in a spec this plan did not touch is re-run alone before being reported, and the
      pre-existing availability red is named in the SUMMARY as excluded.
    - `npm run test:design -- selector-contract` passes: no undeclared hook was introduced, and the
      end-to-end role-query and label-query floors may only go up.
  </acceptance_criteria>
  <done>The two cross-surface claims no single-surface plan could make are commands, and both have been observed failing.</done>
</task>

<task type="auto">
  <name>Task 2: The alarm-token census, written as a pinned per-file map rather than a zero-count scan</name>
  <read_first>
    - ⚠ 14-RESEARCH § The destructive-token census — the true baseline, measured with the repo's own comment
      stripper: the host route tree carries none, and four component files across the host and availability
      trees carry eight spellings between them. The research states plainly that a plan writing this as a
      zero-count scan over the three trees goes red on its first run, and that the correct shape is a pinned
      per-file map — the same shape the surviving-accent-lines and declared-sites inventories already use.
    - `tests/design/price-surface.test.ts:176-181` and :438-448 — the AST-walk idiom, if a walk is the right
      shape; a comment-stripped source scan may be simpler and is acceptable if it uses the repo's own
      stripper.
    - `tests/design/brand-recipe.test.ts:143-152` — the per-file pinned-map shape to copy.
    - `src/components/host/payout-banner.tsx`, `payout-state-badge.tsx`, `host-cancel-dialog.tsx` and
      `src/components/availability/weekly-hours-editor.tsx` — the four files carrying the measured
      occurrences. All four are correct as they stand: a paused payout account is a genuine failure needing a
      human, and a form-field validation message is the canonical alarm use.
    - `src/components/booking/request-countdown.tsx` — its one occurrence is pinned by a different gate at a
      count of one and is retained by decision on this hours-scale surface (D-146). Do not double-pin it here;
      name the other gate as its owner instead.
    - 14-UI-SPEC § `--destructive` gains ZERO new occurrences, and acceptance criterion 39.
  </read_first>
  <files>tests/design/host-tone-census.test.ts</files>
  <action>
    Author a design-suite gate that MEASURES the alarm-token occurrences across the host route tree, the host
    component tree and the availability component tree — with comments stripped by the repo's own stripper,
    because a comment quoting a token is not an occurrence — and compares them against a pinned per-file map
    declared in the test.

    Populate the map by measuring the tree this commit produces. Every entry carries the file, the count and
    the REASON that occurrence is correct: the paused payout state is a genuine failure needing a human; a
    form-field validation message is the canonical alarm use. An entry without a reason is not an entry.

    The assertion is that the map and the measurement are EQUAL — so a new occurrence fails, and a removed one
    fails too, which is the half a floor-style assertion would miss.

    The header states the whole point: the phase's claim is ZERO NEW alarm tokens on a set of calm host
    workflow surfaces, not zero tokens; a request that expires, a booking that was cancelled, a listing with
    no hours, a payout that has not happened yet and a week with nothing set are all normal states of a
    working marketplace, and colour-as-alarm on any of them tells a host something is broken when nothing is.

    Also record in the header which occurrences are owned by OTHER gates and are deliberately not double-pinned
    here.
  </action>
  <verify>
    <automated>npm run test:design -- host-tone-census</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- host-tone-census` passes against the tree as this phase leaves it, on its first
      run.
    - The measured per-file counts are RECORDED in the SUMMARY, with the command that produced them, and
      compared against the research's pre-phase baseline. Any difference is explained by a named plan in this
      phase.
    - The gate is observed failing in BOTH directions: add one alarm token to a host component, run, observe;
      revert. Remove one, run, observe; revert. Record both messages.
    - `npm run test:design -- phase13-surface-gates` still passes with its file unedited — the countdown's
      single occurrence is owned there, not here.
    - `npm run test:design` green with 0 failed.
  </acceptance_criteria>
  <done>The "zero new alarm tokens" claim is a measured, two-directional gate rather than a scan that would have been red from the start.</done>
</task>

<task type="auto">
  <name>Task 3: Nine declared court-only baselines, honestly blocked</name>
  <read_first>
    - `src/lib/design/visual-baselines.ts` — the whole file, and in particular the twenty declared-and-blocked
      rows an earlier phase left, which are the shape and the reason-length to match.
    - `playwright.config.ts:29` (the pinned image), :39 (⚠ the visual project is constructed only on Linux, so
      it DOES NOT EXIST on this machine), :78 (snapshots are never updated automatically) and :94 and :104
      (the two projects are disjoint).
    - `e2e/visual/surfaces.spec.ts-snapshots/` — thirty committed pictures, none of them a host surface. ⚠ If
      one of them moves after a Phase-14 commit, that is a regression on a BOOKER surface and must be
      investigated, never re-minted to green.
    - `scripts/seed-baseline-fixtures.ts` — the existing host fixture: a named host with the host capability,
      a verified email, an activated payout wallet and five published listings with hours, plus a frozen
      clock the spec installs and a reset that deletes the fixture's rows safely.
    - 14-RESEARCH § The visual-regression baseline situation — what the fixture already gives this phase, what
      it still needs, and the honest verdict: buildable, but not inside a single plan and NOT VERIFIABLE ON
      THIS MACHINE.
    - 14-UI-SPEC § Visual Baselines — the nine rows, their hooks, their widths and their determinism notes.
      ⚠ Court only. No Phase-14 surface owes a second-theme pair (PROJECT D-138).
  </read_first>
  <files>src/lib/design/visual-baselines.ts</files>
  <action>
    Declare all nine Phase-14 rows, court only, each carrying its surface id, its route, the hook it waits on
    and its widths, exactly as the design contract specifies. Where a width is load-bearing for a reason —
    for instance the wizard rail needs both a width below the large breakpoint and one above it, because the
    checklist's placement changes there — record that reason in the row.

    Mark ALL NINE blocked, each with its OWN reason rather than one shared sentence:
    - the six clock-dependent rows are blocked on a committed fixture that seeds bookings at fixed literal
      instants, a request at a fixed remaining duration, and a fixed booker identity — because a per-run seed
      mints fresh identifiers and clock-relative instants, so the reference picture disagrees with the next
      dispatch, which trains people to re-mint baselines;
    - the three that need no clock are blocked on the host fixture re-point, which is real work that this
      phase did not take and which cannot be verified on this machine at all.

    Name, in each blocked reason, the fixture file and the specific thing it still needs, so the inventory is
    something a later plan can work from rather than an absence.

    ⚠ Do NOT commit a baseline captured against a live clock. Do not attempt to generate any baseline here:
    the visual project is not constructed off Linux and the command does not exist on this machine. Say so in
    the SUMMARY rather than reporting an untried step as done.

    ⚠ Do not re-mint any of the thirty existing pictures. If one has moved, investigate it as a booker-surface
    regression and report it.
  </action>
  <verify>
    <automated>npm run test:design && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design` passes with the nine new rows declared; report the file and test counts.
    - All nine rows are court only. `grep -c 'grove' src/lib/design/visual-baselines.ts` shows no Phase-14 row
      naming a second theme.
    - Each of the nine blocked reasons is distinct and names the fixture file and the specific missing piece.
      A shared boilerplate reason across rows is a defect in this task.
    - `git status --porcelain e2e/visual/surfaces.spec.ts-snapshots/` is EMPTY — no committed picture moved
      and none was added.
    - The SUMMARY states explicitly that the visual project does not exist on this platform, that no baseline
      was generated, and that the nine rows are declared and blocked rather than covered.
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>Nine baselines exist as an inventory with per-surface reasons, and the phase says it did not generate them rather than implying it did.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document structure → assistive technology | Two first-level headings give two answers to "what page is this"; five different sizes is one product speaking with five voices |
| the 320px floor → reachability | A control below the target floor or a page that scrolls sideways is a functional failure on the smallest device the product supports |
| declared inventory → a claim of coverage | A blocked row is honest; an absent row implies nothing was owed; a minted-against-a-live-clock picture is worse than either |
| alarm colour → a host's reading of their own account | Colour-as-alarm on a normal state tells a host something is broken when nothing is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-16-HEADINGS | Denial of service | all five surfaces | mitigate | One first-level heading asserted per document per state, plus cross-surface size equality reported as a set; observed failing on a restored outlier |
| T-14-16-SMALLSCREEN | Denial of service | all five surfaces | mitigate | The five routes are added to the existing sweep, which already asserts the overflow rule and both size floors |
| T-14-16-FALSEALARM | Repudiation | the host and availability trees | mitigate | The alarm census is a pinned per-file map measured in this commit, asserted in both directions, with a reason on every entry |
| T-14-16-BASELINELIE | Repudiation | the baseline inventory | mitigate | Nine rows declared and blocked with distinct reasons; no baseline generated, no committed picture re-minted, and the platform limitation stated in the summary rather than glossed |
| T-14-16-SNAPSHOTDRIFT | Tampering | the thirty committed pictures | mitigate | A clean status on the snapshot directory is an acceptance criterion; a moved picture is investigated as a booker regression, never re-minted |
| T-14-16-FLAKE | Denial of service | the end-to-end signal | mitigate | Each seeding spec is run alone; the pre-existing standing red is named and excluded |
| T-14-16-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm run build` green — it runs the linter, the whole design suite and the production build, so a leak, an
  undeclared hook, a moved pin or a literal in a skeleton all fail here.
- `npm test` green (needs Docker), plus `npx tsc --noEmit` exit 0.
- `npx playwright test e2e/host-headings.spec.ts --project=chromium` alone, then
  `npx playwright test e2e/overflow-320.spec.ts --project=chromium` alone. Never bare. Never more than three
  named spec files in one invocation. `--project=visual` does not exist on this machine and no plan may claim
  to have run it.
- `git diff --stat drizzle/` EMPTY — the phase's zero-migration rule, as a command.
- `git status --porcelain e2e/visual/surfaces.spec.ts-snapshots/` empty.
- ⚠ The phase must not close on a locally green design suite alone: a CI run is the phase gate, and a prior
  phase closing over a red visual gate with nobody noticing is the record of what that costs.
</verification>

<success_criteria>
- One first-level heading per document on all five surfaces in every state, all five the same size.
- The five surfaces are in the 320px sweep and hold its floors.
- The alarm-token census is a measured, two-directional, per-file pinned map with a reason on every entry.
- Nine court-only baselines are declared and honestly blocked, with distinct reasons naming the fixture work
  they wait on.
- `drizzle/` is unchanged and no committed picture moved.
</success_criteria>

<output>
Create `.planning/phases/14-host-tooling/14-16-SUMMARY.md` when done.
</output>
