---
phase: 14-host-tooling
plan: 07
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/lib/host/bookings-copy.ts
  - src/app/(host)/host/bookings/page.tsx
  - src/app/(host)/host/bookings/loading.tsx
autonomous: true
requirements: [HFLOW-04]
must_haves:
  truths:
    - "/host/bookings gets the design system and NOT a new information architecture — the tab partition, the listing filter, the page size, the keyset cursor and the owner-scoped WHERE are all untouched (D-154)"
    - "The page and its plate render an identical title and lede asserted against ONE exported constant, not two literals that happen to match today"
    - "The status vocabulary is ADOPTED, not extended: the shared derivation is the single authority for the words and this page already consumes it"
    - "The one raised-elevation element on this route stays exactly one — the listing filter's plain select is not swapped for a component and no raised surface is added (landmine 4)"
    - "Adding host-side filtering or sorting would be a new capability and is OUT OF SCOPE (D-154, deferred)"
  artifacts:
    - path: "src/lib/host/bookings-copy.ts"
      provides: "the bookings header title and lede as one exported pair, consumed by the page and by the plate"
    - path: "src/app/(host)/host/bookings/page.tsx"
      provides: "HOST_LIST_SHELL, PageHeader from the shared copy, the four named type roles; everything else unchanged"
    - path: "src/app/(host)/host/bookings/loading.tsx"
      provides: "the same shell constant and the same header strings as the page"
  key_links:
    - from: "src/app/(host)/host/bookings/loading.tsx"
      to: "src/lib/host/bookings-copy.ts"
      via: "the plate imports the same title and lede the page renders"
      pattern: "bookings-copy"
    - from: "src/app/(host)/host/bookings/page.tsx"
      to: "queryHostBookings"
      via: "the owner-scoped read, its tab predicate and its keyset cursor, all unchanged"
      pattern: "queryHostBookings"
---

<objective>
Give the host bookings table the design system and change nothing else about it.

Purpose: D-154 is unusually explicit about what may not move, because this page is the one host surface where
a polish pass has the most opportunity to become a feature. The tab partition handles an attacker-controlled
query parameter, the listing filter is applied INSIDE the host-scoped predicate so it can only narrow, and
the keyset cursor is what makes the list bounded. All three are correctness, not layout.

Output: the shared shell, one header pair owned by one constant, the four named type roles — and a diff that
touches no query, no filter, no cursor and no elevation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-host-tooling/14-CONTEXT.md
@.planning/phases/14-host-tooling/14-UI-SPEC.md
@.planning/phases/14-host-tooling/14-RESEARCH.md
@.planning/phases/14-host-tooling/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One owner for the header pair, and the shared shell on the page and its plate</name>
  <read_first>
    - `src/app/(host)/host/bookings/page.tsx` — the whole file (298 lines). Specifically :61-63 (`parseTab`
      treating the tab parameter as attacker-controlled and failing to a boring default), :66-74 (the refund
      sibling sentences — D-79's wording, never interpolated into the badge), :97 (the clock), :101-105 (the
      owner-scoped filter options), :107-113 (THE READ), :117-142 (the display map), :154 (the hand-typed
      shell), :155-158 (the hand-rolled heading and paragraph — note the strings are IDENTICAL to the
      plate's), :160-189 (tabs and the filter form), :175 (⚠ the ONE raised-elevation element on this route),
      :192-211 (the empty states, forked by tab), :214-277 (the desktop table), :280-284 (the mobile rows),
      :287-293 (the pager).
    - `src/app/(host)/host/bookings/loading.tsx` — the whole file. It carries the same two strings the page
      does; that duplication is what this task ends.
    - `src/lib/listing/hours-signal.ts` — the shape for a small copy module beside its surface.
    - `src/lib/design/measurements.ts` — `HOST_LIST_SHELL`.
    - `src/components/patterns/page-header.tsx` — the whole file.
    - ⚠ `tests/design/elevation-z.test.ts:305-311` — the raised inventory pins this route at exactly ONE
      raised element, and that one is the filter's select. **This is landmine 4.** Swapping the plain select
      for a component, or adding any raised surface here, moves the number.
    - 14-UI-SPEC § The Bookings Table — the untouched list and the change list.
  </read_first>
  <files>src/lib/host/bookings-copy.ts, src/app/(host)/host/bookings/page.tsx, src/app/(host)/host/bookings/loading.tsx</files>
  <action>
    Create a small copy module exporting the bookings header's title and lede as one pair, with a header
    stating why it exists: the page and its plate render the same two strings today and nothing stops them
    drifting, which is precisely the failure a loading plate exists to prevent.

    On the page: replace the hand-typed shell with the shared list-shell constant, and replace the hand-rolled
    heading and paragraph with the header pattern reading from the new copy module. On the plate: the same
    shell constant and the same copy module.

    Apply the four named type roles where a bare small-text utility stands in for the label role, in the
    table cells and the row meta. The guest-pays column keeps its right alignment and tabular figures.

    ⚠ Change NOTHING else:
    - the tab partition and its parameter handling,
    - the listing filter — it stays a plain form with no client script, and its select keeps the class it has,
      including the raised elevation the inventory pins,
    - the page size, the keyset cursor and the load-more control,
    - the owner-scoped read and its display map,
    - the status vocabulary — the shared derivation is the single authority and this page already consumes
      it; minting a host-side word has misread D-154,
    - the refund sibling sentences,
    - the two empty states, which are already the shared pattern and stay byte-for-byte,
    - the mobile row component, which is already the shared row pattern.

    Do not add a filter, a sort, a column or a date range. Each is a new capability and is deferred by name.
  </action>
  <verify>
    <automated>npm run test:design -- elevation-z status-vocab empty-state-adoption loading-coverage && npx vitest run tests/booking/views.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- elevation-z` passes with `git diff --stat tests/design/elevation-z.test.ts`
      EMPTY. The route still carries exactly one raised element.
    - `grep -c 'shadow-raised' 'src/app/(host)/host/bookings/page.tsx'` returns exactly 1, on the same
      element as before.
    - `npm run test:design -- status-vocab` passes with that file unedited — no host-side status word was
      minted.
    - `grep -c 'HOST_LIST_SHELL' 'src/app/(host)/host/bookings/page.tsx' 'src/app/(host)/host/bookings/loading.tsx'`
      returns one hit each; `grep -c 'max-w-4xl'` on both returns 0.
    - Both files import the title and the lede from the copy module; a grep for either literal string in the
      two route files returns ZERO hits.
    - `git diff 'src/app/(host)/host/bookings/page.tsx'` shows zero changed lines inside the tab parser, the
      filter form, the read, the display map or the pager. Quote each untouched block's boundaries in the
      SUMMARY.
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>The table wears the design system; its information architecture, its filter and its one raised surface are provably unmoved.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-NOREG — the partition, the filter, the page size and the owner scope still behave</name>
  <read_first>
    - `tests/security/bookings-owner-scope.test.ts` — the host-scope case (:171), the independence and
      cannot-widen cases (:226) and the empty-set case (:238).
    - `tests/booking/views.test.ts` — the `queryHostBookings` invocation shape and the tab partition
      coverage.
    - `tests/booking/host-booking-row.test.tsx:72` — a requested row still renders both action controls.
      ⚠ 14-03 changed the action cluster's control size and this row renders it too; that test is the proof
      the coupling still holds.
    - 14-UI-SPEC GATE-NOREG rows 1, 4 and 8.
    - 14-RESEARCH § The DB-contention flake pattern — the six rules for reporting an end-to-end result.
  </read_first>
  <files>src/app/(host)/host/bookings/page.tsx</files>
  <action>
    Prove, by re-running the shipped coverage rather than by writing a new assertion for a behaviour that
    already has one, that this restyle changed no behaviour. Where the re-run is not sufficient because no
    test exists, add the missing case to the file that already owns the concern — do not author a new file.

    Specifically: confirm that the two tabs return the same row sets they did before, that a listing filter
    naming a listing the host does not own returns zero rows rather than a widened set, that the page size
    and the load-more cursor produce the same sequence, and that a requested row inside the desktop table
    still renders both action controls at their new size.

    If any of those four has no committed assertion today, add it to `tests/security/bookings-owner-scope.test.ts`
    (for the scoping and filter cases) or `tests/booking/views.test.ts` (for the partition and cursor cases),
    with a comment naming GATE-NOREG and the row it discharges.

    Then confirm the 320px behaviour of this route's mobile cards: no horizontal overflow, every control at
    or above the target-size floor and every primary action at or above the touch floor. Record the measured
    values; do not invent them. If the route needs a fix to satisfy this, make it here — but a fix that
    changes the filter, the tabs or the cursor is a scope alarm and must be raised instead.
  </action>
  <verify>
    <automated>npx vitest run tests/security/bookings-owner-scope.test.ts tests/booking/views.test.ts tests/booking/host-booking-row.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/security tests/booking` reports zero failures.
    - `npx vitest run tests/booking/host-booking-row.test.tsx` passes with that test file UNEDITED.
    - The four GATE-NOREG behaviours each name a passing command in the SUMMARY, with the file and case that
      carries them. Any of the four that had no assertion before this task now has one, and the SUMMARY says
      which were added.
    - The 320px measurements for this route are RECORDED in the SUMMARY as observed numbers, with the command
      that produced them. No number is invented.
    - `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>Every behaviour D-154 protects has a green command behind it, and the ones that did not have one now do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string → the read | The tab and cursor parameters are attacker-controlled and fail to a boring default; the listing filter is applied inside the already-host-scoped predicate so it can only narrow |
| host session → row set | Ownership is a `WHERE` clause; a page branch would be bypassable by a crafted parameter |
| page copy ↔ plate copy | Two renderings of one header; divergence is the exact failure a loading plate exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-IDOR | Information disclosure | the listing filter | mitigate | The filter is not opened; the cannot-widen case in `bookings-owner-scope.test.ts` is re-run and, if absent for the agenda-era shape, extended |
| T-14-07-TABPARSE | Tampering | the tab parameter | mitigate | `parseTab`'s block is not edited; a diff showing zero changed lines inside it is the proof, and the partition cases are re-run |
| T-14-07-CURSOR | Denial of service | the keyset pager | mitigate | Page size and cursor untouched; an unbounded list would be the failure and the sequence is re-asserted |
| T-14-07-VOCAB | Repudiation | booking status words | mitigate | The shared derivation stays the single authority; `status-vocab` passes with zero edits |
| T-14-07-ELEVATION | Tampering | the raised inventory | mitigate | The filter's select keeps its class and no raised surface is added; `elevation-z` passes with its file unedited (landmine 4) |
| T-14-07-MONEY | Tampering | guest-pays column | mitigate | The frozen quote through the shared formatter, zero arithmetic; `price-surface` re-run |
| T-14-07-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm run test:design` green; `elevation-z`, `status-vocab`, `empty-state-adoption`, `loading-coverage` and
  `price-surface` all pass with their files unedited.
- `npx vitest run tests/booking tests/security tests/host` — zero failures.
- `npx tsc --noEmit` exit 0.
- `git diff --stat drizzle/` empty.
- If an end-to-end run is used for the 320px measurement, name at most three spec files; never run bare.
</verification>

<success_criteria>
- The page and its plate read one shell constant and one exported header pair.
- The tab partition, the listing filter, the page size, the cursor, the read, the status vocabulary, the
  refund sentences and both empty states are provably unchanged.
- The route still carries exactly one raised-elevation element and `elevation-z.test.ts` is unedited.
- Each GATE-NOREG behaviour names a green command.
</success_criteria>

<output>
Create `.planning/phases/14-host-tooling/14-07-SUMMARY.md` when done.
</output>
