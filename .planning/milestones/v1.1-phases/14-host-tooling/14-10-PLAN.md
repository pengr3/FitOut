---
phase: 14-host-tooling
plan: 10
type: execute
wave: 4
depends_on: ["14-01", "14-09"]
files_modified:
  - src/components/host/publish-checklist.tsx
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - src/app/(host)/host/listings/[id]/edit/page.tsx
  - src/lib/design/selector-contract.ts
  - src/lib/design/contrast-pairs.ts
  - tests/design/status-vocab.test.ts
  - tests/design/empty-state-adoption.test.ts
  - tests/listing/publish-checklist.test.tsx
autonomous: true
requirements: [HFLOW-02]
must_haves:
  truths:
    - "The publish checklist is PERSISTENT — a side panel from the large breakpoint up, a closed collapsible above the form below it — and stops being an end-of-flow surprise (D-149)"
    - "Exactly ONE checklist instance renders per document at every step, at every width, in both occupancy modes; the review step keeps its inline one and the persistent placement is suppressed there"
    - "The row array and its step resolution are REUSED verbatim, not rewritten — the rows already resolve back to their step by key"
    - "Moving the done marker out of the wizard file moves FOUR pinned inventories that address it BY FILE PATH, and all four are amended in the SAME commit (landmine 2)"
    - "The done marker holds a glyph and nothing else — it is the one surviving filled success surface in the repo and it survives only because of that; no second filled-success surface is added anywhere in this phase"
    - "The wizard shell widens only at the large breakpoint, and the column width and the grid track are ONE number with ONE owner"
  artifacts:
    - path: "src/components/host/publish-checklist.tsx"
      provides: "the lifted checklist rows, the done marker, the fix affordance, and the two persistent placements"
      contains: "publish-checklist"
    - path: "src/app/(host)/host/listings/[id]/edit/page.tsx"
      provides: "the shared panel shell widened at the large breakpoint, and the two-column grid from the declared track"
    - path: "tests/design/status-vocab.test.ts"
      provides: "the legal filled-pairing site and the positive call-site set re-pointed at the new file, in the same commit as the move"
    - path: "tests/design/empty-state-adoption.test.ts"
      provides: "the one legal filled-success surface re-pointed at the new file, with its reason"
    - path: "tests/listing/publish-checklist.test.tsx"
      provides: "exactly one instance per document at every step in both modes, and identical row sets across placements"
  key_links:
    - from: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      to: "src/components/host/publish-checklist.tsx"
      via: "the same row array passed to both the persistent placement and the review placement"
      pattern: "PublishChecklist"
    - from: "src/components/host/publish-checklist.tsx"
      to: "src/components/patterns/panel-card.tsx"
      via: "the large-breakpoint placement uses the pattern's own sticky prop, so the offset has one owner"
      pattern: "sticky"
---

<objective>
Lift the publish checklist out of the review step, give it two persistent placements, and move the four
committed inventories that address its done marker by file path — in one commit, because they go red
together and none of the failures says "you moved a component".

Purpose: HFLOW-02 asks for the checklist as a persistent panel rather than an end-of-flow surprise. The
extraction itself is mechanical; what is not mechanical is that the done marker at the heart of it is
simultaneously the one legal filled-success pairing site, a member of a closed five-file positive set, the
one legal filled-success surface in the tree, and the subject of a note in the contrast inventory — all four
addressed by the string `wizard.tsx`. 14-RESEARCH found this and named it the biggest unbudgeted trap in the
phase.

Output: one shared component with three placements and exactly one instance per document, a shell that
widens only where the panel appears, and four amended inventory rows carrying their reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-host-tooling/14-CONTEXT.md
@.planning/phases/14-host-tooling/14-UI-SPEC.md
@.planning/phases/14-host-tooling/14-RESEARCH.md
@.planning/phases/14-host-tooling/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the checklist, and move the four inventories that address it by file path</name>
  <read_first>
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` — :484-543 (the row array and its mode fork at
      :497-526), :393 (the key-to-index resolver the rows use), :544 (the publish-eligibility derivation),
      :1345-1436 (the review step), :1370-1434 (⚠ THE MARKUP TO LIFT), :1388-1399 (⚠ the done marker's filled
      success pairing — this is the four-gate trap), :1403-1413 (the fix affordance that jumps to the row's
      step).
    - 14-PATTERNS § 5 — the excerpt of exactly what is being moved, and the Gate-Amendment Obligation table.
    - ⚠ `tests/design/status-vocab.test.ts:137` (the legal filled-pairing site constant), :149-154 (the
      positive call-site set — a CLOSED set of five files), :501 (the file must actually be scanned) and the
      two assertions at :654 and :662.
    - ⚠ `tests/design/empty-state-adoption.test.ts:458-461` (the one legal filled-success surface, keyed by
      path) and :475 (its scan scope).
    - ⚠ `src/lib/design/contrast-pairs.ts:250` — the note says "the wizard's completed-step marker". Prose
      only, but it must stay TRUE.
    - `src/components/patterns/panel-card.tsx:48-84` and :158-176 — the props and the sticky offset the
      pattern already owns. `tests/design/sticky-offset.test.ts:491` pins the sticky site count at one; using
      the prop keeps it at one.
    - `src/components/ui/collapsible.tsx` and `tests/design/leak.test.ts:339` — the primitive is vendored and
      already leak-scanned. This is its FIRST host call site and no new block is fetched.
    - 14-UI-SPEC § The persistent publish checklist — the three placements table, the mobile-default
      argument, the done-marker rule and the two falsifiable assertions.
  </read_first>
  <files>src/components/host/publish-checklist.tsx, tests/design/status-vocab.test.ts, tests/design/empty-state-adoption.test.ts, src/lib/design/contrast-pairs.ts</files>
  <action>
    Create the shared component. LIFT the row markup — do not rewrite it. The done and not-done markers keep
    their exact shape: a small round element carrying the filled success pairing with a hidden check glyph
    when done, and the muted surface with a hidden minus glyph when not. It holds a GLYPH AND NOTHING ELSE,
    in both placements. Do not put a text node in it. Do not add a second filled-success surface anywhere.
    The success foreground remains illegal as text.

    The component takes the row array and the step setter as props and derives nothing: the array, the mode
    fork and the key-to-index resolution stay in the wizard, because the wizard is where the form state
    lives.

    It supports two placements behind one prop or one boolean, whichever reads better:
    - the panel placement, for the large breakpoint and up: the shared panel pattern with its own sticky prop
      and the checklist title from the copy contract.
    - the collapsible placement, for below the large breakpoint: the vendored collapsible with a real button
      trigger whose label carries the done-of-total figure and a chevron that rotates on open. It defaults
      CLOSED, because a permanently-open nine-row panel above every field on a phone pushes the first input
      below the fold on all nine steps.

    Both placements carry the checklist hook. The row labels, their order and the fix affordance are
    identical in every placement.

    ⚠ Then, IN THIS SAME COMMIT, amend the four inventories that address the done marker by the wizard's file
    path, each with its reason:
    1. the legal filled-pairing site constant → the new component's path,
    2. the positive call-site set → gains or loses the path so the closed five-file set stays correct,
    3. the one legal filled-success surface, with its reason rewritten to name the checklist component,
    4. the contrast inventory's note, so its prose stays true.

    Each amendment is an inventory edit with a stated reason, which is the shape this repository endorses —
    not a test loosened to make a move pass.
  </action>
  <verify>
    <automated>npm run test:design -- status-vocab empty-state-adoption contrast && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- status-vocab` and `npm run test:design -- empty-state-adoption` both pass.
    - `grep -rn 'wizard.tsx' tests/design/status-vocab.test.ts tests/design/empty-state-adoption.test.ts`
      returns no hit that still points the done marker at the wizard file.
    - `grep -c 'bg-success' src/components/host/publish-checklist.tsx` returns exactly 1, and a repo-wide
      comment-stripped scan of the source tree finds that spelling in exactly one file.
    - `npm run test:design -- contrast` passes and `git diff src/lib/design/contrast-pairs.ts` changes only
      the note's prose — the declared pair list and the excluded-pair shape are unchanged.
    - `npm run test:design -- sticky-offset` passes with its file unedited: the sticky site count is still
      one, because the placement uses the pattern's prop.
    - `npm run test:design -- leak sheet-absent` passes — the collapsible is vendored, and no new block was
      fetched. `git diff components.json` is empty.
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>The checklist is a shared component, and the four inventories that addressed its marker by file path moved with it in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Three placements, exactly one instance, and a shell that widens where the panel appears</name>
  <read_first>
    - `src/app/(host)/host/listings/[id]/edit/page.tsx` — the whole file (131 lines), especially :122 (the
      wrapper that gains the shared panel shell and the large-breakpoint widening). ⚠ THIS file gains the
      shell, not the wizard file.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` as 14-09 left it — the rail, the header pattern,
      the review step at :1345-1436 and the nav row at :1438-1467.
    - `src/app/(host)/host/listings/new/page.tsx` (28 lines — it renders no UI and redirects) and
      `src/app/(host)/host/listings/new/loading.tsx` and `.../edit/loading.tsx` — the plates that share the
      panel shell.
    - `src/lib/design/measurements.ts` — `WIZARD_CHECKLIST_COL` and `WIZARD_CHECKLIST_GRID` from 14-01. ⚠ They
      are one number written twice; use both from the one owner and tune neither in isolation.
    - `src/lib/design/selector-contract.ts:209-227` and `tests/design/selector-contract.test.ts:442-462`.
    - 14-RESEARCH § M5 — MEASURED: at the large breakpoint the form column is 672px and the aside is 288px,
      exactly the spec's arithmetic. ⚠ NOT measured: whether the address step's two-column fields survive
      672px. That is this task's measurement obligation.
    - 14-UI-SPEC § The Wizard — the shell sketch and the one-owner warning about the track and the width.
  </read_first>
  <files>src/app/(host)/host/listings/[id]/edit/page.tsx, src/app/(host)/host/listings/[id]/edit/wizard.tsx, src/lib/design/selector-contract.ts</files>
  <action>
    On the wizard's route shell file, replace the hand-typed wrapper with the shared panel shell plus the
    large-breakpoint widening, and introduce the two-column grid from the declared track constant. Below the
    large breakpoint nothing changes: one column, same width as today.

    In the wizard component, render the checklist in exactly one place at a time:
    - on any step except review, at the large breakpoint and up: the panel placement, inside the aside column
      whose width comes from the declared constant.
    - on any step except review, below the large breakpoint: the collapsible placement, above the form and
      under the step title.
    - on the review step, at every width: the existing inline checklist, and the persistent placement
      SUPPRESSED.

    Both persistent placements are rendered from the same row array the review step uses, so the label set
    cannot differ.

    Declare the checklist hook in the selector contract with the reason a role query cannot carry it: the
    one-instance-per-document assertion counts CONTAINERS, while the rows themselves stay role queries.

    ⚠ Take M5's open measurement: with a real seeded draft listing, render the address step at exactly the
    large breakpoint and confirm the two-column fields survive the narrower form column. RECORD the measured
    result. If they do not survive, the fix is to re-derive the checklist column constant — in
    `measurements.ts`, with its derivation — and NOT to add an arbitrary width at the call site. If the
    re-derivation is needed, note it in the SUMMARY as a change to a Wave-1 constant.

    Do not touch the save path or the nav row; that is plan 14-11.
  </action>
  <verify>
    <automated>npm run test:design -- selector-contract type-scale loading-coverage && npx vitest run tests/listing/wizard-occupancy.test.tsx tests/listing/wizard-rail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- selector-contract` passes — the checklist hook is declared AND rendered.
    - `grep -c 'WIZARD_CHECKLIST_COL\|WIZARD_CHECKLIST_GRID' 'src/app/(host)/host/listings/[id]/edit/page.tsx' 'src/app/(host)/host/listings/[id]/edit/wizard.tsx'`
      shows both constants used and neither value typed at a call site. A grep for the raw track string
      outside `measurements.ts` returns nothing.
    - `grep -c 'HOST_PANEL_SHELL' 'src/app/(host)/host/listings/[id]/edit/page.tsx'` returns 1.
    - `npx vitest run tests/listing/wizard-occupancy.test.tsx tests/listing/wizard-rail.test.tsx` passes with
      BOTH test files unedited.
    - The M5 measurement is RECORDED in the SUMMARY with the width, the observed field behaviour and the
      command or drive that produced it. No number is invented, and "it looked fine" is not a measurement.
    - `npm run test:design -- loading-coverage` still pins 29 / 21 / 8 with zero edits.
    - `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>The checklist is visible from the first step at every width, in exactly one place, and the column and its track have one owner.</done>
</task>

<task type="auto">
  <name>Task 3: Exactly one per document, at every step, in both modes</name>
  <read_first>
    - `tests/listing/wizard-occupancy.test.tsx` — the whole file. Copy the preamble wholesale; cases (6) and
      (7) already exercise the checklist's mode fork and are the model for the row-set assertions.
    - `tests/listing/wizard-rail.test.tsx` as 14-09 left it — the advance-to-step helper it uses.
    - `src/components/host/publish-checklist.tsx` and the wizard as this plan leaves them.
    - 14-UI-SPEC § The persistent publish checklist — falsifiable "exactly once per document" — and
      acceptance criteria 23 and 25.
    - 14-VALIDATION § Wave 0 Requirements — this file is named there and is binding.
  </read_first>
  <files>tests/listing/publish-checklist.test.tsx</files>
  <action>
    Author the jsdom test with the first-line environment pragma and a header stating what silent failure it
    catches: two checklists on one document is two places a host can read a different answer to "am I ready
    to publish", and a suppression rule that is one boolean deep is exactly the kind of thing a later edit
    reintroduces.

    Cases:
    1. Walking every step in whole-space mode, the checklist container count is exactly ONE at each step —
       including the review step, where the one is the inline placement.
    2. The same walk in drop-in mode.
    3. At each step, the SET of row labels in the persistent placement equals the set in the review
       placement, in both modes. Assert set equality, not order-sensitive equality, and assert the mode fork
       matches the publish schema's own fork (the drop-in pair versus the whole-space trio).
    4. The collapsible trigger's label carries the done-of-total figure and the collapsible defaults CLOSED.
    5. A row's fix affordance moves the rendered step to that row's own step.
    6. The done marker contains a hidden glyph and NO text node, in both placements.

    Where the placement depends on viewport width and jsdom cannot decide it, assert on the rendered markup
    for both placements rather than simulating a media query, and say so in the header — the width-dependent
    half is the wizard rail baseline's job and is declared in plan 14-16.
  </action>
  <verify>
    <automated>npx vitest run tests/listing/publish-checklist.test.tsx tests/listing/wizard-occupancy.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files pass; `npx vitest run tests/listing` reports zero failures.
    - Case 1 is observed FAILING against a deliberate regression: temporarily render the persistent placement
      on the review step too, run, observe the count of two, revert. Record the message in the SUMMARY.
    - Case 3 is observed failing against a deliberately divergent row set, then reverted.
    - `head -1 tests/listing/publish-checklist.test.tsx` is the jsdom pragma.
    - `npm run test:design` green; `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>One checklist per document is a command that fails when it stops being true, at every step and in both modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist copy → publish enforcement | The checklist only NAMES what is missing; the publish action re-runs the shared publish schema and remains the authority |
| pinned inventory ↔ file path | Four committed gates address the done marker by the string `wizard.tsx`; moving the markup without moving them turns them red for a non-design reason |
| one document → one answer | Two checklists is two places a host can read a different readiness answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10-GATEPATH | Tampering | four pinned inventories | mitigate | All four are amended in the same commit as the extraction, each with its reason; the two design gates and the contrast gate are re-run as the task's own verify (landmine 2) |
| T-14-10-FALSEREADY | Repudiation | the checklist | mitigate | The rows and their fork are reused verbatim from the array the publish schema's fork already drives; set equality across placements is asserted in both modes |
| T-14-10-DOUBLETRUTH | Repudiation | two placements | mitigate | Exactly one container per document at every step, asserted and observed failing against a two-container regression |
| T-14-10-SUCCESSCREEP | Tampering | the filled-success surface | mitigate | The marker holds a glyph and nothing else; a repo-wide comment-stripped scan finds the filled-success spelling in exactly one file |
| T-14-10-STICKY | Tampering | the sticky offset inventory | mitigate | The panel placement uses the pattern's own sticky prop; `sticky-offset` passes with its file unedited |
| T-14-10-REGISTRY | Tampering | the component registry | mitigate | The collapsible is already vendored and leak-scanned; `git diff components.json` empty and `sheet-absent` green prove no block was fetched |
| T-14-10-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** `npx shadcn add` is explicitly forbidden for this phase; any proposal is a scope alarm |
</threat_model>

<verification>
- `npm run test:design` green; in particular `status-vocab`, `empty-state-adoption`, `contrast`,
  `sticky-offset`, `leak`, `sheet-absent`, `selector-contract` and `brand-recipe` all pass.
- `npx vitest run tests/listing` — zero failures, with the shipped occupancy and rail files unedited.
- `npx tsc --noEmit` exit 0.
- `git diff --stat drizzle/` empty; `git diff components.json` empty.
- No Playwright invocation is required. The M5 measurement may use one; if so, name at most three spec files.
</verification>

<success_criteria>
- One shared checklist component with three placements and exactly one instance per document.
- All four path-pinned inventories amended in the same commit, each with its reason, and all their gates
  green.
- The wizard shell widens only at the large breakpoint, from one owner for the column and its track.
- M5's open measurement is taken and recorded.
</success_criteria>

<output>
Create `.planning/phases/14-host-tooling/14-10-SUMMARY.md` when done.
</output>
