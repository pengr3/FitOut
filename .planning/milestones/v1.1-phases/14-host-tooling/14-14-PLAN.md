---
phase: 14-host-tooling
plan: 14
type: execute
wave: 7
depends_on: ["14-03", "14-11", "14-13"]
files_modified:
  - src/components/listing/address-autocomplete.tsx
  - src/components/listing/photo-uploader.tsx
  - src/lib/design/live-regions.ts
  - tests/design/live-regions.test.tsx
autonomous: true
requirements: [HFLOW-01, HFLOW-02]
must_haves:
  truths:
    - "The exclusion list reaches ZERO: the last excluded file names this phase as its owner and this phase discharges it"
    - "A live region announces a CHANGE. A freshly rendered page is not a change — it is a page. The static hint moves OUT of the region and the region holds only the resolved outcome"
    - "The unnamed status region on the wizard's photos step is NAMED here, not excluded for a later phase — that is what allows the exclusion list to actually reach zero rather than trading one row for another"
    - "The declared file set is a misnomer once it contains host files and is RENAMED in the same commit, along with the numeric type alias whose NAME carries the count on purpose"
    - "The committed non-empty-exclusions guard cannot hold once empty is the correct state; it is REWRITTEN in the same commit so the vacuity it protected against is still guarded (landmine 1)"
    - "Every status region on the five surfaces resolves to a non-empty accessible name, and the two implicitly-assertive validation regions are PINNED as a measured per-file map rather than scanned for as an attribute that is not there"
  artifacts:
    - path: "src/components/listing/address-autocomplete.tsx"
      provides: "the static hint as plain text outside any region, and one named status region holding only the resolved outcome"
    - path: "src/components/listing/photo-uploader.tsx"
      provides: "an author-supplied accessible name on its status region"
    - path: "src/lib/design/live-regions.ts"
      provides: "the renamed declared file set widened to the host files, four new rows, the exclusion list at zero, and the renamed numeric alias"
    - path: "tests/design/live-regions.test.tsx"
      provides: "the rewritten guard, the re-measured declared count, and the five-surface naming assertions"
  key_links:
    - from: "src/lib/design/live-regions.ts"
      to: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      via: "the save-state row naming the wizard file and the sentence it announces"
      pattern: "wizard-save-state"
    - from: "src/lib/design/live-regions.ts"
      to: "src/components/host/request-row.tsx"
      via: "the refusal row naming the request row and the server sentence it announces"
      pattern: "request-action-refusal"
---

<objective>
Take the live-region inventory to zero exclusions, widen it to the host surfaces it was deliberately not
covering, and rewrite the one committed assertion that cannot survive the correct answer.

Purpose: the last exclusion's own reason names this phase — auditing the host wizard early would have frozen
markup this phase was about to rewrite. That markup is now rewritten, so the exclusion is due. Two of the
three regions the inventory must gain were built earlier in this phase and are waiting for their rows; the
third is a shipped unnamed region on the wizard's photos step that is FIXED here rather than deferred,
because deferring it would trade one exclusion row for another and the list would not reach zero.

The trap: the committed test asserts the exclusion list is NON-EMPTY, as a guard against a gutted list making
its own reason-shape filter trivially green. Empty is now the correct state. Both cannot hold, so the guard
is rewritten in the same commit — that is landmine 1.

Output: an inventory whose name, count, rows and exclusions are all true at once, and a gate that still
guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-host-tooling/14-CONTEXT.md
@.planning/phases/14-host-tooling/14-UI-SPEC.md
@.planning/phases/14-host-tooling/14-RESEARCH.md
@.planning/phases/14-host-tooling/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discharge the address lookup, and name the photos-step region</name>
  <read_first>
    - `src/components/listing/address-autocomplete.tsx` — the whole file, and :238-239 in particular: one
      bare polite region with NO role and NO accessible name, whose first-paint content is a STATIC HINT.
      That is a region announcing the empty string on arrival and then a hint nobody asked for.
    - `src/components/listing/photo-uploader.tsx:226` — a status-role element with no accessible name. That
      role takes no name from its own text, so it is unnamed today.
    - `src/components/group/share-link-box.tsx:109-171` — the WORKED EXAMPLE of a discharge, including the
      docblock that states the rule: the label is a LABEL and not a second copy of the sentence.
    - `src/lib/design/live-regions.ts` — the header's mapping table, the region-kind union, the row contract
      at :420-448, and the exclusion row at :374-383 whose reason names this phase.
    - 14-UI-SPEC § Live Regions — the discharge table for the address field, the rule restated, and the three
      declared rows.
    - ⚠ 14-RESEARCH § G9 — the measured arithmetic in the module header drifts, and the two counting methods
      disagree on purpose. Both are re-measured in Task 2.
  </read_first>
  <files>src/components/listing/address-autocomplete.tsx, src/components/listing/photo-uploader.tsx</files>
  <action>
    On the address field: move the static hint OUT of the region entirely and render it as a plain paragraph.
    It is present from first paint and a screen reader reads it in document order like every other hint.

    Keep ONE region for the RESULT — the located outcome and the failure outcome are the two outcomes of one
    action and share one region, because two regions for one outcome is the defect. Give it the status role
    and an author-supplied accessible name from a named constant, following the share-link rule. It is empty
    until a lookup resolves. Its type moves to the declared label role, and the failure branch keeps its
    declared alarm ink — that occurrence is pre-existing and is explicitly kept.

    On the photos step's uploader: add an author-supplied accessible name to the existing status region, from
    a named constant, and change nothing else about that file. This is one attribute. It is done HERE rather
    than deferred, because deferring it would require an exclusion row and the exclusion list would not reach
    zero.

    Write both name constants with the share-link docblock's discipline: state that the role takes no name
    from its own text, and that the label is not a second copy of the sentence.
  </action>
  <verify>
    <automated>npx vitest run tests/listing && npm run test:design -- leak contrast && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-live' src/components/listing/address-autocomplete.tsx` reflects one region only, and the
      static hint's element carries no live-region attribute and no region role.
    - `grep -c 'role="status"' src/components/listing/address-autocomplete.tsx src/components/listing/photo-uploader.tsx`
      returns exactly 1 in each, and each of those elements carries an author-supplied label attribute.
    - `git diff src/components/listing/photo-uploader.tsx` changes exactly one element's attributes plus the
      name constant and its docblock — nothing else in that file moved.
    - `npx vitest run tests/listing` passes; `npx tsc --noEmit` exit 0.
    - `npm run test:design -- leak` passes — no comment quotes a banned spelling.
  </acceptance_criteria>
  <done>Both listing-side regions are named, the static hint is out of the region, and nothing is left needing an exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the set, widen it to the host, add four rows, take the exclusions to zero — and rewrite the guard</name>
  <read_first>
    - `src/lib/design/live-regions.ts` — :305-327 (the declared file set, seventeen files, whose NAME asserts
      it contains no host file), :329 (the closed union typed against it), :331-336 (the exclusion row type),
      :337-383 (the header's measured arithmetic and the one remaining exclusion), :420-448 (the row
      contract), and the numeric type alias near :1078-1095 whose NAME carries the count ON PURPOSE.
    - ⚠ `tests/design/live-regions.test.tsx` — :207 (the declared file count, pinned a SECOND time by design,
      cross-checked at :456-464), :488-506 (⚠ THE GUARD, whose non-empty assertion at :491 cannot hold once
      empty is correct, and whose phase-naming pattern at :492-493 stops having anything to match), and
      :554-660 (the both-directions scan: a declared row whose region is absent fails, and a region with no
      row fails).
    - 14-PATTERNS § 10 — the rename precedent: an earlier phase's numeric alias rename is the worked example.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` (the save-state region from 14-11) and
      `src/components/host/request-row.tsx` (the refusal region from 14-03) — read both so each row's file,
      kind and announced sentence are true.
    - 14-UI-SPEC § Live Regions — the three declared rows and the rename instruction; 14-RESEARCH § G9 and
      Open Question 2 for the recommended replacement guard.
  </read_first>
  <files>src/lib/design/live-regions.ts, tests/design/live-regions.test.tsx</files>
  <action>
    Rename the declared file set to a name that is true of its contents — a closed union containing host
    files cannot keep a name asserting it contains none, and this module's whole design is that its claims are
    compile-checked. Rename its derived union type to match. Update every consumer in the same commit.

    Widen the set with the four host-and-listing files this phase's regions live in: the address field, the
    photo uploader, the wizard and the request row. Re-measure the resulting count.

    Rename the numeric type alias so its NAME carries the new count, and update the count it extends. Update
    the test's own pinned count — it is pinned twice on purpose, and both must move together.

    Add four rows to the declared regions, each naming its file, its kind and the sentence a user actually
    hears:
    - the address lookup's resolved outcome,
    - the photo-uploader's status,
    - the wizard's save state,
    - the request action's refusal.

    Take the exclusion list to ZERO by removing its last row. Then RE-MEASURE both of the header's counting
    methods — the markup reading and the text reading — from scratch against the tree this commit produces,
    and rewrite the header's arithmetic paragraph with the new numbers and the reason the two disagree. Do
    not carry either number forward from the existing comment; the file's own record says that arithmetic has
    drifted before.

    ⚠ Then rewrite the guard test. Its old non-empty assertion was protecting against a gutted list making
    the reason-shape filter trivially green. Empty is now the CORRECT state, so the replacement asserts that
    the list is empty AND keeps the reason-shape filter exercised — against a local fixture row, so the guard
    still guards. The phase-naming pattern it used has nothing left to match; replace it with the fixture's
    own expectation. Say all of that in the test's own comment: this file's culture is that a gate explains
    itself.

    Do not delete a row to make a gate green. Do not weaken the both-directions scan.
  </action>
  <verify>
    <automated>npm run test:design -- live-regions && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- live-regions` passes, including the both-directions scan: every declared row's
      region is present and every region in the scanned set has a row.
    - `grep -rn 'BOOKER_PATH_LIVE_REGION_FILES' src/ tests/` returns ZERO hits — the rename is complete, not
      aliased.
    - The numeric type alias's NAME contains the new count, and the count it extends, the exported set's
      length and the test's pinned count are all the same number. Report that number in the SUMMARY.
    - `grep -n 'toBeGreaterThan(0)' tests/design/live-regions.test.tsx` returns nothing for the exclusion
      list, and the replacement guard is observed FAILING against a gutted reason field: temporarily blank a
      fixture row's reason, run, observe the failure, revert. Record the message in the SUMMARY — a guard
      that cannot fail is not a guard.
    - The exclusion list's length is zero, asserted by the test rather than by a grep alone.
    - The header's two re-measured counts are stated in the SUMMARY with the commands that produced them.
    - `npm run test:design` green; `npx tsc --noEmit` exit 0; `git diff --stat drizzle/` empty.
  </acceptance_criteria>
  <done>The inventory's name, its count, its rows and its empty exclusion list are all true at once, and the guard that could not survive that still guards.</done>
</task>

<task type="auto">
  <name>Task 3: Every status region on the five surfaces is named, and the two assertive validation regions are pinned</name>
  <read_first>
    - `src/components/availability/weekly-hours-editor.tsx:276` and
      `src/components/availability/blocks-editor.tsx:319` — the two alert-role validation messages. ⚠ The
      alert role is IMPLICITLY assertive. Neither is in the declared set nor the exclusion list, and an
      attribute-only scan for the assertive value would miss both.
    - `src/lib/design/live-regions.ts` as Task 2 leaves it.
    - The five surfaces as this phase leaves them: the dashboard, the inbox, the wizard, the bookings table
      and the availability route, plus the host and availability component directories.
    - `dom-accessibility-api` for accessible-name resolution, and `tests/design/price-surface.test.ts` for
      the AST-walk idiom if a source scan is the right shape.
    - 14-RESEARCH § G9 "Unbudgeted live regions" and Assumption A6 — the plan must take this DELIBERATELY, not
      discover it. 14-RESEARCH § The destructive-token census is the shape to copy: a PINNED PER-FILE MAP, not
      a zero-count scan.
    - 14-UI-SPEC § Live Regions falsifiable items 1 to 5, and acceptance criterion 37.
  </read_first>
  <files>tests/design/live-regions.test.tsx</files>
  <action>
    Extend the gate with the phase's own cross-cutting assertions, each with the reason in its message:

    1. **Every status-role element on the five Phase-14 surfaces resolves to a NON-EMPTY accessible name.**
       That role is named by its author, so an unnamed one is silent. Express it over the source of the five
       surfaces and their component directories.

    2. **Zero occurrences of the assertive live-region value** anywhere on those surfaces.

    3. **The implicitly-assertive alert role is pinned as a measured per-file map**, not scanned to zero.
       There are exactly two occurrences today, both native form-field validation messages — the one
       canonical assertive use, both user-initiated rather than fired on page load. Pin them by file with
       that reason. A NEW alert-role element anywhere on a Phase-14 surface fails this assertion.
       ⚠ This is the deliberate take on the research's open assumption: the two shipped regions are NOT
       converted, because changing a validation message's role is an accessibility behaviour change with no
       decision behind it — but they are made VISIBLE rather than left invisible to the gate.

    4. **The week strip renders zero live regions** — assert it directly against the strip's source, so a
       later "helpful" addition fails here rather than in review.

    Write each assertion so its failure message names the file and the element and states the rule. Where a
    number is pinned, the number came from a measurement in this commit; record each measured number in the
    SUMMARY.
  </action>
  <verify>
    <automated>npm run test:design -- live-regions</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- live-regions` passes.
    - Assertion 1 is observed FAILING against a deliberate regression: temporarily remove one region's label
      attribute, run, observe the failure naming the file, revert. Record the message in the SUMMARY.
    - Assertion 3 is observed failing against a deliberately added alert-role element on a Phase-14 surface,
      then reverted.
    - The pinned alert-role map contains exactly the two measured occurrences with their reason, and a grep
      over the two availability editor files confirms the count is two. Report it in the SUMMARY.
    - `npm run test:design` green with 0 failed; report the file and test counts against the 48 / 816 / 3
      baseline plus this phase's additions.
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Every region on the five surfaces is either named and declared, or pinned with its reason — and none of them is invisible to the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered region → assistive technology | An unnamed status region is silent; an assertive one interrupts; a region holding static content announces on every navigation |
| declared inventory ↔ the tree | Both directions are asserted: a declared row whose region is gone fails, and a region with no row fails |
| a guard ↔ the correct state | A guard that becomes unsatisfiable when the code is right must be rewritten, never deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14-SILENTREGION | Denial of service | status regions on the five surfaces | mitigate | Every status region gets an author-supplied name from a named constant, and the naming assertion is observed failing when a label is removed |
| T-14-14-NOISE | Denial of service | the address hint | mitigate | The static hint moves out of the region; the region holds only a resolved outcome, so it announces once per lookup rather than on arrival |
| T-14-14-ASSERTIVE | Denial of service | validation messages | mitigate | The assertive attribute is asserted at zero, and the implicitly-assertive role is pinned as a measured two-file map with its reason; a new one fails |
| T-14-14-GUTTEDGUARD | Repudiation | the rewritten guard | mitigate | The replacement asserts the empty state AND keeps the reason-shape filter exercised against a fixture, and is observed failing on a blanked reason (landmine 1) |
| T-14-14-STALECOUNT | Repudiation | the module header's arithmetic | mitigate | Both counting methods are re-measured from scratch in this commit rather than carried forward, and both numbers are recorded with their commands |
| T-14-14-RENAMEHALF | Tampering | the declared file set | mitigate | A repo-wide grep for the old name returns zero; the alias name, the extended count, the set length and the test's pin are all the same number |
| T-14-14-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** Any proposed install is a scope alarm |
</threat_model>

<verification>
- `npm run test:design` green with zero failures; `live-regions`, `leak` and `contrast` all pass.
- `npx vitest run tests/listing tests/host tests/availability` — zero failures.
- `npx tsc --noEmit` exit 0.
- `git diff --stat drizzle/` empty.
- No Playwright invocation here.
</verification>

<success_criteria>
- The exclusion list is zero and the last excluded file is genuinely discharged.
- The declared file set is renamed, widened, and its count is the same number in all four places it appears.
- Four rows are declared and all four regions exist.
- The non-empty guard is rewritten so it still guards, and has been observed failing.
- Every status region on the five surfaces is named; the two implicitly-assertive validation regions are
  pinned with their reason rather than left invisible.
</success_criteria>

<output>
Create `.planning/phases/14-host-tooling/14-14-SUMMARY.md` when done.
</output>
