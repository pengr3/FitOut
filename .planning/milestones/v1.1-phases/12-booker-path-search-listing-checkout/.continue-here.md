# Continue here — Phase 12, plan 12-14, Task 2

**Written:** 2026-08-19 · **HEAD at handoff:** `18e13f7` · **Tree:** clean except `.planning/config.json` (pre-existing)

## State

| | |
|---|---|
| Plans 12-01 … 12-13 | ✅ complete, 13 SUMMARYs committed |
| 12-14 Task 1 | ✅ **committed as `18e13f7`** (orchestrator-authored inline — subagent dispatch was down) |
| 12-14 Task 2 | ❌ **not started.** Zero edits landed. `visual-baselines.ts` still has `BaselineCountIsTwentySeven` and 27 rows |
| 12-14 Task 3 | ⛔ blocking human checkpoint — needs the operator |
| `12-14-SUMMARY.md` | ❌ not written |

Requirements: 11 of 12 delivered. STATE-07's mechanism and gate shipped in 12-13 but is left
unmarked because 12-14 also claims it.

## Why this handoff exists

Subagent dispatch failed with `API Error: 529 Overloaded` **six consecutive times**. Task 1 was
therefore executed inline by the orchestrator. Task 2 was deliberately NOT started inline: the
inventory and the spec must land in a single commit (the count assertion moves with the rows), and a
partial state that declares surfaces the spec cannot drive would mint baselines of closed lightboxes
and unfired collisions — the permanent, silent failure `visual-baselines.ts` exists to prevent.

## Critical Anti-Patterns

| # | Anti-pattern | severity |
|---|---|---|
| 1 | Committing `visual-baselines.ts` row additions WITHOUT the matching `surfaces.spec.ts` drive logic | warning |
| 2 | Minting a baseline locally, or adding an `updateSnapshots` ternary / CI condition | warning |
| 3 | Marking 12-14 done on the strength of the GENERATION run | warning |

None are `blocking` — they are recorded so the next executor does not rediscover them.

**On #2:** `playwright.config.ts` sets `updateSnapshots: "none"` unconditionally (D-28) and the
`visual` project is not constructed off Linux (D-29). `npx playwright test --project=visual` errors
project-not-found here, and the plan wants that exact message recorded as evidence. New rows stay RED
in CI until Task 3's dispatch — that is the correct intermediate state.

**On #3:** a `GITHUB_TOKEN` push triggers no workflow run, so the job that WRITES baselines never
compares against them. The deliverable is the **comparison** run's id, not the generation run's.

## What Task 1 already shipped (do not redo)

`scripts/seed-baseline-fixtures.ts` exports the constants the spec must import rather than
re-declare — importing it does **not** seed (direct-execution guard):

- `VRT_CLOCK_ISO = "2026-09-15T04:00:00Z"` (12:00 noon Asia/Manila)
- `VRT_COLLISION = { dayIso: "2026-09-16", startUtc, endUtc, startLabel: "9:00 AM", endLabel: "10:00 AM" }`
- `VRT_IDS = { exclusive: "vrt_listing_exclusive", open: "vrt_listing_open", farTennis: "vrt_listing_far_tennis" }`
- `VRT_EXCLUSIVE_TITLE = "Poblacion Boxing Room"`, `VRT_ORIGIN`, `VRT_HOST_ID`, `VRT_RIVAL_ID`

Measured, not assumed:

- **Idempotent** — two consecutive runs gave identical `listings=5 photos=21 bookings=2 hours=35 tags=11`
- `vrt_listing_exclusive`: 8 photos, `unit_count: 2`, exclusive, 0.53 km from origin, host payout-activated (so `/listings/[id]/book` is reachable, not a redirect)
- **The seeded conflict covers BOTH units** over `2026-09-16T01:00Z–03:00Z`. This is load-bearing: the GiST `EXCLUDE` arbitrates per *(listing, unit)*, so a single confirmed row would leave unit 2 free and the window bookable — and the collision baseline would be a picture of the feature not firing
- `vrt_listing_far_tennis` is the **only** tennis supply in the entire catalogue at **20.03 km** → `within10=false, within25=true` (the relaxation-band surface)
- Seeded rows were cleaned back out of the local dev DB, so the catalogue is as it was

`scripts/verify-baselines-workflow.mjs` — 11 parse-based invariants, each printing the value it read.
Watched red recorded: with a `push:` trigger added the parser exits 1 naming
`triggers=[workflow_dispatch, push]` while **all seven substring tokens remain PRESENT** — the
seventh independent reproduction of the header's six-of-six vacuity measurement. Reverted;
`git diff --exit-code` byte-identical.

`.github/workflows/baselines.yml` — postgis service, `DATABASE_URL` by service **LABEL** (this job
runs IN a container ⇒ `ci.yml` job 3's addressing case, not job 2's), migrate + seed steps before the
visual run, header invariant rewritten from "no baselined surface reads a database" to "a baselined
surface may read a seeded ephemeral database and must need nothing else". All 11 invariants green
after the edit: `services=["postgres"]`, run commands 6 → 8, still exactly 1 with `--update-snapshots`.

## Task 2 — the arithmetic, worked out

`og-listing` is **not** a new row. It already exists, BLOCKED, with
`url: "/listings/no-such-listing/opengraph-image"`. Task 1 seeds the published listing it was waiting
for, so Task 2 **unblocks** it: `blocked: null`, real URL from `VRT_IDS.exclusive`, and a `hookWhy`
that now means something.

```
/ with results               320 / 768 / 1280   both themes   =  6
/ zero-result WITH the band  320 / 1280         both themes   =  4
/listings/[id]               320 / 768 / 1280   both themes   =  6
/listings/[id] lightbox      1280               both themes   =  2
/listings/[id] sheet         375               both themes   =  2
/listings/[id]/book          320 / 1280         both themes   =  4   ← REQUIRES THE CLOCK FREEZE
collision notice             1280               both themes   =  2
/listings/[id] OG image      1200x630           court only    =  1   ← the EXISTING og-listing row
                                                              ----
new rows                                                        26
new total                                            27 + 26  =  53
```

- rename `BaselineCountIsTwentySeven` → the 53 total, **in the same commit** as the rows
- blocked surfaces 2 → **1** (`global-error` only), so a complete run commits **52** PNGs, not 25.
  The header states the 27/25 arithmetic in several places — update every one, because a header that
  misstates its own numbers is the defect one section down
- `ThemeSwapExclusionCountIsOne` stays 1
- add the 7 new ids to `SURFACE_IDS`
- every new surface needs `hook` + `hookWhy`. Use the testids the phase actually shipped — grep
  `src/lib/design/selector-contract.ts` for the declared set rather than guessing. Available:
  `search-relax-band`, `photo-lightbox`, `booking-panel`, `booking-sticky-bar`, `sheet-price-total`,
  `price-disclosure`, `checkout-sticky-bar`, `collision-notice`, `hold-countdown`,
  `rail-price-total`, `listing-key-facts`, `skeleton-calendar`

Spec must: install the clock **before navigating**; drive lightbox / sheet / collision each behind
its own reachability hook so a failed interaction fails the run instead of capturing the closed
state; and assert the two D-58 facts (captured byte length **≠ 25,844**, `alt` contains
`VRT_EXCLUSIVE_TITLE`) **before** any pixel comparison.

**Clock gotcha, measured in 12-03:** `page.clock.pauseAt(<absolute instant>)` does NOT move a running
page's digits. The working shape is freeze with `pauseAt`, then jump by a measured delta with
`fastForward`.

**Watched red the plan mandates:** revert the count assertion → `npx tsc --noEmit` fails with
`TS2344: Type 'false' does not satisfy the constraint 'true'` naming the alias → restore → green.
Record both verbatim.

## Resume

```bash
/gsd:execute-phase 12
```

## Standing reds (not introduced by 12-14)

1. **`notFound()` on `/listings/[id]` answers HTTP 200** — 12-08 proved with the `npm run build && npm start` discriminator that this survives a production build, so it is a real rendering-strategy defect, not a dev artefact. Body IS correctly the not-found boundary, so it is a status code issue and not a content leak. Awaiting a Rule 4 decision.
2. **Cross-file DB-contention flakes** — clear after `docker restart fitout-db-1`; the targeted trio is **28 passed**, matching 12-06 through 12-12 exactly.
3. **Three stale `dev-theme-*` baselines** from 12-01's gutter change — need the same CI dispatch as Task 3.

## Corrections to 12-UI-SPEC measured during execution

- the 41px calendar floor is really **38.58** (the spec computed 288÷7, ignoring the calendar's own padding and border)
- the `dark:` pin is **44 across 13 files**, not 54
- `/^Book/` collects `slot-picker.tsx`'s shipped `Book full day`, so the CTA family must be named rather than the control deleted
