---
phase: 12-booker-path-search-listing-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/design/measurements.ts
  - src/lib/design/contrast-pairs.ts
  - src/components/search/search-results.tsx
  - src/components/patterns/card-grid-skeleton.tsx
  - src/app/globals.css
  - tests/design/contrast.test.ts
  - tests/design/pair-drift.test.ts
  - tests/design/skeleton-measurements.test.ts
  - tests/design/sheet-absent.test.ts
  - tests/design/infra.test.ts
  - tests/search/search-card-open.test.tsx
  - e2e/skeleton-geometry.spec.ts
autonomous: true
requirements: [BFLOW-01]

must_haves:
  truths:
    - "D-37: the search-result card keeps the all-in rate and never a computed window total — the only card-level change this phase makes is the grid gutter, and a regression pin proves the meta order and the rate-only price survived"
    - "D-57: the +/-4px grid gutter is resolved mechanically — ResultsGrid and CardGridSkeleton read ONE constant, so they cannot disagree again, and the rendered gutters match on `/` itself rather than on /dev/theme"
    - "D-59: every measurement this phase spends is declared once in measurements.ts before any surface consumes it, so no later plan invents a value at a call site"
    - "GATE-06 is a standing per-run assertion rather than a promise: drizzle/ is at 0025 and a migration file added anywhere fails the DB-free design gate"
    - "The soft-accent notice's border-brand/30 edge — shipping since slot-picker.tsx and measured by nothing — is a declared, reasoned EXCLUDED_PAIRS row before the two Phase-12 adopters render it"
  artifacts:
    - path: "src/lib/design/measurements.ts"
      provides: "RESULT_GRID_GAP, CALENDAR_CELL, SLOT_CHIP_BOX, STICKY_BAR_HEIGHT, STICKY_BAR_CLEARANCE, HOLD_COUNTDOWN_BOX, MOSAIC_ASPECT — the 7 constants every later Phase-12 plan imports"
      contains: "RESULT_GRID_GAP"
    - path: "src/lib/design/contrast-pairs.ts"
      provides: "The brand-30-on-card EXCLUDED_PAIRS row with its measured value and its reason"
      contains: "brand-30"
    - path: "tests/design/infra.test.ts"
      provides: "The GATE-06 tree scan — drizzle/ at 0025, no migration added"
      contains: "0025"
  key_links:
    - from: "src/components/search/search-results.tsx"
      to: "src/lib/design/measurements.ts"
      via: "imported RESULT_GRID_GAP on ResultsGrid"
      pattern: "RESULT_GRID_GAP"
    - from: "src/components/patterns/card-grid-skeleton.tsx"
      to: "src/lib/design/measurements.ts"
      via: "imported RESULT_GRID_GAP replacing the gap-5 literal"
      pattern: "RESULT_GRID_GAP"
---

<objective>
Declare the seven measurement constants Phase 12 spends, add the one contrast exclusion its notices need,
and close the `[11-17]` gutter by giving the search grid and its skeleton a single owner.

Purpose: every later plan in this phase consumes a box class, a hit-area size or a bar height. A constant
declared here is a value one plan can be wrong about; a literal invented at nine call sites is nine.
Output: `measurements.ts` +7, `contrast-pairs.ts` +1 exclusion, one gutter, and two standing assertions
(the rendered-gutter comparison on `/`, and GATE-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-booker-path-search-listing-checkout/12-UI-SPEC.md
@.planning/phases/12-booker-path-search-listing-checkout/12-PATTERNS.md

<interfaces>
The seven constants, values taken verbatim from `12-UI-SPEC.md` § New measurement constants — derivations,
not inventions. Each carries a one-line comment giving its derivation:

  RESULT_GRID_GAP       = "gap-4 sm:gap-6"              // 16/24px, both declared ladder steps. gap-5 (20px) is retired.
  CALENDAR_CELL         = "[--cell-size:--spacing(11)]" // 44px. ui/calendar.tsx ships --spacing(7) = 28px; that is the BFLOW-05 debt.
  SLOT_CHIP_BOX         = "h-11 w-20"                   // 44 x 80px. One source for the shimmer and the real chip.
  STICKY_BAR_HEIGHT     = "h-16"                        // 64px, the same height as the desktop header.
  STICKY_BAR_CLEARANCE  = "pb-20"                       // 80px = 64 bar + 16 gap. The same arithmetic as lg:top-20, from the other end.
  HOLD_COUNTDOWN_BOX    = "h-8 min-w-24"                // 32 x 96px. Width reuses AUTH_SLOT_CONTROL rather than inventing a second reservation.
  MOSAIC_ASPECT         = "aspect-[16/9]"               // The gallery's outer box at every photo count.

The one new `EXCLUDED_PAIRS` row (12-UI-SPEC § Pairings Phase 12 requires):

  fg `brand-30` · bg `card` · measured ≈1.59 court / ≈1.50 grove

  `fg` names the COMPOSITE, not the raw token — the exclusion is about the 30% form specifically, and
  solid `brand on card` is a separate, passing 3.0-bar row that already exists. Its class is identical to
  the already-declared `destructive-40 on card`, `border` and `input` exclusions: a decorative container
  edge, never a control's sole visible boundary and never a focus indicator. The notice carries its
  meaning in `text-foreground` ink on the tint (17.04 / 16.24, declared) and a `text-brand` glyph
  (4.10 / 4.06, declared).

  ZERO new `CONTRAST_PAIRS` rows are required, and that is the finding rather than an oversight —
  12-UI-SPEC § Pairings lists all ten Phase-12 inks against the rows that already declare them.

`tests/design/contrast.test.ts` is the authority (D-12). If the culori gate disagrees with a ratio stated
above, the gate wins and the note is corrected — never the other way round.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The seven measurement constants and the one contrast exclusion</name>
  <files>src/lib/design/measurements.ts, src/lib/design/contrast-pairs.ts, tests/design/contrast.test.ts, tests/design/pair-drift.test.ts</files>
  <read_first>
    - src/lib/design/measurements.ts — the whole file. Its header at :22-26 states why the module lives inside `src/` (Tailwind's `source("../")` root) but outside the DS-13 leak-gate tree; the nine shipped constants are the shape the seven new ones copy.
    - src/lib/design/contrast-pairs.ts — the `ContrastPair` type, the mandatory `note` field, and the existing `EXCLUDED_PAIRS` rows for `destructive-40 on card`, `border` and `input`. The new row is the same class and its note must say so.
    - tests/design/contrast.test.ts and tests/design/pair-drift.test.ts — both pin row counts. Find EVERY pinned number before editing; record old -> new for each.
    - src/components/availability/slot-picker.tsx around the gap-hint block — the `bg-brand/10 border-brand/30 text-brand` recipe that ships today and is what this exclusion is about.
    - .planning/phases/12-booker-path-search-listing-checkout/12-UI-SPEC.md § Spacing Scale and § Color
  </read_first>
  <action>
Add exactly the seven constants in `<interfaces>` to `src/lib/design/measurements.ts`, as `const` string
literals beside the nine shipped ones, each with its derivation comment. Do not add an eighth. Do not
change any shipped constant's value.

Two of the seven carry a hazard note in the file, because a later plan will read the constant and not this
document. On `CALENDAR_CELL`: the variable override re-sizes the nav buttons, the weekday row and the
caption because all four read `--cell-size`, but it does NOT fix the day button — `ui/calendar.tsx`'s day
class carries `aspect-square` and `min-w-(--cell-size)`, so the day-button half goes through the
`components={{ DayButton }}` seam and must be MEASURED rather than assumed (RESEARCH Pitfall 2, owned by
plan 12-09). On `SLOT_CHIP_BOX`: the UI-SPEC derives `w-20` from "the real chip's minimum", and the real
chip has `min-h-11` but NO `min-w-20` (RESEARCH Pitfall 9) — so state that the constant is the shimmer's
box today and that whichever plan makes it the single source of both must add `min-w-20` to the real chip
in the same commit.

Add exactly one `EXCLUDED_PAIRS` row to `src/lib/design/contrast-pairs.ts` for `brand-30` on `card` with
the measured court/grove values and the reason in `<interfaces>`. The note must state (a) that `fg` names
the composite, (b) the class it shares with the three existing decorative-edge exclusions, and (c) the
compensating requirement — the notice's meaning is carried by declared, passing ink and glyph pairs, never
by the edge.

Then update every pinned row count in `tests/design/contrast.test.ts` and `tests/design/pair-drift.test.ts`.
Do not widen an assertion to make a red green: if the culori gate reports a different ratio than the one
above, correct the note.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/contrast.test.ts tests/design/pair-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/design/measurements.ts` exports exactly the seven new named constants with the exact string values in `<interfaces>`, and the nine shipped constants are byte-unchanged
    - `EXCLUDED_PAIRS` grew by exactly 1 and `CONTRAST_PAIRS` by exactly 0 (12-UI-SPEC AC#42)
    - The new exclusion row has a non-empty `note` naming the composite, the shared class and the compensating requirement
    - `npx vitest run --config vitest.design.config.ts tests/design/contrast.test.ts tests/design/pair-drift.test.ts` exits 0, in both themes at `bar + 0.05`
    - Every pinned count changed is recorded as old -> new in the SUMMARY
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Seven values have one owner and the notice edge is a declared, reasoned exclusion instead of an unmeasured shipped class.</done>
</task>

<task type="auto">
  <name>Task 2: One gutter for the search grid and its skeleton (D-57), and the card's regression pin (D-37)</name>
  <files>src/components/search/search-results.tsx, src/components/patterns/card-grid-skeleton.tsx, tests/design/skeleton-measurements.test.ts, tests/search/search-card-open.test.tsx</files>
  <read_first>
    - src/components/search/search-results.tsx — `ResultsGrid`, currently `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 lg:gap-6`. Read the whole component; the inline fetch-error block is FROZEN by `[11-18]` and must not be touched.
    - src/components/patterns/card-grid-skeleton.tsx — the inner grid, currently `grid gap-5 sm:grid-cols-2 lg:grid-cols-3`, and the comment block explaining why the fraction widths stay literal.
    - tests/design/skeleton-measurements.test.ts — the AST string-literal walk, the declared exemption list, and the guard-the-guard floor. The new assertion is added to this file's existing shape, not bolted on.
    - tests/search/search-card-open.test.tsx — case (9), the meta-order pin, and its header's "written FIRST against unchanged src/" discipline.
    - src/components/search/search-result-card.tsx — the rate-only price block and its header comment stating the D-37 rule. This file is NOT modified by this plan.
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/deferred-items.md `[11-17]` (both entries) and `[11-21]`'s closure note
  </read_first>
  <action>
Replace `ResultsGrid`'s gutter classes with the imported `RESULT_GRID_GAP`, leaving the column classes
(`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) in place. The `lg:` gutter step disappears — a third
gutter value at a third breakpoint is a number nobody can justify, and the grid already changes column
count at `sm:` and `lg:`.

Replace `CardGridSkeleton`'s `gap-5` with the same imported constant. `/host/listings` composes this
pattern and its gutter changes with it; that is intended (Phase 14 adopts the constant when it adopts the
skeleton, it does not get a second one) and must be stated in the SUMMARY as a knowingly-changed surface.
The string `gap-5` must survive nowhere in either file, comments included, or the retirement is only
partial.

Extend `tests/design/skeleton-measurements.test.ts` with one new assertion pair: both
`src/components/search/search-results.tsx` and `src/components/patterns/card-grid-skeleton.tsx` import
`RESULT_GRID_GAP` from `@/lib/design/measurements`, and neither contains the literal `gap-5`. Count over
comment-stripped source using this file's existing `stripComments` import — a bare `grep -c` over a file
that explains the hazard in prose counts the explanation. Carry a guard-the-guard: assert both files were
opened and are non-empty before asserting either absence.

Extend `tests/search/search-card-open.test.tsx` with one regression pin for D-37, in case (9)'s form: the
rendered card's full text contains the all-in rate parts and the exact string `Service fee included`, and
contains NO computed window total — asserted as the absence of any money string other than the declared
rate parts, over the whole rendered text, with a searched date+start+end supplied in props. This case must
be GREEN before and after this plan; it exists so a later plan cannot quietly add a total to the tile.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/skeleton-measurements.test.ts && npx vitest run tests/search/search-card-open.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gap-5" src/components/search/search-results.tsx src/components/patterns/card-grid-skeleton.tsx` returns nothing
    - Both files import `RESULT_GRID_GAP` from `@/lib/design/measurements`
    - `ResultsGrid` no longer carries `gap-4` or `lg:gap-6` as literals
    - The new `skeleton-measurements` assertion fails when `RESULT_GRID_GAP` is inlined back as a literal in `card-grid-skeleton.tsx` — watched red, recorded verbatim in the test file's header with its blast radius, then reverted
    - `tests/search/search-card-open.test.tsx` passes with the new D-37 pin and case (9) unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The two grids cannot disagree again because there is only one string, and the card's rate-only contract is pinned by a test rather than by a header comment.</done>
</task>

<task type="auto">
  <name>Task 3: The rendered gutter on `/`, the GATE-06 tripwire, and the --z-sheet zero restated</name>
  <files>e2e/skeleton-geometry.spec.ts, tests/design/infra.test.ts, tests/design/sheet-absent.test.ts, src/app/globals.css</files>
  <read_first>
    - e2e/skeleton-geometry.spec.ts — the whole file. `boxOf` and `expectSameBox` (the null-box guard is the load-bearing part), the both-themes loop, `document.fonts.ready` before any `boundingBox()`, and the NOT-COVERED footer that names `/dev/theme` as its blind spot.
    - e2e/helpers/served-document.ts — the truncator that produces a pending and a resolved state from ONE response; `[11-21]` names it as the way to measure this gutter on `/` itself. Copy the install-then-await-ready-then-goto ordering.
    - e2e/overflow-320.spec.ts — the `tell` reachability guard with its 15s timeout, and the both-themes viewport loop.
    - tests/design/infra.test.ts — the whole file; find where a tree scan already lives so the GATE-06 assertion joins the existing shape.
    - tests/design/sheet-absent.test.ts — `Z_SHEET_INVENTORY` and its emptiness assertion, plus the `package.json` scan for gesture libraries.
    - src/app/globals.css — the `--z-sheet` note in the z-index block.
  </read_first>
  <action>
Add a `/`-based case to `e2e/skeleton-geometry.spec.ts` that measures the SAME gutter twice on the SAME
route: the pending state's `[data-testid="skeleton-card-grid"]` inner grid and the resolved state's
`ResultsGrid`. Use `e2e/helpers/served-document.ts` to hold `/` in its pending state, read the first two
sibling cells' `boundingBox()`es and compute the horizontal gap between them, then release and repeat
against the resolved grid. Assert equality within the file's existing `TOLERANCE_PX`, at 320 / 768 / 1280,
in both themes, and additionally assert the absolute gap value (16px below `sm:`, 24px at and above it) —
two boxes that agree at the wrong number are as "equal" as two that agree at the right one. Copy the
reachability guard, the `document.fonts.ready` wait and the null-box guard verbatim; a blank `/` satisfies
"two numbers are close". Update the file's NOT-COVERED footer: the `/dev/theme` blind spot `[11-21]`
recorded is now covered for this one constant and for nothing else.

Add the GATE-06 tripwire to `tests/design/infra.test.ts`: read `drizzle/` from the repo root, assert the
lexically last `*.sql` file is `0025_audit_resolved_by.sql` and that the count of `*.sql` files equals the
count present today (measure it and pin the number). Guard-the-guard: assert the directory listing is
non-empty first. The failure message must say that a migration in a v1.1 phase is a scope alarm to raise,
never to absorb, and name GATE-06.

Update `src/app/globals.css`'s `--z-sheet` note to record that Phase 12 EXAMINED and DECLINED the step —
the booking sheet is `ResponsiveDialog`, i.e. the vendored dialog in another presentation, so it renders
at `--z-dialog`; the lightbox is a dialog; the sticky bars are `z-(--z-sticky)` and are therefore correctly
beneath the sheet's scrim. Mirror that reason into `tests/design/sheet-absent.test.ts`'s
`Z_SHEET_INVENTORY` emptiness message so a future reader meets it at the gate rather than in a CSS comment.
  </action>
  <verify>
    <automated>npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium && npx vitest run --config vitest.design.config.ts tests/design/infra.test.ts tests/design/sheet-absent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium` passes, including the new `/`-based case at 320 / 768 / 1280 in both `court` and `grove` (12-UI-SPEC AC#2)
    - The new case asserts BOTH the equality and the absolute 16/24px values, in separate expectations with distinct messages
    - Watched red: change `RESULT_GRID_GAP` to `"gap-4"` only -> the `sm:` and above cases fail naming the measured gap; reverted; both runs recorded in the spec header
    - `tests/design/infra.test.ts` fails when a zero-byte `drizzle/0026_probe.sql` is created, naming GATE-06; file removed; both runs recorded (12-UI-SPEC AC#48)
    - `Z_SHEET_INVENTORY` is still asserted empty and its message states Phase 12 examined and declined the step (12-UI-SPEC AC#46)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The gutter is measured where it actually renders, GATE-06 is a per-run assertion instead of a promise, and the `--z-sheet` zero is a contract rather than an accident.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared token inventory <-> rendered surface | A pairing that renders but is not declared is invisible to the AA gate |
| repository tree <-> release scope | A schema migration added inside a v1.1 phase silently breaks GATE-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-PAIRBLIND | Repudiation | `contrast-pairs.ts` | mitigate | The `brand-30 on card` edge ships today at `slot-picker.tsx` and is measured by nothing; declaring it with its reason puts it inside the culori gate before two more surfaces adopt it |
| T-12-01-GEODRIFT | Tampering | grid gutters | mitigate | One `RESULT_GRID_GAP` imported by both grids, plus a rendered `boundingBox()` comparison on `/` itself — the surface `[11-21]` proved `/dev/theme` cannot see |
| T-12-01-MIGRATION | Tampering | `drizzle/` | mitigate | `tests/design/infra.test.ts` tree scan pins the last migration at `0025` and the file count; watched red with a probe migration |
| T-12-01-VACUOUS | Repudiation | new design assertions | mitigate | Guard-the-guard on every absence assertion (files opened and non-empty; directory listing non-empty) and comment-stripped counting, so no zero passes because nothing was read |
| T-12-01-SC | Tampering | npm/registry installs | mitigate | This plan installs nothing. `git diff --stat package.json` must be empty at the end of every task; a new dependency here is a scope alarm to raise, never absorb |
</threat_model>

<verification>
- `npm run build` exits 0 (lint + test:design + next build)
- `npx tsc --noEmit` exits 0
- `npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium` green
- `git diff --stat package.json` is empty
- `ls drizzle/*.sql | tail -1` is still `drizzle/0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- The seven values Phase 12 spends are declared once, with derivations, before any surface consumes one
- The `[11-17]` gutter is closed mechanically and measured on the route where it actually renders
- GATE-06 and the `--z-sheet` zero are standing assertions rather than sentences in a document
</success_criteria>

<output>
Create `.planning/phases/12-booker-path-search-listing-checkout/12-01-SUMMARY.md` when done
</output>
