---
phase: 17-cross-cutting-audit-themes-responsive-a11y-baselines
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/overflow-320.spec.ts
  - src/components/patterns/site-chrome.tsx
autonomous: true
requirements: [RESP-03, GATE-02]

must_haves:
  truths:
    - "The /listings/[id] sheet-open row measures inside the dialog and reports zero offenders, closing [16-D9] which measured 48 (AC#3)"
    - "expectTargets runs after each surface's own tell rather than racing it, closing [15-12] (AC#9)"
    - "17-CONTEXT D-196 — ProfileLink's pointer target is >= 24px on BOTH axes at 320px, reached by padding on the link rather than a bigger glyph (AC#22)"
    - "The signed-in header cluster is re-measured against its 226px budget in the same commit, and a miss is escalated rather than absorbed by shrinking something else (AC#22, D-196)"
    - "The collectControls docblock no longer says the shell-chrome finding is not this plan's to fix, because it now is fixed"
  artifacts:
    - path: "e2e/overflow-320.spec.ts"
      provides: "The [16-D9] scope field, the [15-12] tell-ordering fix, and the AC#22 ProfileLink + cluster assertions"
      contains: "responsive-dialog"
    - path: "src/components/patterns/site-chrome.tsx"
      provides: "Padding on the ProfileLink anchor reaching the 24px conformance floor, with the docblock's 200/226 figures re-measured"
      contains: "ProfileLink"
  key_links:
    - from: "e2e/overflow-320.spec.ts"
      to: "e2e/helpers/overflow.ts"
      via: "the row's scope field consumed by the already-shipped expectNoOverflowWithin call at :772"
      pattern: "expectNoOverflowWithin"
    - from: "e2e/overflow-320.spec.ts"
      to: "src/components/patterns/site-chrome.tsx"
      via: "collectControls no longer excludes site-header, so ProfileLink enters the 24px scan"
      pattern: "site-header"
---

<objective>
Close the two inherited defects in the 320px harness — **[16-D9]** and **[15-12]** — and take
**17-CONTEXT D-196**: the [13-15] ProfileLink fix plus the deliberate re-open of the 226px header
budget, both in the same commit.

Purpose: these three are the harness's own credibility. [16-D9] means the row that exists
*specifically because* the sheet is where the 320px floor is hardest is the row proving nothing: with
the sheet open a Radix modal makes `<body>` a 320px box that clips its own content, so the
document-level clause cannot fire, and the per-element clause reported `clientWidth 320` with **48
named offenders**. [15-12] means AC#30's target-size scan can read controls out of a skeleton before
the resolved document replaces it. And [13-15]'s ProfileLink is a **16×16** pointer target below `sm:`
— 8px under WCAG 2.5.8 AA — on **every signed-in route**, and it is invisible to the shipped scan
because `collectControls` deliberately excludes the app shell.

This plan is the first of three that touch `e2e/overflow-320.spec.ts` (then 17-11, then 17-12); it
takes the edits that are corrections to the harness itself, so the coverage extension lands on a
harness that measures what it claims to.

Output: one added row field, one moved call, one narrowed exclusion with two new assertions, one
padding change, three rewritten docblocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-CONTEXT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md

<!-- ⚠ `.planning/STATE.md` is 499KB. Never read it whole — Grep for a phrase, then Read with
     offset/limit around the hit. -->

<interfaces>
From `e2e/overflow-320.spec.ts` — already shipped, to be used rather than rebuilt:
  :186   const THEMES = ["court", "grove"] as const;
  :296   type RouteRow = { name; path; skip?; tell; served?; open?; scope? }   // `scope` ALREADY EXISTS
  :372   const ROUTES: readonly RouteRow[]                                    // the AC#29 table (21 rows)
  :695   async function expectReachable(page, row, where)                     // the tell guard
  :709   test.describe(`AC#29 — nothing scrolls sideways at ${FLOOR_PX}px`)
  :772   if (row.scope) await expectNoOverflowWithin(page, row.scope, where)  // the CONSUMER already exists
  :905   const TARGET_FLOOR_PX = 24        // the CONFORMANCE floor (WCAG 2.5.8 AA)
  :950   async function collectControls(page): Promise<Control[]>
  :987   async function expectTargets(page, where)
  :1297  test.describe(`AC#30 / AC#22 — every Phase-13 surface at ${FLOOR_PX}px, in both themes`)
  :1570  const TOUCH_FLOOR_PX = 44         // the HOUSE floor, asserted BY NAME per surface — never blanket

From `src/components/patterns/site-chrome.tsx`:
  :114   const NAV_LINK_CLASS = "text-sm font-medium underline-offset-4 hover:underline"   // NO padding
  :206   the single <nav data-testid="site-nav">
  :348-377 ProfileLink and its docblock, which records the measured budget: 226px available, 200px used
  ProfileLink renders <Link aria-label="Profile" className={cn(NAV_LINK_CLASS, "inline-flex items-center gap-1.5")}>
                        <UserIcon aria-hidden="true" className="size-4" />
                        <span className="hidden sm:inline">Profile</span>
                      </Link>
  → 16×16 below `sm:`.

⚠ A blanket 44px assertion would be RED against shipped, reviewed, correct code — the app ships
deliberate sub-44 controls (the 32px header cluster, dense host table actions). The measurement is
recorded at `overflow-320.spec.ts:891-902`. 24 is the conformance bar; 44 is asserted BY NAME.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close [16-D9] and [15-12] — scope the sheet row, and run expectTargets after the tell</name>
  <files>e2e/overflow-320.spec.ts</files>
  <read_first>
    - `e2e/overflow-320.spec.ts` :296-340 (the `RouteRow` type and its `scope` field with its prose), :372-430 (the `ROUTES` table, and specifically the `/listings/[id] · sheet open` row around :394-418), :689-707 (`expectReachable`), :709-776 (the AC#29 loop, including the ordering at :757-767 where `row.open` runs, then `expectReachable`, then the measurements, and the `scope` consumer at :772)
    - `e2e/overflow-320.spec.ts` :1297-1560 — the AC#30 Phase-13 block, and specifically where `expectTargets` is called relative to that block's tell
    - `e2e/helpers/overflow.ts` :186-280 — `measureOverflowWithin` / `expectNoOverflowWithin` and the measured argument for why a Radix modal retires all three document-level clauses (a 500px div inside an open sheet reported `scrollWidth 320, offenders []`)
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-RESEARCH.md` § Code Examples → example 3 (the whole [16-D9] fix, one line), and § Common Pitfalls → **Pitfall 4** (the [15-12] race and its axe twin)
  </read_first>
  <action>
    **[16-D9] is one line.** Add `scope: '[data-testid="responsive-dialog"]'` to the
    `/listings/[id] · sheet open` row. The loop at :772 already reads the field and calls
    `expectNoOverflowWithin`, so no loop change is needed. Put the measurement in a trailing comment on
    that line: the row reported 48 named offenders on the sheet when measured this way, and reported
    `scrollWidth 320, offenders []` before, because with the sheet open `<body>` becomes a 320px box
    that clips its own content.

    Run it and read the result. If the scoped measurement reports offenders, that is the finding [16-D9]
    predicted, and classifying it is the work: an offender fixable with `min-w-0` or a `truncate` on a
    container is "may fix in place"; an offender that can only be resolved by dropping a control, a
    disclosure or a declared measurement is escalate-class and goes to plan 17-13 with its number, its
    theme, its width and its route. AC#3 requires the row to reach **zero** offenders, so if the
    remaining offenders are escalate-class, record that explicitly as an acceptance criterion this phase
    could not reach and raise it immediately — D-199 exception (b), a finding that makes an acceptance
    criterion unreachable, is one of the two named reasons to interrupt rather than batch.

    **[15-12] is a move, not a new assertion.** In the AC#30 Phase-13 block, `expectTargets` currently
    runs without waiting for the row's own tell, so it can read controls out of a skeleton — the recorded
    finding. Move the call so it runs AFTER `expectReachable`, matching the ordering the AC#29 loop
    already uses at :757-767 (`row.open` first where present, because that guard's `tell` is the thing
    the interaction produces; then `expectReachable`; then every measurement). Do not change what
    `expectTargets` asserts or its floor.

    Record both closures in the file header in the house idiom, and annotate the two shipped findings'
    rows in `.planning/phases/16-image-crop-framing/deferred-items.md` and
    `.planning/phases/15-auth-profile-transactional-email/deferred-items.md` — those stay in their own
    phases' files per D-199; keep the row and annotate it CLOSED with the date and this plan's id, never
    delete it, because the MEASUREMENT is the useful part.
  </action>
  <acceptance_criteria>
    - `grep -c "scope: '\[data-testid=\"responsive-dialog\"\]'" e2e/overflow-320.spec.ts` returns ≥ 1
    - That line's row is the `/listings/[id] · sheet open` row — verified by the row `name` within 20 lines above
    - In the AC#30 block, the `expectTargets(` call appears at a HIGHER line number than that block's `expectReachable(` call
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1` runs, and the sheet-open row reports zero offenders — or, if not, the remaining offenders are enumerated in the SUMMARY with theme, width and route, and flagged as a D-199 exception (b) immediate escalation
    - `.planning/phases/16-image-crop-framing/deferred-items.md` contains a `CLOSED` annotation on the D9 row naming plan `17-06`, with the original measurement text still present
    - `.planning/phases/15-auth-profile-transactional-email/deferred-items.md` contains the same for the [15-12] row
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run db:up && npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1 --reporter=list</automated>
  </verify>
  <done>The sheet row measures inside the dialog and reports zero offenders (or an escalated, enumerated set); the target scan waits for the surface it measures; both inherited rows are annotated closed in their own phases' files with their measurements intact.</done>
</task>

<task type="auto">
  <name>Task 2: D-196 — pad ProfileLink to the 24px conformance floor and re-measure the 226px cluster</name>
  <files>src/components/patterns/site-chrome.tsx</files>
  <read_first>
    - `src/components/patterns/site-chrome.tsx` :100-130 (`NAV_LINK_CLASS` at :114 and the four header links that share it), :195-215 (the single `<nav data-testid="site-nav">`), :330-380 (`ProfileLink` and its docblock at :348-377, including the two-mechanisms-two-reasons paragraph and the measured `200px used / 226px available` figures)
    - `.planning/phases/13-confirmation-bookings-trust/deferred-items.md` — the [13-15] row with the original 226px/200px measurement and the note that the obvious `p-2` costs 16px in the cluster the label was already dropped from
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-CONTEXT.md` § D-196 — padding, not a bigger glyph; re-measure in the same commit; a miss is a finding to escalate, never something to make room for silently
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md` § Copywriting Contract — the `hidden` vs `aria-label` paragraph: the two mechanisms must not be merged, and this repo has already shipped that bug once
  </read_first>
  <action>
    Add padding to reach ≥ 24px on BOTH axes at 320px. The padding goes on the `<Link>`'s own `cn()`
    argument — **not** on `NAV_LINK_CLASS`, which four header links share and widening all four is a
    header change nobody asked for; and **not** on the glyph, because D-196 says padding, not a bigger
    icon. `size-4` on `UserIcon` stays exactly as it is.

    A 16×16 glyph needs ≥ 4px on each axis to clear 24. Choose from the declared spacing ladder only —
    `p-1` (4px) reaches exactly 24×24, `p-1.5` (6px) reaches 28×28. Phase 17 authors **zero** new spacing
    values, so do not reach for an arbitrary value; pick a ladder step and say in the docblock which one
    and what it measures to.

    Do NOT merge the two mechanisms. `hidden sm:inline` on the label removes it from the accessibility
    tree (correct here — it is the nav-duplication case), and `aria-label="Profile"` on the wrapper with
    `aria-hidden="true"` on the glyph names the icon-only control. Both stay exactly as shipped.

    **Re-measure the signed-in cluster in the same commit.** The docblock records 200px used against
    226px available; padding adds width. Measure the rendered cluster at 320px signed in, update the
    docblock's two figures to the new measurement, and state the new headroom. If the cluster no longer
    fits its 226px budget, **that is a finding to escalate** — record it with its number for plan 17-13
    and raise it under D-199 exception (b) if it makes AC#22 unreachable. Do NOT shrink something else,
    drop another label, or reduce a gap to make room; D-196 says the re-open is deliberate and a miss is
    a finding, not a licence to redesign the header.

    Rewrite the docblock's budget paragraph in the same commit so it records the new figures and the
    D-196 re-open, not the old ones. A docblock stating a superseded measurement beside the line that
    superseded it is the defect class the UI-SPEC's "may fix in place" table sanctions correcting.
  </action>
  <acceptance_criteria>
    - `src/components/patterns/site-chrome.tsx`'s `ProfileLink` `<Link>` `cn()` call contains a padding utility from the declared ladder (`p-1`, `p-1.5` or `p-2`)
    - `NAV_LINK_CLASS` at :114 is unchanged: `git diff src/components/patterns/site-chrome.tsx | grep -c '^[-+].*NAV_LINK_CLASS =' ` returns `0`
    - `UserIcon`'s `size-4` is unchanged and `aria-hidden="true"` is still present
    - `aria-label="Profile"` is still on the `<Link>` wrapper and `hidden sm:inline` is still on the `<span>`
    - The docblock no longer contains the old pair of figures unqualified — it states a NEW measured `used / 226` pair and names `D-196`
    - `npm run test:design` exits 0 (no new spacing value, no leak, no token drift)
    - `npm run lint` and `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <note>Runtime tradeoff (recorded): `npm run build` = `lint && test:design && next build` and is well
    over 30s (test:design alone is 95s measured). Kept because this task edits product source
    (`site-chrome.tsx`) and `next build` is the only stage that compiles it; the padding change must
    additionally clear the leak, spacing and token-drift gates inside `test:design`. Fast inner-loop
    signal while iterating: `npx tsc --noEmit` then `npx vitest run --config vitest.design.config.ts
    tests/design/token-drift.test.ts tests/design/leak.test.ts` (seconds). The 226px re-measure itself is
    proved by Task 3's e2e run, not by the build.</note>
  </verify>
  <done>The ProfileLink pointer target clears 24px on both axes via padding on the link, the glyph and both accessibility mechanisms are untouched, and the header budget is re-measured and re-recorded rather than quietly absorbed.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow the collectControls shell exclusion and assert AC#22</name>
  <files>e2e/overflow-320.spec.ts</files>
  <read_first>
    - `e2e/overflow-320.spec.ts` :885-1015 — the whole target-size block: the :891-902 measured argument for why 24 and not 44, `TARGET_FLOOR_PX = 24` at :905, the `Control` type at :907, `collectControls` at :950-984 including the `site-header` / `site-footer` `continue` at ~:975, and its docblock at :921-933 which records the shell-chrome finding and says it is NOT this plan's to fix
    - `e2e/overflow-320.spec.ts` :240-286 — `signUpAndReachProfile`, the resolver that produces a signed-in session, and its ⚠ note that it is deliberately NOT memoised because it produces a session cookie and the `page` fixture is per-test
    - `e2e/overflow-320.spec.ts` :45-55 — the note that plans 15-10 and 16-15 forbade editing pre-existing rows and comments; those constraints belonged to those plans, but the idiom stands: flag a superseded phrase as history in the file header rather than silently correcting it in place
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-PATTERNS.md` § `e2e/overflow-320.spec.ts` → edit 4 (AC#22), which quotes the exact `continue` block to delete or narrow and states the docblock-rewrite duty
  </read_first>
  <action>
    Narrow the exclusion at ~:975 so the site header's controls enter the 24px scan. Prefer narrowing to
    deleting: `site-footer` may still carry a documented sub-24 case, and the point of D-196 is to
    re-open the **header** budget specifically. If narrowing to header-only leaves the footer excluded,
    say why in the code, with its measurement — a row without a reason is not a row.

    Then add the AC#22 assertions, on a signed-in surface at 320px, using the existing
    `signUpAndReachProfile` resolver rather than a new fixture:
    - the `ProfileLink` control's rendered box is ≥ `TARGET_FLOOR_PX` (24) on BOTH axes;
    - the signed-in header cluster's total rendered width is ≤ 226px.
    Both are measurements with named numbers in the failure message: the measured width, the measured
    height, the budget, and the headroom. Do NOT assert 44 here — 44 (`TOUCH_FLOOR_PX`) is asserted by
    name per surface and a blanket 44 is red against shipped, reviewed, correct code; that argument is
    already written at :891-902 and this task must not re-litigate it.

    **Rewrite `collectControls`'s docblock at :921-933 in the same commit.** It currently records the
    shell-chrome finding and states it is not this plan's to fix. It now is fixed. Leaving that sentence
    standing beside a fixed finding is precisely the defect class Phase 15 exists to repair, and the
    file's own header says so about itself at :342-349. The replacement records what the exclusion now
    covers, why, and that D-196 retired the header half.

    Annotate the [13-15] row in `.planning/phases/13-confirmation-bookings-trust/deferred-items.md` as
    CLOSED with the date and this plan's id, keeping the original measurement text — the row is kept and
    annotated, never deleted.

    Run the file. If the cluster assertion goes red because Task 2's padding pushed the cluster past
    226px, that is D-196's named escalation path: record the measurement for plan 17-13 and raise it
    immediately under D-199 exception (b), because it makes AC#22 unreachable. Do not shrink another
    header element to make it pass.
  </action>
  <acceptance_criteria>
    - `collectControls` no longer skips `site-header`: `grep -c 'site-header' e2e/overflow-320.spec.ts` shows the string only inside the AC#22 assertion and/or the rewritten docblock, not inside a `continue` guard
    - The file contains a ≥ 24 assertion on both width and height of the ProfileLink control, referencing `TARGET_FLOOR_PX`
    - The file contains a ≤ 226 assertion on the signed-in header cluster width, with `226` in the failure message
    - `grep -c 'TOUCH_FLOOR_PX' e2e/overflow-320.spec.ts` is unchanged from before this task (no blanket 44 was introduced)
    - The stale sentence is gone: the `collectControls` docblock no longer says the shell-chrome finding is not this plan's to fix
    - `.planning/phases/13-confirmation-bookings-trust/deferred-items.md` contains a `CLOSED` annotation naming plan `17-06`, with the original 226/200 measurement still present
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1` green, or every failure already present in `e2e-baseline-reds.md`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run db:up && npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1 --reporter=list</automated>
  </verify>
  <done>ProfileLink is inside the 24px conformance scan and measures ≥24 on both axes at 320px; the header cluster is measured against 226px with the number in the message; the docblock that said this was not fixable here is rewritten; [13-15] is annotated closed in its own phase's file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered DOM under a modal → RESP-03 verdict | A Radix modal makes `<body>` a 320px box that clips its own content, retiring the document-level clause while it still reports green |
| App shell → every signed-in route | A control excluded from a scan is a control nobody measured, on every route at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-24 | Repudiation | `/listings/[id] · sheet open` row | mitigate | `scope: '[data-testid="responsive-dialog"]'` routes the row through `expectNoOverflowWithin`, which carries its own vacuity guard; the trailing comment records the 48-vs-0 measurement so the fix cannot be read as cosmetic. |
| T-17-25 | Repudiation | AC#30 `expectTargets` | mitigate | Call moved to run after `expectReachable`, matching the AC#29 ordering; a scan over a skeleton is now a failure of the tell guard rather than a green over the wrong tree. |
| T-17-26 | Repudiation | `collectControls` shell exclusion | mitigate | Exclusion narrowed rather than deleted, with the surviving half carrying its measured reason; the docblock that declared the finding out of scope is rewritten in the same commit so no stale sentence licenses re-excluding it. |
| T-17-27 | Tampering | header layout under a failing 226px re-measure | mitigate | D-196 forbids shrinking another element to make room. A miss is escalated under D-199 exception (b) (an unreachable acceptance criterion), asserted by requiring the measurement in the SUMMARY rather than a passing run. |
| T-17-28 | Elevation of privilege | `signUpAndReachProfile` creating real accounts | accept | Drives the shipped signup form against the local dev server and a local fixture DB only; `Date.now()` supplies email uniqueness and never reaches a measured string. Pre-existing mechanism (plan 15-10), unchanged here. |
| T-17-29 | Tampering | `drizzle/` | accept | This plan touches no schema; the phase verification asserts `git status --porcelain drizzle/` is empty. |
</threat_model>

<verification>
- `npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1` green, or every failure present in `e2e-baseline-reds.md`
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `git status --porcelain drizzle/` prints nothing (GATE-06)
- Three `deferred-items.md` files (phases 13, 15, 16) each carry a CLOSED annotation naming plan 17-06, with their original measurements intact
</verification>

<success_criteria>
- AC#3 holds: the sheet row is scoped and reports zero offenders, or the survivors are enumerated and escalated immediately
- AC#9 holds: `expectTargets` runs after the tell
- AC#22 holds: ProfileLink ≥ 24px on both axes at 320px AND the signed-in cluster measured against 226px, in the same commit, per D-196
- No blanket 44px assertion was introduced; `NAV_LINK_CLASS` and the glyph are untouched; no new spacing value
- Zero schema migrations proposed or absorbed (AC#32)
</success_criteria>

<output>
Create `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-06-SUMMARY.md` when done
</output>
