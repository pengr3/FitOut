---
phase: 10-design-system-foundation-theme-runtime
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/app/globals.css
  - src/lib/design/contrast-pairs.ts
  - tests/design/font-cycle.test.ts
  - tests/design/theme-tokens.test.ts
  - tests/design/contrast.test.ts
autonomous: true
requirements: [DS-01, DS-06, DS-07, THEME-02, THEME-03]

must_haves:
  truths:
    - "DS-01: the app renders in Geist — the compiled CSS contains no self-referential custom property and `.font-sans` resolves to `var(--font-geist-sans)`"
    - "D-04: two named themes exist, `court` and `grove`, as unlayered equal-specificity `[data-theme]` blocks in source order"
    - "D-03: both themes are light-background and the `.dark` block stays dormant and untouched"
    - "D-11: `--brand` itself is darkened; no second brand token and no `--brand-strong` escape hatch exists"
    - "D-12: every corrected colour is derived by the culori solver at AA_EPSILON = 0.05, not hand-picked"
    - "D-13: a declared inventory of legal foreground/background pairings exists and every pair clears its bar in BOTH themes with alpha pairings composited first"
    - "THEME-02: both theme blocks declare the identical token key set — a key in one and not the other is a silent fallthrough"
    - "THEME-03: `grove` ships in the same commit as `court`"
    - "DS-07: every token in both themes is inside the sRGB gamut"
  artifacts:
    - path: "src/app/globals.css"
      provides: "The two-theme colour and radius contract"
      contains: "[data-theme=\"grove\"]"
    - path: "src/lib/design/contrast-pairs.ts"
      provides: "The declared pair inventory (D-13) plus AA_EPSILON"
      exports: ["CONTRAST_PAIRS", "EXCLUDED_PAIRS", "AA_EPSILON", "TEXT_BAR", "NON_TEXT_BAR"]
    - path: "tests/design/contrast.test.ts"
      provides: "DS-06 + DS-07 proof in both themes"
    - path: "tests/design/theme-tokens.test.ts"
      provides: "THEME-02 key-set equality proof"
  key_links:
    - from: "tests/design/contrast.test.ts"
      to: "src/app/globals.css"
      via: "readThemeTokens() from config/design-tokens-source.mjs"
      pattern: "readThemeTokens"
    - from: "tests/design/font-cycle.test.ts"
      to: "src/app/globals.css"
      via: "compileGlobalsCss() from tests/design/helpers/compile-css.ts"
      pattern: "compileGlobalsCss"
---

<objective>
Fix the `--font-sans` cycle, strip 13 dead tokens, and author the two `[data-theme]` colour blocks with
every value derived rather than picked — then prove all 29 declared pairings clear WCAG AA in both themes.

Purpose: DS-01 changes the rendered typeface of every screen, so it must land before anything else in this
milestone is measured or captured. DS-05/DS-06 land here for the same reason the roadmap gives: before
fifty surfaces are built on values that fail the gate. And `grove` ships in this same commit because it is
D-128's enforcement test, not a feature.

Output: a `globals.css` whose `:root, [data-theme="court"]` and `[data-theme="grove"]` blocks declare the
identical key set, a committed pair inventory, and three green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the --font-sans cycle and strip the 13 dead tokens</name>
  <files>src/app/globals.css, tests/design/font-cycle.test.ts</files>
  <read_first>
    - `src/app/globals.css` — the whole 145-line file. The cycle is at `:10`; `--font-heading` at `:12`;
      the 13 dead `--color-sidebar-*` / `--color-chart-*` entries at `:13-25`; the `--chart-*` and
      `--sidebar-*` declarations at `:80-84` and `:86-93` in `:root` and `:120-132` in `.dark`; the
      already-derived radius chain at `:46-52`; and `@layer base`'s `html { @apply font-sans; }` at
      `:142-144`, which is the one line that carries the fix to every screen.
    - `tests/design/helpers/compile-css.ts` — `compileGlobalsCss` and the two accessors.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md` § Code Examples, the
      `--font-sans` fix, including the verified compiled output.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Full per-theme token
      contract — the paragraph explaining why the 13 sidebar/chart tokens are removed.
  </read_first>
  <action>
    Edit the EXISTING `@theme inline` block in place. Do not restructure it, do not reorder it, and never
    remove the word `inline` — `inline` is the entire mechanism behind THEME-04, and dropping it leaves the
    `<html>`-level switch working while every nested `[data-theme]` subtree silently renders the root theme.

    Change `:10` from `--font-sans: var(--font-sans);` to `--font-sans: var(--font-geist-sans);` and `:12`
    from `--font-heading: var(--font-sans);` to `--font-heading: var(--font-geist-sans);`. The
    `--font-heading` token is KEPT, not dropped (D-20): it stays an alias of the Geist sans family so real
    branding later has a seam to slot a display face into. `--font-geist-sans` is already defined on
    `<html>` by `geistSans.variable` in `src/app/layout.tsx`, so no change to `layout.tsx`'s font setup is
    needed in this plan.

    Delete these 13 entries from `@theme inline`: `--color-sidebar`, `--color-sidebar-foreground`,
    `--color-sidebar-primary`, `--color-sidebar-primary-foreground`, `--color-sidebar-accent`,
    `--color-sidebar-accent-foreground`, `--color-sidebar-border`, `--color-sidebar-ring`, and
    `--color-chart-1` through `--color-chart-5`. Delete the matching 8 `--sidebar-*` and 5 `--chart-*`
    declarations from `:root`, and the same 13 from `.dark`. They are grep-verified as zero-referenced
    anywhere in `src/` and no shadcn CSS reads them; carrying them means authoring 13 grove values nobody
    will ever see, each an unverifiable pairing. Add a one-line comment where they were, worded so it does
    not itself contain the strings `--sidebar` or `--chart-`, recording that a future
    `npx shadcn add sidebar` or `npx shadcn add chart` must re-add those token families to BOTH theme
    blocks.

    Add `--color-destructive-foreground: var(--destructive-foreground);` to `@theme inline` next to
    `--color-destructive` — it is a new token introduced by the button hierarchy in plan 10-06.

    Leave `@custom-variant dark (&:is(.dark *));` at `:5` alone and leave the rest of the `.dark` block
    alone: D-03 keeps it dormant as a cheap future theme.

    Create `tests/design/font-cycle.test.ts` using `compileGlobalsCss()`. Assert: (a) the compiled output
    contains no declaration of the self-referential form `--X: var(--X)` for any X — scan every
    `--name: var(--name)` pair rather than only checking `--font-sans`; (b) the compiled
    `--font-sans` value is `var(--font-geist-sans)`; (c) the compiled `--default-font-family` value is
    `var(--font-geist-sans)`; (d) the compiled `.font-sans` rule sets `font-family` to
    `var(--font-geist-sans)`. Add the repo's guard-the-guard `it()`: assert the compiled string is longer
    than 10000 characters and contains at least one `--font-` declaration, so the real assertions cannot
    pass vacuously against an empty compile.
  </action>
  <verify>
    <automated>npm run test:design -- font-cycle</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- font-cycle` exits 0 with at least 5 passing assertions.
    - `grep -c -- "--font-sans: var(--font-sans)" src/app/globals.css` returns 0.
    - `grep -c -- "--font-heading" src/app/globals.css` returns 1 (D-20 — the seam survives).
    - `grep -c -- "--sidebar" src/app/globals.css` returns 0.
    - `grep -c -- "--chart-" src/app/globals.css` returns 0.
    - `grep -c "@theme inline" src/app/globals.css` returns 1.
    - `grep -c -- "--color-destructive-foreground" src/app/globals.css` returns 1.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The compiled stylesheet resolves `font-sans` to Geist, and 13 dead tokens no longer need grove values.</done>
</task>

<task type="auto">
  <name>Task 2: Author the court and grove colour blocks</name>
  <files>src/app/globals.css, tests/design/theme-tokens.test.ts</files>
  <read_first>
    - `src/app/globals.css` — the `:root` block at `:55-94` as it stands after Task 1.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Full per-theme token
      contract (the 19-row value table) and § Resolved Open Questions 1, 3 and 4 (grove's teal-ward brand,
      the `--success` move, the global re-derived `--destructive`).
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md` § Pattern 1 — the exact
      cascade shape and the two forbidden selector forms.
    - `config/design-tokens-source.mjs` — the parser this task's test reads through.
  </read_first>
  <action>
    Replace the `:root {` selector with `:root,` on one line and `[data-theme="court"] {` on the next, and
    append a new unlayered `[data-theme="grove"] { … }` block immediately after that block and BEFORE
    `.dark`. Both blocks are unlayered, equal specificity (0,1,0), and source order decides — so grove must
    come second. Never write `:root[data-theme="grove"]` or `html[data-theme="grove"]`: those raise
    specificity but stop matching a nested `<div data-theme="grove">`, which silently kills THEME-04 while
    the app-wide switcher keeps working.

    Both blocks declare the IDENTICAL key set. The values, court then grove:

    `--background` `oklch(1 0 0)` / `oklch(0.988 0.006 190)`;
    `--foreground` `oklch(0.145 0 0)` / `oklch(0.17 0.02 190)`;
    `--card` `oklch(1 0 0)` / `oklch(1 0 0)`;
    `--card-foreground` `oklch(0.145 0 0)` / `oklch(0.17 0.02 190)`;
    `--popover` `oklch(1 0 0)` / `oklch(1 0 0)`;
    `--popover-foreground` `oklch(0.145 0 0)` / `oklch(0.17 0.02 190)`;
    `--primary` `oklch(0.205 0 0)` / `oklch(0.24 0.03 190)`;
    `--primary-foreground` `oklch(0.985 0 0)` / `oklch(0.985 0.004 190)`;
    `--secondary` `oklch(0.97 0 0)` / `oklch(0.958 0.010 190)`;
    `--secondary-foreground` `oklch(0.205 0 0)` / `oklch(0.24 0.03 190)`;
    `--muted` `oklch(0.97 0 0)` / `oklch(0.958 0.010 190)`;
    `--muted-foreground` `oklch(0.53 0 0)` / `oklch(0.50 0.015 190)`;
    `--accent` `oklch(0.97 0 0)` / `oklch(0.958 0.010 190)`;
    `--accent-foreground` `oklch(0.205 0 0)` / `oklch(0.24 0.03 190)`;
    `--brand` `oklch(0.58 0.208 25)` / `oklch(0.5445 0.09 190)`;
    `--brand-foreground` `oklch(0.985 0 0)` / `oklch(0.985 0.004 190)`;
    `--success` `oklch(0.58 0.15 150)` in BOTH themes;
    `--success-foreground` `oklch(0.985 0 0)` / `oklch(0.985 0.004 190)`;
    `--destructive` `oklch(0.535 0.215 27.325)` in BOTH themes;
    `--destructive-foreground` `oklch(0.985 0 0)` / `oklch(0.985 0.004 190)` — a NEW token;
    `--ring` `oklch(0.45 0 0)` / `oklch(0.45 0.02 190)`;
    `--border` `oklch(0.922 0 0)` / `oklch(0.905 0.012 190)`;
    `--input` `oklch(0.922 0 0)` / `oklch(0.905 0.012 190)`;
    `--radius` `0.625rem` / `1.25rem`.

    Keep the existing one-line `/* … */` comment convention above each semantic group and cite the
    derivation inline (repo convention B — decision IDs are cited at the point of the decision). Required
    comment content: above `--brand`, that D-11 darkens the brand itself and the accent visibly deepens
    everywhere, and that D-12 derived `oklch(0.58 0.208 25)` as the LIGHTEST coral at hue 25 clearing
    4.5 + AA_EPSILON against `--brand-foreground` (4.57:1) — no `--brand-strong` escape hatch exists;
    above `--success`, that D-14 keeps the hue in the icon only and the value was re-derived because the
    shipped `oklch(0.62 0.17 150)` measures 3.01:1 on grove's `--muted`; above `--destructive`, that the
    shipped `oklch(0.577 0.245 27.325)` was outside the sRGB gamut (DS-07) and the replacement is at the
    gamut ceiling and clears its own `/10` tint in both themes; above `--ring`, that D-132 makes it a
    darkened neutral and NOT `--brand`, and that the token value alone is insufficient — the `/50`
    removal is plan 10-04 and 10-06; above `--border`/`--input`, that they are declared decorative
    dividers at 1.26:1 / 1.28:1 and may never be a control's sole visible boundary or its sole focus
    indicator.

    If the solver in Task 3 disagrees with any hex above, the test wins (D-12): re-solve with the same rule,
    update `globals.css` and correct the corresponding row in
    `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Full per-theme token
    contract in the same commit, and record the discrepancy in the SUMMARY.

    Create `tests/design/theme-tokens.test.ts` reading through `readThemeTokens()`. Assert: the two themes'
    key sets are exactly equal (THEME-02 — a key present in one and missing in the other is a silent
    fallthrough to `:root`); the key set contains all 24 names listed above; `court["--brand"]` differs
    from `grove["--brand"]` and `court["--radius"]` differs from `grove["--radius"]` (THEME-03 / D-02 — the
    second theme really travels, it is not a copy); `court["--destructive"]` equals
    `grove["--destructive"]` and `court["--success"]` equals `grove["--success"]`. Add the guard-the-guard
    `it()`: assert each theme parsed at least 24 keys and that `.dark`'s `--brand` value
    (`oklch(0.645 0.205 25)`) appears in neither theme's parsed values, so a parser that accidentally
    swallowed the dormant block cannot pass.
  </action>
  <verify>
    <automated>npm run test:design -- theme-tokens</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- theme-tokens` exits 0 with at least 6 passing assertions.
    - `grep -c '\[data-theme="grove"\]' src/app/globals.css` returns 1.
    - `grep -c '\[data-theme="court"\]' src/app/globals.css` returns 1.
    - `grep -cE ':root\[data-theme|html\[data-theme' src/app/globals.css` returns 0.
    - `grep -c -- "--brand-strong" src/app/globals.css` returns 0 (D-11 — the rejected escape hatch).
    - `grep -c "^\.dark {" src/app/globals.css` returns 1 (D-03 — the dormant block survives).
    - `npm run build` exits 0 and `npm run test:design` exits 0 overall.
  </acceptance_criteria>
  <done>Two named, light-background, key-set-identical themes exist in one commit, and grove differs from court in both hue and geometry.</done>
</task>

<task type="auto">
  <name>Task 3: The declared pair inventory and the AA proof</name>
  <files>src/lib/design/contrast-pairs.ts, tests/design/contrast.test.ts</files>
  <read_first>
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Declared contrast pair
      inventory (all 27 numbered rows, both themes' measured values, and the excluded decorative row) and
      § Button Hierarchy (the two failing hover pairs found in that session).
    - `src/lib/listing-vocab.ts` `:1-42` — the `as const` array → derived union → derived map idiom and the
      section-divider comment style.
    - `tests/use-server-exports.test.ts` `:89-102` — the `NOT COVERED — real blind spots` heading and tone.
    - `tests/design/helpers/compile-css.ts` — `readThemeTokens`.
    - `src/app/globals.css` — the two theme blocks written in Task 2.
  </read_first>
  <action>
    Create `src/lib/design/contrast-pairs.ts`. `src/lib/design/` sits OUTSIDE the leak gate's scanned tree
    (`src/app/**` and `src/components/**`), which is why token literals are legal here and why the module
    lives at this path. Follow `listing-vocab.ts`'s shape: a header comment naming this file as the single
    source of truth for DS-06 and citing D-13, plus a `NOT COVERED — real blind spots, listed so the next
    reader under-trusts this file rather than over-trusts it` section stating that a cross-element pairing
    (a `text-brand` child inside a `bg-muted` parent) is invisible to the same-string drift check in plan
    10-12 and is covered instead by Phase 17's two-theme axe pass.

    Export `AA_EPSILON = 0.05` with the D-12 rationale inline (a 0.02 margin flips if `--brand-foreground`
    moves a hair or a browser rounds an oklch conversion differently), `TEXT_BAR = 4.5`,
    `NON_TEXT_BAR = 3.0`, and `CONTRAST_PAIRS` as an `as const` array of
    `{ fg, bg, bar, alpha?, note }` objects — plain objects, because the test uses
    `it.each(CONTRAST_PAIRS)` with `$fg` / `$bg` / `$bar` title interpolation. Cover all 27 UI-SPEC rows
    plus the two hover recipes: `brand-foreground` on the brand hover mix (bar 4.5) and
    `destructive-foreground` on `destructive` (bar 4.5). The `alpha` field is `{ value, over }` and is
    required on exactly these composited rows: `destructive` on `destructive` at 10% over `background`;
    the same over `card`; `foreground` on `brand` at 10% over `background`; `brand` on `brand` at 10%
    over `background`. Use the section-divider comment style to group the array into text bars, non-text
    bars, alpha-composited and hover rows. Derive a `TokenName` union from the array.

    Export `EXCLUDED_PAIRS` carrying `border` on `background` with the reason string
    `decorative divider — never a control's sole visible boundary or its sole focus indicator`. This must
    be data, not a silently absent row: an inventory that just omits a failing pair is the exact shape of
    the defect DS-06 exists to remove.

    Create `tests/design/contrast.test.ts`. Read both themes with `readThemeTokens()`. Convert each oklch
    string with culori's `converter("rgb")` and `formatHex`, and compute every ratio on the 8-bit hex,
    never on the float — that is what a contrast checker and a screenshot both see. Composite alpha rows
    over their declared surface FIRST, in gamma space, then measure; asserting the raw token pair is the
    exact mistake that let the focus ring ship at 2.58 as a pair and 1.54 as rendered. Use
    `describe.each(["court","grove"])`.

    Per theme assert: (a) DS-07 — every token value is `inGamut("rgb")`, with the failing token names in
    the assertion message; (b) DS-06 — every `CONTRAST_PAIRS` row measures at least `bar + AA_EPSILON`;
    (c) DS-05 — `--ring` clears `3.0 + AA_EPSILON` against `--background`, `--card` AND `--muted`;
    (d) the court `--brand` formats to the exact hex `#da2d34`; (e) `EXCLUDED_PAIRS` is non-empty and
    every entry carries a non-empty reason. Add the guard-the-guard `it()`: assert `CONTRAST_PAIRS.length`
    is at least 29 and that the token map for each theme has at least 24 entries.
  </action>
  <verify>
    <automated>npm run test:design -- contrast</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- contrast` exits 0 and its output shows at least 29 `court` cases and at
      least 29 `grove` cases.
    - `grep -c "AA_EPSILON = 0.05" src/lib/design/contrast-pairs.ts` returns 1.
    - `grep -c "EXCLUDED_PAIRS" src/lib/design/contrast-pairs.ts` returns 1 or more.
    - `grep -c "NOT COVERED" src/lib/design/contrast-pairs.ts` returns 1.
    - The court `--brand` assertion passes against the committed `globals.css` value. If the solver
      disagrees with `#da2d34`, the SUMMARY records the re-derived value and
      `10-UI-SPEC.md` § Full per-theme token contract is corrected in the same commit (D-12: the test is
      the authority, the document is corrected to match).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every declared pairing clears WCAG AA plus a 0.05 epsilon in both themes, proven by a test rather than by inspection, and every token renders inside sRGB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → every rendered surface | A token value change reaches every screen with no component edit |
| repo → assistive technology | Contrast values are an accessibility contract, not a preference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Repudiation | `tests/design/contrast.test.ts` | mitigate | A contrast test that measures the raw token pair instead of the composited alpha surface passes while the rendered control fails; the inventory carries an explicit `alpha` field and the test composites before measuring. Guard-the-guard pins the row count at 29+. |
| T-10-11 | Tampering | `@theme inline` | mitigate | Removing `inline` silently kills THEME-04 while the root switcher keeps working; the acceptance criteria pin `grep -c "@theme inline"` at 1 and plan 10-16's THEME-04 test asserts the compiled output never contains `var(--color-`. |
| T-10-12 | Information disclosure | `src/lib/design/contrast-pairs.ts` | accept | Contains only token names, bars and notes. No secret, credential or PII; the module is imported by tests only in this plan. |
</threat_model>

<verification>
- `npm run test:design` fully green (harness, infra, font-cycle, theme-tokens, contrast).
- `npm run build` exits 0.
- `npx tsc --noEmit` exits 0.
- Manual sanity, not a gate: `npm run dev` renders `/` in Geist rather than the platform default sans.
</verification>

<success_criteria>
Branding lives in exactly one file; two visually distinct light themes declare the identical key set; and
every pair a surface can use is proven to clear WCAG AA in both of them.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-03-SUMMARY.md` when done.
</output>
