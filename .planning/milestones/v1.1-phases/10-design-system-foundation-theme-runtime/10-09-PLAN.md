---
phase: 10-design-system-foundation-theme-runtime
plan: 09
type: execute
wave: 7
depends_on: ["10-08"]
files_modified:
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - src/app/(host)/host/listings/page.tsx
  - src/app/(host)/host/page.tsx
  - src/components/availability/availability-calendar.tsx
  - src/components/availability/date-pass-picker.tsx
  - src/components/availability/slot-picker.tsx
  - tests/design/brand-recipe.test.ts
autonomous: true
requirements: [DS-08]

must_haves:
  truths:
    - "D-21: the last 5 `<Button>` conversions land on the host surface, and coral still appears only where someone asked for it"
    - "DS-08: exactly 20 `<Button>` call sites carry `variant=\"brand\"` and exactly 9 non-Button `bg-brand` source lines remain as token classes across a named set of 6 files"
    - "The `bg-brand/90` recipe is gone from the repository in every form — hover AND static — because 4.04:1 in court and 3.87:1 in grove is a property of the 90% alpha tint, not of the `<Button>` element"
    - "The availability calendar and the slot picker still render their selected states — the 9 non-Button sites are a recorded deliberate non-conversion, not an oversight"
  artifacts:
    - path: "tests/design/brand-recipe.test.ts"
      provides: "The completed DS-08 gate: the exact 20 conversions, the pinned 9 surviving bg-brand lines across 6 named files, and zero bg-brand/90 repo-wide"
  key_links:
    - from: "src/components/availability/slot-picker.tsx"
      to: "src/app/globals.css --brand"
      via: "data-[state=on]:hover: color-mix replacing the alpha tint, variant prefix preserved"
      pattern: "color-mix\\(in_oklch,var\\(--brand\\)"
    - from: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      to: "src/components/ui/button.tsx"
      via: "variant=\"brand\" on the wizard's submit button while the step markers stay token classes"
      pattern: "variant=\"brand\""
---

<objective>
Convert the last 5 `<Button>` call sites on the host surface, fix the 4 non-Button `bg-brand/90` hovers
and the 1 static `bg-brand/90` step marker, and close the DS-08 gate with the repo-wide counts.

Purpose: exactly 20 of the 29 `bg-brand` source lines are on a `<Button>`. Converting all 29 breaks the
availability calendar and the slot picker — the two surfaces the whole sketch direction is built around —
so the remaining 9 stay as token classes. But the `/90` alpha tint fails contrast wherever it appears, so
it is removed from all 5 non-Button occurrences too. This plan is where the 20 / 9 split becomes a fact
the test suite enforces rather than a claim in a spec.

Output: 5 conversions, 5 alpha-tint removals, 9 deliberately unconverted lines, and a green repo-wide gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 5 host conversions and the wizard step marker</name>
  <files>src/app/(host)/host/listings/[id]/edit/wizard.tsx, src/app/(host)/host/listings/page.tsx, src/app/(host)/host/page.tsx</files>
  <read_first>
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` `:598-609` — the whole step-marker `<span>`: `:602`
      is the `current` state, `:603` is the `done` state carrying `bg-brand/90`, and `:607` is the
      `CheckIcon`-vs-number ternary that already distinguishes done from current. Also `:1416` — a real
      `<Button>` that DOES get converted.
    - `src/app/(host)/host/listings/page.tsx` `:81`, `:93` and `src/app/(host)/host/page.tsx` `:123`, `:155`
      — each site's surrounding `<Button>` props.
    - `src/components/booking/book-cta.tsx` as converted in plan 10-08 — the shape to repeat.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § The 29 / 20 / 9 split.
  </read_first>
  <action>
    Convert exactly 5 `<Button>` call sites: `host/listings/[id]/edit/wizard.tsx:1416` ·
    `host/listings/page.tsx:81,93` · `host/page.tsx:123,155`. At each: delete the literal `bg-brand`,
    `text-brand-foreground` and `hover:bg-brand/90` classes from `className`, add `variant="brand"`, keep
    every layout class. Do not add a hover class at the call site — the darkening
    `hover:bg-[color-mix(in_oklch,var(--brand),var(--foreground)_10%)]` lives once inside the variant.

    Do NOT convert `wizard.tsx:602` or `:603`. They are step markers on a `<span>`, not Buttons, and they
    stay token classes. But `:603` currently reads
    `state === "done" && "bg-brand/90 text-brand-foreground"` — a 90% alpha tint under
    `text-brand-foreground`, which is the same 4.04:1 court / 3.87:1 grove failure the hover ban exists for,
    just in static form. Replace it with `state === "done" && "bg-brand text-brand-foreground"`.

    Solid `bg-brand` is the right target rather than the darkening `color-mix`, for two reasons. First, done
    and current do not need a colour difference: `:607` already renders a `CheckIcon` for done and the step
    number for current, and the `<li>` already carries `aria-current="step"` for current, so the state is
    conveyed by content and by ARIA rather than by a 10% tint nobody can perceive. Second, keeping the line
    on the `bg-brand` token is what holds the pinned count at exactly 9 surviving `bg-brand` source lines —
    a `color-mix` here would silently drop it to 8 and make Task 2's gate wrong. Record that reasoning in a
    short comment on the line.
  </action>
  <verify>
    <automated>test "$(grep -rho 'variant="brand"' 'src/app/(host)' | wc -l)" = 5 && test -z "$(grep -rl 'bg-brand/90' 'src/app/(host)')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rho 'variant="brand"' 'src/app/(host)' | wc -l` returns 5.
    - `grep -rl 'bg-brand/90' 'src/app/(host)'` produces no output.
    - `grep -c 'bg-brand' 'src/app/(host)/host/listings/[id]/edit/wizard.tsx'` returns 2 (the two step
      markers at `:602` and `:603` survive as token classes).
    - `grep -rl 'bg-brand' 'src/app/(host)/host/listings/page.tsx' 'src/app/(host)/host/page.tsx'` produces
      no output.
    - `grep -c 'CheckIcon' 'src/app/(host)/host/listings/[id]/edit/wizard.tsx'` returns 2 or more (the
      done-state affordance was not removed).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every host-surface Button takes its accent from the variant, and the wizard's done marker no longer relies on an alpha tint that fails contrast.</done>
</task>

<task type="auto">
  <name>Task 2: The 4 availability hovers and the closed DS-08 gate</name>
  <files>src/components/availability/availability-calendar.tsx, src/components/availability/date-pass-picker.tsx, src/components/availability/slot-picker.tsx, tests/design/brand-recipe.test.ts</files>
  <read_first>
    - `src/components/availability/slot-picker.tsx` `:210`, `:245`, `:264` — the three counter-analogs: a
      `data-[state=on]:` Toggle recipe carrying `data-[state=on]:hover:bg-brand/90`, a soft-accent
      `bg-brand/10` info panel with no hover at all, and the full-day chip carrying a plain
      `hover:bg-brand/90`.
    - `src/components/availability/availability-calendar.tsx` `:236` — the react-day-picker `DayButton`
      className, a `data-[selected-single=true]:` recipe.
    - `src/components/availability/date-pass-picker.tsx` `:262` — the day chip, same prefix family.
    - `src/components/availability/spots-left-chip.tsx` `:64` and
      `src/components/notifications/notification-item.tsx` `:264` — the two counter-analogs that need NO
      edit at all (a `bg-brand/10` Badge tint and a `bg-brand` unread dot); read them so the gate's file set
      is written from what is there.
    - `tests/design/brand-recipe.test.ts` as written in plan 10-08 — this task extends it.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § The 29 / 20 / 9 split.
  </read_first>
  <action>
    Do NOT convert any of these to `<Button>`. `availability-calendar.tsx:236`, `date-pass-picker.tsx:262`
    and `slot-picker.tsx:210,245,264` are a react-day-picker `DayButton` className, a day chip, a
    `data-[state=on]:` hour chip, a soft-accent info panel and a full-day chip. Converting them breaks the
    availability calendar and the slot picker, which the core booking flow depends on.

    Do make exactly one change, at the 4 sites where it applies: replace `hover:bg-brand/90` — including
    the variant-prefixed forms `data-[state=on]:hover:bg-brand/90` and
    `data-[selected-single=true]:hover:bg-brand/90` — with
    `hover:bg-[color-mix(in_oklch,var(--brand),var(--foreground)_10%)]`, preserving the variant prefix
    exactly. The 4.04:1 / 3.87:1 failure is a property of the `/90` alpha tint, not of the `<Button>`
    element, so it must be fixed wherever the recipe appears. The `bg-brand/10` soft-accent tints at
    `slot-picker.tsx:245` and `spots-left-chip.tsx:64` are untouched — they are declared pairings that
    pass, and they carry `text-foreground`, not `text-brand-foreground`.

    Then extend `tests/design/brand-recipe.test.ts` with the repo-wide half of the gate. Keep plan 10-08's
    booker-tree assertions intact and add:

    - The total count of `variant="brand"` across `src/app/**` and `src/components/**` is exactly 20 — the
      15 from plan 10-08 plus the 5 from Task 1.
    - Exactly 9 source lines under `src/app/**` and `src/components/**` contain `bg-brand` outside
      `src/components/ui/**`, and the set of files containing them is exactly
      `src/components/availability/availability-calendar.tsx`,
      `src/components/availability/date-pass-picker.tsx`,
      `src/components/availability/slot-picker.tsx` (3 lines),
      `src/components/availability/spots-left-chip.tsx`,
      `src/components/notifications/notification-item.tsx` and
      `src/app/(host)/host/listings/[id]/edit/wizard.tsx` (2 lines). Write the D-21 / DS-08 rationale into
      the `it()` name: the 9 is the record of a deliberate non-conversion, not an oversight, and an
      over-eager future conversion must go red here.
    - Zero occurrences of `bg-brand/90` anywhere under `src/` in any form — hover-prefixed, variant-
      prefixed or bare. Name the 4.04:1 court / 3.87:1 grove measurement in the `it()` title.
    - At least 4 occurrences of `color-mix(in_oklch,var(--brand)` under
      `src/components/availability/**`, so a migration that deleted the hover instead of replacing it goes
      red.
    - Guard-the-guard: the scan visited more than 200 files, `src/components/availability/slot-picker.tsx`
      is in the scanned set, and `src/components/ui/button.tsx` is in the scanned set but excluded from the
      9-line count by the `src/components/ui/**` scope rule.
  </action>
  <verify>
    <automated>npm run test:design -- brand-recipe</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- brand-recipe` exits 0 with at least 9 passing assertions.
    - `grep -rc "bg-brand/90" src/ | grep -v ":0$"` produces no output.
    - `grep -rho 'variant="brand"' src/app src/components | wc -l` returns 20.
    - `grep -rc 'color-mix(in_oklch,var(--brand)' src/components/availability | awk -F: '{s+=$2} END {print s}'`
      returns 4.
    - `grep -rn 'bg-brand' src/app src/components --include=*.tsx | grep -v 'src/components/ui/' | wc -l`
      returns 9.
    - Temporarily converting `src/components/availability/slot-picker.tsx:264` to a `<Button variant="brand">`
      makes `npm run test:design -- brand-recipe` exit non-zero; revert and confirm it exits 0 again. Record
      both observations in the test file's header.
    - `npm run test:design` exits 0 overall.
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Coral appears on exactly the 20 surfaces someone asked for it, the 9 non-Button token classes are pinned by name, the calendar and slot picker still render their selected states, and no `bg-brand/90` survives anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| availability chip recipes → the core booking flow | The calendar and slot picker are the surfaces the core value runs through; an over-eager conversion breaks selection rendering |
| call-site `className` string → theme contract | A literal accent recipe repeated per call site is invisible to the variant and drifts silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-25 | Repudiation | the `bg-brand/90` ban, hover AND static | mitigate | `brand-foreground` on `brand/90` measures 4.04:1 in court and 3.87:1 in grove against a 4.5 bar; no value of `--brand` fixes it because the failure is the alpha tint lightening over a light surface. The gate asserts zero `bg-brand/90` anywhere under `src/` in any form, and requires at least 4 `color-mix(in_oklch,var(--brand)` occurrences under `availability/` so a delete-instead-of-replace goes red. |
| T-10-26 | Denial of service | availability calendar / slot picker | mitigate | Converting all 29 `bg-brand` lines would break the two surfaces the core booking flow depends on. `tests/design/brand-recipe.test.ts` pins the surviving count at exactly 9 across a named 6-file set, and the executor observes it going red on a deliberately converted `slot-picker.tsx:264` before closing the task. |
| T-10-41 | Tampering | wizard step-marker count coupling | accept | Fixing `wizard.tsx:603` with a `color-mix` instead of solid `bg-brand` would silently drop the pinned count from 9 to 8. Accepted and mitigated by construction: the action fixes the contrast failure while keeping the line on the `bg-brand` token, and the reasoning is recorded in a code comment so a future edit knows the count is load-bearing. |
</threat_model>

<verification>
- `npm run test:design` fully green.
- `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
- `npm run db:up && npm run db:test:setup && npm test` green.
- `npm run test:e2e` green — the e2e suite drives real slot selection through the calendar and slot picker,
  which is the only mechanical check that the untouched 9 still render their selected states.
- `npm run lint` reports no new errors.
</verification>

<success_criteria>
The 20 / 9 split is a fact the test suite enforces: coral is a variant on every button that asked for it,
a token class on every chip that needs it, and an alpha tint nowhere.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-09-SUMMARY.md` when done.
</output>
