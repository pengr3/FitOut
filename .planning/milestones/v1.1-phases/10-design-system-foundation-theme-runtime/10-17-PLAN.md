---
phase: 10-design-system-foundation-theme-runtime
plan: 17
type: execute
wave: 14
depends_on: ["10-16"]
files_modified:
  - eslint.config.mjs
  - package.json
  - tests/design/leak.test.ts
  - tests/design/pair-drift.test.ts
  - src/lib/design/contrast-pairs.ts
autonomous: false
requirements: [DS-13, DS-06]

must_haves:
  truths:
    - "D-16: the leak rule is enforced by BOTH ESLint and Vitest off one shared exported pattern list — a squiggle at the moment of typing and an authoritative gate"
    - "D-17: the gate scans `src/components/ui/**` with no vendored exemption, proven by a positive control asserting the 30 vendored primitives were visited"
    - "D-13: the pair-drift check fails when a component uses a foreground/background pairing absent from the declared inventory"
    - "DS-13: the build goes red on a leak — `build` is `npm run lint && npm run test:design && next build`, because Next 16 removed ESLint from `next build` and there is no CI in this repository"
    - "The gate has been observed failing on a deliberately injected leak and the failing output is recorded in the test header"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "The fitout/no-raw-design-value rule as an inline flat-config plugin"
      contains: "no-raw-design-value"
    - path: "tests/design/leak.test.ts"
      provides: "The authoritative DS-13 gate plus the recorded observed-red evidence"
    - path: "tests/design/pair-drift.test.ts"
      provides: "D-13's companion drift check"
    - path: "package.json"
      provides: "The blocking build gate"
      contains: "npm run lint && npm run test:design && next build"
  key_links:
    - from: "eslint.config.mjs"
      to: "config/design-leak-patterns.mjs"
      via: "import of the one shared pattern list"
      pattern: "design-leak-patterns"
    - from: "package.json build script"
      to: "npm run test:design"
      via: "the only gate boundary that exists, since there is no CI"
      pattern: "test:design && next build"
---

<objective>
Turn the gates on: enable the leak rule in ESLint, land the authoritative Vitest gate and D-13's
drift companion, rewrite the `build` script so a leak actually fails it, and prove it by watching it go
red.

Purpose: Next.js 16 removed `next lint` and stopped running ESLint during `next build`, and
`.github/workflows` does not exist. Until `package.json`'s `build` script changes, DS-13's "fails the
build" is structurally unmet no matter how good the test is. This plan is last because turning the gate
on before the tree is clean would red-flag the repository's own in-progress state.

Output: the ESLint rule, two gate tests, the build rewrite, recorded observed-red evidence, and the
phase's single human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The ESLint rule, the authoritative leak gate and the pair-drift companion</name>
  <files>eslint.config.mjs, tests/design/leak.test.ts, tests/design/pair-drift.test.ts, src/lib/design/contrast-pairs.ts</files>
  <read_first>
    - `eslint.config.mjs` — the whole 23-line file. It has no `plugins:` key, no `rules:` key and no
      custom rule anywhere; the only things to copy are the `defineConfig([...])` array shape, the
      convention that spread configs come first and overrides after, and the comment-above-each-entry
      style.
    - `src/components/listing/listing-card.tsx` `:234` — the repo's existing escape-hatch precedent,
      `// eslint-disable-next-line @next/next/no-img-element`. The leak rule reuses that mechanism rather
      than inventing a new exemption marker.
    - `config/design-leak-patterns.mjs` — the five patterns, `LEAK_SCAN_GLOBS`, `LEAK_SCAN_PREFIXES` and
      `LEAK_DISABLE_RULE_ID`.
    - `tests/use-server-exports.test.ts` — the whole file, and specifically `:26-62` (the observed-red
      header block convention), `:104-125` (the walker), `:296-310` (scan-once, assert-in-`it`),
      `:302` (Windows path normalisation), `:312-318` (guard-the-guard), `:337-369` (the synthetic-fixture
      self-test) and `:371-373` (the authoritative one-liner against an empty array).
    - `src/lib/design/contrast-pairs.ts` — the declared inventory the drift check reads.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md` § Pattern 7 (the verified
      ESLint mechanism and its two false-positive classes) and § Pattern 9 (the drift check's design and
      its honest blind spot).
  </read_first>
  <action>
    Append one entry to `eslint.config.mjs`'s `defineConfig` array, after the spread configs and the
    `globalIgnores` call, with a comment above it in the file's existing style. Scope it with
    `files: ["src/app/**/*.{ts,tsx}", "src/components/**/*.{ts,tsx}"]` — imported from
    `LEAK_SCAN_GLOBS` so the scope has one home. Without that scope the rule fires on
    `tests/design/*` fixtures and on `src/lib/db/schema.ts:730`'s GitHub issue references. Declare an
    inline plugin under the `fitout` namespace exposing a `no-raw-design-value` rule that imports
    `DESIGN_LEAK_PATTERNS` from `./config/design-leak-patterns.mjs` and visits `Literal` and
    `TemplateElement` nodes, testing each node's string value against every pattern and reporting
    `Raw design value "<match>" — use a design token (DS-13 / D-15).` Set
    `rules: { "fitout/no-raw-design-value": "error" }`. Zero new npm dependencies: the inline flat-config
    plugin was verified working against this repo's installed ESLint 9. The escape hatch is the repo's
    existing form, `// eslint-disable-next-line fitout/no-raw-design-value` — do not invent a
    `/* design-token-exempt */` marker.

    Create `tests/design/leak.test.ts` as the authoritative gate, modelled structurally and in discipline
    on `tests/use-server-exports.test.ts`. Import the shared patterns and prefixes; walk `src/` with
    `collectSourceFiles`; normalise every path with `relative(process.cwd(), file).split("\\").join("/")`;
    scan once at module level and assert inside `it()` blocks. Include a synthetic-fixture self-test
    asserting both what the rule flags and what it exempts, covering RESEARCH § L14's two false-positive
    classes explicitly — `see #3388 for details` must not flag,
    `bg-[color-mix(in_oklch,var(--secondary),var(--foreground)_5%)]` must not flag,
    `text-[0.8rem]` must not flag — and the true positives `= "#E8484E"`, `fill="#fff"`, `bg-zinc-50`,
    `bg-black`, `text-[28px]` and `oklch(0.5 0 0)` must all flag. Add the guard-the-guard `it()`: assert
    the scan visited the 30 files in `src/components/ui/` and that
    `src/components/listing/listing-map.tsx` is in the scanned set — this is D-17 made checkable, and
    without it the real assertion would pass just as happily against a scanner that visited nothing. The
    authoritative assertion is the one-liner `expect(violations).toEqual([])`. Leave a placeholder
    observed-red header block at the top in the `use-server-exports.test.ts:26-62` format; Task 2 fills it
    with the real failing output.

    Create `tests/design/pair-drift.test.ts` — D-13's companion, the thing that stops the declared
    inventory going stale. Scan every string literal and template chunk under the gate tree; strip variant
    prefixes (`hover:`, `focus-visible:`, `active:`, `disabled:`, `group-hover:`, `aria-*:`,
    `data-[...]:`, and the responsive prefixes); classify each remaining class token as a foreground
    (`text-*`), a background (`bg-*`) or a border/ring (`border-*`, `ring-*`); take the cross product
    WITHIN a single string; and fail on any foreground/background combination absent from
    `CONTRAST_PAIRS`. Normalise the alias groups before lookup — `secondary`, `muted` and `accent` share
    one value, and `card` and `popover` share another — so an alias is not reported as a new pairing.

    Any pairing the scan finds that is not in the inventory is resolved one of exactly two ways, never by
    loosening the check: either it is legitimate, in which case add the row to
    `src/lib/design/contrast-pairs.ts` with its bar and note and re-run
    `npm run test:design -- contrast` to prove it clears `bar + AA_EPSILON` in BOTH themes; or it fails
    the bar, in which case the component is fixed. Record the number of rows added in the SUMMARY. The
    file header must carry the `NOT COVERED — real blind spots` section stating that a cross-element
    pairing — a `text-brand` child inside a `bg-muted` parent — is invisible to same-string analysis, and
    that the residue is covered by Phase 17's two-theme axe pass. Saying so in the code stops a later
    reader trusting the check further than it deserves.
  </action>
  <verify>
    <automated>npm run test:design -- leak pair-drift && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- leak` exits 0 with at least 12 passing assertions.
    - `npm run test:design -- pair-drift` exits 0.
    - `npm run test:design -- contrast` exits 0 after any inventory rows were added.
    - `npm run lint` exits 0 with 0 errors (warnings are acceptable; the baseline was 0 errors / 9
      warnings).
    - `grep -c "no-raw-design-value" eslint.config.mjs` returns 2 or more (the rule definition and the
      severity entry).
    - `grep -c "design-leak-patterns" eslint.config.mjs` returns 1.
    - `grep -c "NOT COVERED" tests/design/pair-drift.test.ts` returns 1.
    - `grep -c "src/components/ui" tests/design/leak.test.ts` returns 1 or more (D-17's positive control).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both consumers of the one pattern list are live, and the drift check proves the pair inventory matches what components actually use.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the build script and prove the gate goes red</name>
  <files>package.json, tests/design/leak.test.ts</files>
  <read_first>
    - `package.json` `:5-23` — the current `"build": "next build"`.
    - `tests/design/leak.test.ts` as written in Task 1 — the placeholder observed-red header block.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md` § Landmines L1 and L2,
      and Pitfall 1 — the two facts that make this task mandatory rather than a nicety.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-VALIDATION.md` § Test Infrastructure →
      Build gate wiring.
  </read_first>
  <action>
    Change `package.json`'s `build` script from `next build` to
    `npm run lint && npm run test:design && next build`. Nothing else in the scripts block changes. This
    is the whole of DS-13's "fails the build": Next 16 removed `next lint` and no longer runs ESLint
    during `next build`, and `.github/workflows` does not exist, so the npm script layer IS the gate
    boundary. Do not create a GitHub Actions workflow — no CI exists to amend, and inventing one is
    net-new scope this phase did not agree to. Record in the SUMMARY that `npm run lint` measures roughly
    85 seconds on this box and that the build is therefore noticeably slower, which is the accepted price.

    Then prove the gate blocks, because a gate that has never been watched failing is not a gate — it
    trains reviewers to trust a rubber stamp. Execute this sequence and capture the real output at each
    step: (1) add a raw hex literal such as `const LEAK_PROBE = "#ff0000";` to
    `src/components/ui/badge.tsx`; (2) run `npm run build` and capture the full failing output and the
    non-zero exit code; (3) confirm the output names `fitout/no-raw-design-value` and the injected file;
    (4) run `npm run test:design -- leak` and capture that failing output too, confirming the Vitest gate
    catches it independently of ESLint; (5) revert the edit; (6) run `npm run build` again and confirm it
    exits 0.

    Paste the captured failing output from steps 2 and 4 into `tests/design/leak.test.ts`'s observed-red
    header block, replacing the placeholder, in the `tests/use-server-exports.test.ts:26-62` format: the
    verbatim failing run, dated, with a line naming what was injected and where. Leave the working tree
    with the probe reverted.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "process.exit(require('./package.json').scripts.build==='npm run lint && npm run test:design && next build'?0:1)"` exits 0.
    - `npm run build` exits 0 on the clean tree.
    - `git status --porcelain src/components/ui/badge.tsx` produces no output (the probe was reverted).
    - `tests/design/leak.test.ts`'s header contains the verbatim string `fitout/no-raw-design-value` inside
      a captured failing run, and contains the date of the observation.
    - `ls .github/workflows` fails (no CI workflow was invented).
    - `grep -c "next build" package.json` returns 1.
  </acceptance_criteria>
  <done>A raw hex in a component turns `npm run build` red, and the failing output is recorded in the guard that produced it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase acceptance — the three things only a human can confirm</name>
  <files>none</files>
  <read_first>
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-VALIDATION.md` § Manual-Only
      Verifications — the four-row table, three rows of which this checkpoint discharges (the fourth,
      the deliberately-injected-leak proof, was automated in Task 2).
    - `tests/design/leak.test.ts` — the observed-red header block written in Task 2, which the developer
      reviews as the evidence that the gate blocks.
  </read_first>
  <action>
    Run no further automation. Every testable property in this phase is already green; what remains is
    aesthetic acceptance and one browser-level observation that no test can make. Present the three items
    below, point the developer at `http://localhost:3000/dev/theme` with `npm run dev` running, wait for
    an explicit approval signal, and record the verdict for each of the three items separately in the
    plan SUMMARY. If item 3 (the accent deepening) is rejected, do NOT reintroduce a `--brand-strong`
    escape hatch on the spot — that option was considered and rejected in CONTEXT § Specific Ideas, and a
    rejection here is a scope decision for the developer, not a fix for the executor.
  </action>
  <what-built>
    The whole phase. Automation has already: derived and landed both themes, proven all 29 declared
    pairings clear WCAG AA in each of them, removed every alpha focus ring, converted the 20 brand buttons
    while leaving the 9 non-buttons intact, retired the filled green badge, migrated every arbitrary size,
    shadow and z-index, stripped the app-code `dark:` occurrences while pinning the vendored 56, generated
    and drift-checked the token module and the two themed favicons, built `/dev/theme`, enabled the leak
    rule in both ESLint and Vitest, rewritten `build` to `npm run lint && npm run test:design && next build`,
    and observed the gate going red on a deliberately injected leak with the failing output recorded in
    `tests/design/leak.test.ts`'s header.

    Three items remain that are aesthetic acceptance or a browser-level observation, and are not testable
    properties.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/dev/theme`.
    2. **SC#3 — two plausible brand directions.** Scan the two panes horizontally. Confirm the left
       (court) and right (grove) panes read as two plausible alternate brand directions rather than as a
       test fixture: grove should differ in hue (teal, not coral), in shape (visibly rounder corners), in
       type (larger, heavier headings) and in depth (more pronounced shadows). Confirm the judgement is
       possible on the real components — the slot picker, the status badges, the result card — rather than
       only on the swatches at the bottom.
    3. **D-11 — the accent visibly deepens.** Open `http://localhost:3000/` and any booking page. Confirm
       the coral CTA is visibly deeper than it was before this phase and that you accept it. This was an
       explicit trade: one darkened `--brand` with no per-call-site judgement, rather than a second brand
       token. If it is unacceptable, say so now — reintroducing a `--brand-strong` escape hatch later is
       a much larger change.
    4. **DS-04 — reduced motion actually suppresses an animation.** Enable reduced motion at the OS level
       (Windows: Settings → Accessibility → Visual effects → Animation effects → Off). Reload
       `/dev/theme`, open the Select control in the form-row section, and confirm no zoom or slide
       transition plays. Then turn the OS setting back off and confirm the animation returns, so you have
       seen the reset both engaged and disengaged.
    5. Optional cross-check of the theme seam: open `http://localhost:3000/?theme=grove` and confirm the
       whole app re-skins, then `http://localhost:3000/?theme=court` to return.
  </how-to-verify>
  <acceptance_criteria>
    - The developer has replied with an explicit approval, or has named which of the three items fails
      and how.
    - The SUMMARY records a separate verdict for SC#3 (two plausible directions), D-11 (accent deepening
      accepted) and DS-04 (reduced motion observed both engaged and disengaged).
    - No source file changed during this task: `git status --porcelain src/` produces no output.
  </acceptance_criteria>
  <done>All three manual-only verifications in 10-VALIDATION.md § Manual-Only Verifications carry a recorded human verdict.</done>
  <resume-signal>Type "approved" to close the phase, or describe which of the three items fails and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit → shipped bundle | The build script is the only place a leak can be stopped, since there is no CI |
| gate configuration → reviewer trust | A gate that cannot fail is worse than no gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Repudiation | the DS-13 gate | mitigate | The gate is proven to block by deliberately injecting a raw hex, running `npm run build`, capturing the non-zero exit and the rule id, reverting, and re-running green — with the failing output pasted into the guard's own header. A gate never watched failing is a rubber stamp. |
| T-10-34 | Tampering | `package.json` build script | mitigate | The blocking form is asserted by an exact string comparison in the acceptance criteria, so a later "speed up the build" edit that drops `test:design` is a visible diff rather than a silent regression. |
| T-10-35 | Denial of service | `npm run lint` in `build` | accept | `npm run lint` measures ~85s on this box, making every build noticeably slower. Accepted: it is the only place the rule can run on Next 16, the build is a local/manual step, and there is no CI queue to congest. If it becomes painful, the documented alternative is scoping the lint invocation to `src/`. |
| T-10-36 | Elevation of privilege | eslint inline plugin | accept | The plugin is defined inline in `eslint.config.mjs` from a repo-local pattern list; no new npm dependency enters the tree, so the rule adds no supply-chain surface. |
</threat_model>

<verification>
Phase-gate verification — all of these must hold before `/gsd:verify-work`:
- `npm run build` exits 0 (which now runs lint + the design gate + `next build`).
- `npm run test:design` fully green: harness, infra, font-cycle, theme-tokens, contrast, type-scale,
  elevation-z, motion-budget, button-variants, focus-recipe, brand-recipe, status-vocab, dark-scope,
  token-drift, scaffold-residue, theme-nesting, leak, pair-drift, theme-provider.
- `npm run db:up && npm run db:test:setup && npm test` green across all existing test files, including
  `tests/booking/partial-grant-notice.test.tsx`.
- `npm run test:e2e` green.
- `npx tsc --noEmit` exits 0.
- GATE-06 holds: `git status` and `ls drizzle/` show no new migration; `drizzle/` is still at `0025`.
- No visual-regression baseline was captured: `grep -rc "toHaveScreenshot" e2e/ tests/` returns no
  non-zero counts and no `*-snapshots` directory exists.
</verification>

<success_criteria>
A raw hex, a palette class or an arbitrary pixel size anywhere under `src/app/**` or `src/components/**`
fails `npm run build`, and that has been observed rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-17-SUMMARY.md` when done.
</output>
