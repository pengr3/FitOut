---
phase: 10-design-system-foundation-theme-runtime
plan: 08
type: execute
wave: 6
depends_on: ["10-06"]
files_modified:
  - src/app/(app)/bookings/[id]/page.tsx
  - src/app/(app)/bookings/page.tsx
  - src/components/booking/book-cta.tsx
  - src/components/booking/booking-row.tsx
  - src/components/booking/expired-approval-state.tsx
  - src/components/booking/hold-expired-state.tsx
  - src/components/booking/payment-reversed-state.tsx
  - src/components/booking/reserve-actions.tsx
  - src/components/group/create-group-button.tsx
  - src/components/group/rsvp-form.tsx
  - src/components/search/search-bar.tsx
  - tests/design/brand-recipe.test.ts
autonomous: true
requirements: [DS-08, DS-09]

must_haves:
  truths:
    - "D-21: the coral accent reaches the booker surface only through `variant=\"brand\"` — the literal `bg-brand text-brand-foreground hover:bg-brand/90` string is gone from every booking, group and search call site"
    - "D-22: the two hand-rolled `h-11` heights on the RSVP and search CTAs become the named `size=\"touch\"` opt-in rather than staying magic numbers"
    - "DS-08: 15 of the 20 `<Button>` conversions land here, leaving zero `bg-brand` under the booking, group and search trees"
    - "The brand hover is never `bg-brand/90` — it measures 4.04:1 in court and 3.87:1 in grove, and it now lives once inside the CVA variant"
    - "The shipped CTA copy survives verbatim: \"Book this space\", \"Confirm & pay\", \"Invite people\" and their pending forms"
  artifacts:
    - path: "tests/design/brand-recipe.test.ts"
      provides: "The booker-tree half of the DS-08 gate, with a named per-file conversion map and a guard-the-guard positive control"
    - path: "src/components/booking/book-cta.tsx"
      provides: "The canonical converted call site — variant on the element, only layout classes left in className"
      contains: "variant=\"brand\""
  key_links:
    - from: "src/components/booking/book-cta.tsx"
      to: "src/components/ui/button.tsx"
      via: "variant=\"brand\" replacing the literal recipe"
      pattern: "variant=\"brand\""
    - from: "src/components/group/rsvp-form.tsx"
      to: "src/components/ui/button.tsx"
      via: "size=\"touch\" replacing the hand-rolled h-11"
      pattern: "size=\"touch\""
---

<objective>
Convert the 15 booker-, group- and search-surface `<Button>` call sites onto the CVA contract defined in
plan 10-06.

Purpose: these are the surfaces the core value runs through — search, book, pay, invite. Every one of
them currently repeats `bg-brand text-brand-foreground hover:bg-brand/90` as a literal string, which
means every one of them independently ships the 4.04:1 / 3.87:1 hover failure and independently drifts
if the accent ever changes. After this plan the accent reaches them through one variant.

Output: 11 files carrying `variant="brand"`, two carrying `size="touch"`, and the booker half of the
DS-08 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The booker routes and the canonical CTA</name>
  <files>src/app/(app)/bookings/[id]/page.tsx, src/app/(app)/bookings/page.tsx, src/components/booking/book-cta.tsx</files>
  <read_first>
    - `src/components/booking/book-cta.tsx` `:222` — the canonical shape being replaced
      (`className="w-full bg-brand text-brand-foreground hover:bg-brand/90"`), and `:224` for the CTA copy.
    - `src/app/(app)/bookings/[id]/page.tsx` `:374`, `:421`, `:517` and
      `src/app/(app)/bookings/page.tsx` `:145`, `:211` — each site's surrounding `<Button>` props, so any
      `asChild`, `size`, `disabled` or layout class is preserved rather than assumed away.
    - `src/components/ui/button.tsx` as written in plan 10-06 — the `brand` variant and `touch` size.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § The 29 / 20 / 9 split
      and § Copywriting Contract.
  </read_first>
  <action>
    Convert 6 `<Button>` call sites: `bookings/[id]/page.tsx:374,421,517` · `bookings/page.tsx:145,211` ·
    `booking/book-cta.tsx:222`.

    At each: delete the literal `bg-brand`, `text-brand-foreground` and `hover:bg-brand/90` classes from
    `className` and add `variant="brand"` to the element. Keep every layout class — `book-cta.tsx:222`
    keeps `className="w-full"`. If deleting the colour classes empties a `className` entirely, remove the
    now-empty `className` prop rather than leaving `className=""`.

    Do not write `hover:bg-brand/90` anywhere, and do not re-add a hover class at the call site: the hover
    lives once, inside the variant, as `hover:bg-[color-mix(in_oklch,var(--brand),var(--foreground)_10%)]`.
    An alpha tint over a light surface lightens, so `brand-foreground` on `brand/90` measures 4.04:1 in
    court and 3.87:1 in grove against a 4.5 bar; the `color-mix` form darkens instead — court `#c32b37` at
    5.41:1, grove `#137470` at 5.36:1.

    Preserve the shipped labels verbatim — "Book this space", "Confirm & pay", and their pending forms
    "Starting…", "Taking you to checkout…". Line numbers will shift as edits land; locate each site by its
    surrounding code, not by line number alone.
  </action>
  <verify>
    <automated>test "$(grep -rho 'variant="brand"' 'src/app/(app)/bookings' src/components/booking/book-cta.tsx | wc -l)" = 6 && test -z "$(grep -rl 'bg-brand' 'src/app/(app)/bookings' src/components/booking/book-cta.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rho 'variant="brand"' 'src/app/(app)/bookings' src/components/booking/book-cta.tsx | wc -l` returns 6.
    - `grep -rl 'bg-brand' 'src/app/(app)/bookings' src/components/booking/book-cta.tsx` produces no output.
    - `grep -c "Book this space" src/components/booking/book-cta.tsx` returns 1 (copy preserved).
    - `grep -c 'className=""' src/components/booking/book-cta.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The primary booking CTA and both booker list/detail routes take their accent from the variant, not from a repeated string.</done>
</task>

<task type="auto">
  <name>Task 2: The five booking state components</name>
  <files>src/components/booking/booking-row.tsx, src/components/booking/expired-approval-state.tsx, src/components/booking/hold-expired-state.tsx, src/components/booking/payment-reversed-state.tsx, src/components/booking/reserve-actions.tsx</files>
  <read_first>
    - `src/components/booking/booking-row.tsx` `:113` — note this single line carries BOTH `bg-brand` and a
      `z-10`. Only the colour classes change here; the `z-10` is plan 10-13's edit and must survive intact.
    - `src/components/booking/expired-approval-state.tsx` `:132`, `:147`;
      `src/components/booking/hold-expired-state.tsx` `:33`;
      `src/components/booking/payment-reversed-state.tsx` `:35`;
      `src/components/booking/reserve-actions.tsx` `:81` — each site's surrounding `<Button>` props.
    - `src/components/booking/book-cta.tsx` as converted in Task 1 — the shape to repeat.
  </read_first>
  <action>
    Convert 6 `<Button>` call sites: `booking/booking-row.tsx:113` ·
    `booking/expired-approval-state.tsx:132,147` · `booking/hold-expired-state.tsx:33` ·
    `booking/payment-reversed-state.tsx:35` · `booking/reserve-actions.tsx:81`. Same edit as Task 1:
    delete `bg-brand`, `text-brand-foreground` and `hover:bg-brand/90` from `className`, add
    `variant="brand"`, keep every layout and positioning class.

    At `booking-row.tsx:113` specifically: keep `z-10` exactly as it is. Plan 10-13 remaps it to
    `z-(--z-sticky)`; touching it here would put two plans on the same token and make that plan's count
    wrong.

    Preserve every shipped label verbatim. Do not introduce a hover class at any call site — the
    `bg-brand/90` form is the 4.04:1 / 3.87:1 failure and the variant already carries the darkening
    `color-mix` hover.
  </action>
  <verify>
    <automated>test "$(grep -ho 'variant="brand"' src/components/booking/booking-row.tsx src/components/booking/expired-approval-state.tsx src/components/booking/hold-expired-state.tsx src/components/booking/payment-reversed-state.tsx src/components/booking/reserve-actions.tsx | wc -l)" = 6 && test -z "$(grep -l 'bg-brand' src/components/booking/booking-row.tsx src/components/booking/expired-approval-state.tsx src/components/booking/hold-expired-state.tsx src/components/booking/payment-reversed-state.tsx src/components/booking/reserve-actions.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ho 'variant="brand"' src/components/booking/*.tsx | wc -l` returns 7 (the 6 here plus
      `book-cta.tsx` from Task 1).
    - `grep -rl 'bg-brand' src/components/booking/` produces no output.
    - `grep -c "z-10" src/components/booking/booking-row.tsx` returns 1 (the stacking class survived).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every booking state component takes its accent from the variant and its stacking class is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Group, search, the two touch sizes, and the booker-tree gate</name>
  <files>src/components/group/create-group-button.tsx, src/components/group/rsvp-form.tsx, src/components/search/search-bar.tsx, tests/design/brand-recipe.test.ts</files>
  <read_first>
    - `src/components/group/create-group-button.tsx` `:63`, `src/components/group/rsvp-form.tsx` `:327` and
      `src/components/search/search-bar.tsx` `:406` — each `<Button>`'s full prop list. At `rsvp-form.tsx`
      and `search-bar.tsx` the height is hand-rolled as `h-11`; confirm that before replacing it.
    - `src/components/ui/button.tsx` as written in plan 10-06 — the `touch` size is `h-11` plus the
      `default` size's gap and padding shape, so replacing a bare `h-11` also changes padding. Read the
      surrounding layout to confirm that is acceptable at each site; if a site needs its own padding, keep
      the explicit padding class alongside `size="touch"`.
    - `tests/design/focus-recipe.test.ts` as written in plan 10-07 — the walker and the Windows
      path-normalisation idiom to reuse. If plan 10-07 has not landed yet, copy the same idiom from
      `tests/use-server-exports.test.ts` `:104-125` and `:296-318` instead.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Control Sizing.
  </read_first>
  <action>
    Convert the last 3 `<Button>` call sites in this plan's scope: `group/create-group-button.tsx:63` ·
    `group/rsvp-form.tsx:327` · `search/search-bar.tsx:406`. Same edit as Tasks 1 and 2.

    At `group/rsvp-form.tsx:327` and `search/search-bar.tsx:406` additionally replace the hand-rolled
    `h-11` with `size="touch"` in the same edit. This is D-22 working as intended: 44px is an explicit
    opt-in named size, so the two sites that already wanted it stop expressing it as a magic number.

    Preserve the shipped labels verbatim — "Invite people" and its pending form "Setting up…".

    Create `tests/design/brand-recipe.test.ts`, reusing the walker and the
    `relative(process.cwd(), file).split("\\").join("/")` normalisation. Scope this file's assertions to
    the trees this plan owns; plan 10-09 extends the same file with the host and availability half and the
    repo-wide totals. Assert:

    - The count of `variant="brand"` across `src/app/(app)/bookings/**`, `src/components/booking/**`,
      `src/components/group/**` and `src/components/search/**` is exactly 15, and the per-file map is
      exactly: `bookings/[id]/page.tsx` 3, `bookings/page.tsx` 2, `book-cta.tsx` 1, `booking-row.tsx` 1,
      `expired-approval-state.tsx` 2, `hold-expired-state.tsx` 1, `payment-reversed-state.tsx` 1,
      `reserve-actions.tsx` 1, `create-group-button.tsx` 1, `rsvp-form.tsx` 1, `search-bar.tsx` 1.
    - Zero occurrences of `bg-brand` in any form — including `bg-brand/90` — anywhere under those four
      trees. Name the 4.04:1 / 3.87:1 measurement in the `it()` title so the reason survives the next
      person reading a red test.
    - `src/components/group/rsvp-form.tsx` and `src/components/search/search-bar.tsx` each contain
      `size="touch"` and neither contains a bare `h-11`.
    - Guard-the-guard: the scan visited more than 200 files and `src/components/booking/book-cta.tsx` is in
      the scanned set — a positive control proving the walker really reaches a converted file rather than
      passing vacuously on an empty scan.
  </action>
  <verify>
    <automated>npm run test:design -- brand-recipe</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- brand-recipe` exits 0 with at least 5 passing assertions.
    - Temporarily reinstating `bg-brand` on `src/components/group/create-group-button.tsx` makes
      `npm run test:design -- brand-recipe` exit non-zero; revert and confirm it exits 0 again. Record both
      observations in the test file's header.
    - `grep -c 'size="touch"' src/components/group/rsvp-form.tsx` returns 1 and
      `grep -c 'size="touch"' src/components/search/search-bar.tsx` returns 1.
    - `grep -c 'h-11' src/components/group/rsvp-form.tsx src/components/search/search-bar.tsx` returns 0
      for each file.
    - `grep -c "Invite people" src/components/group/create-group-button.tsx` returns 1 (copy preserved).
    - `npm run test:design` exits 0 overall.
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>All 15 booker-, group- and search-surface conversions are done and pinned by a scan that provably reached them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call-site `className` string → theme contract | A literal accent recipe repeated per call site is invisible to the variant and drifts silently |
| booking CTA copy → booker trust | These are the core-value surfaces; a reworded or removed CTA is a product regression, not a style change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-24 | Repudiation | the `bg-brand/90` hover ban | mitigate | The failure is a property of the `/90` alpha tint, not of any one element: `brand-foreground` on `brand/90` measures 4.04:1 in court and 3.87:1 in grove against a 4.5 bar. `tests/design/brand-recipe.test.ts` asserts zero `bg-brand` in any form under all four trees this plan touches, and the measurement is named in the `it()` title so a future red test explains itself. |
| T-10-39 | Tampering | mechanical multi-file conversion | mitigate | 15 near-identical edits across 11 files is exactly where an over-eager find-and-replace strips a layout class or a label. The gate pins an exact per-file conversion map rather than a bare total, and every task's acceptance criteria re-assert the shipped CTA copy. |
| T-10-40 | Denial of service | `booking-row.tsx:113` shared line | accept | That one line carries both `bg-brand` and `z-10`, and plan 10-13 owns the `z-10`. Recorded rather than mitigated: the two plans are in different waves, the read_first calls the collision out, and an acceptance criterion asserts `z-10` survived this plan. |
</threat_model>

<verification>
- `npm run test:design` fully green.
- `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
- `npm run db:up && npm run db:test:setup && npm test` green — the booking suites render these components.
- `npm run test:e2e` green — the e2e suite drives the real book/pay/invite flow through these CTAs.
- `npm run lint` reports no new errors.
</verification>

<success_criteria>
Every booker-, group- and search-surface button gets its accent from `variant="brand"`, the two 44px CTAs
say so by name, and the shipped copy is unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-08-SUMMARY.md` when done.
</output>
