---
phase: 17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro
plan: 07
type: execute
wave: 7
depends_on: ["17.1-01", "17.1-02", "17.1-03", "17.1-04", "17.1-05", "17.1-06"]
files_modified:
  - .planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md
  - .planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md
autonomous: true
requirements: ["17-D9", "GATE-01"]

must_haves:
  truths:
    - "D-02: the held dispatch is authorised and executed, and the diff is read against §7a's CORRECTED prediction — 2 rows changed (`listing-detail-320`, `listing-detail-768`), 0 added, 0 deleted, disk still 36 — not §4's superseded 3"
    - "D-02: anything outside the corrected list moving is a STOP and a finding written up before close, never a threshold change and never a re-dispatch"
    - "D-03: this plan is the phase's FINAL wave and it runs ALONE. Every other plan is committed and pushed before it starts, because §7d requires the green comparison run's `headSha` to equal `git rev-parse HEAD` and a later commit invalidates it"
    - "D-03: the GENERATION run is not the evidence — a `GITHUB_TOKEN` push triggers no workflow, so at the moment it finishes nothing has ever compared against the PNGs it wrote. The deliverable is the forced COMPARISON run"
    - "D-03: `ci.yml` has no `workflow_dispatch` trigger, so a new commit is the only way to force the comparison. `260830-r4b/PLAN.md` Task 3d's \"or re-dispatch ci.yml\" alternative does not exist on this tree and is not attempted"
    - "D-14: the write path is proved untouched by PARSING it — `node scripts/verify-workflows.mjs`, 38 invariants — never by grepping, which is green six of six on that file including for an added `push:` trigger"
    - "§4's original prediction is left standing verbatim; the correction lives in §7a. A prediction written after reading a diff is not a prediction, it is a description"
  artifacts:
    - path: ".planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md"
      provides: "§7b (the generation run, labelled NOT the evidence), §7c (the five-point diff read), §7d (the comparison run — THE evidence)"
      contains: "7d"
    - path: ".planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md"
      provides: "D1 — the phase's re-prediction, folding in whatever plan 17.1-04 measured"
      contains: "D1"
  key_links:
    - from: ".planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md §7d"
      to: "the ci.yml run on the head commit"
      via: "gh run view --json conclusion,headSha, asserted equal to git rev-parse HEAD"
      pattern: "headSha"
    - from: ".github/workflows/baselines.yml"
      to: "e2e/visual/surfaces.spec.ts-snapshots/"
      via: "the ONE sanctioned baseline write path, dispatched once"
      pattern: "workflow_dispatch"
---

<objective>
Regenerate the two moved visual baselines through the one sanctioned write path, and then prove
something has actually compared against them.

Purpose: `gate-visual` has been **red on `dev` for three consecutive runs** at HEAD, in exactly the
shape `260830-r4b/PLAN.md` calls *"the correct pre-dispatch shape"* — a pixel/size diff on baselined
rows that render, not a connection error and not a missing snapshot:

  listing-detail-320-court.png   Expected an image 320px by 2986px, received 320px by 3018px (+32px)
  listing-detail-768-court.png   Expected an image 768px by 2408px, received 768px by 2488px (+80px)

`baseline-evidence.md` §7a ends *"DISPATCH HELD... That is a PM call, not an executor's."* The PM has
made the call: **dispatch**.

**This plan is the phase's last wave and it runs alone.** `ci.yml` has no `workflow_dispatch` trigger,
so the comparison run can only be reached by a **new commit**; and §7d requires that run's `headSha`
to equal `git rev-parse HEAD`. A commit landing after it invalidates the evidence. Every other plan in
this phase is committed and pushed before this one begins.

**And the generation run is not the evidence.** A `GITHUB_TOKEN` push triggers no workflow — the
recursion-prevention property and the "nobody has verified these" property are the *same sentence*,
and only the first half is memorable. At the moment the generation job goes green, **nothing has ever
compared against the PNGs it just wrote.** Writing is not comparing.

Output: §7b, §7c and §7d filled in, and `dev` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-CONTEXT.md

<interfaces>
**The corrected prediction — §7a, which supersedes §4 (D-02):**
  2 changed: `listing-detail-320-court-visual-linux.png`, `listing-detail-768-court-visual-linux.png`
  0 added · 0 deleted · disk still **36**
  `listing-detail-1280-court-visual-linux.png` is byte-identical, as are `listing-lightbox-1280`,
  `listing-sheet-375`, `checkout-320` and `checkout-1280`.
⚠ **Plan 17.1-04 wrote its own per-PNG prediction into `17.1-EVIDENCE.md` `P4`.** Shape A predicts
ZERO additional movement; if 17.1-04 escalated to Shape B, `checkout-320-court-visual-linux.png` moves
by +32px and the prediction below becomes **3 changed**. Fold `P4`'s numbers in **before** dispatching.

**The write path, read in full (`.github/workflows/baselines.yml`):**
  trigger    `workflow_dispatch` only — no `push`, no `pull_request`, no `schedule` (`:187-188`)
  job        `generate-baselines`, `permissions: contents: write` on the JOB (workflow default is read)
  container  `mcr.microsoft.com/playwright:v1.60.0-noble`, `--ipc=host` — the tag MUST equal the
             `@playwright/test` pin, verified installed at **1.60.0**
  service    `postgis/postgis:18-3.6`, reached by the label `postgres`. **No `secrets.*` anywhere.**
  steps      checkout → node 24 → `npm ci` → git identity + `safe.directory` → `npm run db:migrate` →
             `npx tsx scripts/seed-baseline-fixtures.ts` →
             `npx playwright test --project=visual --update-snapshots` →
             stage `"*-visual-linux.png"` ONLY (fails without committing if anything else is staged) →
             commit + `git push origin "HEAD:${{ github.ref }}"` →
             an `always()` `::warning::` saying the baselines are unverified
  no-op on an empty diff is deliberate — re-dispatching against a current set confirms stability

**The comparison path (`.github/workflows/ci.yml:356-359`):** `on: push: branches: [dev, main]` +
`pull_request`. **No `workflow_dispatch`.** "Re-run all jobs" on an older run re-runs at that run's
SHA, which predates the new PNGs. A new commit is the only way.

**Pins that must not move:** `EXPECTED_BLOCKED` (`e2e/visual/surfaces.spec.ts:179`) and
`EXPECTED_BASELINE_COUNT = 78` (`:301`). On-disk PNG count: **36**, all `*-court-visual-linux.png`,
zero grove / win32 / darwin.

**Fixture shelf life:** `scripts/seed-baseline-fixtures.ts` carries a fixed `2026-09-16` (`:84-85`).
Today is 2026-08-30 — inside the window. Past it, seven baselined surfaces stop being reachable and
the dispatch fails loudly.

**The command sequence is already written out** in `17.1-RESEARCH.md` § Code Examples → "The dispatch
sequence (item 1a)". Follow it; do not improvise a variant.
</interfaces>

<constraints>
**Five things that turn this plan into a defect if they are done differently.**

1. **Prove the write path untouched by PARSING, never by grepping.** `baselines.yml`'s own vacuity
   table is **six of six green** against six real mutations, *including an added `push:` trigger*, while
   all seven substring tokens remain present. Use `node scripts/verify-workflows.mjs` (38 invariants:
   baselines 11, ci 20, cross 7). And the mirror failure is real too — a *prohibition* checked by
   substring goes falsely **RED** on the paragraph that forbids the thing.

2. **`git pull --ff-only` before the forcing commit.** The bot commit lands on `origin/dev`; a local
   `git commit --allow-empty` without pulling first pushes a divergent history.

3. **An unpredicted changed row is a FINDING.** `--update-snapshots` writes whatever renders.
   `dev-theme-1280-court.png` was *1 flaky* one run ago and is the live candidate. If it appears in the
   diff, investigate and write it up in `baseline-evidence.md` before close — never absorb it, never
   widen a threshold, never re-dispatch it away.

4. **`--project=visual` does not exist on this machine and that is by design (D-29).**
   `playwright.config.ts` builds no `visual` project off Linux; `--project=visual` errors
   *"Project(s) \"visual\" not found"* here. Do not try. `baselines.yml`'s `workflow_dispatch` is the
   only path to a baseline, and `updateSnapshots: "none"` (D-28) may not be touched.

5. **Nothing lands after §7d's comparison run.** The only exception is §7d's own record and the phase's
   closing documentation, which cannot exist before the run they describe. Task 3 handles that
   explicitly.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the write path untouched, re-predict the diff, and confirm the tree is final</name>
  <files>.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` — **the whole file**: §1-§3 (the commit, the disk state, the untouched write path), §4 (the superseded prediction, left standing on purpose), §5, §6, and §7a in full including its seven-width table and the two consequences it draws
    - `.github/workflows/baselines.yml` — read in full, header included: the trigger, the job-level `contents: write`, the container tag, the postgis service, the staging tripwire, the T-11-SC supply-chain paragraph, the six-of-six vacuity table, and the `GITHUB_TOKEN` fact with its flip side
    - `.github/workflows/ci.yml` `:340-380` — the trigger block, to see for yourself that there is no `workflow_dispatch`
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md` `P4` — plan 17.1-04's per-PNG prediction, which must be folded in
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Item 1a — all of it, especially "The failure modes, named" and "What §7c and §7d require to be recorded"
  </read_first>
  <action>
    **Confirm the tree is final first.** `git status --porcelain` must be clean and
    `git rev-parse HEAD` must equal `git rev-parse origin/dev` after a `git fetch`. Every plan 17.1-01
    through 17.1-06 is committed **and pushed**. If anything is outstanding, stop — D-03 makes this
    plan unrunnable until it is not.

    **Prove the write path is untouched:**
      git diff --name-only HEAD -- playwright.config.ts .github/workflows/baselines.yml .github/workflows/ci.yml
      git log --oneline -3 -- playwright.config.ts .github/workflows/
      node scripts/verify-workflows.mjs
    The first must print nothing across the whole phase; the parser must exit **0** reporting all 38
    invariants. Record both outputs.

    **Record the pre-dispatch shape**, so the after-state is read against a before-state:
      gh run list --limit 5 --json databaseId,name,status,conclusion,headSha,event
    Capture the current `dev` CI run's id, conclusion, and its per-job breakdown — `gate-db-free`,
    `gate-db`, `gate-price-parity` success, `gate-visual` failure — plus the failing rows' exact
    messages. ⚠ The run ids in RESEARCH (33312650070 and the two before it) are for the **old** HEAD;
    this phase has pushed six plans since. Read the current ones; do not copy the recorded numbers.

    **Confirm the preconditions:**
      - `npx playwright --version` is **1.60.0**, matching `baselines.yml`'s container tag
      - `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l` is **36**
      - `ls e2e/visual/surfaces.spec.ts-snapshots/ | grep -c 'grove\|win32\|darwin'` is **0**
      - `scripts/seed-baseline-fixtures.ts`'s fixed `2026-09-16` is still in the future
      - `EXPECTED_BLOCKED` and `EXPECTED_BASELINE_COUNT = 78` are unchanged from the phase's start

    **Then write the re-prediction** into `17.1-EVIDENCE.md` as **`D1 - the phase's pre-dispatch
    prediction`**: §7a's two rows, plus `P4`'s checkout rows folded in with their measured deltas, plus
    an explicit line for every other PNG that is expected **not** to move — naming `listing-detail-1280`,
    `listing-lightbox-1280`, `listing-sheet-375`, `checkout-320`, `checkout-1280` and `dev-theme-1280`
    (the last because it flaked one run ago and is the live unpredicted-row candidate). State the totals:
    N changed, 0 added, 0 deleted, disk 36.

    ⚠ Write `D1` **before** dispatching. This is the whole discipline: *"a prediction written after
    reading a diff is not a prediction, it is a description."* Do not edit `baseline-evidence.md` §4 —
    it stays standing verbatim as the record of what the quick was authorised against.
  </action>
  <acceptance_criteria>
    - `git status --porcelain` is empty and `git rev-parse HEAD` equals `git rev-parse origin/dev`
    - `git diff --name-only HEAD -- playwright.config.ts .github/workflows/baselines.yml .github/workflows/ci.yml`
      prints nothing, and no commit in this phase touched any of those three paths
    - `node scripts/verify-workflows.mjs` exits **0** and its output names all 38 invariants; the output
      is recorded verbatim
    - `npx playwright --version` reports 1.60.0
    - `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l` is 36 and the grove/win32/darwin count is 0
    - `grep -c "EXPECTED_BASELINE_COUNT = 78" e2e/visual/surfaces.spec.ts` is 1
    - `17.1-EVIDENCE.md` contains a `D1` section, written before any `gh workflow run`, listing every
      expected-to-move and expected-not-to-move PNG by name with its predicted delta, and the totals
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` §4 is byte-identical
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-workflows.mjs && node -e "const cp=require('child_process');const dirty=cp.execSync('git status --porcelain').toString().trim();if(dirty)throw new Error('working tree is not clean; D-03 requires a final tree before the dispatch:\n'+dirty);const n=require('fs').readdirSync('e2e/visual/surfaces.spec.ts-snapshots').length;if(n!==36)throw new Error('baseline count is '+n+', expected 36');const wf=cp.execSync('git diff --name-only HEAD -- playwright.config.ts .github/workflows/baselines.yml .github/workflows/ci.yml').toString().trim();if(wf)throw new Error('the write path is modified: '+wf);const ev=require('fs').readFileSync('.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md','utf8');if(!ev.includes('D1'))throw new Error('the pre-dispatch prediction D1 is not written yet');console.log('ok')"</automated>
  </verify>
  <done>The tree is clean and equals `origin/dev`; the write path is proved untouched by the 38-invariant parser; the pre-dispatch CI shape is recorded; `D1` carries a per-PNG prediction written before any dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch baselines.yml and read the diff against D1, all five points</name>
  <files>.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md</files>
  <read_first>
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` §7b and §7c — the two `To be recorded:` stubs this task fills, and the sentence that makes §7b's label load-bearing
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Code Examples → "The dispatch sequence (item 1a)", steps 2 and 3
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md` `D1` — the prediction the diff is read against
    - `.github/workflows/baselines.yml` — the staging step, which fails without committing if anything other than `*-visual-linux.png` is staged; and the `::warning::` step
  </read_first>
  <action>
    **GENERATION.** `gh workflow run baselines.yml --ref dev`, then poll:
      gh run list --workflow=baselines.yml --limit 3 --json databaseId,status,conclusion,headSha

    Fill in **§7b** with the run id, its conclusion and its `headSha`, **under the heading it already
    carries** — *"Generation run — NOT the evidence"* — and keep that framing in the text you add. Say
    why in the record, not just in the heading: the run's commit is pushed with `GITHUB_TOKEN`, and a
    `GITHUB_TOKEN` push triggers no workflow, so at the moment it finishes nothing has ever compared
    against the PNGs it wrote. If the job goes red, read the log before doing anything else — a red
    generation run is a finding about the fixture or the container, not a reason to re-dispatch.

    **READ THE DIFF.** `git pull --ff-only`, then read it file by file and fill in **§7c**, all five
    points, against **`D1`** (which is §7a's corrected prediction plus plan 17.1-04's folded-in rows) —
    **not** against §4's superseded three:

      1. every changed/added path ends `-visual-linux.png`;
      2. zero `*-grove-*`, zero `win32`, zero `darwin`;
      3. exactly the predicted files changed — list them, with the byte/size deltas from
         `git show --stat HEAD`;
      4. zero added and zero deleted — disk still **36**;
      5. `listing-lightbox-1280` and `listing-sheet-375` did **not** move (they are `viewport` captures).

    Useful reads: `git show --stat HEAD`, `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l`,
    `ls e2e/visual/surfaces.spec.ts-snapshots/ | grep -c 'grove\|win32\|darwin'`.

    ⚠ **STOP if anything outside `D1` moved.** That is an unpredicted changed row and it is a
    **finding**: investigate it and write it into `baseline-evidence.md` before this phase closes.
    `dev-theme-1280-court.png` is the live candidate — it was *1 flaky* one run ago on a surface no plan
    in this phase touches. Do not absorb it, do not widen a threshold, and do not re-dispatch it away.

    ⚠ Do not run `--project=visual` locally to "check" the result. It does not exist here by design
    (D-29) and attempting it produces a misleading error rather than a measurement.
  </action>
  <acceptance_criteria>
    - `baseline-evidence.md` §7b names the generation run's id, conclusion and `headSha`, and states in
      its own text that this run is not the evidence and why
    - §7c is filled in with **all five** numbered points answered, each with the command output that
      answers it
    - §7c's point 3 lists exactly the PNGs `D1` predicted, with their size deltas
    - `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l` is **36** after the pull
    - `ls e2e/visual/surfaces.spec.ts-snapshots/ | grep -c 'grove\|win32\|darwin'` is **0**
    - `git show --stat HEAD` shows only `*-visual-linux.png` paths
    - `node scripts/verify-workflows.mjs` still exits 0 after the pull
    - §4 is still byte-identical
    - If any unpredicted row moved: a finding exists in `baseline-evidence.md`, written before the next
      task runs, and no threshold was changed
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-workflows.mjs && node -e "const cp=require('child_process');const fs=require('fs');const files=fs.readdirSync('e2e/visual/surfaces.spec.ts-snapshots');if(files.length!==36)throw new Error('baseline count moved to '+files.length+' — 0 added and 0 deleted was predicted');const bad=files.filter(f=>!/-visual-linux\.png$/.test(f)||/grove|win32|darwin/.test(f));if(bad.length)throw new Error('unsanctioned baseline files: '+bad.join(', '));const stat=cp.execSync('git show --stat HEAD').toString();const s=fs.readFileSync('.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md','utf8');if(s.includes('To be recorded'))throw new Error('baseline-evidence.md still has unfilled To-be-recorded stubs');console.log('ok')"</automated>
  </verify>
  <done>§7b records the generation run labelled as not-the-evidence; §7c answers all five points against `D1` with command output; disk is still 36 and only `*-visual-linux.png` moved.</done>
</task>

<task type="auto">
  <name>Task 3: Force the comparison run, record §7d, and land nothing after it</name>
  <files>.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md</files>
  <read_first>
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` §7d — the stub this task fills, and its closing sentence: *"A green comparison run on the head commit is the deliverable. Nothing lands after it — a later commit invalidates it."*
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/PLAN.md` Task 3d — ⚠ it offers *"or re-dispatch `ci.yml`"* as an alternative. **That alternative does not exist on this tree** (`ci.yml` has no `workflow_dispatch`) and must not be repeated
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Item 1a → "The failure modes, named", items 1-4
    - `.planning/quick/260828-qd5-declare-tsx-as-a-devdependency-so-creden/260828-qd5-SUMMARY.md` — its frontmatter's `uncommitted_for_orchestrator:` key, which is this repo's established idiom for an artefact that must exist but must not be committed by the executor
  </read_first>
  <action>
    **FORCE THE COMPARISON.** `ci.yml` has no `workflow_dispatch` trigger and "Re-run all jobs" on an
    older run re-runs at that run's SHA, which predates the new PNGs. A new commit is the only way:

      git pull --ff-only
      git commit --allow-empty -m "chore(17.1): force a gate-visual comparison against the regenerated baselines"
      git push origin dev

    The pull is not optional — the bot commit landed on `origin/dev` and committing without it pushes a
    divergent history.

    **RECORD §7d.** Wait for the `ci` run triggered by that push, then:
      gh run view <comparison-run-id> --json name,conclusion,headSha --jq '[.name,.conclusion,.headSha]'
      git rev-parse HEAD

    Fill in §7d with the run's **id, conclusion and `headSha`**, under a heading stating plainly that
    this is the closing evidence, and assert `headSha == git rev-parse HEAD` by showing both strings.
    Record the per-job breakdown including `gate-visual`, and state explicitly that the workflow name is
    **`ci`**, not `baselines` — a recorded run id whose workflow name is `baselines` is the exact
    failure Pitfall 3 names.

    If `gate-visual` is **not** green, stop. A red comparison after a green generation means the
    regenerated PNGs disagree with what CI renders, which is a finding about determinism, not a reason
    to re-dispatch.

    **THE ONE THING THAT MAY EXIST AFTER THE COMPARISON RUN, AND HOW.** §7d's own record cannot be
    written before the run it describes, so it cannot be inside the commit that triggered it. Leave the
    §7d edit — and this plan's SUMMARY — **uncommitted in the working tree**, and list them under
    `uncommitted_for_orchestrator:` in the SUMMARY frontmatter, which is this repo's established idiom
    (quick `260828-qd5` used it). State in §7d itself, in one sentence, that the recording is
    deliberately uncommitted so that `headSha == HEAD` is true at the moment it is read, and that any
    later docs-only commit by the orchestrator will trigger its own `ci` run whose conclusion must also
    be green — appended as a confirmation line, never as a replacement for this one.

    **Nothing else lands.** No code change, no gate change, no `e2e-baseline-reds.md` row, no threshold.
  </action>
  <acceptance_criteria>
    - A `ci` workflow run exists whose `headSha` equals `git rev-parse HEAD` and whose conclusion is
      `success`, with `gate-visual` among its successful jobs
    - §7d records that run's id, conclusion, `headSha`, workflow **name** (`ci`), and the per-job
      breakdown, and shows `git rev-parse HEAD` beside `headSha` as two identical strings
    - §7d states in its own text that the generation run recorded in §7b is not this evidence
    - `git log --oneline -1` is the forcing commit, and `git log --oneline HEAD~1..HEAD --stat` shows it
      is empty of file changes
    - `git status --porcelain` shows exactly the §7d edit and the plan SUMMARY as uncommitted, and the
      SUMMARY frontmatter lists both under `uncommitted_for_orchestrator:`
    - `node scripts/verify-workflows.mjs` exits 0
    - `git diff --name-only HEAD -- playwright.config.ts .github/workflows/` prints nothing
    - `.planning/phases/17-*/e2e-baseline-reds.md` is byte-identical across the whole phase
    - `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l` is 36
  </acceptance_criteria>
  <verify>
    <automated>node -e "const cp=require('child_process');const head=cp.execSync('git rev-parse HEAD').toString().trim();const runs=JSON.parse(cp.execSync('gh run list --workflow=ci.yml --limit 5 --json databaseId,name,conclusion,headSha').toString());const m=runs.find(r=>r.headSha===head);if(!m)throw new Error('no ci run found whose headSha equals HEAD ('+head+') — the comparison has not run on the head commit');if(m.conclusion!=='success')throw new Error('the comparison run '+m.databaseId+' concluded '+m.conclusion+', not success');console.log('comparison run '+m.databaseId+' is green on HEAD '+head)"</automated>
  </verify>
  <done>A green `ci` run exists whose `headSha` equals `git rev-parse HEAD` with `gate-visual` included; §7d records its id, conclusion and headSha beside `git rev-parse HEAD`; the record is deliberately uncommitted and flagged for the orchestrator; nothing else landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → GitHub Actions | `baselines.yml` is the one job carrying `contents: write` and the one path that may mint a baseline |
| the generation run → the comparison run | A `GITHUB_TOKEN` push triggers no workflow, so the two are separated by design and only the second is evidence |
| the recorded evidence → the tree it describes | `headSha == git rev-parse HEAD` is only true while nothing lands after it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-33 | Elevation of Privilege (ASVS V14) | `.github/workflows/baselines.yml` — the one job with `contents: write` | mitigate | The file is **read only, never edited**, and the proof is a PARSE (`node scripts/verify-workflows.mjs`, 38 invariants), never a grep — that file's own vacuity table is six of six green against six real mutations including an added `push:` trigger. `git diff --name-only` on all three write-path files must print nothing across the whole phase. |
| T-17.1-34 | Information Disclosure | a credential entering the baseline job | mitigate | `baselines.yml` holds **zero** `secrets.*` and `verify-workflows.mjs` asserts that over every `env`/`run`/`with` value (T-11-CISECRET, D-35's boundary). This plan adds no step, no action and no package to that job — T-11-SC's supply-chain paragraph is why. |
| T-17.1-35 | Spoofing (of evidence) | recording the generation run as the deliverable | mitigate | §7b's heading and text both state it is not the evidence, and §7d asserts the recorded run's workflow **name** is `ci`. Pitfall 3's warning sign is *"a recorded run id whose workflow name is `baselines`"* — the acceptance criteria check the name, not just the id. |
| T-17.1-36 | Tampering | an unpredicted baseline row absorbed silently | mitigate | `D1` is written **before** the dispatch and the diff is read against it file by file. An unpredicted changed row is a finding written into `baseline-evidence.md`, never a threshold change and never a re-dispatch. `dev-theme-1280-court.png` is named in advance as the live candidate. |
| T-17.1-37 | Repudiation | a commit landing after the comparison run | mitigate | This plan is the final wave and runs alone; Task 1 refuses to start on a dirty tree or a HEAD that differs from `origin/dev`. §7d's own record is deliberately left **uncommitted** and flagged `uncommitted_for_orchestrator`, so `headSha == HEAD` is true at the moment it is read. |
| T-17.1-38 | Denial of Service | a stale or expired fixture | mitigate | `scripts/seed-baseline-fixtures.ts`'s fixed `2026-09-16` is asserted to be in the future before dispatching. Past it, seven baselined surfaces stop being reachable and the dispatch fails loudly — a loud failure, not a silent wrong baseline. |
| T-17.1-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan**, and none is added to the CI job. `git diff --name-only -- package.json package-lock.json .github/workflows/` printing nothing is an acceptance criterion. |
</threat_model>

<verification>
- `node scripts/verify-workflows.mjs` exit 0, before **and** after every step
- `git diff --name-only HEAD -- playwright.config.ts .github/workflows/baselines.yml .github/workflows/ci.yml`
  prints nothing
- `ls e2e/visual/surfaces.spec.ts-snapshots/ | wc -l` is 36; grove/win32/darwin count is 0
- A `ci` run, green, with `gate-visual` included, whose `headSha` equals `git rev-parse HEAD`
- `.planning/phases/17-*/e2e-baseline-reds.md` byte-identical across the whole phase
- ⚠ `--project=visual` does not exist locally and that is by design (D-29). **Do not try.**
  `updateSnapshots: "none"` (D-28) may not be touched.
</verification>

<success_criteria>
- The write path is proved untouched by a 38-invariant parse, recorded verbatim
- `17.1-EVIDENCE.md` `D1` carries a per-PNG prediction written before the dispatch, folding in plan
  17.1-04's `P4` rows
- `baseline-evidence.md` §7b, §7c and §7d are all filled in, with §4 left byte-identical
- §7c answers all five points; exactly the predicted PNGs changed; 0 added, 0 deleted, disk 36
- A green `ci` run exists whose `headSha` equals `git rev-parse HEAD`, with `gate-visual` included, and
  §7d records its id, conclusion and headSha under a heading naming it the closing evidence
- Nothing landed after that run; §7d's record and the plan SUMMARY are the only uncommitted artefacts
  and are flagged `uncommitted_for_orchestrator`
- `dev` is green
</success_criteria>

<output>
Create `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-07-SUMMARY.md` when done — and leave it **uncommitted**, per Task 3.
</output>
