---
phase: 17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro
verified: 2026-08-31T06:30:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 17.1: Close Phase 17 Escalations Verification Report

**Phase Goal:** The four escalate-class findings the PM promoted at Phase 17's D-199 review stop being recorded and start being fixed.
**Verified:** 2026-08-31
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Item 1a: the held `baselines.yml` dispatch ran and the forced comparison run is the recorded, independently-verifiable evidence | ✓ VERIFIED | `D1` prediction (2 changed / 0 added / 0 deleted / disk 36) committed at `9507989` before dispatch. `gh run view 33336650152` (live API call, this session) → `{"conclusion":"success","event":"push","headSha":"d87ff54...","name":"ci"}`. `git rev-parse HEAD` on the forcing commit == that headSha. `gate-visual` job: 43 passed · 42 skipped, both previously-red rows (`listing-detail-320`, `listing-detail-768`) passed first attempt |
| 2 | Item 1b: `/listings/[id]/book`'s 64px bar over 48px padding across 640–1023px is repaired, measured on pixels not classes | ✓ VERIFIED | `src/app/listings/[id]/book/page.tsx:549-550` carries `STICKY_BAR_CLEARANCE, "sm:pb-20 lg:pb-12"`, committed at `41dd74e`, clean in working tree. § P4 measured `getComputedStyle(main).paddingBottom` = 80px at 320/639/640/768/1023, 48px at 1024/1280, both themes. `e2e/mobile-booker-path.spec.ts` band case (`expectDocumentEndClearsBar`) asserts pixels against the rendered bar height, re-run this session: 11 passed / 3 skipped |
| 3 | Item 2: the soft-404 probe on `/listings/[id]` was measured FIRST under a production build, and no repair was licensed unless it regressed | ✓ VERIFIED | `17.1-EVIDENCE.md` § P1: 404/404/200 (draft/nonexistent/published-control) under `next build && next start`, two passes, no 5xx. Task 3's checkpoint correctly not reached. `[17-D1]` carries the dated correction (C1/C2) with 0 deletions on the pre-existing body (`git diff --numstat` recorded as 80 insertions, 0 deletions in the summary). Dev-side control `e2e/public-listing.spec.ts` re-run this session: 8 passed |
| 4 | Item 3: the PayMongo fetch is intercepted at the sanctioned server-side seam (not the refuted `page.route`), process-level, both endpoints, both specs | ✓ VERIFIED | `instrumentation.ts` exists at repo root, committed `6231f54`. Production guard (`NODE_ENV === "production"`) is literally the first statement of `register()`; `NEXT_RUNTIME !== "nodejs"` guard second; `await import("undici")` is dynamic and after both; `agent.enableNetConnect((host) => host !== INTERCEPTED_HOST)` present. `tests/security/paymongo-seam.test.ts` re-run this session: 13/13 passed. `[17-D18]` RESOLVED block in `deferred-items.md` names the same seam and the refuted `page.route` prescription |
| 5 | Item 4: the unreachable `(detail)/not-found.tsx` boundary is deleted and every instrument that named it is re-pointed in the same commit | ✓ VERIFIED | `git log --diff-filter=D` shows `7c3444b` deleted the file with "all four instruments" in the same commit. File confirmed absent from `src/app/listings/[id]/(detail)/`. `grep -rn "detail)/not-found" src tests e2e` → exactly 1 hit, the frozen `[17-11:` history bracket in `overflow-320.spec.ts:498`, exactly as expected — not a gap. `axe-sweep.spec.ts` AC#2 now asserts an empty diff (`{missing:[],extra:[],duplicated:[]}`), no hardcoded reference remains |
| 6 | RESP-03 advances (not closes) via the sticky-bar clause, and REQUIREMENTS.md stays untouched by this phase | ✓ VERIFIED | `git diff d69f1e4 HEAD -- .planning/REQUIREMENTS.md` → empty. `RESP-03` remains `[ ]` at line 107, "Pending" in the traceability table. `17.1-RESEARCH.md` § phase_requirements states explicitly this phase does not close RESP-03 |
| 7 | The phase's own census discovers and files new findings ([17-D27], [17-D28], [17-D29]) rather than silently absorbing or fixing them out of scope | ✓ VERIFIED | All three exist in `deferred-items.md` with measurement, owner file, severity and cheapest-fix sections. `[17-D28]` (14 real `api.resend.com` POSTs on a live key) is explicitly left open/out of scope, matching instruction |
| 8 | No production server left running; no code files/packages touched by items 1a/2 where "no repair" was the outcome; env files untouched | ✓ VERIFIED | `17.1-EVIDENCE.md` § P1 teardown: PID 3188 killed, port 3100 refuses connections. `grep -c PLATFORM_WALLET .env.local` → 0 in the summary's own verification table; independently, `git status --porcelain` on this tree is clean with zero pending changes to `.env.local`/`.env.example` |
| 9 | Full regression suite and design-time gates remain green after all four items land | ✓ VERIFIED | Re-run this session: `npm run test:design` → 67 files / 1255 passed / 3 skipped. `npx tsc --noEmit` → clean, exit 0. (Full `npm test` was already run at the regression gate immediately before this verification: 187 files / 2182 tests passed, 0 failed, per the orchestrator's note — not re-run here to avoid duplicate DB writes) |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/17.1-.../17.1-EVIDENCE.md` | Permanent probe transcript, §P1–P4/W1–W3/P3-AFTER/D1 | ✓ VERIFIED | 2292 lines, all 9 declared sections present and populated, none stubbed |
| `.planning/phases/17-.../deferred-items.md` | `[17-D1]`/`[17-D2]`/`[17-D3]`/`[17-D18]` corrected/RESOLVED; `[17-D27]`/`[17-D28]`/`[17-D29]` filed | ✓ VERIFIED | All RESOLVED/CORRECTED blocks present, dated, with re-run commands and commit hashes; measured bodies left byte-identical per grep of insertion-only diffs recorded in summaries |
| `src/app/listings/[id]/book/page.tsx` | `sm:pb-20 lg:pb-12` appended to `STICKY_BAR_CLEARANCE` | ✓ VERIFIED | Present at line 549-550, committed `41dd74e`, clean working tree |
| `instrumentation.ts` (repo root) | Dev-only PayMongo interception seam, ≥40 lines | ✓ VERIFIED | 158 lines; production guard first, runtime guard second, dynamic import third, `enableNetConnect` predicate present |
| `tests/security/paymongo-seam.test.ts` | Inertness triad + positive control, ≥120 lines | ✓ VERIFIED | 13/13 tests pass on independent re-run |
| `src/app/listings/[id]/(detail)/not-found.tsx` | Deleted | ✓ VERIFIED | Confirmed absent; `git log --diff-filter=D` shows the deleting commit |
| `package.json` / `package-lock.json` | `undici` declared at exact pin `7.27.0` | ✓ VERIFIED | `"undici": "7.27.0"` in both files, matching the already-resolved transitive version |
| `.planning/quick/260830-r4b.../baseline-evidence.md` | §7b/§7c/§7d — generation run, diff read, comparison run | ✓ VERIFIED | All three present; §7d's comparison run independently confirmed live against GitHub API this session |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `17.1-EVIDENCE.md` § P1 | `deferred-items.md` `[17-D1]` | quoted status codes | WIRED | `[17-D1]`'s correction block quotes `404/404/200` and cites the file by path |
| `e2e/mobile-booker-path.spec.ts` band case | `src/app/listings/[id]/book/page.tsx` | `getComputedStyle` on rendered `main` | WIRED | Assertion reads computed pixels, never the class list; re-run passes |
| `instrumentation.ts` | `src/lib/paymongo.ts`'s global fetch | `undici setGlobalDispatcher` | WIRED | Confirmed via test suite driving both `/host/payouts/refresh` fallback rendering and zero-outbound assertions (§ W3, § P3-AFTER) |
| `deferred-items.md` `[17-D1]` correction | `17.1-EVIDENCE.md` § D1 | dispatch prediction folded in | WIRED | § D1 explicitly folds § P4's zero-movement finding into the pre-dispatch prediction, and the actual diff matched it exactly (§7c) |
| `.planning/quick/.../baseline-evidence.md` §7d | `ci.yml` run on HEAD | `gh run view --json headSha` == `git rev-parse HEAD` | WIRED | Independently re-verified live against the GitHub API in this session, not merely trusted from the document |

### Requirements Coverage

| Requirement/Finding ID | Source Plan | Status | Evidence |
|---|---|---|---|
| `17-D1` | 17.1-01 | SATISFIED (finding corrected + re-scoped) | Dated correction on ledger, byte-identical measured body |
| `17-D2` | 17.1-01 | SATISFIED (folded, deliberately not closed) | One-line fold-in note; `(host)/host/loading.tsx` untouched by design |
| `17-D3` | 17.1-02 | SATISFIED (finding closed) | File deleted, all instruments re-pointed in same commit |
| `RESP-03` | 17.1-03/04 | ADVANCED, not closed (by design) | `deferred-items.md`/RESEARCH state this phase advances only the sticky-bar clause; REQUIREMENTS.md correctly untouched |
| `17-D10` | 17.1-03/04 | SATISFIED (checkout half closed) | `deferred-items.md` "THE OTHER HALF — RESOLVED 2026-08-31" block, commits `8b71b21`/`41dd74e` |
| `17-D18` | 17.1-05/06 | SATISFIED (finding closed) | Seam D shipped, RESOLVED block with refuted-prescription correction |
| `17-D9` | 17.1-07 | SATISFIED (dispatch/comparison completed) | Held dispatch executed, comparison run green, independently verified |
| `GATE-01` | 17.1-07 | Unaffected — already Complete since Phase 11 | REQUIREMENTS.md checkbox/traceability untouched by this phase (confirmed via empty diff); this phase discharges a live red instance of the guarantee, not a new closure |

No orphaned requirements: every ID declared across the seven plans' `requirements:` frontmatter (17-D1, 17-D2, 17-D3, RESP-03, 17-D10, 17-D18, 17-D9, GATE-01) is accounted for above.

### Anti-Patterns Found

None blocking. The code review (`17.1-REVIEW.md`, committed `fb4dae9`) found 0 Critical / 6 Warning / 8 Info — advisory, non-blocking per the task's explicit instruction, and referenced here rather than re-litigated:
- WR-01 through WR-06 concern edge-case robustness of the interception seam and stale re-measured counts in `axe-sweep.spec.ts` — none contradicts the seam's core guarantees, which were independently re-verified in this report (production guard ordering, dynamic import, 13/13 test pass).

No `TBD`/`FIXME`/`XXX` debt markers found in the files this phase modified (checked `instrumentation.ts`, `book/page.tsx`, `tests/security/paymongo-seam.test.ts`, `deferred-items.md`, `17.1-EVIDENCE.md`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Padding fix renders correctly on checkout | `npx playwright test e2e/mobile-booker-path.spec.ts --project=chromium --workers=1` | 11 passed, 3 skipped (pre-declared unreachable rows) | ✓ PASS |
| Soft-404 dev-side control still holds | `npx playwright test e2e/public-listing.spec.ts --project=chromium --workers=1` | 8 passed | ✓ PASS |
| PayMongo interception seam inertness | `npx vitest run tests/security/paymongo-seam.test.ts` | 13 passed | ✓ PASS |
| Design-time gates (inventory counts, adopter files) | `npm run test:design` | 67 files / 1255 passed / 3 skipped | ✓ PASS |
| Type safety | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| GitHub comparison run genuinely green | `gh run view 33336650152 --json name,conclusion,headSha,event` | `{"conclusion":"success","event":"push","headSha":"d87ff54...","name":"ci"}` | ✓ PASS |

### Probe Execution

This phase's "probes" (§ P1–P4) are manual production-build/browser drives recorded as permanent evidence transcripts, not `scripts/*/tests/probe-*.sh` shell scripts — no conventional probe files exist in this repo (`find scripts -path '*/tests/probe-*.sh'` returns nothing). The equivalent verification was performed by independently re-running the underlying automated instruments (playwright specs, vitest suites, `gh run view`) rather than trusting the transcript narration, per Step 7b/7c intent.

### Human Verification Required

None. Every claim in this phase is either a build-time/runtime measurement (production probe, computed-style pixels, CI run conclusion) or an automated test, and all were independently re-driven or re-queried in this verification session rather than taken from the SUMMARY narration.

### Gaps Summary

No blocking gaps. Two minor, non-blocking observations recorded for completeness (neither affects the phase goal):

1. **`baseline-evidence.md`'s confirmation-line placeholder is still unfilled.** §7d reserves a line for "the orchestrator's docs-only commit's own CI run" to be appended after the withheld artefacts land. The withheld artefacts were committed locally (`29b75cb`, then `fb4dae9` for the code review), but `origin/dev` is still at `d87ff54` — the docs commits have not been pushed, so no confirming CI run exists yet to fill the placeholder. This does not affect the phase goal: the actual comparison run (`33336650152`) that proves the baseline fix is independently confirmed green. This is a documentation loose end for whoever next pushes `dev`, not a phase gap.
2. **`dev-theme-1280`'s single-write-no-diff anomaly** (named in advance in § D1, observed in §7c) remains an open watch item with two honest readings recorded rather than a fabricated mechanism — exactly as instructed to treat as deliberate, not a gap.

---

*Verified: 2026-08-31*
*Verifier: Claude (gsd-verifier)*
