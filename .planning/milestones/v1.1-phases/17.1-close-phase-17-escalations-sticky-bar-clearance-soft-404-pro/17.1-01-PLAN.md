---
phase: 17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md
  - .planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md
autonomous: false
requirements: ["17-D1", "17-D2"]

must_haves:
  truths:
    - "D-05: the production probe runs BEFORE any item-2 repair is designed, and reads three routes — draft, nonexistent, and a PUBLISHED control — so a 404 cannot be confused with a 500ing server"
    - "D-05: assumption [A6] is settled by a number on `/listings/[id]` under `next build && next start`, not by inference from `[17-D2]`'s reading on `(host)`"
    - "D-05 / RESEARCH C1: the expected reading is 404 / 404 / 200 and a 200 on the draft is recorded as a REGRESSION of 89fb451, not as a reproduction of a known defect"
    - "D-06: no guard is hoisted onto `(app)/bookings/[id]` in this plan; if the probe reproduces, the escalation states that a hoist there changes which document the OWNER GATE serves (T-04-CONFIRMIDOR) and must preserve the served document, not merely the status code"
    - "D-14: the probe's three status codes are recorded verbatim, as numbers on named routes, in a permanent evidence file — not paraphrased into a SUMMARY sentence"
    - "RESEARCH C1 and C2 land as DATED CORRECTION lines on `[17-D1]`; its measured body is left byte-identical, per the `[17-D9]`/`[17-D10]` RESOLVED-line convention"
    - "No production server is left holding a port: the real `next start` PID is killed, not the npm wrapper, and port 3100 is free afterwards"
  artifacts:
    - path: ".planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md"
      provides: "The phase's permanent probe-transcript record, § P1 — modelled on baseline-evidence.md"
      contains: "P1"
    - path: ".planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md"
      provides: "Dated correction lines on [17-D1] (C1 + C2) and the [17-D2] fold-in"
      contains: "CORRECTED 2026-08-30"
  key_links:
    - from: ".planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md"
      to: ".planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md"
      via: "the probe's three numbers quoted into [17-D1]'s correction line"
      pattern: "17\\.1-EVIDENCE"
---

<objective>
Settle assumption **[A6]** on `/listings/[id]` with one production build, and write the number down.

Purpose: every reading of the listing route's soft-404 to date is a `next dev` reading. `[17-D2]`
measured the identical mechanism in a **production** build, but on `(host)` — a different segment.
The question has **inverted** (RESEARCH C1): `89fb451 fix(260826-l1o): /listings/[id] answers a real
404 again` landed 72 minutes after `[16-D6]` measured the 200, so 17-01 measured a **repaired** route
rather than a flaky one. The probe therefore asks *does the shipped layout-assert hold under
`next build && next start`?* — and a 200 on the draft is a **regression**, not a reproduction.

This plan runs **first in the phase** for two reasons: it is the cheapest information with the largest
scope consequence (a 200 re-scopes 17.1 into escalate-class SEO work), and it is the only plan that
runs `next build` + `next start`, which rewrites `.next/` and would poison any concurrently-running
e2e plan.

**No repair is planned here.** D-05: *"Do not restructure ten boundaries before knowing."*

Output: `17.1-EVIDENCE.md` § P1 with three status codes on three named routes, and two dated
correction lines on the ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-CONTEXT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md

<!-- ⚠ `.planning/STATE.md` is ~500KB. Never read it whole — Grep for a phrase, then Read with
     offset/limit around the hit. -->

<interfaces>
The three operational facts this probe fails on if they are not carried (RESEARCH § Item 2, Pitfalls 4/5):

  1. `src/lib/paymongo.ts:39-48` refuses to boot in production without `PLATFORM_WALLET_NUMBER` /
     `PLATFORM_WALLET_NAME`. The guard is exempted only for `NEXT_PHASE=phase-production-build`, which
     is exactly why `npm run build` is green while `npm start` is not. Both vars are ABSENT from
     `.env.local`. Pass throwaway values INLINE for that process only — never write them to `.env.local`.
     `PAYMONGO_SECRET_KEY` and `BETTER_AUTH_SECRET` come from `.env.local`, which `next start` loads.

  2. `playwright.config.ts:113` sets `reuseExistingServer: !process.env.CI`. A production server left
     on :3000 is ADOPTED by every later Playwright run — the `[17-D24]` trap. Use `-p 3100`, the port
     `[17-D2]` used.

  3. Killing the `npm` wrapper leaves `next start` alive holding the port. Find the real PID.

Listing ids are QUERIED, never hard-coded (RESEARCH § Don't Hand-Roll — the seed periodically resets;
22 drafts / 16 published on this DB today):

  docker exec fitout-db-1 psql -U fitout -d fitout -tAc \
    "select id from listing where status = 'draft' limit 1;"
  docker exec fitout-db-1 psql -U fitout -d fitout -tAc \
    "select id from listing where status = 'published' limit 1;"

The mechanism under test, already shipped — `src/app/listings/[id]/(detail)/layout.tsx:27-37` + `:31`:
  `loading.tsx` creates an implicit Suspense boundary; Next flushes the shell (committing `200`)
  before the page body reaches `notFound()`. A LAYOUT renders in the shell, above that boundary, so
  `await assertPublicListing(id)` there blocks the flush and the status is still settable.
  `assertPublicListing` is `cache()`d, so the layout's lookup and the page's are ONE query.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive the production status-line probe and record all three readings verbatim</name>
  <files>.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Item 2 → "The probe", "Four operational facts", "The two possible outcomes"; and § Code Examples → "The production status-line probe (item 2)" — the command sequence is already written out there
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Corrections to the ledger → **C1** — why the expected reading is 404 and why a 200 is a regression
    - `.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` §7a — the recording FORMAT to copy: a table of measured values, the prediction quoted beside it, and the correction appended rather than the prediction rewritten
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md` `[17-D2]` (`:83-110`) — the production probe this one mirrors, including its port choice
    - `src/lib/listing/public-listing.ts` — read the header in full: the whole soft-404 experiment matrix, the layout-seam argument, the nine-route scope and the eight acceptances
  </read_first>
  <action>
    Create `17.1-EVIDENCE.md` in the phase directory with a short header stating it is this phase's
    permanent probe-transcript record — the same role `baseline-evidence.md` plays for quick
    `260830-r4b` — and a section per probe (`P1` item 2, `P2` item 1b band, `P3` item 3 census,
    `P4` item 1b seven-width). Later plans append their own sections; this task writes the header and
    `§ P1` only.

    Clear `.next/` before building (a stale `.next/dev/types` is the recorded cause of a class of false
    reds). Then: `npm run db:up`; query the draft id and the published id with the two `psql` commands
    in `<interfaces>`; `npm run build`; start the production server with
    `PLATFORM_WALLET_NUMBER` and `PLATFORM_WALLET_NAME` set INLINE on that one process, on port
    **3100**; then read all three routes with `curl -s -o /dev/null -w '%{http_code}'`:
      - `/listings/<draft-id>`
      - `/listings/a-listing-that-must-never-exist-17-1`
      - `/listings/<published-id>` — the CONTROL, and it is mandatory: a probe that only reads 404
        cannot distinguish "the guard works" from "the whole app is 500ing".

    Record in `§ P1`, verbatim and as numbers on named routes: the two queried ids, the three status
    codes in the order above, the exact `next start` invocation (with the wallet values shown as the
    throwaway literals they are), the Next version line the server printed, and the commit SHA the
    build was made from. State the prediction (404 / 404 / 200, per C1) BEFORE the readings in the
    section, so the section is a prediction read against a measurement rather than a description.

    Then kill the server by its real PID and verify port 3100 is free. Do not leave a production
    server running — the next plan drives Playwright against `next dev`.

    ⚠ If any route reads a 5xx, that is the boot guard or a build failure, NOT a status-line finding
    (Pitfall 5). Re-read the wallet vars and re-run before recording anything.
  </action>
  <acceptance_criteria>
    - `17.1-EVIDENCE.md` exists and contains a `§ P1` section naming all three routes and their three
      status codes as integers
    - The published-listing control is one of the three readings and its code is recorded (a `§ P1`
      with only two readings is incomplete, whatever the numbers say)
    - The two listing ids in `§ P1` are the ids the two `psql` queries returned on this run — no UUID
      is copied from any planning document
    - `grep -c "PLATFORM_WALLET" .env.local` → 0 (the throwaway values were passed inline and were not
      written into the env file)
    - `git diff --name-only -- .env.local .env.example` prints nothing
    - Port 3100 answers nothing after the task: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3100/`
      fails to connect
    - `npx playwright test e2e/public-listing.spec.ts --project=chromium --workers=1` still passes
      (8 passed) against `next dev` — the dev-side half of the same assertion, unchanged by this task
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/public-listing.spec.ts --project=chromium --workers=1</automated>
    <human-check>The production half is manual-only and RESEARCH § Validation Architecture justifies it: no harness in this repo boots `next start`. The three `curl` codes recorded in `§ P1` ARE the deliverable.</human-check>
  </verify>
  <done>`17.1-EVIDENCE.md` § P1 carries a written prediction and three measured status codes on three named routes; no server is left running; `.env.local` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Land the dated corrections C1 and C2 on [17-D1], and fold [17-D2] in</name>
  <files>.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md</files>
  <read_first>
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md` `[17-D1]` (`:39-81`) and `[17-D2]` (`:83-110`) — the rows being corrected, in full
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md` `[17-D9]` (`:403-412`) and `[17-D10]` (`:403-412`'s sibling) RESOLVED lines — **the format**: the measured body is left BYTE-IDENTICAL and the correction is appended with its date and the plan/commit that made it, including the sentence naming which pointers were wrong
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Corrections to the ledger → **C1** (the git-log claim is false, with the three-commit transcript) and **C2** (neither repair moves `loading-coverage.test.ts`'s pins, with the four-row table)
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-PATTERNS.md` § S5 "The dated bracketed amendment" — the rule the sources share
    - `tests/design/loading-coverage.test.ts` `:502` and `:515` — confirm for yourself that the collector takes `page.tsx` and `loading.tsx` only, before writing that C2 is true
  </read_first>
  <action>
    Append to `[17-D1]`, after its existing body and without editing a byte of it, a
    `**CORRECTED 2026-08-30 — plan 17.1-01.**` block carrying three things:

    (a) **C1.** Re-run `git log --oneline d24b212..HEAD -- "src/app/listings/[id]/(detail)/"` and paste
        the output. The row's claim that the range is empty is false; it contains three commits, one of
        which is `89fb451 fix(260826-l1o): /listings/[id] answers a real 404 again`. State that
        `[16-D6]`'s 200 was measured at 14:51 and the fix landed at 16:03, 72 minutes later, so 17-01's
        non-reproduction measured a REPAIRED route rather than an environment. The row's **measurement**
        stands; its **inference** does not. Do not rewrite the measured body.

    (b) **C2.** The row's claim that both repair shapes *"move `tests/design/loading-coverage.test.ts`'s
        pins"* is false for Shape A and true only for Shape B. Name the four pins by their identifiers
        — `EXPECTED_PAGES`, `EXPECTED_QUALIFYING`, `EXPECTED_NON_QUALIFYING`, and the
        `LOADING_FILES.length === EXPECTED_QUALIFYING` clause — and state that the collector takes
        `page.tsx` and `loading.tsx` only, so `layout.tsx` and `not-found.tsx` are invisible to it.

    (c) **The probe.** Quote `§ P1`'s three status codes and point at `17.1-EVIDENCE.md § P1` by path.
        State what they license, using the branch that actually occurred:
          - 404 / 404 / 200 → `[A6]` is settled POSITIVELY on this route; the shipped layout assert
            survives a production build; **no repair on `/listings/[id]`**; the row's remaining live
            scope is the eight routes `public-listing.ts`'s header already argues are accepted
            (`listings/[id]/book`, reached only by `redirect()` from a server action, and the seven
            `bookings/[id]*` / `host/*` routes that 307 to `/login` and never render anonymously).
          - anything else → see Task 3; do NOT write a disposition in that case, write the finding.

    Append to `[17-D2]` a one-line dated note recording that it is folded into `[17-D1]`'s decision as
    it asks to be, and that it remains the production measurement of the UNFIXED mechanism on `(host)`
    — the row is not closed by this phase and `(host)/host/loading.tsx` is not touched.

    Do not touch any other row. `[17-D19]` in particular stays parked (RESEARCH § Deferred Ideas).
  </action>
  <acceptance_criteria>
    - `grep -c "CORRECTED 2026-08-30 — plan 17.1-01" deferred-items.md` → 2 (one on `[17-D1]`, one on `[17-D2]`)
    - `[17-D1]`'s pre-existing body is byte-identical: `git diff` on the file shows only ADDED lines
      within the `[17-D1]` and `[17-D2]` sections, zero deletions and zero modifications
    - The correction block quotes the real `git log --oneline d24b212..HEAD -- "src/app/listings/[id]/(detail)/"`
      output run on this tree, and that output names `89fb451`
    - The correction block names all four `loading-coverage.test.ts` pin identifiers
    - The correction block cites `17.1-EVIDENCE.md § P1` by path and repeats its three status codes
    - `git diff --stat -- .planning/phases/17-*/deferred-items.md` shows the only changed file, and
      `git diff --name-only -- src tests e2e` prints nothing (this task changes no code)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md','utf8');const n=(s.match(/CORRECTED 2026-08-30 — plan 17\.1-01/g)||[]).length;if(n!==2)throw new Error('expected 2 dated corrections, found '+n);if(!s.includes('89fb451'))throw new Error('C1 correction does not quote the git log');if(!s.includes('EXPECTED_QUALIFYING'))throw new Error('C2 correction does not name the loading-coverage pins');console.log('ok')"</automated>
  </verify>
  <done>`[17-D1]` carries a dated correction proving C1 and C2 with re-run commands, and quotes § P1's three codes; `[17-D2]` is folded in; no measured body was rewritten.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: PM decision — ONLY IF the draft listing read 200 in the production build</name>
  <files>.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md</files>
  <read_first>
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md` § P1 — this task's entry condition is the number recorded there
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Item 2 → "If (and only if) it reproduces: the two repair shapes and what each costs", including the recursion trap
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-CONTEXT.md` **D-06**
    - `tests/design/empty-state-adoption.test.ts` `:424-429` — the sentence that makes `(app)/bookings/[id]/not-found.tsx`'s copy a SECURITY property
  </read_first>
  <action>
    **Gate this task on the number in `17.1-EVIDENCE.md` § P1, and read it before doing anything else.**

    If § P1 read **404 / 404 / 200**, this task is NOT APPLICABLE: record "Task 3 not reached — § P1
    read 404 / 404 / 200, the shipped layout assert holds in a production build" in the plan SUMMARY,
    do not present the checkpoint, and finish the plan.

    If the draft listing read **200**, do three things before asking anything:
      1. Re-run the probe once to rule out a stale `.next/` or a boot-guard 5xx misread as a status-line
         result (Pitfall 5) — a second reading on a cleared `.next/` is cheap and settles it.
      2. Write the finding into `deferred-items.md` in that file's own format — the measurement (status
         code, route, build type, commit SHA), the owner file, why it was not fixed here, and the
         cheapest correct fix — **before** any repair is designed.
      3. Name D-06's constraint inside that finding, in its own sentence: a Shape-A hoist on
         `(app)/bookings/[id]` changes which document the OWNER GATE serves (T-04-CONFIRMIDOR
         deliberately returns the same page for "missing" and "not yours"), so any repair there must
         preserve the served document, not merely the status code.

    Then present the four options below and stop. Do not begin a repair on either shape without an
    answer — a 200 here is escalate-class and is larger than this phase was scoped for.
  </action>
  <decision>
    **This task is NOT REACHED if § P1 read 404 / 404 / 200.** In that case record "not reached —
    the shipped layout assert holds in a production build" in the plan SUMMARY and skip it. It is
    reached only if the draft listing answered **200**, which is a REGRESSION of `89fb451` on the
    product's highest-intent public route, and is escalate-class: bigger than this phase planned for.
  </decision>
  <context>
    A 200 on a draft listing in production means an unpublished listing stays indexable. The repair is
    a route-file argument, and D-06 puts a hard constraint on one of the two shapes.
  </context>
  <options>
    <option id="shape-a-listing-only">
      <name>Shape A, on `listings/[id]` only — hoist/repair the guard above the Suspense boundary</name>
      <pros>It is the one publicly-indexable route of the nine, so it is where a status code is observable by a crawler at all. Moves ZERO `loading-coverage.test.ts` pins (RESEARCH C2). It is the shape the route already ships, so the repair is a diagnosis of why the shipped shape stopped working rather than a new mechanism.</pros>
      <cons>Requires finding out why the shipped assert stopped surviving a production build — an unknown, and this plan has not budgeted for it.</cons>
    </option>
    <option id="shape-a-all-segments">
      <name>Shape A across the remaining eight segments</name>
      <pros>Uniform.</pros>
      <cons>⚠ **D-06 blocks this on two segments.** A `notFound()` raised in a LAYOUT is handled by the PARENT segment's boundary, so hoisting into `(app)/bookings/[id]/layout.tsx` makes `(app)/bookings/[id]/not-found.tsx` unreachable — and that file's copy is what the OWNER GATE returns for BOTH a missing booking and a booking that belongs to somebody else (T-04-CONFIRMIDOR). The same trap applies to `(public)/invite/[token]/not-found.tsx`, a byte-identical inactive twin of the invite surface (T-11-ORACLE). Both are seven routes whose status is unobservable anonymously anyway — they 307 to /login.</cons>
    </option>
    <option id="shape-b">
      <name>Shape B — drop the `loading.tsx` on the routes whose guard must own the status line</name>
      <pros>Directly removes the Suspense boundary that commits the 200.</pros>
      <cons>**NOT a two-line change.** It reds `loading-coverage.test.ts`'s *"gives every async-default page a sibling loading.tsx"* clause AND its `LOADING_FILES.length` pin — a BUILD-BLOCKING gate that runs inside `npm run build` and CI's `gate-db-free`. Taking it means amending a gate to carve out an exception: a decision about STATE-01 coverage, not a fix.</cons>
    </option>
    <option id="record-and-defer">
      <name>Record the regression as an escalate-class finding and defer the repair to its own phase</name>
      <pros>Keeps 17.1 inside the four items it was scoped for. The finding is written in `deferred-items.md`'s format — a number, on a named route, in a named build — so the next phase inherits a measurement rather than an anecdote.</pros>
      <cons>A live SEO defect stays live for another phase.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - If reached: a finding is written into `deferred-items.md` in that file's format (the measurement:
      status code, route, build type, commit SHA; the owner file; why it was not fixed here; the
      cheapest correct fix) BEFORE any repair is attempted
    - If reached: the finding's text names D-06's constraint explicitly — any Shape-A hoist on
      `(app)/bookings/[id]` must preserve the SERVED DOCUMENT, not merely the status code
    - If not reached: the plan SUMMARY records "Task 3 not reached — § P1 read 404 / 404 / 200"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const ev=fs.readFileSync('.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md','utf8');const m=ev.match(/P1[\s\S]*/);if(!m)throw new Error('P1 is missing — this task cannot decide its own entry condition');const reproduced=/draft[^\n]*\b200\b/i.test(m[0]);const led=fs.readFileSync('.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md','utf8');if(reproduced&&!/T-04-CONFIRMIDOR/.test(led))throw new Error('the probe reproduced a 200 but no finding naming D-06 exists in deferred-items.md');console.log(reproduced?'reached — finding recorded':'not reached — P1 read 404 on the draft')"</automated>
    <human-check>If reached, a human selects one of the four options. This checkpoint is not auto-approvable: a 200 here is a live SEO defect on the product's highest-intent public route and is escalate-class.</human-check>
  </verify>
  <done>Either the plan SUMMARY records "Task 3 not reached — § P1 read 404 / 404 / 200", or a dated finding naming D-06's constraint exists in `deferred-items.md` and the PM has selected a repair shape.</done>
  <resume-signal>Select: `shape-a-listing-only`, `shape-a-all-segments`, `shape-b`, or `record-and-defer` — or reply "not reached" if § P1 read 404 / 404 / 200.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → Next server (`/listings/[id]`) | An unauthenticated request decides whether an unpublished listing's existence is disclosed and with what status line |
| operator shell → the `next start` process | Throwaway `PLATFORM_WALLET_*` values enter a production-mode process for the duration of one probe |
| local dev Postgres → the probe | Real seeded listing ids are read out of the DB and written into a planning document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01 | Information Disclosure | `/listings/{draft-id}` status line | mitigate | The probe measures the status line only (`curl -o /dev/null -w '%{http_code}'`); the body is never captured, so a draft listing's title cannot land in a planning document. `[17-D1]` already records that the body IS the not-found document and the draft title is absent from the served bytes. |
| T-17.1-02 | Tampering / Configuration (ASVS V14) | `src/lib/paymongo.ts:39-48` fail-closed boot guard | mitigate | Wallet values are passed INLINE on one process and are never written to `.env.local` or `.env.example`. Acceptance criteria assert `grep -c "PLATFORM_WALLET" .env.local` → 0 and a clean `git diff` on both env files. The guard itself is not edited. |
| T-17.1-03 | Tampering | later Playwright runs adopting a stale production server | mitigate | `-p 3100`, never 3000 (`playwright.config.ts:113` `reuseExistingServer: !CI` — the `[17-D24]` trap). The real `next start` PID is killed, not the npm wrapper, and port-3100 silence is an acceptance criterion. |
| T-17.1-04 | Information Disclosure (ASVS V4) | `(app)/bookings/[id]/not-found.tsx` — the owner gate's served document | accept (this plan) / mitigate (the checkpoint) | This plan hoists nothing. If Task 3 is reached, D-06's constraint is written into the finding before any repair is designed: T-04-CONFIRMIDOR deliberately serves the same page for "missing" and "not yours", and falling through to the root not-found changes that response. |
| T-17.1-05 | Repudiation | the ledger correction | mitigate | Corrections are APPENDED and dated; `[17-D1]`'s measured body is left byte-identical (asserted as zero deletions in the diff). A ledger that can be silently rewritten stops being evidence. |
| T-17.1-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** RESEARCH § Package Legitimacy Audit records zero packages for this item; `git diff --name-only -- package.json package-lock.json` printing nothing is an implicit acceptance condition of the phase gate. |
</threat_model>

<verification>
- `npm run build` exit 0 (= lint + `test:design` + `next build`)
- `npx tsc --noEmit` exit 0
- `npm test` **ALONE** exit 0
- `npx playwright test e2e/public-listing.spec.ts --project=chromium --workers=1` — 8 passed
- Every e2e failure, if any, already appears in `e2e-baseline-reds.md`'s 10 declared rows. **No row may
  be added to that file to make a run read green** (D-14).
- ⚠ `--project=visual` does not exist locally and that is by design (D-29). Do not run it.
</verification>

<success_criteria>
- `17.1-EVIDENCE.md` exists with `§ P1` carrying a written prediction and three measured status codes
  on three named routes, one of them the published control
- `[17-D1]` carries a dated correction proving C1 (with the re-run `git log` output naming `89fb451`)
  and C2 (naming all four `loading-coverage` pins), and quotes `§ P1`
- `[17-D2]` is folded into `[17-D1]`'s decision by a dated line
- Zero code files changed; zero packages; `.env.local` untouched; port 3100 free
- If the draft read 200: a finding in `deferred-items.md`'s format exists and Task 3's checkpoint was
  answered by the PM before any repair was designed
</success_criteria>

<output>
Create `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-01-SUMMARY.md` when done.
</output>
