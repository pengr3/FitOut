---
phase: 17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro
plan: 02
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - src/app/listings/[id]/(detail)/not-found.tsx
  - tests/design/empty-state-adoption.test.ts
  - e2e/axe-sweep.spec.ts
  - e2e/overflow-320.spec.ts
  - src/app/(app)/bookings/[id]/not-found.tsx
  - e2e/shell.spec.ts
  - .planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md
  - .planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md
autonomous: true
requirements: ["17-D3"]

must_haves:
  truths:
    - "D-12: `src/app/listings/[id]/(detail)/not-found.tsx` is gone, and the header that claims it renders goes with it"
    - "D-12: every instrument that named the file moves in the SAME COMMIT as the deletion — never after it — and `grep -rn \"detail)/not-found\" src tests e2e` returns 0 hits"
    - "M2 / refutes RESEARCH A5: all 10 hits across 5 files move, not the 7 across 4 the ledger table names — `e2e/shell.spec.ts:1046` and `e2e/overflow-320.spec.ts:639`/`:648` are unlisted there"
    - "RESEARCH C2: `tests/design/loading-coverage.test.ts`'s four pins do NOT move — that gate counts `page.tsx`/`loading.tsx` only, contrary to what `[17-D3]` claims"
    - "D-14: the free watched red is driven and its transcript recorded verbatim — four gates red in the intermediate state, two of them build-blocking — proving the coupling is enforced rather than remembered"
    - "D-13: the stale-comment corrections are taken as fix-in-place under 17-UI-SPEC § Remediation, and no product copy moves"
    - "The AC#29 skip paragraph's measurement survives its deletion: carried into the commit message and the ledger, not dropped silently"
  artifacts:
    - path: "tests/design/empty-state-adoption.test.ts"
      provides: "The build-blocking inventory, decremented for the first time in its history"
      contains: "EXPECTED_ADOPTER_FILES = 14"
    - path: "e2e/overflow-320.spec.ts"
      provides: "The D-201 inventory and the AC#29 table with the unreachable row gone, plus a dated [17.1:] bracket on the header note"
      contains: "[17.1:"
    - path: ".planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md"
      provides: "W1 — the watched-red transcript from the intermediate state"
      contains: "W1"
  key_links:
    - from: "tests/design/empty-state-adoption.test.ts"
      to: "the on-disk adopter set"
      via: "the disk walk that reds on a row naming a file it never found"
      pattern: "EXPECTED_ADOPTER_FILES = 14"
    - from: "e2e/overflow-320.spec.ts SURFACE_INVENTORY"
      to: "the AC#29 ROWS table"
      via: "coveredBy name resolution — both directions red if only one side moves"
      pattern: "inventory to tables"
---

<objective>
Delete a route boundary that cannot render in any state, and move every instrument that names it in
the same commit.

Purpose: `src/app/listings/[id]/(detail)/not-found.tsx` is dead code that reads like coverage.
`(detail)/layout.tsx` raises `notFound()` from a **layout**, so the parent segment's boundary wins;
`src/app/listings/[id]/` has none, so it falls through to the root — measured at **404**, with **1**
`empty-state`, and this file's own copy (*"This space isn't available"*) appearing **0** times. Its
header asserts the opposite, and has since `89fb451`.

**The deletion reds four instruments, two of them build-blocking.** That is not an obstacle; it is the
proof the coupling is enforced, and it is free because it is the intermediate state of the commit
anyway. Drive it, record it, then close it.

Output: one deletion, four instrument edits, three prose corrections, one watched-red transcript, one
RESOLVED line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-CONTEXT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md

<interfaces>
**The exhaustive reference set — 10 hits across 5 files.** Measured on this tree; RESEARCH's table
names only 7 across 4 (assumption A5 is false; see PATTERNS § M2). All ten move in one commit:

  src/app/(app)/bookings/[id]/not-found.tsx:35        prose  — the `main` vs `div` contrast
  tests/design/empty-state-adoption.test.ts:420       GATE   — build-blocking `ADOPTERS` row
  e2e/axe-sweep.spec.ts:402                           GATE   — `ROWS` entry, AC#2 disk-walked
  e2e/overflow-320.spec.ts:498                        prose  — the header note
  e2e/overflow-320.spec.ts:639                        prose  — inside the AC#29 skip paragraph
  e2e/overflow-320.spec.ts:648                        prose  — inside the AC#29 skip paragraph
  e2e/overflow-320.spec.ts:654                        GATE   — the AC#29 named-skip row's `name`
  e2e/overflow-320.spec.ts:3753                       GATE   — `SURFACE_INVENTORY` entry
  e2e/overflow-320.spec.ts:3754                       GATE   — that entry's `coveredBy`
  e2e/shell.spec.ts:1046                              prose  — NOT in RESEARCH's table (M2)

Historical planning docs under `.planning/phases/11-*/` also name the file. **Do not touch them** —
they are records of what was true when written.

**The two build-blocking constants — `tests/design/empty-state-adoption.test.ts:475-476`:**
  const EXPECTED_ADOPTER_FILES = 15;      // to 14
  const EXPECTED_EMPTY_STATE_SITES = 18;  // to 17

**The gate that reds if the ADOPTERS row stays** — `:912-919`, *"declared here but the walk never
found it"*.
**The gate that reds if the axe row stays** — `e2e/axe-sweep.spec.ts:899-918`, AC#2, disk-walked, so
the row surfaces as `extra: [...]`.
**The two gates that red from opposite directions** — `e2e/overflow-320.spec.ts:3961-3975`
(`inventory to disk`) and `:4005-4019` (`inventory to tables`).

**NOT a gate this deletion touches:** `tests/design/loading-coverage.test.ts`. Its collector takes
`page.tsx` (`:502`) and `loading.tsx` (`:515`) only — `not-found.tsx` is invisible to it. `[17-D3]`
claims otherwise and is wrong (RESEARCH C2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the file, drive the intermediate state, and record the reds verbatim</name>
  <files>src/app/listings/[id]/(detail)/not-found.tsx, .planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md</files>
  <read_first>
    - `src/app/listings/[id]/(detail)/not-found.tsx` — the whole file, header included. The header block *"MEASURED: THIS PANEL IS CLIENT-RENDERED..."* is the false sentence D-12 names; it also cites `(detail)/page.tsx:122` for a `notFound()` now at `:266`. Read it before deleting it — its copy is the string every instrument keys on
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-RESEARCH.md` § Item 4 → "Whether item 4 can support a watched-red drive" — the honest answer, and why the inventory machinery is what can be watched
    - `tests/design/empty-state-adoption.test.ts` `:912-919` — the assertion that reds, in its own words
    - `e2e/axe-sweep.spec.ts` `:899-918` — AC#2, and how the row surfaces as `extra: [...]`
    - `e2e/mobile-booker-path.spec.ts` `:313-379` — the four dated DRIVE blocks: **the transcript format**, each with its verbatim failure text and its counts
  </read_first>
  <action>
    **Do not commit anything in this task.** Task 2 lands the deletion and every instrument edit in ONE
    commit; this task exists to observe the state between them.

    `git rm` the file. Then drive the instruments and capture each failure **verbatim** — the error
    text as printed, not a paraphrase:

    1. `npm run test:design` — expect `tests/design/empty-state-adoption.test.ts` red. Two distinct
       failures are expected: the *"declared here but the walk never found it"* clause naming the
       deleted path, and the two count pins (an `expected 17 to be 18`-shaped message). Record both,
       and record that this gate runs inside `npm run build` and inside CI's `gate-db-free` — it is
       build-blocking.
    2. `npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1` — expect AC#2
       (*"the table's row set equals the declared surface set"*) to report
       `extra: ["src/app/listings/[id]/(detail)/not-found.tsx"]`. Record the object it prints.
    3. `npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1` — expect
       `"inventory to disk: no entry claims a surface that is gone"` to name the stale surface.
       Record it.
    4. Record that the fourth coupling (`"inventory to tables"`) is the mirror — it reds if the
       inventory entry goes and the AC#29 row stays. Do not manufacture it here; if you meet it in
       Task 2 while mid-edit, add its transcript to the same section.

    Write all of it into `17.1-EVIDENCE.md` as **`W1 — item 4's watched red (the intermediate state of
    one commit)`**, in `mobile-booker-path.spec.ts`'s DRIVE format: the command, the verbatim failure
    text, and the counts (`N failed, M passed`). State plainly what it proves — the coupling is
    ENFORCED, not remembered — and state the honest limit RESEARCH names: the deletion cannot be
    watched red from the product side, because an unreachable file's presence and absence render
    identically.

    Compare every OTHER failure in those runs against `e2e-baseline-reds.md`'s 10 declared rows. A
    failure not on that list and not caused by this deletion is this plan's to explain. **No row may be
    added to that file to make a run read green.**
  </action>
  <acceptance_criteria>
    - `17.1-EVIDENCE.md` contains a `W1` section with at least three verbatim failure transcripts, each
      preceded by the exact command that produced it
    - `W1` records `extra: ["src/app/listings/[id]/(detail)/not-found.tsx"]` as printed by axe-sweep AC#2
    - `W1` records both `empty-state-adoption.test.ts` failures — the declared-but-not-found clause AND
      the count pins — and states that this gate is build-blocking
    - `W1` states the honest limit: the deletion itself cannot be watched red from the product side
    - `git status --porcelain` shows the file deleted and NOTHING committed
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/e2e-baseline-reds.md`
      is byte-identical: `git diff --name-only -- '.planning/phases/17-*/e2e-baseline-reds.md'` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>npm run test:design || echo "EXPECTED RED in the intermediate state — a GREEN here means the coupling is not enforced and W1 has nothing to record; that itself is the finding"</automated>
  </verify>
  <done>`W1` carries three verbatim red transcripts from the intermediate state; nothing is committed; `e2e-baseline-reds.md` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Move all four instruments and all three prose references, in ONE commit with the deletion</name>
  <files>tests/design/empty-state-adoption.test.ts, e2e/axe-sweep.spec.ts, e2e/overflow-320.spec.ts, src/app/(app)/bookings/[id]/not-found.tsx, e2e/shell.spec.ts</files>
  <read_first>
    - `tests/design/empty-state-adoption.test.ts` `:405-494` — the `ADOPTERS` rows, the `--- NOT PLAN 11-16'S` section comment at `:413`, the sibling `src/app/not-found.tsx` row at `:414-418` that STAYS, the row to delete at `:419-423`, the constants' docblock at `:457-474` and the two constants at `:475-476`
    - `e2e/axe-sweep.spec.ts` `:391-413` — the `ROWS` entry to delete at `:401-409` and its two siblings, which are the shape the deletion must leave intact
    - `e2e/overflow-320.spec.ts` `:490-505` (the header note and its `[17-11:` / `[17-12:` bracket idiom), `:623-674` (the whole AC#29 named-skip row: its `ADDED BY PLAN 17-11` comment block, `name` at `:654`, `path: null` at `:655`, the multi-line `skip` paragraph, `tell` at `:673`), and `:3750-3757` (the `ROUTE STATES - not-found (4)` section comment and the `SURFACE_INVENTORY` entry at `:3752-3755`)
    - `src/app/(app)/bookings/[id]/not-found.tsx` `:28-43` — the prose contrast; and `tests/design/empty-state-adoption.test.ts:424-429`, which is why this file's rendered copy is a SECURITY property and must not move
    - `e2e/shell.spec.ts` `:1034-1052` — the doubly-stale comment: it names a file this plan deletes AND cites as authority the very header the deletion removes
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-PATTERNS.md` § 6a-6e (the exact excerpts) and § S4 "Deletion coupled to its inventory, in one commit" and § S5 "The dated bracketed amendment"
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md` § Remediation rules — the row that makes a stale-comment correction fix-in-place, and the must-escalate list that keeps product copy out of this commit
  </read_first>
  <action>
    Six edits, then ONE commit that also carries Task 1's deletion.

    **(a) `tests/design/empty-state-adoption.test.ts` — three changes.** Delete rows `:419-423` only.
    **Keep the `--- NOT PLAN 11-16'S...` section comment at `:413`** — its other row
    (`src/app/not-found.tsx`, `:414-418`) survives, so the comment still has a subject. Move
    `EXPECTED_ADOPTER_FILES` 15 to 14 and `EXPECTED_EMPTY_STATE_SITES` 18 to 17. Extend the constants'
    docblock (`:457-474`) with a fourth movement entry in the same shape as the three already there,
    and say the two things that make it different: this is the **first DECREMENT** in that list — every
    prior movement is a growth — and the surface was **deleted**, not "no longer found by the scan". A
    count that moved because a scan went blind is a different and much worse event, and that docblock
    is where the distinction is kept.

    **(b) `e2e/axe-sweep.spec.ts`** — delete the `ROWS` entry at `:401-409` entire, its comment included.

    **(c) `e2e/overflow-320.spec.ts` — three changes.** Delete the `SURFACE_INVENTORY` entry at
    `:3752-3755` and correct its section comment `ROUTE STATES - not-found (4)` to `(3)`. Delete the
    AC#29 named-skip row at `:623-674` entire. Add a `[17.1: ...]` bracket to the header note at
    `:497-500`, in that file's own dated-bracketed-amendment style: leave the `[17-11:` and `[17-12:`
    brackets byte-identical as the history they are, and append that the count of unreachable rows is
    now **zero**, because the surface is gone rather than because a reason changed.

    **(d) `src/app/(app)/bookings/[id]/not-found.tsx:33-36`** — re-point the contrast. The argument
    (one `main` landmark per document, `(app)/layout.tsx:96` already supplies it, D-88.1) is correct
    and stays; only the example changes. `src/app/not-found.tsx` is the surviving `main`-opening
    boundary. Touch the comment only — this file's rendered copy is a security property
    (T-04-CONFIRMIDOR) and moving it is must-escalate.

    **(e) `e2e/shell.spec.ts:1046`** — this comment is stale twice over: it names a deleted file AND
    cites as its authority the header this commit removes, false since `89fb451`. Say what is actually
    measured on `shell.spec.ts`'s own route and drop the borrowed authority. Prose only.

    **(f) The commit.** One commit, carrying the deletion and all six edits. The message must record
    three things the diff does not show:
      - the AC#29 skip paragraph's **measurement**, carried forward rather than deleted silently:
        `/listings/a-listing-that-must-never-exist-17-11` at 320px in both themes, status **404**, the
        document is the ROOT not-found (*"We couldn't find that page"*, one `empty-state`, the public
        header, the site footer), and this boundary's copy *"This space isn't available"* appears **0**
        times;
      - that the deleted `axe-sweep` row's `tell` was `[data-testid="empty-state"]`, which the **root**
        not-found also renders — so that row has been **green while auditing the wrong document**;
      - that `tests/design/loading-coverage.test.ts`'s pins do **not** move (RESEARCH C2), contrary to
        what `[17-D3]` claims — because a reader who expects them to move and finds them still will
        otherwise conclude the deletion did not land.
  </action>
  <acceptance_criteria>
    - `grep -rn "detail)/not-found" src tests e2e` returns **0** hits (was 10 across 5 files)
    - `ls "src/app/listings/[id]/(detail)/not-found.tsx"` fails — the file is gone
    - `grep -c "EXPECTED_ADOPTER_FILES = 14" tests/design/empty-state-adoption.test.ts` is 1 and
      `grep -c "EXPECTED_EMPTY_STATE_SITES = 17" tests/design/empty-state-adoption.test.ts` is 1
    - The constants' docblock gained a fourth movement entry that states plainly this is the first
      DECREMENT and that the surface was deleted rather than no longer found
    - `grep -c "\[17.1:" e2e/overflow-320.spec.ts` is 1, and both the `[17-11:` and `[17-12:` brackets
      appear unchanged in `git diff` (zero deletions inside them)
    - `grep -c "not-found (3)" e2e/overflow-320.spec.ts` is 1
    - `npm run test:design` exit 0 — the build-blocking gate is green again
    - `npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1` passes AC#2 with
      `{ missing: [], extra: [], duplicated: [] }`
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1` passes both
      `inventory to disk` and `inventory to tables`
    - `npx playwright test e2e/shell.spec.ts --project=chromium --workers=1` — any failure is one of
      `e2e-baseline-reds.md` rows 7, 8 or 9 (the contention class); nothing new
    - `git show --stat HEAD` shows the deletion and all six edits in **one** commit
    - `git diff --name-only HEAD~1 HEAD -- tests/design/loading-coverage.test.ts` prints nothing
    - `git diff --name-only HEAD~1 HEAD -- '.planning/phases/11-*'` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>npm run test:design && npx playwright test e2e/axe-sweep.spec.ts e2e/overflow-320.spec.ts --project=chromium --workers=1</automated>
  </verify>
  <done>Zero `detail)/not-found` references remain in `src`/`tests`/`e2e`; both build-blocking and both e2e inventory gates are green; the deletion and all six edits are one commit whose message carries the three facts the diff cannot show.</done>
</task>

<task type="auto">
  <name>Task 3: Close [17-D3] with a dated RESOLVED line naming the deletion and every instrument</name>
  <files>.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md</files>
  <read_first>
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md` `[17-D3]` (`:113-150`) — the row being closed, in full, including its false claim about `loading-coverage.test.ts`'s pins
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md` `[17-D9]`'s RESOLVED block (`:403-412`) — **the format**, including its closing paragraph that names the two pointers the row got wrong and corrects them rather than propagating them
    - `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md` `W1` — the transcript this line points at
  </read_first>
  <action>
    Append a `**RESOLVED 2026-08-30 — plan 17.1-02, commit <sha>.**` block to `[17-D3]`, leaving its
    measured body byte-identical. It carries:

      - the deletion, by path;
      - the instrument edits, enumerated: the `ADOPTERS` row plus both constants (15 to 14, 18 to 17),
        the `axe-sweep` `ROWS` entry, the `overflow-320` `SURFACE_INVENTORY` entry and its AC#29
        named-skip row, and the prose corrections — **naming that there were three, not two**: this
        row's own table lists 7 references across 4 files and the tree carries **10 across 5**
        (`e2e/shell.spec.ts:1046` and `e2e/overflow-320.spec.ts:639`/`:648` are unlisted). Correct the
        count rather than propagating it, exactly as `[17-D9]`'s RESOLVED line corrected two wrong
        pointers;
      - the correction that this row's *"moves `tests/design/loading-coverage.test.ts`'s pins"* claim is
        **false** (RESEARCH C2) — that gate counts `page.tsx`/`loading.tsx` only;
      - the finding uncovered while deleting: the `axe-sweep` row's `tell` was
        `[data-testid="empty-state"]`, which the ROOT not-found also renders, so the row was green
        while auditing the wrong document — an instance of the phase's own key insight, *"the failure
        mode in this codebase is never 'no gate', it is 'a gate that reads the wrong thing and is
        green'"*;
      - a pointer to `17.1-EVIDENCE.md` `W1` for the watched-red transcript, and the honest limit that
        the deletion itself cannot be watched red from the product side.

    Do not touch any other row. `[17-D19]` in particular stays parked, even though this plan opened
    `overflow-320.spec.ts` (RESEARCH § Deferred Ideas: 42 newly-asserting cases, explicitly not ours).
  </action>
  <acceptance_criteria>
    - `grep -c "RESOLVED 2026-08-30 — plan 17.1-02" deferred-items.md` is 1
    - The RESOLVED block names all four instruments AND states `10 across 5` (or equivalently corrects
      the row's 7-across-4 count)
    - The RESOLVED block states that `loading-coverage.test.ts`'s pins do not move
    - The RESOLVED block records the `tell` finding
    - `git diff` on `deferred-items.md` shows only ADDED lines inside the `[17-D3]` section — zero
      deletions, zero modifications
    - The commit SHA cited in the block resolves: `git cat-file -t <sha>` prints `commit`
    - `git diff --name-only -- e2e/overflow-320.spec.ts` prints nothing in this task (no `[17-D19]` work)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/deferred-items.md','utf8');for(const t of ['RESOLVED 2026-08-30 — plan 17.1-02','loading-coverage','10 across 5']){if(!s.includes(t))throw new Error('missing from the RESOLVED block: '+t)}console.log('ok')"</automated>
  </verify>
  <done>`[17-D3]` carries a dated RESOLVED block naming the deletion, all four instruments, the corrected 10-across-5 count, the C2 correction and the `tell` finding; its measured body is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → App Router segment tree | Which not-found boundary wins a 404 decides which document an unauthenticated visitor is served |
| session holder → `(app)/bookings/[id]` owner gate | The served not-found document is the gate's answer for BOTH "missing" and "not yours" |
| the disk → four inventory gates | Each gate walks disk and reds on drift; a gate that stops walking stops protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-06 | Information Disclosure (ASVS V4) | `src/app/(app)/bookings/[id]/not-found.tsx` | mitigate | Only the **comment** at `:33-36` is edited. The rendered copy is what the owner gate returns for both a missing booking and one belonging to somebody else (T-04-CONFIRMIDOR), so it is a security property and moving it is must-escalate. Acceptance requires the diff on this file to be comment-only. |
| T-17.1-07 | Tampering | `tests/design/empty-state-adoption.test.ts` count pins | mitigate | The pins move by exactly 1 each, in the same commit as the row, with a docblock entry stating the surface was **deleted** rather than "no longer found". A pin moved without that distinction is indistinguishable from a scan that went blind — which is the same class as `[17-D20]` and as the `axe-sweep` `tell` finding this plan records. |
| T-17.1-08 | Repudiation | the AC#29 skip paragraph's measurement | mitigate | The measurement (404, root not-found, 1 `empty-state`, this file's copy 0 times) is carried into the commit message and the `[17-D3]` RESOLVED block before the paragraph is deleted. Evidence is moved, never dropped. |
| T-17.1-09 | Denial of Service (of the gate) | the four disk-walked inventories | mitigate | The watched red in Task 1 proves each gate actually reds in the intermediate state. A coupling that is remembered rather than enforced is the failure this repo has already met four times; the transcript is the proof it is enforced here. |
| T-17.1-10 | Spoofing (of coverage) | the deleted `axe-sweep` row | mitigate | The row's `tell` (`[data-testid="empty-state"]`) matches the ROOT not-found too, so the row was green while auditing a different document. Deleting it removes a false coverage claim; the commit message and the ledger both say so, so nobody later reads the row's absence as lost coverage. |
| T-17.1-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** `git diff --name-only HEAD~1 HEAD -- package.json package-lock.json` printing nothing is an acceptance condition of the phase gate. RESEARCH § Package Legitimacy Audit records zero packages for this item. |
</threat_model>

<verification>
- `npm run build` exit 0 (= lint + `test:design` + `next build`)
- `npx tsc --noEmit` exit 0 (clear a stale `.next/` first — `.next/dev/types` is the recorded cause of a class of false reds)
- `npm test` **ALONE** exit 0
- Per file, `--project=chromium --workers=1`: `e2e/axe-sweep.spec.ts`, `e2e/overflow-320.spec.ts`, `e2e/shell.spec.ts`
- Every e2e failure already appears in `e2e-baseline-reds.md`'s 10 declared rows. **No row may be added
  to that file to make a run read green** (D-14).
- `--project=visual` does not exist locally and that is by design (D-29). Do not run it.
</verification>

<success_criteria>
- `grep -rn "detail)/not-found" src tests e2e` returns 0 hits
- The deletion and all six instrument/prose edits are in ONE commit
- `tests/design/empty-state-adoption.test.ts` reads 14 / 17 with a fourth docblock movement entry
- `e2e/overflow-320.spec.ts` carries a `[17.1:` bracket, a `not-found (3)` section comment, and no
  AC#29 named-skip row
- `17.1-EVIDENCE.md` `W1` carries the verbatim intermediate-state reds
- `[17-D3]` carries a dated RESOLVED block with the corrected 10-across-5 count and the C2 correction
- `tests/design/loading-coverage.test.ts` is untouched; `.planning/phases/11-*` is untouched
</success_criteria>

<output>
Create `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-02-SUMMARY.md` when done.
</output>
