---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 19
type: execute
wave: 9
depends_on: [11-09, 11-10]
files_modified:
  - src/lib/group/rsvp.ts
  - src/app/(public)/invite/[token]/page.tsx
  - src/app/not-found.tsx
  - src/app/listings/[id]/(detail)/not-found.tsx
  - src/app/(public)/invite/[token]/not-found.tsx
  - tests/design/invite-notfound-parity.test.ts
autonomous: true
requirements: [STATE-02]

must_haves:
  truths:
    - "Three not-found routes exist where zero exist today: the root, a missing listing, and an inactive invite"
    - "The invite 404 is INDISTINGUISHABLE from the invite page's inactive state — 08-06 deliberately folded malformed and unknown tokens onto one identical response so the page could not become a probe oracle, and a distinguishable not-found reintroduces exactly that oracle"
    - "INACTIVE_TITLE and INACTIVE_BODY are IMPORTED, not retyped — they are module-private constants today, so the requirement needs an explicit hoist"
    - "An unlisted space is a normal state, never '404' and never 'error'"
    - "The root not-found reaches no database — /_not-found is prerendered, and a shell component that reaches @/lib/db there collapses CI job 1's DB-free property"
  artifacts:
    - path: "src/lib/group/rsvp.ts"
      provides: "INACTIVE_TITLE and INACTIVE_BODY hoisted beside GROUP_INACTIVE — the analog-consistent home the invite page's own comment points at"
      contains: "INACTIVE_TITLE"
    - path: "src/app/(public)/invite/[token]/not-found.tsx"
      provides: "The invite 404 rendering the imported constants verbatim"
    - path: "tests/design/invite-notfound-parity.test.ts"
      provides: "AC#22 — the two surfaces render identical text, asserted by import equality rather than string comparison"
  key_links:
    - from: "src/app/(public)/invite/[token]/not-found.tsx"
      to: "src/lib/group/rsvp.ts"
      via: "imported INACTIVE_TITLE / INACTIVE_BODY — two literals in two files are two things that drift, and the moment they drift the difference is the oracle"
      pattern: "INACTIVE_TITLE"
---

<objective>
Add the three not-found routes, and close the invite-token probe oracle by construction rather than by
matching two strings.

Purpose: 08-06 folded "malformed token" and "unknown token" onto one identical response precisely so the
invite page could not become a probe oracle. A *distinguishable* not-found page reintroduces it — an
attacker learns which token shapes exist by which page they land on.
Output: three `not-found.tsx`, one constant hoist, and the parity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
⚠ NO ANALOG. Measured: zero not-found.tsx files exist in this repository.

⚠ CORRECTION TO THE UI-SPEC (11-PATTERNS § H(2), measured): INACTIVE_TITLE and INACTIVE_BODY at
src/app/invite/[token]/page.tsx:71-72 — now src/app/(public)/invite/[token]/page.tsx after plan 11-10 —
are **module-private consts and are NOT exported today**. "Imported, not retyped" therefore needs an
explicit hoist. The analog-consistent home is beside GROUP_INACTIVE in src/lib/group/rsvp.ts, which is
where the same-shaped constant already lives and which the page's own comment (lines 68-70) points at:

  "mirroring how 08-06 froze GROUP_INACTIVE into a single object: two literals in two branches are two
   things that can drift apart, and the moment they do, the difference between them is the oracle."

Current values (do not change them):
  const INACTIVE_TITLE = "This invite is no longer active";
  const INACTIVE_BODY  = "Ask the organizer for the latest link.";

Copy (11-UI-SPEC § Not-found):
  app/not-found.tsx                      "We couldn't find that page" /
                                         "The link may be old, or the page may have moved." + "Back to search" -> /
  listings/[id]/not-found.tsx            "This space isn't available" /
                                         "It may have been unlisted, or the link may be out of date." +
                                         "Find another space" -> /
                                         NEVER "404", NEVER "error" — an unlisted space is a normal state
  invite/[token]/not-found.tsx           INACTIVE_TITLE / INACTIVE_BODY, imported verbatim

bookings/[id]/not-found.tsx (the owner-gated calm copy) is Phase 13, NOT here.

⚠ BUILD HAZARD (11-RESEARCH caveat 2): the root not-found.tsx must reach NO database. /_not-found is
prerendered ○ today; if it renders a shell component that reaches @/lib/db on a path Next can prerender,
the build will try to connect. Falsifiable by re-running the unreachable-DATABASE_URL build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist the invite inactive constants to their analog-consistent home</name>
  <files>src/lib/group/rsvp.ts, src/app/(public)/invite/[token]/page.tsx</files>
  <read_first>
    - src/app/(public)/invite/[token]/page.tsx:58-72 (the static `metadata` export with its non-negotiable `robots`/`referrer`, the comment at :68-70 explaining the drift-is-the-oracle reasoning, and the two module-private consts at :71-72)
    - src/lib/group/rsvp.ts (find `GROUP_INACTIVE` — the same-shaped frozen constant 08-06 introduced, and the shape the hoisted pair should match)
    - src/app/actions/*rsvp* or wherever `submitRsvp` lives (it returns the same sentence for both malformed and unknown tokens — confirm which constant it reads, so the hoist does not create a third copy)
    - tests/group/** (any test asserting the inactive copy — they must keep passing, and they should read the constant after this change, not a literal)
  </read_first>
  <action>
Move `INACTIVE_TITLE` and `INACTIVE_BODY` out of `src/app/(public)/invite/[token]/page.tsx` and into
`src/lib/group/rsvp.ts`, exported, beside `GROUP_INACTIVE`. **Values byte-unchanged.**

Choose the hoist over adding named exports to a `page.tsx`: the analog already lives in `rsvp.ts`, the
page's own comment points there, and an extra named export on a route module is a shape Next's segment-export
validation has opinions about. Record the reasoning in `rsvp.ts`'s header, and leave a one-line pointer in
the page where the consts used to be so a reader following the old line numbers lands somewhere useful.

Update the page to import them. Check whether `submitRsvp` (or whatever returns the identical sentence for
malformed and unknown tokens) already reads its own literal — if so, point it at the hoisted constant in the
same edit. **Three copies would be worse than two**, and the whole property is that there is exactly one.

Do not touch the page's `metadata` export. `robots: { index: false, follow: false }` and
`referrer: "no-referrer"` are non-negotiable and plan `11-20` owns their conversion to `generateMetadata`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/group</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/group/rsvp.ts` exports `INACTIVE_TITLE` and `INACTIVE_BODY` with byte-identical values
    - `grep -c "This invite is no longer active\|Ask the organizer for the latest link" src/ -r` returns exactly 1 for each string across the whole tree (the declaration), and every other use is an import
    - `src/app/(public)/invite/[token]/page.tsx` imports them and still exports `metadata` with `robots: { index: false, follow: false }` and `referrer: "no-referrer"`
    - `npx vitest run tests/group` passes with the phase-baseline counts
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The two literals that could drift are now one constant with one owner.</done>
</task>

<task type="auto">
  <name>Task 2: The three not-found routes</name>
  <files>src/app/not-found.tsx, src/app/listings/[id]/(detail)/not-found.tsx, src/app/(public)/invite/[token]/not-found.tsx</files>
  <read_first>
    - src/lib/group/rsvp.ts (Task 1's output)
    - src/components/patterns/empty-state.tsx and src/components/patterns/site-chrome.tsx and site-footer.tsx (the root not-found sits ABOVE every group layout, so it must compose the chrome and footer explicitly — plan 11-10 put them in group layouts, not the root layout)
    - src/app/(public)/layout.tsx (how the public composition is assembled, so the root not-found matches it)
    - src/app/listings/[id]/(detail)/page.tsx (where `notFound()` is called, so the segment boundary is the right one)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Not-found and § Copywriting Contract
  </read_first>
  <action>
Create the three files. Each is a Server Component using `EmptyState` (or the same visual language) with the
copy in `<interfaces>` and a single action.

**`src/app/not-found.tsx`** — it renders inside the ROOT layout, which plan `11-10` deliberately left free
of chrome, so this file composes `SiteChrome` (public composition, anonymous) and `SiteFooter` **explicitly**
so it still "carries the shell". Say that in a comment: it is not an inconsistency, it is a consequence of
the root layout not owning the chrome.

**It must reach no database.** Import nothing from `@/lib/db` and nothing that transitively does — including
the auth-slot's session read, which would make a prerenderable route dynamic and could dial out. Use the
**anonymous** header composition here, unconditionally, and record why: `/_not-found` is prerendered, and a
database reach on that path collapses CI job 1's DB-free property. Verify with the unreachable-`DATABASE_URL`
build.

**`src/app/listings/[id]/(detail)/not-found.tsx`** — the segment boundary for `notFound()` calls from the
listing page. Copy from the contract. **Never "404", never "error"** — an unlisted space is a normal state,
and occupancy/unavailability are never errors and never red.

**`src/app/(public)/invite/[token]/not-found.tsx`** — renders `INACTIVE_TITLE` and `INACTIVE_BODY`
**imported from `@/lib/group/rsvp`**, never retyped. Its action, its layout and its tone must match the
invite page's inactive branch closely enough that a visitor cannot tell which one they landed on. Put the
oracle reasoning in the file, in one sentence, citing 08-06.

Do **not** create `bookings/[id]/not-found.tsx` — that is Phase 13's, with its own owner-gated calm copy.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Three `not-found.tsx` files exist at the paths in `<files>`; `find src/app -name not-found.tsx | wc -l` returns 3
    - `grep -c "404\|error" src/app/listings/\[id\]/\(detail\)/not-found.tsx` returns 0 (case-insensitive, comments excluded)
    - `grep -c "@/lib/db" src/app/not-found.tsx` returns 0, and the file's imports do not transitively reach it — verified by `npm run build` with `DATABASE_URL=postgres://unreachable:unreachable@127.0.0.1:59999/nope` exiting 0
    - The build's route table still lists `/_not-found`; record whether it is `○` or `ƒ` and note the reason
    - The root not-found renders exactly one `[data-testid="site-header"]` and one `[data-testid="site-footer"]`
    - `src/app/(public)/invite/[token]/not-found.tsx` imports both constants and contains neither literal
    - Manual check: request `/nope`, `/listings/does-not-exist` and `/invite/does-not-exist` against `npm run dev` and record the three rendered pages in the SUMMARY
  </acceptance_criteria>
  <done>Three not-found surfaces exist, the root one carries the shell without touching the database, and the invite one reads its copy from the single source.</done>
</task>

<task type="auto">
  <name>Task 3: The invite-oracle parity gate (AC#22)</name>
  <files>tests/design/invite-notfound-parity.test.ts</files>
  <read_first>
    - tests/design/status-vocab.test.ts (the "pin values by import" idiom — the closest analog: it asserts on imported constants rather than on source text)
    - tests/use-server-exports.test.ts:104-151, :315-318, :320-369, :89-102
    - src/lib/group/rsvp.ts, src/app/(public)/invite/[token]/page.tsx, src/app/(public)/invite/[token]/not-found.tsx (Tasks 1 and 2)
    - vitest.design.config.ts (DB-free home — `rsvp.ts` must be importable without a database; if it is not, assert on the AST instead and say why in the header)
  </read_first>
  <action>
Create `tests/design/invite-notfound-parity.test.ts`.

**Assert by import, not by string comparison.** The property is "one constant, two consumers", so the gate's
job is to prove there is exactly one literal in the tree and that both surfaces reach it:

1. `INACTIVE_TITLE` and `INACTIVE_BODY` are exported from `@/lib/group/rsvp` and are non-empty strings.
   (If importing `rsvp.ts` pulls in a database handle and breaks the DB-free config, fall back to an AST
   read of the declaration and **state that fallback and its reason in the header** — do not add
   `setupFiles` to `vitest.design.config.ts` to work around it. That config's header is a hard constraint.)
2. AST-scan `src/app/(public)/invite/[token]/page.tsx` and `.../not-found.tsx`: both **import** the two
   identifiers from `@/lib/group/rsvp`, and **neither contains either literal** as a string.
3. Tree-wide: each of the two sentences appears as a string literal exactly **once** in `src/`, in
   `rsvp.ts`. A third copy anywhere is the drift that becomes the oracle.

Guard-the-guard: assert both invite files were found and parsed, and that the tree scan visited ≥ 100 files
— a "exactly once" assertion over a scanner that read nothing passes perfectly.

Both-directions self-tests on synthetic sources: a fixture containing the literal inline is flagged; one
importing the identifier is not; one mentioning the sentence in a `//` comment is not.

Header block: state the security property in the phase's own words — *08-06 deliberately folded "malformed
token" and "unknown token" onto one identical response so the page could not become a probe oracle; a
distinguishable not-found page reintroduces exactly that oracle, because an attacker learns which token
shapes exist by which page they land on.* Then state why this gate asserts on imports rather than on
rendered text: two literals that happen to match today are two things that drift tomorrow.

NOT COVERED footer: this gate proves the copy has one source. It does not prove the two surfaces are
visually indistinguishable — that is a human check, and it is recorded as such.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/invite-notfound-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The gate passes against Tasks 1 and 2
    - Watched red: retype `INACTIVE_TITLE`'s sentence as a literal in `not-found.tsx` → assertions 2 and 3 both fail naming the file. Reverted. Both runs recorded in the header.
    - Watched red: change the constant's value in `rsvp.ts` only → the gate still passes (correctly — one source, one value), and the SUMMARY records that this is the *intended* behaviour and why a two-literal comparison would have given a false sense of safety
    - Watched red (vacuity): shrink the tree scan to an empty directory → guard-the-guard fails
    - `vitest.design.config.ts` still has no `globalSetup` and no `setupFiles`
    - `npm run test:design` passes with the file count raised by exactly 1; push and CI green (D-25)
  </acceptance_criteria>
  <done>The oracle is closed at the source-of-truth level, and the gate proves there is exactly one source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invite token in a URL → response shape | A response that varies by token shape tells an attacker which tokens exist |
| prerendered `/_not-found` → database | A prerenderable route that reaches the database collapses the DB-free build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-ORACLE | Information Disclosure | `invite/[token]` not-found vs inactive branch | mitigate | Both surfaces render one hoisted constant; the gate asserts each sentence appears as a literal exactly once in `src/` and that both files reach it by import — closing the drift, not matching two strings |
| T-11-REFERER | Information Disclosure | `invite/[token]` metadata | mitigate | `robots: { index: false, follow: false }` and `referrer: "no-referrer"` are asserted unchanged; the `generateMetadata` conversion is deferred to plan 11-20 so this plan cannot disturb them |
| T-11-NFDB | Tampering | `src/app/not-found.tsx` | mitigate | Zero `@/lib/db` reach, anonymous header composition, and a re-run of the unreachable-`DATABASE_URL` build as an acceptance criterion |
| T-11-FALSEALARM | Repudiation | the listing not-found | mitigate | Never "404", never "error" — an unlisted space is a normal state; asserted by a zero-count over the file |
| T-11-SC | Tampering | npm | mitigate | Zero packages |
</threat_model>

<verification>
- `find src/app -name not-found.tsx | wc -l` returns 3
- `npm run build` exits 0 with an unreachable `DATABASE_URL`
- `npm test` and `npm run test:design` green
- `/nope`, `/listings/does-not-exist` and `/invite/does-not-exist` all render designed pages
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- Three not-found surfaces exist where zero existed
- The invite 404 and the invite inactive state read one constant, and that is machine-checked
- The root not-found carries the shell and reaches no database
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-19-SUMMARY.md` when done
</output>
