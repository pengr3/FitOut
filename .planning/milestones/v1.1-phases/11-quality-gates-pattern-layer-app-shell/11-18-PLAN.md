---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 18
type: execute
wave: 9
depends_on: [11-09, 11-15]
files_modified:
  - src/app/error.tsx
  - src/app/(app)/error.tsx
  - src/app/(host)/host/error.tsx
  - src/app/(auth)/error.tsx
  - src/app/(legal)/error.tsx
  - src/app/global-error.tsx
  - e2e/error-leak.spec.ts
  - tests/design/error-boundaries.test.ts
  - tests/design/global-error.test.ts
autonomous: true
requirements: [STATE-02]

must_haves:
  truths:
    - "Every route group has an error boundary offering BOTH a retry and a route out — a bare 'Something went wrong' with one button is a failure of the contract (STATE-02); zero boundaries exist today"
    - "Neither error.message nor error.stack reaches the DOM — only digest. Server error text routinely carries table names, file paths and connection strings, and this is the phase's largest new security surface"
    - "A boundary thrown with new Error('SENTINEL_LEAK_PROBE') produces a DOM containing that string ZERO times"
    - "global-error.tsx renders its own document and receives no global styles, so it is court unconditionally, painted from THEME_TOKENS hex with inline styles and zero Tailwind classes"
    - "The boundaries are written against `reset` — next@16.2.7's ErrorInfo is { error, reset, unstable_retry } and `retry` does not exist"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "The root boundary covering /, /listings/**, /invite/**"
    - path: "src/app/global-error.tsx"
      provides: "The one surface a theme cannot reach — inline styles from THEME_TOKENS.court"
      contains: "THEME_TOKENS"
    - path: "e2e/error-leak.spec.ts"
      provides: "The SENTINEL_LEAK_PROBE assertion, rendered in a real browser"
      contains: "SENTINEL_LEAK_PROBE"
  key_links:
    - from: "src/app/global-error.tsx"
      to: "src/lib/design/tokens.generated.ts"
      via: "THEME_TOKENS.court hex read as inline style values"
      pattern: "THEME_TOKENS"
    - from: "src/app/error.tsx"
      to: "src/components/patterns/error-state.tsx"
      via: "the two-action pattern, receiving digest and never the error object"
      pattern: "ErrorState"
---

<objective>
Give every route group an error boundary with two real actions, add the global error page, and prove no
server error text reaches the browser.

Purpose: zero error boundaries exist in this repository. Five new ones are the phase's largest new security
surface — V7 Error Handling, ASVS L1 — because server error text routinely carries table names, file paths
and connection strings.
Output: five boundaries, `global-error.tsx`, a browser-level leak probe, and two design gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
⚠ NO ANALOG EXISTS. Measured: this repository contains zero error.tsx, zero global-error.tsx and zero
not-found.tsx. Do not invent a lookalike — anchor to the UI-SPEC's props contract and the installed types.

Verified in node_modules/next/dist/client/components/error-boundary.d.ts (next@16.2.7):
  export type ErrorInfo = { error: Error; reset: () => void; unstable_retry: () => void; };
`retry` does NOT exist. Write against `reset` and leave a `// TODO(next@16.3): retry()` marker.
`error` is typed as plain Error; digest arrives as (error as Error & { digest?: string }).digest.

Placement (11-RESEARCH § STATE-02), post plan-11-10 route groups:
  src/app/error.tsx              root — covers /, /listings/**, /invite/**
  src/app/(app)/error.tsx        group
  src/app/(host)/host/error.tsx  group — on host/, matching the existing layout's location, not (host)/
  src/app/(auth)/error.tsx       group
  src/app/(legal)/error.tsx      group — static routes, so a boundary that should never fire; it must still
                                 exist and still be reachable in a test
  src/app/global-error.tsx       root only — renders its own <html>/<body>, receives NO global styles

Route out, per group:
  root -> "Back to search" /   ·  (app) -> "Your bookings" /bookings  ·  (host) -> "Host dashboard" /host
  (auth) -> "Back to log in" /login  ·  (legal) -> "Back to FitOut" /

Copy (11-UI-SPEC § Copywriting Contract):
  error boundary  "Something didn't load" / "We hit a problem loading this page. Trying again usually
                  fixes it." + "Try again" + the group's route out
  global error    "FitOut hit an unexpected error" / "The page couldn't load. Reloading usually fixes it —
                  if it doesn't, come back in a few minutes." + "Reload" + "Back to FitOut"

global-error constraints: no Tailwind (classes do not exist there) — inline style objects only; colours from
THEME_TOKENS.court hex, the sanctioned duplicate DS-12/D-18 exists for; a system font stack literal (Geist's
CSS variable is not defined there, and a font stack is not a DS-13 pattern); a plain <a href="/">, not
next/link; no header, no footer. The DS-13 leak gate already scans src/app/**, so a raw hex there fails the
build TODAY — the only remaining assertion is that the file IMPORTS THEME_TOKENS.

⚠ BUILD HAZARD (11-RESEARCH caveat 2): global-error.tsx must reach NO database. /_not-found is prerendered
today; a style-less document that reaches @/lib/db on a prerenderable path collapses job 1's DB-free
property. Falsifiable by re-running the unreachable-DATABASE_URL build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The five route-group boundaries</name>
  <files>src/app/error.tsx, src/app/(app)/error.tsx, src/app/(host)/host/error.tsx, src/app/(auth)/error.tsx, src/app/(legal)/error.tsx</files>
  <read_first>
    - src/components/patterns/error-state.tsx (plan 11-09's output — it takes `digest`, `onRetry` and a required `routeOut`, and it deliberately has NO `error` prop)
    - src/components/search/search-results.tsx:169-181 (the only in-repo error shape — note it has ONE action; the contract requires TWO, so this is a shape to extend, not copy)
    - node_modules/next/dist/client/components/error-boundary.d.ts (the `ErrorInfo` type — confirm `reset` and the absence of `retry` before writing)
    - src/app/(app)/layout.tsx and src/app/(host)/host/layout.tsx (plan 11-12's outputs — a group `error.tsx` renders INSIDE its layout, so the shell and its session gate still apply; confirm that before assuming the boundary is unauthenticated)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Error
  </read_first>
  <action>
Create the five boundaries. Each is `"use client"` (Next requires it) and each renders `ErrorState` with:
the copy contract's title and body, `onRetry={reset}`, and the group's `routeOut` as a
`Button variant="outline" asChild` link.

**Extract `digest` and pass only that.** `const digest = (error as Error & { digest?: string }).digest;`
`ErrorState` has no `error` prop, so the leak is prevented by the type rather than by remembering a
convention. Do **not** pass `error`, `error.message` or `error.stack` anywhere, and do **not** log
`error.message` into a client-visible place — `console.error(error)` in the boundary is acceptable and
useful, because the browser console is not the DOM, but say so in a comment so the distinction is deliberate.

Leave a `// TODO(next@16.3): retry()` marker beside `reset`, noting that `unstable_retry` exists in 16.2.7
but the copy contract binds "Try again" to `reset()`.

`(legal)/error.tsx` is a boundary that should never fire — the routes are static. It must still exist and
must still be **reachable in a test**: give it the same structure as the others so plan `11-22`'s baseline
can render it and Task 3's gate can assert it.

Each boundary's route out is the group's own, from `<interfaces>`. A bare "Something went wrong" with one
button is a failure of the contract: retry covers a transient fault, the route out covers a persistent one,
and without it the user's only recourse is the browser's back button into the same error.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npx vitest run --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Five `error.tsx` files exist at the paths in `<interfaces>`; `find src/app -name error.tsx | wc -l` returns 5
    - Every file starts with `"use client"` and renders exactly two actions
    - `grep -rc "error.message\|error.stack" src/app/*/error.tsx src/app/error.tsx src/app/**/error.tsx` returns 0 across all five
    - No file passes `error` to `ErrorState`; only `digest` crosses
    - Each file writes against `reset` and none references `retry` as a prop; each carries the `// TODO(next@16.3)` marker
    - Each group's route out matches the table in `<interfaces>` exactly
    - `npm run build` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Five boundaries exist, each with two actions, and none of them can be handed a message to render.</done>
</task>

<task type="auto">
  <name>Task 2: global-error.tsx — the one surface a theme cannot reach</name>
  <files>src/app/global-error.tsx, tests/design/global-error.test.ts</files>
  <read_first>
    - src/lib/design/tokens.generated.ts:1-27 (its header names THIS file's use case verbatim: "a document-level error page that renders before the stylesheet … They need a literal — and a literal typed by hand drifts." Read `THEME_TOKENS.court["--brand"].hex` and friends; never write a hex.)
    - tests/design/leak.test.ts:335-336 (proof the scanner reaches the whole `src/app` tree, so a raw hex here already fails the build today)
    - tests/design/scaffold-residue.test.ts (the raw-source structural assertion idiom `global-error.test.ts` copies)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § global-error.tsx (the full constraint table)
  </read_first>
  <action>
Create `src/app/global-error.tsx`, `"use client"`, rendering its own `<html>` and `<body>`.

**No Tailwind classes** — they do not exist in this document. Inline `style` objects only. Every colour is
read from `THEME_TOKENS.court` hex: this is the sanctioned duplicate DS-12 / D-18 exists for, and it is
*why* that module exists.

**court, unconditionally.** Not an omission — a documented impossibility, because an app-level `data-theme`
attribute never reaches a document this page renders itself. Record it as data in a comment, not as silence,
and name the consequence for plan `11-22`: `global-error` is the single baselined surface **excluded from
the theme-swap smoke**, and that exclusion is carried as data with its reason.

Font: a system stack literal. Geist's CSS variable is not defined here, and a font stack is not a DS-13
pattern (the gate bans hex / `rgb(` / `oklch(` / `text-[NNpx]`, and a font stack is none of those).

Links: a plain `<a href="/">`, not `next/link`. No header, no footer.

Copy from the contract: **"FitOut hit an unexpected error"** / *"The page couldn't load. Reloading usually
fixes it — if it doesn't, come back in a few minutes."* + **"Reload"** + **"Back to FitOut"**.

Render `digest` only, on the same terms as Task 1.

**The file must reach no database.** Import nothing from `@/lib/db` and nothing that transitively does —
`/_not-found` is prerendered today, and a style-less document that reaches the database on a prerenderable
path collapses CI job 1's DB-free property.

Create `tests/design/global-error.test.ts` asserting (AC#21): the file imports `THEME_TOKENS`; it contains
**zero** `className` attributes; it imports nothing from `@/lib/db`; and it contains no `next/link` import.
Note in the header that the raw-hex half is **already paid for** by `tests/design/leak.test.ts`, which scans
`src/app/**` — two lines of test, one of them free. Add guard-the-guard (the file was found and parsed) and
a both-directions self-test (a synthetic source with a `className` is flagged; one with only inline `style`
is not).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/global-error.test.ts tests/design/leak.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/global-error.tsx` exists, is `"use client"`, and renders `<html>` and `<body>`
    - `grep -c "className" src/app/global-error.tsx` returns 0
    - The file imports `THEME_TOKENS` from `@/lib/design/tokens.generated` and contains no raw hex (proved by `leak.test.ts` passing)
    - `grep -c "@/lib/db\|next/link" src/app/global-error.tsx` returns 0
    - Watched red: add `className="text-red-500"` → both `global-error.test.ts` and `leak.test.ts` fail. Reverted, both runs recorded.
    - `npm run build` exits 0 with `DATABASE_URL=postgres://unreachable:unreachable@127.0.0.1:59999/nope`
    - The court-only decision and the theme-swap exclusion are recorded as comments naming plan `11-22`
  </acceptance_criteria>
  <done>The style-less document renders in FitOut's colours from the one module that can supply them, and reaches nothing.</done>
</task>

<task type="auto">
  <name>Task 3: The SENTINEL leak probe and the boundary-structure gate</name>
  <files>e2e/error-leak.spec.ts, tests/design/error-boundaries.test.ts</files>
  <read_first>
    - e2e/scroll-area-overflow.spec.ts:26-30 (why `/dev/theme` is the DB-free driver page for gates like this) and :215-223 (`expectReachable` — assert the page renders the thing under test before asserting anything about it)
    - src/app/dev/theme/page.tsx (the dev route: no DB, no network, no seed, 404s in production — the natural home for a deliberate throw)
    - src/app/error.tsx (Task 1's output)
    - tests/use-server-exports.test.ts:104-151, :315-318, :320-369, :89-102
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Error (the security property and the SENTINEL assertion)
  </read_first>
  <action>
**`e2e/error-leak.spec.ts`** (AC#20). Arrange for a boundary to render from a real thrown
`new Error("SENTINEL_LEAK_PROBE")` and assert the resulting DOM contains that string **zero** times.

Trigger it from a **dev-only, DB-free** surface so the spec needs no seed and no secret: add a query-param
branch to `/dev/theme` (e.g. `?throw=1`) that throws `new Error("SENTINEL_LEAK_PROBE")` during render. That
route already 404s in production and already carries `robots: { index: false, follow: false }`, so the
affordance ships nowhere real — state that in both files. If a query-param throw proves impractical, use a
dedicated `src/app/dev/throw/page.tsx` under the same `dev/` tree and say why in the SUMMARY.

Assertions: the boundary rendered (`[data-testid="error-state"]` is visible — the `expectReachable` guard,
because a spec that greps a blank page for a string passes vacuously); `page.content()` contains
`SENTINEL_LEAK_PROBE` **zero** times; the `Try again` and route-out controls are both present and
keyboard-reachable.

**`tests/design/error-boundaries.test.ts`** (AC#19). AST-scan `src/app/**` and assert: exactly five
`error.tsx` files exist at the five declared paths; each has the `"use client"` directive in its prologue;
each renders **two** actions; none references `error.message` or `error.stack`; none passes `error` into
`ErrorState`; and `global-error.tsx` exists. Carry the five paths and their route-out targets as a declared
inventory with a reason per row, so a sixth boundary or a moved group fails a named assertion.

Guard-the-guard on both: the e2e spec asserts the probe route actually rendered a boundary rather than a
404; the design gate asserts it parsed five files and found `src/app/error.tsx` by name.

Both-directions self-test on the design gate: a synthetic boundary rendering `{error.message}` must be
flagged; one rendering only `{digest}` must not; one where `error.message` appears in a `//` comment must not.

NOT COVERED footers. The e2e spec: it proves one boundary on one route with one message shape; it cannot
prove a *server* error's text never reaches any surface. The design gate: it reads structure, not rendering.
Name each other's coverage so the pairing is legible.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/error-boundaries.test.ts && npx playwright test e2e/error-leak.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/error-leak.spec.ts` passes: the boundary renders and `SENTINEL_LEAK_PROBE` appears zero times in `page.content()`
    - Watched red: temporarily render `{error.message}` in `src/app/error.tsx` → the e2e spec fails with a non-zero count AND the design gate fails naming the file. Both reverted, both runs recorded in the spec header.
    - Watched red (vacuity): point the spec at a 404 route → the `expectReachable` guard fails rather than the zero-count passing
    - `tests/design/error-boundaries.test.ts` passes with exactly five declared paths, each carrying a reason
    - The throw affordance lives under `src/app/dev/**`, which 404s in a production build — verify with `npm run build && npm start` and a request that returns 404
    - `npm run test:design` passes with the file count raised by exactly 2; push and CI green (D-25)
  </acceptance_criteria>
  <done>The phase's largest new security surface is proven closed in a real browser, and the boundary structure is gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server error object → rendered DOM | Error text routinely carries table names, file paths and connection strings |
| dev throw affordance → production | A route that can force an error must not exist in a production build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-ERRLEAK | Information Disclosure | five `error.tsx` boundaries + `global-error.tsx` | mitigate | Only `digest` crosses into `ErrorState`, which has no `error` prop at all; asserted structurally by `error-boundaries.test.ts` and behaviourally by the `SENTINEL_LEAK_PROBE` e2e assertion in a real browser |
| T-11-THROWROUTE | Elevation of Privilege | the deliberate-throw affordance | mitigate | It lives under `src/app/dev/**`, which 404s in a production build and already carries `robots: noindex`; verified with `npm run build && npm start` returning 404 |
| T-11-GLOBALDB | Tampering | `global-error.tsx` | mitigate | Zero `@/lib/db` imports, asserted; the unreachable-`DATABASE_URL` build is re-run so a prerenderable style-less document cannot reopen job 1's DB-free property |
| T-11-DEADEND | Denial of Service | error surfaces | mitigate | Two actions are structurally required — `routeOut` is a non-optional prop on `ErrorState`, so a one-button boundary does not compile |
| T-11-THEMEBLIND | Repudiation | `global-error` baselines | mitigate | Court-only is recorded as data with its reason, and the theme-swap exclusion is named for plan 11-22 rather than appearing as a silently absent baseline |
| T-11-SC | Tampering | npm | mitigate | Zero packages |
</threat_model>

<verification>
- `find src/app -name error.tsx | wc -l` returns 5, plus one `global-error.tsx`
- `npm run build` exits 0 with an unreachable `DATABASE_URL`
- `npx playwright test e2e/error-leak.spec.ts` passes
- `npm run test:design` green with two new gates
- `npm run build && npm start` returns 404 for the dev throw route
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- Every route group has a two-action error boundary; a global error page exists
- No server error text can reach the DOM, proven in a browser and gated in source
- `global-error` renders in FitOut's colours without a stylesheet and reaches no database
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-18-SUMMARY.md` when done
</output>
