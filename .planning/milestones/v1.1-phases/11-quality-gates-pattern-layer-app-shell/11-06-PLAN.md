---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 06
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-04]
files_modified:
  - src/components/booking/price-breakdown.tsx
  - e2e/price-parity.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [GATE-05]

must_haves:
  truths:
    - "An end-to-end check asserts the price rendered in the DOM equals the price the database holds — the single assertion standing between a restyle and a wrong number charged to a real card (D-35)"
    - "Exactly ONE e2e spec is re-opened into CI, self-seeded, and it needs no production secret — the flow stops before the PayMongo hosted checkout (D-35)"
    - "The checkout total carries data-testid='price-total', the one hook the parity spec reads"
  artifacts:
    - path: "e2e/price-parity.spec.ts"
      provides: "The DB-vs-DOM price equality spec, self-seeding on the search-and-book.spec.ts pattern (D-35)"
      contains: "price-total"
    - path: ".github/workflows/ci.yml"
      provides: "Job 3 — postgis service + dev server + browser, running exactly one spec (D-35)"
  key_links:
    - from: "e2e/price-parity.spec.ts"
      to: "booking.quoted_total_cents"
      via: "a direct postgres.js read compared against the rendered price-total text"
      pattern: "quoted_total_cents"
    - from: "src/components/booking/price-breakdown.tsx"
      to: "src/lib/design/selector-contract.ts"
      via: "the declared price-total id"
      pattern: "price-total"
---

<objective>
Close GATE-05's second clause: prove the number a booker sees is the number the database froze.

Purpose: the AST/build half (plan 11-01) stops a computation crossing the boundary; it cannot tell you the
rendered figure is right. D-35 deliberately re-opens one narrow slice of the e2e surface D-24 closed,
because this is the single assertion between a restyle and a wrong charge.
Output: `data-testid="price-total"` on the checkout total, one self-seeded spec, and CI job 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md

<interfaces>
Analog: e2e/search-and-book.spec.ts. Four load-bearing details, all measured:

  1. The Playwright process does NOT load .env — keep the fallback:
       const DATABASE_URL = process.env.DATABASE_URL ?? "postgresql://fitout:fitout@localhost:5432/fitout";
       const sql = postgres(DATABASE_URL, { max: 1, onnotice: () => {} });
  2. Unique ids per run, never fixed:  `e2e_pp_listing_${randomUUID()}`
  3. Cascade-correct teardown, order is load-bearing (booker_id is ON DELETE RESTRICT):
       notification → booking → "user" (host, cascades listing/photos/hours/tags) → "user" (booker)
  4. The seeded shape: an `instant` listing whose host is payouts-enabled, operating_hours for all 7 days,
     three listing_photo rows, one listing_activity_tag. Reuse `pickWindow` / `selectTargetDay`
     (search-and-book.spec.ts:209-232) — do NOT re-derive the venue-local date math.

CI service addressing (docs.github.com): job 3 needs a browser AND a database. If it runs IN the
Playwright container its DATABASE_URL host is the SERVICE LABEL, not localhost. Getting this backwards
produces a connection-refused that reads like a flaky service.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put the declared hook on the checkout total</name>
  <files>src/components/booking/price-breakdown.tsx</files>
  <read_first>
    - src/components/booking/price-breakdown.tsx (the whole component — find the row that renders the total, and confirm it already uses `formatMoney` with `tabular-nums`)
    - src/components/search/search-result-card.tsx:247-252 (the shipped pre-formatted money render shape AC#33 names)
    - src/lib/design/selector-contract.ts (the declared id — the value must match `price-total` exactly, and plan 11-02's undeclared-id ban will fail on any other string)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § GATE-05 ("what a money figure must look like")
  </read_first>
  <action>
Add `data-testid="price-total"` to the element that renders the booker's **total** on the reserve/checkout
breakdown — the final all-in figure, not a subtotal and not the service-fee line. The attribute goes on the
element whose text content is the money string, so `textContent` is the number and nothing else.

While in the file, confirm every money figure it renders carries `tabular-nums` and goes through
`formatMoney`. This is already the shipped convention; AC#33 names it so it stops being a habit. Add any
missing `tabular-nums` — do not restructure the component, and do not change any layout: this plan predates
the PanelCard adoption (plan `11-13`) that owns this file's container.

Do not add any other `data-testid`. Plan `11-02`'s gate rejects an id that is not in `SELECTOR_CONTRACT`.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="price-total"' src/components/booking/price-breakdown.tsx` returns exactly 1
    - Plan 11-02's undeclared-id assertion passes (the id is declared)
    - Every money figure in the file carries `tabular-nums`
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
    - `git diff --stat src/components/booking/price-breakdown.tsx` shows an attribute-only change plus any `tabular-nums` additions — no structural edit
  </acceptance_criteria>
  <done>The one hook the parity spec reads exists and is the declared name.</done>
</task>

<task type="auto">
  <name>Task 2: The self-seeded DB-vs-DOM price parity spec</name>
  <files>e2e/price-parity.spec.ts</files>
  <read_first>
    - e2e/search-and-book.spec.ts (the whole file: imports and the DATABASE_URL fallback at :28-41, unique ids at :44-51, the seeded shape at :100-132, teardown at :198-207, and `pickWindow`/`selectTargetDay` at :209-232)
    - e2e/scroll-area-overflow.spec.ts:215-223 (`expectReachable` — assert the page renders the thing under test BEFORE asserting anything about it)
    - src/components/booking/price-breakdown.tsx (Task 1's output — where `price-total` lives)
    - src/lib/db/schema.ts (the `booking` table's `quoted_total_cents` column name and type)
    - src/lib/money.ts (`formatMoney` — the spec must normalise the rendered string back to integer centavos rather than string-comparing a formatted figure)
  </read_first>
  <action>
Create `e2e/price-parity.spec.ts`, modelled on `e2e/search-and-book.spec.ts` and narrowed to one assertion.

Seed the same shape as the analog: a host user with payouts enabled, an `instant` listing with
`operating_hours` for all 7 days, three photos and one activity tag, all under `randomUUID` ids. Sign up a
booker with a per-run unique email. Drive search → listing → `Book this space` → the reserve page, using the
analog's `pickWindow` / `selectTargetDay` helpers so the venue-local date math is not re-derived.

Then the single assertion:

  - Read the rendered total: `page.getByTestId("price-total").textContent()`. Guard it with an
    `expectReachable`-style check first — a spec that asserts equality against a missing element passes
    vacuously the moment a restyle moves the hook, which is exactly the regression GATE-04 exists to catch,
    arriving here from the other side.
  - Normalise it back to integer centavos by inverting `formatMoney` (strip the currency symbol, thousands
    separators and the decimal point), rather than formatting the DB value and comparing strings — a locale
    or symbol change would then be a false red on a money gate.
  - Read the persisted figure: `SELECT quoted_total_cents FROM booking WHERE listing_id = $1` for the row
    the hold just created, and assert exact integer equality.

**Stop before PayMongo.** The flow ends on the reserve page where the breakdown renders. D-35's whole point
is that this spec needs no production secret; the analog's own header already documents the hosted checkout
as un-automatable. Assert that property in a comment naming the reason.

Teardown in the analog's order: notification → booking → host user → booker user, then `sql.end()`.

Header block: name D-35, state that this is the ONE e2e spec deliberately re-opened into CI against D-24's
exclusion and why, and add a NOT COVERED footer — this spec proves parity for one instant-book exclusive
listing at one window; it does not cover open-capacity per-head totals, request-to-book, or any refunded or
partially-granted figure.
  </action>
  <verify>
    <automated>npx playwright test e2e/price-parity.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/price-parity.spec.ts --project=chromium` passes against a local `npm run db:up` database
    - Watched red: temporarily add 1 to the rendered total in `price-breakdown.tsx` → the spec fails printing both integers. Reverted.
    - Watched red (vacuity): temporarily rename the hook to `price-total-x` → the spec fails on the reachability guard, not on a passing equality. Reverted.
    - The spec references no PayMongo, Resend, Cloudinary or auth env var
    - Every seeded id contains `randomUUID()`; teardown deletes bookings before users
    - Re-running the spec twice in a row leaves no rows behind: `SELECT count(*) FROM listing WHERE id LIKE 'e2e_pp_%'` returns 0
  </acceptance_criteria>
  <done>The rendered price is provably the persisted price, and the spec cleans up after itself.</done>
</task>

<task type="auto">
  <name>Task 3: CI job 3 — the one deliberate e2e re-opening</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (plan 11-04's output — jobs 1 and 2, the concurrency group, and the comment naming this plan as job 3's owner)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md § "Service containers — the addressing rule that will otherwise burn job 2 or 3" and § "Container-job specifics for the workflow"
    - docker-compose.yml (the `postgis/postgis:18-3.6` tag)
    - playwright.config.ts (its `webServer: npm run dev` and `reuseExistingServer: !process.env.CI` — in CI a fresh server is started, which is what job 3 needs)
  </read_first>
  <action>
Add a third job, `gate-price-parity`, to `.github/workflows/ci.yml`.

Run it **in** the pinned container: `container: { image: "mcr.microsoft.com/playwright:v1.60.0-noble",
options: "--ipc=host" }`. The image ships the browsers and OS dependencies, so there is no browser-install
step; `--ipc=host` is Playwright's documented recommendation for Chromium, which otherwise runs out of
memory and crashes. The image version must match the project's exact `1.60.0` pin or Playwright cannot
locate browser executables.

Add a `services.postgres` entry with image `postgis/postgis:18-3.6` (never `:18` — 404) and a `pg_isready`
health check. **Because the job runs IN a container, address the service by its LABEL, not `localhost`, and
do not map ports** — both are on the same user-defined bridge network. Set
`DATABASE_URL: postgres://fitout:fitout@postgres:5432/fitout` at job level and put a comment on it naming
the container-vs-runner rule, because getting it backwards produces a connection-refused that reads like a
flaky service and gets retried.

Steps: checkout; setup-node with npm cache; `npm ci`; `npm run db:migrate`; then
`npx playwright test e2e/price-parity.spec.ts --project=chromium` — **exactly one spec**, named explicitly.
Do not run `npm run test:e2e`; D-24 keeps the other 11 specs out of CI and this job's whole justification is
that one self-contained spec is a fraction of twelve.

Add a comment asserting the secret property as a contract: this job needs **only** `DATABASE_URL`; if a
future edit makes it need a PayMongo, Resend, Cloudinary or auth secret, that is a signal the spec has grown
past D-35's boundary and must be split, not granted more secrets.

Keep `permissions: contents: read`. Do not change the concurrency group.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');for(const k of ['gate-price-parity','mcr.microsoft.com/playwright:v1.60.0-noble','--ipc=host','@postgres:5432','e2e/price-parity.spec.ts'])if(!y.includes(k)){console.error('missing:',k);process.exit(1)}if(y.includes('@localhost:5432/fitout\n')&&!y.includes('gate-db'))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Job `gate-price-parity` exists, runs in `mcr.microsoft.com/playwright:v1.60.0-noble` with `--ipc=host`
    - Its `DATABASE_URL` host is the service **label** `postgres`, not `localhost`, and the service declares no `ports` mapping
    - The service image is `postgis/postgis:18-3.6`
    - The run command names exactly `e2e/price-parity.spec.ts`; `npm run test:e2e` appears nowhere
    - The job references no secret other than the inline `DATABASE_URL`
    - Push to `origin/dev` and all three jobs are green (D-25) — record the run URL in the SUMMARY
  </acceptance_criteria>
  <done>The price-parity assertion runs on every push, in the pinned image, with one database and no secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered DOM ↔ persisted booking row | The only place a restyle can silently change what a user believes they are paying |
| CI job 3 ↔ secret store | An e2e job is the largest exfiltration surface in the workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-PRICE | Tampering | checkout total rendering | mitigate | Integer-equality assertion between `getByTestId("price-total")` (normalised back to centavos) and `booking.quoted_total_cents`, run on every push |
| T-11-PRICEVACUOUS | Repudiation | `e2e/price-parity.spec.ts` | mitigate | Reachability guard on the hook before the equality assertion, in the `scroll-area-overflow.spec.ts:215-223` form; watched red by renaming the hook |
| T-11-CISECRET | Information Disclosure | job 3 environment | mitigate | Only `DATABASE_URL`; the flow stops before the PayMongo hosted checkout, asserted as a comment-level contract and by an acceptance criterion |
| T-11-SEEDLEAK | Denial of Service | shared CI database | mitigate | Per-run `randomUUID` ids and cascade-correct teardown; re-run leaves zero `e2e_pp_%` rows |
| T-11-SC | Tampering | container images | mitigate | Both image tags verified against live registries; `postgis/postgis:18` is a confirmed 404 and is banned by an acceptance criterion |
</threat_model>

<verification>
- `npx playwright test e2e/price-parity.spec.ts` green locally
- All three CI jobs green on `origin/dev`
- `npm run build` and `npm run test:design` green
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- GATE-05 is complete: the build blocks a boundary crossing, and an e2e check blocks a wrong number
- Exactly one e2e spec runs in CI, with exactly one secret-free database
- The hook is the declared name, so GATE-04's inventory and GATE-05's spec cannot drift apart
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-06-SUMMARY.md` when done
</output>
