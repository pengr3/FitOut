---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - package.json
  - .gitignore
  - tests/design/gitignore-baselines.test.ts
  - e2e/helpers/theme.ts
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "Playwright can never write a baseline implicitly on any machine — updateSnapshots is 'none' unconditionally, with no process.env.CI ternary (D-28)"
    - "The visual specs live in their own project that hard-skips off Linux with a named reason, so a local `npx playwright test` stays usable (D-29)"
    - "@playwright/test is pinned EXACT at 1.60.0 so the container tag mcr.microsoft.com/playwright:v1.60.0-noble can never drift from the project (D-29)"
    - "A Windows- or macOS-generated baseline can never be committed, and that static fact has a standing DB-free test rather than a comment (D-30)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "updateSnapshots 'none' unconditional plus the `visual` project (D-28, D-29)"
      contains: "updateSnapshots"
    - path: ".gitignore"
      provides: "The *-win32.png / *-darwin.png rules it has none of today (D-29)"
    - path: "tests/design/gitignore-baselines.test.ts"
      provides: "The standing guard for both halves — rules present AND git tracks zero matching files (D-30)"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/visual/**"
      via: "the visual project's testMatch, disjoint from the chromium project's"
      pattern: "visual"
    - from: "tests/design/gitignore-baselines.test.ts"
      to: "git ls-files"
      via: "execFileSync, asserting zero tracked platform baselines"
      pattern: "ls-files"
---

<objective>
Close the fail-open half of GATE-01 in configuration, before any baseline exists, and give the one part
of it that can regress *silently* a standing test.

Purpose: D-135 prescribes `updateSnapshots: process.env.CI ? "none" : "missing"`, which on this machine
resolves to `"missing"` and writes `*-win32.png` baselines that report green. D-28 removes the ternary
entirely so no config path on any machine can mint a baseline.
Output: a Playwright config that cannot write, an exact version pin, two `.gitignore` rules, and a
DB-free gate asserting both halves of the platform rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
Measured behaviour of Playwright 1.60.0 in this repo (RESEARCH § GATE-01 Findings, six probe runs) —
these supersede D-135's stated failure mode:

  Finding 1  baseline filename template is `{arg}-{projectName}-{platform}{ext}`
             → a project named `visual` writes `shot-visual-win32.png` on Windows, `…-linux.png` on Linux.
             So the .gitignore rules DO match real filenames and D-30's test can actually fire.
  Finding 2  the config default really is "missing".
  Finding 3  ⚠ D-135's wording is WRONG for 1.60.0. A missing baseline FAILS non-retriably AND WRITES
             the PNG. The fail-open is "green on the NEXT run, off a baseline the failed run just minted"
             — a red that self-heals on a bare re-run, which is the textbook rubber stamp.
  Finding 4  with "none": the error loses its `, writing actual.` clause, ZERO PNGs are written, and the
             failure becomes retriable so it stays red across all attempts.
  Finding 5  the CLI flag overrides config "none" — `takeFirst(configCLIOverrides.updateSnapshots,
             userConfig.updateSnapshots, "missing")` — so D-27's `--update-snapshots` dispatch job works.
  Finding 6  a 2px shift is detected at default thresholds: "249 pixels (ratio 0.01 …) are different."

Current playwright.config.ts (31 lines): no snapshot config, one `chromium` project,
`webServer: npm run dev`, `retries: process.env.CI ? 2 : 0`, `reuseExistingServer: !process.env.CI`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A config that can never write a baseline, and an exact version pin</name>
  <files>playwright.config.ts, package.json, .gitignore, e2e/helpers/theme.ts</files>
  <read_first>
    - playwright.config.ts (the whole 31-line file — the four edits are surgical)
    - e2e/helpers/theme.ts:1-30 (its lines 17-22 state "NO VISUAL-REGRESSION BASELINE MAY BE CAPTURED IN THIS PHASE" — that referred to Phase 10 and is now satisfied; a stale prohibition beside the thing it prohibits is the drift this phase ends, Shared Pattern 10)
    - e2e/reduced-motion.spec.ts:45-51 and e2e/scroll-area-overflow.spec.ts:49-56 (the `reuseExistingServer` stale-stylesheet warning both specs carry — it must be copied into the visual specs, because a stale dev server there yields a baseline captured from stale CSS)
    - .gitignore (the `# typescript` block is the section-heading style to follow; there are zero `*.png` rules today)
    - package.json (`devDependencies["@playwright/test"]` is `^1.60.0`; `scripts.build` must stay `npm run lint && npm run test:design && next build`)
  </read_first>
  <action>
`playwright.config.ts`, four edits:

1. Add `updateSnapshots: "none"` at the top level of `defineConfig`, **unconditionally** — no
   `process.env.CI` ternary. Comment it with D-28's reason and RESEARCH Finding 3's measured behaviour
   (not D-135's wording): under `"missing"` a missing baseline fails once, writes the PNG, and the next
   bare re-run is green. Regeneration happens solely through the `--update-snapshots` CLI flag, which
   Finding 5 confirms overrides config.
2. Scope the existing `chromium` project with `testMatch` so it collects `e2e/*.spec.ts` only and never
   descends into `e2e/visual/**`.
3. Add a second project named exactly `visual` whose `testMatch` is `e2e/visual/**/*.spec.ts`. Give it
   `use: { ...devices["Desktop Chrome"] }`. Make it hard-skip off Linux by guarding the project entry on
   `process.platform === "linux"` and, in the same place, a named reason string —
   "baselines are generated and compared ONLY in mcr.microsoft.com/playwright:v1.60.0-noble (D-27/D-29);
   a non-Linux run would compare against, or be tempted to mint, a platform baseline that can never be
   committed". A local `npx playwright test` must stay usable.
4. Leave `webServer`, `retries` and `reuseExistingServer` unchanged.

`package.json`: change `"@playwright/test": "^1.60.0"` to `"@playwright/test": "1.60.0"` — exact, no caret.
This is the ONLY change to `package.json` in this phase. Run `npm install` and confirm the lockfile records
1.60.0 and that no other dependency moved. The pin is load-bearing because
`mcr.microsoft.com/playwright:v1.60.0-noble` must match the project version exactly or Playwright cannot
locate browser executables.

`.gitignore`: append a new `# playwright visual baselines` section carrying `*-win32.png` and `*-darwin.png`,
with a one-line comment stating that only `*-linux.png` baselines are legal and why (D-29/D-30). Follow the
existing `# typescript` block's style.

`e2e/helpers/theme.ts`: amend the lines 17-22 prohibition. Do not delete the paragraph — rewrite it to say
the prohibition applied to Phase 10, is now satisfied, and that baselines are captured by plan `11-22`
in the pinned Linux image only. Leave `seedTheme` and the imported-not-duplicated storage key untouched.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.devDependencies['@playwright/test']!=='1.60.0')process.exit(1);const c=require('fs').readFileSync('playwright.config.ts','utf8');if(!/updateSnapshots:\s*\"none\"/.test(c))process.exit(1);if(/process\.env\.CI\s*\?[^;]*updateSnapshots/.test(c))process.exit(1);if(!/name:\s*\"visual\"/.test(c))process.exit(1);const g=require('fs').readFileSync('.gitignore','utf8');if(!g.includes('*-win32.png')||!g.includes('*-darwin.png'))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains `updateSnapshots: "none"` and contains no `process.env.CI` ternary anywhere near it
    - A project named exactly `visual` exists; `npx playwright test --list` on this Windows box reports the visual project as skipped/absent with the named reason and still lists the `chromium` project's shipped specs
    - `npx playwright test --list` shows zero overlap between the two projects' collected files
    - `package.json` `devDependencies["@playwright/test"]` is the exact string `1.60.0`; `git diff package.json` shows exactly one changed line; `scripts.build` is unchanged
    - `.gitignore` contains `*-win32.png` and `*-darwin.png` under a commented section heading
    - `e2e/helpers/theme.ts` no longer asserts that no baseline may be captured, and names plan `11-22`
    - `npm run test:e2e` still passes with the same pass/fail signature as the phase baseline (17 passed / 1 failed is the shipped D-6 item-1 signal — record it, do not chase it)
  </acceptance_criteria>
  <done>No config path on any machine can mint a baseline, the version is pinned exact, and the platform rules are in `.gitignore`.</done>
</task>

<task type="auto">
  <name>Task 2: The standing platform-baseline guard — both halves</name>
  <files>tests/design/gitignore-baselines.test.ts</files>
  <read_first>
    - tests/design/scaffold-residue.test.ts:16-20 (why an absence needs a permanent test at all — "a deletion has no symptom … the realistic way it comes back is `npx create-next-app` output being copied in")
    - tests/use-server-exports.test.ts:315-318 (guard-the-guard) and :89-102 (NOT COVERED footer)
    - vitest.design.config.ts (this gate must be DB-free and must not add `globalSetup`/`setupFiles`)
    - .gitignore (Task 1's output)
  </read_first>
  <action>
Create `tests/design/gitignore-baselines.test.ts`. D-30 splits GATE-01's proof by kind: missing-baseline and
few-pixel-shift are triggered deliberately once and recorded (plan `11-22`), but **the Windows rule is a
static fact that can regress silently**, so it earns a standing test. Say that in the header.

Assert both halves:

1. **The rules exist.** Read `.gitignore` and assert it contains `*-win32.png` and `*-darwin.png` as
   standalone rule lines. Strip `#` comment lines before matching — this repo has been bitten repeatedly by
   a grep landing on prose (see `tests/design/strip-comments.test.ts`), and a rule asserted against a comment
   that argues *for* the rule is the same defect.
2. **Git tracks zero such files.** Shell out with
   `execFileSync("git", ["ls-files", "*-win32.png", "*-darwin.png"], { encoding: "utf8" })` and assert the
   output is empty. Choose `git ls-files` over a filesystem walk and **state the reason in the header**: an
   untracked `*-win32.png` sitting in a working tree is harmless and expected (it is what a local
   `--update-snapshots` would produce); the thing that must never happen is one being *committed*. A
   filesystem walk cannot tell those apart. This is the repo's first test that shells out to `git` — note
   that too, and handle a non-repo checkout by failing with a named message rather than throwing raw.

Add guard-the-guard: assert `.gitignore` was actually read and is non-empty, and that the `git ls-files`
call returned exit code 0 (an unavailable `git` must fail the test, not silently return an empty string
that makes half 2 pass vacuously).

Add both-directions self-tests on synthetic strings: a `.gitignore` body containing only
`# *-win32.png is ignored` must NOT satisfy half 1; a body containing the bare rule must.

Close with a NOT COVERED footer: this file does not prove a missing baseline fails (that is `11-21`'s
recorded OBSERVED RED), and it cannot see a baseline committed under a name the rules do not match.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/gitignore-baselines.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/gitignore-baselines.test.ts` passes
    - Watched red (half 1): temporarily comment out the `*-win32.png` rule → the test fails naming it. Restored. Both runs recorded in the header.
    - Watched red (half 2): `git add -f` a throwaway `e2e/visual/x-visual-win32.png` → the test fails naming the tracked path; `git rm --cached` it and the file is deleted. Both runs recorded.
    - Watched red (comment-only): the synthetic `# *-win32.png` fixture is asserted NOT to satisfy half 1
    - The header states why `git ls-files` was chosen over a filesystem walk
    - `npm run test:design` passes with the file count raised by exactly 1
  </acceptance_criteria>
  <done>The one silently-regressing half of GATE-01 has a standing gate that has been watched failing three ways.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author's machine → committed baseline artifact | A Windows-generated PNG committed here becomes the reference every future CI run compares against |
| CLI flag → snapshot writes | The only sanctioned write path; everything else must be inert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-BASEMINT | Tampering | `playwright.config.ts` snapshot behaviour | mitigate | `updateSnapshots: "none"` unconditional — measured to write zero PNGs and stay red across retries (Finding 4); regeneration only via the CLI flag (Finding 5) |
| T-11-PLATBASE | Tampering | committed baseline artifacts | mitigate | `.gitignore` `*-win32.png` / `*-darwin.png` plus a standing test asserting `git ls-files` returns zero matches |
| T-11-VERDRIFT | Tampering | Playwright ↔ container image | mitigate | Exact pin `1.60.0`; the container tag is `v1.60.0-noble`; a version mismatch means Playwright cannot locate browser executables (playwright.dev/docs/docker) |
| T-11-STALECSS | Repudiation | `reuseExistingServer` in visual runs | accept | Documented in-spec warning copied from `reduced-motion.spec.ts:45-51`; CI always starts a fresh server so the exposure is local-only |
| T-11-SC | Tampering | npm installs | mitigate | Only change to `package.json` is loosening a caret to an exact pin — `git diff package.json` showing exactly one line is an acceptance criterion |
</threat_model>

<verification>
- `npm run test:design` green with one new file
- `npx playwright test --list` shows two projects with disjoint file sets and the visual project named-skipped on Windows
- `git diff package.json` is exactly one line
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- The VR gate's fail-open is closed in configuration on every machine, not just on CI
- The platform-baseline rule is asserted rather than trusted
- The stale Phase-10 baseline prohibition in `e2e/helpers/theme.ts` is amended, not left contradicting the tree
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-03-SUMMARY.md` when done
</output>
