---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 05
type: execute
wave: 3
depends_on: [11-04]
files_modified:
  - tests/availability/open-capacity-exclude.test.ts
  - tests/availability/exclusion-race.test.ts
autonomous: false
requirements: [GATE-04]

must_haves:
  truths:
    - "A mutation that lets the (N+1)th booking succeed turns the constraint spec RED, and that has been observed by hand rather than asserted from memory (D-33)"
    - "Nothing destructive lives in the suite — the standing guard is a read-only pg_constraint catalog assertion scoped to the isolated test schema (D-33)"
    - "The mutation ran against fitout_test only, and the constraint's existence was verified from the catalog before anything else happened (D-33)"
  artifacts:
    - path: "tests/availability/exclusion-race.test.ts"
      provides: "The D-33 OBSERVED RED header block — the verbatim failing run with the constraint dropped"
      contains: "OBSERVED RED"
    - path: "tests/availability/open-capacity-exclude.test.ts"
      provides: "The extended standing catalog assertion that booking_no_overlap exists with its expected definition (D-33)"
      contains: "pg_get_constraintdef"
  key_links:
    - from: "tests/availability/open-capacity-exclude.test.ts"
      to: "pg_constraint"
      via: "pg_get_constraintdef scoped by n.nspname = testDb.schema"
      pattern: "nspname"
---

<objective>
Prove GATE-04's second clause — *"a mutation that lets the (N+1)th booking succeed still turns the
constraint spec red"* — by doing it once, by hand, and recording it; and leave behind a read-only standing
guard rather than a DDL harness.

Purpose: the GiST exclusion constraint is the product's core value. DDL against it inside a test suite risks
a failed rollback leaving the test DB with no constraint and every later test silently green — which is
strictly worse than no proof at all (D-33).
Output: a verbatim OBSERVED RED header on `exclusion-race.test.ts`, and an extended catalog assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
The exact query shape to extend — tests/availability/open-capacity-exclude.test.ts:139-153. The namespace
scope is load-bearing and the file's own comment says so: the dev `public` schema carries a same-named
constraint, so an unscoped query reads the wrong one and stays green after a mutation to the isolated
test schema.

  SELECT pg_get_constraintdef(c.oid) AS def
  FROM pg_constraint c
  JOIN pg_namespace n ON n.oid = c.connamespace
  WHERE c.conname = 'booking_no_overlap' AND n.nspname = ${testDb.schema}

The spec the mutation must turn red: tests/availability/exclusion-race.test.ts — `makeRacingClients`
two-connection harness at :82-108. Its accepted-codes set at :44-45 is `23P01` OR `40P01`; a record that
quotes only `23P01` will look wrong to a future reader, so quote the constant.

Machine-enforced safety: tests/helpers/test-db-url.ts throws unless the resolved database name ends in
`_test`. GATE-06 binds — this plan writes ZERO migration files; `drizzle/` stays at 0025.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the standing catalog assertion so the constraint's existence is guarded, not assumed</name>
  <files>tests/availability/open-capacity-exclude.test.ts</files>
  <read_first>
    - tests/availability/open-capacity-exclude.test.ts:139-153 (the existing assertion and its namespace-scope comment — copy the query verbatim)
    - drizzle/ (find the migration that declares `booking_no_overlap` and read its exact `EXCLUDE` clause — the expected definition comes from the catalog, never retyped from memory)
    - tests/helpers/db.ts (`setupTestDb` / `teardownTestDb` — the schema-isolated harness this spec runs in)
  </read_first>
  <action>
Extend the existing `it("the live constraint definition carries the narrow", …)` in
`tests/availability/open-capacity-exclude.test.ts` into the standing guard D-33 asks for.

Keep the query byte-identical, including `n.nspname = ${testDb.schema}`. Add, in the same test or a sibling
`it` in the same block:

1. **Existence.** Assert the catalog returned exactly one row — today the destructuring `const [{ def }]`
   throws an unhelpful `TypeError` when the constraint is missing, which is the wrong failure for the app's
   most important invariant. Assert `rows.length === 1` first, with a message naming the constraint and the
   schema.
2. **Kind.** Assert `def` starts with `EXCLUDE USING gist` — a `UNIQUE` or a plain index bearing the same
   name would satisfy a substring check and would not prevent an overlap.
3. **The three narrows already relied on.** Keep `expect(def).toContain("open_capacity = false")` and
   `expect(def).toContain("'cancelled'")`, and add an assertion for the `tstzrange` half-open form `'[)'`
   — back-to-back bookings (10:00–11:00 then 11:00–12:00) must NOT overlap, and a `'[]'` would make every
   adjacent pair collide.

Add a header note stating this is D-33's **standing** guard and that the destructive proof is recorded once,
by hand, in `exclusion-race.test.ts`'s header — never automated. Name the reason: DDL in a test suite against
this constraint risks a failed rollback leaving the test DB unconstrained and every later test silently green.
  </action>
  <verify>
    <automated>npx vitest run tests/availability/open-capacity-exclude.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/availability/open-capacity-exclude.test.ts` passes
    - The query still carries `n.nspname = ${testDb.schema}`
    - Assertions exist for row count `=== 1`, `EXCLUDE USING gist`, `open_capacity = false`, `'cancelled'` and the half-open `'[)'` form
    - `git status --short drizzle/` is empty — zero migrations (GATE-06)
    - `npm test` passes with the same overall counts as the phase baseline plus these assertions
  </acceptance_criteria>
  <done>The constraint's presence and shape are guarded read-only on every full-suite run, including CI job 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The recorded (N+1)th-booking mutation — hand-run, captured, restored, verified</name>
  <files>tests/availability/exclusion-race.test.ts</files>
  <read_first>
    - tests/availability/exclusion-race.test.ts (the whole file: its header at :9-11, the makeRacingClients two-connection harness at :82-108, and the accepted-codes set at :44-45 — 23P01 OR 40P01; quote the constant, not just 23P01)
    - tests/use-server-exports.test.ts:25-62 (the OBSERVED RED header format this record must follow)
    - tests/helpers/test-db-url.ts (the machine-enforced _test suffix guard that makes step 0 a check rather than a promise)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md, the hand-run mutation procedure under GATE-04 Findings (the six-step safe shape and the psql BEGIN/ROLLBACK alternative)
  </read_first>
  <action>
    Perform the destructive proof BY HAND, exactly once, against `fitout_test` only, and commit the verbatim
    failing run as an OBSERVED RED header block on `tests/availability/exclusion-race.test.ts`. Nothing
    destructive may be left in the suite. Capture the constraint definition from the catalog BEFORE dropping
    it — that string is the restore script and it is never retyped from the migration — and verify the
    restored definition is byte-equal to the captured one before doing anything else. Prefer the psql
    BEGIN/ROLLBACK form: the rollback is atomic and the constraint cannot survive the disconnect, which
    removes the manual re-ADD from the risk surface entirely. Write ZERO migration files (GATE-06).
  </action>
  <what-built>Task 1's read-only catalog guard. What follows is the one destructive proof D-33 requires, and it is deliberately NOT automated. It must be performed by hand against `fitout_test` only, and its verbatim output committed as an OBSERVED RED header block on `tests/availability/exclusion-race.test.ts`.</what-built>
  <how-to-verify>
    Run these in order. Steps 2 and 5 are what make step 3 safe; omitting either is the scenario D-33 rejected.

    0. `npm run db:up` (Docker is not running on this box by default), then confirm the target:
       `psql "$DATABASE_URL" -c "SELECT current_database()"` must end in `_test`. If it does not, STOP.
       `npm run db:test:setup` provisions `fitout_test`.
    1. Record the context the header needs: `git rev-parse --short HEAD` and `git status --short` (must be
       clean for `src/` and `tests/`).
    2. **Capture the constraint definition FIRST, verbatim** — this string is the restore script, and it is
       read from the catalog, never retyped from the migration:
       `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'booking_no_overlap';`
       Save the output.
    3. Prefer the atomic form if psql is available: open a single psql session and run
       `BEGIN; ALTER TABLE booking DROP CONSTRAINT booking_no_overlap;` — leave the transaction OPEN, run
       the spec from another shell, then `ROLLBACK;`. The rollback is atomic and the constraint cannot
       survive a disconnect, which removes the manual re-ADD from the risk surface entirely.
       If the spec cannot see an uncommitted DDL from another connection, fall back to
       `ALTER TABLE booking DROP CONSTRAINT booking_no_overlap;` committed, and follow step 4 exactly.
    4. With the constraint gone, run `npx vitest run tests/availability/exclusion-race.test.ts` and capture
       the **verbatim** failing output and the exit code. Expect the two racing inserts to BOTH succeed —
       `expect(results.filter(fulfilled)).toHaveLength(1)` receiving 2, and the row count assertion
       receiving 2 instead of 1.
    5. Restore: `ROLLBACK;` (or `ALTER TABLE booking ADD CONSTRAINT booking_no_overlap …` using the string
       captured in step 2), then **re-run step 2's query and assert the returned definition is byte-equal to
       the captured one**. Then re-run the spec and confirm green.
    6. Paste the captured failure into a new header block at the top of
       `tests/availability/exclusion-race.test.ts`, in the `tests/use-server-exports.test.ts:25-62` format:
       HEAD sha + clean `git status`, the exact command, the exit code, the verbatim output, and a sentence
       naming what did **not** fail (the rest of `tests/availability/**` was unaffected — proof this is not
       a mutation that breaks everything). Quote the accepted-codes constant `23P01`/`40P01` from
       `exclusion-race.test.ts:44-45` rather than only `23P01`.
       State in the block that the constraint was restored and its definition verified byte-equal, and that
       `tests/availability/open-capacity-exclude.test.ts` is the standing guard.
    7. `npx vitest run tests/availability/` must be fully green before you resume.
  </how-to-verify>
  <resume-signal>Reply "recorded" once the header block is committed, the constraint definition is verified byte-equal to the captured original, and `npx vitest run tests/availability/` is green. Paste the restored-definition comparison result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test database ↔ dev database | Both carry a constraint named `booking_no_overlap`; an unscoped catalog query reads the wrong one |
| hand-run DDL ↔ the product's core invariant | A dropped constraint that is not restored makes every later test silently green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-DDL | Tampering / Denial of Service | `booking_no_overlap` on the test database | mitigate | `_test` suffix guard in `tests/helpers/test-db-url.ts` (machine-enforced); definition captured from the catalog BEFORE the drop; `BEGIN … ROLLBACK` preferred over drop-and-re-ADD; post-run byte-equality verification of the restored definition; nothing destructive committed to the suite |
| T-11-WRONGSCHEMA | Repudiation | the catalog assertion | mitigate | `n.nspname = ${testDb.schema}` retained verbatim — an unscoped query reads the dev `public` constraint and stays green after a mutation to the isolated schema |
| T-11-STALEPROOF | Repudiation | the OBSERVED RED record | mitigate | The block carries the HEAD sha, a clean `git status`, the exact command, the exit code and a "what did not fail" sentence, so a future reader can tell a real observation from a remembered one |
| T-11-SC | Tampering | npm installs / migrations | mitigate | Zero packages, zero migrations — `git status --short drizzle/` empty is an acceptance criterion (GATE-06) |
</threat_model>

<verification>
- `npx vitest run tests/availability/` green after restoration
- The restored constraint definition is byte-equal to the captured original
- `git status --short drizzle/` empty
- `npm test` green; CI job 2 green on push (D-25)
</verification>

<success_criteria>
- The double-booking guarantee has been watched failing when its constraint was removed, and the run is committed verbatim
- The standing guard is read-only and schema-scoped
- The test database is provably back in its constrained state
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-05-SUMMARY.md` when done
</output>
