---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 22
type: execute
wave: 12
depends_on: [11-21]
files_modified:
  - e2e/visual/freeze.css
  - e2e/visual/surfaces.spec.ts
  - e2e/visual/theme-swap.spec.ts
  - src/lib/design/visual-baselines.ts
  - .github/workflows/baselines.yml
  - .github/workflows/ci.yml
  - tests/design/selector-contract.test.ts
autonomous: false
requirements: [GATE-01, GATE-04]

must_haves:
  truths:
    - "Visual-regression baselines exist and are generated ONLY inside the pinned Linux image, by the same runner that later compares against them, so author-vs-CI drift is structurally impossible (D-27)"
    - "A CI run with a missing baseline FAILS and has been watched failing, with the MEASURED behaviour recorded rather than D-135's assumed one: under updateSnapshots 'none' zero PNGs are written and the failure is retriable, so it stays red across every attempt (D-30)"
    - "A deliberate few-pixel shift goes red, and that run is committed verbatim too (D-30)"
    - "The baselines the dispatch job commits are VERIFIED by an explicit follow-up run — a GITHUB_TOKEN push creates no workflow run, so without that step D-27 produces baselines nobody has ever seen pass (D-27)"
    - "Every baselined surface has court.png != grove.png, with exactly one exclusion carried as data with its reason: global-error, which cannot be themed by construction"
    - "GATE-04's forward direction lands: every declared data-testid actually appears in src/, and the two directions are exact complements"
  artifacts:
    - path: ".github/workflows/baselines.yml"
      provides: "The workflow_dispatch commit-back job (D-27), contents:write scoped to it alone, github.event_name in the concurrency group"
      contains: "update-snapshots"
    - path: "e2e/visual/freeze.css"
      provides: "Determinism — forced reduced motion, zeroed animation and transition durations, hidden caret"
    - path: "src/lib/design/visual-baselines.ts"
      provides: "The declared baselined-surface inventory and the single theme-swap exclusion carried as data with its reason (AC#30)"
  key_links:
    - from: ".github/workflows/baselines.yml"
      to: "npx playwright test --project=visual --update-snapshots"
      via: "the CLI flag — the only path that can write a baseline, because config is 'none' unconditionally"
      pattern: "update-snapshots"
    - from: ".github/workflows/ci.yml job 1"
      to: "e2e/visual/**"
      via: "the visual comparison step, added once baselines exist"
      pattern: "--project=visual"
---

<objective>
Shoot the baselines in the pinned image, wire the comparison into CI, and prove the gate can actually fail —
twice, by hand, with the verbatim output committed.

Purpose: this is the phase's closing act and the reason the ordering invariant exists. *A gate that has
silently never failed is worse than no gate — it trains reviewers to trust a rubber stamp.* D-30 splits the
proof by kind: the two dynamic failures are triggered deliberately once and recorded; the static one already
has its standing test from plan `11-03`.
Output: the visual project's specs, the dispatch job, committed Linux baselines, two OBSERVED REDs, and
GATE-04's forward assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
The baseline set (11-UI-SPEC § GATE-01). Scoping rule: Phase 11 baselines ONLY DB-free surfaces. A baseline
that needs seeded data is flaky, and a flaky gate is retried until green — a rubber stamp with extra steps.
Product surfaces get baselines in Phases 12-15 with their own fixtures.

  /dev/theme (extended, sections 1-14)   320 / 768 / 1280   both, side by side   3
  /terms, /privacy                        320 / 768 / 1280   both                12
  root not-found                          320 / 1280         both                 4
  global-error                            1280               COURT ONLY           1
  (auth) shell (login)                    320 / 1280         both                 4
  listing + invite + root OG images       1200x630           court only           3
                                                                       TOTAL     27

Measured Playwright 1.60.0 behaviour (11-RESEARCH § GATE-01 Findings). The OBSERVED RED must describe
THESE, not D-135's wording:
  Finding 3  under updateSnapshots "missing": a missing baseline FAILS NON-RETRIABLY **and writes the PNG**,
             so the next run is green with no code change. The fail-open is "a red that self-heals on a bare
             re-run" — the textbook rubber stamp, because nobody investigates a gate that passes when you
             press the button again.
  Finding 4  under "none" (what plan 11-03 set): the error loses its ", writing actual." clause, ZERO PNGs
             are written, and the failure becomes RETRIABLE so it stays red across all attempts.
  Finding 5  the CLI --update-snapshots flag overrides config "none".
  Finding 6  a 2px shift is detected at default thresholds: "249 pixels (ratio 0.01 of all image pixels)
             are different." No threshold tuning needed.
  Finding 1  the Linux filename shape is `{arg}-visual-linux.png`.

Dispatch-job facts (docs.github.com, cited unless marked):
  - a GITHUB_TOKEN push creates NO workflow run -> recursion is impossible, AND the committed baselines are
    never verified unless the plan adds an explicit re-run (Pitfall 3).
  - `permissions: contents: write` on that job only; every other job stays contents: read.  [ASSUMED, A2]
  - git identity must be set explicitly (github-actions[bot]); the container image has none.  [ASSUMED, A2]
  - concurrency group must include github.event_name, or a push cancels the running dispatch mid-flight and
    leaves a partial baseline commit (Pitfall 4).
  - image mcr.microsoft.com/playwright:v1.60.0-noble, options --ipc=host, no browser-install step (the image
    ships browsers and OS deps).

Plan 11-02 deliberately deferred the selector contract's FORWARD direction ("every declared id appears in
src/") and named THIS plan as its owner in that file's NOT COVERED footer. It lands here because every
owning plan has now run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The visual specs, the determinism sheet, and the declared baseline inventory</name>
  <files>e2e/visual/freeze.css, e2e/visual/surfaces.spec.ts, e2e/visual/theme-swap.spec.ts, src/lib/design/visual-baselines.ts</files>
  <read_first>
    - playwright.config.ts (plan 11-03's output — `updateSnapshots: "none"` unconditional, the `visual` project's `testMatch`, and its non-Linux hard-skip with a named reason)
    - e2e/helpers/theme.ts:45-59 (`seedTheme` on the CONTEXT before the first goto) and :3-9 (why the storage key is imported from the provider and never duplicated — a duplicated literal makes the swap smoke go green while screenshotting the same theme twice, which is exactly AC#30's failure mode)
    - e2e/scroll-area-overflow.spec.ts:26-30 (why a DB-free driver page), :215-223 (`expectReachable`), :49-56 (the `reuseExistingServer` warning)
    - src/lib/design/contrast-pairs.ts:22-36 (the EXCLUDED_PAIRS-with-reasons idiom the baseline inventory copies)
    - src/app/dev/theme/page.tsx (plan 11-21's 14 sections — the primary baselined surface)
    - src/app/global-error.tsx (plan 11-18's output and its recorded court-only decision)
  </read_first>
  <action>
Create `e2e/visual/freeze.css`: force `prefers-reduced-motion`, zero every `animation-duration` and
`transition-duration`, and hide the caret. Record in a comment that **none of the baselined surfaces renders
a clock, so no time freeze is needed** — and that the first surface which does (Phase 12's countdown) must
add one. Without that note a future flake gets blamed on the wrong thing.

Create `src/lib/design/visual-baselines.ts`: an `as const` inventory of all 27 baselines, each row carrying
its route, width, theme and a mandatory reason. Add `THEME_SWAP_EXCLUSIONS` with exactly **one** entry —
`global-error`, reason *"renders its own document and receives no global styles, so an app-level data-theme
attribute never reaches it; identical court/grove screenshots are CORRECT here"*. The OG images are
court-only for the same class of reason (there is no user and no `data-theme` on a server-rendered image) and
carry their own reason rows on the inventory side rather than the exclusion side. Follow the
`EXCLUDED_PAIRS` idiom: a zero that is asserted is a contract; a zero that is merely true is an invitation.

Create `e2e/visual/surfaces.spec.ts`: for every inventory row — `seedTheme(context, theme)` before the first
`goto`, inject `freeze.css` with `page.addStyleTag`, set the viewport, `expectReachable` the surface's
declared hook, then `await expect(page).toHaveScreenshot()`. Drive the root not-found and `global-error`
through the dev throw affordance plan `11-18` added under `src/app/dev/**`. Any surface that cannot be
reached is skipped with a **named reason**, never silently.

Create `e2e/visual/theme-swap.spec.ts` (D-135's smoke): for every surface **not** in
`THEME_SWAP_EXCLUSIONS`, capture court and grove and assert the two PNG buffers are **byte-different** —
identical two-theme screenshots mean that surface ignored the tokens. Assert
`THEME_SWAP_EXCLUSIONS.length === 1`, so a second exclusion must be argued for rather than appended.

Copy the `reuseExistingServer` stale-stylesheet warning into both specs. In the visual project it is worse
than a false red: a stale dev server yields **a baseline captured from stale CSS**.

Do **not** try to run these to completion locally — plan `11-03`'s config hard-skips the visual project off
Linux with a named reason, and that is the design. Confirm the skip fires and record the output.
  </action>
  <verify>
    <automated>npx playwright test --project=visual --list && npx tsc --noEmit && npx vitest run --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/visual/freeze.css` forces reduced motion, zeroes animation and transition durations, hides the caret, and carries the no-clock-yet note naming Phase 12
    - `src/lib/design/visual-baselines.ts` enumerates exactly 27 baselines (3 + 12 + 4 + 1 + 4 + 3) and `THEME_SWAP_EXCLUSIONS` has exactly 1 entry with a non-empty reason
    - `npx playwright test --project=visual --list` on this Windows box reports the named skip; the `chromium` project's collected file list is unchanged
    - Both specs call `seedTheme` on the **context** before the first `goto` and inject `freeze.css`
    - Both specs carry `expectReachable` guards and the `reuseExistingServer` warning verbatim
    - `npx tsc --noEmit` exits 0 and `npm run test:design` is green
    - Zero `*.png` files appear in `git status` after any local run (plan 11-03's config writes none)
  </acceptance_criteria>
  <done>The visual project has specs, a determinism sheet and a declared inventory — and still zero baselines.</done>
</task>

<task type="auto">
  <name>Task 2: The workflow_dispatch commit-back job</name>
  <files>.github/workflows/baselines.yml</files>
  <read_first>
    - .github/workflows/ci.yml (plans 11-04 and 11-06's output — the concurrency-group shape, the container idiom from job 3, and the top-level `permissions: contents: read` this file must NOT inherit blindly)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md, the workflow_dispatch commit-back table under GATE-01 Findings (recursion, the unverified flip side, permissions, git identity, and the concurrency interaction)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md, the container-job specifics table (the image tag, --ipc=host, the root default, and why no browser-install step is needed)
    - playwright.config.ts (plan 11-03's `updateSnapshots: "none"` — the CLI flag in this job is the ONLY thing that overrides it)
  </read_first>
  <action>
Create `.github/workflows/baselines.yml`. There is no analog in this repository — every detail comes from
RESEARCH, not from a pattern.

Trigger: `on: workflow_dispatch` only. Never `push`, never `pull_request` — a job that can write baselines
must be reachable only by a deliberate human action.

`concurrency: { group: "${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}",
cancel-in-progress: true }`. The `github.event_name` term is load-bearing: without it a push to the same
branch cancels this run mid-flight and leaves a partial or absent baseline commit (Pitfall 4).

One job, `generate-baselines`, with `permissions: contents: write` declared **on the job**, not at workflow
level — every other workflow and job in the repository stays `contents: read`. Run it in
`container: { image: "mcr.microsoft.com/playwright:v1.60.0-noble", options: "--ipc=host" }`. No
browser-install step: the image ships browsers and OS deps, and its version must match the project's exact
`1.60.0` pin or Playwright cannot locate browser executables.

Steps: checkout; `actions/setup-node` with `cache: npm`; `npm ci`; then
`npx playwright test --project=visual --update-snapshots`. The flag is the sole sanctioned write path
(Finding 5 confirms it overrides config `"none"`), and it must appear in this file and nowhere else. Set
`DATABASE_URL` to the same unreachable host job 1 uses — every baselined surface is DB-free by the UI-SPEC's
scoping rule, so the property stays asserted here too.

Then commit back: set an explicit git identity (`github-actions[bot]`) because the container image has none,
`git add` only the `*-visual-linux.png` paths, skip the commit when the diff is empty, and push to the
dispatched ref. Mark the identity and the job-level `permissions` line with an `[ASSUMED — RESEARCH A2]`
comment: both are standard Actions practice but were not verified against the docs in the research session,
and a wrong guess fails loudly on the first dispatch rather than silently.

Add a header block recording D-27 and the two consequences a reader needs: (a) a `GITHUB_TOKEN` push creates
**no** workflow run, so recursion is impossible — and (b) that same fact means **the committed baselines are
never verified by a run**, so an explicit follow-up dispatch or push is a required step, owned by Task 3.
Without it, D-27 produces baselines nobody has ever seen pass (Pitfall 3).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/baselines.yml','utf8');for(const k of ['workflow_dispatch','contents: write','--update-snapshots','v1.60.0-noble','--ipc=host','github.event_name','github-actions'])if(!y.includes(k)){console.error('missing:',k);process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/baselines.yml` triggers on `workflow_dispatch` only — no `push`, no `pull_request`
    - `permissions: contents: write` appears at job level, not workflow level
    - The container is `mcr.microsoft.com/playwright:v1.60.0-noble` with `--ipc=host`, and there is no browser-install step
    - `concurrency.group` contains `github.event_name`
    - `--update-snapshots` appears in this file and in no other workflow: `grep -rc -- "--update-snapshots" .github/workflows/` shows 1 for `baselines.yml` and 0 for `ci.yml`
    - The commit step stages only `*-visual-linux.png` paths and no-ops on an empty diff
    - The `[ASSUMED — RESEARCH A2]` comments are present on the identity and permissions lines
    - The header records both halves of the `GITHUB_TOKEN` consequence and names Task 3 as the owner of the verification run
  </acceptance_criteria>
  <done>The one job that can write a baseline exists, is human-triggered only, and documents why its own output is not yet verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dispatch, commit, then VERIFY the baselines</name>
  <files>none</files>
  <read_first>
    - .github/workflows/baselines.yml (Task 2's output — read it in full before dispatching)
    - tests/design/gitignore-baselines.test.ts (plan 11-03's standing guard — the check that no platform baseline was committed)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md, Pitfall 3 (why a follow-up run is required rather than optional)
  </read_first>
  <action>
    Make no code changes in this task. Dispatch the workflow, confirm what it committed, then trigger the
    explicit follow-up run that actually compares against the new baselines. Do NOT treat the dispatch's own
    green as verification — it wrote the PNGs, it did not compare against them, and a `GITHUB_TOKEN` push
    triggers no workflow. If the push fails with a 403, that is plan 11-04 Task 1's read-only answer showing
    up rather than a git problem. Record both run URLs and the committed PNG count in the SUMMARY.
  </action>
  <what-built>`.github/workflows/baselines.yml` from Task 2: a `workflow_dispatch` job running the `visual` project with `--update-snapshots` in `mcr.microsoft.com/playwright:v1.60.0-noble`, committing the resulting `*-visual-linux.png` files back to the branch. Plan `11-04`'s Task 1 already established whether this repository grants `GITHUB_TOKEN` write permission — if that came back **read-only**, this job cannot push and the setting must be changed before dispatching.</what-built>
  <how-to-verify>
    1. Inspect the workflow file before dispatching. It must have: `on: workflow_dispatch`;
       `permissions: contents: write` **on this job only** (every other job stays `contents: read`); an
       explicit git identity (`github-actions[bot]`); the container
       `mcr.microsoft.com/playwright:v1.60.0-noble` with `options: --ipc=host`; **no** browser-install step;
       a `concurrency.group` containing `${{ github.event_name }}`; and the run command
       `npx playwright test --project=visual --update-snapshots`.
    2. Confirm Settings → Actions → General → Workflow permissions is **Read and write**. If plan `11-04`
       reported read-only and it has not been changed, change it now — otherwise the push fails with a 403
       that reads like a git error and costs a debugging session.
    3. Dispatch the workflow against `dev` from the Actions tab. Watch it run.
    4. `git pull origin dev` and confirm only `*-visual-linux.png` files were added — expect 27. **No
       `*-win32.png` or `*-darwin.png` may appear.** Then run `npm run test:design`: plan `11-03`'s
       `tests/design/gitignore-baselines.test.ts` asserts git tracks zero of those, so it is the check.
    5. ⚠ **The committed baselines are NOT verified yet.** A `GITHUB_TOKEN` push creates no workflow run by
       design, so nothing has compared against them. Trigger an explicit follow-up — push an empty commit or
       re-dispatch — and confirm a `ci` run compares against the new baselines and goes **green**. Without
       this step D-27 produces baselines nobody has ever seen pass.
    6. Report: the dispatch run URL, the number of PNGs committed, and the verification run URL with its
       conclusion.
  </how-to-verify>
  <resume-signal>Reply with both run URLs and the committed PNG count, confirming the follow-up comparison run went green and that `npm run test:design` passes after pulling.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Wire the visual comparison into CI job 1, and land GATE-04's forward assertion</name>
  <files>.github/workflows/ci.yml, tests/design/selector-contract.test.ts</files>
  <read_first>
    - .github/workflows/ci.yml (plans 11-04 and 11-06's output — jobs 1, 2 and 3, and the header comment naming THIS plan as the owner of job 1's visual step)
    - .github/workflows/baselines.yml (Task 2's output, verified by Task 3 — job 1's step must use the same image and the same project name)
    - tests/design/selector-contract.test.ts (plan 11-02's output — its NOT COVERED footer names this plan as the owner of the forward direction, and its AST collection of `data-testid` literals is the scan to reuse)
    - src/lib/design/selector-contract.ts (the 17 declared ids and each row's `owner` field, which is what makes this hand-off auditable)
  </read_first>
  <action>
**CI job 1.** Add the visual comparison to `gate-db-free`. Job 1 now needs a browser, so move it into the
pinned container: `container: { image: "mcr.microsoft.com/playwright:v1.60.0-noble", options: "--ipc=host" }`.
Keep the existing `npm run build` step with its unreachable `DATABASE_URL` — every baselined surface is
DB-free by the UI-SPEC's own scoping rule, so the standing DB-free assertion survives — then add
`npx playwright test --project=visual`.

**Do not add `--update-snapshots` to this job.** The CLI flag is the only path that can write a baseline and
it belongs solely to the dispatch job. A flag here would recreate D-135's fail-open in a new place.

**GATE-04's forward direction.** Extend `tests/design/selector-contract.test.ts` with the assertion plan
`11-02` deferred and named this plan for: every id in `SELECTOR_IDS` appears at least once as a
`data-testid` string literal in `src/`. Reuse the AST collection the undeclared-id ban already performs, so
both directions read one scan. Then assert the two directions are **exact complements** — the set of ids
found in `src/` equals `SELECTOR_IDS` — which turns both a missing hook and an ad-hoc one into named
failures.

Update that file's NOT COVERED footer: the gap it named is now closed. Leaving a stale "not covered" beside
a covered thing is precisely the drift this phase exists to end (Shared Pattern 10).

Push and confirm all three CI jobs plus the visual step are green.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!y.includes('--project=visual'))process.exit(1);if(y.includes('--update-snapshots'))process.exit(1);if(!y.includes('127.0.0.1:59999'))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` job 1 runs in `mcr.microsoft.com/playwright:v1.60.0-noble` with `--ipc=host`, keeps the unreachable `DATABASE_URL` on `npm run build`, and runs `npx playwright test --project=visual`
    - `--update-snapshots` appears **only** in `baselines.yml`, never in `ci.yml`
    - `tests/design/selector-contract.test.ts` now asserts both directions and that they are exact complements over all 17 ids
    - Watched red (forward): temporarily remove `data-testid="panel-card"` from `patterns/panel-card.tsx` → the forward assertion fails naming the id and its `owner` plan. Reverted. Both runs recorded in the header.
    - The NOT COVERED footer is updated; it no longer names an uncovered forward direction
    - All three CI jobs green on `origin/dev`, with job 1's visual step comparing against the committed Linux baselines
  </acceptance_criteria>
  <done>Every push now compares against baselines shot by the same runner, and the selector contract is closed in both directions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: The two OBSERVED REDs — a missing baseline, and a few-pixel shift</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - tests/use-server-exports.test.ts:25-62 (the OBSERVED RED header format) and e2e/scroll-area-overflow.spec.ts:59-109 (the browser variant, which adds a measurement table and a GREEN RUN line — copy that variant here)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md, GATE-01 Findings 3, 4 and 6 (the MEASURED behaviour these records must describe, which is not D-135's wording)
    - playwright.config.ts (plan 11-03's unconditional `updateSnapshots: "none"` — the reason the failure stays red across a bare re-run)
    - .github/workflows/ci.yml (where both records are committed)
  </read_first>
  <action>
    Trigger each failure mode deliberately, exactly once, capture the verbatim output, revert, and commit
    both records in `.github/workflows/ci.yml`'s header block. These are manual because automating them means
    deliberately deleting a committed baseline in CI, at which point the gate is testing its own harness.
    For proof 1, the load-bearing extra data point is the **bare re-run that stays red** — under the default
    `"missing"` the same situation writes the PNG and the next run is green with no code change, so the
    re-run is what demonstrates D-28 closed the fail-open. Do NOT tune any threshold for proof 2: Finding 6
    measured a 2px shift as detected at defaults, and a tuned threshold is a gate quietly reduced.
  </action>
  <what-built>Baselines exist, CI compares against them, and the selector contract is closed. What remains is D-30's proof: this gate must be watched failing, twice, by kind, with the verbatim output committed in the workflow file's header. Both are triggered deliberately once and then reverted. They are manual because automating them means deliberately deleting a committed baseline in CI, at which point the gate is testing its own harness.</what-built>
  <how-to-verify>
    **Proof 1 — a missing baseline fails.**
    1. Record the context the header needs: `git rev-parse --short HEAD` and a clean `git status --short`.
    2. Delete exactly one committed baseline PNG (pick `/terms` at 1280 in court — a surface with no
       dependencies), commit and push to `dev`.
    3. Watch the `ci` run. Capture the **verbatim** failing output of job 1's visual step and its exit code.
    4. ⚠ **Record the MEASURED behaviour, not D-135's wording.** Under plan `11-03`'s
       `updateSnapshots: "none"`, expect: the error **without** a `, writing actual.` clause, **zero** PNGs
       written, and the failure **retriable** — so it stays red across all attempts and across a bare
       re-run. That is the whole reason D-28 dropped the ternary: under the default `"missing"` the same
       situation writes the PNG and the next run is green with no code change. Re-run the job once with no
       change and record that it is **still red** — that second data point is the proof the fail-open is
       closed, and it is the one a reviewer will actually care about.
    5. Restore the deleted baseline (`git revert` or re-dispatch), push, and confirm `ci` is green again.

    **Proof 2 — a few-pixel shift fails.**
    6. Nudge one padding token by 2px — e.g. change the footer container's `py-8` to `py-8 pt-[calc(2rem+2px)]`
       or an equivalent single-value shift on a baselined surface. Commit and push.
    7. Capture the verbatim diff output. Expect the Finding-6 shape: a pixel count and a ratio, e.g.
       `249 pixels (ratio 0.01 of all image pixels) are different.` No threshold tuning is needed or allowed.
    8. Revert the nudge, push, and confirm green.

    **Commit both records.** Put them in `.github/workflows/ci.yml`'s header block, in the
    `tests/use-server-exports.test.ts:25-62` format, with the `e2e/scroll-area-overflow.spec.ts:59-109`
    variant's **GREEN RUN** line for the restored state. Each record carries: the HEAD sha and a clean
    `git status`, the exact command or run URL, the exit code, the verbatim output, and a sentence naming
    what did **not** fail — for proof 1, that the other 26 baselines compared clean, which is what makes this
    a gate rather than a scanner that fails on everything.
    9. Confirm `npm run test:design` still passes and `git ls-files "*-win32.png" "*-darwin.png"` is empty.
  </how-to-verify>
  <resume-signal>Reply "recorded" once both OBSERVED RED blocks (each with its GREEN RUN line) are committed in `.github/workflows/ci.yml`'s header, CI is green, and the restored baseline set is intact. Paste the two run URLs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository contents | The dispatch job pushes commits with `GITHUB_TOKEN` |
| author's machine → baseline artifacts | A locally-generated PNG committed here becomes the reference every future run compares against |
| baseline comparison → reviewer trust | A gate that goes green on a bare re-run trains reviewers to press the button again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CIWRITE | Elevation of Privilege | the dispatch job's `GITHUB_TOKEN` | mitigate | `permissions: contents: write` scoped to that job alone; every other job stays `contents: read`; the setting is human-verified before the first dispatch |
| T-11-BASEMINT | Tampering | baseline generation | mitigate | `--update-snapshots` exists only in `baselines.yml`, asserted by a zero-count over `ci.yml`; config remains `"none"` unconditionally, which writes zero PNGs on any machine (Finding 4) |
| T-11-PLATBASE | Tampering | committed baselines | mitigate | Only `*-visual-linux.png` may land; `git ls-files "*-win32.png" "*-darwin.png"` empty is checked after the pull, and plan 11-03's standing test asserts it on every design run |
| T-11-UNVERIFIED | Repudiation | the committed baseline set | mitigate | A `GITHUB_TOKEN` push triggers no workflow, so an explicit follow-up run is a required checkpoint step — otherwise D-27 produces baselines nobody has ever seen pass (Pitfall 3) |
| T-11-CANCEL | Denial of Service | the dispatch run | mitigate | `github.event_name` in the concurrency group, so a push cannot cancel a running baseline generation and leave a partial commit (Pitfall 4) |
| T-11-RUBBERSTAMP | Repudiation | the VR gate itself | mitigate | Both dynamic failure modes are triggered once and recorded verbatim, including the re-run-still-red data point that distinguishes `"none"` from the default `"missing"` fail-open |
| T-11-SC | Tampering | third-party Actions / npm | mitigate | Only first-party actions (`actions/checkout`, `actions/setup-node`) and the two verified images; zero npm packages |
</threat_model>

<verification>
- 27 `*-visual-linux.png` baselines committed; `git ls-files "*-win32.png" "*-darwin.png"` empty
- All three CI jobs plus job 1's visual step green on `origin/dev`
- Both OBSERVED RED blocks committed in `.github/workflows/ci.yml`'s header, each with its GREEN RUN line
- `npm run test:design` green, including the selector contract's two complementary directions
- `npm run build` exits 0 with an unreachable `DATABASE_URL`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql` (GATE-06 — zero migrations across the phase)
</verification>

<success_criteria>
- GATE-01 holds: baselines exist, are Linux-only, are generated by the runner that compares them, and the gate has been watched failing in both of its dynamic modes
- The recorded failure describes Playwright 1.60.0's measured behaviour, not D-135's assumed one
- GATE-04 is closed in both directions
- The phase's own standard is met: no gate here has silently never failed
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-22-SUMMARY.md` when done
</output>
