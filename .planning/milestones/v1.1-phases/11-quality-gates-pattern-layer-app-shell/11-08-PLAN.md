---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 08
type: execute
wave: 4
depends_on: [11-07]
files_modified:
  - src/components/patterns/result-card.tsx
  - src/components/patterns/row-card.tsx
  - src/components/patterns/panel-card.tsx
  - src/components/patterns/page-header.tsx
autonomous: true
requirements: [DS-11]

must_haves:
  truths:
    - "Three named card patterns exist prop-complete, so no later phase re-decides padding, radius or hover — a fourth is a scope alarm (DS-11)"
    - "All three compose ui/card.tsx through its public props; none forks it and none hand-rolls a container, so the shadcn collision surface does not grow"
    - "Every pattern is a Server Component, so composing one cannot drag a surface toward the client boundary"
    - "PanelCard's sticky prop encodes lg:top-20, never lg:top-8 — 64px header + 16px gap"
  artifacts:
    - path: "src/components/patterns/result-card.tsx"
      provides: "The marketplace tile — whole-card Link, AspectRatio media, tabular-nums price, container queries for internals"
    - path: "src/components/patterns/row-card.tsx"
      provides: "The list row — overlay-pseudo-element link, 48px media, actions as siblings not nested"
    - path: "src/components/patterns/panel-card.tsx"
      provides: "The boxed panel with title/description/footer/sticky/tone props"
    - path: "src/components/patterns/page-header.tsx"
      provides: "h1 + lede + optional actions, inline in ~10 pages today"
  key_links:
    - from: "src/components/patterns/result-card.tsx"
      to: "src/lib/design/measurements.ts"
      via: "RESULT_CARD_MEDIA, so the card and its skeleton share one ratio"
      pattern: "RESULT_CARD_MEDIA"
    - from: "src/components/patterns/*.tsx"
      to: "src/components/ui/card.tsx"
      via: "composition through public props, never a fork"
      pattern: "from \"@/components/ui/card\""
---

<objective>
Build the three card patterns and the page header, prop-complete, before any surface adopts them.

Purpose: DS-11 exists because the app re-decides padding, radius and hover per card surface today. The
patterns must be complete enough that Phases 12–15 adopt rather than extend them — and Phase 12 is
sequenced first among {12,13,14,15} precisely so a wrong pattern is cheaper to learn there than after
three phases have adopted it.
Output: four Server Components under `src/components/patterns/`, adopted by nobody yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
Shared and inherited from ui/card.tsx, unchanged: rounded-xl (= --radius-xl = calc(--radius × 1.4) →
14px court / 28px grove, so radius travels per theme), bg-card, ring-1 ring-foreground/10 (now a declared
exclusion at 1.24), text-card-foreground.

ResultCard props: href, media, mediaFallback, title, meta: ReactNode[], price (tabular-nums MANDATORY),
                  badges (inline on the type line — NEVER overlaid on the photo)
RowCard props:    href (overlay pseudo-element, not a wrapping anchor), media, title, meta, status,
                  trailing, actions (siblings of the overlay link, never nested inside it)
PanelCard props:  title, description, footer, sticky = false, tone: "default" | "muted" = "default"
PageHeader props: title, lede, actions

Declared ids (plan 11-02's inventory; any other string fails its undeclared-id ban):
  result-card · row-card · panel-card · page-header
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultCard — the marketplace tile</name>
  <files>src/components/patterns/result-card.tsx</files>
  <read_first>
    - src/components/search/search-result-card.tsx:166-276 (the exact analog — copy the whole-card `<Link>` and its focus recipe verbatim, including the comment at :168-172 explaining why the offset COLOUR is load-bearing: Tailwind's default offset is a hardcoded white and paints a visible halo on grove's tinted background)
    - src/components/search/search-result-card.tsx:191-194 (why badges are inline on the type line and never overlaid on the photo)
    - src/components/ui/card.tsx (Card, CardContent — the public props to compose through)
    - src/components/ui/aspect-ratio.tsx
    - src/lib/design/measurements.ts (RESULT_CARD_MEDIA)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § ResultCard
  </read_first>
  <action>
Create `src/components/patterns/result-card.tsx` as a Server Component (no `"use client"`).

The whole card is one `<Link href={href}>` carrying the canonical DS-05 focus recipe copied byte-for-byte
from `search-result-card.tsx`: `group block rounded-xl outline-none focus-visible:ring-2
focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`. Carry its comment
across — the explicit offset colour is not decoration.

Inside: `<Card className="h-full gap-0 overflow-hidden pt-0 transition-shadow group-hover:bg-muted/40
group-hover:shadow-overlay">`, then `<AspectRatio ratio={4/3} className="bg-muted">` rendering `media` or
`mediaFallback` (never a broken image), then `<CardContent className="space-y-1 py-4">` with title
(`font-semibold leading-snug`), the `meta` nodes in order, `badges` inline on the type line, and `price`
with `tabular-nums` — mandatory, and typed so a caller cannot omit it silently. Use `RESULT_CARD_MEDIA`
from `measurements.ts` wherever the ratio is expressed as a class so the card and `CardGridSkeleton` share
one source.

Author the card's **internals** with container queries, not viewport breakpoints: put `@container` on the
card root and use `@max-[20rem]:` / `@sm:` inside. The card renders at ~330px in a 3-up grid, full width at
320px, and a third width inside a "You might also like" row — a viewport breakpoint gets all three wrong.
The *grid* that lays cards out still uses viewport breakpoints; say so in a comment.

`data-testid="result-card"`. Do not overlay badges on the photo: contrast over arbitrary host photography
is unverifiable in both themes, and the reason is already recorded at `search-result-card.tsx:191-194`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts tests/design/focus-recipe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `"use client"` in the file; it imports nothing from `@/lib/{booking,payments,listing,group,search,availability}`
    - The focus recipe string is byte-identical to `search-result-card.tsx`'s, including `focus-visible:ring-offset-background`
    - `data-testid="result-card"` appears exactly once
    - The `price` prop is required (not optional) and its render carries `tabular-nums`
    - `@container` appears on the card root and every internal responsive utility is a container query, not a `sm:`/`lg:` viewport breakpoint
    - `tests/design/focus-recipe.test.ts` and `tests/design/leak.test.ts` pass — no raw design value, no 50%-alpha ring
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The tile exists with the shipped focus and hover behaviour preserved exactly, and its internals respond to their container.</done>
</task>

<task type="auto">
  <name>Task 2: RowCard — the list row, with its declared DS-05 exception</name>
  <files>src/components/patterns/row-card.tsx</files>
  <read_first>
    - src/components/booking/booking-row.tsx:58-99 (the exact analog — the `<Card className="relative">` + `CardContent space-y-3 p-4` shell, the `size-12` media, and the overlay-link form `after:absolute after:inset-0 after:rounded-xl focus-visible:after:ring-2 focus-visible:after:ring-ring`, plus the comment at :81-83 explaining why the inline CTA stays a sibling)
    - src/components/host/host-booking-row.tsx:81 (the same overlay form, second site)
    - src/lib/design/measurements.ts (ROW_CARD_HEIGHT = "h-20", ROW_CARD_THUMB = "size-12")
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § RowCard (the declared ring-offset exception and its measured 7.46 / 7.36 justification)
  </read_first>
  <action>
Create `src/components/patterns/row-card.tsx` as a Server Component.

Root `<Card className="relative">` + `<CardContent className="space-y-3 p-4">`. Media slot at
`ROW_CARD_THUMB` (`size-12`), `shrink-0 overflow-hidden rounded-md bg-muted`. Title `truncate text-sm
font-semibold`, `meta` beneath it, `status` top-right, `trailing` below status right-aligned with
`tabular-nums` when the caller passes a number.

Navigation is an **overlay pseudo-element**, never a wrapping anchor:
`after:absolute after:inset-0 after:rounded-xl focus-visible:after:ring-2 focus-visible:after:ring-ring` on
the title link. `actions` render as **siblings** of that link, never nested inside it — a button inside an
anchor is invalid HTML and swallows its own click.

**The missing `ring-offset` is a declared exception to DS-05, not a defect to fix.** An overlay
pseudo-element filling its container has no surface to offset into, and `ring on card` measures 7.46 / 7.36
against a 3.0 bar, so the indicator clears without the offset band. Record that in a comment with the two
numbers; adding an offset would render a 2px band inside the card's own content.

The row's resting height at the 48px-media configuration is `ROW_CARD_HEIGHT` — reference the constant in a
comment so `RowListSkeleton` and this component are provably reading the same number rather than agreeing
by coincidence.

`data-testid="row-card"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run --config vitest.design.config.ts tests/design/focus-recipe.test.ts tests/design/selector-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `"use client"`; no domain imports
    - The overlay-link class string matches `booking-row.tsx:79`'s form and contains no `ring-offset`
    - A comment records the 7.46 / 7.36 measurement and the reason the offset is absent
    - `actions` render outside the overlay link in the DOM (assert by reading the JSX structure, not by rendering)
    - `data-testid="row-card"` appears exactly once
    - `tests/design/focus-recipe.test.ts` passes with the new file in tree
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The list row exists with its shipped overlay-link behaviour and its exception documented rather than silently carried.</done>
</task>

<task type="auto">
  <name>Task 3: PanelCard and PageHeader</name>
  <files>src/components/patterns/panel-card.tsx, src/components/patterns/page-header.tsx</files>
  <read_first>
    - src/components/ui/card.tsx:5-21 (Card) and :82-92 (CardFooter — already `border-t bg-muted/50 p-4`, which is exactly the UI-SPEC's `footer` prop treatment)
    - src/app/listings/[id]/page.tsx:346 (the shipped `<Card className="lg:sticky lg:top-8">` rail — the offset PanelCard's `sticky` prop corrects to `lg:top-20`)
    - src/app/(app)/bookings/loading.tsx:12-15 (the shipped `h1` + lede shape PageHeader formalises)
    - src/lib/design/measurements.ts (PANEL_MIN_HEIGHT, HEADER_HEIGHT — the 80px offset is 64 + 16)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § PanelCard and § Supporting patterns
  </read_first>
  <action>
`src/components/patterns/panel-card.tsx`, Server Component. Composes `Card` with padding `p-4 sm:p-6`.
`title` renders `text-heading`; `description` renders `text-sm text-muted-foreground`; `footer` renders
through `CardFooter` (its shipped `border-t bg-muted/50` is already correct — do not restate it).
**Elevation: none at rest.** A panel is flat; `shadow-raised` is for lifted surfaces and the card already
carries its hairline ring — put that sentence in the file so nobody "improves" it later.

`sticky?: boolean` (default `false`) applies `lg:sticky lg:top-20`. **20, not 8**, and the comment must say
why: the new sticky site header is 64px at `sm:` and up, plus a 16px gap; the shipped `lg:top-8` (32px)
would tuck the rail under the header on every scroll. State the general rule once, here: *every `lg:sticky`
offset in the app is ≥ header height + 16px.*

`tone?: "default" | "muted"` (default `"default"`); `"muted"` applies `bg-muted`, which is the treatment the
legal placeholder notice and in-page advisories use — the `neutral` status tone at panel scale, zero new
tones and zero new pairings.

`data-testid="panel-card"`.

`src/components/patterns/page-header.tsx`, Server Component. Props `{ title: string; lede?: string;
actions?: ReactNode }`. Renders an `<h1>` plus optional lede paragraph and a right-aligned actions cluster,
matching the shape already inlined in ~10 pages (`bookings/loading.tsx:12-15` is the cleanest instance).
`data-testid="page-header"`. Wrap at 320px rather than truncating — a page title is not a row title.

Neither file contains product copy: `patterns/` takes `title`/`lede`/`description` as props and never owns
a sentence.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains `"use client"` or a domain import
    - `grep -c "lg:top-20" src/components/patterns/panel-card.tsx` returns 1; `grep -c "lg:top-8" src/components/patterns/panel-card.tsx` returns 0
    - PanelCard renders no `shadow-` utility at rest
    - `tone="muted"` maps to `bg-muted` and introduces no new colour pairing (the `foreground on muted` and `muted-foreground on muted` pairs are already declared)
    - `data-testid="panel-card"` and `data-testid="page-header"` each appear exactly once, and plan 11-02's undeclared-id assertion passes
    - `npm run build` exits 0 and `npm run test:design` is green
  </acceptance_criteria>
  <done>All four patterns exist, prop-complete, adopted by nobody — the adoption plans can now be pure swaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pattern layer ↔ client boundary | A `"use client"` pattern drags every composing surface toward the browser |
| pattern layer ↔ vendored shadcn tree | A fork here grows the `npx shadcn add` collision surface permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CLIENTCREEP | Information Disclosure | `src/components/patterns/**` | mitigate | All four are Server Components with no domain imports, asserted per file; this is the specific reason the UI-SPEC makes `patterns/` server-by-default, and it is what keeps plan 11-01's boundary fix from regressing |
| T-11-FORK | Tampering | `src/components/ui/**` | mitigate | Composition through public props only; zero files under `src/components/ui/**` are edited by this plan, keeping Phase 10's ~15-file shadcn collision surface from growing |
| T-11-FOCUSLOSS | Denial of Service (accessibility) | ResultCard / RowCard focus | mitigate | ResultCard copies the DS-05 recipe including the explicit offset colour; RowCard's offset-less overlay form is a declared exception carrying its measured 7.46 / 7.36 justification |
| T-11-STICKYTUCK | Denial of Service | PanelCard `sticky` | mitigate | `lg:top-20` encoded in the prop, so no surface re-derives the offset; the general rule is stated once in the file |
| T-11-SC | Tampering | shadcn registry | mitigate | No new block is fetched; `components.json` `registries: {}` is unchanged. `npx shadcn add sheet` is a recorded-decision reversal to raise, not absorb. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0 (lint + 458+ design assertions + Turbopack)
- `npm run test:design` green — in particular `focus-recipe`, `leak`, `type-scale`, `elevation-z` and `selector-contract`
- `git status --short src/components/ui/` is empty
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- Three card patterns and one page header exist, prop-complete, with no adopters yet
- Every declared prop from the UI-SPEC is present; nothing was deferred to "when a surface needs it"
- No vendored primitive was edited and no registry block was fetched
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-08-SUMMARY.md` when done
</output>
