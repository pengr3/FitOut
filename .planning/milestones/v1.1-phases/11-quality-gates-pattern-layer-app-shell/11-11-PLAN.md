---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 11
type: execute
wave: 5
depends_on: [11-08]
files_modified:
  - src/components/search/search-result-card.tsx
  - src/components/listing/listing-card.tsx
  - src/app/(host)/host/listings/page.tsx
  - src/components/booking/booking-row.tsx
  - src/components/host/host-booking-row.tsx
  - src/components/host/request-row.tsx
  - src/components/host/payout-row.tsx
  - src/components/notifications/notification-item.tsx
autonomous: true
requirements: [DS-11]

must_haves:
  truths:
    - "Every marketplace tile and every list row in the app renders through ResultCard or RowCard — the containers stop re-deciding padding, radius and hover locally (DS-11)"
    - "Domain logic stays where it is: only the CONTAINER moves, so no money, status or availability computation changes hands"
    - "The shipped hover, focus and overlay-link behaviours are preserved byte-for-byte, because they are what the two new contrast rows measure"
  artifacts:
    - path: "src/components/search/search-result-card.tsx"
      provides: "The search tile rendered through ResultCard, keeping its server-formatted allInRateParts"
    - path: "src/components/booking/booking-row.tsx"
      provides: "The booker list row rendered through RowCard"
    - path: "src/components/host/request-row.tsx"
      provides: "The host request row rendered through RowCard, with its SLA and actions untouched (Phase 14 owns the redesign)"
  key_links:
    - from: "src/components/search/search-result-card.tsx"
      to: "src/components/patterns/result-card.tsx"
      via: "composition — the domain component supplies props, the pattern owns the box"
      pattern: "ResultCard"
    - from: "src/components/booking/booking-row.tsx"
      to: "src/components/patterns/row-card.tsx"
      via: "composition"
      pattern: "RowCard"
---

<objective>
Adopt `ResultCard` and `RowCard` across the seven shipped card surfaces the UI-SPEC names, container-only.

Purpose: AC#25 requires every card surface to render one of the three declared containers. This plan closes
the tile and row families; PanelCard's five surfaces are plan `11-13`.
Output: seven components composing the patterns, with their domain logic byte-unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
UI-SPEC's "Replaces" lists, verbatim:
  ResultCard replaces  search/search-result-card.tsx, listing/listing-card.tsx (presentational core)
  RowCard    replaces  booking/booking-row.tsx, host/host-booking-row.tsx, host/request-row.tsx,
                       host/payout-row.tsx, notifications/notification-item.tsx  (CONTAINERS ONLY —
                       the domain logic stays)

Boundary reminder — this phase does NOT redesign these surfaces. Search/listing polish is Phase 12; the host
inbox, dashboard and wizard are Phase 14. This plan swaps the container and nothing else.

Plan 11-01 already moved `listing-card.tsx`'s price arithmetic server-side; it now receives
`priceParts: string[]` pre-formatted. Do not reintroduce arithmetic here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultCard adoption — the two tile surfaces</name>
  <files>src/components/search/search-result-card.tsx, src/components/listing/listing-card.tsx, src/app/(host)/host/listings/page.tsx</files>
  <read_first>
    - src/components/patterns/result-card.tsx (plan 11-08's output — the prop contract)
    - src/components/search/search-result-card.tsx (the whole file: :122-139 the pre-formatted-price reasoning, :166-276 the markup that becomes props, :191-194 the badges-on-the-type-line reason, :247-252 the tabular-nums render)
    - src/components/listing/listing-card.tsx (the whole file as plan 11-01 left it — `priceParts` arrives as a prop; note its Dialog composition at :31-41, which stays)
    - src/app/(host)/host/listings/page.tsx (the server caller plan 11-01 taught to compose `allInRateParts`)
    - src/lib/design/measurements.ts (RESULT_CARD_MEDIA)
  </read_first>
  <action>
Rewrite both tiles to compose `ResultCard` instead of re-declaring a `Card` + `AspectRatio` + focus recipe.
Each domain component keeps its data mapping and passes `href`, `media`, `mediaFallback`, `title`, `meta`,
`price` and `badges`.

Delete the now-duplicated geometry from both files — the whole-card `<Link>` and its focus recipe, the
`Card` className string, the `AspectRatio` wrapper, the hover classes. Those live in the pattern now. What
must NOT move is the reasoning comment at `search-result-card.tsx:122-139`: keep it on the line that reads
`listing.allInRateParts`, because it explains a money-boundary property, not a layout one.

`listing-card.tsx` keeps its `"use client"` directive and its Dialog composition — the tile's presentational
core is what `ResultCard` replaces, not the component's interactive behaviour. If after the swap the file no
longer needs `"use client"`, remove it and say so in the SUMMARY; do not force it either way.

Badges stay inline on the type line. Do not overlay them on the photo — contrast over arbitrary host
photography is unverifiable in both themes, and `search-result-card.tsx:191-194` already records that.

`data-testid="result-card"` comes from the pattern; do not add a second one at the call site.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/search && npx playwright test e2e/search-and-book.spec.ts e2e/public-listing.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Both files import `ResultCard` from `@/components/patterns/result-card`
    - Neither file declares a `Card` className, an `AspectRatio` wrapper, or a `focus-visible:ring-` recipe of its own
    - `grep -c "data-testid=\"result-card\"" src/components/search/search-result-card.tsx src/components/listing/listing-card.tsx` returns 0 for both (the pattern owns the id)
    - `grep -c "allInRateParts\|computeServiceFee" src/components/listing/listing-card.tsx` returns 0 — plan 11-01's boundary fix is not regressed
    - `npx vitest run tests/search` passes, including `tests/search/search-card-open.test.tsx`
    - `e2e/search-and-book.spec.ts` and `e2e/public-listing.spec.ts` pass — their `getByRole`/`getByText` selectors are unchanged, which is GATE-04's floor doing its job
    - `npm run build` exits 0 and `npm run test:design` is green
  </acceptance_criteria>
  <done>Both tile surfaces render through one container, with their money boundary and accessible selectors intact.</done>
</task>

<task type="auto">
  <name>Task 2: RowCard adoption — the five list-row surfaces</name>
  <files>src/components/booking/booking-row.tsx, src/components/host/host-booking-row.tsx, src/components/host/request-row.tsx, src/components/host/payout-row.tsx, src/components/notifications/notification-item.tsx</files>
  <read_first>
    - src/components/patterns/row-card.tsx (plan 11-08's output — the prop contract and the declared no-ring-offset exception)
    - src/components/booking/booking-row.tsx:58-99 (the canonical shipped shape, including the comment at :81-83 on why the inline CTA stays a sibling of the overlay link)
    - src/components/host/host-booking-row.tsx:81 (the second overlay-link site) and :121 (its descriptive comment about `relative z-10` — the convention `booking-row.tsx:111` established)
    - src/components/host/request-row.tsx (its `APPROVAL_PAYMENT_WINDOW_HOURS` import at :34 is a LEGITIMATE client import of a lead-time constant — do not touch it)
    - src/components/host/payout-row.tsx and src/components/notifications/notification-item.tsx (the two surfaces whose containers differ most from the canonical shape — note the differences in the SUMMARY rather than normalising behaviour)
    - src/lib/design/measurements.ts (ROW_CARD_HEIGHT, ROW_CARD_THUMB)
  </read_first>
  <action>
Rewrite each of the five to compose `RowCard`, passing `href`, `media`, `title`, `meta`, `status`,
`trailing` and `actions`. Delete each file's own `<Card className="relative">` + `CardContent` +
`size-12` media wrapper + overlay-link class string; those are the pattern's now.

**Container only.** Every status computation, SLA countdown, money formatting, approve/decline handler and
notification read-state rule stays exactly where it is. `request-row.tsx` in particular is Phase 14's
surface — this plan changes its box and nothing else, and the SLA countdown must remain the element it is
today.

`actions` must render as **siblings** of the overlay link, never nested inside it. The pattern enforces the
DOM position; the call site must not wrap an action in an anchor to compensate.

Do not add `ring-offset` to the overlay focus form. Its absence is a declared DS-05 exception carrying a
measured 7.46 / 7.36 justification, recorded in `row-card.tsx`.

Money figures keep `tabular-nums` (AC#33). If a row loses it in the swap, that is a regression, not a
simplification.

Where a surface's container genuinely differs from the canonical shape (notification items are not
navigational rows in the same sense), pass the props that fit and record the difference in the SUMMARY. Do
**not** add a fourth container shape — DS-11 says three, and a fourth is a scope alarm to raise.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/booking tests/notifications 2>/dev/null || npx vitest run tests/booking</automated>
  </verify>
  <acceptance_criteria>
    - All five files import `RowCard` from `@/components/patterns/row-card`
    - `grep -c "after:absolute after:inset-0" src/components/booking/booking-row.tsx src/components/host/host-booking-row.tsx` returns 0 for both (the pattern owns the overlay form)
    - `grep -rc "ring-offset" src/components/patterns/row-card.tsx` returns 0 and no call site adds one
    - `grep -c "tabular-nums" ` on each file is >= its pre-plan count
    - No file declares a `<Card` with its own className string
    - `request-row.tsx` still imports `APPROVAL_PAYMENT_WINDOW_HOURS` from `@/lib/payments/config` and still renders its SLA countdown as the same element
    - `npm test` passes with the same counts as the phase baseline
    - `npx playwright test e2e/cancel.spec.ts e2e/open-capacity.spec.ts --project=chromium` passes — the row surfaces the shipped specs select against
    - `npm run build` exits 0; `npm run test:design` green; push and CI green (D-25)
  </acceptance_criteria>
  <done>Five list surfaces share one row container, with every domain behaviour and accessible selector unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container swap ↔ e2e selector surface | A restyle can silently break the specs that prove the double-booking guarantee |
| container swap ↔ money boundary | Reintroducing a computation at a call site would undo plan 11-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SELDOWN | Tampering | `e2e/**` accessible queries | mitigate | GATE-04's floor runs inside `npm run build`; the shipped specs that select these surfaces (`search-and-book`, `public-listing`, `cancel`, `open-capacity`) are re-run as acceptance criteria |
| T-11-FEELEAK | Information Disclosure | `listing-card.tsx` | mitigate | An explicit zero-count grep for `allInRateParts`/`computeServiceFee`, so the container swap cannot walk plan 11-01's fix backwards |
| T-11-NESTEDACTION | Denial of Service (accessibility) | RowCard adopters | mitigate | Actions are siblings of the overlay link; a button inside an anchor is invalid HTML and swallows its own click |
| T-11-FOURTH | Repudiation | container inventory | mitigate | A fourth container shape is a scope alarm to raise explicitly; surfaces that do not fit pass the props that do and record the difference |
| T-11-SC | Tampering | npm / shadcn registry | mitigate | Zero packages, zero registry blocks, zero edits under `src/components/ui/**` |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm test` and `npm run test:design` green
- `npm run test:e2e` retains the phase-baseline signature
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- Seven card surfaces render through two declared containers
- No domain logic moved and no accessible selector count dropped
- No fourth container shape was invented
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-11-SUMMARY.md` when done
</output>
