---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 07
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - src/lib/design/measurements.ts
  - src/lib/design/contrast-pairs.ts
  - src/components/patterns/card-grid-skeleton.tsx
  - src/components/patterns/row-list-skeleton.tsx
  - src/components/patterns/panel-skeleton.tsx
  - tests/design/skeleton-measurements.test.ts
  - tests/design/skeleton-a11y.test.tsx
  - tests/design/contrast.test.ts
  - tests/design/pair-drift.test.ts
autonomous: true
requirements: [STATE-01, DS-11]

must_haves:
  truths:
    - "Skeleton geometry is declared once in src/lib/design/measurements.ts and read by both the skeleton and the real content, so 'does not shift' is mechanical rather than intended"
    - "A skeleton that hardcodes its own box fails a source gate — watched red by inlining one constant"
    - "Every skeleton renders exactly one role=status with a non-empty accessible name, which is what makes the 1.09:1 muted fill a legal declared exclusion rather than a WCAG 1.4.11 failure"
    - "contrast-pairs.ts gains exactly 2 CONTRAST_PAIRS rows and 3 EXCLUDED_PAIRS rows, each with a stated reason, and the culori gate passes on the full set in both themes"
  artifacts:
    - path: "src/lib/design/measurements.ts"
      provides: "RESULT_CARD_MEDIA, ROW_CARD_HEIGHT, ROW_CARD_THUMB, HEADER_HEIGHT, AUTH_SLOT_BOX, PANEL_MIN_HEIGHT — the one source of skeleton box classes"
    - path: "src/components/patterns/card-grid-skeleton.tsx"
      provides: "The 6-card grid skeleton shared by /'s loading.tsx and search-results.tsx's in-component pending state"
    - path: "src/components/patterns/row-list-skeleton.tsx"
      provides: "The 4-row list skeleton (app)/bookings/loading.tsx migrates onto"
    - path: "tests/design/skeleton-a11y.test.tsx"
      provides: "The jsdom-pragma render gate for the mandatory role=status wrapper"
  key_links:
    - from: "src/components/patterns/*skeleton*.tsx"
      to: "src/lib/design/measurements.ts"
      via: "imported box-class constants, zero literal h-/w-/aspect- utilities"
      pattern: "measurements"
    - from: "tests/design/contrast.test.ts"
      to: "src/lib/design/contrast-pairs.ts"
      via: "the culori gate over the extended row set"
      pattern: "CONTRAST_PAIRS"
---

<objective>
Declare the geometry the four state families are measured against, extend the contrast inventory by the
five rows this phase actually renders, and build the three skeleton shapes every later `loading.tsx` reads.

Purpose: STATE-01's requirement is not "a skeleton exists" — it is *"whose skeleton is built from the same
measurement constants as the real content, so nothing shifts on arrival."* A constant in one module is what
makes that true; a matching literal in two files is what makes it drift.
Output: `measurements.ts`, +2/+3 contrast rows, three skeleton patterns, and two gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md

<interfaces>
The six constants, from 11-UI-SPEC § Loading — values, not inventions:

  RESULT_CARD_MEDIA  = "aspect-[4/3]"   // matches AspectRatio ratio={4/3}
  ROW_CARD_HEIGHT    = "h-20"           // 80px: Card p-4 around a 48px thumb
  ROW_CARD_THUMB     = "size-12"        // 48px
  HEADER_HEIGHT      = "h-14 sm:h-16"
  AUTH_SLOT_BOX      = "h-8 min-w-44"
  PANEL_MIN_HEIGHT   = "min-h-40"

The five contrast rows, measured in the UI-SPEC session with gamma-space alpha compositing (the gate is
the authority if it disagrees):

  EXCLUDED_PAIRS  muted / background        1.09 / 1.09   skeleton fill
  EXCLUDED_PAIRS  muted / card              1.09 / 1.13   skeleton fill inside a card or panel
  EXCLUDED_PAIRS  foreground-10 / card      1.24 / 1.24   ui/card.tsx:15's ring-1 ring-foreground/10 —
                                                          the hairline on EVERY card, shipped since v1.0
                                                          and measured by nothing
  CONTRAST_PAIRS  foreground / muted        alpha {value: 0.4, over: "background"}  bar 4.5  19.42 / 17.80
  CONTRAST_PAIRS  muted-foreground / muted  alpha {value: 0.4, over: "background"}  bar 4.5   5.08 /  5.56

  ⚠ `over: "background"`, NOT `over: "card"`. `group-hover:bg-muted/40` REPLACES the Card's bg-card; it
  does not layer over it. Writing `over: card` measures a pairing that never renders — the same class of
  error that let the focus ring ship at 2.58 as a token pair and 1.54 as rendered.

`animate-pulse` is a continuous loading indicator and is EXEMPT by design from the 320ms motion cap
(Phase 10 wrote that exemption into the cap's own wording). It is covered by the global
prefers-reduced-motion reset. Do NOT give the pulse a duration token.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The measurement inventory and the five contrast rows</name>
  <files>src/lib/design/measurements.ts, src/lib/design/contrast-pairs.ts, tests/design/contrast.test.ts, tests/design/pair-drift.test.ts</files>
  <read_first>
    - src/lib/design/contrast-pairs.ts:11-14 (why these modules live at `src/lib/design/` — the leak gate scans `src/app/**` and `src/components/**` only, so a module naming `h-20` and `aspect-[4/3]` can be honest without a per-line exemption), :22-36 (the NOT COVERED footer and the EXCLUDED_PAIRS-with-reasons idiom), :99-116 (the `ContrastPair` type and the mandatory `note`)
    - src/lib/design/status-tones.ts:74-108 (the as-const inventory shape)
    - src/components/ui/card.tsx:15 (`ring-1 ring-foreground/10` — the hairline the third exclusion is about)
    - src/components/search/search-result-card.tsx (the shipped `group-hover:bg-muted/40 group-hover:shadow-overlay` that the two new CONTRAST_PAIRS rows measure)
    - tests/design/contrast.test.ts and tests/design/pair-drift.test.ts (both pin row counts — find every pinned number before editing)
  </read_first>
  <action>
Create `src/lib/design/measurements.ts` exporting the six constants above as `const` string literals, beside
`contrast-pairs.ts` and `status-tones.ts`. Each constant carries a one-line comment with its derivation
(e.g. `ROW_CARD_HEIGHT` = 80px because a Card is `p-4` around a 48px thumbnail — that reasoning already
exists as a comment at `src/app/(app)/bookings/loading.tsx:1-3` and must be moved here, not duplicated).
Add a header stating why the module lives outside the leak-gate tree, quoting `contrast-pairs.ts:11-14`'s
reason, and a NOT COVERED footer: this module declares box classes; it cannot tell you a component stopped
using them (that is `tests/design/skeleton-measurements.test.ts`'s job).

Extend `src/lib/design/contrast-pairs.ts` with exactly the five rows in `<interfaces>` — 3 `EXCLUDED_PAIRS`,
2 `CONTRAST_PAIRS` — each with a mandatory `note` giving the reason, not the measurement. For the two
skeleton-fill exclusions the note must state the compensating requirement explicitly: a skeleton is a
non-informational placeholder and the loading state's *meaning* is carried by a `role="status"` +
`aria-busy="true"` region with an `sr-only` label, never by the fill; without that wrapper this exclusion
becomes a real WCAG 1.4.11 failure. For the hairline exclusion, note that it is the identical class to the
already-declared `--border` / `--input` exclusions — a decorative container edge, never a control's sole
visible boundary and never a focus indicator — and that `fg` names the composite because the exclusion is
about the 10% form specifically.

The two `CONTRAST_PAIRS` rows use `alpha: { value: 0.4, over: "background" }`. Put the `over: "background"`
justification in the note verbatim, because it is the row's whole correctness.

Then find and update every pinned row count in `tests/design/contrast.test.ts` and
`tests/design/pair-drift.test.ts`. Record old → new for each in the SUMMARY. Do not "fix" a red by widening
an assertion: if the culori gate disagrees with a stated ratio, the gate wins and the ratio in the note is
corrected (`11-UI-SPEC.md` says this outright).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/contrast.test.ts tests/design/pair-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/design/measurements.ts` exports exactly the six named constants with the values in `<interfaces>`
    - `CONTRAST_PAIRS` grew by exactly 2 and `EXCLUDED_PAIRS` by exactly 3 (AC#34)
    - Every new row has a non-empty `note`; the two hover rows carry `over: "background"`
    - `npx vitest run --config vitest.design.config.ts` passes for `contrast.test.ts` and `pair-drift.test.ts` in **both** themes at `bar + 0.05`
    - Every pinned count changed is recorded as old → new in the SUMMARY
    - `npm run build` exits 0 (the DS-13 leak gate does not scan `src/lib/design/**`, so the literal class names here are legal — confirm rather than assume)
  </acceptance_criteria>
  <done>The geometry has one owner and the five pairings this phase renders are declared and measured.</done>
</task>

<task type="auto">
  <name>Task 2: The three skeleton patterns</name>
  <files>src/components/patterns/card-grid-skeleton.tsx, src/components/patterns/row-list-skeleton.tsx, src/components/patterns/panel-skeleton.tsx</files>
  <read_first>
    - src/app/(host)/host/listings/loading.tsx:10-18 (the shipped 6-card `aspect-[4/3]` grid — the exact analog for CardGridSkeleton)
    - src/app/(app)/bookings/loading.tsx (the whole 27-line file — the analog for RowListSkeleton, including the `aria-hidden="true"` on the pulse-bar region and the reasoning comment; note it has NO `role="status"` today and is therefore in violation of AC#18)
    - src/components/ui/skeleton.tsx (13 lines — `animate-pulse rounded-md bg-muted`; do not fork it, do not add a duration token)
    - src/lib/design/measurements.ts (Task 1's output)
    - src/lib/design/selector-contract.ts (the declared ids `skeleton-card-grid`, `skeleton-row-list`, `skeleton-panel` — any other string fails plan 11-02's undeclared-id ban)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Design System (the `patterns/` membership rules and the two hard rules — no `"use client"` unless the pattern's own behaviour requires it, and no product copy)
  </read_first>
  <action>
Create `src/components/patterns/` — it does not exist yet — and three Server Components in it. None of them
takes `"use client"`: a client pattern drags every surface that composes it toward the client boundary,
which is the D-130 hazard plan `11-01` just closed.

All three share one mandatory shell: `<div role="status" aria-busy="true" data-testid="skeleton-{shape}">`
with an `sr-only` child naming what is loading, and every pulse bar inside marked `aria-hidden="true"`.
The label is a required `label: string` prop — **no product copy lives in `patterns/`**, so the sentence
("Loading your bookings", "Loading spaces") is supplied by the surface. Exactly one `role="status"` per
component; nested skeletons must not nest status regions.

`card-grid-skeleton.tsx` — props `{ label: string; count?: number }` (default 6). Renders the
`host/listings/loading.tsx:10-18` grid: `grid gap-5 sm:grid-cols-2 lg:grid-cols-3`, each cell a
`RESULT_CARD_MEDIA` media block plus two text bars. **Every box class comes from `measurements.ts`** — the
`aspect-[4/3]` literal in the analog is exactly what `RESULT_CARD_MEDIA` replaces.

`row-list-skeleton.tsx` — props `{ label: string; rows?: number }` (default 4). Renders `rows` bars at
`ROW_CARD_HEIGHT`, `w-full rounded-xl`, `space-y-3`. Carry over the analog's reasoning comment by *reference*
to `measurements.ts` rather than restating the 80px derivation twice.

`panel-skeleton.tsx` — props `{ label: string }`. A single block at `PANEL_MIN_HEIGHT` with two or three
text bars, for the panel shape every new `loading.tsx` and the calendar day panel use.

Width bars (`w-3/4`, `w-1/2`) are proportional, not measurements, and stay literal — say so in a comment so
the Task-3 gate's exemption list is justified rather than convenient.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under `src/components/patterns/`; none contains `"use client"`
    - Each renders exactly one `role="status"` with `aria-busy="true"` and one `sr-only` label element
    - Each carries the declared `data-testid` (`skeleton-card-grid`, `skeleton-row-list`, `skeleton-panel`) and plan 11-02's undeclared-id assertion passes
    - `grep -o 'h-[0-9]\|w-[0-9]\|aspect-\[' src/components/patterns/*skeleton*.tsx | grep -v w-3/4 | grep -v w-1/2` returns nothing except the proportional width bars
    - No file imports anything from `@/lib/{booking,payments,listing,group,search,availability}` (the `patterns/` membership rule)
    - `npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Three domain-ignorant Server-Component skeletons exist, sized from one inventory and announced to screen readers.</done>
</task>

<task type="auto">
  <name>Task 3: The two skeleton gates — the source scan and the jsdom render</name>
  <files>tests/design/skeleton-measurements.test.ts, tests/design/skeleton-a11y.test.tsx</files>
  <read_first>
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker — a class name inside a comment must not be mistaken for a call site)
    - tests/design/theme-nesting-render.test.tsx (the `// @vitest-environment jsdom` per-file pragma; the design config stays `environment: "node"`)
    - tests/design/strip-comments.test.ts (the line-oriented comment stripper and why a `/*…*/` regex swallows 86 lines after `accept="image/*"`)
    - tests/use-server-exports.test.ts:315-318 and :320-369 (guard-the-guard and both-directions self-tests), :89-102 (NOT COVERED)
    - vitest.design.config.ts (both gates must be DB-free and must not add `globalSetup`/`setupFiles`)
  </read_first>
  <action>
**`tests/design/skeleton-measurements.test.ts`** (AC#16, source gate). Glob
`src/components/patterns/*skeleton*.tsx`. For each file assert (a) it imports at least one identifier from
`@/lib/design/measurements`, and (b) it contains **zero** literal `h-{n}` / `w-{n}` / `aspect-[…]` /
`size-{n}` / `min-h-{n}` utilities of its own, with the proportional fraction widths (`w-1/2`, `w-2/3`,
`w-3/4`) as the only declared exemptions, listed as data with their reason. Walk string literals via
`ts.createSourceFile` rather than raw text so a class named in a comment is not a violation. Add
guard-the-guard: the glob matched at least 3 files and includes `card-grid-skeleton.tsx` by name — a gate
that silently matched zero files passes perfectly.

**`tests/design/skeleton-a11y.test.tsx`** (AC#18, render gate). First line is the pragma
`// @vitest-environment jsdom`; the config stays `node`. Render each of the three skeletons with a test
label and assert: exactly one element with `role="status"`; it carries `aria-busy="true"`; it has a
non-empty accessible name equal to the passed label; and every pulse bar exposes **no** accessible name
(`aria-hidden="true"`). Assert the count of `role="status"` is exactly 1, not "at least 1" — two status
regions in one skeleton announce twice.

Both files get a NOT COVERED footer. For the measurements gate: it cannot see a skeleton whose box is right
but whose *content* shape is wrong — that is the Playwright ±2px assertion in plan `11-21`, and jsdom cannot
catch that class of bug at all (D-131). For the a11y gate: it renders in jsdom, so it says nothing about
computed geometry.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/skeleton-measurements.test.ts tests/design/skeleton-a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both gates pass under `vitest.design.config.ts`
    - Watched red (AC#16): inline `ROW_CARD_HEIGHT`'s value as a literal `h-20` in `row-list-skeleton.tsx` → the source gate fails naming the file and the literal. Reverted. Both runs recorded in the file header.
    - Watched red (AC#18): remove `role="status"` from `panel-skeleton.tsx` → the render gate fails with a count of 0. Add a second `role="status"` → it fails with a count of 2. Both reverted, both recorded.
    - Watched red (vacuity): point the glob at a directory with no matches → guard-the-guard fails rather than the real assertion passing
    - `vitest.design.config.ts` still has no `globalSetup` and no `setupFiles`
    - `npm run test:design` passes with the file count raised by exactly 2
  </acceptance_criteria>
  <done>A skeleton cannot hardcode its box or ship without announcing itself, and both rules have been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared token inventory ↔ rendered surface | A pairing that renders but is not declared is invisible to the AA gate |
| loading placeholder ↔ assistive technology | A 1.09:1 fill carrying meaning is a WCAG 1.4.11 failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-A11YFILL | Denial of Service (accessibility) | skeleton regions | mitigate | Mandatory `role="status"` + `aria-busy` + `sr-only` label, asserted by a jsdom render gate; this is the compensating requirement that makes the 1.09:1 exclusion legal, and the exclusion's `note` says so |
| T-11-PAIRBLIND | Repudiation | `contrast-pairs.ts` | mitigate | The three exclusions add pairings that ship today and are measured by nothing — including `ui/card.tsx:15`'s hairline on every card in the app since v1.0 |
| T-11-GEODRIFT | Tampering | skeleton geometry | mitigate | Source gate banning literal box utilities in `patterns/*skeleton*.tsx`, watched red by inlining one constant |
| T-11-BOUNDARY | Information Disclosure | `patterns/` membership | mitigate | No pattern imports from `@/lib/{booking,payments,listing,group,search,availability}`; all three skeletons are Server Components, so composing them cannot drag a surface client-side |
| T-11-SC | Tampering | npm installs | mitigate | Zero packages; `@tailwindcss/typography`, `vaul` and `npx shadcn add sheet` are all recorded-decision reversals to raise, not absorb |
</threat_model>

<verification>
- `npm run test:design` green, three new gate files' worth of assertions
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- One inventory owns every skeleton box class
- The five pairings this phase renders are declared, measured and reasoned
- Three skeleton shapes exist that later `loading.tsx` files compose rather than re-invent
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-07-SUMMARY.md` when done
</output>
