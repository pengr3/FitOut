---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 14
type: execute
wave: 7
depends_on: [11-10, 11-12]
files_modified:
  - src/lib/site.ts
  - src/components/patterns/site-footer.tsx
  - src/app/(public)/layout.tsx
  - src/app/listings/[id]/(detail)/layout.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(app)/layout.tsx
  - src/app/(host)/host/layout.tsx
  - tests/design/site-contacts.test.ts
autonomous: false
requirements: [SHELL-02]

must_haves:
  truths:
    - "A footer with policy, support and contact links exists across the app — on every route EXCEPT /listings/[id]/book (a footer of links is exactly the navigation SHELL-03 forbids while a hold is live) and global-error (which has no styles at all)"
    - "SUPPORT_EMAIL stays null and the footer renders NO support entry at all — no greyed link, no mailto:, no 'coming soon'. Inventing an address remains banned (D-26)"
    - "site-contacts.test.ts INVERTS rather than softens: while SUPPORT_EMAIL is null it asserts ZERO support affordances render anywhere, and it flips back to demanding a non-null string containing @ plus exactly one mailto: the moment the constant is set (D-26)"
    - "The unfilled slot is carried as a named human_needed item on phase completion, replacing the UI-SPEC AC#8 clause that the phase cannot complete with the placeholder in place (D-26 amends AC#8 and § The unfilled slot)"
    - "The footer tagline is IMPORTED from the app's own metadata.description — one sentence, one owner"
  artifacts:
    - path: "src/lib/site.ts"
      provides: "SUPPORT_EMAIL: string | null = null plus the D-26 reason and a pointer to the inverted gate"
      contains: "SUPPORT_EMAIL"
    - path: "src/components/patterns/site-footer.tsx"
      provides: "The three-column footer; the support entry renders only when SUPPORT_EMAIL is non-null (D-26)"
      contains: "site-footer"
    - path: "tests/design/site-contacts.test.ts"
      provides: "The inverted gate that is never absent in either state (D-26)"
  key_links:
    - from: "src/components/patterns/site-footer.tsx"
      to: "src/lib/site.ts"
      via: "the SUPPORT_EMAIL null check that decides whether a support entry renders at all"
      pattern: "SUPPORT_EMAIL"
    - from: "src/components/patterns/site-footer.tsx"
      to: "src/app/layout.tsx"
      via: "the imported metadata.description used as the footer tagline"
      pattern: "description"
---

<objective>
Ship the footer, and ship the gate that keeps its one unfilled slot visible instead of quietly filled.

Purpose: SHELL-02 asks for a footer with policy, support and contact links. FitOut owns no domain and has
no support inbox — `metadataBase` falls back to `localhost:3000` and the only real address anywhere in
`src/` is Resend's `onboarding@resend.dev` sender. The user's call was *"just placeholder for now."*
D-26 takes the spec's own *nothing false ships* path and holds it open, with an **inverted** gate rather
than a softened one, because a gate quietly reduced to nothing is this phase's named failure mode.
Output: `site.ts`, `site-footer.tsx`, five layouts wired, and a gate that is real in both states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
The footer contract (11-UI-SPEC § The Footer) — values, not inventions:

  element     <footer data-testid="site-footer" class="mt-auto">
  surface     bg-muted + border-t
  container   mx-auto w-full max-w-6xl px-4 py-8 sm:px-6 sm:py-12
  layout      single column below sm:, sm:grid-cols-3 at >=640px, gap-8
  headings    text-sm font-medium text-foreground
  links/body  text-sm text-muted-foreground + underline-offset-4 hover:underline hover:text-foreground
  pairings    foreground on muted 18.16 / 16.89 ✓ · muted-foreground on muted 4.82 / 5.28 ✓ — BOTH ALREADY
              DECLARED, so this plan adds ZERO contrast rows
  elevation   none. A footer is not a raised surface.

  Columns:
    FitOut          text wordmark + the app's own metadata.description, IMPORTED not retyped
    Product         Find a space -> /  ·  Host your space -> /host
    Legal & support Terms -> /terms  ·  Privacy -> /privacy  ·  Support -> mailto:{SUPPORT_EMAIL}

⚠ D-26 DEPARTS from the approved UI-SPEC and the departure is deliberate and cited. The UI-SPEC says
site-contacts.test.ts FAILS while SUPPORT_EMAIL is null and that "the phase cannot go green with the
placeholder in place". D-26 amends AC#8 and § The unfilled slot: the gate INVERTS instead, and the
unfilled slot becomes a named human_needed item on phase completion — the same convention the roadmap
already uses for the sales-gated PayMongo threads. Do not reconcile this silently in either direction.

/terms and /privacy do not exist yet — plan 11-15 creates them. The links are authored here and resolve
one plan later. Record that ordering in the SUMMARY so a reviewer testing the footer in between is not
surprised by two 404s.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/lib/site.ts and the footer</name>
  <files>src/lib/site.ts, src/components/patterns/site-footer.tsx</files>
  <read_first>
    - src/lib/payments/config.ts:1-10 (the named-constant module contract: "the exported NAME is imported everywhere, never a hardcoded literal")
    - src/app/layout.tsx:104-108 (the `metadata` object — `description: "Book gyms, courts and studios by the hour."` is the footer tagline's ONE owner; import it, do not retype it)
    - src/components/patterns/site-chrome.tsx (plan 11-10's sibling — the same `patterns/` membership rules apply: Server Component, no domain imports, no product copy beyond what the UI-SPEC's copy contract binds)
    - src/lib/design/selector-contract.ts (the declared `site-footer` id)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The Footer, § The unfilled slot, § Copywriting Contract
  </read_first>
  <action>
Create `src/lib/site.ts` with exactly one export:
`export const SUPPORT_EMAIL: string | null = null;`

Its header states D-26 in full: FitOut owns no domain and has no support inbox, inventing an address is a
real-world claim shipped to users and is banned by the UI-SPEC's anti-pattern list, the footer therefore
renders **no support entry at all** while this is null, and `tests/design/site-contacts.test.ts` inverts to
guard that. Name the one-line edit that flips everything (`= "…@…"`) and name the gate that flips with it.

Create `src/components/patterns/site-footer.tsx` — a Server Component with no domain imports and no product
sentence beyond the copy contract's fixed strings. Implement the geometry in `<interfaces>` exactly.
`data-testid="site-footer"`.

**The support entry is conditional on `SUPPORT_EMAIL !== null` and there is no else-branch.** No greyed
link, no `mailto:` to a placeholder, no "coming soon", no tooltip, no disabled control. Nothing about
support appears in the DOM while the constant is null. Put that sentence in the file so the next person
does not "improve" it into a disabled affordance.

The tagline is `import { metadata } from "@/app/layout"` — or, if importing from a route module is not
clean, hoist the sentence into `src/lib/site.ts` as `SITE_TAGLINE` and have **`layout.tsx` read it**, so
there is still exactly one owner. Pick one and state which; two literals is the outcome that is not allowed.

Elevation: none. `mt-auto` so it sits at the bottom of the `min-h-dvh flex flex-col` wrapper plan `11-10`
put in each layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('src/components/patterns/site-footer.tsx','utf8');if(/mailto:/.test(s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'))&&!/SUPPORT_EMAIL/.test(s))process.exit(1);if(/shadow-/.test(s))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/site.ts` exports `SUPPORT_EMAIL` typed `string | null` with the value `null`
    - `site-footer.tsx` has no `"use client"`, no domain import, and renders `data-testid="site-footer"` exactly once
    - Every `mailto:` in the file is inside the `SUPPORT_EMAIL !== null` branch; there is no else-branch rendering a support affordance
    - The tagline has exactly one owner — either `layout.tsx`'s `metadata.description` imported here, or `SITE_TAGLINE` in `site.ts` read by both. The SUMMARY names which.
    - `grep -c "shadow-" src/components/patterns/site-footer.tsx` returns 0
    - The three column headings and every link label match the copy contract byte-for-byte
    - `npm run build` exits 0 and `tests/design/contrast.test.ts` passes with **zero** new rows added
  </acceptance_criteria>
  <done>The footer exists, and the support slot is absent rather than faked.</done>
</task>

<task type="auto">
  <name>Task 2: Render the footer on every route except the two carve-outs</name>
  <files>src/app/(public)/layout.tsx, src/app/listings/[id]/(detail)/layout.tsx, src/app/(auth)/layout.tsx, src/app/(app)/layout.tsx, src/app/(host)/host/layout.tsx</files>
  <read_first>
    - src/app/(public)/layout.tsx, src/app/listings/[id]/(detail)/layout.tsx, src/app/listings/[id]/book/layout.tsx (plan 11-10's outputs — the `min-h-dvh flex flex-col` wrapper the footer hangs off, and the book layout that must NOT get one)
    - src/app/(app)/layout.tsx and src/app/(host)/host/layout.tsx (plan 11-12's outputs)
    - src/app/(auth)/layout.tsx (plan 11-10 removed its duplicate wordmark)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The Footer ("Where it renders")
  </read_first>
  <action>
Add `<SiteFooter />` to the five layouts listed in `<files>`, after `{children}`, inside the
`min-h-dvh flex flex-col` wrapper so `mt-auto` does what it says.

**Do not add it to `src/app/listings/[id]/book/layout.tsx`.** A footer of links is exactly the navigation
SHELL-03 forbids while a hold is live. Add a one-line comment in that file recording the omission as
deliberate, so a later reader does not "fix" the inconsistency.

**Do not add it to `global-error.tsx`** (plan `11-18` creates it) — it renders its own document and receives
no styles. That file's contract already says "Footer / header: none"; nothing to do here beyond not
assuming otherwise.

`(legal)` does not exist yet; plan `11-15` creates its layout and renders the footer there. Note the
hand-off in the SUMMARY.

Re-run the unreachable-`DATABASE_URL` build afterwards: the footer is static, so nothing should change in
the route table, and a change would mean an import dragged something in.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/public-listing.spec.ts e2e/search-and-book.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlc "SiteFooter" src/app/` lists exactly the five layouts in `<files>` (plus `(legal)` after plan 11-15)
    - `grep -c "SiteFooter" src/app/listings/\[id\]/book/layout.tsx` returns 0, and the file carries the deliberate-omission comment
    - Against `npm run dev`: `/`, `/listings/{id}`, `/invite/{token}`, `/login`, `/bookings` and `/host` each render exactly one `[data-testid="site-footer"]`; `/listings/{id}/book` renders zero
    - No route renders two footers
    - `npm run build` exits 0 with the unreachable `DATABASE_URL` and the route table is unchanged from plan 11-12's
    - `e2e/public-listing.spec.ts` and `e2e/search-and-book.spec.ts` pass
  </acceptance_criteria>
  <done>The footer is app-wide, and the one route that must not have it provably does not.</done>
</task>

<task type="auto">
  <name>Task 3: The inverted support gate</name>
  <files>tests/design/site-contacts.test.ts</files>
  <read_first>
    - tests/design/scaffold-residue.test.ts (the absence-assertion idiom and its :16-20 explanation of why an absence needs a permanent test)
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker so a `mailto:` in a comment is not a hit)
    - tests/use-server-exports.test.ts:315-318, :320-369, :89-102
    - src/lib/site.ts and src/components/patterns/site-footer.tsx (Tasks 1 and 2)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md § D-26 (the exact inversion contract — read it before writing a line of this file)
  </read_first>
  <action>
Create `tests/design/site-contacts.test.ts`. It has **two branches and neither is empty**, and the branch is
selected by reading `SUPPORT_EMAIL` from `src/lib/site.ts` at test time.

**While `SUPPORT_EMAIL === null` (today):**
- Assert `SUPPORT_EMAIL` is exactly `null` — not `""`, not `undefined`.
- Assert **zero** support affordances render anywhere: scan `src/app/**` and `src/components/**` string
  literals via AST for `mailto:`, for the word `Support` used as a link label, and for any address-shaped
  literal matching `/[\w.+-]+@[\w-]+\.[\w.]+/`, and assert the only surviving occurrences are the
  deliberately-excluded ones — Resend's `onboarding@resend.dev` sender in the email layer, and any address
  inside a `//` comment. Carry that exclusion list as data with a reason per entry, in the
  `EXCLUDED_PAIRS` idiom.
- Assert `site-footer.tsx` contains no else-branch support affordance.

**While `SUPPORT_EMAIL !== null`:**
- Assert it is a string containing `@`.
- Assert the footer renders **exactly one** `mailto:` and that it interpolates the constant rather than a
  literal address.

Write both branches now. The second is unreachable today, so **prove it works** with a synthetic fixture:
run the same assertion functions against an in-memory `{ SUPPORT_EMAIL: "help@example.test" }` and a
synthetic footer source, in both directions. A branch nobody has ever executed is not a gate.

Guard-the-guard: assert the scanner walked ≥ 100 files and that it found `site-footer.tsx` by name.

Header block: state D-26 verbatim, including that this **amends `11-UI-SPEC.md` AC#8 and § The unfilled
slot** — the "phase cannot complete with the placeholder in place" clause no longer holds, and a named
`human_needed` item replaces it. Then state the failure mode this inversion exists to prevent, in the
phase's own words: *a gate quietly reduced to nothing is worse than no gate.*

NOT COVERED footer: this gate cannot tell you the address is correct or monitored — only that nothing false
is shipped while there is none.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/site-contacts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The gate passes with `SUPPORT_EMAIL === null`
    - Watched red (null branch): temporarily add `<a href="mailto:support@fitout.ph">Support</a>` to the footer → the gate fails naming the file and the literal. Reverted. Both runs recorded in the header.
    - Watched red (non-null branch): temporarily set `SUPPORT_EMAIL = "help@example.test"` **without** rendering a `mailto:` → the gate fails demanding exactly one. Then render two → it fails demanding exactly one. Reverted to `null`. All three runs recorded.
    - The synthetic-fixture exercise of the non-null branch is committed and passes
    - `grep -c "onboarding@resend.dev" tests/design/site-contacts.test.ts` returns >= 1 — the exclusion is declared data with a reason, not an accident of the regex
    - `npm run test:design` passes with the file count raised by exactly 1
  </acceptance_criteria>
  <done>The gate is real in both states, and both states have been watched failing.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Record the SUPPORT_EMAIL human_needed item</name>
  <files>none</files>
  <read_first>
    - src/lib/site.ts (Task 1's output — the one-line edit that flips everything)
    - tests/design/site-contacts.test.ts (Task 3's output — the branch that flips with it)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md § D-26 (the full departure, including that it amends 11-UI-SPEC AC#8 and its unfilled-slot section)
    - src/app/layout.tsx:52 and :95-102 (DEFAULT_APP_URL and resolveMetadataBase — the second unfilled slot, NEXT_PUBLIC_APP_URL)
  </read_first>
  <action>
    Run no automation. Present the decision below, wait for an explicit selection, and record the outcome in
    the plan SUMMARY. If the answer is carry-both, write BOTH items into the SUMMARY as named human_needed
    entries, using the same convention the roadmap already uses for the sales-gated PayMongo threads, and
    leave SUPPORT_EMAIL at null. Do NOT invent an address under any circumstances — that is banned outright
    by 11-UI-SPEC's anti-pattern list — and do NOT soften the inverted gate to make the slot look filled.
  </action>
  <decision>Whether a real FitOut support address exists yet, and confirmation that the phase completes with the slot recorded as `human_needed` rather than filled.</decision>
  <context>
D-26 keeps `SUPPORT_EMAIL` at `null` and inverts the gate rather than softening it. That makes the footer
honest today and makes the gate flip itself the moment an address arrives — `src/lib/site.ts` is a one-line
edit and `tests/design/site-contacts.test.ts` switches branches automatically.

What D-26 explicitly changes about the approved UI-SPEC: AC#8 said the phase **cannot complete** with the
placeholder in place. That clause no longer holds. A named `human_needed` item replaces it, using the same
convention the roadmap already uses for the sales-gated PayMongo threads.

There is a **second** unfilled slot of the same class, found by research: `NEXT_PUBLIC_APP_URL` is unset, so
`metadataBase` falls back to `http://localhost:3000` and every `og:image` URL will be localhost-absolute
until it is set (`src/app/layout.tsx:52`, `resolveMetadataBase()`). Plan `11-20` depends on this. Carrying
it now is cheaper than discovering it in Phase 17.
  </context>
  <options>
    <option id="carry-both">
      <name>Carry both as named human_needed items (D-26's path)</name>
      <pros>Nothing false ships; the gate stays real; both slots are visible on phase completion rather than forgotten</pros>
      <cons>The footer has no support affordance and unfurled links point at localhost until someone supplies the two values</cons>
    </option>
    <option id="supply-support">
      <name>Supply a real support address now</name>
      <pros>The footer is complete; the gate flips to its demanding form and stays there</pros>
      <cons>Requires a real, monitored inbox — a fabricated one is banned outright by the UI-SPEC's anti-pattern list</cons>
    </option>
    <option id="supply-both">
      <name>Supply a real support address AND set NEXT_PUBLIC_APP_URL</name>
      <pros>Both slots close; SHELL-04's unfurls become real</pros>
      <cons>Requires a domain FitOut does not own yet</cons>
    </option>
  </options>
  <resume-signal>Select `carry-both`, `supply-support` (and give the address), or `supply-both` (and give both values). If `carry-both`, the SUMMARY records two `human_needed` items verbatim: **(1) a real, monitored FitOut support address for `src/lib/site.ts`'s `SUPPORT_EMAIL`** and **(2) `NEXT_PUBLIC_APP_URL` set to the deployed origin so `metadataBase` stops resolving to localhost**.</resume-signal>
  <acceptance_criteria>
    - The developer's selection is recorded verbatim in the SUMMARY
    - If `carry-both`: `src/lib/site.ts` still exports `SUPPORT_EMAIL = null`, `tests/design/site-contacts.test.ts` passes on its null branch, and the SUMMARY carries **two** named `human_needed` entries — the support address, and `NEXT_PUBLIC_APP_URL`
    - If an address was supplied: it is a real, monitored inbox (never fabricated), `site-contacts.test.ts` flips to its non-null branch and passes, and the footer renders exactly one `mailto:`
    - Either way, `npm run test:design` is green and no support affordance renders that does not correspond to a real address
    - The SUMMARY states explicitly that D-26 amends `11-UI-SPEC.md` AC#8 — the "phase cannot complete with the placeholder in place" clause no longer holds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| footer copy → user's real-world expectations | A contact address is a claim the business has made; a fabricated one is a lie shipped to users |
| checkout route ↔ navigation affordances | Any link can lose an active hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-FAKECONTACT | Spoofing | the footer support entry | mitigate | `SUPPORT_EMAIL` stays `null` and the entry is absent, not disabled; the inverted gate asserts zero support affordances anywhere in `src/` with a declared exclusion list (D-26) |
| T-11-GATEZERO | Repudiation | `tests/design/site-contacts.test.ts` | mitigate | The gate inverts rather than softens, both branches are written and both are watched failing — including the unreachable branch, exercised against synthetic fixtures |
| T-11-HOLDLOSS | Denial of Service | `/listings/[id]/book` | mitigate | No footer on that route, asserted by a zero count and a rendered check |
| T-11-OGBASE | Information Disclosure | `metadataBase` | accept | `NEXT_PUBLIC_APP_URL` unset means unfurl URLs are localhost-absolute — carried as a named `human_needed` item rather than papered over with an invented origin |
| T-11-SC | Tampering | npm | mitigate | Zero packages; zero new contrast rows (both footer pairings are already declared) |
</threat_model>

<verification>
- `npm run build` exits 0 with an unreachable `DATABASE_URL`
- `npm run test:design` green with one new gate
- Exactly one footer on every route except `/listings/[id]/book`
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- SHELL-02's footer exists app-wide with the correct two carve-outs
- The support slot is visibly unfilled, never faked, and guarded in both states
- Two `human_needed` items are recorded by name on phase completion
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-14-SUMMARY.md` when done
</output>
