---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 15
type: execute
wave: 8
depends_on: [11-08, 11-14]
files_modified:
  - src/app/(legal)/layout.tsx
  - src/app/(legal)/terms/page.tsx
  - src/app/(legal)/privacy/page.tsx
  - tests/design/legal-copy.test.ts
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "/terms and /privacy return 200 so the footer's links resolve, and both are impossible to mistake for the published documents"
    - "Neither page's body contains 'you agree', 'hereby', 'shall', 'warrant' or 'indemnif' — a source scan that fails loudly the moment someone helpfully drafts real-looking terms"
    - "Long-form prose is the four named type roles inside max-w-prose (65ch), NOT @tailwindcss/typography — a second un-gated type system beside the four roles is what DS-02 exists to prevent"
    - "Both routes are fully static: no await, no DB, no session, therefore no loading.tsx (the async-default rule)"
  artifacts:
    - path: "src/app/(legal)/layout.tsx"
      provides: "The prose container plus the public composition and the footer"
    - path: "src/app/(legal)/terms/page.tsx"
      provides: "The placeholder notice and a structural outline, never drafted legal prose"
      contains: "not FitOut's terms of service"
    - path: "tests/design/legal-copy.test.ts"
      provides: "AC#9 and AC#10 — the exact notice strings and the legalese ban"
  key_links:
    - from: "src/app/(legal)/terms/page.tsx"
      to: "src/components/patterns/panel-card.tsx"
      via: "the placeholder notice rendered as PanelCard tone='muted' with data-testid='legal-placeholder-notice'"
      pattern: "legal-placeholder-notice"
---

<objective>
Give the footer's Terms and Privacy links somewhere real to land, on the app's first long-form prose
surface, without shipping anything that reads as a binding agreement.

Purpose: a page that *looks* like real terms but is filler makes a claim to users the business has not
made. The placeholder notice is therefore part of acceptance, asserted by string equality, and the body is
a structural outline rather than clause language.
Output: a `(legal)` group, two static pages, and a gate over both the notice and the legalese ban.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
Routing and layout (11-UI-SPEC § /terms and /privacy):
  routes     src/app/(legal)/terms/page.tsx, src/app/(legal)/privacy/page.tsx
  layout     src/app/(legal)/layout.tsx — supplies the prose container
  shell      inherits site-chrome (public composition) and the footer, like every other route
  container  mx-auto w-full max-w-prose px-4 py-12 sm:py-16   (max-w-prose = 65ch, inside the 45–75 measure)
  rendering  fully static — no await, no DB, no session -> NO loading.tsx

Prose type contract — the four named roles, BY HAND:
  <h1>  text-display          <h2>  text-heading mt-8       <p>  text-body
  paragraph rhythm space-y-4  <ul>  list-disc pl-6 space-y-2
  last-updated line text-label text-muted-foreground
  Roles carry their own leading — do NOT co-locate leading-*, which overrides the role.

The placeholder notice:
  component PanelCard tone="muted"   hook data-testid="legal-placeholder-notice"
  placement directly beneath the <h1>, above every other element — above the fold at 320px
  icon      InfoIcon size-5 text-muted-foreground
  NOT       not `attention` (DS-10 reserves that for a genuine failure needing a human), not destructive,
            not a toast, not an HTML comment, not a code TODO

Copy — /terms, verbatim:
  **Placeholder — these are not FitOut's terms of service.**
  FitOut has not published its terms yet. This page exists so the site's links resolve. Nothing on it is a
  binding agreement, and nothing on it describes an obligation FitOut has taken on. The published terms will
  replace this page in full.

Copy — /privacy, verbatim:
  **Placeholder — this is not FitOut's privacy policy.**
  FitOut has not published its privacy policy yet. This page exists so the site's links resolve. Nothing on
  it describes how FitOut actually handles your data. The published policy will replace this page in full.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The (legal) group layout and the prose container</name>
  <files>src/app/(legal)/layout.tsx</files>
  <read_first>
    - src/app/(auth)/layout.tsx (the analog: a group layout whose only job is a container — and plan 11-10 already removed its duplicate wordmark, which is the shape to copy)
    - src/app/(public)/layout.tsx (plan 11-10's public composition and its `min-h-dvh flex flex-col` wrapper; plan 11-14 hung `SiteFooter` off it)
    - src/app/invite/[token]/page.tsx:83-86 — now src/app/(public)/invite/[token]/page.tsx (the nearest container idiom: `<main className="mx-auto w-full max-w-lg px-4 py-8 sm:py-12">`)
    - src/components/patterns/site-chrome.tsx and src/components/patterns/site-footer.tsx
  </read_first>
  <action>
Create `src/app/(legal)/layout.tsx`. It renders the **public composition** of `SiteChrome` and `SiteFooter`
inside the same `min-h-dvh flex flex-col` wrapper the other public layouts use — no bespoke chrome. Its own
contribution is the prose container: `<main className="mx-auto w-full max-w-prose px-4 py-12 sm:py-16">`.

`max-w-prose` resolves to 65ch, inside the 45–75 character measure. Do **not** install
`@tailwindcss/typography`: it ships its own type scale, which is a second un-gated type system beside the
four named roles, and that is exactly what DS-02 exists to prevent. It is on the UI-SPEC's anti-pattern list.

Keep the layout free of `await`, database access and session reads so both routes stay `○ Static` — which is
also why they get no `loading.tsx`.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(legal)/layout.tsx` renders `SiteChrome` (public composition) and `SiteFooter`
    - It contains no `await`, no `@/lib/db` import and no session read
    - The container class is exactly `mx-auto w-full max-w-prose px-4 py-12 sm:py-16`
    - `git diff --stat package.json` is empty — `@tailwindcss/typography` is not installed
    - `npm run build` exits 0 with the unreachable `DATABASE_URL`, and the route table lists `/terms` and `/privacy` as `○ (Static)`
    - No `src/app/(legal)/loading.tsx` exists
  </acceptance_criteria>
  <done>The group renders the standard shell plus a 65ch measure, with no second type system.</done>
</task>

<task type="auto">
  <name>Task 2: The two pages — the notice, and an outline that cannot be mistaken for terms</name>
  <files>src/app/(legal)/terms/page.tsx, src/app/(legal)/privacy/page.tsx</files>
  <read_first>
    - src/components/patterns/panel-card.tsx (plan 11-08's output — `tone="muted"` is the neutral status tone at panel scale)
    - src/lib/design/selector-contract.ts (the declared `legal-placeholder-notice` id)
    - src/app/layout.tsx:104-108 (the `title` template `%s · FitOut` these pages' titles compose through)
    - tests/design/type-scale.test.ts (the four named roles and the ban on arbitrary sizes — these pages must add zero new arbitrary sizes)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § /terms and /privacy (the copy, the type contract, and the four acceptance criteria)
  </read_first>
  <action>
Create both pages as static Server Components.

Structure, identical on both: an `<h1>` in `text-display` (`Terms of Service` / `Privacy Policy`), then —
**directly beneath it, above every other element** — the placeholder notice as
`<PanelCard tone="muted" data-testid="legal-placeholder-notice">` carrying an `InfoIcon size-5
text-muted-foreground` and the verbatim copy in `<interfaces>`. It must be above the fold at 320px.

Then a single `<h2 className="text-heading mt-8">` reading **"What the published document will cover"**,
followed by a short `<ul className="list-disc pl-6 space-y-2">` of the section names the real document will
contain, each with **one plain sentence** of what it will cover. Close with a last-updated line in
`text-label text-muted-foreground`.

**Body content is a structural outline, never drafted legal prose.** No clause language. No "you agree", no
"hereby", no defined terms, no "shall", no "warrant", no "indemnif". A reader skimming must be unable to
mistake any sentence for an operative term. Write the outline in the second person only where it is
describing what the future document will do, never where it could read as an obligation.

Type roles are composed by hand and carry their own leading — do not co-locate `leading-*`, which overrides
the role.

Add a `metadata` export per page with a title that composes through the root `%s · FitOut` template.

Add a comment at the top of both files stating AC#4's condition in plain words: **the notice is removed only
in the same commit that supplies real copy, and that commit deletes the two assertions guarding it**
(`tests/design/legal-copy.test.ts`). Name the file so the coupling is findable.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npx vitest run --config vitest.design.config.ts tests/design/type-scale.test.ts tests/design/selector-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Against `npm run dev`, `/terms` and `/privacy` return 200 and each renders exactly one `[data-testid="legal-placeholder-notice"]`
    - `/terms`'s notice contains the exact string `not FitOut's terms of service`; `/privacy`'s contains `not FitOut's privacy policy`
    - Neither page's body contains `you agree`, `hereby`, `shall`, `warrant` or `indemnif` (case-insensitive)
    - `tests/design/type-scale.test.ts` sees **zero** new arbitrary sizes under `src/app/(legal)/**`
    - The notice is the first element after the `<h1>` in DOM order
    - `grep -c "leading-" src/app/\(legal\)/*/page.tsx` returns 0
    - Both pages export `metadata`; `npm run build` classifies both as `○ (Static)`
  </acceptance_criteria>
  <done>Both links resolve to pages that are useful and are impossible to read as binding.</done>
</task>

<task type="auto">
  <name>Task 3: The legal-copy gate (AC#9, AC#10)</name>
  <files>tests/design/legal-copy.test.ts</files>
  <read_first>
    - tests/design/scaffold-residue.test.ts (raw-source structural assertions — the closest analog)
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker, so a banned word inside a `//` comment is not a violation)
    - tests/design/strip-comments.test.ts (the line-oriented stripper and why a `/*…*/` regex is dangerous here)
    - tests/use-server-exports.test.ts:315-318, :320-369, :89-102
    - src/app/(legal)/terms/page.tsx and privacy/page.tsx (Task 2's output)
  </read_first>
  <action>
Create `tests/design/legal-copy.test.ts` with two assertion groups and their anti-vacuity clauses.

**AC#9 — the notice strings.** Assert `src/app/(legal)/terms/page.tsx` contains the exact string
`not FitOut's terms of service` and `privacy/page.tsx` contains `not FitOut's privacy policy`, and that both
files contain `data-testid="legal-placeholder-notice"` exactly once. String **equality** on the sentinel
phrase, not a fuzzy match — deleting the notice must fail a named test.

**AC#10 — the legalese ban.** Walk both pages' JSX text and string literals via AST and assert none contains
`you agree`, `hereby`, `shall`, `warrant` or `indemnif`, case-insensitively. Carry the five terms as a
declared `const` list with a one-line reason: this is crude, and it is meant to be — it fails loudly the
moment someone "helpfully" drafts real-looking terms. Strip `//` comments first so this plan's own
explanatory comments are not violations.

Guard-the-guard: assert both files were found and parsed and that each yielded a non-zero number of text
nodes — a scanner that silently read nothing passes both groups perfectly.

Both-directions self-tests on synthetic sources: a fixture containing `<p>You agree to the following</p>`
must be flagged; one containing `// the word "shall" is banned here` must not; one missing the notice string
must fail AC#9.

Header block: record AC#4's coupling — **these assertions are deleted in the same commit that supplies real
legal copy, and not before.** Name the two page files. A future contributor removing the notice without
removing the tests will hit a red gate that tells them exactly what the rule is.

NOT COVERED footer: this gate reads source, not the rendered page, and it cannot judge whether an outline
sentence *reads* as an obligation — a human does that. Say so.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/legal-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The gate passes against Task 2's pages
    - Watched red (AC#9): delete the notice from `/terms` → the gate fails naming the missing string. Reverted. Both runs recorded in the header.
    - Watched red (AC#10): add `<p>You agree to these terms.</p>` to `/privacy` → the gate fails naming the term and the file. Reverted.
    - Watched red (vacuity): point the scanner at a non-existent path → guard-the-guard fails rather than both groups passing
    - The comment-exemption self-test is committed and passes
    - `npm run test:design` passes with the file count raised by exactly 1; push and CI green (D-25)
  </acceptance_criteria>
  <done>The placeholder cannot be silently deleted and real-looking terms cannot be silently drafted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| placeholder copy → users and regulators | A page that reads as terms is a claim the business has not made |
| prose surface → the design system | A prose plugin is a second, un-gated type system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-FAKETERMS | Spoofing | `/terms`, `/privacy` | mitigate | An unmissable `PanelCard tone="muted"` notice above the fold at 320px, asserted by string equality, plus a five-term legalese ban asserted over both bodies |
| T-11-SILENTDROP | Repudiation | the notice | mitigate | AC#4's coupling is recorded in both the pages and the gate: the notice is removed only in the commit that supplies real copy, and that commit deletes the assertions with it |
| T-11-TYPEFORK | Tampering | the type system | mitigate | `@tailwindcss/typography` is not installed (`git diff --stat package.json` empty); the four named roles are composed by hand and `type-scale.test.ts` sees zero new arbitrary sizes |
| T-11-STATICLEAK | Information Disclosure | build-time prerender | mitigate | Both routes are fully static with no database or session access, so they cannot leak anything and cannot reopen the DB-free build question |
| T-11-SC | Tampering | npm | mitigate | Zero packages |
</threat_model>

<verification>
- `npm run build` exits 0 with an unreachable `DATABASE_URL`; both routes are `○ (Static)`
- `npm run test:design` green with one new gate
- `/terms` and `/privacy` return 200 and render the footer and header
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- The footer's Legal links resolve
- Nothing on either page can be mistaken for a binding term, and that is machine-checked
- The app's first prose surface uses the four named roles and nothing else
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-15-SUMMARY.md` when done
</output>
