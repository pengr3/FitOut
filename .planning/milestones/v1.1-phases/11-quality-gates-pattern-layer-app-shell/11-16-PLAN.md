---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 16
type: execute
wave: 8
depends_on: [11-09, 11-11, 11-13]
files_modified:
  - src/components/search/search-results.tsx
  - src/app/(app)/bookings/page.tsx
  - src/app/(host)/host/requests/page.tsx
  - src/app/(host)/host/listings/page.tsx
  - src/app/(host)/host/bookings/page.tsx
  - src/app/(host)/host/earnings/page.tsx
  - src/app/(app)/bookings/[id]/group/page.tsx
  - src/components/notifications/notification-bell.tsx
  - tests/design/empty-state-adoption.test.ts
autonomous: true
requirements: [STATE-04]

must_haves:
  truths:
    - "Every list surface has a designed empty state, authored ONCE with the shared EmptyState pattern (STATE-04)"
    - "Two drifted shells collapse into one: two radii, two paddings and two title treatments become one, and the shell whose title was a <p> rather than a heading is fixed"
    - "The off-ladder p-10 on five shipped blocks lands on p-8"
    - "Host inbox-zero reads as a POSITIVE state: CheckCircle2 at text-success, text stays --foreground, no bg-success anywhere — D-14's green-retreats-to-the-icon holds at panel scale"
    - "AC#23's zero-border-dashed-outside-the-pattern assertion is SCOPED with a stated reason, because the shipped inventory is 19 sites and not the 8 the UI-SPEC lists"
  artifacts:
    - path: "src/components/patterns/empty-state.tsx"
      provides: "The one shell, adopted by 8+ surfaces (authored in plan 11-09)"
    - path: "tests/design/empty-state-adoption.test.ts"
      provides: "AC#23 and AC#24 with an explicit, reasoned scope"
  key_links:
    - from: "src/app/(host)/host/requests/page.tsx"
      to: "src/components/patterns/empty-state.tsx"
      via: "tone='positive' — the STATE-04 inbox-zero clause"
      pattern: "tone=\"positive\""
---

<objective>
Adopt one empty shell across every list surface, make host inbox-zero read as *done*, and scope AC#23's
assertion honestly against a `border-dashed` inventory that is 19 sites, not 8.

Purpose: the drift STATE-04 ends is measurable and worse than copy-paste — two shells, two radii, two
paddings, two title treatments, and one shell with **no heading element in the empty region at all**.
Output: eight conversions, three newly-authored empty states, one copy change, and a scoped gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
The measured drift (11-UI-SPEC § Empty):
  Shell A  search-results.tsx ×3            rounded-xl  p-8  (32px ✓)   title <p className="font-semibold">  ← NOT a heading
  Shell B  bookings/page.tsx ×2,
           host/requests, host/listings,
           host/bookings                    rounded-lg  p-10 (40px ✗)   title <h2 className="text-lg font-medium">
Shell A's GEOMETRY wins (it is already on the ladder); shell B's HEADING ELEMENT wins (via titleAs).

The 8 conversion sites, measured (11-PATTERNS § J):
  search-results.tsx:170, :204, :242            (shell A)
  (app)/bookings/page.tsx:138, :151             (shell B, p-10 -> p-8)
  (host)/host/requests/page.tsx:140             (shell B)
  (host)/host/listings/page.tsx:88              (shell B)
  (host)/host/bookings/page.tsx:191             (shell B)

Also AUTHORED (no empty state today): /host/earnings payouts list · /bookings/[id]/group attendee roster
at zero · the NotificationBell panel at zero.

Copy: preserved VERBATIM everywhere except ONE change, which STATE-04 explicitly asks for —
  /host/requests   title  "No requests right now"  ->  "You're all caught up"
                   icon   none                     ->  CheckCircle2, text-success
                   body   UNCHANGED (it explains the request mechanism well; there is no reason to rewrite it)

⚠ CORRECTION TO THE PHASE INPUTS (11-PATTERNS § J, measured): `border-dashed` is **19 sites, not 8**. The
11 out of inventory are (host)/host/earnings/page.tsx:180, (host)/host/page.tsx:149,
availability-calendar.tsx:258,265,272, date-pass-picker.tsx:292,299,306,323, photo-uploader.tsx:180.
An UNSCOPED AC#23 assertion lands red on files this phase never touches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the eight shipped empty blocks, copy preserved except the one STATE-04 change</name>
  <files>src/components/search/search-results.tsx, src/app/(app)/bookings/page.tsx, src/app/(host)/host/requests/page.tsx, src/app/(host)/host/listings/page.tsx, src/app/(host)/host/bookings/page.tsx</files>
  <read_first>
    - src/components/patterns/empty-state.tsx (plan 11-09's output — the prop contract, including `titleAs` and `tone`)
    - src/components/search/search-results.tsx:201-215 (shell A, the geometry that wins) and :170, :242 (the other two blocks) — note the `<p className="font-semibold">` title, which is the defect `titleAs` fixes
    - src/app/(app)/bookings/page.tsx:138 and :151 (shell B; `p-10` -> `p-8`; the `Find a space` CTA is `variant="brand"`, already on Phase 10's list — preserve it)
    - src/app/(host)/host/requests/page.tsx:140 (the one copy change; read the shipped body sentence and keep it byte-for-byte)
    - src/app/(host)/host/listings/page.tsx:88 (the `Create your first listing` CTA is `variant="brand"` — preserve it)
    - src/app/(host)/host/bookings/page.tsx:191
    - src/lib/design/status-tones.ts (D-14's rule: green retreats to the icon; `--success` is a 3.0-bar non-text pairing)
  </read_first>
  <action>
Replace each of the eight blocks with `<EmptyState …/>`. The component owns the geometry (`rounded-xl border
border-dashed p-8 text-center`, the icon at `size-6 text-muted-foreground`, the title at `mt-3 text-body
font-semibold text-foreground`, the body at `mx-auto mt-1 max-w-prose text-sm text-muted-foreground`, the
actions row at `mt-5 flex flex-wrap items-center justify-center gap-2`, and
`data-testid="empty-state"`). The surface owns the copy.

`titleAs` is `"h2"` by default. Choose `"h2"` or `"h3"` per surface based on the heading level already
around it — the element is never a `<p>`, which is the shell-A defect being fixed.

**All copy is preserved verbatim except `/host/requests`.** That one becomes `title="You're all caught up"`
with `tone="positive"`, which swaps the icon to `CheckCircle2` at `text-success` (success on background
4.00 / 3.86 against a 3.0 bar, already declared). Its **body is unchanged**. Text stays `--foreground`; no
green fill and no green text — D-14 holds at panel scale with zero new tones and zero new pairings.

The two `variant="brand"` CTAs (`Find a space` on `/bookings` empty, `Create your first listing` on
`/host/listings` empty) are preserved exactly — they are already on Phase 10's accent list and the accent
list is closed.

Delete the now-dead shell markup from each file. Do not restructure anything else on these pages: the host
inbox, dashboard and wizard are Phase 14's, and the search surface is Phase 12's.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/search && npx playwright test e2e/search-and-book.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - All five files import `EmptyState` from `@/components/patterns/empty-state`
    - `grep -rc "p-10" src/components/search/search-results.tsx src/app/\(app\)/bookings/page.tsx src/app/\(host\)/host/requests/page.tsx src/app/\(host\)/host/listings/page.tsx src/app/\(host\)/host/bookings/page.tsx` returns 0 for all five
    - No converted block renders its title as a `<p>`
    - `/host/requests` empty renders the exact string `You're all caught up`, a `text-success` icon, `text-foreground` label text, and **no** `bg-success` anywhere (AC#24)
    - Every other empty-state title and body string is byte-identical to its pre-plan value — diff each one and record the comparison in the SUMMARY
    - The two `variant="brand"` CTAs survive
    - `npm test` and `npm run test:design` pass with the phase-baseline counts
  </acceptance_criteria>
  <done>Eight blocks share one shell, the heading element is real everywhere, and inbox-zero reads as done.</done>
</task>

<task type="auto">
  <name>Task 2: Author the three missing empty states</name>
  <files>src/app/(host)/host/earnings/page.tsx, src/app/(app)/bookings/[id]/group/page.tsx, src/components/notifications/notification-bell.tsx</files>
  <read_first>
    - src/app/(host)/host/earnings/page.tsx (the payouts list and its :180 `border-dashed` block — the UI-SPEC lists this surface as having no empty state today; reconcile what is actually there before writing)
    - src/app/(app)/bookings/[id]/group/page.tsx (the attendee roster at zero)
    - src/components/notifications/notification-bell.tsx (the panel at zero; note it is a client component, and `EmptyState` is a Server Component — a Server Component rendered as `children`/`ReactNode` from a client parent is fine, but importing it INTO a client module makes it a client component. Read the file and choose the composition that keeps the boundary clean, and say which in the SUMMARY.)
    - src/components/patterns/empty-state.tsx
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Copywriting Contract (occupancy and unavailability are normal states — never errors, never red; every state names what to do next)
  </read_first>
  <action>
Author an `EmptyState` for each of the three surfaces that has none today.

`/host/earnings` payouts list at zero: `tone="neutral"`. The copy must not imply a failure — HFLOW-05
records that those numbers have never been real because PayMongo's `/v2` money movement is sales-gated, so
"no payouts yet" is a normal state, not a problem. Name what happens next in one sentence.

`/bookings/[id]/group` attendee roster at zero: `tone="neutral"`. An organizer who has invited nobody yet
is at a normal point in the flow — the action is the existing invite affordance, passed as `actions`.

`NotificationBell` panel at zero: `tone="neutral"`, compact. A quiet bell is not an error.

Copy rules this phase inherits and must not break: occupancy and unavailability are normal states, never
errors and never red; never blame the user; every state names what to do next, because a state with no
action is a dead end. Keep each body to one sentence.

If `notification-bell.tsx`'s client boundary makes importing `EmptyState` awkward, prefer passing it as
`children` from a server parent over marking the pattern `"use client"` — the pattern being a Server
Component is what keeps plan 11-01's boundary fix from regressing, and it is not negotiable for convenience.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npx vitest run --config vitest.design.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three surfaces render `[data-testid="empty-state"]` at zero, verified against `npm run dev` with a seeded account that has no payouts, no attendees and no notifications
    - `src/components/patterns/empty-state.tsx` still has no `"use client"` — the composition adapted, not the pattern
    - No new copy contains "error", "failed", "invalid" or a red/destructive treatment
    - Every authored body is one sentence and every state offers an action or explains why none is needed
    - `npm run build` exits 0 and `npm run test:design` is green
  </acceptance_criteria>
  <done>Three previously-blank list surfaces have designed, non-alarming empty states.</done>
</task>

<task type="auto">
  <name>Task 3: The adoption gate, with AC#23's scope decided explicitly</name>
  <files>tests/design/empty-state-adoption.test.ts</files>
  <read_first>
    - tests/design/scaffold-residue.test.ts (the absence-assertion idiom and its :16-20 reason)
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker)
    - tests/use-server-exports.test.ts:315-318, :320-369, :89-102
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md § J (the measured 19-site `border-dashed` inventory and the 11 that are out of the UI-SPEC's list)
    - src/components/availability/availability-calendar.tsx:258,265,272 · src/components/availability/date-pass-picker.tsx:292,299,306,323 · src/components/listing/photo-uploader.tsx:180 (read enough of each to state WHY it is out of scope — they are not empty-state panels)
  </read_first>
  <action>
Create `tests/design/empty-state-adoption.test.ts`.

**Decide AC#23's scope here, explicitly, and state the reason in the file.** The UI-SPEC says *"zero
`border-dashed` empty blocks survive outside `patterns/empty-state.tsx`"* and inventories 8 sites; the tree
has **19**. The 11 extras are not empty-state panels — they are calendar day-cell placeholders, pass-picker
slots and a photo-upload dropzone, where a dashed edge means "drop or select here", not "there is nothing
here". An unscoped assertion therefore lands red on files this phase never touches.

Scope: assert zero `border-dashed` **empty-state blocks** survive across `src/app/**` plus
`src/components/search/search-results.tsx` and `src/components/notifications/notification-bell.tsx`. Carry
the 11 out-of-scope sites as a declared `NON_EMPTY_STATE_DASHED` inventory with a one-line reason each,
in the `EXCLUDED_PAIRS` idiom — a hole that is declared with a reason is a contract; a hole that is merely
convenient is how a gate rots. Assert the inventory's size is exactly 11, so a twelfth dashed site
somewhere unexpected fails rather than joins silently.

**AC#24:** assert `/host/requests`' empty renders `tone="positive"` with a `text-success` icon and
`text-foreground` label text, and that `bg-success` appears **nowhere** in `src/app/**` or
`src/components/**` outside the one glyph-only wizard step marker Phase 10 pinned by name.

Guard-the-guard: assert the scanner walked ≥ 100 files and found `search-results.tsx` and
`(app)/bookings/page.tsx` by name.

Both-directions self-tests: a synthetic source with `className="rounded-lg border border-dashed p-10
text-center"` must be flagged; one with `<EmptyState title=… />` must not; one where `border-dashed` appears
only in a `//` comment must not.

NOT COVERED footer: this gate proves the shell, not the sentence. It cannot tell you an empty state's copy
is good, and it does not cover surfaces Phases 12–15 add — HFLOW-01 (Phase 14) **adopts** this component and
does not re-decide it, which is the near-overlap REQUIREMENTS.md records between STATE-04 and HFLOW-01.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/empty-state-adoption.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The gate passes against Tasks 1 and 2
    - `NON_EMPTY_STATE_DASHED` has exactly 11 entries, each with a non-empty reason, and its size is asserted
    - Watched red: revert one converted block to its raw dashed shell → the gate fails naming the file. Reverted. Both runs recorded in the header.
    - Watched red (AC#24): change `/host/requests`' empty to `tone="neutral"` → the gate fails. Add a `bg-success` anywhere → the gate fails. Both reverted, both recorded.
    - Watched red (vacuity): shrink the scan scope to an empty directory → guard-the-guard fails
    - The scope decision and its reason are stated in the file header in plain words
    - `npm run test:design` passes with the file count raised by exactly 1; push and CI green (D-25)
  </acceptance_criteria>
  <done>AC#23 is asserted over a scope that is stated rather than assumed, and the 11 exclusions are declared data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empty state ↔ user interpretation | An empty inbox rendered as a failure teaches users to distrust a working system |
| pattern layer ↔ client boundary | Importing a Server Component pattern into a client module converts it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-FALSEALARM | Repudiation | empty-state copy and tone | mitigate | Occupancy and unavailability are normal states, never red; `/host/requests` inbox-zero uses `tone="positive"` with green in the icon only (D-14), asserted by AC#24 including a `bg-success` zero-count |
| T-11-CLIENTCREEP | Information Disclosure | `patterns/empty-state.tsx` | mitigate | The pattern stays a Server Component; `notification-bell.tsx`'s boundary is adapted by composition, never by marking the pattern `"use client"` |
| T-11-SCOPEROT | Repudiation | AC#23's assertion | mitigate | The 19-vs-8 discrepancy is resolved by a declared 11-entry exclusion inventory with a reason each and an asserted size, rather than by widening the regex |
| T-11-COPYDRIFT | Tampering | shipped empty-state copy | mitigate | Every string except the one STATE-04 change is diffed byte-for-byte and the comparison is recorded |
| T-11-SC | Tampering | npm | mitigate | Zero packages |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm test` and `npm run test:design` green
- Every list surface renders `[data-testid="empty-state"]` at zero
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- STATE-04 is satisfied: every list surface has a designed empty state from one shell
- Host inbox-zero reads as a positive state with green only in the icon
- The `border-dashed` scope question is answered with declared data, not a widened assertion
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-16-SUMMARY.md` when done
</output>
