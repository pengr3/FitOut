---
quick_id: 260724-l1s
slug: rewrite-stale-search-and-book-e2e-to-cur
type: quick
gap_closure: true
source: Phase-7 debt — e2e specs executed for the first time; e2e/search-and-book.spec.ts is a stale Phase-4 (203b2e3) test that never ran and no longer matches the current booking flow.
---

<objective>
`e2e/search-and-book.spec.ts` was authored once in Phase 4 (commit 203b2e3) and NEVER executed. Its
`test.beforeAll` seeds the listing as `booking_mode = 'request'`, but both tests drive the INSTANT-book
flow (click "Book this space" → wait for `/book?hold=` reserve page). Per `src/app/actions/booking.ts`
(placeHold docstring, lines 79-82) the current app routes **instant → `/book?hold=` reserve/pay page**
and **request → `/bookings/<id>` directly** — so on the request-seeded listing the flow lands on
`/bookings/<id>` and the test times out waiting for `/book?hold=`. Both tests are `serial`, so the
abandoned-hold test is skipped when the full-flow test fails.

Additionally, the full-flow test's tail (click "Confirm booking" → assert "Booking confirmed") is NOT
automatable: an instant confirm now goes through a PayMongo **hosted checkout** that Playwright cannot
drive — which is exactly why `e2e/cancel.spec.ts` seeds a `confirmed` booking DIRECTLY.

Goal: make the spec match the current app so it passes, WITHOUT weakening what it proves. No source
changes — this is a test-only rewrite.

VERIFY against the RUNNING dev server (localhost:3000, reuseExistingServer) + Docker Postgres. Iterate
with `npx playwright test e2e/search-and-book.spec.ts --workers=1` until green.
</objective>

<tasks>

<task id="1" type="fix">
**Seed the listing as `instant` so the "Book this space" → `/book?hold=` reserve flow is valid.**

- files: e2e/search-and-book.spec.ts
- action: In `seedListing()` change `${"request"}::booking_mode` → `${"instant"}::booking_mode`. Update
  the file's top comment if it describes a request flow. Nothing else in the seed changes (the host is
  already payouts-enabled via `host_payout`, which is the instant-bookability gate).
- verify: `grep -n "instant\"}::booking_mode" e2e/search-and-book.spec.ts` returns the seed line.
- done: the seeded tennis listing is instant-bookable.
</task>

<task id="2" type="test">
**Rewrite the full-flow test to stop at the live reserve page (drop the un-automatable PayMongo tail).**

- files: e2e/search-and-book.spec.ts
- action: Keep the existing test 184 body UP TO AND INCLUDING the reserve-page assertions (the
  `page.waitForURL(/\/book\?hold=/)` now succeeds for instant mode). Keep the assertions already there:
  "Review and book" heading, `(GMT+8)`, the `5:00 PM – 7:00 PM` window, `Total`, a `₱` amount,
  `Held for`, the `role="timer"` countdown, and `You won't be charged yet`.
  REMOVE the tail from the `Confirm booking` click onward (lines ~234-246: the confirm →
  `/bookings/` → "Booking confirmed" / FIT-reference / reload assertions) — an instant confirm now
  redirects to a PayMongo hosted checkout that Playwright cannot complete (see `e2e/cancel.spec.ts`
  header). Rename the test to reflect the new scope, e.g.
  `"search → filter → card → listing → Book → live instant hold on the reserve page (SC#1/#2/#4)"`.
  The durable-confirmation assertions move to Task 3.
- verify: the test drives search → filter tennis → card (img+name+₱/hr) → location distance line →
  listing → pickWindow(5PM,6PM) → "Book this space" → `/book?hold=` reserve page with the live hold
  countdown, and PASSES.
- done: the live search→book→reserve flow is covered and green, with no PayMongo dependency.
</task>

<task id="3" type="test">
**Add a durable-confirmation test against a directly-seeded `confirmed` booking (D-43).**

- files: e2e/search-and-book.spec.ts
- action: Add a `seedConfirmedBooking()` helper mirroring `e2e/cancel.spec.ts:89-113` (status
  `'confirmed'`, `booking_mode 'instant'`, `cancellation_policy 'standard'`, a `payment_id`, a FUTURE
  window `now() + make_interval(hours => N)` / `N+1` so it derives as `confirmed`, NOT `completed`
  (D-102), on THIS spec's seeded `listingId` + booker). Add a new test that logs in as the booker,
  goes to `/bookings/<seededId>`, and asserts the current confirmed-detail copy (see
  `src/app/(app)/bookings/[id]/page.tsx` confirmed branch): the `"Booking confirmed"` heading,
  `"Booking reference"`, the `Confirmed` status badge, and a `FIT-[0-9A-Z]{8}` reference; then
  `page.reload()` and assert the same reference still shows (durable RSC read, D-43).
  Ensure teardown removes the new booking — extend the existing `afterAll` cascade (it already deletes
  bookings for the listing/booker), or delete by id.
- verify: the new test passes; the confirmed booking is cleaned up in `afterAll`.
- done: durable-confirmation coverage is preserved without needing PayMongo.
</task>

<task id="4" type="test">
**Confirm the abandoned-hold test passes on the instant seed, and the whole spec is green.**

- files: e2e/search-and-book.spec.ts (only if the abandoned-hold test needs a touch-up)
- action: The existing abandoned-hold test (D-44) already seeds-instant → force-expires
  `expires_at` → reload → asserts `"Your hold expired"` + `Back to availability` inside `role="status"`
  (matches `src/components/booking/hold-expired-state.tsx`). With Task 1 it should pass unchanged; adjust
  only if a selector drifted. Run the full spec serial to confirm ordering holds.
- verify: `npx playwright test e2e/search-and-book.spec.ts --workers=1` → ALL tests pass (0 failed,
  0 unexpectedly-skipped).
- done: the spec runs fully green against the current app.
</task>

</tasks>

<verification>
- `npx playwright test e2e/search-and-book.spec.ts --workers=1` → all pass, none skipped.
- No files under `src/` changed (test-only).
- `npx tsc --noEmit` clean (the spec is typechecked by the project tsconfig if included) and
  `npx eslint e2e/search-and-book.spec.ts` clean.
- Do NOT try to automate the PayMongo hosted checkout; confirmed-state coverage comes from the seeded
  booking (Task 3), exactly as `e2e/cancel.spec.ts` does.
</verification>
