---
milestone: v1.1
name: Front-End Polish & Placeholder Design System
audited: 2026-08-31T11:40:00Z
status: tech_debt
resolved_since_audit:
  - "TRUST-02 and TRUST-03 closed 2026-08-31 by quick task 260831-rpt (commits 265ca87, 356f133, ecdfe9b). The audit's original gap entries are preserved below under gaps_resolved for the record."
  - "DS-09, RESP-04, GATE-02, GATE-06 ticked in REQUIREMENTS.md 2026-08-31 at milestone close — no code changed; they were verified true at HEAD when this audit ran."
  - "DS-11 closed 2026-08-31 by PM ruling: requirement text amended to name its two measured exceptions, per the D-26 precedent. No code changed; the enforcing gate was already green."
  - "BFLOW-08 closed 2026-08-31 by WALK A — a real hosted PayMongo sk_test_ checkout (booking f6cef95d / FIT-6XNSQXG0), confirmed by a real checkout_session.payment.paid webhook. All four conditions confirmed by the PM. Residual: D-101.1 pending-spinner cap unobserved, webhook won the race both times."
  - "TRUST-05 closed 2026-08-31 by WALK B — booking FIT-TBQWWBTR printed through Chrome's print pipeline with printBackground: false, once per theme; all six conditions met, both PDFs single-page A4."
scores:
  requirements: 69/71
  phases: 11/11
  integration: 8/8
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
gaps_resolved:
  requirements:
    - id: "TRUST-02"
      status: "unsatisfied"
      phase: "Phase 13 + Phase 15"
      claimed_by_plans: ["13-13-PLAN.md"]
      completed_by_plans: []
      verification_status: "passed (Phase 13 on-screen half only); never claimed by any Phase 15 plan"
      evidence: "src/lib/email.ts:141 — subject is `Your FitOut booking is confirmed — ${spaceTitle}`; the FIT- reference appears only in the body at :137. D-78 handed the subject-line half to Phase 15; grep -rln 'TRUST-02' over .planning/phases/15-*/ returns 0 files. Phase 15 plan 15-03 (commit 85ceeda) rewrote this exact function for EMAIL-01's shell adoption with requirements: [EMAIL-01] only."
    - id: "TRUST-03"
      status: "unsatisfied"
      phase: "Phase 13 + Phase 15"
      claimed_by_plans: ["13-11-PLAN.md"]
      completed_by_plans: []
      verification_status: "passed (Phase 13 on-screen half only); never claimed by any Phase 15 plan"
      evidence: "src/lib/email.ts:136-138 — sendBookingConfirmed's paragraphs array is one sentence with no cancellation-policy clause, percentage or date. Same root cause and same commit as TRUST-02."
  integration:
    - seam: "Phase 13 -> Phase 15 (transactional email)"
      resolved: "2026-08-31 by quick task 260831-rpt"
      severity: "blocker"
      requirements: ["TRUST-02", "TRUST-03"]
      detail: "D-78 deferred two email clauses from Phase 13 to Phase 15. Phase 15 shipped the email shell (EMAIL-01/02/03) and edited the exact function that needed both changes, but was never assigned either requirement ID. The handoff was recorded on both sides and executed on neither."
  flows: []
partial:
  - id: "STATE-05"
    reason: "Support path code-complete behind D-64's guard; renders nothing while SUPPORT_EMAIL is null. src/lib/site.ts:70 is the only line that changes. Blocked on a business fact, not code."
  - id: "TRUST-01"
    reason: "Same as STATE-05 — every clause but the support path renders on all ten status branches."
bookkeeping_only:
  - id: "DS-09"
    evidence: "17-05-SUMMARY.md: five hand-rolled h-11 Buttons converted to size=touch; tests/design/brand-recipe.test.ts flipped from advisory toBeLessThanOrEqual(6) to blocking toBe(0) and scans all of src/ returning 0. The plan's frontmatter claimed GATE-02 only, so the box was never ticked."
  - id: "RESP-04"
    evidence: "17-VERIFICATION.md Truth 2 — VERIFIED, one escalated residual ([17-D13]), unchanged."
  - id: "GATE-02"
    evidence: "17-VERIFICATION.md Truth 3 — VERIFIED, gap closed; e2e/axe-sweep.spec.ts 60 passed / 36 skipped re-run independently."
  - id: "GATE-06"
    evidence: "17-VERIFICATION.md Truth 4 plus independent re-check: drizzle/ ends at 0025_audit_resolved_by.sql, last touched by quick task 260811-fh6 on 2026-08-11, before Phase 10's first commit. No commit in any milestone phase touches drizzle/*.sql."
nyquist:
  compliant_phases: ["10", "11", "12", "13", "15", "16.1", "17", "17.1"]
  partial_phases: ["14", "16"]
  missing_phases: ["13.1"]
  overall: "8/11 compliant"
tech_debt:
  - phase: "17"
    items:
      - "28-entry escalate-class ledger; 17-D1/D2/D3 closed by Phase 17.1, 17-D28 closed by quick 260831-9qx; the sticky-bar band and the PayMongo interception seam addressed by 17.1-03..07 without an explicit ID claim. Roughly 22 entries remain open for PM review."
      - "PROCESS: none of Phase 17's seven Playwright specs (axe-sweep, keyboard-composites, one-tree, overflow-320, mobile-booker-path, host-headings, auth-keyboard) run in CI. Pre-existing decision D-24, not introduced by Phase 17, but it is why the GATE-02 gap survived four review passes."
  - phase: "10"
    items: ["16 deferred items filed during the phase"]
  - phase: "14"
    items: ["11 deferred items; nine GATE-VRT baselines never generated (--project=visual does not exist on win32); nyquist_compliant: false"]
  - phase: "16"
    items: ["10 deferred items (D2..D9 open); nyquist_compliant: false"]
  - phase: "15"
    items: ["9 deferred items. (WR-05 was listed here as open and unfixed — CORRECTED 2026-08-31: it was closed by commit 3494fba on 2026-08-25, before 15-VERIFICATION.md re-confirmed it as open. This audit inherited that stale claim. The allow-list is now pinned build-blocking by tests/design/email-preview-harness-env.test.ts.)"]
  - phase: "13.1"
    items: ["6 deferred items; no VALIDATION.md exists for this inserted phase"]
  - phase: "13"
    items: ["5 deferred items; four manual-only walks (WALK A-D) outstanding"]
  - phase: "quick-260831-ndc"
    items: ["3 deferred items"]
---

# Milestone Audit: v1.1 — Front-End Polish & Placeholder Design System

**Status:** ⚡ `tech_debt` — *was `gaps_found` when written; the one blocker was closed the same day, see Resolution Log at the foot of this file*
**Audited:** 2026-08-31
**Phases in scope:** 10, 11, 12, 13, 13.1, 14, 15, 16, 16.1, 17, 17.1 (+ quick task `260831-ndc`)

## Verdict in one paragraph

The milestone did what it set out to do. The token contract exists, both themes ship, the gate
machinery is real and capable of failing, and every one of the eleven phases carries a
VERIFICATION.md that a verifier signed. Seven of eight cross-phase seams are wired, and all five
end-to-end flows trace complete. **One thing is genuinely broken:** a two-clause handoff from Phase 13
to Phase 15, recorded on both sides under decision D-78 and executed by neither. Separately, four
requirements are satisfied in the tree but were never ticked, which made the milestone look worse than
it is; correcting those is bookkeeping, not work.

## Requirements: 69/71 satisfied

79 requirements were defined. Eight (`SEARCH-06..09`, `MAP-01..04`) were deferred wholesale to backlog
999.3 by **D-141** before any plan was written — they are out of scope, not gaps. Of the 71 in scope:

| Disposition | Count | IDs |
|---|---|---|
| Satisfied and recorded | 60 | — |
| Satisfied but unticked — **ticked 2026-08-31** | 4 | `DS-09`, `RESP-04`, `GATE-02`, `GATE-06` |
| Unsatisfied at audit time — **closed 2026-08-31** | 2 | `TRUST-02`, `TRUST-03` |
| Amended by PM ruling — **closed 2026-08-31** | 1 | `DS-11` |
| Closed by human walk — **2026-08-31** | 2 | `BFLOW-08` (WALK A), `TRUST-05` (WALK B) |
| Partial — blocked on one business fact | 2 | `STATE-05`, `TRUST-01` |

The two unsatisfied IDs forced `gaps_found` when this audit was written. Both were closed the same
day by quick task `260831-rpt`, and the four unticked IDs were flipped in `REQUIREMENTS.md`. **No
unsatisfied requirement remains**, so the milestone now sits at `tech_debt`: the five partials are all
blocked on a human decision or a business fact, not on code. `DS-11` was subsequently closed by PM
ruling, bringing the total to 67/71.

### The four that are done but unrecorded

Phase 17's own verification says this in plain words:

> Flipping these four rows (and the four checkboxes) to `Complete` in both locations is process
> bookkeeping this verification surfaces but does not perform — it is the one remaining mechanical
> step for whoever closes this phase out.

Only `RESP-03` was ever flipped. `RESP-04`, `GATE-02` and `GATE-06` are verified true at `HEAD` and
still read `[ ] Pending` in both the checkbox list and the traceability table. `DS-09` is the same
story from a different phase: plan 17-05 converted the last five hand-rolled `h-11` buttons onto
`size="touch"` and flipped the adoption gate from advisory to blocking at `toBe(0)`, but its
frontmatter claimed `GATE-02` only, so `DS-09` was never ticked either.

**Consequence:** the ROADMAP progress table's note that "RESP-03/RESP-04/GATE-02/GATE-06 remain
Pending by design" is no longer true. They remain Pending by omission.

### The two that are genuinely open — the Phase 13 to 15 email handoff

This is the one finding that would make a "milestone complete" claim inaccurate.

**Decision D-78** forbade Phase 13 from touching the email shell and handed two clauses to Phase 15:

- **`TRUST-02`** — the booking reference must appear in the confirmation email's **subject line**.
- **`TRUST-03`** — the cancellation policy must be disclosed **in the confirmation email**, with
  concrete dates.

Phase 13 delivered both on-screen halves and un-ticked both IDs specifically because the email halves
were owed. The handoff is recorded in five separate Phase 13 artifacts, and Phase 14's research doc
repeats it. Phase 15 then shipped the email shell — and never received either ID.

Verified in the code. The subject, at `src/lib/email.ts:141`, carries the space title and nothing
else; the FIT- reference appears only in the body at `:137`. And the body itself, at
`src/lib/email.ts:136-138`, is a single sentence — "You're booked at {space} on {when}. Booking
reference {ref}." — with no cancellation policy, no percentage, and no date.

What makes this a dropped handoff rather than an accepted deferral: Phase 15's plan **15-03**
(commit `85ceeda`) rewrote this exact function from a hand-built HTML string to `renderEmail({...})`
for EMAIL-01. It was editing the two lines that needed changing. Its frontmatter reads
`requirements: [EMAIL-01]`, and a grep for `TRUST-02` or `TRUST-03` across the entire Phase 15
directory returns **zero files**. Nobody decided to defer this; it fell between two phases.

REQUIREMENTS.md is honest about the gap — both IDs still read `[ ]` with the email half named as
outstanding. The documentation is accurate. The work is missing.

> **RESOLVED 2026-08-31 — quick task `260831-rpt`** (commits `265ca87`, `356f133`, `ecdfe9b`).
> Both clauses are closed and both IDs are now `[x]`.
> - **TRUST-02** — the subject is `Your FitOut booking is confirmed — {space} ({reference})`.
> - **TRUST-03** — the body carries the policy as a second paragraph in BOTH the html and the text
>   projection, with a concrete venue-local date.
>
> The sentence is composed exactly once, server-side in the emitter's read, by
> `composePolicyEmailLine()` (`src/lib/booking/policy-disclosure.ts`), which hands off to the SAME
> `policySummaryLine` the on-screen half renders — so not one percentage and not one hour figure is
> typed in the email layer, and a rung edit in `cancellation.ts` rewrites the confirmation email
> automatically. Both reds were WATCHED at assertion level before the implementation existed
> (`tests/notifications/confirmation-email.test.ts`, transcribed verbatim in the task's SUMMARY).
> D-78's own boundary held: `src/lib/email-shell.ts` is byte-identical and no send trigger was added,
> moved or removed, so `EMAIL-01/02/03` stay closed. `drizzle/` still ends at
> `0025_audit_resolved_by.sql` — `notification.payload` is `jsonb`, so the new `policyLabel` key
> needed no migration (GATE-06 holds).

### The five partials

None of these is a code defect, and none is closable by writing code:

| ID | What is missing | Who can close it |
|---|---|---|
| `STATE-05`, `TRUST-01` | A real monitored support address; `src/lib/site.ts:70` is the only line that changes | PM — a business fact. D-64 forbids a placeholder |

## Phases: 11/11 verified

All eleven phases carry a VERIFICATION.md. None reports `gaps_found`.

| Phase | Status | Score |
|---|---|---|
| 10 Design-System Foundation & Theme Runtime | `passed` | 5/5 criteria; 16/18 req IDs |
| 11 Quality Gates, Pattern Layer & App Shell | `human_needed` | 11/11 truths (1 declared exception) |
| 12 Booker Path | `passed` | 17/17 must-haves |
| 13 Confirmation, Bookings & Trust | `human_needed` | 9/9 characterised; 2 complete + 7 code-complete partial |
| 13.1 Payment Reconciliation | `human_needed` | 6/6 must-haves, `gaps_remaining: []` |
| 14 Host Tooling | `human_needed` | 5/5 must-haves |
| 15 Auth, Profile & Transactional Email | `human_needed` | 5/6 (EMAIL-03 walked after verification) |
| 16 Image Crop & Framing | `passed` | 4/4 requirements |
| 16.1 Upload Hardening & Storage Economy | `passed` | 6/6 criteria |
| 17 Cross-Cutting Audit | `passed` | 4/4 must-haves |
| 17.1 Close Phase 17 Escalations | `passed` | 9/9 must-haves |

The five `human_needed` phases are open on manual walks and business facts, not on unfinished code.

## Integration: 8/8 seams wired (7/8 at audit time)

| # | Seam | Verdict |
|---|---|---|
| 1 | Token contract (10 to 11..17) | Wired — scan scope never widened; no later phase touched the leak-pattern config |
| 2 | Pattern layer (11 to 12..17) | Wired — each phase deletes its `ALLOWED_RAW_CARD` rows rather than adding them |
| 3 | App shell (11 to everywhere) | Wired — the checkout carve-out is SHELL-03 by design |
| 4 | Theme runtime (10 to everywhere) | Wired — one `data-theme` setter outside the provider, the deliberate `/dev/theme` preview |
| 5 | **Phase 13 to 15 email (D-78)** | ~~BROKEN~~ → **Wired 2026-08-31** by quick task `260831-rpt` |
| 6 | Phase 13 to 13.1 reconciliation | Wired — all five `RetireTrigger` members at their real call sites |
| 7 | Phase 16 to 16.1 uploads | Wired — two paths by design (avatar server-side, listing signed-widget), non-conflicting |
| 8 | quick `260831-ndc` to Phase 14 | Wired — mounted in `weekly-hours-editor.tsx:527`; server action byte-unchanged |

### GATE-06 — independently confirmed clean

`drizzle/` ends at `0025_audit_resolved_by.sql`, committed 2026-08-11 by quick task `260811-fh6` —
**before** Phase 10's first commit the same day. No commit under any milestone phase, or under the
quick task, touches `drizzle/*.sql`. The milestone shipped zero schema migrations.

## Flows: 5/5 traced complete

| Flow | Verdict |
|---|---|
| A search to listing to checkout to payment redirect | Complete |
| B payment return to confirmation to decay to receipt | Complete (mechanism); WALK A/B are the declared manual gaps |
| C host approve, booker sees it | Complete |
| D signup to profile to avatar crop, renders app-wide | Complete |
| E availability copy-to-all, booker sees new availability | Complete |

## Nyquist coverage: 8/11 compliant

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 10, 11, 12, 13, 15, 16.1, 17, 17.1 | exists | true | — |
| 14 | exists | **false** | `/gsd:validate-phase 14` |
| 16 | exists | **false** | `/gsd:validate-phase 16` |
| 13.1 | **missing** | — | `/gsd:validate-phase 13.1` |

## Tech debt

Roughly **85 deferred items** across the milestone's phase ledgers. The two that matter at milestone
scale:

**1. Phase 17's escalate-class ledger — ~22 of 28 entries still open.** `17-D1/D2/D3` were closed by
Phase 17.1 and `17-D28` by quick task `260831-9qx`; the sticky-bar clearance band and the PayMongo
interception seam were addressed by `17.1-03..07`. The remainder were filed for PM review and never
dispositioned.

**2. The milestone's closing proof is not enforced by CI.** None of Phase 17's seven Playwright specs
run in any CI job — only `e2e/price-parity.spec.ts` and `--project=visual` do. This is **D-24**, a
pre-existing project-wide decision that predates Phase 17, but Phase 17's own verifier names it as the
concrete reason the GATE-02 gap survived four independent review passes: the assertion worked exactly
as designed and nothing was running it. Every gate this milestone declares is real; most of them are
only run by hand.

Also open: Phase 14's nine GATE-VRT baselines were never generated (`--project=visual` does not exist
on win32).

⚠ **This paragraph originally also claimed Phase 15's WR-05 was unfixed. That was wrong** — inherited
from `15-VERIFICATION.md`, which re-confirmed WR-05 as open on 2026-08-25 when commit `3494fba` had
already closed it earlier that same day. `scripts/send-email-previews.ts:238` hydrates all three keys
on both paths, so the CTA on messages 6 and 7 resolves correctly and nothing blocks the email walk.
Corrected 2026-08-31, and the allow-list is now pinned build-blocking by
`tests/design/email-preview-harness-env.test.ts` — the absence of which is exactly why a fixed defect
could go on being reported as open for six days.

## Documentation accuracy

| Defect | Where |
|---|---|
| Four satisfied requirements read `[ ] Pending` | `REQUIREMENTS.md` checkbox list + traceability table |
| "RESP-03/RESP-04/GATE-02/GATE-06 remain Pending by design" is no longer true | `ROADMAP.md` progress table, Phase 17 row |
| Phase 13 still renders `[ ]` in the phase list while the progress table records it verified | `ROADMAP.md` |
| Phase 10's F-01 (THEME-02 marked Pending despite being built) | **Resolved** — THEME-02 now reads `[x]` |

## What has to happen before v1.1 can be called complete

1. **Close `TRUST-02` and `TRUST-03`** — add the FIT- reference to `sendBookingConfirmed`'s subject and
   a cancellation-policy sentence to its body, composed from the same `composePolicyDisclosure()` the
   on-screen halves already use. This is the only code in this list.
2. **Rule on `DS-11`** — amend the requirement text to name the two exceptions, or leave it Pending
   deliberately. Phase 11's UAT has been waiting on this since 2026-08-17.
3. **Tick the four** — `DS-09`, `RESP-04`, `GATE-02`, `GATE-06` in both the checkbox list and the
   traceability table.
4. **Decide `SUPPORT_EMAIL`** — supply a monitored address and `STATE-05` + `TRUST-01` close with a
   one-line change, or accept them as deferred.
5. **Accept or schedule** WALK A and WALK B (`BFLOW-08`, `TRUST-05`), and disposition Phase 17's ~22
   open escalations.

Items 2-5 are PM decisions. Item 1 is a small, well-specified change to one function.

---

*Audited against the codebase at `HEAD` on branch `dev`, not against SUMMARY claims.*

---

## Resolution Log

**2026-08-31 — the blocker closed.** Quick task `260831-rpt` closed `TRUST-02` and `TRUST-03`
(commits `265ca87` red, `356f133` composer + payload + emitter, `ecdfe9b` email layer, `4f8d5f2`
docs). The confirmation email's subject now carries the FIT- reference, and the cancellation policy
rides as a second paragraph in both the HTML and plain-text projections, composed once by
`composePolicyEmailLine()` from the same `LADDER` the refund engine evaluates. Watched red before the
change (4 assertion-level failures), green after; `email-shell.ts` byte-identical; `drizzle/` still at
`0025`. Two decisions recorded: **D-RPT-01** (the sentence travels pre-composed on the notify payload,
because D-86/D-91 forbid raw dates and money internals there) and **D-RPT-02** (a lib-to-component
import for `policySummaryLine`, with the hoist-into-lib cleanup deferred).

**2026-08-31 — the four ticked.** `DS-09`, `RESP-04`, `GATE-02` and `GATE-06` flipped to `[x]` in both
the checkbox list and the traceability table. **No code changed** — all four were already true at
`HEAD` when this audit ran, and Phase 17's own verification named the flip as the one remaining
mechanical step. `REQUIREMENTS.md` now records 66 of 71 in-scope requirements complete.

**Still open (all five are partials, none is code):** `DS-11` needs a PM ruling on whether two
measured structural refusals satisfy the requirement text; `STATE-05` and `TRUST-01` need a monitored
support address at `src/lib/site.ts:70`; `BFLOW-08` needs WALK A and `TRUST-05` needs WALK B. The tech
debt recorded above — Phase 17's ~22 undispositioned escalations and the D-24 CI gap — is unchanged.

**2026-08-31 — DS-11 closed by ruling.** The PM amended the requirement text to read "...with two
named, measured exceptions", following the D-26 precedent. Recorded in three places: the requirement
text and traceability row in `REQUIREMENTS.md`; `RowCard`'s `Replaces` list in `11-UI-SPEC.md` (the
`ResultCard` half was already corrected by plan 14-13 on 2026-08-23); and Phase 11's UAT scenario 4,
now PASS. No code changed — `tests/design/card-pattern-coverage.test.ts` already enforced both
exclusions by name and stays green at 11 passed.

**Running total: 67 of 71 in-scope requirements complete.** The remaining four are `STATE-05` and
`TRUST-01` (one monitored support address at `src/lib/site.ts:70` closes both), `TRUST-05` (WALK B) and
`BFLOW-08` (WALK A).

**2026-08-31 — both walks done.** `TRUST-05` closed by **WALK B**: booking `FIT-TBQWWBTR` printed
through Chrome's own print pipeline with `printBackground: false`, once per theme; no black flood in
either, reference and Total readable, status a word not a pill, zero visible controls, single A4 page
so the itemisation cannot split. `BFLOW-08` closed by **WALK A**: a real hosted PayMongo `sk_test_`
checkout (booking `f6cef95d` / `FIT-6XNSQXG0`, ₱2,100.00) paid on PayMongo's own page and confirmed by
a real `checkout_session.payment.paid` webhook over the tunnel; the PM confirmed the moment renders,
the URL sheds `?paid=1`, a reload shows the ordinary detail page, and Back leaves for PayMongo. Both
recorded in `13-UAT-LOG.md`.

The same walk incidentally proved **D-RPT-01** end-to-end. The real notification payload for that
booking reads `"policyLabel": "50% refund until Tue, Sep 1, 5:00 AM (Makati time)"` and
`"referenceLabel": "FIT-6XNSQXG0"` — a pre-composed sentence with a concrete venue-local date, the
percentage derived from `LADDER` rather than typed, travelling on the payload exactly as designed.

**Two new open items, neither blocking:**
- **D-101.1's pending-spinner cap is unobserved.** The webhook won the race on both payments, so the
  settling state never rendered. Needs a deliberately delayed webhook, not another ordinary payment.
- **Local UAT email cannot reach a seeded address.** `RESEND_API_KEY` *is* set, so `send()` takes the
  real Resend branch; Resend then rejects any recipient other than the account owner with HTTP 403
  (`validation_error`) until a domain is verified. The confirmation email for `FIT-6XNSQXG0` was
  correctly composed and dispatched, and rejected at the rail because it was addressed to
  `host@fitout.test`. This is env, not code — but it means **no v1.1 email has ever been read in a real
  client at its real recipient**, which is the standing `EMAIL-03` caveat.

**Running total: 69 of 71.** The last two are `STATE-05` and `TRUST-01`, both closed by one monitored
support address at `src/lib/site.ts:70`.
