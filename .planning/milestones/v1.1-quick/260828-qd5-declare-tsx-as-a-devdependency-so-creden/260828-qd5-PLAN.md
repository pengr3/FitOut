---
phase: quick-260828-qd5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md
autonomous: true
requirements: [W-3]

must_haves:
  truths:
    - "`npm ls tsx` exits 0 and shows a ROOT-level `tsx@4.22.4`, with every other tsx line also reading 4.22.4 (deduped) and no `invalid` / `missing` / `extraneous` marker"
    - "The version installed after the change is byte-identical to the version installed before it: 4.22.4, same `resolved` URL, same `integrity` hash"
    - "A future `npm install` cannot silently move the executor under the seven tsx scripts — the declared range admits exactly one version"
    - "`npm run cloudinary:preset` still executes under tsx: it compiles the `.ts`, resolves the `@/` alias, and reaches its arg-parse hard stop"
    - "`package.json` and `package-lock.json` remain in sync, so the four CI jobs that run `npm ci` still resolve the same tree"
    - "Warning W-3 in `16.1-SECURITY.md` is recorded as resolved, so the next audit run does not re-raise it"
  artifacts:
    - path: "package.json"
      provides: "Root-level exact declaration of the tsx executor"
      contains: '"tsx": "4.22.4"'
    - path: "package-lock.json"
      provides: "Root dev dependency entry promoting the already-resolved tsx@4.22.4"
      contains: '"tsx": "4.22.4"'
    - path: ".planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md"
      provides: "W-3 resolution record, in the W-1 precedent's format"
      contains: "W-3 resolved"
  key_links:
    - from: "package.json devDependencies"
      to: "package-lock.json packages[\"\"].devDependencies"
      via: "npm lockfile regeneration"
      pattern: '"tsx": "4\.22\.4"'
    - from: "package.json scripts (7 tsx entries)"
      to: "node_modules/.bin/tsx"
      via: "npm run resolution of a now-declared bin"
      pattern: "tsx scripts/"
---

<objective>
Declare `tsx` in `package.json`'s `devDependencies` at an exact pin, and carry that declaration into
`package-lock.json`, so the seven npm scripts that run under it — and in particular the one that runs
with `CLOUDINARY_API_SECRET` in its environment — execute under an interpreter whose version this
repo's own manifest fixes, rather than whatever version `drizzle-kit` or `vite` happens to hoist.

Purpose: closes Warning **W-3** from the Phase 16.1 security audit.

**This is not a phase violation and must not be written up as one.** Phase 16.1 installed nothing,
declared no dependency, and left `package-lock.json` untouched — `T-16.1-SC` is closed and stays
closed. 16.1-03's executor *saw* the transitive and recorded it deliberately ("the cost recorded
rather than absorbed", `16.1-03-SUMMARY.md:95`), quoting the rule from
`scripts/verify-workflows.mjs:116-121`: **the fix for this class of diagnostic is a dependency
decision to raise, never the deletion of the thing that runs the script.** This task is that
dependency decision being taken. Six of the seven consumers pre-date the phase; the phase added the
seventh. What changed is the stakes, not the fault.

Output: one added manifest line, its lockfile consequence, and a resolution note on W-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@package.json

<measurements date="2026-08-28">
Taken on this machine immediately before planning. npm 11.8.0, Node v24.13.0.

`npm ls tsx` — the state being changed:

    fitout@0.1.0 C:\Users\Admin\Roaming\FitOut
    +-- @vitejs/plugin-react@6.0.2
    | `-- vite@8.0.16
    |   `-- tsx@4.22.4 deduped
    `-- drizzle-kit@0.31.10
      `-- tsx@4.22.4

`package-lock.json:16687-16691` — tsx is present, hoisted to the top level, and flagged
`"devOptional": true` (reached only via dev/optional paths, no direct declaration):

    "node_modules/tsx": {
      "version": "4.22.4",
      "resolved": "https://registry.npmjs.org/tsx/-/tsx-4.22.4.tgz",
      "integrity": "sha512-X8EX+XV4QR5xCsrgxaED954zTDfY8KqlDtskKEL0cHhyS/P8b4IFOvGDQpsC9Q1XnLq915wEfwwY/zzskCtmhg==",
      "devOptional": true,

The **seven** consumers, read from `package.json:17-26` rather than from memory:

| Script | Command | Since |
|--------|---------|-------|
| `db:seed` | `tsx scripts/seed.ts` | pre-16.1 |
| `db:test:setup` | `tsx scripts/db-test-setup.ts` | pre-16.1 |
| `cloudinary:preset` | `tsx scripts/cloudinary-preset.ts` | **Phase 16.1** |
| `email:previews` | `tsx scripts/send-email-previews.ts` | pre-16.1 |
| `ops:alerts` | `tsx scripts/ops-alerts.ts list` | pre-16.1 |
| `ops:alerts:resolve` | `tsx scripts/ops-alerts.ts resolve` | pre-16.1 |
| `ops:alerts:history` | `tsx scripts/ops-alerts.ts history` | pre-16.1 |

CI runs `npm ci` in four jobs (`ci.yml:568,725,828`, `baselines.yml:296`), so a manifest and a
lockfile that disagree is not a cosmetic defect — it is `EUSAGE` on every one of them.

`package.json:30` declares `"postinstall": "node scripts/patch-kysely-adapter.mjs"`. That patcher is
idempotent by construction (`patch-kysely-adapter.mjs:13`, marker-guarded), but Task 1 avoids
invoking it at all — see the install-command reasoning.
</measurements>

<the_version_decision>
**Declared: `"tsx": "4.22.4"` — exact, no caret, no tilde.**

Four reasons, in order of weight:

1. **This is a PROMOTION, not a version move.** 4.22.4 is what is installed right now and what every
   consumer already resolves to. The correct diff adds a root declaration of the version already in
   the tree and changes no `version` / `resolved` / `integrity` line anywhere. `^4.22.4` would leave
   the door open for a future `npm install` to pull 4.23.x and quietly turn a hygiene fix into an
   unreviewed interpreter upgrade — the exact failure mode W-3 exists to close, re-entering through
   semver instead of through hoisting.
2. **The point of W-3 is a version this repo FIXES.** "Pinned by a manifest this repo owns" is not
   satisfied by a range. A caret moves the resolution luck from npm's hoisting order to npm's semver
   selection; it does not remove it. For the interpreter of a script that loads
   `CLOUDINARY_API_SECRET`, an exact pin is the whole deliverable.
3. **Repo precedent.** The same `devDependencies` block already exact-pins two tools whose version is
   load-bearing: `"@playwright/test": "1.60.0"` and `"eslint-config-next": "16.2.7"`. This is the
   established local convention for that category, not a new one.
4. **It keeps ONE copy and makes divergence visible.** `drizzle-kit@0.31.10` and `vite@8.0.16` both
   resolve to 4.22.4 today, so declaring 4.22.4 keeps a single hoisted install. If a future
   drizzle-kit or vite ever demands a tsx outside 4.22.4, npm will nest a second copy and `npm ls tsx`
   will print it — a reviewable event, instead of a silent swap of the binary under seven scripts.

**Placement:** alphabetically between `"tailwindcss": "^4.3.0"` and `"typescript": "^5"` in
`devDependencies`, matching the block's existing ordering.
</the_version_decision>

<scope_fence>
In scope: `package.json`, `package-lock.json`, and the W-3 resolution note in `16.1-SECURITY.md`.

**Explicitly OUT of scope — do not do these even though they will look tempting:**

- **Do not "fix" the other six scripts.** No rewrites, no `npx` prefixes, no `--` argument changes,
  nothing under `scripts/`. Declaring the executor is the entire fix; the scripts were already
  correct and are already covered by the same declaration.
- **Do not touch `scripts/cloudinary-preset.ts`.** Its header paragraph describing tsx as an
  undeclared transitive becomes historically-scoped rather than wrong, and that file is asserted over
  by `tests/design/cloudinary-preset-script.test.ts` with a regex-based comment stripper that has
  already produced one false RED on this exact header (`16.1-03-SUMMARY.md:105-112`). Editing it is
  out of scope and carries a known booby-trap. Leave it.
- **Do not promote `yaml`.** `scripts/verify-workflows.mjs:115-121` records a second undeclared
  transitive with the same shape. It is a separate dependency decision on the CI path and a separate
  task. Noticing it is correct; fixing it here is scope creep.
- **Do not upgrade anything.** No `npm update`, no `npm audit fix`, no lockfile refresh.
</scope_fence>

<supply_chain_note>
Read before Task 1, because this plan contains a manifest dependency line and the reflex is to
demand a package-legitimacy checkpoint.

**The legitimacy gate does not fire here, and that is an evidence claim, not a waiver.** The gate
exists to stop *new code entering the tree* unreviewed. No new code enters:
`tsx@4.22.4` is already installed, already hoisted at `node_modules/tsx`, already committed to
`package-lock.json` with the `sha512-X8EX+XV4…` integrity hash quoted above, and is already
transitively required by two declared devDependencies (`drizzle-kit`, `vite`). The operation adds a
pointer to that existing resolution.

The proof obligation is therefore inverted and mechanical: **Task 1's acceptance criteria require the
`resolved` URL and `integrity` hash to be UNCHANGED in the diff.** If either moves, the operation was
not a promotion, and the executor must stop rather than proceed.
</supply_chain_note>

<no_test_run>
**This plan calls for no vitest run, deliberately.** Nothing under `src/` or `tests/` changes, so a
suite run would test nothing this task touched while carrying real cost:
`tests/global-setup.ts` TRUNCATEs every `public` base table in `fitout_test` on every run, so a
gratuitous run destroys any fixture state a concurrent process is holding.

If a suite run happens anyway for some reason, it must be a **single scoped run, alone** — not
adjacent to another `vitest` or an `npm run build` (which itself invokes `vitest run --config
vitest.design.config.ts` via `package.json:11`). **A first red immediately after another vitest or
build run is not trustworthy evidence**; re-run it alone before believing it.
</no_test_run>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare tsx@4.22.4 in devDependencies and promote it in the lockfile</name>
  <files>package.json, package-lock.json</files>
  <action>
Add exactly one line to `package.json`'s `devDependencies`: `"tsx": "4.22.4",` placed between
`"tailwindcss": "^4.3.0",` and `"typescript": "^5",`. Exact version, no caret — the reasoning is in
`<the_version_decision>` above and must be preserved in the commit message and SUMMARY, because a
future reader who sees a bare pin and "helpfully" carets it reopens W-3.

Then carry the declaration into the lockfile with:

    npm install --package-lock-only --no-audit --no-fund

`--package-lock-only` is chosen over a plain `npm install` on purpose: it rewrites only
`package-lock.json`, touches nothing under `node_modules/`, and runs no lifecycle scripts — so the
`postinstall` kysely patcher does not re-run and the installed tree cannot churn as a side effect of
a manifest edit. The tree already satisfies the new declaration (tsx@4.22.4 is hoisted at
`node_modules/tsx`), so no install work is actually needed; only the lock's bookkeeping is stale.

**The expected diff, stated in advance so a wrong one is recognisable rather than rationalised:**

1. `packages[""].devDependencies` gains `"tsx": "4.22.4"` — one added line.
2. `packages["node_modules/tsx"]` flips its reachability flag `"devOptional": true` → `"dev": true`.
   The same flag may flip on tsx's own subtree (`esbuild` and its platform binaries) for the same
   reason. Flag-only changes are expected and fine.
3. **Nothing else.** No `version`, `resolved`, or `integrity` line changes anywhere in the file, and
   no `node_modules/...` package entry added or removed.

If the diff exceeds that shape — a version moves, a package appears or disappears, an integrity hash
changes — **do not commit and do not rationalise it**. Run `git checkout -- package-lock.json`, keep
the `package.json` edit, and instead make the two lockfile edits above by hand (add the root
`devDependencies` entry; change `devOptional` to `dev` on `node_modules/tsx`), then re-run the same
verification gates. Record whichever path was taken, and why, in the SUMMARY.

If the command fails for lack of network, that is a stop-and-report, not a reason to reach for
`--offline` or `--force`.
  </action>
  <verify>
    <automated>
# Run from the repo root, in this order. Every gate must pass.

# --- G1: the manifest declares the exact pin (anchors the whole audit; must be non-vacuous first) ---
grep -q '"tsx": "4\.22\.4"' package.json && echo "G1 PASS: manifest declares tsx 4.22.4" || echo "G1 FAIL"

# --- G2: the lockfile diff EXISTS and carries the promotion (proves G3/G4's zero-gates are meaningful) ---
git diff -U0 package-lock.json | grep -c '^+.*"tsx": "4\.22\.4"'
# expect >= 1

# --- G3: no version/resolved/integrity line moved anywhere ---
git diff -U0 package-lock.json | grep -cE '^[+-][[:space:]]*"(version|resolved|integrity)"'
# expect exactly 0

# --- G4: no package entry added or removed ---
git diff -U0 package-lock.json | grep -cE '^[+-][[:space:]]*"node_modules/'
# expect exactly 0

# --- G5: the installed tree agrees with the manifest, and every tsx is 4.22.4 ---
npm ls tsx; echo "npm ls exit: $?"
# expect exit 0 (nonzero means invalid/missing/extraneous), a ROOT-level `tsx@4.22.4` entry,
# and no `invalid` / `missing` / `extraneous` / `UNMET` marker in the output
npm ls tsx 2>&1 | grep -o 'tsx@[0-9][0-9.]*' | sort -u
# expect exactly one line: tsx@4.22.4

# --- G6: the declared binary resolves and reports the pinned version ---
npx tsx --version
# expect: tsx v4.22.4  (with a node line beneath it)

# --- G7: RUNTIME PROOF — a real one of the seven scripts actually executes under the declared tsx ---
npm run cloudinary:preset 2>&1 | grep -q 'FATAL: no mode given' && echo "G7 PASS: script ran under tsx" || echo "G7 FAIL"
    </automated>
  </verify>
  <done>
- `package.json` `devDependencies` contains `"tsx": "4.22.4"` between `tailwindcss` and `typescript`.
- `package-lock.json`'s diff is confined to the promotion shape enumerated in `<action>`:
  G2 >= 1, **G3 == 0, G4 == 0**. (G2 is what makes those two zeros load-bearing rather than vacuous —
  a zero diff would also satisfy G3 and G4, so G2 must be checked first and must be non-zero.)
- `npm ls tsx` exits **0** and prints `tsx@4.22.4` and nothing but `tsx@4.22.4`, now including a
  root-level entry. The version installed is the version that was installed before the change.
- `npx tsx --version` prints `tsx v4.22.4`.
- **G7 is the acceptance criterion that matters most: a declared-but-broken executor is strictly
  worse than an undeclared working one.** `npm run cloudinary:preset` with no arguments prints
  `FATAL: no mode given.` and **exits 1 — that nonzero exit IS the pass signal**, not a failure.

  Why this command is the safe choice, and why the obvious alternatives are not:
  `scripts/cloudinary-preset.ts:808-843` parses arguments and fires every hard stop **before** line
  846's `loadFromEnvLocal(CREDENTIAL_KEYS)` — its own header says so ("ARGUMENTS FIRST, BEFORE ANY
  CREDENTIAL WORK … so every hard stop below is reachable with no environment at all"). So this path
  opens no socket, reads no `.env.local`, needs no Cloudinary credentials, and mutates nothing —
  while still proving the full chain: tsx resolved, tsx compiled the `.ts`, and tsx honoured the
  `@/` alias to import `@/lib/listing/upload-policy` at line 110 (module load precedes `main`).
  `--apply` MUTATES live vendor state and is forbidden here. `--verify` is read-only but needs live
  credentials and network, which may be absent, so its exit 3 would be ambiguous. `db:seed` and
  `db:test:setup` are destructive; `email:previews` sends mail; `ops:alerts` needs a live DB.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record W-3 as resolved in the Phase 16.1 security audit</name>
  <files>.planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md</files>
  <action>
Append a resolution note directly beneath the W-3 row in the `## Warnings (non-blocking)` table,
following the format the file already uses for **W-1** at `16.1-SECURITY.md:106` verbatim — a bolded
`**W-3 resolved (2026-08-28, quick task `260828-qd5`).**` lead, then the measurement.

State, in a short paragraph: `tsx` is now declared in `devDependencies` at the exact pin `4.22.4`;
the change was a promotion of the already-installed resolution, evidenced by `resolved` and
`integrity` being unchanged in the lockfile diff; `npm ls tsx` reports `tsx@4.22.4` with a root-level
entry; and `npm run cloudinary:preset` was observed reaching its arg-parse hard stop under the
declared executor. Paste the actual `npx tsx --version` output as the record.

Add one sentence that carries the fact forward rather than closing it silently: **the sibling finding
`yaml`, recorded at `scripts/verify-workflows.mjs:115-121` and running on the CI path, has the same
shape and is NOT resolved by this task** — so a future audit reader is not misled by the symmetry.

Do not alter the W-3 row itself, do not renumber warnings, do not touch the threat register, and do
not change `T-16.1-SC`'s `closed` disposition or the audit-trail counts — the phase's own claim
("this phase installs nothing") remains true and this task is explicitly repo-level, not phase-level.
  </action>
  <verify>
    <automated>
# The resolution note exists and names the quick task
grep -c 'W-3 resolved' .planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md
# expect >= 1
grep -q '260828-qd5' .planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md && echo "PASS: task id recorded"

# The sibling finding is carried forward, not silently absorbed
grep -q 'verify-workflows' .planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md && echo "PASS: yaml sibling noted"

# The phase's own closed threat was not disturbed
git diff .planning/phases/16.1-upload-hardening-storage-economy/16.1-SECURITY.md | grep -cE '^[+-].*T-16\.1-SC'
# expect exactly 0
    </automated>
  </verify>
  <done>
- A `W-3 resolved (2026-08-28, quick task `260828-qd5`)` note sits beneath the warnings table in the
  same format as the existing W-1 note, carrying the actual `npx tsx --version` output as evidence.
- The note names the unresolved `yaml` sibling so the next audit does not read the symmetry as
  "both handled".
- The W-3 row, the threat register, `T-16.1-SC`'s `closed` disposition, and the audit-trail counts
  are untouched (diff gate == 0 on `T-16.1-SC`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `node_modules/` | third-party code enters the tree; the gate this task is about |
| `.env.local` → `scripts/cloudinary-preset.ts` | `CLOUDINARY_API_SECRET` crosses into a process whose interpreter this task pins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QD5-SC | Tampering | `npm install --package-lock-only` | mitigate | The operation must be a promotion, not a fetch. Gates G3/G4 in Task 1 require **zero** changed `version`/`resolved`/`integrity` lines and **zero** added/removed `node_modules/` entries; G2 proves the diff is non-empty so those zeros cannot pass vacuously. Any excess → revert the lockfile and hand-edit the two fields. |
| T-QD5-01 | Tampering | tsx version drift under the 7 scripts | mitigate | Exact pin `4.22.4` (not `^4.22.4`): a future `npm install` cannot select a different interpreter for the script that reads `CLOUDINARY_API_SECRET`. Divergence forced by a future `drizzle-kit`/`vite` becomes a visible nested entry in `npm ls tsx` rather than a silent hoist swap. |
| T-QD5-02 | Denial of Service | CI (`npm ci` × 4 jobs) | mitigate | A manifest/lock desync is `EUSAGE` on every CI job. Task 1's G5 (`npm ls tsx` exit 0) proves manifest, lock, and tree agree before commit. |
| T-QD5-03 | Spoofing | `tsx` package legitimacy | accept | No new artifact is fetched: 4.22.4 is already in the tree, already committed to the lock under `sha512-X8EX+XV4…`, and already required by two declared devDependencies. The legitimacy gate's trigger (new code entering the tree) does not fire; G3's unchanged-integrity gate is the mechanical proof. See `<supply_chain_note>`. |
</threat_model>

<verification>
The whole task is verified by Task 1's G1-G7 and Task 2's gates. The two that would be easiest to
skip and are the actual point:

- **G7** — the executor was proven by *running one of the seven scripts*, not by reading the manifest.
- **G2 before G3/G4** — the diff-shape zeros are only meaningful once the diff is known non-empty.

No vitest run. See `<no_test_run>` for why, and for the rule about distrusting a first red.
</verification>

<success_criteria>
- `tsx` appears in `package.json` `devDependencies` as `"tsx": "4.22.4"` — exact, reasoning recorded.
- `package-lock.json` promotes the existing resolution and moves no version, no `resolved`, no
  `integrity`, and no package entry.
- `npm ls tsx` exits 0; every tsx in the tree is 4.22.4; a root-level entry now exists.
- `npm run cloudinary:preset` reaches `FATAL: no mode given.` under the declared executor.
- W-3 is recorded resolved in `16.1-SECURITY.md`, with the `yaml` sibling explicitly carried forward.
- Nothing under `src/` or `scripts/` changed: `git status --porcelain` lists only `package.json`,
  `package-lock.json`, and `16.1-SECURITY.md`.
</success_criteria>

<output>
Create `.planning/quick/260828-qd5-declare-tsx-as-a-devdependency-so-creden/260828-qd5-SUMMARY.md` when done.

The SUMMARY must carry forward, because they are the reasons a future reader will otherwise undo this:
1. **Why the pin is exact and not `^4.22.4`** (the four reasons in `<the_version_decision>`).
2. **The actual lockfile diff shape observed**, and whether `--package-lock-only` produced it or the
   hand-edit fallback was needed.
3. **That `yaml` remains an undeclared transitive on the CI path** (`verify-workflows.mjs:115-121`) —
   an open, unclaimed sibling of W-3.
</output>
</content>
</invoke>
