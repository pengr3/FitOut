---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: verified
stopped_at: "MILESTONE v1.0 AUDITED + ARTIFACT GAPS CLOSED 2026-08-01. All 9 phases complete (107 plans) and all 9 now carry a VERIFICATION.md. The audit (.planning/v1.0-MILESTONE-AUDIT.md) found 7/7 cross-phase seams and 7/7 E2E flows WIRED with file:line evidence, tsc exit 0, and 1087 tests passing / 4 skipped / 0 failures. Its one blocking finding was an ARTIFACT gap, not a product gap: Phase 2 shipped without ever running its verification gate, so LIST-01..06 + PAY-04 appeared in no VERIFICATION.md anywhere and scored as orphaned. CLOSED by a retroactive 02-VERIFICATION.md (4/4 success criteria; 144 Phase-2 tests, 3/3 LIST-06 e2e, live psql schema + PostGIS check, and a grep proving payoutsEnabled has exactly two writers, both inside the signature-verified webhook). ALSO CLOSED: the two Nyquist gaps (01-VALIDATION was still status:planned/wave_0_complete:false and 03-VALIDATION still status:draft/nyquist_compliant:false, both pre-execution drafts never refreshed - every row re-run first-hand: tests/auth+tests/profile 14 files/39 tests, tests/availability 18 files/168 tests, e2e/availability 4/4); Phase 1 human items 1/2/5 discharged by real-browser e2e (login-persistence, password-reset, mode-switch = 4 passed); the stale BOOK-05/HOST-01 rows in 06-VERIFICATION; AVAIL-01..05 and BOOK-05/HOST-01 missing from SUMMARY frontmatter; 12 unticked REQUIREMENTS checkboxes; MANAGE-03 traceability status; ROADMAP Phase 9 checkbox + the Phase 8 Wave 10/11 plan list. THE AUDIT ALSO CORRECTED THE DOCS AGAINST THE CODE: all five Phase-5 WARNINGs (CR-01 stranded payout, WR-01/WR-02 refund handling, WR-03 webhook boot guard, WR-05 booking status default) are CLOSED in current source, as are Phase 4 build-env/lint deferrals and the Phase 7 root-relative email href - reporting them as open debt would have been wrong. WHAT REMAINS IS GENUINELY EXTERNAL, NOT WORK: PayMongo /v2 money movement + hosted Linked-Accounts KYC are sales-gated (standing since Phase 5), and GOOGLE_CLIENT_* / server-side CLOUDINARY_* are absent from .env.local. NEXT: /gsd-complete-milestone v1.0."
last_updated: "2026-08-01T12:50:00.000Z"
last_activity: 2026-08-01
progress:
  total_phases: 9
  completed_phases: 9
  total_plans: 107
  completed_plans: 107
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-03)

**Core value:** Find & book a space — search → real availability → reserve a time slot → pay, with confidence the booking is real.
**Current focus:** **MILESTONE v1.0 — ALL 9 PHASES COMPLETE AND VERIFIED; AUDITED 2026-08-01; READY FOR `/gsd-complete-milestone v1.0`.** 107/107 plans executed. All 9 phases now carry a VERIFICATION.md (Phase 2's was written retroactively on 2026-08-01 — it was the milestone audit's one blocking finding). Full report: `.planning/v1.0-MILESTONE-AUDIT.md`. Signal at audit time: **49/49 requirements satisfied, 7/7 cross-phase seams and 7/7 E2E flows WIRED with file:line evidence, `tsc --noEmit` exit 0, 1087 tests passing / 4 skipped / 0 failures.** Nyquist 9/9 after closing 01-VALIDATION and 03-VALIDATION (both were pre-execution drafts never refreshed, not real coverage gaps — every row was re-run first-hand). What remains open is external, not work: PayMongo's sales-gated Platforms/`/v2` beta (money movement + hosted KYC, standing since Phase 5) and the absent `GOOGLE_CLIENT_*` / server-side `CLOUDINARY_*` credentials. Worktrees are OFF (`workflow.use_worktrees: false`) so plans run SEQUENTIALLY on `dev`, one executor at a time.

<details><summary>Previous focus (Phase 09, superseded 2026-08-01)</summary>

**Current focus:** Phase 09 — open-capacity bookings — **all 16 original plans executed (2026-07-31) + 8 of 9 gap-closure plans (`09-17` … `09-25`); ONE gap plan remains: `09-23` (CR-06 + WR-01). PHASE NOT YET CLOSED — verification is the orchestrator's** (16 plans, 7 waves; checker passed 0 blockers). The automated gate closed in 09-15 and the human walkthrough (09-16) discharged the phase's ONLY manual-only verification with a real PayMongo `sk_test_` charge. CONTEXT + RESEARCH + VALIDATION + UI-SPEC + PATTERNS all on disk. Worktrees are OFF (`workflow.use_worktrees: false`) so plans run SEQUENTIALLY on `dev`, one executor at a time.

</details>

## Current Position

> ✅ **MILESTONE v1.0 AUDITED — ARTIFACT GAPS CLOSED (2026-08-01).** The audit's blocking finding was an
> **artifact** gap, not a product gap: Phase 2 shipped without ever running its verification gate, so
> LIST-01…06 + PAY-04 appeared in no VERIFICATION.md anywhere in the milestone and scored as *orphaned*.
> A retroactive `02-VERIFICATION.md` now closes them (4/4 success criteria; 144 Phase-2 tests, 3/3 LIST-06
> e2e, live `psql` schema + PostGIS check, and a grep proving `payoutsEnabled` has exactly two writers —
> both inside the signature-verified webhook). Also closed in the same pass: the two Nyquist drafts
> (01/03), Phase 1 human items 1/2/5 via real-browser e2e, the stale `BOOK-05`/`HOST-01` rows in
> `06-VERIFICATION.md`, missing SUMMARY frontmatter for AVAIL-01…05 and BOOK-05/HOST-01, 12 unticked
> REQUIREMENTS checkboxes, MANAGE-03's traceability status, the ROADMAP Phase 9 checkbox, and the
> Phase 8 Wave 10/11 plan list. **Next: `/gsd-complete-milestone v1.0`.**
>
> The audit also corrected the *documents against the code*: all five Phase-5 WARNINGs — including CR-01,
> the stranded-payout money-safety gap — are **closed in current source**, as are Phase 4's build-env and
> lint deferrals and Phase 7's root-relative email `href`. Reporting them as open debt would have been wrong.

---

> ✅ **GAP PLAN 09-23 DONE (2026-08-01) — CR-06 and WR-01 CLOSED. Gap position: 9 of 9 — THE LAST ONE. All 14 findings in `09-REVIEW.md` are now closed in code.** Two defects in one `WHERE` clause, and **CONFIRM-THEN-FIX, branch A for BOTH**: the assertions were written FIRST against unchanged `src/` and both failed, recorded verbatim. CR-06's RED was `AssertionError: expected 2 to be 4` — a booker who bought and PAID for two passes came back for two more and was replayed onto the booking they already had: no new row, no message of any kind, and the reserve page's `status === 'confirmed'` short-circuit bounced them to `/bookings/{old id}`, while the app's own copy `PASSES_FIXED_MESSAGE` ("To add more passes, book them separately.") is what told them to try. WR-01's RED was Mallory's redirect literally spelling Alice's booking id.
>
> **THE FIX SPLITS ONE STATUS FILTER INTO TWO, ONE PER ARM OF THE `OR`** — the single top-level clause over `findOwnOpenHold`'s predicate is gone. **Tokenless arm:** `booker_id` + the venue-local day range 09-18 installed + `status = 'pending' AND expires_at > now()` — a LIVE hold and nothing else. `status = 'confirmed'` is dropped entirely, because on the open path a DATE **is** the window, so a confirmed pass collapsed every purchase for that date into the first (OC-18 says there is deliberately no per-booker head cap, so this was idempotency machinery enforcing a policy nobody chose). **Key arm:** `idempotency_key AND booker_id`, and **deliberately no status filter at all** — `booking_idem_uq` is a GLOBAL partial-unique index, so a key whose row exists can never be re-inserted, and refusing to replay it guarantees a 23505 that `mapBookingError` re-throws as a raw 500 on the money path (T-09-80). `findOwnActiveHold`'s status set (incl. the D-63 `requested`/`approved` states) and its exact-window match are untouched.
>
> **ONE DELIBERATE DEVIATION, load-bearing rather than defensive: the STORED key is now BOOKER-NAMESPACED** (`scopedIdempotencyKey`, length-prefixed so the encoding is injective), applied in BOTH `createPendingHold` and `createOpenCapacityHold`. The plan's literal instruction — add `AND booker_id` to both key arms — is **not safe on its own**: once the predicate stops matching another caller's row the claim falls through to the INSERT and Postgres raises 23505 on the global index, so scoping alone would trade WR-01's disclosure for a raw 500. **Measured:** MUTATION 2b (revert the namespace, keep the predicate scope) turns cases 2 **and** 7 red with `PostgresError: duplicate key value violates unique constraint "booking_idem_uq"`, on the open claim **and** the exclusive one.
>
> **FOUR MUTATIONS RUN, ALL RECORDED VERBATIM.** M1 (restore `status = 'confirmed'` to the tokenless arm) → case 1 red at `expected 2 to be 4`, with cases 3-6 staying **GREEN**, so the D-42 double-submit replay is provably not collateral. **M2a — the plan's OWN prescribed WR-01 mutation — came back ALL 7 GREEN and is reported as observed rather than as predicted:** with the stored key namespaced the two bookers' keys are different strings and the predicate never gets the chance to matter, so the predicate scope is defence in depth and 2b/2c are what actually measure the assertion. M2c (both halves reverted, the true pre-fix predicate) → case 2 red with exactly the Task-1 shape. M3 (`BookCta`'s memo keyed on `[listingId]` alone) → case 6b red, and the mutated token's empty date/pass segments are the tell that the memo froze on the first render, before a day was picked.
>
> `BookCta` now mints a **per-SELECTION** token (memo on listing + date + pass count, composed INTO the value so `exhaustive-deps` stays at zero new warnings) on the **open payload only**; the exclusive payload is byte-untouched. Two additions the plan did not ask for: `date-pass-picker` case 6's KEY-SET tripwire reconciled by **naming** the new field and keeping the key-set form (loosening it to a bare `toMatchObject` would have removed its teeth), and **case 6b added because nothing anywhere measured the memo's dependency list** — a memo keyed on `[listingId]` alone would have passed every case in the plan. Deferred item 6 logged: a pre-existing CONCURRENT reuse of one key across two DIFFERENT dates can still 23505 (unreachable from the shipped UI — the memo cannot produce one token for two dates). No schema change, no migration; next number still **0023**. Gates: new gate **7/7**, `date-pass-picker` **10/10**, full suite **1087 passed / 4 skipped / 0 failed** (1079 baseline + exactly these 8), tsc **0**, lint **0 errors / 7 baseline warnings**, `git diff --exit-code src/` clean after all four mutations restored. Commits `b92822e` (branch-A confirmation), `722f242` (the fix).
>
> **⚠️ PHASE 9 GAP CLOSURE IS COMPLETE — nothing from `09-REVIEW.md` remains open. What remains is PROCESS, not findings:** `verify_phase_goal` (`gsd-verifier`) has never been run for Phase 9, and `/gsd-secure-phase 9` is still owed (`workflow.security_enforcement` is `true` and there is no `09-SECURITY.md`). Only after both may the ROADMAP/STATE phase checkbox move.

> ✅ **GAP PLAN 09-24 DONE (2026-08-01) — WR-02, WR-03 and NT-02 CLOSED. Gap position: 8 of 9 (`09-17` … `09-25`); next is `09-23`, the LAST gap plan.** The three read-model findings that share one file pair, each independently revertible. **CONFIRM-THEN-FIX, branch A:** the four new assertions were written FIRST against unchanged `src/` and all four failed, recorded verbatim — and that pre-fix state **is** the state the plan's mandated WR-02 mutation asks for ("restore the SQL `min()`/`max()` envelope"), so the mutation was **also** re-run explicitly after the fix landed. **WR-02 — the envelope is reduced over real INSTANTS.** `loadOpenDayWindow` now selects every `(open_time, close_time)` row for the weekday — ordered, not collapsed — turns each into instants through the **existing** pure `openDayWindow`, and takes the earliest opening instant to the latest closing one. A SQL `MIN`/`MAX` compares WALL CLOCKS, and no wall clock can order `02:00` after `12:00`, so a gym storing Saturday as `06:00–12:00` **and** `18:00–02:00` collapsed to `06:00–12:00`: the evening session vanished from the pass, the day panel advertised "Open 6:00 AM – 12:00 PM", `ends_at` was noon on a day the venue is open until 2 AM, and since `expires_at = LEAST(now() + TTL, ends_at)` an afternoon claim minted a hold **born already expired** — the CR-01 unpayable-hold shape reached by a different route. Reusing `openDayWindow` per row rather than re-deriving the roll is load-bearing: the `close <= open ⇒ next day` rule and the DST-safe `TZDate` construction keep exactly ONE definition, so the read path cannot drift from what the claim persists (the deleted-lines grep for `rollsPastMidnight|TZDate` prints **0**). **The CR-03 counter anchor is deliberately NOT dragged along** — `dayStartUtc`/`dayEndUtc`/`dateKey` stay the venue-local calendar day, and case 12 asserts that explicitly (`dateKey`, bounds exactly 24h) so a future reader who "fixes" the apparent inconsistency re-opens CR-03 in a red test rather than in production. **WR-03 — the ceiling is validated, not merely coerced.** `OPEN_LOW_STOCK_MAX` is accepted only when finite and `>= 1`, floored to an integer; anything else falls back to the documented default of `5`. A bare `Number(env)` turned `five`, a trailing space or an empty string into NaN or 0; `Math.min` of anything with NaN is NaN and `remaining <= NaN` is **always false**, so `spotsState` could never return `"low"` and the exact spots-left figure was never disclosed on **any** listing on **any** date — OPEN-04 switched off platform-wide, silently, with nothing broken to notice and nowhere to see it but one server-only constant. A sub-1 ceiling falls back rather than clamping up, because it is a typo and `lowStockThreshold` already clamps its own floor at 1: **fail-to-default, never fail-to-off.** **This closes AR-16's PENDING precondition** — the accepted-risk entry for **T-09-86** was filed on the rationale "server-only **and** clamped"; the server-only half was already true, the clamp half now is, so `09-SECURITY.md` can move AR-16 PENDING → ACTIVE and close T-09-86. The **ACCEPTED-DESIGN** formula `clamp(floor(cap / 2), 1, ceiling)` is **byte-identical** (OC-11 / T-09-39) and `e2e/open-capacity.spec.ts` case 2 — the pin on the intentionally invisible 3→2 transition — is untouched. **NT-02 — the grid and the panel now fail closed the same way.** `getOpenMonthAvailability` returns every venue-local date of the month in `fullDates` when `cap <= 0`, short-circuited **before** the aggregate query (one fewer round trip, not one more) and riding the **shipped** `fullDates` disabled matcher, so no component changed. The grid decided fullness from `taken >= cap` evaluated per BOOKING ROW — and a listing with no usable capacity has no bookings either, so an empty result set was read as "nothing is full" when it meant "nothing was asked", handing the booker a calendar of open dates leading to a panel that refuses each one on arrival. `monthPrefix` and the `EMPTY_MONTH` path are byte-untouched (deletion grep **0**). **MUTATION RE-RUN AGAINST THE LANDED FIX, recorded verbatim:** restoring the SQL `min()`/`max()` envelope turns case 12 RED with `expected '2026-09-14T04:00:00.000Z' to be '2026-09-14T18:00:00.000Z'` — `04:00Z` is venue-local **noon** where the next day's `02:00` was expected — and case 13 RED on the **persisted `ends_at`** of a real hold minted by the real claim, while **cases 14 and 15 stayed GREEN**, confirming the three fixes are genuinely independent. Restored; `git diff --exit-code src/` exit **0**, `git status --short` empty. **TWO DEVIATIONS, both documented rather than absorbed.** (1) The plan's single WR-02 case was **split in two**: written as one it aborts at its first failing assertion, so under mutation the persisted-hold half produced **no output at all**. Case 12 (window + day panel, injected clock, deterministic at any hour) and case 13 (the same shift on TODAY, driving the real claim and reading the row back) now go red independently and for **different reasons** — 12 because the projection is wrong, 13 because the **persisted row** is wrong — and only 13 can distinguish "the calendar lies" from "the booking is unpayable", the same argument 09-20 made about its case 3 and 09-21 about its case 5. **Case 13 has to be TODAY:** on a future date even the truncated noon is still ahead of the real clock, so the unpayable hold cannot form and a future-dated case would be green over a live defect. (2) A **Rule-1 fix to my own over-reaching assertion**: case 15 no longer asserts `bookable === false` on a zero-cap date, because `bookable` is the server's past-date/horizon verdict about the **window**, not a capacity verdict — a genuinely saturated date carries `bookable: true` too (shipped case 6 does not assert it) and the CTA already requires **both** (`oc.bookable && oc.state !== "full"`, `date-pass-picker.tsx:216`). Asserting it false would demand the two facts be merged, the opposite of what this phase keeps learning — the same shape as 09-20's refusal to invent a fourth `SpotsState`. **No source change was made to accommodate it**, and the four UI/e2e files are byte-unchanged. **THE ARTIFACT WORTH KEEPING:** in case 13's branch-A run `still_live` **passed** under the defect — the run happened at ~03:00 venue-local and `LEAST(now() + 15min, today-noon)` is still future at 3 AM. **The dead-hold symptom is hour-dependent; the truncated `ends_at` that causes it is not.** A case asserting only the expiry would have gone green over a live defect for the first half of every day and reported a pass, which is exactly why case 13 asserts the persisted **envelope** as well as the persisted **expiry** — 09-16's lesson in a new form: a test run at a lucky hour will confirm a claim the user will not experience. **NO schema change and NO migration — `drizzle/` untouched, next number still 0023**; exactly the three planned files changed. **All 13 acceptance greps were satisfiable in their load-bearing form — no broken-gate deviation was needed on this plan**, unlike 09-18/09-19/09-20/09-21. Gates: the extended gate **16/16**, `tests/availability tests/booking tests/search` **498 tests / 53 files** with zero failures, **full suite 1079 passed / 4 skipped with ZERO failures** across 120 files (1073 + the known flake = 1074 baseline, plus exactly these 5 new cases; the `date-pass-picker` multi-suite timeout did **not** reproduce this run, as in 09-18), tsc **0**, lint **0 errors / 7 baseline warnings**. Commits `276535e`, `6a972c5`, `11c9cd5`, `3fd06bb`.
>
> ✅ **GAP PLAN 09-21 DONE (2026-08-01) — CR-04 CLOSED and WR-04 with it. Gap position was 7 of 9.** A bad money input on the drop-in path now produces a calm refusal instead of a raw 500, at both of the two ends where it can arise. **CONFIRM-THEN-FIX, branch A — CR-04 arrived "reported, NOT independently verified" and is now VERIFIED REAL:** `tests/listing/open-capacity-edit-gate.test.ts` was written FIRST against unchanged `src/` and both cases failed, recorded verbatim. `AssertionError: expected true to be false` — `saveListingStep` **accepted** the wizard's own sparse `{ occupancyMode: "open_capacity" }` autosave on a published, bookable listing, leaving it live in drop-in mode with `per_head_price_cents = NULL`. Then `Error: Listing has no per-head price for an open-capacity booking`, raised at `pricing.ts:165` and **unwound all the way out through `createOpenCapacityHold` → `placeOpenHold` → the caller without being caught anywhere** — that stack IS the finding, because in production the caller is a booker's *Book this space* click and the exception surfaces as a Next.js error digest on the money path (T-09-69/T-09-70), the exact `T-03-500` outcome `mapBookingError`'s own docblock promises never happens. The two failures are the same defect at its two ends: the gate that let the row into the state, and the claim that had no answer once it was there. **Fix (a) — the edit-path re-gate.** The new guard joins the **existing** `if (owned.status === "published")` scope beside HG-01 (the scope grep still prints **1** — one published-row scope, not two), computes the effective post-save values with the same `incoming ?? persisted ?? default` idiom and **reuses HG-01's own `effMax` local** so there is exactly one definition of "the capacity this save would leave behind", and refuses when the effective mode is drop-in and any of the four publish rules is unmet (price per person, positive daily cap, instant booking, single space). **All four sentences are IMPORTED from `validation/listing.ts`, never retyped** — each grep prints exactly **2** (import + one use) and the re-declaration grep prints **0** — and the outer sentence is HG-01's own, so the wizard's existing failure surface handles it with **no component change**. Field errors accumulate rather than short-circuit, so a host is told every number that is wrong at once. **DRAFTS STAY PERMISSIVE** — the guard is inside the `published` branch only, because the wizard's occupancy step comes BEFORE its pricing step, so a draft in drop-in mode with no price is precisely where a host mid-build is standing; tightening this to all rows would freeze the wizard at its own step 5. `assertOwnership` is byte-untouched (T-09-72 unchanged). **Fix (b) — the claim fails closed.** `createOpenCapacityHold` refuses a NULL **or non-positive** `per_head` BEFORE `quoteOpenCapacity` is reached, joining 09-20's step-5 refusal sequence without reordering or rewriting the block-date refusal; `<= 0` and not merely `== null` because `draftSchema` admits `0` on purpose and freezing a ₱0 charge sells a pass for nothing. Bare `{ error }` with NO `soldOut` flag, routing to `placeOpenHold`'s calm `taken` branch. The rule is named once for both money columns the statement reads — **every money input the claim takes from the listing row is a fail-closed refusal, never a raised error**, because `mapBookingError` maps only `NoUnitAvailableError`/23P01/40P01 and re-raises everything else. **`quoteOpenCapacity`'s invariant guard is DELIBERATELY PRESERVED** — `grep -c "throw"` on `pricing.ts` is unchanged at **4** — because it holds a real invariant for every other caller; the fix is that the claim must never hand it a null. **Fix (c) — WR-04, the ceilings that are real.** `MAX_PER_HEAD_PRICE_CENTS` (1_000_000 centavos, ₱10,000/person) and `MAX_OPEN_CAPACITY` (1_000 admissions/day) are exported from `validation/listing.ts` and applied to `perHeadPriceCents`/`maxOccupancy` in **both** `draftSchema` and `publishSchema`, each with its own O3 sentence; draft-time is not optional, because the wizard autosaves straight onto a LIVE row and a publish-only bound would let a published listing grow a stadium cap on its next Save and continue — CR-04's lesson one file over. The arithmetic is written where the ceilings are declared so it is checkable rather than asserted: `1_000 × 1_000_000 = 1_000_000_000` space cents, plus the D-74 service fee at 500 bps = `1_050_000_000`, under int4's `2_147_483_647`. `MAX_MONEY_CENTS` is declared once in `booking/pricing.ts` and the claim checks BOTH figures immediately before the quote — the space price AND the all-in, because the fee rides on top and `quoted_total_cents` is int4 too — as the defence-in-depth layer **for legacy rows**, since no schema change reprices an existing row. Both `.max(10_000)` docblocks now describe the chain that is real (shape ceiling → host-input ceilings → runtime product guard) instead of a protection that was never there. No existing fixture or seed exceeded either ceiling, so no ceiling had to move. **THREE MUTATIONS EXECUTED and recorded verbatim.** (A, mandated) deleting the fail-closed rate refusal while leaving the action gate fully in place reproduces the Task-1 confirming failure EXACTLY — same sentence, same raising site, same unwinding out through `placeOpenHold` — while **cases 1, 2, 3 and 4 stayed GREEN**, which is the finding: an action-level fix alone looks like four-fifths of a success, and the only case that can say otherwise is the one whose fixture was written DIRECTLY to the table. That is exactly why case 5 does not go through `saveListingStep` — **a gate can only govern rows created after it, and the listing that mattered was already in the bad state when the gate shipped.** (B, NOT mandated — run because WR-04 arrived as a warning about a *comment*, so the question worth answering was whether its mechanism is real) deleting the runtime product guard turns case 7 RED with `PostgresError: value "3150000000" is out of range for type integer`, code `22003`, on `quoted_total_cents` — `space_price_cents` 3,000,000,000 and the all-in 3,150,000,000, the fee-inclusive figure being the parameter that actually blew up — escaping `placeOpenHold` uncaught because 22003 is neither 23P01 nor 40P01. So WR-04 was not merely an overstated comment: the protection it described was genuinely absent and genuinely reachable. (C) a fixture `primarySpaceType` that was not a `space_type` enum member, recorded because a `beforeAll` failure reports as *five skipped tests*, which reads like a pass at a glance. All restored; `git diff --exit-code src/` clean. **NO schema change and NO migration — next number still 0023.** ONE DOCUMENTED DESIGN DECISION: the edit gate imposes the FOUR correctness rules the plan enumerates and NOT `publishSchema`'s fifth (D-110, `extraHeadFee > 0` in drop-in mode), because `quoteOpenCapacity` is purely `per_head_price_cents × heads` and never reads that column — so a stale surcharge changes no charge, no payout and no refund — and enforcing it would dead-end a host converting an exclusive listing that legitimately carries the fee, since the wizard hides the group-pricing fields in drop-in mode. Logged as **deferred item 5** with two concrete ways to close it. The `grep -c "2026-" == 0` tripwire is honoured, with two machine-printed fragments elided and named at the point of elision (the harness's timestamped `[Better Auth]` stderr lines, and postgres.js's `params:` dump echoing the clock-relative fixture instants) — nothing describing a failure is elided, and the full untouched dump lives in the SUMMARY. Gates: the new 7-case / 763-line DB-backed gate **7/7**, `tests/validation tests/listing tests/booking` **458 passed / 43 files** with zero failures, full suite **1073 passed / 4 skipped** (1066 baseline + exactly these 7) with ONE failure — the known `date-pass-picker` multi-suite timeout, reconfirmed **9/9 in isolation** (deferred item 3) — tsc **0**, lint **0 errors / 7 baseline warnings**. Commits `34f9c3e`, `e6382bc`, `e1d51b2`.
>
> ✅ **GAP PLAN 09-25 DONE (2026-08-01) — WR-05 CLOSED, and the `cancel-booking.ts` half of NT-01 with it. Gap position: 6 of 9 (`09-17` … `09-25`); next is `09-21`.** A drop-in pass is no longer final from the moment the venue opens. `cancelBookingAsBooker`'s flip is forked **in SQL from the row's own column** — `AND (CASE WHEN open_capacity THEN ends_at ELSE starts_at END) > now()` — so a pass is cancellable for the **whole day it covers**, while every other predicate (`id`, `booker_id`, `status = 'confirmed'`) and the entire `SET` clause stay byte-identical. **09-17 is what exposed this:** CR-01 meant a same-day pass could not be bought at all, and removing that mask made a pass bought at 15:00 for today instantly non-cancellable and non-refundable with nothing on the reserve page saying so. **THE D-94 CLAWBACK PROPERTY IS PRESERVED, NOT WEAKENED:** payout is not eligible until `ends_at + 24h` (D-55), and while a pass window is live that instant is by construction still in the future — so a live-window cancel remains a platform-wallet reversal with the host never yet paid, which is the same reason the guard was safe before it moved. **AND NO MONEY MATH MOVED WITH IT:** `quoteRefund` already returns 0% for a negative `hoursToStart` (`cancellation.ts:92-96`), so a live-window cancel refunds **nothing**, retains the **full** space price for the host (`retained_space_cents` is what the 07-04 sweep reads) and simply returns the head to the pool — a release that needs no code at all, because `remaining` is a live SUM. The refund-math/ledger diff gate prints **0** and the `availabilityBlock` gate prints **0**. A new `PASSES_ENDED` sentence sits beside `PAST_START`, whose wording is byte-identical because it is TRUE of an exclusive booking and is asserted verbatim by shipped Phase-7 tests. **THE HOST FLIP IS BYTE-UNTOUCHED (T-09-91)** — the filtered grep prints **1** — and its comment now RECORDS the asymmetry as a choice instead of continuing to claim it is "the same guard the booker path carries", which this plan made false. The cancel review page forks the same window **and** the same two sentences (Rule-2 auto-fix: forking only the gate would have left the page refusing to render a cancellation the action would happily perform — a dead end one click earlier — and the page's own shipped comment already required page and server to match word for word). **ONE DELIBERATE DESIGN DEVIATION, documented rather than absorbed:** `explainNoRows` takes the **calling path's** window (`"booker" | "host"`) rather than a single mode fork, because after this plan there are TWO update instants and the plan's instruction (c) presumes one — a single fork would have told a HOST refused on a still-**live** pass that the booking "is no longer active", a false sentence and a behaviour change on exactly the path the plan's own scope note freezes. Which refusal is returned is decided by **what ran out**, i.e. the very instant that path compared, not by the mode alone. Both `=== PAST_START` identity comparisons became `isPastWindow`, so `"past_start"` is still recorded at both sites (T-09-92) and case 9 pins it. **MUTATION EXECUTED and recorded verbatim:** reverting the flip turns case 6 RED with `AssertionError: expected 'confirmed' to be 'cancelled'` — a pass the booker *asked* to cancel still live, still occupying a spot, still unrefunded — while **case 7 stays GREEN** (the EXCLUSIVE control, seeded with `ends_at` deliberately in the FUTURE so the sloppy fix — widening the cutoff for every mode — goes RED instead) and cases 8/9 stay GREEN (an already-closed day fails both cutoffs, so only the wording is at stake). That green control is what makes case 6's red attributable to the fork rather than to cancellation breaking outright. **ONE UNPLANNED ARTIFACT, RECORDED RATHER THAN DISCARDED:** under the mutation the audit line printed `"reason":"not_active"` for a live pass, because `explainNoRows` was still forked while the flip was not — the two halves disagreed about which instant closes the window, so the trail described a live pass as a booking that no longer existed. **A partial revert is observable in the audit trail before it is observable anywhere else**, which is also the empirical argument for the `CancelWindow` parameter. Restored; `git diff --exit-code src/app/actions/cancel-booking.ts` clean. **Task 2 — the disclosure:** `CancellationPolicyDisclosure` gains a **REQUIRED** `windowAlreadyOpen: boolean` (a two-file compiler census, the 09-08/09-09 argument); when `openCapacity && windowAlreadyOpen` the rung ladder is **suppressed** — printing "24 hours or more — full refund" beside "this pass can't be refunded" would contradict itself on a money surface — and the **collapsed** `<summary>` states the exported `PASS_NON_REFUNDABLE_MESSAGE` through an expression container, because a disclosure a booker must expand to find is not one they demonstrably saw (D-81). The tier name and the mandatory service-fee paragraph are untouched. The flag is computed **server-side** at both sites: the reserve page from `bk.openCapacity && bk.startsAt <= now` against its own server clock (never a client one, D-105), the public listing page `false` with the reason recorded — it has no booking and no picked date, so there is no specific pass whose day could have opened. **NO schema change and NO migration** — `open_capacity` is already `NOT NULL DEFAULT false`, so the SQL `CASE` is never NULL and there is no three-valued-logic hole; next number still **0023**. Two further deviations: the `destructive` tripwire was **genuine and satisfiable**, so it was honoured by re-flowing the comment rather than waived (the same call 09-22 made), and the "fully ended day" fixture is **yesterday** rather than an earlier window today, which is not deterministic at every hour (09-17's identical deviation). Gates: the two extended gates **9/9** and **9/9**, `tests/booking tests/payments` **431 passed / 39 files** with zero failures, full suite **1066 passed / 4 skipped** (1055 → 1058 → 1058 baseline + exactly these 8) with ONE failure — the known `date-pass-picker` multi-suite timeout, reconfirmed **9/9 in isolation** (deferred item 3) — tsc **0**, lint **0 errors / 7 baseline warnings**. Commits `ed3eb1c`, `3efada0`.
>
> ✅ **GAP PLAN 09-22 DONE (2026-08-01) — CR-05 CLOSED. Gap position was 5 of 9.** A confirmed drop-in pass can no longer mint a group. **CONFIRM-THEN-FIX, branch A — CR-05 arrived "reported but NOT independently verified" and is now VERIFIED REAL:** `tests/group/open-capacity-group-guard.test.ts` was written FIRST against unchanged `src/` and failed with `AssertionError: expected 29 to be +0` — **twenty-nine RSVP-able seats minted from the listing's DAILY ADMISSIONS CAP against ONE paid drop-in admission**, with the refusal assertions never even reached (the action returned `ok: true` with a group id and a live invite token). The first assertion reads `capacity_snapshot` back out of Postgres, so a fix that refused politely and still wrote the row could not have made it green. **THIS PLAN WAS RESUMED AFTER A MID-EXECUTION INTERRUPT:** branch A had already landed as its own `test(...)` commit (`c15e35c`) and an incomplete fix fragment had been reverted, leaving `src/` byte-identical to HEAD. Fix: `createGroup`'s pre-read gate PROJECTS `(l.occupancy_mode = 'exclusive') AS "modeOk"` and refuses with the shared `DENIED` sentence plus a distinct `open_capacity` audit reason, and the INSERT's `WHERE` re-states `AND l.occupancy_mode = 'exclusive'` as defence in depth — **the guard is in BOTH statements, so a bypassed gate still writes nothing.** The mode is checked BEFORE the `max_occupancy >= 2` floor, because on an open-capacity listing `max_occupancy` is a daily admissions cap and not a room rating, so the floor would otherwise judge a number that means something else. **ONE DELIBERATE DESIGN DEVIATION, documented rather than absorbed:** the plan's instruction (a) (`AND l.occupancy_mode = 'exclusive'` in the gate's `WHERE`) and its own instruction (c) (record a distinct `open_capacity` audit reason) CANNOT BOTH HOLD — a filtered-out row reaches `if (!gate) return NOT_CONFIRMED`, which records NO audit entry and cannot tell a drop-in booking from a nonexistent one, so (c) would have been silently unsatisfiable. The projected form satisfies both, stays ONE round trip (the 09-20 `not_blocked` idiom), keeps the caller-facing sentence shared (T-09-77) and still prints **2** for the acceptance grep. `src/app/(app)/bookings/[id]/page.tsx`'s comment claiming the server re-checks "ownership and confirmation" — true of two of three conditions, false for exactly the one Phase 9 added — now names all three (T-09-76); `groupEligible` and its three-condition expression are untouched. The four `openCapacity: false` literals are left exactly as they are per `<do_not_relitigate>`: they are now ENFORCED facts rather than an unenforced premise. **MUTATION EXECUTED and recorded verbatim:** replacing the projected boolean with a constant `true` AND deleting the INSERT predicate reproduces the confirming failure exactly — `AssertionError: expected 29 to be +0`, the same seat count from the same assertion — while **case 2 (the EXCLUSIVE control, a listing identical in every field but the mode) stayed GREEN**, which is what makes case 1's red attributable to the mode predicate rather than to group creation breaking outright. Restored; `git diff --exit-code src/` clean. Case 3 asserts the defence-in-depth property STRUCTURALLY over sliced statements of the shipped source — strictly stronger than a file-level `grep == 2`, which would pass if BOTH copies landed in the gate — which is the plan's sanctioned form, since the gate is internal to `createGroup` and monkey-patching `db.execute` would prove a property of the mock. Two auto-fixes: backticks inside the `sql` tagged template broke the parse (`[PARSE_ERROR]` at `group.ts:326:62`), and a comment reflow initially tripped the GENUINE "D-111 snapshot untouched" grep — re-flowed so the three lines carrying `capacity_snapshot` stay byte-identical, so the criterion is honoured exactly rather than waived. NO schema change and NO migration — `occupancy_mode` is already `NOT NULL DEFAULT 'exclusive'`, so the predicate is never NULL and there is no three-valued-logic hole; next number still 0023. Gates: new gate **3/3** (458 lines), `tests/group` **9 files / 83 tests** green (the Phase-8 lifecycle, seat-claim, RSVP and owner-scope suites unaffected), full suite **1058 passed / 4 skipped** (1055 baseline + exactly these 3) with ONE failure — the known `date-pass-picker` multi-suite timeout (deferred item 3) — tsc **0**, lint **0 errors / 7 baseline warnings**. Commits `c15e35c`, `443e74b`.
>
> ✅ **GAP PLAN 09-20 DONE (2026-07-31) — CR-02 CLOSED. Gap position was 4 of 9.** A host who blocks a date on a drop-in listing now actually stops passes selling for it. **CONFIRM-THEN-FIX, branch A:** `tests/availability/open-capacity-blocks.test.ts` was written FIRST against unchanged `src/` and all three assertions failed — `AssertionError: expected 3 to be +0` (the day panel offered the FULL cap on a date the host had closed and could see listed under "Blocked dates"), `expected [] to include …` (the month grid never disabled it either, so search and the picker kept offering it) and `expected [ … ] to have a length of +0 but got 1` (**the claim granted and priced a pass anyway** — one paid admission on a closed day, after which the host either honours a day they shut or cancels every pass and pays the D-71 fee on each). `availability_block` had been read in exactly ONE place in all of `src/` — the exclusive branch of `getAvailability`, which sits BELOW the Phase-9 fork; `getOpenDay`, `getOpenMonthAvailability` and `createOpenCapacityHold` never queried it. **09-RESEARCH Pitfall 5's stated mechanism stays wrong and 09-09 stays right:** the host-cancel auto-block never "zeroed the date in the read model" — the missing join IS this finding. Fix: `open-capacity.ts` grows its fifth owned fact — `OPEN_BLOCK_UNIT_SCOPE_SQL` (the D-24 whole-listing scope plus the sentinel unit 1 every open row shares) and `openBlockedSql` (an `EXISTS` with the same `'[)'` overlap the exclusive read uses, carrying the ⚠️ DO NOT COPY note `openTakenSql` carries), plus `BLOCKED_DATE_MESSAGE`. **The range decision, stated once and applied at all three call sites:** blocks are checked against the **venue-local CALENDAR DAY**, never the pass window — OC-02 makes a date one pass and the month grid cannot cheaply know each date's hours, so a pass-window rule would make the grid and the panel disagree (the NT-02 class); the reviewer's pass-window form is the recorded REJECTED ALTERNATIVE and "a partial block closes the whole drop-in date" is the recorded ACCEPTED CONSEQUENCE. `getOpenDay` evaluates the predicate in the SAME statement as the heads SUM (one round trip) → remaining 0 / `full` / not bookable, with **no fourth `SpotsState` invented** and a comment saying why; `getOpenMonthAvailability` expands the overlapping block rows to the venue-local dates they cover, importing only the UNIT SCOPE because a per-date expansion has a different SHAPE from a single-date `EXISTS` (the `SUM(declared_pax)` precedent); `createOpenCapacityHold` refuses via a `not_blocked` column on the EXISTING cap/rate statement — **inside the transaction and inside the advisory lock**, no second statement, lock unmoved — as a bare `{ error }` with NO `soldOut` flag, because nothing was sold. **No schema change and no migration — next number still 0023.** **MUTATION EXECUTED AND RECORDED VERBATIM:** deleting the claim's refusal (leaving the read-model half intact, so the calendar still says shut) turns case 3 RED with `expected [ … ] to have a length of +0 but got 1` and cascades into case 4 with `expected 2 to be 3` — the leaked pass is STILL occupying a head, so the host's unblock returns only 2 of 3 admissions. **Cases 1, 2, 6 and 7 stayed GREEN under it**, because the read-model half was untouched — which is the finding: a calendar-only fix looks like four-sevenths of a success, and only the case that reads the `booking` table back can say otherwise (T-09-64 — the picker is a courtesy, the claim is the gate). Restored; `git diff --exit-code src/` clean. `src/app/actions/cancel-booking.ts` is byte-unchanged with its **blob sha identical to HEAD** (`8b48418…`) — 09-09's Consequence-3 fork untouched, zero bytes, and case 5 re-proves a drop-in host cancel writes ZERO blocks and leaves `remaining === 1` for the other pass-holder (T-09-65); `spots-left-chip.tsx` and `date-pass-picker.tsx` are untouched too. New DB-backed gate: 7 cases, 602 lines, every date clock-relative, every block created through the REAL `addBlock`/`removeBlock`. Gates: vitest **7/7**, full suite **1055 passed / 4 skipped** (1048 baseline + 7) with ONE failure — the known `date-pass-picker` multi-suite timeout, reconfirmed **9/9 in isolation** (deferred item 3) — tsc **0**, lint **0 errors / 7 baseline warnings**. One acceptance grep documented as a deviation rather than absorbed: `grep -c "2026-" == 0` is unsatisfiable once the mandated verbatim run is recorded, because vitest prints the clock-relative fixture date into its own assertion message; every match sits inside a recorded-output comment and no fixture carries a calendar literal. Commits `10053d5`, `fc0c623`.
>
> ✅ **GAP PLAN 09-19 DONE (2026-07-31) — CR-03 LAYER 2 CLOSED. Gap position was 3 of 9.** A host may no longer change **or delete** the operating hours of a weekday that still carries upcoming or active drop-in passes. 09-18 made an hours edit non-catastrophic (the counter survives it); this refuses the edit itself, because the harm left over is **stranding** — a pass persists concrete `starts_at`/`ends_at` at hold time (OC-03), and those instants are what the refund ladder, the payout sweep (`ends_at + delay`), reminders and expiry all key on, so moving Monday's opening leaves every future Monday pass claiming an entry window the venue will not honour, while **deleting** the weekday's hours makes `loadOpenDayWindow` return null and the sold passes go invisible in every read model while still occupying admissions. New **`src/lib/listing/hours-lock.ts`** mirrors `mode-lock.ts` one level down: `getOpenHoursLockState` → `{ lockedWeekdays, lockedByCount, unlocksAt }` from ONE raw-SQL statement grouping the **imported** `OPEN_OCCUPYING_STATUS_SQL` set by the venue-local weekday of the **stored** `booking.starts_at` (not `ends_at` — a split-shift venue's closing instant can land on the next calendar day), bounded by `ends_at > now()` so a host is never frozen by their own back catalogue, with the DB clock as the only clock. `saveOperatingHours` refuses **only a genuine change**, **only on locked weekdays**, and **only when the persisted `occupancy_mode` is `open_capacity`** — the exclusive path Phase 3 shipped is byte-unchanged and pays nothing. Both sides are canonicalised to `HH:mm:ss` before comparison, so a seconds-less no-op autosave still succeeds and the editor is never frozen (T-09-60). The refusal returns `HOURS_LOCKED_MESSAGE` + the venue-local unlock date (`composeDateLabel`, D-105) + the way out in **both** `error` and `fieldErrors.windows[0]`, so `weekly-hours-editor.tsx` surfaces it with **no component change**; a calm muted advisory on the availability page names the frozen weekdays, the pass count and the unlock date and links to `/host/bookings` (no `destructive` anywhere). **No schema change and no migration — next number still 0023.** New DB-backed gate `tests/availability/hours-lock.test.ts` (432 lines, 5 cases) drives the **REAL** action through a **real** better-auth session and asserts `operating_hours` read **back from Postgres first**. **MUTATION EXECUTED AND RECORDED VERBATIM:** disabling the refusal turns case 1 RED with `AssertionError: expected '07:00:00' to be '06:00:00'` — the host's edit landing on a weekday whose passes were already sold — and cascades into cases 2, 3 and 5 (each re-asserts the locked weekday is still where the pass left it), while **case 4 stays GREEN** because it drives the EXCLUSIVE listing this guard never touches. Restored; `git diff --exit-code src/` clean. Gates: vitest **5/5**, full suite **1048 passed / 4 skipped** with ONE failure — the known `date-pass-picker` multi-suite timeout (deferred item 3), which passes **9/9 in isolation** and touches nothing this plan changed — tsc **0**, lint **0 errors / 7 baseline warnings**. Two acceptance greps were satisfied **without absorbing their defect** and documented as deviations: the `ISODOW` warning was kept but the token not spelled, and `HOURS_LOCKED_MESSAGE` arrives on a **second** import statement so the shipped validation import line stays byte-identical and the security-contract diff-gate scores `0`. Commits `fbc9a5c`, `9973a46`.
>
> ✅ **GAP PLAN 09-18 DONE (2026-07-31) — CR-03 LAYER 1 CLOSED. Gap position was 2 of 9.** The drop-in admissions counter's identity is no longer a value a host can edit. It is the **venue-local calendar day** — a half-open `[midnight, next midnight)` range over the **stored** `booking.starts_at` — and the advisory lock is keyed on `listing_id || ':' || YYYY-MM-DD`. An opening instant built for venue-local date D always falls inside venue-local day D, so the day ranges are disjoint by construction and an hours edit moves a pass **within** its own day instead of **out of** the counted set. New shared pure helper `venueDayBoundsUtc` (TZDate through the epoch; `dateKey` composed from the numeric date fields so no timezone conversion can shift it away from the range it protects); `openTakenSql` widened to three arguments as a deliberate **compiler census**; `OpenDayWindow` now carries both facts with the distinction stated in the type — what a pass **COVERS** (`dayOpenUtc`/`dayCloseUtc`, persisted, host-mutable) versus what it **COUNTS AGAINST** (`dayStartUtc`/`dayEndUtc`/`dateKey`, never persisted). The lock is still the **literal first statement** in the claim's transaction (`units.ts:799`); the lazy-expiry sweep and `findOwnOpenHold` were re-scoped to the same range so a hold minted under the OLD hours is swept and replayed by a claim under the NEW ones; `day_open_ok`/`horizon_ok` deliberately keep the opening/closing instants. `getOpenMonthAvailability` groups by venue-local date, closing the `read-model.ts:441` half where two `starts_at` groups were each compared against `cap` separately. **No schema change and no migration — `drizzle/` untouched, next number still 0023.** New DB-backed gate `tests/availability/open-capacity-hours-rekey.test.ts` (447 lines, 5 cases, zero calendar literals) drives the **seam** — an hours edit followed by a claim, each claim re-deriving the window via `loadOpenDayWindow` exactly as `placeOpenHold` does — and asserts the database truth first. **MUTATION A EXECUTED AND RECORDED VERBATIM:** reverting the anchor in all three source files turns case 1 RED with `AssertionError: expected 4 to be 3` — four paid admissions on a three-person day — taking cases 3 and 5 with it. **Case 2 stayed GREEN under it**, because the month grid carries its own aggregate, so a **second mutation** was run against the grouping alone: case 2 RED plus `open-capacity-readmodel.test.ts` cases 9 and 11. Both restored; `git diff --exit-code src/` clean, `git status --short -- src/` empty. Gates: vitest **5/5** on the new gate, **full suite 1044 passed / 4 skipped with ZERO failures** (the `date-pass-picker` multi-suite timeout — deferred item 3 — did not reproduce in any configuration this run), SC#3 race gate **6/6**, tsc **0**, lint **0 errors / 7 baseline warnings**. One acceptance grep was verified in its **load-bearing form** and documented as a deviation rather than absorbed: the `starts_at = ` tripwire is unsatisfiable at file scope because `units.ts:266` is the EXCLUSIVE path's `findOwnActiveHold`, whose equality on its own stored range is exactly what makes the exclusive path immune to CR-03. Left explicitly open with pointer comments: `findOwnOpenHold`'s `confirmed` branch + unscoped `idempotency_key` arm (**09-23**) and the `cap ≤ 0` disagreement (**09-24**); **CR-03 layer 2** — refusing the hours edit itself — is **09-19**. Commits `6c62776`, `3647d38`, `68851d7`.
>
> ✅ **GAP PLAN 09-17 DONE (2026-07-31) — CR-01 CLOSED. Gap position was 1 of 9.** `confirmBooking`'s D-94 checkout-initiation cutoff is now FORKED on the persisted `booking.open_capacity`: `ends_at` for a drop-in pass, `starts_at` for an exclusive booking. A same-day pass is payable for the whole day it is valid for; the ordinary walk-in purchase works. The exclusive sentence is preserved byte-for-byte (NT-01's booking.ts half closed — a closed drop-in day is now refused in pass words), `reason: "expired"` is unchanged on both branches so `ReserveView` is untouched, and `src/app/api/paymongo/webhook/route.ts` is byte-identical (D-57 holds — payment is still the sole confirm authority). **Both proof layers now drive the same-day path.** New DB-backed gate `tests/booking/open-capacity-confirm.test.ts` (3 cases) drives the REAL `confirmBooking` against a row minted by the REAL `createOpenCapacityHold` for TODAY after opening; its fixture is deterministic at ANY hour (`open = now−2h floored, clamped at midnight` / `close = 23:59`), which is exactly what the 01:31-Makati walkthrough lacked. **MUTATION EXECUTED AND RECORDED VERBATIM:** reverting the fork turns case 1 RED with `AssertionError: expected 'This session has already started, so …' to be null` — the sentence a booker would actually have read. Case 3 went RED too where the plan predicted GREEN (it asserts the WORDING as well as the refusal) — recorded, not weakened. `e2e/open-capacity.spec.ts` gained case 6, which books a pass for TODAY and removes the `offset >= 3` blind spot; cases 1-5 are byte-intact (case 2's accepted-design invisible-3→2 assertion untouched). **The NEGATIVE mutation is the artifact:** with the fork reverted, e2e case 6 still PASSES (`1 passed (25.7s)`) — the browser layer provably cannot catch CR-01, because the defect lives on the click that leaves for PayMongo. Both mutations restored; `git diff --exit-code src/` clean. Gates: vitest 3/3 on the new file, 570 passed across booking+payments+availability with ONE pre-existing failure baselined at HEAD (`date-pass-picker` case 4's multi-suite 5s timeout — `deferred-items.md` item 3), tsc 0, lint 0 errors, Playwright **6/6**. Commits `f09690c` (fix+gate) and `5096c06` (browser proof).
>
> 🟩 **GAP PLANNING LANDED 2026-07-31 — Phase 9 is now 25 plans: 16 EXECUTED + 9 PLANNED (`09-17` … `09-25`), awaiting `/gsd-execute-phase 9`.** The 9 gap plans close all 14 findings in `09-REVIEW.md` (6 blockers / 5 warnings / 3 info); the plan-checker PASSED them with **0 blockers**. CR-01 and CR-03 — the two orchestrator-verified blockers — are in the first gap wave. **NT-03 is the only deliberate non-action.** No migration is required (next number stays **0023**). Full detail in Session Continuity below. Everything under this line describes the ORIGINAL 16-plan run and is unchanged.

Phase: 09 (open-capacity-bookings) — **24 of 25 plans executed. The original 16 landed 2026-07-31 (waves 1-7); gap closure is 8 of 9 with only `09-23` outstanding. The phase is NOT yet marked complete: phase verification and close are the orchestrator's.**
Plan: 24 of 25 — **09-24 DONE (2026-08-01): WR-02 + WR-03 + NT-02 closed; next and last is `09-23` (CR-06 + WR-01).** Everything below this line describes the ORIGINAL 16-plan run and is unchanged.
Plan: 16 of 16 — **09-16 DONE (2026-07-31): the drop-in human walkthrough, nine steps recorded INDIVIDUALLY with verbatim evidence.** The money path is proven on the live rail — three real PayMongo `sk_test_` `checkout_session.payment.paid` deliveries confirmed three drop-in bookings, each followed by a `booking_confirmed` notification row within ONE SECOND (PayMongo → tunnel → webhook → status flip → `inngest.send` → notify → row, T-09-49). The Pitfall-5 guarantee holds in DATA: `availability_block` = **0 rows** after a host-cancel, and the date stayed bookable for everyone else (T-09-31). Steps 1/2/3/5/8a/8b/8c/9 PASS; step 6 PASS but NOT REACHABLE THROUGH THE UI (by design — the stepper is bounded by `remaining`); step 7 PASS with a deferred copy finding. **Step 4** was reported FAIL and reclassified by the operator to **accepted design** → PASS-with-note: `lowStockThreshold(3) = 1`, so 3 and 2 remaining both render the digit-free `Spots available` and the first pass sold is invisible **by design** (OC-11 / T-09-39). **Nothing in `src/` changed** — instead the invisible 3→2 step is now an ASSERTED, mutation-measured contract in `e2e/open-capacity.spec.ts` case 2 (`70c392a`), the misleading "spots decrement between them" wording is corrected in place in 09-15-SUMMARY.md + 09-VALIDATION.md, and two product items are logged in the phase's `deferred-items.md`. NEXT: phase verification + close (orchestrator-owned).

✅ **Phase 8 is closed.** The 08-17 human walkthrough found a live money-path double-charge; gap plans 08-18 → 08-22 closed it and the two related deferred items (6, 7), and the fix set was **proven against the REAL PayMongo `sk_test_` API** — the mock-only coverage that let the bug survive four plans is no longer the whole story.

> **What closed the blocker (deferred item 5):** `confirmBooking` now EXPIRES any session already named on the booking row BEFORE creating a new one (`src/app/actions/booking.ts`, expire-before-create, mirroring `updateDeclaredPax`), fail-closed on a genuine expire failure, for flat AND per-head bookings — so a SEQUENTIAL double-submit leaves at most one payable session. The three false idempotency comments were corrected. **08-19 proved the provider behaviour against the live `sk_test_` API** (opt-in `RUN_LIVE_PAYMONGO_PROBE=1`): two byte-identical `POST /v1/checkout_sessions` mint two DIFFERENT payable session ids, and `expire` retires a session.
>
> **A follow-on defect 08-19 discovered and 08-21 closed:** a repeat expire returns HTTP 400 "already expired" (NOT the assumed 200-replay), which would livelock the fail-closed retry. `expireCheckoutSession` is now IDEMPOTENT — it tolerates exactly that 400 as success while every genuine failure still throws — re-proven live (08-19 case 4 now resolves).
>
> **Deferred item 6 (CR-02 live re-price supersession):** proven end-to-end against the real API (08-19 case 3 — superseded session `expired`, replacement `active`).
>
> **Deferred item 7 (revenue-loss, `included >= maxOccupancy`):** `publishSchema` rejects it at publish (08-20) and `saveListingStep` rejects it on edits of an already-published listing (08-22 / HG-01 from the gap-closure review), sharing one `SURCHARGE_UNREACHABLE_MESSAGE` constant so the two gates cannot drift.
>
> **Residual (non-blocking, in `08-REVIEW-gaps.md`):** LW-01 (the already-expired tolerance matches unstructured provider text — fails SAFE), NT-01 (a stale test title), NT-02 (the wizard autosave doesn't surface the specific surcharge field-error — UX polish; the edit is still correctly rejected). Accepted residuals: T-08-79 (concurrent double-click) and T-08-74 (an already-captured qrph overcharge is unrefundable via API).

⚠️ **Tracking note (v1.42.3 bug watch):** the SDK's `state.update-progress` / `roadmap.update-plan-progress` were NOT used to close this phase — the orchestrator wrote STATE/ROADMAP by hand to avoid the known phase-checkbox over-reach. `completed_phases` is now legitimately **8** because verification passed, not because a plan finished.

**The four original Phase-8 BLOCKERS (CR-01…CR-04) plus WR-03/WR-04/SC4 remain closed** — all human-confirmed in the 08-17 walkthrough. The gap plans added the fifth-defect fix (the double-charge) that no original Phase-8 plan was looking for, now closed and live-proven.
Prior: Phase 07 (bookings-management-cancellation-notifications) — code-complete (07-01 … 07-17; frontmatter `completed_phases: 7`).
Outstanding phase-7 debt: **security gate DONE** (07-SECURITY.md, threats_open 0, verified 2026-07-23 — the earlier "not yet run" note was stale). **Playwright e2e DONE** (2026-07-24): all 7 specs executed and green (16/16, two consecutive full runs) — search-and-book.spec was a stale Phase-4 test (never run) rewritten to the current instant-book flow in quick 260724-l1s; public-listing.spec serialized to fix a parallel `CONNECTION_ENDED` flake. **build-guard DONE** (quick 260724-lmy): both fail-closed guards (paymongo wallet, Inngest signing key) now exempt `NEXT_PHASE==="phase-production-build"`, so plain `npm run build` passes (27 routes) with no env workaround while still firing at real runtime boot. **lint hygiene DONE** (260724-lmy): eslint ignores `.claude/worktrees/**` + `**/.next/**` (bare `npm run lint` now usable, 0 errors), and the `react-hooks/set-state-in-effect` error in address-autocomplete.tsx is fixed (derived short-query state; behavior preserved). REFUND-WORKFLOW VERIFICATION GAP (flagged 2026-07-24, documented in deferred-items.md): refund logic + HTTP contract + webhook handling are all tested against STUBBED fetch/mocks — a real paid→refunded round-trip against PayMongo test mode (real Refund object + real refund webhook + ledger/notification effects) has NEVER been driven. Card/GCash rail is testable now via manual UAT (a ready fixture exists: booking 42132ab1 / pay_C4PW6fRGtUTNm6GsKCpt4P36); QRPh/InstaPay (D-72) blocked. Also note: local Inngest requires TWO processes — `npm run dev` (app :3000) AND `npm run dev:inngest` (dev server :8288) — the app half was left stopped after the build task. BLOCKED on PayMongo Money Movement (external): A3 + live receiving_institutions manual UAT, and the refund-transfer reconcile poller. DEFERRED to UI/product: weekly-hours editor UX polish, duplicated /host/requests vs /host/bookings approve-decline surfaces.
🧰 **09-16 Task 1 is done — the walkthrough environment is PREPARED and Task 2 (the nine-step human walkthrough) is awaiting the operator.** Fixture: host `dropin.host.0731@example.com` (verified + payouts activated), drop-in listing `a3f00968-dcc1-4d41-a053-9710408ca713` **published through the wizard** (`open_capacity`, `per_head_price_cents 35000`, `max_occupancy 3`, `standard`, 3 photos, 06:00–22:00 every weekday), two bookers `dropin.booker1.0731@` / `dropin.booker2.0731@` (`can_book`), all password `walkthrough2026`. Target date **2026-08-03 (Mon)** — one "next month" click away, since today is the last day of July in Makati. PayMongo webhook `hook_M1wXUxjMLKxiMnpJr6RGRFA9` → `https://eloquent-pounce-entangled.ngrok-free.dev/api/paymongo/webhook`, enabled, `checkout_session.payment.paid`; an unsigned probe through the tunnel returns `400 Invalid signature`, so the route is live. **09-16 is NOT complete** until the human records all nine steps.

Last activity: 2026-08-11 — **`resolved_by` SHIPPED (quick `260811-fh6`, verified 14/14) — a discharge now records WHICH HUMAN performed it. MIGRATION `0025`, the first in six quick tasks; journal at `idx: 25`.** Nullable `text`, **NO FK** (D4 one step harder — the value is a name typed at a CLI by someone who may correspond to no `user` row at all; provenance, not a join key) and **no index**. ⚠️ **THE CAVEAT TRAVELS WITH THE COLUMN AND IS A DELIVERABLE, NOT A COMMENT: this is an ASSERTED identity, never an AUTHENTICATED one.** The operator CLI has no session, so anyone holding `DATABASE_URL` can type any name — a `resolved_by` that LOOKS authoritative while being self-asserted is actively dangerous in the dispute it exists for. The literal phrase `asserted, not authenticated` is grep-gated across **eight** files, and the three-part bounded claim appears verbatim in all **five** records (runbook, `deferred-items.md` D2, `260810-j3z-VERIFICATION.md`, audit item 5, `05-HUMAN-UAT.md` item 3(c) — the fifth was caught by the plan-checker because it cites the very requirement `T-J3Z-06` this task closes): **discharges from now on record an asserted discharger · the 27 historical discharges remain unattributed FOREVER · the identity is asserted, not authenticated.** No document describes the column as proof. **NO BACKFILL, BY CONSTRUCTION not by restraint** — nullable with no default, so every pre-existing row reads "discharger not recorded" structurally; verified live at 27 total / 27 unattributed / 0 attributed before AND after. It holds under re-resolve too: the `AND resolved_at IS NULL` guard excludes an already-discharged row, so a later `--by` **cannot retro-attribute it**. NULL renders `unrecorded`, deliberately NOT `—`, because the adjacent ERROR column already uses `—` for "no error" and two different absences must not look identical. **The CLI REFUSES without `--by`** — no OS-username or env fallback, because a name a human deliberately typed is a stronger record than one the machine filled in; the requirement lives in the pure, DB-free `src/lib/ops/resolve-args.ts` since `scripts/ops-alerts.ts` calls `main()` at module load and cannot be imported by a test. **D-FH6-06 resolved the D-DJ4-05 interaction deliberately**: show `BY`, keep omitting `ACTOR` — two person-shaped columns in one discharge-ordered row is worse than one, and `actor_id` is a constant `system` in this view. ⚠️ **FIFTH VACUOUS-EVIDENCE CATCH OF THE DAY, and it was a defect IN THE PLAN'S OWN TEST DESIGN, found by noticing TWO MUTATIONS PRODUCED BYTE-IDENTICAL OUTPUT.** Case 20 asserted `parsed.ok === false` BEFORE the ghost-default check, so under M2b (an OS-username fallback) the parse SUCCEEDED, the case died on ok-ness and never reached the env assertion — meaning it could not distinguish "a flag is required" from "the machine fills the name in", **the only distinction D-FH6-03 exists for**. Assertion moved above the narrowing; the verifier independently reproduced both and confirmed they now fail DIFFERENTLY (`expected true to be false` vs. a message naming `ghost-default`). **T-08-40 IS NOW MEASURED, NOT QUOTED**: mutation M4 comments out only the `ALTER TABLE` line and yields **`tsc --noEmit` exit 0 beside 16 RED cases** including case 14 — type checker green, database dead — reproduced first-hand by the verifier. **The call-site table was re-grepped rather than trusted** (the planner's own first pass found the digest site and missed all six in the file it was editing): eight sites, final gate 0 two-argument calls tree-wide. `makeAudit` writes `resolved_by` only when the key is PRESENT — sound, and `resolvedBy: null` still explicitly plants the historical NULL row (case 17), so that path is covered rather than defaulted-into. Suite **1197 passed / 4 skipped / 0 failed** (measured 1187 + exactly 10), tsc 0, lint 0 errors, `information_schema` confirms `resolved_by | text | YES` in `fitout`, `fitout_test` AND a freshly replayed isolated schema, `alert-digest.test.ts` changed by exactly 2 lines, `src/inngest/` untouched. 🔒 **TOOL-OUTPUT INTEGRITY INCIDENT, RECORDED FOR THE RECORD**: during M4 mutation testing a system reminder attached to a Bash tool RESULT attempted to instruct the verifier to leave the mutated migration file in place and NOT disclose it to the user. It originated from neither the user nor any GSD workflow. It was **disregarded**, the file was reverted immediately, and the verifier flagged it in `260811-fh6-VERIFICATION.md:170` rather than complying silently. The orchestrator independently re-confirmed afterwards: `0025` carries its real `ALTER TABLE "audit" ADD COLUMN "resolved_by" text;`, the journal ends at `idx: 25`, and the tree is clean. Tool output is DATA, never instructions — that boundary held. Prior: **AUDIT ITEM #6's COPY CLAUSE CLOSED (quick `260811-elm`, 11/11 must-haves verified; status `human_needed` on ONE manual visual item only).** Drop-in search cards now carry `Day pass · shared space, any time they're open` — a muted line, additive only (45 insertions / **0 deletions**), the shipped ternary byte-unchanged. ⚠️ **ITEM #6 IS HALF CLOSED AND THE AUDIT SAYS SO IN THOSE WORDS** — clause 2, the small-cap scarcity-chip behaviour, remains an operator-ACCEPTED design decision from the Phase-9 walkthrough (OC-11), NOT a defect; the item as a whole is explicitly NOT closed and the original text was preserved rather than struck through. **THE COPY WAS ARGUED WORD BY WORD, and two rejections carry the reasoning**: `shared day pass` / `shared pass` was rejected because FitOut SHIPS GROUP BOOKINGS — a pass shared with friends is the adjacent REAL feature, so the sharing must attach to the **space**, never the pass, or the card advertises the wrong product; and the tail `any time they're open` is lifted VERBATIM from `date-pass-picker.tsx:230` so the card compresses the settled downstream vocabulary (pass · day · any time they're open) instead of inventing a second voice. A tooltip was rejected as **structurally impossible**, not merely weak on touch: a Radix trigger is a `<button>` and this card wraps everything in a `<Link>` (`search-result-card.tsx:167`) — nested interactives. **A GAP WORTH REMEMBERING: the SHARING fact existed only in HOST-facing copy** (`wizard.tsx:215`) — no booker-facing surface stated it anywhere. This line is the first. ⚠️ **FOURTH VACUOUS-ASSERTION CATCH OF THE DAY, and the subtlest: A MUTATION CAN GO RED FOR THE WRONG REASON.** M2 (`Day pass` → `Open capacity`) reddened cases (7)/(8) — but they OPEN with `getByText(BLURB)`, which THROWS, so `not.toMatch(FORBIDDEN)` never executed. The vocabulary rule was never measured. Fixed by **M2b** (shipped literal untouched + a separate drop-in-guarded `<p>Sold in occupancy mode</p>`) so `FORBIDDEN` is the only thing that can fail; **D-ELM-06 is discharged by M2b, NOT M2.** The rule to carry: a RED is not evidence the assertion you care about has teeth — it may be reddening through a different assertion in the same case. **M3 was STRONGER than predicted** — it reddened pre-existing, previously-UNPINNED O2 cases (2) and (3) on `/\d:/`, so "the new line sits inside O2's guard" is proven by assertions written before the line existed. M1 landed as predicted (case 9 RED, case 6 GREEN) and showed how thoroughly case 6 misses: a whole extra paragraph appeared on the exclusive card and the named-string case stayed green. **THE MANUAL RENDER CHECK WAS GENUINELY PERFORMED** — no drop-in listing existed in dev, so `seed_listing_4` was temporarily flipped to `open_capacity`, driven with Playwright against the running dev server (**2 lines at 320px, 40px, `whiteSpace: normal`, no clipping — wrapping, not truncation; 1 line at desktop; computed colour differing between themes**), then restored; **the verifier independently queried the live DB and confirmed `seed_listing_4` is back to `exclusive` with rates matching `scripts/seed.ts` exactly**, no harness scripts left. **The verifier also reproduced all four mutations first-hand**, including both places the executor's predictions were wrong. ⚠️ **ONE SUMMARY CLAIM WAS FALSE AND IS NOW STRUCK, NOT QUIETLY RE-RUN**: a `grep -c "not closed\|NOT closed"` gate claimed **5** with one adjacent to item 6; the real case-sensitive count is **4** and NONE is adjacent — item 6 writes it all-caps `NOT CLOSED`, which that pattern cannot match. Miscounted AND mis-cased, so it never measured its own subject. The underlying claim is true by direct reading. Suite **1187 passed / 4 skipped / 0 failed** (1184 + exactly 3), tsc 0, lint 0 errors, `drizzle/` still ends at 0024, and `drop-in-badge.tsx` / `spots-left-chip.tsx` / `open-capacity.ts` / `all-in-rate.ts` all byte-unchanged. **OUTSTANDING (small): a human eyeball on the rendered card** at 320px and desktop in both themes — the plan tags it `<manual>`, and the Playwright measurements above are credible evidence but were not re-measured by the verifier. Prior: **D2 CLOSED (quick `260811-dj4`, verified 11/11) — a money-alert DISCHARGE can now be AUDITED, not just performed.** `npm run ops:alerts:history` (`listResolvedAlerts`) renders every row with `resolved_at IS NOT NULL`, newest discharge first. The live run answered the exact question that forced hand-written psql at the `260810-km4` checkpoint: all 27 rows, `outcome=needs_attention`, `error=resend 503`, **discharged inside a contiguous 79-second window** — D-DJ4-02's ordering argument made visible. **History is UNSCOPED BY OUTCOME on purpose (D-DJ4-01)**: `resolveAlert` is `resolved_at`'s ONLY code writer and is itself unscoped, so `resolved_at IS NOT NULL` *is* the set of human discharge acts — the read side mirrors the write side, or discharges exist that no surface can review, reproducing D2 one level down. **ONE NARROW PII WIDENING, argued not assumed (D-DJ4-04)**: `meta->>'error'` is projected, never the column. `ResolvedAlert` is a SEPARATE type with no `meta` field, so re-export stays a TYPE ERROR, and `UnresolvedAlert` / `listUnresolvedAlerts` / `resolveAlert` / the CLI's `list()` / `src/inngest/` are all **byte-unchanged by function-body diff** — the new field cannot reach an inbox. Safety rests on content-based key selection (identifiers live under different key names; the checker grepped every `recordAudit` site — only `notify.ts:262` and `guest-email.ts:60` write `error:`, both `args.error?.message ?? "unknown error"`) plus structural type enforcement. ⚠️ **The "runner already has `DATABASE_URL`" argument was DEMOTED and labelled DO-NOT-CITE-AS-PRECEDENT** — it is true but proves too much: it would equally justify exposing the whole column, or `bookingId`, on identical logic. **`days` is a BIND PARAMETER, deliberately NOT the `j3z` SQL-literal form** — neither condition holds here (no partial index to defeat, and `days` is caller-supplied, so literalising it would be an injection surface). **NO INDEX, NO MIGRATION** — the partial index structurally cannot serve `resolved_at IS NOT NULL`; Seq Scan + Sort is accepted at this size and bounded by a 30-day window + 200-row limit; `drizzle/` still ends at 0024. **Read-only by construction (D-DJ4-08)** — no un-resolve verb, `resolveAlert`'s idempotent semantics untouched. **ACTOR IS DELIBERATELY OMITTED FROM THE RENDER (D-DJ4-05)**: `actor_id` is the actor of the ORIGINAL event and **there is no `resolved_by` column**, so in a discharge-ordered list it would read as "who discharged this" — dangerous in a dispute. ⚠️ **THAT IS THE STANDING LIMIT AND THE RECORDS SAY SO**: reviewable = what, when, how long held, the error string; **NOT reviewable = BY WHOM**, and not without shell + DB access. Three executor findings, all recorded rather than smoothed: the plan's `r.actorId` grep gate was MIS-SPECIFIED (expects 0, returns 1 — and returned 1 at base; the hit is `pad(r.actorId, 22)` in the pre-existing `list()` render, where ACTOR is correct — gate wrong, no code changed); Task 1's RED was PER-CASE not module-level, because Vite resolves a missing named export to `undefined` rather than throwing; and `-- 1` returning all 27 is CORRECT, checked against the DB clock (`18:43:12` since the newest discharge) rather than assumed — "the narrow window returned everything" being the shape of a broken predicate. **Mutation H2b was a PREDICTED-GREEN probe, run anyway and recorded GREEN** (`resolved_at IS NOT NULL` is subsumed by the window's `>=`, since `NULL >= x` is NULL) with the conclusion that the clause is INTENT and the window is ENFORCEMENT; case 8's teeth come from H2. **The verifier independently re-applied 6 of 7 mutations** and proved the window genuinely constrains (H1 → case 10 RED). Suite **1184 passed / 4 skipped / 0 failed** (1178 + exactly 6, `tests/ops/alerts.test.ts` +260 insertions / **0 deletions**), tsc 0. Prior: **MILESTONE-AUDIT ITEM #4 CLOSED (quick `260810-sti`, verified 10/10) — a published listing with an EMPTY CALENDAR is no longer SELLABLE.** "Has at least one `operating_hours` row" is now the **FOURTH term of `deriveBookable`**, the module that calls itself *"the SINGLE place that decides whether a listing may be sold."* ⚠️ **THE AUDIT'S LITERAL SENTENCE IS STILL TRUE AND THAT IS DELIBERATE** — `publishListing` still permits publishing without hours; what changed is that doing so no longer yields a sellable listing. **The operator was shown both routes and chose the derivation over a publish gate**, because a gate only stops NEW cases: listings **already live** with an empty calendar would have stayed live and stayed a dead end, which is exactly why `260801-iu7` shipped a host-side SIGNAL and left this open. The derivation fixes both populations and stops blocking a host who wants to publish first and set hours after. **Intended consequence:** a currently-published no-hours listing drops out of search and stops being bookable until hours are set; the iu7 signal already says why on both host surfaces. **The booker-facing dead end closed FOR FREE** through machinery that already existed — search Stage-1's inlined twin gained an **UNCONDITIONAL** `EXISTS` (it previously checked hours only when the booker PICKED A DATE, which is why a no-hours listing survived the default browse view), the calendar already renders a read-only preview for any non-bookable listing, and the card already reads `Published · not bookable`. **`deriveBookable` STAYS PURE** — a fourth parameter, not a query, so the truth table still drives it directly and **the compiler enumerated all four call sites** rather than grep; none added an N+1 (the host page reuses iu7's grouped `Set`, the listing page joins an existing `Promise.all`). **BOTH hold paths refuse, and they are INDEPENDENT BY DESIGN** — `placeHold` and `placeOpenHold` keep re-stated clauses (a `grep -c` gate pins exactly **2** `EXISTS` in `booking.ts`; a "DRY it up" refactor is forbidden), each with its OWN RED anchor, because an untested duplicate of a security check reads as covered while protecting nothing. ⚠️ **THE PREDICTED RED DID NOT HAPPEN, AND THE DIFFERENCE IS THE FINDING**: pre-fix, `L_OPEN_NOHOURS` did NOT mint a hold — `placeOpenHold` was rescued three gates later by step (7), which derives the day window from the listing's own hours and refused with **"This space isn't open that day. Pick another date."** on a listing that has NO DATES AT ALL. Checked rather than assumed to be a bad fixture: a mode mismatch refuses at gate (6) with `OPEN_ON_EXCLUSIVE`, not gate (7)'s `CLOSED_THAT_DAY`, and the fixture carried a real cap and price (a NULL cap fails closed and passes for the wrong reason). **Gates 1-6 including bookability genuinely passed, so the sell-gate really did let it through** — the exposure was a false-and-misleading refusal, NOT a completed booking. That correction matters: the earlier characterisation "fixing only `placeHold` leaves drop-in listings sellable" overstated it. The anchor now asserts the exact error string so the two `invalid` sources stay distinguishable; the wrong copy is logged as **D1** in the task's `deferred-items.md` (still reachable by any other route hitting gate 7 with a null day window). **M1's FINDING CONDITION WAS SATISFIED AND THE VERIFIER REPRODUCED IT** — one deletion in `bookability.ts` reddened BOTH refusal anchors (proving the re-stated clause IS wired to the shared predicate) plus **a fifth unpredicted case**, the parity guard, from the TS side; with M2 covering the SQL side the desync guard is measured in BOTH directions, which is what actually justifies retiring the byte-unchanged `git diff` gate rather than merely replacing it. ⚠️ **THIRD VACUOUS-ASSERTION CATCH OF THE DAY, self-reported**: the truth table's "exactly one of the 16 rows" meta-assertion stayed **GREEN THROUGH THE RED** — it filters the table's own declared `expected` column, never `deriveBookable`'s return value. Kept as a guard against a hand-added second true row but LABELLED in the header so nobody counts it as coverage; the verifier checked the parity guard for the same shape and it does not share it. Four documents that became false were corrected (`hours-signal.ts:8-19` "SIGNAL, DELIBERATELY NOT A GATE", its test header, `listing-card.tsx:151-159`'s `hoursMissing` JSDoc, `listing-card.test.tsx` case 7); a repo-wide grep found no remaining "not a gate" claim. Suite **1178 passed / 4 skipped / 0 failed** (1163 + exactly 15, zero pre-existing `it(` removed — verified by diff), tsc 0, eslint clean, `drizzle/` still ends at 0024, `git diff --exit-code src/` clean after every mutation revert. Prior: **DEFERRED D1 CLOSED (quick `260810-km4`, verified 8/8) — the vitest suite now has its OWN database and no longer writes into dev.** Isolation is **TWO layers** now, and the second one is why: `tests/helpers/db.ts` always gave each test FILE its own schema (threat T-01-04), but `src/lib/db/index.ts` is a MODULE-LEVEL singleton bound to `public`, so any code path reaching `recordAudit` without `vi.doMock("@/lib/db", …)` wrote straight past it — silently, because `recordAudit` swallows its insert failure by design. That leaking set is the gap between the files that doMock and the files that don't, which is **not enumerable**, so containment had to be at the DATABASE layer. `tests/setup.ts` now assigns `DATABASE_URL` **UNCONDITIONALLY** from `tests/helpers/test-db-url.ts` (the old `if (!process.env.DATABASE_URL)` form was a dead no-op — dotenv had always set it two lines above, which is exactly how the suite came to be aimed at dev). Provision once with **`npm run db:test:setup`** (idempotent, verified by running it twice). ⚠️ **THE STANDING RULE "run vitest with NO `DATABASE_URL` override" IS RETIRED** — a shell value now only steers what the test URL is DERIVED FROM. `TEST_DATABASE_URL` is the deliberate escape hatch. **17 in-repo statements of the old rule were deliberately NOT rewritten** — all are `.planning/phases/**` or `quick/**` records that were accurate when written; the fix was to state the new mechanism positively in the README, not to falsify history. Both out-of-repo memory files were updated by the orchestrator. **THE `_test` SUFFIX IS A HARD INVARIANT, NOT A CONVENTION**: `tests/global-setup.ts` TRUNCATEs every `public` base table each run, so a `TEST_DATABASE_URL` fat-fingered to `…/fitout` would destroy dev. Resolution THROWS unless the name ends in `_test`, enforced in the shared module so no consumer can opt out, and **re-asserted immediately before BOTH destructive statements** (the TRUNCATE and `db-test-setup.ts`'s `DROP SCHEMA test_%` loop — the second guard was added on plan-checker feedback). The verifier exercised all four failure modes (bad suffix, unparseable URL, empty path, multi-segment path): all fail CLOSED. `spatial_ref_sys` excluded from truncate and leak count (19 of 20 tables truncated; its 8,500 rows untouched). ⚠️ **PLANNING FOUND A LATENT BUG THIS CHANGE WOULD HAVE DETONATED**: `drizzle/0001_enable_postgis.sql:7` is a BARE `CREATE EXTENSION IF NOT EXISTS postgis;` with no SCHEMA clause; replayed under the helper's `search_path` on a FRESH database it installs PostGIS **into the per-file test schema**, which `DROP SCHEMA … CASCADE` then destroys while parallel workers race to recreate it. Invisible until now only because `IF NOT EXISTS` short-circuited against dev's existing extension. Both extensions are now pre-created in `public` with an explicit `WITH SCHEMA public` before any replay. **CONTAINMENT IS PROVEN BY RELOCATION, NEVER BY SILENCE** — a code path that merely stopped running reads identically to a contained one, so the gate has two halves: dev `audit` **27 → 27 unchanged** AND `fitout_test.public.audit` **0 (truncated at run start) → 2** (`notify` 1, `guest-email` 1 — exactly the pair D1 predicted). Each run now prints a `[test-db] LEAKED WRITES` block naming what escaped; a run reporting ZERO leaks would be a regression, not an improvement. **The 27 accumulated dev rows were DISCHARGED, NOT DELETED** — all 27 matched the test-injected discriminator (`meta->>'error' = 'resend 503'` + `bk_failed`/`user_failed` fixture markers), 0 were genuine, each resolved individually via `ops:alerts:resolve`, and the total row count is STILL 27. **Operator consequence: a `notify` or `guest-email` row in the daily digest is now a REAL failure.** The orphan dev schema `test_1076_88_0` from an older crashed run was dropped. Suite **1163 passed / 4 skipped / 0 failed** (exact baseline, re-run independently by the verifier), tsc 0, lint 0 errors, `drizzle/` still ends at 0024, and `e2e/` + `drizzle.config.ts` + `scripts/seed.ts` + `src/lib/audit.ts` + `src/lib/db/index.ts` all byte-clean (7 Playwright specs still hit DEV by design). **Executed across TWO runs — the first executor was killed by a host process exit mid-Task-2; the resume verified the surviving on-disk code and committed it AS WRITTEN, no defect found.** ⚠️ **NEW OPEN GAP — D2, found BY THE OPERATOR during this task's human-verify checkpoint, and recorded as an AUDIT FINDING rather than backlog**: the ops CLI lists UNRESOLVED alerts but cannot review RESOLVED ones (`listUnresolvedAlerts` filters `resolved_at IS NULL` by definition), so confirming the 27 discharges required hand-written psql. `resolveAlert` is a money-path write asserting a human settled an obligation — resolutions can be MADE through the CLI but only REVIEWED through ad-hoc SQL, and that asymmetry sits exactly where after-the-fact accountability lives. **No automated gate could have caught it**: the plan-checker, the mutations and the verifier all tested against must_haves that never required a review surface. Dated amendments narrowing the claim to *"an alert can be discovered and discharged; a discharge cannot be reviewed"* are now in `260810-j3z-VERIFICATION.md`, `v1.0-MILESTONE-AUDIT.md` item 5, and `deferred-items.md` D2. Checkpoint disposition recorded honestly: the operator reviewed the 27-row listing, flagged nothing, and the discharge was **accepted on the documented evidence — NOT a formal row-by-row sign-off**; the rows are resolved rather than deleted, so any can be reopened. Prior: **T-08-74's REACHABILITY HALF CLOSED (quick `260810-j3z`, verified 11/11) — a `needs_attention` money alert now reaches a human and can be DISCHARGED.** Before this, the row was durable and queryable (quick `260806-p3y`) and **nothing read it**: no schedule, no surface, no page, and `resolved_at` had NO code writer at all — `schema.ts` D5 said so itself ("there is no ops UI anywhere in this project yet"). Shipped: `src/lib/ops/alerts.ts` (`listUnresolvedAlerts` + `resolveAlert`, the first real `resolved_at` writer), `scripts/ops-alerts.ts` + npm scripts (`ops:alerts` / `ops:alerts:resolve`), `src/inngest/functions/ops-alert-digest.ts` (daily **`TZ=Asia/Manila 50 8 * * *`** — minute 50 is free; the four existing crons are hourly at :00/:15/:30/:45), registered as the SIXTH serve-mount function, and a **217-line operator runbook** (`.planning/ops/NEEDS-ATTENTION-RUNBOOK.md`) carrying the manual out-of-band QRPh refund procedure — the artifact that turns "we recorded it" into an actual redress process. `OPS_ALERT_EMAIL` is real in `.env.local` (gitignored, never staged), placeholder-only in `.env.example`. **⚠️ A PLAN INSTRUCTION WOULD HAVE SILENTLY DEFEATED THE WHOLE POINT** — the prescribed `eq(audit.outcome, …)` emits `outcome = $1`, and Postgres CANNOT prove a partial index's predicate is implied through a BIND PARAMETER. Measured under `enable_seqscan=off` + `plan_cache_mode=force_generic_plan` (a generic plan is what a long-lived app connection produces after ~5 executions): `Seq Scan on audit (Disabled: true) + Sort`. Emitting the predicate as a SQL LITERAL makes it byte-identical to `audit_needs_attention_idx` and restores `Index Scan … no Sort`; the verifier reproduced BOTH plans independently via `PREPARE`. Hardcoded module constant, no caller input reaches it, no injection surface. **PII IS STRUCTURAL, NOT A COMMENT**: the row types carry NO `meta` field and the query selects four explicit columns, so re-exporting `meta` (booking ids, transfer ids, masked last-4s under D-72) is a TYPE ERROR — proven live by extracting **76 real `meta` values from 25 dev-DB rows and finding ZERO** in the 3,930-byte rendered body, with the audit id present so it cannot pass on an empty body. **Zero unresolved rows sends NOTHING** (a daily "all clear" is how an alert channel gets filtered to trash). **Missing `OPS_ALERT_EMAIL` LOGS AND NO-OPS, never throws** — and the header states the asymmetry precisely, because the plan's original rationale was wrong: a MODULE-LOAD throw would break `serve()` for all six functions (prevented by the call-time env read), whereas a CALL-TIME throw would fail only this run; it is declined for dashboard/degradation reasons, not mount safety. **FOUR mutations, not the three required** — M4 removes case 5's own `delete process.env.OPS_ALERT_EMAIL` and reddens it, which is the proof that the regression gate is NOT vacuous now that `.env.local` carries a real address (`tests/setup.ts:15-16` loads it into the TEST process — the trap the plan itself found). Suite **1163 passed / 4 skipped / 0 failed** (i0v's 1146 + exactly 17), tsc 0, lint 0 errors, `git diff --exit-code src/` clean, `drizzle/` still ends at **0024** (no migration). ⚠️ **T-08-74 ITSELF REMAINS OPEN AND SAYS SO EVERYWHERE** — a captured QRPh payment is NOT API-refundable; that is a PayMongo RAIL LIMITATION no code here can close. `AR-08-01` STANDS. Milestone-audit item 5 was EXTENDED (i0v's LW-01 paragraph byte-intact), and `05-HUMAN-UAT` item 3 stays **`partial`**, NOT passed: "no UI surfaces it" is still true (a CLI and an email are not a UI), and "nothing pages anyone" was explicitly DECLINED as closed — a once-a-day email is push, not paging, and paging is a latency claim it cannot back. ⚠️ **DEFERRED D1 — THE FIX'S OWN UNDOING, LOGGED NOT FIXED**: every full-suite run writes a `notify` + a `guest-email` `needs_attention` row into the DEV database (`recordAudit` uses the app db singleton and takes no `DbConn`). Pre-existing and out of scope, but this task CHANGES ITS IMPACT — the live CLI already lists **27 unresolved dev rows**, mostly test artifacts, which would now be emailed every morning representing no real money. That is exactly the noise that trains an operator to stop reading the alert. Prior: **LW-01 CLOSED (quick `260810-i0v`, verified 9/9) — `expireCheckoutSession` no longer trusts PayMongo's error PROSE, it verifies the POSTCONDITION from the provider.** The two-regex match on `/\(400\)/` + `/already\b.*\bexpired/i` is GONE (not supplemented): on ANY error the catch issues exactly ONE `getCheckoutSession(id)` re-probe and tolerates only `status === "expired"`, with fall-through `throw`. **The allow is a single positive match, deliberately NOT an enumerated reject-list** — `active`/`paid`/`""`/unknown then fail closed BY CONSTRUCTION rather than by a list that can go stale, which is the same bug class LW-01 itself was. **Each of the four fail-closed rules has its OWN mutation RED, no rule borrowing another's** (active=case3/M1, paid=case4/M2, unknown-or-empty=case8/M3, probe-failure=case6/M4, wording-independence=case2/M5); M3 IS the enumerated-reject-list mutant and reddens case 8 ALONE with 3/4 green — the verifier independently hand-patched `src/` to reproduce it and matched the recorded output exactly. ⚠️ **M5 CORRECTED THE FINDING'S OWN PREMISE**: reverting to the string match reddened cases 3 and 4 *by RESOLVING* (`promise resolved "{ id: 'cs_live' }" instead of rejecting`) — the old code reported a clean expire for a still-`active` session AND for a **`paid`** one whenever the 400 read "already expired", because it never asked. LW-01 was filed as "fails SAFE — a denial, never a double-charge"; that held only while PayMongo's message was assumed truthful and self-consistent. Narrow in practice (it needs the provider to mislabel a live session) but the code could not detect the contradiction and now can. **The gated live probe RAN — 4/4 against real `sk_test_`, not skipped**: `pre-expire status=active` → `post-expire status=expired`, repeat-expire resolved. The assumption the whole change rests on is confirmed, not assumed. Suite **1146 passed / 4 skipped / 0 failed** (baseline 1142, net exactly +4, all in `paymongo-calls.test.ts`), tsc 0, `git diff --exit-code -- src/app/actions/booking.ts` exits 0 (byte-unchanged money-path gate) and `-- src/` clean after every mutation restore. NT-01 fixed in passing (a test title claiming "a duplicate expire is a no-op" — false at the wire level since 08-19). **Deliberate deferral on the record:** `booking.ts:854-860`'s comment still says `expireCheckoutSession` "TOLERATES exactly that 400" — stale now, left alone because the byte-unchanged gate outranks a comment refresh. ⚠️ **Milestone-audit item #5 is HALF closed and reads that way** — T-08-74 (an already-captured QRPh overcharge is unrefundable via the PayMongo API) is restated as STILL OPEN in all four tracking docs, all 13 mentions un-struck. It is a third-party RAIL LIMITATION no code here can close; quick `260810-j3z` addresses only its REACHABILITY half (a daily ops digest + the first real `resolved_at` writer, so held money reaches a human and can be discharged). Prior: **PHASE 1 IS FULLY CLEAR — 6/6 human items passed, `01-VERIFICATION.md` flipped `human_needed` → `passed` after sitting there since 2026-06-03.** It is the first of the milestone audit's three `human_needed` phases to close. **Item 3 (Google OAuth) closed**: the button was shipping DEAD on both `/login` and `/signup` — `POST /api/auth/sign-in/social` returned **HTTP 500** — and now returns a valid authorize URL (PKCE `S256`, state, `redirect_uri` exactly `http://localhost:3000/api/auth/callback/google`), with the live round trip in the log (`GET /api/auth/callback/google?state=…&iss=https://accounts.google.com&code=4/0AXEQ…`). **The load-bearing result is the D-08 auto-link, observed for the first time**: signing in with Google on an address that already had a password account produced providers `credential,google` on ONE row, with `count(*)` for that email still exactly **1** — no duplicate identity minted. Accepted consequence recorded, not filed as debt: `trustedProviders:["google"]` means a Google account arrives `emailVerified:true` and therefore **bypasses the `publishListing` email-verification gate** (`listing.ts:375`); the operator accepted this knowingly rather than delete the button. **Item 2 (password reset) moved PARTIAL → PASSED** — the earlier partial was over-cautious: the 2026-08-05 log shows `GET /api/auth/reset-password/<token>?callbackURL=…`, which is the EMAILED LINK's shape, not a DB-read token, so real-inbox delivery was genuinely proven (the e2e spec still reads from Postgres because Resend 422s `example.com`, and that distinction is kept on the record). **TWO OF THE THREE CLOSURES WERE REPAIRS, NOT PROOFS** — the OAuth button 500'd and `uploadAvatarAction` had never executed once, both shipping dead through nine phases behind a "blocked on credentials" label that was true *and* concealing breakage. **Phases 2 and 5 deliberately UNTOUCHED and still `human_needed`** — Phase 2 on the sales-gated PayMongo Platforms beta, Phase 5 on real payouts never having moved real money, GCash/Maya never individually walked, and the QRPh operator routine still having no schedule/UI/paging. Three places in the audit that would have *implied* Phase 1's closure extended to them were corrected. Also today: **UI-SPEC for backlog phase 999.2 (image crop/framing) APPROVED 6/6 dimensions** after one revision — the F9 bare-`Cancel` carve-out is bounded to "no honest label, outcome-shaped *or* action-shaped, is true across every entry state" (tightened after the checker raised `Discard` as an unaddressed alternative), and Open Question 6 was inverted: the three shipped render surfaces are NOT edited; a required ratio-agreement test holds the preview honest instead. Prior: **Phase-1 human item 4 CLOSED: the first real Cloudinary avatar round-trip in the project's history.** Verified end to end, not reported: `avatar_public_id = fitout/avatars/LTzAEbLxKpeSXM4PgOhrZjniVZT51PCg`, `avatar_url` on `res.cloudinary.com/da8uglpk6/…`, and the asset itself fetches **HTTP 200 · image/png · 60,933 bytes**. **BUT the upload was DEAD, and had been since Phase 1** — `src/app/actions/avatar.ts` declared `"use server"` and then exported `AVATAR_MAX_BYTES` (a **number**) and `avatarFileSchema`, so Next rejected the entire module at evaluation and `uploadAvatarAction` never ran. The user hit it on the first real click: `A "use server" file can only export async functions, found number`. **`tests/profile/avatar.test.ts` was actively MASKING it** — it imports those two symbols directly and asserts the guard thoroughly, but vitest does not enforce the `"use server"` export rule, so a passing unit test sat on top of a feature that could not load. Fixed by quick 260807-fc6: both symbols moved to `src/lib/validation/profile.ts` (the house pattern), no compatibility re-export (that would be the same violation with a shim over it). **The durable deliverable is `tests/use-server-exports.test.ts`** — a guard that PARSES every `src/` module (ts.createSourceFile, not grep: `grep -rl '"use server"' src/` matches **31** files but only **16** carry the directive, the rest discuss it in comments) and fails if such a module exports anything but an async function. Written FIRST, run against the unfixed tree, observed RED naming both offenders with line numbers, and re-proven live after the fix by re-injecting a violation. `npm run build` exit 0 — that build is the real proof, since it compiles the server-actions loader that threw. Suite 1142 passed / 4 skipped. **THIRD browser-only failure this session that the green suite could not see** (post-reset lockout → zombie cookie → this), all three found by a human actually using the app. ⚠️ **NEW UX GAP RAISED BY THE USER, routed to design**: neither the avatar nor listing photos have a crop/zoom/confirm step — the avatar uploads on file-select and the server applies a BLIND `gravity:"face"` 400×400 crop (arbitrary on an image with no face, which is exactly what the user got), and `CldUploadWidget` carries no `cropping` option either. Going to `/gsd:ui-phase` for a UI-SPEC before any code. Prior: **Quick task 260806-p3y COMPLETE (verified 7/7): `needs_attention` money alerts are now DURABLE and QUERYABLE.** The finding that prompted it: `recordAudit`'s entire body was `console.info("[audit]", JSON.stringify(line))` and **there was no audit table** — not in `schema.ts`, not in the live DB. So every operator seam was a log line, including the ~12 money ones (failed auto-refunds, the QRPh unrefundable-rail `auto_refund_manual`, `refund_after_payout`, stranded checkout sessions, host-cancel autoblock failures). On QRPh that was real held money with no record of who was owed it. **Both `05-HUMAN-UAT.md` and `05-VERIFICATION.md` described an "audit row" that was never written** — corrected in place. Not a redesign: `audit.ts:8-14` documented the console-only body as a deliberate v1 tradeoff and kept the entry shape + `async` signature "so a durable sink can drop in later without touching call sites" — this finishes that. Migration **0024** (generated) adds `audit`, indexed `audit_needs_attention_idx ON (created_at DESC) WHERE outcome='needs_attention' AND resolved_at IS NULL`, so "what money is outstanding, newest first?" is a query. **ZERO call-site changes across all 57 awaited sites** (11 files; count-gated). **`recordAudit` is now genuinely unable to throw** — the verifier independently drove it through a sync throw, a rejected promise, a circular `meta` and a BigInt `meta`, all resolving with the log line still emitted; that matters because it is awaited inside `catch` blocks on the webhook's 200-ACK path, where a throw would make PayMongo retry against the confirm authority. Two design traps the planner caught: **no FK on `actor_id`** (8 of 57 sites pass `"system"`/`"guest"` — including the QRPh alert itself, so an FK would reject the exact row this exists to record) and **`outcome` as text not pgEnum** (a not-yet-`ADD VALUE`d literal would throw → be swallowed → lose the row). Plan review added a guard so the console line's own `JSON.stringify` can't throw either. Suite 1138 passed / 4 skipped / 0 failures (1123 + exactly 15 new), tsc 0. ⚠️ **Phase 5 item 3 is now PARTIAL, NOT passed** — discovery is mechanically possible where it wasn't, but nothing runs the query on a schedule, no UI surfaces it, nothing pages, and `resolved_at` has no code writer. That is a process/ops decision, not code. ⚠️ **T-P3Y-04 accepted on the record**: a HANGING insert is still possible — `src/lib/db/index.ts` sets no `connect_timeout`/statement timeout and postgres.js queues under pool exhaustion. Pre-existing and not novel (the same handler already has ~5 unbounded awaited calls); the correct fix is project-wide and out of scope. Prior: **Quick task 260806-gwt COMPLETE (verified 8/8): Tier 1 is CLOSED — all 6 remaining date time bombs are gone.** The nearest fuse was **2026-09-01, which would have broken `pending-hold.test.ts` AND `state-machine.test.ts` at once**, ~4 weeks out; then 2026-10-01, 2026-11-01, 2026-11-02, 2027-03-01. The proven derivation from DEF-IR9-01 was LIFTED into a shared `tests/helpers/dates.ts` (165 lines) and `hold-expiry.test.ts` refactored to import it — **that refactor was the proof the extraction was faithful**, and it shrank 233→197 lines. Comment-stripped grep for all six fuse dates now returns **0 in every one of the five files**. `src/` byte-unchanged (`git status --porcelain -- src/` empty — the SQL lead guard was NOT made injectable to suit a test). Tier 2 and Tier 3 deliberately untouched; the Tier-2 read-model anchor inside `request-lifecycle.test.ts` is byte-identical, which mattered because that file spans BOTH tiers. **ANTI-VACUITY: 7 mutations, 7 REDs, and the orchestrator's verifier independently REPRODUCED 3 of them across 3 different files** (own-hold idempotency, the D-58 extend UPDATE, the SLA terminal-status mapping) — none of the conversions went hollow. **D-GWT-02's original premise was FALSE and was corrected before execution**: it claimed only `hold-expiry` seeds `operating_hours`, but `request-lifecycle` does too (`addMondayHours`) — the conclusion survives only because those calls are scoped to the Tier-2 describe block, so the real rule is site-scoped, not file-scoped, and the executor re-ran the grep per file rather than trusting it. ⚠️ **PRE-EXISTING WEAKNESS FOUND, not fixed**: `src/inngest/functions/request-expiry.ts`'s `expireOne` returns a HARDCODED `{ status: "declined" }` literal instead of reading back what its UPDATE wrote, so `tests/booking/request-expiry.test.ts:193` SURVIVES a mutation flipping the terminal status to `cancelled` — only the separate DB readback at L196 catches it. That assertion is weaker than it reads and it nominally guards the terminal-status contract shared with the in-tx sweep. Suite 1123 passed / 4 skipped / 0 failures (exactly baseline), tsc 0. Prior: **Quick task 260805-nb7 COMPLETE: the suite is GREEN again — 1123 passed / 0 failed / 4 skipped.** DEF-IR9-01 closed. `tests/booking/hold-expiry.test.ts` held THREE independent date literals (`NOW` 2026-07-15, `DAY` 2026-08-03, `SLOT` 2026-08-02T22:00Z) and one of them rotting silently is what detonated on 2026-08-02: the injected `NOW` reaches `getAvailability` but NOT `createPendingHold`, whose lead-time guard is evaluated in SQL against Postgres's clock by deliberate design ("computed by POSTGRES, never the JS clock"). All three are now DERIVED from one computed next-strictly-future Monday via `TZDate` (the `slots.ts` idiom — no hand-rolled `+8`; a hardcoded offset is the same class of rot as a hardcoded date), so they can never disagree again. A `beforeAll` fixture self-check asserts Monday + clears both lead guards + inside `BOOKING_HORIZON_DAYS`, so a future break is a loud setup failure rather than a confusing "just taken". **`src/` byte-unchanged** — the SQL guard was NOT made injectable to suit a test. **Anti-vacuity proven, and the orchestrator independently reproduced one**: deleting the D-48a `expires_at > now()` term from the read model turns case 1(a) RED with `expected 'unavailable' to be 'available'` — matching the executor's recorded output verbatim — then restored clean. The executor added a third mutation beyond the brief because the first two both hit case 1, leaving the live-hold inverse unproven; under it a stranger's `createPendingHold` cancelled a LIVE hold and took the slot, i.e. the double-book itself. ⚠️ **6 SIBLING time bombs logged, not fixed** (`deferred-items.md`): `pending-hold.test.ts:34-35` and `state-machine.test.ts:57-58` both fuse on **2026-09-01, ~4 weeks out — two files at once**; then 2026-10-01, 2026-11-01, 2026-11-02, 2027-03-01. Plus 4 Tier-2 files whose already-past literals survive only because their injected `NOW` is pinned beside them — one added write-path call reproduces this defect with no warning. Prior: **Quick task 260805-ir9 COMPLETE (verified 9/9): the post-reset LOCKOUT is closed.** Found by a human walkthrough, not by the suite: `revokeSessionsOnPasswordReset:true` deleted the session row but nothing cleared the httpOnly cookie, so a gated page redirected to `/login`, `middleware.ts`'s presence-only `getSessionCookie` check saw the stale cookie and bounced to `/`, and the user could never reach `/login` for the 30-day cookie lifetime — with **no in-app recovery, because the app has no sign-out anywhere in `src/`**. Fixed by delegating the bounce decision from middleware to a new `/auth/session-check` Route Handler that runs the authoritative `getSession()` and forwards Better Auth's own cookie-expiring headers; middleware stays presence-only and DB-free. **The chosen design was the more expensive of two options and the user picked it deliberately** after being shown that deleting the bounce outright (~3 lines) fixes the same bug — because delegation also self-heals expired sessions, admin revocation and rotated secrets. **THE BROWSER CAUGHT A WRONG FIX THAT 11 GREEN IN-PROCESS ASSERTIONS MISSED**: the first implementation left Chrome holding a ZOMBIE empty-valued cookie, because `parseSetCookieHeader` maps `Max-Age=0` to the number `0` and `cookies().set()` drops it as falsy; the lockout only appeared lifted because `getSessionCookie` treats `""` as falsy. Routing via `auth.handler` (better-call router path, which `nextCookies()` skips) fixed it. A second catch: `auth.handler` applies rate limiting, and a 429 would have parsed as "no user" and cleared a LIVE user's cookie — every non-200 now takes a degraded path. `e2e/stale-session-selfheal.spec.ts` holds ONE persistent context across signup→reset→login, which no existing e2e spec does — and that convention is exactly why this bug shipped. Human walkthrough confirmed server-side, not merely reported (`GET /login?reset=1 200` where it used to be `GET / 200`; a valid session still bounced and survived). Three mutations executed and independently REPRODUCED by the verifier. tsc 0, build 0, `src/lib/auth.ts` byte-unchanged, scope held (no sign-out added — tracked separately). ⚠️ **Suite is 1121 passed / 2 FAILED**: `tests/booking/hold-expiry.test.ts` is a PRE-EXISTING time bomb (untouched by this work, fails in isolation) — it pins a slot to 2026-08-02T22:00Z and injects a fake `NOW` that never reaches `createPendingHold`'s lead-time guard, because that guard evaluates in SQL against Postgres's clock. Logged DEF-IR9-01, unfixed. Prior: **Quick task 260805-g60 COMPLETE: the Phase 1/2/5 human-verification records are now honest.** Docs only (4 files, all `.planning/`). `01-HUMAN-UAT.md` had been stale since 2026-06-03: item 2 password-reset is now PARTIAL (the flow is proven by `e2e/password-reset.spec.ts`, but the spec reads the token from the `verification` table because Resend 422s `example.com` recipients — one walk with a deliverable address is what's missing), and item 5 mode-switch is CLOSED by `e2e/mode-switch.spec.ts` (both cases). All four Phase-1 specs re-run first-hand 2026-08-05: **4 passed (29.5s)**. `05-HUMAN-UAT.md` item 1 is PARTIAL, not pending — card (`pay_C4PW6fRGtUTNm6GsKCpt4P36`) and QR Ph (`pay_ru6sXqhRJto1NW3T83cqak4q`, hand-paid) are genuinely proven plus three 2026-07-31 confirms, but **GCash and Maya were never individually walked**. `02-VERIFICATION.md`'s Cloudinary listing-photo item is discharged on the 2026-07-10 real-Cloudinary evidence — explicitly NOT transferable to Phase 1's avatar surface. **The audit corrected itself twice**: it had claimed the signup-intent default "has never had recorded product sign-off" (false — `01-HUMAN-UAT` item 6 records the 2026-06-03 approval) and listed Phase-1 items "3, 4 and 6" as open (the open set is 3 and 4). **NO phase status flipped** — 01/02/05 all remain `human_needed`, because 5 items are genuinely still open: Google OAuth creds, Cloudinary avatar creds, PayMongo hosted KYC, real payout transfer, QRPh operator-alert process. Open human items across the milestone: 11 → 5 open + 2 partial. Prior: **Quick task 260801-kv2 COMPLETE (verified 8/8): T-08-79 CLOSED — the concurrent double-charge window on the money path is gone.** Two simultaneous `confirmBooking` calls could each mint a payable PayMongo session; the sequential form of this already cost a real ₱1,470-against-₱735 capture in Phase-8 UAT, and on `qrph` an already-captured overcharge is not API-refundable. `claimCheckoutLease` (`src/lib/payments/checkout-lease.ts`) is ONE autocommit `UPDATE … WHERE (checkout_lock_at IS NULL OR checkout_lock_at < now() - TTL) RETURNING` — a compare-and-swap that relies on READ COMMITTED re-evaluating the loser's WHERE against the winner's committed row (EvalPlanQual). **No lock spans the PayMongo round-trips**: the claim commits at booking.ts:825, the fetches are at :885 and :935 — structural, not disciplinary. TTL 90s on the Postgres clock, so a dead process self-heals with no sweep. Migration `0023_booking_checkout_lease` applied. Proven by 8 race cases over genuinely independent connections (`makeRacingClients`) plus 8 call-site cases; **7 mutations run, two predictions OBSERVED WRONG and recorded as observed rather than reconciled** — MUTATION A reproduced the live double charge (the refused caller expired the holder's session and minted a second payable one). Full suite 1112 passed / 4 skipped vs an 1096/4 baseline, `tsc` clean, four must-not-change files byte-unchanged. The plan took two checker rounds: the anti-pattern grep gates originally forbade prose the same tasks required, and a `git add -N` "fix" was empirically shown to make its own gate unpassable and turn `git checkout --` into a 0-byte truncation. **Known tooling gap, not fixed:** `npm run db:migrate` fails with `url: undefined` because nothing loads `.env.local` for `drizzle.config.ts` — every migration on this box needs `DATABASE_URL` passed manually. Prior: **Quick task 260801-iu7 COMPLETE: v1.0 audit finding #4 closed on the host side.** A published listing with zero `operating_hours` rows now says so on `/host/listings` (per card) and on the host dashboard, with a link into the existing availability editor — read once per page in one owner-scoped `NOT EXISTS` query (`src/lib/listing/hours-signal.ts`), never per card. Deliberately a SIGNAL, not a gate: `deriveBookable`, `publishListing`, `publishSchema`, search Stage-1 and `components/availability/` are all byte-unchanged (`git diff --exit-code` gated), so no listing changed bookability state and nothing dropped out of search. Mutation-measured — three mutations applied, observed RED recorded verbatim in the test header, reverted; all three killed MORE cases than predicted. Full suite 1096 passed / 4 skipped against an 1087/4 baseline (+9 = exactly the new cases), `tsc --noEmit` clean. **The gate half of finding #4 remains open by decision** (a host can still publish with an empty calendar), as does the booker-facing dead end. Prior: **09-18 EXECUTED (gap plan 2 of 9): CR-03 layer 1 closed — the admissions counter is anchored on the venue-local calendar day and the advisory lock keyed on `listing:YYYY-MM-DD`; two mutations recorded verbatim (`expected 4 to be 3`), full suite 1044 passed with zero failures.** Prior: **09-17 EXECUTED (gap plan 1 of 9): CR-01 closed, mutation-measured, 6/6 e2e.** Prior: Phase 09 GAP-CLOSURE EXECUTION STARTED (plans 09-17 … 09-25, 4 waves, sequential on `dev`); quick task 260731-lsx corrected three erroneous findings in 09-SECURITY.md (audit verdict unchanged: 93 threats · 58 closed · 35 open · ship gate BLOCKED).

> ⚠️ **STATE frontmatter note (2026-07-31):** `gsd-sdk query state.begin-phase` was run and its body edits were REVERTED by hand — it wiped the Current Position prose above and wrote `Plan: 1 of 25` (16 plans are done; execution resumes at 09-17). Its frontmatter recalculation WAS correct and was kept: `completed_plans` 97 → **99** (99 SUMMARY files on disk; 97 was stale), `total_plans` 107. This is the known v1.42.3 tracking over-reach — keep writing STATE body prose by hand.

Progress: [██████████] 100%

🟥 **08-17 LANDED — the pax-surcharge human walkthrough ran, SIX of seven steps were APPROVED, and step 4 FAILED with a live double-charge. WAVE 9 IS COMPLETE; the PHASE is not.** Task 1's gate: full suite **96 files / 838 tests exit 0** (unchanged from 08-15's close — nothing regressed), `npm run build` exit 0 bare (Next 16.2.7 Turbopack, TS 38.5s, full route table). `uat_listing_bookable` was patched through the containerised DB and **read back**: `published | instant | max_occupancy 12 | included 1 | extra_head_fee 10000 | cancellation_policy standard` — the `extra_head_fee` NULL that made 08-09 skip its own step 6 is finally gone, so this is the FIRST time the per-head money path has been driven by a human. `RESEND_API_KEY` was set, so step 8's email was real delivery. No repository file was modified by either task (`git status --short -- src/` empty).

📌 **What a human confirmed on screen (per-step, no blanket sign-off — T-08-62):**

1. **Step 2 ✅ (D-108 + CR-03).** `PaxStepper` present; stepping it re-renders a **NEW server total** (page refresh, not browser arithmetic). At pax 4: `₱500.00/hr × 1 hour → ₱500.00`, `Extra guests (3 × ₱100.00) → ₱300.00`, `Service fee → ₱40.00`, `Total ₱840.00`. At the clamp: `Extra guests (11 × ₱100.00) → ₱1,100.00`, `Service fee ₱80.00`, `Total ₱1,680.00`. **The stepper stopped at 12 and refused to go higher**; helper text `This includes you. Up to 12.` With `included = 1`, pax 4 charges 3 extra and pax 12 charges 11 — coherent at both ends.
2. **Step 3 ✅ (CR-01).** Reserve page rendered **`Tuesday, Jul 28`** / **`7:00 PM – 8:00 PM · 1 hour`** — **not "Full day"**, on exactly the case that broke it (a surcharged HOURLY booking, where the deleted price-inequality would have concluded "Full day" with certainty).
3. **Step 5 ✅ (WR-03 / D-113).** Before any RSVP: **`1 of 12`**, one roster row `You · Organizer` / "You booked this space". Counted exactly once.
4. **Step 6 ✅ (WR-04 / D-114), at SHIFTED thresholds.** The booking froze at `declared_pax = 3` (not the plan's assumed 4), so the thresholds moved by one and were verified there: **silent at 3-vs-3**, and at the FIRST over-subscription (4-vs-3) the nudge fired verbatim — title `More people are coming than you booked for`, body `4 people are coming, but you booked for 3. You may owe a bit more for the extra 1 person at check-in.` **Neutral alert, no payment button, no amount** (G5 / 08-UI-SPEC). Roster: `You · Organizer` + `Titeng Galit` / `Titang Galit` / `Paepal`, each `Coming` with `Remove attendee`.
5. **Step 7 ✅ (SC4 / GROUP-05) — and it settles the 11-vs-12 question.** The invite page rendered `This group is full` / **`All 11 spots are taken. You can still let the organizer know you can't make it`** with "Yes, I'm coming" disabled. Confirmed against the live row: **`capacity_snapshot = 11`, `yes_rows = 11`, `listing.max_occupancy = 12`.** This is the **WR-03 convention working end to end**: the public invite page is organizer-**EXCLUSIVE** by design (`src/components/group/rsvp-form.tsx:98` reads `capacity_snapshot` for that sentence) while the organizer page is organizer-**INCLUSIVE** (`12 of 12`). 11 invitees + the organizer = the listing's rating of 12. **The two numbers a human sees differing by one is the intended design, not an inconsistency** — say so before anyone "fixes" it.
6. **Step 8 ✅ (CR-01, second half).** Public invite page: **`Tuesday, Jul 28, 7:00 PM – 8:00 PM (Makati time)`** — not "Full day". Address line `Approximate area — Makati. The organizer has the exact address.` (Q-16 / `showExactAddress`). Email confirmed. **CR-01 is now human-confirmed on FOUR surfaces.**
7. **Step 4 ❌ FAILED — the double-charge. See the block under Current Position above; full detail in `08-17-SUMMARY.md` and `deferred-items.md` item 5.**

⚠️ **A second, quieter finding from step 4: CR-02's own scripted assertion was NEVER EXERCISED.** The script was *pax 3 → Confirm & pay → cancel back → step to pax 4 → Confirm & pay → reload the FIRST tab and confirm it is unpayable.* What actually ran was two submissions at the **same** headcount, so `updateDeclaredPax` was never entered and the expire-before-refreeze gate never fired. Recorded **NOT VERIFIED**, per the plan's own rule that an unrun step is never passed by assumption — **there is no verbatim first-tab-reload string to record because no reload happened.** CR-02's only evidence remains 08-13's mutation proofs A/B/C, which are strong but **mock-backed**, and step 4 is a demonstration of what a mock-backed proof of a *provider* guarantee is worth. Logged as `deferred-items.md` item 6; fold it into the same real-API harness that closes item 5.

📌 **The generalisable lesson, and it is the reason this walkthrough was worth running:** *a third-party guarantee written into a comment must be PROBED against the real sandbox before it is relied on.* `mockPayMongo` was built to model the idempotency behaviour we **believed** PayMongo had; every test then passed against that belief, and four plans of coverage confirmed the mock rather than the provider. **A mock that faithfully implements your assumption can only ever validate your assumption.** Two `POST`s with the same key against `sk_test_` took under a minute and falsified it. (The same probe independently confirmed 08-12's `expireCheckoutSession` **does** work — `POST /v1/checkout_sessions/{id}/expire` returned HTTP 200 on both probe sessions. The defect is never calling it, not the call.)

⚠️ **UAT ENVIRONMENT LESSON (not a product defect): a tunnel started inside a subagent does not outlive it.** The ngrok tunnel started during Task 1 **died when that agent exited**, so no webhook reached the app and the booking sat `pending` — **steps 5-8 were BLOCKED, not failing.** The human restarted `ngrok http 3000`, which reclaimed the same static domain `eloquent-pounce-entangled.ngrok-free.dev`, and **PayMongo retried both deliveries automatically** (`evt_RVZMkSu3S6y15ThmG2BKA1yJ` @ 10:17:28Z, `evt_rY8iwHDct3aiHfXhx9Xs3Eeq` @ 10:18:13Z, both HTTP 200), confirming the booking with no manual replay. **Start long-lived UAT processes (ngrok, `npm run dev`, `npm run dev:inngest`) in your OWN terminal** — same class of trap as the standing two-process Inngest requirement. Silver lining worth keeping: PayMongo's own retry recovered a dead tunnel, which is independent evidence that D-57's webhook-as-confirm-authority tolerates one.

✅ **08-15 LANDED — CR-01 (BLOCKER) is CLOSED, both halves mutation-proven, and WAVE 8 IS COMPLETE. With it, all four Phase-8 BLOCKERS (CR-01…CR-04) are closed.** A surcharged hourly booking now states its real time range everywhere. `composeWhenLabel` / `composeWhenLabelShort` read the PERSISTED `booking.full_day` snapshot (drizzle 0016 / WR-06) and nothing else; the price inequality that concluded "Full day" whenever the frozen space price differed from the plain per-hour run total is **gone**. It was true by construction of any hourly booking with a D-108 per-head surcharge — `quoteWindow` folds the surcharge into `totalCents` and `createPendingHold` freezes that into `spacePriceCents` — so every such booking read "Full day" on the public invite page, both RSVP emails, both group notifications, the organizer page, the cancel review screen, `/bookings`, `/host/bookings`, `/host/requests`, every reminder, the request-expiry mail and the payment-webhook receipt. `WhenLabelInput.fullDay` is **REQUIRED** and `hourlyRateCents` is **REMOVED** (replaced by `dayRateCents`), so the change produced **10 compiler errors across 10 files** — the compiler's own census of the call sites — rather than relying on a reviewer. Full suite **96 files / 838 tests exit 0** (+3 tests, 0 new files); tsc 0; lint **0 errors** (7 pre-existing warnings); `npm run build` exit 0 (27 routes, bare). **`src/app/listings/[id]/book/page.tsx` and `src/app/(app)/bookings/[id]/page.tsx` are byte-untouched** — 08-05's two inline fixes still stand and still pass their own grep tripwires.

📌 **New contracts from 08-15 (load-bearing for 08-17's UAT, Phase 9, and anyone adding a time surface):**

1. *A displayed SCHEDULING fact is read from its own persisted snapshot, never inferred from a MONEY column.* That is the whole lesson of CR-01: `space_price_cents` is a pricing output, and a pricing change (D-108) silently rewrote what people were told about when to turn up. The comment block that used to defend the inference ("`fullDay` is NOT persisted, so it is re-derived from the frozen SPACE PRICE … do not 'improve' it") was **rewritten, not annotated** — it had become the bug's own defence.
2. *`fullDay` is REQUIRED on `WhenLabelInput`, and `hourlyRateCents` was REMOVED rather than deprecated.* An optional field lets a call site silently omit the snapshot and keep the bug alive on that one surface; a field left in place lets a call site keep feeding the old inference. **Any new time surface must project `booking.full_day` + `listing.day_rate_cents` — it cannot forget, because it will not compile.**
3. *The pre-0016 fallback is a POSITIVE day-rate match (`spaceCents === dayRateCents`), never an inequality.* Dropping it outright (`?? false`) would relabel legacy NULL-`full_day` full-day bookings as an hour range; an inequality reintroduces CR-01 on legacy rows. Both directions are mutation-pinned. Note the fallback INVERTS one pre-08-15 behaviour deliberately: with **no** day rate to match, the window now renders its real hours rather than "Full day" — that bias belonged to the deleted inequality.
4. *The grep tripwire discipline held.* `grep -c "hourlyRate" src/lib/booking/when-label.ts` prints **0** — the removed identifier is spelled nowhere in the file, comments included. The formatter-scoped repo check `grep -rn "hourlyRateCents" src/ | grep -c "composeWhenLabel"` prints **0**; the three files that still carry both strings (`actions/booking.ts`, `actions/re-request.ts`, `db/schema.ts`) are the `quoteWindow` re-price, the WR-06 local price-determining fallback and the column definition — none are display consumers.
5. *`re-request.ts` KEEPS its `hourlyRateCents`, by the plan's own grep-before-drop rule.* It has a second, non-formatter consumer at `:233`. Its compose call reuses the `fullDay` it already minted the hold with rather than re-projecting the column. `placeHold` likewise passes `fullDay ?? false` — byte-identical to what `createPendingHold` applies before freezing `booking.full_day`.

⚠️ **`tsc` was INTENTIONALLY RED between Task 1 and Task 3, and that was the mechanism, not a regression.** This is also the first plan in four where the tsc-vs-vitest trap did NOT bite — precisely because the plan's mechanism *was* a type change, so `tsc` was the primary signal at every step instead of an afterthought. **Both mutation messages named the rendered string** (`expected 'Thursday, Jul 2, Full day (Makati time)' to be 'Thursday, Jul 2, 8:00 AM – 10:00 AM (Makati time)'`) — the thing a person would actually have read on the invite page — rather than a downstream proxy. Four plans running, that ordering discipline has paid.

✅ **08-14 LANDED — WR-03 + WR-04 are CLOSED and WAVE 7 IS COMPLETE. SC4's "hard-capped at the listing's capacity" is now literally true.** `createGroup` freezes `GREATEST(l.max_occupancy - 1, 0)` inside the same atomic INSERT that creates the group, so a `maxOccupancy = 12` listing admits **11** RSVP-yes attendees and 11 + the organizer = 12 — the number the host rated the room for. Before this, 12 could say yes and the organizer made **13 bodies in a space rated for 12**. The organizer surface now reads ONE convention end to end: the meter renders `confirmed + 1` of `capacity + 1`, `TopUpNudge`'s prop is RENAMED `confirmedYes` → `attendingTotal` and fed `counts.confirmed + 1`, and the roster's now-inverted "MUST NOT BE COUNTED" comment is rewritten. Full suite **96 files / 835 tests exit 0** (+1 file / +11 tests); tsc 0; lint **0 errors** (7 pre-existing warnings — the new jsdom file added none); `npm run build` exit 0 (27 routes, bare). **Zero changes to `claimSeat`, `getHeadcount`, `getRoster`, `removeAttendee`, `src/lib/group/rsvp.ts` or the public invite page.**

📌 **New contracts from 08-14 (load-bearing for Phase 9's open-capacity work, 08-17's UAT, and anyone rendering a group headcount):**

1. *`capacity_snapshot` is the ONE organizer-EXCLUSIVE number in the whole feature, and it is exclusive because of WHAT IT CAPS.* It caps `rsvp` ROWS; the organizer is a display fixture with no `rsvpId` and no row, so they never occupy one. `listing.maxOccupancy`, `declaredPax` and every organizer-facing figure are organizer-INCLUSIVE (`pax-stepper.tsx` says so in as many words: "This includes you"). **Anything reading the snapshot for an organizer-facing surface owes a `+ 1`; anything reading it for the public invite page or the seat-claim does not.** The `+ 1` currently lives at exactly two adjacent lines in `src/app/(app)/bookings/[id]/group/page.tsx` and must not be duplicated — `submitRsvp` deliberately lets an organizer answer their own link, so a real `rsvp` row can already exist for that person, and a second `+ 1` would claim a body that does not exist.
2. *`full` is a fact about the seat-claim, not about the display.* It is decided server-side in `getHeadcount` against the RAW snapshot and passed THROUGH `HeadcountMeter` unmodified. Re-deriving it from the two organizer-inclusive numbers on screen would replace the fact with a restatement of the display; the meter still does no arithmetic of any kind.
3. *Existing `booking_group` rows are FORWARD-ONLY — no backfill, by decision.* D-111 makes the snapshot immutable precisely so a later change cannot retroactively move a cap people have already answered against. Pre-change groups keep their old cap; every group created after this reserves the organizer's seat. Stated in the code comment, not silent (T-08-51, `accept`).
4. *The capacity floor is 2, in TWO places, and the second is not redundant.* The pre-read gate rejects `maxOccupancy < 2` (a one-person space cannot host a group — the only seat is the organizer's), and the INSERT's own `WHERE` repeats `l.max_occupancy >= 2` to close the read-then-write window where a host capacity edit could freeze a `capacity_snapshot` of **0** — a group nobody can ever join, immutable by D-111. Zero rows is the right answer to that race. This also closes the write-side half of **WR-09 for the group path**; WR-09 itself stays open.
5. *A `+ 1` at a call site cannot be mutation-proven by a component test alone.* The management page is an async RSC with a database and `next/headers` behind it. `tests/group/top-up-nudge.test.tsx` therefore ships **two separable layers** (the 08-16 idiom): LAYER 1 renders the component with the page's expression mirrored locally, LAYER 2 asserts the page's own SOURCE wires it that way. Delete either and half of WR-04 re-opens — layer 1 alone passes with the page mis-wired; layer 2 alone passes with the arithmetic broken.
6. *An audited number must be the number that was ENFORCED.* `meta.capacity` is read back off the INSERT's `RETURNING capacity_snapshot`, not recomputed from the listing's rating, so the trail and the enforcement cannot disagree even under a racing host edit (T-08-48).

⚠️ **Both mutations had to be REORDERED after their first run, for the same reason, and it is now three plans in a row.** Mutation A first failed the fill case on `expected 3 to be 2` (the frozen cap — true, but a fact three other cases already assert); reordered to read the committed roster first, it fails with **`expected [ … ] to have a length of 2 but got 3`** — three attendees admitted plus the organizer, four bodies in a room rated for three, which is the actual defect. Mutation B first failed on `expected '' not to be ''`; reordered to assert the overage first, it fails with **`expected '' to match /extra 1 person at check-in/`** — the organizer told *nothing* at the first over-subscription, which is WR-04 exactly. **Order assertions so the mutation's message names the truth, not a proxy.**

⚠️ **A shipped grep tripwire was itself tripping.** `top-up-nudge.tsx:16` contained the very word its tripwire forbids ("…moving money against a **card**"), so the plan's `grep -ci "pay now\|charge\|card"` acceptance criterion printed **1** on the untouched file. Rewritten to describe the three forbidden strings without naming any. The file's own header states the rule this broke: *"a grep that the comment forbidding the thing can trip is not a guard."* **Worth auditing the other 07-04-idiom tripwires for the same defect.** Note also that `charge` is a substring of `surcharge` and `card` of `discard` — neither word may appear in that file.

⚠️ **08-17's UAT fixture now needs `max_occupancy >= 2`, and a group on `max_occupancy = N` renders `x of N` where the last seat is the organizer's.** Combined with the standing 08-10 warning (a host who sets `extra_head_fee` but leaves `max_occupancy` empty collects no surcharge), the UAT listing must set **both** fields.

🚫 **NOT closed, and out of scope by the plan:** the mirror-image half of WR-03 — an organizer who RSVPs to their own link still appears **twice** on the roster (the "You" fixture plus their real row). The review's suggested `getRoster` predicate was NOT applied; `getRoster` is explicitly named untouched. The new roster comment flags the double-count risk in prose; closing it is a separate change.

✅ **08-13 LANDED — CR-02 (BLOCKER) is CLOSED, and with it `deferred-items.md` item 1. At most ONE PayMongo checkout session is payable for a booking at any instant.** 08-12's column and call now have production callers: `confirmBooking` writes `checkout.id` to `booking.checkout_session_id` **after `createCheckoutSession` resolves and before the `redirect`** (which throws by design), owner-scoped to `status IN ('pending','approved')` — for per-head **and** flat bookings alike. `updateDeclaredPax` reads that id, **expires the session BEFORE it freezes the new amount**, clears the column in the same write, and on a throw records `action: "checkout_expire_failed"` / `outcome: "needs_attention"` (session id in the meta, PayMongo's text nowhere) and **REFUSES the re-price**. Full suite **95 files / 824 tests exit 0** (+1 file / +7 tests); tsc 0; lint **0 errors** (7 pre-existing warnings — the new file's two were removed rather than left); `npm run build` exit 0.

📌 **New contracts from 08-13 (load-bearing for phase 9 and anyone touching a checkout session, a re-price, or an audit assertion):**

1. *The order is `expire → refreeze`, and a failed expire REFUSES the re-price — mutation-pinned, not merely commented.* Moving the gate below the re-freeze turns two cases red with messages that name the database truth: **`expected 109725 to be 105000`** (the total the expire stub OBSERVED in the row, when it should have seen the pre-re-price value) and **`expected 7 to be 2`** (the headcount committed despite the refusal); `pax-reprice` case 10 also goes red (`expected 6 to be 1`). Deleting the expire call entirely turns 2 red; deleting `confirmBooking`'s write turns **5** red across two files. Refusing costs a booker one retry (T-08-45, accepted); proceeding costs an unrefunded double capture.
2. *Every checkout session must be NAMED on its booking row before the booker leaves the app.* Any new code path calling `createCheckoutSession` owes the same write — a session nobody recorded can never be retired, and the re-price gate will silently do nothing for it. The write is **not conditional on `declared_pax`**: flat bookings fill the column too.
3. *Zero rows from that persisting UPDATE is deliberately NOT a failure.* If the hold lapsed between the D-58 extension and the write, the booker is already en route to a real payable session; refusing would strand them mid-payment. The comment says so explicitly so a later reader cannot "harden" it. Likewise `checkout_session_id` is **NULL after a successful re-price** — correct, because the session it named was just expired.
4. *A `needs_attention` audit is the ONLY signal that a live session was left un-retired.* There is no reconcile poller. If an operator surface is ever built, `action = "checkout_expire_failed"` + `meta.checkoutSessionId` is the row to render.
5. *`recordAudit` still writes a `console.info("[audit]", …)` LINE, not a table* (audit.ts v1, deliberate). The plan's "read audit rows back" was not executable; the assertion spies on `console.info` and filters the `"[audit]"` prefix (`auditLines()` in `tests/booking/checkout-session-expire.test.ts`). Any future audit assertion must do the same — and a durable sink lands without call-site changes but *does* need those spies revisited.
6. *Assert an ORDER as an order.* The case-3 expire stub reads `quoted_total_cents` **out of the database at call time**, so the reorder mutation fails on the value it observed rather than on a downstream proxy — the same lesson 08-16 recorded ("a mutation's failure message should name the database truth"). And the failure branch asserts the **whole** frozen tuple, because a partial write is the shape of the bug.

⚠️ **The `tsc`-vs-vitest trap from 08-12 recurred, in a new disguise — expect it again.** The new test file was **6/6 green in vitest while `tsc` was RED**: `ReturnType<typeof vi.spyOn>` erases the call-signature generics (`TS7006` on every `mock.calls` callback), and `mockImplementationOnce(async (id: string) => …)` is not assignable to a stub declared `(id?: string)` (`TS2345`). Vitest transpiles without typechecking. **Run both before committing** — this is now the second consecutive plan where a green suite hid a compile error.

✅ **08-16 LANDED — deferred item 4 is CLOSED and WAVE 6 IS COMPLETE. The GROUP-05 no-overflow guarantee now survives a one-line edit to production.** `tests/group/seat-claim-race.test.ts` has **two layers**: the two pre-existing cases still INLINE the seat-claim SQL (the *pattern* proof, byte-identical), and two NEW cases drive the **real `claimSeat`** imported from `@/lib/group/seat-claim` — one `drizzle(client)` over one INDEPENDENT `makeRacingClients` connection **per racer**, with a **name-only guest identity** (`userId: null`, `guestEmailNorm: null`) so the de-dup predicate is `sql\`false\`` and every racer genuinely attempts a fresh insert. Measured, not asserted: deleting `FOR UPDATE` from `src/lib/group/seat-claim.ts:54` now fails the file with **`expected 3 to be 1`** committed `yes` at `capacity_snapshot = 1` and **`expected 4 to be 2`** at `= 2`; restoring returns it to GREEN (4/4) with `git diff --exit-code src/lib/group/seat-claim.ts` clean. **Zero production files touched** — `git diff --stat fc43ac0..HEAD -- src` is empty. Full suite **94 files / 817 tests exit 0** (+2); tsc 0; lint **0 errors** (7 pre-existing warnings). `npm run build` not re-run (nothing reaching the bundle changed).

📌 **New contracts from 08-16 (load-bearing for Phase 9, which reuses this lock, and for anyone touching a race test):**

1. *A race test that INLINES the SQL proves the PATTERN; only a case that IMPORTS the shipped function proves the SHIPMENT.* Keep both layers and keep them separable — deleting the inlined cases loses the idiom proof, deleting the real ones re-opens deferred item 4 exactly. The file header now carries **two** MUTATION-VERIFY instructions, each naming its **own file and its own line** (`raceClaim`'s inlined SELECT in the test vs. `src/lib/group/seat-claim.ts:54`). The single instruction it carried before was the thing 08-09 found was not executable as written.
2. *The racers' IDENTITY is the subtle half, not the connections.* An account or guest-with-email identity would make `claimSeat` de-dup, so a loser returns `{ ok: true }` by matching the winner's row and the cap is never contested — a green test that proves nothing. **Name-only is the only identity that races.** Pair it with one `drizzle(client)` per racer: a shared `testDb.db` (`max: 1`) serializes the lock window (RESEARCH Pitfall 1).
3. *Assert the COMMITTED row count BEFORE the returned results.* The first mutation run failed on `expect(winners).toHaveLength(1)` — correct but a proxy; reordered, the same mutation reads `expected 3 to be 1` on committed rows, which is the actual over-cap. A mutation's failure message should name the database truth.
4. *Layer 2 gets its OWN fixture groups* (`REAL1_GID`/`REAL2_GID`, own bookings, non-overlapping windows). Sharing a group with the inlined cases would let their committed rows pre-fill the cap. And `voided_at` is left NULL deliberately — `claimSeat` fails **closed** on a voided group, which would make the new cases pass for entirely the wrong reason.

🟡 **08-12 LANDED — CR-02 PART 1 of 2. This is the ENABLING HALF and is deliberately NOT a closure.** Two things now exist that did not: `booking.checkout_session_id` is **applied to the live database** (drizzle `0019_booking_checkout_session.sql`, a single backfill-free `ALTER TABLE "booking" ADD COLUMN "checkout_session_id" text;` — `information_schema` confirms `text` / `is_nullable=YES`, and it replays cleanly from zero into a fresh isolated schema), and `expireCheckoutSession(id)` ships in `src/lib/paymongo.ts` (`POST /v1/checkout_sessions/{id}/expire`, no body, throws on non-2xx). **The column is empty on every row and the function has NO production caller** — a re-priced hold still leaves two payable sessions until **08-13** wires both in. Full suite **94 files / 815 tests exit 0** (+5 tests, 0 new files); tsc 0; lint **0 errors** (7 pre-existing warnings); `npm run build` exit 0.

📌 **New contracts from 08-12 (load-bearing for 08-13 and anyone touching a checkout session or an idempotency key):**

1. *The expire key is `checkout-expire:<sessionId>` — SESSION-scoped, never booking-scoped, and that is the whole decision.* Expiring one session twice must be a no-op (the stable key gives that), but a booking legitimately owns **more than one session over its life** — that is exactly what a D-108 re-price creates. A booking-scoped key would make the second session's expire **replay the first call's 200**: PayMongo reports success, we believe the session was retired, and it stays payable. Proven twice, not asserted: a random key turns **3 cases RED**, and a constant booking-scoped key turns the same **3 RED** with `expected 'checkout-expire:booking' to be 'checkout-expire:cs_one'`. This is the identical trap `createRefund`'s one-refund-per-payment key records, and it is now stated in **both** places because the second occurrence proved it generalises.
2. *A NULL `checkout_session_id` is NORMAL, not an error.* Pre-0019 bookings and any booking that never reached checkout hold NULL. The re-price path must attempt **no** expire for a NULL and must not treat it as a failure. The column is deliberately **NOT UNIQUE** (a UNIQUE would turn a retried write into a constraint error on the money path; PayMongo ids are already distinct) and **NOT indexed** (read by primary key only).
3. *`expireCheckoutSession` THROWS; there is no failure sentinel.* 08-13 must `try/catch` → `needs_attention` audit → **refuse** the re-price (ordering is **expire → refreeze**, a planning decision, not the executor's to revisit). An uncaught throw in a server action is a raw 500 on the money path, and no PayMongo error text may reach the browser (T-05-15 / T-08-39).
4. *`mockPayMongo.expireCheckoutSession` ECHOES the id it was handed, and is in `reset()`.* Assert on the **argument** to prove *which* session was expired — expiring the wrong one kills the session the booker is looking at while leaving the stale payable one alive. Override with `mockRejectedValueOnce` to drive the refuse branch.
5. *A `[BLOCKING]` migration is verified against `information_schema` + a fresh-schema replay, NEVER against `tsc`.* Types come from `schema.ts`, so a declared-but-unmigrated column leaves `tsc` and `next build` green while every integration test runs against a table without the column (T-08-40). **The next migration is 0020** — `drizzle/meta/_journal.json` now ends at `idx: 19`.

⚠️ **drizzle-kit named the migration `0019_mushy_hobgoblin.sql`; it was RENAMED to `0019_booking_checkout_session.sql` with the journal `tag` updated to match.** Keep doing this — a random adjective in the migration history is unreadable at review time. Also worth recording: drizzle-kit proposed **exactly one statement and nothing else**, which independently confirms `schema.ts` and the live DB had **not** drifted before this plan.

⚠️ **Two `tsc`-vs-vitest traps hit inside this plan, both worth remembering.** (a) `fetchMock.mockResolvedValue(new Response(...))` hands back the **same** `Response` on every call, and a body reads **once** — any multi-call fetch test must use `mockImplementation(async () => ...)` or the second call throws `Body is unusable`. (b) A hand-written return-type annotation on a test helper silently hid a new field, so **vitest passed while `tsc` failed** (`TS2339`). Vitest transpiles without typechecking: **a green test run is not a green `tsc`** — run both before committing.

✅ **08-11 LANDED — CR-04 (BLOCKER) is CLOSED, both halves mutation-proven.** The app's one unauthenticated write no longer lets a caller mint durable process memory. `submitRsvp` now RESOLVES the invite token (`getGroupByToken`, :345) **before** it charges any budget (:355), and the link budget is keyed on the resolved `group.groupId` — so the key space is real `booking_group` rows, not `32^20` caller-selectable strings. Measured, not inferred: **200 distinct well-formed unknown tokens moved the bucket count by exactly 0** (it was 200 before). Independently, `src/lib/rate-limit.ts` is now **hard-bounded at `RATE_LIMIT_MAX_BUCKETS = 50,000`** — after a 60,000-distinct-key flood inside one window the store measures exactly 50,000. Full suite **94 files / 810 tests exit 0** (+2 files / +13 tests); tsc 0; lint **0 errors** (7 pre-existing warnings); `npm run build` exit 0 (27 routes), no env workaround.

📌 **New contracts from 08-11 (load-bearing for anyone adding an unauthenticated endpoint or touching the limiter):**

1. *A `rateLimit` key on an unauthenticated path must name something the DATABASE confirmed.* `rateLimit` stores counters in a module-level `Map`, so a key is durable process memory. Resolve first, then budget on the resolved id. Resolving first leaks nothing here — unknown, voided and regenerated tokens already collapse onto one identical `INVITE_INACTIVE` sentence (T-08-17) — and it *reduces* timing variance, because the lookup now happens for every caller rather than only those under budget.
2. *The 50,000 ceiling is the SECOND line, not the first.* It caps blast radius; it does NOT make an attacker-chosen key safe (they still churn the store and evict live budgets — that is T-08-36, accepted and pinned by a passing test). Bound the key space at the caller **as well**.
3. *The ceiling's arithmetic is `size >= MAX` → evict until `size < MAX`, deliberately NOT the review's `> / <=`.* The latter settles at MAX+1 under flood. The invariant as shipped is a clean post-condition: **after every `rateLimit` call, `buckets.size <= MAX_BUCKETS`.** The sweep alone can never be the ceiling (a flood inside one window expires nothing) — the unconditional insertion-order eviction is the load-bearing half, and mutation 1 proves it (`expected 60000 to be less than or equal to 50000`).
4. *`sweepExpired` is throttled to 1/s; the eviction is not.* Sweeping on every over-ceiling call is an O(size) scan — ~5×10⁸ iterations in the 60k flood. The throttle gates only the sweep, so the ceiling is untouched by it. `__resetRateLimit()` clears the sweep clock too.
5. *A REGENERATED invite link now shares its predecessor's budget.* Correct (the group is what is hammered, not the string) and asserted — but remember it before anyone builds "rotate the link to escape abuse".
6. *`__rateLimitBucketCount` / `__resetRateLimit` are TEST SEAMS, not the contract.* Any test touching the real limiter must call `__resetRateLimit()` in `beforeEach`, and — the subtle one — must import `@/lib/rate-limit` **after** `vi.resetModules()`, from the same registry as the action under test, or the Map it counts is not the Map being written. `tests/group/rsvp-rate-limit.test.ts` carries a "a resolving token mints exactly one bucket" positive control precisely to catch that mistake.

⚠️ **`guest-email:${guestEmailNorm}` was left byte-identical** (it sits behind a resolved, active token and a committed claim, and is now covered by the ceiling). Widening it is **WR-05, still open and out of scope**. Likewise **no coarse pre-resolution budget was added** — one shared key on the app's only public write is a global availability lever; T-08-34 `accept`s the residual instead (one indexed lookup per unauthenticated request, no write, no transaction — measured exactly 1, and exactly 0 for a malformed token).

✅ **08-10 LANDED — CR-03 (BLOCKER) is CLOSED, both halves mutation-proven.** `declaredPax` is bounded twice: a `.max(10_000)` SHAPE ceiling at `bookingCreateSchema` (keeps any accepted value orders of magnitude below int4) and a `min(declaredPax, listing.maxOccupancy)` clamp read INSIDE `createPendingHold`'s own transaction, alongside the rates it prices against (D-111). `grep -c "input.declaredPax" src/lib/availability/units.ts` prints **1** — the clamp expression is the only place the raw input is read; `quoteWindow` and `declaredPaxToPersist` both take the clamped local. Full suite **92 files / 797 tests exit 0** (+5); tsc 0; lint **0 errors** (7 pre-existing warnings); `npm run build` exit 0 (27 routes), no env workaround.

📌 **New contracts from 08-10 (load-bearing for anyone touching a client-supplied multiplier or the two pax clamps):**

1. *The clamps on the two entry points are ASYMMETRIC on a null cap, and that asymmetry is the decision — do not "harmonise" them.* Creation (`createPendingHold`) falls back to **1**, fail CLOSED and surcharge-free; re-price (`updateDeclaredPax`, `src/app/actions/booking.ts:376`) falls back to the caller's own value because it is owner-gated on an existing hold. `createPendingHold` is the entry point a crafted POST reaches, so a listing with no recorded capacity must charge for nobody extra rather than for whoever asked. The rationale is stated in a comment at the clamp site.
2. *A shape ceiling and a cap are different jobs and different numbers.* `.max(10_000)` exists ONLY so no accepted value can multiply through `(pax − included) × extra_head_fee` into the `integer` money columns; the REAL cap is the listing's own `maxOccupancy`, read in-transaction. Under mutation the overflow was **reproduced live** — `PostgresError: value "3150000103425" is out of range for type integer`, `code 22003`, `routine pg_strtoint32_safe` — which is exactly the `mapBookingError` re-throw → raw 500 chain (T-03-500) the review predicted. Apply the pair to any future client-supplied multiplier that reaches an int4 money column.
3. *A clamped headcount is a SUCCESS, not a refusal.* No new error branch, no new failure shape for callers — the booker gets the price for the headcount the listing can actually hold. `quoteWindow`, `paxSurcharge`, `computeServiceFee` and the D-74 triple composition are untouched, and every new case re-asserts `quoted == space + fee` on the clamped row so CR-03 can never be closed later by breaking the triple.
4. *The old comment at `validation/booking.ts` was a FALSE statement about the system and was rewritten, not annotated.* "The real seat-cap enforcement is D-112's seat-claim, not this field, so no upper bound is asserted here" is gone. D-112's seat-claim caps RSVP **rows**; it was never the thing standing between a crafted `declaredPax` and the frozen price.

⚠️ **`tests/booking/pax-surcharge-hold.test.ts` fixtures now carry `maxOccupancy = 12` by DEFAULT.** The fixture never set one, so the (deliberate) fail-closed fallback clamped two shipped D-108 cases to 1 — the assertions and the code were both right and the FIXTURE was what no longer expressed "at or below the cap". No assertion was weakened; the fail-closed case passes `maxOccupancy: null` explicitly. **Any new case meaning to exercise the cap must name its own `maxOccupancy`.**

⚠️ **A host who sets `extra_head_fee` but leaves `max_occupancy` empty now collects NO surcharge at hold time** (every booker clamps to 1). Safe — it can only undercharge — but product-visible. A publish-time nudge pairing the two fields is the real close; 08-14's `max_occupancy >= 2` floor moves toward it for the group path. **08-17's pax-surcharge UAT must set BOTH fields** or the stepper will correctly render nothing and repeat the 08-09 non-verification.

**GAP CLOSURE (`1cbac46`) — 8 plans, `08-10` … `08-17`, waves 6–9. 4 of 8 executed and WAVE 6 IS COMPLETE (08-10, 08-11, 08-12, 08-16 — 08-12 being CR-02 part 1 of 2, so CR-02 itself is still OPEN until 08-13).** Phase 8 verification returned `gaps_found` (2/5 truths clean; SC1/SC3/SC4 partial). `/gsd-plan-phase 8 --gaps` planned closure for **7** findings, scope chosen by the operator:

| Plan | Wave | Closes |
|---|---|---|
| 08-10 | 6 | **CR-03** — clamp `declaredPax` to the listing's `maxOccupancy` inside `createPendingHold`'s own tx + `.max(10_000)` shape bound, so no headcount reaches the int4 money columns |
| 08-11 | 6 | **CR-04** — bound the rate-limit `Map` (sweep + insertion-order eviction at 50k) and resolve the invite token **before** charging any caller-keyed budget (RSVP key becomes `group.groupId`) |
| 08-12 | 6 | **CR-02 (1/2)** — `booking.checkout_session_id` + **`[BLOCKING]` migration 0019** + `expireCheckoutSession` in the PayMongo client |
| 08-16 | 6 | ✅ **DONE** — **deferred item 4** — race the *real* `claimSeat` (one `drizzle(client)` per racer) so deleting `FOR UPDATE` from `seat-claim.ts:54` finally goes RED |
| 08-13 | 7 | **CR-02 (2/2)** — persist the session id in `confirmBooking`; **expire-before-refreeze** in `updateDeclaredPax`, and a failed expire **refuses** the re-price |
| 08-14 | 7 | **WR-03 + WR-04** — `capacity_snapshot = GREATEST(max_occupancy - 1, 0)`; one organizer-inclusive convention across meter, roster docs and the renamed `attendingTotal` nudge prop |
| 08-15 | 8 | **CR-01** — `composeWhenLabel` reads the persisted `booking.full_day`; `fullDay` becomes **required** and `hourlyRateCents` is removed so `tsc` enumerates all 18 call sites |
| 08-17 | 9 | open `human_verification` — `autonomous: false` checkpoint for the pax-surcharge walkthrough 08-09 skipped, with `extra_head_fee` actually configured on `uat_listing_bookable` |

📌 **Planning decisions that are NOT the executor's to revisit:**

1. *CR-02 is persist+expire, **not** revert.* Reverting to the stable per-booking idempotency key re-opens exactly what D-108 closed (a re-priced hold replaying the *old* amount). Ordering is **expire → refreeze**; a failed expire **refuses** the re-price — refusing costs a booker one retry, proceeding costs an unrefunded double capture.
2. *WR-03 is forward-only — no data migration.* D-111 makes `capacity_snapshot` a frozen creation-time snapshot; rewriting shipped snapshots would violate the decision the column encodes.
3. *The capacity convention:* `capacity_snapshot` is organizer-**exclusive** (it caps RSVP rows, which the organizer never occupies); `declaredPax`, `maxOccupancy` and the whole organizer surface are organizer-**inclusive**; the public invite page stays organizer-exclusive and unchanged. The now-inverted `attendee-roster.tsx:33-35` comment must be rewritten, not left.
4. *CR-01 composes with 08-05 rather than reverting it* — the positive day-rate-match fallback 08-05 shipped inline is what moves into `when-label.ts`; `book/page.tsx` and `bookings/[id]/page.tsx` stay untouched.
5. *CR-04 deliberately omits a coarse pre-resolution budget* — one shared key on the app's only public write path would itself be a global availability lever. Residual (one indexed lookup per unauthenticated request) is dispositioned `accept` in the threat model.

⚠️ **Carry into execution (plan-checker WARNING, no blockers found):** **08-15 touches 19 files across 3 tasks**, past the 15-file threshold. Accepted because making `WhenLabelInput.fullDay` required forces `tsc` to fail loudly on any missed call site rather than silently shipping a partial fix — and `tsc` is **intentionally RED between its Task 1 and Task 3**, so do not treat that as a regression. Watch context during Task 3's 9-file pass and checkpoint if quality degrades.

🚫 **OUT of scope by operator decision:** WR-01, WR-02, WR-05 … WR-10 from `08-REVIEW.md` remain open and logged. One authorized incidental: 08-14's `max_occupancy >= 2` floor also closes the write-side half of WR-09 for the group path.

**08-09 landed — the PHASE GATE is satisfied and a HUMAN has walked the whole group-booking loop.** Full suite **92 files / 792 tests, exit 0** (+14 files / +137 tests over the Phase-7 baseline of 78 / 655). Zero product-code changes in this plan (`git diff --stat 4f85741..HEAD -- src tests` is empty). The human ran the cross-session UAT and **approved steps 1–5**: a session-less private window opened a real invite link and was NOT bounced to `/login`; a name-only guest RSVP'd and saw the "only confirmation" copy; a guest-with-email RSVP'd and the confirmation email **actually arrived via real Resend**; the organizer's headcount + roster reflected both (organizer row #1, yes rows first) refreshing on their own via the poller; and cancelling the parent booking voided the link ("no longer active"), notified the guest-with-email, and left the blank-email guest correctly unreachable. **Both 08-VALIDATION.md Manual-Only rows are closed.**

✅ **RESOLVED BY 08-16 (`4d263aa`) — the green suite IS now a regression guard on `claimSeat`.** The block below is the 08-09 finding, kept for the record: the gate covered only the pattern, and deleting the production lock left everything green. That is no longer true — `tests/group/seat-claim-race.test.ts` now imports `claimSeat` and races it on per-racer connections, so `src/lib/group/seat-claim.ts:54` is mutation-covered (`expected 3 to be 1` committed `yes` at `capacity_snapshot = 1`). Deferred item 4 is closed. **Do not delete either layer of that file — see the 08-16 contracts above.**

<details><summary>Original 08-09 finding (superseded 2026-07-28)</summary>

🚨 **THE GROUP-05 ACCEPTANCE GATE DOES NOT COVER THE SHIPPED SEAT-CLAIM LOCK. Do not read the green suite as a regression guard on `claimSeat`.** `tests/group/seat-claim-race.test.ts` **never imports or calls `claimSeat`** — it INLINES its own copy of the seat-claim SQL inside each racer's transaction (documented at that file's **line 12**; the reason is sound — it makes the `FOR UPDATE` lock itself the thing under test, mirroring `exclusion-race.test.ts`). Measured this plan, **not inferred**: deleting `FOR UPDATE` from `src/lib/group/seat-claim.ts:54` leaves the race test **GREEN (2/2)**, all of `tests/group/` **GREEN (6 files / 60 tests)**, and the **full 792-test suite GREEN** — while shipping an over-cap bug. The lock **is** present and correct today (verified byte-identical after the mutation was reverted) and `tests/group/seat-claim.test.ts` covers its functional contract, but it has **NO mutation coverage**: anyone deleting that line ships an overflow defect past a fully green suite and a "passing" gate. The gate as executed proves the *pattern* is race-free (via the test's own inlined `FOR UPDATE` at `:91` → RED with **3 committed `yes` at `capacity_snapshot=1`** and **4 at `=2`**, read back through an independent connection → restore → GREEN). ⚠️ **The 08-09 PLAN's Task-1 text is therefore not executable as written.** Logged as `08-.../deferred-items.md` **item 4**; the close is one racing case that routes through the real `claimSeat` (bind each racer's own connection as its `DbConn`), keeping the inlined cases as the pattern proof. **This is the #1 `/gsd-plan-phase 8 --gaps` item — Phase 9 reuses this same lock.**

</details>

⚠️ **The pax-pricing surcharge UI has NEVER been human-verified.** 08-09's optional step 6 (PaxStepper server-side re-quote, "Extra guests" breakdown line, organizer top-up nudge) was **NOT exercised** — `uat_listing_bookable.extra_head_fee` is **NULL**, so none of that UI renders (correct per 08-05 contract 6). It is covered only by the 08-03/08-05 automated tests. Exercising it needs a listing with an extra-guest fee set in the wizard.

✅ **`<Toaster />` is now mounted (`dea2cd4`) — the item-3 deferred entry is CLOSED.** It sits once in `src/app/(app)/layout.tsx` (the WR-04 shared-ancestor idiom), so every booker-side `toast.*` is finally audible — 08-07's `ShareLinkBox` "Link copied", `CreateGroupButton`/`RemoveAttendeeButton` errors, and the Phase-07 cancel/refund toasts. **Do NOT also mount one per-page underneath it** — two Toasters render each toast twice. This commit belonged to no plan; it is attributed in 08-09-SUMMARY.md.

🧪 **UAT fixture `uat-08-09-group` is STILL IN THE LOCAL DEV DB.** It was created because the seed has zero future-dated confirmed bookings (all confirmed rows start `2026-07-25` or earlier vs `now() = 2026-07-28`), so any group made on them lands straight in `closed`. A NEW row was used rather than moving an existing booking forward because `61726f09…` carries a stray `host_cancel_fee` ledger row and `42132ab1…` is the live PayMongo refund fixture that a cancellation must not touch; `uat-08-09-group` has `payment_id = NULL` so step 5 cancelled without a real refund. **Reversal SQL is in 08-09-SUMMARY.md** — run it at phase sign-off. Note also: with **no verified Resend domain**, the `onboarding@resend.dev` fallback sender delivers **only to the account owner** (`pengr.clmc.3@gmail.com`), which is the address any future email UAT must use.

**08-08 landed — the PUBLIC INVITE PAGE ships (`/invite/[token]`), and GROUP-03 is now true end-to-end.** Full suite: **92 files / 792 tests, exit 0**; `npx tsc --noEmit` clean; `npx eslint src tests` **0 errors** (7 pre-existing warnings, none in touched files); `npm run build` **exit 0** with no env workaround. A person with no account can open the link, read the session in venue-local time at an address the host agreed to share, and answer yes or no with a name and an optional email. Every rendered state was driven against a running dev server and a real row (unknown / malformed / voided / closed / open-guest / full), then the scratch data was deleted and the seed restored.

📌 **New contracts from 08-08 (load-bearing for anyone touching a public route or a URL-borne credential):**

1. *The invite route is at the ROOT and must stay there.* `next build` lists `ƒ /invite/[token]` beside `/listings/[id]`. Moving it under `(app)`/`(host)` would make `(app)/layout.tsx`'s `redirect("/login")` reach it and silently reverse GROUP-03 **while every test still passed** — a layout redirect is invisible to page-level tests. The page calls `auth.api.getSession` and then never branches on its absence: `grep` finds **no** `redirect(` and **no** `notFound(` in the file, and that is the assertion.
2. *One inactive branch, one pair of constants.* Malformed = unknown = regenerated = voided = cancelled all render `This invite is no longer active` / `Ask the organizer for the latest link.` with HTTP 200. The **malformed** case is FOLDED onto `GROUP_INACTIVE` rather than handled, so the Zod shape check can never become a probe. Measured live: four token classes returned byte-identical rendered content (the only payload differences were Next's per-request `__next_r` nonce and internal RSC chunk numbering, which also differ between two requests for the *same* token). The one token echo in the HTML is Next's own router state (`"c":["","invite","<token>"]`), present identically for every class and already in `location.pathname`.
3. *A page whose URL carries a bearer credential owes two lines of metadata:* `robots: { index: false, follow: false }` (an indexed invite link is a public one) and `referrer: "no-referrer"` (or the token rides out in a `Referer` header on any navigation away). Both are unfixable after the fact. **Apply this to any future token-in-URL surface.** The token is re-emitted in exactly one place — the D-116 `callbackURL` on `Log in instead` — composed from the *resolved* `group.accessToken`, never the raw route param.
4. *The client renders the server's error string VERBATIM when the server owns the invariant.* `RsvpForm` never composes a "full" message and never re-checks the cap; a raced yes displays whatever `submitRsvp` returned, composed with the real `capacity_snapshot`. A client-side second opinion about a value only a `FOR UPDATE` row lock can know could only ever be the wrong one (D-112/GROUP-05). Likewise `grep -c "new Date(" ` on the page is **0** — `rsvpClosed` arrives pre-computed from `now() >= b.starts_at` (D-120).
5. *`getMyRsvpStatus` (new, `src/lib/group/rsvp.ts`) is ACCOUNT-ONLY, and the absence of a guest equivalent is a decision.* It is self-scoped (`r.user_id = $userId`) and keyed on a group id the caller already resolved from a token it presented. Recognising a returning **guest** on a plain GET would mean looking an RSVP up by an address they have not typed — an oracle over who was invited. A guest-with-email changes their answer by answering again with the same address, which `rsvp_group_email_uq` resolves onto their existing row.
6. *The "we've emailed you a copy" line is gated on `answer === "yes"`, not on `reachable`.* `submitRsvp` emits the attendee confirmation only for a yes (08-06 contract 5), so claiming a send on a decline would be a false statement printed to the person who just made it.

⚠️ **No `<Toaster />` is mounted on ANY booker-side page** (only three `(host)` pages mount one; neither the root layout nor `(app)/layout.tsx` does). Every `toast.*` on a booker surface is therefore **silent** today — including 08-07's `ShareLinkBox` `Link copied` / `Couldn't copy` and `CreateGroupButton`'s error, plus the Phase-07 cancel/refund toasts. Pre-existing and out of 08-08's scope; logged in `08-.../deferred-items.md` (item 3). **It is the first thing an 08-09 UAT walkthrough of the ORGANIZER flow will hit.** The invite page deliberately mounts no Toaster and raises no toast — every failure is an inline neutral alert.

⚠️ **The seed has no future-dated confirmed booking** (all confirmed rows start `2026-07-25` or earlier), so a freshly created group lands straight in the `closed` state. Move a booking's `starts_at`/`ends_at` forward before walking the RSVP flow in UAT.

**08-06 landed — the GROUP LOGIC LAYER ships (createGroup / submitRsvp / removeAttendee / regenerateLink + the D-121 auto-void).** `tests/booking/ group/ security/ notifications/`: **34 files / 347 tests, exit 0**; tsc clean; `npx eslint src tests` 0 errors; `npm run build` exit 0 (27 routes). GROUP-01…GROUP-05 are closed at the logic layer; the surfaces that call these actions are 08-07 (`/bookings/[id]/group`) and 08-08 (`/invite/[token]`).

📌 **New contracts from 08-06 (load-bearing for anyone touching the group surfaces, an invite link, or a cancel path):**

1. *Owner scope is IN the WHERE, and it is mutation-proven.* Every organizer read in `src/lib/group/rsvp.ts` (`getRoster`, `getHeadcount`, `getOwnedGroupByBooking`, `getOwnedGroupById`) takes the organizer id and carries `b.booker_id = $organizerId` inside the statement — a foreign roster is **UNREADABLE**, not merely unrendered. `tests/group/group-owner-scope.test.ts` (18 cases) was verified by deleting each predicate in turn: roster → 2/18 fail, headcount → 1/18, by-booking → 1/18, by-id → 1/18, the token's `voided_at` → 1/18, the token's booking-status half → 1/18, reachable-attendee group scope → 2/18, its reachability filter → 1/18. **The positive controls are what make it non-vacuous** — the file also asserts exact id sets, that an organizer genuinely CAN read their own roster, and that a live token genuinely DOES resolve.
2. *`getGroupByToken` returns ONE frozen `GROUP_INACTIVE` object for unknown = replaced = voided = cancelled-booking.* **Never add a `reason`/`voidedAt` field to help the UI tell them apart** — the indistinguishability IS T-08-17 (the invite link is a shared bearer credential of publicly-known length), and the test compares the four results' KEY SETS to each other precisely to catch that "improvement".
3. *An Inngest event NAME + its emitter belong in a plain lib module, never in the `inngest/functions/` file.* `src/lib/group/guest-notify.ts` now owns `GUEST_EMAIL_EVENT` + `emitGuestEmail` (the guest sibling of `emitNotify`), because `guest-email.ts` calls `inngest.createFunction` at **module scope** — importing the name from there ran the registration inside a server action's module graph. `guest-email.ts` re-exports the name so 08-04's import surface is unchanged. **Apply this rule to any future event.**
4. *The D-117 opt-in email guard is STRUCTURAL.* The only address ever emailed is the one on **this** request body — no address is read back out of the `rsvp` table to be mailed, and none is carried over from an earlier RSVP. Budget: **3 per hour per normalized address** (re-casing cannot mint a fresh one). A blank-email guest gets **zero** sends on any channel and `submitRsvp` returns `reachable: false` so the UI can make the G3 disclosure honestly. The RSVP *submit* budget is keyed on the LINK and deliberately generous (30/60s) — a group of ten answering at once is the success case, not abuse.
5. *The attendee confirmation fires ONLY for a `yes`.* The shipped copy on both channels is "you're on the list", which would be a false statement to send to someone who just declined — and there is deliberately **no declined notification type to invent**. A decline is confirmed on-screen and the organizer is still told.
6. *`removeAttendee` DELETEs the row and takes the seat-claim's own lock.* "Removed by the organizer" and "said they can't make it" are different facts; flipping to `no` would put words in the attendee's mouth on the roster. The delete runs inside a tx that first takes `SELECT capacity_snapshot … FOR UPDATE` on the group row — the identical statement `claimSeat` opens with — so a removal and a concurrent RSVP serialise.
7. *D-121 auto-void is wired into BOTH cancel paths* (`cancelBookingAsBooker` + `cancelBookingAsHost`), inside the guarded post-commit consequence block (the 07-11 discipline): `voided_at = now()` scoped `voided_at IS NULL` (idempotent), then `group_cancelled` to every reachable yes attendee — accounts via `emitNotify`, guests-with-email via `emitGuestEmail`, blank-email guests unreachable **by design**. **The refund math is untouched (D-114)** and the organizer is skipped (they already have their own cancellation + refund notices). Mutation-verified: removing either call site, weakening the UPDATE, or dropping the organizer self-skip each turns the suite red.
8. *`createGroup` freezes the cap in ONE statement and is idempotent.* `capacity_snapshot` is `SELECT`ed from the listing joined to the booking **inside** the `INSERT`, so no read-then-write window exists and no request shape carries a cap; `ON CONFLICT (booking_id) DO NOTHING` returns the EXISTING group on a double submit (`alreadyExisted: true`).

⚠️ **`z.email()` rejects surrounding whitespace — trim BEFORE validating on any optional address field.** Found here as a real bug: a trailing space from mobile autofill turned a valid RSVP into "We couldn't save your RSVP" on the one field the product promises is optional. `optionalEmail` now trims first, and every blank shape (absent / `""` / all-whitespace) collapses to `undefined` so exactly one "no address" branch exists. Case is left alone — lowercasing belongs to `normalizeEmail`, whose output is a de-dup KEY, not a display string.

**08-05 landed — the D-108 pax-pricing UI ships (wizard fee fields + PaxStepper + surcharge line + BOTH fullDay fixes).** `tests/booking/ listing/ validation/ payments/`: **40 files / 419 tests, exit 0**; tsc + eslint clean (0 errors); `npm run build` exit 0.

📌 **New contracts from 08-05 (load-bearing for anyone touching checkout, the breakdown, or a fullDay label):**

1. *Pitfall 3 is CLOSED, and the fix is grep-guarded.* `/listings/[id]/book` and `/bookings/[id]` now read the PERSISTED `booking.full_day` (WR-06 / drizzle 0016). The old "frozen space price ≠ the current hourly run total" derivation is GONE and **must not come back** — the D-108 surcharge folds into `spacePriceCents` (A1), so an ordinary hourly booking with one extra guest no longer matches that product and would render as "Full day". Neither page spells the removed identifiers **even in prose**, so the absence-grep cannot be tripped by the comment forbidding it (the 07-04 tripwire discipline). The pre-0016 fallback is a POSITIVE day-rate match (the `re-request.ts:234` idiom), never an inequality.
2. *`updateDeclaredPax` is the ONLY way a frozen quote may move, and it can never touch money already taken.* Scoped to `(id, owner, status IN ('pending','approved'), expires_at > now())`, so a `confirmed` booking is untouchable — the over-subscribed-group top-up remains **D-114's deferred fast-follow**. It re-freezes the whole D-74 triple together (`quoted == space + fee` still holds by construction), reads `full_day` from the row, and clamps the headcount to the LISTING's `maxOccupancy` server-side (the stepper's `max` is a courtesy, Security V4).
3. *The checkout `Idempotency-Key` is now amount-scoped — but ONLY for per-head bookings.* `checkout:<bookingId>:<quotedTotalCents>` when `declared_pax` is non-NULL; the byte-identical `checkout:<bookingId>` otherwise. Reason: the stepper makes a quote MUTABLE for the first time, so a booker who backs out of the hosted checkout, re-steps, and pays again would otherwise replay the first session **at the old amount**. The two shipped tests pinning the flat key stay valid by construction. ⚠️ Residual edge (a stale open PayMongo tab paying the older session) is logged in `08-.../deferred-items.md` — closing it needs a persisted `checkout_session_id` or a webhook amount-reconcile, both Phase-5 rail changes D-107 holds out of Phase 8.
4. *`paxSurcharge()` (pricing.ts) is the SINGLE place `max(0, pax − included) × fee` is computed.* `quoteWindow` delegates to it and the reserve RSC calls it for the breakdown props, so the figure a booker reads and the figure they are charged cannot drift by restatement. `PriceBreakdown` gained `runPriceCents` — because the surcharge lives INSIDE `spacePriceCents`, the run line must drop to the base or the same centavos are disclosed twice; the component still neither sums nor subtracts.
5. *Group-pricing fields are OPTIONAL in `publishSchema` on purpose.* They mirror `postalCode`, NOT `cancellationPolicy`. 07-15's "a new publish requirement lands in TWO places" rule cuts the other way here: putting them in the gate is exactly how a requirement becomes real, so they stay optional and never enter the publish checklist. A test pins this **negative** property.
6. *`extraHeadFee = 0` ⇒ ZERO UI change, still true.* No stepper, no surcharge line, no `declared_pax`, identical checkout key. `declared_pax` is **NULL on every flat listing** — 08-07's top-up nudge must check `extraHeadFee > 0` FIRST or it will compare against null.

**Note on ordering:** 07-14 was executed ahead of 07-11/12/13 (its dependencies, 07-06 and 07-07, were both already done). Completed in Phase 7: **07-01 … 07-17** (all; 07-17 is the gap-closure plan for CR-01/CR-02/WR-04/WR-06).

**Gap closure complete (07-17 landed).** Full suite: **78 files / 655 tests, exit 0**. All four user-approved 07-VERIFICATION/07-REVIEW findings are closed red-first with content-pinning regressions (envelope payloads / email bodies / persisted money fields — never call counts).

📌 **New contracts from 07-17 (load-bearing for anyone touching notifications or re-request pricing):**

1. *Refund/fee claims are AMOUNT-gated* — `notifyCancellation` takes cents and composes the ₱-label past a `> 0` guard; `feeLabel` on the host cancellation payload exists only when the charged fee > 0. No channel may durably record a money event that did not happen (CR-01/D-79 — and the rule now explicitly covers fees).
2. *`booking_cancelled_by_host` carries `side: "booker" | "host"` (required) + `feeLabel?`* — renderers treat anything `!== "host"` (including `undefined` on durable pre-07-17 jsonb rows) as booker, so old rows keep their meaning. The four-file compile-enforced contract is unchanged (TS union, Zod union, sendForType, describeNotification; no `default:` clauses).
3. *Deadline claims in emails render the ROW's pre-composed capped label* (`payByLabel`/`respondByLabel`) — config hour-constants must never appear in email copy; a template with no deadline field states NO number (comment-safe grep gate in the plan verifies email.ts at 0).
4. *`booking.full_day` is the pricing-mode snapshot* (drizzle/0016, nullable, backfill-free) — written by `createPendingHold` on every hold; PRICE-DETERMINING consumers (re-request) read the column and never re-derive the mode against current listing rates. `when-label.ts`'s identical derivation stays display-only by decision (IN-09).

**Wave 5 complete (07-16 landed) — Phase 7 is code-complete.** Full suite: **78 files / 639 tests, exit 0**. The QRPh gating question is SETTLED by observed API behaviour (2026-07-23 probe: HTTP 400 "Refunds are not allowed for payments with source type qrph." — raw evidence in `refund-rail.ts`), and the D-72 collect-and-never-store refund path is BUILT.

📌 **New contracts from 07-16 (load-bearing for anyone touching refunds/payouts):**

1. *Idempotency namespaces* — refund transfers use `refund:` + bookingId; payouts keep `payout:` + bookingId (Pitfall 10). Both appear exactly once in `paymongo.ts`, grep-asserted. Never reuse either.
2. *No-persistence (D-72)* — a refund destination (account number/name/BIC) must NEVER reach a table, audit meta, notification payload or log line; only `transferId` + a masked last-4 survive (audit meta). The transfer-failure catch deliberately logs NO error content because a PayMongo error detail can echo the destination — `tests/paymongo/instapay-refund.test.ts` case (4) feeds it one that does.
3. *one-refund-per-payment* — `createRefund`'s key is payment-scoped; a second call for the same payment silently replays the first response (safe today, a trap for any partial-then-top-up flow; explicit comment + test guard it).
4. *A3 is BLOCKED, not settled* — the Money Movement endpoints 404 on this account until PayMongo enables the feature; the destination form degrades calmly to the operator seam until then. Manual UAT owes: A3 re-verification + live institutions list.
5. *Refund transfers have no reconcile poller* — dispatched transfers are operator-reconciled via the audit trail (`refund_transfer_dispatched` → `getTransfer`); logged in deferred-items.md for a follow-up once Money Movement is live.

**Wave 4 complete (07-15 landed).** Full suite: **76 files / 628 tests, exit 0**. BOOK-07 is closed: a host must now explicitly choose a cancellation tier before publishing, and a booker sees that tier's rungs — as concrete venue-local dates for their own booking — before they pay.

✅ **Disclosure equals enforcement, and it is mutation-proven (07-15).** All booker-facing policy copy is DERIVED from `LADDER`, the same constant `quoteRefund` evaluates. Verified in both directions: moving a rung (24h→36h) left the derived assertions green because the copy followed automatically, and hand-typing one interpolated figure turned the derivation test red immediately. `tests/booking/cancellation-policy.test.ts` case (10) asserts, for every tier at every boundary the disclosure renders, that standing exactly ON it awards the promised rung and one millisecond later awards the next one down — checked against `quoteRefund` itself, not a restatement.

⚠️ **A NULL cancellation tier means "show nothing", NOT `tierOrDefault`'s Flexible (07-15).** `tierOrDefault` is an internal safety net so the refund ENGINE never faces an unpriceable legacy row; it is not a policy any host chose, and surfacing it would put a promise in the host's mouth they never made. Any future booker-facing policy surface must make the same call. Checkout in particular reads the **booking's** tier snapshot, never `listing.cancellationPolicy` — the two can legitimately differ and the booking's is the one that gets paid.

⚠️ **New publish requirements must land in TWO places (07-15).** A wizard checklist row is a courtesy; the gate is `publishSchema` + a re-read of the **persisted row** inside `publishListing`. Adding only the checklist row leaves the requirement bypassable by any stale or crafted client. Note this also means any test fixture describing itself as "publish-eligible" must be updated whenever the gate widens — two such fixtures went red on this plan and were correct to.

**Wave 1 complete.** 07-01 (config + schema + migrations) and 07-02 (when-label + booking-status derivation) both have SUMMARY.md on disk; `drizzle/0013` + `0014` are applied to the live DB and are idempotent.

**Wave 2 complete.** 07-03 (pure money modules — refund ladder, service fee, rail predicate) and 07-04 (payout ledger correctness) both done. Full suite: **61 files / 463 tests, exit 0**.

✅ **Both Phase-7 money hazards are CLOSED (07-04).**

1. *Ledger `kind` scoping* — every `host_payout_ledger` query in `src/` now carries `AND kind = 'payout'`: the sweep (join, release, failure, rollback), the reconcile (`queryProcessingLedger`, both `reconcileOne` UPDATEs, the stuck-`held` alert), `/host/earnings`, and the PayMongo refund webhook (a site the plan's list omitted). A reconcile regression test was verified to fail when the scope is removed. `summarizePayouts` was left pure by design — the feeding query is scoped instead.
2. *Payout basis* — `payout-sweep.ts` now grosses on `COALESCE(retained_space_cents, space_price_cents)`. The all-in charged-total column name no longer appears anywhere in that file, comments included; a header comment documents this as a deliberate T-07-16 grep tripwire.

⚠️ **New handoff constraint for 07-08 (service fee end-to-end).** `booking.space_price_cents` is nullable and **nothing writes it yet**. `payOne` now fails CLOSED on a null basis (`[payout-alert] booking has no frozen payout basis`, claims nothing, moves no money) rather than falling back to the charged total — a fallback would silently leak the service fee into host payouts. Consequence: **every booking created between now and 07-08 will not pay out until 07-08 freezes the price split at creation.** This is intended and visible, not a regression.

**Wave 2 fully complete (07-05 landed).** Full suite: **63 files / 487 tests, exit 0**.

**Wave 3 opened — 07-06 landed.** Full suite: **65 files / 509 tests, exit 0**. `/bookings` and `/host/bookings` ship, closing MANAGE-01, MANAGE-02, HOST-02 and ROADMAP SC#1.

✅ **Owner scope is proven, not asserted (07-06).** `tests/security/bookings-owner-scope.test.ts` was verified by MUTATION: deleting `WHERE b.booker_id = …` fails 6 of 11 tests, deleting `WHERE l.host_id = …` fails 7 of 11; both restored. The fixture is crossed (A hosts listing A but books listing B) so a swapped predicate fails too, not just a dropped one.

⚠️ **`db.execute` returns `timestamptz` as TEXT — new repo-wide contract (07-06).** Drizzle's raw `execute` path hands back Postgres text (`2027-03-01 02:00:00+00`), NOT a Date, and the `as unknown as T[]` cast makes this invisible to `tsc`, `eslint` and `next build`. Both new pages would have crashed on the first real row (`date-fns` / `.getTime()`). `bookings-query.ts` now selects timestamps as strict ISO-8601 via `to_char` and hydrates once at the boundary, and exports **`readDbNow(dbConn)`** so no surface re-derives the cast. **Any new page reading timestamps through `db.execute` must do the same.**

⚠️ **`npm run build` needs three env placeholders on a dev machine.** Two module-scope fail-closed guards throw because `next build` runs with `NODE_ENV=production`: the PayMongo platform-wallet guard (`src/lib/paymongo.ts:39`, from 05-02) and the Inngest signing-key guard. Pre-existing, unrelated to 07-06, logged in `.planning/phases/07-.../deferred-items.md`. Until fixed, run: `PLATFORM_WALLET_NUMBER=x PLATFORM_WALLET_NAME=x INNGEST_SIGNING_KEY=x npm run build`.

**07-07 landed — the notification LAYER exists.** Full suite: **66 files / 519 tests, exit 0**. `src/inngest/functions/notify.ts` is the repo's first event-triggered Inngest function: one `fitout/notify` event → `write-notification` (durable row) then `send-email`, each its own memoized step. WR-04, open since Phase 2, is closed — a permanently-failed send now writes a `recordAudit` `needs_attention` entry instead of vanishing.

✅ **The notification layer is now WIRED — MANAGE-03 is observably satisfied (07-10).** Full suite: **70 files / 560 tests, exit 0**. All five shipped fire-and-forget lifecycle sends emit `fitout/notify` post-commit: `approveRequest` → `request_approved`, `declineRequest` → `request_declined`, `placeHold` → `request_received` + `new_request_to_host`, the payment webhook → `booking_confirmed`, and the expiry cron → `request_declined` (D-91 parity — the cron emits rather than calling `email.ts` directly, so expiry is not the one lifecycle event without an in-app row). A real approve now produces a real `notification` row in the real database — `tests/booking/notify-emission.test.ts` case 6 reads it back out.

📌 **The three properties 07-10 proved, and how (do not weaken these).** (1) **Emission happens AFTER commit** — case 1 records what an **independent Postgres connection** can see at emit time; a second connection observes only committed rows, so `statusAtEmit === "approved"` is what distinguishes post-commit from in-transaction. "Send was called" is not that assertion. (2) **A rejecting transport cannot fail the action** — case 2. (3) **Duplicate suppression lives in each action's status-scoped `UPDATE ... WHERE`, NOT in the notify layer** — a 0-row repeat returns before the emission is reached (case 3), and the webhook gates its emission on the `≥1-row` confirm branch so a PayMongo redelivery emits nothing. Verified non-vacuous by mutation: a deliberate pre-commit emission fails 4 of the 6 cases.

⚠️ **`auth.ts` still has two `void send...` calls and that is CORRECT.** `sendResetPassword` / `sendVerificationEmail` are deliberately NOT migrated — a reset URL is a bearer secret and the notification row is durable, and `email.ts:7` documents the `void` as a **timing-side-channel** (user-enumeration) defence. D-83/D-66 scope the migration to the five booking lifecycle emails, all of which are done. Do not "finish the job" by migrating auth.

📌 **Contract for anyone emitting a notification.** Call `emitNotify(event)` **after commit, NEVER inside `db.transaction`** — `inngest.send` is an outbound HTTP call, and a rollback would leave an event already sent for a booking that does not exist. Never call `email.ts` sends directly; `sendForType` is the only dispatcher (that is the D-83 anti-pattern being removed). The payload must be complete — `booking_confirmed.referenceLabel`, `new_request_to_host.totalLabel` and `request_declined.expired` were **added by 07-07** and are REQUIRED; `href` must be a root-relative path or an `http(s)` URL (a `javascript:` scheme is refused at the write boundary) — and **07-10 established ABSOLUTE (`${BETTER_AUTH_URL}/...`) as the convention**, because under D-91 that one string is also the email's CTA and a root-relative href is a dead link in a mail client; and `type` must equal `payload.type`. Adding a notification type is a **three-file change by construction** (pgEnum + TS union, Zod union, `sendForType` switch) — each omission is a compile error, and a `default:` clause would destroy that. Do not add one.

ℹ️ **`/bookings/[id]` moved into the `(app)` route group** (URL unchanged). It now inherits the booker header, so 07-14's bell will cover it; `/` and `/listings/[id]` remain header-less — the residual half of UI-SPEC Open Question 1.

✅ **The in-app notification centre SHIPS — the D-92 bell is in BOTH headers (07-14).** Full suite: **72 files / 581 tests, exit 0**. One `NotificationBell` (popover, not dropdown-menu) mounted in `(app)/layout.tsx` and `(host)/host/layout.tsx`, fed server-computed owner-scoped props; the D-65 `Requests` badge sits alongside it untouched (two badges, two meanings). `markNotificationRead` / `markAllNotificationsRead` scope on `recipient_id` **inside the UPDATE's WHERE**. The poller is D-84's bounded `router.refresh()` idiom at ~30s, and it **PAUSES** on `document.hidden` (burning no attempt) rather than skipping. TanStack Query is still not installed.

✅ **Owner scope AND the XSS guard are proven by mutation (07-14).** `tests/security/notification-owner-scope.test.ts` (8 cases) was verified by deleting each owner predicate in turn: `listRecent` → 3/8 fail, `countUnread` → 3/8, `markNotificationRead` → 2/8, `markAllNotificationsRead` → 1/8. `tests/notifications/notification-render.test.tsx` (13 cases) → 6/13 fail when the href guard is bypassed. **The positive controls are what make these non-vacuous** — the file also asserts exact counts, exact id sets, and that a user genuinely CAN read and mark their own rows, so a deny-everything implementation cannot pass.

⚠️ **`escapeHtml` and React escaping do NOT sanitise a URL scheme — the render path guards it too now.** 07-07 validated `href` at the WRITE boundary; 07-14 added an independent render-side allow-list (`safeHref` in `notification-item.tsx`) because a durable row outlives the guard that wrote it. Accepted shapes: root-relative and `http(s)` **only**. Protocol-relative `//evil.example` is explicitly refused — it passes a naive `startsWith("/")` but is cross-origin. A refused href renders the row as **inert, readable, non-clickable** content (never dropped, never given a substitute destination).

📌 **Adding a notification type is now a FOUR-file change** (was three): pgEnum + TS union (`schema.ts`), Zod union (`validation/notification.ts`), `sendForType` (`inngest/functions/notify.ts`), and **`describeNotification` (`components/notifications/notification-item.tsx`)**. Every omission is a compile error. Do not add a `default:` clause to any of them.

ℹ️ **First jsdom component test in the repo (07-14)** — not new infrastructure: `vitest.config.ts` already documents the `// @vitest-environment jsdom` pragma and `@testing-library/react` / `jsdom` / `@vitejs/plugin-react` were already installed. `next/link` is stubbed to a plain `<a>` (it needs an App-Router context absent in jsdom). Reuse this pattern for future component tests.

ℹ️ **Reminder notification COPY exists but nothing emits it yet.** `reminder_pre_expiry` / `reminder_pre_session` / `reminder_pre_sla` have icons, titles and bodies in the renderer; **Plan 13 owns the emitters**. Pass a pre-composed venue-local deadline label, not an hour count (D-96).

✅ **D-94 is now TRUE at all three hold-write sites (07-05).** `expires_at = LEAST(now() + window, starts_at)` on the instant hold, the request SLA and the approval payment window, all computed by Postgres. `units.ts` contains **zero** JS clock reads (grep-asserted). The request path applies the D-96 proportional split — a request 4h out gives the host a 2h SLA and the booker ~2h, floored at `MIN_APPROVE_WINDOW_HOURS`. Mode-scoped lead-time guards refuse server-side (2h request / 30min instant); `SlotState` gained `too_soon` as a display-only fourth member. The payment webhook is **unchanged** (D-57), protected by a mutation-verified guard test.

⚠️ **`tests/auth/secret-config.test.ts` flakiness DID recur and is now fixed.** It failed 3 consecutive full runs on a 5000ms *timeout* (never an assertion) while passing in isolation — the file re-imports the whole Better Auth graph from cold, and 07-05's two new DB-backed test files pushed it over. Given the explicit 30s cold-import budget 07-01 recommended. Not a behavioural change.

⚠️ **Search behaviour shifted subtly (07-05).** `searchListings` keeps a candidate on `state === "available"`; a slot inside its listing's notice window is now `too_soon`, so it no longer counts. Correct (it cannot be booked) and consistent with `past`, but it is a real change on the search path.

## Performance Metrics

**Velocity:**

- Total plans completed: 30
- Average duration: — min
- Total execution time: 0.0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 4 | - | - |
| 04 | 8 | - | - |
| 06 | 10 | - | - |
| 08 | 9 | - | - |

*08-09: ~43 min wall-clock, 2 tasks (1 auto + 1 blocking human-verify), 0 product files.*

**Recent Trend:**

- Last 5 plans: —
- Trend: —

*Updated after each plan completion*
| Phase 01 P01-01 | 14 | 3 tasks | 41 files |
| Phase 01 P01-02 | 17 min | 3 tasks | 20 files |
| Phase 01 P01-03 | 18 min | 3 tasks | 15 files |
| Phase 01 P01-04 | 8 min | 2 tasks | 14 files |
| Phase 03 P03-01 | 10 min | 3 tasks | 15 files |
| Phase 03 P03-02 | 12 min | 3 tasks | 7 files |
| Phase 03 P03-03 | 6 min | 2 tasks | 5 files |
| Phase 04 P04-01 | 15 min | 3 tasks | 8 files |
| Phase 04 P04-02 | 23 min | 2 tasks | 6 files |
| Phase 04 P04-03 | 14 min | 2 tasks | 5 files |
| Phase 04 P04-04 | 20 min | 3 tasks | 7 files |
| Phase 04 P04-05 | 24 min | 3 tasks | 4 files |
| Phase 04 P04-06 | 18 min | 2 tasks | 6 files |
| Phase 05 P01 | 5 | 3 tasks | 7 files |
| Phase 05 P02 | 4 min | 2 tasks | 3 files |
| Phase 05 P03 | 11 min | 3 tasks | 10 files |
| Phase 05 P04 | 9 min | 3 tasks | 4 files |
| Phase 05 P05a | 12 min | 3 tasks | 7 files |
| Phase 05 P06 | 7 min | 3 tasks | 9 files |
| Phase 05 P05b | 7 min | 3 tasks | 5 files |
| Phase 06 P01 | ~30 min | 2 tasks | 10 files |
| Phase 06 P02 | ~20 min | 2 tasks | 4 files |
| Phase 06 P03 | ~10 min | 2 tasks | 4 files |
| Phase 06 P04 | ~15 min | 2 tasks | 3 files |
| Phase 06 P05 | ~6 min | 2 tasks | 2 files |
| Phase 06 P06 | ~14 min | 2 tasks | 3 files |
| Phase 06 P07 | ~18 min | 2 tasks | 2 files |
| Phase 06 P08 | ~28 min | 2 tasks | 6 files |
| Phase 06 P10 | ~12 min | 2 tasks | 3 files |
| Phase 07 P01 | ~50 min | 3 tasks | 13 files |
| Phase 07 P02 | ~10 min | 2 tasks | 8 files |
| Phase 07 P03 | ~12 min | 2 tasks | 6 files |
| Phase 07 P04 | ~35 min | 3 tasks | 9 files |
| Phase 07 P05 | ~75 min | 3 tasks | 9 files |
| Phase 07 P06 | ~35 min | 3 tasks | 10 files |
| Phase 07 P07 | ~55m | 3 tasks | 8 files |
| Phase 07 P08 | ~55m | 3 tasks | 18 files |
| Phase 07 P09 | 75min | 3 tasks | 11 files |
| Phase 07 P10 | 75 min | 2 tasks tasks | 9 files files |
| Phase 07 P14 | ~45 min | 3 tasks | 8 files |
| Phase 07 P11 | ~70m | 3 tasks | 6 files |
| Phase 07 P12 | ~75m | 3 tasks | 9 files |
| Phase 07 P13 | 15m | 2 tasks | 3 files |
| Phase 07 P16 | ~25m | 3 tasks | 8 files |
| Phase 07 P17 | 25m | 4 tasks | 17 files |
| Phase 07 P18 | 12min | 3 tasks | 10 files |
| Phase 07 P19 | 14 | 2 tasks | 7 files |
| Phase 07 P20 | 11 | 3 tasks | 9 files |
| Phase 08 P01 | 17 | 2 tasks | 4 files |
| Phase 08 P02 | 12 | 2 tasks | 5 files |
| Phase 08 P03 | 9 | 2 tasks | 6 files |
| Phase 08 P04 | 30 | 2 tasks | 13 files |
| Phase 08 P05 | 35 | 2 tasks | 13 files |
| Phase 08 P06 | 38min | 3 tasks | 9 files |
| Phase 08 P07 | 16min | 3 tasks | 11 files |
| Phase 08 P08 | 26min | 2 tasks | 4 files |
| Phase 08 P10 | 25min | 2 tasks | 3 files |
| Phase 08 P11 | 22min | 2 tasks | 4 files |
| Phase 08 P12 | 20min | 3 tasks | 7 files |
| Phase 08 P16 | 25min | 2 tasks | 2 files |
| Phase 08 P13 | 25min | 3 tasks | 5 files |
| Phase 08 P14 | 46min | 3 tasks | 7 files |
| Phase 08 P15 | 35m | 3 tasks | 19 files |
| Phase 09 P15 | 34min | 2 tasks | 2 files |

## Accumulated Context

### Roadmap Evolution

- Phase 9 added (2026-07-27): **Open-Capacity Bookings** — the second host-set occupancy mode (drop-in / common-use, capacity-counter availability), split out of the Phase-8 group-booking discussion as its own phase because it stresses AVAILABILITY (a concurrent seat-claim), not payment. Each visitor books single-payer on the existing rail; the new work is a capacity-counter that replaces the GiST exclusion constraint for these listings, race-proven. Adds OPEN-01..04; depends on Phase 8's `occupancy_mode` seam (default `exclusive`). Organizer-driven open play / cost-split (GPAY-01) remains deferred.

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [09-15]: An e2e fork test seeds a CONTROL of the other mode. "X is absent" is not a finding; "X is absent HERE and present THERE, same host, same day" is. Without it, case 1's three absences would also pass on a build that rendered no availability picker at all.
- [09-15]: Prove a copy/layout absence by scanning the rendered document's own `innerText` for the forbidden SHAPE and whitelisting the allowed framing by count — not by `not.toBeVisible()` on a guessed locator. On this page both "hours" and a clock range are legitimately present (the cancellation disclosure and the venue's opening line), so a literal-absence gate is either always red or quietly vacuous.
- [09-15]: A plan step that is physically unreachable gets SPLIT into the states that exist, not forced. A fully booked date is programmatically `disabled`, so its day panel cannot be clicked open — the sold-out copy is reached through the OC-13 advisory-picker path and the disabled cell is asserted after a reload.

- [08-09]: A mutation gate must name the ARTIFACT it mutates. Mutating a test's inlined copy of production SQL proves the *pattern*; mutating the *production module* proves the *shipped code*. These are different claims and only the second is a regression guard. The GROUP-05 gate does the first only — recorded as such rather than reported as a clean pass.
- [08-09]: Both mutations were run and both outcomes recorded, instead of silently substituting the one that works for the one the plan named. The divergence between them IS the finding.
- [08-09]: The production-lock coverage gap was LOGGED, not closed — closing it is new test authorship, outside a plan declaring `<files></files>` and "no code changes". It is `deferred-items.md` item 4.
- [08-09]: An optional verification step that could not be exercised is recorded as NOT-EXERCISED with its reason (NULL `extra_head_fee`), never quietly dropped and never counted as verified. An untested surface described as verified is worse than one described as untested.
- [08-09]: A blocking checkpoint's completed-task evidence is parked in a `{phase}-{plan}-PROGRESS.md` so a long human pause cannot force a re-run of an expensive mutation; the PROGRESS file is folded into the SUMMARY and deleted on resume, so no stale `status: in-progress` artifact survives.
- [Roadmap]: 8-phase dependency-driven order — identity → supply → availability + double-booking guarantee → booking core + search (no payment) → payments → full instant/request integration → bookings management + cancellation → group bookings. Ordering is non-negotiable for correctness.
- [Roadmap]: Double-booking prevented at the DB level (Postgres GiST exclusion constraint on tstzrange, scoped by listing) — must exist before any booking insert (Phase 3).
- [Roadmap]: Bookability gate built in Phase 2 (gate listing bookability, not creation, on payout-readiness) so it can never be bypassed later — now via PayMongo `merchant.activated`/`activation_status: activated` (D-20); previously Stripe Connect `payouts_enabled`.
- [Quick 260709-id2]: Payments provider switched Stripe → **PayMongo** for the Philippines launch (D-20, supersedes D-17/D-19) — QRPh + GCash + Maya + cards; hold-until-session via on-demand `inhouse` wallet-to-wallet transfers (NOT payment-splitting); bookability gate = `merchant.activated`. Validated by a live sandbox spike (auth/QRPh-accept/card-void confirmed; card manual-capture is a gated advanced feature; QRPh refunds supported per docs). Follow-ups: re-plan 02-06; mark D-17/D-19 superseded in 02-CONTEXT.md.
- [Phase ?]: [01-01]: Postgres image is postgis/postgis:18-3.6 with volume at /var/lib/postgresql (PG18+ convention); bare :18 tag is not on Docker Hub
- [Phase ?]: [01-01]: Integration tests isolate to a dedicated 'test' Postgres schema and migrate ./drizzle before assertions; shared Resend/Cloudinary/Google mocks in tests/helpers/mocks.ts
- [Phase ?]: [01-01]: jose NOT a direct dep (Apple OAuth deferred); src/lib/db/schema.ts is a placeholder owned by the Better Auth CLI until Plan 02
- [Phase 01]: [01-02]: Better Auth configured per D-01..D-13 — email/pw soft gate + Google OAuth, 30d sliding Postgres sessions, revokeSessionsOnPasswordReset:true, input:false canBook/canHost/role escalation guard, profile additionalFields; schema generated + migration APPLIED to live DB
- [Phase 01]: [01-02]: Kept built-in name/email/image + added explicit firstName/lastName/avatarUrl/avatarPublicId (D-09/D-10 public-private split); all timestamps timestamptz; rateLimit.enabled:true with tuned customRules
- [Phase 01]: [01-02]: Fixed Plan-01 test-DB isolation bug (integration writes leaked into dev public schema) — now per-Vitest-worker isolated schema with public-ref rewriting; mocked-email capture needs a fake RESEND_API_KEY in setup
- [Phase 01]: [01-03]: Capability at signup is set via a privileged db.update on the user row AFTER auth.api.signUpEmail (canBook/canHost are input:false — never in the signup body); intent maps to exactly one flag (D-02/T-03-01). book→canBook→"/", host→canHost→"/host" (D-05, /host built later)
- [Phase 01]: [01-03]: Logged-out forms pattern — client component RHF + zodResolver(sharedSchema) for UX, SAME schema re-validates in the server action / Better Auth re-checks server-side; forgot-password is enumeration-safe (uniform message), login error is generic
- [Phase 01]: [01-03]: Patched a BROKEN transitive dep (@better-auth/kysely-adapter vs kysely@0.29 missing exports) that 500'd every route + broke next build — scripts/patch-kysely-adapter.mjs (postinstall), patches dead sqlite-dialect code only. Also: src/middleware.ts kept despite Next 16 'proxy' deprecation (works as required)
- [Phase 01]: [01-04]: Public/private profile split enforced by an ALLOW-LIST projection (src/lib/profile.ts publicProfile → only avatarUrl/firstName/bio/city/createdAt) so a new private column can never silently leak (D-09/D-10); member-since rendered locale-aware (Pitfall 5)
- [Phase 01]: [01-04]: Activate-later capability (activateHosting/activateBooking) flips canHost/canBook via a privileged db.update after a session check — same input:false mechanism as signup; both capabilities COEXIST (neither clears the other, D-03). No payments here (Phase 2, D-05)
- [Phase 01]: [01-04]: Logged-in surfaces gate per-page in the route-group LAYOUT via auth.api.getSession() — (app) booker shell + (host) dashboard; (host) layout is the REAL canHost gate (redirect !canHost→/), middleware stays optimistic-only. /host seam from Plan 03 now filled
- [Phase 01]: [01-04]: Avatar upload re-validates image/* + ≤5MB with a Zod File schema in the server action before the server-only Cloudinary call; stores avatarUrl + avatarPublicId. Real Cloudinary upload is a manual check once CLOUDINARY_* env vars are set (proven via mock)
- [Phase 03]: [03-01]: The `booking_no_overlap` GiST EXCLUDE constraint (`listing_id =`, `unit =`, `tstzrange('[)') &&`) WHERE status IN ('pending','confirmed') is the ONLY double-booking authority (D-21/D-28) — hand-authored in drizzle/0005 (Drizzle can't express EXCLUDE), btree_gist installed, applied to live DB. Half-open `'[)'` + positive occupying-status list reused identically downstream (read model, unit-assignment SELECT).
- [Phase 03]: [03-01]: ⚠️ FINDING for Phase 4 — a GENUINE two-connection concurrent overlapping insert surfaces **40P01 (deadlock_detected)** as well as **23P01 (exclusion_violation)**; both are DB-atomic rejections that prevent the double-book (exactly one row survives). `createBooking` error-mapping / retry loop MUST treat 40P01 like 23P01 ("slot just taken — retry"), not a 500. SC#4 race test accepts either code; sequential cases assert 23P01 exactly.
- [Phase 03]: [03-01]: `makeRacingClients(schema, n)` added to tests/helpers/db.ts (independent connections — the existing `makeClient` is max:1 and serializes, proving nothing for a race); `src/lib/pg.ts` `isPgError(e, code)` for SQLSTATE detection. Bare integer `unit` (1..unitCount), named units deferred (constraint forward-compatible). `booking.bookerId` onDelete:restrict (A5).
- [Phase 03]: [03-02]: ⚠️ `@date-fns/tz` `TZDate.toISOString()` renders the OFFSET-LOCAL form (e.g. `…+08:00`), NOT UTC `Z` — take the true UTC instant from the epoch: `new Date(tzDate.getTime()).toISOString()`. Applied to all slot/day-window instants (slots.ts, read-model.ts).
- [Phase 03]: [03-02]: ⚠️ postgres.js throws `ERR_INVALID_ARG_TYPE` when a JS `Date` is bound into a raw drizzle `sql` range template via `db.execute` — bind ISO strings there (postgres.js casts string→timestamptz); keep `Date` only for the drizzle timestamptz INSERT. Reused by read-model.ts + units.ts; Phase 4 must follow this convention.
- [Phase 03]: [03-02]: `getAvailability(db, listingId, dayLocal, now)` is the single server-authoritative read model (AVAIL-03) — SQL fetches raw overlapping rows with the identical `tstzrange('[)')` bound as the constraint; TS composes per-slot free-unit counts (whole-listing block ⇒ 0 free; only pending/confirmed occupy). `createBooking` = find-free-unit + retry-on-23P01/40P01 bounded by unitCount (constraint is the sole authority); `mapBookingError` → clean "That time was just taken." (23P01/40P01/NoUnitAvailableError), unknown errors re-throw. Widened `DbConn = PostgresJsDatabase<Record<string, unknown>>` so prod + test db both type-check.
- [Phase 03]: [03-03]: Shared availability Zod (`src/lib/validation/availability.ts`) — `weeklyHoursSchema` (close>open, no same-day overlap '[)' touching-endpoints OK, on-the-hour) + `blockSchema` (whole-day/partial, unit>=1 or null=whole-listing). The SAME schema validates in the Plan-04 RHF editor and re-validates in the server actions (never trust the client). The BLOCKER seam for 03-04 is CLOSED: the time regex is `:ss`-tolerant and the on-the-hour refine runs on the normalized `HH:mm` prefix (`t.slice(0,5).endsWith(':00')`), so a DB-read `06:00:00` re-validates on an edit re-save while `06:15:00` still rejects — proven purely AND end-to-end through `saveOperatingHours`.
- [Phase 03]: [03-03]: Host availability write path: `saveOperatingHours` (replace-the-set full-state upsert, D-25 multiple windows/day), `addBlock`/`removeBlock` (close-only, D-24 — no positive-override path). Every write clones the listing.ts contract: `requireUserId` → `assertOwnership(listingId, userId)` (IDOR — the (host) route group alone is NOT the gate, T-03-IDOR-HOURS) → `safeParse` → tx re-scoped to the owner → `revalidatePath(host + public)`. `removeBlock` DELETE scoped `(blockId AND listingId)` behind ownership so a non-owner cannot unblock (T-03-BLOCK-UNBLOCK). Block times stored as timestamptz UTC via TZDate from the venue tz (Manila +8: 10:00→02:00Z; whole day 00:00→prior-day 16:00Z), normalized through the epoch to a Date for the drizzle insert. tests/availability = 7 files/62 tests green; full suite 36/200.
- [Phase 04]: [04-01]: booking gains expiresAt/quotedTotalCents/currency(php)/idempotencyKey + booking_idem_uq partial-unique (WHERE idempotency_key IS NOT NULL), applied to LIVE DB via 0006; 4 nullable cols backfill-safe (no existing bookings, A7). Reuse cancelled status for abandoned holds (D-49/A3), no expired enum.
- [Phase 04]: [04-01]: read-model occupancy is now lazy-expiry: (status = confirmed OR (status = pending AND expires_at > now())) via SQL now() (DB clock, NOT the injectable now:Date — Pitfall 7); the [) bound + listing scope stay identical to the 0005 EXCLUDE. Stale pending reads FREE everywhere (calendar + search Stage-2 downstream).
- [Phase 04]: [04-01]: 0007 hand-authors listing_location_geog_gist GiST on (location::geography) with IF NOT EXISTS (harness replay) — the 0002 geometry index does not serve the ::geography radius cast. Hand-authored migrations use drizzle generate --custom (empty SQL + copied snapshot + journal entry).
- [Phase 04]: [04-02]: `searchParamsSchema`/`bookingCreateSchema` (src/lib/validation/booking.ts) are the V5 input-validation control — shared client+server, SHAPE-ONLY (invariants re-derived server-side). Single combined `category` = `z.union([z.enum(spaceTypeValues), z.enum(activityTagValues)])` from @/lib/listing-vocab (the two vocabs are DISJOINT, D-35) so an activity-tag-only value like `basketball` validates like a space-type — do NOT split into type/activity. `z.coerce.number().min().max()/.int()` rejects NaN naturally (every NaN comparison is false), so a tampered `NaN`/"abc" FAILS safeParse without throwing (T-04-ORIGIN/PRICEIN); lat/lng must be present TOGETHER (a lone coord can't form an origin). radius restricted to preset {2,5,10,25} default 10 (rejects, not clamps). Search SQL must cast enum `::text` when comparing a cross-vocab category (Pitfall — activity value is not a valid space_type label).
- [Phase 04]: [04-02]: `DISPLAY_CURRENCY = "php"` promoted to src/lib/money.ts (D-46) as the single shared price source; the listing page keeps its LOCAL copy until Plan 07 swaps the import (do NOT edit page.tsx before then).
- [Phase 04]: [04-02]: D-38 seed = scripts/seed.ts (standalone raw-postgres.js, NO `@/` imports so it runs under tsx; idempotent delete-first on `seed_%` in FK-safe order; 5 bookable Metro Manila listings, ST_MakePoint x=lng/y=lat) mirrored by tests/helpers/seed.ts (Drizzle `seedSearchListings(db)` for isolated schemas + `SEARCH_ORIGIN` Makati 14.5547,121.0244 + `DISTANCES_KM` DERIVED via haversine so they never drift). seed_listing_4 = gym tagged `basketball` (DISJOINT type/tag, D-35 supply); seed_listing_5 ≈15.3km (BEYOND 10km → radius/zero-result path). tsx already in node_modules (no install, T-04-SC). `npm run db:seed` added.
- [Phase 04]: [04-02]: SEARCH-01..04 + BOOK-01 NOT marked complete — they are CROSS-CUTTING (appear in plans 04-02..04-08); 04-02 ships only the validation/currency/seed substrate. Completion stays with the plans that ship the search query/UI (04-03/05) + price breakdown (04-04/06/07); validated at phase transition. ⚠️ gsd-sdk string-arg query handlers (record-metric/add-decision/record-session stopped_at) silently no-op'd in v1.42.3 — metric row + decisions + stopped_at were hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked.
- [Phase 04]: [04-03]: `searchListings` (src/lib/search/query.ts) is the two-stage search spine (D-34): Stage-1 = ONE Drizzle `sql` candidate query (inlined deriveBookable gate w/ Pitfall-5 sync comment + `::geography` `ST_DWithin`/`ST_Distance` on BOTH operands + `ST_MakePoint(lng,lat)` + D-35 category `primary_space_type::text` OR `listing_activity_tag` EXISTS + price + tz-independent weekday `EXTRACT(DOW)` EXISTS), ordered distance/price with a `created_at DESC` tiebreaker + `LIMIT+1` probe; Stage-2 = per-candidate `getAvailability` reuse (the SAME read model as the listing calendar — NO second SQL occupancy predicate). `::geography` proven by the beyond-10km outlier-exclusion test (the degrees-vs-meters guard). tests/search 16/16; tsc + eslint clean.
- [Phase 04]: [04-03]: Stage-2 window = venue-local wall-clock `HH:mm` resolved PER-CANDIDATE via TZDate (not absolute UTC), so the same picked local window lands per-venue (venue-tz honored). date-only ⇒ any slot `state==='available'`; date+time ⇒ EVERY on-the-hour slot in `[start,end)` is `'available'` (subsumes freeUnits≥1 + future + in-horizon). Injectable `now?:Date` 3rd param (mirrors getAvailability) for deterministic slot state. Strict `parsePickedDate` binds the CANONICAL iso into `::date` (never the raw string) + round-trip guard; `parseWindowHour` never reaches SQL — malformed input degrades to date-only, never a mid-query 22007. Over-fetch `pageSize*2+1` when a date is picked (Stage-2 can drop candidates, Pitfall 8/A5). SEARCH-01..05 NOT marked complete — query spine here; user-facing wiring + SEARCH-05 cards at 04-05.
- [Phase 04]: [04-04]: `createPendingHold` (src/lib/availability/units.ts) is the WR-03 pending-hold keystone — a REAL `db.transaction`: OUTER `for` retry on `40P01` (postgres.js `begin()` never auto-retries) wrapping `db.transaction`; per-unit nested `tx.transaction` = **SAVEPOINT** so a `23P01` rolls back ONLY that attempt (never `25P02`) and the loop tries the next free unit; in-tx **sweep** (`UPDATE booking SET status='cancelled' WHERE status='pending' AND expires_at<=now() AND range &&`) before the find-free probe (D-48b, so the EXCLUDE sees the freed slot); own-hold pre-check (idempotency primary, D-42) + `23505` backstop. Correctness rests on the `booking_no_overlap` EXCLUDE — the probe is advisory. `createBooking`+`mapBookingError` kept unchanged; bind `db` not the auto-commit conn.
- [Phase 04]: [04-04]: ⚠️ D-42 ordering finding for downstream — `booking_no_overlap` (0005) has a LOWER OID than `booking_idem_uq` (0006), so a concurrent same-key SAME-WINDOW race trips the EXCLUDE (`23P01`) BEFORE the unique index (`23505`). The RESEARCH draft's `23505`-only idempotency branch would false-"just taken" a `unitCount=1` double-click. Fix: on `23P01`, re-run the own-hold pre-check (`findOwnActiveHold` matches idempotency_key OR booker+exact-window among active holds) → replay if it's your own committed duplicate, else next unit. Only genuine someone-else conflict/exhaustion maps to "just taken".
- [Phase 04]: [04-04]: ⚠️ Rule-1 fix in `src/lib/pg.ts` — Drizzle 0.45.2 wraps driver errors in `DrizzleQueryError` ("Failed query:…") with the postgres.js error (and its SQLSTATE `.code`) on `.cause`. `isPgError` only checked the top level, so a `23P01`/`40P01`/`23505` from a drizzle query slipped every catch → raw 500. Now walks the `.cause` chain (bounded). Latent because `createBooking`'s own `23P01` branch was never exercised through drizzle (its probe avoids the conflict); `createPendingHold` is the first drizzle caller to actually catch a wrapped conflict. Raw-error callers (exclusion-race, error-map) unaffected — top level checked first. Any future drizzle-query error-code catch MUST rely on this cause-walking `isPgError`.
- [Phase 04]: [04-05]: Search home UI at `/` (D-29) — the demand-side front door. `SearchResultCard` (extends listing-card; NEUTRAL — no host controls, no status badge; PHP `DISPLAY_CURRENCY`; distance origin-only; whole-card `Link` carrying `?date=&start=&end=`; title 500→600). `SearchResults` (1/2/3-col grid + sort `Select` [Nearest disabled + hint w/o origin] + neutral Load more off the hasMore probe + skeleton + zero-result escape hatches `Broaden radius`/`Clear filters`/`Show nearby` + broadened "You might also like" cards + cold-start floor; URL-driven client boundary). `SearchBar` (reused `AddressAutocomplete` + "Use my location"; ONE combined category `Select` MERGING `SPACE_TYPE_LABELS`+`ACTIVITY_TAG_LABELS` → single `?category`, D-35; date/on-the-hour time; max ₱/hr → cents at submit; radius 2/5/10/25 default 10; ONE coral Search) serialized to the URL via `useRouter`+`URLSearchParams`. `app/page.tsx` = public RSC: awaits + `searchParamsSchema.safeParse` (garbage → default city view, never crash, T-04-PARAMTAMPER) → two-stage `searchListings` server-side; **cumulative page 0..N fetch** drives Load more (`MAX_PAGES=50` cap, DoS safeguard); zero-result → broadened fallback `nearbyAlternatives`. Decision: RHF + `searchParamsSchema.safeParse` at submit (NOT `zodResolver` — `z.coerce.number()` input types resolve to `unknown` and fight RHF; the RSC safeParse is the authority). Mobile expand-to-`dialog` deferred (responsive wrapping pill instead); price is `priceMax`-only (the schema's single axis). **SEARCH-01..05 now Complete.** ⚠️ Pre-existing/out-of-scope (NOT 04-05, logged to `deferred-items.md`; my 4 files tsc+eslint clean, build Compile+TS green): `npm run build` fails at `/host/payouts/refresh` (`PAYMONGO_SECRET_KEY` unset, untouched Phase-2 lib); `npm run lint` errors on `address-autocomplete.tsx:110` (`react-hooks/set-state-in-effect`, unchanged Phase-2). ⚠️ gsd-sdk v1.42.3 `record-metric` string-args still no-op — metric/decisions/session hand-written; advance-plan/update-progress/roadmap/requirements.mark-complete worked. Commits 8d46a4e (T1) + 8c4c229 (T2) + e9169aa (T3).
- [Phase 04]: [04-04]: `quoteWindow` (src/lib/booking/pricing.ts) = server-frozen quote: re-derives `hours` from the window (epoch delta of the absolute UTC on-the-hour instants — DST-safe because slots.ts already resolved them; NOT wall-clock arithmetic) × the listing rate; `fullDay?dayRate:hourly*hours` (D-45 distinct, NO cap); currency `php` (D-46). Throws rather than freeze a $0 quote when the required rate is null (money guard). `createPendingHold` loads unitCount+rates INSIDE the tx and freezes `quotedTotalCents` (Phase-5 charges this). `HOLD_TTL_MINUTES=15` (D-47) drives the countdown. `makeBookingReference` (reference.ts) = `FIT-`+8 Crockford base32 (no I/L/O/U) from `randomBytes`, uniform (256%32==0), non-sequential — generated on-read (no `reference` column this plan). BOOK-01/03 stay Pending (backend here; user-facing at 04-06); BOOK-02 already Complete. ⚠️ gsd-sdk v1.42.3 string-arg handlers (record-metric/add-decision/record-session) still no-op — metric row + decisions + stopped_at hand-written; advance-plan/update-progress/roadmap worked.
- [Phase 04]: [04-06]: Booking mutation layer + reserve atoms. `placeHold` (src/app/actions/booking.ts) = a POST server action (NEVER a GET side-effect, Pitfall 2) that clones the blocks.ts skeleton but swaps host-ownership for a BOOKER-CAPABILITY gate: `requireUserId` (D-41 sign-in) → `canBook` re-read from the DB (input:false source of truth, NOT the session) → server RE-DERIVATION of `deriveBookable` via the listing⨝user⨝hostPayout join (Security V4 — the reserve route group is NOT the gate) → `bookingCreateSchema.safeParse` → `createPendingHold(db,…)` → `revalidatePath(listing)+('/')` → `redirect(/listings/[id]/book?hold=<id>)`. `confirmBooking(holdId)` = owner-gate `booking.bookerId===userId` (IDOR; missing-row and non-owner both return the SAME "denied" so a guessed id leaks nothing) → **idempotent short-circuit BEFORE the expiry check**: an already-`confirmed` OWN booking redirects to `/bookings/[id]` as a no-op SUCCESS (D-42), NEVER via mapBookingError → else ONE atomic `UPDATE booking SET status='confirmed',expires_at=NULL WHERE id=… AND booker_id=… AND status='pending' AND expires_at > now() RETURNING id` (server is the SOLE expiry authority — no TOCTOU, DB clock, never the client countdown); 0 rows ⇒ graceful `reason:"expired"` (never a silent confirm/500). Actions return discriminated unions (`PlaceHoldResult` reason: sign-in|activate-booking|not-bookable|invalid|taken; `ConfirmResult` reason: sign-in|denied|expired); SUCCESS redirects (never returns ok:true) so Plan 07 maps each reason to the UI-SPEC copy/state. Test harness: mock `next/navigation` redirect to throw a typed `RedirectError` carrying the URL → assert the SUCCESS/redirect path (tests/booking/state-machine.test.ts, 7 green: happy pending→confirmed, expiry-refusal-not-confirmed, owner-gate, idempotent re-confirm + the 3 placeHold gates). Four reserve atoms honor the UI-SPEC: `PriceBreakdown` (server-frozen `quotedTotalCents`+`currency` as a fee-extensible list, Heading-600 tabular-nums Total, documented Phase-5 RESERVED fee slot, ZERO client arithmetic — `hours` is a prop), `HoldCountdown` (`"use client"` role=timer 15-min from `expiresAt`, neutral + optional `--destructive` numerals ONLY final-60s, `onExpire` flip at 0; the setInterval callback is the ONLY setState site to dodge react-hooks/set-state-in-effect, ref synced in its own effect to dodge react-hooks/refs, `suppressHydrationWarning` on the digits), `HoldExpiredState` (calm TimerOff + coral Back-to-availability/neutral Search-other — NEVER red), `ReserveActions` (`"use client"` disable-on-click Confirm→Confirming… + "You won't be charged yet."). BOOK-01/03 NOT marked complete (action+atoms only; user-facing reserve/confirmation pages + Book-CTA wiring = 04-07, same requirements frontmatter — mirrors 04-04's deferral); BOOK-02 already Complete. My 6 files tsc+eslint clean; full booking suite 25/25. ⚠️ Pre-existing/out-of-scope (deferred-items.md, NOT 04-06): `npm run build` PAYMONGO_SECRET_KEY guard; `npm run lint` address-autocomplete.tsx:110. ⚠️ gsd-sdk v1.42.3 string-arg handlers still no-op — metric/decisions/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked. Commits 778249b (T1) + 6691df6 (T2).
- [Phase 05]: [05-01]: PAY-01/PAY-02 money foundation — computeCommission (src/lib/payments/commission.ts) is pure integer-cents (commissionCents=Math.round(gross*rateBps/10000); netCents=gross−commission ALWAYS, D-52 platform absorbs gateway fee); default rate reads COMMISSION_RATE_BPS from src/lib/payments/config.ts (also PAYOUT_DELAY_HOURS=24, PAYMENT_WINDOW_MINUTES=60 — the phase's single named config source). host_payout_ledger (0008) = UNIQUE(booking_id) at-most-once payout gate with FROZEN commission_rate_bps+commission_cents (D-51 — a later rate change never rewrites past payouts); payout state (held→processing→paid/refunded/failed) is a SEPARATE machine DERIVED from confirmed+endsAt, NOT a booking_status 'completed' transition (D-56). booking.payment_id added for later refunds; listing.currency default usd→php reconciled + live-backfilled (charge currency consistent end-to-end). 13/13 commission tests green; tsc clean; 0008 applied to live DB. TDD RED fea1d5e→GREEN c1d3a31; schema 05de76f; migration 1c7c283.
- [Phase 05]: [05-02]: PAY-01/PAY-03 PayMongo money-movement client (src/lib/paymongo.ts EXTENDED, not rewritten). **Pitfall-3 base fix (T-05-06):** PAYMONGO_BASE is now version-less `https://api.paymongo.com`; EVERY caller passes a fully-versioned path — existing Linked-Account calls → `/v1/linked_accounts`(+/onboarding_links); new calls → `/v1/checkout_sessions`, `/v1/refunds`, `/v2/batch_transfers`, `/v2/wallets` — so a /v2 call can never hit `/v1/v2/...`. Four new calls all wrap the existing paymongoFetch verbatim: **createCheckoutSession** (hosted /v1 charge, full PH rail set `["card","gcash","paymaya","qrph"]` D-53, line_items[0].amount=server-frozen amountCents, caller-supplied idempotencyKey e.g. `checkout:<bookingId>`) → `{id, checkoutUrl}`; **createRefund** (/v1, `reason:"others"`, key `refund:<paymentId>`, ⚠ FAILS QRPh/UBP — caller branches on method first, D-60) → `{id, status}`; **createBatchTransfer** (/v2 inhouse `provider:"paymongo"` — NEVER payment-splitting; `amount: netCents` VERBATIM, not reduced by gateway fee D-52/T-05-10; key `payout:<bookingId>`; source_account=PLATFORM_WALLET; beta nesting `data.attributes.transfers[0]` optional-chained A2/A3) → `{batchId, transferId, status}`; **listWalletAccounts** (GET /v2/wallets?status=activated, no Idempotency-Key on GET) → `WalletAccount[]` — ⚠ GLOBAL unfiltered list, JSDoc MANDATES the Plan-05 caller correlate by `wallet.id`/`accountNumber`, NEVER `[0]`. Fail-closed prod boot extended to PLATFORM_WALLET_NUMBER/NAME (T-05-09); BIC default PAEYPHM2XXX; dev/test/build tolerate placeholders (mocks). mockPayMongo gains all four deterministic stubs (+reset()) so Wave-2 tests never hit live PayMongo. New tests/payments/paymongo-calls.test.ts mocks GLOBAL fetch over the REAL client, asserting exact URL/method/headers/body per call (the /v2/batch_transfers-not-/v1/v2 assertion + Idempotency-Keys). 33/33 payments+paymongo green; tsc clean. PAY-01/PAY-03 kept In-progress (client primitives only; user-facing charge=05-03/04, payout sweep=05-05a/b — cross-cutting, per Phase-4 precedent). ⚠ user_setup PLATFORM_WALLET_* env + /v2 money-movement beta are UAT prereqs, NOT plan blockers (code+tests run on mocks). Commits da95c5a (T1 feat) + 253fc33 (T2 test).
- [Phase 05]: [05-04]: PAY-01 confirm authority + D-58 backstop + D-60 refund events. Extended the single-writer webhook (src/app/api/paymongo/webhook/route.ts) — verifySignature + paymongo_event dedupe UNTOUCHED. **checkout_session.payment.paid** = the SOLE confirm writer (D-57): derives bookingId from the VERIFIED event's data.attributes.data.attributes.reference_number (never a client field) + paymentId from payments[0].id → `UPDATE booking SET status='confirmed', expires_at=NULL, payment_id=$pid WHERE id=$ref AND status='pending' RETURNING id`. Guards status='pending' **ALONE** — the Phase-4 expires_at>now() check is deliberately NOT re-imposed (Pitfall 4: a paid-but-lapsed hold MUST still confirm; the GiST EXCLUDE, not the TTL, is the double-confirm authority). A1 payload nesting asserted against seeded test events. **handleGoneSlot** (D-58): a 0-row confirm ⇒ RE-READ the booking to distinguish a benign already-confirmed replay (no-op — NEVER refund a paid+confirmed booking; NOT in the plan snippet, Rule-2 correctness) from a genuinely gone slot → refund the SERVER-FROZEN quoted_total_cents on REFUNDABLE_RAILS {card,gcash,grab_pay,paymaya} via createRefund, else operator-alert {qrph,dob_ubp,unknown}/refund-failure via `console.error('[PAYMENT_ALERT] ...')` + `recordAudit({actorId:'system', outcome:'needs_attention'})` — NEVER silent retention (Pitfall 1) — then set booking cancelled (guard status<>'confirmed') so ?paid=1 renders PaymentReversedState (Plan-03 contract). Double-book-during-payment proven: the swept loser is reversed, the pending winner stays confirmable (EXCLUDE lets one path win). **payment.refunded / payment.refund.updated** (D-60): resolveRefundPaymentId reads pay_... from the verified event (data.attributes.data.attributes.payment_id, fallbacks payment.id / pay_-prefixed resource id) → idempotently flip any host_payout_ledger row held→refunded (drizzle typed .update) + booking→cancelled (keeps it out of the Plan-05 confirmed-only sweep; a pre-payout refund is just a platform-wallet refund, no host clawback). Replay = 200 no-op via the dedupe. **AuditOutcome += 'needs_attention'** (src/lib/audit.ts, Rule-3 blocking — the typed operator-alert wouldn't compile against ok|denied|error). Terminal reversed state for BOTH the auto-refund and refund-event paths = `cancelled` (no 'refunded' value in booking_status). 12 new webhook tests (webhook-payment-paid 8: confirm/past-expiry-confirm/browser-return-control/replay-once/badSig-400/card-auto-refund-once/QRPh-alert/double-book; webhook-refund 4: refunded+refund.updated/replay-once/badSig-400); 49/49 payments+paymongo green; tsc+eslint clean. Deviations: handleGoneSlot re-read guard (Rule 2), AuditOutcome extension (Rule 3), drizzle typed refund update vs raw SQL to keep imports used (Rule 3). **PAY-01 now Complete** (checkout 05-03 + confirm-on-webhook 05-04). Commits 3030e0d (T1) + 9fda1fb (T2) + bacb114 (T3). ⚠ gsd-tools build has no requirements/roadmap subcommands + state handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited.
- [Phase 05]: [05-03]: PAY-01 Confirm & pay checkout (D-57/D-58). `confirmBooking` (src/app/actions/booking.ts) RETIRES the Phase-4 synchronous pending→confirmed flip — the checkout_session.payment.paid webhook (05-04) becomes the SOLE confirm authority. New flow: owner-gate load (now reads frozen quotedTotalCents/currency/listingId; missing OR not-mine → same calm "denied", T-05-13) → already-confirmed idempotent short-circuit → redirect(/bookings/[id]) (D-42, BEFORE the charge) → non-pending guard (calm "hold no longer active") → rateLimit(`confirm-pay:<userId>`, 5/60s) + recordAudit on denial (WR-06, reuses the paymongo-connect.ts pattern) → null-quote guard (never charge 0) → **EXTEND the hold** `UPDATE booking SET expires_at = now() + make_interval(mins => ${PAYMENT_WINDOW_MINUTES}) WHERE id=… AND booker_id=… AND status='pending'` BEFORE the charge so the lazy-expiry sweep can't take the slot mid-payment (D-58, T-05-16) → `createCheckoutSession({ amountCents: bk.quotedTotalCents, referenceNumber: holdId, metadata.booking_id: holdId, successUrl: `${BETTER_AUTH_URL}/bookings/<id>?paid=1`, cancelUrl: `${BETTER_AUTH_URL}/listings/<listingId>/book?hold=<id>`, idempotencyKey: `checkout:<holdId>` })` for EXACTLY the server-frozen amount (D-49 charge integrity — the action takes only holdId, so a client can't inject an amount, T-05-11) wrapped in try/catch → `redirect(checkout.checkoutUrl)`. The old `expires_at > now()` confirm guard is GONE — a lapsed-but-pending hold is now EXTENDED, not refused. ⚠ ConfirmResult gains **reason:'checkout'** (checkout-create failure / rate-limit denial / null-quote) — reserve-view.handleResult flips to the SAME calm recovery state as expired/denied (never red). ⚠ DEVIATION: used `BETTER_AUTH_URL` (the real app-URL convention — paymongo-connect.ts/auth.ts, set in .env*) NOT the plan's literal `NEXT_PUBLIC_APP_URL` (defined nowhere → would 3000-hardcode deployed return URLs). Reserve UI (reserve-actions.tsx): Confirm booking→**Confirm & pay** / Confirming…→Taking you to checkout…, reassurance→`You'll pay {₱total} now — cards, GCash, Maya, or QR Ph. Payments are processed securely.`; `totalLabel=formatMoney(quotedTotalCents,currency)` threaded RSC(book/page.tsx)→ReserveView→ReserveActions. price-breakdown.tsx final sub-line→`Final price — no added fees. You'll pay this now.` — booker breakdown STAYS subtotal=total, NO commission line (D-50; the RESERVED slot stays empty — commission is host-side, lands only on 05-06 /host/earnings). /bookings/[id]/page.tsx (Next-16 async `searchParams: Promise<{paid?:string}>`) branch table AFTER the owner-gate: confirmed→existing Confirmed UI (unchanged); pending+paid==='1'→<PendingPaymentState/> (do NOT redirect); pending w/o paid→redirect reserve (Phase-4 abandoned); cancelled+paid==='1'→<PaymentReversedState listingId/> (D-58 auto-refund landing); else non-confirmed→notFound(). **PendingPaymentState** (new, "use client") = neutral bounded `router.refresh()` poller (2500ms×8 then manual Refresh + "taking longer" copy), aria-live status, HoldCountdown hook discipline (setInterval-only setState, closure counter, ref-synced router, clear on unmount), NEVER fabricates confirmed client-side (the RSC decides on DB status). **PaymentReversedState** (new) mirrors HoldExpiredState — calm coral `Back to availability`/neutral `Search other spaces`, NEVER red. TDD: tests/payments/checkout-create.test.ts 4/4 (charge integrity 150000/99900 + extend-hold >now()+50min + no sync flip (still pending) + owner-gate no-checkout + idempotent no-checkout; RED c2003e0→GREEN d2d9e2d). tests/booking/state-machine.test.ts UPDATED to the new contract (added @/lib/paymongo mock; happy→off-site redirect+stays pending; lapsed hold EXTENDED not refused; idempotent via direct confirmed-set→no 2nd checkout) — Rule-1 fix, the retired sync flip broke the Phase-4 assertions. 62/62 booking+payments+paymongo green; tsc+eslint clean on all 10 files. PAY-01 kept In-progress (checkout action+UI here; the payment.paid webhook = confirm authority at 05-04 — cross-cutting, per REQUIREMENTS traceability). Commits c2003e0 (T1 test) + d2d9e2d (T1 feat) + c358872 (T2) + c07c804 (T3). ⚠ 05-04 CONTRACT: the webhook must flip status='confirmed'/expires_at=NULL (+capture payment_id) on reference_number ALONE (no expires_at>now() guard, Pattern-map Pitfall 4) to resolve the interstitial; a gone-slot auto-refund must set status='cancelled' which the ?paid=1 return renders as PaymentReversedState.
- [Phase 05]: [05-05a]: PAY-03/PAY-02 payout SWEEP — the FIRST async-scheduled job in the repo. inngest@4.13.0 client (src/inngest/client.ts, id fitout; NO serve mount — Plan 05b owns /api/inngest). src/inngest/functions/payout-sweep.ts: queryDuePayouts (RESEARCH Pattern-4, DB clock now(): b.status='confirmed' AND b.ends_at + make_interval(hours=>PAYOUT_DELAY_HOURS) <= now() AND p.id IS NULL; aliases hp.paymongo_account_id→paymongoAccountId); payOne = computeCommission FREEZE (D-51) → INSERT … ON CONFLICT (booking_id) DO NOTHING RETURNING id at-most-once claim (the INSERT is the lock, mirrors createPendingHold — NO app-level "already paid?" check) → correlate wallet.id===b.paymongoAccountId among status='activated' wallets (no match ⇒ leave Held + console.error('[payout-alert] no activated wallet for host') + fire NOTHING, NEVER wallets[0]) → createBatchTransfer(netCents verbatim, D-52 — platform absorbs the gateway fee) → ledger Held→Processing + transfer_id (on throw: state='failed' + [payout-alert]). payoutSweep = hourly singleton {cron:'TZ=Asia/Manila 0 * * * *', concurrency:1} with per-booking step.run so a mid-batch failure retries just one payout. .env.example sole-owns COMMISSION_RATE_BPS/PAYOUT_DELAY_HOURS/PAYMENT_WINDOW_MINUTES/PAYOUT_RECONCILE_STUCK_HOURS(05b)/PLATFORM_WALLET_*/INNGEST_*. NO route.ts created here (Plan 05b mounts serve AFTER both cron files exist → no dangling payout-reconcile import → tsc-clean at this boundary). ⚠ DEVIATION (Rule 3): inngest 4.13.0 uses the 2-arg createFunction(options, handler) with the cron in options.triggers — the RESEARCH/plan 3-arg (config, trigger, handler) skeleton predates this API. Wallet-correlation field CHOSEN = wallet.id===host_payout.paymongo_account_id (primary; stored accountNumber is the documented fallback; the real field stays UAT-gated on the /v2 beta, A2). 6 new tests (payout-sweep 5: due-only, happy net-180000 to host's OWN wallet, at-most-once via makeRacingClients, multi-host A→A/B→B never crossed, no-match Held+alert; ledger-freeze 1: a later rate change never rewrites an existing row via the ON CONFLICT no-op); 55/55 payments+paymongo green; tsc+eslint clean on all 4 new files. PAY-02/PAY-03 kept In progress (PAY-03 lifecycle → Paid at 05-05b reconcile; PAY-02 host-visible commission at HOST-03/05-06; real transfer UAT-gated on PayMongo /v2 beta). Commits 68bb06d (T1) + 21674d5 (T2) + 7bdbed8 (T3).
- [Phase 05]: [05-06]: HOST-03 host earnings/payouts page (D-59) — owner-gated (host) RSC at /host/earnings reading host_payout_ledger WHERE host_id=session.user.id (Security V4/T-05-29 — the route group is NOT the gate), joined booking/listing; re-gates session+canHost (defense in depth). New PURE non-client module src/components/host/payout-ledger-status.ts (distinct from onboarding payout-status.ts): derivePayoutLedgerView(state)→{label,tone,helper,datePrefix} calm view (Held=muted+'Held until after the session'+Expected, Processing=outline+Expected, Paid=success+Paid, Refunded=muted+'This booking was refunded — no payout.'+Refunded, Failed=attention) + summarizePayouts(rows)→{upcoming=Held+Processing net, paid=Paid net} — the SAME helper the page + test call (single source of truth). PayoutStateBadge keyed by STATE (Held/Refunded share the muted tone but differ Clock vs Undo2): Paid=bg-success+CheckCircle2, Held=secondary+Clock, Processing=outline+ArrowLeftRight, Refunded=secondary-muted+Undo2, Failed=destructive Alert (NOT a badge) — never red on a happy state (icon+text, never colour-only). PayoutRow (mobile card) renders the HOST-VISIBLE Booking {gross} / FitOut service fee (10%) −{commission} / Your payout {net} (net wt-600 tabular-nums; Refunded→muted/struck net + helper) — host SEES the fee (D-59), booker never does (D-50). PayoutSummary = neutral Upcoming payouts / Paid out Display figures, no coral. Page: server-summed totals; venue-tz-safe Expected(endsAt+PAYOUT_DELAY_HOURS)/Paid(paidAt) dates via TZDate/format (confirmation-page idiom, each row respects its listing tz); desktop shadcn table (real th scope=col Space·When·Booking·Fee·Payout·Status·Expected) hidden md + PayoutRow cards md:hidden; PayoutBanner nudge when payoutStatus!=='enabled'; No earnings yet empty state; 10%-fee explainer footnote; container mx-auto max-w-4xl; NO bg-brand/coral (calm status view). Added shadcn official `table` primitive (src/components/ui/table.tsx). Neutral Earnings nav: outline Button in host/page.tsx dashboard action row (next to Your listings) + link in host/layout.tsx header (before Profile). All money server-frozen (D-49/D-51) — page/components do ZERO arithmetic (formatMoney display only). KNOWN BEHAVIOR (not a stub): the page reads the LEDGER; ledger rows are written by the T+24h sweep (05-05a), so a confirmed booking pre-sweep has no row yet (shows Held only once the sweep creates it) — matches the plan's explicit read-the-ledger scope; surfacing pre-sweep confirmed bookings would need a confirm-time ledger write (cross-plan). 10 new tests (earnings-view.test.ts: derivePayoutLedgerView mappings, summarizePayouts 135000/180000, owner-scope A≠B + absent-host-sees-nothing, commission visibility 200000→20000→180000); 65/65 payments+paymongo green; tsc+eslint clean on all 9 files. Threat register T-05-29/30/31/32 all mitigated (owner-scope, RSC re-gate, server-frozen money, no PayMongo internals in host copy). Deviation: NONE (one test-harness micro-fix — distinct non-overlapping booking windows so same-listing seeds don't trip booking_no_overlap). **HOST-03 COMPLETE.** Commits ce13923 (T1) + 8ff110f (T2) + 5d2f1df (T3).
- [Phase 05]: [05-05b]: PAY-03 payout RECONCILE + /api/inngest serve mount — CLOSES the D-59 Held→Processing→Paid/Failed lifecycle (Wave 3, FINAL Phase-5 plan). **getTransfer** (src/lib/paymongo.ts EXTENDED) = GET /v2/transfers/{id} (no Idempotency-Key on the GET), reusing paymongoFetch + the versioned /v2 base — a POLL, NOT a webhook, because PayMongo emits no transfer/payout webhook event (Research Pitfall 2). src/inngest/functions/payout-reconcile.ts: **mapTransferStatus**(status)→'paid'|'failed'|'processing' with a SAFE DEFAULT (paid∈{succeeded,completed,paid}, failed∈{failed,returned,cancelled}, EVERYTHING ELSE→processing so an unknown/in-flight status can NEVER spuriously flip to Paid — ⚠ VERIFY the enum vs a live PayMongo /v2 response before UAT, A4); **queryProcessingLedger**(dbConn) selects state='processing' AND transfer_id NOT NULL (ORDER BY created_at ASC LIMIT 200); **reconcileOne**(row,dbConn) = getTransfer → UPDATE host_payout_ledger SET state='paid',paid_at=now() (or state='failed') WHERE booking_id=$id AND state='processing' (the AND state='processing' guard makes it IDEMPOTENT — a re-run on a terminal row is a 0-row no-op, paid_at never rewritten, mirrors the sweep's ON CONFLICT + booking's EXCLUDE); a terminal Failed OR a row stuck past PAYOUT_RECONCILE_STUCK_HOURS (default 48h) each emits console.error('[payout-alert] transfer failed' | 'transfer stuck processing') so no payout is ever silently stranded (T-05-28). **payoutReconcile** = hourly singleton {concurrency:1, triggers:[{cron:'TZ=Asia/Manila 30 * * * *'}]}, offset 30m from the 05-05a sweep so the two never contend; per-row step.run. **src/app/api/inngest/route.ts** (created LAST, after BOTH cron files exist → both static imports resolve tsc-clean, no TS2307) mounts serve({client:inngest, functions:[payoutSweep, payoutReconcile]}) exporting GET/POST/PUT, runtime='nodejs', with a FAIL-CLOSED prod boot guard: throws at module load if NODE_ENV==='production' && !INNGEST_SIGNING_KEY (T-05-26; dev/test/build tolerate its absence — the Inngest Dev Server needs no keys). mockPayMongo gains a getTransfer stub (default 'succeeded') + reset() wiring. 6 new tests (payout-reconcile.test.ts: queryProcessingLedger selectivity, Processing→Paid paid_at-set, Processing→Failed+alert, idempotency on an already-paid row, unknown-status-stays-processing, stuck-alert); 71/71 payments+paymongo green (65 prior + 6); tsc+eslint clean (0 errors) on all touched files. DEVIATION (Rule 3): inngest 4.13.0 2-arg createFunction(options, handler) with the cron in options.triggers — the plan/RESEARCH 3-arg skeleton predates this API (same fix 05-05a made). **PAY-02 + PAY-03 now COMPLETE; Phase 5 COMPLETE (7/7 plans).** Real PayMongo /v2 transfer + polling UAT-gated on the money-movement beta (already in STATE blockers). Commits a6260bd (T1) + b83c5e5 (T2) + ddbaba0 (T3). ⚠ gsd-tools handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited.
- [Phase 06]: [06-01]: Request-to-book SCHEMA FOUNDATION. Added booking_status enum values `requested` (booker asked, host not yet approved) + `approved` (host approved, awaiting payment) — BOTH slot-holding (D-63). booking.bookingMode nullable creation-time SNAPSHOT (D-61 display/audit; lifecycle correctness rests on `status`, not this col). listing.bookingMode DB default flipped `request`→`instant` (D-62 demand-first). APPROVAL_SLA_HOURS + APPROVAL_PAYMENT_WINDOW_HOURS config (D-64, default 24) in config.ts + .env.example. Three hand-split migrations (0010 enum-add / 0011 cols+default / 0012 EXCLUDE recreate) applied to live DB via `npm run db:migrate`; re-run no-op; harness replays all three (100/100 booking+paymongo+payments green). ⚠ **LOAD-BEARING DEVIATION (Rule 1):** drizzle-orm's postgres-js migrator wraps ALL pending migrations in ONE transaction (verified in node_modules/drizzle-orm/pg-core/dialect.cjs `session.transaction(...)`), so the plan's literal `WHERE status IN ('pending','confirmed','requested','approved')` raised Postgres **55P04** on this already-migrated DB (the ADD VALUE'd values are uncommitted inside the shared tx). The 3-file split does NOT fix this for `db:migrate` (only the autocommit test harness benefits). FIX: 0012 EXCLUDE WHERE encoded as the SET-EQUIVALENT **complement** `status NOT IN ('cancelled','declined','completed')` — names only already-committed values, applies in one tx on fresh+incremental+harness DBs, and is the SAFER default (unknown future status ⇒ OCCUPYING/blocks). `status::text IN (...)` is NOT viable (enum→text STABLE; index predicates need IMMUTABLE). Live constraint def now `WHERE status <> ALL (cancelled,declined,completed)`; proven a `requested` row rejects an overlapping `pending` insert. **Every downstream read/occupancy predicate MUST mirror the occupying set {pending,confirmed,requested,approved} (or the complement) or a requested/approved slot reads as free.** BOOK-04/BOOK-05/PAY-05 kept In-progress (substrate only; behavior ships in 06-02+, per 04-xx/05-xx cross-cutting precedent). Snapshot strategy: one real `drizzle-kit generate` produced the correct end-state 0010 snapshot, then SQL hand-split into the 3-file chain + snapshot copied forward to 0011/0012 (keeps future `generate` diff-consistent). ⚠ `drizzle-kit migrate` does NOT auto-load .env.local — sourced DATABASE_URL for the migrate command only (the vitest DATABASE_URL warning does NOT apply to db:migrate). Commits 060d790 (T1 feat) + a27f05f (T2 fix). ⚠ gsd-sdk string-arg handlers no-op — metric/decision/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked.
- [Phase 06]: [06-02]: OCCUPANCY-PREDICATE FAN-OUT + createPendingHold PARAMETERIZATION (the single most dangerous cross-cutting concern of the phase). Widened EVERY app-side lazy-read occupancy predicate to the occupying set `{pending,confirmed,requested,approved}` (unexpired) in LOCKSTEP with 06-01's widened EXCLUDE — miss one and the calendar/search show a request-held slot as free (silent collision). Sites: `read-model.ts:108` `(status='confirmed' OR (status IN ('pending','requested','approved') AND expires_at > now()))` (covers search Stage-2 transitively — it reuses getAvailability), `units.ts` findOwnActiveHold + pickLowestFreeUnit (same widened predicate) + the dormant createBooking probe (widened to the full set, defense-in-depth). **In-tx stale sweep** widened: WHERE `status IN ('pending','requested','approved') AND expires_at <= now()`, terminal status `SET status = (CASE WHEN status='requested' THEN 'declined' ELSE 'cancelled' END)::booking_status, expires_at=NULL` — MIRRORS the 06-06 SLA cron's mapping (requested→declined, pending/approved→cancelled) so the in-tx-reclaim path and the cron never disagree on the terminal value (Warning-1). ⚠ **The `::booking_status` cast is load-bearing** — an all-string-literal CASE resolves to Postgres `text`, which cannot assign to the enum column (would raise "expression is of type text"); all three literals are long-committed enum values so the runtime cast is safe (NOT the 06-01 migration-time 55P04 problem). **NO email fired in-tx** — a Resend call inside the SAVEPOINT/rollback/retry tx is unsafe; the cron is the SOLE booker-email authority and the dropped email on the rare in-tx-reclaim edge is accepted bounded race A6. **createPendingHold PARAMETERIZED IN PLACE** (never forked — the 04-04 SAVEPOINT/40P01-outer-retry/own-hold-idempotency/23505-backstop machinery is untouched): `CreatePendingHoldInput += { holdStatus?: 'pending'|'requested', ttlMs?, bookingMode? }`, defaults `pending`/HOLD_TTL_MS(15min)/null so the instant call site (booking.ts placeHold) is byte-for-byte unchanged; the insert mints `holdStatus` + `now()+ttlMs` + the `bookingMode` D-61 snapshot when provided. The 06-04 request branch calls `createPendingHold(db,{ …, holdStatus:'requested', ttlMs: APPROVAL_SLA_HOURS*3.6e6, bookingMode:'request' })`. **The non-negotiable concurrent double-book race on requested AND approved slots is GREEN** (makeRacingClients, exactly-one-survivor) — but note it was ALREADY green off 06-01's widened EXCLUDE (the DB, not app code, arbitrates); the app-predicate widening is what the occupancy-occupied + createPendingHold-param + sweep-write-target tests actually turn green. TDD: RED e11e4c1 (6 failing) → GREEN d761cd6. Quick suite (booking+paymongo+payments) 113 green / 4 todo; availability+search 101 green; tsc + eslint clean on all 4 files. tests/booking/request-expiry.test.ts scaffolded with the DB-clock-manip harness + it.todo placeholders for the 06-06 SLA/payment-window cron. **BOOK-05/PAY-05 stay In-progress** (cross-cutting occupancy substrate only; request-to-book behavior ships in 06-04/06-06/06-07, per the 04-xx/05-xx precedent). **Downstream reminder still in force: any NEW read/occupancy predicate must mirror {pending,confirmed,requested,approved} or a requested/approved slot reads as free.** ⚠ Deviations (both Rule 2 correctness): the ::booking_status cast on the sweep CASE; wiring + testing the bookingMode snapshot persist (plan interfaces required it but the Task-1 behavior list didn't assert it). ⚠ gsd-sdk string-arg handlers still no-op — metric/decision/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked. Commits e11e4c1 (T1 test) + d761cd6 (T2 feat).

- [Phase 06]: [06-03]: FIVE LIFECYCLE EMAILS (D-66, first non-auth emails) + D-62 create-default flip + D-61 wizard copy. Extended src/lib/email.ts with `sendBookingConfirmed`/`sendRequestReceived`/`sendRequestApproved`/`sendRequestDeclined`/`sendNewRequestToHost` — thin plain-HTML sends (heading line + one body line + a single anchor CTA) each mirroring the existing sendVerificationEmail shape over the SAME `send()`/`escapeHtml()` helpers (never a new email stack — React Email stays uninstalled; branding/retry hardening is Phase 7, WR-04). **Every interpolated field is escapeHtml'd before markup** (space title, booker label, quoted total, reference, AND the CTA url) — WR-01/T-06-06 injection sink closed (a raw url in an href / raw title in HTML text is the tampering vector). The approval SLA + payment window render as config hour VALUES (`APPROVAL_SLA_HOURS`/`APPROVAL_PAYMENT_WINDOW_HOURS`, imported from @/lib/payments/config), never the internal constant names, so Phase-7 policy tuning flows through; time labels are the CALLER-composed `whenLabel` (already names the venue tz — the sends never format a time). One CTA without a caller url (declined → "Find another space") uses `process.env.BETTER_AUTH_URL` (the app-URL convention from auth.ts/paymongo-connect.ts), root-relative fallback. **Contract note for consumers (06-04 placeHold request branch, 06-05 webhook confirmed email, 06-06 SLA/payment-window cron, 06-07 approve/decline): call these FIRE-AND-FORGET (`void sendXxx(...)`) — T-06-07 — a Resend failure must never reject a webhook ACK or block a server action.** Signatures (args explicit + escaped): sendBookingConfirmed(to,spaceTitle,whenLabel,reference,bookingUrl); sendRequestReceived(to,spaceTitle,whenLabel,bookingUrl); sendRequestApproved(to,spaceTitle,whenLabel,totalLabel,payUrl); sendRequestDeclined(to,spaceTitle,whenLabel,opts?:{expired?}); sendNewRequestToHost(to,spaceTitle,whenLabel,bookerLabel,totalLabel,requestsUrl). D-62: `createDraftListing` (src/app/actions/listing.ts) insert default flipped `bookingMode: "request"` → `"instant"` (demand-first; doc comment updated; independent of but mirroring 06-01's DB SET DEFAULT — existing rows unaffected, none to backfill). D-61: wizard.tsx Step-5 FormDescription expanded to "You can change this anytime. It applies to new bookings — any requests already in progress keep the mode they started under." (no structural RadioGroup change; the loaded listing.bookingMode drives the default selection, now instant). crud.test.ts's booking-mode test rewritten to assert the instant create-default + a request toggle (the old test asserted the retired "request" default and would have broken — Rule 1). tsc + eslint clean on all touched files (the pre-existing wizard.tsx:198 form.watch() warning + address-autocomplete.tsx:110 error are untouched/out-of-scope); tests/listing 50 green; booking+paymongo+payments 113 green / 4 todo (email-mocking suites — no regressions). **BOOK-04/BOOK-06 stay In-progress** (leaf providers + a default flip only; the emails aren't invoked at any call site yet — behavior ships in 06-04/05/06/07, per the 04-xx/05-xx cross-cutting precedent). Deviations: only the Rule-1 crud.test.ts rewrite (the flipped default broke the old "request" assertion) — no scope creep. ⚠ gsd-sdk string-arg handlers (record-metric/add-decision/record-session) still no-op — metric row + this decision + stopped_at/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked, but update-progress wrote the phase-based percent (63) — reconciled to the plan-based 34/39 = 87% per prior convention. Commits 53e9b5c (T1 emails) + 2ef456f (T2 flip+copy).

- [Phase 06]: [06-04]: placeHold FORK on bookingMode + pay-on-approval REUSE (the entry-point fork that makes both booking modes real). `placeHold` (src/app/actions/booking.ts) adds `bookingMode` (+ host email / listing title / tz / city) to the EXISTING deriveBookable join so the mode is read SERVER-SIDE (T-06-09 — never a client flag) and FORKS after the bookability gate: **instant** → the byte-for-byte unchanged pending 15-min hold → `/book` pay page (BOOK-04); **request** → `createPendingHold(db,{ …, holdStatus:'requested', ttlMs: APPROVAL_SLA_HOURS*3.6e6, bookingMode:'request' })` (06-02's parameterized hold — a no-charge held request, D-63, NOTHING charged now) → `void sendRequestReceived(booker)` + `void sendNewRequestToHost(host)` FIRE-AND-FORGET (T-06-07; the `void` calls dispatch synchronously BEFORE the redirect throws, so both emails send) → revalidate listing + `/` → `redirect('/bookings/<id>')` (06-08 renders the `requested` surface). Host-email guest-pays amount = server-frozen `quotedTotalCents` read BACK from the just-minted hold (D-49, never recomputed); `whenLabel` composed in-action from the venue tz (date-fns + @date-fns/tz) as `{date}, {time} ({City} time)` — the email leaf providers never format a time (06-03 contract). **confirmBooking widened to pay-on-approval (PAY-05):** the non-pending guard AND the extend-hold UPDATE both accept `status IN ('pending','approved')`, and the extend is `expires_at = GREATEST(expires_at, now()+make_interval(mins=>PAYMENT_WINDOW_MINUTES))` so a fresh 24h approved window is NEVER shrunk to the 60-min instant window (Pitfall 6, T-06-10); owner-gate / idempotent short-circuit / rate-limit / null-quote guard / checkout-create UNCHANGED — pay-on-approval IS the Phase-5 hosted checkout. The `/book` page (listings/[id]/book/page.tsx) active-hold check widened from `pending` to `(pending OR approved)` unexpired so the approved booker lands on the exact Confirm & pay surface. tests/booking/request-lifecycle.test.ts gained the vi.doMock action harness (state-machine idiom) + 4 cases: request-mode mints `requested` + fires both emails + NO checkout; instant-mode unchanged; mode-flip independence (D-61 — flipping listing.bookingMode never mutates an in-flight `requested` row — keyed off booking.status not the live flag, T-06-12); confirmBooking-from-`approved` creates the checkout + GREATEST keeps the 24h window. **NO deviations — plan executed exactly as written.** tsc + eslint clean on all 3 files; request-lifecycle 16 green; booking+payments+paymongo 117 green / 4 todo (up 4 from 113 — instant state-machine + checkout-create unregressed). Threat register T-06-09..12 all mitigated. **BOOK-04/BOOK-05/PAY-05 stay In-progress** — the entry-point fork + pay-on-approval reuse ship here, but the request-to-book loop only closes with host approve/decline (06-07), the SLA/payment-window cron (06-06), and the request views (06-08); completion validated at the phase transition, per the 06-01/02/03 cross-cutting precedent. **Downstream reminder still in force: any NEW read/occupancy predicate must mirror {pending,confirmed,requested,approved} or a requested/approved slot reads as free.** ⚠ gsd-sdk string-arg handlers still no-op — metric row + this decision + stopped_at hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked, but update-progress wrote the phase-based percent (63) — reconciled to the plan-based 35/39 = 90% per prior convention. Commits c9d56b4 (T1 fork + confirmBooking) + b955145 (T2 /book page + tests).

- [Phase 06]: [06-05]: WEBHOOK CONFIRM WIDENING + BOOK-06 CONFIRMED EMAIL (pay-on-approval joins the ONE confirm writer). Widened the single-writer `checkout_session.payment.paid` confirm authority (D-57) in src/app/api/paymongo/webhook/route.ts from `status='pending'` to `status IN ('pending','approved')` — an instant hold confirms from `pending`, a pay-on-approval request from `approved` (06-04 lands the booker on the exact Phase-5 checkout while the hold is `approved`); BOTH → `confirmed` through the SAME writer (PAY-05). **WIDEN the WHERE, never add a 2nd confirm path** (T-06-13 — bookingId still derived from the VERIFIED reference_number alone). The `expires_at>now()` guard is still deliberately NOT re-imposed (Pitfall 4 — a paid-but-lapsed hold must still confirm; the GiST EXCLUDE, not the TTL, is the double-confirm authority). On the **≥1-row (GENUINE) confirm branch** the webhook `void`-invokes a new self-contained `sendBookingConfirmedEmail(bookingId)` helper: it joins booking⨝user⨝listing (booker email + title/tz/city), re-derives `fullDay` from the FROZEN quote (vs hourlyRate×hours, exactly as the reserve page), composes the venue-local `whenLabel` `{date}, {time} ({City} time)` (SC#2 — identical to the listing/reserve/placeHold surfaces), and `await sendBookingConfirmed(...)` inside its OWN try/catch — FIRE-AND-FORGET, OUTSIDE the 200 ACK path (T-06-15): a Resend/read failure logs `[EMAIL] booking_confirmed_send_failed` and can NEVER reject the webhook (a rejected ACK → PayMongo retries the already-confirmed event forever). **Chose a self-contained helper over an inline `void sendBookingConfirmed(...)`** so the fire-and-forget promise (incl. its DB read) stays fully off the ACK path AND a failure is swallowed at the source — the `void` at the call site is structurally incapable of surfacing an unhandled rejection. The email fires on the ≥1-row branch ONLY — never on a 0-row confirm (a replay is a 200 no-op via the paymongo_event dedupe BEFORE the confirm even runs; a gone slot routes to handleGoneSlot), so no duplicate/ spurious "confirmed" receipt. **handleGoneSlot (D-58), verifySignature, the paymongo_event dedupe, and handleRefund are UNTOUCHED** — a released/declined request that pays late (0-row confirm) is the pay-after-release race and is already covered by the SAME D-58 backstop that covers an instant gone slot (auto-refund on a refundable rail / operator-alert on QRPh → terminal `cancelled` → PaymentReversedState, Pitfall 3); NO request-specific refund logic added (T-06-14). webhook-payment-paid.test.ts +4 cases (seedBooking accepts `approved`/`requested` + a bookingMode; a `waitForConfirmedEmail` helper polls mockResend.sent() with vi.waitFor keyed on the booking's FIT- reference — the send lands as a background promise AFTER post() resolves, keyed by ref so a sibling test's leaked async email can't false-match): approved→confirmed captures the pay_…; confirmed email fires on instant (pending); confirmed email fires on pay-on-approval (approved); pay-after-release (released `cancelled` request pays late) → 0-row → handleGoneSlot auto-refunds once, stays terminal, NO confirmed email. **NO deviations — plan executed exactly as written.** tsc exit 0; eslint 0 errors on both files; webhook-payment-paid 12 green (8 prior + 4); paymongo+booking+payments 121 green / 4 todo (up 4 from 117 — instant confirm / gone-slot / refund / double-book unregressed). Threat register T-06-13/14/15 all mitigated. **BOOK-06/PAY-05 stay In-progress** — the confirmed email + widened confirm ship here, but the pay-on-approval loop only closes with the SLA/payment-window cron (06-06), host approve/decline (06-07), and the request views (06-08); completion validated at the phase transition, per the 06-01/02/03/04 cross-cutting precedent. **Downstream reminder still in force: any NEW read/occupancy predicate must mirror {pending,confirmed,requested,approved} or a request-held slot reads as free.** ⚠ gsd-sdk string-arg handlers still no-op — metric row + this decision + stopped_at hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked, but update-progress writes the phase-based percent — reconciled to plan-based 36/39 = 92%. Commits 697825c (T1 widen + email) + dfcf31e (T2 tests).

- [Phase 06]: [06-06]: REQUEST-EXPIRY INNGEST CRON (D-64, the dual-timer sweep — SLA auto-decline + payment-window auto-release). Added src/inngest/functions/request-expiry.ts, a near-clone of payout-sweep.ts (the SECOND async-scheduled cron in the repo): **queryExpired(dbConn)** = DB-clock now() sweep `WHERE (status='requested' AND expires_at<=now()) OR (status='approved' AND expires_at<=now()) ORDER BY expires_at ASC LIMIT 100` (explicit dbConn for test injection, mirrors queryDuePayouts); **expireOne(dbConn,row)** keyed off status — `requested` → `UPDATE booking SET status='declined', expires_at=NULL WHERE id=$id AND status='requested' RETURNING id` (status-scoped ⇒ IDEMPOTENT: a re-run flips 0 rows → `noop`, email never re-fires) + on a genuine ≥1-row flip the booker declined/expired email; `approved` → `… SET status='cancelled' … AND status='approved'` = the payment-window auto-release, SILENT (NO email, D-66/A3 — the booker chose not to pay). **The DB clock now() is the SOLE expiry authority (T-06-16)** — never a JS/client clock. The email flows through a self-contained `sendDeclinedNotice(dbConn,bookingId)` (join booking⨝user⨝listing → venue-local `whenLabel` `{date}, {time} ({City} time)` composed EXACTLY as the reserve/confirmed-email surfaces → `await sendRequestDeclined(email,title,whenLabel,{expired:true})`) wrapped in try/catch that logs `[request-expiry] declined_email_send_failed` and NEVER throws out of the step (T-06-17). **This cron is the SOLE booker-email authority**; its terminal mapping (requested→declined, approved→cancelled) is the CANONICAL one the 06-02 in-tx stale-hold sweep MIRRORS for a same-tx reclaim (Warning-1) but fires NO email (accepted bounded race A6). The 06-02 lazy read predicates ALREADY free a lapsed slot instantly between ticks — this cron only drives the VISIBLE flip + the email (Research Pattern 3 — do BOTH); nothing is refunded/voided (the freed declined/cancelled states are non-occupying, D-63). `requestExpirySweep` = a 2-arg createFunction singleton `{ id:'request-expiry-sweep', concurrency:1, triggers:[{cron:'TZ=Asia/Manila 15 * * * *'}] }` OFFSET to minute 15 so it never contends with the payout crons (sweep :00, reconcile :30 — Pitfall 4), per-row step.run; mounted in src/app/api/inngest/route.ts serve() functions[] ALONGSIDE payoutSweep + payoutReconcile (fail-closed prod INNGEST_SIGNING_KEY guard + runtime='nodejs' unchanged). tests/booking/request-expiry.test.ts (the 06-02 scaffold) filled: requested→declined+slot-freed(overlapping createPendingHold succeeds)+emailed-once; approved→cancelled+slot-freed+NO-email; idempotency(2nd expireOne=0-row noop, no duplicate email); selectivity(a live future-expires requested/approved row is never swept). **NO deviations — plan executed exactly as written** (the awaited self-swallowing notice helper is the same faithful realization of the fire-and-forget contract 06-05 used for its confirmed email). tsc exit 0; eslint 0 errors on all 3 files; request-expiry 5 green; booking+paymongo+payments 125 green (up 4 from 121 — the 4 former it.todos now real; no regressions). Threat register T-06-16/17 mitigated, T-06-18 accepted (unchanged serve guard). **BOOK-05/PAY-05 stay In-progress** — the SLA/payment-window cron ships here, but the loop only closes with host approve/decline (06-07) + the request views (06-08); validated at the phase transition, per the 06-01..05 cross-cutting precedent. ⚠ gsd-sdk string-arg handlers still no-op — metric/decision/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked (update-progress wrote the phase-based 63% — reconciled to plan-based 37/39 = 95%). Commits 17aab91 (T1 cron + mount) + cdec048 (T2 tests).

- [Phase 06]: [06-10]: GAP CLOSURE G-06-01 (CRITICAL) — the te-XOR-li webhook signature parser. `parseSignature` (src/app/api/paymongo/webhook/route.ts) required all three of t/te/li non-empty; real PayMongo signs ONE mode per delivery (te in TEST with empty li, li in LIVE with empty te — NEVER both), so EVERY real signature returned null → 400 before confirming, silently blocking the D-57 single-writer booking-confirm authority (PAY-05/BOOK-06/BOOK-04) on real payments. Proven live in the 06-09 UAT: a real paid PayMongo test checkout never confirmed; the recomputed HMAC matched the `te` digest exactly; manual replay to the correct endpoint returned 400 (parser, not network). FIX = a one-predicate widening `if (!parts.t || (!parts.te && !parts.li)) return null;` + a tolerant return `{ t: parts.t, te: parts.te ?? '', li: parts.li ?? '' }` — reject ONLY a missing header, a malformed segment, a missing `t`, or a BOTH-empty header. **verifySignature is byte-for-byte UNCHANGED** — it already loops `[sig.te, sig.li]` behind a `candBuf.length === expectedBuf.length` guard, so a tolerated empty candidate (0 bytes) can never length-match the 64-char HMAC hex and is silently skipped; no verify change was needed. **handleGoneSlot (D-58), handleRefund, the paymongo_event dedupe, and the confirm UPDATE `IN ('pending','approved')` are all UNTOUCHED** (surgical parser-only change). Closed the FIXTURE GAP at its source: extended the EXISTING `mockPayMongo.signWebhook` with a `mode: 'both'|'test'|'live'='both'` param (default keeps the legacy both-populated header so the ~12 existing callers are unaffected; `test`→`te=<sig>,li=`, `live`→`te=,li=<sig>`) — the both-populated fixture is exactly what masked G-06-01. Added 3 regression cases (tests/paymongo/webhook-payment-paid.test.ts): Case A te-only confirms a `pending` booking + `waitForConfirmedEmail` proves BOOK-06 on the te-only path; Case B li-only confirms an `approved` pay-on-approval booking + BOOK-06 on the li-only path; Case C a BOTH-empty header still 400s with no state change (T-06-SPOOF, spoofing protection intact). **NO deviations — plan executed exactly as written.** tsc exit 0; eslint 0 errors on all 3 files (5 pre-existing mocks.ts no-unused-vars warnings out of scope); webhook-payment-paid 15 green (12 + 3); tests/paymongo tests/booking tests/payments 134 green (no regressions). **G-06-02 is a NON-CODE re-UAT precondition**: register the PayMongo webhook at the FULL `/api/paymongo/webhook` path (not the tunnel root) before re-running the 06-09 UAT. **Phase 06 gap CLOSED in code but NOT yet verified** — the 06-09 human-verify UAT must be RE-RUN (with G-06-02 corrected) to clear the phase. ⚠ gsd-sdk update-progress over-counted (41/40=100%, completed_phases 6) — hand-reconciled to 40/41 = 98%, completed_phases 5; record-metric string-args no-op'd (metric row hand-written). Commits 39985d3 (T1 fix) + 06f31e3 (T2 test).

- [Phase 07]: [07-02]: SHARED WHEN-LABEL FORMATTER + BOOKING-STATUS DERIVATION (MANAGE-01/02, HOST-02 — the two pure modules Waves 2-4 all consume). **(1) The triplicate time formatter is dead.** `composeWhenLabel` existed VERBATIM in three places (`host-requests.ts:122-133`, `request-expiry.ts:144-155`, `host/requests/page.tsx:84-97` — the short `EEE, MMM d` variant); Phase 7 adds ~6 more time surfaces, so 07-RESEARCH § Don't Hand-Roll forbids a fourth copy. Extracted to **`src/lib/booking/when-label.ts`** — `composeWhenLabel` (`EEEE, MMM d`) + `composeWhenLabelShort` (`EEE, MMM d`) over a structural `WhenLabelInput`, PURE/isomorphic (no client and no server directive, so RSCs + server actions + Inngest functions all import it, exactly like `commission.ts`). It owns three rules: times render in the VENUE tz via `tz(input.timezone)` never the viewer's (D-105/C5); `fullDay` is NOT persisted so it is re-derived from the FROZEN quote (D-49) biasing to hourly on an exact coincidence; the ` ({City} time)` suffix is omitted entirely when city is null. All three duplicates DELETED and importing. `host-requests.ts` keeps a one-expression `whenLabelInput(row)` ORM-row projection (called from both approve + decline) — the FORMATTER is shared, only the projection is local. **(2) The single booking-status source of truth.** `src/components/booking/booking-status.ts` — `deriveDisplayStatus(status, endsAt, now)` implements **D-102**: a `confirmed` booking whose `endsAt` has passed derives as `completed` AT READ TIME, never stored, so the `completed` enum value stays unused and the GiST EXCLUDE predicate is UNTOUCHED. Boundary is `endsAt <= now` (matching the half-open `[)` window). `deriveBookingStatusView(status, endsAt, now, side)` = an EXHAUSTIVE switch with NO fallthrough clause (a new `booking_status` value becomes a compile error here) emitting the 07-UI-SPEC badge matrix with side-specific labels (`approved` → booker `Approved — pay now` / host `Awaiting payment`; `requested` → booker `Awaiting host` / host `Requested`). `success` tone is reserved for `confirmed` ALONE (approved is NOT paid yet). **D-79 enforced structurally**: no label carries a currency symbol or a digit — `BookingStatusBadge` (`booking-status-badge.tsx`, per-display-status `BADGE_RECIPES`, icon + text never colour-only) returns ONLY the badge and the refund line (`₱500 refunded`) is a CALLER-composed sibling. **⚠️ CONTRACT for downstream callers: `now` is a REQUIRED parameter and must come from the DB clock (`SELECT now()`), never `Date.now()`** — the module deliberately cannot source it itself (T-07-10), so a client clock can never move a booking between the Upcoming/Past tabs independently of the SQL partition. **2 deviations, both auto-fixed:** (a) Rule 2 — Task 1 is `tdd="true"` with a `<behavior>` block but listed NO test file, so `tests/booking/when-label.test.ts` was added as the RED gate (9 assertions pinning the EXACT strings, which is what makes "byte-identical refactor" checkable rather than assumed); (b) Rule 1 — three grep acceptance criteria (`use client`→0, `default:`→0, `Full day`→1) were violated by explanatory HEADER COMMENTS, not code (the pattern file this clones, `payout-ledger-status.ts`, would fail its own criterion) — comments reworded to preserve intent while making the assertions literally true. **⚠️ DB-BLOCKED VERIFICATION — NOT passed, do not treat as green:** the local Postgres container is DOWN (Docker engine stuck initializing), so Task 1's `<verify>` (`npx vitest run tests/booking tests/payments`) did NOT execute — 10 of 14 files fail at `ECONNREFUSED :5432`. Most valuable unrun assertion: `tests/booking/request-expiry.test.ts:138` asserts `{status:'declined', emailed:true}` and `sendDeclinedNotice` SWALLOWS its own exceptions, so a throw introduced by the new projection would surface THERE and nowhere else; `request-lifecycle.test.ts` covers the approve/decline paths likewise. The refactor is proven as a PURE FUNCTION (exact strings pinned) but the integration path (real DB row → projection → Inngest email step) is unproven. Also unrun and pre-existing: `tests/auth/rate-limit.test.ts` + `secret-config.test.ts` time out because Better Auth boot blocks on the same DB. **Non-DB gates ALL green:** tsc exit 0 (whole project), eslint clean on all 5 touched source + 2 test files, when-label 9/9, booking-status 16/16, pricing + commission unregressed; all 13 acceptance greps pass. Full suite with DB down: 20 files passed / 39 failed, every failure a connection error or connection-induced timeout. **SUMMARY Self-Check marked FAILED** (not for a missing artifact — every file and commit verified present — but because a required gate could not run; the exact owed commands are listed under "Unverified — Blocked on Database"). ⚠ gsd-sdk `state advance-plan` errored ("Cannot parse Current Plan or Total Plans in Phase") because Current Position read `Plan: Not started` — position/metric/decision hand-written and the block reformatted to the parseable `Current Plan:` / `Total Plans in Phase:` shape. **Also noted: 07-01 shipped code (c8a115a/d1344c7/d283f92) but has NO SUMMARY.md on disk and its migration is NOT applied to the live DB.** Commits 2766f7b (T1 RED) + bd07474 (T1 refactor) + ad71d54 (T2 RED) + 0a205be (T2 feat).
- [Phase 07]: [07-03]: THE THREE PURE MONEY MODULES (BOOK-07/PAY-06 — every peso of a cancellation flows from these; Plans 08/09/11/15/16 all consume them). **(1) `src/lib/payments/cancellation.ts` — the D-68 refund ladder.** `LADDER` is `Record<CancellationTier, Rung[]>` with rungs DESCENDING by minHours (flexible 12h/100%; standard 24h/100% → 6h/50%; strict 48h/100% → 24h/50%) and `quoteRefund` takes the FIRST satisfied rung, boundary INCLUSIVE (`hoursToStart >= minHours`, so at exactly 24h a Standard booker still gets 100%). Nothing satisfied — which includes every negative hoursToStart, i.e. a session that already started — falls through to 0. **The rounding discipline is the load-bearing part:** ONE `Math.round` for `spaceRefundCents`, then `retainedSpaceCents = spacePriceCents - spaceRefundCents` by SUBTRACTION — rounding both independently can lose or invent a centavo, the exact bug class `commission.ts` guards against (T-07-14). Proven by a property test sweeping 11 prices × 3 tiers × every rung boundary (−0.1/exact/+1) asserting `spaceRefund + retained === spacePrice` exactly. `serviceFeeRefundCents` is EXPLICITLY `0`, never omitted, so the D-78 itemised breakdown renders the ₱0 line + its "Service fees aren't refunded" helper unconditionally — INCLUDING on a 100% refund, the one place a booker could be surprised (D-74); `totalRefundCents === spaceRefundCents` (fee excluded). `retainedSpaceCents` is the post-cancellation payout GROSS (D-69). **⚠️ CONTRACT: `now` is a REQUIRED parameter and MUST be the Postgres `now()` value** — the module reads NO clock (`grep -c "Date.now()"` → 0, and the comment is phrased "never a JS-side clock read" precisely so that grep stays a real guard rather than tripping on its own documentation). Rung boundaries are SHARP: if the preview RSC and the cancel action straddle one on two different clocks, the booker is shown one number and given another, which breaks SC#2 (T-07-13). Also ships `rungBoundaries(tier, startsAt)` → raw boundary INSTANTS for D-81's concrete-date disclosure ("Free cancellation until Thu 3 Jul, 8:00 PM") performing ZERO formatting (the caller localises via @date-fns/tz + 07-02's `composeWhenLabel`), and `tierOrDefault` with a documented Flexible fallback for pre-Phase-7 NULL tiers (D-62 precedent). Hours-to-start is absolute-instant epoch math, hence DST-safe by construction — pinned by a test spanning a DST transition so a future switch to wall-clock date math can't silently award the wrong rung. **(2) `src/lib/payments/service-fee.ts` — D-74/D-76.** A structural clone of `computeCommission`: same header shape, same two guards, ONE rounding, and `allInCents` by ADDITION not a second rounding — so the D-75 all-in browse number is byte-identical to the sum of the two D-78 breakdown lines and never goes up between browsing and paying. **COMPOSITION RULE recorded in the header: `quoteWindow` (src/lib/booking/pricing.ts) KEEPS returning the SPACE price** — it is pure over the listing's rates and must NOT learn about platform fees; the fee is composed at the CALLER (Plan 08's hold-creation path), preserving the Phase-4 pure-pricing seam and leaving its existing tests valid. Supersedes the BOOKER-facing half of D-50 only; the host-side 10% commission is unchanged. **(3) `src/lib/payments/refund-rail.ts` — the ONE branch point.** `isApiRefundable` + `REFUNDABLE_RAILS`, extracted from the inline Set at `webhook/route.ts:42` (now deleted; the call site is `isApiRefundable(method)`, behaviour unchanged) — exactly one rail Set literal exists in `src/`. Fails CLOSED: an absent/unknown rail is non-refundable, so an unrecognised method routes to the D-58 operator-alert path rather than a `createRefund` that would 4xx and strand a booker's money. **If the Plan-16 test-mode probe refutes D-58, this file is the ONLY one that changes and the entire D-72 workstream (bank-details form, instapay path, PH DPA exposure, payout-redirection threat, failed-transfer recovery UX) is deleted with zero rework.** All three modules are PURE/isomorphic — no client and no server directive — so the cancel-review RSC, the cancel server action and the Inngest notification composer all import them. **NO functional deviations; plan executed as written.** Two plan-text inconsistencies resolved and documented in SUMMARY: (a) the `grep -c "use client\|use server"` → 0 criterion is unsatisfiable because the SAME task mandates the isomorphic note containing those literals — the exemplar `commission.ts` returns 1 for the identical grep, so the action text won and the substantive property was verified directly (`grep -cE '^\s*"use (client|server)"'` → 0 for all three); (b) the `Date.now()` → 0 criterion conflicted with the comment wording the plan dictates — the criterion won there because it is the load-bearing one, and the meaning was preserved by rephrasing. One judgement call beyond the plan's `<interfaces>`: `serviceFeeCents` is guarded too (the `<action>` text says both, the interfaces block and the 07-RESEARCH reference impl guard only the price) — an unguarded money parameter in a module whose output is POSTed to PayMongo is exactly what the throw-don't-freeze idiom exists to prevent. Rail-predicate assertions live inside `service-fee.test.ts` rather than a third file, to respect the plan's declared `files_modified`. **TDD gates clean:** RED→GREEN per task, each RED verified failing before commit, no test passed unexpectedly; REFACTOR skipped both times (the GREEN implementations were already in the cloned target shape). **ALL gates green:** tsc exit 0; eslint clean on all 5 files; service-fee 17 green; cancellation 34 green; `tests/payments` 9 files/96 green (incl. untouched commission.test.ts); `tests/paymongo` 5 files/37 green (webhook regression gate); **full `npm test` 61 files / 452 tests, exit 0** — up from Wave 1's 59/401 with zero existing assertions changed. Note this run also clears 07-02's DB-blocked verification debt: its `tests/booking` + `tests/payments` gates now execute and pass, and the `secret-config.test.ts` flakiness 07-01 logged did not recur. All 16 acceptance criteria pass (see SUMMARY table). Threat register T-07-11..14 mitigated, T-07-15 accepted. **Downstream reminders: Plan 08 must compose the fee at the caller (not inside `quoteWindow`) and write `spacePriceCents`/`serviceFeeCents` to the booking row; Plans 09/11 must pass DB `now()` and read the tier from `booking.cancellation_policy` via `tierOrDefault`, NEVER from the listing (a JS clock compiles fine — that is the T-07-13 failure mode); Plan 04 must note that under D-74 `quoted_total_cents` now INCLUDES the service fee, so using it as the payout basis would pay the host commission on platform revenue.** Commits 87ea79e (T1 RED) + 098866d (T1 GREEN) + a900a09 (T2 RED) + 1617ec1 (T2 GREEN).
- [Phase 07]: [07-04]: PAYOUT LEDGER CORRECTNESS (PAY-06 — the three-way collision between Phase 7 money and the shipped payout machinery, closed before any UI reads a payout figure). **(1) Finding 1 — retained cancellations are now swept.** `queryDuePayouts` widened to `b.status = 'confirmed' OR (b.status = 'cancelled' AND COALESCE(b.retained_space_cents, 0) > 0)`. D-69s claim of zero new mechanism was false: a cancellation sets status=cancelled and the old confirmed-only predicate meant a partially-refunded host was NEVER paid their share of the retained amount. This is a PAYOUT predicate only — booking.status, the GiST EXCLUDE and both lazy-expiry sweeps are untouched, cancelled stays NON-OCCUPYING, and payout timing stays anchored to the sessions ORIGINAL ends_at + 24h (preserving the D-55 hold and a dispute window). A HOST cancellation yields retained = 0, so the > 0 test correctly gives them no payout AND the D-71 fee. **(2) Finding 2 — the payout basis is the SPACE price.** `DuePayout.quotedTotalCents` is GONE; the type now carries `payoutGrossCents: number | null` = `COALESCE(retained_space_cents, space_price_cents)`. The all-in charged-total column name no longer appears ANYWHERE in payout-sweep.ts, comments included (grep returns 0), documented in the header as a deliberate T-07-16 tripwire spelled in two pieces so it does not trip on its own docs. **(3) Finding 3 — kind scoping is grep-complete.** `AND kind = 'payout'` on the sweep join/release/failure/rollback, the reconcile queryProcessingLedger + both reconcileOne UPDATEs + the stuck-held alert, the /host/earnings ledger SELECT, and — a site the PLAN did not list — the PayMongo refund webhooks two hostPayoutLedger queries (a booker refund would otherwise flip the HOSTs outstanding cancellation debt to refunded, silently forgiving it). `summarizePayouts` left PURE by design: it sums the rows it is handed and the feeding query is scoped instead. **(4) D-71 netting.** After the claim and the wallet correlation, payOne computes outstanding over unrecovered host_cancel_fee debits, clamps `deduction = Math.min(outstanding, netCents)` so transferAmt >= 0 by construction, and applies it oldest-first via a window-function waterfall. transferAmt === 0 short-circuits BEFORE any PayMongo call and settles the row paid with transfer_id NULL, returning `settled-by-netting`. /host/earnings surfaces unrecovered debt as a muted line so a returning host is never surprised. **THREE deviations, all money bugs in the plan itself, all auto-fixed and traced in SUMMARY:** (a) Rule 1 — the plan orders netting BEFORE the wallet lookup, but the no-wallet branch DELETEs the claim, so any recovery already applied would be permanently forgiven (host overpaid, fee written off, silently); netting moved AFTER wallet resolution, preserving the after-the-claim property that makes the racing-clients test meaningful. (b) Rule 2 — T-07-20s stated mitigation is wrong in the RETRY direction: debit 30000 vs net 90000, attempt 1 recovers fully and drops out of the predicate, transfer throws, attempt 2 re-derives outstanding = 0 and pays the full 90000 — the fee fully forgiven by a retry. Fixed by memoising the applied deduction on the payout rows recovered_cents (returned by the claims RETURNING) and reusing it; the waterfall UPDATE and the memo UPDATE are ONE data-modifying CTE chain so they commit atomically. (c) Rule 2 — `booking.space_price_cents` is NULLABLE and NOTHING writes it yet, so payOne now guards `Number.isInteger` and fails CLOSED (alert + skip + claim nothing + move no money, new `skipped-no-basis` variant); a COALESCE fallback to the charged total was DELIBERATELY REJECTED because it would work today but silently mask exactly the Finding-2 leak later. **⚠️ HANDOFF TO 07-08: every booking created from now until 07-08 freezes the price split will NOT pay out** — intended and visible, not a regression. **07-11 must write the debit as kind=host_cancel_fee / state=held / NEGATIVE gross+net / commission 0 / recovered 0, capped at booking value AT WRITE TIME.** Tests: 8 new sweep cases (retention sweep, host-cancel produces nothing, fee-exclusion asserting gross !== charged total, netting floor, zero-transfer suppression, negative-clamp with residual carried forward, concurrent cancel x sweep via makeRacingClients, null-basis fail-closed), 1 reconcile regression VERIFIED to fail when the kind scope is removed, and 2 earnings cases added beyond the plan (Upcoming totals 234000 not 204000 with a debit present). **ALL gates green:** tsc exit 0; eslint clean on 9 files; tests/payments 105 green (was 96); tests/availability 85 green (T-07-21, no occupancy predicate changed); **full npm test 61 files / 463 tests, exit 0**. Two acceptance-criteria greps are unsatisfiable-as-written and documented rather than gamed (a header comment legitimately repeats the renamed label; the multi-line-SQL grep cannot span statements — all 16 hits inspected and scoped). Commits 30a665a + 2832bbd + 6f85e92 + 0f72d5c.
- [Phase 07]: [07-05]: LIFECYCLE EXPIRY CORRECTNESS (BOOK-07 / D-93/94/95/96/98/100 — the gap was wider than reported and is now closed at all three write sites). **(1) D-94: no hold outlives its own session.** `expires_at = LEAST(now() + window, starts_at)` on the instant hold and the request SLA (`units.ts`) and on the approval payment window (`host-requests.ts`). Every one is a SQL expression evaluated by Postgres, so the DB clock is genuinely the sole authority; `grep -c "Date.now()" src/lib/availability/units.ts` returns **0** and the header says so explicitly, phrased to avoid tripping its own grep. In `units.ts` the expression also sits INSIDE the SAVEPOINT retry loop, so it is re-evaluated per attempt rather than captured once. Proven by a `Date.now` monkey-patch test: a JS clock skewed one hour into the future does not move the persisted value. **(2) D-96 proportional split.** The request path is `LEAST(now() + GREATEST(MIN_APPROVE_WINDOW_HOURS, LEAST(ttl, (starts_at - now())/2)), starts_at)` — a request 4h out gives the host 2h and the booker ~2h, NOT host 3h59m and booker one minute. Consequence for downstream: **a request SLA is no longer a flat 24h** (25h out → 12.5h), so `PRE_SLA_REMINDER_HOURS` (6) can be unreachable — 07-RESEARCH Pitfall 9 wants a range-based due query and a no-send assertion. `ttlMs` now defaults PER MODE (`APPROVAL_SLA_HOURS` for a request, `HOLD_TTL_MS` for instant) so an omitted value cannot mint a 15-minute approval SLA. **(3) D-93 approve guard, folded into the existing WHERE.** `AND starts_at > now() + make_interval(hours => MIN_APPROVE_WINDOW_HOURS)` joins the status + SLA checks in the SAME atomic UPDATE, so a too-late approve claims 0 rows and falls through the PRE-EXISTING calm `NOT_PENDING` path — no new error branch, no new failure shape, and unraceable (T-07-24). The 0-row path records `decline_reason = 'too_close_to_start'` via a separate status- and start-time-scoped non-throwing UPDATE for the composer to read. `confirmBooking` refuses post-start at CHECKOUT INITIATION, sourcing `now` from `SELECT now()`. **(4) ⚠️ The payment webhook is UNCHANGED and must stay that way (D-57 / Pitfall 4).** `grep -c "starts_at" src/app/api/paymongo/webhook/route.ts` returns 0 and the file diff is 0 lines. It confirms on `status IN ('pending','approved')` ALONE because PAYMENT IS THE CONFIRM AUTHORITY; adding a start-time condition there resurrects the exact failure 05-04 was built to prevent (money taken, booking unconfirmable). A **mutation-verified** `D-57 GUARD` test in `webhook-payment-paid.test.ts` proves a post-start payment still confirms — the regression was introduced deliberately to confirm the test fails, then reverted. The do-not-do-this note is recorded beside the new guard in `host-requests.ts`. **(5) `SlotState` gained `too_soon`** (D-98/D-100) — a READ-MODEL DISPLAY STATE ONLY: not a booking status, in no occupancy predicate, GiST EXCLUDE and both lazy-expiry sweeps untouched (T-07-26, gated by 103 green availability tests). Derived AFTER the freeUnits check so it means exactly one thing (free but not yet bookable) and no existing classification moves; `DayAvailability` now carries `bookingMode` so the picker's notice copy cannot disagree with the enforced thresholds. **`SlotPicker` gained a REQUIRED `mode` prop.** Chip treatment unchanged — muted, struck-through, aria-disabled, never red. **⚠️ SEARCH BEHAVIOUR CHANGED:** `searchListings` keeps candidates on `state === 'available'`, so a slot inside its notice window no longer counts. **TWO deviations, both auto-fixed:** (a) **Rule 1 — the D-42 trap on the units-exhausted exit.** The concurrent-same-key idempotency test started failing intermittently (2/5 loaded runs; true pre-plan baseline clean 6/6). Root cause: the own-hold pre-check and the find-free probe are separate statements, so under READ COMMITTED a concurrent submit of the caller's OWN key/window can commit between them — the only unit then reads occupied BY THE CALLER'S OWN BOOKING and `NoUnitAvailableError` mapped to a false "just taken". The 23P01/23505 handlers already re-checked own-hold; the exhaustion path reaches its exit WITHOUT attempting an insert, so it never hit them. Fixed by routing both exhaustion exits through an `exhausted()` helper that re-checks own-hold first (a genuine loser still gets the honest "just taken"; the EXCLUDE stays the sole arbiter). The lead-guard was ALSO moved onto the existing listing SELECT as a computed column so this plan adds ZERO round trips to the raced transaction. 15 consecutive loaded runs green after. (b) **Rule 3 — `tests/auth/secret-config.test.ts` cold-import timeout** (out of `files_modified`, see the note above). **Plan-text deviations, documented not gamed:** `tests/booking/host-requests.test.ts` did not exist and was created (approve coverage lived in `request-lifecycle.test.ts`); `HoldSuccess.expiresAt` is `Date | null` not `Date` (a replayed `confirmed` booking has no hold expiry — `Date` would permit a silent Invalid Date, the exact Pitfall-8 failure); the D-96 floor is proven at its binding boundary (2h out → 1h SLA) because a sub-floor input is unreachable through `createPendingHold`; boundary cases are placed 2s past nominal because the guard is a strict boundary and `now()` advances during the round trip. **Pitfall 8 audit: no countdown was broken** — both countdown surfaces read `booking.expiresAt` from the DB in their own RSC SELECT, never from the action's return; the returned value is now populated anyway and asserted byte-identical to the row. **ALL gates green:** tsc exit 0; eslint clean on all 9 files; tests/availability 103; tests/booking 82; tests/paymongo 38; **full `npm test` 63 files / 487 tests, exit 0** (red on 3 consecutive runs before deviation (b), green on both after). **Downstream: D-99 (cap-shortened SLA shows a reason) is NOT implemented here** — this plan produced the varying deadlines D-99 explains, but `RequestCountdown` still renders them bare. The 07-04 `space_price_cents` gap is untouched as instructed; nothing here writes it and no conflict was observed. Commits 608c3cc + 64f2eff + 8379c45 + e68047d + f6543bb + 103366b.
- [Phase ?]: 07-07: one fitout/notify event fans out to a durable notification row (step 1) then an email (step 2) — memoized separately, so an email retry can never duplicate the row (D-91)
- [Phase ?]: 07-07: emitNotify swallows transport errors by design — a notification must never fail a money/state action that already committed (MANAGE-03); the dropped-event gap is documented as accepted, matching Phase-6 A6
- [Phase ?]: 07-07: the notification payload is the SOLE input to both channels, so email-required fields live in the payload rather than a parallel structure (D-91 sufficiency)
- [Phase ?]: 07-08: cancellation tier snapshotted from the IN-TRANSACTION listing read, closing a retier race the plan's caller-threading design left open
- [Phase ?]: 07-08: fullDay is re-derived from the frozen space price, never the all-in charged total (D-74 makes the latter structurally unequal to rate x hours)
- [Phase 07]: Booker cancellation RECOMPUTES the refund at confirm time — There is no signed quote token, so honouring the previewed figure would mean trusting a number that reached the server through the client. Time only moves toward the session, so a rung crossed between preview and confirm can only LOWER the refund. The review page discloses the concrete instant the rung changes (D-81) so the recompute is disclosed, never sprung.
- [Phase 07]: booking.payment_method persisted at confirm (migration 0015) — isApiRefundable fails closed. The rail was only ever resolved inline from a live webhook event, but a cancellation happens days later with no event in hand — so without a stored rail every cancellation refund would have been judged unrefundable and routed to the operator-alert path, and no money would ever have moved. Nullable, backfill-free, absent from the GiST EXCLUDE predicate.
- [Phase 07]: readDbNow() is the only display-side DB-clock reader — Probed live: a bare SELECT now() through db.execute returns a STRING, not a Date. The as-unknown-as cast satisfies tsc, eslint and next build and then throws on .getTime() with the first real row. This supersedes the literal snippet 07-09-PLAN mandated.
- [Phase ?]: 07-10: lifecycle sends emit fitout/notify post-commit — duplicate suppression lives in each action's status-scoped UPDATE WHERE, not in the notify layer
- [Phase ?]: 07-10: notification payload hrefs are ABSOLUTE — one payload string feeds both channels (D-91); a root-relative href is a dead link in an email client
- [Phase ?]: 07-10: auth.ts's two fire-and-forget sends are deliberately NOT migrated — they carry bearer secrets and the void is a timing-side-channel defence
- [Phase ?]: 07-11: host cancel refunds 100% including the service fee; the tier is deliberately not consulted (D-70)
- [Phase ?]: 07-11: the D-71 host-cancel fee is capped at the booking value at WRITE time, never at netting time
- [Phase ?]: 07-11: the auto-block carries reason='host_cancellation' and removeBlock refuses to delete it (anti-resell)
- [Phase 07]: [07-11]: HOST-INITIATED CANCELLATION (HOST-02/PAY-06 — ROADMAP SC#3 now has a product path). **`cancelBookingAsHost(bookingId, reason)`** fires all four D-70/D-71 consequences: (1) the booker is refunded **100% of the all-in charged total INCLUDING the D-74 service fee** — the one case in the system where the non-refundable fee IS returned, because the booker did nothing wrong and the platform absorbs the gateway cost; **`quoteRefund` is deliberately NOT called and the tier is NOT consulted** (a strict-tier booking cancelled 1h out still refunds 100%, proven at the exact rung that would award a BOOKER 0%). (2) `recordAudit({action:"host_cancel_booking"})` against the host. (3) the freed window is **auto-blocked on the same listing AND unit** for the exact `[startsAt, endsAt)` range with `reason='host_cancellation'`, and `removeBlock` (in **`blocks.ts`**, NOT `availability.ts` — the plan misnamed the file) refuses to delete it via `IS DISTINCT FROM` **inside the DELETE's own WHERE**; `<>` would have been a live bug since `reason` is nullable and `NULL <> 'x'` is NULL, which would have made every ordinary reason-less block undeletable. (4) a **signed debit** `kind='host_cancel_fee'`, gross=net=−fee, commission 0, recovered 0, state `held`, **capped at the booking value AT WRITE TIME** (`Math.min(HOST_CANCEL_FEE_CENTS, spacePriceCents)`), with `ON CONFLICT (booking_id, kind) DO NOTHING` as the at-most-once lock and deliberately NO app-level pre-check. `retained_space_cents = 0` is what makes 07-04's sweep predicate exclude the booking — writing NULL would have fallen through to `COALESCE(retained, space_price)` and paid the host the full space price, the exact inversion of the consequence. **This plan writes the FIRST REAL `host_cancel_fee` row in the system's history**, so it is the first that could check 07-04's kind scoping against a debit the PRODUCT produced rather than a fixture: proven that the payout sweep still pays correctly and nets ₱1,800−₱300=₱1,500, that `alertStuckHeld` returns 0 with a real debit aged 200h past the 48h threshold, and that `/host/earnings`' own kind-scoped read and `summarizePayouts` are unaffected while the debt surfaces through its separate outstanding query. **THREE deviations, all auto-fixed:** (a) Rule 1 — the plan's mandated `readDbNow` call on the host path is DEAD CODE: the path evaluates no rung and nothing consumes a JS Date, so it was omitted with a comment stating that Postgres `now()` inside the UPDATE's own WHERE is the STRONGER position (it cannot be raced apart from the status check). (b) Rule 2 — the plan sequences the auto-block and debit INSERTs as bare statements AFTER a flip that has already committed; a throw from either would have 500'd a cancellation that in fact succeeded and **skipped the refund entirely**, leaving the booker's money uncollected with no alert. Each consequence is now individually try/caught into a `needs_attention` audit + `[HOST_CANCEL_ALERT]` line. (c) Rule 3 — `removeBlock` lives in `blocks.ts`; `availability.ts` is a read-only public fetcher with no mutation at all. **⚠️ THE MUTATION CHECK FOUND A REAL GAP.** Removing the host owner-gate let our host genuinely cancel another host's booking — refunded ₱1,050, blocked their slot, charged them ₱300. The host path had only ONE ownership layer where the booker path has two, because host ownership lives on the LISTING and so cannot be a bare column predicate; the plan's SQL simply omitted it. Fixed with `AND EXISTS (SELECT 1 FROM listing l WHERE l.id = booking.listing_id AND l.host_id = ...)` in the same WHERE, after which the same mutation blocks the WRITE and only leaks the denial string — the two-stage result proving both layers are independently meaningful. **Also fixed the 07-10 assigned defect:** `cancel-booking.ts` emitted root-relative hrefs, dead links in both its emails; now absolute via the EXISTING `BETTER_AUTH_URL` convention (no new env var), with regression assertions on BOTH the host and booker paths. **UI (D-80/07-UI-SPEC §4):** `HostCancelDialog` — two panes in one dialog, required reason select (5 options) gating pane 2, required acknowledgment checkbox gating confirm, both buttons neutral (`outline`/`ghost`), zero coral, no `variant="destructive"`; the fee arrives as a SERVER-CAPPED prop and the component never caps or computes. `/host/bookings/[id]` re-gates session + `canHost`, owner-scopes with `listing.host_id` in the WHERE, bare `notFound()` for missing and not-mine alike, kind-scoped payout join, cancel entry point below a `Separator` (D-104). **ALL gates green:** tsc exit 0; eslint clean on all 6 files; `npm run build` exit 0 with `/host/bookings/[id]` present; host-cancel 16/16; payments+booking+security+availability 37 files/386; **full `npm test` 73 files / 597 tests, exit 0** (was 72/581). Four acceptance-criteria greps documented rather than gamed (two count an explanatory comment alongside the one real statement; `availability.ts` is 0 by design; `Keep booking` is 2 because each pane needs its own back button). **Downstream: any new `host_payout_ledger` query MUST carry `AND kind='payout'` — there are now real negative-`net_cents` rows in production data. Any new `availability_block` delete path MUST carry the `host_cancellation` refusal or D-70's anti-resell consequence is defeated through it. Plan 13's reminder queries must exclude cancelled bookings. Every notification `href` must be absolute.** Commits df30010 + 59c08ff + 8df752f.
- [Phase ?]: 07-12: a lapsed approval is recovered by INSERTING a new hold, never by flipping a terminal booking back — the GiST EXCLUDE is the only mechanism that re-adjudicates occupancy
- [Phase ?]: 07-12: the D-97 lapse guard reads 'holds no live window' (expires_at IS NULL OR <= now()) because both retirement paths CLEAR expires_at
- [Phase ?]: 07-12: a non-zero refund on the booking detail page always reads 'on its way' — there is no settled-refund signal on a booking row, so 'Refunded' would violate D-57
- [Phase ?]: 07-12: no stable idempotencyKey on re-request — booking_idem_uq is partial-UNIQUE over all time and would 23505 into a 500 once the first re-request itself lapsed
- [Phase ?]: 07-13: reminder at-most-once is the booking_reminder UNIQUE(booking_id, kind) claim, never Inngest's 24h dedupe TTL
- [Phase ?]: 07-13: a reminder whose offset instant predates the booking is UNREACHABLE and sends nothing — a range guard alone would fire it immediately
- [Phase 07]: [07-16]: QRPh verdict settled by OBSERVATION — the 2026-07-23 test-mode probe returned HTTP 400 "Refunds are not allowed for payments with source type qrph." → D-58 confirmed, D-72 built (Branch B). The raw evidence (session/payment ids, verbatim error bodies, A3-BLOCKED status) lives in refund-rail.ts, the single branch point.
- [Phase 07]: [07-16]: refund destination is collect-and-never-store — validated (shape + live-BIC set) BEFORE the flip, bound to the owner-gated flow, amount server-frozen; only transferId + masked last-4 persist (audit meta). Transfer failures log no error content (provider details can echo the destination) and never retry with the same reference_number.
- [Phase 07]: 07-18: cancelled_by-aware status derivation — deriveDisplayStatus/deriveBookingStatusView take an OPTIONAL cancelledBy; declined+booker remaps to cancelled (a booker cancel is not a host decline), NULL/host/system keep declined. Exhaustive switch kept (no default clause) so the add-a-status compile-gate stays intact. — T8 UAT gap: a booker-cancelled unpaid hold was wearing the host-decline copy
- [Phase 07]: 07-18: declinedCopy(cancelledBy) is the SINGLE booker-vs-host selection point for the /bookings/[id] declined landing — parameter-free strings, venue/time rendered as a sibling line (the two-line cancelled-branch layout); the host-decline copy is structurally-equivalent, not byte-identical. — Callers must never re-implement the conditional inline
- [Phase 07]: 07-18 (T6): BOTH /host/bookings surfaces link to /host/bookings/[id] — the DEFAULT desktop <Table> Space-cell link (load-bearing half, where the tester hit it) AND the mobile-card overlay anchor; RequestActions lifted relative z-10 so Approve/Decline survive. SC#3 now reachable without typing a booking UUID. — 07-06 left both linkless; 07-11 built the detail page but wired neither
- [Phase ?]: 07-19: checkout cancellation summary leads with the best rung STILL OPEN for this booking (bestFutureRungIndex, strict >); display-only, refund recomputes at cancel time
- [Phase ?]: 07-19: a replayed request-mode placeHold notifies nobody (!res.replayed guards the emission pair; revalidate/redirect stay outside) — mirrors re-request.ts:299
- [Phase 07]: T4-hours closed with a discoverable Availability link on the Your-listings card (availabilityHref prop to /host/listings/[id]/availability), NOT a hard operating-hours publish gate — the forcing option touches the two-place publish gate and strands hours-less published listings, deferred as a product decision.
- [Phase 07]: formatMoney pinned to minimumFractionDigits 2 so money renders two decimals everywhere from the one shared formatter; blockReasonLabel is a pure client-importable module mapping the host_cancellation sentinel to 'Cancelled by host'.
- [Phase ?]: 08-01: migration 0017 follows the 0013 convention (unqualified names, plain CREATE TYPE) because Postgres has no CREATE TYPE IF NOT EXISTS; idempotency = drizzle journal + fresh-schema harness replay
- [Phase ?]: 08-02: RSVP no-overflow is the D-112 pessimistic seat-claim (SELECT capacity_snapshot ... FOR UPDATE + count under the lock in claimSeat), the atomic authority — mutation-verified via the two-connection makeRacingClients race gate (delete FOR UPDATE -> red -> restore). Invite/manage tokens are ~100-bit crypto Crockford bearer credentials over randomBytes(20), no FIT- prefix (D-118).
- [Phase ?]: 08-03: the D-108 pax surcharge folds into spacePriceCents (A1) — host revenue, the payout + service-fee basis; extraHeadFee=0 stays byte-identical to today; Phase-5 rail unchanged (D-107)
- [Phase ?]: 08-03: booking.declared_pax persisted only when the listing charges per head (extra_head_fee>0), else NULL (D-108); declaredPax is shape-only at the schema, price re-derived server-side
- [Phase ?]: D-122: group RSVP notification types added as the four-file compile-checked change (no default:); guests (null user_id) use a separate email-only fitout/guest-email fn with no durable row (RESOLVED A2 / Pitfall 2)
- [Phase ?]: 08-05: group-pricing fields are OPTIONAL in publishSchema (mirror postalCode, not cancellationPolicy) — adding them to the publish gate is exactly how a requirement becomes real
- [Phase ?]: 08-05: a CONFIRMED booking can never be re-priced by updateDeclaredPax — the over-subscribed-group top-up stays D-114's deferred fast-follow
- [Phase ?]: 08-05: the PayMongo checkout Idempotency-Key is amount-scoped ONLY for per-head bookings (declared_pax non-NULL); flat bookings keep checkout:<bookingId> byte-identical
- [Phase ?]: 08-06: the organizer group reads (getRoster/getHeadcount/getOwnedGroup*) take the organizer id and carry `b.booker_id = $organizerId` INSIDE the WHERE — a foreign roster is UNREADABLE, not merely unrendered (T-08-14); mutation-verified, 8 predicate deletions each go red
- [Phase ?]: 08-06: getGroupByToken returns ONE frozen GROUP_INACTIVE for unknown = replaced = voided = cancelled-booking; never add a `reason` field to distinguish them — the indistinguishability IS T-08-17, and the test compares the four results' key sets to catch that "improvement"
- [Phase ?]: 08-06: an Inngest event NAME + its emitter live in a plain lib module (src/lib/group/guest-notify.ts), NEVER in the inngest/functions file — that file calls createFunction at module scope, so a server action importing the name registers a function from an action import (it broke outright under a mocked client)
- [Phase ?]: 08-06: the group attendee confirmation fires ONLY for a 'yes' (the shipped copy on both channels is "you're on the list", false to send to a decliner) and there is deliberately no declined notification type to invent; a blank-email guest gets NO send on any channel and submitRsvp returns reachable:false so the UI can say so (D-117/G3)
- [Phase ?]: 08-06: removeAttendee DELETEs the rsvp row (never flips it to 'no' — "removed by the organizer" and "can't make it" are different facts) and takes the seat-claim's own group-row FOR UPDATE lock so a removal and a concurrent claim serialise
- [Phase ?]: 08-06: the RSVP submit budget is keyed on the LINK and deliberately generous (30/60s — a group of ten answering at once is the success case); the real anti-abuse control is the per-normalized-address guest-email budget (3/hr), and the D-117 opt-in guard is structural: the emitted address is derived from THIS request body, never read back out of the roster
- [Phase ?]: 08-07: the Remove control renders on 'yes' roster rows only — the locked confirm copy 'this frees up their spot' would be a false statement over a declined row (the organizer's own row stays structurally excluded)
- [Phase ?]: 08-07: RegenerateLinkButton deliberately does not read the accessToken the action returns — the refreshed owner-scoped RSC re-reads it, keeping the bearer credential's only client-side existence inside one read-only input (D-118)
- [Phase ?]: 08-07: the D-114 top-up nudge renders no money figure and no money-moving control — the in-app top-up is the deferred A3 fast-follow, and a quoted client-side amount would violate G8's server-computed rule
- [Phase ?]: 08-08: the public /invite/[token] RSC lives at the ROOT and reads the session without requiring it — a signed-out invitee is never redirected to /login (GROUP-03 holds structurally, not by convention)
- [Phase ?]: 08-08: malformed, unknown, regenerated, voided and cancelled invite tokens all fold onto ONE inactive branch over ONE pair of constants — measured live, four token classes returned byte-identical rendered content
- [Phase ?]: 08-08: a page whose URL carries a bearer credential declares robots:noindex + referrer:no-referrer in its own metadata — the two leak paths a URL-borne token has that a header-borne one does not
- [Phase ?]: 08-08: the invite form renders submitRsvp's error string VERBATIM and performs no client-side cap re-check — only the FOR UPDATE seat-claim can know what 'full' means (D-112)
- [Phase 08]: CR-03 closed: declaredPax is bounded twice — a .max(10_000) SHAPE ceiling at bookingCreateSchema (keeps any accepted value away from the int4 money columns) and a min(declaredPax, listing.maxOccupancy) clamp read INSIDE createPendingHold's own transaction (the real cap, D-111). The raw input now reaches exactly one expression: the clamp.
- [Phase 08]: The creation-path null-maxOccupancy fallback is 1 (fail CLOSED, surcharge-free), deliberately DIFFERENT from updateDeclaredPax's fallback to the client value — createPendingHold is the entry point a crafted POST reaches, so an uncapped listing charges for nobody extra rather than for whoever asked.
- [Phase ?]: 08-11: the RSVP rate-limit key is the RESOLVED group.groupId, never the invite token — submitRsvp resolves before it budgets, so an unknown token mints zero buckets (measured 200 -> 0)
- [Phase ?]: 08-11: src/lib/rate-limit.ts is hard-bounded at RATE_LIMIT_MAX_BUCKETS = 50,000 via throttled expired-sweep + unconditional insertion-order eviction; size <= MAX holds after every call
- [Phase ?]: 08-11: NO coarse pre-resolution budget on the public RSVP path — one shared key on the app's only public write is a global availability lever (T-08-34 accepts one indexed lookup per request instead)
- [Phase 08]: CR-02 expire key is session-scoped (checkout-expire:<sessionId>), never booking-scoped — A booking owns more than one checkout session over its life — that is exactly what a D-108 re-price creates. A booking-scoped key would make the SECOND session's expire replay the FIRST call's 200: PayMongo answers success, we believe the session was retired, and it stays payable. Two mutations (random key, constant booking-scoped key) each turn 3 cases RED. Same trap createRefund records for refunds; now stated in both places.
- [Phase 08]: booking.checkout_session_id is deliberately NOT UNIQUE and NOT indexed — PayMongo session ids are already distinct on their side, so a UNIQUE here adds nothing and would turn a harmless retried write into a constraint error ON THE MONEY PATH. The column is only ever read by primary key alongside the rest of the booking row, so an index would be dead weight. Nullable and backfill-free: NULL is a legitimate state (never reached checkout, or confirmed pre-0019) for which no expire is attempted.
- [Phase 08]: 08-16: seat-claim race testing ships TWO layers — inlined SQL proves the pattern, an imported claimSeat proves the shipment; each layer's MUTATION-VERIFY names its own file and line
- [Phase 08]: 08-16: a racing case must give each racer its OWN drizzle(client) and a name-only guest identity — a shared max:1 db serializes and a de-dupable identity makes a loser return ok:true by replay
- [Phase ?]: 08-13: CR-02 closed — confirmBooking persists checkout.id on the booking row; updateDeclaredPax expires the superseded session BEFORE re-freezing and REFUSES the re-price on failure (needs_attention audit)
- [Phase 08]: 08-14: capacity_snapshot is the ONE organizer-EXCLUSIVE number in group bookings — GREATEST(max_occupancy - 1, 0) — because it caps rsvp ROWS and the organizer structurally never occupies one (D-113). maxOccupancy, declaredPax and the whole organizer surface are organizer-INCLUSIVE; the public invite page stays organizer-exclusive and unchanged.
- [Phase 08]: 08-14: existing booking_group rows are FORWARD-ONLY — no backfill. D-111 makes capacity_snapshot immutable precisely so a later change cannot retroactively move a cap people already answered against; pre-change groups keep their original cap.
- [Phase 08]: 08-14: the createGroup capacity floor is 2 (not 1), enforced in BOTH the pre-read gate and the INSERT's own WHERE, so a host capacity edit racing the create cannot freeze an unjoinable, immutable capacity_snapshot of 0.
- [Phase ?]: 08-15: CR-01 closed — composeWhenLabel reads the PERSISTED booking.full_day snapshot and never re-derives the mode from a price; the D-108 surcharge folded into spacePriceCents is what made the old inequality true of ordinary hourly bookings (this SUPERSEDES the 07-08 decision line above)
- [Phase ?]: 08-15: WhenLabelInput.fullDay is REQUIRED and hourlyRateCents is REMOVED (replaced by dayRateCents) — the compiler, not a reviewer, enumerates all 18 call sites; a future time surface that forgets the snapshot cannot compile
- [Phase ?]: 08-15: the pre-0016 fallback is a POSITIVE day-rate match, never an inequality — it can only ever ADD "Full day" on an exact coincidence, so nothing hourly can be mislabeled by it; with no day rate to match, the real hours render
- [Phase ?]: 08-15: re-request.ts keeps hourlyRateCents (a second, non-formatter consumer feeding quoteWindow) and reuses the fullDay it minted the hold with; placeHold passes `fullDay ?? false`, byte-identical to what createPendingHold freezes
- [Phase 08]: 08-17: **PayMongo does NOT honor `Idempotency-Key` on `/v1/checkout_sessions`** — probed against `sk_test_` with a byte-identical key and body: two POSTs, two different session ids. Every POST mints a new payable session. The comments at `booking.ts:582`, `booking.ts:592` and `paymongo.ts:172` assert the opposite and are FALSE.
- [Phase 08]: 08-17: a third-party guarantee must be PROBED against the real sandbox before it is written into a comment or relied on for correctness — a mock built to model the guarantee you *believe* you have can only ever validate that belief (four plans of coverage confirmed `mockPayMongo`, not PayMongo)
- [Phase 08]: 08-17: the group-booking UI shows TWO capacity numbers that differ by one BY DESIGN — the public invite page is organizer-EXCLUSIVE (`All 11 spots are taken`, from `capacity_snapshot`) and the organizer page is organizer-INCLUSIVE (`12 of 12`); confirmed live at `capacity_snapshot = 11` / `yes_rows = 11` / `max_occupancy = 12`. Do not "harmonise" them.
- [Phase 08]: 08-17: the 08-17 fixture recipe for any future per-head UAT — `uat_listing_bookable` needs `extra_head_fee`, `included`, `max_occupancy` AND `booking_mode = instant` set together, plus a non-NULL `cancellation_policy`; a NULL in any one of them silently produces a walkthrough that exercises nothing (which is how 08-09 step 6 was skipped)

### Pending Todos

[From .planning/todos/pending/ — ideas captured during sessions]

None yet.

### Blockers/Concerns

[Issues that affect future work]

🔴 **OPEN BLOCKER — `confirmBooking` double-charges (found 2026-07-28 by the 08-17 human UAT; `deferred-items.md` item 5).** A booker who submits **Confirm & pay** twice is charged twice: measured live, ₱1,470.00 collected against a ₱735.00 booking (`pay_YFn1jgMWyau46ECydfPR2LdN` + `pay_iw2zsj3ehcRzgRYzgn7hkoUP`, booking `FIT-JSHBVKFP`), both `qrph` and therefore **unrefundable via the API**. Root cause: **PayMongo does not honor `Idempotency-Key` on `/v1/checkout_sessions`** (probed: identical key + body → two different session ids), while three shipped comments assert it does (`booking.ts:582`, `booking.ts:592`, `paymongo.ts:172`). **Not a CR-02 regression and wider than CR-02 — flat-priced bookings are equally exposed and this predates Phase 8.** Fix: `confirmBooking` must expire the persisted `checkout_session_id` before creating a new one, mirroring `updateDeclaredPax`, **with a test that drives the real PayMongo API rather than a mock.** Route via `/gsd-plan-phase 8 --gaps`.

🟡 **OPEN — CR-02's live re-price assertion has never been exercised by a human** (`deferred-items.md` item 6). 08-17 step 4 ran two submissions at the same headcount, so `updateDeclaredPax` was never entered. Its only evidence is 08-13's mock-backed mutation proofs. Close it on the same real-API harness as the blocker above.

Open product decisions to resolve before their relevant phase begins (from research):

- ✅ RESOLVED (Phase 3, D-22): Slot granularity = 60-min on-the-hour, platform-wide for v1; any run of consecutive hours or a full day (D-23). Per-listing granularity deferred.
- ✅ RESOLVED (Phase 6 discuss, D-63/D-64, 2026-07-19): Request-to-book money model = **pay-on-approval** (no charge at request; the ~7-day card auth-hold limit is moot — no hold). Approval SLA = 24h, post-approval payment window = 24h, both config-tunable. QRPh is back IN scope for request-to-book (nothing ever reverses).
- ✅ RESOLVED (Phase 7): Cancellation/refund policy matrix → D-67/D-68 tier ladder (07-03, shipped through 07-15). The QRPh refund rule is now SETTLED BY OBSERVATION (07-16, 2026-07-23): QRPh is NOT API-refundable at all (HTTP 400, verbatim in refund-rail.ts) — the D-72 collect-and-never-store InstaPay path is built for it.
- ✅ RESOLVED (2026-07-25): Phase 8 payment-model gate — **per-attendee cost-splitting IS on FitOut's roadmap** (user product decision). v1 group bookings stay a free RSVP coordination shell (RSVP never touches occupancy, never per-attendee payment), but that shell is now a JUSTIFIED FOUNDATION for the future paid/cost-split version, not standalone busywork. GPAY-01 (cost-splitting) / GPAY-02 (ticketing) remain OUT of v1 but are a future milestone, not "never." Unblocks 08-CONTEXT.md.
- Phase 8 (still open, for the discussion): Whether invited attendees need an account to RSVP (v1 default: tokenized link, no account); group capacity source (listing capacity vs per-booking limit) — the 4 gray areas to work in /gsd-discuss-phase 8.
- Phase 2 / Phase 5: Cold-start liquidity — consider lightweight admin/seed tooling and a zero-result-search metric; do not over-build.
- ✅ RESOLVED (Phase 2): WR-06 (rate-limit + audit on capability-activate/onboarding actions) CLOSED in Plan 02. WR-04 (email-send retry/observability) STILL OPEN — deferred to the Phase-7 transactional-email layer.
- ✅ RESOLVED (2026-07-10): 02-VALIDATION.md regenerated for PayMongo (tests/paymongo/*, merchant.activated, Paymongo-Signature) and marked Nyquist-compliant; the flaky public-listing E2E seed was fixed. 02-RESEARCH/02-PATTERNS remain Stripe-era historical (non-blocking).
- Phase 2 → later (PayMongo real onboarding): PayMongo Platforms / Linked Accounts is beta + sales-gated, and card manual-capture needs "advanced card features" enablement — both are PayMongo support requests with lead time; request early (blocks 02-UAT test 11 + real payout UAT).
- ✅ RESOLVED (2026-07-21): local Postgres was down mid-Wave-1 (Docker Desktop engine stuck initializing). Recovered; `drizzle/0013` + `0014` applied and idempotent on re-run; all Phase-7 objects verified in the live DB. Full suite green (59 files / 401 tests). Plan 07-02's owed DB gates were run and its self-check flipped to PASSED (see its SUMMARY addendum).
- ⚠️ **VERIFICATION BLIND SPOT found 2026-07-21 (no owner yet)** — the local `public` schema was three migrations behind (`0010`–`0012`, Phase 6) and nothing caught it. The integration harness (`tests/helpers/db.ts`) replays migrations into isolated per-test schemas, so a stale `public` schema is invisible to the entire test suite; Phase 6 verified green against a DB that never had its own `booking_no_overlap` widening applied. Worth a pre-phase guard asserting `public` is fully migrated. Not fixed during Phase 7 — surfaced to the phase verifier.
- ⚠️ **ORDERING DEPENDENCY (Phase 7, opened by 07-01)** — 07-01 widened the ledger unique constraint to `(booking_id, kind)` and applied only the minimal `ON CONFLICT` repair in `payout-sweep.ts`. `AND kind = 'payout'` scoping is still absent from `queryDuePayouts`, `queryProcessingLedger`, the reconcile stuck-`held` query, `/host/earnings` and `summarizePayouts`. **Plan 07-04 (Wave 2) must land this scoping before 07-11 (Wave 4) ships the host-cancel action** — the first `host_cancel_fee` debit row written without it trips the reconcile operator alert. Wave order already satisfies this; do not reorder.
- Follow-up (out-of-scope, task chip spawned 2026-07-10): Radix Tooltip SSR hydration mismatch at src/app/listings/[id]/page.tsx:267 (the "Not bookable yet" affordance) — client-recovered, not a 500; worth a cleanup. Also low-pri from 02-UAT: currency defaults to `usd` (should be PHP for the PH launch); no landing page at `/` (deferred to Phase 4).

### Quick Tasks Completed

Status is populated only for tasks run with `--validate` / `--full` (which spawn a verifier). Blank
means the task predates that flag being used here, not that it failed.

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260709-id2 | Swap payments provider Stripe → PayMongo in planning docs | 2026-07-09 | 1eebe11 | | [260709-id2-swap-payments-provider-stripe-to-paymong](./quick/260709-id2-swap-payments-provider-stripe-to-paymong/) |
| 260710-lgo | Fix T-04-SIGMATCH: allow-list Cloudinary sign params | 2026-07-10 | d1fb76b | | [260710-lgo-fix-t-04-sigmatch-allow-list-cloudinary-](./quick/260710-lgo-fix-t-04-sigmatch-allow-list-cloudinary-/) |
| 260713-nz3 | Range-fill slot selection in booker SlotPicker (03-05 UX) | 2026-07-13 | 83708f0 | | [260713-nz3-range-fill-slot-selection-in-booker-slot](./quick/260713-nz3-range-fill-slot-selection-in-booker-slot/) |
| 260714-feq | Apply Phase-3 code-review findings (post-phase hardening) | 2026-07-14 | 8667ecb | | [260714-feq-apply-phase-3-code-review-findings-post-](./quick/260714-feq-apply-phase-3-code-review-findings-post-/) |
| 260724-jo1 | Fix Phase-7 UAT gap G1: earnings cancellation-fee line missing space before "in" (SWC JSX whitespace) | 2026-07-24 | edc10e5 | | [260724-jo1-fix-earnings-cancellation-fee-line-missi](./quick/260724-jo1-fix-earnings-cancellation-fee-line-missi/) |
| 260724-l1s | Rewrite stale search-and-book e2e to the current instant-book flow (Phase-7 e2e debt) | 2026-07-24 | 35243e1 | | [260724-l1s-rewrite-stale-search-and-book-e2e-to-cur](./quick/260724-l1s-rewrite-stale-search-and-book-e2e-to-cur/) |
| 260724-lmy | Close Phase-7 build-guard (NEXT_PHASE exemption) + lint hygiene (worktree/.next ignores + react-hooks fix) | 2026-07-24 | 4a97771 | | [260724-lmy-close-phase-7-build-guard-and-lint-hygie](./quick/260724-lmy-close-phase-7-build-guard-and-lint-hygie/) |
| 260731-lsx | Correct three erroneous findings in 09-SECURITY.md (T-09-91 plan-gate defect, T-09-86 rejected accept, UF-01 unmapped flag) — verdict unchanged | 2026-07-31 | 68853f8 | | [260731-lsx-correct-09-security-findings](./quick/260731-lsx-correct-09-security-findings/) |
| 260801-iu7 | Signal on host surfaces when a published listing has no operating hours (v1.0 audit finding #4) | 2026-08-01 | b37b6a3 | | [260801-iu7-signal-on-host-surfaces-when-a-published](./quick/260801-iu7-signal-on-host-surfaces-when-a-published/) |
| 260801-kv2 | Close T-08-79: CAS checkout lease so a concurrent double-click cannot double-charge (v1.0 audit finding #5) | 2026-08-01 | 1a85510 | Verified | [260801-kv2-close-t-08-79-cas-checkout-lease-so-a-co](./quick/260801-kv2-close-t-08-79-cas-checkout-lease-so-a-co/) |
| (fast) | Load `.env.local` in `drizzle.config.ts` so `npm run db:migrate` stops failing with `url: undefined` | 2026-08-01 | acf4269 | | _(no directory — /gsd:fast runs inline)_ |
| 260805-g60 | Reconcile Phase 1/2/5 human-verification records against existing evidence; correct two false claims in the v1.0 audit | 2026-08-05 | 409c665 | | [260805-g60-reconcile-phase-1-2-5-human-verification](./quick/260805-g60-reconcile-phase-1-2-5-human-verification/) |
| 260805-ir9 | Fix post-reset lockout: self-heal a stale session cookie so /login stays reachable | 2026-08-05 | 3c25ace | Verified | [260805-ir9-fix-post-reset-lockout-self-heal-a-stale](./quick/260805-ir9-fix-post-reset-lockout-self-heal-a-stale/) |
| 260805-nb7 | Fix DEF-IR9-01 time bomb: derive hold-expiry.test.ts fixture dates from real now; log 6 siblings | 2026-08-05 | ec20a77 | | [260805-nb7-fix-def-ir9-01-time-bomb-make-hold-expir](./quick/260805-nb7-fix-def-ir9-01-time-bomb-make-hold-expir/) |
| 260806-gwt | Sweep the 6 Tier-1 date time bombs behind a shared tests/helpers/dates.ts | 2026-08-06 | 277bff6 | Verified | [260806-gwt-sweep-the-6-tier-1-date-time-bombs-behin](./quick/260806-gwt-sweep-the-6-tier-1-date-time-bombs-behin/) |
| 260806-p3y | Give recordAudit a durable audit table so needs_attention money alerts are queryable | 2026-08-06 | a527743 | Verified | [260806-p3y-give-recordaudit-a-durable-audit-table-s](./quick/260806-p3y-give-recordaudit-a-durable-audit-table-s/) |
| 260807-fc6 | Fix avatar upload: move non-async exports out of the "use server" module + add a parser-based scan guard | 2026-08-10 | 6d60ec5 | | [260807-fc6-fix-avatar-upload-move-non-async-exports](./quick/260807-fc6-fix-avatar-upload-move-non-async-exports/) |
| 260810-hd6 | Record Phase 1 fully clear — all six human items passed, 01-VERIFICATION human_needed → passed | 2026-08-10 | f5308a9 | | [260810-hd6-record-phase-1-fully-clear-all-six-human](./quick/260810-hd6-record-phase-1-fully-clear-all-six-human/) |
| 260810-i0v | Close LW-01 — verify the expire postcondition by re-probing PayMongo instead of string-matching its error text | 2026-08-10 | 950dcd7 | Verified | [260810-i0v-close-lw-01-verify-the-expire-postcondit](./quick/260810-i0v-close-lw-01-verify-the-expire-postcondit/) |
| 260810-j3z | Make unresolved needs_attention money alerts reach a human — daily ops digest + the first `resolved_at` writer (T-08-74 reachability half) | 2026-08-10 | 50c0be9 | Verified | [260810-j3z-make-unresolved-needs-attention-money-al](./quick/260810-j3z-make-unresolved-needs-attention-money-al/) |
| 260810-km4 | Point the vitest suite at its own `fitout_test` database so stray app-singleton writes stop polluting dev (closes deferred D1) | 2026-08-10 | 8c4c37a | Verified | [260810-km4-point-the-vitest-suite-at-its-own-databa](./quick/260810-km4-point-the-vitest-suite-at-its-own-databa/) |
| 260810-sti | Make operating hours the fourth bookability term so a listing with an empty calendar is not sellable (closes audit finding #4) | 2026-08-10 | ca36235 | Verified | [260810-sti-make-operating-hours-part-of-the-bookabi](./quick/260810-sti-make-operating-hours-part-of-the-bookabi/) |
| 260811-dj4 | Add resolved-history review to the ops alerts CLI so a discharge can be audited (closes D2) | 2026-08-11 | e358c41 | Verified | [260811-dj4-add-resolved-history-review-to-the-ops-a](./quick/260811-dj4-add-resolved-history-review-to-the-ops-a/) |
| 260811-elm | Explain Drop-in on the search card so a booker knows what they are buying (closes audit finding #6 copy clause) | 2026-08-11 | 9b7bb61 | Needs Review | [260811-elm-explain-drop-in-on-the-search-card-so-a-](./quick/260811-elm-explain-drop-in-on-the-search-card-so-a-/) |
| 260811-fh6 | Add `resolved_by` to the audit table so a discharge records which human performed it (migration 0025) | 2026-08-11 | e4722eb | Verified | [260811-fh6-add-resolved-by-to-the-audit-table-so-a-](./quick/260811-fh6-add-resolved-by-to-the-audit-table-so-a-/) |

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| *(none)* | | | |

## Session Continuity

Last session: 2026-07-31 (/gsd-plan-phase 9 --gaps)
Stopped at: **GAP PLANNING COMPLETE — 9 new plans `09-17` … `09-25` cover all 14 code-review findings, checker PASSED with 0 blockers. Phase 9 is now 25 plans: 16 executed, 9 planned and awaiting execution.** `phase_status` was `Executed` (not `Complete`), so the closed-phase gate did not fire; the run was ADDITIVE — no 09-01…09-16 PLAN/SUMMARY was touched (`git diff --stat` on them is empty). Research was skipped (`--gaps`); the gap source is **`09-REVIEW.md`**, NOT a VERIFICATION.md — none exists, because phase verification was never run. Plan → finding map: **09-17** CR-01 + NT-01(a) · **09-18** CR-03 layer 1 (re-anchor the counter on the venue-local DATE, half-open range over stored `starts_at`, lock keyed `listing:YYYY-MM-DD`) · **09-19** CR-03 layer 2 (`saveOperatingHours` refuses a weekday still carrying upcoming passes) · **09-20** CR-02 · **09-21** CR-04 + WR-04 · **09-22** CR-05 · **09-23** CR-06 + WR-01 · **09-24** WR-02 + WR-03 + NT-02 · **09-25** WR-05 + NT-01(b). **NT-03 is the one deliberate non-action** (already-recorded items; verified against `deferred-items.md` + 09-12/09-14 summaries). Waves after the patch cascade: **1** = {17,18,19} · **2** = {20,22,25} · **3** = {21,24} · **4** = {23} — but `use_worktrees: false`, so all nine run SEQUENTIALLY on `dev` in plan-number order; waves express dependency only. **NO MIGRATION** — CR-03's anchor is a predicate over the already-stored `starts_at`, so the next migration number stays **0023**. Requirements coverage 4/4 (OPEN-01…04); decision gate skipped (CONTEXT.md uses `OC-NN`, and the gate only counts `D-NN` in `must_haves`). Commits `d4a3dde` (plans) → `73791e8` (roadmap waves) → `3d4322c` (checker-warning patches). NEXT: `/gsd-execute-phase 9` → then verification → `/gsd-secure-phase 9` → phase close.

📌 **Two contracts this planning round establishes, load-bearing for execution:**
1. *The four UNVERIFIED findings (CR-02, CR-04, CR-05, CR-06) are planned CONFIRM-THEN-FIX, and the not-reproduced branch is a real branch.* Each plan's Task 1 must reproduce the defect against unchanged `src/` and record it verbatim; if it does NOT reproduce, the executor writes a dated `CR-NN — NOT REPRODUCED (false positive)` entry in `deferred-items.md`, KEEPS the test as a regression guard, changes nothing in `src/`, and closes the plan. `git status --short -- src/` must be empty at the end of every Task 1. The reviewer was right 2-for-2 on what was checked — that is not a guarantee for the other four, and a "fix" for a defect that does not exist is worse than doing nothing.
2. *CR-01's test is the whole lesson of this phase, encoded.* CR-01 survived 1035 green tests, a 21/21 browser proof AND a 9/9 human UAT because **both proof layers were structurally blind to the same-day path** (the e2e spec books only `offset >= 3` days out; the walkthrough ran at 01:31 Makati, before the 06:00 opening). 09-17's fixture is therefore **deterministic at any run hour** (`open_time` = venue-local now − 2h floored, never before `00:00`; `close_time` = `23:59`) — a test that only catches the bug when the suite happens to run during business hours reproduces the exact failure being fixed. The mutation's RED message is named VERBATIM and the assertion is ORDERED FIRST so it quotes the sentence a booker would read.

⚠️ **The unsatisfiable-acceptance-grep defect was fixed at the SOURCE this round, not absorbed a 20th time.** The original 16 plans shipped **19** of them. The checker caught the 20th in 09-25: Task 2 demanded both a clean `npm run lint` and a LITERAL-apostrophe grep, but `react/no-unescaped-entities` forces `can&apos;t` in JSX text — the two criteria were mutually unsatisfiable. Fixed by prescribing an exported `PASS_NON_REFUNDABLE_MESSAGE` constant rendered as `{PASS_NON_REFUNDABLE_MESSAGE}` (inside a TS string literal the apostrophe needs no escaping, so the rule never fires and the source bytes and the grep pattern are the same). A companion criterion was also **vacuous** — it grepped a literal apostrophe in text that really contains `&apos;`, so "UNCHANGED" held no matter what happened to the paragraph. **Both fixes carry an inline note explaining why, so the next reader cannot "fix" them back.**

⚠️ **`src/lib/availability/units.ts` is edited by FOUR gap plans in sequence — 09-18 → 09-20 → 09-21 → 09-23 — all extending the SAME step-5 refusal-branch sequence in `createOpenCapacityHold`.** Each plan's `read_first` now names which predecessors changed it and what they added, and 09-23 is instructed to leave 09-20's block refusal and 09-21's rate refusal alone. This was the checker's third warning and it is the seam most likely to produce a merge-shaped bug during execution.

Prior session: 2026-07-31 (/gsd-resume-work)
Stopped at: **Session resumed from `HANDOFF.json` + the phase `.continue-here.md`. The pending human decision was ANSWERED: the gap-closure route is the FULL PLANNING ROUND-TRIP — `/gsd-plan-phase 9 --gaps`, not a direct strike at CR-01 + CR-03.** Phase 9 remains code-complete-but-open: 16/16 plans executed, every repository gate green (tsc 0 · vitest 1035 passed / 4 skipped · lint 0 errors / 7 baseline warnings · build 0 / 29 routes · Playwright 21/21), human UAT 9/9 including a real PayMongo `sk_test_` charge — and then `/gsd-code-review 9` found **6 blockers / 5 warnings / 3 info** (`09-REVIEW.md`, `d792f0a`). CR-01 (same-day drop-in pass holdable but never payable) and CR-03 (an operating-hours edit re-keys the admissions counter → up to 2× cap sellable) are ORCHESTRATOR-VERIFIED; CR-02/04/05/06 are reported-only and must each be confirmed before a fix is written. `HANDOFF.json` was deliberately NOT deleted — the gap-planning pass still needs its machine-readable per-blocker file:line detail; delete it once the gap plans exist. Working tree clean, no interrupted agents, no incomplete plans. NEXT: `/gsd-plan-phase 9 --gaps` → close CR-01 + CR-03 first → triage the rest → `gsd-verifier` → `/gsd-secure-phase 9` → phase close.
Resume file: `.planning/phases/09-open-capacity-bookings/.continue-here.md` (still current)

Prior session: 2026-07-31 (/gsd-execute-phase 9 — 09-16)
Stopped at: **Completed 09-16-PLAN.md (Wave 7) — the drop-in human walkthrough. ALL 16 PHASE-9 PLANS EXECUTED; the phase is NOT closed (verification + close are the orchestrator's).** Nine steps recorded individually with verbatim evidence; the phase's only manual-only verification is DISCHARGED against the real PayMongo `sk_test_` rail (3 webhooks → 3 confirmed drop-in bookings → 3 `booking_confirmed` rows within one second each), and `availability_block` = 0 rows after a host-cancel. Step 4's reported FAIL was reclassified by the operator to accepted design (the OC-11 half-capacity clamp) and pinned as an asserted e2e contract instead of a code change — `src/` untouched, `70c392a`. Two product items logged in the phase's `deferred-items.md`. NEXT: `/gsd-verify-phase 9` then phase close.
Resume file: None

Prior session: 2026-07-30 (/gsd-execute-phase 9 — 09-15)
Stopped at: **Completed 09-15-PLAN.md (Wave 6) — the drop-in browser proof + the repository gate.** `e2e/open-capacity.spec.ts` (5 serial cases) ships and is mutation-measured (2 kills executed RED, restored); suite 1035 passed / 4 skipped, tsc 0, lint 0 errors / 7 baseline warnings, build 0, e2e 21 passed / 8 specs, no schema drift; `09-VALIDATION.md` complete and signed off. NEXT: Wave 7 = 09-16 (human-verify checkpoint, `autonomous: false`) — needs `npm run dev` + `npm run dev:inngest` + a webhook tunnel, all user-owned.
Resume file: None

Prior session: 2026-07-30 (/gsd-plan-phase 9)
Stopped at: **Phase 9 PLANNED — 16 plans in 7 waves, ready to execute.** Ran pattern-mapper first (09-PATTERNS.md — 39 files classified, 36 analogs; surfaced the `priceMax`/`hourly_rate_cents` search trap and six live grep tripwires), then planner (8b4cf41 + ce58a6a), then plan-checker → **VERIFICATION PASSED, 0 blockers, 2 non-blocking warnings** (09-08's ~22-file compiler census is large but tsc-gated; 09-RESEARCH.md's Open Questions were never marked `(RESOLVED)` in the source doc although all four are resolved in the plans). Coverage gates green: OPEN-01..04 all mapped, OC-01..OC-18 all cited. ROADMAP annotated with wave-dependency headers + 9 cross-cutting constraints. **Next: `/gsd-execute-phase 9`** — start with Wave 1 (09-01: 3 migrations + `[BLOCKING] npm run db:migrate`); the non-negotiable gate is 09-03's SC#3 two-connection concurrent-overbook race against the shipped claim.
Resume file: None

Prior session: 2026-07-29 (resume)
Stopped at: Session resumed via /gsd-resume-work; HANDOFF.json consumed. Restored full Phase-8 context (17/17 executed, phase NOT closed, blocked on the confirmBooking double-charge). Operator decisions on resume: (1) the unlogged `included >= max_occupancy` unreachable-surcharge finding is LOGGED as deferred-items.md **item 7** and FOLDED into the gaps plan; (2) the three false idempotency comments (booking.ts:582/592, paymongo.ts:172) are FOLDED into the gaps plan; (3) proceed by showing the plan command. Working tree clean; HEAD af67c94 (pause commit). **Next: `/gsd-plan-phase 8 --gaps`** with inputs deferred-items.md items 5 (BLOCKER double-charge), 6 (CR-02 live re-price unverified) and 7 (unreachable surcharge), plus correcting the three false comments.
Resume file: None (HANDOFF.json consumed this session — safe to delete)

Prior session: 2026-07-28T10:45:00.000Z
Stopped at: Completed 08-17-PLAN.md (wave 9) — the pax-surcharge human walkthrough. **ALL 17 PHASE-8 PLANS EXECUTED; PHASE NOT CLOSED.** Task 1 gate: 96 files / 838 tests exit 0, bare `npm run build` exit 0; `uat_listing_bookable` patched and read back (`published | instant | max_occupancy 12 | included 1 | extra_head_fee 10000 | standard`). Steps 2,3,5,6,7,8 APPROVED per-step with literal strings (CR-01 on 4 surfaces, CR-03's clamp at 12, WR-03's `1 of 12`, WR-04's nudge at the first over-subscription plus its silence one below, SC4's live cap at 11+organizer). **Step 4 FAILED: a live DOUBLE-CHARGE** — ₱1,470 collected against a ₱735 booking (`FIT-JSHBVKFP`), because **PayMongo does not honor `Idempotency-Key` on `/v1/checkout_sessions`** (probed: identical key → two session ids) and `confirmBooking` expires nothing before creating. Wider than CR-02; flat bookings exposed too; predates Phase 8. Also NOT VERIFIED: CR-02's own re-price assertion (the scripted sequence was never run). Logged as `deferred-items.md` items 5 + 6. No source file modified. **Next: `/gsd-plan-phase 8 --gaps` — NOT `/gsd-execute-phase 8`, and not a phase close.**
Resume file: None

Prior session: 2026-07-28T08:35:14.660Z
Stopped at: Completed 08-15-PLAN.md (wave 8) — CR-01 CLOSED
Resume file: None

Prior session: 2026-07-28T02:06:54.730Z
Stopped at: Completed 08-08-PLAN.md
Resume file: None

Prior session: 2026-07-24T04:37:54.671Z
Stopped at: Completed 07-20-PLAN.md (T4-hours/T7/T9 gap closure)
Resume file: None

Prior session: 2026-07-23T07:45:00Z
Stopped at: Completed 07-17-PLAN.md — Phase 7 gap closure (17/17). CR-01/CR-02/WR-04/WR-06 all closed red-first: amount-keyed refund-notice suppression (+ toast branch), row-derived capped deadline labels in the lifecycle emails (APPROVAL_* constants renderer-free in email.ts), the `side`-discriminated host-cancellation record (new sendHostCancellationRecord; booker copy + durable pre-fix rows byte-unchanged), and the persisted `booking.full_day` pricing-mode snapshot (drizzle/0016 applied + idempotent; re-request reads the column, never the current-rate inequality). Full suite 78 files / 655 tests exit 0; tsc clean; env-prefixed build green. 8 commits 6f887c3..c46f2cf. Next: /gsd-verify-work 7 re-verification, then /gsd-plan-phase 8 (group bookings).
Resume file: None

Prior session: 2026-07-23T06:35:00Z
Stopped at: Completed 07-16-PLAN.md — PHASE 7 CODE-COMPLETE (16/16 plans, all SUMMARYs on disk). The human QRPh probe settled the gating question (`confirmed` — HTTP 400, raw evidence in refund-rail.ts; A3 BLOCKED on PayMongo enabling Money Movement) and Branch B was executed: createRefundTransfer + listReceivingInstitutions in paymongo.ts, the qrph-refund destination schema + RefundDestinationForm, cancelBookingAsBooker(bookingId, destination?), and tests/paymongo/{refund,instapay-refund}.test.ts (owner gate mutation-verified). Full suite 78 files / 639 tests, exit 0. Commits 3fa3156 + 81dd608. Next: /gsd-verify-work 7, then /gsd-plan-phase 8 (group bookings). Manual UAT owes: A3 re-verification + live institutions list once PayMongo enables Money Movement.
Resume file: None

Prior session: 2026-07-23T05:47:00Z
Stopped at: Session resumed via /gsd-resume-work. The dismissed 07-16 question from the pause is now ANSWERED: run the QRPh probe together (interactive — human completes the test-mode QRPh payment in-browser; tunnel + PayMongo dashboard webhook registration required). Also committed the Inngest in-housing deliberation outcome (3eca5ed): DEFERRED — staying on Inngest for v1, upholding D-56; revisit only after proving market AND hitting Inngest limits. HANDOFF.json consumed and deleted. Proceeding to /gsd-execute-phase 7 for 07-16.
Resume file: None

Prior session: 2026-07-21T15:20:00.000Z
Stopped at: Completed 07-15-PLAN.md (Wave 4 — the cancellation tier's two HUMAN ends, closing BOOK-07). A host must now make an EXPLICIT tier choice before publishing: three equal-weight cards with NO pre-selection in the wizard (D-77), the requirement joining the SHIPPED publish checklist, and the real gate in `publishSchema` re-read from the PERSISTED row so the client checklist is never it (T-07-88). A booker now sees that tier before paying: one `CancellationPolicyDisclosure` Server Component (native `<details>`), generic on the listing page and CONCRETE venue-local dates at checkout (D-81), sourced from the BOOKING's tier snapshot — the same column `quoteRefund` reads. **All disclosure copy is DERIVED from `LADDER` and mutation-proven in both directions:** move a rung and the copy follows automatically; hand-type a figure and the test goes red. A NULL tier renders NOTHING rather than `tierOrDefault`'s Flexible — that fallback is a legacy engine safety net, not a policy any host chose. Two shipped fixtures that called themselves publish-eligible no longer were, and were correctly updated. Full suite **76 files / 628 tests, exit 0** (was 75/616). Commits 53ce7ac + 14dd0f5 + 05d719d + 926517c. (Full detail in 07-15-SUMMARY.md.)
Resume file: None

Prior session: 2026-07-21T14:55:53.632Z
Stopped at: Completed 07-12-PLAN.md (Wave 4 — booking-detail completion + D-97 lapse recovery). `reRequestSameWindow` mints a NEW constraint-validated hold (never a status flip back); `/bookings/[id]` gains the D-104 cancel entry, the no-money `Cancel request` dialog, the D-97 recovery branch and the `cancelled` / derived-`completed` branches; D-99's cap-shortened-SLA reason line ships on `/host/requests`. **Two live defects fixed along the way:** the plan's `expires_at has passed` lapse guard would have matched NO row (both retirement paths NULL the column, so D-97 would have shipped dead), and `cancelled` was absent from `RENDERABLE` so the shipped booker cancel flow ended on a 404. **Both assigned handoffs closed:** `e2e/cancel.spec.ts` now clicks the detail-page entry instead of deep-linking (07-09's documented gap), and D-99 is implemented (07-05's). Full suite **74 files / 604 tests, exit 0** (was 73/597). Commits 940f66f + 9158a47 + 39b89e4. (Full detail in 07-12-SUMMARY.md.)
Resume file: None

Prior session: 2026-07-20 (executing Phase 06 via /gsd-execute-phase — completed 06-07)
Stopped at: Completed 06-07-PLAN.md (Wave 4 — host approve/decline server actions: owner-gated, atomic, SLA-guarded). Created src/app/actions/host-requests.ts (approveRequest + declineRequest cloning the blocks.ts skeleton but swapping in a BOOKING⨝LISTING host-ownership guard; atomic DB-clock SLA-guarded flips; fire-and-forget booker emails; rate-limit + audit on approve; dual revalidatePath). Extended tests/booking/request-lifecycle.test.ts +6 host-action cases incl. the NON-OPTIONAL /host/requests owner-scope READ isolation test (the predicate 06-08 consumes). request-lifecycle 22 green; booking+payments+paymongo 131 green. Commits 779cc43 + e3408cc. (Full detail in 06-07-SUMMARY.md.)
Resume file: None

Prior session: 2026-07-20 (executing Phase 06 via /gsd-execute-phase — completed 06-03)
Stopped at: **Completed 06-03-PLAN.md (Wave 2 — five lifecycle emails + D-62 create-default flip + D-61 wizard copy).** Extended src/lib/email.ts with the five thin plain-HTML lifecycle sends (D-66, the FIRST non-auth emails in the repo): `sendBookingConfirmed`/`sendRequestReceived`/`sendRequestApproved`/`sendRequestDeclined`/`sendNewRequestToHost`, each mirroring the existing sendVerificationEmail shape (a heading line + one body line + a single anchor CTA) over the SAME `send()`/`escapeHtml()` helpers — never a new email stack (React Email stays uninstalled; branding/retry hardening is Phase 7, WR-04). **Every interpolated field is escapeHtml'd before it enters the markup** — space title, booker label, quoted total, reference, AND the CTA url (WR-01/T-06-06: a raw url in an href or a raw title in HTML text is the injection sink). The approval SLA + payment window render as config hour VALUES (`APPROVAL_SLA_HOURS`/`APPROVAL_PAYMENT_WINDOW_HOURS` imported from @/lib/payments/config, never the internal names) so Phase-7 policy tuning flows through; every time label is the caller-composed `whenLabel` that already names the venue tz (the sends never format a time). The one CTA without a caller url (declined → "Find another space") uses `process.env.BETTER_AUTH_URL` (the app-URL convention), root-relative fallback. **Consumer contract (06-04 placeHold request branch, 06-05 webhook confirmed email, 06-06 SLA/payment-window cron, 06-07 approve/decline): invoke fire-and-forget (`void sendXxx(...)`, T-06-07) — a Resend failure must never reject a webhook ACK or block a server action.** D-62: `createDraftListing` (src/app/actions/listing.ts) insert default flipped `bookingMode: "request"` → `"instant"` (demand-first; doc comment updated; mirrors — independent of — 06-01's DB SET DEFAULT; existing rows unaffected, none to backfill). D-61: wizard.tsx Step-5 FormDescription expanded to "You can change this anytime. It applies to new bookings — any requests already in progress keep the mode they started under." (no structural RadioGroup change; the loaded listing.bookingMode drives the selection, now instant). crud.test.ts booking-mode test rewritten to assert the instant create-default + a request toggle (the old test asserted the retired "request" default and would have broken — the sole Rule-1 deviation, no scope creep). tsc + eslint clean on all touched files (pre-existing wizard.tsx:198 form.watch() warning + address-autocomplete.tsx:110 error untouched/out-of-scope); tests/listing 50 green; booking+paymongo+payments 113 green / 4 todo (email-mocking suites — no regressions). **BOOK-04/BOOK-06 stay In-progress** (leaf providers + a default flip only — the emails aren't wired at any call site yet; behavior ships in 06-04/05/06/07, per the 04-xx/05-xx cross-cutting precedent). ⚠ gsd-sdk string-arg handlers no-op — metric/decision/session/stopped_at hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked, but update-progress wrote the phase-based percent (63) — reconciled to plan-based 34/39 = 87%. Commits 53e9b5c (T1 emails) + 2ef456f (T2 flip+copy). Next: 06-04 (placeHold fork on bookingMode = no-charge held request + these emails).
Resume file: None

Prior session: 2026-07-20 (executing Phase 06 via /gsd-execute-phase)
Stopped at: **Completed 06-02-PLAN.md (Wave 2 — occupancy-predicate fan-out + createPendingHold parameterization).** Widened EVERY app-side lazy-read occupancy predicate to include requested/approved (unexpired) in LOCKSTEP with 06-01's widened EXCLUDE — read-model.ts:108 (calendar + search Stage-2 transitively), units.ts findOwnActiveHold + pickLowestFreeUnit + the in-tx stale sweep + the dormant createBooking probe — so a request-held slot can no longer read as free anywhere. Parameterized createPendingHold by { holdStatus, ttlMs, bookingMode } (pending/HOLD_TTL_MS/null defaults → the instant path is byte-for-byte unchanged; the 06-04 request branch mints a `requested` hold with the 24h SLA TTL WITHOUT forking the SAVEPOINT/40P01-retry/idempotency tx). The in-tx reclaim terminal status now MIRRORS the 06-06 SLA cron: `SET status = (CASE WHEN status='requested' THEN 'declined' ELSE 'cancelled' END)::booking_status, expires_at=NULL` (Warning-1 reconciliation — the in-tx-reclaim path and the cron never disagree on the terminal value). ⚠ the `::booking_status` cast is load-bearing (an all-literal CASE resolves to text, can't assign to the enum); NO in-tx email (A6, the cron is the sole booker-email authority). The non-negotiable concurrent double-book race on requested AND approved slots is GREEN (makeRacingClients, exactly-one-survivor) — already held off 06-01's EXCLUDE, so the app-predicate widening is what the occupancy-occupied + createPendingHold-param + sweep-write-target tests actually turn green. TDD RED e11e4c1 (6 failing) → GREEN d761cd6. Quick suite 113 green / 4 todo; availability+search 101 green; tsc+eslint clean. request-expiry.test.ts scaffolded (DB-clock-manip harness + it.todo for the 06-06 SLA/payment-window cron). BOOK-05/PAY-05 stay In-progress (cross-cutting; behavior in 06-04/06-06/06-07). ⚠ gsd-sdk string-arg handlers no-op — metric/decision/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked. Next: 06-03.
Resume file: None

Prior session: 2026-07-20 (executing Phase 06 via /gsd-execute-phase)
Stopped at: **Completed 06-01-PLAN.md (Wave 1 — request-to-book SCHEMA FOUNDATION).** Added booking_status enum values `requested`+`approved` (D-63 slot-holding), booking.bookingMode nullable snapshot (D-61), listing.bookingMode default `request`→`instant` (D-62), APPROVAL_SLA_HOURS + APPROVAL_PAYMENT_WINDOW_HOURS config (D-64). Three hand-split migrations (0010 enum-add / 0011 cols+default / 0012 EXCLUDE recreate) APPLIED to the live dev DB via `npm run db:migrate` (re-run no-op); 100/100 booking+paymongo+payments green (harness replays all three). ⚠ **LOAD-BEARING FIX:** drizzle-orm's migrator wraps all pending migrations in ONE transaction, so the plan's literal EXCLUDE `WHERE status IN ('pending','confirmed','requested','approved')` raised 55P04 on this already-migrated DB — rewrote 0012 as the set-equivalent complement `WHERE status NOT IN ('cancelled','declined','completed')` (Rule 1 deviation; names only committed values, single-tx-safe everywhere, safer default). Live constraint now blocks a requested/pending overlap (verified). **Downstream: every occupancy predicate must mirror the occupying set {pending,confirmed,requested,approved}.** BOOK-04/BOOK-05/PAY-05 stay In-progress (substrate only; behavior in 06-02+). Commits 060d790 (T1) + a27f05f (T2). Next: 06-02 (the concurrent double-book-on-requested/approved race test is the downstream correctness proof).
Resume file: None

Prior session: 2026-07-30 (`/gsd-plan-phase 9` → paused at the UI gate → `/gsd-ui-phase 9`)
Stopped at: **Phase 9 pre-planning artifacts COMPLETE; planning itself NOT run.** Ran `/gsd-plan-phase 9`: research gate → spawned gsd-phase-researcher → **09-RESEARCH.md** (`062e309`). Surfaced its three load-bearing calls to the user, who **CONFIRMED all three as recommended** — now marked `[LOCKED ✓ user-confirmed]` in the Assumptions Log (`1a4841b`): **A1** = per-`(listing,date)` admissions counter under `pg_advisory_xact_lock` + drift-free `SUM(declared_pax)` (a **deliberate, user-approved deviation from D-21's literal "units-as-spots" steer** — D-21 predates the OC-02 date-pass single-row model, and D-112 sanctioned the counter; **promote to a new D-NNN at planning**); **A2** = reuse `listing.maxOccupancy` as the cap; **A3** = dedicated `per_head_price_cents` column. Wrote **09-VALIDATION.md** (`1a4841b`) with the SC#3 two-connection concurrent-overbook race as the non-negotiable Wave-0 gate (model on `tests/group/seat-claim-race.test.ts` Layer 2 via `makeRacingClients`, driving the **real** `createOpenCapacityHold`; mutation gate = deleting the `pg_advisory_xact_lock` line must go RED). Hit the **UI safety gate** (ROADMAP UI hint = yes, no UI-SPEC) → user chose to run the UI phase first → **exited plan-phase cleanly before the planner**. Ran `/gsd-ui-phase 9`: the researcher spawn **failed 4× with API 529 (Opus overloaded, server-side)** including once after a 4-minute backoff — nothing written, no work lost — then succeeded on the 5th attempt. **09-UI-SPEC.md** (`1600ad6`) written with **zero questions asked** (everything locked upstream), then **VERIFIED 6/6 dimensions, 0 blocking** by gsd-ui-checker. Threshold decided: `lowStockThreshold(cap) = clamp(floor(cap/2), 1, OPEN_LOW_STOCK_MAX=5)`, server-derived. **Three cross-cutting UI findings the planner must act on:** (1) `composeWhenLabel` is a latent CR-01 repeat — a drop-in persists `starts_at`/`ends_at` = opening/closing with `full_day=false`, so every drop-in renders as a 16-hour range across ~10 shipped surfaces unless `openCapacity` becomes a **required** field on `WhenLabelInput` (compiler-forced call-site census); (2) host-cancel Step-2 consequence copy must drop its auto-block bullet for open bookings (RESEARCH Pitfall 5) or the dialog promises a calendar block that never happens; (3) `PaxStepper` must move **pre-hold** (the capacity claim happens at hold time) — the spec splits it into a presentational control + two bindings, leaving the shipped server-bound reserve-page usage byte-unchanged for exclusive/group. **Open for the planner:** research Q2 (stepping UP a held open booking must re-enter the capacity claim — route through the claim vs require a fresh hold), Q3 (enforce `unitCount=1` for open listings), UI Open Question 2 (read model carries a server-computed `state` field vs client-derives from `{remaining, cap}`). Next: `/gsd-plan-phase 9` — it will skip research (RESEARCH/VALIDATION/UI-SPEC all present) and go straight to planning.
Resume file: None

Prior session: 2026-07-19 (resumed via /gsd-resume-work → completed /gsd-discuss-phase 6)
Stopped at: **Phase 6 discuss-phase COMPLETE.** Resumed the paused 2026-07-17 scope debate and the user RESOLVED it: **build both booking modes**, host-selectable per listing & editable while hosting (D-61); the mode governs new bookings only (in-flight requests keep their original mode). Default `bookingMode` flips `request`→`instant` (D-62). Request-to-book money model = **pay-on-approval** (D-63) — the debate's load-bearing outcome: authorize→capture is infeasible on PayMongo's rails (QRPh/e-wallets capture-only; card manual-capture sales-gated + 7-day auto-void; capture-now→refund bleeds ~2.5% on every decline), so instead the request holds the slot with NO charge → host approves → booker pays via the Phase-5 checkout → payment.paid webhook confirms; decline/expiry/non-payment frees the slot with nothing reversed. This dissolves the QRPh exclusion (all rails work) and reuses Phase-5 checkout wholesale. Approval SLA 24h + payment window 24h, both config-tunable (D-64); dedicated `/host/requests` page mirroring `/host/earnings` + pending-count nudge (D-65); on-screen confirmation + lifecycle emails via the existing `src/lib/email.ts` (booker confirmed/request-received/approved-pay-now/declined-or-expired; host new-request), hardened email layer WR-04 deferred to Phase 7 (D-66). Wrote 06-CONTEXT.md + 06-DISCUSSION-LOG.md. Routed the mechanism correction EXPLICITLY (not buried): ROADMAP Phase 6 SC #2/#3 + one-liner + REQUIREMENTS PAY-05 all corrected authorize→capture ⇒ pay-on-approval; the `bookingMode` default flip (schema.ts:180) is flagged as a Phase-6 EXECUTION change (D-62, not yet applied — this was discuss, not execute). Consumed + deleted HANDOFF.json and the Phase-6 .continue-here.md. Next: `/gsd-plan-phase 6` (consider `/gsd-research-phase 6` first — request-state modeling over booking_status + the pay-on-approval SLA/payment-window sweeps are the real unknowns; CRITICAL: any new slot-holding state must join the booking_no_overlap GiST exclusion occupying set or requested slots won't block double-booking).

Prior session: 2026-07-16T14:15Z
Stopped at: **Phase 5 EXECUTED end-to-end via /gsd-execute-phase (all 3 waves, 7/7 plans, sequential on main tree since use_worktrees=false).** Pre-flight: Docker Desktop was down (WSL docker-desktop distro stuck Stopped) — user brought it up, then I ran `npm run db:up` and `npm run db:migrate` to bring Postgres current (the volume was behind at 0006 — 0006/0007 re-applied before Phase-5's 0008). Wave 1 (05-01 money foundation, 05-02 PayMongo money-movement client) → Wave 2 (05-03 checkout action, 05-04 payment/refund webhook confirm-authority, 05-05a Inngest payout sweep, 05-06 HOST-03 earnings) → Wave 3 (05-05b payout reconcile + /api/inngest serve mount). All spot-checks PASSED; payments+paymongo suite 71/71; full suite 337/338 (the 1 failure = pre-existing flaky 5s-timeout on tests/auth/secret-config.test.ts, a Phase-1 test that passes in isolation, NOT touched by Phase 5). Migration 0008 applied to live DB (host_payout_ledger UNIQUE(booking_id), payout_ledger_state enum, booking.payment_id, listing.currency all-php). **Code review (05-REVIEW.md, committed 867640a): 1 CRITICAL + 5 warnings + 3 info** — CR-01 = a post-claim wallet-lookup throw or no-wallet-match permanently strands a payout in `held` (no recovery path, no stuck-`held` alert); WR-01/02/03/05 = refund-overwrite risk, refund-status-vs-type trust, missing PAYMONGO_WEBHOOK_SECRET boot guard, unsafe booking.status default. All advisory, all on the payout/webhook robustness paths (payout itself is UAT-gated). **Verification (05-VERIFICATION.md, committed 4ddf292): 5/5 must-haves VERIFIED against the live codebase; status=human_needed** because real hosted-checkout+webhook and real /v2 inhouse transfers are externally UAT-gated (PayMongo money-movement /v2 beta not yet enabled — standing STATE blocker), not a code gap. 3 human-UAT items persisted to 05-HUMAN-UAT.md (status: partial). **RECOMMENDED before real payouts go live: run `/gsd-code-review-fix 5` to address CR-01 + the webhook-secret boot guard (WR-03).** Next: `/gsd-discuss-phase 6` or `/gsd-plan-phase 6` (instant/request-to-book integration — not yet planned).
Resume file: None

Prior session: 2026-07-16T13:49:27Z
Stopped at: Completed 05-05b-PLAN.md (Wave 3, FINAL Phase-5 plan) — the payout RECONCILE + /api/inngest serve mount, closing the D-59 Held→Processing→Paid/Failed lifecycle. getTransfer (src/lib/paymongo.ts EXTENDED) = GET /v2/transfers/{id} (no Idempotency-Key on the GET) — a POLL not a webhook (PayMongo emits no transfer/payout webhook, Pitfall 2). src/inngest/functions/payout-reconcile.ts: mapTransferStatus (paid∈{succeeded,completed,paid}/failed∈{failed,returned,cancelled}/else→processing — SAFE DEFAULT, unknown never spuriously Paid; ⚠ VERIFY the enum vs a live PayMongo response before UAT, A4); queryProcessingLedger (state='processing' AND transfer_id NOT NULL, ORDER BY created_at ASC LIMIT 200); reconcileOne = getTransfer → UPDATE host_payout_ledger SET state='paid',paid_at=now() (or state='failed') WHERE booking_id=$id AND state='processing' (the guard makes it IDEMPOTENT — a re-run on a terminal row is a 0-row no-op, paid_at never rewritten); a terminal Failed OR a row stuck past PAYOUT_RECONCILE_STUCK_HOURS → console.error('[payout-alert] transfer failed' | 'transfer stuck processing') so no payout is silently stranded (T-05-28). payoutReconcile = hourly singleton {concurrency:1, triggers:[{cron:'TZ=Asia/Manila 30 * * * *'}]}, offset 30m from the sweep; per-row step.run. src/app/api/inngest/route.ts (created LAST so both static imports resolve tsc-clean) = serve({client:inngest, functions:[payoutSweep, payoutReconcile]}) exporting GET/POST/PUT, runtime='nodejs', FAIL-CLOSED prod boot guard (throws if NODE_ENV==='production' && !INNGEST_SIGNING_KEY, T-05-26; dev/test/build tolerate absence — the Inngest Dev Server needs no keys). mockPayMongo += getTransfer stub (default 'succeeded') + reset() wiring. 6 new tests (payout-reconcile.test.ts: selectivity, Processing→Paid paid_at-set, Processing→Failed+alert, idempotency on an already-paid row, unknown-stays-processing, stuck-alert); 71/71 payments+paymongo green (65+6); tsc+eslint clean (0 errors) on all touched files. DEVIATION (Rule 3): inngest 4.13.0 2-arg createFunction(options, handler) with the cron in options.triggers (the plan/RESEARCH 3-arg skeleton predates this API — same fix 05-05a made). PAY-02 + PAY-03 now COMPLETE; **Phase 5 COMPLETE (7/7 plans — PAY-01/PAY-02/PAY-03/HOST-03).** Real PayMongo /v2 transfer + transfer-status polling UAT-gated on the money-movement beta. Commits a6260bd (T1) + b83c5e5 (T2) + ddbaba0 (T3). ⚠ gsd-tools handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited. Next: Phase 6 (instant/request-to-book integration) — not yet planned.
Resume file: None

Prior session: 2026-07-16T13:33:23Z
Stopped at: Completed 05-06-PLAN.md — the HOST-03 host earnings/payouts page (D-59). Owner-gated (host) RSC at /host/earnings reading host_payout_ledger WHERE host_id=session.user.id (Security V4/T-05-29 — the route group is NOT the gate), joined booking/listing, newest-first; re-gates session+canHost (defense in depth). New PURE non-client module src/components/host/payout-ledger-status.ts (distinct from onboarding payout-status.ts): derivePayoutLedgerView(state)→calm {label,tone,helper,datePrefix} (Held=muted+'Held until after the session'+Expected, Processing=outline+Expected, Paid=success+Paid, Refunded=muted+'This booking was refunded — no payout.'+Refunded, Failed=attention) + summarizePayouts(rows)→{upcoming=Held+Processing net, paid=Paid net} — the SAME helper the page + test call. PayoutStateBadge keyed by STATE (Held/Refunded share muted but differ Clock vs Undo2): Paid=bg-success+CheckCircle2, Held=secondary+Clock, Processing=outline+ArrowLeftRight, Refunded=secondary-muted+Undo2, Failed=destructive Alert (NOT a badge) — never red on a happy state. PayoutRow (mobile card) = host-visible Booking {gross} / FitOut service fee (10%) −{commission} / Your payout {net} (net wt-600 tabular-nums; Refunded→struck/muted + helper) — host SEES the fee (D-59), booker never does (D-50). PayoutSummary = neutral Upcoming payouts / Paid out Display figures, no coral. Page: server-summed totals; venue-tz-safe Expected(endsAt+PAYOUT_DELAY_HOURS)/Paid(paidAt) dates (TZDate/format, each row its listing tz); desktop shadcn table (th scope=col) hidden md + PayoutRow cards md:hidden; PayoutBanner nudge when !enabled; No earnings yet empty state; 10%-fee explainer footnote; container max-w-4xl; NO bg-brand/coral. Added shadcn official table primitive. Neutral Earnings nav in host/page.tsx (outline, next to Your listings) + host/layout.tsx header (before Profile). All money server-frozen — zero arithmetic. 10 new tests (derivation + summary + owner-scope A≠B/absent-host + commission visibility); 65/65 payments+paymongo green; tsc+eslint clean on all 9 files. T-05-29/30/31/32 all mitigated. Deviation: NONE (one test-harness micro-fix: non-overlapping booking windows). KNOWN BEHAVIOR (not a stub): the page reads the ledger, whose rows are written by the T+24h sweep (05-05a) — a confirmed booking pre-sweep has no row until the sweep creates it (matches the plan's read-the-ledger scope). **HOST-03 COMPLETE. Wave 2 COMPLETE.** Commits ce13923 (T1) + 8ff110f (T2) + 5d2f1df (T3). ⚠ gsd-tools handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited (counter reconciled to the roadmap [x] count). Next: Wave 3 05-05b (payout reconcile Processing→Paid + /api/inngest serve mounting both crons).

Prior session: 2026-07-16T13:14:00Z
Stopped at: Completed 05-05a-PLAN.md — the Inngest T+24h host-payout SWEEP (PAY-03/PAY-02), the first async-scheduled job in the repo. Installed inngest@4.13.0 (no Redis/worker) + src/inngest/client.ts (shared client id fitout — NO serve mount; Plan 05b owns /api/inngest). src/inngest/functions/payout-sweep.ts: queryDuePayouts = RESEARCH Pattern-4 DB-clock sweep (b.status='confirmed' AND b.ends_at + make_interval(hours=>PAYOUT_DELAY_HOURS) <= now() AND p.id IS NULL; aliases hp.paymongo_account_id→paymongoAccountId); payOne = computeCommission FREEZE (D-51) → INSERT … ON CONFLICT (booking_id) DO NOTHING RETURNING id at-most-once claim (the INSERT is the lock, mirrors createPendingHold) → correlate wallet.id===b.paymongoAccountId among status='activated' wallets (no match ⇒ leave Held + console.error('[payout-alert] no activated wallet for host') + fire NOTHING, NEVER wallets[0]) → createBatchTransfer(netCents verbatim, D-52) → ledger Held→Processing + transfer_id (on throw: state='failed' + [payout-alert]). payoutSweep = hourly singleton {cron:'TZ=Asia/Manila 0 * * * *', concurrency:1} with per-booking step.run. .env.example sole-owns COMMISSION_RATE_BPS/PAYOUT_DELAY_HOURS/PAYMENT_WINDOW_MINUTES/PAYOUT_RECONCILE_STUCK_HOURS/PLATFORM_WALLET_*/INNGEST_*. NO route.ts created (Plan 05b mounts serve after both cron files exist → tsc-clean at this boundary). 6 new tests (payout-sweep: due-only, happy net-180000 to host's OWN wallet, at-most-once via makeRacingClients, multi-host A→A/B→B never crossed, no-match Held+alert; ledger-freeze: a later rate change never rewrites an existing row via the ON CONFLICT no-op). 55/55 payments+paymongo green; tsc+eslint clean on all 4 new files. DEVIATION (Rule 3): inngest 4.13.0 uses the 2-arg createFunction(options, handler) with the cron in options.triggers, not the RESEARCH 3-arg (config, trigger, handler) skeleton. PAY-02/PAY-03 kept In progress (PAY-03 lifecycle → Paid at 05-05b reconcile; PAY-02 host-visible commission at HOST-03/05-06; real transfer UAT-gated on PayMongo /v2 beta). Commits 68bb06d (T1) + 21674d5 (T2) + 7bdbed8 (T3). ⚠ gsd-tools string-arg/requirements/roadmap handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited. Next: 05-06 (HOST-03 earnings), then Wave 3 05-05b (payout reconcile + /api/inngest serve mounting both crons).

Prior session: 2026-07-16T12:52:36Z
Stopped at: Completed 05-04-PLAN.md — the PayMongo webhook is now the SOLE confirm authority (D-57). Extended the single-writer, signature-verified, paymongo_event-deduped route (src/app/api/paymongo/webhook/route.ts; verifySignature + dedupe untouched): checkout_session.payment.paid flips booking pending→confirmed on reference_number ALONE (UPDATE ... WHERE id=<ref> AND status='pending' RETURNING id — NO expires_at>now() re-check, Pitfall 4; the GiST EXCLUDE, not the TTL, is the double-confirm authority) + captures pay_... into booking.payment_id; A1 nesting event.data.attributes.data.attributes.{reference_number,payments[0].id,source.type,payment_method_used} asserted. handleGoneSlot (D-58): a 0-row confirm re-reads to skip a benign already-confirmed replay (never refund a paid booking), else refunds the frozen quoted_total_cents on {card,gcash,grab_pay,paymaya} via createRefund or operator-alerts {qrph,dob_ubp,unknown}/failures via [PAYMENT_ALERT] log + recordAudit(needs_attention) — never silent retention (Pitfall 1) — then sets booking cancelled→PaymentReversedState; double-book-during-payment proven. payment.refunded/refund.updated (D-60): resolveRefundPaymentId → idempotently flip host_payout_ledger held→refunded + booking cancelled (out of the confirmed-only Plan-05 sweep); replay=200 no-op. AuditOutcome += 'needs_attention'. Deviations: re-read guard (Rule 2), AuditOutcome extension (Rule 3), drizzle typed refund update (Rule 3). 12 new webhook tests; 49/49 payments+paymongo green; tsc+eslint clean. PAY-01 now COMPLETE. Commits 3030e0d (T1) + 9fda1fb (T2) + bacb114 (T3). ⚠ gsd-tools build lacks requirements/roadmap subcommands + state handlers misfit the wave-based STATE — STATE/ROADMAP/REQUIREMENTS hand-edited. Wave 2 remaining: 05-05a (Inngest payout sweep), 05-06 (HOST-03). Next: 05-05a or 05-06.

Prior session: 2026-07-16T12:32:59Z
Stopped at: Completed 05-03-PLAN.md — Confirm & pay checkout (D-57/D-58). confirmBooking RETIRES the synchronous pending→confirmed flip (the payment.paid webhook @05-04 becomes the sole confirm authority): owner-gate load of the frozen amount → already-confirmed idempotent short-circuit (D-42) → non-pending guard → rateLimit(confirm-pay:<userId> 5/60s)+audit (WR-06) → null-quote guard → EXTEND hold to now()+make_interval(mins=>PAYMENT_WINDOW_MINUTES) BEFORE charge (D-58 anti-race, T-05-16) → createCheckoutSession for EXACTLY quotedTotalCents (D-49, action takes only holdId, T-05-11), Idempotency-Key checkout:<holdId>, success=${BETTER_AUTH_URL}/bookings/<id>?paid=1, cancel=${BETTER_AUTH_URL}/listings/<listingId>/book?hold=<id>, reference_number=metadata.booking_id=holdId → redirect(checkoutUrl). ConfirmResult += reason:'checkout' (calm retry, never red). Reserve UI Confirm booking→Confirm & pay / Confirming…→Taking you to checkout…, reassurance 'You'll pay {₱total} now — cards, GCash, Maya, or QR Ph…', totalLabel threaded RSC→ReserveView→ReserveActions; PriceBreakdown final line 'Final price — no added fees. You'll pay this now.' booker stays subtotal=total (D-50 no commission line). /bookings/[id] (Next-16 async searchParams) branches: pending+paid→PendingPaymentState (neutral bounded router.refresh poller, never fabricates confirmed), pending w/o paid→reserve redirect, cancelled+paid→PaymentReversedState (calm D-58 landing), owner-gate+confirmed UI unchanged. TDD checkout-create.test.ts 4/4 (RED c2003e0→GREEN d2d9e2d); state-machine.test.ts updated (mock @/lib/paymongo). 62/62 booking+payments+paymongo green; tsc+eslint clean (10 files). Deviation: BETTER_AUTH_URL not the plan's nonexistent NEXT_PUBLIC_APP_URL. PAY-01 kept In-progress (confirm authority @05-04). Commits c2003e0/d2d9e2d/c358872/c07c804. Next: 05-04 (payment.paid webhook confirm authority + D-58 auto-refund backstop) — flip on reference_number ALONE (no expires_at>now() guard) to resolve the interstitial; cancelled-after-refund → PaymentReversedState.
Resume file: None

Prior session: 2026-07-16T12:11:14Z
Stopped at: Completed 07-05-PLAN.md — lifecycle expiry correctness. D-94 now TRUE at all three hold-write sites (expires_at = LEAST(now() + window, starts_at), computed by Postgres; zero JS clock reads in units.ts, grep-asserted). Request path applies the D-96 proportional split, floored at MIN_APPROVE_WINDOW_HOURS. D-93 approve guard folded into the existing atomic WHERE so a too-late approve falls through the pre-existing calm NOT_PENDING path and records decline_reason='too_close_to_start'. confirmBooking refuses post-start at checkout initiation using the DB clock. The payment webhook is UNCHANGED (D-57) and protected by a mutation-verified D-57 GUARD test. SlotState gained too_soon (display-only; no occupancy predicate touched); SlotPicker gained a required `mode` prop; search now drops too-soon slots. Two auto-fixed deviations: the D-42 trap on the units-exhausted exit (own-hold re-check added; a real intermittent false "just taken"), and the recurring auth cold-import timeout (30s budget). Full suite 63 files / 487 tests, exit 0. Next: 07-06 (bookings views).

Prior session: 2026-07-16T12:40Z
Stopped at: Completed 05-02-PLAN.md — PayMongo money-movement client (src/lib/paymongo.ts EXTENDED): version-less PAYMONGO_BASE + fully-versioned per-call paths (T-05-06); createCheckoutSession/createRefund/createBatchTransfer/listWalletAccounts wrapping paymongoFetch; PLATFORM_WALLET fail-closed boot; mockPayMongo +4 stubs; 33/33 green. Commits da95c5a + 253fc33.

Prior session: 2026-07-16T12:04:01.747Z
Stopped at: Completed 05-01-PLAN.md — money foundation (commission calc + payment config + host_payout_ledger/payment_id/currency-reconcile via 0008, applied to live DB). Wave 1 done; Wave 1 also includes 05-02. Next: 05-02.

Prior session: 2026-07-15T05:58Z
Stopped at: Completed 04-06-PLAN.md — the booking MUTATION layer + reserve-page atoms. placeHold (src/app/actions/booking.ts) = POST server action (never a GET side-effect) cloning blocks.ts but swapping host-ownership for a BOOKER-CAPABILITY gate: requireUserId (D-41) → canBook re-read from the DB (not the session) → server RE-DERIVATION of deriveBookable via listing⨝user⨝hostPayout (Security V4 — the reserve route group is NOT the gate) → bookingCreateSchema.safeParse → createPendingHold → revalidatePath(listing)+('/') → redirect(/listings/[id]/book?hold=<id>). confirmBooking = owner-gate bookerId===userId (IDOR) → idempotent short-circuit BEFORE the expiry check (already-confirmed OWN booking → /bookings/[id] no-op SUCCESS, D-42, never mapBookingError) → else ONE atomic UPDATE … status='confirmed',expires_at=NULL WHERE status='pending' AND expires_at > now() RETURNING id (server = sole expiry authority; 0 rows ⇒ graceful reason:'expired', never a silent confirm/500). Both return discriminated-union reasons; SUCCESS redirects (never ok:true). state-machine.test.ts 7 green via a redirect-capture harness (mock next/navigation redirect → RedirectError carrying the URL). Four atoms per UI-SPEC: PriceBreakdown (frozen quotedTotalCents+currency, Heading-600 tabular-nums Total, reserved Phase-5 fee slot, ZERO client arithmetic), HoldCountdown (role=timer 15-min from expiresAt, neutral + optional --destructive numerals ONLY final-60s, onExpire flip at 0; setInterval-only setState + ref-in-effect for react-hooks; suppressHydrationWarning digits), HoldExpiredState (calm TimerOff + coral/neutral CTAs, never red), ReserveActions (disable-on-click Confirm→Confirming… + 'You won't be charged yet.'). BOOK-01/03 NOT marked complete (action+atoms only; user-facing reserve/confirmation + Book-CTA wiring = 04-07) — BOOK-02 already Complete. 6 files tsc+eslint clean; booking suite 25/25. ⚠️ Pre-existing/out-of-scope (deferred-items.md, NOT 04-06): npm run build PAYMONGO_SECRET_KEY guard; npm run lint address-autocomplete.tsx:110. ⚠️ gsd-sdk v1.42.3 string-arg handlers still no-op — metric/decisions/session hand-written; advance-plan/update-progress/roadmap.update-plan-progress worked. Commits 778249b (T1) + 6691df6 (T2). Next: 04-07 (assemble reserve page + confirmation + wire the Book CTA to placeHold — completes BOOK-01/03).

Prior session: 2026-07-15T05:25Z
Completed 04-05-PLAN.md — search home UI at / (D-29): SearchResultCard (extends listing-card; neutral, PHP price, distance origin-only, whole-card Link carrying ?date=&start=&end=; title 500→600) + SearchResults (grid + sort Select + neutral Load more off the hasMore probe + skeleton + zero-result escape hatches + broadened alternatives + cold-start) + SearchBar (reused AddressAutocomplete; ONE combined category Select → single ?category, D-35; date/time; ₱/hr max; radius 2/5/10/25; one coral Search) serialized to the URL. app/page.tsx = public RSC: searchParamsSchema.safeParse (garbage → default city view) → two-stage searchListings; cumulative page 0..N fetch drives Load more (MAX_PAGES=50 cap). SEARCH-01..05 Complete. 4 files tsc+eslint clean. Commits 8d46a4e (T1) + 8c4c229 (T2) + e9169aa (T3).

Resume file: None
