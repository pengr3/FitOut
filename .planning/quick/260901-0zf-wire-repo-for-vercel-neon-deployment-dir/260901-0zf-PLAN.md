---
quick_id: 260901-0zf
slug: wire-repo-for-vercel-neon-deployment
phase: quick-260901-0zf
plan: 01
type: execute
wave: 1
depends_on: []
created: 2026-09-01
source: >-
  Every Vercel deploy of this repo has failed. The measured failure (Vercel build log, commit
  273a774 on `dev`, 2026-08-31) was `BETTER_AUTH_SECRET is not set. Refusing to boot in
  production` -> `Failed to collect page data for /api/cloudinary/sign` -> `Command "npm run
  build" exited with 1`. That specific error is fixed by setting env vars in the Vercel
  dashboard and is NOT in scope here. This plan does the REPO-SIDE wiring a Neon-backed deploy
  needs before it can work at all. No database is provisioned yet; Neon (Postgres 18, PostGIS,
  Singapore) is the PM's choice.
autonomous: true
requirements: [NEON-MIGRATE, VERCEL-BUILD, NEON-DOCS]

files_modified:
  - drizzle.config.ts
  - vercel.json
  - .env.example

must_haves:
  truths:
    - "NEON-MIGRATE — with DIRECT_DATABASE_URL unset (the overwhelmingly common local case), drizzle.config.ts resolves the SAME url it resolved before this change, proven by running one probe command on both sides of the edit and comparing the two RESOLVED lines, not by reading the code."
    - "NEON-MIGRATE — with DIRECT_DATABASE_URL set, it wins over DATABASE_URL, so `db:migrate` / `db:generate` / `db:studio` reach Neon's DIRECT endpoint while the app runtime keeps using the pooled one."
    - "NEON-MIGRATE — both `loadEnv` calls survive, in the same order, unedited: the header comment's argument for why they exist (drizzle-kit runs outside Next, so `.env.local` is not loaded for it) is unchanged and still true."
    - "NEON-MIGRATE — the local-Docker fallback string is byte-identical, so drizzle.config.ts still agrees with tests/setup.ts:20-22 and scripts/seed.ts:16-18 as the comment claims."
    - "NEON-MIGRATE — the new variable is EXPLAINED in the file's header comment at the same density as the surrounding prose. A bare code change with no WHY is off-style in this repo and does not count as done."
    - "VERCEL-BUILD — a Vercel build runs `next build` and nothing else. It no longer re-runs lint + the design suite, which .github/workflows/ci.yml already runs on every push to dev/main and every pull_request (ci.yml:356-359)."
    - "VERCEL-BUILD — vercel.json contains no `regions` key, no `env`/`build.env`, and no secret-bearing key of any kind."
    - "VERCEL-BUILD — package.json's `build` script is byte-unchanged, so CI's gate composition is untouched."
    - "NEON-DOCS — .env.example documents DIRECT_DATABASE_URL adjacent to the DATABASE_URL block, states plainly that UNSET is correct for local Docker work, and names the one-hostname-segment difference between Neon's pooled (`-pooler`) and direct endpoints."
    - "NEON-DOCS — the doc states which commands the drizzle.config.ts change covers (db:migrate, db:generate, db:studio) and which it does NOT (db:seed, db:test:setup read DATABASE_URL directly), rather than implying blanket coverage."
    - "NEON-DOCS — the doc warns that migrations never run themselves: no build step invokes db:migrate, so a fresh Neon database is EMPTY until an operator runs it once; and warns against adding it to the Vercel build, where every preview would re-run migrations against a shared database."
    - "NEON-DOCS — the doc records that `prepare: false` is deliberately NOT set on the app's postgres.js client, so a future reader does not 'fix' a non-problem."
    - "No secret value — no Neon password, no connection string with credentials, no key — is added anywhere in the repo."
  artifacts:
    - path: "drizzle.config.ts"
      provides: "DIRECT_DATABASE_URL-first resolution for drizzle-kit, with the WHY in the header comment"
      contains: "DIRECT_DATABASE_URL"
    - path: "vercel.json"
      provides: "The Vercel build override, version-controlled rather than set in the dashboard"
      contains: "buildCommand"
    - path: ".env.example"
      provides: "The operator-facing contract for DIRECT_DATABASE_URL: what sets it, what reads it, what does not"
      contains: "DIRECT_DATABASE_URL"
  key_links:
    - from: "drizzle.config.ts"
      to: "process.env.DIRECT_DATABASE_URL"
      via: "nullish-coalescing precedence ahead of DATABASE_URL"
      pattern: "process\\.env\\.DIRECT_DATABASE_URL\\s*\\?\\?"
    - from: "vercel.json"
      to: "next build"
      via: "buildCommand override of package.json's composed build script"
      pattern: "\"buildCommand\"\\s*:\\s*\"next build\""
    - from: ".env.example"
      to: "drizzle.config.ts"
      via: "the doc block names the file that consumes the variable"
      pattern: "drizzle\\.config\\.ts"
---

<objective>
Wire this repo for a Vercel + Neon deployment. Three edits, three commits, no runtime code touched.

Purpose: the project has no database yet and every Vercel deploy has failed. The specific measured
failure (a missing `BETTER_AUTH_SECRET`) is a dashboard problem and is deliberately NOT addressed here.
What IS addressed is the repo-side wiring that must exist before a Neon-backed deploy can work at all:
migrations must be able to reach a connection that supports them, the deploy build must stop re-running
gates CI already owns, and the new variable must be documented to this repo's standard so the next
operator does not have to reverse-engineer it.

Output: `drizzle.config.ts` prefers `DIRECT_DATABASE_URL`; a committed `vercel.json` pins the build
command to `next build`; `.env.example` carries a `DIRECT_DATABASE_URL` block that states its own limits.

`NEON-MIGRATE` / `VERCEL-BUILD` / `NEON-DOCS` are LOCAL labels for this quick task's three scope items.
They are not roadmap requirement IDs and not decision IDs. Do not invent a `D-NN` for any of this work —
no decision record exists for it, and citing a fabricated one would corrupt a load-bearing convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

@drizzle.config.ts
@.env.example
@package.json
@components.json
</context>

<measured_facts>
<!-- Verified before planning. Do NOT re-derive these, and do NOT plan around their opposites. -->

1. **git IS available in the Vercel build.** The design gate PASSED on Vercel (71 files / 1291 tests).
   Vercel does a `git clone --depth=10`, so `.git` is present and the `git ls-files` design tests work
   there. No change in this plan may be premised on git being unavailable at build time.

2. **`prepare: false` does NOT apply to Neon.** Neon's pooler runs PgBouncer 1.22+, which supports
   PROTOCOL-LEVEL prepared statements. The familiar Supabase/PgBouncer advice to disable prepared
   statements is about an older transaction-pooler limitation and is wrong here.

3. **`vercel.json` does not exist yet** (`ls vercel.json` -> No such file). `.vercel` is already
   gitignored (.gitignore, `# vercel` block). Nothing else in the repo root is a Vercel config.

4. **`$schema` is the house convention for root JSON config**: `components.json` opens with
   `"$schema": "https://ui.shadcn.com/schema.json"`. It is the only committed root JSON config besides
   package.json / package-lock.json / tsconfig.json.

5. **CI already runs lint + the design suite on every push and PR.** `.github/workflows/ci.yml:356-359`
   is `on: push: branches: [dev, main]` + `pull_request:` with no path filter. Its `gate-db-free` job is
   lint + design gate + `next build`. Re-running the first two inside every Vercel deploy is pure
   duplication (~90s) plus an extra failure surface on every preview.

6. **`db:migrate` is invoked by CI jobs only** (ci.yml:731, 834, 1036 and baselines.yml:326 — each
   against that job's own ephemeral service container). No build script and no deploy path runs it.
   That is exactly why a fresh Neon database stays empty until an operator runs it by hand.

7. **The probe harness in Task 1 is real — it was run against the current tree before this plan was
   written** and printed `RESOLVED: postgresql://probe:probe@example.invalid:5432/probe`. `npx tsx -e`
   with a relative import of `./drizzle.config.ts` works, and a shell-exported `DATABASE_URL` beats the
   value dotenv injects from `.env.local` (dotenv does not override an already-set variable).

8. **`.env.example` is not asserted by any test.** No file under `tests/` references it, so nothing
   turns red if the doc block is wrong. The only gate on Task 3 is the reviewer.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: drizzle.config.ts — prefer DIRECT_DATABASE_URL, and say why</name>
  <files>drizzle.config.ts</files>

  <action>
FIRST, BEFORE EDITING ANYTHING, capture the baseline the verification compares against. Confirm your
`.env.local` does not already define `DIRECT_DATABASE_URL` (`grep -c DIRECT_DATABASE_URL .env.local`
must print 0 — if it does not, the probe is meaningless; unset it for the duration). Then run the probe
in `<verify>` and record the `RESOLVED:` line verbatim. This is the "before" value. If you edit first
you cannot produce it, and the task's central claim becomes unprovable.

Then change the `DATABASE_URL` constant so resolution is exactly, in this precedence:
`process.env.DIRECT_DATABASE_URL`, then `process.env.DATABASE_URL`, then the existing local-Docker
default string. Nullish coalescing throughout, matching the existing idiom.

Three hard constraints, each protecting a claim some other file already makes in prose:

- Keep BOTH `loadEnv` calls and their ordering EXACTLY as they are (lines 22-23), untouched. The
  header comment spends eleven lines explaining why they exist and ties the idiom to `tests/setup.ts`.
  That reasoning is unchanged by this task, so the code it describes must be unchanged too.
- Keep the local-Docker fallback string BYTE-IDENTICAL. The comment above it asserts it is the same
  literal `tests/setup.ts:20-22` and `scripts/seed.ts:16-18` use; a stray character silently falsifies
  a cross-file correspondence three files rely on.
- A developer with only `DATABASE_URL` set must see NO behavioural change at all. Not "almost none".
  The `<verify>` block proves this by string equality, not by reading.

Then extend the file's HEADER COMMENT to explain the new variable. This is real work, not an
afterthought — this repo documents the WHY of every non-obvious line, and the surrounding prose is
dense, specific, and cites file:line. Match that voice and that density. Do not invent a decision ID.
The comment must convey:

- Neon exposes TWO endpoints per branch that differ by ONE hostname segment: the pooled host contains
  `-pooler`, the direct host does not. They are otherwise easy to confuse at a glance.
- The pooled endpoint is transaction-mode PgBouncer. Schema migrations need session-scoped state, and
  Neon's own docs list "schema migrations (Drizzle Kit)" as direct-connection work. Hence drizzle-kit,
  and only drizzle-kit, prefers the direct URL.
- The app runtime (`src/lib/db/index.ts`) keeps reading `DATABASE_URL` and should be pointed at the
  POOLED host: serverless instances multiply under load and the pooler is what absorbs that. The two
  variables are not a fallback pair for the same thing — they name two different endpoints on purpose.
- Locally, `DIRECT_DATABASE_URL` is unset, so resolution is the old expression unchanged. Nothing about
  a Docker-only workflow moves.
- The existing shell-beats-dotenv ordering still holds, so a one-off `DATABASE_URL=… npm run db:migrate`
  keeps working, and `DIRECT_DATABASE_URL=… npm run db:migrate` now works the same way.
- The coverage LIMIT, stated plainly: this file is read by `db:migrate`, `db:generate` and `db:studio`
  only. `scripts/seed.ts` and `scripts/db-test-setup.ts` read `DATABASE_URL` themselves and are NOT
  covered — against Neon they must be given the direct URL explicitly on the command line. (`.env.example`
  says the same thing in Task 3; the two must agree.)

Do NOT write the quoted fallback literal inside the comment — the verification greps for exactly one
occurrence of the quoted string in non-comment lines, and a second copy in prose defeats that gate.

Do NOT touch `src/lib/db/index.ts`. It is `drizzle(postgres(DATABASE_URL))` with no options, and TWO
long comments elsewhere — `src/lib/db/schema.ts` around line 836 and
`src/lib/payments/checkout-lease.ts` around line 30 — assert that fact and build on it: postgres.js's
default `max: 10` pool is the PREMISE of the argument for why the checkout lease is a CAS column rather
than an advisory lock. Adding client options would falsify both records. Neon needs no change there.
  </action>

  <verify>
  <automated>
# --- (a) BEFORE the edit: capture the baseline ---------------------------------------------
grep -c DIRECT_DATABASE_URL .env.local            # must print 0 (or the file must not exist)
DATABASE_URL='postgresql://probe:probe@example.invalid:5432/probe' \
  npx tsx -e "import c from './drizzle.config.ts'; console.log('RESOLVED:', c.dbCredentials.url)" \
  | grep '^RESOLVED:'
# Expected, and confirmed live against the current tree:
#   RESOLVED: postgresql://probe:probe@example.invalid:5432/probe

# --- (b) AFTER the edit: the no-DIRECT path must be string-identical ------------------------
DATABASE_URL='postgresql://probe:probe@example.invalid:5432/probe' \
  npx tsx -e "import c from './drizzle.config.ts'; console.log('RESOLVED:', c.dbCredentials.url)" \
  | grep '^RESOLVED:'
# PASS iff this line is byte-identical to (a). Any difference fails the task.

# --- (c) AFTER the edit: DIRECT wins when both are set --------------------------------------
DATABASE_URL='postgresql://probe:probe@example.invalid:5432/probe' \
DIRECT_DATABASE_URL='postgresql://direct:direct@example.invalid:5432/direct' \
  npx tsx -e "import c from './drizzle.config.ts'; console.log('RESOLVED:', c.dbCredentials.url)" \
  | grep '^RESOLVED: postgresql://direct:direct@example.invalid:5432/direct$'
# PASS iff this grep matches (exit 0).

# --- (d) the two structural guards ----------------------------------------------------------
git diff -U0 -- drizzle.config.ts | grep -E '^[-+].*loadEnv' | wc -l     # must be 0
grep -v '^\s*//' drizzle.config.ts \
  | grep -c '"postgresql://fitout:fitout@localhost:5432/fitout"'         # must be exactly 1
  </automated>
  <note>PowerShell equivalent for the env-prefixed lines: `$env:DATABASE_URL='...'; npx tsx -e "..."`.
  The Bash tool (git bash) accepts the POSIX form above as written.</note>
  </verify>

  <done>
(b) is byte-identical to (a); (c) matches; (d) prints 0 then 1. The header comment covers all six points
in the action list, in the surrounding voice, with no fabricated decision ID and no quoted copy of the
fallback literal. Committed alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: vercel.json — build with `next build`, not the CI-composed script</name>
  <files>vercel.json</files>

  <action>
Create `vercel.json` at the repo root with exactly two keys: `$schema` set to
`https://openapi.vercel.sh/vercel.json` (matching the house convention established by
`components.json`, which opens with its own `$schema`), and `buildCommand` set to `next build`.

The reason, which belongs in the commit message and the SUMMARY: `package.json`'s `build` script is
`npm run lint && npm run test:design && next build`. That composition exists to serve CI, and CI already
runs both halves — `.github/workflows/ci.yml:356-359` is `on: push: branches: [dev, main]` plus
`pull_request:`, with `gate-db-free` doing lint + design gate + build. Re-running them inside every
Vercel deploy buys nothing, costs roughly 90 seconds per deploy, and adds a large extra failure surface
to every preview. Vercel prefers `vercel.json` over a dashboard override, and unlike the dashboard it is
version-controlled and reviewable.

Do NOT add a `regions` key. A single-region pin risks an unsupported-config build failure depending on
plan tier; the region is being set through the Vercel dashboard instead. Adding it here would reintroduce
the exact class of failure this work exists to remove.

Do NOT add `env`, `build.env`, `builds`, or any secret-bearing key. Secrets belong in the Vercel
dashboard, never in a committed file.

Do NOT change `package.json`'s `build` script, and do not touch `package.json` at all. Four CI jobs
depend on it as-is.

Do NOT add a `_comment` / `//` pseudo-key to carry the rationale. This is the one place in this repo
where the WHY cannot live beside the code: JSON has no comments, and Vercel's config schema rejects
unknown properties — a comment key would fail config validation at deploy time, which is precisely the
failure class this task removes. Record the rationale in the commit message and the SUMMARY instead, and
say in the SUMMARY that this omission is deliberate, so a later reader does not read the bare file as
carelessness.
  </action>

  <verify>
  <automated>
# valid JSON, exactly the two intended keys, correct value, and none of the forbidden keys
node -e "const c=JSON.parse(require('fs').readFileSync('vercel.json','utf8'));
  const keys=Object.keys(c).sort();
  if (JSON.stringify(keys)!==JSON.stringify(['\$schema','buildCommand'])) { console.error('KEYS',keys); process.exit(1); }
  if (c.buildCommand!=='next build') { console.error('CMD',c.buildCommand); process.exit(2); }
  for (const f of ['regions','env','build','builds','secrets']) if (f in c) { console.error('FORBIDDEN',f); process.exit(3); }
  console.log('OK', keys.join(','));"

# package.json untouched by this task
git diff --quiet -- package.json && echo 'package.json UNCHANGED'

# no secret-shaped value anywhere in the new file
grep -nEi 'npg_|secret|key=|://[^/]*:[^/]*@' vercel.json || echo 'NO SECRETS'
  </automated>
  </verify>

  <done>
The node check prints `OK $schema,buildCommand`; `package.json UNCHANGED`; `NO SECRETS`. Committed alone,
with the CI-duplication rationale in the commit body.
  </done>
</task>

<task type="auto">
  <name>Task 3: .env.example — document DIRECT_DATABASE_URL and its limits</name>
  <files>.env.example</files>

  <action>
Add a `DIRECT_DATABASE_URL` block to `.env.example`, placed adjacent to the existing
`DATABASE_URL` / `TEST_DATABASE_URL` block at the top: after the commented `TEST_DATABASE_URL=` example
line (currently line 16) and before the `# --- Better Auth core ---` header (currently line 18).

Match this file's established voice. Read the `TEST_DATABASE_URL` block first — it is the direct model:
it opens by saying UNSET is the normal path, explains the mechanism, names the exact commands affected,
names what is NOT affected, and ends with a fully commented-out example line. Follow that shape. The
file is heavily prose-documented throughout and uses uppercase for the load-bearing clause; do not write
a terse two-line stub.

The block must convey, all of it:

- UNSET IS CORRECT for local Docker work. This variable exists for a hosted Postgres (Neon) deployment
  and does nothing otherwise.
- Neon gives two connection strings per branch that differ by ONE hostname segment: the pooled host
  contains `-pooler`, the direct host does not. They are trivially easy to mix up.
- The APP RUNTIME uses `DATABASE_URL` and should be pointed at the POOLED host — serverless instances
  multiply under load and the pooler is what absorbs that.
- OUT-OF-BAND TOOLING uses the DIRECT host, because transaction-mode pooling does not support the
  session-scoped features schema migrations need. `npm run db:migrate`, `db:generate` and `db:studio`
  pick this up automatically via `drizzle.config.ts` once the variable is set.
- The LIMIT, stated plainly rather than implied away: `npm run db:seed` and `npm run db:test:setup` read
  `DATABASE_URL` directly (`scripts/seed.ts`, `scripts/db-test-setup.ts`) and are NOT covered by the
  `drizzle.config.ts` change. Against Neon they must be given the direct URL explicitly on the command
  line. Do not describe coverage as blanket, and do not silently change those scripts.
- MIGRATIONS DO NOT RUN THEMSELVES. No build step invokes `db:migrate` (only CI jobs do, each against
  its own throwaway service container), so a freshly created Neon database is EMPTY until an operator
  runs it once by hand. Warn explicitly against adding `db:migrate` to the Vercel build: every preview
  deploy would then re-run migrations against a shared database.
- `prepare: false` is deliberately NOT set on the app's postgres.js client. Neon's pooler runs
  PgBouncer 1.22+, which supports protocol-level prepared statements, so the familiar Supabase/PgBouncer
  advice does not apply here. Say this so a future reader does not "fix" a non-problem — and note that
  `src/lib/db/index.ts` is intentionally optionless, because two other comments in the tree depend on
  its default pool size.
- End with a commented-out example line using an OBVIOUSLY fake placeholder host and no credentials,
  in the style of the commented `TEST_DATABASE_URL=` line above it.

Put NO real value anywhere: no Neon hostname from an actual project, no `npg_`-prefixed password, no
copied connection string. This file is committed.
  </action>

  <verify>
  <automated>
# the block exists ABOVE the Better Auth header, i.e. adjacent to the DATABASE_URL block
awk '/^# --- Better Auth core/{exit} /DIRECT_DATABASE_URL/{n++} END{exit !(n>0)}' .env.example \
  && echo 'PLACEMENT OK'

# it is documented as UNSET: no active assignment
test "$(grep -c '^DIRECT_DATABASE_URL=' .env.example)" -eq 0 && echo 'UNSET OK'

# every required topic is present (comment lines are the content here, so no comment filter)
for t in '\-pooler' 'db:migrate' 'db:seed' 'db:test:setup' 'prepare: false' 'db:studio'; do
  grep -q -- "$t" .env.example && echo "TOPIC OK: $t" || echo "TOPIC MISSING: $t";
done

# no real credential or real Neon host leaked into a committed file
grep -nEi 'npg_[A-Za-z0-9]|[A-Za-z0-9.-]+\.neon\.tech' .env.example && echo 'LEAK — FIX' || echo 'NO LEAK'

# nothing else in the file moved
git diff --stat -- .env.example
  </automated>
  </verify>

  <done>
`PLACEMENT OK`, `UNSET OK`, six `TOPIC OK` lines, `NO LEAK`, and the diffstat shows insertions only
(no deletions outside the inserted block). The prose covers all seven points and matches the
`TEST_DATABASE_URL` block's shape and density. Committed alone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public git history | `.env.example` and `vercel.json` are COMMITTED. Anything written into them is permanent and public-shaped. |
| repo -> Vercel build env | `vercel.json` is read by Vercel and determines what executes on every deploy and preview. |
| tooling -> Neon endpoints | Which hostname a migration reaches is decided by env-var precedence in `drizzle.config.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0zf-01 | Information disclosure | `.env.example`, `vercel.json` | mitigate | Task 3 forbids any real value and greps for `npg_`-prefixed passwords and `*.neon.tech` hosts; Task 2 forbids `env`/`build.env`/`secrets` keys and greps for credential-shaped strings. Both gates are in `<verify>`. |
| T-0zf-02 | Tampering | Vercel build pipeline | mitigate | `buildCommand` is pinned in a version-controlled, reviewable file rather than a dashboard field no diff ever shows. `regions` is deliberately excluded so no unsupported-config surface is added. |
| T-0zf-03 | Denial of service | Neon database | mitigate | Task 3 explicitly warns against wiring `db:migrate` into the Vercel build, where every preview deploy would re-run migrations against one shared database. Documented as a prohibition, not left implicit. |
| T-0zf-04 | Elevation of privilege | app runtime DB connection | accept | The app runtime is unchanged: `src/lib/db/index.ts` is untouched, still reads `DATABASE_URL`, still optionless. `DIRECT_DATABASE_URL` is read only by drizzle-kit, an operator-invoked tool. No new runtime path is created. |
| T-0zf-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. `package.json` and `package-lock.json` are explicitly out of scope; Task 2 asserts `package.json` is unchanged. The supply-chain surface is unmoved. |
</threat_model>

<explicitly_out_of_scope>
Doing any of these fails the plan even if the three tasks pass.

- `src/lib/db/index.ts` — do not touch. It is `drizzle(postgres(DATABASE_URL))` with NO options, and
  two long comments (`src/lib/db/schema.ts` ~line 836, `src/lib/payments/checkout-lease.ts` ~line 30)
  assert that as fact and reason from postgres.js's default `max: 10` to why the checkout lease is a CAS
  column rather than an advisory lock. Changing the client's options falsifies both records. Neon needs
  no change there, so the correct action is none.
- `scripts/seed.ts`, `scripts/db-test-setup.ts`, `scripts/ops-alerts.ts` — do not modify. Document the
  `DATABASE_URL` limitation (Task 3) instead of papering over it.
- `package.json`, `.github/workflows/ci.yml`, anything under `src/` — do not touch.
- Setting `BETTER_AUTH_SECRET` or any other Vercel env var. That is the measured deploy failure and it
  is a dashboard action, out of scope here. No task may attempt it.
- Any secret value, generated or copied, anywhere in the repo.
- A fourth task. Three, exactly.
</explicitly_out_of_scope>

<verification>
Run after all three tasks:

```
npm run lint                       # the repo's own gate; must pass
git status --porcelain             # exactly 3 tracked files changed, nothing untracked left behind
git diff --stat HEAD~3             # drizzle.config.ts, vercel.json, .env.example — and nothing else
```

Then re-run Task 1's probe (c) and Task 2's node check to confirm nothing later in the plan regressed
them. Three commits, one per task, each independently revertible.
</verification>

<success_criteria>
- Task 1 probe (b) is byte-identical to probe (a); probe (c) shows DIRECT winning; guard (d) prints 0 then 1.
- `drizzle.config.ts`'s header comment explains the new variable at the density of its neighbours, with no
  fabricated decision ID.
- `vercel.json` parses, has exactly `$schema` + `buildCommand`, `buildCommand` is `next build`, and none of
  `regions` / `env` / `build` / `builds` / `secrets` is present.
- `.env.example` documents DIRECT_DATABASE_URL adjacent to DATABASE_URL, unset, covering all seven points
  including the seed/test-setup limitation, the migrations-never-run-themselves warning, and the
  `prepare: false`-is-deliberately-absent note.
- `git diff HEAD~3 --name-only` lists exactly the three files.
- `npm run lint` passes.
- No secret anywhere.
</success_criteria>

<output>
Create `.planning/quick/260901-0zf-wire-repo-for-vercel-neon-deployment-dir/260901-0zf-SUMMARY.md` when done.

It must record, because these do not live anywhere else in the tree:
- The exact before/after `RESOLVED:` strings from Task 1's probe, side by side.
- Why `vercel.json` carries no comment key (JSON has none; Vercel's schema rejects unknown properties, so
  a `_comment` would be a deploy-time config failure) and no `regions` key.
- That `src/lib/db/index.ts` was deliberately left alone, and the two comments that depend on it.
- The operator's remaining manual steps, which this plan does NOT perform: set the Vercel env vars
  (including `BETTER_AUTH_SECRET`), set `DATABASE_URL` to Neon's POOLED host and `DIRECT_DATABASE_URL` to
  the DIRECT host, set the region in the dashboard, and run `npm run db:migrate` ONCE by hand against the
  fresh Neon database.
</output>
