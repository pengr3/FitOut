---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 10
type: execute
wave: 5
depends_on: [11-08, 11-09]
files_modified:
  - src/lib/nav.ts
  - src/components/patterns/site-chrome.tsx
  - src/components/patterns/auth-slot-skeleton.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/invite/[token]/page.tsx
  - src/app/listings/[id]/(detail)/layout.tsx
  - src/app/listings/[id]/(detail)/page.tsx
  - src/app/listings/[id]/book/layout.tsx
  - src/app/(auth)/layout.tsx
  - tests/design/sticky-offset.test.ts
autonomous: true
requirements: [SHELL-01]

must_haves:
  truths:
    - "Every public route carries a real FitOut header — /, /listings/[id], /listings/[id]/book and /invite/[token] render no navigation at all today"
    - "Header height is fixed by a spacing token, never by type: 56px below sm:, 64px at >=640px, IDENTICAL in court and grove"
    - "D-04 survives — what is shared is the BOX, not the content; the host and booker headers keep their own wordmarks and link sets"
    - "/listings/[id]/book renders the minimal presentation: zero <a href> inside the header and no footer, because SHELL-03 forbids navigation that can silently lose an active hold"
    - "The listing rail moves lg:top-8 -> lg:top-20; every lg:sticky offset in the app is >= header height + 16px, asserted as a source scan"
  artifacts:
    - path: "src/components/patterns/site-chrome.tsx"
      provides: "One geometry, three named slots (brand, nav, actions), and the reserved auth slot that cannot shift the page"
      contains: "site-header"
    - path: "src/lib/nav.ts"
      provides: "One source of truth for the host link data, rendered in two DOM placements"
    - path: "src/app/(public)/layout.tsx"
      provides: "The public composition for / and /invite/[token]"
    - path: "src/app/listings/[id]/book/layout.tsx"
      provides: "The minimal composition — brandHref null, empty nav and actions, no footer"
    - path: "tests/design/sticky-offset.test.ts"
      provides: "The source scan proving exactly one lg:sticky site exists and it is >= 80px"
  key_links:
    - from: "src/components/patterns/site-chrome.tsx"
      to: "src/lib/design/measurements.ts"
      via: "HEADER_HEIGHT and AUTH_SLOT_BOX, so the shell and its skeleton share one geometry"
      pattern: "HEADER_HEIGHT"
    - from: "src/app/listings/[id]/(detail)/page.tsx"
      to: "src/components/patterns/site-chrome.tsx"
      via: "the (detail) group layout, so book/ can present minimally as a sibling"
      pattern: "SiteChrome"
---

<objective>
Give the app one chrome geometry with three compositions, and restructure the route tree so the checkout
route can present minimally without inheriting a header it must not have.

Purpose: SHELL-01's four named routes render no navigation at all today, and three independently-drifting
header boxes exist. `site-chrome` ends the drift by sharing the box, not the content — the in-file
instruction at `(app)/layout.tsx:38-45` and `(host)/host/layout.tsx:56-63` (*"Do NOT refactor the two
headers into one here"*) is honoured, not contradicted.
Output: `site-chrome.tsx`, `nav.ts`, four layouts, three route moves, and the sticky-offset correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md

<interfaces>
The geometry contract (11-UI-SPEC § The geometry contract) — values, not inventions:

  element    <header data-testid="site-header">      one <header> landmark per document
  height     h-14 (56px) below sm:, sm:h-16 (64px)   FIXED BY TOKEN, NOT BY TYPE, identical in both themes
  container  mx-auto flex h-full w-full max-w-6xl items-center gap-3 px-4 sm:px-6
  surface    bg-background (public, booker) · bg-muted (host, shipped, preserved)
  boundary   border-b       ← NO shadow. --elevation-sticky has a NEGATIVE y-offset (an upward cast for
                              bottom-anchored bars); on a top header it renders nothing at all.
  sticky     sticky top-0 z-(--z-sticky)
  brand      text-lg font-semibold tracking-tight, --foreground, byte-identical to the three shipped wordmarks
  nav links  text-sm font-medium underline-offset-4 hover:underline, byte-identical to shipped

The auth slot, pinned as a measurement:
  <div data-testid="site-auth-slot" class="ml-auto flex h-8 min-w-44 items-center justify-end gap-3">
    <Suspense fallback={<AuthSlotSkeleton />}>   {/* also h-8 min-w-44 */}
  height h-8 is the only axis that reflows the page; ml-auto/justify-end anchors right so resolution moves
  only the cluster's own left edge; min-w-44 (176px = 4 × 44) bounds the horizontal delta.

The three compositions:
  public  brand "FitOut" -> /            nav —      actions anonymous: Log in (ghost) + Sign up (default)
  booker  brand "FitOut" -> /            nav —      actions ModeSwitch + NotificationBell + Profile
  host    brand "FitOut · Hosting" -> /host  nav Earnings, Requests (+count badge), in a drawer below md:
                                         actions ModeSwitch + NotificationBell + Profile

Measured at 320px in GROVE (the larger type — the worst case), 226px available after the wordmark:
  public/anonymous 138px  fits — NEVER collapses (hiding the acquisition CTA behind a hamburger is not a
                                 responsive strategy)
  booker           200px  fits, barely — NEVER collapses. Profile is an icon button below sm: and gains its
                          text label at sm: via a CSS-revealed <span className="hidden sm:inline">, one
                          instance, never a conditional render. The wrapper MUST carry aria-label="Profile"
                          and the icon aria-hidden="true" — `hidden` removes content from the a11y tree, so
                          without the label the control has NO accessible name below 640px (WCAG 4.1.2).
  host             352px  does NOT fit — Earnings and Requests move into a drawer behind a size-8
                          aria-label="Menu" trigger. ModeSwitch, NotificationBell and Profile stay inline.

Adding a session-aware slot flips /login, /signup, /forgot-password, /reset-password and /_not-found from
○ Static to ƒ Dynamic. Harmless for CI job 1 (dynamic routes are not prerendered); only the build marker
changes. RESEARCH caveat 1: re-run the unreachable-DATABASE_URL build after this lands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route groups — so the checkout route can present minimally as a sibling, not a child</name>
  <files>src/app/(public)/layout.tsx, src/app/(public)/page.tsx, src/app/(public)/invite/[token]/page.tsx, src/app/listings/[id]/(detail)/layout.tsx, src/app/listings/[id]/(detail)/page.tsx, src/app/listings/[id]/book/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (the root layout — it renders `<html>`/`<body>`, the theme provider, the Toaster and the metadata; it must NOT gain chrome, because everything including `(app)` and `(host)` nests inside it and would get two headers)
    - src/app/page.tsx, src/app/invite/[token]/page.tsx, src/app/listings/[id]/page.tsx, src/app/listings/[id]/book/page.tsx (the four routes being regrouped — read each one's exports before moving it)
    - src/app/(auth)/layout.tsx (the existing group-layout-that-only-supplies-a-container idiom)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The three compositions (the `/listings/[id]/book` and `(auth)` carve-outs)
  </read_first>
  <action>
The chrome must live in a **layout** (a `loading.tsx` replaces the page, so chrome rendered in a page
vanishes during load), and `/listings/[id]/book` must NOT inherit the public chrome. A layout at
`src/app/listings/layout.tsx` would nest into `book/`, so the route tree is regrouped instead. Route groups
do not change URLs — this is a pure file move plus three new layouts. Verify every URL after the move.

Moves (use `git mv` so history follows):
  src/app/page.tsx                    -> src/app/(public)/page.tsx
  src/app/invite/[token]/**           -> src/app/(public)/invite/[token]/**
  src/app/listings/[id]/page.tsx      -> src/app/listings/[id]/(detail)/page.tsx

New layouts:
  src/app/(public)/layout.tsx                    public composition
  src/app/listings/[id]/(detail)/layout.tsx      public composition
  src/app/listings/[id]/book/layout.tsx          MINIMAL composition

Each renders `<SiteChrome>` (Task 2) and `{children}` inside a `min-h-dvh flex flex-col` wrapper so the
footer plan (`11-14`) can hang a `mt-auto` footer off it without re-touching these files.

`book/layout.tsx` passes `brandHref={null}` so the brand renders as a `<span>`, not a `<Link>`, and passes
**neither** `nav` nor `actions`. That is not a fourth composition — it is the same shell with empty slots.
The `actions` slot is left empty **on purpose**: Phase 12's SHELL-03 fills it with the live hold countdown
rather than replacing a header this phase just built. Put that sentence in the file.

`src/app/(auth)/layout.tsx`: render the public composition and **remove its own centred wordmark**
(lines 24-31). The shell supplies it, and two wordmarks on one screen is exactly the duplication this phase
ends. Change nothing else about those screens — Phase 15 owns them.

Do **not** move `src/app/listings/[id]/book/page.tsx`, `src/app/(app)/**`, `src/app/(host)/**`,
`src/app/(auth)/**` or `src/app/dev/**`. Do not add chrome to the root layout.

After the moves, re-run the unreachable-`DATABASE_URL` build from RESEARCH § The Named Open Risk and record
the route table. The five `○ Static` routes are expected to flip to `ƒ Dynamic` once the session-aware slot
lands in Task 3 — that is harmless and predicted; a **connection error** is not, and would mean something now
reads the database at build time.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/public-listing.spec.ts e2e/search-and-book.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx`, `src/app/invite/`, and `src/app/listings/[id]/page.tsx` no longer exist; their `(public)` / `(detail)` counterparts do
    - `git log --follow src/app/(public)/page.tsx` shows the pre-move history (the move used `git mv`)
    - `curl -s -o /dev/null -w "%{http_code}" localhost:3000/` and the same for `/listings/{id}`, `/listings/{id}/book`, `/invite/{token}`, `/login` all return 200 against `npm run dev` with the seeded local data
    - `npm run build` exits 0 with `DATABASE_URL=postgres://unreachable:unreachable@127.0.0.1:59999/nope`
    - `e2e/public-listing.spec.ts` and `e2e/search-and-book.spec.ts` pass unchanged — no spec's URLs were edited
    - The root `src/app/layout.tsx` renders no `SiteChrome`
  </acceptance_criteria>
  <done>The route tree can express three compositions, and every URL is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: site-chrome, the nav data module, and the non-shifting auth slot</name>
  <files>src/components/patterns/site-chrome.tsx, src/components/patterns/auth-slot-skeleton.tsx, src/lib/nav.ts</files>
  <read_first>
    - src/app/(host)/host/layout.tsx:87-132 (the widest of the three shipped headers — the analog for the slots; copy the link classes and the Requests badge markup BYTE-FOR-BYTE)
    - src/app/(app)/layout.tsx (the booker header, for its wordmark and link set) and its lines 38-45 (the "Do NOT refactor the two headers into one here" instruction — honoured by sharing only the box, and AMENDED in plan 11-12, not here)
    - src/components/patterns/responsive-dialog.tsx (plan 11-09's output — the host drawer is a ResponsiveDialog, not a second overlay mechanism)
    - src/lib/design/measurements.ts (HEADER_HEIGHT, AUTH_SLOT_BOX)
    - src/lib/design/selector-contract.ts (site-header, site-brand, site-auth-slot, site-nav — any other id fails plan 11-02's ban)
    - src/lib/listing-vocab.ts or src/lib/design/status-tones.ts (the typed-inventory idiom `nav.ts` follows)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The geometry contract, § The auth slot, § Responsive behaviour
  </read_first>
  <action>
`src/lib/nav.ts` — one `as const` inventory of the host nav links (`Earnings` → `/host/earnings`,
`Requests` → `/host/requests`), each row carrying a mandatory reason/label pair, in the
`status-tones.ts` shape. **One source of truth for the links, two placements for the DOM.**

`src/components/patterns/site-chrome.tsx` — presentational, domain-ignorant, owns **geometry only**:
height, padding, container width, border, stickiness, and three named slots `brand`, `nav`, `actions`.
Props: `{ brand: ReactNode; brandHref: string | null; nav?: ReactNode; actions?: ReactNode; surface?:
"background" | "muted" }`. It imports nothing from `@/lib/{booking,payments,listing,group,search,availability}`
and it renders no product copy — the wordmark text is passed in, which is what lets the host keep
`FitOut · Hosting` and its `bg-muted` surface while all three produce an identical `boundingBox().height`.

Height comes from `HEADER_HEIGHT` in `measurements.ts`, never from padding around a `text-lg` wordmark:
grove's type is one step larger throughout, and a type-driven header measures differently per theme, which
makes every VR baseline theme-dependent in its *geometry* rather than only in its colour.

**Do not put `shadow-sticky` on this header.** Its y-offset is negative — an upward cast authored for
bottom-anchored bars — so on a top header it renders nothing. The boundary is `border-b`. Record that in a
comment; `shadow-sticky` keeps its zero call-site inventory until Phase 12's mobile CTA bar.

`data-testid="site-header"` on the `<header>`, `data-testid="site-brand"` on the brand element,
`data-testid="site-nav"` on the nav landmark, `data-testid="site-auth-slot"` on the actions wrapper.
When `brandHref` is `null` the brand renders as a `<span>`, not a `<Link>`.

The actions wrapper is the reserved auth slot: `ml-auto flex h-8 min-w-44 items-center justify-end gap-3`,
read from `AUTH_SLOT_BOX`. Create `src/components/patterns/auth-slot-skeleton.tsx` (Server Component) as
the `<Suspense>` fallback — one `Skeleton h-8 w-24` plus one `Skeleton size-8`, the whole thing
`aria-hidden`, sized from the same `AUTH_SLOT_BOX`. It reads as "two controls are coming", not as a blank.

Nav placement: render `nav` in a `hidden md:flex` bar and, when a `drawer` is requested, the **same**
presentational `<NavLinks>` inside a `md:hidden` `ResponsiveDialog` behind a `size-8` `aria-label="Menu"`
trigger. Use **`hidden`** — not `sr-only`, not `opacity-0` — so the inactive copy leaves the accessibility
tree and there is exactly **one** navigation landmark at any viewport.

`Profile` renders as an icon button below `sm:` and gains its text label at `sm:` through
`<span className="hidden sm:inline">` on **one** instance — never a conditional render. The wrapper carries
`aria-label="Profile"` and the icon `aria-hidden="true"`. Two mechanisms for two different reasons, and the
comment must say both: `hidden` to avoid duplicating nav text in the a11y tree, `aria-label` because an
icon-only control otherwise has no accessible name below 640px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts tests/design/elevation-z.test.ts tests/design/leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `site-chrome.tsx` imports nothing from `@/lib/{booking,payments,listing,group,search,availability}` and contains no product sentence
    - `grep -c "shadow-sticky" src/components/patterns/site-chrome.tsx` returns 0; `grep -c "border-b" ` returns >= 1
    - Height classes come from `HEADER_HEIGHT`; `grep -o "h-14\|h-16" src/components/patterns/site-chrome.tsx` returns nothing (the literal lives only in `measurements.ts`)
    - The four declared ids appear exactly once each and plan 11-02's undeclared-id assertion passes
    - `brandHref={null}` renders a `<span>` — assert by reading the JSX branch
    - The Profile control carries `aria-label="Profile"` and its icon `aria-hidden="true"`; the label span uses `hidden sm:inline`
    - The drawer is a `ResponsiveDialog`; `grep -c "ui/sheet" src/components/patterns/site-chrome.tsx` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>One geometry with three slots exists, the auth slot reserves its box, and the host drawer reuses the one overlay primitive.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the public compositions, correct the sticky offset, and gate it</name>
  <files>src/app/(public)/layout.tsx, src/app/listings/[id]/(detail)/layout.tsx, src/app/listings/[id]/book/layout.tsx, src/app/(auth)/layout.tsx, src/app/listings/[id]/(detail)/page.tsx, tests/design/sticky-offset.test.ts</files>
  <read_first>
    - src/lib/auth.ts and src/app/(app)/layout.tsx:28-31 (how `auth.api.getSession({ headers: await headers() })` is called — the public slot reads the session the SAME way, server-side; a client fetch would leak session shape into the bundle and reintroduce the layout shift the slot contract exists to prevent)
    - src/components/patterns/site-chrome.tsx (Task 2's output)
    - src/app/listings/[id]/(detail)/page.tsx (the moved file; its line ~346 renders `<Card className="lg:sticky lg:top-8">`)
    - src/components/patterns/panel-card.tsx (plan 11-08's `sticky` prop already encodes `lg:top-20` — this task only corrects the raw class; plan 11-13 converts the container)
    - tests/design/leak.test.ts (the per-file source-scan idiom `sticky-offset.test.ts` copies)
  </read_first>
  <action>
Wire the three public-composition layouts and `(auth)/layout.tsx` to `SiteChrome`.

The auth slot's session read is **server-side**: an async child inside the `<Suspense>` boundary calls
`auth.api.getSession({ headers: await headers() })` and renders either `Log in` (ghost) + `Sign up`
(`variant="default"`, neutral — not accent; D-21 reserves coral for where someone asked for it, and Phase 15
owns the auth surfaces) or the signed-in cluster. **Never a client fetch.** State the reason in the file.

`Sign up` is `variant="default"`, not `variant="brand"`. Making the acquisition CTA coral is a defensible
product call — it is just not this phase's to make, and the accent reserved-for list is closed at 8 entries.

Correct `src/app/listings/[id]/(detail)/page.tsx`'s rail: `lg:sticky lg:top-8` → `lg:sticky lg:top-20`.
32px < 64px, so the rail would tuck under the new sticky header on every scroll. This is a class-only edit;
plan `11-13` converts the container to `PanelCard sticky`.

Create `tests/design/sticky-offset.test.ts`: scan `src/app/**` and `src/components/**` for `lg:sticky` and
assert (a) the count is exactly 1 today, and (b) every site's paired `lg:top-{n}` resolves to ≥ 80px
(64px header + 16px gap). State the rule once in the header: *every `lg:sticky` offset in the app is ≥ header
height + 16px*. Strip comments before matching (`tests/design/strip-comments.test.ts`'s stripper) — this repo
has been bitten repeatedly by a grep landing on prose. Add guard-the-guard: the scanner walked ≥ 50 files and
found `listings/[id]/(detail)/page.tsx` by name, so a count of 1 cannot come from a scanner that read
nothing. Add a both-directions self-test: a synthetic `lg:sticky lg:top-8` must be flagged, `lg:top-20` must
not. NOT COVERED footer: this gate reads classes, not computed geometry — the rendered assertion is plan
`11-21`'s shell measurement spec.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --config vitest.design.config.ts tests/design/sticky-offset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "lg:top-8" src/app src/components` returns 0
    - `tests/design/sticky-offset.test.ts` passes; watched red by restoring `lg:top-8` at the rail (fails naming the file and the offset) and by adding a second `lg:sticky` site (fails the count). Both reverted, both recorded in the header.
    - The auth slot's session read happens in a Server Component via `await headers()`; `grep -rn "authClient\|useSession\|fetch(\"/api/auth" src/components/patterns/ src/app/\(public\)/layout.tsx` returns nothing
    - `Sign up` renders `variant="default"`; `grep -c 'variant="brand"' src/app/\(public\)/layout.tsx` returns 0
    - `src/app/(auth)/layout.tsx` no longer renders its own centred wordmark, and `/login` shows exactly one wordmark
    - `npm run build` exits 0 with the unreachable `DATABASE_URL`; record the route table and note which routes flipped `○` → `ƒ`
    - Push to `origin/dev`; all three CI jobs green before the next plan starts (D-25)
  </acceptance_criteria>
  <done>Every public route carries the header, the checkout route carries the minimal one, and the sticky-offset rule is asserted rather than remembered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session store → header render | The auth slot's content is session-derived on a fully public page |
| public route tree ↔ checkout route | A navigation affordance on `/listings/[id]/book` can silently lose an active hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SESSION | Information Disclosure | the header auth slot | mitigate | The session is read server-side with `await headers()` + `auth.api.getSession` inside an async Suspense child; a client fetch is banned by an acceptance criterion, because it would both leak session shape into the bundle and reintroduce layout shift |
| T-11-HOLDLOSS | Denial of Service | `/listings/[id]/book` | mitigate | Route-group restructure so `book/` is a sibling of `(detail)`, not a child; its layout passes `brandHref={null}` and no `nav`/`actions`, giving zero `<a href>` inside the header |
| T-11-NAVDUP | Denial of Service (accessibility) | host nav drawer | mitigate | One link inventory (`src/lib/nav.ts`), two DOM placements, `hidden` rather than `sr-only`/`opacity-0` so exactly one navigation landmark exists at any viewport |
| T-11-NONAME | Denial of Service (accessibility) | the Profile control below 640px | mitigate | `aria-label="Profile"` on the wrapper and `aria-hidden` on the icon — `hidden` alone would leave the control with no accessible name (WCAG 4.1.2) |
| T-11-DBFREE | Tampering | build-time database reads | mitigate | Re-run the unreachable-`DATABASE_URL` build after the shell lands (RESEARCH caveat 1 / Assumption A5); CI job 1 asserts it on every push thereafter |
| T-11-SC | Tampering | npm / shadcn registry | mitigate | Zero packages, zero registry blocks; the drawer reuses `patterns/responsive-dialog.tsx` |
</threat_model>

<verification>
- `npm run build` exits 0 with an unreachable `DATABASE_URL`
- `npm run test:design` green with one new gate file
- `npm run test:e2e` retains its phase-baseline signature (17 passed / 1 failed, the pre-existing D-6 item 1)
- All three CI jobs green on `origin/dev` (D-25)
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- SHELL-01's four navigation-less routes carry a real header
- Header height is token-fixed and theme-independent
- The checkout route presents minimally by construction, not by a conditional
- The one shipped `lg:sticky` offset is corrected and the rule is gated
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-10-SUMMARY.md` when done
</output>
