---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 21
type: execute
wave: 11
depends_on: [11-13, 11-16, 11-17, 11-18, 11-19, 11-20]
files_modified:
  - src/app/dev/theme/page.tsx
  - e2e/shell.spec.ts
  - e2e/overflow-320.spec.ts
  - e2e/skeleton-geometry.spec.ts
autonomous: true
requirements: [RESP-01, SHELL-01, STATE-01]

must_haves:
  truths:
    - "/dev/theme is EXTENDED with sections 10-14 rather than a second dev route being built — it already renders two themes in nested [data-theme] subtrees, needs no DB, no network and no seed, and 404s in production"
    - "Header and brand bounding boxes are byte-identical before and after the auth slot resolves, at 375/768/1280 in court and grove, in all three compositions — twelve assertions a reviewer cannot rubber-stamp"
    - "getByRole('navigation') resolves to exactly 1 at 320px and at 1280px"
    - "document.documentElement.scrollWidth <= clientWidth at 320px on all 12 named routes, in BOTH themes — this harness is NET-NEW: e2e/ contains zero setViewportSize calls and zero scrollWidth assertions today"
    - "Skeleton and resolved containers match within +/-2px for the card-grid, row-list and panel shapes, in both themes — jsdom cannot catch this class of bug at all"
  artifacts:
    - path: "src/app/dev/theme/page.tsx"
      provides: "Sections 10-14: the three card patterns, both empty tones, the error state, the sheet in both presentations, and the auth-slot fallback"
    - path: "e2e/shell.spec.ts"
      provides: "AC#2/#3/#4/#5 — header geometry, the no-shift auth slot, one navigation landmark, and the checkout carve-out"
    - path: "e2e/overflow-320.spec.ts"
      provides: "AC#29 — the 320px sweep across 12 routes x 2 themes, net-new work"
    - path: "e2e/skeleton-geometry.spec.ts"
      provides: "AC#17 — the +/-2px rendered assertion GATE-STATES requires as a RENDERING check"
  key_links:
    - from: "e2e/shell.spec.ts"
      to: "e2e/helpers/theme.ts"
      via: "seedTheme on the context before the first goto — the key is imported from the provider, never duplicated"
      pattern: "seedTheme"
    - from: "src/app/dev/theme/page.tsx"
      to: "src/components/patterns/**"
      via: "one ThemePane function called twice, so a difference between the columns can only have come from the theme"
      pattern: "ThemePane"
---

<objective>
Give the pattern layer a DB-free exercising surface and write the three rendered measurement specs the
source gates structurally cannot cover.

Purpose: GATE-STATES is explicitly a *rendering* assertion — *"jsdom cannot catch this class of bug at
all"*. Header geometry, the no-shift auth slot, one navigation landmark, 320px overflow and skeleton
fidelity are all measurements in a real browser, and all five need a surface with no seed behind it.
Output: `/dev/theme` sections 10–14 and three Playwright measurement specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md

<interfaces>
⚠ CORRECTION TO THE PHASE INPUTS (11-PATTERNS § F(2), measured): 11-RESEARCH marks RESP-01's harness as
"⚠ harness exists (10-16)". Measured: **no setViewportSize call and no documentElement.scrollWidth
assertion exists anywhere in e2e/.** Only the MEASUREMENT IDIOM transfers
(scroll-area-overflow.spec.ts:143-172 — a typed measurement object returned from the browser, asserted in
Node against a named tolerance constant). The 320px sweep across 12 routes × 2 themes is NET-NEW work.

The 12 routes for AC#29 (11-UI-SPEC § Responsive Baseline): /, /listings/[id], /listings/[id]/book,
/invite/[token], /terms, /privacy, the root not-found, and each of the five error boundaries.

The theme seam — e2e/helpers/theme.ts:45-59. Call seedTheme on the CONTEXT, before the first goto. The
storage key is IMPORTED from the provider, never duplicated: lines 3-9 explain that a duplicated literal
makes the swap smoke "go green while screenshotting the same theme twice".

Anti-vacuity idiom to copy — scroll-area-overflow.spec.ts:215-223 (`expectReachable`): assert the page
renders the thing under test BEFORE asserting anything about it.

⚠ Inherited trap: playwright.config.ts's `reuseExistingServer: !process.env.CI`. Both reduced-motion.spec.ts
:45-51 and scroll-area-overflow.spec.ts:49-56 carry the same warning — a dev server left running across a
checkout serves a stale stylesheet and produces a false red. Copy that warning into every new spec.

/dev/theme's existing shape: nine sections (Section calls at :175, :190, :231, :255, :263, :273, :329, :347,
:364), a `Section` helper at :139-162, and the one-function-called-twice rule at :164-170 — "ONE function
called twice, rather than two copies — a preview whose panes can drift is a preview that will eventually
attribute a difference to the theme that actually came from the markup." It already exports
`metadata` with `robots: { index: false, follow: false }` and 404s in production.

Deviation recorded: 11-VALIDATION maps the ±2px skeleton assertion to `--project=visual`. It is placed in
the DEFAULT chromium project here instead, because a boundingBox comparison needs no baseline and coupling
it to the Linux-only project would make it unrunnable locally. The GATE-STATES requirement it satisfies is
"a rendering assertion in a real browser", which this is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /dev/theme sections 10–14</name>
  <files>src/app/dev/theme/page.tsx</files>
  <read_first>
    - src/app/dev/theme/page.tsx (the whole file: the `Section` helper at :139-162, the `ThemePane` one-function-called-twice rule at :164-170, the nine existing `Section` calls, the `metadata` export at :87-90, and the `STICKY_INVENTORY` zero-inventory idiom at :96-107)
    - src/components/patterns/{result-card,row-card,panel-card,empty-state,error-state,responsive-dialog,auth-slot-skeleton}.tsx (everything sections 10-14 must exercise)
    - src/components/patterns/{card-grid,row-list,panel}-skeleton.tsx (Task 3's ±2px spec needs both a skeleton and its resolved twin on this page)
    - e2e/reduced-motion.spec.ts:58 and e2e/scroll-area-overflow.spec.ts:120 (both drive `/dev/theme`; :26-30 states why — it needs no auth, no DB and no seed)
  </read_first>
  <action>
Extend `/dev/theme` with sections 10–14, continuing the existing numbering and reusing the `Section` helper.
**Do not build a second dev route** — this page already renders two themes in nested `[data-theme]` subtrees,
needs no database, no network and no seed, 404s in production, and is already the home of the theme-override
seam this phase's smoke depends on.

  10. The three card patterns — `ResultCard`, `RowCard`, `PanelCard`, each with fixture props defined
      **inside this file** (no domain import, no fetch), including a `ResultCard` with `mediaFallback` so the
      no-broken-image path is visible.
  11. Both empty tones — `EmptyState tone="neutral"` and `tone="positive"`, side by side, so the
      green-retreats-to-the-icon rule is comparable across themes.
  12. `ErrorState` with a `digest` present and a `digest` absent.
  13. `ResponsiveDialog` in both presentations. It cannot be open at both breakpoints at once in one
      screenshot, so render the trigger plus a **statically-open** instance whose classes are the mobile
      presentation, and label which is which. State the limitation in the section note rather than faking it.
  14. The auth-slot fallback — `AuthSlotSkeleton` beside a resolved actions cluster of the same
      `AUTH_SLOT_BOX`, so the reserved geometry is visually comparable, plus each of the three skeleton
      shapes beside its resolved twin (this is the pair Task 3 measures).

**The one-function-called-twice rule at :164-170 must not break.** Everything goes inside `ThemePane`, which
is called once per theme. A section rendered outside it would attribute a markup difference to the theme.

Keep the `robots: { index: false, follow: false }` metadata and the production 404 behaviour. Add the new
sections' fixture data as module constants with a comment stating they are fixtures, so a reader does not
mistake them for product copy.

Note the D-9 consequence in the SUMMARY: this page's utilities ship to production even though the route
404s. Plan 10-16 measured the cost at +1,103 bytes (+0.93%) and dispositioned it as accepted debt; measure
the new delta the same way (sum the emitted chunks on clean rebuilds of both sides) and record it.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/reduced-motion.spec.ts e2e/scroll-area-overflow.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `/dev/theme` renders 14 sections per pane; both panes render the same section count
    - Every new section is inside `ThemePane`; `grep -c "ThemePane" src/app/dev/theme/page.tsx` shows it still called exactly twice
    - The page needs no database: `npm run build` with `DATABASE_URL=postgres://unreachable:unreachable@127.0.0.1:59999/nope` exits 0 and `/dev/theme` is still `○ (Static)`
    - `npm run build && npm start` → `/dev/theme` returns 404
    - Zero duplicate DOM ids across the two panes (re-run 10-16's check)
    - `e2e/reduced-motion.spec.ts` and `e2e/scroll-area-overflow.spec.ts` still pass against the extended page
    - The stylesheet byte delta is measured on clean rebuilds and recorded
  </acceptance_criteria>
  <done>Every pattern this phase built is exercised on one DB-free page, in both themes, from one function.</done>
</task>

<task type="auto">
  <name>Task 2: The shell measurement spec and the 320px sweep</name>
  <files>e2e/shell.spec.ts, e2e/overflow-320.spec.ts</files>
  <read_first>
    - e2e/scroll-area-overflow.spec.ts (the whole file: the `page.evaluate` measurement harness at :143-172, the named tolerance constant at :123, the `expectReachable` guard at :215-223, the OBSERVED RED/GREEN RUN header at :59-109, and the `reuseExistingServer` warning at :49-56)
    - e2e/helpers/theme.ts (`seedTheme`, and plan 11-03's amended header)
    - src/components/patterns/site-chrome.tsx (the four declared ids and the `min-w-44` / `h-8` reservation)
    - src/lib/design/measurements.ts (HEADER_HEIGHT and AUTH_SLOT_BOX — the spec asserts the rendered numbers those classes resolve to: 56 and 64)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The auth slot (the twelve-assertion acceptance criterion) and § Responsive Baseline (the 12 routes)
  </read_first>
  <action>
**`e2e/shell.spec.ts`** — AC#2, #3, #4, #5.

  AC#2: `page.getByTestId("site-header").boundingBox().height` is **56** at 375px and **64** at ≥640px,
        identical in court and grove, in all three compositions (public, booker, host). Assert the number,
        not a range.
  AC#3: capture `site-header` and `site-brand` bounding boxes with the auth slot still **pending** and again
        after it resolves, and assert both boxes are **byte-identical** — at 375 / 768 / 1280 × court /
        grove, in all three compositions. Stall the session request with `page.route` and a delay rather
        than measuring CLS, which is flaky. That is twelve assertions and it is deliberately not
        rubber-stampable.
  AC#4: `getByRole("navigation")` resolves to exactly **1** at 320px and at 1280px on the host composition
        (the one that collapses into a drawer).
  AC#5: `/listings/[id]/book` renders **zero** `<a href>` inside `[data-testid="site-header"]` and **no**
        `[data-testid="site-footer"]`.
  AC#6: the host composition renders `bg-muted` and a wordmark whose text differs from the booker's.

Signed-in compositions need a session. Use the shipped e2e sign-in idiom rather than seeding rows — read
`e2e/login-persistence.spec.ts` for how it authenticates — and keep the spec out of CI (D-24); it runs
locally and before `/gsd:verify-work`.

**`e2e/overflow-320.spec.ts`** — AC#29, net-new. For each of the 12 named routes × both themes:
`await page.setViewportSize({ width: 320, height: 800 })`, then a single `page.evaluate` returning
`{ scrollWidth, clientWidth, offenders }` where `offenders` lists the selectors of elements whose right edge
exceeds the viewport — so a failure **names the element** instead of just reporting a number. Assert
`scrollWidth <= clientWidth` with the named tolerance constant idiom.

The five error boundaries need to be reachable: drive them through the same dev throw affordance plan
`11-18` added under `src/app/dev/**`, per group. If a group's boundary cannot be reached that way, say so
and skip that route with a **named reason**, never silently.

Both specs: `expectReachable` guards before every measurement, and the `reuseExistingServer` stale-stylesheet
warning copied into the header verbatim.
  </action>
  <verify>
    <automated>npx playwright test e2e/shell.spec.ts e2e/overflow-320.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Both specs pass locally against a freshly started dev server
    - `e2e/shell.spec.ts` contains at least 12 distinct box-equality assertions for AC#3, across 3 widths × 2 themes × 2 boxes
    - Header height assertions are the exact integers 56 and 64, and pass in both themes
    - `getByRole("navigation")` count is asserted `=== 1` at both 320px and 1280px
    - `/listings/[id]/book` yields zero header anchors and zero footers
    - `e2e/overflow-320.spec.ts` covers 12 routes × 2 themes = 24 measurements; any skipped route carries a named reason in the skip message
    - Watched red: temporarily add `min-w-[400px]` to the footer container → the sweep fails at 320px **naming the offending selector**. Reverted. Both runs recorded in the spec header, in the `scroll-area-overflow.spec.ts:59-109` OBSERVED RED / GREEN RUN format.
    - Watched red: temporarily change the header to `py-3` around the wordmark → AC#2 fails with different heights in court and grove, which is the type-driven-header defect the token-fixed height exists to prevent. Reverted, recorded.
  </acceptance_criteria>
  <done>The shell's geometry, its no-shift contract and the 320px floor are measured in a real browser, and each has been watched failing.</done>
</task>

<task type="auto">
  <name>Task 3: The ±2px skeleton fidelity spec</name>
  <files>e2e/skeleton-geometry.spec.ts</files>
  <read_first>
    - e2e/scroll-area-overflow.spec.ts:143-172 (the typed `page.evaluate` measurement object) and :123 (`const TOLERANCE_PX = 0.5` — the named-tolerance idiom)
    - src/app/dev/theme/page.tsx section 14 (Task 1's output — each skeleton beside its resolved twin)
    - src/components/patterns/{card-grid,row-list,panel}-skeleton.tsx and src/lib/design/measurements.ts
    - e2e/helpers/theme.ts
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Loading (the two-layer approach; layer 1 is the source gate from plan 11-07, this is layer 2)
  </read_first>
  <action>
Create `e2e/skeleton-geometry.spec.ts` — AC#17, the rendered half of "does not shift".

For each of the three canonical shapes (card grid, row list, panel), on `/dev/theme` section 14, capture the
**skeleton container's** `boundingBox()` and its **resolved twin's**, and assert their width and height
match within a named `const TOLERANCE_PX = 2`. Run in **both themes** — grove's larger type is exactly where
a skeleton sized against court silently drifts, and that is the whole reason the both-themes clause exists.

Use `expectReachable` first: assert both containers rendered before comparing them. A spec that compares two
missing boxes passes vacuously.

State in the header why this layer exists at all: `11-UI-SPEC` § Loading specifies two layers because one
alone is a rubber stamp — the source gate (`tests/design/skeleton-measurements.test.ts`) proves the classes
come from `measurements.ts`, and this spec proves the resulting geometry actually matches. GATE-STATES is
explicitly a rendering assertion and *"jsdom cannot catch this class of bug at all"*.

Record the deviation from `11-VALIDATION.md` explicitly: it maps this to `--project=visual`, and it is
placed in the default `chromium` project instead because a `boundingBox` comparison needs no baseline, and
coupling it to the Linux-only project would make it unrunnable on the developer's machine. The requirement
it satisfies — a rendering assertion in a real browser — is unchanged.

Copy the `reuseExistingServer` stale-stylesheet warning.
  </action>
  <verify>
    <automated>npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - The spec passes in both themes for all three shapes — six comparisons
    - `TOLERANCE_PX` is a named constant equal to 2, and the failure message prints both measured boxes
    - Watched red: temporarily change `ROW_CARD_HEIGHT` in `measurements.ts` from `h-20` to `h-24` → the row-list comparison fails by 16px in both themes. Reverted. Both runs recorded in the header.
    - Watched red (vacuity): point the spec at a section that does not exist → `expectReachable` fails rather than the comparison passing
    - The deviation from `11-VALIDATION.md`'s `--project=visual` mapping is recorded in the header with its reason
    - Push and CI green (D-25); note that this spec, like the other 11, is deliberately NOT in CI (D-24)
  </acceptance_criteria>
  <done>The "does not shift" claim is measured in a browser in both themes, and it has been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev preview route → production | `/dev/theme` must remain unreachable in a production build |
| measurement spec → seeded data | A gate with a seed dependency stops running the first time the seed changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-DEVROUTE | Information Disclosure | `/dev/theme` sections 10-14 | mitigate | The route keeps `robots: { index: false, follow: false }` and 404s in a production build; verified with `npm run build && npm start`. Section fixtures are module constants, never product data. |
| T-11-VACUOUS | Repudiation | all three measurement specs | mitigate | `expectReachable` guards before every measurement, in the `scroll-area-overflow.spec.ts:215-223` form; each spec is watched failing on a real defect and on a vacuity probe |
| T-11-STALECSS | Repudiation | local spec runs | mitigate | The `reuseExistingServer` stale-stylesheet warning is copied into all three spec headers, matching the two shipped specs that already carry it |
| T-11-SESSION | Information Disclosure | `e2e/shell.spec.ts`'s signed-in compositions | mitigate | Uses the shipped e2e sign-in idiom; the spec stays out of CI (D-24), so no session secret enters the workflow |
| T-11-THEMEBLIND | Repudiation | both-themes coverage | mitigate | `seedTheme` on the context with the storage key imported from the provider, never duplicated — a duplicated literal makes the smoke go green while screenshotting the same theme twice |
| T-11-SC | Tampering | npm | mitigate | Zero packages |
</threat_model>

<verification>
- `npm run build` exits 0 with an unreachable `DATABASE_URL`; `/dev/theme` still `○ (Static)` and 404s in production
- `npx playwright test e2e/shell.spec.ts e2e/overflow-320.spec.ts e2e/skeleton-geometry.spec.ts` all pass
- `npm run test:design` green
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- Every pattern this phase built is exercised on one DB-free, two-theme page
- Header geometry, the no-shift auth slot, the single navigation landmark and the 320px floor are measured, not asserted
- The "skeleton does not shift" claim has a rendered proof in both themes
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-21-SUMMARY.md` when done
</output>
