---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 13
type: execute
wave: 6
depends_on: [11-08, 11-10]
files_modified:
  - src/app/listings/[id]/(detail)/page.tsx
  - src/components/booking/price-breakdown.tsx
  - src/components/host/payout-summary.tsx
  - src/app/(public)/invite/[token]/page.tsx
  - src/app/(host)/host/page.tsx
  - tests/design/card-pattern-coverage.test.ts
autonomous: true
requirements: [DS-11]

must_haves:
  truths:
    - "Every card surface in the app renders result-card, row-card or panel-card — a fourth container shape is a review failure (DS-11, AC#25)"
    - "The listing sticky rail is a PanelCard with sticky=true, so its lg:top-20 offset comes from the pattern rather than from a class someone can edit back to 8"
    - "A source gate enumerates the card surfaces and asserts each renders one of the three declared containers"
  artifacts:
    - path: "src/components/booking/price-breakdown.tsx"
      provides: "The price breakdown's container as PanelCard, keeping the data-testid='price-total' hook plan 11-06 added"
    - path: "tests/design/card-pattern-coverage.test.ts"
      provides: "AC#25 — the declared card-surface inventory and the assertion that each composes a declared pattern"
  key_links:
    - from: "src/app/listings/[id]/(detail)/page.tsx"
      to: "src/components/patterns/panel-card.tsx"
      via: "the sticky prop, replacing the raw lg:sticky lg:top-20 class"
      pattern: "sticky"
    - from: "tests/design/card-pattern-coverage.test.ts"
      to: "src/lib/design/selector-contract.ts"
      via: "the three declared container ids"
      pattern: "panel-card"
---

<objective>
Adopt `PanelCard` across the five boxed-panel surfaces and gate the "every card surface uses one of three"
claim so it is checked rather than asserted.

Purpose: DS-11's second half is adoption, and AC#25 makes it falsifiable. The listing rail in particular
must take its sticky offset from the pattern, so the `lg:top-8` correction cannot be undone by a later
edit to a raw class.
Output: five surfaces on `PanelCard`, plus the coverage gate that closes AC#25.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
UI-SPEC's PanelCard "Replaces" list, verbatim:
  the listing sticky rail (listings/[id]/page.tsx:346 — now listings/[id]/(detail)/page.tsx after plan 11-10)
  booking/price-breakdown.tsx's container
  host/payout-summary.tsx
  invite/[token]'s InviteCard
  the hours-missing notices

PanelCard props: title, description, footer (renders CardFooter, already border-t bg-muted/50),
                 sticky = false (-> lg:sticky lg:top-20), tone: "default" | "muted" = "default"
Padding p-4 sm:p-6. Elevation: NONE at rest.

Boundary reminder: this phase does NOT restructure earnings/payouts (HFLOW-05 is a token pass only, because
that data has never been real) and does NOT redesign checkout (Phase 12). Container swap only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PanelCard adoption — the listing rail, the price breakdown, the payout summary</name>
  <files>src/app/listings/[id]/(detail)/page.tsx, src/components/booking/price-breakdown.tsx, src/components/host/payout-summary.tsx</files>
  <read_first>
    - src/components/patterns/panel-card.tsx (plan 11-08's output — the prop contract and the `lg:top-20` encoding)
    - src/app/listings/[id]/(detail)/page.tsx (the rail around line 346 as plan 11-10 left it: `<Card className="lg:sticky lg:top-20">` + `<CardContent className="space-y-4 py-6">`)
    - src/components/booking/price-breakdown.tsx (the whole file — its container, and the `data-testid="price-total"` hook plan 11-06 added, which must survive)
    - src/components/host/payout-summary.tsx (the whole file)
    - src/components/ui/card.tsx:82-92 (CardFooter — `border-t bg-muted/50 p-4`, which is what the `footer` prop renders through)
  </read_first>
  <action>
Replace each surface's hand-rolled `Card` container with `PanelCard`, passing `title`, `description` and
`footer` where the surface has them.

**The listing rail** takes `sticky` — the boolean prop, not a class. Delete the raw `lg:sticky lg:top-20`
string from the page; the offset now has exactly one home, and plan `11-10`'s `sticky-offset.test.ts` count
of `lg:sticky` sites in `src/app/**`/`src/components/**` must be updated to reflect that the only remaining
occurrence is inside `panel-card.tsx`. Update that gate's pinned count and record old → new.

**The price breakdown** keeps `data-testid="price-total"` on the total exactly where plan `11-06` put it,
and keeps `tabular-nums` on every money figure. The container changes; the numbers do not. If the swap
moves the total into a `footer` slot, the hook moves with it — verify by re-running
`e2e/price-parity.spec.ts`, which is the only thing standing between this restyle and a wrong number.

**The payout summary** is a container swap and nothing else: HFLOW-05 is deliberately a token pass because
those numbers have never been real (PayMongo `/v2` is sales-gated). Do not restructure it.

Elevation stays none at rest on all three — a panel is flat and the card already carries its hairline ring.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --config vitest.design.config.ts tests/design/sticky-offset.test.ts && npx playwright test e2e/price-parity.spec.ts e2e/public-listing.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - All three files import `PanelCard` and declare no `<Card` with their own className
    - `grep -rc "lg:sticky" src/app src/components --include=*.tsx` returns 1, and that one occurrence is inside `src/components/patterns/panel-card.tsx`
    - `grep -c 'data-testid="price-total"' src/components/booking/price-breakdown.tsx` still returns 1
    - `e2e/price-parity.spec.ts` passes — the DOM total still equals the DB total
    - `e2e/public-listing.spec.ts` passes — the rail still renders and its selectors are unchanged
    - `grep -rc "shadow-" src/components/host/payout-summary.tsx` returns 0 at rest
    - `tests/design/sticky-offset.test.ts`'s pinned count is updated and the change is recorded old → new
  </acceptance_criteria>
  <done>The rail's offset lives in the pattern, and the price hook survived the container swap.</done>
</task>

<task type="auto">
  <name>Task 2: PanelCard adoption — the invite card and the hours-missing notices</name>
  <files>src/app/(public)/invite/[token]/page.tsx, src/app/(host)/host/page.tsx</files>
  <read_first>
    - src/app/(public)/invite/[token]/page.tsx (the whole file: its `InviteCard` container, the container idiom at :83-86, the module-private `INACTIVE_TITLE`/`INACTIVE_BODY` at :71-72 — **do not move those here, plan 11-19 owns the hoist** — and the static `metadata` export at :58-64 whose `robots`/`referrer` are non-negotiable)
    - src/app/(host)/host/page.tsx (the hours-missing notice around :149, and its `border-dashed` block, which plan 11-16 owns — coordinate, do not double-edit)
    - src/lib/listing/hours-signal.ts (what the notice is derived from — unchanged by this plan)
    - src/components/patterns/panel-card.tsx (`tone="muted"` is the in-page-advisory treatment)
  </read_first>
  <action>
Convert `InviteCard`'s container to `PanelCard`. The invite page's copy, its RSVP behaviour and its
`INACTIVE_TITLE`/`INACTIVE_BODY` constants are untouched — plan `11-19` owns hoisting those, and touching
them here would collide. The static `metadata` export with `robots: { index: false, follow: false }` and
`referrer: "no-referrer"` stays exactly as it is; plan `11-20` converts it to `generateMetadata` and must
preserve both.

Convert the hours-missing notice(s) to `PanelCard tone="muted"` — the `neutral` status tone at panel scale,
zero new tones and zero new pairings. **Do not use `attention`:** DS-10 reserves that for a genuine failure
needing a human, and a listing published without hours is a normal, fixable state.

If the notice currently renders inside a `border-dashed` block, leave the `border-dashed` question to plan
`11-16`, which owns the scoped assertion over all 19 shipped sites. Note in the SUMMARY which sites this
plan converted so `11-16` does not re-count them.
  </action>
  <verify>
    <automated>npm run build && npx playwright test e2e/open-capacity.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Both files import `PanelCard`; neither declares a `<Card` with its own className
    - `src/app/(public)/invite/[token]/page.tsx` still exports `metadata` carrying `robots: { index: false, follow: false }` and `referrer: "no-referrer"`
    - `INACTIVE_TITLE` and `INACTIVE_BODY` are byte-unchanged in that file
    - The hours-missing notice renders `tone="muted"`; `grep -c "attention\|destructive" src/app/\(host\)/host/page.tsx` shows no new occurrence
    - `npm test` passes with the same counts as the phase baseline
    - The SUMMARY lists which `border-dashed` sites this plan converted
  </acceptance_criteria>
  <done>The invite card and the hours notices are boxed by the pattern, with the invite route's security metadata untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The card-surface coverage gate (AC#25)</name>
  <files>tests/design/card-pattern-coverage.test.ts</files>
  <read_first>
    - src/lib/design/selector-contract.ts (the three declared container ids)
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker)
    - tests/use-server-exports.test.ts:315-318 and :320-369 (guard-the-guard, both-directions self-tests), :89-102 (NOT COVERED)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § The Pattern Layer (the three "Replaces" lists — the declared surface inventory)
  </read_first>
  <action>
Create `tests/design/card-pattern-coverage.test.ts`.

Declare `CARD_SURFACES` as an `as const` inventory of the twelve files the UI-SPEC's three "Replaces" lists
name, each row carrying `{ file, pattern: "result-card" | "row-card" | "panel-card", why }`. Assert for
every row that the file exists and imports the corresponding pattern module from
`@/components/patterns/…` — an AST import check, not a substring match, so a mention in a comment does not
satisfy it.

Add the inverse assertion, which is the one that catches a fourth shape: scan `src/components/**` and
`src/app/**` for JSX elements named `Card` imported from `@/components/ui/card`, and assert every such call
site is either inside `src/components/patterns/**` or is listed in a small, explicit `ALLOWED_RAW_CARD`
inventory with a stated reason per entry. A fourth container shape then arrives as a named test failure
rather than as a review opinion.

Guard-the-guard: assert `CARD_SURFACES.length === 12` and that the scanner parsed every listed file — a
coverage gate whose inventory silently emptied passes perfectly.

Both-directions self-tests on synthetic sources: a fixture importing `Card` from `@/components/ui/card`
outside `patterns/` must be flagged; one importing `PanelCard` from `@/components/patterns/panel-card` must
not; a fixture where `Card` appears only in a comment must not be flagged.

NOT COVERED footer: this gate proves the container, not the rendering. It cannot tell you a `ResultCard` was
handed the wrong props, and it says nothing about surfaces Phases 12–15 add — those phases adopt the
patterns and extend this inventory.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The gate passes against the tree after plans 11-11 and 11-13
    - Watched red: temporarily revert one adopter to a raw `<Card>` → the gate fails naming the file and the expected pattern. Reverted. Both runs recorded in the header.
    - Watched red (fourth shape): temporarily add a raw `<Card>` container to a new file outside `patterns/` → the inverse assertion fails. Reverted.
    - Watched red (vacuity): empty the `CARD_SURFACES` inventory → guard-the-guard fails
    - `ALLOWED_RAW_CARD` entries each carry a non-empty reason string, and the SUMMARY lists them
    - `npm run test:design` passes with the file count raised by exactly 1; push and CI green (D-25)
  </acceptance_criteria>
  <done>AC#25 is machine-checked in both directions, and a fourth container shape now fails a named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container swap ↔ the price hook | The DB-vs-DOM assertion reads one `data-testid`; a restyle that moves it silently disarms GATE-05's e2e half |
| invite route metadata ↔ crawlers and referrers | The URL contains a credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-PRICE | Tampering | `price-breakdown.tsx` container swap | mitigate | `data-testid="price-total"` count asserted at 1 and `e2e/price-parity.spec.ts` re-run as an acceptance criterion |
| T-11-REFERER | Information Disclosure | `invite/[token]` metadata | mitigate | `robots: { index: false, follow: false }` and `referrer: "no-referrer"` asserted unchanged; the `generateMetadata` conversion is deliberately deferred to plan 11-20 so this container swap cannot touch them |
| T-11-STICKYTUCK | Denial of Service | the listing rail offset | mitigate | The offset moves from a raw class into `PanelCard`'s `sticky` prop; `lg:sticky` is asserted to occur exactly once in the tree, inside the pattern |
| T-11-FOURTH | Repudiation | container inventory | mitigate | The coverage gate's inverse assertion turns a fourth container shape into a named failure with an explicit, reasoned allow-list |
| T-11-SC | Tampering | npm / shadcn registry | mitigate | Zero packages, zero registry blocks, zero edits under `src/components/ui/**` |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm test`, `npm run test:design` green
- `e2e/price-parity.spec.ts`, `e2e/public-listing.spec.ts`, `e2e/open-capacity.spec.ts` pass
- All three CI jobs green on `origin/dev`
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- DS-11's adoption half is complete: twelve card surfaces on three declared containers
- The sticky offset has exactly one home in the codebase
- AC#25 is a test, not a review instruction
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-13-SUMMARY.md` when done
</output>
