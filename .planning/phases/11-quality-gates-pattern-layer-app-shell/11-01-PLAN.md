---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/payments/fees.ts
  - src/lib/payments/config.ts
  - src/lib/payments/service-fee.ts
  - src/lib/payments/commission.ts
  - src/lib/booking/all-in-rate.ts
  - src/lib/booking/pricing.ts
  - src/lib/availability/horizon.ts
  - src/lib/availability/slots.ts
  - src/lib/availability/read-model.ts
  - src/lib/availability/units.ts
  - src/app/actions/cancel-booking.ts
  - src/app/listings/[id]/page.tsx
  - src/app/(host)/host/listings/page.tsx
  - src/components/listing/listing-card.tsx
  - src/components/availability/availability-calendar.tsx
  - src/components/availability/date-pass-picker.tsx
  - vitest.config.ts
  - vitest.design.config.ts
  - tests/helpers/server-only.stub.ts
  - tests/design/server-only-guards.test.ts
  - tests/booking/service-fee-hold.test.ts
  - tests/payments/service-fee.test.ts
  - tests/payments/commission.test.ts
  - tests/payments/host-cancel.test.ts
  - tests/booking/open-capacity-cancel.test.ts
autonomous: true
requirements: [GATE-05]

must_haves:
  truths:
    - "npm run build fails at Turbopack the moment any client graph reaches a guarded money or availability module (D-34) — the build is the enforcer, not a resolver of ours"
    - "The verbatim failing build naming listing-card.tsx AND availability-calendar.tsx is committed as an OBSERVED RED header block (D-36)"
    - "Zero emitted client chunks contain the string SERVICE_FEE_BPS??500 after the fix (5 chunks contain it today)"
    - "The two legitimate client imports (lead-time constants, BOOKING_HORIZON_DAYS) still compile — the deny-list is drawn at computation modules, not whole trees (D-34)"
    - "A design test asserts every declared module still carries its guard, by AST and never by grep (D-34)"
  artifacts:
    - path: "src/lib/payments/fees.ts"
      provides: "The guarded fee/commission constants split out of config.ts so D-34 does not break slot-picker.tsx and request-row.tsx"
      contains: "server-only"
    - path: "src/lib/availability/horizon.ts"
      provides: "BOOKING_HORIZON_DAYS, client-safe, so guarding slots.ts does not break two shipped client imports (D-34, second split)"
    - path: "tests/design/server-only-guards.test.ts"
      provides: "AST guard-presence gate plus the D-36 OBSERVED RED header block"
      contains: "OBSERVED RED"
    - path: "tests/helpers/server-only.stub.ts"
      provides: "The Vitest resolve-alias target — `server-only` is not resolvable from Node, so ~35 shipped test files would fail resolution without it"
  key_links:
    - from: "src/components/availability/availability-calendar.tsx"
      to: "src/app/listings/[id]/page.tsx"
      via: "a server-computed all-in cents table passed as a prop instead of computeServiceFee"
      pattern: "allInCents"
    - from: "src/components/listing/listing-card.tsx"
      to: "src/app/(host)/host/listings/page.tsx"
      via: "pre-formatted allInRateParts strings composed server-side"
      pattern: "allInRateParts"
---

<objective>
Make `next build` the enforcer of the client/server money boundary, prove the gate fails on real
shipped code, and fix the two live D-130 violations — atomically, so no later plan runs against a red
build.

Purpose: GATE-05's requirement text is *"fails the build"*. `import "server-only"` hands transitive
enforcement to Turbopack (D-34) instead of an AST import-graph walker we would have to maintain.
Output: guarded computation modules, two split client-safe modules, both violations fixed, and an
AST guard-presence gate carrying the OBSERVED RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-CONTEXT.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md

<interfaces>
Measured 2026-08-13. Four client components import values from D-34's deny-list; only two are violations.

VIOLATIONS (must be fixed by this plan):
  src/components/listing/listing-card.tsx:43        allInRateParts   from @/lib/booking/all-in-rate
  src/components/availability/availability-calendar.tsx:32  computeServiceFee from @/lib/payments/service-fee
    called at :346  computeServiceFee(spaceCents, serviceFeeBps).allInCents
    called at :411  computeServiceFee(perHeadPriceCents * passes, serviceFeeBps).allInCents

LEGITIMATE (must keep compiling — the gate failing on these is Pitfall 1):
  src/components/availability/slot-picker.tsx:43    MIN_LEAD_INSTANT_MINUTES, MIN_LEAD_REQUEST_HOURS from @/lib/payments/config
  src/components/host/request-row.tsx:34            APPROVAL_PAYMENT_WINDOW_HOURS                     from @/lib/payments/config
  src/components/availability/availability-calendar.tsx:29  BOOKING_HORIZON_DAYS from @/lib/availability/slots
  src/components/availability/date-pass-picker.tsx:42       BOOKING_HORIZON_DAYS from @/lib/availability/slots

Existing signature, unchanged by this plan:
  computeServiceFee(spacePriceCents: number, feeBps: number = SERVICE_FEE_BPS): { feeBps, serviceFeeCents, allInCents }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the deny-list twice, add the guards, teach both Vitest configs the specifier, and capture the OBSERVED RED</name>
  <files>src/lib/payments/fees.ts, src/lib/payments/config.ts, src/lib/payments/service-fee.ts, src/lib/payments/commission.ts, src/lib/booking/all-in-rate.ts, src/lib/booking/pricing.ts, src/lib/availability/horizon.ts, src/lib/availability/slots.ts, src/lib/availability/read-model.ts, src/lib/availability/units.ts, src/app/actions/cancel-booking.ts, src/components/availability/availability-calendar.tsx, src/components/availability/date-pass-picker.tsx, vitest.config.ts, vitest.design.config.ts, tests/helpers/server-only.stub.ts, tests/booking/service-fee-hold.test.ts, tests/payments/service-fee.test.ts, tests/payments/commission.test.ts, tests/payments/host-cancel.test.ts, tests/booking/open-capacity-cancel.test.ts</files>
  <read_first>
    - src/lib/payments/config.ts (all 17 exports and its 1-10 header — the module contract fees.ts inherits and must amend)
    - src/lib/payments/service-fee.ts (lines 16-17 carry the "Pure/isomorphic: … Server Components, server actions, and the payout sweep can all import it" paragraph that `server-only` makes FALSE)
    - src/lib/booking/all-in-rate.ts (lines 18-19 carry the same false-after-guard paragraph)
    - src/lib/availability/slots.ts:17 (`export const BOOKING_HORIZON_DAYS = 90` — the second, unflagged split)
    - vitest.config.ts and vitest.design.config.ts (both `resolve.alias` blocks; the design config's lines 17-22 header)
    - tests/use-server-exports.test.ts:16-23 (Shared Pattern 10 — a false header comment is a defect)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md § G (the exact stays/moves table for config.ts)
  </read_first>
  <action>
Create `src/lib/payments/fees.ts` carrying exactly three exports moved out of `config.ts` — `SERVICE_FEE_BPS`
(config.ts:59), `COMMISSION_RATE_BPS` (config.ts:13) and `HOST_CANCEL_FEE_CENTS` (config.ts:67) — with their
`process.env` reads and doc comments intact, plus `import "server-only"` as the first statement. `config.ts`
keeps its other 14 timing/window/payout constants and takes **no** guard: `slot-picker.tsx:43` and
`request-row.tsx:34` import lead-time constants from it legitimately, and guarding it whole is research
Pitfall 1 (the build fails naming the wrong files).

Create `src/lib/availability/horizon.ts` exporting `BOOKING_HORIZON_DAYS = 90`, moved out of `slots.ts:17`,
with **no** guard. `slots.ts` re-imports it for its own use. Repoint the two shipped client importers —
`availability-calendar.tsx:29` and `date-pass-picker.tsx:42` — to `@/lib/availability/horizon`. This is the
same split as `fees.ts` and it is NOT in D-34's literal text: measured, `slots.ts` is both a computation
module and the home of a constant two client components need, so guarding it unsplit reproduces Pitfall 1 in
the availability tree.

Add `import "server-only"` as the first statement of: `src/lib/payments/fees.ts`,
`src/lib/payments/service-fee.ts`, `src/lib/payments/commission.ts`, `src/lib/booking/all-in-rate.ts`,
`src/lib/booking/pricing.ts`, `src/lib/availability/slots.ts`, `src/lib/availability/read-model.ts`,
`src/lib/availability/units.ts`. `commission.ts` extends D-34's named list by one module on the same
rationale — it is a money computation and has zero client importers (measured); record that in its header.
Do **not** `npm install server-only`: Next aliases the specifier and declares the module at
`node_modules/next/types/global.d.ts:57`. An `npm install server-only` in this plan is a recorded-decision
reversal.

In every guarded module, replace the now-false "Pure/isomorphic … anyone can import it" paragraph with a
one-line statement naming D-34 and the reason the module is server-only. A false header comment beside the
thing it describes is the defect `tests/use-server-exports.test.ts` was written after.

Update the three moved constants' importers to `@/lib/payments/fees`: `src/app/actions/cancel-booking.ts:62`,
`src/lib/payments/commission.ts:13`, `src/lib/payments/service-fee.ts:25`, and the five test files listed in
`<files>`.

**`server-only` is NOT resolvable from Node** (`require.resolve("server-only")` → MODULE_NOT_FOUND) and
~35 shipped test files import modules this task guards, so Vitest must be taught the specifier or the whole
suite — and therefore CI job 2 — goes red. Create `tests/helpers/server-only.stub.ts` as an empty module and
add `"server-only": resolve(__dirname, "./tests/helpers/server-only.stub.ts")` to the existing
`resolve.alias` block in **both** `vitest.config.ts` and `vitest.design.config.ts`. State in a comment that
`resolve.alias` is neither `globalSetup` nor `setupFiles`, so the design config's DB-free guarantee
(its header, lines 17-22) is untouched.

Then run `npm run build` with the working tree otherwise clean and capture the failure. Record it as the
OBSERVED RED block that Task 3 commits, following `tests/use-server-exports.test.ts:25-62`: the HEAD sha and
a clean `git status --short -- src/`, the exact command, the exit code, the verbatim Turbopack output, and a
sentence naming what did **not** fail (specifically that `slot-picker.tsx` and `request-row.tsx` are absent
from the diagnostic — that is what makes the split correct rather than lucky). Expect the diagnostic to name
`listing-card.tsx` and `availability-calendar.tsx` and to contain the template
`cannot be imported from a Client Component module. It should only be used from a Server Component.`
Capture whatever Turbopack actually prints; the surrounding layout is unverified (RESEARCH Assumption A1).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/payments/fees.ts','utf8'); if(!/^import \"server-only\";/m.test(s)) process.exit(1); for(const k of ['SERVICE_FEE_BPS','COMMISSION_RATE_BPS','HOST_CANCEL_FEE_CENTS']) if(!s.includes(k)) process.exit(1); const c=require('fs').readFileSync('src/lib/payments/config.ts','utf8'); if(/server-only/.test(c.replace(/\/\/.*$/gm,''))) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import \"server-only\";" src/lib/payments/fees.ts src/lib/payments/service-fee.ts src/lib/payments/commission.ts src/lib/booking/all-in-rate.ts src/lib/booking/pricing.ts src/lib/availability/slots.ts src/lib/availability/read-model.ts src/lib/availability/units.ts` returns `1` for every file
    - `src/lib/payments/config.ts` contains no `server-only` import outside comments; it still exports `MIN_LEAD_REQUEST_HOURS`, `MIN_LEAD_INSTANT_MINUTES` and `APPROVAL_PAYMENT_WINDOW_HOURS`
    - `src/lib/availability/horizon.ts` exports `BOOKING_HORIZON_DAYS = 90` and contains no `server-only` import
    - `grep -n "lib/availability/slots" src/components/availability/availability-calendar.tsx src/components/availability/date-pass-picker.tsx` returns zero value-import lines
    - `git diff --stat package.json` is empty — zero packages installed
    - `npm run build` exits **non-zero**, and the captured output names `src/components/listing/listing-card.tsx` and `src/components/availability/availability-calendar.tsx` and does **not** name `slot-picker` or `request-row`
    - Both Vitest configs contain a `"server-only"` alias entry; neither gained `globalSetup` or `setupFiles`
    - The captured OBSERVED RED text carries the HEAD sha, the exact command, the exit code, verbatim output and the "what did not fail" sentence
  </acceptance_criteria>
  <done>The guards are in place, the build is red for exactly the two real violations, and the verbatim failure is captured for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Fix both live violations — pre-formatted strings for the card, a server-computed all-in table for the calendar</name>
  <files>src/components/listing/listing-card.tsx, src/app/(host)/host/listings/page.tsx, src/components/availability/availability-calendar.tsx, src/app/listings/[id]/page.tsx</files>
  <read_first>
    - src/components/search/search-result-card.tsx:122-139 (the sibling that already does it correctly, with the comment explaining why — lift the reasoning verbatim) and :247-252 (the pre-formatted `tabular-nums` render, which is also AC#33's shape)
    - src/components/listing/listing-card.tsx (lines 1, 43, 202-215 — the "use client" directive, the import, the call site)
    - src/app/(host)/host/listings/page.tsx:25 (the Server Component that renders listing-card and must compose the strings)
    - src/components/availability/availability-calendar.tsx:305-360 (RailSelectionSummary, its `serviceFeeBps` prop doc and the :346 call) and :375-420 (RailPassSummary and the :411 call)
    - src/app/listings/[id]/page.tsx:29, :390-410 (where `SERVICE_FEE_BPS` is imported and threaded into both rail summaries)
    - src/lib/payments/service-fee.ts (the exact `allInCents = spacePriceCents + Math.round(spacePriceCents*feeBps/10000)` contract — the figure must stay byte-identical to what checkout freezes)
  </read_first>
  <action>
**`listing-card.tsx`** — delete the `allInRateParts` import and the lines 202-215 arithmetic. Add a
`priceParts: string[]` prop. `src/app/(host)/host/listings/page.tsx` composes the strings server-side by
calling `allInRateParts` there and passing them down, exactly as `src/lib/search/query.ts` already does for
`SearchResultCard`. The card renders them as a plain joined string with `tabular-nums`, and keeps the
existing `"Price on request"` fallback for the empty array. Copy the reasoning comment from
`search-result-card.tsx:122-139` so the next reader learns why rather than re-discovering it.

**`availability-calendar.tsx`** — this is the violation D-36's one-file description under-sizes, and the
props contract is decided here (RESEARCH Open Question 3):

*Decision: a server-computed all-in cents TABLE, not a unit rate.* A "server-computed all-in unit rate
multiplied client-side" is rejected on a measured ground — `computeServiceFee` rounds **once** over the whole
space price, so `n × allIn(unit)` differs from `allIn(n × unit)` by up to `n-1` centavos, and both call sites'
own comments make byte-identical agreement with the frozen quote load-bearing (D-75). A table preserves
exactness and removes the rate from the client entirely.

Replace the `serviceFeeBps: number` prop on `RailSelectionSummary` and `RailPassSummary` with a single
`allIn` prop of shape `{ hourly: Record<number, number>; fullDay: number | null; perPass: Record<number, number> }`,
where every value is all-in integer centavos. `src/app/listings/[id]/page.tsx` builds it with the same
`computeServiceFee` checkout uses: `hourly` keyed 1…`ALL_IN_TABLE_MAX_HOURS` (24 — a selection longer than a
day is a full-day selection) from `hourlyRateCents`, `fullDay` from `dayRateCents`, `perPass` keyed
1…`row.listing.capacity` from `perHeadPriceCents`. Export `ALL_IN_TABLE_MAX_HOURS` from
`src/lib/availability/horizon.ts` so both sides read one constant. At the call sites, `:346` becomes a lookup
by `hours` (or `fullDay` when `selection.fullDay`) and `:411` a lookup by `passes`; a missing key renders no
estimate line, which is the shipped behaviour when a rate is null. Delete the `computeServiceFee` import and
the `SERVICE_FEE_BPS` import in `listings/[id]/page.tsx` if it becomes unused. Amend both rail summaries'
`serviceFeeBps` prop docs — they describe a prop that no longer exists.

Then re-run the build to green and grep the emitted client chunks to prove the leak is closed.
  </action>
  <verify>
    <automated>npm run build && node -e "const{readdirSync,readFileSync}=require('fs');let n=0;const walk=d=>{for(const e of readdirSync(d,{withFileTypes:true})){const p=d+'/'+e.name;if(e.isDirectory())walk(p);else if(p.endsWith('.js')&&readFileSync(p,'utf8').includes('SERVICE_FEE_BPS??500'))n++;}};walk('.next/static/chunks');console.log('leaky chunks:',n);process.exit(n===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Zero files under `.next/static/chunks/**` contain the string `SERVICE_FEE_BPS??500` (5 contain it at HEAD)
    - `grep -n "computeServiceFee\|allInRateParts" src/components/availability/availability-calendar.tsx src/components/listing/listing-card.tsx` returns zero import or call lines
    - `grep -c "serviceFeeBps" src/components/availability/availability-calendar.tsx` returns 0 outside comments
    - Every money figure the two components render still carries `tabular-nums`
    - `npx vitest run tests/availability tests/booking tests/payments tests/search` passes with the same counts as the pre-plan baseline
  </acceptance_criteria>
  <done>Both violations are fixed, the build is green, and the fee constant is provably absent from every client chunk.</done>
</task>

<task type="auto">
  <name>Task 3: The AST guard-presence gate, carrying the OBSERVED RED</name>
  <files>tests/design/server-only-guards.test.ts</files>
  <read_first>
    - tests/use-server-exports.test.ts (all four conventions: the OBSERVED RED header at 25-62, `ts.createSourceFile` scanning at 104-151, the guard-the-guard assertion at 315-318, the both-directions synthetic self-tests at 320-369, and the NOT COVERED footer at 89-102)
    - tests/design/leak.test.ts:211-249 (the AST string-literal walker idiom)
    - tests/design/infra.test.ts:5-9 (the one-sentence anti-vacuity principle to quote)
    - vitest.design.config.ts (the DB-free home this file must belong to)
  </read_first>
  <action>
Create `tests/design/server-only-guards.test.ts` in `tests/design/**` so it runs under
`vitest.design.config.ts` and inside `npm run build`.

Open with the Task-1 OBSERVED RED block, verbatim, in the `use-server-exports.test.ts:25-62` format.

Declare `GUARDED_MODULES` as an `as const` tuple of the eight paths guarded in Task 1, each with a mandatory
reason string. Assert with `ts.createSourceFile` that each still carries the guard, using the predicate
`ts.isImportDeclaration(stmt) && !stmt.importClause && stmt.moduleSpecifier.text === "server-only"` — an
`ImportDeclaration` with **no import clause**, not a directive-prologue string literal. Grep is banned here
specifically: measured, `grep -rn "server-only" src/` returned 6 hits across 5 files at HEAD and **every one
was a comment**, so a grep guard passes on prose and would keep passing after every guard was deleted.

Assert the negative side too: `src/lib/payments/config.ts` and `src/lib/availability/horizon.ts` must NOT
carry the guard, naming the two shipped client importers of each as the reason — that row is what stops a
future "tidy-up" reintroducing Pitfall 1.

Add the guard-the-guard assertion (the scanner resolved and parsed all eight files, and
`GUARDED_MODULES.length === 8`) and both-directions self-tests on synthetic fixture strings never written to
disk: a comment mentioning `server-only`, a `const x = "server-only"` string literal, and
`import x from "server-only"` must all be `false`; a bare `import "server-only";` must be `true`.

Close with a NOT COVERED footer naming the blind spots: this file does not follow imports (transitivity is
Turbopack's job, per D-34), and it cannot see a guard deleted together with the module.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/server-only-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/server-only-guards.test.ts` passes
    - Watched red: deleting the `import "server-only"` line from `src/lib/booking/pricing.ts` makes it fail naming that file; restored, it passes. Record both runs in the file header.
    - Watched red the other way: adding `import "server-only"` to `src/lib/payments/config.ts` makes the negative assertion fail. Reverted.
    - The file contains the string `OBSERVED RED` and a `NOT COVERED` footer
    - `npm run test:design` passes with the file count raised by exactly 1
  </acceptance_criteria>
  <done>The guard cannot be quietly deleted, the gate has been watched failing in both directions, and the OBSERVED RED is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server module graph → client bundle | Every value crossing here is shipped to an untrusted browser |
| deployed env vars → rendered price | A non-`NEXT_PUBLIC_` env read that reaches the client silently resolves to its literal fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-FEELEAK | Information Disclosure | `src/lib/payments/fees.ts`, `service-fee.ts`, `all-in-rate.ts` | mitigate | `import "server-only"` makes Turbopack hard-fail the build on any client graph reaching them; verified by a zero-count grep of `.next/static/chunks/**` for `SERVICE_FEE_BPS??500` (5 chunks at HEAD) |
| T-11-PRICEDRIFT | Tampering | `availability-calendar.tsx` rail estimate | mitigate | The all-in table is computed server-side with the same `computeServiceFee` checkout freezes, so the browsed figure stays byte-identical to the quoted total (D-75); a unit-rate multiply was rejected because its rounding diverges by up to `n-1` centavos |
| T-11-GUARDROT | Tampering | the eight guarded modules | mitigate | AST guard-presence test in `tests/design/`, run inside `npm run build`, with guard-the-guard and both-directions self-tests |
| T-11-SC | Tampering | npm installs | mitigate | This plan installs zero packages; `server-only` is aliased by Next. `git diff --stat package.json` empty is an acceptance criterion. A proposed `npm install server-only` is a recorded-decision reversal to raise, not absorb. |
</threat_model>

<verification>
- `npm run build` exits 0 with the guards live
- `npm run test:design` green
- `npm test` green (this is the run that proves the Vitest `server-only` alias works across ~35 importing files)
- `git diff --stat package.json` empty
- `ls drizzle/ | tail -1` is still `0025_audit_resolved_by.sql` (GATE-06)
</verification>

<success_criteria>
- GATE-05's build-side half exists and has been watched failing on real shipped code
- The two measured D-130 violations are gone and provably absent from the emitted bundle
- The two legitimate client import sites still compile
- Zero packages installed, zero migrations
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-01-SUMMARY.md` when done
</output>
