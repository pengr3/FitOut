---
phase: 11-quality-gates-pattern-layer-app-shell
plan: 09
type: execute
wave: 4
depends_on: [11-07]
files_modified:
  - src/components/patterns/empty-state.tsx
  - src/components/patterns/error-state.tsx
  - src/components/patterns/responsive-dialog.tsx
  - src/app/globals.css
  - tests/design/sheet-absent.test.ts
  - tests/design/elevation-z.test.ts
autonomous: true
requirements: [STATE-02, STATE-04, RESP-01]

must_haves:
  truths:
    - "One mobile-overlay primitive exists, composed over ui/dialog — the shadcn sheet block is NOT installed, so there is one focus trap, one escape behaviour and one baseline set"
    - "EmptyState collapses two drifted shells (two radii, two paddings, two title treatments, one with no heading element at all) into one component whose title is never a <p>"
    - "ErrorState always renders TWO actions — retry covers a transient fault, the route out covers a persistent one; a bare 'Something went wrong' with one button is a failure of the contract"
    - "--z-sheet now has ZERO call sites, and that zero is asserted with its reason rather than merely true"
  artifacts:
    - path: "src/components/patterns/responsive-dialog.tsx"
      provides: "The RESP-01 mobile-overlay primitive: bottom sheet below sm:, the vendored centred dialog byte-unchanged at sm: and up"
      contains: "max-h-[85dvh]"
    - path: "src/components/patterns/empty-state.tsx"
      provides: "The single empty shell with icon/title/titleAs/body/actions/tone props"
    - path: "src/components/patterns/error-state.tsx"
      provides: "The two-action error surface that renders digest and never error.message or error.stack"
    - path: "tests/design/sheet-absent.test.ts"
      provides: "AC#26/#27/#28 — ui/sheet.tsx absent, Z_SHEET_INVENTORY empty, 85dvh present with no bare vh unit"
  key_links:
    - from: "src/components/patterns/responsive-dialog.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "className passed to DialogContent — a class override, never a fork"
      pattern: "DialogContent"
    - from: "tests/design/sheet-absent.test.ts"
      to: "src/components/ui/sheet.tsx"
      via: "an existsSync absence assertion"
      pattern: "sheet"
---

<objective>
Ship the empty, error and mobile-overlay patterns, and assert the zero that the overlay decision creates.

Purpose: RESP-01's requirement is *one* mobile-overlay primitive rather than every mobile pattern degrading
into a full-screen dialog. `FEATURES.md` F1 asks for the shadcn `sheet` block; `ARCHITECTURE.md` §6.3
refuses it; the roadmap's Phase 16 dependency line — *"Phase 11's responsive-dialog pattern"* — breaks the
tie. That decision leaves `--z-sheet` with no call sites, and a zero that is merely true is an invitation.
Output: three patterns, one `globals.css` line, and a gate over the absence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-PATTERNS.md
@.planning/phases/11-quality-gates-pattern-layer-app-shell/11-RESEARCH.md

<interfaces>
EmptyState props (11-UI-SPEC § Empty):
  icon: LucideIcon (size-6, aria-hidden) · title: string · titleAs: "h2" | "h3" = "h2"
  body: string · actions: ReactNode · tone: "neutral" | "positive" = "neutral"
Visual (shell A's geometry wins — it is already on the ladder):
  rounded-xl border border-dashed p-8 text-center · icon text-muted-foreground
  title mt-3 text-body font-semibold text-foreground
  body mx-auto mt-1 max-w-prose text-sm text-muted-foreground
  actions mt-5 flex flex-wrap items-center justify-center gap-2
  tone="positive" → icon becomes CheckCircle2 at text-success (4.00 / 3.86 ✓ declared, 3.0 bar).
  TEXT STAYS --foreground. No green fill, no green text — D-14's "green retreats to the icon" at panel scale.

ErrorState slots (11-UI-SPEC § Error):
  icon AlertTriangleIcon size-6 text-destructive (5.76 / 5.56 on background ✓ declared)
  heading text-heading text-foreground · body text-sm text-muted-foreground max-w-prose
  action 1 "Try again" Button variant="secondary" → onRetry
  action 2 a route out, Button variant="outline" asChild
  reference: digest when present, text-label text-muted-foreground tabular-nums, prefixed "Reference"

next@16.2.7's ErrorInfo is { error, reset, unstable_retry } — VERIFIED in the installed types. `retry` does
not exist. Write against `reset` and leave a `// TODO(next@16.3): retry()` marker.

AC#28, corrected after UI-check: assert `/\[\d+vh\]/.test(source) === false` AND
`source.includes("[85dvh]") === true`. A `not.toContain("vh]")` would go RED against the mandated correct
class, because "dvh]" ends in "vh]".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmptyState and ErrorState</name>
  <files>src/components/patterns/empty-state.tsx, src/components/patterns/error-state.tsx</files>
  <read_first>
    - src/components/search/search-results.tsx:201-215 (shell A — the geometry that wins; note the `<p className="font-semibold">` title, which is exactly the "no heading element in the empty region" defect `titleAs` fixes)
    - src/components/search/search-results.tsx:169-181 (the inline error shape — it has ONE action; the UI-SPEC requires TWO, so this is a shape to extend, not copy wholesale)
    - src/app/(app)/bookings/page.tsx:138 and :151 (shell B — `rounded-lg` + `p-10` + `<h2 className="text-lg font-medium">`; the off-ladder `p-10` is what lands on `p-8`)
    - src/lib/design/status-tones.ts (the closed four-tone union and D-14's icon-only-hue rule that `tone="positive"` must honour)
    - src/components/ui/button.tsx (the `secondary` and `outline` variants and the DS-05 focus recipe)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § Empty, § Error, § Copywriting Contract
  </read_first>
  <action>
`src/components/patterns/empty-state.tsx` — Server Component (no `"use client"`). Implements the props and
geometry in `<interfaces>`. `titleAs` renders a real heading element, defaulting to `"h2"`; it is never a
`<p>`. `border-dashed` reads `--border`, a declared decorative exclusion, and is legal here because this
panel is not a control and the border is not its focus indicator — put that in a comment so plan `11-16`'s
scoped `border-dashed` assertion has a stated home. `data-testid="empty-state"`.

**No product copy.** `EmptyState` never contains the string "No spaces match those filters" — that sentence
is owned by the surface. The component takes `title` and `body` as props and that is the whole rule.

`src/components/patterns/error-state.tsx` — this one **is** `"use client"`, and only because `error.tsx`
boundaries must be. Say that in the file's first comment so the exception is legible rather than assumed.
Props: `{ title: string; body: string; digest?: string; onRetry: () => void; routeOut: ReactNode }`.
Renders the icon, heading, body, both actions and the reference line. `routeOut` is a required prop typed as
`ReactNode` so a caller physically cannot ship a one-button error surface.

**Security property, and it is the phase's largest new surface:** `ErrorState` accepts a `digest` string
and nothing else from the error. It must not accept, and must not render, `error.message` or `error.stack`
— server error text routinely carries table names, file paths and connection strings. Encode that in the
prop *type*: there is no `error` prop. Add a comment naming the `SENTINEL_LEAK_PROBE` assertion in plan `11-18` that will prove it.

Copy is the caller's, but the default sentences the UI-SPEC binds are: **"Something didn't load"** /
*"We hit a problem loading this page. Trying again usually fixes it."* / **"Try again"**. Do not hardcode
them here; the boundaries in plan `11-18` supply them.

Leave the `// TODO(next@16.3): retry()` marker beside the `onRetry` prop, noting that `reset` is the stable
16.2.7 API and `unstable_retry` is deliberately not used because the copy contract binds "Try again" to
`reset()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run --config vitest.design.config.ts tests/design/selector-contract.test.ts tests/design/status-vocab.test.ts tests/design/leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `empty-state.tsx` has no `"use client"`; `error-state.tsx` has it with a stated reason on line 1-3
    - `titleAs` defaults to `"h2"` and renders a heading element — the rendered title is never a `<p>`
    - `tone="positive"` changes only the icon (`CheckCircle2`, `text-success`); `grep -c "bg-success" src/components/patterns/empty-state.tsx` returns 0 and the title/body classes are identical across tones
    - `error-state.tsx` declares no `error`, `message` or `stack` prop; `grep -c "error.message\|error.stack" src/components/patterns/error-state.tsx` returns 0
    - `routeOut` is a required prop (omitting it is a `tsc` error — watch it and record the verbatim error)
    - Neither file contains a product sentence; both take copy as props
    - `data-testid="empty-state"` and `data-testid="error-state"` each appear once and plan 11-02's undeclared-id assertion passes
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>One empty shell and one two-action error surface exist, and the error surface cannot be handed a message to leak.</done>
</task>

<task type="auto">
  <name>Task 2: ResponsiveDialog — one dialog, two presentations</name>
  <files>src/components/patterns/responsive-dialog.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (the vendored primitive — `DialogContent` already takes `className`, which is why the mobile presentation is an override and not a fork)
    - src/components/listing/listing-card.tsx:31-41 (the vendored-Dialog composition idiom already used in this repo)
    - src/app/globals.css (the `--z-sheet` / `--z-dialog` declarations and the CONSUMPTION FORM comment — `z-(--z-dialog)` is the only spelling Tailwind v4 compiles; the bare `z-dialog` emits nothing at all)
    - .planning/phases/11-quality-gates-pattern-layer-app-shell/11-UI-SPEC.md § the mobile-overlay primitive (including the superseded-FEATURES.md-F1 note and the drag/grab-handle exclusions)
  </read_first>
  <action>
Create `src/components/patterns/responsive-dialog.tsx`, `"use client"` (its own behaviour requires it).
It **wraps** `ui/dialog` and passes classes to `DialogContent`. Do not fork `src/components/ui/dialog.tsx`
and do not run `npx shadcn add sheet` — that block is a recorded-decision reversal, not a dependency choice.

Below `sm:` the content presents as a bottom sheet:
`bottom-0 top-auto left-0 right-0 w-full max-w-none translate-x-0 translate-y-0 rounded-t-xl rounded-b-none
max-h-[85dvh] overflow-y-auto`. At `sm:` and up the vendored centred dialog renders **byte-unchanged**.

`dvh`, never `vh`, and the reason goes in a comment: on iOS Safari `vh` includes the retracted URL bar and
clips the sheet's footer, which is where its confirm button lives.

Motion: mobile `slide-in-from-bottom`, desktop `zoom-in-95` (shipped). Both are inside the 320ms budget and
both are neutralised by the global reduced-motion reset — do not add a duration token.

Dismiss affordances: overlay click, `Escape`, and a **visible close button in the sheet's own header**.
**No grab handle** — drag-to-dismiss is out of scope (it needs `vaul`, a new dependency in a milestone whose
net-new test bars it), and a handle implying draggability is an affordance that lies. Record both exclusions.

`data-testid="responsive-dialog"`.

Add a header block recording the `--z-sheet` consequence in plain words: the overlay is the dialog in another
presentation, so it renders at `--z-dialog` (30) in both, and `--z-sheet` (20) now has **zero** call sites.
Point at `tests/design/sheet-absent.test.ts` as the assertion of that zero.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('src/components/patterns/responsive-dialog.tsx','utf8');if(/\[\d+vh\]/.test(s)){console.error('bare vh unit');process.exit(1)}if(!s.includes('[85dvh]'))process.exit(1);if(require('fs').existsSync('src/components/ui/sheet.tsx'))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/sheet.tsx` does not exist and `git diff --stat src/components/ui/` is empty
    - `git diff --stat package.json` is empty — no `vaul`, no registry fetch
    - `/\[\d+vh\]/.test(source)` is `false` and `source.includes("[85dvh]")` is `true`
    - The file contains no grab-handle element and no drag handler
    - A visible close button renders inside the sheet header at every breakpoint
    - `data-testid="responsive-dialog"` appears exactly once
    - `npm run build` exits 0 and `tests/design/motion-budget.test.ts` still passes
  </acceptance_criteria>
  <done>One overlay primitive exists, presenting two ways from one focus trap and one baseline set.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the --z-sheet zero, with its reason</name>
  <files>src/app/globals.css, tests/design/sheet-absent.test.ts, tests/design/elevation-z.test.ts</files>
  <read_first>
    - tests/design/elevation-z.test.ts (the `shadow-sticky` zero-inventory precedent 10-12 set, and every pinned z-scale count — find them before editing)
    - src/app/dev/theme/page.tsx:96-107 (`STICKY_INVENTORY` — the one-entry inventory idiom that records a zero with its reason)
    - src/lib/design/contrast-pairs.ts:34-36 (the EXCLUDED_PAIRS-with-reasons idiom Shared Pattern 6 names)
    - tests/design/scaffold-residue.test.ts:16-20 (why an absence needs a permanent test — "a deletion has no symptom")
    - src/app/globals.css (the `--z-sheet` declaration and the surrounding comment block style)
  </read_first>
  <action>
Add one line to `src/app/globals.css` beside the `--z-sheet` declaration recording **why** it is empty: the
mobile-overlay primitive is `ui/dialog` in another presentation, so it renders at `--z-dialog`; the step is
kept because Phase 12's mobile filters and booking rail may yet need a layer strictly below the dialog. Do
not delete the token.

Create `tests/design/sheet-absent.test.ts` with three assertions and their anti-vacuity clauses:

- **AC#26:** `existsSync("src/components/ui/sheet.tsx")` is `false`, and
  `existsSync("src/components/patterns/responsive-dialog.tsx")` is `true`. Assert both — the absence alone
  is satisfied by a tree with no overlay at all.
- **AC#27:** declare `Z_SHEET_INVENTORY: Record<string, string> = {}` in the test file itself (a declared
  inventory with a mandatory reason per row, `contrast-pairs.ts` shape) and assert it is empty; separately
  assert `globals.css` still declares `--z-sheet` and that zero files under `src/app/**` or
  `src/components/**` contain `z-(--z-sheet)` or `-z-(--z-sheet)`. Count both the positive and negative
  consumption forms as their own inventories — 10-13 established that a negative form is its own thing.
- **AC#28:** on `responsive-dialog.tsx`'s source, `/\[\d+vh\]/.test(source) === false` and
  `source.includes("[85dvh]") === true`. Add the UI-check correction as a comment: the original wording
  banned the substring `vh]`, which `dvh]` contains, so a literal `not.toContain("vh]")` goes RED against the
  mandated correct class — a criterion that fails on the correct implementation is this phase's named
  failure mode arriving from the opposite direction.

Guard-the-guard: assert the scanner walked at least 50 files under `src/components/**` before concluding
zero `z-(--z-sheet)` call sites. Both-directions self-test: a synthetic source containing
`className="z-(--z-sheet)"` must be flagged; one containing only the comment `// z-(--z-sheet) is unused`
must not (strip comments first, per `tests/design/strip-comments.test.ts`).

Update any pinned totals in `tests/design/elevation-z.test.ts` that this plan's new files move, and record
old → new.

NOT COVERED footer: this file proves the sheet block was not installed; it cannot prove the responsive
dialog *behaves* correctly at either breakpoint — that is plan `11-21`'s `/dev/theme` sections and plan `11-22`'s baselines.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/sheet-absent.test.ts tests/design/elevation-z.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/sheet-absent.test.ts tests/design/elevation-z.test.ts` passes
    - Watched red: create an empty `src/components/ui/sheet.tsx` → AC#26 fails naming it. Deleted. Recorded.
    - Watched red: add `className="z-(--z-sheet)"` to any component → AC#27's call-site count fails. Reverted. Recorded.
    - Watched red: change `max-h-[85dvh]` to `max-h-[85vh]` → AC#28 fails on the bare-unit regex. Reverted. Recorded.
    - Watched red (vacuity): point the scanner at an empty directory → guard-the-guard fails rather than the zero passing
    - `globals.css` still declares `--z-sheet` and now carries the one-line reason
    - Any pinned count changed in `elevation-z.test.ts` is recorded old → new
  </acceptance_criteria>
  <done>The zero the overlay decision created is a contract rather than a coincidence, and the criterion that would have failed on correct code is fixed with its reason recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server error object ↔ rendered DOM | Error text carries table names, file paths and connection strings |
| overlay implementation ↔ focus management | Two overlay mechanisms mean two focus traps and two escape behaviours |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-ERRLEAK | Information Disclosure | `patterns/error-state.tsx` | mitigate | The component has no `error` prop at all — only `digest`. The leak is prevented in the TYPE, not by a rendering convention; plan `11-18`'s `SENTINEL_LEAK_PROBE` proves it end to end |
| T-11-FOCUSTRAP | Denial of Service (accessibility) | mobile overlay | mitigate | One primitive composed over `ui/dialog`; `src/components/ui/sheet.tsx` asserted absent, so there is exactly one focus trap and one escape behaviour |
| T-11-DEADZERO | Repudiation | `--z-sheet` | mitigate | `Z_SHEET_INVENTORY` asserted empty plus a `globals.css` line recording why — a zero that is asserted is a contract; a zero that is merely true is an invitation |
| T-11-CRITFAIL | Repudiation | AC#28's assertion form | mitigate | The `vh]`-substring trap is corrected to a bounded regex and the correction is recorded in the test, so the gate cannot fail on the correct implementation |
| T-11-SC | Tampering | shadcn registry / npm | mitigate | No `sheet` block, no `vaul`, no `@tailwindcss/typography`; `git diff --stat package.json` and `src/components/ui/` both empty are acceptance criteria |
</threat_model>

<verification>
- `npm run test:design` green with one new file plus the updated `elevation-z` totals
- `npm run build` exits 0
- `git diff --stat package.json src/components/ui/` is empty
- `ls drizzle/ | tail -1` still `0025_audit_resolved_by.sql`
</verification>

<success_criteria>
- The empty, error and overlay patterns exist prop-complete with no adopters yet
- The error surface cannot be handed a message to leak, by construction
- The `--z-sheet` zero and the `sheet`-block absence are both asserted with their reasons
</success_criteria>

<output>
Create `.planning/phases/11-quality-gates-pattern-layer-app-shell/11-09-SUMMARY.md` when done
</output>
