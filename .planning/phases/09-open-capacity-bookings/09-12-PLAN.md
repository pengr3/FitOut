---
phase: 09-open-capacity-bookings
plan: 12
type: execute
wave: 4
depends_on: ["09-04", "09-07", "09-11"]
files_modified:
  - src/components/availability/date-pass-picker.tsx
  - src/components/availability/availability-calendar.tsx
  - src/components/booking/book-cta.tsx
  - src/app/listings/[id]/page.tsx
  - tests/availability/date-pass-picker.test.tsx
autonomous: true
requirements: [OPEN-02, OPEN-04]

must_haves:
  truths:
    - "A booker on a drop-in listing picks a DAY and a number of passes — there are no hour chips and no full-day button anywhere on the page (OC-02)"
    - "A fully booked date is programmatically disabled in the month grid, struck through and labelled — never merely greyed, never a dead end (OC-11, UI-SPEC § 2a)"
    - "Booking a drop-in listing sends {listingId, date, requestedPasses} and nothing resembling a time window (OC-02, Security V5)"
    - "Losing the race renders 'Just sold out — pick another date.' in the calm shipped notice and refreshes the calendar (OC-13)"
    - "The exclusive hourly calendar renders byte-identically — the fork adds a branch, it does not replace a surface"
  artifacts:
    - path: "src/components/availability/date-pass-picker.tsx"
      provides: "the month grid + day panel for drop-in listings"
      min_lines: 150
    - path: "src/components/booking/book-cta.tsx"
      provides: "the open branch + the sold-out reason + the date/passes sign-in resume"
      contains: "placeOpenHold"
  key_links:
    - from: "src/components/booking/book-cta.tsx"
      to: "placeOpenHold"
      via: "the open branch of the CTA submit"
      pattern: "placeOpenHold"
    - from: "src/components/availability/date-pass-picker.tsx"
      to: "getOpenMonthAvailability"
      via: "the fully-booked date set feeding react-day-picker's disabled matcher"
      pattern: "getOpenMonthAvailability"
---

<objective>
Build the booker-facing drop-in surface: a `DatePassPicker` (month grid + day panel, no hour chips), the
`AvailabilityCalendar` fork that mounts it, the rail's per-person price / pass count / estimate, and the
`BookCta` open branch that calls `placeOpenHold`.

Purpose: OPEN-02 and OPEN-04 on the page where a booker actually decides. 09-UI-SPEC's first visual rule is
that **the hour chips are gone, and their absence is the primary signal** — this is not a disabled picker,
it is a different question ("which day?" instead of "which hours?").

Output: the picker, the calendar fork, the rail branch, the CTA branch, and a jsdom test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-open-capacity-bookings/09-UI-SPEC.md
@.planning/phases/09-open-capacity-bookings/09-PATTERNS.md
@.planning/phases/09-open-capacity-bookings/09-04-SUMMARY.md
@.planning/phases/09-open-capacity-bookings/09-07-SUMMARY.md
@.planning/phases/09-open-capacity-bookings/09-11-SUMMARY.md

Source to fork:
@src/components/availability/availability-calendar.tsx
@src/components/booking/book-cta.tsx
@src/app/listings/[id]/page.tsx
</context>

<interfaces>
The lifted selection context to extend (`availability-calendar.tsx:32-61`):
```typescript
type DayLocal = { year: number; month: number; day: number };
type SelectionContext = {
  selection: SlotSelectionValue | null;
  setSelection: (sel: SlotSelectionValue | null) => void;
};
export function BookingSelectionProvider({ children }): JSX.Element;
export function useBookingSelection(): SelectionContext;   // consumed by the calendar, the rail summary AND BookCta
```

The calendar's props (`:67-76`): `{ listingId, timezone, cityLabel, gmtLabel, unitCount, bookable, initialDate, initialDay }`.

Shipped fragments to reuse VERBATIM:
- the `Calendar` + coral `DayButton` override (`:157-175`) — same `timeZone`, `startMonth`/`endMonth`,
  `BOOKING_HORIZON_DAYS` horizon and `data-[selected-single=true]:bg-brand` treatment
- `handleDaySelect` (`:118-141`) including `setSelection(null)` at `:127` (a new day clears the prior
  selection) and the IN-03 stale-clear rule in its `catch`
- the dashed empty-state shells and their copy (`:186-206`)
- the rail summary's `serviceFeeBps`-threading contract (`:230-292`, especially the docblock at `:235-242`)

From 09-04: `DayAvailability.occupancyMode`, `DayAvailability.openCapacity: OpenCapacityDay | null` where
`OpenCapacityDay = { remaining, cap, state, perHeadPriceCents, dayOpenUtc, dayCloseUtc, openTime, closeTime, bookable }`;
actions `getDayAvailability(listingId, dayLocal)` and `getOpenMonthAvailability(listingId, monthLocal)`
returning `{ cap, fullDates: string[] }`.

From 09-07: `placeOpenHold(input)`, `PlaceHoldResult` including `reason: "sold-out"`.
From 09-11: `SpotsLeftChip`, `DropInBadge`, `PassStepper`.

`book-cta.tsx` fragments (`:48-90`): the `taken` branch (`setNotice(result.error); setPending(false);
if (result.reason === "taken") router.refresh();`) and the D-41 sign-in resume that builds
`new URLSearchParams({ start, end, resume: "1" })` then pushes `/login?callbackURL=…`. The coral CTA class
is at `:122`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DatePassPicker — month grid + day panel</name>
  <files>src/components/availability/date-pass-picker.tsx, src/components/availability/availability-calendar.tsx</files>
  <read_first>
    - src/components/availability/availability-calendar.tsx (WHOLE FILE, 292 lines — the selection context at
      :32-61, `handleDaySelect` at :118-141, the Calendar at :157-175, the empty-state shells at :186-206,
      and the rail summary at :230-292)
    - src/components/availability/spots-left-chip.tsx (09-11)
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 2, § 2a, § 2b, § 2c
  </read_first>
  <action>
**(a) Extend the lifted selection context** so it can hold EITHER shape, without breaking the exclusive one:
```typescript
export type OpenSelectionValue = { kind: "open"; date: DayLocal; dateIso: string; passes: number };
type SelectionContext = {
  selection: SlotSelectionValue | null;
  setSelection: (sel: SlotSelectionValue | null) => void;
  /** Phase-9: the drop-in selection ({date, passes}). Null on an exclusive listing, and vice versa —
   *  the two are mutually exclusive because a listing is exactly one mode. */
  openSelection: OpenSelectionValue | null;
  setOpenSelection: (sel: OpenSelectionValue | null) => void;
};
```
Keep the provider's existing `useState`/`useMemo` shape; add a second pair. Do not change
`SlotSelectionValue` or any exclusive consumer.

**(b) Create `src/components/availability/date-pass-picker.tsx`** (`"use client"`). Props:
`{ listingId, timezone, cityLabel, gmtLabel, bookable, initialDate, initialDay, initialFullDates }`.

Structure, top to bottom:
1. **Section header** — the `Availability` heading gains an inline `<DropInBadge />`, and beneath it, at
   14/400 muted: `Pick a day — your pass is good any time they're open.` The shipped tz note
   (`Times shown in {City} time ({GMT±N})`) stays unchanged and still `aria-describedby`-linked.
2. **Month grid** — the shipped `Calendar` reused verbatim (same `timeZone`, `startMonth`/`endMonth`,
   horizon, and coral `DayButton` override). Fully-booked dates are added to the `disabled` matcher array
   from the `fullDates` set, so they are **programmatically disabled, not merely greyed** (a11y,
   non-negotiable), and additionally rendered `text-muted-foreground` with `line-through` on the numeral and
   `aria-label="{date} — fully booked"`. Re-fetch `getOpenMonthAvailability` on month change; seed from
   `initialFullDates`. The grid is BINARY — selectable or fully booked. **No per-cell counts, no dots**
   (09-UI-SPEC Open Q3: 42 markers competing with 42 numerals is illegible at 320px).
3. **Day panel** (replaces the hour-chip rail entirely — no `SlotPicker`, no `ScrollArea`, no "Book full
   day" button). States, with copy exactly:

| state | contract |
|---|---|
| Available | `Friday, Aug 8` (14/600) → `<SpotsLeftChip>` inside `role="status"` → `Open 6:00 AM – 10:00 PM · Makati time` (14/400 muted, `tabular-nums`) → `Your pass covers the whole day — come any time while they're open.` (16/400) |
| Fully booked | date heading → `Fully booked` chip → `All {cap} passes for this day are taken. Try another day.` The grid stays fully interactive — never a dead end. **OC-14: no waitlist in v1** — render no "Notify me" / "Join the waitlist" control; the next step is another day. |
| Closed that day | dashed shell → `Closed on {day}` · `This space isn't open on this day. Try another day.` |
| No hours at all | dashed shell, shipped copy VERBATIM → `No availability yet` · `This host hasn't set their hours yet. Check back soon.` |
| Loading | ONE `Skeleton` (`h-24 w-full rounded-xl`) with `aria-live="polite" aria-busy="true"` — not the 8 chip skeletons |
| Error | shipped copy VERBATIM → `Couldn't load this day` · `Something went wrong fetching availability. Pick the day again to retry.` (`role="alert"`) |

4. **Pass stepper** (`PassStepper` from 09-11) — rendered in the day panel or the rail per § 2c, `min 1`,
   `max = openCapacity.remaining`. **Changing the picked date resets the count to 1** and re-bounds the max,
   mirroring `setSelection(null)` at `availability-calendar.tsx:127`.
5. On every day change, write `{ kind:"open", date, dateIso, passes }` into `setOpenSelection` so the rail
   and the CTA read one source. Clear it (`setOpenSelection(null)`) when the fetch fails — the IN-03
   stale-clear rule: a failed fetch must never leave the prior day's state on screen.

Reuse `handleDaySelect`'s exact structure (venue-tz `format` for the `DayLocal`, `setLoading`, `setError`,
the `catch` that nulls the payload).

**(c) Fork `AvailabilityCalendar`.** Add `occupancyMode` to its props (server-read from the listing) and, at
the top of the component body, `if (occupancyMode === "open_capacity") return <DatePassPicker … />;`.
The exclusive path below must stay **byte-unchanged**. Comment that the fork is on the SERVER-READ mode and
never on a client flag (Security V4).
  </action>
  <acceptance_criteria>
    - `grep -c "SlotPicker" src/components/availability/date-pass-picker.tsx` prints `0`
    - `grep -c "Book full day\|fullDay" src/components/availability/date-pass-picker.tsx` prints `0`
    - `grep -c "getOpenMonthAvailability" src/components/availability/date-pass-picker.tsx` prints ≥ 1
    - `grep -ci "waitlist|notify me|get alerted" src/components/availability/date-pass-picker.tsx` prints `0` (OC-14)
    - `grep -c "disabled" src/components/availability/date-pass-picker.tsx` prints ≥ 1 and `grep -c "line-through" src/components/availability/date-pass-picker.tsx` prints ≥ 1
    - All six shipped/spec strings present verbatim: `grep -c "No availability yet"`, `"Couldn't load this day"`, `"Closed on"`, `"Try another day."`, `"Your pass covers the whole day"`, `"Pick a day — your pass is good any time they're open."` each print ≥ 1
    - `grep -c 'occupancyMode === "open_capacity"' src/components/availability/availability-calendar.tsx` prints `1`
    - `git diff -U0 src/components/availability/availability-calendar.tsx | grep '^-' | grep -v '^---' | grep -c "SlotPicker\|CalendarDayButton\|handleDaySelect"` prints `0` (the exclusive path was added around, not edited)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>A drop-in listing renders a month grid with programmatically disabled full dates and a single day panel with all six states; there is no hour picker anywhere; the exclusive calendar is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: the open rail — per-person price, pass count, exact all-in estimate</name>
  <files>src/components/availability/availability-calendar.tsx, src/app/listings/[id]/page.tsx</files>
  <read_first>
    - src/components/availability/availability-calendar.tsx lines 226-292 (`RailSelectionSummary` and its
      `serviceFeeBps` docblock at :235-242 — the D-75 bundle-inlining trap)
    - src/app/listings/[id]/page.tsx lines 180-300 (the page container, the rail grid, the capacity line,
      the price block, the policy disclosure and the CTA)
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 2d
  </read_first>
  <action>
**(a) `RailPassSummary`** — an open branch of `RailSelectionSummary`, same file, same money discipline:
`{Fri, Aug 8}` (14/600) · `{N} passes` (14/400 muted) · `Est. {₱all-in}`.
The estimate is `computeServiceFee(perHeadPriceCents * passes, serviceFeeBps).allInCents` using the
**server-threaded `serviceFeeBps`** — copy the existing docblock's reasoning verbatim: this is a
`"use client"` module and must NEVER fall back to the `SERVICE_FEE_BPS` default, because a non-public
override is not inlined into the browser bundle and the rail would quote 5% while checkout charged 7% —
the number going UP between browsing and paying, which D-75 forbids.

Note in a comment that for open capacity this figure is EXACT rather than approximate: open pricing is
purely linear, so `computeServiceFee(perHead × N)` is precisely what the claim will freeze.

**(b) The listing page's open rail** (`listings/[id]/page.tsx`):
- price: `₱350/person` at the shipped rail size, from `allInRateParts` (server-composed, all-in, 09-05) plus
  the shipped muted `Service fee included` line
- capacity line: `Up to {N} people a day` (replacing `Up to {N} people`), same shipped `UsersIcon`
- policy: `CancellationPolicyDisclosure` — the **unchanged component**, given `openCapacity` (09-09's
  required prop) so it renders the generic drop-in copy
- selection summary: `RailPassSummary`
- CTA: the inherited coral `Book this space`, disabled until a selectable date is chosen, with the hint
  `Pick a day above to book.` (adapted from the shipped `Pick a time above to book.`)
- not-bookable: the inherited `Not bookable yet` tooltip branch, unchanged
- pass `occupancyMode` into `AvailabilityCalendar` and the `Availability` heading's `DropInBadge`

The exclusive branch of this page must be untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "Pick a day above to book." "src/app/listings/[id]/page.tsx"` prints `1` and `grep -c "Pick a time above to book." "src/app/listings/[id]/page.tsx"` still prints `1` (both branches present)
    - `grep -c "people a day" "src/app/listings/[id]/page.tsx"` prints `1`
    - `grep -c "serviceFeeBps" src/components/availability/availability-calendar.tsx` increases by ≥ 1 and `grep -c "SERVICE_FEE_BPS" src/components/availability/availability-calendar.tsx` prints `0` (no client-side fallback)
    - `grep -c "RailPassSummary" src/components/availability/availability-calendar.tsx` prints ≥ 1
    - `grep -c "formatMoney\|computeServiceFee" "src/app/listings/[id]/page.tsx"` — the page composes money SERVER-side only; no arithmetic was added to a client module
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The open rail shows a per-person all-in rate, the daily capacity line, the pass summary with an exact server-rated estimate, and a CTA that stays disabled with a day-shaped hint until a date is picked.</done>
</task>

<task type="auto">
  <name>Task 3: BookCta open branch + sold-out + the picker test</name>
  <files>src/components/booking/book-cta.tsx, tests/availability/date-pass-picker.test.tsx</files>
  <read_first>
    - src/components/booking/book-cta.tsx (WHOLE FILE, 144 lines — the submit handler at :48-90 including
      the `taken` branch and the D-41 sign-in resume, the ref-guarded auto-resume at :84-90, and the coral
      CTA class at :122)
    - src/app/actions/booking.ts `placeOpenHold` (09-07) and the extended `PlaceHoldResult`
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 3 and § "BookCta (open)"
  </read_first>
  <action>
**(a) Fork the submit.** When the listing is open-capacity, read `openSelection` from the lifted context and
call `placeOpenHold({ listingId, date: dateIso, requestedPasses: passes })` instead of `placeHold`. The
exclusive branch keeps sending `{ startUtc, endUtc, fullDay }` unchanged.

**(b) Handle `sold-out` through the SHIPPED notice path.** Extend the existing branch:
```typescript
      // taken / sold-out / not-bookable / invalid — calm neutral notice + refresh so the calendar reflects
      // reality. `sold-out` is the drop-in twin of `taken` (OC-13) and deliberately reuses this exact path:
      // one grammar, one treatment, never red, never a modal.
      setNotice(result.error);
      setPending(false);
      if (result.reason === "taken" || result.reason === "sold-out") router.refresh();
```
The copy itself comes from the server (`SOLD_OUT_MESSAGE`) — **do not hardcode the sold-out string in this
component**; a second copy is a second source of truth.

**(c) Fork the D-41 sign-in resume.** For an open listing build
`new URLSearchParams({ date: dateIso, passes: String(passes), resume: "1" })` instead of `start`/`end`/
`fullDay`, then the same `router.push('/login?callbackURL=' + encodeURIComponent(callback))`. The
ref-guarded auto-resume at `:84-90` and the coral CTA class at `:122` are unchanged.

**(d) `tests/availability/date-pass-picker.test.tsx`** (jsdom, mocking the two availability actions and
`placeOpenHold`):
- an available date renders the date heading, the chip, the `Open 6:00 AM – 10:00 PM · {City} time` line and
  the pass helper — and renders NO hour chip and no "Book full day" control
- a fully booked date renders `Fully booked` and `All 4 passes for this day are taken. Try another day.`,
  and the month grid remains interactive
- a fully-booked date in the month grid carries `aria-disabled="true"` (or the disabled attribute) and an
  `aria-label` containing `fully booked`
- changing the picked date resets the pass count to 1
- a failed day fetch renders `Couldn't load this day` and clears the prior day's panel (IN-03)
- clicking `Book this space` calls `placeOpenHold` with exactly `{ listingId, date, requestedPasses }` —
  assert the argument object has **no** `startUtc`/`endUtc`/`fullDay` key
- a `sold-out` result renders the server-supplied notice text and triggers `router.refresh()`
  </action>
  <acceptance_criteria>
    - `grep -c "placeOpenHold" src/components/booking/book-cta.tsx` prints `1`
    - `grep -c '"sold-out"' src/components/booking/book-cta.tsx` prints `1`
    - `grep -c "Just sold out" src/components/booking/book-cta.tsx` prints `0` (the copy stays server-owned)
    - `grep -c "passes:" src/components/booking/book-cta.tsx` prints ≥ 1 (the sign-in resume carries the pass count)
    - `npx vitest run tests/availability/date-pass-picker.test.tsx` exits 0 with ≥ 7 tests passing
    - `npx vitest run tests/availability tests/booking` exits 0
    - `npx tsc --noEmit` exits 0; `npm run lint` reports 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/availability tests/booking && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>The CTA sends a date and a pass count for drop-in listings, resumes sign-in with the same pair, and routes a race loss through the shipped calm notice using the server's own copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker state → `placeOpenHold` | the date and pass count leave the browser and become a money-bearing claim |
| public listing page → availability actions | unauthenticated reads of a published listing's occupancy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Tampering | a smuggled time window on the open path | mitigate | The CTA's open branch sends only `{listingId, date, requestedPasses}`, asserted by a test on the call argument, and `openHoldSchema` strips anything else server-side. |
| T-09-13 | Tampering | client-derived scarcity | mitigate | The picker passes the server's `state` straight into `SpotsLeftChip` and computes no threshold; the fully-booked date set is server-computed by `getOpenMonthAvailability`. |
| T-09-40 | Tampering | client-side money in the rail | mitigate | The rail estimate uses `computeServiceFee` with the SERVER-THREADED `serviceFeeBps`; a grep gate asserts the client module never references `SERVICE_FEE_BPS`. |
| T-09-41 | Information Disclosure | month-level occupancy map | accept | Same class as the exclusive calendar's per-hour free/taken grid (T-03-ENUM, accepted); only date→full is exposed. |
| T-09-42 | Denial of Service | month re-fetch on every navigation | accept | One additional read per month change on a public read model, bounded by the 90-day horizon. |
</threat_model>

<verification>
- `npx vitest run tests/availability tests/booking` → green including ≥ 7 new picker cases
- `npx tsc --noEmit` → 0; `npm run lint` → 0 errors; `npm run build` → 0
- `git diff` on `availability-calendar.tsx` shows no removed exclusive-path line
</verification>

<success_criteria>
- A drop-in listing shows a day-shaped picker with no hour chips and no full-day control
- Fully booked dates are programmatically disabled, struck and labelled; the day panel always offers a next step
- The rail shows a per-person all-in rate, the pass count and an exact estimate at the server's fee rate
- Booking sends only a date and a pass count; a race loss shows the server's calm sold-out copy and refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/09-open-capacity-bookings/09-12-SUMMARY.md`
</output>
