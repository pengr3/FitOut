---
phase: 09-open-capacity-bookings
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - tests/availability/open-capacity-exclude.test.ts
  - tests/availability/open-capacity-race.test.ts
autonomous: true
requirements: [OPEN-03]

must_haves:
  truths:
    - "N+1 genuinely concurrent single-head claims against a cap of N commit exactly N heads — the (cap+1)-th is refused (SC#3, OC-05)"
    - "Concurrent MULTI-head claims never overshoot the cap; the boundary racer is granted exactly `remaining` and REPORTS it (OC-07)"
    - "A cancelled open booking frees its heads — a fresh claim on the same date then succeeds (OC-15)"
    - "A second open-capacity booking on the SAME date and unit=1 commits — the narrowed EXCLUDE no longer calls it a double-book (Pitfall 1)"
    - "Deleting the pg_advisory_xact_lock line from the SHIPPED claim turns this file RED with a committed-SUM-over-cap message"
  artifacts:
    - path: "tests/availability/open-capacity-race.test.ts"
      provides: "the SC#3 acceptance gate — two layers, two named mutations"
      min_lines: 180
      contains: "makeRacingClients"
    - path: "tests/availability/open-capacity-exclude.test.ts"
      provides: "the Pitfall-1 same-date multi-booking DDL proof"
      contains: "open_capacity"
  key_links:
    - from: "tests/availability/open-capacity-race.test.ts"
      to: "src/lib/availability/units.ts createOpenCapacityHold"
      via: "Layer 2 drives the REAL shipped claim over independent connections"
      pattern: "createOpenCapacityHold"
    - from: "tests/availability/open-capacity-race.test.ts"
      to: "tests/helpers/db.ts makeRacingClients"
      via: "one independent postgres.js connection per racer"
      pattern: "makeRacingClients"
---

<objective>
Ship the phase's non-negotiable acceptance gate: a GENUINE two-connection concurrent-overbook race against
the real `createOpenCapacityHold` (the Phase-3 SC#4 analog for a shared slot), plus the DDL proof that the
narrowed EXCLUDE lets multiple open bookings share one date.

Purpose: OPEN-03 / SC#3. D-123's guarantee is PROCEDURAL — the app must take the advisory lock — and Phase 8
recorded exactly how that fails silently: deleting the production lock left the race file GREEN (2/2), all of
`tests/group/` GREEN and the full 792-test suite GREEN while shipping an over-cap bug, because the only
proofs INLINED the SQL. This file must go RED when the SHIPPED lock line is deleted, and that must be
measured, not asserted.

Output: two integration test files and two recorded mutation runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-open-capacity-bookings/09-RESEARCH.md
@.planning/phases/09-open-capacity-bookings/09-VALIDATION.md
@.planning/phases/09-open-capacity-bookings/09-PATTERNS.md
@.planning/phases/09-open-capacity-bookings/09-02-SUMMARY.md

Test analogs to model on (read, do not modify):
@tests/group/seat-claim-race.test.ts
@tests/availability/exclusion-race.test.ts
@tests/helpers/db.ts
</context>

<interfaces>
From `tests/helpers/db.ts`:
```typescript
export async function setupTestDb(): Promise<TestDb>;              // isolated schema, replays ALL migrations
export async function teardownTestDb(testDb: TestDb): Promise<void>;
export function makeRacingClients(schema: string, n: number): postgres.Sql[]; // n INDEPENDENT connections
export type TestDb = { db: ReturnType<typeof drizzle>; client: postgres.Sql; schema: string };
```

From `src/lib/availability/units.ts` (shipped by 09-02):
```typescript
export type OpenHoldSuccess = HoldSuccess & { granted: number; requested: number };
export type OpenHoldResult = OpenHoldSuccess | { error: string; soldOut?: true };
export async function createOpenCapacityHold(db: DbConn, input: {
  listingId: string; bookerId: string;
  dayOpenUtc: Date | string; dayCloseUtc: Date | string;
  requestedHeads: number; idempotencyKey?: string | null;
}): Promise<OpenHoldResult>;
```

From `src/lib/availability/open-capacity.ts`: `SOLD_OUT_MESSAGE`
From `src/lib/pg.ts`: `isPgError(e, code)`

Fixture idiom (from `exclusion-race.test.ts:61-79`): insert `user` rows then a `listing` row via
`testDb.db.insert(...)`, then raw `testDb.client\`INSERT INTO booking ...\`` for DDL-level cases.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tests/availability/open-capacity-exclude.test.ts — the Pitfall-1 DDL proof</name>
  <files>tests/availability/open-capacity-exclude.test.ts</files>
  <read_first>
    - tests/availability/exclusion-race.test.ts (WHOLE FILE — the `ins` raw-insert helper at :27-36, the
      `expect23P01` helper at :50-59, the fixture beforeAll at :61-79, and the
      "frees the slot when the occupying row leaves the partial WHERE" case at :128-138)
    - drizzle/0022_booking_exclusion_v3.sql (the constraint this file proves)
    - .planning/phases/09-open-capacity-bookings/09-RESEARCH.md §"Pitfall 1"
  </read_first>
  <action>
Create `tests/availability/open-capacity-exclude.test.ts`, modelled on `exclusion-race.test.ts`. Header
comment must state: this file proves drizzle/0022's NARROW — that open-capacity rows are removed from
`booking_no_overlap` while exclusive rows are still arbitrated by it. It goes RED if 0022 stops applying, in
EITHER direction (an un-narrowed predicate breaks case 1; an over-wide narrow breaks case 2).

Fixture: one host, one booker, one listing (`maxOccupancy: 20`, `unitCount: 1`, `perHeadPriceCents: 35000`).
Extend the raw insert helper to carry the flag:
```typescript
const ins = (c: postgres.Sql, id: string, unit: number, start: string, end: string,
             openCapacity: boolean, status = "confirmed") => c`
  INSERT INTO booking (id, listing_id, unit, booker_id, starts_at, ends_at, status, open_capacity, declared_pax)
  VALUES (${id}, ${LISTING}, ${unit}, ${BOOKER}, ${start}, ${end}, ${status}, ${openCapacity}, 1)`;
```

Cases (four, each on its OWN date so committed rows never pre-fill another case):

1. **"admits many open bookings on the same date, same unit, identical window"** — insert THREE rows with
   `open_capacity = true`, unit 1, `starts_at = '2026-09-01T22:00:00Z'`, `ends_at = '2026-09-02T14:00:00Z'`
   (the OC-03 whole-day envelope). All three must commit. Assert
   `SELECT count(*) ... WHERE starts_at = '2026-09-01T22:00:00Z'` is `3`. Comment: under the un-narrowed
   predicate the SECOND row raised 23P01 — that is Pitfall 1, and it is a functional break the exclusive
   race test could never catch because its fixture is exclusive.
2. **"still rejects the second EXCLUSIVE overlapping booking on the same unit (the keystone is untouched)"** —
   two rows with `open_capacity = false` on `2026-09-03`, unit 1, identical window: the second must reject
   with 23P01 (reuse the `expect23P01` helper shape).
3. **"an open row does not block an exclusive row and vice versa"** — an open row and then an exclusive row
   on `2026-09-04`, unit 1, identical window: BOTH commit. Comment that in production a listing is exactly
   one mode so this combination never occurs; the case exists to pin that the narrow keys on the FLAG and
   not on some incidental property of the window.
4. **"the live constraint definition carries the narrow"** — read it back from the isolated schema:
   ```typescript
   const [{ def }] = await testDb.client`
     SELECT pg_get_constraintdef(c.oid) AS def FROM pg_constraint c WHERE c.conname = 'booking_no_overlap'`;
   expect(def).toContain("open_capacity = false");
   expect(def).toContain("'cancelled'");   // the occupying complement set is unchanged
   ```
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/availability/open-capacity-exclude.test.ts` exits 0 with 4 tests passing
    - `grep -c "open_capacity" tests/availability/open-capacity-exclude.test.ts` prints ≥ 6
    - `grep -c "pg_get_constraintdef" tests/availability/open-capacity-exclude.test.ts` prints `1`
    - `grep -c "23P01" tests/availability/open-capacity-exclude.test.ts` prints ≥ 1
    - `git status --short -- src/` is empty (this task modifies no production file)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/availability/open-capacity-exclude.test.ts</automated>
  </verify>
  <done>Four cases green: many open rows share a date, exclusive overlap still rejects with 23P01, the two modes do not block each other, and the live constraint text carries the narrow.</done>
</task>

<task type="auto">
  <name>Task 2: tests/availability/open-capacity-race.test.ts — the SC#3 gate, two layers</name>
  <files>tests/availability/open-capacity-race.test.ts</files>
  <read_first>
    - tests/group/seat-claim-race.test.ts (WHOLE FILE — the two-layer header at :12-34, the per-racer
      independent-connection wrapper at :172-180, the assert-committed-rows-FIRST ordering at :235-241,
      and the fixture-isolation rule at :49-51)
    - tests/helpers/db.ts lines 40-70 (`makeRacingClients` and why a shared `max:1` client proves nothing)
    - src/lib/availability/units.ts `createOpenCapacityHold` (as shipped by 09-02 — especially the D-42
      own-hold idempotency pre-check, which decides the racers' identity, below)
    - .planning/phases/09-open-capacity-bookings/09-VALIDATION.md §"The SC#3 acceptance-gate test"
  </read_first>
  <action>
Create `tests/availability/open-capacity-race.test.ts` with the **two-layer structure and two named
mutations**, copying `seat-claim-race.test.ts`'s header discipline (each mutation names its OWN file and its
OWN line; run BOTH — neither substitutes for the other):

- **LAYER 1 — the PATTERN proof.** Each racer opens its own transaction on its own connection and INLINES
  the critical section: `pg_advisory_xact_lock(hashtextextended(listing||':'||dayOpen, 0))` →
  `SELECT COALESCE(SUM(declared_pax),0)` over the occupying set → conditional `INSERT`. This proves the
  advisory-lock PATTERN serializes. Mutation 1 deletes the inlined lock line.
- **LAYER 2 — the SHIPPED proof.** Each racer calls the REAL `createOpenCapacityHold` over its OWN
  `drizzle(client)`. Mutation 2 deletes the lock line from `src/lib/availability/units.ts`.

**⚠️ THE RACERS' IDENTITY IS THE SUBTLE HALF, NOT THE CONNECTIONS.** `createOpenCapacityHold` carries a D-42
own-hold pre-check keyed on `(bookerId, listingId, starts_at)`. If every racer used the SAME `bookerId`, the
losers would MATCH the winner's hold and return `{ ok: true, replayed: true }` — the cap is never contested
and the test is green while proving nothing (the Phase-8 name-only-identity lesson, restated). **Seed one
DISTINCT `user` row per racer and pass a distinct `bookerId` to each**, with `idempotencyKey: null`. State
this in the file header.

Helper (the Layer-2 idiom):
```typescript
function realClaim(client: TestDb["client"], listingId: string, bookerId: string, heads: number) {
  return createOpenCapacityHold(drizzle(client), {
    listingId, bookerId,
    dayOpenUtc: DAY_OPEN, dayCloseUtc: DAY_CLOSE,
    requestedHeads: heads, idempotencyKey: null,
  });
}
```

**Fixture isolation:** a distinct `(listing, date)` per case — a separate listing id per case is simplest
(cap differs per case anyway). Set `unitCount: 1`, `perHeadPriceCents: 35000`, `cancellationPolicy: "standard"`,
`status: "published"`, and the case's `maxOccupancy`.

Cases (each asserts the COMMITTED rows FIRST, read back through `testDb.client` — an INDEPENDENT connection —
so a mutation's failure message names the database truth, not a returned-value proxy):

- **Case 1 (Layer 1, pattern).** cap 3, four inlined racers × 1 head. Assert committed
  `SUM(declared_pax) === 3` FIRST, then exactly one racer failed to insert.
- **Case 2 (Layer 1, pattern, multi-head).** cap 5, three inlined racers × 2 heads. Assert committed
  `SUM(declared_pax) <= 5` FIRST.
- **Case 3 (Layer 2, SHIPPED — the SC#3 gate).** cap 3, four racers × 1 head via `realClaim`, four distinct
  bookers, `makeRacingClients(schema, 4)`, `Promise.allSettled`. Assertions, in this order:
  1. committed `SELECT COALESCE(SUM(declared_pax),0)::int FROM booking WHERE listing_id = ... AND open_capacity = true AND starts_at = DAY_OPEN AND status = 'pending'` **=== 3**
  2. exactly THREE results are `{ ok: true }` and exactly ONE is `{ error: SOLD_OUT_MESSAGE, soldOut: true }`
  3. every successful result has `granted === 1` and `replayed === false`
- **Case 4 (Layer 2, SHIPPED — OC-07 partial fill).** cap 5, three racers × 2 heads via `realClaim`, three
  distinct bookers. Assertions in order:
  1. committed SUM **=== 5** (never 6)
  2. the three `granted` values, sorted, are `[1, 2, 2]` — the boundary racer was granted exactly `remaining`
     and **reported** it (`granted < requested`), and NO racer returned `soldOut`
  3. every row's `quoted_total_cents` equals `computeServiceFee(35000 × granted).allInCents` — i.e. the money
     was frozen for the GRANTED heads, never the requested ones (no silent over-charge, OC-07/O6)
- **Case 5 (Layer 2 — the release path, OC-15).** cap 1. Claim 1 head → succeeds. A second claim (different
  booker) → `soldOut`. `UPDATE booking SET status = 'cancelled' WHERE id = <first>`. A third claim (third
  booker) → succeeds. Assert committed occupying SUM is `1` at the end. Comment: no release code exists —
  `remaining` is a live SUM, so a cancelled row simply leaves the occupying set (Pitfall 3).
- **Case 6 (Layer 2 — D-42 replay, T-09-08).** cap 5. The SAME booker calls `realClaim(...)` twice
  sequentially with 2 heads. Assert: the second returns the SAME booking `id` with `replayed === true`, and
  the committed SUM is **2, not 4**.

Always `await client.end()` for every racing client in a `finally`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/availability/open-capacity-race.test.ts` exits 0 with 6 tests passing
    - `grep -c "makeRacingClients" tests/availability/open-capacity-race.test.ts` prints ≥ 1
    - `grep -c "createOpenCapacityHold" tests/availability/open-capacity-race.test.ts` prints ≥ 1 (Layer 2 drives the SHIPPED function)
    - `grep -c "pg_advisory_xact_lock" tests/availability/open-capacity-race.test.ts` prints ≥ 1 (Layer 1 inlines the pattern)
    - `grep -c "LAYER 1\|LAYER 2" tests/availability/open-capacity-race.test.ts` prints ≥ 2
    - `grep -c "MUTATION" tests/availability/open-capacity-race.test.ts` prints ≥ 2 (each mutation names its own file and line)
    - Distinct bookers per racer: `grep -c "booker" tests/availability/open-capacity-race.test.ts` prints ≥ 4, and the header explains why a shared booker would make the test vacuous
    - `git status --short -- src/` is empty
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/availability/open-capacity-race.test.ts</automated>
  </verify>
  <done>Six cases green across two layers, driving the real claim over independent connections with distinct booker identities, asserting committed rows before returned values.</done>
</task>

<task type="auto">
  <name>Task 3: run BOTH mutations, restore, and record the failure messages verbatim</name>
  <files>(temporary edits only — both must be reverted)</files>
  <read_first>
    - tests/availability/open-capacity-race.test.ts (as written in Task 2)
    - src/lib/availability/units.ts (the `pg_advisory_xact_lock` line inside `createOpenCapacityHold`)
    - tests/group/seat-claim-race.test.ts lines 12-34 (the two-mutation discipline being reproduced)
  </read_first>
  <action>
A mutation that is described but never run is a comment. Run both, record the exact failure text, restore.

**Mutation 1 — the PATTERN (Layer 1).** Delete the inlined `SELECT pg_advisory_xact_lock(...)` statement from
the Layer-1 racer helper inside `tests/availability/open-capacity-race.test.ts`. Run the file. Cases 1 and 2
MUST go RED with a message naming the committed over-cap SUM (e.g. `expected 4 to be 3`). Restore the line;
re-run; 6/6 green.

**Mutation 2 — the SHIPMENT (Layer 2). This is the one that matters.** Delete the
`await tx.execute(sql\`SELECT pg_advisory_xact_lock(...)\`)` line from `createOpenCapacityHold` in
`src/lib/availability/units.ts`. Run the file. Cases 3 and 4 MUST go RED with a committed-SUM-over-cap
message (e.g. `expected 4 to be 3`). Then restore and prove the restore:
```
git diff --exit-code src/lib/availability/units.ts     # must exit 0
npx vitest run tests/availability/open-capacity-race.test.ts   # must be 6/6 green
```

**If Mutation 2 does NOT go red, the gate is broken and the phase must not proceed.** The most likely causes,
in order: (a) all racers share a `bookerId`, so the D-42 pre-check makes losers replay the winner's hold;
(b) the racers share one `drizzle(testDb.db)` instead of one `drizzle(client)` per `makeRacingClients`
connection (a `max: 1` pool serializes the lock window — RESEARCH Pitfall 1); (c) `Promise.allSettled` fires
after an `await` that already serialized them. Fix the TEST, never weaken the assertion.

Record in the summary, verbatim: both mutation failure messages, the restored 6/6 green run, and the
`git diff --exit-code` result.
  </action>
  <acceptance_criteria>
    - Mutation 1 run recorded: at least one RED case with the failure message quoted verbatim
    - Mutation 2 run recorded: at least one RED case with the failure message quoted verbatim, and the message names a committed SUM greater than the cap (not a returned-value proxy)
    - After restore: `git diff --exit-code src/lib/availability/units.ts` exits 0
    - After restore: `npx vitest run tests/availability/open-capacity-race.test.ts` exits 0, 6/6
    - After restore: `git status --short -- src/` is empty
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code src/lib/availability/units.ts && npx vitest run tests/availability/open-capacity-race.test.ts tests/availability/open-capacity-exclude.test.ts</automated>
  </verify>
  <done>Both mutations were executed and observed RED with verbatim messages, both edits are restored, git is clean, and the file is 6/6 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → shipped claim | Layer 2 is the only thing standing between a deleted lock and a shipped over-cap bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering / DoS-of-correctness | concurrent over-claim (the phase's headline threat) | mitigate | Case 3/4 fire N+1 genuinely concurrent claims through independent backends against the SHIPPED function and assert the committed SUM, with Mutation 2 proving the assertion has teeth. |
| T-09-08 | Repudiation | double-submit replay claiming heads twice | mitigate | Case 6 asserts a same-booker second call returns the SAME booking id with `replayed: true` and leaves the committed SUM at 2, not 4. |
| T-09-11 | Tampering | vacuous race test (identity/pool mistakes) | mitigate | Distinct booker per racer + one `drizzle(client)` per `makeRacingClients` connection, both stated in the header and both named in Task 3's "if it does not go red" checklist. |
| T-09-01 | Tampering | over-wide EXCLUDE narrow | mitigate | Exclude-test case 2 keeps an exclusive overlap rejecting with 23P01 and case 4 asserts the live constraint text still carries the cancelled/declined/completed complement. |
</threat_model>

<verification>
- `npx vitest run tests/availability/open-capacity-race.test.ts tests/availability/open-capacity-exclude.test.ts` → 10 tests green
- Mutation 1 (test-inlined lock) → RED, message recorded, restored
- Mutation 2 (shipped lock in `units.ts`) → RED, message recorded, restored, `git diff --exit-code` clean
- `git status --short -- src/` empty at plan end
</verification>

<success_criteria>
- SC#3 is proven: the (cap+1)-th concurrent claim is refused with `Just sold out — pick another date.` and the
  committed head SUM never exceeds the cap, under a genuine multi-connection race against the shipped code
- OC-07's partial fill is proven: `granted === remaining` at the boundary, reported, and the money frozen for
  the granted heads
- Pitfall 1 is closed: multiple open bookings share one date under the narrowed EXCLUDE, and exclusive
  overlap still rejects
- Both mutations were RUN and observed RED, then restored with a clean git diff
</success_criteria>

<output>
After completion, create `.planning/phases/09-open-capacity-bookings/09-03-SUMMARY.md`
</output>
