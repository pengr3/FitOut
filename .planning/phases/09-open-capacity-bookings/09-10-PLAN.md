---
phase: 09-open-capacity-bookings
plan: 10
type: execute
wave: 3
depends_on: ["09-06"]
files_modified:
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - src/app/(host)/host/listings/[id]/edit/page.tsx
  - src/components/host/mode-lock-notice.tsx
  - src/app/(host)/host/listings/[id]/availability/page.tsx
  - tests/listing/wizard-occupancy.test.tsx
autonomous: true
requirements: [OPEN-01]

must_haves:
  truths:
    - "A host chooses between 'Whole space' and 'Drop-in passes' on its own wizard step, in plain language, with no enum vocabulary and no pre-selection on a new listing (OC-08, UI-SPEC O1)"
    - "In drop-in mode the pricing step asks for a price per person and people per day, and hides hourly rate, day rate and extra-guest pricing (OC-08)"
    - "In drop-in mode the booking-mode step is removed from the flow AND its removal is explained on the review step (OC-10, UI-SPEC § 1d)"
    - "When a booking is still to come, the mode control is locked with a reason, an unlock date, and a route forward (OC-17, UI-SPEC O7)"
    - "The publish checklist lists the fields that mode actually requires, with no hardcoded step index left in the file (UI-SPEC § 1a/1e)"
  artifacts:
    - path: "src/components/host/mode-lock-notice.tsx"
      provides: "the OC-17 lock alert — why, when, and a way out"
      contains: "View your bookings"
  key_links:
    - from: "src/app/(host)/host/listings/[id]/edit/page.tsx"
      to: "getModeLockState"
      via: "server-computed lock state passed to the wizard as props"
      pattern: "getModeLockState"
---

<objective>
Give the host the occupancy choice: a new wizard step before pricing, a mode-forked pricing step and publish
checklist, a removed-and-explained booking-mode step, and the OC-17 lock rendered with a reason, an unlock
instant and an escape hatch.

Purpose: OPEN-01's host half, and 09-UI-SPEC calls it **"the highest-stakes comprehension moment in the
phase"** — a host who misreads it either sells their whole gym to one person or sells one court thirty times.
That is why the choice gets its own screen with two explanatory cards, no pre-selection, and neutral (never
coral) selection markers.

Output: the wizard fork, `ModeLockNotice`, the lock data on the edit page, and a jsdom test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-open-capacity-bookings/09-UI-SPEC.md
@.planning/phases/09-open-capacity-bookings/09-PATTERNS.md
@.planning/phases/09-open-capacity-bookings/09-CONTEXT.md
@.planning/phases/09-open-capacity-bookings/09-06-SUMMARY.md

Source to fork:
@src/app/(host)/host/listings/[id]/edit/wizard.tsx
@src/lib/validation/listing.ts
</context>

<interfaces>
`wizard.tsx:118-134` — the step list and the derived-index idiom already in the file:
```typescript
const STEPS = [
  { key: "type", title: "What kind of space is it?" },
  { key: "details", title: "Tell guests about your space" },
  { key: "location", title: "Where is it?" },
  { key: "photos", title: "Add photos of your space" },
  { key: "pricing", title: "Set your rates" },
  { key: "booking", title: "How do you want to accept bookings?" },
  { key: "cancellation", title: "What happens if a guest cancels?" },
  { key: "review", title: "Review and publish" },
] as const;
/** Index of the D-77 tier step, so the publish-checklist row links back to it without a magic number. */
const CANCELLATION_STEP = STEPS.findIndex((s) => s.key === "cancellation");
```
`wizard.tsx:342-367` — the checklist with HARDCODED indices (`step: 1`, `step: 4`, …) that this plan must
migrate. `:369` computes `progress` and `:378-381` renders `Step {step + 1} of {STEPS.length}`.

`wizard.tsx:819-868` — the booking-mode `RadioGroup` card step to clone; `:870-881` — the D-77 rationale for
neutral `border-primary` selection and NO pre-selection; `:661-693` — the currency-prefixed rate input to
clone for `Price per person`; `symbol` comes from `currencySymbol(listing.currency)` (`:162-170`).

⚠️ Shipped grep tripwire (`wizard.tsx:125-128`): the cancellation step TITLE must stay a unique string,
distinct from the checklist row label. Do not break it while editing nearby.

From `src/lib/listing/mode-lock.ts` (09-06):
```typescript
export type ModeLockState = { locked: boolean; lockedByCount: number; unlocksAt: Date | null };
export async function getModeLockState(dbConn: DbConn, listingId: string): Promise<ModeLockState>;
```
From `src/lib/validation/listing.ts` (09-06): `OCCUPANCY_MODE_VALUES`, `MODE_LOCKED_MESSAGE`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: derive every step index before inserting a step (pure refactor, zero behaviour change)</name>
  <files>src/app/(host)/host/listings/[id]/edit/wizard.tsx</files>
  <read_first>
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx lines 110-140 (STEPS + CANCELLATION_STEP) and
      lines 330-390 (the checklist, `publishEligible`, `progress`, and the "Step X of Y" line)
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 1a (the migration warning)
  </read_first>
  <action>
Inserting a step renumbers everything after it, and the publish checklist currently carries literal indices
(`step: 1`, `step: 4`, …) that would silently point at the WRONG step — the host would click "Hourly rate"
and land on photos. Migrate first, in isolation, so the diff that adds the step cannot hide a renumbering
bug.

1. Add a derived index for every step the checklist references, next to the existing `CANCELLATION_STEP`:
   ```typescript
   const stepIndex = (key: (typeof STEPS)[number]["key"]) => STEPS.findIndex((s) => s.key === key);
   ```
   (Keep `CANCELLATION_STEP` as-is or redefine it through the helper — but do not delete the name; the
   checklist row and any other reference use it.)
2. Replace EVERY literal `step: <number>` in the checklist array (`:342-367`) with `stepIndex("<key>")`.
3. Change nothing else. `STEPS` still has 8 entries, so every index resolves to exactly the number it
   replaced — verify by running the wizard test suite and asserting no rendered output changed.
  </action>
  <acceptance_criteria>
    - `sed -n '/const checklist/,/publishEligible/p' "src/app/(host)/host/listings/[id]/edit/wizard.tsx" | grep -cE "step: [0-9]"` prints `0`
    - `grep -c "stepIndex(" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 6
    - `npx vitest run tests/listing` exits 0
    - `npx tsc --noEmit` exits 0
    - `git diff -U0 "src/app/(host)/host/listings/[id]/edit/wizard.tsx" | grep '^+' | grep -cE "step: [0-9]"` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/listing</automated>
  </verify>
  <done>No hardcoded step index remains in the wizard; behaviour is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: the occupancy step, the two mode cards, and the OC-17 lock</name>
  <files>src/app/(host)/host/listings/[id]/edit/wizard.tsx, src/components/host/mode-lock-notice.tsx, src/app/(host)/host/listings/[id]/edit/page.tsx</files>
  <read_first>
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx lines 800-900 (the booking-mode RadioGroup step to
      clone AND the D-77 "no pre-selection, neutral not coral" rationale at :870-881)
    - src/app/(host)/host/listings/[id]/edit/page.tsx (WHOLE FILE — how the listing row reaches the wizard)
    - src/components/host/cancellation-fee-notice.tsx (the neutral host-side alert shell to borrow)
    - src/lib/listing/mode-lock.ts (09-06)
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 1a, § 1b, § 1f and § Color
  </read_first>
  <action>
**(a) Insert the step.** Add `{ key: "occupancy", title: "How do people use your space?" }` to `STEPS`
immediately BEFORE `pricing`, so the exclusive flow becomes
`type → details → location → photos → occupancy → pricing → booking → cancellation → review` (9 steps).
Rationale comment: the mode decides which fields the pricing step renders AND whether the booking-mode step
exists at all, so it must precede both; burying a mutually-exclusive business-model choice inside a scroll
of currency inputs guarantees misconfigured listings.

**(b) The two cards.** Clone the booking-mode `RadioGroup` card markup (`:827-858`) exactly — same
`label` wrapper, same `cn("flex cursor-pointer items-start gap-3 rounded-lg border p-4", field.value === opt.value && "border-primary")`,
same `RadioGroupItem className="mt-1"`. Copy, verbatim:

| value | icon | title | body | example line |
|---|---|---|---|---|
| `exclusive` | lucide `DoorClosed` | `Whole space` | `One booking at a time. Whoever books gets the space to themselves for the hours they picked.` | `Best for court rentals, studio hire, and private training.` |
| `open_capacity` | lucide `Users` | `Drop-in passes` | `Lots of people share the space on the same day. Each person buys a day pass and can come any time you're open. You set the price per person and how many people you'll let in each day.` | `Best for gyms with day passes, open court sessions, and open-mat classes.` |

`FormDescription`: `This changes how you're paid and how people book. You can change it later, as long as you have no bookings still to come.`

**NO card is pre-selected on a NEW listing** (a listing whose stored `occupancyMode` the wizard has never
written). An EXISTING listing shows its stored mode selected. Restate the D-77 rationale in a comment: the
marker is neutral `border-primary` and **NEVER coral** — coral would advertise a recommendation the product
deliberately does not make (09-CONTEXT records that the same space type goes both ways). Both cards render at
equal weight. The step title is `How do people use your space?`; do not reuse a checklist row label as this
title (the file's tripwire discipline).

The values come from `OCCUPANCY_MODE_VALUES` (09-06) — do not retype the string union.

**(c) `src/components/host/mode-lock-notice.tsx`** — a new presentational component:
```typescript
type ModeLockNoticeProps = {
  lockedByCount: number;     // server-computed
  unlocksAtLabel: string;    // ALREADY formatted venue-local with the tz named — the component formats nothing
};
```
Neutral shadcn `alert` (**never destructive** — a locked control is not a warning), lucide `Lock` icon,
rendered ABOVE the cards so it is read first.
- Title: `You can't change this while bookings are still to come`
- Body: `{N} booking{s} on this space {is|are} still ahead. You can switch after the last one finishes on {unlocksAtLabel}. To switch sooner, cancel those bookings first — that refunds your guests in full.`
- Action: `View your bookings` — a neutral `outline` link to `/host/bookings`. **The escape hatch is
  mandatory**: a disabled control with no route forward is the dead end this product does not ship (O7).
Both the count and the unlock instant are required props — a lock that says only "you can't" and never
"until when" is exactly what O7 exists to prevent, so neither may be optional.

**(d) Wire the lock.** In `edit/page.tsx` (an RSC), call `getModeLockState(db, listingId)` and pass
`{ locked, lockedByCount, unlocksAtLabel }` to the wizard, formatting `unlocksAt` venue-local with the
listing's timezone and city (the shipped label helpers), because the component formats nothing (D-105).

In the wizard, when `locked` is true:
- the STORED mode's card stays visually selected
- the OTHER card is `aria-disabled="true"`, `pointer-events-none`, `opacity-60`, and carries a lucide `Lock`
  glyph plus the visible word `Locked` — never opacity-only
- the `RadioGroup` is `aria-describedby` the alert's id, so the reason is ANNOUNCED, not merely visible
- the field cannot change value

The wizard's lock is a courtesy — `saveListingStep` already refuses the change server-side (09-06). Say so
in a comment so nobody later "simplifies" one of the two away.
  </action>
  <acceptance_criteria>
    - `grep -c '{ key: "occupancy"' "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints `1`
    - `grep -c "Drop-in passes" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 1 and `grep -c "Whole space" ...` prints ≥ 1
    - `grep -ci "occupancy mode\|open_capacity passes\|per-head\|admissions" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` returns no HOST-VISIBLE string containing those terms (enum values may appear only as form values/comparisons, never inside a rendered label) — verify by reading the rendered copy
    - `grep -c "bg-brand\|text-brand\|border-brand" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` is UNCHANGED from before this task (the mode cards add no coral)
    - `grep -c "View your bookings" src/components/host/mode-lock-notice.tsx` prints `1`
    - `grep -c "destructive" src/components/host/mode-lock-notice.tsx` prints `0`
    - `grep -c "unlocksAtLabel: string" src/components/host/mode-lock-notice.tsx` prints `1` and `grep -c "?:" src/components/host/mode-lock-notice.tsx` prints `0` (both props required)
    - `grep -c "getModeLockState" "src/app/(host)/host/listings/[id]/edit/page.tsx"` prints `1`
    - `grep -c "aria-describedby" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>The occupancy step renders two equal-weight neutral cards with the exact UI-SPEC copy, no pre-selection on a new listing, and — when bookings are still ahead — a locked control with a reason, a date and a link out.</done>
</task>

<task type="auto">
  <name>Task 3: mode-forked pricing step, checklist, review summary, hidden booking-mode step, host availability note</name>
  <files>src/app/(host)/host/listings/[id]/edit/wizard.tsx, src/app/(host)/host/listings/[id]/availability/page.tsx, tests/listing/wizard-occupancy.test.tsx</files>
  <read_first>
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx lines 640-700 (the rate-field pattern to clone),
      lines 330-390 (the checklist + progress + "Step X of Y"), and the review step
    - src/lib/validation/listing.ts (09-06 — the exact publish requirements each mode has, so the checklist
      and the gate cannot disagree; 07-15's "a new publish requirement must land in TWO places" rule)
    - src/app/(host)/host/listings/[id]/availability/page.tsx
    - .planning/phases/09-open-capacity-bookings/09-UI-SPEC.md § 1c, § 1d, § 1e, § 1g
  </read_first>
  <action>
**(a) Mode-forked pricing step (OC-08).**
- `exclusive` → **unchanged**: `Hourly rate`, `Day rate`, and the optional `Group pricing` block
  (`extraHeadFee` / `included`).
- `open_capacity` → renders ONLY:
  - **`Price per person`** bound to `perHeadPriceCents`, cloning the currency-prefixed input at `:661-693`
    byte-for-byte (same `{symbol}` span, same `pl-8`, same `/100` ↔ `Math.round(major * 100)` conversion).
    `FormDescription`: `What one person pays for a day pass. The same price applies whether they stay one hour or all day.`
  - **`People per day`** bound to the SAME RHF field name as the details step's `Max occupancy`
    (`maxOccupancy`) — two views of ONE value, never a second piece of state.
    `FormDescription`: `The most people you'll let in on one day. This is your drop-in cap.`
  `Hourly rate`, `Day rate` and the whole `Group pricing` block are **not rendered** in this mode.
  Comment the deliberate duplication: D-124 puts the cap on `maxOccupancy`, which the host met in step 1 as
  "most people allowed at once"; in drop-in mode that number silently becomes *passes sold per day*, a
  materially different promise, so it is rendered again under the label it actually performs. Same `name` ⇒
  same value ⇒ no drift.
- Also make the DETAILS step's `Max occupancy` helper mode-aware: in open mode it reads
  `The most people you'll let in on one day — your drop-in cap.`

**(b) Hidden booking-mode step (OC-10), but never silent.** In `open_capacity`, remove the `booking` entry
from the step list the wizard actually walks, so `Step X of Y` and the progress bar stay truthful (9 → 8).
Derive the walked list from `STEPS`, e.g.
`const steps = mode === "open_capacity" ? STEPS.filter(s => s.key !== "booking") : STEPS;` and use it for the
title, the count, the progress and every `stepIndex` lookup. Then add ONE muted line to the open-mode review
summary — mandatory, this is the only place the removal is explained:
`Drop-in passes are always instant — people book without waiting for your approval.`
Also render the open-mode review summary line:
`Drop-in passes · {₱perHead} per person · up to {N} people a day · Instant book`.

**(c) Mode-forked publish checklist (UI-SPEC § 1e).**
- `exclusive` (unchanged): Title · Description · Space type · Address · Capacity · Hourly rate · Day rate ·
  3+ photos · Cancellation policy
- `open_capacity`: Title · Description · Space type · Address · **Drop-in cap** · **Price per person** ·
  3+ photos · Cancellation policy
Each row's `step` uses `stepIndex(...)` against the WALKED list. No new blocked affordance — the shipped
`Almost there — finish these to publish:` panel handles it. State in a comment that this checklist and
`publishSchema`'s mode fork are the two halves of 07-15's rule and must be changed together.

**(d) Host availability page (§ 1g).** `/host/listings/[id]/availability` is otherwise **unchanged** for
open listings. Add ONE muted line at the top, open mode only:
`These are the hours your drop-in passes are good for. A pass covers the whole day you're open.`

**(e) `tests/listing/wizard-occupancy.test.tsx`** (jsdom):
- open mode: the pricing step renders `Price per person` and `People per day` and does NOT render
  `Hourly rate`, `Day rate` or the group-pricing block
- exclusive mode: the pricing step renders `Hourly rate` and `Day rate` and NOT `Price per person`
- open mode: the walked step count is 8 and `booking` is absent; exclusive is 9
- open mode review renders the instant-only explanation line verbatim
- open checklist rows are `Drop-in cap` / `Price per person`; exclusive rows are `Hourly rate` / `Day rate`
- locked: the non-selected card is `aria-disabled="true"`, the word `Locked` is present, and the alert's
  title and the `View your bookings` link both render
- unlocked: neither `Locked` nor the alert renders
  </action>
  <acceptance_criteria>
    - `grep -c "Price per person" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 2 (field label + checklist row)
    - `grep -c "People per day" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 1
    - `grep -c "Drop-in passes are always instant" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints `1`
    - `grep -c "perHeadPriceCents" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints ≥ 2
    - `grep -c 'STEPS.length' "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` prints `0` — the count comes from the WALKED list, not the constant, or the open flow would advertise 9 steps and show 8
    - `grep -c "These are the hours your drop-in passes are good for." "src/app/(host)/host/listings/[id]/availability/page.tsx"` prints `1`
    - `npx vitest run tests/listing/wizard-occupancy.test.tsx` exits 0 with ≥ 7 tests passing
    - `npx vitest run tests/listing && npx tsc --noEmit && npm run lint` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/listing && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>The pricing step, step count, review summary and publish checklist all fork on the chosen mode; the removed booking-mode step is explained on review; the host availability page carries its one note; seven jsdom cases are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard form state → `saveListingStep` / `publishListing` | every control here is a courtesy; the server re-validates the persisted row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Elevation of Privilege / Tampering | the locked mode control | mitigate | The disabled card is presentation only; `saveListingStep` re-reads the persisted mode and `getModeLockState` before accepting a change (09-06). Both halves are commented so neither is later removed as "redundant". |
| T-09-35 | Tampering | client-side publish eligibility | mitigate | Unchanged posture: the checklist is a courtesy and `publishListing` re-parses the PERSISTED row through the mode-forked `publishSchema`. This plan changes only which rows are DISPLAYED. |
| T-09-36 | Repudiation | a host mis-selling their space through a misread control | mitigate | Its own step, two equal-weight explanatory cards, no pre-selection on a new listing, plain-language copy with worked examples, and the cap rendered a second time under the label it actually performs in this mode. |
| T-09-37 | Information Disclosure | the lock alert naming other bookings | accept | The host owns those bookings; only a count and the last end instant are shown, never a guest identity. |
</threat_model>

<verification>
- `npx vitest run tests/listing` → green including 7 new wizard cases
- `npx tsc --noEmit` → 0; `npm run lint` → 0 errors; `npm run build` → 0
- No hardcoded step index anywhere in `wizard.tsx`; the walked-step count drives the "Step X of Y" line
</verification>

<success_criteria>
- The occupancy step ships with the exact UI-SPEC copy, neutral selection, and no pre-selection on a new listing
- Drop-in mode shows only per-person price + people-per-day, removes the booking-mode step, and explains it on review
- The publish checklist matches what each mode's publish gate actually requires
- The OC-17 lock states why, when it lifts, and how to lift it sooner
</success_criteria>

<output>
After completion, create `.planning/phases/09-open-capacity-bookings/09-10-SUMMARY.md`
</output>
