---
phase: 10-design-system-foundation-theme-runtime
plan: 12
type: execute
wave: 9
depends_on: ["10-10"]
files_modified:
  - src/app/(host)/host/bookings/page.tsx
  - src/components/booking/bookings-tabs.tsx
  - src/components/search/search-bar.tsx
  - src/components/ui/tabs.tsx
  - src/components/search/search-result-card.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/select.tsx
  - tests/design/elevation-z.test.ts
autonomous: true
requirements: [DS-03]

must_haves:
  truths:
    - "D-01: elevation really travels between themes because no surface pins a shadow to a literal any more"
    - "DS-03 (part 1): every shadow in the app maps to one of exactly three named steps — 9 named sites, 5 untouched `shadow-none`, and zero default `shadow-*` utilities"
    - "Five distinct shipped values collapse into three, so some surfaces visibly gain or lose depth — that is the point of a three-step scale, not a regression to report"
    - "`shadow-sticky` stays declared-but-unused rather than being given an invented home"
  artifacts:
    - path: "tests/design/elevation-z.test.ts"
      provides: "The shadow half of the DS-03 source-scan gate, with a floor count so a delete-instead-of-rename goes red"
  key_links:
    - from: "src/components/search/search-result-card.tsx"
      to: "src/app/globals.css --elevation-overlay"
      via: "shadow-overlay named step replacing the frozen shadow-md literal"
      pattern: "shadow-overlay"
    - from: "src/components/ui/tabs.tsx"
      to: "src/app/globals.css --elevation-raised"
      via: "shadow-raised named step replacing the frozen shadow-sm literal"
      pattern: "shadow-raised"
---

<objective>
Collapse the 14 shadow call sites onto the three named elevation steps.

Purpose: Tailwind's default `shadow-*` utilities compile to literal values, not `var()` references, so a
surface carrying `shadow-md` is frozen no matter what a theme declares. The grove proof is only true if
every elevated surface reads from `--elevation-*`.

Output: 9 named-step edits across 8 files, 5 `shadow-none` sites deliberately untouched, and the shadow
half of the DS-03 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 4 raised surfaces</name>
  <files>src/app/(host)/host/bookings/page.tsx, src/components/booking/bookings-tabs.tsx, src/components/search/search-bar.tsx, src/components/ui/tabs.tsx</files>
  <read_first>
    - `src/app/(host)/host/bookings/page.tsx:173` (`shadow-xs`), `src/components/booking/bookings-tabs.tsx:39`
      (`shadow-sm`), `src/components/search/search-bar.tsx:194` (`shadow-sm`) and
      `src/components/ui/tabs.tsx:66` (`shadow-sm` AND `shadow-none` on the same line) — read each site so
      the surface's role (card, tab trigger, search bar) is known rather than guessed.
    - `src/app/globals.css` — the three `--shadow-*` named steps and their per-theme `--elevation-*` values
      written in plan 10-04.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Elevation — the table
      naming which of today's values each step collapses.
  </read_first>
  <action>
    The collapse direction is the one mechanical decision CONTEXT deliberately left to planning, and it is
    settled here: `shadow-xs` (1 site) and `shadow-sm` (3 sites) become `shadow-raised`. Five distinct
    values collapse into three, so some surfaces visibly gain or lose depth — that is the point of a
    three-step scale, not a regression to report.

    At `src/components/ui/tabs.tsx:66` the same line carries both `shadow-sm` and `shadow-none`. Change
    only the `shadow-sm`; the `shadow-none` stays exactly as it is and is one of the 5 that must survive.

    Do not add a fourth step, do not reintroduce a default `shadow-*` utility anywhere, and do not touch
    `dark:`-prefixed utilities in the vendored files — THEME-05 pins the vendored `dark:` count at 56.
  </action>
  <verify>
    <automated>test "$(grep -rho 'shadow-raised' src/app src/components | wc -l)" = 4 && test -z "$(grep -rlE 'shadow-(xs|sm)\b' src/app src/components)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rho "shadow-raised" src/app src/components | wc -l` returns 4.
    - `grep -rEc "shadow-(xs|sm)\b" src/app src/components | grep -v ":0$"` produces no output.
    - `grep -c "shadow-none" src/components/ui/tabs.tsx` returns 1 (the co-located none survived).
    - `grep -rc "dark:" src/components/ui/ | awk -F: '{s+=$2} END {print s}'` still returns 56.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every low-elevation surface reads from the theme's raised step.</done>
</task>

<task type="auto">
  <name>Task 2: The 5 overlay surfaces</name>
  <files>src/components/search/search-result-card.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/popover.tsx, src/components/ui/select.tsx</files>
  <read_first>
    - `src/components/search/search-result-card.tsx:171` (`shadow-md`),
      `src/components/ui/dropdown-menu.tsx:46` (`shadow-md`) and `:247` (`shadow-lg`),
      `src/components/ui/popover.tsx:33` (`shadow-md`) and `src/components/ui/select.tsx:72` (`shadow-md`)
      — read each site so the surface's role (card, menu content, sub-menu, popover, select content) is
      known.
    - `src/components/ui/command.tsx:74` and `src/components/ui/input-group.tsx:69,125,141` — the 4
      `shadow-none` sites that are NOT touched, so the exemption is seen rather than inferred.
    - `src/app/globals.css` — the `--shadow-overlay` and `--shadow-sticky` steps written in plan 10-04.
  </read_first>
  <action>
    `shadow-md` (4 sites) and `shadow-lg` (1 site) become `shadow-overlay`. `shadow-none` (5 sites total,
    4 of them in `command.tsx` and `input-group.tsx`) stays `shadow-none` and is not edited.

    `shadow-sticky` is reserved for bottom-anchored sticky bars. Apply it only where a surface is already a
    sticky bottom bar; do not invent a new elevated surface to give it a home. No such surface exists in
    the shipped tree today, so `shadow-sticky` stays declared-but-unused in this phase and is exercised by
    `/dev/theme`'s elevation ladder in plan 10-16. Record that in the SUMMARY so it is not later read as an
    unused-token bug.

    Do not add a fourth step and do not touch `dark:`-prefixed utilities in the vendored files.
  </action>
  <verify>
    <automated>test "$(grep -rho 'shadow-overlay' src/app src/components | wc -l)" = 5 && test -z "$(grep -rlE 'shadow-(md|lg|xl|2xl)\b' src/app src/components)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rho "shadow-overlay" src/app src/components | wc -l` returns 5.
    - `grep -rEc "shadow-(md|lg|xl|2xl)\b" src/app src/components | grep -v ":0$"` produces no output.
    - `grep -rho "shadow-none" src/app src/components | wc -l` returns 5.
    - `grep -rho "shadow-sticky" src/app src/components | wc -l` returns 0 (declared-but-unused is the
      expected state at the end of this plan).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every overlay surface reads from the theme's overlay step, and the reserved sticky step was not given an invented home.</done>
</task>

<task type="auto">
  <name>Task 3: The DS-03 shadow gate</name>
  <files>tests/design/elevation-z.test.ts</files>
  <read_first>
    - `tests/design/elevation-z.test.ts` as written in plan 10-04 — this task extends it; keep the
      token-side assertions intact.
    - `tests/design/focus-recipe.test.ts` as written in plan 10-07 — the walker and the Windows
      path-normalisation idiom to reuse.
    - The 8 files edited in Tasks 1 and 2, to confirm the replacements landed before pinning them.
  </read_first>
  <action>
    Extend `tests/design/elevation-z.test.ts` with a shadow source-scan section reusing the walker and the
    `relative(process.cwd(), file).split("\\").join("/")` normalisation. Assert: zero occurrences of
    `shadow-xs`, `shadow-sm`, `shadow-md`, `shadow-lg`, `shadow-xl` or `shadow-2xl` anywhere under
    `src/app/**` or `src/components/**`; and that the only shadow utilities present are `shadow-none`,
    `shadow-raised`, `shadow-overlay` and `shadow-sticky`.

    Add the positive control: assert the combined count of `shadow-raised` plus `shadow-overlay` plus
    `shadow-sticky` is exactly 9, and that `shadow-none` is exactly 5 — so a migration that deleted the
    shadows instead of renaming them goes red, and so does one that swept up the untouched `shadow-none`
    sites. Assert the scan visited more than 200 files and that `src/components/ui/input-group.tsx` is in
    the scanned set, which is the file whose 3 `shadow-none` sites make the 5 meaningful.

    Do not add z-index assertions here; plan 10-13 extends the same file with those.
  </action>
  <verify>
    <automated>npm run test:design -- elevation-z</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- elevation-z` exits 0 with at least 8 passing assertions.
    - Temporarily reinstating `shadow-md` at `src/components/ui/popover.tsx` makes
      `npm run test:design -- elevation-z` exit non-zero; revert and confirm it exits 0 again. Record both
      observations in the test file's header.
    - `grep -c "toBeGreaterThan(200)" tests/design/elevation-z.test.ts` returns 1 or more.
    - `grep -c "input-group" tests/design/elevation-z.test.ts` returns 1 or more (the positive control
      exists).
    - `npm run test:design` exits 0 overall.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Every shadow in the app maps to one of exactly three theme-aware named steps, proven by a scan that reached the untouched sites too.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component class strings → theme contract | A default `shadow-*` compiles to a literal, not a `var()`, and silently freezes a surface against a theme swap |
| vendored `src/components/ui/**` → upstream shadcn | Four of the eight edited files are vendored; edits are a permanent fork surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-45 | Repudiation | DS-03 shadow gate | mitigate | A zero-violations assertion passes just as happily against a scanner that visited nothing, and a migration that simply deleted every shadow would also pass. The gate pins exact counts on both sides — 9 named steps and 5 surviving `shadow-none` — plus a >200-file floor and a named positive-control file, and the executor observes it going red on a reinstated `shadow-md`. |
| T-10-46 | Tampering | vendored shadow edit surface | accept | `dropdown-menu.tsx`, `popover.tsx`, `select.tsx` and `tabs.tsx` join the fork surface D-17 already accepts for `src/components/ui/**`. Recorded so a future `npx shadcn add` knows what it will re-collide with. |
| T-10-47 | Information disclosure | visible depth changes | accept | Five shipped values collapse to three, so some surfaces gain or lose depth. Accepted and recorded as the intended outcome of a three-step scale, so it is not filed as a visual regression during `/gsd:verify-work`. |
</threat_model>

<verification>
- `npm run test:design` fully green.
- `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
- `npm run db:up && npm run db:test:setup && npm test` green.
- `npm run lint` reports no new errors.
</verification>

<success_criteria>
No component in the app pins a shadow to a value a theme cannot reach, and the three-step scale has no
fourth step hiding in it.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-12-SUMMARY.md` when done.
</output>
