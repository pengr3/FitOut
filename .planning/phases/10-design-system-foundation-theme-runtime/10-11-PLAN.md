---
phase: 10-design-system-foundation-theme-runtime
plan: 11
type: execute
wave: 9
depends_on: ["10-10"]
files_modified:
  - src/app/(app)/bookings/[id]/cancel/page.tsx
  - src/app/(app)/bookings/[id]/page.tsx
  - src/app/listings/[id]/book/page.tsx
  - src/app/listings/[id]/page.tsx
  - src/app/page.tsx
  - src/components/booking/refund-breakdown.tsx
  - src/components/group/headcount-meter.tsx
  - src/components/host/payout-summary.tsx
  - src/components/booking/booking-row.tsx
  - src/components/notifications/notification-bell.tsx
  - tests/design/type-scale.test.ts
autonomous: true
requirements: [DS-02]

must_haves:
  truths:
    - "D-01: the type scale really travels between themes because no surface pins a font size to a literal any more"
    - "DS-02: zero arbitrary `text-[NNpx]` values survive under `src/app/**` or `src/components/**`"
    - "The 4 vendored `text-[0.8rem]` sites are deliberately untouched and are proven not to match the px-only pattern"
    - "The 156 co-located `font-semibold` / `tracking-tight` / `leading-tight` call sites need no edits — plan 10-04 made those per-theme aliases precisely so they travel unchanged"
  artifacts:
    - path: "tests/design/type-scale.test.ts"
      provides: "DS-02 source-scan gate with the vendored-rem positive control that proves the px-only pattern is a deliberate exemption"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/globals.css --text-display"
      via: "sm:text-display replacing the arbitrary 28px literal"
      pattern: "sm:text-display"
    - from: "src/components/booking/booking-row.tsx"
      to: "src/app/globals.css built-in text-xs step"
      via: "text-xs replacing the 10px sub-label numeral"
      pattern: "text-xs"
---

<objective>
Migrate the 14 arbitrary `text-[NNpx]` values onto the named type steps.

Purpose: an arbitrary `text-[28px]` is frozen — a theme that redefines the Display step cannot reach it,
so a swap that looks complete in `globals.css` silently leaves 12 page titles and money figures at the old
size. These are the sites that would make the grove proof a lie.

Output: 14 mechanical edits across 10 files, and a source-scan gate with a positive control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 8 route-level Display sites</name>
  <files>src/app/(app)/bookings/[id]/cancel/page.tsx, src/app/(app)/bookings/[id]/page.tsx, src/app/listings/[id]/book/page.tsx, src/app/listings/[id]/page.tsx, src/app/page.tsx</files>
  <read_first>
    - Each of the 5 listed files at its `sm:text-[28px]` occurrence — `cancel/page.tsx:278`,
      `bookings/[id]/page.tsx:267,328,579,584`, `listings/[id]/book/page.tsx:435`,
      `listings/[id]/page.tsx:257`, `page.tsx:122` — so the co-located `font-*` / `tracking-*` /
      `leading-*` utilities are seen rather than assumed. Note the real paths: `listings/[id]/**` sits
      directly under `src/app/`, NOT under `src/app/(app)/`.
    - `src/app/globals.css` — the four named type steps and the per-theme built-in ladder written in plan
      10-04, so the replacement step is chosen from the declared set.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Typography — the
      declared roles table and the warning about the `/` modifier form.
  </read_first>
  <action>
    Replace every `sm:text-[28px]` in these 5 files with `sm:text-display`. All 8 sites carry the `sm:`
    prefix already; keep it. That is the Display role — page titles and money figures — and it is exactly
    what these sites are.

    Where the same element also carries a co-located `font-semibold`, `tracking-tight` or `leading-tight`,
    leave those utilities in place: plan 10-04 made `--font-weight-semibold`, `--tracking-tight` and
    `--leading-tight` per-theme precisely so those 156 call sites need no edits and still travel between
    themes.

    Never write the `/` modifier form (`sm:text-display/tight`) — it compiles to font-size and line-height
    only, and letter-spacing and font-weight vanish.

    Change nothing else in these files.
  </action>
  <verify>
    <automated>test -z "$(grep -rlE 'text-\[[0-9.]+px\]' src/app)" && test "$(grep -rho 'sm:text-display' src/app | wc -l)" = 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEc "text-\[[0-9.]+px\]" src/app | grep -v ":0$"` produces no output.
    - `grep -rho "sm:text-display" src/app | wc -l` returns 8.
    - `grep -rEc "text-(display|heading|body|label)/" src/app | grep -v ":0$"` produces no output (the
      forbidden `/` modifier form was not used).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>No route pins a page title or a money figure to a pixel literal.</done>
</task>

<task type="auto">
  <name>Task 2: The 4 component Display sites and the 2 sub-label numerals</name>
  <files>src/components/booking/refund-breakdown.tsx, src/components/group/headcount-meter.tsx, src/components/host/payout-summary.tsx, src/components/booking/booking-row.tsx, src/components/notifications/notification-bell.tsx</files>
  <read_first>
    - `src/components/booking/refund-breakdown.tsx:100`, `src/components/group/headcount-meter.tsx:63` and
      `src/components/host/payout-summary.tsx:27,35` at their `sm:text-[28px]` occurrences. Note the real
      paths: `headcount-meter.tsx` lives under `src/components/group/`, NOT under
      `src/components/availability/`.
    - `src/components/booking/booking-row.tsx:69` (`text-[10px]`) and
      `src/components/notifications/notification-bell.tsx:117` (`text-[11px]`) — both sub-label numerals.
    - `src/components/ui/button.tsx:27`, `src/components/ui/calendar.tsx:93,102` and
      `src/components/ui/toggle.tsx:20` — the 4 vendored `text-[0.8rem]` sites that are NOT touched, so the
      exemption is seen rather than inferred.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-UI-SPEC.md` § Typography — the note
      that `text-xs` is the sub-label utility step for numerals and counters and is not a fifth semantic
      role.
  </read_first>
  <action>
    Replace the 4 `sm:text-[28px]` occurrences in `refund-breakdown.tsx`, `headcount-meter.tsx` and
    `payout-summary.tsx` with `sm:text-display`, keeping the `sm:` prefix and every co-located
    `font-*` / `tracking-*` / `leading-*` utility, exactly as in Task 1.

    Replace `booking-row.tsx:69`'s `text-[10px]` and `notification-bell.tsx:117`'s `text-[11px]` with
    `text-xs`. Both are sub-label numerals and counters, which is what the `text-xs` utility step is for;
    do not introduce a fifth semantic role for them.

    Do NOT touch the 4 vendored `text-[0.8rem]` sites at `src/components/ui/button.tsx:27`,
    `src/components/ui/calendar.tsx:93`, `src/components/ui/calendar.tsx:102` and
    `src/components/ui/toggle.tsx:20`. The leak pattern is px-only by decision (UI-SPEC Resolved Q2) and
    those four are recorded, tolerated vendored debt.

    At `booking-row.tsx` keep everything else on the file untouched: `:113` carries a `variant="brand"`
    from plan 10-08 and a `z-10` that plan 10-13 remaps.
  </action>
  <verify>
    <automated>test -z "$(grep -rlE 'text-\[[0-9.]+px\]' src/components)" && test "$(grep -rho 'sm:text-display' src/components | wc -l)" = 4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEc "text-\[[0-9.]+px\]" src/components | grep -v ":0$"` produces no output.
    - `grep -rho "sm:text-display" src/components | wc -l` returns 4.
    - `grep -rc "text-\[0.8rem\]" src/components/ui | awk -F: '{s+=$2} END {print s}'` returns 4 (the
      vendored exemption is intact).
    - `grep -c "z-10" src/components/booking/booking-row.tsx` returns 1 and
      `grep -c 'variant="brand"' src/components/booking/booking-row.tsx` returns 1 (neighbouring plans'
      edits survived).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every component numeral and money figure reads from a named step, and the vendored rem sites are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The DS-02 source-scan gate</name>
  <files>tests/design/type-scale.test.ts</files>
  <read_first>
    - `tests/design/type-scale.test.ts` as written in plan 10-04 — this task extends it; keep the token-side
      assertions intact.
    - `tests/design/focus-recipe.test.ts` as written in plan 10-07 — the walker and the Windows
      path-normalisation idiom to reuse.
    - The 10 files edited in Tasks 1 and 2, to confirm the replacements landed before pinning them.
  </read_first>
  <action>
    Extend `tests/design/type-scale.test.ts` with a source-scan section, reusing the walker and the
    `relative(process.cwd(), file).split("\\").join("/")` normalisation. Assert: zero occurrences of a
    `text-[NNpx]` arbitrary value anywhere under `src/app/**` or `src/components/**`, and that the total
    `sm:text-display` count is exactly 12.

    Then add the positive control that makes the zero meaningful: assert the same scan finds exactly 4
    occurrences of `text-[0.8rem]`, and that they sit in `src/components/ui/button.tsx`,
    `src/components/ui/calendar.tsx` and `src/components/ui/toggle.tsx` — this proves the walker really
    reaches vendored files and that the px-only pattern is deliberately not matching them, rather than the
    scan silently finding nothing.

    Also assert zero occurrences of the forbidden `/` modifier form
    (`text-display/`, `text-heading/`, `text-body/`, `text-label/`) under the gate tree, and that the scan
    visited more than 200 files.
  </action>
  <verify>
    <automated>npm run test:design -- type-scale</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design -- type-scale` exits 0 with at least 9 passing assertions.
    - Temporarily reinstating `sm:text-[28px]` at `src/app/page.tsx` makes
      `npm run test:design -- type-scale` exit non-zero; revert and confirm it exits 0 again. Record both
      observations in the test file's header.
    - `grep -c "0.8rem" tests/design/type-scale.test.ts` returns 1 or more (the positive control exists).
    - `grep -c "toBeGreaterThan(200)" tests/design/type-scale.test.ts` returns 1 or more.
    - `npm run test:design` exits 0 overall.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>A scan that provably reaches the vendored tree reports zero arbitrary pixel font sizes under the gate tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component class strings → theme contract | A literal here is invisible to a theme swap and silently freezes a surface |
| static gate → reviewer trust | A zero-violations assertion is indistinguishable from a scanner that visited nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-42 | Repudiation | DS-02 source gate | mitigate | A zero-violations assertion passes just as happily against a scanner that visited nothing. The gate carries a positive control — the 4 vendored `text-[0.8rem]` sites must still be found in three named files — plus a >200-file floor, and the executor observes it going red on a reinstated `sm:text-[28px]` before closing the task. |
| T-10-43 | Tampering | the `/` modifier form | mitigate | `sm:text-display/tight` compiles to font-size and line-height only, silently dropping the per-theme letter-spacing and weight that make D-01 true. Both the action and the gate forbid the form explicitly, and the gate asserts zero occurrences across all four named steps. |
| T-10-44 | Denial of service | shared files with neighbouring plans | accept | `booking-row.tsx` carries edits from plans 10-08 (`variant="brand"`) and 10-13 (`z-10`). Accepted: the three plans are in different waves, and this plan's acceptance criteria re-assert both neighbouring edits survived. |
</threat_model>

<verification>
- `npm run test:design` fully green.
- `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
- `npm run db:up && npm run db:test:setup && npm test` green.
- `npm run lint` reports no new errors.
</verification>

<success_criteria>
No surface pins a font size to a pixel literal, and the vendored rem sites are proven to be a deliberate
exemption rather than a miss.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-11-SUMMARY.md` when done.
</output>
