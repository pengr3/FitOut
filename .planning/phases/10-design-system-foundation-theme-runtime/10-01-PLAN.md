---
phase: 10-design-system-foundation-theme-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - vitest.design.config.ts
  - tests/design/harness.test.ts
autonomous: false
requirements: [DS-06, DS-13]

must_haves:
  truths:
    - "D-12: culori is installed as a devDependency and is the colour authority — a hand-rolled OKLab solver is rejected"
    - "D-16: one design gate exists that runs with no Docker and no Postgres, so both the ESLint and Vitest consumers can be wired to it later"
    - "`npm run test:design` completes in under ~5s on a machine with no database running"
    - "A `.tsx` design test is collected by the design config only, never by the DB-backed main config"
  artifacts:
    - path: "vitest.design.config.ts"
      provides: "DB-free Vitest config for the design gate"
      contains: "tests/design"
    - path: "tests/design/harness.test.ts"
      provides: "Guard-the-harness smoke test proving culori resolves and the DB-free config runs"
    - path: "package.json"
      provides: "test:design script"
      contains: "test:design"
  key_links:
    - from: "package.json"
      to: "vitest.design.config.ts"
      via: "test:design npm script"
      pattern: "vitest run --config vitest.design.config.ts"
    - from: "vitest.config.ts"
      to: "tests/design/**"
      via: "exclude entry preventing double collection"
      pattern: "tests/design"
---

<objective>
Stand up the DB-free design gate that every other plan in this phase asserts through, and install the
one new dependency the colour derivation needs.

Purpose: `vitest.config.ts:59` runs `globalSetup: tests/global-setup.ts`, which hard-fails when the
`fitout_test` Postgres database is unreachable — for every `vitest run`, including a single-file run. A
build-blocking design gate that needs Docker is not a gate anyone keeps. Nothing else in this phase can be
proven until a second, DB-free config exists.

Output: `culori` + `@types/culori` installed, `vitest.design.config.ts`, the `test:design` npm script, the
`.tsx` collection trap closed, and one harness test that proves all of it runs green with no database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-design-system-foundation-theme-runtime/10-VALIDATION.md
@.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for culori and @types/culori</name>
  <files>package.json</files>
  <read_first>
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-RESEARCH.md` § Package Legitimacy
      Audit — the one-row table covering `culori`, and its closing note that `@types/culori` must be
      checked separately because culori 4.x may or may not ship its own types.
    - `package.json` `devDependencies` — confirm neither package is already present.
  </read_first>
  <action>
    Run NO package-manager install in this task. Present the two packages and their audit status to the
    developer, wait for an explicit approval signal, and record the verdict for each package by name in
    the plan SUMMARY. `culori@4.0.2` carries a slopcheck `[OK]` verdict and is Approved;
    `@types/culori@4.0.1` is `[ASSUMED]` because it is absent from the audit table. A package legitimacy
    checkpoint is never auto-approvable, regardless of `workflow.auto_advance`. Task 2 performs the
    install only after this gate resolves.
  </action>
  <what-built>
    Nothing yet — this gate runs BEFORE any package-manager install, per the phase's package legitimacy
    protocol. Two packages are about to enter the tree:

    - `culori@4.0.2` — **covered** by `10-RESEARCH.md` § Package Legitimacy Audit: npm, published
      2026-04-03, 1.59M weekly downloads, source repo `github.com/Evercoder/culori`, MIT, no postinstall
      script, `slopcheck install culori` returned `[OK]` (1 scanned, 1 OK). Disposition: **Approved**.
      It is named explicitly by D-12, a locked user decision.
    - `@types/culori@4.0.1` — **NOT covered** by that audit table. It is required because
      `npm view culori types` returns empty (culori 4.x ships no bundled types) and the design tests are
      TypeScript that must pass `tsc --noEmit`. Treated as **[ASSUMED]** until a human confirms it.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/culori — confirm the repository link is
       `github.com/Evercoder/culori`, the licence is MIT, and weekly downloads are ~1.5M+.
    2. Open https://www.npmjs.com/package/@types/culori — confirm the publisher is `types`
       (the DefinitelyTyped bot account) and the repository is
       `github.com/DefinitelyTyped/DefinitelyTyped`, with the latest version `4.0.1` tracking culori 4.x.
    3. Confirm neither package page shows a postinstall/install script warning.
  </how-to-verify>
  <acceptance_criteria>
    - The developer has replied with an explicit approval, or has named a replacement for the [ASSUMED]
      package.
    - `node -e "const d=require('./package.json').devDependencies||{};process.exit(d.culori?1:0)"` exits 0
      at the moment this task runs — nothing was installed before the gate resolved.
    - The SUMMARY records the verdict for each package by name.
  </acceptance_criteria>
  <done>The developer has explicitly approved both packages, or named a replacement for the [ASSUMED] one.</done>
  <resume-signal>Type "approved" to allow both installs, or name the package to drop and the alternative to use.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install culori and create the DB-free design gate</name>
  <files>package.json, vitest.config.ts, vitest.design.config.ts, tests/design/harness.test.ts</files>
  <read_first>
    - `package.json` — the current `scripts` block (`:5-23`); note `"build": "next build"` and the
      `namespace:verb` naming convention (`db:*`, `ops:*`, `test:*`), and that `.mjs` scripts run under
      plain `node` while `.ts` scripts run under `tsx`.
    - `vitest.config.ts` — the whole file. Specifically: the `defineConfig` + `plugins: [react()]` +
      `resolve.alias` `"@" -> resolve(__dirname, "./src")` shape at `:1-34`, the 23-line header-comment
      convention at `:5-27`, the `setupFiles` / `globalSetup` pair at `:57-59` (the two lines that must
      NOT be copied), and the `include` / `exclude` shape at `:60-62`.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-PATTERNS.md` § 3 — the analog and the
      documented `.tsx` config trap.
    - `.planning/phases/10-design-system-foundation-theme-runtime/10-VALIDATION.md` § Test Infrastructure.
  </read_first>
  <action>
    Run `npm install --save-dev culori @types/culori`.

    Create `vitest.design.config.ts`. Copy from `vitest.config.ts`: the `defineConfig` import from
    `vitest/config`, `react` from `@vitejs/plugin-react` (required because
    `tests/design/theme-provider.test.tsx` in plan 10-05 is a `.tsx` render test), `resolve` from
    `node:path`, `plugins: [react()]`, and `resolve.alias` mapping `"@"` to `resolve(__dirname, "./src")`.
    Set `test.environment` to `"node"`, `test.include` to `["tests/design/**/*.test.ts", "tests/design/**/*.test.tsx"]`,
    and `test.exclude` to `["node_modules/**", ".next/**", "e2e/**"]`. Declare NO `setupFiles` and NO
    `globalSetup` — those two keys are exactly what makes the existing config require Docker. Individual
    `.tsx` tests opt into a DOM with the repo's existing per-file `// @vitest-environment jsdom` pragma
    (see `tests/booking/partial-grant-notice.test.tsx:1`), so the config default stays `"node"`.
    Carry a header comment in the same style as `vitest.config.ts:5-27` stating: why a second config exists
    (`vitest.config.ts:59`'s `globalSetup` hard-fails without the `fitout_test` Postgres database, and the
    design gate must run inside `next build` on a machine with no database), that it has no `globalSetup`
    and no `setupFiles` by design, and that adding either would silently reintroduce the Docker dependency.

    Close the `.tsx` collection trap: add `"tests/design/**"` to the existing `exclude` array in
    `vitest.config.ts:62`, with a one-line comment saying the design suite is owned by
    `vitest.design.config.ts` and must never pay the Postgres preflight. Without this, the main config's
    `include: ["tests/**/*.test.tsx"]` would also collect the design `.tsx` tests.

    Add `"test:design": "vitest run --config vitest.design.config.ts"` to `package.json` scripts,
    immediately after the existing `"test"` entry. Do NOT add `--passWithNoTests` — a gate that passes
    when its include glob is broken is the vacuous-pass failure mode this phase exists to remove.
    Do NOT touch the `build` script in this plan: `"build"` stays `"next build"` until plan 10-12. Turning
    the gate on before the tree is clean would red-flag the repo's own in-progress state.

    Create `tests/design/harness.test.ts` as the guard-the-harness smoke test: assert that `culori`
    exports `wcagContrast`, `inGamut`, `formatHex` and `converter` as functions, and that
    `wcagContrast("#ffffff", "#000000")` rounds to 21. Carry a header comment naming this file as the
    proof that the DB-free config runs and that the colour authority (D-12) resolves — if this file goes
    red, no other design test's result means anything.
  </action>
  <verify>
    <automated>npm run test:design</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:design` exits 0, reports `tests/design/harness.test.ts` with 2 passing tests, and
      completes with Docker/Postgres NOT running.
    - `node -e "const s=require('./package.json').scripts; process.exit(s['test:design']==='vitest run --config vitest.design.config.ts' && s['build']==='next build' ? 0 : 1)"` exits 0.
    - `grep -c "globalSetup\|setupFiles" vitest.design.config.ts` returns 0.
    - `grep -c "tests/design" vitest.config.ts` returns 1 or more, and that occurrence is inside the
      `exclude` array.
    - `node -e "const d=require('./package.json').devDependencies; process.exit(d.culori && d['@types/culori'] ? 0 : 1)"` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`npm run test:design` is a real, green, sub-5-second, database-free command, and the design suite is collected by exactly one config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Third-party code enters the dependency tree |
| npm script → developer shell | `test:design` runs arbitrary local test code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | `npm install culori @types/culori` | mitigate | `culori` carries a slopcheck `[OK]` verdict in `10-RESEARCH.md` § Package Legitimacy Audit; `@types/culori` is not in that table and is gated by the blocking human checkpoint in Task 1. Both are `--save-dev` only, so neither reaches the client bundle. |
| T-10-06 | Repudiation | `test:design` script | mitigate | No `--passWithNoTests`: a broken include glob fails loudly rather than reporting green. `tests/design/harness.test.ts` is the positive control that the suite really ran. |
| T-10-07 | Denial of service | design gate in `build` | accept | `npm run lint` measures ~85s on this box and will be added to `build` in plan 10-12; the design gate itself is sub-second. Accepted: the build is a local/manual step, there is no CI to slow down. |
</threat_model>

<verification>
- `npm run test:design` green with the database stopped.
- `npx tsc --noEmit` exit 0.
- `npm run build` still exits 0 and still resolves to `next build` (unchanged by this plan).
- `git diff --stat` shows changes confined to `package.json`, `package-lock.json`, `vitest.config.ts`,
  and the two new files.
</verification>

<success_criteria>
A design test can be written and run in under five seconds on a machine with no Postgres, and the two
Vitest configs have disjoint ownership of the test tree.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation-theme-runtime/10-01-SUMMARY.md` when done.
</output>
