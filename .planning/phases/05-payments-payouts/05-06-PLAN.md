---
phase: 05-payments-payouts
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/ui/table.tsx
  - src/components/host/payout-ledger-status.ts
  - src/components/host/payout-state-badge.tsx
  - src/components/host/payout-row.tsx
  - src/components/host/payout-summary.tsx
  - src/app/(host)/host/earnings/page.tsx
  - src/app/(host)/host/page.tsx
  - src/app/(host)/host/layout.tsx
  - tests/payments/earnings-view.test.ts
autonomous: true
requirements: [HOST-03]
must_haves:
  truths:
    - "A host can open /host/earnings and see per-booking payout rows with a Held/Processing/Paid/Refunded badge, the gross → −10% → net breakdown, and the expected/paid date"
    - "The page shows summary totals (Upcoming payouts = Held+Processing, Paid out = Paid)"
    - "The host SEES the commission line (unlike the booker); the payout is a fixed, explainable gross − 10% (D-52/59)"
    - "Every ledger row is owner-scoped to the signed-in host (a host never sees another host's rows)"
    - "Payout states are calm: Held/Processing/Refunded are neutral, Paid is --success, never red"
  artifacts:
    - path: "src/app/(host)/host/earnings/page.tsx"
      provides: "Owner-gated HOST-03 earnings RSC"
      contains: "host_payout_ledger"
    - path: "src/components/host/payout-ledger-status.ts"
      provides: "Pure (non-client) ledger-state → label/badge derivation"
      exports: ["derivePayoutLedgerView"]
    - path: "src/components/host/payout-row.tsx"
      provides: "Per-booking row with the gross/−fee/net breakdown + badge"
  key_links:
    - from: "src/app/(host)/host/earnings/page.tsx"
      to: "host_payout_ledger (owner-scoped by host_id)"
      via: "server query filtered to session.user.id"
      pattern: "hostPayoutLedger"
    - from: "src/app/(host)/host/page.tsx"
      to: "/host/earnings"
      via: "Earnings nav link"
      pattern: "/host/earnings"
---

<objective>
Build the HOST-03 host earnings/payouts page (D-59): an owner-gated `(host)` RSC at `/host/earnings` reading `host_payout_ledger` and rendering per-booking rows — a `Held → Processing → Paid` (plus `Refunded`) state badge, the `gross booking → −10% commission → net payout` breakdown (the host DOES see the commission line, unlike the booker), and the expected/paid date — plus summary totals (Upcoming vs Paid out). Add the neutral `Earnings` nav entry.

Purpose: HOST-03 payout-status view (D-59), making the host-side commission deduction explainable. Wave 2 — depends only on Plan 01 (the ledger schema). No file overlap with Plans 03/04/05.
Output: shadcn `table`, the payout components + pure state derivation, the earnings page + nav links, and an owner-gate/rows/summary test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-payouts/05-UI-SPEC.md
@.planning/phases/05-payments-payouts/05-PATTERNS.md
@.planning/phases/05-payments-payouts/05-CONTEXT.md

<interfaces>
<!-- From Plan 01 (src/lib/db/schema.ts). -->
```typescript
// host_payout_ledger: id, bookingId, hostId, paymentId, grossCents, commissionRateBps, commissionCents,
//   netCents, currency, state payout_ledger_state ('held'|'processing'|'paid'|'refunded'|'failed'), transferId, paidAt, createdAt
```

<!-- Analog: the pure NON-client status-derivation idiom (src/components/host/payout-status.ts). Mirror this shape (must NOT be "use client" so the RSC can call it). -->
```typescript
export type PayoutStatus = "not_started" | "incomplete" | "enabled" | "paused";
export function derivePayoutStatus(row): PayoutStatus; // pure, server-callable
```

<!-- Analog: (host)/host/listings/page.tsx RSC gate (lines 23-39): getSession → !user redirect /login → !canHost redirect / → query filtered to session.user.id. Owner-scope the ledger read the SAME way (host_id === session.user.id — the route group is NOT the gate). -->
<!-- Analog: bookings/[id]/page.tsx money row (145-150): formatMoney(cents, currency); the confirmed --success Badge (111-114) is the treatment the Paid badge mirrors. -->
<!-- Money: formatMoney(cents, currency) + DISPLAY_CURRENCY from @/lib/money. Dates: date-fns + @date-fns/tz (venue-tz-safe, epoch discipline like the confirmation page). -->
<!-- Nav analog: (host)/host/page.tsx action row (63-71) has "Your listings" (variant="outline"); add "Earnings" the same way. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payout state derivation + badge + row + summary components (+ shadcn table)</name>
  <files>src/components/ui/table.tsx, src/components/host/payout-ledger-status.ts, src/components/host/payout-state-badge.tsx, src/components/host/payout-row.tsx, src/components/host/payout-summary.tsx</files>
  <read_first>
    - src/components/host/payout-status.ts (the pure, NON-client derivation idiom to mirror — never "use client")
    - src/app/bookings/[id]/page.tsx lines 105-152 (the --success Badge + money-row treatment)
    - src/lib/money.ts (formatMoney, DISPLAY_CURRENCY)
    - .planning/phases/05-payments-payouts/05-UI-SPEC.md § Payout state badge recipes + § Copywriting Contract (Host earnings copy) + § Component Inventory (PayoutRow/PayoutSummary/PayoutStateBadge)
  </read_first>
  <action>
    1. Add the shadcn official `table` block: `npx shadcn@latest add table` (official registry — no vetting gate). This creates `src/components/ui/table.tsx`.

    2. Create `src/components/host/payout-ledger-status.ts` — a PURE, NON-"use client" module (so the RSC can call it), distinct from the onboarding `derivePayoutStatus`:
       ```typescript
       import type { LucideIcon } from "lucide-react";
       export type PayoutLedgerState = "held" | "processing" | "paid" | "refunded" | "failed";
       export type PayoutLedgerView = { label: string; tone: "muted" | "outline" | "success" | "attention"; helper?: string; datePrefix: "Expected" | "Paid" | "Refunded" };
       export function derivePayoutLedgerView(state: PayoutLedgerState): PayoutLedgerView;
       ```
       Map per the UI-SPEC recipe: held → { label:"Held", tone:"muted", helper:"Held until after the session", datePrefix:"Expected" }; processing → { label:"Processing", tone:"outline", datePrefix:"Expected" }; paid → { label:"Paid", tone:"success", datePrefix:"Paid" }; refunded → { label:"Refunded", tone:"muted", helper:"This booking was refunded — no payout.", datePrefix:"Refunded" }; failed → { label:"Needs attention", tone:"attention", datePrefix:"Expected" }.

    3. Create `src/components/host/payout-state-badge.tsx` — maps a `PayoutLedgerView.tone` + label to the badge recipe with a lucide icon, NEVER color-only (icon + text). Paid → `bg-success text-success-foreground` + `CheckCircle2`; held → `secondary` (muted) + `Clock`; processing → `outline` (bordered) + `ArrowLeftRight`; refunded → muted `secondary` + `Undo2`; failed → reuse the destructive `Alert` pattern (icon + text), NOT a badge. Held/Processing/Refunded are neutral — never red.

    4. Create `src/components/host/payout-row.tsx` — a per-booking row rendering (table row on desktop / stacked card content on mobile — the page composes the wrapper): `Booking {₱gross}` / `FitOut service fee (10%) −{₱commission}` / `Your payout {₱net}` (net weight 600, tabular-nums) + the PayoutStateBadge + the expected/paid date meta (`{datePrefix} {MMM d}`, venue-tz-safe). Refunded → net rendered muted/struck + the helper. The host SEES the commission line (D-59) — render the negative `−{₱commission}` in Label size, muted. Props are the frozen ledger figures + the venue-tz date; ZERO price arithmetic (all cents come server-frozen).

    5. Create `src/components/host/payout-summary.tsx` — two neutral figures (Display typography, tabular-nums): `Upcoming payouts` = sum(net where state ∈ held/processing), `Paid out` = sum(net where state = paid), each with a Label-600 caption. No coral. Props are the two precomputed cent totals (summed server-side).
  </action>
  <acceptance_criteria>
    - src/components/ui/table.tsx exists (shadcn table added)
    - payout-ledger-status.ts exports `derivePayoutLedgerView` and is NOT "use client" (grep: `export function derivePayoutLedgerView`; grep must NOT show `"use client"` in that file)
    - payout-state-badge.tsx maps paid → `bg-success` and pairs every state with a lucide icon (grep: `bg-success`, `CheckCircle2`)
    - payout-row.tsx renders `FitOut service fee (10%)` and `Your payout` and the negative commission (grep: `FitOut service fee (10%)`, `Your payout`)
    - payout-summary.tsx renders `Upcoming payouts` and `Paid out` captions (grep both)
    - `npx tsc --noEmit` exits 0 and `npx eslint src/components/host/payout-*.tsx src/components/host/payout-ledger-status.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/host/payout-state-badge.tsx src/components/host/payout-row.tsx src/components/host/payout-summary.tsx</automated>
  </verify>
  <done>The payout state derivation, badges (icon + text, Paid=success, others neutral), per-booking row with the visible commission line, and summary figures exist per the UI contract.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-gated earnings RSC + Earnings nav links + states</name>
  <files>src/app/(host)/host/earnings/page.tsx, src/app/(host)/host/page.tsx, src/app/(host)/host/layout.tsx</files>
  <read_first>
    - src/app/(host)/host/listings/page.tsx (the RSC canHost gate + owner-scoped query + max-width shell to mirror)
    - src/app/(host)/host/page.tsx (the action row where the Earnings link goes; PayoutBanner reuse)
    - src/app/(host)/host/layout.tsx (the (host) header — add the Earnings nav link)
    - src/components/host/payout-banner.tsx + payout-status.ts (reuse for the not-onboarded nudge)
    - .planning/phases/05-payments-payouts/05-UI-SPEC.md § Discretionary decisions (Earnings URL/nav/layout) + § Screen/State Contract (Host earnings) + § Empty/zero states + § Copywriting Contract
  </read_first>
  <action>
    1. Create `src/app/(host)/host/earnings/page.tsx` — an RSC:
       - Gate like host/listings: `getSession` → `!user` redirect `/login` → `!canHost` redirect `/`.
       - OWNER-SCOPE the ledger read (Security V4 — the route group is NOT the gate): query `host_payout_ledger` `WHERE host_id = session.user.id` joined to `booking`/`listing` for the space title + endsAt + venue timezone, newest-first. NEVER read another host's rows.
       - Compute summary totals server-side: sum(net_cents where state ∈ held/processing) and sum(net_cents where state = paid). Pass to `<PayoutSummary/>`.
       - Expected date: compute `endsAt + PAYOUT_DELAY_HOURS` server-side (import PAYOUT_DELAY_HOURS from Plan 01 config) and format venue-tz-safe (TZDate/epoch like the confirmation page); paid rows use `paidAt`.
       - States (Screen/State Contract): loading (a `loading.tsx` skeleton is optional — reuse the host loading idiom), not-onboarded → render `<PayoutBanner/>` on top (any confirmed bookings still show as Held), empty → `No earnings yet` + the empty copy, populated → `<PayoutSummary/>` + the payout rows (desktop `table` with real `<th scope>` headers Space·When·Booking·Fee·Payout·Status·Expected; mobile stacked cards via `<PayoutRow/>`), row Refunded → muted/struck net, edge failed → the destructive `Alert`. Container `mx-auto w-full max-w-4xl px-4 py-10`. NO coral CTA (status view). Every date names/respects the venue timezone.
       - Money is all server-frozen (quotedTotalCents/commission_cents/net_cents) — the page never recomputes.

    2. In `src/app/(host)/host/page.tsx` action row (next to `Your listings`), add a neutral `Earnings` link: `<Button asChild variant="outline"><Link href="/host/earnings">Earnings</Link></Button>`.

    3. In `src/app/(host)/host/layout.tsx` header, add a neutral `Earnings` nav link → `/host/earnings` (alongside the existing nav; `outline`/neutral, not coral).
  </action>
  <acceptance_criteria>
    - earnings/page.tsx queries `hostPayoutLedger` filtered by `host_id`/session.user.id (grep: `hostPayoutLedger`, `host_id` or `hostId`, `session.user.id`)
    - it gates on `canHost` and redirects `!canHost` → `/` (grep: `canHost`)
    - it renders `PayoutSummary` and `PayoutRow` and the `No earnings yet` empty state (grep all three)
    - host/page.tsx and layout.tsx contain a link to `/host/earnings` (grep in both)
    - the container uses `max-w-4xl` and there is NO `bg-brand` CTA on the earnings page (grep: `max-w-4xl`; grep must NOT show `bg-brand` in earnings/page.tsx)
    - `npx tsc --noEmit` exits 0 and `npx eslint "src/app/(host)/host/earnings/page.tsx"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "src/app/(host)/host/earnings/page.tsx" "src/app/(host)/host/page.tsx" "src/app/(host)/host/layout.tsx"</automated>
  </verify>
  <done>An owner-scoped /host/earnings shows summary totals + per-booking payout rows with the visible commission line and venue-tz dates, reachable via the neutral Earnings nav link; no coral, calm states.</done>
</task>

<task type="auto">
  <name>Task 3: Earnings owner-gate + rows + summary test (HOST-03)</name>
  <files>tests/payments/earnings-view.test.ts</files>
  <read_first>
    - tests/paymongo/webhook-merchant-activated.test.ts (isolated-schema harness + makeHost helpers)
    - src/components/host/payout-ledger-status.ts (Task 1 — the pure derivation to unit-test directly)
    - src/app/(host)/host/earnings/page.tsx (Task 2 — the owner-scoped query)
    - .planning/phases/05-payments-payouts/05-VALIDATION.md § Requirement→Test Map (HOST-03 row)
  </read_first>
  <action>
    Create `tests/payments/earnings-view.test.ts`:
      - Pure derivation: assert `derivePayoutLedgerView('paid')` → tone 'success', datePrefix 'Paid'; 'held' → muted + the "Held until after the session" helper + 'Expected'; 'refunded' → the refunded helper; 'processing' → 'outline'.
      - Summary totals: given a mix of ledger rows (held 90000, processing 45000, paid 180000, refunded 30000), assert Upcoming = 135000 (held+processing) and Paid out = 180000 (the page's summing logic — extract it to a small pure helper the test can call directly, or assert via the RSC's computed props).
      - Owner-gate (Security V4): seed ledger rows for host A and host B (isolated schema). Assert the earnings query for host A returns ONLY A's rows and never B's (call the page's data-loading function with A's session id, or run the owner-scoped query directly and assert the host_id filter). A guessed/absent host sees nothing.
      - Commission visibility: a row exposes gross/commission/net so the host sees the fee (assert the row data includes commissionCents > 0 for a 10% booking).
  </action>
  <acceptance_criteria>
    - earnings-view.test.ts asserts derivePayoutLedgerView mappings (paid→success, held→muted+helper, refunded→helper)
    - it asserts Upcoming = held+processing and Paid out = paid summing
    - it asserts the owner-scoped query returns only the signed-in host's rows (host A never sees host B)
    - `npx vitest run tests/payments/earnings-view.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/payments/earnings-view.test.ts</automated>
  </verify>
  <done>The earnings derivation, summary summing, and owner-scoping are proven; a host can only ever see their own payout rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host (browser) → /host/earnings | Signed-in host session; the route group is NOT the authorization gate |
| ledger read → host_id filter | Rows must be owner-scoped so no cross-host disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-29 | Elevation / Info-disclosure (IDOR) | A host viewing another host's payout rows | mitigate | Owner-scope the ledger query `WHERE host_id = session.user.id` (Security V4 — route group is not the gate); test asserts A never sees B (Tasks 2/3) |
| T-05-30 | Info-disclosure | An unauthenticated / non-host reaching the earnings page | mitigate | `!user` → /login, `!canHost` → / (RSC gate, defense in depth beyond the layout, Task 2) |
| T-05-31 | Tampering | The page recomputing/altering money client-side | mitigate | All figures are server-frozen ledger cents (gross/commission/net); the page/components do ZERO price arithmetic (Tasks 1/2) |
| T-05-32 | Info-disclosure | Leaking PayMongo internals (transfer id, wallet, gateway fee) to the host | mitigate | Host copy never says wallet/transfer/gateway fee; the page shows only gross/−10%/net + a calm state (D-52/59 copy contract, Task 1) |
</threat_model>

<verification>
- `npx vitest run tests/payments/earnings-view.test.ts` green (derivation + summary + owner-gate).
- `npx tsc --noEmit` + `npx eslint` clean on all new/modified files.
- grep confirms the ledger read is owner-scoped by host_id and the Earnings link exists in the dashboard + header.
</verification>

<success_criteria>
- /host/earnings shows owner-scoped per-booking rows (Held/Processing/Paid/Refunded), the gross→−10%→net breakdown, expected/paid venue-tz dates, and summary totals.
- The host sees the commission line and a fixed, explainable gross−10% payout; states are calm (Paid=success, others neutral, never red).
- A host can only ever see their own payout rows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-payouts/05-06-SUMMARY.md` (record the earnings query shape, the summary-summing helper, the state→badge mapping, and the nav-link placements).
</output>
