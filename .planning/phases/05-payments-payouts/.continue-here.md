---
context: phase
phase: 05-payments-payouts
stage: discuss-phase
task: 4
total_tasks: 9
status: in_progress
last_updated: 2026-07-15T10:26:37Z
---

# Resume: Phase 5 (Payments & Payouts) — mid discuss-phase

No blocking anti-patterns were discovered this session (nothing failed). This is a
clean pause partway through the Phase-5 discussion — 2 of 4 selected gray areas resolved.

<current_state>
Running `/gsd-discuss-phase 5`. Prior context is fully loaded and the existing PayMongo
surface has been scouted. The user selected 4 gray areas to discuss; **Commission model**
and **Payment methods** are resolved. The **Payout timing** picker was presented and the
user **dismissed it** (chose not to answer via the options), then paused. **Payment wiring
depth** has not been opened. No `05-CONTEXT.md` exists yet.

Resume state lives in `.planning/phases/05-payments-payouts/05-DISCUSS-CHECKPOINT.json`
(the discuss-phase `check_existing` step detects it and offers to resume).
</current_state>

<completed_work>
- Task 1: Load prior context (PROJECT/ROADMAP/REQUIREMENTS/STATE + phases 02/04 CONTEXT) + scout PayMongo code — Done
- Task 2: Present gray areas; user selected Commission model, Payment methods, Payout timing, Payment wiring depth — Done
- Task 3: Discuss **Commission model** — Done
  - Host-side deduction (booker pays listed price, no fee line; breakdown stays subtotal = total)
  - **10%** commission, config-tunable (not hardcoded)
  - **Platform absorbs** PayMongo's gateway fee → host always receives exactly `quotedTotalCents − 10%`; platform's effective take ≈ 6.5–8%
- Task 4: Discuss **Payment methods** — Done
  - Full PH rails: **cards + GCash + Maya + QRPh** (D-20 rails; PAY-01 "by card" is a floor)
  - **Hosted PayMongo page** (redirect → return to confirmation), PCI-light; researcher picks the exact primitive (Checkout Session vs Links)
  - Reinforces webhook-as-source-of-truth: the return redirect is a UX signal; the payment webhook confirms the booking
</completed_work>

<remaining_work>
- Task 5: Discuss **Payout timing** — release window after session + trigger mechanism. **The picker was dismissed — re-open the area and let the user steer; do NOT just re-fire the same options.** Recommended (not accepted): T+24h after session end via a scheduled batch sweep (fits PayMongo `POST /v2/batch_transfers` + STATE "don't over-build").
- Task 6: Discuss **Payment wiring depth** — does the charge replace the Phase-4 placeholder confirm, and does confirm become webhook-driven (booking confirms on `payment.paid`, not the synchronous return, per SC#3)? Recommended: yes.
- Task 7 (optional): Refund/dispute scope split with Phase 7 (Phase 5 = refund/dispute **mechanism**; Phase 7 = policy **matrix**).
- Task 8 (optional): Host earnings / payout-status view (HOST-03).
- Task 9: Write `05-CONTEXT.md` + `05-DISCUSSION-LOG.md`, commit, update STATE.md, remove the checkpoint.
</remaining_work>

<decisions_made>
- Commission = host-side deduction, 10% (tunable), platform absorbs the gateway fee — chosen for a demand-side-first, no-surprise-fee booker experience and a fixed, explainable host payout.
- Full PH rails via a hosted PayMongo page — chosen because GCash/Maya/QRPh dominate PH payments (the reason PayMongo was picked over Stripe) and a hosted page is PCI-light and fastest for Phase 5's instant-capture scope.
</decisions_made>

<blockers>
- None. (PayMongo Platforms beta + advanced-card-features enablement are lead-time support requests for real UAT, but they do NOT block discuss/plan — tests mock PayMongo.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-payments-payouts/05-DISCUSS-CHECKPOINT.json` — exact resume state + captured decisions
2. `CLAUDE.md` § "Marketplace Payments — Prescriptive Detail" + the D-20 rows — hold-until-session, `POST /v2/batch_transfers`, `Idempotency-Key`, `Paymongo-Signature` HMAC, "never pay the host at booking time", do-NOT-use payment-splitting
3. `.planning/PROJECT.md` § Key Decisions **D-20** (PayMongo) — supersedes D-17/D-19 (Stripe)
4. `.planning/phases/02-listings-host-onboarding/02-CONTEXT.md` — the PayMongo onboarding + bookability-gate foundation Phase 5 builds on
5. `.planning/phases/04-booking-core-search-no-payment/04-CONTEXT.md` — D-46/47/48 + the `confirmBooking` seam
6. `src/lib/paymongo.ts`, `src/app/api/paymongo/webhook/route.ts`, `src/app/actions/booking.ts`, `src/lib/db/schema.ts` — the code Phase 5 extends

## Critical Framing (do NOT lose — the ROADMAP text is misleading)
- **The ROADMAP Phase 5 goal/success-criteria are STALE**: they say "Stripe Connect / reverse_transfer / account.updated". D-20 replaced Stripe with **PayMongo**: collect the FULL amount to the **platform wallet** → **HOLD** → after the session, on-demand `inhouse` **`POST /v2/batch_transfers`** of (booking − commission) to the host. There is **no `reverse_transfer`** — a pre-payout refund is just a platform-wallet refund (the host hasn't been paid yet). `account.updated` = the **`merchant.activated`** webhook, already built in Phase 2.
- **Phase 5 = payment infra on the SINGLE, non-forked path.** The instant-book vs request-to-book fork, authorize→capture, and host approve/decline are **Phase 6**. Every Phase-5 booking is instant-capture.
- **Phase 7 owns cancellation-policy tiers** — Phase 5 builds the refund/dispute **mechanism**, not the policy matrix. (PayMongo e-wallet refund rule from STATE: same-day = full-refund-only; partial only from the next day.)

## Infrastructure State
- No background processes were started. No dev server, no DB writes this session (discuss-phase only).
- Nothing was migrated. The only new files are planning artifacts (checkpoint + handoff).

<context>
This is a thinking-partner discussion, capturing founder-level decisions for downstream
research + planning. Two areas resolved cleanly. The user dismissed the Payout-timing
picker (likely wanted to steer it in their own words rather than pick a canned option),
then paused. On resume, honor that: open Payout timing conversationally and ask how they
want it, offering the recommendation as a starting point rather than re-firing the same
4-option picker.
</context>

<next_action>
Start with: reload `05-DISCUSS-CHECKPOINT.json`, then re-open the **Payout timing** area —
ask the user (conversationally) for the release window after the session and how the
transfer should be triggered, offering "T+24h after session end via a scheduled batch
sweep" as the recommended starting point. Then discuss Payment wiring depth, optionally
the two remaining areas, then write `05-CONTEXT.md`.
</next_action>
