---
phase: 05-payments-payouts
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/actions/booking.ts
  - src/components/booking/reserve-actions.tsx
  - src/components/booking/price-breakdown.tsx
  - src/app/bookings/[id]/page.tsx
  - src/components/booking/pending-payment-state.tsx
  - src/components/booking/payment-reversed-state.tsx
  - tests/payments/checkout-create.test.ts
autonomous: true
requirements: [PAY-01]
user_setup:
  - service: paymongo
    why: "The booker checkout redirect + return flow needs a live checkout session and a tunnelled webhook to fully exercise (test mode)"
    dashboard_config:
      - task: "Ensure the checkout return URLs (success_url /bookings/[id]?paid=1, cancel_url the reserve page) are reachable from the deploy origin; local dev uses ngrok/cloudflared (no PayMongo CLI)"
        location: "PayMongo dashboard / local tunnel"
must_haves:
  truths:
    - "Confirm & pay creates a PayMongo Checkout Session for exactly booking.quotedTotalCents and redirects off-site"
    - "Creating the checkout extends booking.expiresAt to now()+PAYMENT_WINDOW so the hold is not swept mid-payment (D-58)"
    - "The synchronous pending→confirmed flip is RETIRED from confirmBooking (D-57) — the webhook is the confirm authority"
    - "On return the booker sees a 'payment received, finalizing…' pending state until status='confirmed'; the browser return alone never claims Confirmed"
    - "The booker price breakdown stays subtotal = total — no commission/fee line (D-50)"
  artifacts:
    - path: "src/app/actions/booking.ts"
      provides: "confirmBooking that extends the hold + creates the checkout + redirects (no sync flip)"
      contains: "createCheckoutSession"
    - path: "src/components/booking/pending-payment-state.tsx"
      provides: "Client poller interstitial that refreshes until confirmed"
      contains: "router.refresh"
    - path: "src/components/booking/payment-reversed-state.tsx"
      provides: "Calm auto-refund landing (never red)"
  key_links:
    - from: "src/app/actions/booking.ts"
      to: "src/lib/paymongo.ts createCheckoutSession"
      via: "server action charge"
      pattern: "createCheckoutSession"
    - from: "src/app/bookings/[id]/page.tsx"
      to: "PendingPaymentState"
      via: "?paid=1 + status='pending' branch"
      pattern: "PendingPaymentState"
---

<objective>
Turn the Phase-4 "Confirm booking" terminal action into "Confirm & pay" (D-57): `confirmBooking` now EXTENDS the hold, creates a PayMongo hosted Checkout Session for the server-frozen `quotedTotalCents`, and redirects off-site — the synchronous `pending→confirmed` flip is retired (the webhook, Plan 04, is the confirm authority). On return, the confirmation page renders a "payment received, finalizing…" interstitial that self-resolves once the webhook confirms, plus a calm auto-refund landing for the rare reversed edge.

Purpose: PAY-01 booker-facing checkout (D-53/54/57/58 action side). The booker breakdown stays `subtotal = total` (D-50).
Output: rewired `confirmBooking`, "Confirm & pay" reserve UI, the pending-payment + payment-reversed states on `/bookings/[id]`, and a charge-integrity + extend-hold test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-payouts/05-CONTEXT.md
@.planning/phases/05-payments-payouts/05-UI-SPEC.md
@.planning/phases/05-payments-payouts/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- From Plan 02 (src/lib/paymongo.ts) — the charge primitive. -->
```typescript
export async function createCheckoutSession(input: {
  amountCents: number; currency?: string; name: string; description?: string;
  referenceNumber: string; metadata?: Record<string, string>;
  successUrl: string; cancelUrl: string; idempotencyKey: string;
}): Promise<{ id: string; checkoutUrl: string }>;
```

<!-- From Plan 01 (src/lib/payments/config.ts). -->
```typescript
export const PAYMENT_WINDOW_MINUTES: number; // D-58 extend-hold window (default 60)
```

<!-- src/app/actions/booking.ts today — confirmBooking (lines 139-186). RETIRE the sync flip (164-168). -->
```typescript
export type ConfirmResult =
  | { ok: false; reason: "sign-in"; error: string }
  | { ok: false; reason: "denied"; error: string }
  | { ok: false; reason: "expired"; error: string };
// owner-gate (147-153): load booking.bookerId/status by holdId; missing OR not-mine → { reason:"denied" } (same calm copy)
// idempotent short-circuit (155-158): an already-'confirmed' OWN booking falls through to redirect(/bookings/[id]) — KEEP
// requireUserId(), auth, db, booking, listing, revalidatePath, redirect already imported
```

<!-- Money-adjacent server-action pattern to reuse (src/app/actions/paymongo-connect.ts): session-gate → rateLimit → recordAudit on denial → try/catch calm error. -->
<!-- src/components/booking/reserve-actions.tsx: "use client"; disables on click; label "Confirm booking"→"Confirming…"; calls confirmBooking(holdId), surfaces ConfirmResult via onResult. -->
<!-- src/app/bookings/[id]/page.tsx: owner-gated RSC (bookerId===userId else notFound); today `status==='pending'` → redirect to reserve (line 68); `status!=='confirmed'` → notFound; confirmed → success UI. -->
<!-- src/components/booking/price-breakdown.tsx: RESERVED fee slot renders nothing for the booker; final line copy "Final price. No booking or service fees in this step." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire confirmBooking — extend hold, create checkout, redirect (D-57/D-58); retire the sync flip</name>
  <files>src/app/actions/booking.ts, tests/payments/checkout-create.test.ts</files>
  <read_first>
    - src/app/actions/booking.ts (WHOLE file — keep the SECURITY CONTRACT header + placeHold untouched; rewrite only confirmBooking)
    - src/app/actions/paymongo-connect.ts (the money-adjacent server-action pattern: session → rateLimit → recordAudit → try/catch calm error)
    - src/lib/paymongo.ts (Plan 02 — createCheckoutSession)
    - src/lib/payments/config.ts (Plan 01 — PAYMENT_WINDOW_MINUTES)
    - src/lib/booking/reference.ts (bookingReference for the checkout line-item name)
    - .planning/phases/05-payments-payouts/05-RESEARCH.md § Architecture (confirmBooking box) + Pitfall 4
    - CLAUDE.md § Marketplace Payments (charge exactly quotedTotalCents; Idempotency-Key)
  </read_first>
  <behavior>
    - confirmBooking on an own pending hold: sets booking.expires_at = now() + PAYMENT_WINDOW_MINUTES (past the 15-min TTL), calls createCheckoutSession with amountCents === the booking's frozen quotedTotalCents (NEVER a client value), reference_number === booking.id, and redirects to the returned checkoutUrl.
    - It does NOT flip status to 'confirmed' (that moves to the webhook, D-57). A DB read after confirmBooking shows status still 'pending' with a pushed-out expires_at.
    - A client-supplied amount is impossible to inject (confirmBooking takes only holdId; the amount is read server-side from booking.quotedTotalCents).
    - Owner-gate preserved: a non-owner/missing holdId → { reason: "denied" } (same calm copy). No session → { reason: "sign-in" }.
    - Idempotent: an already-'confirmed' own booking short-circuits to redirect(/bookings/[id]) WITHOUT creating a second checkout (D-42) — use a stable Idempotency-Key `checkout:<bookingId>` so even a race returns the same session.
    - A PayMongo/checkout failure returns a calm ConfirmResult (new reason), never a raw 500 or a leaked secret.
  </behavior>
  <action>
    Rewrite `confirmBooking(holdId)` in `src/app/actions/booking.ts`:

    1. Keep `requireUserId()` (sign-in gate) and the owner-gated load (select bookerId + status + listingId + quotedTotalCents + currency by holdId; missing OR bookerId !== userId → `{ ok:false, reason:"denied", error:"We can't show this booking." }`).
    2. Keep the idempotent short-circuit: if `status === "confirmed"` → `redirect('/bookings/' + holdId)` (no checkout).
    3. If `status !== "pending"` (cancelled/declined/etc.) → return `{ ok:false, reason:"expired", error:"Your hold is no longer active. Check availability again." }`.
    4. Money-adjacent guard (reuse paymongo-connect.ts pattern): `rateLimit('confirm-pay:' + userId, { window, max })`; on denial `recordAudit({ actorId:userId, action:'confirm_pay', outcome:'denied', meta:{ holdId } })` and return a calm retry result.
    5. EXTEND the hold (D-58) atomically BEFORE creating the checkout so the sweep can't take the slot mid-payment:
       ```typescript
       await db.execute(sql`UPDATE booking SET expires_at = now() + make_interval(mins => ${PAYMENT_WINDOW_MINUTES})
         WHERE id = ${holdId} AND booker_id = ${userId} AND status = 'pending'`);
       ```
    6. Create the checkout with the SERVER-FROZEN amount (D-49):
       ```typescript
       const base = process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000"; // reuse the existing app-URL convention
       const ref = bookingReference(holdId);
       const cs = await createCheckoutSession({
         amountCents: bk.quotedTotalCents!, currency: bk.currency ?? "php",
         name: `Booking ${ref}`, referenceNumber: holdId, metadata: { booking_id: holdId },
         successUrl: `${base}/bookings/${holdId}?paid=1`,
         cancelUrl: `${base}/listings/${bk.listingId}/book?hold=${holdId}`,
         idempotencyKey: `checkout:${holdId}`,
       });
       redirect(cs.checkoutUrl);
       ```
       Wrap the create in try/catch — on a thrown PayMongo error, `recordAudit({ ..., outcome:'error' })` and return `{ ok:false, reason:"expired", error:"We couldn't start checkout. Please try again." }` (never surface the raw error/secret). Guard a null `quotedTotalCents` → same calm result (a mis-frozen quote must not charge 0).
    7. Add the new reason to `ConfirmResult` if needed (reuse "expired" for checkout-failure or add `reason:"checkout"` — keep the union exhaustive and update reserve-actions/reserve-view mapping accordingly in Task 2).
    8. Delete the old `UPDATE booking SET status='confirmed' … expires_at > now()` block (lines ~164-168) — the flip is gone (D-57). Update the confirmBooking header comment to say the webhook is now the confirm authority.

    Do NOT change placeHold.

    Create `tests/payments/checkout-create.test.ts` (isolated-schema harness like tests/paymongo/webhook-merchant-activated.test.ts; mock `@/lib/paymongo` with `mockPayMongo` and mock `@/lib/db`; mock `next/navigation` redirect to a typed RedirectError capturing the URL like tests/booking/state-machine.test.ts):
      - Seed a listing + an own pending booking with quotedTotalCents=150000, expires_at = now()+2min.
      - Call confirmBooking(holdId); assert createCheckoutSession was called ONCE with amountCents === 150000 and referenceNumber === holdId; assert the redirect URL === the mocked checkoutUrl.
      - Assert the booking row is STILL status='pending' (no sync flip) and expires_at has moved to > now()+50min (extend-hold; D-58).
      - Charge integrity: even if the test passes a bogus amount anywhere, confirmBooking only reads quotedTotalCents — assert the mock received 150000, not any other number.
      - Owner-gate: a different userId → { reason:"denied" }, no checkout created.
      - Idempotent: an already-'confirmed' booking → redirect(/bookings/[id]) and createCheckoutSession NOT called.
  </action>
  <acceptance_criteria>
    - booking.ts confirmBooking calls `createCheckoutSession` and `redirect(` on its checkoutUrl (grep both)
    - booking.ts confirmBooking no longer contains `SET status = 'confirmed'` (grep must NOT match in confirmBooking; the sync flip is gone)
    - the extend-hold UPDATE uses `make_interval(mins =>` and PAYMENT_WINDOW_MINUTES (grep both)
    - the checkout amount is `quotedTotalCents` (grep: `amountCents: ` ... `quotedTotalCents`)
    - `idempotencyKey: ` includes `checkout:` + holdId (grep: `checkout:`)
    - placeHold is unchanged (grep still shows createPendingHold + the placeHold reasons)
    - `npx vitest run tests/payments/checkout-create.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/payments/checkout-create.test.ts</automated>
  </verify>
  <done>confirmBooking extends the hold, charges exactly the frozen quote via a hosted checkout, redirects off-site, retires the sync flip, and stays owner-gated + idempotent; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve UI — "Confirm & pay" + charged-amount reassurance; breakdown copy (D-50)</name>
  <files>src/components/booking/reserve-actions.tsx, src/components/booking/price-breakdown.tsx</files>
  <read_first>
    - src/components/booking/reserve-actions.tsx (WHOLE — the disable-on-click idiom stays)
    - src/components/booking/price-breakdown.tsx (WHOLE — the RESERVED slot stays EMPTY for the booker)
    - src/components/booking/reserve-view.tsx (handleResult maps ConfirmResult reasons → expiry state — keep in sync with any new reason)
    - .planning/phases/05-payments-payouts/05-UI-SPEC.md § Copywriting Contract + § Modified existing components + § Reserve price-breakdown contract
  </read_first>
  <action>
    In `src/components/booking/reserve-actions.tsx`:
      - Change the button label: `pending ? "Confirming…" : "Confirm booking"` → `pending ? "Taking you to checkout…" : "Confirm & pay"`.
      - The action now redirects off-site on success (the component unmounts on navigation — the existing on-success assumption holds). Keep the disable-on-click double-click guard and `onResult` surfacing.
      - Accept a `totalLabel` prop (the server-formatted `formatMoney(quotedTotalCents, currency)` string) and replace the reassurance line `You won't be charged yet.` with: `You'll pay {totalLabel} now — cards, GCash, Maya, or QR Ph. Payments are processed securely.`
      - If a new `reason` (e.g. "checkout") was added to ConfirmResult in Task 1, ensure reserve-view.tsx's handleResult treats it as a graceful failure (flip to the calm expiry/retry state, never a red error). Pass `totalLabel` from ReserveView (it already renders the frozen breakdown; thread the formatted total).

    In `src/components/booking/price-breakdown.tsx`:
      - Change ONLY the final sub-line copy: `Final price. No booking or service fees in this step.` → `Final price — no added fees. You'll pay this now.`
      - Do NOT add any commission/service-fee line — the booker breakdown STAYS `subtotal = total` (D-50/Pitfall 6). The RESERVED slot renders nothing for the booker. Keep the Total at Heading weight 600, tabular-nums.
  </action>
  <acceptance_criteria>
    - reserve-actions.tsx contains `Confirm & pay` and `Taking you to checkout…` (grep both) and no longer contains `Confirm booking` or `You won't be charged yet` (grep must NOT match)
    - reserve-actions.tsx reassurance mentions `cards, GCash, Maya, or QR Ph` (grep)
    - price-breakdown.tsx contains `Final price — no added fees. You'll pay this now.` and contains NO `service fee`/`commission` line for the booker (grep: no `commission`, no added fee `<div>` in the reserved slot)
    - `npx tsc --noEmit` exits 0 and `npx eslint src/components/booking/reserve-actions.tsx src/components/booking/price-breakdown.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/booking/reserve-actions.tsx src/components/booking/price-breakdown.tsx</automated>
  </verify>
  <done>The terminal CTA reads "Confirm & pay" with a truthful charged-amount reassurance; the booker breakdown stays subtotal=total with the updated final line; lint + types clean.</done>
</task>

<task type="auto">
  <name>Task 3: Confirmation page pending-payment + payment-reversed states (D-57)</name>
  <files>src/app/bookings/[id]/page.tsx, src/components/booking/pending-payment-state.tsx, src/components/booking/payment-reversed-state.tsx</files>
  <read_first>
    - src/app/bookings/[id]/page.tsx (WHOLE — owner-gate + confirmed UI stay; change only the pending branch + add reversed)
    - src/components/booking/hold-expired-state.tsx (the calm recovery-state idiom PaymentReversedState mirrors — coral CTA, never red)
    - src/components/booking/hold-countdown.tsx (the "use client" setInterval + suppressHydrationWarning idiom for the poller)
    - .planning/phases/05-payments-payouts/05-UI-SPEC.md § Discretionary decisions (Pending-payment landing; Interstitial auto-resolve) + § Copywriting Contract (booker pending microcopy) + § Screen/State Contract (Confirmation page)
  </read_first>
  <action>
    1. Create `src/components/booking/pending-payment-state.tsx` — a `"use client"` interstitial poller:
       - Renders a neutral (NOT success-green, NOT coral) `Card` with a spinning lucide `Loader2`, heading `Payment received`, body `We're finalizing your booking — this usually takes a few seconds and updates on its own.`
       - Auto-resolve: `useEffect` starting a bounded `setInterval` (~every 2500ms) that calls `router.refresh()` (from `next/navigation`) so the RSC re-renders; stop after a cap (e.g. 8 attempts). After the cap, swap to the "taking longer" copy `Still finalizing… you can safely wait here. We've got your payment — your booking will appear in a moment.` + a manual `Refresh` button (also `router.refresh()`).
       - Accessibility: put the finalizing copy in an `aria-live="polite"` region; the spinner is decorative (aria-hidden). Follow the hold-countdown "use client" hook discipline (setInterval is the only state-mutation site; clear on unmount).
       - NEVER fabricate the confirmed state client-side — it only refreshes; the RSC decides confirmed vs pending.

    2. Create `src/components/booking/payment-reversed-state.tsx` — mirror `HoldExpiredState`: a calm `Card` (NOT red) with lucide `Undo2`, heading `We couldn't complete this booking`, body `Your payment was reversed — you haven't been charged for a slot we couldn't confirm. That time may have just been taken; check availability again.`, a coral primary `Back to availability` (→ `/listings/{listingId}`) and a neutral `Search other spaces` (→ `/`). Takes `listingId` prop.

    3. In `src/app/bookings/[id]/page.tsx`, read `searchParams` for `paid` and add branches AFTER the owner-gate load, BEFORE the current confirmed render:
       - `status === 'confirmed'` → the existing confirmed UI (unchanged).
       - `status === 'pending'` AND `paid === '1'` → render `<PendingPaymentState/>` (the finalizing interstitial). Do NOT redirect to reserve in this case.
       - `status === 'pending'` WITHOUT `paid` (abandoned) → keep the Phase-4 behavior: `redirect('/listings/' + bk.listingId + '/book?hold=' + bk.id)`.
       - `status === 'cancelled'` AND `paid === '1'` (the D-58 auto-refund landing — the webhook reversed a payment for a gone slot) → render `<PaymentReversedState listingId={bk.listingId}/>`.
       - any other non-confirmed status → `notFound()` (unchanged).
       Keep the owner-gate (bookerId === userId else notFound) exactly as-is — the route group is not the gate.
       Note: `page.tsx` must accept `searchParams: Promise<{ paid?: string }>` per Next 16 async params and await it.
  </action>
  <acceptance_criteria>
    - pending-payment-state.tsx is `"use client"`, contains `router.refresh` and `Payment received` and an `aria-live` region (grep each)
    - payment-reversed-state.tsx contains `We couldn't complete this booking` and `Back to availability` and uses the coral `bg-brand` class, NOT `bg-destructive` (grep: `bg-brand`, no `destructive`)
    - bookings/[id]/page.tsx branches on `paid` and renders `PendingPaymentState` for pending+paid and `PaymentReversedState` for cancelled+paid (grep: `PendingPaymentState`, `PaymentReversedState`, `paid`)
    - the confirmed-UI branch and the owner-gate (`bookerId !== userId`) are still present (grep both)
    - `npx tsc --noEmit` exits 0 and `npx eslint src/app/bookings src/components/booking/pending-payment-state.tsx src/components/booking/payment-reversed-state.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/app/bookings/[id]/page.tsx src/components/booking/pending-payment-state.tsx src/components/booking/payment-reversed-state.tsx</automated>
  </verify>
  <done>On return from checkout the page shows a truthful, neutral finalizing interstitial that self-resolves to Confirmed only when the DB says so, plus a calm reversed landing; owner-gate + confirmed UI intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booker (browser) → confirmBooking server action | Untrusted holdId + session cross here; amount is NEVER taken from the client |
| PayMongo return redirect → /bookings/[id] | The `?paid=1` return is a UX signal only — never proof of payment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | Client injecting a charge amount | mitigate | confirmBooking reads booking.quotedTotalCents server-side (D-49); action takes only holdId; test asserts the mock received the frozen amount (Task 1) |
| T-05-12 | Spoofing | Browser forging "I paid" via `?paid=1` to see a Confirmed page | mitigate | The page renders Confirmed ONLY when DB `status='confirmed'`; `?paid=1` + pending → neutral finalizing interstitial, never Confirmed (D-57, Task 3) |
| T-05-13 | Elevation / IDOR | Confirming/paying for someone else's hold | mitigate | Owner-gate `bookerId === userId` before any checkout; missing/not-mine → same calm "denied" (Task 1) |
| T-05-14 | Tampering (financial) | Double-click creating two checkouts / two charges | mitigate | Stable Idempotency-Key `checkout:<bookingId>` + the already-confirmed short-circuit + client disable-on-click (Task 1/2) |
| T-05-15 | Info-disclosure | PayMongo error / secret leaking to the booker | mitigate | try/catch returns a calm generic ConfirmResult + recordAudit; never surfaces the raw error (Task 1) |
| T-05-16 | Availability | Hold swept mid-payment → charged for a lost slot | mitigate | Extend-hold to now()+PAYMENT_WINDOW before checkout (D-58); the webhook auto-refund backstop (Plan 04) covers the residual edge |
</threat_model>

<verification>
- `npx vitest run tests/payments/checkout-create.test.ts` green (charge integrity + extend-hold + no sync flip + owner-gate + idempotent).
- `npx tsc --noEmit` + `npx eslint` clean on all modified/new files.
- grep confirms the sync `status='confirmed'` flip is gone from confirmBooking and "Confirm & pay" is the CTA.
</verification>

<success_criteria>
- Confirm & pay charges exactly the frozen quote via a hosted checkout, extends the hold, and redirects off-site — the webhook is the confirm authority.
- The confirmation page truthfully shows finalizing → Confirmed (never claiming Confirmed from the return alone) and a calm reversed landing.
- The booker breakdown stays subtotal = total (no commission line).
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-payouts/05-03-SUMMARY.md` (record the final ConfirmResult union, the checkout URL convention success_url/cancel_url, and the exact bookings/[id] branch conditions Plan 04's webhook must produce to resolve the interstitial).
</output>
