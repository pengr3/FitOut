---
phase: 15-auth-profile-transactional-email
plan: 08
type: execute
wave: 3
depends_on: [15-06, 15-07]
files_modified:
  - src/app/(app)/profile/page.tsx
  - src/app/(app)/profile/loading.tsx
  - src/app/(app)/profile/profile-form.tsx
  - tests/design/card-pattern-coverage.test.ts
autonomous: true
requirements: [AUTHUI-02]

must_haves:
  truths:
    - "/profile renders exactly one h1, at the 20px every adopted surface uses, through PageHeader."
    - "The page and its loading plate read ONE shell constant, so the fallback cannot draw a different box than the page it stands in for."
    - "/profile renders exactly two panel-card containers, and the plate renders two skeletons to match — the loading/loaded disagreement is resolved toward the pattern language."
    - "Zero accent fills, zero destructive variants and zero success tokens render on /profile."
    - "The save-state machine is behaviourally unchanged: saved-from-result, cleared on next submit, no timer on the save path."
    - "The avatar block gains no removal control and loses no upload capability — removal is Phase 16 CROP-03."
  artifacts:
    - path: "src/app/(app)/profile/page.tsx"
      provides: "BOOKING_SHELL + PageHeader composition"
      contains: "BOOKING_SHELL"
    - path: "src/app/(app)/profile/loading.tsx"
      provides: "the same shell and header plus two PanelSkeletons"
      contains: "PanelSkeleton"
    - path: "src/app/(app)/profile/profile-form.tsx"
      provides: "two PanelCards around an unmoved state machine"
      contains: "PanelCard"
  key_links:
    - from: "src/app/(app)/profile/page.tsx and loading.tsx"
      to: "src/lib/design/measurements.ts"
      via: "one BOOKING_SHELL import each — the duplication ends mechanically"
      pattern: "BOOKING_SHELL"
    - from: "src/app/(app)/profile/profile-form.tsx"
      to: "src/components/patterns/panel-card.tsx"
      via: "two PanelCard call sites replacing two bare sections"
      pattern: "PanelCard"
---

<objective>
Give `/profile` the design-system pass: the shared booker shell, the pattern's `<h1>`, two panels
around a save-state machine that does not move, and a loading plate that finally agrees with the page
it stands in for.

Purpose: AUTHUI-02 wants the profile page to carry the design system. Today it hand-types
`mx-auto w-full max-w-2xl px-4 py-10` **twice** — once on the page and once on the plate — renders the
app's last 24px `<h1>` outlier, draws no panel while its own plate draws one, and hangs two bare
`<section>`s off `text-lg` headings. Every one of those files' own comments already argues for exactly
the change this plan makes; the plate's header even says it renders its heading by hand only because
the page had not adopted the pattern.

`profile-form.tsx` is the repository's reference truthful-save model — 14-CONTEXT D-150 copied it. This
plan restyles **around** it and rewrites none of it.

Output: three restyled files and one inventory count moved by exactly the one adopter this plan adds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-07-SUMMARY.md

<interfaces>
From `src/lib/design/measurements.ts:402`:
  export const BOOKING_SHELL = "mx-auto w-full max-w-2xl px-4 py-8 sm:py-12"
  ⚠ It is a CONTAINER, not a landmark (`measurements.ts:397-400`). `(app)/layout.tsx` owns the one
  `<main>` per document (D-88.1) — do NOT wrap it in a `<main>`.
  Already adopted by `bookings/[id]/cancel` and `bookings/[id]/group`, page + plate each.

From `src/components/patterns/page-header.tsx`:
  { title: string; lede?: string; actions?: ReactNode }
  Renders `<h1 className="text-xl font-semibold tracking-tight">`; `lede` is measured at `max-w-prose`.

From `src/components/patterns/panel-card.tsx` (widened by plan 15-06):
  { title?, titleAs?: "h1" | "h2" | "h3" (default "h2"), description?, … }
  Its CardContent already carries `space-y-4 p-4 sm:p-6` — ADD NO PADDING AND NO `space-y-4`.

The two panels, with their copy byte-identical to the shipped section headings and sentences:
  "Public profile"        — "What other people on FitOut can see."       — avatar block + first name, About, city
  "Private account info"  — "Only you can see this. Never shown to other people." — last name, phone

The shipped page today (`profile/page.tsx:36-44`) and plate (`loading.tsx:18-23`) both hand-type
`mx-auto w-full max-w-2xl px-4 py-10` with a `<header className="mb-8">` and a `text-2xl` `<h1>`.
The only rendering change from adopting `BOOKING_SHELL` is vertical padding 40px → 32px mobile /
48px at `sm:` and up — which is what stops `/profile` being the one booker-shell page with its own
rhythm (flagged in 15-UI-SPEC § Open Questions 3, PM-deferrable, not blocking).

Inventory this plan moves:
  tests/design/card-pattern-coverage.test.ts — `EXPECTED_SURFACES` is a plain const at :431, left at
  **20** by plan 15-07. This plan adds `profile-form.tsx` as the fifth adopter and moves it 20 → 21.
  `ALLOWED_RAW_CARD` is NOT touched — `profile-form.tsx` was never on it, because it had no card.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The page and its plate read one shell and one header</name>

  <read_first>
    - src/app/(app)/profile/page.tsx (all 59 lines — the header block at :36-44 and the `memberSince` derivation at :31-33)
    - src/app/(app)/profile/loading.tsx (all 26 lines — its header comment's first clause becomes false this phase; its second clause survives)
    - src/app/(app)/bookings/[id]/group/page.tsx lines 205-225 (the shipped BOOKING_SHELL + PageHeader-with-lede composition to copy)
    - src/app/(host)/host/earnings/loading.tsx (the shell-constant + PageHeader + skeleton plate shape, and the comment naming the property it buys)
    - src/lib/design/measurements.ts lines 376-402 (BOOKING_SHELL's own docblock — the argument to cite, and the container-not-landmark warning)
    - src/components/patterns/page-header.tsx (the `lede?` contract)
    - tests/design/loading-coverage.test.ts (confirm for yourself that no route is added and the 29/21/8 counts do not move)
  </read_first>

  <files>src/app/(app)/profile/page.tsx, src/app/(app)/profile/loading.tsx</files>

  <action>
    **page.tsx.** Replace the hand-typed container string with `BOOKING_SHELL`, imported from
    `@/lib/design/measurements`. Replace the `<header className="mb-8">` block — the `text-2xl` `<h1>`
    and the conditional muted paragraph — with `PageHeader`, `title="Your profile"`, and `lede` set to
    the composed `Member since {memberSince}` sentence when `memberSince` is non-null and left
    **undefined** otherwise. The conditional stays a conditional: an absent `createdAt` renders no lede,
    which is `PageHeader`'s own `lede?` contract. Do not fabricate a fallback sentence.

    Keep the `mb-8`-equivalent separation between the header and the form by whatever spacing step the
    group-page analog uses — read it and match it rather than inventing a value; every gap must be on
    the declared ladder. Do **not** wrap `BOOKING_SHELL` in a `<main>`: `(app)/layout.tsx` owns the one
    landmark per document.

    Everything above the return stays byte-identical: the session read, the `/login` redirect (this is
    the per-page gate, not the middleware's), the widened `session.user` cast, the `formatMemberSince`
    call, and the whole `<ProfileForm>` prop list. Keep the file's header comment about the D-09/D-10
    public/private split and the `publicProfile()` boundary verbatim.

    **loading.tsx.** The same `BOOKING_SHELL` import, the same `PageHeader title="Your profile"` — with
    **no lede**, because the member-since line is data-derived and conditional and a plate must not fake
    it. Then **two** `PanelSkeleton`s instead of one, matching the two panels the loaded page will now
    draw. Give each a distinct `label` so the two loading regions are distinguishable.

    Rewrite the plate's header comment. Its first clause — that the heading is rendered through the
    page's own classes because the page is `text-2xl` and the pattern's contract is `text-xl` — becomes
    false the moment page.tsx adopts `PageHeader`; rewrite it to say what is now true, in the register of
    `host/earnings/loading.tsx`'s comment: the two read one constant now, so the fallback cannot draw a
    different box than the page it stands in for. **Keep the second clause word for word**: the
    `Member since` line is data-derived AND conditional, so it is not faked — a bar standing in for a
    line that may not exist is a shift in whichever direction the data goes. Keep the "a panel, not a
    row list" paragraph and update only its count.

    `tests/design/loading-coverage.test.ts` must pass with **zero edits** — no route is added, and the
    29/21/8 counts do not move. If it goes red, you added or removed a `loading.tsx`; undo that.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design &amp;&amp; git diff --exit-code tests/design/loading-coverage.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'BOOKING_SHELL' 'src/app/(app)/profile/page.tsx'` returns 1 and likewise for `loading.tsx`
    - `grep -c 'mx-auto w-full max-w-2xl px-4 py-10' 'src/app/(app)/profile/page.tsx' 'src/app/(app)/profile/loading.tsx'` returns 0 for each — the duplication is gone
    - `grep -c 'text-2xl' 'src/app/(app)/profile/page.tsx' 'src/app/(app)/profile/loading.tsx'` returns 0 for each
    - `grep -c 'PageHeader' 'src/app/(app)/profile/page.tsx'` returns at least 2 and likewise for `loading.tsx`
    - `grep -c 'PanelSkeleton' 'src/app/(app)/profile/loading.tsx'` returns at least 3 (import + two call sites)
    - `grep -c 'lede' 'src/app/(app)/profile/loading.tsx'` returns 0 — the plate carries no lede
    - `grep -c '<main' 'src/app/(app)/profile/page.tsx'` returns 0
    - `grep -c 'redirect("/login")' 'src/app/(app)/profile/page.tsx'` returns 1 — the per-page gate is untouched
    - `grep -c 'publicProfile' 'src/app/(app)/profile/page.tsx'` is unchanged from its pre-task value
    - `git diff --exit-code tests/design/loading-coverage.test.ts` exits 0 and `npx vitest run tests/design/loading-coverage.test.ts --config vitest.design.config.ts` reports 29/21/8
    - `loading.tsx`'s comment still contains the "data-derived AND conditional" sentence word for word
    - `npm run test:design` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>One shell constant with two readers, one 20px h1 through the pattern, and a plate that draws the same number of boxes as the page.</done>
</task>

<task type="auto">
  <name>Task 2: Two panels around a state machine that does not move</name>

  <read_first>
    - src/app/(app)/profile/profile-form.tsx (all 272 lines — the two `<section>` blocks at :85-95 and :196-201, the avatar block at :97-131, the three live regions at :127, :249 and :260, and the whole save-state machine)
    - src/components/booking/reserve-view.tsx (the repo's one `"use client"` PanelCard adopter — the shape `profile-form.tsx` becomes)
    - src/components/patterns/panel-card.tsx lines 185-205 (what the pattern renders, so you add nothing it supplies)
    - tests/profile/ (every spec in that directory — read the assertions before touching the state machine)
    - tests/design/card-pattern-coverage.test.ts lines 195-215 and 425-435 (the CARD_SURFACES row shape and EXPECTED_SURFACES, left at 20 by plan 15-07)
  </read_first>

  <files>src/app/(app)/profile/profile-form.tsx, tests/design/card-pattern-coverage.test.ts</files>

  <action>
    Replace the two bare `<section>` + `text-lg font-medium` `<h2>` + muted-sentence blocks with two
    `PanelCard` call sites:
    - `title="Public profile"`, `titleAs="h2"`, `description="What other people on FitOut can see."`,
      containing the avatar block and the public fields (first name, About, city).
    - `title="Private account info"`, `titleAs="h2"`,
      `description="Only you can see this. Never shown to other people."`, containing last name and
      phone.

    Both sentences are shipped copy carrying the D-09/D-10 argument — byte-for-byte. The
    `aria-labelledby` / `id` pairs retire with the `<section>`s, because `PanelCard` renders the heading
    itself. Add no padding and no `space-y-4` at either call site.

    The save row stays **below** the second panel, outside both, with its elements unchanged: the
    `role="alert"` refusal line, then the `role="status"` saved line, then the `Save profile` button as
    a **neutral solid — never brand**. A profile edit is a form, not a conversion. Give the saved line
    `aria-label="Save state"` — the wizard's precedent, and the only markup change any of the three live
    regions receives. The avatar-upload refusal keeps its `role="alert"` and its classes.

    **Untouchable in this file**, and every one of these is a real contract rather than caution:
    - The state machine. `saved` is set from the actual `updateProfile` result and cleared on the next
      submit, with **no timer** on the save path; `avatarBusy` and `avatarError` come from the actual
      `uploadAvatarAction` result. This is the repository's reference truthful-save model. Restyle
      around it; do not introduce a `setTimeout`, an optimistic flag, or a derived "probably saved".
    - `updateProfile` and `uploadAvatarAction` call shapes and their server-side re-validation.
    - The avatar mechanism: the hidden file input, its `aria-label="Upload avatar"`, the `Upload photo`
      outline button and the `JPG or PNG, up to 5 MB. Optional.` hint, all byte-identical. **Add no
      second control.** Removal and crop are Phase 16 (CROP-01/CROP-03); leave the action row able to
      hold a future control and add nothing to it now.
    - Every form sentence and every field label, byte-for-byte.

    Render **zero** accent fills, **zero** `variant="destructive"` controls and **zero** success tokens
    on this surface. `Profile saved.` stays neutral ink — the file already carries the D-14 argument for
    that in a comment; preserve it.

    **The inventory, in this same commit.** Add `src/app/(app)/profile/profile-form.tsx` to
    `CARD_SURFACES` as a declared `panel-card` adopter with a real `why` sentence, and move
    `EXPECTED_SURFACES` 20 → 21. Observe the red first — run the gate with the row added and the number
    still at 20 — and quote the failure verbatim in the test's changelog. `ALLOWED_RAW_CARD` is not
    touched: this file was never on it, because it had no card to exempt.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design &amp;&amp; npx vitest run tests/profile/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'PanelCard' 'src/app/(app)/profile/profile-form.tsx'` returns at least 3 (import + two call sites)
    - `grep -c 'titleAs="h2"' 'src/app/(app)/profile/profile-form.tsx'` returns 2
    - `grep -c 'text-lg font-medium' 'src/app/(app)/profile/profile-form.tsx'` returns 0
    - `grep -c 'aria-labelledby' 'src/app/(app)/profile/profile-form.tsx'` returns 0
    - `grep -c 'aria-label="Save state"' 'src/app/(app)/profile/profile-form.tsx'` returns 1
    - `grep -c 'variant="brand"' 'src/app/(app)/profile/profile-form.tsx'` returns 0
    - `grep -c 'variant="destructive"' 'src/app/(app)/profile/profile-form.tsx'` returns 0
    - `grep -c 'setTimeout' 'src/app/(app)/profile/profile-form.tsx'` returns 0
    - `grep -c 'JPG or PNG, up to 5 MB. Optional.' 'src/app/(app)/profile/profile-form.tsx'` returns 1
    - `grep -c 'aria-label="Upload avatar"' 'src/app/(app)/profile/profile-form.tsx'` returns 1
    - `grep -c 'What other people on FitOut can see.' 'src/app/(app)/profile/profile-form.tsx'` returns 1 and `grep -c 'Only you can see this. Never shown to other people.' 'src/app/(app)/profile/profile-form.tsx'` returns 1
    - `grep -cE 'Remove|Delete' 'src/app/(app)/profile/profile-form.tsx'` returns 0 — no removal affordance was pre-empted
    - `tests/design/card-pattern-coverage.test.ts` has `EXPECTED_SURFACES = 21` and its changelog quotes the observed red verbatim
    - `npx vitest run tests/profile/` exits 0
    - `npm run test:design` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Two panels, one save row, one accessible name added, one adopter declared — and the state machine reads byte-identically in behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session → `/profile` | The per-page `getSession()` gate plus the `/login` redirect, not the middleware, is the boundary |
| form input → `updateProfile` / `uploadAvatarAction` | Server actions that re-validate; the client is never trusted |
| private fields → `publicProfile()` | The D-09/D-10 public/private leak boundary, enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-27 | Elevation of privilege | A restyle weakening the per-page session gate | mitigate | `page.tsx`'s session read and `redirect("/login")` are byte-identical and asserted by a grep; `publicProfile()` is not touched (GATE-NOREG #10) |
| T-15-28 | Information disclosure | Private fields moving into the public panel | mitigate | The two panels keep exactly their shipped field membership and both D-09/D-10 sentences byte-for-byte; the split is asserted by field-level review against the shipped file |
| T-15-29 | Repudiation | A save reported as successful when it was not | mitigate | The save-state machine is untouched: saved-from-result, cleared on next submit, zero `setTimeout` on the save path — asserted by a grep and by `tests/profile/` |
| T-15-30 | Tampering | Pre-empting Phase 16's avatar removal control | accept | Explicitly out of scope (CROP-03). Asserted negatively: no Remove/Delete affordance appears; the upload mechanism is byte-identical |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:design` green — including `loading-coverage` at 29/21/8 with zero edits,
  `card-pattern-coverage` at 21, `type-scale`, `contrast`, `accent-uses` and `empty-state-adoption`.
- `npx vitest run tests/profile/` green.
- `git diff --exit-code tests/design/loading-coverage.test.ts tests/design/empty-state-adoption.test.ts src/lib/profile.ts` exits 0.
- `git diff --name-only` at plan end lists only the four files in `files_modified`.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never concurrently.
</verification>

<success_criteria>
- One `<h1>` at 20px, exactly two `panel-card` containers, a plate that draws two skeletons.
- One `BOOKING_SHELL` import on the page and one on the plate; the hand-typed duplication is gone.
- Zero coral, zero destructive, zero success tokens, zero new controls on the avatar row.
- `EXPECTED_SURFACES` at 21, moved with the one row that justified it.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-08-SUMMARY.md` when done.
</output>
