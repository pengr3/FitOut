---
phase: 15-auth-profile-transactional-email
plan: 14
type: execute
wave: 7
depends_on: [15-12, 15-13]
gap_closure: true
files_modified:
  - tests/ops/alert-digest.test.ts
  - .planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md
  - .planning/phases/15-auth-profile-transactional-email/deferred-items.md
autonomous: true
requirements: [AUTHUI-01, EMAIL-01]

must_haves:
  truths:
    - "The ops digest's authored-markup check fails when the wrapper its comment names is re-added — proven by applying that exact mutation and watching it go red."
    - "Every raw-tag absence assertion in the email and ops suites is classified failable-or-fixed by measurement, and the classification is written down."
    - "AUTHUI-01's evidence is re-confirmed against the tree as it stands today — gate, greps, committed baselines and the CI comparison run — and exactly one plan claims it."
    - "15-VALIDATION.md maps a row to AUTHUI-03's keyboard clause and a row to its AA clause, so the requirement's Nyquist coverage matches its conjunctive text."
    - "No send trigger, subject, recipient or call site moved: src/lib/email.ts is byte-identical at plan end."
  artifacts:
    - path: "tests/ops/alert-digest.test.ts"
      provides: "Case 11, made able to fail for the regression it names, in both projections"
      contains: "audit-id"
    - path: ".planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md"
      provides: "Validation rows for AUTHUI-03's keyboard and AA clauses, and the closure of the post-execution sampling note"
      contains: "15-12-01"
  key_links:
    - from: "tests/ops/alert-digest.test.ts"
      to: "src/lib/email.ts renderOpsAlertDigest"
      via: "both projections of the runbook paragraph — raw in text/plain, entity-escaped in HTML"
      pattern: "renderOpsAlertDigest"
    - from: ".planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md"
      to: "15-12-PLAN.md and 15-13-PLAN.md"
      via: "per-task rows naming the automated command that samples each newly-covered clause"
      pattern: "auth-keyboard.spec.ts"
---

<objective>
Close the phase's three remaining non-blocking items: the one assertion left standing that cannot
fail for the regression it names, AUTHUI-01's traceability, and the two validation rows AUTHUI-03's
conjunctive text was never given.

Purpose:

**Gap C (WARNING).** `tests/ops/alert-digest.test.ts:431` asserts that the HTML part does not contain
a literal `code` element open-tag, for a regression that would render as the entity-escaped form —
paragraphs are escaped structurally at the choke point, so the literal string can never appear and
the assertion can never fail. Review finding WR-04 measured this directly and it is still unfixed in
the tree. This is the phase's THIRD instance of the same anti-pattern; two were caught and fixed
mid-phase (`notify.test.ts`'s apostrophe case in 15-03, the `/profile` 320px
redirect-satisfies-the-hook row in 15-10). Fix this one the same way, and prove it with a deliberate
red.

**AUTHUI-01's traceability.** The verifier independently confirmed the requirement — a mutation-tested
build-blocking gate, component-reuse greps, and direct inspection of the four CI-captured baseline
PNGs against a same-session `checkout-1280` — but no plan's frontmatter ever claimed it: 15-09
expected 15-11 to close the remaining visual clause, and 15-11 explicitly declined. This plan claims
it by CITING that evidence and verifying it still holds.

**AUTHUI-03's Nyquist coverage.** 15-VALIDATION.md maps AUTHUI-03 to exactly three rows — `15-10-02`,
`15-11-01`, `15-11-02`, all discharged — and never maps a keyboard row or an AA row at all. Two of its
five clauses were never sampled. Plans 15-12 and 15-13 sample them; this plan puts them in the table.

Output: one assertion that can fail, one audit written down, one requirement claimed on evidence, and
a validation table that matches the requirement it validates.

⚠ NO NEW AUTHUI-01 IMPLEMENTATION. Task 2 VERIFIES existing evidence; it does not rebuild it. If the
re-verification finds the evidence no longer holds, that is a finding to report — not a licence to
start building.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-VERIFICATION.md
@.planning/phases/15-auth-profile-transactional-email/15-REVIEW.md
@.planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md
@.planning/phases/15-auth-profile-transactional-email/deferred-items.md

<interfaces>
THE DEFECT, MEASURED — `15-REVIEW.md` § WR-04, quoting its own probe: feeding `renderEmail` a
paragraph containing a `code`-wrapped command, the HTML part contains the literal open-tag → FALSE,
and contains the entity-escaped form → TRUE.

WHY IT CANNOT FAIL. `renderOpsAlertDigest` (`src/lib/email.ts:669-712`) passes its runbook sentence
through `renderEmail`'s `paragraphs` field, and paragraphs are escaped at the choke point. A re-added
wrapper therefore reaches the HTML part entity-escaped — visible tag text in an operator's inbox —
and never as the literal string the assertion looks for.

TWO PROJECTIONS, TWO FAILABLE FORMS, ONE REGRESSION. `renderOpsAlertDigest` builds ONE `paragraphs`
array and `renderEmail` produces both parts from it: the HTML part is escaped, the text/plain twin
takes the RAW string. So a re-added wrapper appears entity-escaped in `html` AND literal in `text`.
Both are assertable and both fail on the same mutation; the current single assertion checks the one
place the string provably cannot be.

THE POSITIVE CONTROL THAT IS ALREADY THERE, one line above: the assertion that the angle-bracketed
audit-id placeholder arrives entity-escaped in the HTML part. The placeholder is written RAW in the
source, so that line is the proof the entity form is reachable in this projection — which is what
makes an entity-form absence check meaningful rather than merely differently spelled. Keep it, and
say in the comment that it is the control for the line below it.

THE FULL RAW-TAG ASSERTION SET, measured 2026-08-25 by grepping `tests/` for a `not.toContain` whose
argument opens with an angle bracket — 8 assertions in 5 files, the entire audit surface. A listable
set, not a sweep:
  tests/auth/email-escaping.test.ts:21        a script element with a payload
  tests/auth/email-injection.test.ts:334      a script open-tag
  tests/notifications/guest-email.test.ts:53  a script element with a payload
  tests/notifications/notify.test.ts:612      a script element with a payload
  tests/ops/alert-digest.test.ts:296          a script open-tag
  tests/ops/alert-digest.test.ts:402          a table-cell open-tag — asserted on `text`
  tests/ops/alert-digest.test.ts:403          a table open-tag — asserted on `text`
  tests/ops/alert-digest.test.ts:431          the code open-tag — THE DEFECT
The distinction the audit turns on: an assertion about INJECTED DATA (a script tag arriving in a
user-supplied field) is failable, because escaping is the thing under test and a broken escape puts
the literal string there. An assertion about AUTHORED MARKUP entering an already-escaped slot is not,
because the literal form is unreachable by construction. Classify each by that rule, with the reason,
BY MEASUREMENT — not by reading the list above and agreeing with it.

AUTHUI-01's EVIDENCE, as the verifier recorded it (`15-VERIFICATION.md` Truth #1) — the four things
Task 2 re-checks rather than re-derives:
  • `tests/design/auth-composition.test.tsx` (877 lines, build-blocking via `test:design` inside
    `npm run build`), mutation-tested six ways with every red transcribed in its own header
  • all four `(auth)/*/page.tsx` import `PanelCard`
  • `(auth)/layout.tsx` and `site-chrome.tsx` share one `BRAND_CLASS` constant
  • the four CI-captured baseline PNGs (`auth-login`, `auth-signup`, `auth-forgot`, `auth-reset` at
    1280) show one coral accent-filled control per screen, one card, shared wordmark treatment and
    typography, matching `checkout-1280`; comparison run `32752143309`, `gate-visual` green

15-VALIDATION.md's row shape (copy it exactly — the table is columnar and a row that drops a column
breaks the file's own alignment):
  Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command |
  File Exists | Status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The digest assertion is made able to fail, and the raw-tag set is classified</name>

  <read_first>
    - tests/ops/alert-digest.test.ts lines 280-310 (case 6's script-tag assertion and its positive control — the shape a FAILABLE absence check has in this file) and lines 385-435 (cases 9, 10 and 11 — the two text-projection assertions, the truncation pair, and case 11 in full with its five-line comment)
    - src/lib/email.ts lines 636-715 (`renderOpsAlertDigest`'s docblock — including the "ONE STATED FORMATTING CHANGE (15-04)" paragraph that names this exact regression — the `cells` map, the `tableHtml`/`tableText` pair, and the `paragraphs` array you will mutate and revert)
    - src/lib/email.ts lines 15-40 and the `renderEmail` choke point (which fields are escaped and which single field is inserted raw — `tableHtml` — because that asymmetry is the whole reason case 11 exists)
    - .planning/phases/15-auth-profile-transactional-email/15-REVIEW.md § WR-04 (the measured probe output this task reproduces and then fixes)
    - .planning/phases/15-auth-profile-transactional-email/15-03-SUMMARY.md and 15-10-SUMMARY.md (this phase's two earlier instances of the same anti-pattern and how each was fixed, so the third fix looks like the first two rather than inventing a third style)
  </read_first>

  <files>tests/ops/alert-digest.test.ts</files>

  <action>
    **Part A — make case 11 able to fail, in both projections.**

    Keep the existing positive control (the audit-id placeholder asserted present in its entity form)
    exactly where it is, and label it in the comment as the CONTROL for what follows: it proves the
    entity form is reachable in this projection, which is what makes an entity-form absence check
    meaningful.

    Add the two assertions that can actually fail for the regression the comment names:
      • on `html`: the ENTITY-ESCAPED form of the code open-tag must be absent — that is where a
        re-added wrapper actually lands, and it is the assertion WR-04 says is missing;
      • on `text`: the LITERAL code open-tag must be absent — the plain-text twin takes the RAW
        paragraph string, so the same regression lands there unescaped. Two projections, two
        independent failures, one mutation.

    Keep the existing literal-form assertion on `html`, but REWRITE its comment so it no longer claims
    what it cannot do. What it legitimately still asserts is that no raw markup reaches the HTML part
    by some other route — a future `tableHtml`-style raw slot, say. Say that, and say plainly that it
    is NOT the line that catches a re-added wrapper. An assertion whose comment overstates it is the
    defect; the assertion itself is harmless.

    Update the case's leading comment so every sentence in it is true of the assertions beneath it, and
    add the one sentence this file has earned three times over: paragraphs are escaped at the choke
    point, so an absence check about AUTHORED markup must be written in the escaped form, or it checks
    a string that cannot exist.

    **Part B — the red, watched and transcribed (this phase's convention, not optional).**

    Apply the mutation WR-04 describes: in `renderOpsAlertDigest`'s `paragraphs` array
    (`src/lib/email.ts`), wrap the `npm run ops:alerts` command in a code element, exactly the wrapper
    15-04 removed.

    Run `npx vitest run tests/ops/alert-digest.test.ts` and transcribe BOTH facts verbatim into the
    file's own header — both halves are the finding, and the first half is the one that proves the old
    line was unfailable:
      1. the pre-existing literal-form assertion STILL PASSES under the mutation;
      2. the two new assertions FAIL, and the failure text names the entity form and the raw twin.
    Then revert the mutation and re-run to green.

    ⚠ `src/lib/email.ts` IS THE FILE EMAIL-01's WHOLE GUARANTEE IS ABOUT — not one send trigger,
    subject, recipient or call site may move. The mutation is temporary and reverted;
    `git diff --exit-code src/lib/email.ts` must exit 0 at task end and the summary must say so.

    **Part C — classify the whole raw-tag set, by measurement.**

    Take the 8 assertions listed in this plan's interfaces block, re-running the grep rather than
    trusting the list — if the count has changed, THAT is the first finding. For each, record in the
    summary: the file and line, the projection it asserts on (`html` or `text`), whether the string it
    names is REACHABLE in that projection and therefore failable, and the one-sentence reason.

    Fix only what is in this task's file. If the audit finds an unfailable assertion in one of the
    other four files, do NOT fix it here — those files are outside `files_modified`, and
    `tests/auth/email-escaping.test.ts` in particular is protected by the ROADMAP's carried constraint
    from backlog 999.1 (it must not be weakened). Log it to `deferred-items.md` in Task 3 with its
    file, line and reason.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/ops/ tests/auth/ tests/notifications/ &amp;&amp; git diff --exit-code src/lib/email.ts</automated>
  </verify>

  <acceptance_criteria>
    - The HTML-part entity-form absence assertion exists in case 11, and the text-part literal-form absence assertion exists beside it — two new assertions, two projections
    - The pre-existing audit-id positive control is still present and is named in the comment as the control for the line below it
    - The file's header transcribes the watched red with BOTH facts: the old assertion passing under the mutation, and the new ones failing, each quoted verbatim with the command that produced them
    - `git diff --exit-code src/lib/email.ts` exits 0 — the mutation is reverted, no trigger, subject, recipient or call site moved
    - `git diff --name-only src/` is EMPTY at task end
    - `npx vitest run tests/ops/alert-digest.test.ts` exits 0 and the case count is unchanged from before this task — record both numbers in the summary
    - `npx vitest run tests/auth/ tests/notifications/` exits 0 — the neighbouring escaping suites are untouched
    - `git diff --exit-code tests/auth/email-escaping.test.ts` exits 0 — the named-and-protected test is not weakened
    - The summary carries the 8-row classification table with a projection and a reason per row
  </acceptance_criteria>

  <done>The third instance of this phase's recurring unfailable-assertion pattern is fixed the way the first two were, and the whole raw-tag set is classified rather than spot-checked.</done>
</task>

<task type="auto">
  <name>Task 2: AUTHUI-01 claimed by citing evidence that is re-checked, not rebuilt</name>

  <read_first>
    - .planning/phases/15-auth-profile-transactional-email/15-VERIFICATION.md lines 43-53 and 103-117 (Truth #1's evidence, and the Requirements Coverage row recording the process gap — 15-09 expected 15-11 to close it, 15-11 declined)
    - .planning/phases/15-auth-profile-transactional-email/15-11-SUMMARY.md (the sentence declining AUTHUI-01: it is "outside this plan's requirements… and is untouched here" — the hand-off that never landed)
    - tests/design/auth-composition.test.tsx lines 1-200 (the file's own header: the four-link chain and the six transcribed mutation reds — the evidence you are re-checking, not re-deriving)
    - .planning/REQUIREMENTS.md lines 84-95 and 220-232 (AUTHUI-01's text, and the traceability table this plan does NOT edit)
    - .planning/ROADMAP.md § Phase 15 Success Criteria (SC #1 — the conjunction AUTHUI-01 and AUTHUI-03 split between them)
  </read_first>

  <files>(no source or test file is modified — this task produces measured evidence and the summary's claim)</files>

  <action>
    Re-run and re-measure AUTHUI-01's four pieces of evidence against the tree AS IT STANDS TODAY.
    Cite, do not rebuild. Record each result in the summary as a table with the command and its output.

      1. **The gate.** `npx vitest run tests/design/auth-composition.test.tsx --config
         vitest.design.config.ts` — green, with the case count recorded. Then `npm run test:design`
         and `npm run build` — green, which is what makes the gate build-blocking rather than merely
         present.
      2. **Component reuse.** All four `(auth)/*/page.tsx` import `PanelCard`; zero raw Card elements
         survive on any of them; `(auth)/layout.tsx` and `src/components/patterns/site-chrome.tsx`
         both reference the one `BRAND_CLASS` constant — grep BOTH sides, not one.
      3. **The committed baselines.** `git ls-files` over the visual snapshot directory's `auth-*`
         pattern returns exactly 8 files. Name them in the summary.
      4. **The CI comparison run.** `gh run view 32752143309` — all four jobs green, `gate-visual`
         green. If `gh` is unavailable or unauthenticated here, say so plainly and record the run id
         as cited-from-`15-VERIFICATION.md` rather than re-fetched; an honest provenance note is worth
         more than a re-run you cannot actually perform.

    Then state the claim in the summary explicitly: AUTHUI-01 is CLOSED, on this evidence, by this
    plan — and name why no plan claimed it before (15-09's hand-off, 15-11's decline). Set
    `requirements-completed: [AUTHUI-01]` in the summary's frontmatter. That frontmatter line is the
    artifact that closes the traceability gap; everything above is what makes it true.

    ⚠ IF ANY OF THE FOUR CHECKS FAILS, STOP AND REPORT. Do not repair, do not restyle, do not
    regenerate a baseline. A failing check means the verifier's evidence has decayed since 2026-08-25,
    which is a finding for the re-verification pass and a different plan's work. Say which check
    failed, with its output, and leave `requirements-completed` empty.

    ⚠ DO NOT EDIT `.planning/REQUIREMENTS.md`. Ticking a requirement's checkbox and its traceability
    row is the verification pass's act, not a plan's; this phase has a recorded history of those two
    drifting apart, and the fix is one writer, not two.

    The PM glance recorded in `15-VERIFICATION.md` § Human Verification Required #2 — confirm the four
    auth screens read as the same product as the booking/checkout flow — stays an OPEN human item and
    is NOT a blocker for this claim: the verifier already performed that inspection directly on the
    four CI-captured PNGs against a same-session `checkout-1280` and found it convincing. Note in the
    summary that the item remains open for the re-verification pass, so it is carried rather than
    quietly dropped.
  </action>

  <verify>
    <automated>npx vitest run tests/design/auth-composition.test.tsx --config vitest.design.config.ts &amp;&amp; npm run build</automated>
    <human-check>Optional, non-blocking: open /login, /signup, /forgot-password and /reset-password (or the four committed auth baseline PNGs) and confirm they read as the same product as the booking/checkout flow. Carried forward as an open human-verification item either way.</human-check>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/design/auth-composition.test.tsx --config vitest.design.config.ts` exits 0, with the case count recorded in the summary
    - `npm run build` exits 0
    - All four `src/app/(auth)/*/page.tsx` files contain `PanelCard`, and a grep for a raw Card element returns 0 for each
    - `grep -c "BRAND_CLASS" 'src/app/(auth)/layout.tsx'` returns at least 1 AND `grep -c "BRAND_CLASS" src/components/patterns/site-chrome.tsx` returns at least 1 — both sides of the shared constant
    - `git ls-files` over the visual snapshot directory's `auth-*` pattern returns exactly 8 paths, named in the summary
    - The summary carries a citation table: four evidence rows, each with its command and its result
    - The summary's frontmatter carries `requirements-completed: [AUTHUI-01]` — or, if a check failed, an empty list and a stated finding
    - `git diff --exit-code .planning/REQUIREMENTS.md` exits 0 — the checkbox and traceability row are untouched by this plan
    - `git diff --name-only src/ tests/ e2e/` shows nothing attributable to this task — nothing was rebuilt
  </acceptance_criteria>

  <done>AUTHUI-01 is claimed by exactly one plan, on evidence that was re-checked today rather than quoted from a report.</done>
</task>

<task type="auto">
  <name>Task 3: The validation table maps every clause AUTHUI-03 actually asserts</name>

  <read_first>
    - .planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md (the whole file — the per-task table's exact ten columns, the Wave 0 list, the Manual-Only Verifications table, the sign-off block, and the post-execution note at the end that names this very gap)
    - .planning/phases/15-auth-profile-transactional-email/15-12-SUMMARY.md (plan 15-12's real commands, results and stop counts — the row you write records what happened, not what was planned)
    - .planning/phases/15-auth-profile-transactional-email/15-13-SUMMARY.md (plan 15-13's real commands and results, including whether a `contrast-pairs.ts` row was needed)
    - .planning/phases/15-auth-profile-transactional-email/deferred-items.md (the "[15-11] AUTHUI-03's keyboard and AA clauses are not evidenced" entry you are about to close, and the file's per-entry format: file, what is wrong, why deferred, what fixing it looks like)
    - .planning/phases/15-auth-profile-transactional-email/15-VERIFICATION.md § Gaps Summary (so the closure notes name the gaps the way the verifier did)
  </read_first>

  <files>.planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md, .planning/phases/15-auth-profile-transactional-email/deferred-items.md</files>

  <action>
    **Part A — the missing rows.**

    Append to 15-VALIDATION.md's Per-Task Verification Map, in the existing ten-column shape and in
    task-id order, one row per task of plans 15-12, 15-13 and 15-14. Fill every column from the plans'
    SUMMARYs — real commands, real statuses — never from the plans' intentions:

      15-12-01 · plan 12 · wave 6 · AUTHUI-03 (keyboard) · threat — · the wordmark-first focus
        sequence over six auth documents · e2e ·
        `npx playwright test e2e/auth-keyboard.spec.ts --project=chromium`
      15-12-02 · plan 12 · wave 6 · AUTHUI-03 (keyboard) · T-15-25 · the reset token input is
        unreachable forward and backward, and every stop paints a visible indicator · e2e · the same
        command
      15-13-01 · plan 13 · wave 6 · AUTHUI-03 (AA) · threat — · every rendered ink-on-ground pair
        measured in both themes · design ·
        `npx vitest run tests/design/auth-contrast.test.ts --config vitest.design.config.ts`
      15-13-02 · plan 13 · wave 6 · AUTHUI-03 (AA) · T-15-29 · an undeclared auth pairing turns the
        build red · design · `npm run build`
      15-14-01 · plan 14 · wave 7 · EMAIL-01 · threat — · the digest's authored-markup check can fail
        in both projections · unit · `npx vitest run tests/ops/alert-digest.test.ts`
      15-14-02 · plan 14 · wave 7 · AUTHUI-01 · threat — · the composition gate is build-blocking and
        the eight auth baselines are committed · design + CLI · `npm run build`

    Mark each row's `File Exists` and `Status` from the ACTUAL runs. Do not write a green status for
    anything you did not watch exit 0.

    **Part B — close the post-execution note in place, without erasing it.**

    The note at the end of the file is the phase's honest self-assessment and it stays. Append a dated
    closure block beneath it stating: which two clauses were unsampled, which rows now sample them,
    which plans produced those rows, and the one sentence that matters for the next phase — that
    ticking on "every mapped row is green" is only sound when the map covers every clause of a
    conjunctive requirement, which is why this table gained rows rather than the requirement gaining a
    tick. Do not rewrite or soften the original note: a corrected record that hides what it corrected
    is worth less than the mistake it fixes.

    Update the sign-off block's sampling-continuity line only if the new rows change it — they should
    not, since every new task carries an automated command — and say which it was.

    **Part C — deferred-items.md.**

    Mark the "[15-11] AUTHUI-03's keyboard and AA clauses are not evidenced" entry CLOSED, dated, with
    the two plans and the two artifacts that closed it (`e2e/auth-keyboard.spec.ts` and
    `tests/design/auth-contrast.test.ts`) and the measured facts each produced. Keep the entry's body:
    a deferred item that gets closed is a record of a decision, not a paragraph to delete.

    Append any NEW deferred item this wave produced, each in the file's existing shape. Candidates
    observed while planning, to include only if the executing plans actually confirmed them:
      • the signup intent pair's two independently-tabbable radio buttons (no roving tabindex) — WCAG
        2.1.1 is satisfied, WAI-ARIA authoring practice would prefer one stop plus arrow keys, and
        15-07 froze that markup byte-identical, so it is a future call and not this wave's;
      • any unfailable raw-tag assertion Task 1's audit found in a file outside its scope.

    Do NOT touch the entries the scope fence protects: the stale `visual-baselines.ts` head paragraph,
    the `EXPECTED_BLOCKED` docblock's count, the ten re-minted baselines, and anything concerning
    `src/lib/email.ts:54-55` (CR-01) — that last is a pre-existing Critical on the money path and
    belongs in the project backlog, not in a Phase-15 gap plan.
  </action>

  <verify>
    <automated>grep -c "15-12-01" .planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - 15-VALIDATION.md's Per-Task Verification Map contains rows `15-12-01`, `15-12-02`, `15-13-01`, `15-13-02`, `15-14-01` and `15-14-02`, each with all ten columns filled
    - At least one row names AUTHUI-03 with the keyboard clause and at least one names it with the AA clause — the two clauses the table never mapped
    - `grep -c "auth-keyboard.spec.ts" .planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md` returns at least 1, and the same for `auth-contrast.test.ts`
    - `grep -c "T-15-25" .planning/phases/15-auth-profile-transactional-email/15-VALIDATION.md` returns at least 1 — the threat reference travels with its row
    - Every new row's Status is taken from a run that was actually watched; no row claims green for an unrun command, and the summary says so explicitly
    - The original post-execution note is byte-identical and a dated closure block is appended beneath it — confirm with `git diff` that no line of the original note changed
    - `deferred-items.md`'s AUTHUI-03 keyboard/AA entry is marked CLOSED with a date, the two plan numbers and the two artifact paths, and its original body survives
    - `git diff --name-only .planning/` lists only 15-VALIDATION.md, deferred-items.md and this plan's summary
    - `git diff --exit-code .planning/REQUIREMENTS.md .planning/ROADMAP.md` exits 0 — requirement ticking is the verification pass's act, not a plan's
  </acceptance_criteria>

  <done>AUTHUI-03's validation map covers all five clauses of its own conjunctive text, and the phase's honest self-assessment is closed by the work that answered it rather than edited away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator alert content → the operator's inbox | `renderOpsAlertDigest` composes free-text audit fields and authored runbook prose into one document; the escaping choke point is the boundary, and this plan's assertion is what watches it |
| a temporary mutation → `src/lib/email.ts` | The money path's send layer is mutated for one run and reverted; the boundary is the revert, and it is asserted rather than assumed |
| a report's sentence → a requirement's tick | AUTHUI-01 is claimed from re-run commands, not from quoting `15-VERIFICATION.md` back at itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-31 | Repudiation | An operator digest assertion that cannot fail for the regression it names | mitigate | The check is rewritten into both projections — entity-escaped in `html`, literal in `text` — and proven able to fail by applying WR-04's exact mutation and transcribing both facts: the old line passing, the new lines failing |
| T-15-32 | Tampering | A temporary mutation left in `src/lib/email.ts` | mitigate | `git diff --exit-code src/lib/email.ts` in Task 1's automated verify AND in its acceptance criteria, plus `git diff --name-only src/` empty at task end. EMAIL-01's guarantee is that no trigger, subject, recipient or call site moved; a stranded mutation would break it silently |
| T-15-33 | Tampering | A requirement ticked on evidence that has decayed | mitigate | AUTHUI-01's four evidence pieces are RE-RUN (gate, greps, `git ls-files`, CI run), not cited; any failing check stops the claim and reports instead of repairing |
| T-15-37 | Information disclosure | Weakening an escaping test while editing its neighbours | mitigate | `git diff --exit-code tests/auth/email-escaping.test.ts` — the ROADMAP's carried constraint from backlog 999.1 names that file specifically; the audit classifies its assertion but this plan does not edit it |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. `15-RESEARCH.md § Package Legitimacy Audit` records the gate as not applicable for this phase, and this plan adds no dependency |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/ops/ tests/auth/ tests/notifications/` exits 0.
- `npm run build` exits 0 — the AUTHUI-01 citation's build-blocking claim is a command, not a memory.
- `git diff --exit-code src/lib/email.ts` exits 0 — the mutation is reverted and EMAIL-01's
  no-trigger-moved guarantee is intact.
- `git diff --exit-code tests/auth/email-escaping.test.ts .planning/REQUIREMENTS.md .planning/ROADMAP.md`
  exits 0.
- `git diff --name-only` at plan end lists only the three files in `files_modified` plus the summary.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never
concurrently. `npm run build` chains `test:design`, so do not run it alongside `npm test` either.
</verification>

<success_criteria>
- The phase's third unfailable assertion is failable, in both projections, with the red transcribed —
  and the whole 8-assertion raw-tag set is classified rather than spot-checked.
- AUTHUI-01 is claimed by exactly one plan's frontmatter, on evidence re-measured today, with no new
  implementation and no requirement checkbox edited.
- 15-VALIDATION.md samples all five clauses AUTHUI-03 actually asserts, and its post-execution note is
  closed by the work rather than erased.
- `src/` is byte-identical at plan end.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-14-SUMMARY.md` when done.
Set `requirements-completed: [AUTHUI-01]` (Task 2's claim) and `requirements-advanced: [EMAIL-01,
AUTHUI-03]`. Do NOT tick AUTHUI-03 here: its five clauses are now all sampled, but the requirement's
closure is the re-verification pass's call, on the evidence plans 15-12 and 15-13 produced.
</output>
