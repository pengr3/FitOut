---
phase: 15-auth-profile-transactional-email
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/patterns/panel-card.tsx
  - src/components/patterns/site-chrome.tsx
  - src/app/(auth)/layout.tsx
  - tests/design/loading-coverage.test.ts
autonomous: true
requirements: [AUTHUI-01, AUTHUI-03]

must_haves:
  truths:
    - "The four auth documents share one composition: a wordmark above one card on a quiet ground, inside exactly one main landmark (15-CONTEXT D-162)."
    - "The wordmark treatment has one owner — the auth layout and the site chrome read the same constant, so the two surfaces cannot drift."
    - "A PanelCard title may render as the document's h1 without any shipped call site changing what it renders."
    - "The main landmark is present in every auth state, page and error boundary alike."
    - "The layout is still not the security boundary, and its note saying so survives verbatim."
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "the dedicated auth composition — main, quiet ground, wordmark, column, footer"
      contains: "BRAND_CLASS"
    - path: "src/components/patterns/site-chrome.tsx"
      provides: "BRAND_CLASS exported"
      exports: ["BRAND_CLASS"]
  key_links:
    - from: "src/app/(auth)/layout.tsx"
      to: "src/components/patterns/site-chrome.tsx"
      via: "import { BRAND_CLASS } — one identity, two surfaces"
      pattern: "BRAND_CLASS"
    - from: "src/components/patterns/panel-card.tsx"
      to: "the four auth pages"
      via: "titleAs widened to admit h1 while defaulting to h2"
      pattern: "\"h1\" \\| \"h2\" \\| \"h3\""
---

<objective>
Put the two enablers and the composition in place: export the wordmark's one class constant, widen
`PanelCard`'s heading-level union by exactly one member, and rewrite `(auth)/layout.tsx` into the
dedicated auth composition 15-CONTEXT D-162 selected.

Purpose: D-162 chose one centered card on a quiet background with the wordmark above it. That puts the
wordmark in the LAYOUT, so it is present in every state including the error boundary — and it makes a
second wordmark in a page header the exact duplication SHELL-01 removed. The `<main>` landmark belongs
in the layout for the same reason: `(auth)/error.tsx`'s own header records that it could not add one,
because a landmark present only in the failure state encodes an error. Putting it in the layout
dissolves that caveat.

Output: three source edits and one prose correction to a test whose measurement this phase invalidates.
Everything downstream in the app half depends on these.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-RESEARCH.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md

<interfaces>
From `src/components/patterns/panel-card.tsx`:
  export type PanelCardProps = {
    title?: string;
    titleAs?: "h2" | "h3";        // ← widens to "h1" | "h2" | "h3"; default stays "h2"
    description?: string;
    footer?: ReactNode;
    sticky?: boolean;
    tone?: "default" | "muted";
    children?: ReactNode;
  }
  Its CardContent already carries `space-y-4 p-4 sm:p-6` — call sites add NO padding and NO space-y.

From `src/components/patterns/site-chrome.tsx:95`:
  const BRAND_CLASS = "text-lg font-semibold tracking-tight"    // currently NOT exported

The shipped `(auth)/layout.tsx` (45 lines) renders:
  <div className="flex min-h-dvh flex-col">
    <PublicHeader />
    <div className="flex flex-1 flex-col items-center justify-center bg-muted px-4 py-12">
      <div className="w-full max-w-sm">{children}</div>
    </div>
    <SiteFooter />                                    ← SHELL-02, kept
  </div>

The target composition (15-UI-SPEC § The layout):
  the outer flex column keeps its classes; `PublicHeader` is deleted; the centering div becomes
  `<main>` with the same class list; the inner column gains `space-y-6` and, above `{children}`,
  a `Link` to `/` carrying `BRAND_CLASS` plus block/centre classes, with the text `FitOut`;
  `SiteFooter` stays.

From `src/lib/design/selector-contract.ts`: `SELECTOR_IDS` already declares `panel-card` and
`site-brand`. 15-UI-SPEC pins `SELECTOR_IDS` at **+0** for this phase — the auth wordmark takes NO
`data-testid`, because `site-brand` belongs to the chrome.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the wordmark constant and widen the heading-level union</name>

  <read_first>
    - src/components/patterns/site-chrome.tsx lines 85-200 (the `BRAND_CLASS` declaration at :95 and the two-branch wordmark render at :180-190 whose comment explains why both branches read the constant)
    - src/components/patterns/panel-card.tsx lines 48-110 (the props type and the `titleAs` docblock at :59-78) and lines 185-205 (the render, so you can see the default resolution)
    - src/lib/design/measurements.ts lines 376-402 (the "promoted out of its call sites" idiom — the register for a constant that gains an export)
    - tests/design/type-scale.test.ts (confirm the wordmark's classes are already inside a declared role and that exporting the constant moves nothing)
  </read_first>

  <files>src/components/patterns/site-chrome.tsx, src/components/patterns/panel-card.tsx</files>

  <action>
    In `site-chrome.tsx`, add `export` to the `BRAND_CLASS` declaration at line 95. That is the whole
    edit — the value, the two render branches and the comment beside them stay byte-identical. Append
    one sentence to the constant's own comment recording why it is now exported: the auth layout renders
    the same wordmark on a different surface, and 15-UI-SPEC's "one identity, two surfaces" claim is
    falsifiable only if both read one constant. Follow `measurements.ts:376-402`'s register for a
    constant promoted out of its call sites.

    In `panel-card.tsx`, widen `titleAs` from `"h2" | "h3"` to `"h1" | "h2" | "h3"`. **The default stays
    `"h2"`**, so every shipped call site renders byte-identically. Append a sentence to the existing
    `titleAs` docblock — the one that already argues "the level is the surface's decision" — recording
    the Phase-15 reason: an auth card IS the document, so its title is that document's `<h1>`. This is
    the same one-union-member extension plan 14-12 made in the other direction, and the docblock already
    carries the argument for it; extend that argument rather than writing a new one.

    Do not touch anything under `src/components/ui/**`. Do not add a prop, a variant, a class or a
    testid. Two one-line edits and two appended sentences.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'export const BRAND_CLASS' src/components/patterns/site-chrome.tsx` returns 1
    - `grep -c 'text-lg font-semibold tracking-tight' src/components/patterns/site-chrome.tsx` is unchanged from its pre-task value — the class string was not edited
    - `grep -c '"h1" | "h2" | "h3"' src/components/patterns/panel-card.tsx` returns 1
    - `panel-card.tsx` still resolves `titleAs` to `"h2"` when the prop is absent
    - `git diff --stat src/components/ui/` shows no files — no vendored primitive was hand-edited
    - `npm run test:design` exits 0
    - `npx tsc --noEmit` exits 0
    - `git diff --name-only` lists exactly the two files
  </acceptance_criteria>

  <done>Two constants are reachable by the surfaces that need them, and every shipped call site still renders exactly what it rendered before.</done>
</task>

<task type="auto">
  <name>Task 2: The auth composition</name>

  <read_first>
    - src/app/(auth)/layout.tsx (the whole 45-line file — the three comment blocks at :1-25 and the render at :30-45; two of those blocks are load-bearing and must survive)
    - src/components/patterns/site-chrome.tsx lines 178-192 (the wordmark render whose Link branch this copies)
    - src/app/(auth)/error.tsx (its header records why it could not add a landmark — that caveat is what this task dissolves)
    - src/lib/design/selector-contract.ts (confirm for yourself that `site-brand` belongs to the chrome; the auth wordmark takes no testid)
    - tests/design/loading-coverage.test.ts lines 15-40 (the measured paragraph about the four `(auth)` routes building `ƒ Dynamic` because of `PublicHeader` — this task invalidates it)
    - e2e/password-reset.spec.ts and e2e/login-persistence.spec.ts (the accessible names and sentences that must survive — read them so you can predict what would break)
  </read_first>

  <files>src/app/(auth)/layout.tsx, tests/design/loading-coverage.test.ts</files>

  <action>
    Rewrite `(auth)/layout.tsx`'s render into the composition this plan's `<interfaces>` block gives.
    Concretely: delete the `PublicHeader` import and its element; change the centering `<div>` to a
    `<main>` keeping its class list byte-identical (`flex flex-1 flex-col items-center justify-center
    bg-muted px-4 py-12`); add `space-y-6` to the inner column so the wordmark sits 24px above the card;
    and render, above `{children}`, a `Link` to `/` whose className is the imported `BRAND_CLASS`
    combined with block and centre classes, carrying the text `FitOut`. Keep `SiteFooter` and its
    SHELL-02 comment exactly as they are. Add **no** `data-testid` — 15-UI-SPEC pins `SELECTOR_IDS` at
    +0 and `site-brand` belongs to the chrome, so copying it here would put one id on two unrelated
    elements.

    Three things about the comments, and they are not decoration:
    - The security note at :5-7 ("this layout is purely presentational; the real auth gate is per-page
      `getSession()` and the optimistic redirect in `src/middleware.ts`") stays **verbatim**. It is
      GATE-NOREG #4's written half, and removing the header moves no security boundary.
    - The SHELL-01 block at :16-25 already says "Phase 15 may decide otherwise with the whole surface in
      front of it." Do not delete it. Cite it and record that this is that decision, taken, with D-162's
      reason: the wordmark goes above the card, and a second wordmark in a header is byte-for-byte the
      duplication SHELL-01 removed. Record the accepted tradeoff the checker verified: a signed-in
      visitor on `/forgot-password` or `/reset-password` — an intentionally reachable path per
      `src/middleware.ts` — loses the header's mode switch, notification bell and profile link, and
      routes wordmark → home to reach them. No route is orphaned.
    - The colour block at :9-14 stays; `bg-muted` is unchanged and is now also the email's outer-ground
      figure.

    The wordmark is the first tabbable element on the page and keeps the browser-default focus
    indicator — the DS-05 recipe is for controls that override their outline, and this link overrides
    nothing.

    Then measure the predicted build-output flip and correct the one now-false paragraph. With the
    header's session read gone, the four auth routes may flip from `ƒ Dynamic` to `○ Static`. Run
    `npm run build` and read the four route markers. `tests/design/loading-coverage.test.ts:15-40`
    contains a MEASURED claim that all four build `ƒ Dynamic` **because** the layout renders
    `PublicHeader`. Rewrite that paragraph with the newly measured markers quoted verbatim, dated, and
    attributed to this plan. **Change no count and no assertion**: the gate stays at 29/21/8, no route
    is added, and the `async default export ⇒ ƒ Dynamic` rule and its "do not simplify this gate to read
    the manifest" instruction survive word for word. The edit is confined to the invalidated
    measurement, because a stale measured claim in a gate's own header is the thing this repository
    treats as a defect. Record the correction in the summary.

    Do not edit `(auth)/error.tsx`, `PublicHeader`, `SiteChrome`, `SiteFooter`, or any page under
    `(auth)/`. The four pages are plan 15-07's.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'PublicHeader' 'src/app/(auth)/layout.tsx'` returns 0
    - `grep -c '<main' 'src/app/(auth)/layout.tsx'` returns 1
    - `grep -c 'BRAND_CLASS' 'src/app/(auth)/layout.tsx'` returns 1
    - `grep -c 'data-testid' 'src/app/(auth)/layout.tsx'` returns 0
    - `grep -c 'bg-muted' 'src/app/(auth)/layout.tsx'` returns 1 and `grep -c 'SiteFooter' 'src/app/(auth)/layout.tsx'` returns at least 1
    - `grep -c 'purely presentational' 'src/app/(auth)/layout.tsx'` returns 1 — the security note survives
    - `grep -c 'SHELL-01' 'src/app/(auth)/layout.tsx'` returns at least 1 — the removal precedent block survives
    - `npm run build` exits 0, and the four `(auth)` route markers it prints are quoted verbatim in the summary
    - `git diff tests/design/loading-coverage.test.ts` shows changes only inside a comment block — no assertion, no count, no array element moved
    - `npx vitest run tests/design/loading-coverage.test.ts --config vitest.design.config.ts` exits 0 and reports the same 29/21/8
    - `npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts` exits 0 with both spec files unedited (`git diff --exit-code e2e/password-reset.spec.ts e2e/login-persistence.spec.ts`)
  </acceptance_criteria>

  <done>One composition, one landmark in every state, one wordmark whose class has one owner — and the one measured claim this change invalidated has been re-measured rather than left standing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → the auth routes | The layout is presentational; the real gate is per-page `getSession()` plus `src/middleware.ts` |
| signed-in visitor → `/forgot-password`, `/reset-password` | Intentionally reachable per middleware, for a user resetting from a verified-but-stale device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-18 | Elevation of privilege | Removing `PublicHeader` mistaken for removing a gate | mitigate | The layout was never the boundary; its own note saying so is preserved verbatim and asserted by a grep. Per-page `getSession()` and `src/middleware.ts` are not edited by this plan |
| T-15-19 | Information disclosure | A duplicated `data-testid` making a container query resolve on the wrong element | mitigate | The auth wordmark takes no testid; `SELECTOR_IDS` stays at +0 |
| T-15-20 | Tampering | An auth e2e flow silently broken by a layout rewrite | mitigate | `e2e/password-reset.spec.ts` and `e2e/login-persistence.spec.ts` run in this plan's verify and are asserted unedited |
| T-15-21 | Repudiation | A gate's header carrying a measurement this change invalidated | mitigate | The `loading-coverage` paragraph is re-measured against a real `npm run build` and the new markers quoted; counts and assertions byte-identical |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` exits 0 (this is `lint && test:design && next build`).
- `npx playwright test e2e/password-reset.spec.ts e2e/login-persistence.spec.ts` exits 0.
- `git diff --exit-code e2e/password-reset.spec.ts e2e/login-persistence.spec.ts 'src/app/(auth)/error.tsx' src/components/site/public-header.tsx src/components/patterns/site-footer.tsx` exits 0.
- `git diff --name-only` at plan end lists only the four files in `files_modified`.

⚠ The `auth-login` visual baseline's hook (`[data-testid="site-header"]`) is stale from the moment this
plan lands. That is expected and is plan 15-11's to fix. The Playwright `visual` project is not
constructed off Linux, so nothing on this machine will report it — do not "fix" it here.
</verification>

<success_criteria>
- Four auth documents, one composition, one `main` landmark each, in every state.
- One wordmark class constant with two readers.
- `PanelCard` can render an `h1` and every existing call site is byte-identical.
- The two auth e2e specs pass unmodified.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-06-SUMMARY.md` when done.
</output>
