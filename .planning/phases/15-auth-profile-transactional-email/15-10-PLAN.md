---
phase: 15-auth-profile-transactional-email
plan: 10
type: execute
wave: 4
depends_on: [15-07, 15-08]
files_modified:
  - tests/design/profile-pass.test.tsx
  - e2e/overflow-320.spec.ts
autonomous: true
requirements: [AUTHUI-02, AUTHUI-03]

must_haves:
  truths:
    - "A build-blocking gate proves /profile renders one h1 at the adopted scale, exactly two panel containers, zero accent fills and zero destructive variants."
    - "A build-blocking gate proves every shipped profile sentence is byte-identical, by string-literal comparison over the source rather than by review."
    - "A build-blocking gate proves the save path carries no timer, so a saved notice can only come from an actual result."
    - "The four auth routes are measured at 320px and nothing on them is wider than the viewport, in every state."
    - "Where a route cannot be reached by the anonymous harness, the row says so in the string a skipped run prints — a silent skip is not a skip."
  artifacts:
    - path: "tests/design/profile-pass.test.tsx"
      provides: "the AUTHUI-02 design-pass gate, jsdom + AST"
      min_lines: 150
    - path: "e2e/overflow-320.spec.ts"
      provides: "ROUTES extended with the four auth routes and the profile row"
      contains: "auth"
  key_links:
    - from: "tests/design/profile-pass.test.tsx"
      to: "src/app/(app)/profile/"
      via: "AST scan of page, plate and form plus a DOM read of the pattern's testid"
      pattern: "profile"
    - from: "e2e/overflow-320.spec.ts"
      to: "the four auth routes"
      via: "declared rows whose tell is the panel-card the auth composition renders"
      pattern: "panel-card"
---

<objective>
Make AUTHUI-02's falsifiable set falsifiable, and put the four auth routes under the 320px gate.

Purpose: 15-UI-SPEC's profile claims are precise — one `<h1>` at the adopted 20px, exactly two
`panel-card` containers, zero accent, zero destructive, every sentence byte-identical, and a save path
with no timer — and every one of them is prose until something reads the tree and counts. Separately,
AUTHUI-03's first gate is `scrollWidth <= clientWidth` at 320px on all four auth routes in all states,
and `e2e/overflow-320.spec.ts` currently measures twelve routes, none of which is an auth form.

Output: one jsdom + AST design gate and an extended e2e route table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-auth-profile-transactional-email/15-UI-SPEC.md
@.planning/phases/15-auth-profile-transactional-email/15-PATTERNS.md
@.planning/phases/15-auth-profile-transactional-email/15-07-SUMMARY.md
@.planning/phases/15-auth-profile-transactional-email/15-08-SUMMARY.md

<interfaces>
From `e2e/overflow-320.spec.ts`:
  type RouteRow = {
    readonly name: string;
    readonly path: ((page: Page) => Promise<string | null>) | string | null;
    readonly skip?: string;      // REQUIRED when `path` is null — "a silent skip is not a skip"
    readonly tell: string;       // a selector only THIS route produces; every width assertion is
                                 // true of a blank page, a 404 and a redirect to /login
    readonly served?: boolean;
    readonly open?: (page: Page) => Promise<void>;   // runs AFTER goto, BEFORE measurement
  }
  `const ROUTES: readonly RouteRow[]` — its docblock currently says "THE TWELVE ROUTES `11-UI-SPEC
  § Responsive Baseline` names, in its order. Eight are reachable ... four are not."
  A resolver-shaped `path` already exists in the table (`firstListingPath`), so a row whose path is
  computed by driving the browser is a supported shape, not a new mechanism.

15-UI-SPEC's AUTHUI-02 falsifiable set (AC#9-12):
  one PageHeader h1 at 20px · exactly two `panel-card` containers · page and plate share BOOKING_SHELL
  via one import · zero accent fills · zero `variant="destructive"` · zero success tokens ·
  every shipped sentence byte-identical (AST string-literal comparison over `profile/**`) ·
  save-state saved-from-result, cleared on next submit, zero `setTimeout` on the save path ·
  the status region carries `aria-label="Save state"` · the avatar block gains no removal control
  and loses no upload capability.

⚠ `src/app/(app)/profile/page.tsx` is an async Server Component and cannot be rendered in jsdom.
`profile-form.tsx` is `"use client"` but imports server actions, which may not resolve under the
design config — measure that rather than assume it, and fall back as this plan's Task 1 instructs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The AUTHUI-02 design-pass gate</name>

  <behavior>
    - `page.tsx` composes `PageHeader` and `BOOKING_SHELL`, renders no `text-2xl` heading and no `main`.
    - `loading.tsx` composes the same `BOOKING_SHELL` import, the same `PageHeader`, and exactly two `PanelSkeleton` elements.
    - `profile-form.tsx` composes exactly two `PanelCard` elements, both with `titleAs="h2"`, and zero raw `Card` elements — aliased imports resolved.
    - `profile-form.tsx` contains zero `variant="brand"`, zero `variant="destructive"` and zero success-token class references.
    - `profile-form.tsx` contains zero `setTimeout`.
    - Every shipped sentence in the profile tree is present byte-for-byte, compared as string literals collected from the AST.
    - Rendering `PanelCard` emits the declared testid and that id is a member of `SELECTOR_IDS`.
    - A deliberately violating module fixture is caught by every scanner the file claims.
  </behavior>

  <read_first>
    - tests/design/cancel-page-shell.test.tsx (the whole file — the four-link chain, the `(path, text)` scanner signature, the guard-the-guard block, the violating fixture, the disarmed-tripwire idiom)
    - src/app/(app)/profile/page.tsx, loading.tsx and profile-form.tsx as converted by plan 15-08 (assert against what they actually render)
    - src/components/patterns/page-header.tsx and panel-skeleton.tsx (what each renders, so the DOM link is real)
    - src/lib/design/selector-contract.ts (`SELECTOR_ATTRIBUTE`, `SELECTOR_IDS`)
    - src/lib/design/measurements.ts (the `BOOKING_SHELL` declaration — the gate asserts the IMPORT, not the string, so the constant stays the owner)
  </read_first>

  <files>tests/design/profile-pass.test.tsx</files>

  <action>
    Create `tests/design/profile-pass.test.tsx` on the same model as `tests/design/cancel-page-shell.test.tsx`
    and the sibling `auth-composition.test.tsx` plan 15-09 wrote. Open with the
    `// @vitest-environment jsdom` pragma. Reuse the same scanner shape — `(path, text)`, aliased imports
    resolved through `propertyName ?? name` — rather than inventing a second one.

    **Guard-the-guard first** over all three profile files: a length floor per file and a JSX-element
    floor per scan. Every assertion below is a count or an absence.

    Assert, by AST:
    - `page.tsx` imports `BOOKING_SHELL` from the measurements module and `PageHeader` from the pattern
      layer; renders exactly one `PageHeader`; renders zero `main` elements; contains no `text-2xl`
      class literal and no hand-typed `mx-auto w-full max-w-2xl` literal.
    - `loading.tsx` imports the same `BOOKING_SHELL` — assert the import, not the string, so the
      constant remains the owner — renders exactly one `PageHeader` with no `lede` attribute, and
      exactly two `PanelSkeleton` elements.
    - `profile-form.tsx` renders exactly two `PanelCard` elements, both carrying `titleAs="h2"`, and
      zero elements from the raw card family; contains zero `variant="brand"`, zero
      `variant="destructive"`, and zero references to the success token; contains zero `setTimeout`;
      carries exactly one `aria-label="Save state"`.
    - The avatar block still carries its hidden file input, its upload `aria-label` and its hint
      sentence, and carries no control whose accessible name reads as a removal — assert the absence by
      collecting the file's string literals rather than by grepping prose, so a comment explaining that
      removal is Phase 16's cannot be read as the violation.

    Then the **byte-identical copy assertion**: collect every string literal in the three profile files
    from the AST and assert the shipped sentences are present exactly — the two panel descriptions, the
    avatar hint, `Profile saved.`, the page title and the member-since composition. Write those literals
    into this gate, so a copy change has to move this file too and be seen. This is the earnings-freeze
    idiom the repository already uses for pinned copy.

    Then the **DOM link**. Attempt to render `ProfileForm` with fixture props and count the rendered
    `panel-card` testids. If the server-action import cannot resolve under the design config, do not
    force it: fall back to rendering `PanelCard` directly to prove the pattern emits the declared testid,
    keep the two-panel count as an AST assertion, and **record the measured reason in the file's header**
    — what was attempted, what failed, and which link of the chain is therefore proved by source rather
    than by DOM. An honestly named weaker link is a finding; a silently weaker link is a hole.

    In either case, assert the testid value is a member of `SELECTOR_IDS`.

    Finish with a **positive-control fixture**: a violating module string carrying a raw `Card` under an
    alias, a third `PanelCard`, a `variant="brand"` button, a `setTimeout` on a save path and a
    "Remove photo" control — fed to the same scanner, each defect asserted reported. Never write it to
    disk.

    Apply the mutation-verification discipline: mutate the corresponding source per assertion group,
    record the RED verbatim in the header, revert, and confirm `git diff --exit-code src/` clean.
  </action>

  <verify>
    <automated>npx vitest run tests/design/profile-pass.test.tsx --config vitest.design.config.ts &amp;&amp; npm run test:design</automated>
  </verify>

  <acceptance_criteria>
    - `tests/design/profile-pass.test.tsx` exists and begins with the `@vitest-environment jsdom` pragma
    - Its scanner is declared with a `(path: string, text: string)` signature
    - It asserts the `BOOKING_SHELL` **import** on both `page.tsx` and `loading.tsx`, not the class string
    - It contains a guard-the-guard block over all three files, before the first absence assertion
    - It contains the pinned copy literals: both panel descriptions, the avatar hint and `Profile saved.`
    - It imports `SELECTOR_IDS` and asserts the rendered testid is a member of it
    - It contains a violating fixture string that is never written to disk
    - `npx vitest run tests/design/profile-pass.test.tsx --config vitest.design.config.ts` exits 0
    - `npm run test:design` exits 0
    - The header records at least four mutations with their RED output quoted verbatim, and — if the DOM render of `ProfileForm` was not achievable — the measured reason and which link is proved by source instead
    - `git diff --exit-code src/` exits 0 after the mutation walk
  </acceptance_criteria>

  <done>Every one of AUTHUI-02's falsifiable claims is a command, and where a link of the chain is weaker than the analog's, the file says so and says why.</done>
</task>

<task type="auto">
  <name>Task 2: The four auth routes join the 320px gate</name>

  <read_first>
    - e2e/overflow-320.spec.ts (the whole file — the `RouteRow` type at :185-211, the `ROUTES` docblock and array from :214, the `firstListingPath` resolver as the model for a computed path, the four `path: null` rows at :340-372 and the register their `skip` strings are written in, and `expectReachable` at :375+)
    - e2e/login-persistence.spec.ts (the signup flow this harness would have to drive to reach `/profile`, and the accessible names it uses)
    - src/app/(auth)/layout.tsx and the four converted pages (what selector only an auth route produces)
    - src/lib/design/selector-contract.ts (confirm `panel-card` is declared — the `tell` must be a declared id)
  </read_first>

  <files>e2e/overflow-320.spec.ts</files>

  <action>
    Add four reachable rows to `ROUTES`, one per auth route, each with a literal `path` (`/login`,
    `/signup`, `/forgot-password`, and `/reset-password?token=` with a fixed literal token string) and a
    `tell` of the declared `panel-card` testid. The reset route needs the token in the URL because the
    page branches on its presence; the form does not validate it client-side beyond non-emptiness, so a
    fixed literal is a deterministic fixture and not a real token. Use an absolute instant nowhere and a
    clock nowhere — these four rows need no seed and no determinism work at all, which is what makes
    them the cheapest rows in the table.

    Add a fifth row for `/profile`. Prefer a **resolver-shaped `path`**: a function that drives the
    signup flow through the UI — the same approach `e2e/login-persistence.spec.ts` uses — and then
    returns `/profile`, with `tell` set to the declared `panel-card` testid. The `RouteRow` type already
    supports a resolver (`firstListingPath` is one), so this is the shape the table was built for.
    Measure whether it works. If the harness genuinely cannot carry a signed-in row — for instance
    because the surrounding fixture has no seed context — then declare the row with `path: null` and a
    `skip` string that names **exactly** what is missing, in the register of the four existing skipped
    rows: what the row needs, why this harness does not have it, and what covers the geometry instead.
    A row that is skipped for a stated reason is an inventory somebody can work from; a row that is
    quietly absent is a gap nobody will find. Record which branch you took, and why, in the summary.

    Update the `ROUTES` docblock's count prose: it currently says "THE TWELVE ROUTES ... Eight are
    reachable ... four are not." Re-measure both numbers against the array after the addition and write
    the new figures with the Phase-15 rows named. Do not leave a stale measured count in a gate's header.

    Add the auth rows to whatever state coverage the file already applies — if the table measures each
    row at both themes, these four ride that for free. Additionally, cover the two auth branches
    15-UI-SPEC names as separate states: forgot-password's post-submit branch (the form replaced by the
    uniform sentence) and reset-password's missing-token branch (reachable by omitting the token
    parameter). Use the `open?` hook for the forgot branch — it runs after `goto` and before
    measurement, which is exactly its purpose — and a second row with a token-less path for the reset
    branch. Give each a `tell` that only that branch produces.

    Change nothing about the existing twelve rows, the two traps, or `expectReachable`.
  </action>

  <verify>
    <automated>npx playwright test e2e/overflow-320.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'name: "/login"' e2e/overflow-320.spec.ts` returns 1, and the same for `/signup`, `/forgot-password` and `/reset-password`
    - Each of the four auth rows has `tell` set to the declared `panel-card` selector
    - A `/profile` row exists — either with a resolver `path` or with `path: null` and a `skip` string longer than 80 characters naming what is missing
    - Rows exist for forgot-password's post-submit branch and reset-password's missing-token branch, each with a branch-specific `tell`
    - The `ROUTES` docblock's route count and reachable/unreachable split are re-measured against the array — `grep -c 'TWELVE ROUTES' e2e/overflow-320.spec.ts` returns 0
    - `git diff e2e/overflow-320.spec.ts` shows no edit to `expectReachable` or to any pre-existing row
    - `npx playwright test e2e/overflow-320.spec.ts` exits 0
    - The summary records the M2 observation: whether the signup card (the tall one) clips at 320×568 inside the new `main`, and states it as a measurement rather than a prediction
  </acceptance_criteria>

  <done>Nothing on any auth route is wider than a 320px viewport, in any of its states, and the one route this harness may not be able to sign into says so in the string a skipped run prints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a gate's assertions → the tree they run over | An absence asserted over a file the walker never opened reports a clean result forever |
| the 320px harness → a route that redirects | "Nothing is wider than the viewport" is true of a blank page, a 404 and a redirect to `/login` |
| private profile fields → the rendered public panel | The D-09/D-10 split is a leak boundary and the gate pins both of its sentences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | A vacuous gate over an empty parse | mitigate | Guard-the-guard over all three profile files, asserted first; a positive-control violating fixture; mutation walk with RED quoted |
| T-15-28 | Information disclosure | The public/private split drifting | mitigate | Both D-09/D-10 sentences pinned as string literals in the gate, so a change must move this file and be seen |
| T-15-29 | Repudiation | A save reported as successful when it was not | mitigate | `setTimeout` absence on the save path asserted by AST rather than by review |
| T-15-32 | Repudiation | A 320px row passing over a redirect to `/login` | mitigate | Each new row's `tell` is a declared selector only that route produces; `expectReachable`'s existing trap is not weakened |
| T-15-33 | Repudiation | A route quietly missing from the table | mitigate | The `/profile` row exists in either branch; when unreachable it carries a `skip` naming what is missing; the docblock's counts are re-measured |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this phase — 15-RESEARCH § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `npm run test:design` green.
- `npx playwright test e2e/overflow-320.spec.ts` green; skipped rows print their reasons.
- `npx tsc --noEmit` exits 0.
- `git diff --exit-code src/` exits 0 after the mutation walk.
- `git diff --name-only` at plan end lists only the two files in `files_modified`.

⚠ `npm test` and `npm run test:design` share `fitout_test` — run them sequentially, never concurrently.
</verification>

<success_criteria>
- AUTHUI-02's ten falsifiable claims are commands, and the one weaker link is named and explained.
- The four auth routes and their two extra branches are measured at 320px.
- No stale measured count is left in `overflow-320.spec.ts`'s header.
</success_criteria>

<output>
Create `.planning/phases/15-auth-profile-transactional-email/15-10-SUMMARY.md` when done.
</output>
