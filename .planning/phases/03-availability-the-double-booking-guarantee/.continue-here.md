---
context: phase
phase: 03-availability-the-double-booking-guarantee
task: 0
total_tasks: 5
status: in_progress
last_updated: 2026-07-12T17:02:28.415Z
---

# Resume: Phase 3 — Availability & the Double-Booking Guarantee

**Phase 3 is CODE-COMPLETE on `dev`. Paused at the Wave-3 human-verify gate.** All 5 plans are built and committed; 03-04 and 03-05 are NOT yet marked complete in STATE.md/ROADMAP.md because they require human UAT approval first.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree isolation for an npm + live-DB phase | 03-01 installs deps (gitignored `node_modules`) + migrates the live DB; a fresh worktree has no `node_modules` and it doesn't merge back, so later waves would run dependency-less | advisory | This phase ran sequentially on the main tree. Keep worktrees OFF for any phase whose Wave 1 installs deps or migrates the DB. |
| Docker Desktop 4.69.0 Inference-manager crash | Engine aborts on `starting services: initializing Inference manager … dockerInference: The file cannot be accessed` — a stale reparse point at `%LOCALAPPDATA%\Docker\run\dockerInference` Windows can't delete | advisory | FIXED: set `EnableDockerAI:false` in `%APPDATA%\Docker\settings-store.json` AND rename `%LOCALAPPDATA%\Docker\run` aside so Docker recreates it clean. If it recurs, repeat. |
| Long agent runs vs account spend limit | The 03-05 executor was killed mid-run by a monthly-spend-limit API error; it had committed its build tasks but not the E2E spec or SUMMARY | advisory | Recovered inline by the orchestrator (spot-check git/filesystem, commit the authored artifact, write the SUMMARY). Verify limit headroom before spawning the verifier/code-review agents on resume. |

<current_state>
On branch `dev` (clean tree, ahead of `origin/dev`). Phase 3 delivered: the `booking` table + Postgres GiST `booking_no_overlap` EXCLUDE constraint (live in the DB), the `operating_hours`/`availability_block` tables + `listing.unitCount`/`timezone`, the server correctness layer (`slots`/`read-model`/`units`), the host hours/blocks backend (validated + IDOR-guarded), the host availability editor UI (with client-side overlap-prevention just added per checkpoint feedback), and the booker availability calendar wired into the public listing page. The availability E2E is green (2/2). Waiting on the user's manual human-verify approval for 03-04 and 03-05.
</current_state>

<completed_work>

Completed & committed (Waves 1–2 fully done; Wave 3 built, awaiting UAT):
- **03-01** (`afba1ac`): DB keystone — `booking` + GiST EXCLUDE constraint, `operating_hours`/`availability_block`, `unitCount`/`timezone`, deps + shadcn calendar/toggle/toggle-group/scroll-area, `makeRacingClients`, `isPgError`, SC#4 two-connection race test (green). Deviation: a genuine race can raise `40P01` as well as `23P01`.
- **03-02** (`2b0cfd0`): server correctness — `slots.ts` (DST-correct TZDate), `read-model.ts` (`getAvailability`, free-unit math, `[)` overlap), `units.ts` (`createBooking` find-free-unit + retry on 23P01 **and** 40P01, `mapBookingError`).
- **03-03** (`2b7f7f2`): host backend — shared Zod (`weeklyHoursSchema`/`blockSchema`, on-the-hour, `:ss`-tolerant round-trip) + `saveOperatingHours`/`addBlock`/`removeBlock` (session + `assertOwnership` IDOR + server re-validate).
- **03-04** (`9d1cdaa` build; `40e9cf6` UX): host editor UI — `WeeklyHoursEditor`, `BlocksEditor` + `AddBlockDialog`, gated RSC page (`notFound()` IDOR). **Plus** checkpoint-feedback enhancement: client-side overlap prevention (disabled conflicting open/close dropdown options) + `mode:"onChange"` live validation + non-overlapping "Add hours" default.
- **03-05** (`43a2db9`): booker calendar — `booking.ts` schema, `SlotPicker` (consecutive-run, aria-disabled unavailable, never red), `AvailabilityCalendar` (react-day-picker venue tz + always-visible tz note, SC#2), read-only `getDayAvailability`, public page placeholder replaced + rail summary, `e2e/availability.spec.ts`. E2E fixed (assert seeded city "Makati", not "Manila") → 2/2 green.
</completed_work>

<remaining_work>

- **Human-verify 03-04** — editor: create-overlap now blocked in the dropdowns + live validation; add/remove blocks (whole-day + partial, unit vs whole-listing, reversible neutral confirm); IDOR → 404 on another host's listing; tz note visible. Round-trip: reload → add a window without touching others → Save succeeds.
- **Human-verify 03-05** — booker calendar: venue-tz display holds with the browser OS tz set to non-Manila (SC#2); occupied/blocked/closed hours muted + struck-through (never red), unselectable; consecutive-run selectable → rail summary; not-payable listing read-only with "Not bookable yet"; "Book this space" CTA is a no-op (Phase-4 placeholder).
- **OPTIONAL** — apply the same live-validation + `end>start` dropdown treatment to `AddBlockDialog` (user asked "why not do it originally" re: the hours editor; hasn't decided on the block dialog).
- **On approval** — edit STATE.md + ROADMAP.md to mark 03-04 and 03-05 complete, then resume the execute-phase completion flow: `code_review_gate` → `regression_gate` → `schema_drift_gate` → `verify_phase_goal` (spawn `gsd-verifier`, model sonnet) → `update_roadmap` (`phase.complete`) → offer Phase 4 routing. `auto_advance` is false, so STOP after completion and present options (do not auto-advance).
</remaining_work>

<decisions_made>

- Ran the phase **sequentially on the main tree** (worktrees OFF) — npm deps + live DB migration are incompatible with per-plan worktree isolation.
- `createBooking`/`mapBookingError` treat **40P01 like 23P01** (both atomically reject the double-book) — carry-forward for Phase 4's hold flow.
- Weekly-hours **overlap prevention is client-side** (disabled options + live validation); the server action still re-validates (never trust the client).
- Executor model `opus`, verifier model `sonnet` (from init).
</decisions_made>

<blockers>
- 03-04 and 03-05 are at BLOCKING human-verify checkpoints — the phase cannot be marked complete or verified until the user approves the manual UAT. No code blockers; the tree is clean and all gates (tsc/eslint/build/tests/E2E) are green.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state (completed/remaining tasks, decisions, next_action).
2. `~/.claude/get-shit-done/workflows/execute-phase.md` — resume the completion flow from `code_review_gate` onward (after marking 03-04/03-05 complete on approval).
3. `.planning/phases/03-availability-the-double-booking-guarantee/03-04-SUMMARY.md` and `03-05-SUMMARY.md` — what was built + the pending checkpoint items.
4. `~/.claude/projects/C--Users-franc-dev-FitOut/memory/gsd-tools-invocation.md` — the SDK is `node ".../gsd-tools.cjs" <command>` (NOT on PATH); many `query <dotted.path>` subcommands don't exist — edit STATE.md/ROADMAP.md directly.

## Infrastructure State
- **Docker**: fixed and running (v29.4.0). `EnableDockerAI:false`; the old run dir is parked at `%LOCALAPPDATA%\Docker\run.broken-inference` (inert, un-deletable orphan socket — ignore).
- **DB**: `fitout-db-1` (postgis/postgis:18-3.6) up on `localhost:5432`; migrations 0000–0005 applied; `booking_no_overlap` constraint + all 3 availability tables verified present.
- **Dev server**: NOT running — `npm run dev` (http://localhost:3000) needed for manual UAT. Playwright boots its own webServer for the E2E.
- **Git**: branch `dev`, clean tree, pushed to `origin/dev` at pause (see WIP handoff commit).

<context>
The build phase went smoothly — the correctness keystone (GiST constraint) and the whole read/write stack are done and tested. The session's friction was environmental: a spend-limit interruption (recovered inline) and a Docker Desktop 4.69.0 bug (fixed). The user is mid-UAT and gave sharp UX feedback on the hours editor (prevent overlaps in the UI, validate live) which is now implemented and committed. Next real work is purely gated on their manual approval.
</context>

<next_action>
Start with: ask the user for their human-verify result on 03-04 and 03-05 (and whether to also fix AddBlockDialog). On "approved", edit STATE.md + ROADMAP.md to mark both plans complete, then resume execute-phase.md at the `code_review_gate` step through `phase.complete`. Do NOT auto-advance to Phase 4 (auto_advance=false) — present routing options and stop.
</next_action>
