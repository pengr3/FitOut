---
phase: 07-bookings-management-cancellation-notifications
plan: 15
type: execute
wave: 4
depends_on: ["07-03", "07-08"]
files_modified:
  - src/components/booking/cancellation-policy-disclosure.tsx
  - src/components/host/wizard.tsx
  - src/lib/validation/listing.ts
  - src/app/actions/listing.ts
  - src/app/listings/[id]/page.tsx
  - src/app/listings/[id]/book/page.tsx
  - tests/booking/cancellation-policy.test.ts
autonomous: true
requirements: [BOOK-07]

must_haves:
  truths:
    - "A host must make an EXPLICIT cancellation-tier choice before a listing can be published — there is no default (D-77)"
    - "The tier requirement joins the EXISTING live publish checklist rather than inventing a new blocked affordance (D-77)"
    - "The policy is surfaced on the listing detail page AND at checkout, expandable to the actual rungs (D-81)"
    - "At checkout the rungs render as CONCRETE DATES for this booking, never abstract percentages alone (D-81, C3)"
    - "Every expanded policy view ends with the mandatory line that the service fee isn't refunded (C2)"
  artifacts:
    - path: "src/components/booking/cancellation-policy-disclosure.tsx"
      provides: "One shared disclosure with a generic mode and a concrete-dates mode"
    - path: "src/components/host/wizard.tsx"
      provides: "Three-card tier step with NO pre-selection + a publish-checklist row"
    - path: "tests/booking/cancellation-policy.test.ts"
      provides: "Publish gate + concrete-boundary derivation"
  key_links:
    - from: "src/components/booking/cancellation-policy-disclosure.tsx"
      to: "src/lib/payments/cancellation.ts rungBoundaries"
      via: "concrete-date derivation"
      pattern: "rungBoundaries\\("
    - from: "src/app/actions/listing.ts"
      to: "listing.cancellation_policy"
      via: "publish gate requiring non-null"
      pattern: "cancellationPolicy"
---

<objective>
Make the cancellation tier a required, explicit host choice before publishing, and surface the resulting promise to the booker on the listing page and at checkout — with concrete dates for their specific booking.

Purpose: a refund promise the booker demonstrably saw is the only kind enforceable in spirit (D-81), and D-77 deliberately breaks the D-62 precedent of defaulting to the most booker-friendly option because this is not a setting that should be set by accident.
Output: the shared disclosure component, the wizard step and publish gate, and two booker-facing mounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-bookings-management-cancellation-notifications/07-CONTEXT.md
@.planning/phases/07-bookings-management-cancellation-notifications/07-RESEARCH.md
@.planning/phases/07-bookings-management-cancellation-notifications/07-PATTERNS.md
@.planning/phases/07-bookings-management-cancellation-notifications/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The listing-wizard tier step with no default, and the publish gate</name>
  <read_first>
    - src/components/host/wizard.tsx lines 270-320 (the live publish checklist and `publishEligible = checklist.every(c => c.done)`) and lines 660-720 (the Step-5 booking-mode two-card RadioGroup to clone with THREE cards)
    - src/lib/validation/listing.ts (the shared listing schema — the publish-time requirements live here)
    - src/app/actions/listing.ts (the publish action that must reject a null tier server-side)
    - src/lib/db/schema.ts listing.cancellationPolicy (from Plan 01 — nullable, NO default)
    - .planning/phases/07-bookings-management-cancellation-notifications/07-UI-SPEC.md § 6 (the EXACT three tier-card titles and descriptions, and the FormDescription copy — copy verbatim)
  </read_first>
  <files>src/components/host/wizard.tsx, src/lib/validation/listing.ts, src/app/actions/listing.ts</files>
  <action>
(a) Add a cancellation-policy step to the existing editor wizard, adjacent to the Step-5 booking-mode step, cloning that step's `RadioGroup` card pattern with THREE cards instead of two.

**NO card is pre-selected** (D-77). Record the rationale as a comment: this deliberately breaks the D-62 precedent of defaulting to the most booker-friendly option, because the cancellation tier is not a setting that should be set by accident — it governs real money.

All three cards render at equal weight in the unchosen state. **Do not visually imply a recommendation.** Selected card = neutral `border-primary`, **no coral**.

Exact card copy (UI-SPEC § 6):
| value | title | description |
|---|---|---|
| `flexible` | `Flexible` | `Guests get a full refund if they cancel at least 12 hours before the session. Best for spaces that are easy to rebook.` |
| `standard` | `Standard` | `Full refund up to 24 hours before; half back up to 6 hours before. A middle ground most hosts pick.` |
| `strict` | `Strict` | `Full refund up to 48 hours before; half back up to 24 hours before. Best for spaces you turn down other bookings for.` |

Muted `FormDescription`: `Pick the policy that fits your space. You can change it later — it applies to new bookings only; bookings already made keep the policy they were booked under.`

(b) Add ONE row to the EXISTING live publish checklist (`wizard.tsx:284-302`), linking back to this step exactly like every other unmet requirement:
```typescript
{ label: "Choose a cancellation policy", done: Boolean(values.cancellationPolicy), step: N },
```
**Do NOT invent a new blocked affordance** — the shipped `Almost there — finish these to publish:` panel already handles this, and `publishEligible = checklist.every(c => c.done)` picks it up automatically.

(c) Enforce it SERVER-SIDE. The client checklist is a courtesy, never the gate. In `src/lib/validation/listing.ts`, add `cancellationPolicy` to the publish-time schema as a required enum (`z.enum(["flexible","standard","strict"])`), leaving the draft-time schema permissive so existing drafts stay editable. In `src/app/actions/listing.ts`'s publish path, reject a null/invalid tier with a calm message routing the host back to the step.

Record the D-77 gate placement in a comment: the tier gates PUBLISHING, not listing creation — mirroring how bookability (not creation) is gated on payout-readiness, so an existing draft is never bricked by the new requirement and the gate cannot be bypassed by a stale client.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run tests/listing 2>/dev/null; npx eslint src/components/host/wizard.tsx src/lib/validation/listing.ts src/app/actions/listing.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Choose a cancellation policy" src/components/host/wizard.tsx` returns 1
    - `grep -c "Best for spaces that are easy to rebook." src/components/host/wizard.tsx` returns 1
    - `grep -c "A middle ground most hosts pick." src/components/host/wizard.tsx` returns 1
    - `grep -c "Best for spaces you turn down other bookings for." src/components/host/wizard.tsx` returns 1
    - `grep -c "defaultValue=\"flexible\"\|defaultValue=\"standard\"\|defaultValue=\"strict\"" src/components/host/wizard.tsx` returns 0 (no pre-selection — D-77)
    - `grep -c "bg-brand" src/components/host/wizard.tsx` does not increase relative to the pre-change count (no coral added to the tier cards)
    - `grep -c "cancellationPolicy" src/lib/validation/listing.ts` returns >= 1 AND `grep -c "cancellationPolicy" src/app/actions/listing.ts` returns >= 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The wizard offers three equal-weight, unselected tier cards; the requirement joins the shipped publish checklist; and the publish action rejects a missing tier server-side.</done>
</task>

<task type="auto">
  <name>Task 2: The shared CancellationPolicyDisclosure with generic and concrete-dates modes</name>
  <read_first>
    - src/lib/payments/cancellation.ts (from Plan 03 — `LADDER` and `rungBoundaries`, which return the boundary INSTANTS this component formats)
    - src/lib/booking/when-label.ts (from Plan 02 — the venue-local formatting convention; do NOT create a new time format)
    - src/components/booking/price-breakdown.tsx (the server-computed-props, zero-arithmetic contract to mirror)
    - .planning/phases/07-bookings-management-cancellation-notifications/07-UI-SPEC.md § 5 (the EXACT collapsed and expanded copy for both modes) and § Copywriting Contract C2/C3
    - src/app/globals.css (the tokens available — no new tokens may be introduced)
  </read_first>
  <files>src/components/booking/cancellation-policy-disclosure.tsx</files>
  <action>
Create `src/components/booking/cancellation-policy-disclosure.tsx` — a SERVER component (no client directive) using a native `<details>` / `<summary>` styled with existing tokens.

Record the researcher's call as a comment: no `accordion` or `collapsible` block is installed; `<details>` needs zero new blocks, zero client boundary (it works inside an RSC), and degrades without JS. A future planner may substitute the official-registry `accordion` — that registry needs no vetting gate — if a design-consistency argument emerges.

Two modes, one component. All strings arrive as SERVER-COMPUTED props; the component performs no date math and no arithmetic.

**Mode `generic`** (listing detail page — there is no booking yet, so rungs are stated relative to session start):
- Collapsed summary: `Free cancellation up to {N} hours before the session.` · `See cancellation policy`
- Expanded: the tier name as a heading, then one bullet per rung stated relatively, then the mandatory closing line.

**Mode `concrete`** (checkout — D-81's actual requirement):
- Collapsed summary: `**Free cancellation until {Thu 3 Jul, 8:00 PM} ({City} time)**` · `See cancellation policy`
- Expanded, Standard-tier shape:
  > **Standard cancellation policy**
  > - Cancel before **Thu 3 Jul, 8:00 PM** — full refund of the space price
  > - Cancel before **Fri 4 Jul, 2:00 PM** — 50% of the space price refunded
  > - After that — no refund
  >
  > The service fee isn't refunded.

Rules encoded as comments AND behaviour:
- **Concrete dates, never abstract percentages alone** (D-81/C3). A percentage may ACCOMPANY a date; it may never replace one. The caller derives each boundary from `rungBoundaries(tier, startsAt)` and formats it venue-local via `@date-fns/tz`, reusing the existing label composition rather than inventing a fifth format.
- Every boundary names the venue timezone.
- The line `The service fee isn't refunded.` is **MANDATORY on every expanded view**, in both modes, at every tier — including Flexible, where a full refund is the only rung. C2: this is the one place a booker could be surprised.
- The component introduces **zero new tokens** — muted/secondary/border only.

Mount it in two places, with NO other change to either page:
- `src/app/listings/[id]/page.tsx` — mode `generic`, near the price block.
- `src/app/listings/[id]/book/page.tsx` (the checkout/reserve page) — mode `concrete`, with the boundaries derived server-side from the selected window's `startsAt`.

When the listing has no tier (an unpublished draft, `cancellation_policy IS NULL`), render NOTHING rather than an empty shell — such a listing is not bookable anyway.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx eslint src/components/booking/cancellation-policy-disclosure.tsx "src/app/listings/[id]/page.tsx" "src/app/listings/[id]/book/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use client" src/components/booking/cancellation-policy-disclosure.tsx` returns 0
    - `grep -c "<details" src/components/booking/cancellation-policy-disclosure.tsx` returns >= 1 AND `grep -c "<summary" src/components/booking/cancellation-policy-disclosure.tsx` returns >= 1
    - `grep -c "The service fee isn&apos;t refunded.\|The service fee isn't refunded." src/components/booking/cancellation-policy-disclosure.tsx` returns >= 1 and is rendered unconditionally in the expanded branch (verify by reading — not inside a tier or rung conditional)
    - `grep -c "See cancellation policy" src/components/booking/cancellation-policy-disclosure.tsx` returns >= 1
    - `grep -c "CancellationPolicyDisclosure" "src/app/listings/[id]/page.tsx" "src/app/listings/[id]/book/page.tsx"` returns >= 1 for both
    - `grep -c "rungBoundaries" "src/app/listings/[id]/book/page.tsx"` returns >= 1 (concrete dates derived server-side)
    - `grep -cE "oklch|#[0-9a-fA-F]{6}" src/components/booking/cancellation-policy-disclosure.tsx` returns 0 (no new colour values)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>One disclosure component serves both surfaces; checkout shows concrete venue-local boundary dates derived from `rungBoundaries`; the non-refundable service-fee line is unconditional in every expanded view.</done>
</task>

<task type="auto">
  <name>Task 3: Publish-gate and boundary-derivation tests</name>
  <read_first>
    - tests/helpers/db.ts (the isolated-schema harness)
    - src/app/actions/listing.ts (the publish path from Task 1)
    - src/lib/payments/cancellation.ts (rungBoundaries)
    - tests/payments/cancellation.test.ts (from Plan 03 — the ladder cases this builds on; do not duplicate them)
  </read_first>
  <files>tests/booking/cancellation-policy.test.ts</files>
  <action>
Create `tests/booking/cancellation-policy.test.ts` (integration, isolated schema):
1. **Publish gate, server-side** — attempting to publish a listing whose `cancellation_policy IS NULL` is REJECTED with the calm message and the listing's status is unchanged. This proves the client checklist is not the gate.
2. **Publish succeeds with a tier** — the same listing with `cancellation_policy = 'standard'` publishes successfully.
3. **Draft is not bricked** — an existing listing with a NULL tier can still be saved as a DRAFT (only publishing is gated), so the new requirement does not strand pre-Phase-7 drafts.
4. **Concrete boundaries** — for `startsAt = 2026-07-10T12:00:00Z` and tier `standard`, `rungBoundaries` returns boundaries at `startsAt − 24h` and `startsAt − 6h`, in that order, with the correct `refundBps` (10000 then 5000).
5. **Flexible has one rung** — `rungBoundaries("flexible", startsAt)` returns exactly one entry at `startsAt − 12h`.
6. **Tier is retierable and applies forward only** — updating a published listing's tier succeeds, and a booking created BEFORE the change retains its snapshot (this complements, and does not duplicate, the Plan-08 snapshot test — here assert the LISTING side changed while the booking side did not).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/booking/cancellation-policy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/booking/cancellation-policy.test.ts` succeeds
    - `grep -ci "publish" tests/booking/cancellation-policy.test.ts` returns >= 3
    - `grep -c "rungBoundaries" tests/booking/cancellation-policy.test.ts` returns >= 2
    - `npx vitest run tests/booking/cancellation-policy.test.ts` exits 0 with all 6 cases passing
  </acceptance_criteria>
  <done>The publish gate is proven server-side, existing drafts are proven not stranded, and the concrete rung boundaries are proven to match the D-68 ladder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → publish action | The wizard's checklist is client state and can be bypassed |
| listing tier → booker-facing promise | The displayed policy is a commitment the platform must honour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-88 | Tampering | Publishing a listing with no cancellation tier by bypassing the client checklist | mitigate | The publish-time Zod schema requires the enum AND `src/app/actions/listing.ts` re-validates server-side; test 1 publishes directly through the action with a NULL tier and asserts rejection. |
| T-07-89 | Repudiation | Host disputes the refund terms a booker was shown | mitigate | The policy is surfaced on the listing page AND at checkout with concrete dates (D-81), and the tier is snapshotted onto the booking at creation (Plan 08), so the terms shown are provably the terms applied. |
| T-07-90 | Tampering | Retiering a listing to change an in-flight booking's terms | mitigate | The refund calculator reads `booking.cancellation_policy`, never the listing; test 6 asserts the listing changed while the booking's snapshot did not. |
| T-07-91 | Information disclosure | Policy disclosure on an unpublished draft | mitigate | The component renders NOTHING when `cancellation_policy IS NULL`; such a listing is not bookable. |
| T-07-92 | Tampering | Boundary dates rendered from a client clock | mitigate | `rungBoundaries` is called server-side in the RSC and formatted venue-local; the component is a Server Component receiving pre-formatted strings and performs no date math. |
| T-07-93 | Denial of Service | Existing drafts bricked by the new requirement | mitigate | The gate is on PUBLISH only; the draft schema stays permissive and `listing.cancellation_policy` is nullable. Test 3 asserts a NULL-tier draft still saves. |
</threat_model>

<verification>
1. `npx tsc --noEmit` and `npm run build` exit 0.
2. `npx vitest run tests/booking/cancellation-policy.test.ts` green (6 cases).
3. No new colour values or tokens introduced.
4. `npx eslint` clean on all touched files.
</verification>

<success_criteria>
- No tier is pre-selected in the wizard; the requirement appears in the shipped publish checklist and is enforced server-side.
- Existing NULL-tier drafts remain saveable.
- The policy renders on the listing page (generic) and at checkout (concrete venue-local dates).
- Every expanded view ends with the non-refundable service-fee line.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bookings-management-cancellation-notifications/07-15-SUMMARY.md`.
</output>
