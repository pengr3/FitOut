---
context: phase
phase: 07-bookings-management-cancellation-notifications
activity: verify-work (UAT)
status: partial
uat_progress: 11 passed / 2 issues / 2 pending of 15
last_updated: 2026-07-23T18:14:20Z
---

# Resume Point — Phase 7 UAT (conversational verify-work)

This is a **UAT session**, not an execute-phase session. All 17 Phase-7 plans are code-complete;
this session is walking a human tester through 15 observable-behaviour tests in `07-UAT.md`.

<current_state>
11 tests pass with strong live evidence, 2 are open issues (both major, both logged as gaps),
and **2 tests remain to run**:
- **Test 13 — Too-Soon Slots**: awaiting the user's visual confirmation that today's within-2h
  evening slots on a request-mode listing render muted/struck-through (never red).
- **Test 14 — Expired Approval Recovery (re-request)**: not yet staged. Needs: booker submits a
  request, host approves, then simulate the payment-window lapse, then exercise "Request this time
  again" on the booking detail.

After both are resolved, the verify-work workflow auto-diagnoses the gaps (parallel debug agents),
plans fixes (gsd-planner --gaps → gsd-plan-checker), and readies `/gsd-execute-phase 7 --gaps-only`.
</current_state>

<completed_work>
Tests PASS: 1 (cold start), 2 (all-in pricing), 3 (publish gate), 4 (policy disclosure),
5 (my bookings), 7 (booker cancel + exact refund), 9 (host cancel — all 4 D-70/D-71 consequences),
10 (earnings commission label + fee-debt line), 11 (notification bell), 12 (lifecycle emails),
15 (reminders cron — verified live at the :45 tick).

Real PayMongo test-mode payments + refunds were exercised end-to-end through the ngrok webhook:
- Booker refund ref_9SBDgBhQNMhEi1U9v3ciKZAj (₱307.50, succeeded)
- Host-cancel full refund ref_5rWAuwjETjqCY52hU9ci88sZ (₱774.90, succeeded)
</completed_work>

<remaining_work>
- Test 13: user visual confirmation of too-soon slot rendering.
- Test 14: stage request → approve → lapse → re-request, then confirm the recovery UI.
- Then: workflow diagnosis + gap-closure planning for the 6 gaps below.
</remaining_work>

## Open Gaps (in 07-UAT.md Gaps section — feed /gsd-plan-phase --gaps)

| # | Sev | Gap |
|---|-----|-----|
| T8 | major | Booker-cancelled unpaid request shows host-decline copy ("Declined" / "The host couldn't take your booking"). Row is status=declined + cancelled_by=booker; the detail page's `declined` branch and `deriveBookingStatusView` ignore `cancelled_by`. |
| T6 | major | `/host/bookings` list rows have no link to `/host/bookings/[id]`; host cancel flow (SC#3) is UI-unreachable without hand-typing a UUID. `host-booking-row.tsx` has no href. |
| T4 | major | Nothing links to `/host/listings/[id]/availability` (weekly-hours editor); no wizard hours step, publish checklist doesn't require hours → a published listing sits at "No availability yet". Pre-existing Phase 3/4 gap. |
| T4 | minor | Checkout policy SUMMARY line advertises an already-lapsed top rung ("Free cancellation until <past instant>") — no boundary-vs-now check. |
| T11 | minor | `placeHold` re-emits the request notification pair on idempotent replay (double-click) — duplicate host notifications/emails. `re-request.ts` suppresses; `placeHold` does not. |
| T7/T9 | cosmetic | `formatMoney` drops trailing zero (₱307.5 not ₱307.50); "₱300in" missing space on earnings; raw enum "host_cancellation" shown as a block label on the availability page. |

<decisions_made>
- Activated host payout + assigned cancellation tiers via direct SQL — PayMongo Platforms is
  sales-gated in test mode (no merchant.activated webhook) and the pre-Phase-7 seed left tiers NULL.
- Used REAL PayMongo test-mode payments/refunds (not mocks) — money correctness is the phase's core
  value; refunds verified against the live PayMongo API.
</decisions_made>

<blockers>
- Tests 13/14 need the human tester in the loop (verify-work is conversational). Not a code blocker.
</blockers>

## Infrastructure State (running background processes — likely still up)

- **Postgres**: docker `fitout-db-1` (postgis/postgis:18) on :5432.
- **Dev server**: `next dev` on :3000 (started via the preview tool).
- **Inngest dev server**: `npx inngest-cli dev` on :8288 — crashed once mid-session and was
  restarted; the `booking_confirmed` notification for the first paid booking was lost during that
  outage (07-07's documented bounded gap, not a defect).
- **ngrok tunnel**: `https://eloquent-pounce-entangled.ngrok-free.dev` → :3000. PayMongo test-mode
  webhook registered at the **full** `/api/paymongo/webhook` path (event: checkout_session.payment.paid).
  The tunnel URL is **ephemeral** — if it changed, re-register the webhook before running test 14.

## Critical Anti-Patterns (advisory)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| INNGEST_DEV missing | Without `INNGEST_DEV=1` in `.env.local`, `/api/inngest` returns 500 ("cloud mode, no signing key") and every `emitNotify` enqueue fails silently — no notifications, no emails. | advisory | It's now set in `.env.local` (gitignored). Keep the Inngest dev server running for any notification test. |
| Seed predates Phase 7 | `scripts/seed.ts` publishes listings with NULL `cancellation_policy`, bypassing the D-77 gate; needed manual SQL patching. | advisory | A spawn-task chip was filed to update the seed. |

## Required Reading (on resume, in order)
1. `.planning/HANDOFF.json` — machine-readable state (tests, gaps, infra).
2. `.planning/phases/07-bookings-management-cancellation-notifications/07-UAT.md` — the live UAT with per-test notes + the 6-item Gaps section.
3. `.planning/phases/07-bookings-management-cancellation-notifications/deferred-items.md` — 3 UX polish notes (weekly-hours editor UX, duplicated approve/decline surfaces, seed script).

<context>
The tester (pengr.clmc.3@gmail.com = host `test@gmail.com` in-app RdJ4E83J...; a second incognito
booking account was used as the booker). Pickletickle listing id 3ab8e6c5-1359-4126-8d84-26fd7d4c90bb.
The session found real product-usability gaps (tests 6, 8) that the plans' automated tests could not
catch because they assert behaviour, not navigation or landing-state copy. That's the value of the
manual pass — keep test 14 real (drive the actual UI), don't shortcut it with a DB assertion.
</context>

<next_action>
Re-run `/gsd-verify-work 7`. It will resume at test 13 (ask the user the too-soon-slot visual
question) then test 14 (stage request→approve→lapse→re-request). Ensure Postgres, the dev server,
the Inngest dev server, and the ngrok tunnel are up first; re-register the PayMongo webhook if the
tunnel URL changed. Then let the workflow diagnose + plan the 6 gaps.
</next_action>
