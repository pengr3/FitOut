---
phase: 25.1-paymongo-production-release-readiness-controlled-proofs
plan: 03
type: execute
wave: 3
depends_on: [25.1-02]
files_modified:
  - .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RETURN-PROOF.md
autonomous: false
requirements: [] # ROADMAP.md has no mapped requirement ID for Phase 25.1.
user_setup:
  - service: PayMongo production account and return operations
    why: "Refund eligibility, manual-return handling, and a bounded financial operation require a named account authority and operations owner."
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every enabled or proposed rail has either a provider-observed controlled refund result or an approved manual-return path with an owner, monitored destination, SLA, reconciliation, and communication policy."
    - "An unknown or unsupported rail cannot be routed around the existing server-side allowlist or declared refundable by documentation."
    - "A controlled refund or manual return is never performed unless a blocking human checkpoint records authority, bounded scope, stop/return procedure, observation, reconciliation, and alert ownership; otherwise its outcome is HOLD."
  artifacts:
    - path: .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RETURN-PROOF.md
      provides: "Per-rail refund/manual-return control record with redacted evidence and HOLD fallback."
  key_links:
    - from: "verified refund event and isApiRefundable rail policy"
      to: "return proof record"
      via: "Each rail's evidence names the provider result or the owned manual-return route; unknown rail values remain non-API-refund paths."
    - from: "manual-return alert and reconciliation owner"
      to: "release decision"
      via: "An unacknowledged return, absent SLA, or unresolved durable state keeps the rail and broad release on HOLD."
---

<objective>
Prepare and gate the per-rail refund and manual-return proof path without moving money by default.

Purpose: Phase 25 established that a return path is an operational obligation, particularly for QR Ph and every unverified rail. This plan makes each required authority, observation, stop/return boundary, and recovery owner explicit before anyone can elect a controlled return.
Output: `25.1-RETURN-PROOF.md` and a blocking-human controlled-return decision that has HOLD as its only default.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RESEARCH.md
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md
@.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
@.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
@src/lib/payments/refund-rail.ts
@src/app/api/paymongo/webhook/route.ts
</context>

<interfaces>
Existing return contract: `isApiRefundable(method)` is the sole rail-policy predicate; it permits only the defined API-refundable rails and fails closed for unknown values. Verified `payment.refunded` and terminal-success `payment.refund.updated` handling is already in `/api/paymongo/webhook`; a post-payout return raises an operations attention path rather than rewriting paid money. This plan creates no new refund endpoint, rail policy, confirmation route, or wallet behavior.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one return decision from rail policy to a reconciled, owned outcome</name>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RETURN-PROOF.md</files>
  <action>Create a redacted per-rail proof record beginning in `HOLD`. Include one row each for card, GCash, PayMaya, QR Ph, GrabPay, and any rail proposed for availability. For every row capture current entitlement evidence, the existing API-eligibility classification, controlled-return authority, one amount or cap, one redacted booking/payment boundary, participant boundary, executing operator, expected provider observation or manual-return confirmation, durable booking/ledger reconciliation, monitored alert destination, acknowledgement owner, escalation route, SLA, customer/host communication policy, and provider-side method to halt related checkout activity or execute the documented return/escalation boundary. GrabPay and every other listed rail must receive exactly one explicit disposition: `VERIFIED` only with provider-verified refund capability and observed reconciliation; an owned manual-return record; or an approved `OPTED OUT` record with named fallback and owner. Use the Plan 01 reference-only schema and its comprehensive non-printing redaction scan; record only an opaque evidence ID and generic violation class on scanner failure. State that all omitted or contradictory fields mean HOLD and that no default wallet, customer, or rail can be selected. Do not edit the return implementation or invoke a provider.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const p='.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RETURN-PROOF.md';const t=fs.readFileSync(p,'utf8');const protectedPatterns=[/(?:sk|pk)_(?:live|test)_[A-Za-z0-9_-]{8,}/i,/whsec_[A-Za-z0-9_-]{8,}/i,/\b(?:bearer|basic)\s+[A-Za-z0-9._~+\/=:-]{12,}/i,/\b(?:api[_-]?key|access[_-]?token|client[_-]?secret|secret|password|token)\s*[:=]\s*['\"]?(?!redacted\b|reference\b)[A-Za-z0-9._~+\/=:-]{6,}/i,/\b(?:PAYMONGO|INNGEST|DATABASE|PLATFORM_WALLET)_[A-Z0-9_]*\s*=\s*\S+/i,/\b(?:mongodb(?:\+srv)?|postgres(?:ql)?|mysql|redis):\/\/[^\s]+/i,/\b(?:pay|pi|cs|cus|acct|wal|tr|evt)_[A-Za-z0-9_-]{8,}/i,/\"(?:data|attributes|metadata)\"\s*:\s*\{/i];for(const h of ['Per-rail evidence matrix','API rail policy','Manual return boundary','Reconciliation record','Alert ownership','Provider-side stop and return','HOLD disposition'])if(!t.includes(h))throw new Error('missing '+h);for(const rail of ['card','gcash','paymaya','qrph','grabpay'])if(!t.toLowerCase().includes(rail))throw new Error('missing rail '+rail);if(protectedPatterns.some((r)=>r.test(t)))throw new Error('protected-value class in return proof');console.log('return proof contract passes');"</automated>
    <fails_when>Fails when the command exits nonzero, a mandatory proof section or listed rail (including GrabPay) is absent, or the scanner finds a protected-value or raw-payload class; stop the return-proof workflow, record only the generic failure class and owner, and retain `HOLD`.</fails_when>
  </verify>
  <done>Each candidate rail has an evidence-bearing API-refund or manual-return path with explicit HOLD behavior, and no documentation expands the server-side policy.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide one controlled return/manual return or retain HOLD for every rail</name>
  <decision>Keep every rail on `HOLD` unless the named product release authority, PayMongo account authority, and operations owner jointly supply a complete per-rail record. A positive decision is limited to one rail, one amount/cap, one redacted payment boundary, and one named operator; it does not authorize checkout, payout, another return, or broad availability.</decision>
  <context>Refund and manual-return actions can move money or create customer obligations. The executor is not authorized to call the refund API, send funds, select a provider destination, alter an account, or create a provider event. The account-authorized human/operator alone may perform a bounded operation after this gate; a missing field remains HOLD.</context>
  <options>
    <option id="hold">HOLD (default): preserve the existing safe state for the rail and record the accountable resolver.</option>
    <option id="bounded-return">Authorize one redacted, single-rail controlled return only when every authority, cap, stop/return, observation, reconciliation, and alert field is complete.</option>
  </options>
  <action>Re-read the checkout/recovery proof and reject the checkpoint if its local baseline or stop boundary is HOLD. Require, for the selected rail: named release authority, account authority, and operations owner; current entitlement; exact amount or cap; one redacted payment/participant boundary; executing operator; expected provider refund event or manual-return confirmation; pre-recorded provider-side stop/return action; reconciliation steps for booking and payout ledger; monitored alert destination; acknowledgement owner; escalation route; SLA; and communication policy. If a rail is unsupported or lacks any field, record `HOLD` or a concrete approved opt-out with fallback/owner and take no external action. If a complete explicit authorization is supplied, the named human performs exactly one bounded API refund or manual return, observes the provider and durable reconciliation results, records only redacted reference/outcome data, and stops new related activity through the documented provider boundary on mismatch. The executor updates the record but performs no provider action.</action>
  <how-to-verify>
    1. Confirm the selected rail is currently entitled and its full authority/cap/participant/operator record is current.
    2. Confirm the documented provider-side stop/return method can be used by the named account authority before the operation starts.
    3. If explicitly authorized, observe one provider refund event or manual-return confirmation and its booking/ledger reconciliation.
    4. Confirm the alert recipient acknowledges the result within the stated SLA and can follow the escalation route.
    5. If any observation differs, stop further action, use the named provider-side boundary, reconcile durable state, and write `HOLD` with its owner.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RETURN-PROOF.md','utf8').toLowerCase();for(const term of ['authority','cap','participant','operator','stop','return','reconciliation','alert','sla','hold','grabpay','verified','opted out'])if(!t.includes(term))throw new Error('missing return gate field '+term);console.log('controlled return checkpoint fields are present');"</automated>
    <fails_when>Fails when the command exits nonzero because a return-gate field, GrabPay disposition, or `HOLD` is absent; block the controlled-return decision, record the missing-field class and resolver, and retain `HOLD` with no external action.</fails_when>
  </verify>
  <done>The return record contains either a fully observed, owned, and reconciled single-rail controlled result or a clearly attributable HOLD/approved opt-out; no operation is authorized by default.</done>
  <resume-signal>Reply `HOLD` to preserve the safe state, or provide one complete redacted per-rail authorization record and explicit decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider refund/manual-return evidence → FitOut ledger state | A claimed return may be pending, failed, or post-payout and cannot be inferred from a request alone. |
| return rail policy → financial operation | The existing server policy rejects unknown rails; documentation must not bypass it. |
| operations alert → human response | A notification without an accountable acknowledgement/escalation path leaves a customer-money exception unowned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-10 | Tampering | refund rail selection | high | mitigate | Bind every proof to the existing fail-closed rail policy and require provider-observed evidence; unknown values remain HOLD/manual-return-only. |
| T-25.1-11 | Repudiation | manual return operation | high | mitigate | Blocking-human record captures authorities, cap, participant, operator, provider-side stop/return, reconciliation, communications, alert ownership, and SLA. |
| T-25.1-12 | Tampering | post-payout refund state | high | mitigate | Require durable booking/ledger reconciliation and the existing attention/escalation path rather than treating a request as a completed return. |
| T-25.1-13 | Information disclosure | return proof artifact | high | mitigate | Store redacted references/outcomes only and run the secret-shaped-material structural check. |
</threat_model>

<verification>
Validate per-rail matrix completeness and redaction with the Node checks. A controlled return is manual-only and blocking-human; the plan completes safely with a recorded HOLD or an approved opt-out when a full record is unavailable.
</verification>

<success_criteria>
- Each applicable checkout rail has a provider-proven return path or an approved, owned fallback; unknown remains HOLD.
- The existing rail allowlist and post-payout attention model are preserved unchanged.
- No refund/manual-return operation, provider-side configuration change, or secret handling is executed by the executor.
</success_criteria>

<output>
Create `.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-03-SUMMARY.md` when done.
</output>
