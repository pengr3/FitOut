---
phase: 25.1-paymongo-production-release-readiness-controlled-proofs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md
autonomous: false
requirements: [] # ROADMAP.md declares this inserted phase's requirements as TBD; no ID is invented.
user_setup:
  - service: Managed Node/npm toolchain
    why: "The current npm shim and local executable tree are unavailable; restoring the organization's trusted Node/npm distribution is outside repository control."
  - service: Local Docker Engine
    why: "The isolated fitout_test payment suite requires a reachable local Docker daemon."
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fresh local payment validation result is distinguished from Phase 25's historic result and is never implied when the executable or database prerequisites are unavailable."
    - "The phase owns a dated, non-secret ledger that records validation commands, aggregate outcomes, blockers, and the continuing HOLD disposition."
    - "No provider, console, credential, payment, refund, transfer, or payout action is performed while restoring local validation."
  artifacts:
    - path: .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md
      provides: "Redacted local-toolchain readiness, focused-suite, static-check, and HOLD evidence ledger."
  key_links:
    - from: "package.json scripts and lockfile-resolved local executables"
      to: "25.1-VALIDATION-EVIDENCE.md"
      via: "The exact inherited database, focused-test, ESLint, and TypeScript commands and their aggregate outcomes are recorded as fresh evidence only after they exit successfully."
    - from: "25.1-VALIDATION-EVIDENCE.md"
      to: "all later controlled-proof checkpoints"
      via: "A BLOCKED or failed local baseline leaves the release disposition at HOLD and prevents a financial checkpoint from being requested."
---

<objective>
Recover and reproduce the local PayMongo validation baseline while creating the phase's first non-secret evidence ledger.

Purpose: A production proof cannot rely on a historic test run or a broken local toolchain. This plan makes the local gate fresh and attributable without contacting a provider or moving money.
Output: `25.1-VALIDATION-EVIDENCE.md` with the exact validation contract, observed outcomes, redaction boundary, and safe-HOLD fallback.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RESEARCH.md
@.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
@.planning/phases/25-finalize-paymongo-production-payments/25-VERIFICATION.md
@.planning/phases/25-finalize-paymongo-production-payments/25-VALIDATION.md
@package.json
@package-lock.json
@vitest.config.ts
@docker-compose.yml
</context>

<interfaces>
The validation contract already exists: `package.json` supplies `db:up`, `db:test:setup`, and `test`; `vitest.config.ts` requires `fitout_test`, `tests/setup.ts`, and `tests/global-setup.ts`. The focused payment command is inherited verbatim from Phase 25; this plan does not change application, provider, test, or database interfaces.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the safe local-validation path into a redacted evidence record</name>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md</files>
  <action>Create the phase-local ledger as the end-to-end tracer from workstation prerequisites through the existing focused payment suite to an explicit release disposition. Include: a UTC run identifier; repository revision; Node, npm, local Vitest, TypeScript, ESLint, and Docker availability fields; the exact `db:up`, `db:test:setup`, inherited focused-suite, scoped ESLint, and TypeScript commands; aggregate exit/result fields; a separate historic-versus-fresh evidence label; and a mandatory `HOLD` result whenever a prerequisite or command is blocked or fails. Define a reference-only schema: permit only UTC timestamps, revision identifiers, command names, aggregate test counts/outcomes, opaque evidence IDs matching `E-25.1-[A-Z0-9-]+`, named owners, and bounded error classes. Require a comprehensive redaction scan that rejects, without printing the match, live/test public or secret keys, webhook secrets, Bearer/Basic credentials, token/password/API-key assignments, connection URLs, payment/customer/wallet/transaction identifiers, raw provider JSON, and environment-variable assignments with values; on detection record only the violation class, accountable owner, and `HOLD`. State that the ledger must never contain credentials, provider identifiers, customer data, payment identifiers, wallet data, raw payloads, or command output that could disclose them. Do not change package manifests, lockfiles, source, tests, Docker configuration, provider configuration, or external accounts.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const p='.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md';const t=fs.readFileSync(p,'utf8');const protectedPatterns=[/(?:sk|pk)_(?:live|test)_[A-Za-z0-9_-]{8,}/i,/whsec_[A-Za-z0-9_-]{8,}/i,/\b(?:bearer|basic)\s+[A-Za-z0-9._~+\/=:-]{12,}/i,/\b(?:api[_-]?key|access[_-]?token|client[_-]?secret|secret|password|token)\s*[:=]\s*['\"]?(?!redacted\b|reference\b)[A-Za-z0-9._~+\/=:-]{6,}/i,/\b(?:PAYMONGO|INNGEST|DATABASE|PLATFORM_WALLET)_[A-Z0-9_]*\s*=\s*\S+/i,/\b(?:mongodb(?:\+srv)?|postgres(?:ql)?|mysql|redis):\/\/[^\s]+/i,/\b(?:pay|pi|cs|cus|acct|wal|tr|evt)_[A-Za-z0-9_-]{8,}/i,/\"(?:data|attributes|metadata)\"\s*:\s*\{/i];for(const h of ['Run identity','Toolchain and database prerequisites','Focused payment validation','Static validation','Redaction boundary','Current disposition'])if(!t.includes(h))throw new Error('missing '+h);if(!t.includes('HOLD'))throw new Error('missing HOLD fallback');if(protectedPatterns.some((r)=>r.test(t)))throw new Error('protected-value class in ledger');console.log('validation evidence ledger structure and redaction boundary pass');"</automated>
    <fails_when>Fails when the command exits nonzero, a mandatory heading or `HOLD` is absent, or the scanner detects any protected-value or raw-payload class; stop the proof workflow, record only the generic failure class and accountable owner, and retain `HOLD`.</fails_when>
  </verify>
  <done>The ledger can represent both a fresh passing local baseline and a precise blocked state without upgrading Phase 25's historical evidence or exposing a protected value.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Recover the trusted local runtime only if the tracer records it unavailable</name>
  <what-built>The tracer has recorded whether `npm --version`, the lockfile-resolved local executables, and Docker are currently available; no external payment system has been touched.</what-built>
  <action>If the ledger records a broken npm shim, absent local executable tree, or unavailable Docker daemon, stop before any database or payment test. The workstation owner must restore the organization's approved Node/npm distribution and lockfile-controlled local dependency tree using its normal trusted software process, and make Docker reachable. Preserve `package.json` and `package-lock.json` byte-for-byte; do not add, update, substitute, or approve any package in this plan. After the owner reports completion, use read-only checks for `node --version`, `npm --version`, the three expected module paths, and `docker info --format '{{.ServerVersion}}'`, then update the ledger with aggregate availability only. This is a toolchain recovery gate, not financial authorization.</action>
  <instructions>Restore only the trusted workstation runtime, existing lockfile-resolved dependency tree, and local Docker availability through the owner's approved process. Do not share protected values, make a provider request, or change repository manifests/lockfiles. Return after the executor can run the read-only availability checks.</instructions>
  <how-to-verify>
    1. The workstation owner confirms the managed Node/npm installation and the existing locked dependency tree were restored through the organization's approved process.
    2. The owner confirms Docker is running locally; neither step requires sharing credentials or provider access.
    3. The executor checks versions, module-path presence, and Docker reachability without printing configuration or secrets.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('node:fs');for(const p of ['node_modules/vitest/vitest.mjs','node_modules/typescript/bin/tsc','node_modules/eslint/bin/eslint.js'])if(!fs.existsSync(p))throw new Error('missing '+p);console.log('local validation executables present');"</automated>
    <fails_when>Fails when the command exits nonzero because any required local executable is absent; do not run database-backed validation, record the toolchain-unavailable class and accountable recovery owner in the ledger, and retain `HOLD`.</fails_when>
  </verify>
  <verification>The executor validates Node/npm availability, the three local executable paths, and Docker reachability with read-only checks before it runs any database-backed command.</verification>
  <done>Either all required local runtime and Docker prerequisites are demonstrably available, or the ledger remains HOLD with the exact unavailable prerequisite and no partial test claim.</done>
  <resume-signal>Report `toolchain-ready` only after the approved local recovery is complete; otherwise report the blocker and retain HOLD.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run and record the inherited isolated payment validation baseline</name>
  <precondition>`npm --version` exits 0; `node_modules/vitest/vitest.mjs`, `node_modules/typescript/bin/tsc`, and `node_modules/eslint/bin/eslint.js` exist; and `docker info --format '{{.ServerVersion}}'` succeeds without exposing configuration.</precondition>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md</files>
  <action>Run `npm run db:up`, then `npm run db:test:setup`, before executing the inherited Phase 25 focused payment suite exactly as written. Next run the inherited scoped ESLint command and `node node_modules/typescript/bin/tsc --noEmit`. Record UTC time, command, exit status, aggregate suite result, and an error-class summary only; do not include raw logs, environment values, database URLs, payment data, or provider calls. If any command fails, stop the sequence at the failure boundary, record HOLD and the engineering owner, and do not use the historic Phase 25 pass as a substitute. A passing local baseline still leaves every live-money capability and release status at HOLD.</action>
  <verify>
    <automated>npm test -- tests/paymongo/webhook-signature.test.ts tests/paymongo/webhook-payment-paid.test.ts tests/paymongo/webhook-refund.test.ts tests/paymongo/webhook-merchant-activated.test.ts tests/paymongo/checkout-idempotency-real.test.ts tests/payments/checkout-create.test.ts tests/payments/confirm-idempotency.test.ts tests/payments/payment-reconcile.test.ts tests/payments/payment-reconcile-cadence.test.ts tests/payments/payout-sweep.test.ts tests/payments/payout-reconcile.test.ts</automated>
    <fails_when>Fails when the focused suite exits nonzero; stop the remaining validation sequence, record only the failed-stage class, aggregate result, and accountable engineering owner in the ledger, and retain `HOLD`.</fails_when>
    <automated>node node_modules/eslint/bin/eslint.js src/lib/paymongo.ts src/app/api/paymongo/webhook/route.ts src/lib/payments/confirm-booking-payment.ts src/app/actions/booking.ts src/lib/payments/checkout-probe.ts src/lib/payments/checkout-lease.ts src/lib/payments/retire-checkout.ts src/lib/payments/refund-rail.ts src/lib/payments/cancellation.ts src/inngest/functions/payment-reconcile.ts src/inngest/functions/payout-reconcile.ts src/inngest/functions/payout-sweep.ts src/app/api/inngest/route.ts src/lib/payments/config.ts</automated>
    <fails_when>Fails when scoped ESLint exits nonzero; stop the remaining validation sequence, record only the failed-stage class and accountable engineering owner in the ledger, and retain `HOLD`.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>Fails when TypeScript exits nonzero; stop validation, record only the failed-stage class and accountable engineering owner in the ledger, and retain `HOLD`.</fails_when>
  </verify>
  <done>The ledger has one fresh, redacted local-validation result with all prerequisite and command outcomes, and its final disposition remains HOLD regardless of a passing result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation tooling → isolated test database | A damaged or substituted local toolchain could create a false local-validation claim. |
| validation output → tracked evidence ledger | Command output can contain protected configuration or be mistaken for live provider evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-01 | Tampering | local Node/npm and executable availability | high | mitigate | Human-gated trusted runtime recovery, byte-preserved manifests/lockfile, explicit path checks, and a fresh command result before the ledger advances. |
| T-25.1-02 | Repudiation | validation evidence ledger | medium | mitigate | Record UTC, revision, exact command, aggregate outcome, blocker, and owner; distinguish historical from fresh evidence. |
| T-25.1-03 | Information disclosure | tracked validation evidence | high | mitigate | Allowlist aggregate evidence only and fail the structural check on secret-shaped key material. |
| T-25.1-04 | Spoofing | local test result as production proof | high | mitigate | The ledger's disposition remains HOLD and later financial checkpoints require independent provider, authority, reconciliation, and alert evidence. |
| T-25.1-SC | Tampering | dependency recovery | high | mitigate | This plan performs no package acquisition or manifest change; toolchain restoration is blocking-human and confined to the owner's approved locked-dependency process. |
</threat_model>

<verification>
Run the exact inherited focused payment suite, scoped ESLint command, and TypeScript command only after the isolated database precondition is met. Run the ledger structural/redaction check after each evidence update. A blocked, failed, or incomplete command is a successful detection of HOLD, not a release pass.
</verification>

<success_criteria>
- The local validation record is fresh and redacted, or it names the exact recovery blocker and stays HOLD.
- No source, package, provider, deployment, secret, or financial operation is changed or executed.
- Later plans can read one unambiguous local-baseline disposition rather than relying on historic Phase 25 evidence.
</success_criteria>

<output>
Create `.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-01-SUMMARY.md` when done.
</output>
