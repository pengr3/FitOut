---
phase: 25.1-paymongo-production-release-readiness-controlled-proofs
plan: 04
type: execute
wave: 3
depends_on: [25.1-02]
files_modified:
  - .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-PAYOUT-PROOF.md
autonomous: false
requirements: [] # The phase is intentionally unmapped while ROADMAP.md says requirements TBD.
user_setup:
  - service: PayMongo marketplace, linked-account, and wallet authority
    why: "Only an authorized provider account operator can prove entitlement, a redacted recipient correlation, a cancellation/return boundary, and a bounded transfer scope."
  - service: Money-alert operations
    why: "A named operations owner must prove monitored acknowledgement, escalation, and SLA for failed or stuck transfers."
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A payout stays HOLD unless provider entitlement, redacted recipient correlation, authority, cap, operator, cancellation/return boundary, observed read-back, reconciliation, and alert ownership are all evidenced."
    - "No source wallet, recipient, or transfer is inferred from code, configuration, or a first available account."
    - "Unknown or in-flight transfer status remains processing and prevents payout or broad-release clearance."
  artifacts:
    - path: .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-PAYOUT-PROOF.md
      provides: "Controlled-payout entitlement, observation, durable read-back, stop/return, recovery, and alert-ownership evidence contract."
  key_links:
    - from: "merchant activation and redacted host-to-wallet correlation"
      to: "payoutSweep"
      via: "The record requires the same eligible recipient boundary that existing code correlates before it can create a transfer."
    - from: "provider transfer read-back"
      to: "payoutReconcile"
      via: "A terminal provider-derived state may be reconciled; unknown/in-flight remains processing and produces owned alert handling."
    - from: "payout proof packet"
      to: "release decision"
      via: "Missing entitlement, stop/return procedure, or alert acknowledgement holds payout and broad availability."
---

<objective>
Prepare and gate the controlled host-payout proof path while retaining payout HOLD by default.

Purpose: Code-level sweep and reconciliation controls do not demonstrate marketplace entitlement or permit real transfer. This plan captures every external boundary that must exist before a human can decide on one bounded payout.
Output: `25.1-PAYOUT-PROOF.md` and a blocking-human payout decision with no inferred wallet, recipient, or launch authority.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RESEARCH.md
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md
@.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
@.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
@src/inngest/functions/payout-sweep.ts
@src/inngest/functions/payout-reconcile.ts
@src/lib/paymongo.ts
@src/app/api/inngest/route.ts
</context>

<interfaces>
Existing payout contract: `payoutSweep` selects an eligible due booking, requires a wallet correlated to that host's linked-account identity, creates a transfer, and changes the payout ledger from held to processing. `payoutReconcile.reconcileOne(row, dbConn?)` obtains provider transfer status; only recognized terminal values map to paid/failed while unknown/in-flight remains processing and can alert. This plan documents external evidence for those controls and never adds a payout route, worker, wallet selection strategy, or terminal-state mapping.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one safe payout-proof decision from entitlement to reconciled terminality</name>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-PAYOUT-PROOF.md</files>
  <action>Create a phase-local payout proof packet with a starting `HOLD` status. Its one-operation record must require: named product release authority, PayMongo account authority, and operations owner; provider-approved marketplace/linked-account and platform-wallet entitlement; a redacted activated merchant-to-host-to-recipient correlation; exact amount or cap; participant/booking boundary; executing operator; expected transfer observation; provider-side mechanism to stop new checkout activity; cancellation before irreversible cutoff; post-terminal return or escalation procedure; durable ledger read-back; payoutReconcile observation; monitored alert destination; acknowledgement owner; escalation route; and SLA. Model outcomes as provider-derived `paid`, `failed`, or `processing`; explicitly keep unknown/in-flight as `processing` and HOLD. Use the Plan 01 reference-only schema and its comprehensive non-printing redaction scan; record only an opaque evidence ID and generic violation class on scanner failure. Store redacted references only, include a concrete provider limitation/owner/fallback option, and prohibit defaulting to any wallet or recipient. Do not invoke a transfer, query a provider, alter configuration, or change source/test code.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const p='.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-PAYOUT-PROOF.md';const t=fs.readFileSync(p,'utf8');const protectedPatterns=[/(?:sk|pk)_(?:live|test)_[A-Za-z0-9_-]{8,}/i,/whsec_[A-Za-z0-9_-]{8,}/i,/\b(?:bearer|basic)\s+[A-Za-z0-9._~+\/=:-]{12,}/i,/\b(?:api[_-]?key|access[_-]?token|client[_-]?secret|secret|password|token)\s*[:=]\s*['\"]?(?!redacted\b|reference\b)[A-Za-z0-9._~+\/=:-]{6,}/i,/\b(?:PAYMONGO|INNGEST|DATABASE|PLATFORM_WALLET)_[A-Z0-9_]*\s*=\s*\S+/i,/\b(?:mongodb(?:\+srv)?|postgres(?:ql)?|mysql|redis):\/\/[^\s]+/i,/\b(?:pay|pi|cs|cus|acct|wal|tr|evt)_[A-Za-z0-9_-]{8,}/i,/\"(?:data|attributes|metadata)\"\s*:\s*\{/i];for(const h of ['Entitlement boundary','Recipient correlation','Controlled payout record','Provider-side stop and return','Terminal read-back','Reconciliation and recovery','Alert ownership','HOLD disposition'])if(!t.includes(h))throw new Error('missing '+h);for(const state of ['paid','failed','processing'])if(!t.toLowerCase().includes(state))throw new Error('missing state '+state);if(protectedPatterns.some((r)=>r.test(t)))throw new Error('protected-value class in payout proof');console.log('payout proof contract passes');"</automated>
    <fails_when>Fails when the command exits nonzero, a mandatory payout section/state is absent, or the scanner finds a protected-value or raw-payload class; stop the payout-proof workflow, record only the generic failure class and owner, and retain `HOLD`.</fails_when>
  </verify>
  <done>The payout packet can represent one completely bounded, provider-entitled payout with reconciliation or an accountable HOLD/limitation without ever selecting a wallet or recipient by default.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide one controlled host payout or retain HOLD</name>
  <decision>Retain `HOLD` unless the named product release authority, PayMongo account authority, and operations owner provide the complete payout record. Any positive decision permits only one amount/cap and one redacted correlated recipient boundary; it never authorizes another payout, wallet, checkout rail, refund, or broad release.</decision>
  <context>A payout is one-way external money movement. The executor may validate local documents and existing code contracts but must not call PayMongo, start or replay an Inngest run, select an account, or manipulate payout ledger data. The named human operator alone can perform the explicitly authorized operation after the blocking decision.</context>
  <options>
    <option id="hold">HOLD (default): record the missing payout boundary and leave all transfer paths uninvoked.</option>
    <option id="bounded-payout">Authorize one redacted, correlated payout only after every entitlement, authority, cap, stop/return, observation, reconciliation, and alert field is complete.</option>
  </options>
  <action>Before a decision, read the fresh validation, checkout/recovery, and payout proof records. Require current evidence of provider entitlement; named joint authorities; redacted activated-recipient correlation; one amount/cap and booking/participant boundary; executing operator; observed cancellation-before-cutoff method; return/escalation path after a terminal transfer; expected provider read-back; durable ledger reconciliation; payoutReconcile observation; monitored alert destination; acknowledgement owner; escalation route; and SLA. If one field is absent, stale, contradictory, or cannot be evidenced without exposing protected values, record HOLD with its resolver and do nothing externally. If every field is complete and the human explicitly chooses the bounded payout, the named operator performs only that transfer, observes the provider read-back and durable reconciliation, keeps unknown/in-flight results processing, exercises the alert response, and invokes the documented cancellation/return/escalation boundary at the first mismatch. The executor records redacted outcomes only and performs no provider operation.</action>
  <how-to-verify>
    1. Verify provider marketplace/linked-account and source entitlement without recording account values.
    2. Verify the redacted activated-recipient correlation and amount/cap are exactly the authorized one-operation scope.
    3. Verify the named authority can stop/cancel before the provider's irreversible cutoff and owns the post-terminal return/escalation route.
    4. If explicitly authorized, observe provider-derived transfer status, the matching durable ledger state, payout reconciliation, and alert acknowledgement by the named owner.
    5. Treat unknown/in-flight, missing acknowledgement, or any mismatch as `processing`/`HOLD`; stop further scope and follow the documented boundary.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-PAYOUT-PROOF.md','utf8').toLowerCase();for(const term of ['authority','entitlement','recipient','cap','operator','stop','cancellation','return','reconciliation','alert','sla','hold'])if(!t.includes(term))throw new Error('missing payout gate field '+term);console.log('controlled payout checkpoint fields are present');"</automated>
    <fails_when>Fails when the command exits nonzero because any payout-gate field is absent; block the controlled-payout decision, record the missing-field class and resolver, and retain `HOLD` with no external action.</fails_when>
  </verify>
  <done>The payout record has a complete, observed, and owned single-operation outcome or holds the payout track; no provider entitlement, recipient, or money movement is inferred or authorized by default.</done>
  <resume-signal>Reply `HOLD` to preserve the safe state, or provide the complete redacted payout authorization record and explicit one-operation decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PayMongo entitlement/wallet data → transfer selection | Provider account capability and recipient correlation are protected external facts, not repository defaults. |
| provider transfer read-back → payout ledger | A transfer-create response is not terminal; status must originate from provider read-back. |
| payout alert → operations response | Failed/stuck money needs a named acknowledgement and escalation owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-14 | Spoofing | payout entitlement/recipient | high | mitigate | Require account-authority evidence and redacted activated-recipient correlation; neither source nor a first available wallet can select a recipient. |
| T-25.1-15 | Tampering | transfer terminality | high | mitigate | Require provider read-back through existing payoutReconcile mapping; unrecognized/in-flight states remain processing. |
| T-25.1-16 | Repudiation | one-way payout authority | high | mitigate | Blocking-human record requires joint authorities, cap, operator, cancellation/return procedure, observation, reconciliation, alert owner, escalation, and SLA. |
| T-25.1-17 | Denial of service | stuck/failed payout response | high | mitigate | Require observed alert delivery and acknowledgement/escalation before the payout track may clear HOLD. |
| T-25.1-18 | Information disclosure | payout proof artifact | high | mitigate | Permit redacted references/outcomes only and scan the artifact for secret-shaped material. |
</threat_model>

<verification>
Use the packet structural/redaction check to confirm every financial boundary exists before the blocking checkpoint. The only automatic state transition is documentation validation; payer, provider, wallet, recipient, transfer, and console operations are outside executor authority.
</verification>

<success_criteria>
- The payout path remains unavailable unless its entitlement, correlation, authority, stop/return, provider read-back, reconciliation, and alert controls are all observed and owned.
- Unknown/in-flight transfer state is recorded as processing/HOLD, never terminal success.
- No PayMongo transfer, wallet selection, dashboard change, secret capture, or code workaround is performed by the executor.
</success_criteria>

<output>
Create `.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-04-SUMMARY.md` when done.
</output>
