---
phase: 25.1-paymongo-production-release-readiness-controlled-proofs
plan: 02
type: execute
wave: 2
depends_on: [25.1-01]
files_modified:
  - .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md
autonomous: false
requirements: [] # ROADMAP.md declares requirements TBD; preserve the no-fabricated-requirement precedent from Phase 25.
user_setup:
  - service: PayMongo production account
    why: "Only the authorized account owner can attest to current rail entitlement, provider-side stop authority, and one bounded operation."
  - service: Inngest production operations
    why: "A registered application does not establish an observed recovery run, alert delivery, or human acknowledgement."
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A controlled checkout can advance only from a complete, redacted authority record; otherwise it is explicitly HOLD."
    - "A browser return never counts as payment confirmation; the required proof is a verified signed provider delivery or bounded server-side provider probe reaching the existing single confirmation writer."
    - "Missed-webhook recovery and money-alert ownership are independently observed or remain HOLD; configuration and registration records alone are insufficient."
  artifacts:
    - path: .planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md
      provides: "Redacted controlled-checkout, signed-delivery, recovery, stop/return, reconciliation, and alert-ownership evidence contract."
  key_links:
    - from: "25.1-VALIDATION-EVIDENCE.md"
      to: "25.1-CHECKOUT-RECOVERY-PROOF.md"
      via: "A fresh passing local baseline is an explicit precondition; a blocked local baseline preserves HOLD."
    - from: "PayMongo signed event or provider probe"
      to: "confirmPaidBooking"
      via: "The existing raw-HMAC webhook path or paymentReconcile path remains the sole confirmation route; the proof record names the observed durable outcome once only."
    - from: "paymentReconcile and money alerts"
      to: "release decision"
      via: "Observed recovery result, monitored destination, acknowledgement owner, escalation route, and SLA are required before the checkout track can clear HOLD."
---

<objective>
Prepare a controlled-checkout and missed-webhook-recovery proof packet, then stop for explicit human authorization or HOLD.

Purpose: Phase 25 proved configured webhook and Inngest registration only. This plan distinguishes those prerequisites from the human/provider evidence needed to claim one bounded payment and recovery path.
Output: `25.1-CHECKOUT-RECOVERY-PROOF.md` plus a blocking-human decision record that never defaults to a live operation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-RESEARCH.md
@.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-VALIDATION-EVIDENCE.md
@.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
@.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
@src/app/api/paymongo/webhook/route.ts
@src/lib/payments/confirm-booking-payment.ts
@src/inngest/functions/payment-reconcile.ts
@src/app/api/inngest/route.ts
</context>

<interfaces>
Existing confirmation contract: `POST /api/paymongo/webhook` verifies raw-body HMAC and durable event identity before its paid-event handler calls `confirmPaidBooking(input, dbConn?)`. Existing recovery contract: `paymentReconcile` calls `reconcileOne(row, dbConn?)`, which reads provider state and converges on `confirmPaidBooking`; it is registered through `serve({ functions: [...] })` in `/api/inngest`. This plan documents proof for those paths and does not introduce any sender, callback, browser confirmation, worker, or state writer.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one safe checkout-proof decision from local gate to durable outcome</name>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md</files>
  <action>Create the phase-local proof packet with an initial `HOLD` disposition. Make one end-to-end record flow explicit: fresh local validation reference; named product release authority and PayMongo account authority; one approved rail observed on the same day; one amount and cap; one participant/cohort boundary; success signal; abort condition; provider-side stop method and authorized invoker; refund/manual-return owner; and redacted reference handling. Use the Plan 01 reference-only schema and its comprehensive non-printing redaction scan; record only an opaque evidence ID and generic violation class on scanner failure. State that confirmation evidence can be only a verified signed `checkout_session.payment.paid` delivery or a bounded server-side Checkout Session probe through the existing confirmation writer, never a browser navigation. Add a second recovery record requiring a registered `paymentReconcile` execution, provider-derived result, durable result, money-alert destination, acknowledgement owner, escalation route, and response SLA. Keep every value category redacted and make absent, stale, contradictory, or unexpected evidence resolve to HOLD without an external call.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const p='.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md';const t=fs.readFileSync(p,'utf8');const protectedPatterns=[/(?:sk|pk)_(?:live|test)_[A-Za-z0-9_-]{8,}/i,/whsec_[A-Za-z0-9_-]{8,}/i,/\b(?:bearer|basic)\s+[A-Za-z0-9._~+\/=:-]{12,}/i,/\b(?:api[_-]?key|access[_-]?token|client[_-]?secret|secret|password|token)\s*[:=]\s*['\"]?(?!redacted\b|reference\b)[A-Za-z0-9._~+\/=:-]{6,}/i,/\b(?:PAYMONGO|INNGEST|DATABASE|PLATFORM_WALLET)_[A-Z0-9_]*\s*=\s*\S+/i,/\b(?:mongodb(?:\+srv)?|postgres(?:ql)?|mysql|redis):\/\/[^\s]+/i,/\b(?:pay|pi|cs|cus|acct|wal|tr|evt)_[A-Za-z0-9_-]{8,}/i,/\"(?:data|attributes|metadata)\"\s*:\s*\{/i];for(const h of ['Authorization boundary','Controlled checkout record','Signed-delivery observation','Recovery observation','Provider-side stop and return','Alert ownership','HOLD disposition'])if(!t.includes(h))throw new Error('missing '+h);if(protectedPatterns.some((r)=>r.test(t)))throw new Error('protected-value class in proof packet');console.log('checkout and recovery proof contract passes');"</automated>
    <fails_when>Fails when the command exits nonzero, a required checkout/recovery section is absent, or the scanner finds a protected-value or raw-payload class; stop the checkout-proof workflow, record only the generic failure class and owner, and retain `HOLD`.</fails_when>
  </verify>
  <done>The proof packet can carry one fully bounded checkout/recovery observation or an attributable HOLD, with no route that treats configuration or a browser return as confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Turn Phase 25 evidence limits into objective checkout-track release criteria</name>
  <files>.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md</files>
  <action>Expand the proof packet into a cross-reference table for the Phase 25 rows: approved checkout rail, paid webhook, missed-webhook reconciliation, manual return/refund ownership, production secret boundary, Inngest operations and alerts, controlled transaction and rollback. For each row, record the Phase 25 evidence reference, current status, required fresh proof, accountable owner, safe-HOLD effect, and the exact artifact section that will hold the redacted result. Define objective checkout-track clearance as: fresh local baseline PASS; complete authorization record; one bounded operation observed through the signed-event or approved probe path; exactly one expected durable outcome; observed recovery/alert exercise; and a working provider-side stop/return boundary. Do not alter Phase 25 artifacts or claim that the current endpoint/function configuration fulfills any observation requirement.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md','utf8');for(const term of ['Paid checkout webhook','Missed-webhook reconciliation','Manual refund / clawback','Inngest operations and alerts','Controlled transaction and rollback','exactly one expected durable outcome'])if(!t.includes(term))throw new Error('missing '+term);console.log('checkout track coverage is complete');"</automated>
    <fails_when>Fails when the command exits nonzero because a required Phase 25 checkout-track row is absent; stop expansion of the packet, record the missing-row class and accountable owner, and retain `HOLD`.</fails_when>
  </verify>
  <done>Every checkout/recovery release criterion has a dated-evidence slot, owner, and explicit HOLD effect; no configuration-only record can clear the track.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Decide one controlled checkout/recovery proof or retain HOLD</name>
  <decision>Choose `HOLD` unless the accountable product release authority and PayMongo account authority jointly supply the complete current authorization and recovery record. A positive choice authorizes at most the single redacted boundary in the packet; it never authorizes another rail, participant, amount, refund, payout, or broad availability.</decision>
  <context>The default and safe outcome is HOLD. The executor must not create a Checkout Session, send a provider request, edit a console, trigger a job, resend a delivery, or handle payment details. Only an authorized human operator can perform the one approved provider-side operation, and any mismatch requires the pre-recorded stop/return procedure and an immediate return to HOLD.</context>
  <options>
    <option id="hold">HOLD (default): record the missing, stale, contradictory, or unowned field and make no external change.</option>
    <option id="bounded-proof">Authorize one bounded proof only after all listed fields are supplied in redacted form and the named authorities explicitly choose it.</option>
  </options>
  <action>Before asking for a decision, re-read the fresh validation ledger and proof packet. Require all of these fields to be present and current: named release authority; named PayMongo account authority; one approved rail; one exact amount or cap; participant/cohort boundary; operating human; expected signed-delivery or permitted probe observation; single durable outcome; abort condition; provider-side method to stop new checkout activity and its authorized invoker; refund/manual-return owner; recovery observation method; monitored alert destination; acknowledgement owner; escalation route; and SLA. If any field is missing, stale, contradictory, or not independently observable, record HOLD with the responsible resolver and do nothing externally. If every field is complete and the human explicitly selects the bounded proof, the human operator performs exactly that one operation, observes the signed delivery or approved probe plus its one durable result, observes the reconciliation/alert path, invokes the documented stop/return boundary on mismatch, and records only redacted references and outcomes. The executor then reconciles the packet; it never performs provider actions.</action>
  <how-to-verify>
    1. Compare the authorization record to every required field in the proof packet; a missing field means `HOLD`.
    2. If explicitly authorized, the named human performs one operation within the named rail, cap, participant, and time boundary.
    3. Observe a signed server-to-server delivery or allowed provider probe and exactly one durable result; do not accept a hosted-return page as proof.
    4. Observe the registered recovery/alert behavior and acknowledgement by its named owner.
    5. On any mismatch, use the named provider-side stop/return method, reconcile durable state, notify the named owner, and set the track back to `HOLD`.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-CHECKOUT-RECOVERY-PROOF.md','utf8');for(const term of ['Authority','rail','cap','participant','stop','return','reconciliation','alert','SLA','HOLD'])if(!t.toLowerCase().includes(term.toLowerCase()))throw new Error('missing decision field '+term);console.log('controlled checkout checkpoint fields are present');"</automated>
    <fails_when>Fails when the command exits nonzero because any checkpoint field is absent; block the decision, record the missing-field class and resolver, and retain `HOLD` with no external action.</fails_when>
  </verify>
  <done>The packet records either an explicit human-authorized, bounded checkout/recovery proof with all required observations and ownership, or HOLD; no operation is authorized by default.</done>
  <resume-signal>Reply `HOLD` to retain the safe state, or provide the complete redacted authorization record and explicit decision for one bounded proof.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PayMongo provider event/probe → FitOut confirmation | Untrusted network evidence must be authenticated and cannot be replaced by a browser return. |
| Inngest recovery/alert delivery → operations | Registered functions and logs do not prove an acknowledged human response. |
| human authorization record → external checkout | A financial scope and stop boundary must be supplied by accountable humans, not inferred from source or configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-05 | Spoofing | checkout confirmation | high | mitigate | Require raw-HMAC-signed delivery or bounded server-side probe through the existing sole writer; browser return is excluded as proof. |
| T-25.1-06 | Tampering | duplicate delivery and durable outcome | high | mitigate | Require observed event identity handling and exactly one durable result; reconcile against the existing event-deduped route and status-scoped writer. |
| T-25.1-07 | Repudiation | controlled checkout authority | high | mitigate | Blocking-human decision captures named authorities, bounds, abort, stop/return, observations, and redacted evidence reference. |
| T-25.1-08 | Denial of service | missed-webhook recovery | medium | mitigate | Require an observed paymentReconcile outcome plus monitored alert destination, acknowledgement owner, escalation, and SLA before clearance. |
| T-25.1-09 | Information disclosure | proof packet | high | mitigate | Record only redacted references and aggregate outcomes; structural scan rejects secret-shaped material. |
</threat_model>

<verification>
Validate the proof packet's field completeness and redaction boundary with its Node checks. The financial decision is never automated: a checkpoint can only produce an explicit human-bounded record or HOLD. Re-run the local-baseline checks from Plan 01 if their evidence is stale or blocked.
</verification>

<success_criteria>
- Controlled checkout and recovery have a complete redacted proof contract, not an inferred launch claim.
- The only automatic outcomes are documentation validation and HOLD recording; all provider actions remain outside executor authority.
- A future positive proof would demonstrate signed confirmation, single durable outcome, recovery/alert ownership, and provider-side stop/return readiness inside a one-operation limit.
</success_criteria>

<output>
Create `.planning/phases/25.1-paymongo-production-release-readiness-controlled-proofs/25.1-02-SUMMARY.md` when done.
</output>
