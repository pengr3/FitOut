# Phase 25.1 Controlled Checkout and Recovery Proof Packet

## Purpose and redaction boundary

This packet is a redacted, evidence-reference contract for one possible controlled checkout and its missed-webhook recovery exercise. It is not authorization to create a Checkout Session, contact a provider, invoke a job, change a console, or move money.

Record only an opaque evidence identifier, UTC observation time, aggregate outcome, accountable role, and a generic failure class. Do not record credentials, environment values, provider or customer identifiers, payment data, raw payloads, browser URLs, or command output. Before every update, run the comprehensive non-printing redaction scan from `25.1-VALIDATION-EVIDENCE.md`. If it fails, retain only the opaque evidence identifier and generic violation class, notify the accountable owner, and set this packet to **HOLD**.

## Authorization boundary

The fresh local-validation reference is `25.1-VALIDATION-EVIDENCE.md`. Its current local baseline is **HOLD** because `TYPESCRIPT_CHECK_FAILED`; historic Phase 25 configuration or registration evidence is not a substitute. A controlled checkout can advance only after a fresh local baseline is **PASS** and the following record is present, current, internally consistent, and independently observable:

| Required boundary | Redacted record required | Current state | Safe effect if absent, stale, contradictory, or unexpected |
| --- | --- | --- | --- |
| Product release authority | Named accountable role and same-day authorization reference | Not supplied | **HOLD**; no external call |
| PayMongo account authority | Named accountable role and same-day authorization reference | Not supplied | **HOLD**; no external call |
| Operating human | Named accountable role | Not supplied | **HOLD**; no external call |
| Approved rail | One provider-approved rail category observed on the same day | Not supplied | **HOLD**; no external call |
| Amount boundary | One exact amount or cap, recorded as a redacted category | Not supplied | **HOLD**; no external call |
| Participant boundary | One approved participant or cohort category | Not supplied | **HOLD**; no external call |
| Time and abort boundary | One time window and abort condition | Not supplied | **HOLD**; no external call |

No field may be inferred from source, a dashboard, a hosted return, an earlier run, configuration, or job registration. Only the named accountable humans may authorize the one bounded operation, and this packet never expands that authorization to another rail, participant, amount, refund, transfer, payout, or broad availability.

## Controlled checkout record

| Field | Redacted evidence slot | Current state |
| --- | --- | --- |
| Local baseline reference | Opaque evidence identifier and aggregate PASS result | **HOLD** — baseline is not PASS |
| Joint authority reference | Opaque same-day authorization reference | Not supplied |
| Rail and same-day entitlement | Redacted rail category and observation reference | Not supplied |
| Amount or cap | Redacted bounded category | Not supplied |
| Participant or cohort | Redacted boundary category | Not supplied |
| Success signal | Verified signed delivery or permitted server-side probe, plus one durable result | Not observed |
| Abort condition | Redacted condition category and accountable operator | Not supplied |

The permitted success signal is exclusively either a verified signed `checkout_session.payment.paid` delivery through the existing raw-body HMAC and durable event-identity path, or a bounded server-side Checkout Session probe that converges through the existing `confirmPaidBooking` writer. A browser navigation or hosted-return page is presentation only and never payment-confirmation evidence. The expected durable result is exactly one status-scoped confirmation outcome from that existing writer; a duplicate, missing, conflicting, or additional durable outcome is an abort condition and returns this packet to **HOLD**.

## Signed-delivery observation

| Requirement | Redacted evidence slot | Current state |
| --- | --- | --- |
| Signed delivery or permitted probe | Opaque evidence identifier and aggregate verification result | Not observed |
| Existing sole confirmation path | `confirmPaidBooking` aggregate outcome only | Not observed |
| Durable event-identity handling | Aggregate deduplication result only | Not observed |
| Exactly one durable outcome | Aggregate single-result assertion only | Not observed |
| Browser-return exclusion | Operator attestation that no browser return was treated as confirmation | Not observed |

The signed delivery must be authenticated before its payment evidence is accepted. A permitted probe must be provider-derived and bounded to the authorized checkout record. Neither observation creates a second state writer, callback, browser confirmation path, worker, or request sender.

## Recovery observation

The missed-webhook exercise is independent of the checkout observation. It requires an observed execution of the registered `paymentReconcile` path, a provider-derived result, and a durable result through the same existing confirmation writer. Function registration, schedule configuration, logs without a run, and a browser return do not satisfy this record.

| Requirement | Redacted evidence slot | Current state | Safe effect if absent, stale, contradictory, or unexpected |
| --- | --- | --- | --- |
| Registered `paymentReconcile` execution | Opaque run reference and aggregate execution result | Not observed | **HOLD**; do not trigger a job |
| Provider-derived recovery result | Opaque evidence identifier and aggregate result | Not observed | **HOLD**; do not probe externally |
| Durable reconciliation result | Aggregate `confirmPaidBooking` outcome only | Not observed | **HOLD**; no release claim |
| Single-result reconciliation check | Aggregate exactly-one assertion | Not observed | **HOLD**; investigate through accountable owner |

## Provider-side stop and return

| Boundary | Redacted evidence slot | Current state |
| --- | --- | --- |
| Stop new checkout activity | Provider-side method category and authorized invoker role | Not supplied |
| Mismatch response | Abort condition, stop invocation record, and reconciliation instruction | Not supplied |
| Refund or manual-return path | Named owner and redacted procedure reference | Not supplied |
| Return verification | Aggregate reconciliation outcome and owner acknowledgement | Not observed |

On any mismatch, the authorized human invokes the pre-recorded provider-side stop method, follows the owned refund or manual-return procedure when applicable, reconciles durable state, notifies the accountable owner, and returns the checkout track to **HOLD**. This packet does not identify a provider control, invoke it, or authorize a refund or return.

## Alert ownership

| Requirement | Redacted evidence slot | Current state |
| --- | --- | --- |
| Monitored money-alert destination | Redacted destination category | Not supplied |
| Acknowledgement owner | Named accountable role | Not supplied |
| Escalation route | Redacted route category | Not supplied |
| Response SLA | Redacted SLA category | Not supplied |
| Observed alert and acknowledgement | Opaque evidence identifier and aggregate outcome | Not observed |

An alert configuration or registered Inngest application is not alert delivery or acknowledgement evidence. The recovery record cannot clear until the monitored destination, acknowledgement owner, escalation route, response SLA, and an observed acknowledgement are all current and attributable.

## HOLD disposition

**HOLD**

The fresh local baseline is not PASS, and the accountable authority, approved rail, cap, participant boundary, operating human, signed-delivery or probe observation, exactly-one durable outcome, recovery observation, provider-side stop boundary, manual-return owner, and alert ownership records are not supplied. Therefore no controlled checkout or recovery proof is authorized, and no provider, Inngest, payment, refund, transfer, payout, credential, console, or configuration action may be taken from this packet.
