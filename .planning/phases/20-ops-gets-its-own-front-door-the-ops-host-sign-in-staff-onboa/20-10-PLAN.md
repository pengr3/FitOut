---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 10
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - tests/auth/stale-session-selfheal.test.ts
  - tests/design/dark-scope.test.ts
  - tests/design/ops-guard-coverage.test.ts
  - tests/security/paymongo-seam.test.ts
  - src/lib/session-check.ts
  - src/lib/ops/staff.ts
  - src/app/auth/session-check/route.ts
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/dev-throw-auth/page.tsx
autonomous: true
requirements: [OPS-07]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After Plan 20-01's isolated rename commit, every tracked test and load-bearing source comment points to `src/proxy.ts` and the named `proxy` export before Host logic is added."
    - "The retarget changes no runtime expression, JSX node, test meaning, assertion count, or authorization boundary."
  artifacts:
    - path: tests/design/ops-guard-coverage.test.ts
      provides: "Tracked Proxy source/export reference while retaining page/action guards as authorization"
    - path: src/lib/ops/staff.ts
      provides: "Three-layer guard explanation retargeted to the Next.js 16 Proxy source"
  key_links:
    - from: tests/auth/stale-session-selfheal.test.ts
      to: src/proxy.ts
      via: "direct tracked-source import/reference"
      pattern: "src/proxy.ts|proxy"
---

<objective>
Retarget the remaining tests and load-bearing comments immediately after the isolated Proxy rename and before the exact-host partition.

Purpose: keep source-of-truth references accurate without mixing mechanical retargeting into either the convention rename commit or later Host behavior.
Output: four retargeted test contracts and five comment-only source updates.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@AGENTS.md
</context>

## Artifacts this phase produces

- Tracked-source references to `src/proxy.ts` in the four existing regression/design suites.
- Comment-only path/export retargeting in the five existing source files.

<tasks>

<task type="auto">
  <name>Task 1: Retarget test contracts to the tracked Proxy source</name>
  <reversibility rating="reversible">These are mechanical source-reference updates with no runtime behavior.</reversibility>
  <files>tests/auth/stale-session-selfheal.test.ts, tests/design/dark-scope.test.ts, tests/design/ops-guard-coverage.test.ts, tests/security/paymongo-seam.test.ts</files>
  <read_first>
    - tests/auth/stale-session-selfheal.test.ts
    - tests/design/dark-scope.test.ts
    - tests/design/ops-guard-coverage.test.ts
    - tests/security/paymongo-seam.test.ts
    - src/proxy.ts
  </read_first>
  <action>Replace stale tracked-source references to the retired convention with `src/proxy.ts` and the `proxy` export in these four test files. Preserve every test's semantic claim, mutation description, inventory count, and security boundary: Proxy is routing/optimistic behavior and never the authorization boundary. Do not change expected counts or weaken any assertion to accommodate the rename.</action>
  <verify>
    <automated>npx vitest run tests/auth/stale-session-selfheal.test.ts tests/design/dark-scope.test.ts tests/design/ops-guard-coverage.test.ts tests/security/paymongo-seam.test.ts</automated>
    <fails_when>The command exits non-zero, or the summary reports 0 tests or 0 passed.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four suites load the tracked Proxy origin and preserve their prior assertion totals.
    - `tests/design/ops-guard-coverage.test.ts` still states that page/action guards, not Proxy, enforce staff authorization.
    - No test reads `.next/server/middleware-manifest.json` as source truth.
  </acceptance_criteria>
  <done>The existing regression and design contracts follow the Next.js source rename without changing what they prove.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget source-of-truth comments without changing runtime bytes</name>
  <reversibility rating="reversible">Only comments and source-path citations change.</reversibility>
  <files>src/lib/session-check.ts, src/lib/ops/staff.ts, src/app/auth/session-check/route.ts, src/app/(auth)/layout.tsx, src/app/(auth)/dev-throw-auth/page.tsx</files>
  <read_first>
    - src/lib/session-check.ts
    - src/lib/ops/staff.ts
    - src/app/auth/session-check/route.ts
    - src/app/(auth)/layout.tsx
    - src/app/(auth)/dev-throw-auth/page.tsx
    - src/proxy.ts
  </read_first>
  <action>Update only the tracked-path and exported-name citations that point readers at the retired convention. Keep the expensive QK-IR9 30-day lockout explanation, the forgeable `_sc` tradeoff, the no-database/no-auth boundary, and the three-layer staff guard language intact. Do not alter imports, expressions, JSX, route behavior, or the positive `role === "staff"` predicate.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>The command exits non-zero or any TypeScript diagnostic is printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every changed line in the five files is comment text naming `src/proxy.ts` or the Proxy convention.
    - `git diff -U0` for these files contains no changed executable token or JSX node.
    - TypeScript completes with zero diagnostics.
  </acceptance_criteria>
  <done>The load-bearing explanations point to the tracked Next.js 16 source before Host routing begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked source -> tests/comments | Security reasoning must follow the real Proxy source rather than an ignored runtime mirror or retired path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-10-01 | Tampering | test retargeting | medium | mitigate | Preserve assertions/counts and run all four focused suites after path/export changes. |
| T-20-10-02 | Elevation of Privilege | Proxy/ops guard commentary | high | mitigate | Retain the explicit rule that Proxy routes only and page/action staff guards authorize. |
| T-20-10-03 | Information Disclosure | stale source references | low | accept | References disclose no secret; retarget them so later security work follows the tracked source. |
| T-20-10-SC | Tampering | package installs | high | mitigate | This plan changes no manifest or lockfile and authorizes no dependency install. |
</threat_model>

<verification>
Run the four focused suites followed by `npx tsc --noEmit`; inspect the scoped diff so source changes are comment-only.
</verification>

<success_criteria>
- All remaining tracked references name `src/proxy.ts` and `proxy`.
- Test semantics and runtime source bytes are unchanged.
- Plan 20-02 can add Host routing only after this retarget slice completes.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-10-SUMMARY.md` when done.
</output>
