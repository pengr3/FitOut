---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 09
type: execute
wave: 8
depends_on: [20-06, 20-07, 20-08]
files_modified:
  - scripts/verify-ops-local-state.mjs
  - scripts/verify-ops-cloak.mjs
  - tests/auth/ops-cloak-probe.test.ts
  - tests/design/ops-host-invariants.test.ts
  - e2e/ops-auth.spec.ts
  - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
  - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-VALIDATION.md
  - .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md
autonomous: false
requirements: [OPS-07, OPS-08, OPS-09, OPS-10, OPS-11, OPS-12]
user_setup:
  - service: Vercel custom domain
    why: "Production must terminate TLS and route the exact `ops.` hostname to the FitOut project."
    env_vars:
      - name: OPS_APP_URL
        source: "Exact HTTPS ops origin in each Vercel environment; never a wildcard or request-derived value."
      - name: NEXT_PUBLIC_APP_URL
        source: "Exact marketplace origin already configured for the same environment."
    dashboard_config:
      - task: "Assign the named ops subdomain to the project, publish the required DNS record, and wait for a valid certificate."
        location: "Vercel project -> Settings -> Domains"
  - service: Resend
    why: "A real staff invitation must leave the application and arrive at a permitted recipient."
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend API key for the deployed environment."
      - name: EMAIL_FROM
        source: "Verified sender on the Resend domain."
    dashboard_config:
      - task: "Verify the sender domain and choose a real allowed recipient for UAT; do not use seeded `@fitout.test` delivery as evidence."
        location: "Resend -> Domains and API Keys"
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The legacy `host@fitout.test` conflict is removed only after a separate staff identity is onboarded; the host keeps `canHost=true` and loses only staff authorization."
    - "Every supported exact host round-trips sign-in, reset, and invitation URLs inside its own origin; a session presented to the other host is anonymous/nonstaff."
    - "The final post-route cloak reading records staff 200 plus nonstaff/signed-out/nonexistent 404 with byte-identical three-way denial hashes for every new ops route."
    - "Marketplace `/ops` and all internal ops-auth targets are cloaked, ops `/api/auth/*` does not expose an `INVALID_ORIGIN` oracle, and no `(ops)`-scoped `not-found.tsx` exists."
    - "A real delivered invitation on the configured production ops host can be inspected without consumption, accepted exactly once, and used to sign in as staff-only."
  artifacts:
    - path: scripts/verify-ops-local-state.mjs
      provides: "read-only staff/capability split assertion without printing PII"
    - path: .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
      provides: "partition and final cloak transcripts, host/session round trips, state remediation, and live-service UAT"
    - path: .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-VALIDATION.md
      provides: "final Nyquist command/owner/status matrix"
  key_links:
    - from: scripts/verify-ops-cloak.mjs
      to: all Phase 20 routes
      via: "non-empty stage-final route census with status/header/raw-byte SHA-256 rows"
    - from: e2e/ops-auth.spec.ts
      to: src/lib/app-origins.ts
      via: "same-origin auth/reset/invite round trips and cross-host anonymity"
    - from: production invitation
      to: Resend and ops sign-in
      via: "real received link, read-only GET, one POST acceptance, then email/password sign-in"
---

<objective>
Close Phase 20 with stored-state separation, the mandatory second production cloak reading, host/session invariant evidence, and live Vercel/Resend UAT.

Purpose: The phase is complete only when the new front door works in the deployed environment, the old test identity no longer violates exclusivity, and the route-existence cloak is re-proven after every route exists.
Output: State verifier, final cloak/host/session evidence, completed validation matrix, and human-approved live-service results.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-VALIDATION.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `scripts/verify-ops-local-state.mjs` with `--report` and `--require-separated-host` flags.
- Final-route extensions to `scripts/verify-ops-cloak.mjs --stage final`.
- Host/session/reset/invite round-trip scenarios in `e2e/ops-auth.spec.ts`.
- `20-EVIDENCE.md` sections `Stored-state separation`, `Final cloak reading`, `Host/session round trips`, and `Live service UAT`.
- A completed `20-VALIDATION.md` and local-env decision-memory note.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate the legacy host/staff test identity after onboarding a replacement</name>
  <files>scripts/verify-ops-local-state.mjs, e2e/ops-auth.spec.ts, .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md, .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md</files>
  <read_first>
    - scripts/ops-grant.ts
    - scripts/seed.ts
    - src/lib/ops/grant.ts
    - e2e/ops-auth.spec.ts
    - .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <precondition>A local/test PostgreSQL database is reachable through the repository's configured `DATABASE_URL`, and any nonlocal environment remains read-only until the human checkpoint.</precondition>
  <behavior>
    - A separate staff-only identity is accepted before the legacy combined identity loses staff.
    - `host@fitout.test` retains host capability and is no longer staff; the replacement is staff with neither marketplace capability.
    - The verifier reports counts/booleans and stable ids only, never addresses, cookies, tokens, or password material.
    - Production conflicts are reported for a human decision and are never silently mutated by this task.
  </behavior>
  <action>Add a read-only local-state verifier that asserts at least one active staff user, zero staff users with `canBook` or `canHost`, and—when `--require-separated-host` is set—the known seeded host retains `canHost=true` and `isStaff=false`; redact email/token/cookie/password fields from all output. Extend the E2E fixture to sign in as the existing local bootstrap staff, issue an invitation for a separate staff-only test address, inspect and accept it through the shipped UI, verify it can reach `/ops`, then revoke only the legacy host's staff role through the shipped management surface while preserving host capability. Never clear the legacy capability or mutate a nonlocal database automatically. Record before/after nonsecret assertions in `20-EVIDENCE.md`, and append the dated local-env remediation to the tracked todo beside the D-14/D-275 supersession note.</action>
  <reversibility rating="costly">Onboarding creates an identity and audit history; revoking the legacy staff grant is reversible through the retained CLI, but must follow successful replacement onboarding.</reversibility>
  <verify>
    <automated>node scripts/verify-ops-local-state.mjs --require-separated-host</automated>
    <fails_when>The command exits non-zero, reports zero staff, reports a capability-bearing staff account, finds the seeded host still staff, finds its host capability cleared, or emits an email/token/cookie/password field.</fails_when>
    <automated>npx playwright test e2e/ops-auth.spec.ts --project=chromium --grep "onboards a separate staff identity before removing legacy staff" --reporter=list</automated>
    <fails_when>The command exits non-zero, reports 0 passed, revokes before replacement sign-in succeeds, or changes the legacy host capability.</fails_when>
  </verify>
  <acceptance_criteria>
    - Evidence order is replacement invite -> acceptance -> replacement `/ops` 200 -> legacy staff revoke -> separated-state assertion.
    - `host@fitout.test` remains a host and is not staff; at least one different staff-only account exists.
    - The verifier cannot mutate and its output contains only counts, booleans, and stable ids.
    - Any production conflict is recorded as pending human action, not converted/revoked by automation.
  </acceptance_criteria>
  <done>The local/test state satisfies OPS-11 without creating a staff lockout or damaging the host identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the final complete-route cloak and host/session invariant suites</name>
  <files>scripts/verify-ops-cloak.mjs, tests/auth/ops-cloak-probe.test.ts, tests/design/ops-host-invariants.test.ts, e2e/ops-auth.spec.ts, .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md</files>
  <read_first>
    - scripts/verify-ops-cloak.mjs
    - tests/auth/ops-cloak-probe.test.ts
    - tests/design/ops-host-invariants.test.ts
    - e2e/ops-auth.spec.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
    - src/proxy.ts
    - src/app/_ops-cloak/page.tsx
    - src/app/not-found.tsx
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - Final census is non-empty and includes `/ops`, `/login`, `/forgot-password`, `/reset-password`, `/invite/:token`, every internal `/_ops-auth/*` target, and `/api/auth/*` on both relevant hosts.
    - Staff `/ops` is 200; nonstaff, signed-out, nonexistent, marketplace `/ops`, and disallowed internal targets are 404 before streaming.
    - Required denial bodies are compared as raw bytes and have identical SHA-256; ops auth does not return `403 INVALID_ORIGIN`.
    - Public and ops hosts each keep auth/reset/invite URLs on their own allowed origin and reject the other host's session.
  </behavior>
  <action>Extend the stage-final probe census from actual route files and make an empty/missing expected set a hard error. After `npm run build`, start the production server and capture status, selected cache/content headers, byte length, and SHA-256 of unmodified bodies for staff/nonstaff/signed-out/nonexistent controls on every completed ops route and both hosts. Record a separate status assertion before hash comparison so streamed `200` with 404 markup fails. Exercise `/api/auth/*` on the exact ops origin and fail on `403 INVALID_ORIGIN`. Extend E2E/design invariants so login, recovery, reset email URL, invite email URL, and acceptance redirect remain within their initiating/configured origin, while presenting each host's cookie on the other yields anonymous/nonstaff. Assert the sole top-level cloak/root 404 remains static and no `(ops)` or `(ops-auth)` scoped `not-found.tsx` exists. Write the final transcript after—not instead of—the partition transcript.</action>
  <reversibility rating="reversible">This extends executable probes and evidence; any routing defect is repairable without stored-data migration.</reversibility>
  <verify>
    <automated>node scripts/verify-ops-cloak.mjs --check-evidence .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md --stage final</automated>
    <fails_when>The command exits non-zero, the final route set is empty/incomplete, a denial status differs from 404, staff differs from 200, auth returns `INVALID_ORIGIN`, or required denial hashes differ.</fails_when>
    <automated>npx playwright test e2e/ops-auth.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>The command exits non-zero, reports 0 passed, a reset/invite/callback escapes its allowed origin, or either host accepts the other's session.</fails_when>
  </verify>
  <acceptance_criteria>
    - `20-EVIDENCE.md` retains both separately timestamped `Partition reading` and `Final cloak reading` sections.
    - Final rows cover every visible/internal ops route on both host classes plus `/api/auth/*`, with status recorded separately from body hash.
    - All denial controls required by OPS-12 have one SHA-256 value and staff control is 200.
    - Host round-trip and cross-host-cookie tests implement the promoted request-surface/session-context invariant.
    - Source/design gates find exactly one `src/proxy.ts`, one top-level cloak, static root 404, and zero scoped ops not-found files.
  </acceptance_criteria>
  <done>The complete Phase 20 route set passes OPS-07/08/12's status, byte-cloak, origin, and session isolation contracts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify the deployed ops domain and a real delivered one-time invitation</name>
  <files>.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md, .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-VALIDATION.md</files>
  <read_first>
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-VALIDATION.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
    - .env.example
  </read_first>
  <precondition>Vercel serves the exact production ops host with valid TLS and exact origin variables, Resend has a verified sender, and a real permitted recipient is available.</precondition>
  <what-built>The exact-host Proxy/auth boundary, ops sign-in/recovery/sign-out, one-time staff invitation journey, transactional staff policy, two-section management panel, and final automated cloak/session evidence.</what-built>
  <how-to-verify>Use the deployed exact ops origin and a real Resend-deliverable address to perform the ordered checks in this task: domain/TLS, staff sign-in, conflict census, invite delivery, GET non-consumption, one POST acceptance, second-use inactivity, new-staff sign-in, cross-host anonymity, sign-out, self/last reasons, persistent management outcomes, and 320px/1280px inspection. Redact every secret and address in the evidence.</how-to-verify>
  <action>On the deployed exact ops origin, confirm TLS/domain assignment and sign in as an existing authorized staff member. Read-only census production for capability-bearing staff; if any conflict exists, stop for a named operator decision, onboard a separate staff account first, and never silently convert/revoke. Invite the real recipient, confirm delivery through Resend, open the received absolute ops-host link with GET, verify it remains active, submit name/password once, verify the same link is inactive afterward, then sign in on the ops host. Confirm cross-host anonymity, explicit sign-out confirmation, self/last visible disabled reasons, persistent Invite/Resend/Cancel/Revoke results, and the operational queue preceding the two-section panel. Inspect login/recovery/invite/roster/dialog at 320px and 1280px, including long email/refusal text and keyboard focus. Record nonsecret observations and timestamps only; redact addresses, raw tokens, cookies, passwords, and provider payloads. Mark `20-VALIDATION.md` compliant only when every automated and human row has evidence.</action>
  <reversibility rating="costly">The live invitation creates a real staff identity and audit history; use an approved recipient and revoke later only through the protected policy.</reversibility>
  <verify>
    <automated>node scripts/verify-ops-cloak.mjs --check-evidence .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md --stage final</automated>
    <fails_when>The command exits non-zero, final production rows or live-UAT attestations are missing, a secret/PII pattern is present, or any required status/hash differs.</fails_when>
    <human-check>Approve only after the real invite arrives, GET leaves it active, one POST consumes it, recipient signs in at the exact ops origin, sessions remain host-isolated, self/last reasons are visible, and 320px/1280px UI checks pass.</human-check>
  </verify>
  <acceptance_criteria>
    - Production domain has valid TLS and exact configured origin; no wildcard/forwarded-host derivation is used.
    - A real Resend-delivered link stays active after GET, accepts once, becomes uniformly inactive, and creates staff-only access.
    - Production capability conflicts are absent or explicitly resolved after replacement onboarding; no automated production conversion occurred.
    - 320px and 1280px checks pass for auth, invite, roster, dialogs, long text, target size, focus, and no horizontal page scroll.
    - Evidence/validation contain no email, raw token, cookie, password, secret, or provider response body.
  </acceptance_criteria>
  <done>Human confirms the deployed host, delivery, single-use onboarding, authorization safety, cloak, and responsive UI; validation is fully evidenced.</done>
  <resume-signal>Reply `approved` after every acceptance criterion is recorded in `20-EVIDENCE.md`, or provide the failing criterion and observed nonsecret result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DNS/TLS/Vercel -> Proxy | The configured production hostname becomes the request-surface authority. |
| Production browser/email -> deployed app | Real cookies and bearer links exercise origin, scanner, and acceptance boundaries. |
| Production systems -> evidence | Operational observations must be recorded without secrets or PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-09-01 | Spoofing | production Host/origin | high | mitigate | Verify exact DNS/TLS/origin configuration and final exact-host matrix; reject wildcard/forwarded derivation. |
| T-20-09-02 | Elevation of Privilege | cross-host cookie | high | mitigate | Run two-direction deployed session-isolation assertions. |
| T-20-09-03 | Information Disclosure | route-existence cloak | high | mitigate | Check denial status separately, then compare unmodified bytes/SHA-256 over complete route census. |
| T-20-09-04 | Tampering / Denial of Service | real invitation | high | mitigate | Verify GET read-only, one POST winner, second use inactive, and production conflict checks. |
| T-20-09-05 | Information Disclosure | evidence/audit | high | mitigate | Record only timestamps, status, headers, hashes, counts, booleans, and stable ids; run secret/PII scan. |
| T-20-09-06 | Denial of Service | production staff remediation | high | mitigate | Onboard and verify replacement first; block silent automated production mutation and preserve CLI recovery. |
| T-20-09-SC | Tampering | package installs | high | mitigate | No package install; final build/tests must leave manifests and lockfile unchanged. |
</threat_model>

<verification>
- `node scripts/verify-ops-cloak.mjs --check-evidence .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md --stage final`
- `npx playwright test e2e/ops-auth.spec.ts --project=chromium --reporter=list`
- `npx vitest run tests/ops/staff-policy.test.ts tests/ops/staff-invitation.test.ts tests/ops/staff-management-actions.test.ts`
- `npx vitest run --config vitest.design.config.ts tests/design/ops-host-invariants.test.ts tests/design/ops-guard-coverage.test.ts tests/design/loading-coverage.test.ts`
- `npm run build`
- Failure direction: any command exits non-zero, any suite reports 0 passed, the route census is incomplete, a required status/hash/origin/session assertion differs, or the production build fails.
</verification>

<success_criteria>
- Stored test state and approved production state contain no capability-bearing staff identity and never pass through zero active staff.
- Both host origins round-trip their own auth URLs and reject the other host's session.
- The post-route cloak evidence includes every new route and proves required 200/404/raw-byte properties.
- A real invitation is delivered, remains after GET, accepts once, and signs the new staff-only identity into the deployed ops host.
- Final validation and evidence are complete and contain no secret/PII/token material.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Own ops address, own sign-in context, application-based colleague onboarding without production DB credentials | 20-01 through 20-09; final proof in this plan Tasks 2-3 |
| REQ | OPS-07 exact ops host and marketplace cloak | 20-01, 20-02, 20-08, 20-09 |
| REQ | OPS-08 host-scoped sign-in/session | 20-03, 20-08, 20-09 |
| REQ | OPS-09 invitation/onboarding and retained CLI | 20-05, 20-06, 20-07, 20-09 |
| REQ | OPS-10 self/last refusal | 20-04, 20-07, 20-09 |
| REQ | OPS-11 staff/marketplace exclusivity and recorded reversal | 20-04, 20-05, 20-07, 20-09 |
| REQ | OPS-12 complete-route second cloak reading | 20-02 partition reading; 20-09 final reading |
| RESEARCH | Proxy rename first, exact host/origin authority, no auth/database import in Proxy | 20-01 Task 1; 20-02 Tasks 1-2 |
| RESEARCH | Dynamic Better Auth origin, host-only cookies, safe callback, no role cookie cache | 20-03; 20-04 Task 3; 20-09 Task 2 |
| RESEARCH | Verification-row state machine, no new table, guarded single-use transaction, real email delivery | 20-05; 20-06; 20-09 Task 3 |
| RESEARCH | Serialized conditional staff policy, explicit CLI conversion, state remediation | 20-04; 20-09 Task 1 |
| RESEARCH | One-page roster, independent ordering, persistent feedback, absolute cross-host exits | 20-07; 20-08 |
| RESEARCH | Two cloak reads, complete census, status-before-hash, deployed domain/Resend UAT | 20-02 Task 3; 20-09 Tasks 2-3 |
| CONTEXT | D-01 email/password only and invitation-only staff onboarding | 20-03 Tasks 2-3; 20-08 Task 2 |
| CONTEXT | D-02 familiar auth structure with explicit FitOut Ops identity | 20-03 Task 2; 20-08 Tasks 2-3 |
| CONTEXT | D-03 recovery journey stays on ops host | 20-03 Task 3; 20-09 Task 2 |
| CONTEXT | D-04 safe ops-only callback | 20-03 Tasks 1-2; 20-09 Task 2 |
| CONTEXT | D-05 neutral nonstaff refusal and immediate cleanup | 20-03 Task 2; 20-09 Task 2 |
| CONTEXT | D-06 sign-out returns to ops login with confirmation | 20-08 Task 1; 20-09 Task 3 |
| CONTEXT | D-07 24-hour, one-success invitation | 20-05 Tasks 1-2 |
| CONTEXT | D-08 resend rotates, cancel invalidates, one live invite | 20-05 Task 1; 20-07 Task 3 |
| CONTEXT | D-09 fixed email, name/password POST, GET read-only | 20-05 Task 2; 20-06 Tasks 1-2 |
| CONTEXT | D-10 uniform inactive invite state | 20-05 Task 2; 20-06 Task 2 |
| CONTEXT | D-11 marketplace conflict requires separate staff email | 20-05 Tasks 1-2 |
| CONTEXT | D-12 existing staff refusal and no email | 20-05 Task 1 |
| CONTEXT | D-13 duplicate Invite refuses and directs to Resend | 20-05 Task 1; 20-07 Task 3 |
| CONTEXT | D-14 explicit CLI-only conversion | 20-04 Task 2; 20-09 Task 1 |
| CONTEXT | D-15 two sections below existing queue | 20-07 Tasks 2-3 |
| CONTEXT | D-16 visible row facts and hidden internal ids | 20-07 Tasks 1-3 |
| CONTEXT | D-17 oldest active/newest pending ordering | 20-07 Tasks 1 and 3 |
| CONTEXT | D-18 confirmations, immediate Resend, persistent results | 20-07 Tasks 2-3 |
| CONTEXT | D-19 visible-disabled self/last controls plus identical server reasons | 20-04 Task 1; 20-07 Tasks 1-2 |
| CONTEXT | D-20 visible neutral empty pending section | 20-07 Task 3 |

Edge-probe accounting is exact: 12 applicable items = nine classified OPS-09/10/12 edges resolved as explicit truths/acceptance criteria plus the three OPS-07/08/11 unclassified candidates retained as flagged assumptions in Plans 02/03/04. Prohibition recall accounting is exact: three descriptor-less `status: unverified`, `flagged: true` items live in Plans 02/04/05; routine engineering and canonical security/compliance candidates remain in actions/threat models rather than being misclassified as product prohibitions. No source item is omitted, no deferred tiered-permission idea is planned, and no schema or external-API coverage artifact is introduced.

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-09-SUMMARY.md` when done.
</output>
