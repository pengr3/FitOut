---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 04
type: execute
wave: 5
depends_on: [20-11]
files_modified:
  - src/lib/ops/grant.ts
  - scripts/ops-grant.ts
  - tests/ops/staff-policy.test.ts
  - tests/ops/grant-cli.test.ts
  - tests/ops/staff-session-revocation.test.ts
  - .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md
autonomous: true
requirements: [OPS-10, OPS-11]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-19, self-revocation and removal of the final active staff member are refused by one shared transactional policy used by UI and CLI callers."
    - "OPS-10 adjacency: concurrent attempts that just meet at the one-staff boundary serialize; at most one succeeds and the invariant never reaches zero active staff."
    - "OPS-10 empty: an already-empty staff set is reported as an invariant violation, and a single-element staff set cannot be reduced."
    - "OPS-10 ordering: equal-time staff rows use stable `createdAt, id` ordering for display/readback while mutation outcomes are determined by the serialized transaction, not response arrival order."
    - "Per D-14, ordinary staff grant refuses marketplace-capable accounts; only an explicit CLI conversion can atomically clear `canBook` and `canHost` before granting staff."
    - "Revoked staff authorization fails on the next protected request rather than surviving in a role-bearing cookie cache."
  artifacts:
    - path: src/lib/ops/grant.ts
      provides: "`writeRole`, staff-boundary locking, explicit marketplace conversion policy, and shared refusal reasons"
    - path: scripts/ops-grant.ts
      provides: "`grant`, `revoke`, and `staff` subcommands plus `--convert-marketplace-account`"
    - path: tests/ops/staff-policy.test.ts
      provides: "transactional self/last/concurrency regression coverage"
    - path: tests/ops/staff-session-revocation.test.ts
      provides: "next-request revocation coverage"
  key_links:
    - from: scripts/ops-grant.ts
      to: src/lib/ops/grant.ts
      via: "all role writes delegate to `writeRole`"
    - from: src/lib/ops/grant.ts
      to: src/lib/auth.ts
      via: "database-backed `isStaff` remains authoritative; no role cookie cache"
  assumptions:
    - requirement_id: OPS-11
      category: unclassified
      status: unresolved
      flagged: true
      probe: "unclassified — review manually"
      statement: "OPS-11 has no SPEC edge taxonomy; every CLI/UI caller reaching the shared exclusivity policy is the explicit assumption boundary."
  prohibitions:
    - statement: "Never silently strip marketplace capability or history to make an account staff; conversion requires the explicit CLI flag and an audit event."
      status: unverified
      flagged: true
---

<objective>
Create one concurrency-safe staff-role policy and make every local/admin mutation path use it, implementing D-14 and D-19.

Purpose: Staff access must not be removable by races, stale sessions, or a caller that bypasses the same invariants enforced by the management UI.
Output: Shared transactional role writer, explicit conversion CLI, and policy/session regression suites.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `writeRole(input, tx?)` and the staff-boundary lock/refusal result types in `src/lib/ops/grant.ts`.
- `--convert-marketplace-account` on `scripts/ops-grant.ts grant`, while retaining the `grant`, `revoke`, and `staff` subcommands.
- `tests/ops/staff-policy.test.ts` and `tests/ops/staff-session-revocation.test.ts`.
- A supersession note for the old exclusivity wording in the tracked staff-management todo.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize self- and last-staff protection in the shared role writer</name>
  <files>src/lib/ops/grant.ts, tests/ops/staff-policy.test.ts</files>
  <read_first>
    - src/lib/ops/grant.ts
    - src/lib/ops/staff.ts
    - scripts/ops-grant.ts
    - tests/ops/grant-cli.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - A staff actor cannot revoke their own staff role, per D-19.
    - Revoking the sole remaining staff member returns one stable refusal reason, per D-19.
    - Two concurrent revocations at a two-staff boundary serialize and leave exactly one staff member.
    - A successful role change and its audit record commit together; a refusal changes neither.
  </behavior>
  <action>Test first, then make `writeRole` the single mutation authority. For every staff revoke, begin one database transaction, acquire `pg_advisory_xact_lock(hashtextextended('fitout:staff-role-policy', 0))` before reading the active-staff set, re-read target and actor inside that transaction, and execute one conditional `UPDATE ... WHERE target.id = ? AND target.id != ? AND EXISTS (another active staff row) RETURNING`. Treat a returned row as the only success signal, map zero rows to the shared self/last/stale refusal after an in-transaction diagnostic read, and insert the existing audit event in the same transaction only for the returned-row success. Return typed reasons reusable by CLI and Server Functions. Do not count outside the lock or enforce only in a caller. Per D-19, concurrent last-staff attempts must be deterministic at the database predicate; for equal timestamps use `createdAt ASC, id ASC` only as stable read ordering, never as a substitute for serialization.</action>
  <reversibility rating="reversible">The lock key and typed refusal mapping can be changed while preserving the database state and audit history.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-policy.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, permits self-removal, reaches zero active staff, or commits a role change without its audit event.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/ops/staff-policy.test.ts` proves the self, single-member, empty-set invariant, stable equal-time ordering, and two-concurrent-revocation cases.
    - Source inspection shows the named advisory lock, conditional `UPDATE ... WHERE ... EXISTS ... RETURNING`, and audit insert inside one transaction in `writeRole`.
    - Both refusal paths return checkable typed outcomes and leave the target row unchanged.
  </acceptance_criteria>
  <done>OPS-10's role-removal boundary is transactional, race-safe, audited, and shared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the single ops CLI and require explicit marketplace conversion</name>
  <files>src/lib/ops/grant.ts, scripts/ops-grant.ts, tests/ops/grant-cli.test.ts</files>
  <read_first>
    - scripts/ops-grant.ts
    - src/lib/ops/grant.ts
    - src/lib/ops/resolve-args.ts
    - tests/ops/grant-cli.test.ts
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
  </read_first>
  <behavior>
    - `grant --role staff` refuses a target with either `canBook` or `canHost` and changes nothing.
    - `grant --role staff --convert-marketplace-account` atomically clears both marketplace capabilities, grants staff, and audits the explicit conversion.
    - Existing `grant`, `revoke`, and `staff` subcommands continue to invoke the shared writer.
  </behavior>
  <action>Extend the existing `scripts/ops-grant.ts` command surface; do not create parallel grant/revoke/staff scripts. Parse `--convert-marketplace-account` only for a staff grant. In `writeRole`, refuse ordinary staff grant when the target has `canBook=true` or `canHost=true`. When the explicit flag is present, use the same transaction to clear both capabilities, grant `isStaff`, and record actor, target, prior capability booleans, and conversion intent in the established audit shape without email, token, or password data. Preserve existing dry-run and exit-code behavior and delegate every subcommand to `writeRole`.</action>
  <reversibility rating="costly">Explicit conversion changes authorization state and requires deliberate re-grants to reverse, but audit history preserves what changed.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/grant-cli.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, creates a second CLI entry point, or an ordinary grant changes a marketplace-capable account.</fails_when>
  </verify>
  <acceptance_criteria>
    - The help/parser contract exposes one exact flag: `--convert-marketplace-account`.
    - Tests prove ordinary refusal is side-effect free and explicit conversion changes `canBook`, `canHost`, and `isStaff` atomically.
    - `ops:grant`, `ops:revoke`, and `ops:staff` continue to resolve through the tracked `scripts/ops-grant.ts` implementation.
  </acceptance_criteria>
  <done>Staff/marketplace exclusivity is explicit, atomic, and available through the one established CLI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove next-request revocation and record the exclusivity decision</name>
  <files>src/lib/ops/grant.ts, tests/ops/staff-session-revocation.test.ts, .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md</files>
  <read_first>
    - src/lib/ops/grant.ts
    - src/lib/ops/staff.ts
    - src/lib/auth.ts
    - tests/ops/staff-guard.test.ts
    - .planning/todos/pending/2026-09-01-ops-staff-management-surface-and-invite-flow.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
  </read_first>
  <behavior>
    - A cookie issued while `isStaff=true` becomes nonstaff on the first protected request after revocation.
    - Session identity may remain valid, but role authorization is always read from current database state.
  </behavior>
  <action>Add the revoked-role regression around the existing `requireStaff()` path: create/authenticate a staff identity, revoke it through `writeRole`, reuse the same session cookie, and assert the next protected call refuses staff access. Pin `session.cookieCache` as absent rather than adding manual session revocation. In the tracked staff-management todo, mark the old marketplace/staff coexistence sentence as superseded by D-14 and project decision D-275; retain the todo's history instead of deleting it.</action>
  <reversibility rating="reversible">This adds a regression contract and planning-history note without changing persistent data format.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-session-revocation.test.ts tests/ops/staff-guard.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, or the reused session remains authorized after the database role is revoked.</fails_when>
  </verify>
  <acceptance_criteria>
    - The test reuses the pre-revocation cookie and observes refusal on the immediately following protected request.
    - `src/lib/auth.ts` still has no role-bearing cookie-cache configuration.
    - The todo retains its original decision record and explicitly points to D-14 and project decision D-275 as the superseding rule.
  </acceptance_criteria>
  <done>Revoked authorization cannot survive in a cookie, and the project record reflects the locked exclusivity policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/UI caller -> `writeRole` | Actor, target, requested transition, and conversion intent enter the authorization mutation boundary. |
| Session cookie -> `requireStaff()` | A previously valid identity presents current authorization claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-04-01 | Elevation of Privilege | `writeRole` callers | high | mitigate | Centralize actor/target policy and marketplace conversion in the transaction; forbid direct caller writes. |
| T-20-04-02 | Tampering | concurrent last-staff removal | high | mitigate | Acquire one transaction advisory lock and re-read/count before mutation. |
| T-20-04-03 | Elevation of Privilege | revoked-role session | high | mitigate | Keep role cache disabled and regression-test database authorization on the next request. |
| T-20-04-04 | Information Disclosure | PII/token leakage into role-change audit data | medium | mitigate | Record IDs/booleans/reasons only; exclude email, cookies, passwords, and tokens. |
| T-20-04-SC | Tampering | package installs | high | mitigate | No dependency install is authorized by this plan; package-lock changes fail review. |
</threat_model>

<verification>
- `npx vitest run tests/ops/staff-policy.test.ts tests/ops/grant-cli.test.ts tests/ops/staff-session-revocation.test.ts tests/ops/staff-guard.test.ts`
- `npx tsc --noEmit`
- Failure direction: either command exits non-zero, any suite reports 0 passed, or TypeScript reports an error.
</verification>

<success_criteria>
- One shared transaction protects self-removal, the last-staff boundary, audit atomicity, and marketplace conversion.
- Concurrent revokes never reduce active staff below one.
- A revoked staff cookie is refused on the next protected request.
- All three existing ops CLI commands remain on `scripts/ops-grant.ts`.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-04-SUMMARY.md` when done.
</output>
