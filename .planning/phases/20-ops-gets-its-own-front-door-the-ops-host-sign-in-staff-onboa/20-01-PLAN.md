---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/proxy.ts
  - tests/auth/login-reachable-after-reset.test.ts
  - tests/auth/stale-session-selfheal.test.ts
  - tests/design/dark-scope.test.ts
  - tests/design/ops-guard-coverage.test.ts
  - tests/security/paymongo-seam.test.ts
  - src/lib/session-check.ts
  - src/lib/ops/staff.ts
  - src/app/auth/session-check/route.ts
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/dev-throw-auth/page.tsx
autonomous: true
requirements: [OPS-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "OPS-07 prerequisite: Next.js loads src/proxy.ts with a named proxy export, while /login, /signup, and the _sc loop guard behave exactly as they did before the convention migration."
    - "The migration is the first implementation commit of Phase 20 and contains no Host routing, matcher widening, auth configuration, or ops UI work."
    - "Every modified source and test reference names the tracked src/proxy.ts origin rather than an ignored .next runtime mirror."
  artifacts:
    - path: src/proxy.ts
      provides: "Next.js 16.2.7 Proxy entry point with preserved logged-out-only behavior"
      exports: [proxy, config]
    - path: tests/auth/login-reachable-after-reset.test.ts
      provides: "Behavioral proof that _sc, /login, and /signup did not change during migration"
  key_links:
    - from: src/proxy.ts
      to: src/lib/session-check.ts
      via: "SESSION_CHECK_PATH, RETURN_PARAM, CHECKED_PARAM, and LOGGED_OUT_ONLY imports"
      pattern: "SESSION_CHECK_PATH|RETURN_PARAM|CHECKED_PARAM|LOGGED_OUT_ONLY"
---

<objective>
Migrate the deprecated Next.js entry convention before any host behavior lands.

Purpose: satisfy the phase's mandatory first-commit ordering while keeping the measured stale-session recovery path byte-for-byte equivalent.
Output: `src/proxy.ts`, updated tracked references, and unchanged `/login`, `/signup`, and `_sc` behavior.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@AGENTS.md
</context>

## Artifacts this phase produces

- `src/proxy.ts`: the sole Proxy convention file, exporting `proxy(request: NextRequest)` and the preserved `config` matcher.
- No new route, schema/type field, action, table, CLI flag, dependency, or migration is introduced by this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Next.js 16 Proxy migration with the logged-out session-check path intact</name>
  <reversibility rating="reversible">The rename can be reverted locally while the installed Next.js version remains pinned, although doing so restores a deprecation.</reversibility>
  <files>src/middleware.ts, src/proxy.ts, tests/auth/login-reachable-after-reset.test.ts</files>
  <read_first>
    - src/middleware.ts
    - src/proxy.ts (new tracked destination; must not exist before the rename)
    - tests/auth/login-reachable-after-reset.test.ts
    - src/lib/session-check.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
    - AGENTS.md
  </read_first>
  <behavior>
    - `/login` and `/signup` with no session cookie still continue without a redirect.
    - A present session cookie still redirects through the existing session-check route unless `_sc=1` is present.
    - Existing query strings and the `_sc` loop-guard order remain unchanged.
  </behavior>
  <action>Rename the tracked `src/middleware.ts` file to `src/proxy.ts` and rename only the exported function identifier from `middleware` to `proxy`, per the installed Next.js 16.2.7 convention. Update the direct test import and calls in `tests/auth/login-reachable-after-reset.test.ts`. Preserve the full header, imports, `_sc`-before-cookie branch order, `LOGGED_OUT_ONLY` handling, redirect construction, and `config.matcher` values exactly. Do not add Host parsing, rewrites, new matchers, auth/database imports, or any unrelated cleanup. Commit this task alone before beginning Task 2.</action>
  <verify>
    <automated>npx vitest run tests/auth/login-reachable-after-reset.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests or 0 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - Git records `src/middleware.ts` as removed and `src/proxy.ts` as its tracked rename destination.
    - `src/proxy.ts` exports `proxy` and retains `matcher: ["/login", "/signup"]` in this commit.
    - The focused regression suite exercises both routes and `_sc` and passes with a non-zero test count.
    - The task commit contains no `Host`, `host`, `rewrite`, `OPS_APP_URL`, auth, or database logic beyond text already present before the rename.
  </acceptance_criteria>
  <done>The first Phase 20 implementation commit is an isolated convention migration whose real session-check path remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget test contracts to the tracked Proxy source</name>
  <reversibility rating="reversible">These are mechanical source-reference updates with no runtime behavior.</reversibility>
  <files>tests/auth/stale-session-selfheal.test.ts, tests/design/dark-scope.test.ts, tests/design/ops-guard-coverage.test.ts, tests/security/paymongo-seam.test.ts</files>
  <read_first>
    - tests/auth/stale-session-selfheal.test.ts
    - tests/design/dark-scope.test.ts
    - tests/design/ops-guard-coverage.test.ts
    - tests/security/paymongo-seam.test.ts
    - src/proxy.ts
  </read_first>
  <action>Replace stale tracked-source references to the retired convention with `src/proxy.ts` and the `proxy` export in these four test files. Preserve every test's semantic claim, mutation description, inventory count, and security boundary: Proxy is routing/optimistic behavior and never the authorization boundary. Do not change expected counts or weaken any assertion to accommodate the rename.</action>
  <verify>
    <automated>npx vitest run tests/auth/stale-session-selfheal.test.ts tests/design/dark-scope.test.ts tests/design/ops-guard-coverage.test.ts tests/security/paymongo-seam.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests or 0 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - All four suites load the tracked Proxy origin and preserve their prior assertion totals.
    - `tests/design/ops-guard-coverage.test.ts` still states that page/action guards, not Proxy, enforce staff authorization.
    - No test reads `.next/server/middleware-manifest.json` as source truth.
  </acceptance_criteria>
  <done>The existing regression and design contracts follow the Next.js source rename without changing what they prove.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget source-of-truth comments without changing runtime bytes</name>
  <reversibility rating="reversible">Only comments and source-path citations change.</reversibility>
  <files>src/lib/session-check.ts, src/lib/ops/staff.ts, src/app/auth/session-check/route.ts, src/app/(auth)/layout.tsx, src/app/(auth)/dev-throw-auth/page.tsx</files>
  <read_first>
    - src/lib/session-check.ts
    - src/lib/ops/staff.ts
    - src/app/auth/session-check/route.ts
    - src/app/(auth)/layout.tsx
    - src/app/(auth)/dev-throw-auth/page.tsx
    - src/proxy.ts
  </read_first>
  <action>Update only the tracked-path and exported-name citations that point readers at the retired convention. Keep the expensive QK-IR9 30-day lockout explanation, the forgeable `_sc` tradeoff, the no-database/no-auth boundary, and the three-layer staff guard language intact. Do not alter imports, expressions, JSX, route behavior, or the positive `role === "staff"` predicate.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or any TypeScript diagnostic is printed</fails_when>
  </verify>
  <acceptance_criteria>
    - Every changed line in the five files is comment text naming `src/proxy.ts` or the Proxy convention.
    - `git diff -U0` for these files contains no changed executable token or JSX node.
    - TypeScript completes with zero diagnostics.
  </acceptance_criteria>
  <done>The load-bearing explanations now point to the tracked Next.js 16 source without altering the application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| request → Proxy | Untrusted path/cookie presence enters pre-routing code that must remain data-free |
| Proxy → ops guards | Routing must never be treated as staff authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-01 | Spoofing | `src/proxy.ts` convention migration | medium | mitigate | Preserve behavior exactly and defer all host classification to Plan 02 with focused regression tests. |
| T-20-02 | Elevation of Privilege | Proxy/ops boundary | high | mitigate | Preserve the explicit no-auth/no-database header and the existing `assertStaff`/`requireStaff` boundaries. |
| T-20-03 | Information Disclosure | stale source references | low | accept | Comments disclose no secret; update them so future security work follows the tracked source rather than a stale path. |
</threat_model>

<verification>
Run the focused Proxy regression suites and `npx tsc --noEmit`; confirm Task 1 is the first isolated Phase 20 implementation commit.
</verification>

<success_criteria>
- Next.js 16.2.7 discovers `src/proxy.ts` without the deprecated convention warning.
- Login/signup session-check behavior is unchanged.
- No Host routing or ops feature work appears before the isolated migration commit.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-01-SUMMARY.md` when done.
</output>
