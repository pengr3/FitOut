---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 14
type: execute
wave: 6
depends_on: [20-03]
files_modified:
  - src/lib/ops/staff.ts
  - src/app/actions/ops-review.ts
  - src/app/actions/ops-contact.ts
  - src/app/actions/cancel-booking.ts
  - tests/ops/ops-audit.test.ts
  - tests/ops/host-contact-reveal.test.ts
  - tests/payments/ops-cancel.test.ts
  - tests/design/ops-guard-coverage.test.ts
  - e2e/ops-auth.spec.ts
autonomous: true
requirements: [OPS-07, OPS-08, OPS-09, OPS-10, OPS-11]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every privileged ops Server Function proves exact configured ops Host and Origin server-side before `requireStaff()`, parsing, rate limiting, reads, domain mutation, or audit writes."
    - "Proxy remains routing only; the positive current-database staff guard follows the origin guard and remains the authorization boundary for actor identity."
    - "A real privileged action dispatch replayed on the marketplace host with an ops staff cookie deliberately supplied is neutrally refused and produces zero target or audit mutation."
    - "Missing, malformed, forged, public-preview, marketplace, or cross-origin authority values fail closed without revealing whether the target exists."
  artifacts:
    - path: src/lib/ops/staff.ts
      provides: "Reusable exact ops Host+Origin Server Function guard"
      exports: [requireOpsMutationOrigin, requireStaff, assertStaff]
    - path: tests/design/ops-guard-coverage.test.ts
      provides: "AST census requiring origin guard first and staff guard second for every privileged ops action"
    - path: e2e/ops-auth.spec.ts
      provides: "Focused marketplace-host forged action-dispatch integration probe"
  key_links:
    - from: src/app/actions/ops-review.ts
      to: src/lib/ops/staff.ts
      via: "first-statement exact action-origin guard followed immediately by current staff authorization"
      pattern: "requireOpsMutationOrigin.*requireStaff"
    - from: e2e/ops-auth.spec.ts
      to: src/app/actions/ops-review.ts
      via: "capture a real action id on the ops host, replay the POST on marketplace host with copied staff cookie, then assert no mutation/audit delta"
      pattern: "marketplace-host action dispatch"
---

<objective>
Close the marketplace-host Server Function boundary before any new Phase 20 ops mutation is added.

Purpose: host-only cookies and Proxy rewrites are insufficient authorization evidence for public Server Function POSTs; the action itself must reject the wrong request authority even when a valid staff cookie is deliberately presented.
Output: shared exact Host+Origin guard, coverage across every shipped privileged ops mutation, AST enforcement, and a focused integration probe.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-PATTERNS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-03-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

- `requireOpsMutationOrigin()` in `src/lib/ops/staff.ts`.
- Exact guard ordering in all five `ops-review.ts` actions, `revealHostContact`, and `cancelBookingAsOps`.
- Updated action fixtures/AST census and a real cross-host action-dispatch Playwright probe.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Guard one real ops-review action end to end, then cover the full review action set</name>
  <reversibility rating="reversible">The host/origin check is an internal guard composed before the unchanged current-role authorization contract.</reversibility>
  <files>src/lib/ops/staff.ts, src/app/actions/ops-review.ts, tests/ops/ops-audit.test.ts, tests/design/ops-guard-coverage.test.ts</files>
  <read_first>
    - src/lib/ops/staff.ts
    - src/lib/app-origins.ts
    - src/app/actions/ops-review.ts
    - tests/ops/ops-audit.test.ts
    - tests/design/ops-guard-coverage.test.ts
    - src/proxy.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Exact ops Host plus exact same ops Origin passes to the current `requireStaff()` guard.
    - Missing/malformed Host or Origin, marketplace/public-preview/unknown Host, or any mismatched Origin produces the same neutral not-found refusal before role/domain/audit spies run.
    - `approveHost`, `rejectHost`, `suspendHost`, `approveListing`, and `rejectListing` all use the same two-guard order.
    - Proxy imports no auth/database code and is never consulted as authorization evidence.
  </behavior>
  <action>Test the `approveHost` path first, then add `requireOpsMutationOrigin()` in `src/lib/ops/staff.ts`. Read request headers through the installed Next.js API; compare the normalized `Host` exactly with `OPS_APP_ORIGIN.host` and the parsed `Origin` exactly with `OPS_APP_ORIGIN.origin`; reject missing, malformed, marketplace, public-preview, unknown, or mismatched values through the same neutral not-found path before any role read. In every exported privileged function in `src/app/actions/ops-review.ts`, call the origin guard as the first statement and `requireStaff()` immediately second, before parsing, rate limiting, reads, writes, or audit. Update test fixtures to provide the exact ops headers for allowed calls and prove forged cases reach none of the downstream spies. Update the AST gate from the old single-first-call contract to require the resolved origin guard first and resolved staff guard second; it must still reject local decoys/re-exports. Keep Proxy database/auth-free.</action>
  <verify>
    <automated>npx vitest run tests/ops/ops-audit.test.ts --config vitest.config.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, or a wrong/missing Host or Origin reaches role/domain/audit work.</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/ops-guard-coverage.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, or any ops-review action lacks the resolved origin-first/staff-second sequence.</fails_when>
  </verify>
  <acceptance_criteria>
    - All five ops-review functions have exact origin-first/staff-second guard order before any other statement.
    - Exact ops authority proceeds to unchanged role and domain policy; every invalid authority returns the same not-found result.
    - Tests prove invalid authority triggers zero parse, rate-limit, query, mutation, notification, or audit calls.
    - Design mutation cases fail for swapped guard order, missing origin guard, local decoys, and Proxy-only gating.
  </acceptance_criteria>
  <done>The shipped review actions are host-bound inside their public POST boundary while retaining current staff authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the exact action-origin boundary to contact reveal and money-moving cancellation</name>
  <reversibility rating="reversible">Both functions compose the shared guard before their existing authorization and transaction logic.</reversibility>
  <files>src/app/actions/ops-contact.ts, src/app/actions/cancel-booking.ts, tests/ops/host-contact-reveal.test.ts, tests/payments/ops-cancel.test.ts, tests/design/ops-guard-coverage.test.ts</files>
  <read_first>
    - src/app/actions/ops-contact.ts
    - src/app/actions/cancel-booking.ts
    - src/lib/ops/staff.ts
    - tests/ops/host-contact-reveal.test.ts
    - tests/payments/ops-cancel.test.ts
    - tests/design/ops-guard-coverage.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - `revealHostContact` refuses the wrong request authority before actor lookup, target parsing, PII read, or audit.
    - `cancelBookingAsOps` refuses the wrong request authority before actor lookup, booking parsing/read, refund/cancel mutation, notification, or audit.
    - Allowed exact-ops requests preserve every existing disclosure, cancellation, idempotency, and audit outcome.
  </behavior>
  <action>Apply the same resolved `requireOpsMutationOrigin()` first and `requireStaff()` second sequence to `revealHostContact` and only the `cancelBookingAsOps` export inside the mixed cancellation module. Update their load-bearing comments to explain the two-stage boundary and why Proxy is insufficient. Extend the existing focused tests with exact ops headers for allowed cases and a matrix of missing/malformed/marketplace/mismatched Host+Origin cases whose downstream spies remain untouched. Extend the AST census to enumerate these functions under the same order without altering the owner/host cancellation functions that are not ops actions.</action>
  <verify>
    <automated>npx vitest run tests/ops/host-contact-reveal.test.ts tests/payments/ops-cancel.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, leaks contact/booking existence, or a refused authority reaches disclosure, money, notification, or audit work.</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/ops-guard-coverage.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, or either privileged action lacks origin-first/staff-second ordering.</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven existing privileged ops actions use the same exact Host+Origin then current-staff sequence.
    - Wrong-authority contact and cancellation calls disclose no target existence and write no audit row.
    - Exact-ops behavior and every existing notification/refund/idempotency assertion remain green.
    - Non-ops owner/host cancellation exports retain their existing ownership-first contracts.
  </acceptance_criteria>
  <done>Every shipped privileged ops Server Function has the same internal host, origin, role, and domain boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Probe marketplace-host action dispatch with a deliberately presented staff cookie</name>
  <precondition>A local/test PostgreSQL database is reachable and the exact public and ops origins are configured for the Playwright web server.</precondition>
  <reversibility rating="reversible">This is a focused browser/request integration scenario over existing test state.</reversibility>
  <files>e2e/ops-auth.spec.ts</files>
  <read_first>
    - e2e/ops-auth.spec.ts
    - playwright.config.ts
    - src/app/(ops)/ops/page.tsx
    - src/app/actions/ops-review.ts
    - src/lib/ops/staff.ts
    - tests/ops/ops-audit.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - A staff session signs in on the ops host and exposes a real rendered privileged Server Function dispatch.
    - The probe deliberately copies that staff cookie into a raw marketplace-host POST carrying the real action id/payload rather than relying on browser cookie isolation.
    - Marketplace dispatch receives the neutral refusal and leaves target plus audit counts unchanged.
    - The same action on the exact ops Host+Origin succeeds or reaches its established domain outcome.
  </behavior>
  <action>Add one focused integration scenario named `refuses marketplace-host action dispatch even with an ops staff cookie`. Sign in a seeded staff identity on the ops host, load the real `/ops` form/control, capture the actual Server Function action id and a safe fixture payload, snapshot the target and audit state, and issue a raw POST to the marketplace origin with the ops staff cookie deliberately supplied plus marketplace Host/Origin headers. Assert the neutral not-found refusal and unchanged target/audit state, then submit the equivalent request with exact ops authority and assert it crosses the guard to the established domain result. Never print the cookie, action payload secrets, email, or database URL. This probe tests dispatch itself; a two-cookie-jar assertion alone does not satisfy the task.</action>
  <verify>
    <automated>npx playwright test e2e/ops-auth.spec.ts --project=chromium --grep "refuses marketplace-host action dispatch even with an ops staff cookie" --reporter=list</automated>
    <fails_when>The command exits non-zero, reports 0 passed, cannot capture a real action id, accepts marketplace dispatch, reaches domain/audit mutation, or logs credential/session material.</fails_when>
  </verify>
  <acceptance_criteria>
    - The scenario posts a real rendered action id on both hosts and deliberately supplies the staff cookie to the marketplace request.
    - Marketplace Host/Origin is refused before role/domain work and target/audit snapshots remain equal.
    - Exact ops Host/Origin crosses the origin guard and remains subject to `requireStaff()` plus the existing domain policy.
    - Test output and committed evidence contain no cookie, token, email, password, or database URL.
  </acceptance_criteria>
  <done>The security-critical marketplace action-dispatch question is resolved by executable integration evidence, not an assumption about Proxy or cookie jars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| request Host/Origin -> Server Function | Attacker-controlled authority data selects whether a public POST may enter the ops authorization path. |
| session cookie -> current staff lookup | A valid identity alone must not authorize an action dispatched on the wrong host. |
| staff guard -> privileged domain mutation | Existing role, validation, rate limit, transaction, notification, and audit policy must remain intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-14-01 | Spoofing | Host/Origin action authority | high | mitigate | Exact normalized configured ops Host plus exact parsed ops Origin, fail closed on missing/malformed/mismatch. |
| T-20-14-02 | Elevation of Privilege | marketplace-host staff-cookie dispatch | high | mitigate | Enforce the origin guard inside every action before `requireStaff()` and prove a deliberately copied cookie cannot cross it. |
| T-20-14-03 | Information Disclosure | wrong-host target probing | high | mitigate | Return the common not-found refusal before parsing, target reads, or audit differences. |
| T-20-14-04 | Tampering | guard drift/new action omission | high | mitigate | AST census resolves imports and requires origin-first/staff-second ordering for every privileged ops export. |
| T-20-14-05 | Repudiation | probe/domain side effects | medium | mitigate | Snapshot target/audit state around wrong-host dispatch and require zero delta. |
| T-20-14-SC | Tampering | package installs | high | mitigate | Use installed Next.js headers, Vitest, and Playwright only; manifests and lockfile remain unchanged. |
</threat_model>

<verification>
Run focused ops-review/contact/cancellation suites, the AST guard gate, the marketplace-host forged action-dispatch Playwright scenario, and `npx tsc --noEmit`.
</verification>

<success_criteria>
- Every existing privileged ops action validates exact ops Host+Origin before current staff and domain work.
- Proxy remains database/auth-free routing and is not treated as authorization.
- A marketplace-host action POST with a deliberately copied staff cookie is refused with zero state/audit mutation.
- Plans 20-12, 20-06, and 20-07 can reuse the same guard for every new Phase 20 mutation.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-14-SUMMARY.md` when done.
</output>
