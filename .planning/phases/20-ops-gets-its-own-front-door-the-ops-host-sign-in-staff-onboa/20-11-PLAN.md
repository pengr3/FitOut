---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 11
type: execute
wave: 4
depends_on: [20-02]
files_modified:
  - src/app/api/paymongo/webhook/route.ts
  - src/app/api/didit/webhook/route.ts
  - scripts/verify-ops-cloak.mjs
  - tests/auth/ops-cloak-probe.test.ts
  - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
autonomous: true
requirements: [OPS-07, OPS-12]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact-host partition receives its first production `next start` reading before any auth, invitation, or staff-management implementation begins."
    - "Staff `/ops` is 200 while nonstaff, signed-out, nonexistent ops, and marketplace `/ops` controls are 404; the required denial bodies are byte-identical by SHA-256."
    - "The stage-partition route census is non-empty, sorted deterministically, and fails on missing controls, streamed 200 denials, `INVALID_ORIGIN`, or secret/PII evidence."
  artifacts:
    - path: scripts/verify-ops-cloak.mjs
      provides: "Production HTTP status/header/raw-body hash probe with partition/final stages"
    - path: tests/auth/ops-cloak-probe.test.ts
      provides: "Probe parser, non-empty census, deterministic ordering, and failure-direction coverage"
    - path: .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
      provides: "Dated partition-stage production transcript"
  key_links:
    - from: scripts/verify-ops-cloak.mjs
      to: .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
      via: "validated `partition` status/header/length/SHA-256 rows"
      pattern: "partition"
---

<objective>
Measure and record the first production cloak reading immediately after the exact-host partition.

Purpose: prove the hard routing prerequisite before later ops routes can obscure whether the partition itself preserved the wire-level 404 cloak.
Output: production probe, focused tests, corrected webhook matcher comments, and the dated `Partition reading` evidence section.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

- `scripts/verify-ops-cloak.mjs` with `--probe`, `--stage partition|final`, and `--check-evidence` modes.
- `tests/auth/ops-cloak-probe.test.ts`.
- `20-EVIDENCE.md` section `Partition reading`.
- Comment-only matcher/source retargeting in the PayMongo and Didit webhook routes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the non-vacuous production cloak probe</name>
  <reversibility rating="reversible">The probe and its parser can evolve without changing application behavior.</reversibility>
  <files>src/app/api/paymongo/webhook/route.ts, src/app/api/didit/webhook/route.ts, scripts/verify-ops-cloak.mjs, tests/auth/ops-cloak-probe.test.ts</files>
  <read_first>
    - src/app/api/paymongo/webhook/route.ts
    - src/app/api/didit/webhook/route.ts
    - scripts/verify-ops-cloak.mjs (new)
    - tests/auth/ops-cloak-probe.test.ts (new)
    - src/proxy.ts
    - src/app/_ops-cloak/page.tsx
    - src/app/not-found.tsx
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - Partition-stage input requires a non-empty route/actor matrix and one staff 200 control.
    - Probe rows record host, path, actor class, status, selected headers, byte length, and SHA-256 of unmodified response bytes.
    - Rows sort by host, path, and actor class before evidence comparison.
    - Missing controls, a denial status other than 404, a staff status other than 200, or unequal required denial hashes return non-zero.
  </behavior>
  <action>Update only the two webhook header comments whose matcher/source claim changed with the widened Proxy; leave webhook runtime code untouched. Create the Node probe and focused tests first. Give the script explicit `--probe`, `--stage partition|final`, and `--check-evidence` modes; require a non-empty expected matrix, validate each control independently before comparing raw-body SHA-256, sort rows deterministically, and redact cookie/token/email/database/secret material from output. Treat `403 INVALID_ORIGIN` as a blocking result rather than a credential failure. Do not normalize or decode response bodies before hashing.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-cloak-probe.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, accepts an empty/missing control set, or permits status/hash failure cases.</fails_when>
  </verify>
  <acceptance_criteria>
    - Mutation cases prove empty census, missing control, streamed 200 denial, `INVALID_ORIGIN`, and unequal bytes all fail.
    - Repeated equivalent input produces the same host/path/actor ordering.
    - Probe output contains no cookie/token/email/database URL/secret field.
    - Both webhook diffs are comment-only.
  </acceptance_criteria>
  <done>The production probe can distinguish a real cloak from a vacuous, streamed, reordered, or redacted-away result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record and validate the immediate partition reading</name>
  <precondition>A local/test PostgreSQL database is reachable, the exact public and ops origins are configured, and the production build can start through `next start`.</precondition>
  <reversibility rating="reversible">The transcript is append-only evidence and can be rerun after a routing repair.</reversibility>
  <files>scripts/verify-ops-cloak.mjs, tests/auth/ops-cloak-probe.test.ts, .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md</files>
  <read_first>
    - scripts/verify-ops-cloak.mjs
    - tests/auth/ops-cloak-probe.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md (new)
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
    - src/proxy.ts
  </read_first>
  <behavior>
    - Production build completes before `next start` begins.
    - Independent staff, nonstaff, and signed-out contexts exercise staff `/ops`, the three required 404 classes, marketplace `/ops`, sign-in on both hosts, and ops-host `/api/auth/*`.
    - Evidence retains raw status and SHA-256 facts without cookies, addresses, tokens, passwords, or secrets.
  </behavior>
  <action>Run the complete build, start `next start` only after a successful build, and execute the partition probe with isolated staff/nonstaff/signed-out cookie contexts without logging cookie values. Record the commit, date, Next.js version, exact configured host labels, production runtime, status, selected headers, byte length, and SHA-256 for staff `/ops`; nonstaff `/ops`; signed-out `/ops`; nonexistent ops path; marketplace `/ops`; sign-in on both hosts; and ops-host `/api/auth/*`. Preserve this as the `Partition reading` section for Plan 20-09 to append to, never overwrite it with the final reading. Repair the partition within this plan if any required status/hash/origin property fails; no later ops implementation may begin while the validator is red.</action>
  <verify>
    <automated>node scripts/verify-ops-cloak.mjs --check-evidence .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md --stage partition</automated>
    <fails_when>The command exits non-zero, a required route row is absent, a denial status is not 404, the staff control is not 200, `INVALID_ORIGIN` appears, or the required denial hashes differ.</fails_when>
  </verify>
  <acceptance_criteria>
    - The partition section names commit, date, Next.js version, configured host labels, and production `next start`.
    - Required 404 classes have identical raw-body SHA-256 and the staff control is 200.
    - Header differences are recorded and do not reveal a distinct denied route class.
    - Evidence contains no cookie/token/email/password/database URL/secret material.
  </acceptance_criteria>
  <done>The first required cloak measurement is dated, machine-validated, and green before ops auth work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| production HTTP response -> evidence | Status, headers, and raw bytes can disclose route existence; evidence can also leak credentials if not redacted. |
| build completion -> production server | A stale or partially built server would make the partition reading invalid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-11-01 | Information Disclosure | route-existence cloak | high | mitigate | Assert status separately, then compare SHA-256 of unmodified denial bodies over a non-empty control matrix. |
| T-20-11-02 | Tampering | evidence ordering/completeness | medium | mitigate | Require exact controls and deterministic host/path/actor sorting before validation. |
| T-20-11-03 | Information Disclosure | probe logs/evidence | high | mitigate | Redact secrets/PII and fail evidence validation on credential-bearing fields. |
| T-20-11-04 | Spoofing | stale production build | medium | mitigate | Require successful build completion before starting the probed `next start` server. |
| T-20-11-SC | Tampering | package installs | high | mitigate | Use installed Node, Next.js, Vitest, and crypto only; manifests and lockfile remain unchanged. |
</threat_model>

<verification>
Run the focused probe suite, `npm run build`, the production probe, and the stage-partition evidence validator. Later plans remain blocked until all are green.
</verification>

<success_criteria>
- The partition reading exists before any auth or onboarding route implementation.
- Staff 200 and required 404/status/raw-byte contracts pass under `next start`.
- Evidence is complete, deterministic, and free of secrets/PII.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-11-SUMMARY.md` when done.
</output>
