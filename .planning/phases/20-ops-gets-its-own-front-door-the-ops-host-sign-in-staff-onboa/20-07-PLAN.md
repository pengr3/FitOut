---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 07
type: execute
wave: 6
depends_on: [20-04, 20-05, 20-06]
files_modified:
  - src/lib/ops/staff-management.ts
  - src/app/actions/ops-staff.ts
  - src/components/ops/staff-management-panel.tsx
  - src/components/ops/staff-action-dialog.tsx
  - src/app/(ops)/ops/page.tsx
  - tests/ops/staff-management-actions.test.ts
  - tests/ops/staff-management-panel.test.tsx
  - tests/ops/staff-policy.test.ts
  - tests/design/ops-guard-coverage.test.ts
autonomous: true
requirements: [OPS-09, OPS-10, OPS-11]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-15, one staff-management panel appears below the existing `/ops` operational queue with visibly separate Active staff and Pending invitations sections; no second route or hidden disclosure is introduced."
    - "Per D-16, active rows show email, staff-since date, and `You`; pending rows show email, sent date, absolute expiry, and inviter, with internal ids absent from visible output."
    - "Per D-17, active staff remain oldest-first and pending invitations newest-first with stable id tie-breakers."
    - "Per D-18, Revoke and Cancel require confirmation, Resend is immediate and names prior-link invalidation, and successful lifecycle outcomes persist in-page rather than disappearing as a toast."
    - "Per D-19, self- and last-staff Revoke controls remain visibly disabled with the same server refusal reason beside them; stale/direct requests are independently refused."
    - "Per D-20, Pending invitations stays visible when empty and contains one neutral sentence rather than a promotional empty state."
    - "Populated: active and pending rows expose their required facts/actions inside their named sections while the operational queue remains the page's first surface."
    - "Zero/one/many: zero pending is explicit, one active disables final-staff revoke, and many active/pending rows preserve the locked independent sort orders."
    - statement: "Overflow: at 320px and with multiple rows, the panel and confirmation dialogs remain inside the viewport; row actions wrap without horizontal page scrolling."
      verification: backstop
    - statement: "Long-text resilience: long emails, inviter labels, refusal text, and confirmation text wrap without covering controls or exposing internal ids."
      verification: backstop
  artifacts:
    - path: src/lib/ops/staff-management.ts
      provides: "`readStaffManagementSnapshot` with active/pending rows and server-derived action eligibility"
    - path: src/app/actions/ops-staff.ts
      provides: "`revokeStaffAction` plus persistent outcomes for all staff-management mutations"
    - path: src/components/ops/staff-management-panel.tsx
      provides: "the two-section staff roster, invite form, actions, and in-page result region"
    - path: src/components/ops/staff-action-dialog.tsx
      provides: "confirmation UI for revoke and cancel"
  key_links:
    - from: src/app/(ops)/ops/page.tsx
      to: src/lib/ops/staff-management.ts
      via: "protected page reads the snapshot after the operational queue"
    - from: src/components/ops/staff-management-panel.tsx
      to: src/app/actions/ops-staff.ts
      via: "server-bound row actions with persistent typed outcomes"
    - from: src/app/actions/ops-staff.ts
      to: src/lib/ops/grant.ts
      via: "revoke delegates to `writeRole`; UI eligibility is not the enforcement boundary"
---

<objective>
Ship the one-page ops staff roster and invitation management surface exactly as D-15 through D-20 specify.

Purpose: Staff can onboard and safely remove colleagues from the existing console with explicit state, durable feedback, and the same server policy behind every control.
Output: Management read model/actions, two-section panel, confirmation dialog, and behavior/accessibility tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `StaffManagementSnapshot`, `ActiveStaffRow`, `PendingStaffInvitationRow`, and `readStaffManagementSnapshot` in `src/lib/ops/staff-management.ts`.
- `revokeStaffAction` and persistent `OpsStaffActionState` outcomes in `src/app/actions/ops-staff.ts`.
- `StaffManagementPanel` and `StaffActionDialog`.
- `tests/ops/staff-management-actions.test.ts` and `tests/ops/staff-management-panel.test.tsx`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the protected roster snapshot and revoke action</name>
  <files>src/lib/ops/staff-management.ts, src/app/actions/ops-staff.ts, tests/ops/staff-management-actions.test.ts, tests/design/ops-guard-coverage.test.ts</files>
  <read_first>
    - src/lib/ops/staff.ts
    - src/lib/ops/grant.ts
    - src/lib/ops/invitations.ts
    - src/app/actions/ops-review.ts
    - tests/ops/staff-guard.test.ts
    - tests/design/ops-guard-coverage.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - Snapshot requires staff and returns active rows oldest-first, pending rows newest-first, each with stable id tie-breakers.
    - Snapshot marks the current actor, derives self/last disablement, and supplies the exact shared refusal reason.
    - Revoke requires staff, ignores client actor identity/eligibility, and delegates to `writeRole`.
    - Successful and refused outcomes are serializable persistent action states.
  </behavior>
  <action>Create `readStaffManagementSnapshot()` as a server-only protected query that calls `requireStaff()`, reads active staff and live invitation metadata, joins inviter display data, and maps only the visible fields required by D-16. Preserve shipped active ordering `createdAt ASC, id ASC`; use pending `sentAt DESC, id DESC` per D-17. Derive `isCurrentActor`, `canRevoke`, and `revokeDisabledReason` from the snapshot, using the same exact self/last reason constants exported by `writeRole`. Add `revokeStaffAction(boundTargetUserId, previousState, formData)` that calls `requireStaff()` first and then `writeRole`; never accept actor id, staff count, or eligibility from the browser. Return a typed persistent success/refusal state and revalidate `/ops` after success. Extend the guard inventory for the new read model and action.</action>
  <reversibility rating="reversible">The read-model/action interfaces are internal and backed by existing role/invitation state.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-management-actions.test.ts tests/ops/staff-policy.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, returns unstable ordering, trusts a client eligibility value, or a direct action bypasses self/last refusal.</fails_when>
  </verify>
  <acceptance_criteria>
    - Fixtures with equal timestamps prove active `createdAt,id` ascending and pending `sentAt,id` descending.
    - Snapshot DTOs omit internal ids from display fields while retaining opaque bound ids server-side for actions.
    - Signed-out/nonstaff direct action calls stop before mutation; stale staff-count assumptions still receive the shared server refusal.
    - `tests/design/ops-guard-coverage.test.ts` enumerates the new query and action.
  </acceptance_criteria>
  <done>The management surface has one protected, deterministically ordered source of truth and a server-enforced revoke path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Active staff with explainable confirmation-gated revocation</name>
  <files>src/components/ops/staff-management-panel.tsx, src/components/ops/staff-action-dialog.tsx, src/app/(ops)/ops/page.tsx, tests/ops/staff-management-panel.test.tsx, tests/ops/staff-policy.test.ts</files>
  <read_first>
    - src/app/(ops)/ops/page.tsx
    - src/components/ops/ops-reject-dialog.tsx
    - src/components/patterns/responsive-dialog.tsx
    - src/components/ops/ops-row-fact.tsx
    - tests/ops/enforcement-dialog.test.tsx
    - tests/design/responsive-dialog-autofocus.test.tsx
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
  </read_first>
  <behavior>
    - Panel follows the queue and Active staff precedes Pending invitations.
    - Active rows show email/date/current-actor marker and no visible internal id.
    - Revoke opens a responsive confirmation; self/final controls remain visible disabled with shared inline reasons.
    - Success/refusal remains in an in-page status region across the refreshed snapshot.
  </behavior>
  <action>Insert `StaffManagementPanel` below the existing operational queue in `/ops`; do not create another route or move the queue. Build the Active staff heading/row semantics from the UI contract, showing email, formatted staff-since date, and exact `You` marker. Render Revoke for every row. For self/final rows keep it visible and disabled with the shared reason adjacent per D-19. For eligible rows open `StaffActionDialog` using the existing responsive-dialog pattern, explicit target email, Cancel/Revoke controls, destructive styling, initial-focus/cancel behavior, and bound `revokeStaffAction`. Keep success/refusal in a persistent `role=status`/live region above the sections, never a toast. Ensure rows/dialog content wrap at 320px.</action>
  <reversibility rating="reversible">The panel composes established UI/action primitives and introduces no persistent representation.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-management-panel.test.tsx tests/ops/enforcement-dialog.test.tsx</automated>
    <fails_when>The command exits non-zero, reports 0 passed, hides disabled revoke controls/reasons, places the panel above the queue, or loses confirmation after refresh.</fails_when>
  </verify>
  <acceptance_criteria>
    - DOM order is queue, staff panel, Active staff, Pending invitations.
    - Active rows expose required facts and `You` only for the current operator; no internal id is visible.
    - Self/last controls are visible-disabled and name the exact server reasons; eligible Revoke requires confirmation.
    - Dialog autofocus/escape/cancel behavior follows the shared responsive-dialog contract and long content stays within 320px.
    - A successful or refused revoke produces persistent in-page status text.
  </acceptance_criteria>
  <done>Operators can understand and perform eligible revocations without mistaking UI disablement for enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete Pending invitations and durable lifecycle feedback</name>
  <files>src/components/ops/staff-management-panel.tsx, src/components/ops/staff-action-dialog.tsx, src/app/actions/ops-staff.ts, tests/ops/staff-management-panel.test.tsx, tests/ops/staff-management-actions.test.ts</files>
  <read_first>
    - src/components/ops/staff-management-panel.tsx
    - src/components/ops/staff-action-dialog.tsx
    - src/app/actions/ops-staff.ts
    - src/lib/ops/staff-management.ts
    - tests/ops/staff-management-panel.test.tsx
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
  </read_first>
  <behavior>
    - Empty pending section remains visible with one neutral sentence and the invite form.
    - Pending rows show email, sent date, absolute expiry, inviter, immediate Resend, and confirmation-gated Cancel.
    - Resend success explicitly says the prior link was invalidated; Invite/Resend/Cancel outcomes persist in-page.
    - Long addresses/copy wrap and multiple rows create no horizontal page scroll at 320px.
  </behavior>
  <action>Add the email-only Invite form and Pending invitations section to `StaffManagementPanel`. When no pending rows exist, render D-20's neutral sentence while keeping the section and form. For each row render D-16's email, sent date, absolute expiry, and inviter; keep opaque id/version only in bound Server Function arguments. Wire Resend directly with pending state and a durable success message that explicitly says the prior link is invalidated. Wire Cancel through `StaffActionDialog` with target email and bound current version. Use the one shared persistent result region for Invite/Resend/Cancel/Revoke, clearing only when a later submitted action supplies a new result. Test zero/one/many lists, equality tie-breaks, long strings, focus, pending buttons, and non-toast persistence.</action>
  <reversibility rating="reversible">This expands the existing panel over the already reversible lifecycle actions.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-management-panel.test.tsx tests/ops/staff-management-actions.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, hides the empty pending section, rotates on duplicate Invite, skips Cancel confirmation, or omits prior-link invalidation from Resend success.</fails_when>
  </verify>
  <acceptance_criteria>
    - Empty, one-row, many-row, equal-time, long-email, and long-result fixtures all pass.
    - Invite has only email input and an active duplicate points to Resend without credential rotation.
    - Pending rows reveal no internal ids, preserve newest-first ordering, and show absolute—not relative-only—expiry.
    - Cancel requires confirmation; Resend is immediate; every success remains in the shared in-page region after revalidation.
  </acceptance_criteria>
  <done>The complete Active/Pending panel fulfills D-15 through D-20 at both data and interaction boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protected `/ops` render -> roster queries | Current identity controls access to staff and invitation metadata. |
| UI controls -> Server Functions | Stale or forged row/action data crosses into role and invitation mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-07-01 | Information Disclosure | roster surface | high | mitigate | Keep panel on guarded `/ops`, include query/action in guard census, and omit visible internal ids. |
| T-20-07-02 | Tampering | revoke eligibility | high | mitigate | Treat UI disablement as explanation only; re-run `writeRole` self/last/concurrency checks server-side. |
| T-20-07-03 | Elevation of Privilege | revoked-role cookie | high | mitigate | Every query/action calls current database-backed `requireStaff()` before work. |
| T-20-07-04 | Repudiation | invite/revoke actions | medium | mitigate | Preserve typed persistent outcomes and the domain's actor/target/action audit event. |
| T-20-07-05 | Information Disclosure | UI/audit text | medium | mitigate | Keep token/password/cookie/provider content out of DTOs and audit data; display only required roster facts. |
| T-20-07-SC | Tampering | package installs | high | mitigate | Reuse existing dialog, form, date, and test dependencies; no install task exists. |
</threat_model>

<verification>
- `npx vitest run tests/ops/staff-management-actions.test.ts tests/ops/staff-management-panel.test.tsx tests/ops/staff-policy.test.ts`
- `npx vitest run --config vitest.design.config.ts tests/design/ops-guard-coverage.test.ts tests/design/responsive-dialog-autofocus.test.tsx`
- `npx tsc --noEmit`
- Failure direction: any command exits non-zero, a suite reports 0 passed, or TypeScript reports an error.
</verification>

<success_criteria>
- The existing `/ops` page contains one two-section staff-management panel below its queue.
- Active/pending data, ordering, empty state, disabled reasons, confirmations, and persistent feedback meet D-15 through D-20.
- Direct/stale actions remain protected by current staff checks and the shared transactional policy.
- 320px and long-text backstops have explicit automated component coverage and final human verification in Plan 09.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-07-SUMMARY.md` when done.
</output>
