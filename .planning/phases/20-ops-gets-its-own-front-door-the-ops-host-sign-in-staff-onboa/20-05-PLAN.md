---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 05
type: execute
wave: 6
depends_on: [20-04]
files_modified:
  - src/lib/validation/ops-staff.ts
  - src/lib/ops/invitations.ts
  - src/lib/email.ts
  - src/lib/ops/grant.ts
  - tests/ops/staff-invitation.test.ts
autonomous: true
requirements: [OPS-09, OPS-11]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-07, an invitation expires at database time plus 24 hours and has exactly one successful acceptance."
    - "Per D-08 and D-13, each normalized email has at most one active invitation; Invite refuses an active duplicate, Resend atomically rotates it, and Cancel invalidates it without replacement."
    - "Per D-09, GET can inspect an invitation but only POST acceptance may consume it."
    - "Per D-10, expired, cancelled, used, malformed, and unknown credentials collapse to one inactive result without revealing prior existence."
    - "Per D-11 and D-12, marketplace-account and existing-staff conflicts are refused before email delivery or account mutation."
    - "Active unexpired verification rows are live pending state; accept/cancel/expired-row pruning may delete them only with an immutable nonsecret audit event as the durable invitation history, and no inactive-invite listing or new table is introduced."
    - "OPS-09 adjacency: when accept/resend/cancel operations meet on the same live invitation, one guarded mutation wins and every loser receives the same inactive/stale outcome."
    - "OPS-09 empty: absent or null credentials and empty name/password input never mutate; an email with no pending invitation may be issued exactly one live invitation."
    - "OPS-09 ordering: a successful resend makes its newer credential authoritative immediately; completion order cannot reactivate the older credential."
  artifacts:
    - path: src/lib/validation/ops-staff.ts
      provides: "normalized issue and acceptance input schemas"
    - path: src/lib/ops/invitations.ts
      provides: "`issueStaffInvitation`, `resendStaffInvitation`, `cancelStaffInvitation`, `inspectStaffInvitation`, and `acceptStaffInvitation`"
    - path: src/lib/email.ts
      provides: "`sendStaffInviteEmail` using an absolute ops-host URL"
    - path: tests/ops/staff-invitation.test.ts
      provides: "lifecycle, conflict, disclosure, and concurrency coverage"
  key_links:
    - from: src/lib/ops/invitations.ts
      to: verification table
      via: "deterministic email-scoped identifier plus versioned hashed-token payload; no new table"
    - from: src/lib/ops/invitations.ts
      to: src/lib/ops/grant.ts
      via: "acceptance delegates the staff role transition and audit policy to `writeRole` inside the transaction"
    - from: src/lib/email.ts
      to: src/lib/app-origins.ts
      via: "invite links are absolute ops-origin URLs"
  prohibitions:
    - statement: "Never consume an invitation on GET or place its raw credential in persisted rows, logs, errors, analytics, or audit metadata."
      status: unverified
      flagged: true
---

<objective>
Implement the storage, email, and transactional domain mechanics for the complete staff invitation lifecycle, delivering D-07 through D-14 without adding a table.

Purpose: Ops can onboard staff without production database credentials while raw credentials, link scanners, retries, and races cannot create or consume accounts incorrectly.
Output: Invitation domain service, validation contracts, real email sender, and concurrency/security tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `issueStaffInvitationInput` and `acceptStaffInvitationInput` in `src/lib/validation/ops-staff.ts`.
- `StaffInvitationState`, `issueStaffInvitation`, `resendStaffInvitation`, `cancelStaffInvitation`, `inspectStaffInvitation`, and `acceptStaffInvitation` in `src/lib/ops/invitations.ts`.
- `sendStaffInviteEmail` in `src/lib/email.ts`.
- `tests/ops/staff-invitation.test.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Issue, resend, and cancel one hashed 24-hour invitation per email</name>
  <files>src/lib/validation/ops-staff.ts, src/lib/ops/invitations.ts, src/lib/email.ts, tests/ops/staff-invitation.test.ts</files>
  <read_first>
    - src/lib/db/schema.ts
    - src/lib/auth.ts
    - src/lib/email.ts
    - src/lib/app-origins.ts
    - src/lib/validation/ops.ts
    - tests/auth/reset-revokes-sessions.test.ts
    - tests/auth/email-escaping.test.ts
    - tests/auth/email-injection.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - A normalized email with no account/pending row gets one verification-backed invitation expiring in 24 hours.
    - An active duplicate returns `active-invitation-exists` and neither rotates nor sends.
    - Resend conditionally replaces the active hashed credential and makes the previous link inactive before email delivery.
    - Cancel conditionally deletes/invalidates the active credential and sends no email.
    - Delivery failure reports `pending-delivery-failed` while the credential remains pending for operator retry.
    - Successful issue, resend, cancel, and expired-row pruning commit their immutable nonsecret invitation audit event with the live-state mutation.
  </behavior>
  <action>Create validation for normalized email and server-bound pending-row identity. Reuse the existing Better Auth verification table only: use a deterministic, normalized-email-scoped identifier; store SHA-256 of a cryptographically random raw token rather than the token; store a versioned JSON value with invited email, inviter user id, and database `sentAt`; set `expiresAt` from database time plus exactly 24 hours. Use conditional insert/update/delete with returned rows so duplicate Invite refuses per D-13, Resend per D-08 replaces only the currently observed version, and Cancel invalidates only the currently observed version. In the same database transaction as each successful issue/resend/cancel mutation, insert an immutable audit event containing actor/invitation ids and enum-shaped outcome only. Keep expired rows inert and excluded from the live list; if implementation prunes an expired row, require the same transaction to insert its expiry audit event before deletion. Run account conflict checks before persistence: D-11 refuses `canBook` or `canHost` with `separate-staff-email`, D-12 refuses `isStaff` with exact `Already a staff member`. After the database commits, call `sendStaffInviteEmail` with an absolute `OPS_APP_URL` invite link; expose the raw token only to that call. A delivery error returns a typed pending state and must not print token/email payloads. Do not add an inactive-invitation query/surface or change schema files.</action>
  <reversibility rating="reversible">The verification-row encoding is versioned and uses an existing table; pending rows can be invalidated without migrating schema.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-invitation.test.ts -t "issue|resend|cancel|conflict|delivery"</automated>
    <fails_when>The command exits non-zero, reports 0 passed, creates two live credentials for one email, sends on conflict, or exposes a raw token in captured logs/audit data.</fails_when>
  </verify>
  <acceptance_criteria>
    - Stored verification data contains a SHA-256 token digest and versioned metadata, never the raw token.
    - Expiry is asserted against database time at exactly 24 hours, including equality at the boundary as inactive.
    - Duplicate Invite is side-effect free; Resend rotates once; Cancel leaves no active credential; racing equal-version operations have one winner.
    - Existing staff and marketplace-capable emails produce their D-12/D-11 refusals and no outgoing message.
    - Successful issue/resend/cancel and any expired-row prune retain an immutable audit event after the verification row changes or disappears; the audit payload contains no email or credential.
    - `git diff -- src/lib/db/schema.ts drizzle` is empty.
  </acceptance_criteria>
  <done>The pending-invitation lifecycle is single-row, hash-only, conflict-safe, and email-capable without schema work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Accept exactly once through one guarded transaction</name>
  <files>src/lib/ops/invitations.ts, src/lib/ops/grant.ts, tests/ops/staff-invitation.test.ts</files>
  <read_first>
    - src/lib/ops/invitations.ts
    - src/lib/ops/grant.ts
    - src/lib/auth.ts
    - src/lib/db/schema.ts
    - tests/auth/reset-revokes-sessions.test.ts
    - tests/ops/staff-policy.test.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
  </read_first>
  <behavior>
    - Inspection is read-only and all inactive credential classes return the same public state.
    - The acceptance POST consumes the exact live digest once, then creates user, credential account, verified email state, staff role, and audit record atomically.
    - Two simultaneous acceptance attempts yield one staff account and one inactive loser.
    - The created staff account has `canBook=false`, `canHost=false`, `isStaff=true`.
  </behavior>
  <action>Implement `inspectStaffInvitation(rawToken)` as a read-only digest lookup that maps malformed, missing, expired-at-or-before-now, cancelled, and used cases to one `inactive` result per D-10. Implement `acceptStaffInvitation({ token, name, password })` as POST-only domain work: validate name/password against established auth bounds; start a transaction; acquire the invitation/email advisory lock; guard-consume the matching unexpired token using conditional `DELETE ... RETURNING`; re-check email/account conflicts inside the lock; hash the password with Better Auth's installed public `hashPassword` export; create the user and credential account records in the shapes expected by the existing auth adapter; mark email verified; set `canBook=false`, `canHost=false`; delegate the staff transition/audit to transaction-aware `writeRole`; insert the immutable invitation-accepted audit event with ids/enums only; and commit all-or-nothing. A zero-row consume and every stale/conflict outcome return the same inactive/refusal class without route-existence detail. Never select an invitation by client-provided email or row id, and never retain or list an inactive invitation row as history because the audit event is the durable record.</action>
  <reversibility rating="costly">Acceptance creates a real identity and irreversible audit history, but a failed transaction leaves no partial account and the staff role can later be revoked safely.</reversibility>
  <verify>
    <automated>npx vitest run tests/ops/staff-invitation.test.ts -t "inspect|accept|concurrent|inactive"</automated>
    <fails_when>The command exits non-zero, reports 0 passed, GET/inspection mutates, concurrent acceptance creates more than one user, or any partial user/account/role/audit state survives a forced failure.</fails_when>
  </verify>
  <acceptance_criteria>
    - Tests prove GET/inspection leaves the verification row unchanged.
    - Exactly one of two concurrent accepts succeeds; the loser and every inactive token class expose the same public state.
    - A forced error after each write point rolls back user, account, role, audit, and consume together.
    - After successful acceptance the live row is absent and both the role-change and invitation-accepted audit events remain queryable without email/token/password metadata.
    - The resulting credential signs in through the existing Better Auth email/password path and the user is verified, staff-only, and marketplace-incapable.
  </acceptance_criteria>
  <done>A live invitation can create exactly one complete staff credential, and scanners/retries/races cannot consume or partially create it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator input -> invitation service | Email and lifecycle intent enter authenticated domain logic. |
| Email link -> public inspection/acceptance | An untrusted bearer credential reaches read-only GET and mutating POST paths. |
| Application -> email provider | The sole raw token crosses into an outbound absolute ops-host link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-05-01 | Elevation of Privilege | arbitrary-target invite acceptance | high | mitigate | Select only by hashed bearer token and bind email/metadata server-side; never accept target email/id from the client. |
| T-20-05-02 | Information Disclosure | invitation token disclosure through persistence/log/audit | high | mitigate | Persist SHA-256 only, pass raw token only to mail delivery, and assert captured diagnostics contain no token or email payload. |
| T-20-05-03 | Tampering | GET/link scanner | high | mitigate | Keep inspection read-only; allow consumption only in guarded POST transaction. |
| T-20-05-04 | Tampering | duplicate/stale lifecycle operations | high | mitigate | Advisory-lock by invitation/email and use conditional returned-row mutations with one winner. |
| T-20-05-05 | Elevation of Privilege | marketplace-account collision | high | mitigate | Re-check staff/marketplace conflicts inside acceptance transaction and never convert from invitation flow. |
| T-20-05-06 | Information Disclosure | audit/event data | medium | mitigate | Use immutable audit events as durable history for issue/resend/cancel/expiry/accept; store ids, action, timestamps, and nonsecret state only, omitting email, token, password, cookie, and provider body. |
| T-20-05-SC | Tampering | package installs | high | mitigate | Use installed crypto/auth/email libraries only; package manifest or lockfile changes are outside scope. |
</threat_model>

<verification>
- `npx vitest run tests/ops/staff-invitation.test.ts`
- `npx tsc --noEmit`
- `git diff --exit-code -- src/lib/db/schema.ts drizzle`
- Failure direction: any command exits non-zero, the invitation suite reports 0 passed, TypeScript reports an error, or schema/migration paths differ.
</verification>

<success_criteria>
- Invite, resend, cancel, inspect, and accept honor D-07 through D-14 and use no new table.
- Raw credentials exist only in memory for the outbound link/request and never in persisted/audit/log data.
- GET never consumes, POST consumes once, and equal-time races have one guarded winner.
- Acceptance creates one complete Better Auth credential and staff-only user atomically.
- Only active pending rows appear in the roster; immutable audit events retain accepted/cancelled/expired history without a new table or inactive listing.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-05-SUMMARY.md` when done.
</output>
