---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 12
type: execute
wave: 7
depends_on: [20-03, 20-14]
files_modified:
  - src/lib/ops/ops-callback.ts
  - src/app/actions/ops-auth.ts
  - src/app/(ops-auth)/_ops-auth/layout.tsx
  - src/app/(ops-auth)/_ops-auth/login/page.tsx
  - src/app/(ops-auth)/_ops-auth/forgot-password/page.tsx
  - src/app/(ops-auth)/_ops-auth/reset-password/page.tsx
  - tests/auth/ops-host-auth.test.ts
  - tests/security/ops-callback.test.ts
autonomous: true
requirements: [OPS-08]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: ops sign-in is a familiar FitOut Ops email/password card with no Google control, signup route, marketplace language, or security boast."
    - "D-03/D-06: password recovery/reset and sign-out remain on the ops host from entry through return-to-sign-in and do not create a marketplace session."
    - "D-04: callback normalization admits only `/ops` or an `/ops/...` descendant on the exact ops origin; every other value returns `/ops`."
    - "D-05: valid nonstaff credentials produce one neutral refusal and the just-created ops-host session is cleared before the response."
    - "Every ops-auth Server Function verifies the exact configured ops Host+Origin server-side before parsing, credential/session work, or domain mutation."
    - "Blank, loading, validation, credential, nonstaff, recovery, and reset states preserve the UI-SPEC's labelled controls, bounded messages, first-invalid focus, and local in-flight behavior."
  artifacts:
    - path: src/lib/ops/ops-callback.ts
      provides: "Safe ops-only callback normalization"
      exports: [safeOpsCallback]
    - path: src/app/actions/ops-auth.ts
      provides: "Origin-bound sign-in, sign-out, recovery, and reset Server Functions"
      exports: [signInOps, signOutOps, requestOpsPasswordReset, resetOpsPassword]
    - path: src/app/(ops-auth)/_ops-auth/login/page.tsx
      provides: "Dedicated FitOut Ops sign-in surface"
  key_links:
    - from: src/app/actions/ops-auth.ts
      to: src/lib/ops/staff.ts
      via: "exact ops action-origin guard followed by post-credential role read and immediate nonstaff sign-out"
      pattern: "requireOpsMutationOrigin|signInOps|signOutOps"
    - from: src/app/(ops-auth)/_ops-auth/login/page.tsx
      to: src/app/actions/ops-auth.ts
      via: "email/password submission with bounded callback/result state"
      pattern: "signInOps"
---

<objective>
Deliver the dedicated ops sign-in, recovery, reset, and sign-out journey after exact auth origins and the server-side action-origin boundary exist.

Purpose: staff authenticate at the ops origin without sharing sessions, accepting marketplace-host action dispatch, or inheriting consumer-facing auth UI.
Output: safe callbacks, origin-bound auth Server Functions, and sibling `(ops-auth)` pages.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-14-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

- `safeOpsCallback(raw, opsOrigin): string`.
- `OpsSignInResult`, bounded arrival/result enums, and origin-bound Server Functions `signInOps`, `signOutOps`, `requestOpsPasswordReset`, `resetOpsPassword`.
- Internal sibling routes `/_ops-auth/login`, `/_ops-auth/forgot-password`, and `/_ops-auth/reset-password`, exposed only by exact-ops-host rewrites.
- `tests/security/ops-callback.test.ts` plus expanded `tests/auth/ops-host-auth.test.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the origin-bound credential transition and callback contract</name>
  <reversibility rating="reversible">The page/action composition is internal and can change behind the same sign-in contract.</reversibility>
  <files>src/lib/ops/ops-callback.ts, src/app/actions/ops-auth.ts, tests/auth/ops-host-auth.test.ts, tests/security/ops-callback.test.ts</files>
  <read_first>
    - src/lib/ops/ops-callback.ts (new)
    - src/app/actions/ops-auth.ts (new)
    - tests/auth/ops-host-auth.test.ts
    - tests/security/ops-callback.test.ts (new)
    - src/app/actions/auth.ts
    - src/lib/safe-callback-url.ts
    - src/lib/validation/auth.ts
    - src/lib/ops/staff.ts
  </read_first>
  <behavior>
    - Ops auth functions require exact configured ops Host+Origin before parsing or calling Better Auth; marketplace/missing/mismatched request authorities are neutrally refused.
    - Staff credentials return a safe ops callback; invalid credentials return the generic credential error.
    - Correct nonstaff credentials are immediately signed out and receive only the neutral D-05 refusal.
    - Cross-origin, protocol-relative, backslash, dot-segment, encoded, public-path, and non-ops callbacks fall back to `/ops`.
  </behavior>
  <action>Implement `safeOpsCallback` by delegating to the shipped parse-and-origin-check helper, then allow only `/ops` and `/ops/...`. Create `ops-auth.ts` Server Functions using the existing login Zod schema. Begin each function with Plan 20-14's `requireOpsMutationOrigin()` exact Host+Origin check before parsing, credential/session work, or any domain call; then pass `await headers()` into direct Better Auth calls. `signInOps` must credential-authenticate, read back the role, clear any nonstaff session before returning the fixed refusal, and return only the normalized callback for staff. Keep expected credential/nonstaff outcomes typed and bounded for the UI; accept no arbitrary result copy from query or form data.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-host-auth.test.ts tests/security/ops-callback.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, accepts a non-ops Host/Origin, retains a nonstaff session, or accepts an unsafe callback.</fails_when>
  </verify>
  <acceptance_criteria>
    - A marketplace-host dispatch with a deliberately presented credential/session is refused before Better Auth/domain spies run.
    - Nonstaff successful credentials leave no ops-host session and disclose no booker/host classification.
    - All direct Better Auth calls carry incoming headers and all unsafe callbacks return `/ops`.
  </acceptance_criteria>
  <done>The server-side credential transition accepts only exact ops authority and never leaves a nonstaff session behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the dedicated FitOut Ops sign-in shell and form</name>
  <reversibility rating="reversible">The sibling route-group UI composes existing auth primitives behind the stable action contract.</reversibility>
  <files>src/app/(ops-auth)/_ops-auth/layout.tsx, src/app/(ops-auth)/_ops-auth/login/page.tsx, tests/auth/ops-host-auth.test.ts</files>
  <read_first>
    - src/app/(ops-auth)/_ops-auth/layout.tsx (new)
    - src/app/(ops-auth)/_ops-auth/login/page.tsx (new)
    - tests/auth/ops-host-auth.test.ts
    - src/app/(auth)/login/page.tsx
    - src/app/actions/ops-auth.ts
    - src/lib/validation/auth.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
  </read_first>
  <behavior>
    - Exact D-01/D-02 identity and copy render with email/password only and recovery as the sole secondary route.
    - Labelled/autocomplete fields preserve input and focus the first invalid field.
    - Credential/nonstaff refusals and bounded signed-out/account-created arrival notices use the UI-SPEC's exact disclosure boundary.
  </behavior>
  <action>Create the sibling `(ops-auth)` shell and login card over Task 1's origin-bound action. Use the UI-SPEC's `PanelCard`, field, validation, and neutral button patterns with exact `FitOut Ops`, `Sign in`, and `Sign in with your staff account.` copy. Include only email, password, and password recovery; omit Google, signup, marketplace language, and security boasts. Render bounded signed-out/account-created notices, preserve input on expected refusal, focus the first invalid field, and never render arbitrary query-string text. Keep the route group beside `(ops)`, not beneath its staff-cloaking layout.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-host-auth.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, omits the locked identity/copy, renders a prohibited consumer control, or bypasses the origin-bound action.</fails_when>
  </verify>
  <acceptance_criteria>
    - The login document contains exact D-01/D-02 copy and one email/password form with recovery as the sole secondary route.
    - No Google control, signup link, marketplace language, mode switch, or security boast renders.
    - Validation focuses the first invalid field; expected refusals preserve input and use bounded copy.
    - The route group is a sibling of `(ops)`, not nested beneath it.
  </acceptance_criteria>
  <done>The dedicated sign-in surface is familiar, explicitly FitOut Ops, and wired only to the origin-bound credential transition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep password recovery, reset, and sign-out inside the ops origin</name>
  <reversibility rating="reversible">These screens wrap the installed Better Auth reset lifecycle without changing its persisted schema or token format.</reversibility>
  <files>src/app/actions/ops-auth.ts, src/app/(ops-auth)/_ops-auth/forgot-password/page.tsx, src/app/(ops-auth)/_ops-auth/reset-password/page.tsx, tests/auth/ops-host-auth.test.ts</files>
  <read_first>
    - src/app/actions/ops-auth.ts
    - src/app/(ops-auth)/_ops-auth/forgot-password/page.tsx (new)
    - src/app/(ops-auth)/_ops-auth/reset-password/page.tsx (new)
    - tests/auth/ops-host-auth.test.ts
    - src/app/(auth)/forgot-password/page.tsx
    - src/app/(auth)/reset-password/page.tsx
    - src/lib/ops/staff.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-UI-SPEC.md
  </read_first>
  <behavior>
    - Recovery returns the same enumeration-safe success for absent, nonstaff, staff, and delivery outcomes.
    - Reset links are minted with the exact ops origin and missing/malformed/expired tokens share one refusal and recovery route.
    - Successful reset and explicit sign-out return to ops `/login`; neither establishes a public-host session.
    - Marketplace/missing/mismatched Host or Origin reaches no reset/session domain mutation.
  </behavior>
  <action>Wrap the installed Better Auth recovery/reset APIs with the exact ops action-origin guard first, incoming headers second, and exact ops callbacks. Do not create a parallel reset token or change the global email-verification rule. Build the two internal pages from existing auth patterns and the locked UI-SPEC copy. Implement `signOutOps` as a server-confirmed transition to visible ops `/login?signedOut=1`; sign-out must clear only the current host's cookie. Preserve the installed one-hour reset expiry and revoke-sessions-on-reset setting. Extend the tests so forged marketplace Host/Origin combinations fail before delivery, reset, or sign-out spies run.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-host-auth.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 tests or 0 passed, a recovery/reset/sign-out escapes the ops origin, or a non-ops request reaches a domain mutation.</fails_when>
  </verify>
  <acceptance_criteria>
    - Recovery email URLs, reset POSTs, back links, and post-reset sign-in all remain on the exact ops origin.
    - The request result cannot enumerate account existence or staff role.
    - The reset form uses the shared 10–128 password bounds and never changes `requireEmailVerification` globally.
    - Explicit sign-out renders only the bounded `Staff session ended.` notice.
    - Missing or mismatched Host/Origin fails closed before any delivery/session/reset mutation.
  </acceptance_criteria>
  <done>The credential recovery and exit journey is origin-bound, host-local, and enumeration-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| request Host/Origin -> ops-auth Server Function | Public POST dispatch must prove the exact configured ops request authority before auth/session work. |
| credentials -> staff role read | Valid credentials alone do not authorize ops access. |
| reset email -> browser | Absolute reset URL must remain inside the trusted ops origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-12-01 | Spoofing | marketplace-host ops-auth action dispatch | high | mitigate | Require exact configured ops Host+Origin server-side before every auth/domain call. |
| T-20-12-02 | Elevation of Privilege | nonstaff credential transition | high | mitigate | Read current role after sign-in and clear the just-created session before neutral refusal. |
| T-20-12-03 | Spoofing | callback open redirect | high | mitigate | Parse/recheck exact origin and admit only `/ops` or `/ops/...`. |
| T-20-12-04 | Information Disclosure | credential/recovery result | medium | mitigate | Use generic credential and enumeration-safe recovery outcomes with bounded query-state enums. |
| T-20-12-05 | Elevation of Privilege | cross-host session bleed | high | mitigate | Preserve host-only cookies and test non-ops dispatch/session behavior. |
| T-20-12-SC | Tampering | package installs | high | mitigate | Reuse installed Better Auth, validation, and UI dependencies; manifests and lockfile remain unchanged. |
</threat_model>

<verification>
Run the focused ops-auth and callback suites plus `npx tsc --noEmit`; Plan 20-08 and Plan 20-09 own browser sign-out/cross-host completion.
</verification>

<success_criteria>
- Staff sign-in/recovery/reset/sign-out stay on the exact ops request authority.
- Marketplace-host Server Function dispatch reaches no ops-auth mutation even with deliberately presented credentials/session.
- Nonstaff credentials leave no ops session and reveal only the fixed refusal.
- The dedicated UI meets D-01 through D-06 without consumer controls.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-12-SUMMARY.md` when done.
</output>
