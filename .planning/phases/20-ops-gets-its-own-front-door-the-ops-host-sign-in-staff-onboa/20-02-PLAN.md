---
phase: 20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/lib/app-origins.ts
  - .env.example
  - src/proxy.ts
  - src/app/_ops-cloak/page.tsx
  - tests/auth/ops-host-routing.test.ts
  - tests/design/ops-host-invariants.test.ts
  - src/app/api/paymongo/webhook/route.ts
  - src/app/api/didit/webhook/route.ts
  - scripts/verify-ops-cloak.mjs
  - tests/auth/ops-cloak-probe.test.ts
  - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
autonomous: true
requirements: [OPS-07, OPS-12]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "OPS-07: only the exact configured ops host receives the ops console/auth surface; public, exact preview, and unknown hosts remain marketplace/public and cloak protected/internal ops paths."
    - "OPS-12 first reading: after the partition lands, staff /ops is 200 while non-staff, signed-out, and nonexistent ops routes are 404 with byte-identical raw bodies by SHA-256."
    - "The Proxy imports no auth instance or database client and never authorizes; layout/page/action guards remain authoritative."
    - "OPS-12 adjacency edge: `/ops` and `/ops/...` are protected segment matches, while `/opsfoo` is not silently merged into the protected namespace."
    - "OPS-12 empty edge: an empty route census or missing control request is a failing probe, never a vacuous success."
    - "OPS-12 ordering edge: probe rows are sorted by host, path, and actor class so repeated evidence diffs reflect real response drift."
  artifacts:
    - path: src/lib/app-origins.ts
      provides: "Validated exact public/ops origin and hostname authority"
      exports: [PUBLIC_APP_ORIGIN, OPS_APP_ORIGIN, classifyRequestHost, absolutePublicUrl]
    - path: src/proxy.ts
      provides: "Exact-host route partition and internal ops-auth rewrites"
      exports: [proxy, config]
    - path: src/app/_ops-cloak/page.tsx
      provides: "Shared pre-stream denied-route target without an ops-scoped not-found file"
    - path: scripts/verify-ops-cloak.mjs
      provides: "Production HTTP status/header/raw-body hash probe and evidence validator"
  key_links:
    - from: src/proxy.ts
      to: src/lib/app-origins.ts
      via: "exact normalized host classification"
      pattern: "classifyRequestHost"
    - from: scripts/verify-ops-cloak.mjs
      to: .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md
      via: "partition-stage status/header/SHA-256 transcript"
      pattern: "partition"
  prohibitions:
    - statement: "The ops front door must never silently become a wildcard preview-host console or a second public staff-signup surface."
      status: unverified
      flagged: true
  assumptions:
    - requirement_id: OPS-07
      category: unclassified
      status: unresolved
      flagged: true
      probe: "unclassified — review manually"
      statement: "OPS-07 has no SPEC edge taxonomy; the exact-host matrix and first production cloak reading are the explicit assumption boundary."
---

<objective>
Install the exact Host partition and prove its first production cloak reading before any ops authentication or onboarding feature lands.

Purpose: make the ops host the milestone's hard routing prerequisite while keeping authorization out of Proxy.
Output: one origin authority, exact route matrix, shared cloak target, and a dated partition-stage transcript.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-CONTEXT.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
@AGENTS.md
</context>

<assumption_delta_decision>
Primary noun: request surface/session context, identified by exact configured host + origin.
Decision: promote.
Rationale: the apex is no longer the universal application context; marketplace and ops hosts are first-class variants, while each retains its own cookie jar, trusted-origin rules, callback boundary, and absolute cross-host exits.
Invariant test: every supported host round-trips sign-in/reset/invite URLs within its own origin, and a session presented to the other host is anonymous/non-staff.
</assumption_delta_decision>

## Artifacts this phase produces

- `src/lib/app-origins.ts`: `PUBLIC_APP_ORIGIN`, `OPS_APP_ORIGIN`, `classifyRequestHost()`, `absolutePublicUrl()` and exact-host types.
- `.env.example`: server-only `OPS_APP_URL` with localhost guidance; no secret or public client variable.
- `src/app/_ops-cloak/page.tsx`: internal denial route that calls the shipped root `notFound()` path before streaming.
- `tests/auth/ops-host-routing.test.ts`, `tests/design/ops-host-invariants.test.ts`, `tests/auth/ops-cloak-probe.test.ts`.
- `scripts/verify-ops-cloak.mjs`: `--probe`, `--stage partition|final`, and `--check-evidence` flags.
- `20-EVIDENCE.md` § `Partition reading`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route exact configured hosts through one origin authority</name>
  <reversibility rating="costly">Changing the shared origin keys later requires coordinated Proxy, auth, email, navigation, deployment, and test updates.</reversibility>
  <files>src/lib/app-origins.ts, .env.example, src/proxy.ts, tests/auth/ops-host-routing.test.ts</files>
  <read_first>
    - src/lib/app-origins.ts (new)
    - .env.example
    - src/proxy.ts
    - tests/auth/ops-host-routing.test.ts (new)
    - src/lib/safe-callback-url.ts
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-RESEARCH.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Exact ops host: visible `/login`, `/forgot-password`, `/reset-password`, and `/invite/:token` rewrite to `/_ops-auth/...`; `/ops`, `/api/auth/*`, Next assets, and required public assets continue.
    - Public/configured-preview/unknown host: marketplace auth/invite routes continue, while `/ops*` and direct `/_ops-auth*` access use the cloak.
    - Host comparison lowercases and normalizes an optional port against parsed configured URL hosts; it never uses suffix or wildcard matching.
    - Existing `_sc` deferral still runs for marketplace `/login` and `/signup` only.
  </behavior>
  <action>Create `src/lib/app-origins.ts` as the sole validated server origin authority over existing `BETTER_AUTH_URL`/`NEXT_PUBLIC_APP_URL` fallback and new `OPS_APP_URL`. Export parsed origins/hosts and a pure `classifyRequestHost(rawHost)` returning `ops | public | unknown`; admit `VERCEL_URL` only as one exact public preview hostname. Extend `src/proxy.ts` after Plan 01's isolated commit: use an explicit pathname matrix and `NextResponse.rewrite`, widen the static matcher so every relevant request is classified, and keep the module database-free/auth-instance-free. Preserve marketplace `_sc` behavior. Write the pure matrix tests first, including case, port, missing/forged Host, exact preview, direct internal path, `/opsfoo`, assets, auth API, and both visible auth surfaces.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-host-routing.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests or 0 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `OPS_APP_URL` is parsed fail-closed and is absent from client-exposed `NEXT_PUBLIC_*` configuration.
    - No suffix/wildcard classification can turn a generated preview or attacker-controlled hostname into `ops`.
    - The test matrix distinguishes `/ops`, `/ops/...`, and `/opsfoo`, and asserts a non-empty case count.
    - `src/proxy.ts` imports neither `@/lib/auth` nor `@/lib/db` and contains no query or session-role read.
  </acceptance_criteria>
  <done>The exact Host partition selects surfaces without becoming an authorization layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give every denied host/path combination one pre-stream cloak target</name>
  <reversibility rating="costly">The denial route shape is shared by all protected paths and is measured at the wire, so changing it requires coordinated route and evidence updates.</reversibility>
  <files>src/app/_ops-cloak/page.tsx, src/proxy.ts, tests/auth/ops-host-routing.test.ts, tests/design/ops-host-invariants.test.ts</files>
  <read_first>
    - src/app/_ops-cloak/page.tsx (new)
    - src/proxy.ts
    - tests/auth/ops-host-routing.test.ts
    - tests/design/ops-host-invariants.test.ts (new)
    - src/app/not-found.tsx
    - src/app/(ops)/ops/layout.tsx
    - tests/design/ops-guard-coverage.test.ts
  </read_first>
  <behavior>
    - Public-host `/ops*`, direct internal ops-auth paths, and exact-ops-host nonexistent protected paths take the shared denial route.
    - The root static `src/app/not-found.tsx` is byte-unchanged and no `(ops)` or `(ops-auth)` scoped `not-found.tsx` exists.
    - Staff `/ops` still reaches the existing guarded page; Proxy never tests staff role.
  </behavior>
  <action>Add a top-level internal cloak page that terminates with `notFound()` before returning JSX and is outside both `(ops)` and `(ops-auth)`. Route only denied matrix cells to it. Extend the design gate to pin the sole Proxy file/export, exact internal route census, root 404 byte identity, absence of scoped not-found files, database/auth import ban, and continued three-layer guard coverage. Keep the candidate only if Task 3's production hashes prove the three denial bodies equal; if a route-tree distinction remains, converge all denied matrix cells on the same top-level target without adding host-dependent root 404 logic.</action>
  <verify>
    <automated>npx vitest run tests/auth/ops-host-routing.test.ts --config vitest.config.ts</automated>
    <fails_when>The command exits non-zero or reports 0 tests or 0 passed.</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/ops-host-invariants.test.ts</automated>
    <fails_when>The command exits non-zero or reports 0 tests or 0 passed.</fails_when>
  </verify>
  <acceptance_criteria>
    - All denied route-matrix cells resolve to the same internal target and no redirect reveals the ops host.
    - Root `src/app/not-found.tsx` has no changed bytes and remains prerenderable/host-independent.
    - The design gate fails on an added scoped not-found file, auth/database import, wildcard ops host, or missing route case.
    - The existing `(ops)` layout and page guards remain intact.
  </acceptance_criteria>
  <done>The partition has a single measurable cloak path without weakening staff authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the first 404-cloak reading immediately after partition</name>
  <reversibility rating="reversible">The probe and transcript can evolve without changing product behavior.</reversibility>
  <files>src/app/api/paymongo/webhook/route.ts, src/app/api/didit/webhook/route.ts, scripts/verify-ops-cloak.mjs, tests/auth/ops-cloak-probe.test.ts, .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md</files>
  <read_first>
    - src/app/api/paymongo/webhook/route.ts
    - src/app/api/didit/webhook/route.ts
    - scripts/verify-ops-cloak.mjs (new)
    - tests/auth/ops-cloak-probe.test.ts (new)
    - .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md (new)
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md
  </read_first>
  <action>Update the two webhook header comments whose matcher claim became stale, without changing webhook code. Create a Node production probe that requires a non-empty route matrix, records status, selected headers, byte length, and SHA-256 over unmodified response bytes, and validates stage-specific evidence. Build fully, start `next start` only after the build exits, create isolated staff/nonstaff/signed-out cookie contexts without logging cookie values, and record the partition reading in `20-EVIDENCE.md`: staff `/ops` 200; nonstaff `/ops`, signed-out `/ops`, nonexistent ops path, marketplace `/ops`, sign-in on both hosts, and ops-host `/api/auth/*`. Treat `403 INVALID_ORIGIN`, any 200 denial, any missing control, or unequal denial hash as a blocking finding and repair the partition before proceeding.</action>
  <verify>
    <automated>node scripts/verify-ops-cloak.mjs --check-evidence .planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-EVIDENCE.md --stage partition</automated>
    <fails_when>non-zero exit, a required route row is absent, a denial status is not 404, the staff control is not 200, or the three denial hashes differ</fails_when>
  </verify>
  <acceptance_criteria>
    - Evidence names the commit, date, Next.js version, exact configured hosts, production `next start`, and every partition-stage route/account class.
    - Three locked 404 classes have identical raw-body SHA-256; header differences are recorded and must not reveal a different route class.
    - Cookie/token values, email addresses, database URLs, and secrets are absent from evidence and console output.
    - Both webhook files change only their source-path/matcher commentary.
  </acceptance_criteria>
  <done>The first required cloak measurement is dated, machine-validated, and green before ops auth work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host header → surface classifier | Attacker-controlled authority data chooses only a public or ops route surface |
| Proxy rewrite → App Router | Internal targets must not become directly reachable public routes |
| HTTP response → anonymous observer | Status, headers, and bytes can reveal route existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-04 | Spoofing | Host spoofing / classification | high | mitigate | Exact parsed configured hosts, exact preview allowlist, no suffix/wildcard matching, pure route-matrix tests. |
| T-20-05 | Information Disclosure | route-existence oracle on marketplace `/ops` and internal routes | high | mitigate | Shared pre-stream cloak target plus production status/header/raw-body hash probe. |
| T-20-06 | Elevation of Privilege | Proxy | high | mitigate | Proxy never imports auth/database and every existing ops guard remains authoritative. |
| T-20-07 | Denial of Service | all-request matcher | medium | mitigate | Explicit asset/auth/public pass cells and focused route-matrix coverage preserve required static/runtime traffic. |
</threat_model>

<verification>
Run routing/design tests, `npm run build`, and the partition-stage evidence validator. Stop the phase if the first cloak hashes are unequal.
</verification>

<success_criteria>
- Exact configured host/origin identifies the request surface.
- Marketplace `/ops` is a byte-identical cloak, never a redirect.
- The first 404 reading exists before any later ops route is added.
</success_criteria>

<output>
Create `.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-02-SUMMARY.md` when done.
</output>
