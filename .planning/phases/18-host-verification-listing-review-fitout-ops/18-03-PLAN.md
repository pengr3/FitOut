---
phase: 18-host-verification-listing-review-fitout-ops
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - src/lib/bookability.ts
  - src/lib/search/query.ts
  - src/app/actions/booking.ts
  - src/app/listings/[id]/(detail)/page.tsx
  - src/app/(host)/host/listings/page.tsx
  - tests/helpers/seed.ts
  - tests/search/bookable-gate.test.ts
  - tests/listing/bookability.test.ts
  - tests/booking/state-machine.test.ts
  - tests/booking/open-capacity-hold.test.ts
  - tests/availability/open-capacity-blocks.test.ts
  - tests/availability/open-capacity-hours-rekey.test.ts
  - tests/booking/notify-emission.test.ts
  - tests/booking/open-capacity-cancel.test.ts
  - tests/booking/open-capacity-replay.test.ts
  - tests/booking/request-lifecycle.test.ts
  - tests/booking/service-fee-hold.test.ts
  - tests/listing/open-capacity-edit-gate.test.ts
  - tests/payments/host-cancel.test.ts
  - tests/payments/ledger-freeze.test.ts
  - tests/payments/payout-sweep.test.ts
  - tests/paymongo/webhook-merchant-activated.test.ts
  - tests/search/availability-filter.test.ts
  - tests/search/open-capacity-search.test.ts
  - scripts/seed.ts
  - scripts/seed-baseline-fixtures.ts
  - e2e/helpers/booker-seed.ts
  - e2e/availability.spec.ts
  - e2e/cancel.spec.ts
  - e2e/host-dashboard.spec.ts
  - e2e/host-headings.spec.ts
  - e2e/host-inbox-hierarchy.spec.ts
  - e2e/keyboard-composites.spec.ts
  - e2e/one-tree.spec.ts
  - e2e/open-capacity.spec.ts
  - e2e/overflow-320.spec.ts
  - e2e/price-parity.spec.ts
  - e2e/public-listing.spec.ts
  - e2e/search-and-book.spec.ts
  - e2e/skeleton-geometry.spec.ts
autonomous: true
requirements: [LVER-01, HVER-03, ENF-01]

must_haves:
  truths:
    - "D-224: `deriveBookable` gains a FIFTH and SIXTH term as NEW REQUIRED fields on the existing parameter objects — `listing.reviewState` and `host.verificationStatus`. Never optional, never defaulted: a required field is what makes every call site a compile error, and tsc is the census."
    - "D-225: the two new terms are INDEPENDENT of `payoutsEnabled`. They are never expressed in terms of it, and `payoutsEnabled` is never removed."
    - "D-226: the inlined SQL twin in `src/lib/search/query.ts` Stage-1 moves in lockstep, and `tests/search/bookable-gate.test.ts` fixtures grow to cover EVERY new enum value — 5→14 fixtures, 3→8 hosts, 1→3 passing set members — or the equality proves less than it did before."
    - "D-227: `placeHold` and `placeOpenHold` stay deliberate RE-STATEMENTS, each with its own NEW refusal anchor. They are NOT folded into a shared helper — duplicated security code on the money path only stays honest if each copy is measured independently."
    - "D-222: `suspended` fails the host term, so suspension is enforced by the SAME gate read as verification. There is no second suspension check anywhere."
    - "D-228: search needs no separate work — Stage-1 already filters on the gate, so a pending listing drops out the moment the twin lands. The `gate_lr_pending` fixture IS that assertion; it is asserted, not re-implemented."
    - "D-210: Success Criterion 2 is NARROWED to listings created or materially edited after this phase. `grandfathered` PASSES both terms, deliberately."
    - "D-248: the gate change, the 19-file Vitest fixture sweep and the 14 non-Vitest seed sites ship in ONE atomic commit. A red suite spanning 19 files discovered a wave later is indistinguishable from a real regression."
    - "A host with NO `host_verification` row is UNVERIFIED, never verified: SQL uses `COALESCE(hv.status::text,'unverified')`, TypeScript uses `?? \"unverified\"`, and both terms compare against positive literals — never `!== 'suspended'`."
  artifacts:
    - path: src/lib/bookability.ts
      provides: "A six-term pure predicate with a `THE FIFTH AND SIXTH TERMS` banner on the shipped `:12-46` change-log model"
      contains: "verificationStatus"
    - path: src/lib/search/query.ts
      provides: "Stage-1 with LEFT JOIN host_verification and both new terms, COALESCE on the nullable side"
      contains: "host_verification"
    - path: tests/helpers/seed.ts
      provides: "makeVerifiedHost() — the one expression the 19-file sweep converges on"
      exports: ["makeVerifiedHost"]
    - path: tests/search/bookable-gate.test.ts
      provides: "14 fixtures over 8 hosts with 3 passing set members, every enum value appearing exactly once"
  key_links:
    - from: "src/lib/bookability.ts"
      to: "src/lib/search/query.ts Stage-1"
      via: "set-equality over shared fixtures in tests/search/bookable-gate.test.ts"
      pattern: "review_state IN \\('approved','grandfathered'\\)"
    - from: "src/app/actions/booking.ts placeHold"
      to: "host_verification"
      via: "leftJoin in the existing select, own refusal anchor L_pending_review"
      pattern: "leftJoin\\(hostVerification"
    - from: "src/app/actions/booking.ts placeOpenHold"
      to: "host_verification"
      via: "a SEPARATE re-statement with its own anchor L_OPEN_PENDING_REVIEW"
      pattern: "leftJoin\\(hostVerification"
---

<objective>
Make ops approval a term of the sell-gate itself — at all seven sites — and repair the fixture blast
radius in the same commit.

Purpose: This is the ROADMAP's named trap. `deriveBookable` is built so that adding a required field
makes every call site a compile error, but `tsc` sees only four of the seven sites. The other three —
the inlined SQL twin and the two deliberate re-statements — are held by tests, and one of those tests
currently has exactly ONE passing member in its set-equality. Adding two terms with five and six enum
values turns that from a real drift guard into a weak one unless its fixtures grow in a specific way.

Output: six terms in one pure predicate, its SQL twin, two independently-anchored re-statements, two
RSC call sites, a `makeVerifiedHost()` helper, and every fixture and seed in the repository moved with
them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-CONTEXT.md
@.planning/REQUIREMENTS.md

Consult by path:
- `18-RESEARCH.md` § F5, § F5a, § F5b, § F5c, § F9a, § Code Examples, § Pitfall 1, § Pitfall 2, § Pitfall 7, § Pitfall 8
- `18-PATTERNS.md` § 7, § 8, § 9, § 13, § 15a, § 15b, § 15c
- `.planning/ROADMAP.md` § Phase 18 (the ⚠ THE SELL-GATE IS THE TRAP block)

<interfaces>
From 18-02 (already migrated):
  export const hostVerificationStatus = pgEnum("host_verification_status",
    ["unverified","pending","approved","rejected","grandfathered","suspended"]);
  export const listingReviewState = pgEnum("listing_review_state",
    ["pending","approved","rejected","grandfathered","withdrawn"]);
  export type HostVerificationStatus / ListingReviewState  — derived unions, exported from schema.ts
  hostVerification: { userId (PK→user.id), status, provider, vendorRef, result, checkedAt,
                      decidedByStaffId, reason, createdAt, updatedAt }
  listing.reviewState: listingReviewState, default 'pending', notNull

Current predicate — src/lib/bookability.ts:56-66:
  deriveBookable(
    listing: { status: "draft"|"published"|"unlisted"; hasOperatingHours: boolean },
    host:    { emailVerified: boolean; payoutsEnabled: boolean },
  ): boolean

The four tsc-forced call sites:
  1. src/app/listings/[id]/(detail)/page.tsx:286  (query at :249-254 already innerJoins user + leftJoins hostPayout)
  2. src/app/(host)/host/listings/page.tsx:155     (r is a full listing row; payoutsEnabled read once at :67-71 for the whole grid)
  3. src/app/actions/booking.ts:192  placeHold
  4. src/app/actions/booking.ts:448  placeOpenHold

The three sites tsc does NOT see:
  5. src/lib/search/query.ts:211-217  — held ONLY by tests/search/bookable-gate.test.ts
  6. placeHold's refusal          — anchor L_nohours in tests/booking/state-machine.test.ts
  7. placeOpenHold's refusal      — anchor L_OPEN_NOHOURS in tests/booking/open-capacity-hold.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [ATOMIC — D-248, ONE COMMIT]: All seven gate sites plus the whole fixture and seed sweep</name>
  <files>src/lib/bookability.ts, src/lib/search/query.ts, src/app/actions/booking.ts, src/app/listings/[id]/(detail)/page.tsx, src/app/(host)/host/listings/page.tsx, tests/helpers/seed.ts, and every fixture/seed site listed in this plan's files_modified</files>
  <read_first>
    - src/lib/bookability.ts (the WHOLE file — `:12-46` is a written record of how the FOURTH term was added, and this change is that operation performed twice; reproduce the banner, the why-a-parameter-not-a-lookup argument, and the inventory of twins)
    - src/lib/search/query.ts:60-120 (RawRow, toRow) and :190-240 (Stage-1, its two hazard comments: no backticks inside the template literal, and `oh_any` vs `oh` staying distinct)
    - src/app/actions/booking.ts:41-46 (the header on deliberate duplication), :150-205 (placeHold's gate and the `=== true` discipline), :355-365 (placeOpenHold's docblock forbidding extraction), :420-460 (placeOpenHold's gate)
    - src/app/listings/[id]/(detail)/page.tsx:245-300 (the existing joined query and the Promise.all — the new read joins the EXISTING query, it does not add a fifth await)
    - src/app/(host)/host/listings/page.tsx:45-90, :150-165 (the pre-collapsed Set idiom — one grouped query, never per card)
    - tests/helpers/seed.ts (the whole file — this is where makeVerifiedHost lands)
    - tests/booking/open-capacity-hold.test.ts:300-320 (the comment "deriveBookable needs a VERIFIED host with ACTIVATED payouts, or every case below would refuse" — wherever that sentence appears, a host_verification row must now appear too)
    - tests/setup.ts:40-50 (`tsx scripts/*` and every Playwright spec hit DEV, not the test DB — seeds are not tests)
  </read_first>
  <action>
This is ONE task and ONE commit (D-248). Do not split it across commits; do not leave the suite red
between steps.

**(a) `src/lib/bookability.ts`.** Add `reviewState: ListingReviewState` to the listing parameter and
`verificationStatus: HostVerificationStatus` to the host parameter, both REQUIRED. Extend the return
expression with `(listing.reviewState === "approved" || listing.reviewState === "grandfathered")` and
`(host.verificationStatus === "approved" || host.verificationStatus === "grandfathered")`. Positive
literals only — a negative test would let `withdrawn` or a future value through. Write a
`── THE FIFTH AND SIXTH TERMS ──` banner on the shipped `:12-46` model, stating: why they are
parameters and not lookups; that they are INDEPENDENT of `payoutsEnabled` (D-225) and why that flag
could never be the identity gate; that `suspended` failing here is the whole of ENF-01's block-new
lever (D-222); and that `grandfathered` PASSES, deliberately, because D-210 narrows Success Criterion
2 to listings created or materially edited after this phase. Extend the twins inventory at `:35-46` to
name the two NEW anchors.

**(b) Site 5 — `src/lib/search/query.ts` Stage-1.** Add `LEFT JOIN host_verification hv ON hv.user_id
= u.id` beside the existing `LEFT JOIN host_payout hp`. Add `AND l.review_state IN ('approved',
'grandfathered')` and `AND COALESCE(hv.status::text, 'unverified') IN ('approved','grandfathered')`.
Update the "All FOUR terms" comment above the WHERE to say SIX and to enumerate them. Comment the
COALESCE the way the line above it is commented: a host with no verification row is UNVERIFIED, never
verified — the same fail-closed shape as `COALESCE(hp.payouts_enabled, false)`. Note that `suspended`
fails here, which is why suspension needs no second check anywhere. No backticks or dollar-braces in
any comment inside the template literal.

**(c) Sites 3 and 6 — `placeHold`.** Fold `reviewState: listing.reviewState` and `verificationStatus:
hostVerification.status` into the EXISTING select, adding `.leftJoin(hostVerification,
eq(hostVerification.userId, user.id))` — zero extra round trips. Pass
`{ …, reviewState: lr.reviewState }` and `{ …, verificationStatus: lr.verificationStatus ?? "unverified" }`
into `deriveBookable`, mirroring `lr.payoutsEnabled ?? false`. The refusal string and shape are
UNCHANGED (`{ ok:false, reason:"not-bookable", … }`).

**(d) Sites 4 and 7 — `placeOpenHold`.** Do the same again, BY RE-STATEMENT. Do not extract a helper.
Do not import anything from `placeHold`. Extend the existing ⚠ block above the query to name the two
new terms and to say that a wrong alias or a missing `?? "unverified"` here would compile, pass tsc,
pass `placeHold`'s anchor and pass the whole suite while leaving a real drop-in booking hole open.

**(e) Site 1 — `src/app/listings/[id]/(detail)/page.tsx`.** Add `leftJoin(hostVerification, …)` to the
query at `:249-254` and read `row.listing.reviewState` and `row.host_verification?.status ??
"unverified"` at the `deriveBookable` call. Do not add a fifth `Promise.all` element.

**(f) Site 2 — `src/app/(host)/host/listings/page.tsx`.** `r.reviewState` comes free from the existing
`select()`. Read the host's own `host_verification.status` ONCE for the whole grid, beside the existing
`payoutsEnabled` read at `:67-71` — never per card.

**(g) `tests/helpers/seed.ts` — `makeVerifiedHost()`.** One expression the sweep converges on: it
inserts the `user`, the `host_payout` row and a `host_verification` row at `approved` (accepting an
override so a fixture can ask for any other enum value, or for NO row at all). This is a TEST fixture
helper — D-227's no-shared-helper rule governs the SECURITY code in `src/`, not fixtures. Say that at
the site so a later reader does not "consistently" extract the two re-statements.

**(h) The sweep.** Every file in this plan's `files_modified` that hand-builds a bookable host gets a
`host_verification` row and a `review_state`, routed through `makeVerifiedHost()` where the file's
shape allows. Wherever a file already carries a comment of the form *"deriveBookable needs a VERIFIED
host with ACTIVATED payouts, or every case below would refuse"*, extend that sentence to name the new
terms — those comments are the map. The 19 Vitest files are a FLOOR, not a certainty: the real census
is `npm test`. After the sweep, run `npm test` ALONE and repair every remaining red that traces to a
missing verification row or a non-approved `review_state`, adding any file it finds to
`files_modified` in the SUMMARY.

**(i) The 14 non-Vitest seed sites** — `scripts/seed.ts`, `scripts/seed-baseline-fixtures.ts`,
`e2e/helpers/booker-seed.ts` and the twelve e2e specs with inline seeds — get the same treatment.
These are outside the Vitest suite and hit DEV, so a green suite says nothing about them: after the
edit, run `npm run db:seed` and confirm `/` shows listings. A seeded catalogue that silently empties is
this project's Pitfall 7.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 — which is the census: all four TypeScript call sites were forced red by the required fields and all four were answered.
    - `npm test` (run ALONE, nothing else running) is green across all 192 files.
    - `grep -c "verificationStatus" src/app/actions/booking.ts` returns 2 — one per re-statement — and `grep -c "function deriveBookableInternal\|sharedGate\|assertBookable" src/app/actions/booking.ts` returns 0 (no helper was extracted).
    - `grep -c "host_verification" src/lib/search/query.ts` is at least 2 (the LEFT JOIN and the COALESCE term).
    - `grep -v '^\s*[-/*]' src/lib/bookability.ts | grep -c "!== \"suspended\"\|!== 'suspended'"` returns 0 (positive literals only).
    - `grep -c "review_state IN" src/lib/search/query.ts` returns 1 and `grep -c "COALESCE(hv.status" src/lib/search/query.ts` returns 1.
    - `npm run db:seed` then loading `/` in `npm run dev` shows a non-empty catalogue (Pitfall 7 — seeds are not tests).
    - `git log --oneline -1 --stat` shows the gate files and the fixture/seed files in ONE commit (D-248).
  </acceptance_criteria>
  <done>All seven gate sites carry both new terms, the whole repository's fixtures and seeds have moved with them, `tsc` and `npm test` are green, and it is one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Make the instruments strong again — 29 truth-table assertions, 14 parity fixtures, two new refusal anchors</name>
  <files>tests/listing/bookability.test.ts, tests/search/bookable-gate.test.ts, tests/booking/state-machine.test.ts, tests/booking/open-capacity-hold.test.ts</files>
  <read_first>
    - tests/listing/bookability.test.ts (the whole file — the 16-row truth table, the honest caveat at `:46-50` that the meta-assertion is self-consistency and NOT coverage, the auto-revert cases at `:148-166`, and the "exactly one true row" invariant at `:130-140`)
    - tests/search/bookable-gate.test.ts (the whole file — the FIXTURES table at `:150-163`, the set-equality at `:221-238`, the one-reason-per-fixture discipline at `:104-109`, the `gate_deleted` exclusion note at `:145-149`, and the mutation log at `:24-72` recording that this file once passed VACUOUSLY)
    - tests/booking/state-machine.test.ts:36-45, :225-290 (the `L_nohours` anchor: the fixture is identical to a bookable one in every respect but the one under test, and the assertion counts ROWS IN THE DB, not a return value)
    - tests/booking/open-capacity-hold.test.ts:290-330 (the `L_OPEN_NOHOURS` anchor and its own seeding comment)
  </read_first>
  <action>
**(a) `tests/listing/bookability.test.ts` → 29 assertions.** Do NOT expand to a 480-row cross product.
Keep the existing 16 rows unchanged in MEANING by pinning both new terms at a passing value, with a
comment saying those 16 rows measure the original four terms and nothing else. Add a 5-row
listing-review table (every `listing_review_state` value against everything-else-passing → exactly 2
true) and a 6-row host-verification table (every `host_verification_status` value → exactly 2 true).
Add two auto-revert cases on the shipped `:148-166` model: `verificationStatus` `approved → suspended`
flips true→false with nothing else changed (the ENF-01 pure-predicate anchor), and `reviewState`
`approved → pending` does the same (the LVER-03 material-edit anchor). Restate the "exactly one true
row" invariant per new table — it is the file's own anti-vacuity device.

**(b) `tests/search/bookable-gate.test.ts` → 14 fixtures / 8 hosts / 3 passing members.** Extend
`makeHost(id, opts)` to take `verificationStatus` and insert a `host_verification` row — or
deliberately none, for the unverified-by-absence case. Extend `makeListing(...)` to take
`reviewState`. Every `FIXTURES` row gains both fields explicitly (no defaults — the file's discipline
is that a fixture fails or passes for its OWN single reason).

Add four listing-review fixtures, all sharing the fully-passing host and differing ONLY in
`review_state`: `gate_lr_grandfathered` (PASS — the second passing member, and the one D-212 makes
non-obvious), `gate_lr_pending` (fail — this fixture IS D-228's whole verification), `gate_lr_rejected`
(fail), `gate_lr_withdrawn` (fail). Add five host fixtures, EACH WITH ITS OWN HOST so each fails for
its own single reason, all on approved listings with hours: `gate_hv_grandfathered` (PASS — the third
passing member), `gate_hv_unverified` (NO `host_verification` row at all — the fixture that proves the
COALESCE), `gate_hv_pending`, `gate_hv_rejected`, `gate_hv_suspended` (fail — the ENF-02/D-222 anchor
in the search half).

Every one of the 5 listing-review values and all 6 host-verification values must appear exactly once.
Add one sentence to the `gate_deleted` exclusion note explaining why review-state and
verification-status ARE inside the parity set while soft-delete is not.

Record TWO mutations in the file's own mutation log, with their observed reds: SQL-side permissive
(delete one of the two new WHERE terms in `query.ts` → the set-equality fails) and TS-side permissive
(widen one predicate term in `bookability.ts` → the set-equality fails). Without both, the parity
assertion is itself untested.

**(c) The two refusal anchors (D-227).** In `tests/booking/state-machine.test.ts` add `L_pending_review`
— a listing seeded IDENTICALLY to the bookable one in every respect except `review_state = 'pending'` —
and assert `placeHold` refuses AND that `SELECT count(*) FROM booking WHERE listing_id =
'L_pending_review'` is 0. Add a sibling case for a `suspended` host. In
`tests/booking/open-capacity-hold.test.ts` add `L_OPEN_PENDING_REVIEW` with its own separate seeding
and its own assertion — a SEPARATE anchor, not a shared one, because the two re-statements are
independently duplicated security code. Each file's header gains a line naming its new anchor.
  </action>
  <verify>
    <automated>npx vitest run tests/listing/bookability.test.ts tests/search/bookable-gate.test.ts tests/booking/state-machine.test.ts tests/booking/open-capacity-hold.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/listing/bookability.test.ts` reports 29 assertions in the truth-table describes (16 + 5 + 6 + 2).
    - In `tests/search/bookable-gate.test.ts`, `FIXTURES` has 14 entries and the passing subset computed by `deriveBookable` has exactly 3 members; every `listing_review_state` value and every `host_verification_status` value appears exactly once across the table.
    - `grep -c "gate_hv_unverified" tests/search/bookable-gate.test.ts` is at least 1 AND that fixture inserts no `host_verification` row (the COALESCE proof).
    - `grep -c "L_pending_review" tests/booking/state-machine.test.ts` is at least 1 and `grep -c "L_OPEN_PENDING_REVIEW" tests/booking/open-capacity-hold.test.ts` is at least 1; the two identifiers do not appear in each other's file.
    - Both anchor assertions count rows in the database, not the action's return value: `grep -c "count(\*)" tests/booking/state-machine.test.ts` increased.
    - Both parity mutations recorded in the file's mutation log with the observed failure text, and reproduced in the SUMMARY.
  </acceptance_criteria>
  <done>The parity set-equality has three passing members and covers every enum value; the truth table is 29 diagnostic assertions; each re-statement has its own watched anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booker browser → `placeHold` / `placeOpenHold` | The money path. The reserve route group is NOT the gate; both actions re-derive server-side |
| database → search Stage-1 result set | A pending listing must never leave Postgres |
| `deriveBookable`'s parameters → its callers | The only place "is this sellable?" is answered; a second check anywhere else is the same disease in a new place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-0301 | Elevation | `placeHold` minting a hold on an unreviewed listing | mitigate | Server-side re-statement inside the existing joined SELECT, with its own anchor `L_pending_review` asserting zero booking rows |
| T-18-0302 | Elevation | `placeOpenHold` — the same hole in the drop-in path | mitigate | A SEPARATE re-statement with a SEPARATE anchor `L_OPEN_PENDING_REVIEW`; no shared helper, so a wrong alias in one cannot be masked by the other |
| T-18-0303 | Spoofing / Elevation | fail-open on a missing `host_verification` row | mitigate | `COALESCE(hv.status::text,'unverified')` in SQL, `?? "unverified"` in TS, positive-literal comparison in both; the `gate_hv_unverified` fixture (no row at all) proves it |
| T-18-0304 | Information disclosure | unreviewed listing leaking through search | mitigate | The term is inlined in Stage-1 SQL so the row never leaves Postgres; `gate_lr_pending` is the assertion (D-228) |
| T-18-0305 | Tampering | predicate/SQL drift after this change | mitigate | Set-equality over 14 shared fixtures with 3 passing members and two watched mutation REDs; a singleton expected set would have been a much weaker instrument than it looks |
| T-18-0306 | Elevation | a suspended host continuing to sell | mitigate | `suspended` fails the host term at all seven sites — one gate, one read, no second check (D-222). Anchored by `gate_hv_suspended` and the pure-predicate auto-revert case |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed |
</threat_model>

<verification>
1. `npx tsc --noEmit` — green. This IS the four-site census; a green tsc with an unedited call site is impossible because the new fields are required.
2. `npm test` **alone** — green across 192 files. ⚠ `tests/global-setup.ts` TRUNCATEs the shared test DB at run start, so never run this concurrently with or immediately after `npm run test:design` or `npm run build`. A red here is a truncation artefact until it survives a solo re-run.
3. `npm run test:design` **alone** — green (nothing in this plan touches a design gate; a red would be a truncation artefact).
4. `npm run db:seed` then `/` renders a non-empty catalogue in dev.
5. ⚠ The 7 Playwright specs in `e2e/` do NOT run in CI (D-24). This plan edits 12 of them; re-run `npm run test:e2e` BY HAND once and record the result as a one-time audit, not as a gate.
6. Both parity mutations reproduced and their REDs quoted in the SUMMARY.
</verification>

<success_criteria>
- LVER-01: ops approval is a term of `deriveBookable` itself, held equal across the predicate, its inlined SQL twin, and both server-side re-derivations — each of the latter with its own refusal anchor.
- HVER-03: host verification status gates selling, independently of `payoutsEnabled`, which is neither removed nor referenced by the new terms.
- ENF-01 (gate half): `suspended` fails the host term, so the block-new lever is the same read as verification.
- The whole repository's fixtures and seeds moved in the SAME commit; `npm test` and `npm run db:seed` are both green.
</success_criteria>

<output>
Create `.planning/phases/18-host-verification-listing-review-fitout-ops/18-03-SUMMARY.md` when done.

Record: the final six-term predicate verbatim; the actual number of fixture files `npm test` forced
(19 was a floor); the two parity mutation REDs with their observed text; the two new anchor ids; and
an explicit statement that the e2e specs were re-run by hand because CI does not run them.
</output>
