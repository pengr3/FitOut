---
phase: 18-host-verification-listing-review-fitout-ops
plan: 09
type: execute
wave: 6
depends_on: ["18-05", "18-08"]
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0028_ops_notification_types.sql
  - drizzle/meta/_journal.json
  - src/lib/notifications.ts
  - src/components/notifications/notification-item.tsx
  - src/inngest/functions/notify.ts
  - src/lib/email.ts
  - src/app/actions/ops-review.ts
  - src/app/actions/cancel-booking.ts
  - tests/notifications/ops-decision-notify.test.ts
  - tests/design/enum-first-use-tripwire.test.ts
autonomous: true
requirements: [OPS-05]
blocking_input:
  - item: "D-250 — `SUPPORT_EMAIL` is null (`src/lib/site.ts:70`). D-243 says a suspended host is told 'and name the support address'; there is no address to name. Every sentence in this plan is already written to stand WITHOUT one. This is a one-line PM unblock (it also closes the carried-forward STATE-05 / TRUST-01), recorded and raised in the phase summary — NOT a reason to hold this plan."

must_haves:
  truths:
    - "D-245: OPS-05 needs the NOTIFICATION, not just a host-surface signal. A status a host has to go and look for is not being TOLD — and the thing being communicated blocks their income. Host-surface status (D-230, plan 18-13) is IN ADDITION to this, not instead of it."
    - "D-245: reuse the shipped Phase-7 model (D-86/D-91/D-92) — ONE `notification` row written by the SAME Inngest function that sends the email, so the two channels cannot drift."
    - "D-91 sufficiency: the payload is the SOLE input to BOTH channels. A field the email needs belongs in the payload, never in a second parallel structure."
    - "`notification_type` is an EXISTING, COMMITTED enum, so the new values take the 55P04 two-migration split: `drizzle/0028` does NOTHING but `ALTER TYPE … ADD VALUE`, and the first runtime INSERT is a later emit, never a migration — exactly as `drizzle/0018` did for the three group kinds."
    - "Adding a value to `notificationType` without handling it in the dropdown renderer is a COMPILE error by construction (the schema header says so) — that compile error is the census and must be answered, not defaulted away."
    - "D-243: a suspended host IS told, with the reason. But host appeals are backlog 999.6 and OUT — the message must NOT promise an appeal route, a reply, a timeline or a way back. Say what happened, say why, and stop."
    - "D-250: `SUPPORT_EMAIL` is null and stays null. Every sentence must be complete and honest without it. Do not fabricate an address, do not route to a placeholder, and do NOT weaken `tests/design/site-contacts.test.ts`, which asserts ZERO support affordances anywhere under `src/` while the constant is null."
    - "The operator's stored sentence is rendered VERBATIM, ONCE, in the body — never paraphrased, never summarised into a subject line, never split across the two channels."
    - "The ops-cancel path (18-08) gets its correct booker and host copy here; it must still never emit `booking_cancelled_by_host`."
  artifacts:
    - path: drizzle/0028_ops_notification_types.sql
      provides: "ALTER TYPE notification_type ADD VALUE for each new ops kind — and nothing else"
    - path: src/lib/db/schema.ts
      provides: "The new notificationType values plus their NotificationPayload variants"
      contains: "listing_review"
    - path: src/inngest/functions/notify.ts
      provides: "One function writing the durable row AND dispatching the email for the new kinds"
    - path: tests/notifications/ops-decision-notify.test.ts
      provides: "OPS-05 — one notification row per decision, the operator sentence verbatim, and no appeal/timeline/support-address language"
  key_links:
    - from: "src/app/actions/ops-review.ts"
      to: "emitNotify"
      via: "one emit per decision, payload carrying the stored sentence"
      pattern: "emitNotify"
    - from: "src/inngest/functions/notify.ts"
      to: "notification row + email"
      via: "the same function, so the two channels cannot drift (D-91)"
      pattern: "sendForType"
---

<objective>
Tell the host — in the app and in their inbox, from one payload, with the operator's own sentence
intact.

Purpose: OPS-05 says approve and reject carry *"a reason the host is actually told"*. D-245 settles
what "told" means: a status a host has to go and look for is not being told, and the thing being
communicated blocks their income. The mechanism is already shipped and proven — one Inngest function
writes the durable row and sends the email from one payload, which is exactly the drift D-91 exists to
prevent.

Output: an ALTER-TYPE-only migration, the new kinds with their compile-forced payload variants and
renderer branches, the email copy, the emissions from the ops actions and the ops-cancel path, and a
test that pins both the verbatim sentence and the sentences that must NOT appear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-CONTEXT.md
@.planning/REQUIREMENTS.md

Consult by path:
- `18-RESEARCH.md` § F8 (the enum cost and the compile-forced union), § F3 (Probe B and the split idiom), § Pitfall 3, § Pitfall 5
- `18-PATTERNS.md` § Shared Patterns § Telling the host (OPS-05 / D-230)
- `18-UI-SPEC.md` § Telling the host — OPS-05's notification (the five events and their exact copy), § The support address FitOut does not have

<interfaces>
src/lib/db/schema.ts:471-497 — `notificationType`, 14 values today, with the 55P04 note at :488-491
  naming `drizzle/0018` as the precedent.
src/lib/db/schema.ts:497-520 — `NotificationPayload`, a discriminated union keyed on the SAME
  discriminant, with the STORE DISPLAY STRINGS / no-PII / D-91 sufficiency rules stated.
src/app/actions/cancel-booking.ts:1345, :1364 — the two shipped `emitNotify` call sites and their
  payload shape (`side: "booker" | "host"`, `href`, `whenLabel`, `refundLabel`).
src/inngest/functions/notify.ts — the ONE function that writes the row and dispatches the email.
src/components/notifications/notification-item.tsx — the dropdown renderer; a total branch over the
  discriminant, which is what turns a new enum value into a compile error.
src/lib/site.ts:70 — `export const SUPPORT_EMAIL: string | null = null;`
src/components/booking/support-path.tsx — the guard shape any support affordance must use: the whole
  control lives inside a `SUPPORT_EMAIL !== null` conditional, with the literals authored INSIDE it.
From 18-05: `src/app/actions/ops-review.ts` — the five decision actions.
From 18-08: `cancelBookingAsOps` — the path that must NOT emit `booking_cancelled_by_host`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: The ALTER-TYPE-only migration and the compile-forced kinds</name>
  <files>drizzle/0028_ops_notification_types.sql, drizzle/meta/_journal.json, src/lib/db/schema.ts, src/components/notifications/notification-item.tsx, tests/design/enum-first-use-tripwire.test.ts</files>
  <read_first>
    - drizzle/0018_group_notification_types.sql (the whole file — the exact precedent: ADD VALUEs only, first runtime INSERT is a later emit)
    - drizzle/0020_open_capacity_enum.sql (the same discipline stated again, with `IF NOT EXISTS` and unqualified names)
    - src/lib/db/schema.ts:471-560 (`notificationType`, the 55P04 note, and the full `NotificationPayload` union with its three stated rules)
    - src/components/notifications/notification-item.tsx (the whole file — the total branch that makes a new value a compile error)
    - tests/design/enum-first-use-tripwire.test.ts (written in 18-02 — this task extends it to the new literals)
    - 18-UI-SPEC.md § Telling the host (the five events and their copy)
  </read_first>
  <action>
Add five values to `notificationType` in `src/lib/db/schema.ts`:
`listing_review_approved`, `listing_review_rejected`, `host_verification_approved`,
`host_verification_rejected`, `host_suspended`. Extend the enum's existing 55P04 comment block to name
`drizzle/0028` the way it currently names `drizzle/0018`.

Create `drizzle/0028_ops_notification_types.sql` containing ONLY
`ALTER TYPE "notification_type" ADD VALUE IF NOT EXISTS '<value>';` per new kind, unqualified type
name, with the `drizzle/0020`-style header: this file does NOTHING ELSE because an
`ALTER TYPE … ADD VALUE` and the FIRST USE of the new value cannot share a transaction (PG 55P04) and
both migrators wrap all pending migrations in one transaction. Record that the first runtime INSERT is
task 3's emit, long after commit.

Add one `NotificationPayload` variant per new kind. Follow the union's three stated rules exactly:
STORE DISPLAY STRINGS, never ids to join at read time (a rejection the host already read must not
silently re-render if the taxonomy is later re-worded); NO PII — no email addresses, no payment ids;
and D-91 sufficiency — the payload is the SOLE input to both the in-app row and the email, so every
string the email needs lives here. Each variant carries at minimum: the heading, the body, the
operator's stored sentence where one applies, and an `href` to the host surface.

Answer the compile error in `src/components/notifications/notification-item.tsx` with a real branch
per kind. Do NOT add a default case to make it go away — the compile error IS the census the schema
header describes.

Extend `tests/design/enum-first-use-tripwire.test.ts` (from 18-02) so each new single-quoted literal
appears in exactly one migration file — `0028` — and in no migration at or after it. Filter comment
lines before counting; a header naming the tokens would otherwise invalidate its own gate.

⚠ Then run the schema push: **`npm run db:migrate`** (drizzle-kit migrate), NOT `drizzle-kit push`.
Types come from the schema file, not the live database, so `tsc` and `next build` pass WITHOUT the
migration and skipping it produces a false-positive verification state.
  </action>
  <verify>
    <automated>npm run db:migrate && npx tsc --noEmit && npx vitest run --config vitest.design.config.ts tests/design/enum-first-use-tripwire.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:migrate` completes and is idempotent on a second run.
    - `drizzle/0028_ops_notification_types.sql` contains only ALTER TYPE statements: `grep -v '^\s*--' drizzle/0028_ops_notification_types.sql | grep -vc 'ALTER TYPE'` returns 0 for non-blank lines.
    - `npx tsc --noEmit` exits 0 — AND it was RED first, in `notification-item.tsx`, before the branches were written (record that red in the SUMMARY; it is the census).
    - `grep -c "default:" src/components/notifications/notification-item.tsx` is unchanged — no default case was added to silence the union.
    - `npx vitest run --config vitest.design.config.ts tests/design/enum-first-use-tripwire.test.ts` passes with the five new literals covered. (⚠ `--config` is mandatory; the bare form's exit 1 is indistinguishable from a real failure.)
    - `drizzle/meta/_journal.json` lists `0028` last.
  </acceptance_criteria>
  <done>Five new kinds exist in an ALTER-TYPE-only migration that has been applied, each with a payload variant and a real renderer branch, and the tripwire covers them.</done>
</task>

<task type="auto">
  <name>Task 2: The copy — one payload, two channels, and the sentences that must not appear</name>
  <files>src/lib/notifications.ts, src/inngest/functions/notify.ts, src/lib/email.ts</files>
  <read_first>
    - src/inngest/functions/notify.ts (the WHOLE file — how one function writes the durable row and dispatches `sendForType`, and where a new kind plugs in)
    - src/lib/email.ts (the shipped template composition and the escaping discipline)
    - src/lib/notifications.ts (the shared composition helpers, incl. `composeWhenLabel`)
    - src/lib/site.ts:55-80 (the D-26 block above `SUPPORT_EMAIL` and why the constant is the only line that needs changing)
    - src/components/booking/support-path.tsx (the guard shape — the whole control inside a `SUPPORT_EMAIL !== null` conditional, literals authored INSIDE the guard's character range)
    - tests/design/site-contacts.test.ts (the WHOLE file — it asserts ZERO support affordances under `src/` while the constant is null, and INVERTS to demanding one when it is set)
    - tests/auth/email-escaping.test.ts and tests/auth/email-injection.test.ts (the shipped anchors for the email half)
    - 18-UI-SPEC.md § Telling the host (the exact five subject/body pairs)
  </read_first>
  <action>
Wire the five kinds through the existing notify function so ONE invocation writes the durable
`notification` row AND dispatches the email — never two code paths, never a second parallel structure.

Use the copy from `18-UI-SPEC.md § Telling the host` exactly:
- Listing approved — heading "{Listing title} is live"; body: it passed FitOut's check and can now be booked.
- Host approved — "You're approved to host on FitOut"; body: someone at FitOut checked your account; listings can go live once each one is approved.
- Listing rejected — "{Listing title} wasn't approved"; body carries the operator's sentence VERBATIM, once, then states it won't take bookings.
- Host rejected — "We couldn't approve your host account"; same structure.
- Host suspended — "FitOut has paused your hosting"; the operator's sentence verbatim, then: spaces can't be booked and payouts are on hold.

⚠ **D-243 / 999.6.** No sentence may promise an appeal, a reply, a timeline or a route back.
Specifically banned on every one of these surfaces: "contact us to appeal", "we'll be in touch",
"reply to this message", "within N days". Say what happened, say why, and stop.

⚠ **D-250.** `SUPPORT_EMAIL` is null. Every sentence above is already written to be complete and
honest without an address — do NOT add a trailing "email us at…" clause that renders half the time,
do NOT fabricate an address, and do NOT weaken `tests/design/site-contacts.test.ts`. If a support
affordance is ever added here, it must use `support-path.tsx`'s guard shape with the literals authored
INSIDE the `SUPPORT_EMAIL !== null` conditional.

The operator's stored sentence is rendered verbatim, once, in the BODY — never paraphrased, never
summarised into a subject line, never split across the two channels. It is host-authored-adjacent free
text, so it rides the shipped email escaping path; do not interpolate it into any HTML-bearing
position the existing templates do not already escape.
  </action>
  <verify>
    <automated>npx vitest run tests/auth/email-escaping.test.ts tests/auth/email-injection.test.ts && npx vitest run --config vitest.design.config.ts tests/design/site-contacts.test.ts tests/design/email-shell.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/site-contacts.test.ts` passes UNMODIFIED — `git diff --exit-code tests/design/site-contacts.test.ts` succeeds.
    - `git diff --exit-code src/lib/site.ts` succeeds — `SUPPORT_EMAIL` is still null.
    - The five copy blocks contain none of the banned phrases: `grep -iEc "appeal|we'll be in touch|reply to this|within [0-9]+ days" src/lib/notifications.ts src/lib/email.ts` returns 0 for the new copy.
    - `npx vitest run tests/auth/email-escaping.test.ts tests/auth/email-injection.test.ts` passes — the operator sentence rides the shipped escaping path.
    - The durable row and the email are produced by ONE function invocation: `grep -c "emitNotify\|sendForType" src/inngest/functions/notify.ts` shows the new kinds handled in the same dispatch, with no second emitter added.
  </acceptance_criteria>
  <done>Five events, one payload each, both channels from one function, no appeal language, and `SUPPORT_EMAIL` untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Emit from the decisions — and prove the row exists and says the right thing</name>
  <files>src/app/actions/ops-review.ts, src/app/actions/cancel-booking.ts, tests/notifications/ops-decision-notify.test.ts</files>
  <read_first>
    - src/app/actions/ops-review.ts (written in 18-05 — the five actions and their post-flip ordering)
    - src/app/actions/cancel-booking.ts (`cancelBookingAsOps` from 18-08, and its comment naming this plan as where the correct copy lands)
    - src/app/actions/cancel-booking.ts:1214-1220 (the post-flip discipline: nothing below may unwind the flip and nothing may throw past it; each consequence is individually guarded and a failure becomes a `needs_attention` audit row)
    - tests/booking/notify-emission.test.ts (the WHOLE file — the clone target for asserting an emission, and for asserting one did NOT happen)
  </read_first>
  <action>
Add exactly one `emitNotify` call per decision to `src/app/actions/ops-review.ts` — approve host,
reject host, suspend host, approve listing, reject listing — placed AFTER the flip and AFTER the audit
row, inside the post-flip discipline: individually guarded, never throwing past the flip, and any
failure becoming a `needs_attention` audit row on the established operator-alert channel. Nothing
here may unwind a decision that has already committed.

Add the ops-cancel notifications to `cancelBookingAsOps` in `src/app/actions/cancel-booking.ts`,
replacing the comment left there by 18-08. The booker is told their booking was cancelled by FitOut
and what they are getting back; the host is told the booking was cancelled and — explicitly — that
they are charged no cancellation fee (D-235 made visible, so a host does not believe a fee applied).
⚠ Do NOT reintroduce `booking_cancelled_by_host` on this path; the Pitfall-5 assertion in
`tests/payments/ops-cancel.test.ts` must stay green.

Create `tests/notifications/ops-decision-notify.test.ts` (integration, isolated schema):
- One `notification` row per decision, read back OUT OF THE TABLE, with the right recipient and kind.
- The operator's stored sentence appears in the body VERBATIM and exactly ONCE, and does not appear in
  the subject/heading.
- The email dispatch and the durable row come from the same invocation, so a kind cannot be delivered
  on one channel only.
- The banned-language assertion: none of the five bodies contains an appeal, a reply promise, a
  timeline or an address-shaped literal.
- The suspension case: the host is told, with the reason, and no way-out is promised (D-243).
- A crafted operator note containing HTML is stored and rendered as text on both channels.
  </action>
  <verify>
    <automated>npx vitest run tests/notifications/ops-decision-notify.test.ts tests/payments/ops-cancel.test.ts tests/ops/reject-reason.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/notifications/ops-decision-notify.test.ts tests/payments/ops-cancel.test.ts tests/ops/reject-reason.test.ts` passes.
    - The notification row is asserted by SELECTing it back, not by trusting the action's return value.
    - `grep -c "booking_cancelled_by_host" src/app/actions/cancel-booking.ts` is unchanged from after 18-08 — the ops path still emits none.
    - The verbatim assertion is exact-match on the stored sentence, and asserts a count of ONE occurrence in the body.
    - MUTATION PROOF, recorded in the SUMMARY: paraphrase the operator sentence in one body and confirm the verbatim case goes RED; revert.
    - MUTATION PROOF, recorded in the SUMMARY: add "reply to this message if you'd like to appeal" to the suspension body and confirm the banned-language case goes RED; revert.
  </acceptance_criteria>
  <done>Every ops decision produces one durable row and one email from one payload, the operator's sentence survives verbatim, and no surface promises a route that does not exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator free text → durable notification payload → outbound email | The highest-risk field in the phase crosses two channels here |
| notification payload → host's inbox | The payload is the SOLE input to both channels; anything not in it cannot reach the email, and anything in it reaches a third party |
| migration → committed `notification_type` enum | An `ALTER TYPE … ADD VALUE` on a committed type is the 55P04 hazard that passes every test and fails only in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-0901 | Tampering (stored XSS / email injection) | the operator's free-text sentence on two channels | mitigate | Zod-bounded at write (18-05); rendered as React text in-app; the email half rides the shipped `email-escaping` / `email-injection` anchors, which are run and must stay green |
| T-18-0902 | Information disclosure | PII in the notification payload | mitigate | The union's stated rule — display strings only, no email addresses, no payment ids — applied to the new variants and asserted by the existing payload tests |
| T-18-0903 | Tampering | 55P04 defect that passes the whole suite and fails in production | mitigate | `drizzle/0028` does nothing but ADD VALUE; first runtime INSERT is task 3; `tests/design/enum-first-use-tripwire.test.ts` mechanises the grep tripwire over the five new literals |
| T-18-0904 | Repudiation / integrity | a decision delivered on one channel only, the two drifting | mitigate | One Inngest function writes the row and sends the email from one payload (D-91); asserted by a case that fails if either channel is missing |
| T-18-0905 | Information disclosure | inventing a support address that does not exist | mitigate | `SUPPORT_EMAIL` stays null, `tests/design/site-contacts.test.ts` runs UNMODIFIED, and `git diff --exit-code` on both files is an acceptance criterion |
| T-18-0906 | Integrity | promising an appeal route that is backlog 999.6 | mitigate | Banned-phrase assertion across all five bodies, mutation-proved RED |
| T-18-0907 | Integrity | the booker told their host cancelled when FitOut pulled the listing | mitigate | `booking_cancelled_by_host` count on `cancel-booking.ts` is pinned unchanged; the ops path emits its own kinds |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed |
</threat_model>

<verification>
1. `npm run db:migrate` — RUN. The [BLOCKING] gate for this plan's own migration.
2. `npx tsc --noEmit` — green, after having been RED in the renderer (the compile-forced census).
3. `npx vitest run tests/notifications/ops-decision-notify.test.ts tests/payments/ops-cancel.test.ts tests/ops/reject-reason.test.ts tests/auth/email-escaping.test.ts tests/auth/email-injection.test.ts` — green.
4. `npx vitest run --config vitest.design.config.ts tests/design/site-contacts.test.ts tests/design/email-shell.test.ts tests/design/enum-first-use-tripwire.test.ts` — green, with `site-contacts.test.ts` unmodified.
5. `npm test` **alone**, then `npm run test:design` **alone**.
6. Both mutation REDs recorded.
7. Raise the D-250 `blocking_input` in the SUMMARY as a one-line PM unblock that also closes the carried-forward `STATE-05` / `TRUST-01`. It does not hold the plan.
</verification>

<success_criteria>
- OPS-05: every ops decision reaches the host on both channels from one payload, carrying the operator's sentence verbatim.
- D-243: a suspended host is told, with the reason, and nothing promises an appeal, a reply or a timeline.
- D-250: `SUPPORT_EMAIL` is still null, `site-contacts.test.ts` is unmodified and green, and every sentence stands without an address.
- The new enum values were added in an ALTER-TYPE-only migration, applied, with their first runtime write in the same phase but a different transaction.
</success_criteria>

<output>
Create `.planning/phases/18-host-verification-listing-review-fitout-ops/18-09-SUMMARY.md` when done.

Record: the compile red in `notification-item.tsx` before the branches were written; the five copy
blocks as shipped; both mutation REDs; and the D-250 blocking-input note, framed as one line the PM
can settle that also closes STATE-05 and TRUST-01.
</output>
