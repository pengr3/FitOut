---
phase: 18-host-verification-listing-review-fitout-ops
plan: 14
type: execute
wave: 8
depends_on: ["18-08", "18-09", "18-11", "18-12", "18-13"]
files_modified:
  - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
  - .planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md
autonomous: false
requirements: [OPS-02, HVER-04]
human_needed:
  - item: "D-236 — the ops-cancel refund conflict. `OPS_CANCEL_REFUNDS_SERVICE_FEE = false` ships the PM's answer, but the host-cancel path already refunds 100% INCLUDING the fee on the stated principle that 'the booker did nothing wrong'. An ops-forced cancellation on a confirmed-fake listing is a strictly stronger instance of that. The PM answered without this precedent in view. One line flips it. This LEADS the phase summary."
  - item: "D-250 — `SUPPORT_EMAIL` is null. One line at `src/lib/site.ts:70` closes it AND the carried-forward `STATE-05` / `TRUST-01`. Every surface in this phase is written to stand without it."
  - item: "F11 gap — a suspended host's `/host/earnings` shows a due session that never produces a payout row, with no explanation. Whether the host is told is a product decision."
  - item: "A4 — D-231 'photos' was read as the photo SET, with `reorderPhotos` excluded as non-material. Cheap to overturn."
  - item: "A3 — FitOut is assumed NOT to be a BSP-supervised institution, so BSP Circular 1170 does not bind it directly. One line of counsel review; it materially changes which KYC vendor tier is appropriate."

must_haves:
  truths:
    - "OPS-02 status line: NO automated instrument in this repository can read an HTTP status line — `tests/design/soft-404-status.test.ts:31-39` says so in its own words, and D-24 keeps Playwright out of CI. The proof is a one-time production-build `curl` transcript recorded as evidence, in the 17.1-01 § P1 shape."
    - "The reading must be taken under a PRODUCTION BUILD (`npm run build` then `next start`). A dev-server reading does not settle it: the Suspense boundary `loading.tsx` creates commits a 200 before a page-level `notFound()` runs, and dev and prod differ on exactly this."
    - "Four readings, not one: staff → 200, non-staff → 404, signed-out → 404, and a nonexistent `/ops/xyz` → 404. The last is the control — without it the other three prove nothing about indistinguishability."
    - "D-238: `18-KYC-VENDOR-COMPARISON.md` is a phase DELIVERABLE, written for the PM as a FORK — not a feature matrix. The tonal model is `src/lib/payments/refund-rail.ts:1-33`: a verdict settled by OBSERVED behaviour with the raw responses quoted."
    - "D-206: the doc must state that Success Criterion 7's literal 'runs through a third-party vendor' is DEFERRED, and that the storage contract it exists to protect — no government ID at FitOut — is satisfied in full by this phase."
    - "The doc must LEAD with the structural objection to PayMongo Linked Accounts: it re-couples identity to `payoutsEnabled`, which D-225 exists to decouple. PayMongo's activation IS the payouts gate, so choosing it collapses the fifth and sixth gate terms back into the third — and back to a gate that in production never turns true on its own merits. That is the argument, not the pricing."
    - "The doc must state its own gaps out loud: NO sandbox was walked; PayMongo's Platforms doc page 404s; PH-specific pricing is unpublished by every vendor found; and a FIFTH option — staying manual — was not costed and may dominate both sides of the fork at launch volumes."
    - "Every figure carries its retrieval date. Vendor pricing pages change without notice and 18-RESEARCH marks its vendor section valid only until 2026-09-15."
    - "Given the HVER-01 port, adopting any standalone vendor is one provider module + one env credential + one webhook route + a new `provider` value in existing rows. NO schema change, NO gate change. Quantify that as roughly one plan — it is the port's return on investment."
  artifacts:
    - path: .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
      provides: "§ P1 — the four OPS-02 status-line readings under a production build, with commands, output and date"
      contains: "http_code"
    - path: .planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md
      provides: "HVER-04 — the PM's fork: PayMongo Linked Accounts vs a standalone PH KYC vendor, verdict-first with evidence"
  key_links:
    - from: "18-EVIDENCE.md § P1"
      to: "tests/design/ops-guard-coverage.test.ts"
      via: "the structural test pins that the guard is CALLED; the transcript records what it RETURNS"
      pattern: "ops-guard-coverage"
    - from: "18-KYC-VENDOR-COMPARISON.md"
      to: "src/lib/verification/port.ts"
      via: "the switching-cost section, quantified against the shipped port"
      pattern: "port"
---

<objective>
Close the two things the phase owes that no test can produce: a real HTTP status line, and a decision
document for the PM.

Purpose: OPS-02's last clause — *"a non-staff caller cannot distinguish an ops route from a route that
does not exist"* — is a claim about four numbers, and this repository has no instrument that can read
one. HVER-04 is a written fork the PM decides the KYC vendor from; D-206 deliberately deferred that
decision and this is the document it was deferred to.

Output: `18-EVIDENCE.md § P1` with four readings, `18-KYC-VENDOR-COMPARISON.md`, and a checkpoint at
which the PM reads both and settles the five carried questions — D-236 first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-CONTEXT.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-PM-DECISIONS.md
@.planning/REQUIREMENTS.md

Consult by path:
- `18-RESEARCH.md` § HVER-04 Vendor Evidence (both sides of the fork, the regulatory facts, the switching cost, and the four honest gaps), § F2b, § Assumptions Log, § Open Questions
- `.planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md` § P1 — the transcript SHAPE this plan reproduces
- `src/lib/payments/refund-rail.ts:1-33` — the tonal model for the deliverable: a verdict settled by observed API behaviour, with the raw responses quoted
- `.planning/ops/NEEDS-ATTENTION-RUNBOOK.md` — the other shipped operator-facing document, for register and length

<interfaces>
tests/design/soft-404-status.test.ts:31-39 — "The e2e spec is the ONLY instrument in this repo that can
  see an HTTP status line." D-24 then keeps e2e out of CI. Hence a manual transcript.
From 18-12: `/ops` exists, with `assertStaff()` in the layout, `requireStaff()` in the page, a
  `loading.tsx`, and NO `(ops)` not-found.tsx.
From 18-01: `npm run ops:grant -- --by <handle> <email>` grants staff.
From 18-05: `src/lib/verification/port.ts` — the contract the switching-cost section is measured against.
Probed 2026-07-23, recorded at src/lib/payments/refund-rail.ts:22-30 — the PayMongo negative evidence:
  GET /v2/wallets?status=activated → HTTP 200 with ZERO wallets;
  GET /v2/transfers/receiving_institutions?provider=instapay → HTTP 404.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The OPS-02 status-line audit — four readings under a production build</name>
  <files>.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/17.1-close-phase-17-escalations-sticky-bar-clearance-soft-404-pro/17.1-EVIDENCE.md (§ P1 — the exact transcript shape: command, raw output, date, and the sentence stating what the reading does and does not settle)
    - tests/design/soft-404-status.test.ts:1-60 (why this cannot be a Vitest assertion, and what the structural test does prove instead)
    - src/app/(ops)/ops/layout.tsx and page.tsx (18-12 — the guards being measured)
    - 18-VALIDATION.md § Manual-Only Verifications (the exact four readings owed)
  </read_first>
  <action>
Build and serve a PRODUCTION build, then take four `curl` readings and record the raw transcript.

Sequence, and the serialisation matters: run `npm run build` and let it FULLY EXIT before anything
else touches the database — `tests/global-setup.ts` TRUNCATEs the shared test DB at every run start
and `npm run build` runs the design suite. Then `node ./node_modules/next/dist/bin/next start -p 3100`.

Seed or grant the accounts you need first: one `role='staff'` user (via `npm run ops:grant`), one
ordinary `role='user'` account, and no session for the third reading.

The four readings, each with `curl -o /dev/null -w '%{http_code}'`:
1. `/ops` with a STAFF session cookie → expect **200**
2. `/ops` with a NON-STAFF session cookie → expect **404**
3. `/ops` with NO session → expect **404**
4. `/ops/xyz` (a route that does not exist) → expect **404** — **this is the control.** Without it the
   other three prove nothing about indistinguishability.

Also capture, for readings 2–4, that the response BODY is the root not-found body and carries no ops
chrome — a prober must not see an "Ops" header on a page claiming not to exist.

Create `.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md` with a `§ P1`
section in the 17.1-01 shape: the exact commands, the raw output, the date, the Next version, and a
closing sentence stating precisely what the reading settles (the status line and the body) and what it
does not (it is a one-time audit, not an ongoing gate — the ongoing structural pin is
`tests/design/ops-guard-coverage.test.ts`).

⚠ If reading 2 or 3 returns **200**, that is a REAL FINDING, not a test to adjust: the layout assert is
not winning the status line. Record the reading verbatim, stop, and report it — do not tune the
transcript to the expected answer.
  </action>
  <verify>
    <automated>grep -c "http_code" .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `18-EVIDENCE.md § P1` contains FOUR readings, each with its command, its raw output and the account state it was taken under.
    - Readings 2, 3 and 4 are all the same number, and reading 1 differs — that equality IS the OPS-02 claim.
    - The transcript states the Next version and the date, and that it was taken under `next start`, not `next dev`.
    - The body check for readings 2–4 is recorded: the root not-found body, no ops chrome, no "Ops" string.
    - The closing sentence names `tests/design/ops-guard-coverage.test.ts` as the ongoing pin and this transcript as a one-time audit (Pitfall 9 — a Playwright/manual assertion is not a gate).
    - `git diff --exit-code src/` succeeds — the audit changed no source.
  </acceptance_criteria>
  <done>Four production-build status codes are on the record, with the nonexistent-route control, and the transcript says what it does and does not settle.</done>
</task>

<task type="auto">
  <name>Task 2: 18-KYC-VENDOR-COMPARISON.md — a fork, not a matrix</name>
  <files>.planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md</files>
  <read_first>
    - 18-RESEARCH.md § HVER-04 Vendor Evidence (the WHOLE section — both sides, the four candidate vendors with their confidence labels, the regulatory and residency facts, the switching-cost paragraph, and the four honest gaps)
    - 18-RESEARCH.md § Assumptions Log (A1, A2, A3 — each is a line in this document, not a footnote)
    - src/lib/payments/refund-rail.ts:1-33 (the tonal model: a verdict settled by OBSERVED behaviour with the raw responses quoted; note it is 54 lines total — length is not the deliverable)
    - src/lib/verification/port.ts (18-05 — the contract the switching cost is measured against)
    - .planning/ops/NEEDS-ATTENTION-RUNBOOK.md (register and length for an operator-facing document in this project)
  </read_first>
  <action>
Write `18-KYC-VENDOR-COMPARISON.md` for the PM, as a FORK to be decided — verdict first, evidence
under it, with every claim labelled by HOW IT WAS OBTAINED and WHEN.

**Lead with the structural argument, not the pricing.** PayMongo Linked Accounts re-couples identity
to `payoutsEnabled`, which D-225 exists to decouple: PayMongo's activation IS the payouts gate, so
choosing it collapses the fifth and sixth gate terms back into the third — and back to a gate that in
production never turns true on its own merits, because Platforms is sales-gated. That is the decisive
consideration; the marginal cost being ≈₱0 does not outweigh it.

Cover the six dimensions D-238 names, for both sides: cost, sandbox reachability, KYC depth, data
residency, what FitOut would store, and what switching costs given the HVER-01 port.

**Side A — PayMongo Linked Accounts.** Quote this project's OWN probe transcript verbatim (2026-07-23,
recorded at `refund-rail.ts:22-30`): `GET /v2/wallets?status=activated` → HTTP 200 with ZERO wallets;
`GET /v2/transfers/receiving_institutions?provider=instapay` → HTTP 404. Record that the developer doc
page for Platforms itself now 404s, so its current self-serve status is unknown and the last
first-hand evidence is negative. Note that its onboarding is hosted-redirect, so the child account's
representative supplies a government ID and selfie to PayMongo and not to FitOut — which IS the
storage contract D-206 wants.

**Side B — a standalone PH KYC vendor.** The four candidates spanning the reachability spectrum, each
with its confidence label and retrieval date: Didit (self-serve, free tier, published PAYG), Sumsub
(self-serve trial, published per-verification plus a monthly minimum), Innov8tif / EMAS eKYC (no
published self-serve; the strongest PH document depth found, with per-document API references
including PhilSys), Verihubs and Persona (both LOW confidence — one blog copy, one 403 to automated
fetch with third-party figures only).

**The regulatory section, and it inverts an intuition.** PhilSys is the anchor and BSP Circular 1170
governs eKYC for BSP-supervised institutions — cite the circular directly rather than the
vendor-authored explainers. ⚠ **FitOut is not a BSP-supervised institution**; its obligation is the
Data Privacy Act of 2012 (RA 10173), not BSP CDD. Do not import a bank's compliance burden onto a
marketplace — and flag this for one line of counsel review, because if it is wrong the bar is
materially higher. There is **no data-residency mandate**: the DPA does not restrict cross-border
transfer given consent and contractual safeguards, and the NPC published model clauses in Advisory
No. 2024-01 (30 May 2024). A US/EU-hosted vendor is therefore not disqualified, which is the single
most decision-relevant fact in the section.

**The switching cost — the port's return on investment.** Given HVER-01, adopting any Side-B vendor
is: one new provider module implementing the port, one env credential, one webhook route if the vendor
is asynchronous, and `provider` starting to carry a new value in existing rows. NO schema change —
`{ result, vendorRef, checkedAt, provider }` already anticipates a vendor reference. NO gate change —
the sell-gate reads `status`, which the port sets. Quantify it as roughly one plan.

**The fifth option, presented as a real one: stay manual.** D-206 shipped an ops-manual provider that
satisfies the storage contract in full. At launch supply volumes, "no vendor" may dominate both sides
of the fork on cost and on time-to-value. Do not present it as the status quo the fork is trying to
leave.

**State the gaps out loud**, in their own section: no sandbox was walked, so every figure is published
rather than measured — which directly contradicts the tonal model and must be said; PayMongo's
Platforms doc page 404s; PH-specific pricing is unpublished by every vendor found; and Success
Criterion 7's literal "third-party vendor" is DEFERRED while the storage contract it protects is
satisfied in full.

Do not pad. `refund-rail.ts` settles its verdict in 54 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md && grep -c "2026-07-23" .planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and opens with a VERDICT and a recommendation, not a table of contents.
    - All six D-238 dimensions appear for BOTH sides.
    - The PayMongo probe transcript is quoted verbatim with its 2026-07-23 date, including both the HTTP 200-with-zero-wallets and the HTTP 404.
    - The D-225 re-coupling objection appears in the first screen of the document, before any price.
    - Every vendor figure carries a retrieval date and a confidence label.
    - The BSP-Circular-1170-does-not-bind-FitOut assumption is flagged for counsel review, explicitly labelled as an assumption.
    - The switching cost is quantified as "roughly one plan" and states NO schema change and NO gate change.
    - The "stay manual" fifth option is presented as a real option.
    - A gaps section states that no sandbox was walked.
  </acceptance_criteria>
  <done>The PM has a fork they can decide from, with the structural argument first, every figure dated and labelled, and the document's own blind spots stated in it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PM review — the deliverable, and the five carried decisions</name>
  <files>.planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md, .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-KYC-VENDOR-COMPARISON.md (task 2)
    - .planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md (task 1)
    - 18-PM-DECISIONS.md § ⚠ CONFLICT FOUND AFTER THE PM ANSWERED PM-4
  </read_first>
  <what-built>
HVER-04's comparison document and OPS-02's four production-build status-line readings. Everything else
in Phase 18 is shipped and measured; these two are the deliverables no test can produce.
  </what-built>
  <action>
Present the two documents and the five carried decisions to the PM, D-236 FIRST. Do not attempt to
settle any of them; record the answers.
  </action>
  <how-to-verify>
1. Read `18-EVIDENCE.md § P1`. Confirm readings 2, 3 and 4 are the same number and reading 1 differs.
   That equality is the whole of OPS-02's last clause.
2. Read `18-KYC-VENDOR-COMPARISON.md`. It should read as a fork you can decide, not a matrix. Check
   the first screen carries the D-225 re-coupling argument before any price.
3. Then settle, or explicitly defer, these five — **D-236 first, because it is about money already
   being handled differently for two nearly identical cases**:

   **(a) D-236 — the ops-cancel refund.** `OPS_CANCEL_REFUNDS_SERVICE_FEE = false` ships your answer:
   the booker gets the space price back and FitOut retains the service fee. But `cancel-booking.ts`
   already refunds 100% INCLUDING that fee when a HOST cancels, on its own stated principle: *"This is
   the ONE case where the non-refundable fee IS returned: the booker did nothing wrong, so the
   platform, not the booker, absorbs the gateway cost of the reversal."* An ops-forced cancellation on
   a listing FitOut has confirmed is FAKE is a strictly stronger instance of "the booker did nothing
   wrong". As shipped, FitOut is less generous to a defrauded booker than to one whose host merely
   flaked. The question was put to you without this precedent in view — that omission was in the
   framing, not in your answer. Flipping it is one line.

   **(b) D-250 — the support address.** `SUPPORT_EMAIL` is null. Every rejection and suspension surface
   in this phase is written to stand without one, deliberately. One line at `src/lib/site.ts:70` closes
   this and the carried-forward `STATE-05` and `TRUST-01` at the same time. Is there a monitored inbox?

   **(c) A suspended host's earnings page.** Their due session simply never produces a payout row, with
   no explanation anywhere. This project's own rule is that a signal names the state, the reason and
   the way out. Should a suspended host be told their payouts are on hold on `/host/earnings`?

   **(d) D-231 "photos".** Read as the photo SET: adding or removing a photo trips re-review;
   REORDERING does not, because reordering does not change what the space is. Confirm or overturn.

   **(e) The KYC vendor.** From the comparison doc: PayMongo Linked Accounts, a standalone vendor, or
   stay manual. Staying manual is a real option at launch volumes, not a deferral.
  </how-to-verify>
  <verify>
    <automated>MISSING — a PM decision has no automated instrument. The answers are recorded in 18-14-SUMMARY.md and in .planning/STATE.md § Decisions.</automated>
    <human-check>The PM has read both documents and has answered, or explicitly deferred, all five items.</human-check>
  </verify>
  <acceptance_criteria>
    - Every one of the five items has a recorded answer or an explicit, dated deferral in `18-14-SUMMARY.md`.
    - If D-236 is flipped, the change is exactly one line in `src/lib/payments/fees.ts` and `tests/payments/ops-cancel.test.ts`'s constant-flip case already covers the other branch — no new test is needed, which is the point of having written it.
    - If a `SUPPORT_EMAIL` is supplied, note that `tests/design/site-contacts.test.ts` INVERTS and begins demanding support affordances — that is a follow-on task, not a same-commit edit.
    - The KYC answer is recorded as a D-number in `.planning/STATE.md` so a future phase inherits it.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the five answers, or describe what needs changing in either document.</resume-signal>
  <done>The PM has the evidence and the fork, and all five carried decisions are recorded rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous prober → `/ops` under a production build | The boundary this plan MEASURES rather than implements |
| published vendor claims → a PM's decision | Unlabelled figures are how a stale price becomes a signed contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-1401 | Information disclosure | route-existence oracle surviving every structural test | mitigate | Four production-build `curl` readings including a nonexistent-route CONTROL; a 200 on reading 2 or 3 is recorded as a real finding, never tuned away |
| T-18-1402 | Repudiation | a one-time audit later mistaken for an ongoing gate | mitigate | The transcript states in its own closing sentence that it is a one-time audit and names the structural test that IS ongoing (Pitfall 9) |
| T-18-1403 | Information disclosure | a decision document presenting unlabelled or stale vendor figures as fact | mitigate | Every figure carries its retrieval date and a confidence label; a gaps section states that no sandbox was walked |
| T-18-1404 | Elevation (compliance) | importing a bank's KYC burden onto a marketplace, or missing one that binds | accept + escalate | A3 is labelled an ASSUMPTION and flagged for one line of counsel review; the phase does not act on it either way |
| T-18-1405 | Tampering | a money-policy conflict quietly settled by a planner instead of the PM | mitigate | D-236 is presented as a blocking checkpoint item and leads the summary; no plan in this phase resolves it |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed. This plan writes two markdown files and runs `curl` |
</threat_model>

<verification>
1. `npm run build` completes, `next start -p 3100` serves, and the four `curl` readings are recorded verbatim in `18-EVIDENCE.md § P1`. ⚠ Let `npm run build` FULLY EXIT before anything else touches the test database.
2. Readings 2, 3 and 4 are identical; reading 1 differs. If not, that is a finding — report it and stop.
3. `18-KYC-VENDOR-COMPARISON.md` exists and satisfies every acceptance criterion in task 2.
4. The checkpoint has been presented and all five items have recorded answers or dated deferrals.
5. `git diff --exit-code src/` succeeds — this plan changes no source code.
6. Phase-close gates, run ONE AT A TIME with each fully exiting first: `npm run build` → `npm test` → `npm run test:design`.
</verification>

<success_criteria>
- OPS-02 is complete: the structural pin from 18-12 plus a production-build transcript showing a non-staff caller cannot distinguish `/ops` from a route that does not exist.
- HVER-04 is delivered: a fork the PM can decide from, verdict-first, every figure dated and labelled, its own blind spots stated in it.
- Five carried decisions — D-236 first — are in front of the PM with the full picture, and recorded.
</success_criteria>

<output>
Create `.planning/phases/18-host-verification-listing-review-fitout-ops/18-14-SUMMARY.md` when done.

**Lead the summary with D-236**, quoting `cancel-booking.ts:1134-1137` verbatim and stating that
flipping `OPS_CANCEL_REFUNDS_SERVICE_FEE` is one line with its opposite branch already tested. Then the
four status-line readings, the vendor recommendation, and the remaining four carried decisions with
their recorded answers or dated deferrals.
</output>
