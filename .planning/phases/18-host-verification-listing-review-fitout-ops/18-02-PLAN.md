---
phase: 18-host-verification-listing-review-fitout-ops
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0026_host_verification_listing_review.sql
  - drizzle/0027_cancelled_by_ops.sql
  - drizzle/meta/_journal.json
  - tests/ops/verification-schema.test.ts
  - tests/ops/grandfather-backfill.test.ts
  - tests/design/enum-first-use-tripwire.test.ts
autonomous: true
requirements: [HVER-02, LVER-04]

must_haves:
  truths:
    - "D-207: every listing already `published` at migration time, and its host, is marked `grandfathered` by the migration and is never retroactively re-reviewed."
    - "D-211: `grandfathered` is a FIRST-CLASS, DISTINCT enum value, never written as if a human approved it — one statement (`UPDATE … SET review_state='pending' WHERE review_state='grandfathered'`) burns the backlog down later."
    - "D-220: `host_verification` is 1:1 to `user` with columns `status, provider, vendor_ref, result, checked_at, decided_by_staff_id, reason, created_at, updated_at` and NO column for a document, an ID number or an image — enforced by an exact-column-set test against `information_schema`, not by convention."
    - "D-221: `listing.review_state` (denormalised current state) plus a `listing_review` history table."
    - "D-223: every new `pgEnum` is declared immediately before the table it backs (const TDZ), matching the `payoutLedgerState` / `ledgerKind` idiom."
    - "D-240: the backfill takes the drizzle/0014 shape — `DEFAULT 'pending' NOT NULL` plus a scoped, idempotent `UPDATE … WHERE status='published' AND deleted_at IS NULL`. `draft` and `unlisted` rows become `pending`. A host is grandfathered iff they own at least one row that UPDATE touched. Both statements are re-runnable and can never move an `approved`/`rejected` row."
    - "D-244: `'ops'` is added to the `cancelled_by` pgEnum in its OWN migration file that does NOTHING ELSE — PG 55P04 forbids using a value added to an already-committed type in the same transaction, and both migrators wrap all pending migrations in one transaction. Nothing writes `'ops'` at migration time; the first write is at runtime in plan 18-08."
    - "A grandfathered `host_verification` row carries `checked_at` NULL and `provider = 'migration'` — writing a timestamp for a check that never happened would fabricate an audit record."
    - "The D-240 backfill is a NO-OP in all 192 test files because `setupTestDb()` replays into an EMPTY schema; its test must read the UPDATE statement from the migration file on disk."
  artifacts:
    - path: src/lib/db/schema.ts
      provides: "hostVerificationStatus + listingReviewState pgEnums, hostVerification + listingReview tables, listing.reviewState column, the review-queue partial indexes, and 'ops' on cancelledBy"
      contains: "host_verification_status"
    - path: drizzle/0026_host_verification_listing_review.sql
      provides: "Both new enums, both new tables, the listing column, and the two grandfather statements — one file, one transaction (safe: brand-new types)"
    - path: drizzle/0027_cancelled_by_ops.sql
      provides: "ALTER TYPE cancelled_by ADD VALUE IF NOT EXISTS 'ops' — and nothing else"
    - path: tests/ops/verification-schema.test.ts
      provides: "HVER-02 — the exact column-set allow-list against information_schema"
    - path: tests/ops/grandfather-backfill.test.ts
      provides: "LVER-04 — the backfill measured by executing the statement read off disk, twice"
  key_links:
    - from: "drizzle/0026_*.sql"
      to: "listing.review_state"
      via: "ADD COLUMN … DEFAULT 'pending' NOT NULL, then scoped UPDATE to 'grandfathered'"
      pattern: "review_state"
    - from: "drizzle/0026_*.sql"
      to: "host_verification"
      via: "INSERT … SELECT DISTINCT l.host_id … ON CONFLICT (user_id) DO NOTHING"
      pattern: "ON CONFLICT"
---

<objective>
Land the data model and the migration that grandfathers the live catalogue — the one runtime system
that still holds old state after every file in the repo is updated.

Purpose: The sell-gate (18-03) reads two columns that do not exist yet. The console (18-12) reads two
tables that do not exist yet. And there is a deploy window in which a column defaulting to `'pending'`
without its backfill would make **the entire live catalogue unsellable** — which is why the column and
the backfill are in the SAME migration.

Output: two enums, two tables, one column, two partial indexes, two migration files, one applied
migration, and the two tests that measure what `tsc` structurally cannot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-CONTEXT.md
@.planning/REQUIREMENTS.md

Consult by path:
- `18-RESEARCH.md` § F3 (the three PG 18.4 probes and the migrator-source reading), § F4b (the column-set idiom), § Runtime State Inventory, § Pitfall 3, § Pitfall 10
- `18-PATTERNS.md` § 5 (four migration precedents), § 6 (the schema idioms), § 15d
- `18-VALIDATION.md` § Schema-push gate

<interfaces>
src/lib/db/schema.ts:280-297 — `hostPayout`: the 1:1-to-user, PK-is-the-FK table shape
  `host_verification` must clone, including the header that states WHO may write the columns.

src/lib/db/schema.ts:401-417 — `payoutLedgerState` / `ledgerKind`: the pgEnum-before-table (const TDZ)
  idiom, with the reason stated in the comment.

src/lib/db/schema.ts:379-401 — `audit`: the history-table shape with the `(t) => [ … ]` second
  argument and the PARTIAL index that IS the operator query.

src/lib/db/schema.ts:428-466 — `hostPayoutLedger`: `onDelete: "restrict"` on financial/audit rows
  ("a financial record must never cascade-delete").

src/lib/db/schema.ts:698 — `cancelledBy` pgEnum, today `booker | host | system`.

src/lib/db/schema.ts:11-13 — timestamps are `timestamp(..., { withTimezone: true })`; the header
  records this is a manual post-generation tweak that is kept on regen.

drizzle/0021:6-8 — unqualified table/type names throughout, so `tests/helpers/db.ts` replays the file
  idempotently into every isolated schema via the schema-first search_path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The schema — two enums, two tables, one column, two indexes</name>
  <files>src/lib/db/schema.ts</files>
  <read_first>
    - src/lib/db/schema.ts:1-60 (the file header, the Better-Auth-owned region rule, and `user.role`)
    - src/lib/db/schema.ts:200-300 (the `listing` table, its index list, and `hostPayout`)
    - src/lib/db/schema.ts:379-470 (`audit`, `payoutLedgerState`, `ledgerKind`, `hostPayoutLedger`)
    - src/lib/db/schema.ts:690-710 (`cancelledBy` and its neighbours)
    - src/lib/ops/alerts.ts:12-24 (the rule that a query's ORDER BY must byte-match the index's declared ordering, or Postgres adds a Sort node on top of the scan)
  </read_first>
  <action>
Edit `src/lib/db/schema.ts` only. Do not touch the Better-Auth-owned region (`user`, `session`,
`account`, `verification`) — the header says it is regenerated by `npx @better-auth/cli generate`.

Declare `hostVerificationStatus = pgEnum("host_verification_status", [...])` with exactly
`unverified, pending, approved, rejected, grandfathered, suspended`, immediately above the
`hostVerification` table (D-223). Its comment states that `suspended` rides this same enum so that
suspension is enforced by the SAME gate read as verification (D-222) — one gate, one read, no second
check a code path can forget.

Declare `listingReviewState = pgEnum("listing_review_state", [...])` with exactly
`pending, approved, rejected, grandfathered, withdrawn`, immediately above its first use.

Add `hostVerification = pgTable("host_verification", {...})` on the `hostPayout` shape: `userId` text
PRIMARY KEY referencing `user.id` with `onDelete: "cascade"`; `status` using the enum, default
`'unverified'`, notNull; `provider` text notNull; `vendorRef` text NULLABLE; `result` text NULLABLE;
`checkedAt` timestamptz NULLABLE; `decidedByStaffId` text NULLABLE with NO `.references()` (the
`audit.actorId` precedent — a decision record must not cascade or block a user delete); `reason` text
NULLABLE; `createdAt`/`updatedAt` timestamptz with the `$onUpdate` idiom. Its header must state the
HVER-02 contract explicitly: **no column exists, or may ever exist, for a document, an ID number or an
image**, and that the contract is enforced structurally by an exact-column-set assertion against the
migrated table, not by this comment.

Add `listingReview = pgTable("listing_review", {...})` as history rows: `id` text primary key
(randomUUID idiom); `listingId` text referencing `listing.id` with `onDelete: "restrict"`;
`state` using `listingReviewState`, notNull; `reason` text NULLABLE; `decidedByStaffId` text NULLABLE,
no `.references()`; `submittedAt` timestamptz notNull defaultNow; `decidedAt` timestamptz NULLABLE;
`createdAt` timestamptz notNull defaultNow. `submittedAt` is load-bearing for D-249: a resubmission
enters the queue at ITS submission time, never the listing's original one — say so at the column.

Add `reviewState: listingReviewState("review_state").default("pending").notNull()` to the `listing`
table, and add `index("listing_review_state_idx").on(t.reviewState)` to its index array.

Add the two review-queue partial indexes that ARE the OPS-04 query, each ordered ASC to byte-match
the console's `ORDER BY … ASC`: one on `listing` over `created_at ASC` where `review_state = 'pending'`,
one on `host_verification` over `created_at ASC` where `status = 'pending'`.

Add `'ops'` to the `cancelledBy` pgEnum value list. Write at the site that the value is added here and
in `drizzle/0027` but is NOT written by any code path until plan 18-08 — that separation is what makes
the migration safe under PG 55P04 — and that `deriveDisplayStatus` / `deriveBookingStatusView` take
`cancelledBy` as a widened `string`, so `tsc` will NOT flag the display forks and they were reviewed
by hand.

Export the two derived TS union types (`HostVerificationStatus`, `ListingReviewState`) from the enums
so `deriveBookable` and the badge can consume them without re-declaring string unions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "withTimezone: true" src/lib/db/schema.ts` increased by exactly 5 (three on `host_verification`, two on `listing_review` — `submittedAt`, `decidedAt`, `createdAt` minus the two counted already; verify the new columns each carry it: `grep -A2 'checked_at\|decided_at\|submitted_at' src/lib/db/schema.ts | grep -c 'withTimezone: true'` is 3).
    - `grep -n "pgEnum(\"host_verification_status\"" src/lib/db/schema.ts` appears on a LOWER line number than `pgTable("host_verification"` (const TDZ, D-223).
    - `grep -v '^\s*//' src/lib/db/schema.ts | grep -iEc 'document|passport|id_number|idNumber|image_url|attachment' ` returns 0 within the `host_verification` block.
    - `grep -n "'ops'" src/lib/db/schema.ts` shows the value only inside the `cancelledBy` enum list.
    - `grep -c 'onDelete: "restrict"' src/lib/db/schema.ts` increased by 1 (the `listing_review` FK).
  </acceptance_criteria>
  <done>The schema declares both enums before their tables, both tables, the listing column, both partial indexes and the widened `cancelled_by` — and `tsc` is green.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Generate the migrations, hand-append the grandfather backfill, and apply them</name>
  <files>drizzle/0026_host_verification_listing_review.sql, drizzle/0027_cancelled_by_ops.sql, drizzle/meta/_journal.json</files>
  <read_first>
    - drizzle/0017_group_bookings.sql:1-20 (brand-new enums + tables in ONE file, and the header that explains why no 55P04 split is needed)
    - drizzle/0014_phase7_ledger_kind.sql:1-27 (the hand-authored, scoped, idempotent backfill shape)
    - drizzle/0020_open_capacity_enum.sql (the whole file — an ALTER TYPE migration that does NOTHING else)
    - drizzle/0021_open_capacity_columns.sql:1-12 (the unqualified-names rule AND the grep tripwire it describes)
    - drizzle/0024_audit_table.sql:1-9 (which migrations go through the generator, and why the journal/snapshot must stay consistent)
    - drizzle/0025_audit_resolved_by.sql:30-34 (a migration is NEVER verified with tsc)
    - tests/helpers/db.ts:100-140 (how migrations are replayed into an isolated schema — each statement in its own call)
  </read_first>
  <action>
Run `npm run db:generate` to produce the additive DDL for the new enums, tables, column and indexes.
Rename the generated file to `drizzle/0026_host_verification_listing_review.sql`, keeping
`drizzle/meta/_journal.json` and the snapshot consistent (do not hand-author what the generator can
emit — `drizzle/0024`'s rule).

Strip any schema-qualified `"public".` prefixes the generator emits, so unqualified names resolve via
the schema-first search_path in every isolated test schema (`drizzle/0013`/`0021` precedent).

Hand-append two statements to `0026`, each separated by `--> statement-breakpoint`, with a header
block above them stating: (a) both types here are brand-new `CREATE TYPE`, so their labels are usable
in this same transaction — PROBED on PostgreSQL 18.4, and the 55P04 restriction applies only to
`ALTER TYPE … ADD VALUE` on an already-committed type; (b) the backfill takes the `drizzle/0014`
shape rather than `drizzle/0017`'s DEFAULT-only shape, because D-240 grandfathers what is already
SELLING and nothing else.

Statement one: `UPDATE "listing" SET "review_state" = 'grandfathered' WHERE "status" = 'published'
AND "deleted_at" IS NULL AND "review_state" = 'pending';` — the third predicate is what makes it
re-runnable and structurally unable to move an `approved` or `rejected` row.

Statement two: `INSERT INTO "host_verification" ("user_id","status","provider","checked_at",
"created_at","updated_at") SELECT DISTINCT l."host_id", 'grandfathered', 'migration', NULL, now(),
now() FROM "listing" l WHERE l."status" = 'published' AND l."deleted_at" IS NULL ON CONFLICT
("user_id") DO NOTHING;` — with a comment stating that `checked_at` stays NULL and `provider` says
`'migration'` because nothing was checked, and writing a timestamp would fabricate an audit record
(the `drizzle/0025` `resolved_by` principle). Note the intended consequences at the site: `draft` and
`unlisted` rows become `pending`; a host with only drafts starts `unverified`, not `grandfathered`.

Create `drizzle/0027_cancelled_by_ops.sql` containing exactly one statement —
`ALTER TYPE "cancelled_by" ADD VALUE IF NOT EXISTS 'ops';` — and a header on the
`drizzle/0020_open_capacity_enum.sql` model: this file does NOTHING ELSE because an
`ALTER TYPE … ADD VALUE` and the FIRST USE of the new value cannot share a transaction (PG 55P04),
and both migrators wrap all pending migrations in one transaction. Record that the first write of
`'ops'` happens at runtime in plan 18-08, long after commit.

⚠ Then run the schema push. **`npm run db:migrate` (drizzle-kit migrate) — NOT `drizzle-kit push`.**
This project hand-edits generated migration SQL and `push` bypasses the ledger. `tsc` and `next build`
pass WITHOUT the migration because types come from the schema file, not the live database, so skipping
this produces a false-positive verification state.

⚠ Do NOT add any statement to `0026` or `0027` that USES the value `'ops'`.
  </action>
  <verify>
    <automated>npm run db:migrate && node -e "const j=require('./drizzle/meta/_journal.json');const t=j.entries.map(e=>e.tag);if(!t.some(x=>x.includes('host_verification'))||!t.some(x=>x.includes('cancelled_by_ops')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:migrate` completes without error and is idempotent on a second run.
    - `drizzle/0027_cancelled_by_ops.sql` contains exactly ONE semicolon-terminated statement: `grep -v '^\s*--' drizzle/0027_cancelled_by_ops.sql | grep -c ';'` returns 1.
    - No migration file at or after 0027 contains the single-quoted literal `'ops'` other than the ALTER TYPE itself: `grep -l "'ops'" drizzle/00{2,3}*.sql | wc -l` resolves to only `drizzle/0027_cancelled_by_ops.sql`.
    - `drizzle/0026_*.sql` contains no `"public".` prefix: `grep -c '"public"\.' drizzle/0026_*.sql` returns 0.
    - Against the live dev database, `psql` (or `drizzle-kit studio`) shows every `listing` row with `status='published' AND deleted_at IS NULL` at `review_state='grandfathered'`, and every `draft`/`unlisted` row at `'pending'`.
    - `drizzle/meta/_journal.json` lists both new tags in order.
  </acceptance_criteria>
  <done>Both migrations exist, the ALTER TYPE file does nothing else, `npm run db:migrate` has been RUN, and the live catalogue is grandfathered.</done>
</task>

<task type="auto">
  <name>Task 3: The two tests tsc structurally cannot replace — column set and backfill</name>
  <files>tests/ops/verification-schema.test.ts, tests/ops/grandfather-backfill.test.ts, tests/design/enum-first-use-tripwire.test.ts</files>
  <read_first>
    - tests/ops/alerts.test.ts:555-600 (case 14 — the `information_schema.columns` + `pg_constraint` idiom AND its "MEASURED AGAINST THE DATABASE, NEVER AGAINST tsc" header)
    - tests/design/cloudinary-preset-script.test.ts:395-415 (the shipped `readFileSync`-a-source-artefact idiom)
    - tests/helpers/db.ts (setupTestDb / teardownTestDb / the isolated schema name)
    - src/lib/profile.ts:44-52 (why an allow-list, not a deny-list: "adding a new private column … does not silently leak it")
    - drizzle/0021_open_capacity_columns.sql (the grep-tripwire discipline the third test mechanises)
  </read_first>
  <action>
Create `tests/ops/verification-schema.test.ts` (integration, isolated schema). Query
`information_schema.columns` for `table_schema = ${testDb.schema} AND table_name = 'host_verification'`,
sort the `column_name` values, and assert the WHOLE SET equals an explicit allow-list —
`checked_at, created_at, decided_by_staff_id, provider, reason, result, status, updated_at, user_id`.
An allow-list, never a subset check and never a deny-list: a deny-list of `document`/`id_number`/
`image` fails the day someone names a column `attachment_url`. Add a second case asserting
`host_verification` carries no FK to anything but `user` (via `pg_constraint`), and a third asserting
`vendor_ref`, `result` and `checked_at` are all NULLABLE — a row in `pending` has no result yet and the
manual provider has no vendor reference. Copy the alerts.test.ts header verbatim in spirit: the row
type comes from `schema.ts`, so a column declared there but never migrated leaves `tsc` and
`next build` perfectly green.

Create `tests/ops/grandfather-backfill.test.ts` (integration, isolated schema). It must:
(a) `setupTestDb()`; (b) seed four listings — `published`, `draft`, `unlisted`, and a soft-deleted
`published` — plus one host owning only a draft and one host owning a published row; (c) reset every
`listing.review_state` to the column default `'pending'` and delete any `host_verification` rows, so
the harness's empty-schema replay is undone and the statements have something to act on; (d) read the
two grandfather statements **out of `drizzle/0026_host_verification_listing_review.sql` on disk** with
`readFileSync` and execute them — reading rather than retyping is what makes this measure the
migration instead of a copy of it; (e) assert row by row: published → `grandfathered`, draft →
`pending`, unlisted → `pending`, soft-deleted published → `pending`, the published host gets a
`host_verification` row at `grandfathered` with `checked_at` NULL and `provider = 'migration'`, and
the draft-only host gets NO row (so `deriveBookable` will read them as `unverified`); (f) run both
statements a SECOND time and assert the identical outcome; (g) set one row to `approved` and one to
`rejected`, re-run, and assert neither moved.

Create `tests/design/enum-first-use-tripwire.test.ts` (design config — DB-free). It walks `drizzle/*.sql`
and asserts that the single-quoted literal `'ops'` appears in exactly one file, `0027_cancelled_by_ops.sql`,
and in no migration at or after it. Filter comment lines before counting — `grep -c` counts comments,
and a header that names the token would otherwise invalidate its own gate. This mechanises the
tripwire `drizzle/0021`'s header describes in prose.
  </action>
  <verify>
    <automated>npx vitest run tests/ops/verification-schema.test.ts tests/ops/grandfather-backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/ops/verification-schema.test.ts tests/ops/grandfather-backfill.test.ts` passes.
    - `npx vitest run --config vitest.design.config.ts tests/design/enum-first-use-tripwire.test.ts` passes. (⚠ The bare form prints "No test files found" and exits 1 — indistinguishable from a real failure. The `--config` flag is mandatory.)
    - `grep -c "readFileSync" tests/ops/grandfather-backfill.test.ts` is at least 1 and the path it reads is `drizzle/0026_host_verification_listing_review.sql`.
    - `tests/ops/verification-schema.test.ts` asserts set EQUALITY (`toEqual` on a sorted array), not `toContain`: `grep -c "toContain" tests/ops/verification-schema.test.ts` returns 0 for the column-set case.
    - MUTATION PROOF, recorded in the SUMMARY: temporarily add a `document_url` column to `host_verification` in a scratch migration replay and confirm `verification-schema.test.ts` goes RED; revert.
    - MUTATION PROOF, recorded in the SUMMARY: temporarily widen the backfill WHERE to drop `AND "status" = 'published'` in a scratch copy and confirm `grandfather-backfill.test.ts` goes RED; revert.
  </acceptance_criteria>
  <done>HVER-02 is enforced by a database assertion with a watched RED, and the D-240 backfill is measured against the statement on disk, twice, including the never-move-an-approved-row property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production database | The only boundary this plan crosses; it rewrites `review_state` on every live listing |
| schema.ts declaration → migrated table | A column declared but not migrated is invisible to `tsc` and to `next build`; a column migrated but not declared is invisible to `tsc` and can hold anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-0201 | Information disclosure | `host_verification` growing a document column | mitigate | Exact-column-set allow-list against `information_schema` in `tests/ops/verification-schema.test.ts`, with a watched RED mutation. FitOut never becomes a custodian of government IDs (D-206 / ASVS V8) |
| T-18-0202 | Tampering | fabricated audit record | mitigate | Grandfathered rows carry `checked_at` NULL and `provider = 'migration'`; the backfill test asserts both. A timestamp on a check that never happened is a fabricated audit record |
| T-18-0203 | Denial of service | the whole live catalogue unsellable | mitigate | Column default and backfill ship in the SAME migration file — there is no deploy window in which `review_state` is `'pending'` for published rows |
| T-18-0204 | Tampering | PG 55P04 defect that passes every test | mitigate | `'ops'` goes in its own ALTER-TYPE-only migration; `tests/design/enum-first-use-tripwire.test.ts` mechanises the grep tripwire. The harness replays statements in separate transactions and a fresh DB creates the type in-transaction, so neither can reproduce the failure — only the discipline catches it |
| T-18-0205 | Elevation | a re-run of the backfill re-grandfathering a rejected listing | mitigate | `AND "review_state" = 'pending'` in the UPDATE and `ON CONFLICT DO NOTHING` on the INSERT; the test runs both statements twice and after a hand-set `approved`/`rejected` |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed. `drizzle-kit generate` uses the already-installed 0.31.10 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — green.
2. `npm run db:migrate` — RUN, and idempotent on a second invocation. This is the [BLOCKING] gate: build and type checks pass without it, so skipping it produces a false-positive verification state.
3. `npx vitest run tests/ops/verification-schema.test.ts tests/ops/grandfather-backfill.test.ts` — green.
4. `npx vitest run --config vitest.design.config.ts tests/design/enum-first-use-tripwire.test.ts` — green.
5. `npm test` **alone**, then `npm run test:design` **alone**. Nothing else in the suite reads `review_state` yet, so both should be green; a red here is a truncation artefact until proved otherwise — re-run alone before investigating.
6. Both mutation REDs recorded in the SUMMARY with their observed failure messages.
</verification>

<success_criteria>
- HVER-02: `host_verification` exists, is 1:1 to `user`, and its exact column set is pinned by a database assertion that has been watched RED against a document column.
- LVER-04: every listing `published` at migration time — and only those — reads `grandfathered`, as does every host owning one; the state is first-class and distinct, and one statement burns it down later.
- `npm run db:migrate` has been run against the live dev database.
- `'ops'` exists on `cancelled_by` and is written by nothing.
</success_criteria>

<output>
Create `.planning/phases/18-host-verification-listing-review-fitout-ops/18-02-SUMMARY.md` when done.

Record: the exact allow-list of `host_verification` columns; the two backfill statements verbatim; the
counts observed against the live dev DB (published → grandfathered, draft/unlisted → pending); both
mutation REDs; and an explicit note that `'ops'` is added but written nowhere until 18-08.
</output>
