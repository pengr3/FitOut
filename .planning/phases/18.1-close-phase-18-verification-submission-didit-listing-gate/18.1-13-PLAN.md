---
phase: 18.1-close-phase-18-verification-submission-didit-listing-gate
plan: 13
type: execute
wave: 6
depends_on: ["18.1-11"]
files_modified:
  - src/app/actions/ops-contact.ts
  - src/components/ops/ops-contact-reveal.tsx
  - src/components/ops/ops-queue-row.tsx
  - tests/ops/ops-queue-row.test.tsx
  - tests/ops/host-contact-reveal.test.ts
  - tests/design/ops-guard-coverage.test.ts
  - tests/design/site-contacts.test.ts
  - src/lib/design/live-regions.ts
  - tests/design/live-regions.test.tsx
autonomous: true
requirements: [OPS-06]
must_haves:
  truths:
    - "D-257 (PM-E): host contact in /ops is REVEAL-ON-CLICK WITH AN AUDIT ROW PER REVEAL, never always-visible. The reveal is a server action under requireStaff() that returns the values and writes the audit row in the SAME call."
    - "D-271: the reveal is on BOTH queue rows, host and listing — an ops question is usually about a specific listing, and the contact must be where the subject of the question is. mailto: for email, PLAIN TEXT for phone (tel: is meaningless on a desktop console), and an explicit 'Not provided' when a field is empty."
    - "D-72: the audit row carries ids and enum-shaped values ONLY — never the email, never the phone, never any address. Asserted on the SERIALISED row so a nested value cannot slip through."
    - "FINDING F-6 RESOLVED: the queue projections and their exported types DO NOT WIDEN. Shipping email and phone on the queue item would put both values in the initial RSC payload, which D-271's own warning forbids. Both branches already carry the id the affordance needs; the reveal returns a SEPARATE type."
    - "requireStaff() is the FIRST statement of the action — before parse and before rate limit — so a non-staff caller learns nothing about which ids exist and consumes nobody's rate-limit budget. A non-staff caller gets notFound(), never a sentence."
    - "session.cookieCache stays UNCONFIGURED. requireStaff() is correct only while it does; enabling it would keep a REVOKED staff grant working for the cache TTL and no test would go red."
    - "Every gate this touches is AMENDED, never deleted: the terminal-row zero-anchor assertion gains one declared mailto: exception, the interactive-set assertion grows to three in DOM order with an amended message, EXPECTED_OPS_ACTIONS goes 6 to 7 with its docblock, and site-contacts gains a narrow EXCLUDED_MAILTO map in its own shipped shape."
  artifacts:
    - path: "src/app/actions/ops-contact.ts"
      provides: "export async function revealHostContact — a function DECLARATION with requireStaff() first, returning OpsHostContact and writing the audit row in the same call"
      exports: ["revealHostContact"]
      min_lines: 80
    - path: "src/components/ops/ops-contact-reveal.tsx"
      provides: "The client island holding the revealed values in React state, plus its own named role=status refusal region"
    - path: "tests/ops/host-contact-reveal.test.ts"
      provides: "Authz-before-read proof plus the D-72 serialised-meta assertion"
  key_links:
    - from: "src/app/actions/ops-contact.ts"
      to: "src/lib/ops/staff.ts"
      via: "import { requireStaff } from \"@/lib/ops/staff\" — the AST gate resolves the binding through the import"
      pattern: "requireStaff"
    - from: "src/components/ops/ops-queue-row.tsx"
      to: "src/components/ops/ops-contact-reveal.tsx"
      via: "one new <Fact term=\"Contact\"> inside the existing <dl>, on both row kinds"
      pattern: "ops-contact-reveal"
---

<objective>
Land **PM-E / OPS-06**: ops can reach a host, and every reach is on the record.

The values must **not** be in the initial RSC payload behind a `hidden` attribute — that is a reveal
with no audit, and the payload is readable. So the reveal is a **server action under `requireStaff()`
that returns the values and writes the audit row in the same call**, and the revealed values live in
the client island's React state afterwards.

⚠ **FINDING F-6.** `18.1-CONTEXT` phrases D-271 as *"adds `email` and `phone` to both branches"* of
`src/lib/ops/review-queue.ts`, and **D-271's own warning forbids exactly that**. Both cannot hold.
The resolution, recorded here rather than discovered in review: what the queue projections need is
not the contact **values** but the affordance's **inputs**, and both branches already carry them
(`hv.user_id AS "userId"` on the host branch, `l.host_id AS "hostId"` at `:273` on the listing
branch). **The projections and their exported types do not change.** The reveal returns a separate
type.

⚠ **Four gates were written to stop exactly this edit.** Each gets a **narrow, documented amendment
with its reason in the assertion's own failure message** — never a deletion. The
`ops-queue-row.tsx` row is asserted TERMINAL (zero anchors, zero link-role elements) and a host row
is asserted to have exactly TWO interactive descendants; `site-contacts.test.ts` bans every unguarded
`mailto:` in `src/`; `ops-guard-coverage.test.ts` pins the ops action census and is **build-blocking**.

Purpose: an operator with a question about a queued row can reach the person, and the trail says who
looked and at whom — never at what.
Output: one ops action, one client island, one new fact on both rows, four amended gates, one
integration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-UI-SPEC.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-RESEARCH.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: revealHostContact — guards first, values back, audit in the same call</name>
  <files>src/app/actions/ops-contact.ts</files>
  <read_first>
    - `src/app/actions/ops-review.ts:19-38` (the header's numbered guard ordering), `:96-160` (the
      NAMED result constants `OK` / `DENIED` / `STALE` / `TOO_FAST` / `PROVIDER_UNAVAILABLE`,
      `OPS_ACTION_RATE_LIMIT = { window: 60, max: 30 }` at `:139`, `type DenialReason` at `:159`),
      and `:354-441` (an ops action end to end, both audit branches).
    - `src/lib/ops/staff.ts:110-114` — `requireStaff()` `notFound()`s. ⚠ Its header records the
      standing hazard: **`requireStaff()` is correct only while `session.cookieCache` stays
      unconfigured.**
    - `tests/design/ops-guard-coverage.test.ts:116-132` (`EXTRA_OPS_ACTIONS`, `EXPECTED_OPS_ACTIONS`
      at `:119`, `EXPECTED_OPS_PAGES` at `:122`), `:185` / `:310` / `:328` / `:362` — the census
      algorithm. Read all seven of its requirements before writing a line of the action.
    - `src/lib/validation/ops.ts:41-45` — `ID_MAX = 128` and why an unbounded id is memory exhaustion
      once it becomes a rate-limit key. ⚠ `:30-40` — the no-half-sanitiser rule.
    - `src/lib/ops/review-queue.ts` — the two projections, `OpsQueueHostItem`'s *"NOTHING ELSE"*
      docblock, and ⚠ header line 24: a certain table's NAME appears ZERO times in that file.
      **Do not name it in any new comment there, and do not widen the projections.**
    - `src/lib/audit.ts:32-51, 83-90` — the entry shape and the deliberate swallow.
    - `18.1-UI-SPEC.md` § Surface 4 § The refusal region — the two server sentences, verbatim.
  </read_first>
  <action>
    Create `src/app/actions/ops-contact.ts`. The filename matters: `OPS_ACTION_FILES` is
    `readdirSync("src/app/actions")` filtered to names starting `ops-` and ending `.ts`, so this file
    is found by the glob and needs **no** `EXTRA_OPS_ACTIONS` row.

    Export **`export async function revealHostContact(...)`** — a function **DECLARATION**. ⚠ An
    arrow const (`export const revealHostContact = async () => {}`) is **invisible** to the census's
    AST walk and would silently reduce coverage.

    ⚠ **`requireStaff()`, imported as `import { requireStaff } from "@/lib/ops/staff"`, is the FIRST
    STATEMENT of the function** — before the parse and before the rate limit. The census resolves the
    binding through the import and requires the guard to be the first statement; a locally declared
    same-named helper or an `await gate()` re-export yields nothing. The stated reason is the security
    property: *a non-staff caller is refused without consuming anybody's rate-limit budget and without
    learning whether the ids they sent exist.*

    Then, in order: Zod-parse the host id bounded at `ID_MAX = 128`; `rateLimit` on
    `OPS_ACTION_RATE_LIMIT`'s shape keyed on the authenticated staff id, writing an audit row on the
    deny branch; read `user.email` and `user.phone` for that host; write the audit row; return.

    Declare the result constants in `ops-review.ts:96-120`'s exact style — **do not invent a new
    result shape.** Take the two refusal sentences **verbatim** from `18.1-UI-SPEC.md` § Surface 4
    (the read-failed sentence and the rate-limited sentence). ⚠ A non-staff caller gets `notFound()`,
    **never a sentence** — `FORBIDDEN_REFUSALS = ["forbidden","redirect"]` bans the other two shapes
    on this route group.

    Declare `OpsHostContact = { email: string; phone: string | null }` beside the action, in the same
    explicit-columns style, and write **FINDING F-6** in the header: this is a SEPARATE type precisely
    because `src/lib/ops/review-queue.ts`'s projections must not widen, and 18.1-CONTEXT phrases
    D-271 the other way. `user.email` is `notNull().unique()`, so there is no empty case for email;
    `user.phone` is nullable.

    The result type is `{ ok: true; contact: OpsHostContact } | { ok: false; error: string }`.

    ⚠ **D-72:** the audit row is `{ actorId: staff.id, action: <the reveal action name>, outcome,
    meta: { userId } }`. **Ids and enum-shaped values only — never the email, never the phone, never
    an address.** A reveal audit row records *that* staff member X looked at host Y's contact details,
    never *what* they were. Write that sentence at the site.

    ⚠ Read the audit row back out of the table in the test, never inferred from the `ok` return —
    `recordAudit` swallows its own insert failure by design.

    ⚠ **Do NOT enable `session.cookieCache` anywhere.** `requireStaff()` is correct only while it
    stays unconfigured; enabling it would keep a REVOKED staff grant working for the cache TTL and no
    test would go red.
  </action>
  <acceptance_criteria>
    - `grep -c "^export async function revealHostContact" src/app/actions/ops-contact.ts` returns 1
    - `grep -c "import { requireStaff } from \"@/lib/ops/staff\"" src/app/actions/ops-contact.ts` returns 1
    - The first statement of the function body is `await requireStaff()` or `const staff = await requireStaff();` — verified by `npx vitest run --config vitest.design.config.ts tests/design/ops-guard-coverage.test.ts` passing after task 3's count bump
    - `grep -Ec "forbidden\(|redirect\(" src/app/actions/ops-contact.ts` returns 0
    - `grep -c "OpsHostContact" src/app/actions/ops-contact.ts` returns at least 2 and the type is NOT exported from `src/lib/ops/review-queue.ts`
    - `git diff src/lib/ops/review-queue.ts` is EMPTY
    - `grep -c "cookieCache" src/lib/auth.ts` returns 0 and `git diff src/lib/auth.ts` is empty
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>A staff-guarded action returns the contact and writes the audit row in one call, and the queue projections are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The reveal island and the Contact fact on both row kinds</name>
  <files>src/components/ops/ops-contact-reveal.tsx, src/components/ops/ops-queue-row.tsx</files>
  <read_first>
    - `src/components/ops/ops-queue-row.tsx` — the header at `:11-21` (the TERMINAL decision, in its
      own words), `ROW_TERM_CLASS` (`:97`), `ROW_VALUE_CLASS` (`:91`), `ROW_MONEY_CLASS` (`:94`),
      `ROW_LEAD_CLASS` (`:107`), `addressOf()` (`:190`) and its use at `:280`, and the `<dl>` at
      `:277`.
    - `src/components/ops/ops-decision-actions.tsx:315-333` — the touch controls, the
      `disabled` + `aria-disabled` in-flight idiom, and ⚠ `:330`, the author-named `role="status"`
      refusal region this reproduces exactly.
    - `src/components/patterns/row-card.tsx` — its docblock on why the `<dl>` exists (*"the
      label↔value association survives linearisation on a narrow screen"*), and the render order:
      `children` (`:215`) before `actions` (`:222`).
    - `18.1-UI-SPEC.md` § Surface 4 in full — the four-step interaction, the two values table, the
      control, the refusal region, and the privacy constraints.
  </read_first>
  <action>
    Create `src/components/ops/ops-contact-reveal.tsx` as a `"use client"` island. It renders:
    - **before reveal**: a `<Button variant="outline" size="touch" aria-label={\`Show contact for ${hostLabel}\`}>`
      labelled **Show contact**. `size="touch"` because it ACTS — it writes an audit row and returns
      PII. `variant="outline"` because it is secondary weight, not brand and not destructive.
    - **in flight**: the same button, `disabled` **and** `aria-disabled`, labelled **Showing…**.
    - **after success**: the `Contact` fact is **replaced by TWO facts** — `Email` and `Phone` — and
      **focus moves to the email anchor**. ⚠ Two facts, not one line: the `<dl>`'s whole reason for
      existing on this row is that the label-value association survives linearisation on a narrow
      screen, and two values crammed into one `<dd>` throws that away at exactly the width where it
      matters. ⚠ Focus moving IS the announcement — GATE-03 rule 7's *polite region + moved focus*
      mechanism. **No second live region for success, and none may be added.**
    - `Email` renders `<a href={"mailto:" + email}>{email}</a>`. `user.email` is `notNull().unique()`,
      so there is no empty case and no fallback string.
    - `Phone` renders the value or the literal **"Not provided"** — **plain text, no `tel:`**. `tel:`
      is meaningless on a desktop console and would be a second unguarded scheme to argue about. The
      "Not provided" shape is `addressOf()`'s precedent: *"a blank cell reads as 'fine' rather than as
      'there is nothing here to check'"*.
    - **on refusal**: the button returns to idle and the server's sentence lands in the island's own
      named `role="status"` region, ordinary ink, verbatim.

    ⚠ **The revealed values live in the island's React state after the action returns — NEVER in
    props from the RSC.** That is what makes the reveal auditable.

    In `src/components/ops/ops-queue-row.tsx`, add ONE `<Fact term="Contact">` inside the existing
    `<dl>` holding the island, on **both** row kinds. Reuse `ROW_TERM_CLASS` and `ROW_VALUE_CLASS` —
    the identical constants the six existing facts use. ⚠ **No `tabular-nums` on a phone number**:
    `ROW_MONEY_CLASS` is for money and counts, and giving a phone the money treatment is the first
    step toward reading it as a figure to compare down a column. Nothing on the row is promoted by
    the reveal; the loudest thing is still the wait figure.

    ⚠ **Amend the component header at `:11-21` in the SAME commit**, naming D-271. Do not leave the
    header saying "zero anchors" while the test permits one.

    ⚠ **No new lucide glyph, no `data-testid`, no coral, no destructive ink, no dialog.**
  </action>
  <acceptance_criteria>
    - `grep -c "tel:" src/components/ops/ops-contact-reveal.tsx src/components/ops/ops-queue-row.tsx` returns 0
    - `grep -c "Not provided" src/components/ops/ops-contact-reveal.tsx` returns 1
    - `grep -c "tabular-nums\|ROW_MONEY_CLASS" src/components/ops/ops-contact-reveal.tsx` returns 0
    - `grep -c "role=\"status\"" src/components/ops/ops-contact-reveal.tsx` returns exactly 1
    - `grep -Ec "data-testid|matchMedia|useMediaQuery" src/components/ops/ops-contact-reveal.tsx` returns 0
    - `src/components/ops/ops-queue-row.tsx`'s header at `:11-21` names D-271 and the one-scheme exception
    - `git diff src/lib/design/selector-contract.ts` is empty
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/one-tree.test.ts</automated>
  </verify>
  <done>Both row kinds carry a touch-sized reveal whose values arrive in client state, render as two facts, and never in the RSC payload.</done>
</task>

<task type="auto">
  <name>Task 3: Amend four gates, move the live-region count to 31, and prove D-72</name>
  <files>tests/ops/ops-queue-row.test.tsx, tests/design/ops-guard-coverage.test.ts, tests/design/site-contacts.test.ts, src/lib/design/live-regions.ts, tests/design/live-regions.test.tsx, tests/ops/host-contact-reveal.test.ts</files>
  <read_first>
    - `tests/ops/ops-queue-row.test.tsx` — `INTERACTIVE` (`:89`), assertion 1 at `~:190` (*"renders
      zero anchors and zero link-role elements, on BOTH kinds"*) INCLUDING its long failure message,
      the guard-the-guard at `~:206` (which renders a real anchor and asserts the selector finds 1),
      assertion 2 at `~:213` (*"exactly TWO interactive descendants"*) INCLUDING its failure message
      about a **fabricated document affordance**, and the `DOCUMENT_WORDS` scan.
    - `tests/design/site-contacts.test.ts` — `unguardedMailto(scan)` at `:507`, the address scanner at
      `:520`, and `EXCLUDED_ADDRESSES` at `:316-324` with the `:237` note that it is a DECLARATION
      matching the tree today. ⚠ Read the null-branch reasoning before touching it.
    - `tests/design/ops-guard-coverage.test.ts` — the seven census requirements and the header that
      explains every pinned number.
    - `src/lib/design/live-regions.ts:1798` (the alias history paragraph, which plan 18.1-11 extended)
      and `:1917`, now reading `DeclaredFileCountIsThirty`; `tests/design/live-regions.test.tsx:231`
      and `:549`.
    - `tests/ops/reject-reason.test.ts:299-325` — the D-72 SERIALISED-meta case idiom, verbatim.
    - `tests/ops/ops-audit.test.ts:46-215` — the harness and `trailRows` (`:132-138`).
    - `18.1-UI-SPEC.md` § Gate Amendments rows 1, 2, 3, 4 and 7 — each amendment's exact shape and
      why a deletion would be wrong.
  </read_first>
  <action>
    Four amendments. **A deletion is wrong in every one**, and each carries its reason in the
    assertion's own failure message — this file family's established style.

    1. **`ops-queue-row.test.tsx` assertion 1.** `querySelectorAll("a")` becomes *zero anchors
       **except** those matching `a[href^="mailto:"]`, and at most **1** of those per revealed row*.
       The `[role="link"]` clause stays at **0, unchanged**. ⚠ **The guard-the-guard case stays
       untouched** — it is what makes the exemption honest. Put the reason in the failure message: a
       `mailto:` is a **compose action, not navigation**; the row still browses nowhere. Deleting the
       clause would license a **destination** on the row — a detail page you click into to see the
       photos — which is exactly what OPS-04 / D-246 forbid.
    2. **`ops-queue-row.test.tsx` assertion 2.** The expected array grows to **three, in DOM order**:
       the reveal control's accessible name, then Approve, then Reject. (`RowCard` renders `children`
       before `actions`, and the reveal lives in the `<dl>` inside `children`.) Add a **second case**
       for the revealed state. ⚠ **Amend the failure message** — its stated fear is a *fabricated
       document affordance*; the new text must say the third control is the D-271 contact reveal
       **and that a fourth is still forbidden**, or the next reader will read the widened array as the
       guard having been loosened generically. ⚠ **`DOCUMENT_WORDS` stays untouched** — none of
       `Contact`, `Show contact`, `Email`, `Phone`, `Not provided` or `Showing…` matches it.
       Add a case asserting an **absent phone renders "Not provided"**, never an empty `<dd>`.
    3. **`tests/design/site-contacts.test.ts`** (build-blocking). ⚠ **This is the finding the research
       did not surface**: `unguardedMailto` scans all of `src/**` for literals containing `mailto:`
       and requires every one to sit inside a verified `SUPPORT_EMAIL !== null` guard, so the ops
       reveal's `mailto:` **will redden the build**. Add a new
       `EXCLUDED_MAILTO: Readonly<Record<string, string>>` keyed `file — literal` with a **mandatory
       reason**, in the shipped `EXCLUDED_ADDRESSES` (`:316`) shape, filtered inside
       `unguardedMailto`. Add a **guard-the-guard**: the map is non-empty, the named file exists, and
       the scanner still finds that literal — so a typo in a key cannot silently widen the ban.
       ⚠ **The non-null branch is untouched.** The reason for the exemption: the null branch's subject
       is *FitOut publishing an address to write TO*; the ops reveal is the **inverse direction** — an
       authenticated staff member composing TO a host whose address FitOut already holds. Relaxing
       `unguardedMailto` itself would re-open the footer/email-shell hole D-26 exists to close, for
       every file at once.
    4. **`tests/design/ops-guard-coverage.test.ts`** (build-blocking). `EXPECTED_OPS_ACTIONS`
       **6 -> 7** **and its docblock**, in the same commit. `EXPECTED_OPS_PAGES` stays **1** (D-246,
       one queue, one page). `EXTRA_OPS_ACTIONS` **unchanged** — the `ops-*` glob finds the new file.
       ⚠ Measure the RED at 6 before writing 7.

    5. **`src/lib/design/live-regions.ts`** — `LIVE_REGION_FILES` **30 -> 31**, adding
       `src/components/ops/ops-contact-reveal.tsx`. `LIVE_REGION_IDS` **+1**; `LIVE_REGIONS` **+1
       row**, `kind: "status"`, rule 1; `AUTHOR_NAMED_REGIONS` **+1** for the ops contact region name
       with its reason. Rename the compile alias `DeclaredFileCountIsThirty` ->
       `DeclaredFileCountIsThirtyOne` and move the second pin at
       `tests/design/live-regions.test.tsx:231`. Extend the alias-history paragraph at `:1798` to
       record this second step, completing the 29 -> 31 the UI-SPEC describes for the phase.

    6. **Create `tests/ops/host-contact-reveal.test.ts`** on the `ops-audit.test.ts` harness. Cases:
       - a **non-staff** caller is refused **before any read** — the named-`notFound` idiom
         (`tests/ops/staff-guard.test.ts:101`) — with a **positive staff control** in the same file so
         the refusal is not passing for the wrong reason;
       - a signed-out caller is refused the same way;
       - a successful reveal returns the correct `{ email, phone }` and writes **exactly one** audit
         row, read **BACK OUT of the table** via `trailRows`, carrying the authenticated `actorId`;
       - **D-72**: seed the host with an unmistakable email and phone, serialise the WHOLE audit row
         with `JSON.stringify`, and assert it contains neither value nor any address string — the
         `reject-reason.test.ts:300-325` idiom;
       - a host with a null phone returns `phone: null` and still writes exactly one audit row;
       - the rate limit denies and **writes its audit row**, read back out of the table;
       - an over-long host id is rejected by the Zod bound **after** the staff guard, not before.

    ⚠ **Measure every count RED at its old value before writing the new one**, and record the reds in
    the SUMMARY.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/ops/ops-queue-row.test.tsx` passes; the guard-the-guard case and the `DOCUMENT_WORDS` scan are byte-unchanged; the `[role="link"]` clause still asserts 0
    - Assertion 2's expected array has three entries in DOM order and its failure message names D-271 AND states that a fourth control is still forbidden
    - `grep -c "EXPECTED_OPS_ACTIONS = 7" tests/design/ops-guard-coverage.test.ts` returns 1 and its docblock was updated in the same commit; `EXPECTED_OPS_PAGES` is still 1; `EXTRA_OPS_ACTIONS` diff is empty
    - `grep -c "EXCLUDED_MAILTO" tests/design/site-contacts.test.ts` returns at least 2 (the declaration and its use in `unguardedMailto`), the map has exactly one entry with a non-empty reason, and a guard-the-guard case asserts the scanner still finds that literal
    - The `SUPPORT_EMAIL !== null` branch of `site-contacts.test.ts` is byte-unchanged
    - `grep -c "DeclaredFileCountIsThirtyOne" src/lib/design/live-regions.ts` returns 1; `DeclaredFileCountIsThirty\b` returns 0; `LIVE_REGION_FILES` has 31 members
    - `npx vitest run tests/ops/host-contact-reveal.test.ts` passes with at least 7 cases
    - The D-72 case asserts on `JSON.stringify` of the whole row, not on individual fields
    - `npm run build` succeeds — `site-contacts` and `ops-guard-coverage` are build-blocking
    - Each moved count has a recorded RED at its old value in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ops/host-contact-reveal.test.ts tests/ops/ops-queue-row.test.tsx</automated>
  </verify>
  <done>Four gates are narrower-but-declared rather than deleted, the live-region ledger reaches 31, and no contact value can reach the audit table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **B6** | `revealHostContact()` -> staff browser. **PII out.** The only surface in the product that hands a host's email and phone to another person. |
| RSC payload -> browser | Everything the queue page serialises is readable by whoever loads it. This is why the values are fetched on demand rather than shipped and hidden. |
| durable audit column | `audit.meta` is a jsonb column under an indefinite retention policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-1301 | Elevation of Privilege | **IDOR on the reveal** — a non-staff caller enumerating host ids | mitigate | `requireStaff()` is the **FIRST statement**, before the parse and before the rate limit, so a non-staff caller learns nothing about which ids exist and burns nobody's budget. Pinned syntactically by `guardsFirst()` in the build-blocking census, and behaviourally by a refused-before-any-read case with a positive staff control. |
| T-18.1-1302 | Repudiation | **an unaudited reveal via the RSC payload** | mitigate | **D-271**: a server action, not a CSS/`hidden` toggle. The values are never in the initial payload — asserted structurally by `git diff src/lib/ops/review-queue.ts` being empty and by `tsc` (the row TYPE has no field for a contact column, so re-exporting one is a type error at every consumer). The action returns the values and writes the audit row in the SAME call. |
| T-18.1-1303 | Information disclosure | **PII in the durable audit column** | mitigate | **D-72.** `meta` is `{ userId }` — ids and enum-shaped values only. Asserted on the **SERIALISED** whole row with unmistakable fixture values, so a nested value cannot slip through a field the test forgot to check. |
| T-18.1-1304 | Information disclosure | the reveal widening into a general PII export | mitigate | `OpsHostContact` is exactly two fields and is declared beside the action in the explicit-columns style. No address is returned — D-267 keeps the address on the listing row the operator is already reading, and D-72 keeps it out of the trail. |
| T-18.1-1305 | Elevation of Privilege | **a REVOKED staff grant surviving in a session cache** | mitigate | `session.cookieCache` stays UNCONFIGURED. `git diff src/lib/auth.ts` is asserted empty and `grep -c cookieCache` returns 0. ⚠ No test would go red if this were enabled, which is why it is an acceptance criterion rather than an assumption. |
| T-18.1-1306 | Denial of Service | reveal-endpoint hammering | mitigate | `OPS_ACTION_RATE_LIMIT = { window: 60, max: 30 }` keyed on the AUTHENTICATED staff id, with an audit row on the deny branch read back out of the table. The Zod bound (`ID_MAX = 128`) prevents an unbounded id becoming a rate-limit key — the memory-exhaustion path `ops.ts:41-45` names. |
| T-18.1-1307 | Spoofing | a fabricated document-upload affordance arriving on the host row under cover of a widened interactive set | mitigate | Assertion 2's failure message is AMENDED to say the third control is the D-271 reveal and that a **fourth is still forbidden**; `DOCUMENT_WORDS` stays untouched and none of the reveal's copy matches it. The zero-anchor clause gains **one scheme, one count, one reason** — never a deletion. |
| T-18.1-1308 | Information disclosure | the `mailto:` exemption re-opening the footer/email-shell hole D-26 closes | mitigate | The exemption is a narrow `EXCLUDED_MAILTO` map keyed `file — literal` with a mandatory reason, filtered inside `unguardedMailto`, plus a guard-the-guard proving the scanner still finds the literal. `unguardedMailto` itself is NOT relaxed and the non-null branch is byte-unchanged. ⚠ Recorded as a real cost: the null branch is now *"zero except one declared row"*, which is strictly weaker than *"zero"*, and should be re-read the day `SUPPORT_EMAIL` is set. |
| T-18.1-13-SC | Tampering | npm installs | mitigate | Zero package installs; no primitive vendored; `components.json` declares `"registries": {}`. `package.json` and the lockfile are not in `files_modified`. |
</threat_model>

<verification>
Run each gate ALONE — never chained:

1. `npx vitest run tests/ops/host-contact-reveal.test.ts`
2. `npx vitest run tests/ops/ops-queue-row.test.tsx`
3. `npx vitest run --config vitest.design.config.ts tests/design/ops-guard-coverage.test.ts`
4. `npx vitest run --config vitest.design.config.ts tests/design/site-contacts.test.ts`
5. `npx vitest run --config vitest.design.config.ts tests/design/live-regions.test.tsx`
6. `npx tsc --noEmit`
7. `npm test`
8. `npm run test:design`
9. `npm run build`

Manual, deferred to plan 18.1-14: the revealed `/ops` row hand-measured at **320 and 1280, both
themes** — overflow, focus order, and that focus lands on the email anchor after a reveal.
</verification>

<success_criteria>
- `revealHostContact` is a function declaration in `src/app/actions/ops-contact.ts` with `requireStaff()` first, covered by the census at 7
- A non-staff and a signed-out caller are each refused before any read, with a positive staff control in the same file
- One reveal writes exactly one audit row whose serialised meta contains no email, no phone and no address
- The row renders at most one anchor and it is `mailto:`; `[role="link"]` stays 0; the guard-the-guard is untouched
- The host row's interactive set is three in DOM order, with an amended message forbidding a fourth
- An absent phone renders "Not provided"
- `src/lib/ops/review-queue.ts` is byte-unchanged
- `src/lib/auth.ts` is byte-unchanged and `cookieCache` appears zero times
- `live-regions` reads 31; `EXPECTED_OPS_ACTIONS` reads 7; `EXPECTED_OPS_PAGES` stays 1
- All four gates green, each invoked separately
</success_criteria>

<output>
Create `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-13-SUMMARY.md` when done
</output>
