# Phase 18.1: Close Phase 18 — the verification path FitOut is legally required to have · Pattern Map

**Mapped:** 2026-09-02
**Files analyzed:** 34 (18 create · 16 modify)
**Analogs found:** 30 exact/role-match · 2 partial · 2 no-analog

> **How to read this file.** Every excerpt below is real, shipped code, read first-hand on 2026-09-02,
> with its path and line numbers verified against the working tree today. Where an excerpt carries a
> comment, **the comment is part of the pattern** — this repository states *why* at the site, and a new
> file that copies the shape without the discipline reads as foreign. Quote paths in plans; do not
> paraphrase the code.
>
> **This document EXTENDS `18-PATTERNS.md`, it does not replace it.** Every idiom there still binds.
> Where 18-PATTERNS already excerpted a site (the audit entry shape, the guards-in-the-WHERE flip, the
> `deriveBookable` re-statements, the migration precedents), this file points at it rather than
> re-quoting it, and adds only what 18.1 needs that Phase 18 did not.

---

## Line-number verification pass

The research cites file:line throughout. Every citation this map carries forward was re-resolved today.
Three had drifted and are corrected here — **use these, not the research's**:

| Cited in 18.1-RESEARCH | Actual (verified 2026-09-02) |
|---|---|
| `paymongo/webhook/route.ts:39-43` (prod boot guard) | comment `:34-39`, the `if` is **`:40-44`** |
| `wizard.tsx:775-780` (Verified-email checklist row) | **`:779-784`**; `publishEligible` is **`:785`** (research said `:782`) |
| `listing.ts:93` (18.1-CONTEXT) / `:91` (research) | **`:91`** — the research is right, the CONTEXT is off by two |

Everything else resolved exactly: `listing.ts:91`, `ops-review.ts:354/379/396-408/449/474/487-499`,
`manual.ts:1`, `port.ts:118-121/129-131/141-153/167-174`, `schema.ts:36/48/401-445`,
`ops-queue-row.tsx:91/97/190/280`, `validation/ops.ts:61/65`, `verification-status.ts:62-81`,
`review-signal.ts:202-243`, `earnings/page.tsx:160/172-181`, `ops-decision-actions.tsx:327-333`,
`review-queue.ts:213-218/233-252/257-302`, `ops-queue-row.test.tsx:89/190/206/213`,
`ops-guard-coverage.test.ts:116-132/185/310/328/362`, `loading-coverage.test.ts:290-292`,
`card-pattern-coverage.test.ts:612`, `live-regions.ts:1917`.

**Pinned counts measured today (the planner's starting values):**
`EXPECTED_PAGES = 34` · `EXPECTED_QUALIFYING = 22` · `EXPECTED_NON_QUALIFYING = 12` ·
`EXPECTED_SURFACES = 22` · `EXPECTED_OPS_ACTIONS = 6` · `EXPECTED_OPS_PAGES = 1` ·
`DeclaredFileCountIsTwentyNine`. All match the UI-SPEC's stated deltas. **Still measure the reds.**

---

## The six idioms every plan in this phase must hold

Carried from 18-PATTERNS's five, plus one this phase adds.

| # | Idiom | Canonical site | What it means for 18.1 |
|---|-------|----------------|------------------------|
| 1 | **Guards live in the UPDATE's own WHERE; 0 rows is the single calm no-op** | `ops-review.ts:396-421` | The submission upsert, the webhook verdict write and the resubmission cooldown are all ONE guarded statement each. `suspended` absent from the WHERE ⇒ D-266 is structural. |
| 2 | **The audit row is read BACK OUT of the table, on BOTH branches** | `ops-review.ts:359-366` (deny) + `:423-428` (allow) · `tests/ops/ops-audit.test.ts:132-138` | `recordAudit` swallows its own insert failure (`audit.ts:83-90`). An `ok` return proves nothing about the row. |
| 3 | **Guard the module a client must never REACH, not the one it legitimately IMPORTS** | `providers/manual.ts:1` + `port.ts:44-60` | `providers/didit.ts` opens with the bare guard import. `port.ts` stays directive-free — `verification-port.test.ts` case 13 reads its comment-stripped source. |
| 4 | **The port is the ONLY branch point and it FAILS CLOSED** | `port.ts:29-42, 141-153` | Never `provider === "didit"` at a call site. Never a hand-built `VerificationResult`. `Object.hasOwn`, not a bare index. |
| 5 | **Every figure and date arrives at the component FINISHED from the server** | `ops-queue-row.tsx:108-114` (`waitLabel` / `submittedLabel`) · `earnings/page.tsx:172-181` | The retry timestamp and the frozen-session sentence are composed server-side and passed as strings. PROJECT D-130 / GATE-05. |
| 6 | **`$onUpdate` does NOT fire through raw SQL** — set `updated_at` (and here `created_at`) explicitly | `ops-review.ts:404` `updated_at = now()` · `schema.ts:432-435` | FINDING F-1. Every shipped ops write spells `updated_at = now()` even though the column declares `$onUpdate`, because `db.execute(sql\`…\`)` bypasses the query builder. |

---

## File Classification

### CREATE

| New file | Role | Data Flow | Closest Analog | Match |
|---|---|---|---|---|
| `src/app/actions/host-verification.ts` | server action (**host**, not ops) | request-response + CRUD | `src/app/actions/capability.ts` (whole file) + `ops-review.ts:354-441` (guarded flip + audit) | exact (two halves) |
| `src/lib/verification/providers/didit.ts` | service (provider adapter) | request-response (outbound HTTP) | `src/lib/verification/providers/manual.ts` (whole) + `src/lib/paymongo.ts:1-90` (thin `fetch` wrapper + prod boot guard) | exact (two halves) |
| `src/lib/verification/didit-verdict.ts` | utility (pure mapper) | transform | `src/lib/payments/refund-rail.ts` (whole file — single branch point, fails closed, pure/isomorphic) + `review-queue.ts:196-218` (`toDate` defensive normaliser) + `ops-queue-row.tsx:170-186` (total `Record` over a union) | role-match |
| `src/app/api/didit/webhook/route.ts` | route handler (webhook) | event-driven, untrusted-in | `src/app/api/paymongo/webhook/route.ts` (whole file) | **exact — the single most important analog in the phase** |
| `src/app/(host)/host/verify/page.tsx` | page (RSC) | request-response | `src/app/(host)/host/earnings/page.tsx:140-185` | exact |
| `src/app/(host)/host/verify/loading.tsx` | loading plate | — | `src/app/(host)/host/earnings/loading.tsx` (whole) | exact |
| `src/components/host/verification-panel.tsx` | component (presentational) | — | `src/components/host/hosting-paused-notice.tsx` (whole file) | exact |
| `src/lib/host/verification-signal.ts` | config (copy module) | — | `src/lib/listing/review-signal.ts:190-243` + `src/lib/host/requests-signal.ts:45-75` | exact |
| `src/lib/host/frozen-payouts.ts` | service (DB read) | CRUD (read) | `src/lib/host/verification-status.ts` (loader shape) + `payout-sweep.ts:115-167` (the query this INVERTS) | exact (two halves) |
| `src/lib/host/resend-verification.ts` | utility (client-safe) | request-response | `wizard.tsx:682-692` (the function being extracted) | exact |
| `src/app/actions/ops-contact.ts` | server action (ops) | request-response | `src/app/actions/ops-review.ts:354-441` (guards-first ordering) | exact |
| `src/components/ops/ops-contact-reveal.tsx` | component (client island) | request-response | `src/components/ops/ops-decision-actions.tsx:315-333` (refusal region + touch controls) | exact |
| `src/inngest/functions/didit-reconcile.ts` (F-4) | job (cron) | batch | `src/inngest/functions/payment-reconcile.ts` (whole file — the "the webhook may never arrive" precedent) | exact |
| `tests/ops/host-verification-submit.test.ts` | test (integration) | — | `tests/ops/ops-audit.test.ts:46-215` (harness) | exact |
| `tests/verification/didit-verdict.test.ts` | test (unit, table-driven) | — | `tests/ops/verification-port.test.ts` | exact |
| `tests/verification/didit-webhook.test.ts` | test (integration) | — | `tests/paymongo/webhook-payment-paid.test.ts:30-230` | exact |
| `tests/ops/host-contact-reveal.test.ts` | test (integration) | — | `tests/ops/ops-audit.test.ts` + `tests/ops/reject-reason.test.ts:299-325` (the D-72 serialised-meta case) | exact |
| `e2e/host-verification.spec.ts` | test (e2e) | — | `e2e/axe-sweep.spec.ts` / `e2e/overflow-320.spec.ts` (⚠ PROJECT D-24 — not in CI) | role-match |

### MODIFY — mapped to the convention each must PRESERVE (not to an analog)

| Modified file | Role | The convention the edit must not break |
|---|---|---|
| `src/lib/verification/port.ts` | service (port) | ⚠ **Directive-free heading** — `verification-port.test.ts` case 13 reads comment-stripped source and asserts the guard specifier appears ZERO times. Widen `VerificationProvider.verify` (`:118-121`) to `VerificationResult \| Promise<VerificationResult>`; add ONE `await` in `runVerification` (`:167-174`); registry (`:129-131`) becomes two rows. `isVerified` (`:181-183`) stays synchronous. **Do not spell the specifier in any new comment here.** |
| `src/lib/host/verification-status.ts` | service (read) | Additive field only: `readonly updatedAt: Date \| null` on `HostVerificationState` (`:44-51`) + `hostVerification.updatedAt` in the `.select()` (`:67`). All four readers keep compiling. Write the reason in the header — the displayed retry instant must derive from the same column the server's `WHERE` reads. ⚠ Header rule stands: **no row means `unverified`, never verified, never suspended.** |
| `src/app/actions/listing.ts` | server action | `createDraftListing` at **`:91`**. The refusal fits the shipped `ListingResult` (`:61-63`) — no type change. `saveListingStep` stays UNGATED (D-270). D-231's `materialEdit` at `:283-287` gains `titleChanged`/`descriptionChanged` via the existing `changed()` helper (`:249-250`). |
| `src/app/(host)/host/listings/new/page.tsx` | page (RSC) | The whole file is 28 lines and every line is a gate or a redirect. Insert the `loadHostVerification` read + `redirect("/host/verify")` **between** the `canHost` check and the `createDraftListing()` call. Say at the site, in `listing.ts`'s own security-header words, that **the page read is a HINT and the action is the GATE**. Keep the `!res.ok` bounce (recorded as an unfixed blind spot in the UI-SPEC). |
| `src/lib/ops/review-queue.ts` | service (SQL) | ⚠ **DO NOT WIDEN THE PROJECTIONS** (FINDING F-6). Both branches already carry the id the reveal needs — `hv.user_id AS "userId"` (`:234`) and `l.host_id AS "hostId"` (`:273`). ⚠ Header `:24-26`: a certain table's NAME appears ZERO times in this file — **do not name it in any new comment.** |
| `src/components/ops/ops-queue-row.tsx` | component (client island) | One new `<Fact term="Contact">` inside the existing `<dl>` (`:277`). Reuse `ROW_TERM_CLASS` (`:97`) and `ROW_VALUE_CLASS` (`:91`) — **no `tabular-nums` on a phone.** Amend the terminal-row header (`:11-21`) naming D-271 in the same commit as the test exemption. |
| `src/lib/payments/fees.ts` | config (money) | `:82` `false → true`; the ~35-line docblock `:47-81` **inverted and dated, never deleted**. Keep the A-LITERAL-NOT-`process.env` paragraph and the "absent from `.env.example`" clause verbatim. `src/lib/ops/cancel-impact.ts`'s `opsRefundBasisCents` comment gets the same treatment. |
| `src/lib/listing/re-review.ts` | utility (shared write helper) | ⚠ **Do not type the fifth `listing_review_state` literal into this file** — `18-06-PLAN.md:139` asserts `grep -c` returns 0, and there is no live Vitest test, which makes it *more* dangerous. `MATERIAL_FIELDS` grows to seven; rewrite the `:84-95` docblock to record D-231's promotion and its date, following the instruction that docblock already gives. ⚠ Stays SHARED; `booking.ts`'s re-statements stay DUPLICATED (D-227). |
| `src/lib/listing/review-signal.ts` | config (copy) | The copy owner. Add the frozen-session sentence beside `SUSPENDED_HOST_SIGNAL` (`:202-206`). ⚠ `wayOut: null` is a decision recorded in the data — **do not name what unfreezes a suspension.** `appendOperatorSentence` (`:227-230`) is the null-note fallback pattern the vendor sentence reuses. |
| `src/app/(host)/host/earnings/page.tsx` | page (RSC) | ⚠ `earnings-freeze.test.ts`'s `FROZEN` map freezes **every host-readable string literal** in this scope. The page may gain **an import specifier and a JSX attribute only** — ZERO new string literals. The `:167` comment claims the freeze gate is unmodified and green; that claim must survive. |
| `src/components/host/hosting-paused-notice.tsx` | component | One optional prop, one conditional `<p>`. Not a second panel. ⚠ `:31-34` records that this file is grep-scanned for banned phrasings and that the names are deliberately not quoted there. |
| `src/components/host/host-signals.tsx` | component | A fourth conditional advisory on the shipped `PanelCard tone="muted"` + sentence + inline underlined `<Link>` shape (`:109-119`). Rendered for `unverified \| pending \| rejected` only. |
| `src/app/(host)/host/listings/[id]/edit/wizard.tsx` | component (client) | `resendVerification` (`:682-692`) is EXTRACTED, and the wizard's local function becomes a call into the extracted module. **The two toast strings must end up with exactly one owner.** Checklist row shape at `:779-784`. |
| `tests/design/ops-guard-coverage.test.ts` | test (design gate, build-blocking) | `EXPECTED_OPS_ACTIONS` **6 → 7** (`:119`) **and its docblock**. `EXPECTED_OPS_PAGES` stays 1 (`:122`). `EXTRA_OPS_ACTIONS` (`:116-118`) unchanged. |
| `tests/ops/ops-queue-row.test.tsx` | test (component) | Two **documented exemptions, never deletions** — assertion 1 (`:190`), assertion 2 (`:213`). Guard-the-guard (`:206`) untouched. `DOCUMENT_WORDS` untouched. |
| `tests/design/site-contacts.test.ts` | test (design gate, build-blocking) | A new `EXCLUDED_MAILTO` map in the shipped `EXCLUDED_ADDRESSES` shape (`:316-324`) — keyed `file — literal`, **reason mandatory**, plus a guard-the-guard. The non-null branch untouched. |
| `tests/payments/ops-cancel.test.ts` · `tests/listing/material-edit.test.ts` | test (integration) | Each contains its own change instruction in a failure message ("swap cases 1 and 2", "UPDATE rather than delete"). ⚠ **Read the message before changing the assertion.** Do not delete `withFlippedConstant`. |

---

## Pattern Assignments

### 1. `src/app/actions/host-verification.ts` (server action · host · request-response + CRUD) — **NEW**

**Analogs:** `src/app/actions/capability.ts` (the whole file is the HOST-action shape) ·
`src/app/actions/ops-review.ts:354-441` (the guarded flip + both audit branches).

⚠ **This is deliberately NOT an `ops-*` file.** `tests/design/ops-guard-coverage.test.ts:362-373` globs
`src/app/actions` for `ops-*.ts`; `host-verification.ts` does not match, so the guard census does not
police it — **and must not be made to.** It is session-gated, not staff-gated.

**Module opening + imports** — `capability.ts:1-51`, the exact import set a host action needs:

```typescript
"use server";

import { eq } from "drizzle-orm";
import { headers } from "next/headers";
import { auth } from "@/lib/auth";
import { db } from "@/lib/db";
import { user } from "@/lib/db/schema";
import { rateLimit } from "@/lib/rate-limit";
import { recordAudit } from "@/lib/audit";

export type CapabilityResult =
  | { ok: true; redirectTo: string }
  | { ok: false; error: string };

/** Resolve the signed-in user's id, or null if there is no session. */
async function requireUserId(): Promise<string | null> {
  const session = await auth.api.getSession({ headers: await headers() });
  return session?.user?.id ?? null;
}

// WR-06: privileged capability flips are bounded to 5 attempts per 60s per AUTHENTICATED identity
// (mirrors the Better-Auth credential-endpoint budget in src/lib/auth.ts). Keyed on the user id —
// never IP — because these are session-gated escalations. Hosting + booking share the budget.
const ACTIVATE_RATE_LIMIT = { window: 60, max: 5 } as const;
```

⚠ `requireUserId` is **copied, not imported** — `listing.ts:65` does the same and says so in its own
comment (*"copied from capability.ts"*). Follow that; do not extract a fourth shared helper.

**The burst guard, with the audit row on the deny branch** — `capability.ts:63-73`, verbatim:

```typescript
  // WR-06: bound the privileged escalation per identity; audit the denial so it is non-repudiable.
  const limit = rateLimit(`activate:${userId}`, ACTIVATE_RATE_LIMIT);
  if (!limit.ok) {
    await recordAudit({
      actorId: userId,
      action: "activateHosting",
      outcome: "denied",
      meta: { reason: "rate_limit", retryAfter: limit.retryAfter },
    });
    return { ok: false, error: "Too many attempts. Please try again in a moment." };
  }
```

⚠ **The UI-SPEC pins that refusal sentence verbatim** — *"Too many attempts. Please try again in a
moment."* Reuse the string; do not rewrite it.

⚠ **The limiter is PROCESS-LOCAL.** `src/lib/rate-limit.ts:10-15`: *"the counter store is a single
module-level Map — correct for the single-region / single-instance launch."* And `:29-32`:
*"under sustained pressure a still-LIVE key can be evicted and get a fresh window."* **Say at the site
that this is the burst guard and the 24h predicate in the UPDATE's WHERE is the durable cap** (D-264).

**The allow-branch audit row** — `capability.ts:76-77`:

```typescript
  // WR-06: record the successful escalation (actorId + action + outcome).
  await recordAudit({ actorId: userId, action: "activateHosting", outcome: "ok" });
```

**The guarded UPDATE + the 0-row branch + the audit on BOTH sides** — `ops-review.ts:396-428`. This
is the shape the submission upsert copies, including the ISO-string cast:

```typescript
  // ⚠ MEASURED (2026-09-01): a JS `Date` CANNOT be bound as a parameter through
  // a raw `db.execute` statement on postgres.js — the raw-parameter path answers
  // `ERR_INVALID_ARG_TYPE: ... Received an instance of Date` at Bind time … So the
  // instant is bound as an ISO-8601 string with an explicit cast, which Postgres parses exactly.
  const flipped = (await db.execute(sql`
    UPDATE host_verification
    SET status = 'approved',
        provider = ${check.provider},
        result = ${check.result},
        checked_at = ${check.checkedAt?.toISOString() ?? null}::timestamptz,
        reason = NULL,
        decided_by_staff_id = ${staff.id},
        updated_at = now()
    WHERE user_id = ${parsed.data.userId}
      AND status IN ('pending', 'unverified')
    RETURNING user_id
  `)) as unknown as { user_id: string }[];

  if (flipped.length === 0) {
    await recordAudit({
      actorId: staff.id,
      action: "ops_approve_host",
      outcome: "denied",
      meta: {
        reason: "not_applicable" satisfies DenialReason,
        userId: parsed.data.userId,
      },
    });
    return STALE;
  }

  await recordAudit({
    actorId: staff.id,
    action: "ops_approve_host",
    outcome: "ok",
    meta: { userId: parsed.data.userId, provider: check.provider, result: check.result },
  });
```

**Three things to carry across verbatim:**

1. `updated_at = now()` is **spelled explicitly** even though `schema.ts:432-435` declares
   `$onUpdate(() => new Date())`. `$onUpdate` fires only through Drizzle's query **builder**. FINDING
   F-1 requires **`created_at = now()` as well** on the resubmission branch, for the same reason.
2. The `as unknown as { user_id: string }[]` cast on `db.execute` — the shipped raw-SQL idiom.
3. `satisfies DenialReason` on the meta's `reason`. Declare a local `DenialReason` union in the new
   module the same way (`ops-review.ts:159-165`), so a typo in a denial reason is a compile error.

**The Zod validation** — the phone crosses the boundary, so it is re-parsed server-side.
`src/lib/validation/ops.ts:57-100` is the shape; the bound argument is at `:58-61`:

```typescript
/**
 * A hard bound on any id crossing this boundary. FitOut ids are app-generated UUIDs (36 chars) or
 * Better Auth ids; 128 is comfortably above every real one. The bound is not cosmetic: an unbounded
 * id becomes a rate-limiter key and an audit-meta value, and `src/lib/rate-limit.ts:36-40` records
 * why a caller-chosen unbounded key space is memory exhaustion against the whole process.
 */
const ID_MAX = 128;
const id = () => z.string().min(1).max(ID_MAX);
```

Per research: a **permissive** phone bound (7–20 chars, digits/`+`/spaces/hyphens), not E.164.

⚠ **D-72 on this action's audit meta:** `user.phone` and `user.email` may NEVER appear in `audit.meta`.
The submission's rows record `{ userId }` plus enum-shaped denial reasons and nothing else.

---

### 2. `src/lib/verification/providers/didit.ts` (service · provider adapter · outbound HTTP) — **NEW**

**Analog A — `src/lib/verification/providers/manual.ts`, the whole file.** Copy its structure line for
line: the guard on line 1, the four-section header, the one-constant registry/persisted-name rule, the
`'migration'` note, and the docblock on the adapter object.

**The guard, and where it sits** — `manual.ts:1`, the FIRST line of the file, before every comment:

```typescript
import "server-only";
```

Its argument — `manual.ts:34-46`, reproduce it for the Didit adapter (which additionally holds an API
key, making the case stronger):

```
// ════════════════════════════════════════════════════════════════════════════════════════════════
// WHY THE GUARD IS ON THIS FILE
// ════════════════════════════════════════════════════════════════════════════════════════════════
// `src/lib/payments/fees.ts:1-30`'s rule: guard the module a client must never REACH, not the module
// a client legitimately IMPORTS. This is a WRITE path for a privileged, money-adjacent decision;
// nothing in a browser bundle has any business reaching it. The port that imports it inherits the
// guard by transitivity, so a client component that pulls a VALUE out of the port fails `next build`
// naming THIS file — the correct file to name.
//
// NO PACKAGE WAS INSTALLED. Next aliases the `server-only` specifier (node_modules/next/types/
// global.d.ts:57) and both Vitest configs alias it to tests/helpers/server-only.stub.ts, so this
// costs the suite nothing. `npm install server-only` is a recorded-decision reversal (D-34 /
// GATE-05), not a fix.
```

**The one-constant rule** — `manual.ts:54-63`:

```typescript
/**
 * The registry key AND the value written to `host_verification.provider`. One constant, so the
 * registered name and the persisted name cannot drift — a drift would leave rows attributed to a
 * provider the port can no longer resolve, i.e. rows nobody can explain.
 *
 * ⚠ `'migration'` is the OTHER provider string in this system (drizzle/0026's grandfather rows) and
 * it is deliberately NOT registered here: nothing was checked on those rows, so there is no adapter
 * that could have produced them and no code path that may re-run one.
 */
export const MANUAL_PROVIDER_NAME = "manual";
```

**The adapter object** — `manual.ts:78-90`. Four fields, nothing else:

```typescript
export const manualVerificationProvider: VerificationProvider = {
  name: MANUAL_PROVIDER_NAME,
  verify(decision: VerificationDecision): VerificationResult {
    return {
      result: decision.result,
      // No vendor ⇒ no reference. See the header: a synthetic id here would be a lie in a
      // compliance column.
      vendorRef: null,
      checkedAt: new Date(),
      provider: MANUAL_PROVIDER_NAME,
    };
  },
};
```

**Analog B — `src/lib/paymongo.ts:1-90`, the thin `fetch` wrapper + prod boot guard.** CLAUDE.md
prescribes this pattern for a vendor with no official SDK; the research says apply it verbatim to Didit.

Header (`paymongo.ts:1-17`) — the four claims to restate for Didit:

```
// PayMongo REST client — a thin `fetch` wrapper (there is NO official PayMongo SDK; CLAUDE.md § D-20).
//
// This module is the ONLY place that talks to PayMongo over HTTP. It is deliberately tiny: HTTP Basic
// auth …, a JSON `fetch` helper that sends an `Idempotency-Key` on POSTs and throws on non-2xx, and the
// two Platforms / Linked-Accounts calls the payout-onboarding action needs …. The webhook route does
// its own node-crypto signature verification and does NOT import this client.
//
// FAIL-CLOSED (copied from the src/lib/auth.ts WR-03 boot guard): in PRODUCTION we refuse to boot
// without PAYMONGO_SECRET_KEY rather than silently make unauthenticated calls. In dev/test/build the
// placeholder in .env is tolerated so local setup, the fully-mocked test suite, and `next build` all
// pass — only a real production boot is keyless-fatal.
```

The boot guard itself (`paymongo.ts:26-32`) — copy the shape for `DIDIT_API_KEY` / `DIDIT_WORKFLOW_ID`:

```typescript
// Fail-closed at module load — production only (dev/test/build tolerate the .env placeholder).
if (!process.env.PAYMONGO_SECRET_KEY && process.env.NODE_ENV === "production") {
  throw new Error(
    "PAYMONGO_SECRET_KEY is not set. Refusing to boot in production without the PayMongo secret key. " +
      "Set it in the deploy environment (it is the HTTP-Basic username for every PayMongo API call).",
  );
}
```

⚠ Note the `NEXT_PHASE !== "phase-production-build"` escape used on the second guard (`paymongo.ts:41`)
— required for any guard that would otherwise fire during `next build`.

The versioned-path rule (`paymongo.ts:21-24`) applies directly: Didit's base is
`https://verification.didit.me` and every caller passes a **fully versioned** `/v3/...` path.

**What the Didit adapter must NOT do** (`port.ts:29-34, 94-96`): no `provider === "didit"` branch
anywhere; no fifth field on `VerificationResult`; no hand-built result literal outside a provider.

---

### 3. `src/lib/verification/didit-verdict.ts` (utility · pure mapper · transform) — **NEW**

**There is no shipped module that is exactly "a total map over an external vocabulary with an explicit
unknown branch."** The closest three each contribute one half, and the plan should compose them.

**(a) The single-branch-point + fail-closed + pure/isomorphic module** —
`src/lib/payments/refund-rail.ts`, the whole file (54 lines). This is the structural analog:

```typescript
// This module remains the ONLY branch point for refund dispatch. Do NOT inline this predicate anywhere.
//
// Pure/isomorphic: no "use client"/"use server" directive — the webhook route, the cancel server action and
// the refund-preview RSC all import it.

/** Payment rails PayMongo can API-refund (Pitfall 1). QRPh + UBP Online Banking are NOT refundable via the
 *  API — the gone-slot backstop must operator-alert those, never call createRefund (it would 4xx). */
export const REFUNDABLE_RAILS: ReadonlySet<string> = new Set([
  "card",
  "gcash",
  "grab_pay",
  "paymaya",
]);

/** Can PayMongo refund this rail through the API? Fails CLOSED: an absent/unknown rail is treated as
 *  non-refundable, so an unrecognised payment method routes to the operator-alert path rather than to a
 *  call that would 4xx and leave a booker's money in limbo. */
export function isApiRefundable(rail: string | null | undefined): boolean {
  return rail != null && REFUNDABLE_RAILS.has(rail);
}
```

**This IS the `DIDIT_SAFE_RISKS` allow-list shape.** Declared, not inferred; fails closed on anything
unknown; pure, so the webhook AND the Inngest sweep import the same module (D-105's "never a second
road", one domain over). Its header is also the precedent for **stating the observed evidence verbatim**
— `refund-rail.ts:9-20` quotes the raw API response that settled the verdict. Do the same with the
captured Didit sandbox payloads.

**(b) The total `Record` over a union, so a new member is a compile error** —
`src/components/ops/ops-queue-row.tsx:170-186`:

```typescript
/**
 * THE OTHER TERM OF THE SELL-GATE, IN WORDS AN OPERATOR CAN ACT ON.
 *
 * A total `Record` over the pgEnum-derived union, so a seventh status is a compile error here rather
 * than a blank cell in front of somebody deciding whether approving a listing actually makes it
 * sellable. …
 */
const HOST_VERIFICATION_LABEL: Record<HostVerificationStatus, string> = {
  unverified: "not checked yet",
  pending: "waiting on a decision",
  approved: "checked",
  rejected: "not approved",
  grandfathered: "never checked",
  suspended: "hosting paused",
};
```

⚠ **Didit's ten statuses are NOT a TypeScript union derived from a pgEnum** — they are strings from a
vendor. So `tsc` cannot enumerate them for you. The mapper needs a **declared** `const` tuple of the ten
plus an **explicit unknown branch that leaves the row `pending` and writes an audit row** (Pitfall 1),
pinned by a table-driven test over all ten values **in both casings**.

**(c) The own-property lookup that makes an allow-list actually fail closed** — `port.ts:141-152`:

```typescript
/**
 * `Object.hasOwn` IS LOAD-BEARING AND NOT DEFENSIVE NOISE. A bare `PROVIDERS[name]` on an object
 * literal answers TRUTHILY for `"constructor"`, `"toString"`, `"valueOf"` and `"__proto__"` —
 * inherited `Object.prototype` members — so a caller-influenced provider name could resolve to a
 * function that is not a provider at all. The own-property check is the difference between a
 * fail-closed lookup and one that fails closed only for names nobody thought to try.
 */
export function resolveVerificationProvider(
  name: string | null | undefined,
): VerificationProvider | null {
  if (name == null || name === "") return null;
  if (!Object.hasOwn(PROVIDERS, name)) return null;
  return PROVIDERS[name];
}
```

⚠ **`warning.risk` is vendor-supplied and reaches an object lookup.** Use `Object.hasOwn` on
`DIDIT_SAFE_RISKS`, or a `ReadonlySet` (which has no prototype-key hazard at all — the `refund-rail.ts`
choice, and the simpler one).

**(d) The defensive normaliser precedent for the casing hazard** — `review-queue.ts:196-218`. The
research explicitly calls the `"Approved"` / `"APPROVED"` split *"the same class of defect"*:

```typescript
/**
 * … The Postgres wire form is not strict ISO 8601 … V8 happens to parse it through its LENIENT
 * non-standard path, and relying on that is exactly the kind of dependency that breaks on a runtime
 * upgrade with no test to catch it — so the value is normalised to strict ISO first and the lenient
 * parse is kept only as a fallback for a shape this normaliser does not recognise.
 */
function toDate(value: Date | string): Date {
  if (value instanceof Date) return value;
  const iso = value.replace(" ", "T").replace(/([+-]\d{2})$/, "$1:00");
  const parsed = new Date(iso);
  return Number.isNaN(parsed.getTime()) ? new Date(value) : parsed;
}
```

**(e) The rejection sentence composer already exists — do not write a new one.**
`src/lib/validation/ops.ts:194-207`:

```typescript
/**
 * THE VALUE THAT GOES IN THE DURABLE COLUMN — the taxonomy sentence, plus the operator's note when
 * there is one, as ONE readable string.
 *
 * One function, so the host-facing surface, the notification and any later reader all read the same
 * bytes. Nothing is escaped or stripped (header point 4); the note is trimmed only because trailing
 * whitespace in a sentence a person reads is noise, never meaning.
 */
export function composeReason(reason: string, noteText?: string | null): string {
  const trimmed = noteText?.trim() ?? "";
  return trimmed.length > 0 ? `${reason} ${trimmed}` : reason;
}
```

with `REJECT_NOTE_MAX = 280` (`validation/ops.ts:65`) and
`HOST_REJECT_REASONS[0] = "We couldn't confirm who this account belongs to."` (`validation/ops.ts:85`).
**That constant IS D-265's canned fallback.** `composeReason(HOST_REJECT_REASONS[0], "")` returns it
alone — the identical behaviour `appendOperatorSentence` gives for a null note
(`review-signal.ts:227-230`).

⚠ `validation/ops.ts:38-40` states the no-half-sanitiser rule that governs the vendor string too:
*"A half-sanitiser is worse than none, because it reads like the problem was handled."* Bound it and
allow-list it; do not "clean" it.

---

### 4. `src/app/api/didit/webhook/route.ts` (route handler · event-driven · untrusted-in) — **NEW**

**Analog: `src/app/api/paymongo/webhook/route.ts` — the whole file, property for property.** This is
the single most important analog in the phase. Every excerpt below is verbatim.

**(a) Runtime declaration, with its reason** — `:31-32`:

```typescript
// Signature verification needs node crypto + the RAW request body — this MUST be the Node runtime, not edge.
export const runtime = "nodejs";
```

**(b) The middleware note** — `:9-11`. Restate it for `/api/didit`:

```
// NOTE (verified, no code change): src/middleware.ts matches ONLY /login and /signup — it does NOT
// touch /api/paymongo, so this endpoint is reachable by PayMongo unauthenticated (correct — PayMongo
// is the caller, and the Paymongo-Signature is the authentication).
```

**(c) The fail-closed production boot guard** — `:34-44`. Replicate for `DIDIT_WEBHOOK_SECRET`:

```typescript
// Fail-closed prod boot guard (WR-03, mirrors src/lib/paymongo.ts + src/app/api/inngest/route.ts): the
// webhook is the SOLE booking-confirm authority (D-57), so a missing/rotated-out secret would silently make
// verifySignature fail-closed and 400 EVERY delivery — bookers charged, no booking ever confirms, with no
// startup signal. Refuse to boot in production without the secret so a misconfigured deploy is a loud boot
// FAILURE, not a silent confirmation outage. dev/test/build tolerate its absence (the mocked webhook suite
// sets it per-test and `next build` must not require prod secrets).
if (process.env.NODE_ENV === "production" && !process.env.PAYMONGO_WEBHOOK_SECRET) {
  throw new Error(
    "PAYMONGO_WEBHOOK_SECRET is required in production — the webhook is the sole booking-confirm authority (D-57).",
  );
}
```

**(d) The length-guarded constant-time compare** — `:102-117`. **This is the `RangeError` trap already
solved. Copy the guard, not just the call:**

```typescript
/**
 * Constant-time verify the signature over `${t}.${rawBody}` against BOTH the test-mode (te) and
 * live-mode (li) digests. GUARD: timingSafeEqual THROWS a RangeError on unequal-length buffers, so we
 * compare byte lengths first — a length mismatch is simply "not a match", never a thrown 500.
 */
function verifySignature(rawBody: string, sig: SigParts, secret: string): boolean {
  const expected = createHmac("sha256", secret).update(`${sig.t}.${rawBody}`).digest("hex");
  const expectedBuf = Buffer.from(expected, "utf8");
  for (const candidate of [sig.te, sig.li]) {
    const candBuf = Buffer.from(candidate, "utf8");
    if (candBuf.length === expectedBuf.length && timingSafeEqual(candBuf, expectedBuf)) {
      return true;
    }
  }
  return false;
}
```

⚠ **The signed message differs.** PayMongo signs `${t}.${rawBody}`; Didit's `X-Signature` is an
HMAC-SHA256 hex digest over the **raw bytes alone**. Change the `.update(...)` argument and say so at the
site — the loop-over-candidates and the length guard stay.

Imports (`:24`): `import { createHmac, timingSafeEqual } from "node:crypto";` — **no package.**

**(e) The header parser's tolerance rule** — `:84-100`. The Didit analogue is
*primary `X-Signature`, fallback `X-Signature-V2`, never `X-Signature-Simple`*, plus `X-Timestamp`:

```typescript
/**
 * Parse `t=<ts>,te=<testSig>,li=<liveSig>` into its parts. PayMongo signs ONE mode per delivery: `te` in
 * TEST mode (empty `li`) and `li` in LIVE mode (empty `te`) — NEVER both. So we tolerate one empty part and
 * reject only a missing header, a malformed segment (no `=`), a missing `t`, or a header with BOTH
 * signatures empty. verifySignature already length-skips an empty candidate, so an empty te/li never matches.
 */
```

⚠ Project memory records the te-XOR-li gotcha that 400'd every confirm webhook in local UAT. Didit's
`X-Timestamp` ±300s window is a **strengthening PayMongo has no equivalent of** — document it as a
deliberate divergence, and pin it with a `>300s` test case.

**(f) The POST body — the ordering IS the security property** — `:226-264`, verbatim:

```typescript
export async function POST(req: Request): Promise<Response> {
  // RAW body — do NOT req.json() first: parsing changes the bytes the HMAC covers (T-06-SPOOF).
  const rawBody = await req.text();
  const secret = process.env.PAYMONGO_WEBHOOK_SECRET ?? "";

  // --- Verify the Paymongo-Signature. ANY failure (missing header/part, length mismatch, content
  //     mismatch, or a crypto throw) → a clean 400. Never an uncaught 500 on a forged/tampered sig. ---
  let verified = false;
  try {
    const sig = parseSignature(req.headers.get("paymongo-signature"));
    if (sig && secret) verified = verifySignature(rawBody, sig, secret);
  } catch {
    verified = false;
  }
  if (!verified) {
    return new Response("Invalid signature", { status: 400 });
  }

  // --- Parse the now-verified body. ---
  let event: PayMongoEvent;
  try {
    event = JSON.parse(rawBody) as PayMongoEvent;
  } catch {
    return new Response("Invalid payload", { status: 400 });
  }
  const eventId = event.data?.id;
  const type = event.data?.attributes?.type;
  if (!eventId || !type) {
    return new Response("Invalid payload", { status: 400 });
  }

  // --- Idempotency (T-06-REPLAY): PayMongo retries + may double-deliver. A seen event id → 200 skip. ---
  const seen = await db
    .select({ id: paymongoEvent.id })
    .from(paymongoEvent)
    .where(eq(paymongoEvent.id, eventId));
  if (seen.length > 0) {
    return new Response("ok", { status: 200 });
  }
```

**(g) State derived from the VERIFIED type, never a body field** — `:266-268`:

```
  // --- Apply the state transition. The new state is DERIVED from the verified event TYPE, never from a
  //     client-supplied body field (T-05-18 / T-06-PRIV). This route is the single writer of both
  //     host_payout.payoutsEnabled (Phase 2) and booking→confirmed on payment (D-57). ---
```

**(h) The always-ACK rule** — `:286-292` + `:317-319`:

```
      // The outcome is DISCARDED at this call site, deliberately. The webhook's behaviour is unchanged by
      // design (it ACKs 200 either way — a rejected ACK makes PayMongo retry an already-handled event
      // forever, T-06-15) …
```

```typescript
  // --- Mark processed (idempotency ledger) and ACK 200 so PayMongo stops retrying handled events. ---
  await db.insert(paymongoEvent).values({ id: eventId, type }).onConflictDoNothing();
  return new Response("ok", { status: 200 });
}
```

⚠ **Divergence to make deliberately and document (research 18.1-07):** skip the `didit_event` ledger
table; the guarded `UPDATE … WHERE status = 'pending'` **is** the idempotency, because a re-delivery
flips 0 rows. **Return 200 on the 0-row path** — a non-2xx burns one of Didit's only two retries.

**(i) The optional-chained resource type + the "asserted against a captured test event" rule** — `:48-82`:

```typescript
/**
 * The nested resource under `event.data.attributes.data`. … All fields optional-chained (A1 — the exact
 * nesting is asserted against a captured test event in tests/paymongo/webhook-payment-paid.test.ts).
 */
```

⚠ **Pitfall 2:** Didit V3 returns **plural arrays** (`id_verifications[]`, `liveness_checks[]`).
Type them as arrays. `response.id_verification.status` is `undefined`, which fails closed and rejects
*every* host silently.

**(j) The "never a second road" rule this route inherits** — `:20-22`:

```
// ⚠ ADDING A SECOND CONFIRM SITE — here or anywhere — IS EXACTLY WHAT D-105 FORBIDS. Two roads to
// `confirmed` is how one payment produces two payout rows and two emails. Widen `confirmPaidBooking`
// instead; never write another `UPDATE booking SET status = 'confirmed'`.
```

**Applied one domain over:** the webhook and the F-4 sweep must both go through
`didit-verdict.ts` + ONE write module. Never two `UPDATE host_verification SET status='approved'`.

---

### 5. `src/app/(host)/host/verify/page.tsx` + `loading.tsx` (page RSC + plate) — **NEW**

**Analog — `src/app/(host)/host/earnings/page.tsx:154-181`.** The shipped shape for reading
verification state and rendering the suspended branch first:

```typescript
  // D-252 — THE MISSING EXPLANATION ON THE ONE PAGE A HOST GOES LOOKING FOR IT.
  // … Owner-scoped, read once, from the same authority `/host` and `/host/listings`
  // read so the three surfaces cannot disagree.
  const verification = await loadHostVerification(db, session.user.id);

  return (
    <div className={HOST_LIST_SHELL}>
      <PageHeader title="Earnings" />

      {verification.suspended ? (
        <div className="mt-6">
          <HostingPausedNotice reason={verification.reason} />
        </div>
      ) : null}
```

⚠ `/host/verify` uses **`HOST_PANEL_SHELL`** (`measurements.ts:479`), not `HOST_LIST_SHELL` — it is a
single-panel surface (UI-SPEC § Spacing). ⚠ **The page component takes NO `searchParams` prop and the
file contains zero occurrences of `searchParams`** (UI-SPEC criterion 3): Didit's `callback` redirect
carries no authority, and the page re-reads the row.

**Analog — `src/app/(host)/host/earnings/loading.tsx`, the whole file.** The plate composes exactly
one declared skeleton, reads the shell constant rather than retyping it, and writes **no box class of
its own**:

```typescript
import { HOST_LIST_SHELL } from "@/lib/design/measurements";
import { PageHeader } from "@/components/patterns/page-header";
import { RowListSkeleton } from "@/components/patterns/row-list-skeleton";

export default function HostEarningsLoading() {
  return (
    // The container is the DECLARED host-list shell, and the `mt-8` data-region offset is
    // `(host)/host/earnings/page.tsx`'s own. Until now this plate agreed with its page about the
    // container only because the same string was typed in both files; the two read one constant now,
    // so the fallback cannot draw a different box than the page it stands in for.
    <div className={HOST_LIST_SHELL}>
      <PageHeader title="Earnings" />

      <div className="mt-8">
        <RowListSkeleton label="Loading your earnings" />
      </div>
    </div>
  );
}
```

Swap `RowListSkeleton` → `PanelSkeleton label="Loading your account check"` and `HOST_LIST_SHELL` →
`HOST_PANEL_SHELL` per the UI-SPEC. **The `PageHeader` strings must be the same expressions the page
carries, character for character.**

⚠ `(host)/host/loading.tsx:52-58` records the discipline: *"the count is measured here rather than
remembered, and `tests/design/loading-coverage.test.ts` is what keeps the property true."*

---

### 6. `src/components/host/verification-panel.tsx` (component · presentational) — **NEW**

**Analog: `src/components/host/hosting-paused-notice.tsx` — the whole file, 77 lines.** Copy the
header's argument structure as well as the JSX.

**The four-part header** (`:1-52`) — the sections to reproduce: *why one component and not three
paragraphs*, *the tone and what is deliberately absent*, *the operator's sentence crosses a trust
boundary here*, *not "use client"*. Two paragraphs to lift almost verbatim:

```
// It renders on THREE surfaces … and what a suspended host is told must be identical on all three. A
// sentence typed into three page files is the shape `requests-signal.ts`'s header describes by name:
// the badge starts calling them one thing, the row another, and nothing goes red when they diverge.
// The WORDS live in `src/lib/listing/review-signal.ts`; this file is the one place that decides how
// they are presented.
```

```
// `host_verification.reason` is FREE TEXT an operator typed, rendered on a surface the host controls
// nothing about. It arrives already joined to the product's sentence by the shared composer and is
// interpolated into a JSX expression container — a React text node, escaped by construction. There is
// no [raw-HTML escape hatch] on this path, and there must never be one.
```

⚠ **T-18-1301 now covers a VENDOR's sentence as well as an operator's.** Same path, same guarantee.
⚠ Do not spell the raw-HTML property name in any new comment under `src/` — `reject-reason.test.ts`
case 10 counts its occurrences over comment-stripped source.

**The JSX** (`:59-77`):

```tsx
export function HostingPausedNotice({
  reason,
}: {
  /**
   * `host_verification.reason` — the operator's own sentence, read owner-scoped by the page from
   * `loadHostVerification`. Null-safe: a suspension recorded without a note renders the product's
   * sentence alone rather than a dangling separator.
   */
  reason: string | null;
}) {
  return (
    // The STATE is the panel's own heading, at the level every host surface's outline wants: each of
    // the three pages is an `h1` and then this. `titleAs` is left at the pattern's default rather than
    // passed, because all three call sites agree — and a prop that is the same everywhere is a prop
    // that will eventually be passed wrong somewhere.
    <PanelCard tone="muted" title={SUSPENDED_HOST_SIGNAL.state}>
      <p className="text-body text-muted-foreground" data-hosting-paused>
        {composeSuspendedSentence(reason)}
      </p>
    </PanelCard>
  );
}
```

⚠ The `suspended` branch of `/host/verify` **imports and renders this component unchanged** — it does
not re-author the words (UI-SPEC § The six states).

**The refusal region** — `src/components/ops/ops-decision-actions.tsx:327-333`, reproduced exactly for
both new named regions (`"Check not started"` and `"Contact not shown"`):

```tsx
      {/* THE ONE REGION. Mounted only while a refusal exists, following the shipped shape, because the
          success path owes zero regions. Its name is a label; its content is the server's sentence. */}
      {refusal ? (
        <p role="status" aria-label={OPS_REFUSAL_REGION_NAME} className={ROW_VALUE_CLASS}>
          {refusal}
        </p>
      ) : null}
```

---

### 7. `src/lib/host/verification-signal.ts` (config · copy module) — **NEW**

**Analog A — `src/lib/listing/review-signal.ts:190-243`.** The signal-object shape, the `wayOut: null`
decision, and the composer:

```typescript
/**
 * ⚠ AND IT ENDS THERE. No route back, no promise of a reply, no timeline, no address (D-243 / D-250).
 * What resolves a suspension is a FitOut ops review, which this product cannot offer as a control and
 * will not offer as a promise — so the honest surface names the state and the reason and stops. That is
 * a decision taken in 18-CONTEXT and not an omission; `wayOut` is null on purpose.
 */
export const SUSPENDED_HOST_SIGNAL = {
  state: "Hosting paused",
  reason: "FitOut has paused your hosting. Your spaces can't be booked, and payouts are on hold.",
  wayOut: null,
} as const;
```

```typescript
function appendOperatorSentence(sentence: string, operatorReason: string | null | undefined): string {
  const trimmed = operatorReason?.trim() ?? "";
  return trimmed.length > 0 ? `${sentence} ${trimmed}` : sentence;
}
```

Its three-property docblock (`:208-225`) is the argument for composing in JS rather than as JSX text —
**lift it**, especially property 2 (SWC drops the leading space after an expression container; the
defect that shipped `"₱300.00in cancellation fees"`).

**Analog B — `src/lib/host/requests-signal.ts:52-75`.** The state / reason / way-out triple as three
separate exports plus a composer, and the rule the whole phase's copy answers to:

```typescript
/**
 * WHY it matters, and what happens if the host does nothing.
 *
 * Rule O7: a signal names the state, the reason AND the way out. The reason here is a mechanism with a
 * deadline, and the deadline is the shared constant — interpolated, never typed. …
 */
export const REQUESTS_WAITING_REASON =
  `They expire ${APPROVAL_SLA_HOURS} hours after they're made, and the slot frees automatically.`;

/** The WAY OUT — the link label into the inbox that owns the decision. */
export const REQUESTS_WAITING_CTA = "Review requests";
```

⚠ **The 18.1 map is TOTAL**, not partial (UI-SPEC improvement 1): a
`satisfies Record<HostVerificationStatus, VerificationSignal>` clause, so a seventh enum member fails
`tsc` until somebody decides in writing what the host reads. `review-signal.ts` splits partial +
declared-silence because three of its states render nothing on a *card*; `/host/verify` is a
**destination**, and a destination that renders nothing is a broken page.

---

### 8. `src/lib/host/frozen-payouts.ts` (service · DB read) — **NEW**

**Analog A — the loader shape: `src/lib/host/verification-status.ts:53-81`.** Injected `DbConn`,
owner-scoped **by argument**, one query, a named return type:

```typescript
/**
 * The signed-in host's own verification row — ONE query, owner-scoped BY ARGUMENT.
 *
 * Owner-scoped by an argument every call site fills from `session.user.id`, never from anything a
 * client can send: there is no id parameter to tamper with and no path where a search param reaches
 * this function, so one host can never be shown another host's decision or another host's reason
 * (T-18-1302). …
 */
export async function loadHostVerification(
  dbConn: DbConn,
  hostId: string,
): Promise<HostVerificationState> {
```

Import set (`:33-35`) — copy exactly:

```typescript
import { eq } from "drizzle-orm";
import type { DbConn } from "@/lib/availability/read-model";
import { hostVerification, type HostVerificationStatus } from "@/lib/db/schema";
```

**Analog B — the query being INVERTED: `src/inngest/functions/payout-sweep.ts:115-167`.** The frozen
sessions are exactly the rows this SELECT drops. Copy its joins and its fail-closed `COALESCE`:

```sql
    FROM booking b
    JOIN listing l ON l.id = b.listing_id
    JOIN host_payout hp ON hp.user_id = l.host_id
    -- LEFT, not INNER, and for the same reason src/lib/search/query.ts states at its own copy of this
    -- join: host_verification is 1:1 to user but is NOT created with the user, so a host with NO ROW is
    -- the common case. An INNER JOIN here would silently stop paying every un-checked host.
    LEFT JOIN host_verification hv ON hv.user_id = l.host_id
    LEFT JOIN host_payout_ledger p ON p.booking_id = b.id AND p.kind = 'payout'
    …
      AND COALESCE(hv.status::text, 'unverified') <> 'suspended'
```

⚠ `payout-sweep.ts:112-114` — *"Invariant 4 (ENF-02) lives HERE and nowhere else."* **The new module
must not re-implement the freeze**; it reports the consequence of it. Write that at the site, and say
that a divergence between the two predicates is what makes the host's sentence a lie.

⚠ **Placement is a gate requirement, not a preference** (UI-SPEC § Where the query lives): this module
lives OUTSIDE `src/app/(host)/host/earnings/**` and `src/components/host/payout*`, because
`tests/design/earnings-freeze.test.ts`'s `FROZEN` map is a per-file **equality** map over every string
literal in those scopes. **Measure that `FROZEN` did not move; if it did, move the code, not the map.**

⚠ **The sentence is composed server-side and arrives finished.** `ops-queue-row.tsx:108-114` states the
rule for the analogous figure: *"Computed against the DB clock, never `Date.now()` in a render."*

---

### 9. `src/app/actions/ops-contact.ts` (server action · ops · request-response) — **NEW**

**Analog: `src/app/actions/ops-review.ts:354-441`.** The header's numbered ordering (`:19-38`) is the
contract; clause 1 is what the census asserts:

```
//   1. THE STAFF GATE, FIRST. Before any read, before any parse, before the rate limit — so a
//      non-staff caller is refused without consuming anybody's budget and without learning whether
//      the id they guessed exists. Every action gates ITSELF (D-216): the `(ops)` route group is not
//      the gate for a MUTATION, because a Server Action is reachable by POST whatever the UI shows.
```

**Four mechanical requirements the AST census (`tests/design/ops-guard-coverage.test.ts`) enforces:**

| # | Requirement | Where it is enforced |
|---|---|---|
| 1 | Filename matches `ops-*.ts` under `src/app/actions` | `:362-373` `OPS_ACTION_FILES` |
| 2 | `export async function revealHostContact(…)` — a **function declaration**. `export const … = async () => {}` is invisible to the walk. | `:328-338` `exportedFunctions` |
| 3 | `import { requireStaff } from "@/lib/ops/staff"` — the binding is resolved **through the import**; a local same-named helper yields nothing. | `:185-197` `bindingsFor` |
| 4 | The guard is the function's **FIRST statement** — `const staff = await requireStaff();` or a bare `await requireStaff();`. | `:310-322` `guardsFirst` |

Plus: `FORBIDDEN_REFUSALS = ["forbidden", "redirect"]` (`:132`) — zero bare calls to either at any
`(ops)` call site. `requireStaff()` already `notFound()`s, and `src/lib/ops/staff.ts:101-105` says why:

```
 * A refused caller gets `notFound()` — byte-identical to what a URL nobody ever routed produces —
 * never a 403 and never a bounce to /login (D-219). A distinguishable refusal is an EXISTENCE
 * ORACLE …
```

**The named-refusal constants** — `ops-review.ts:96-120`. Declare the two UI-SPEC sentences in this
style; **do not invent a new result shape**:

```typescript
export type OpsActionResult = { ok: true } | { ok: false; error: string };

const OK: OpsActionResult = { ok: true };

const DENIED: OpsActionResult = {
  ok: false,
  error: "That decision couldn't be recorded. Reload the queue and try again.",
};

const TOO_FAST: OpsActionResult = {
  ok: false,
  error: "Too many decisions at once. Give it a moment and try again.",
};
```

⚠ The reveal RETURNS VALUES, so its result type is `{ ok: true; contact: OpsHostContact } | { ok: false; error: string }`.
Keep the refusal half byte-identical in shape.

**The D-72 audit row for the reveal.** The exact `meta` discipline — `ops-review.ts:423-428` plus
`validation/ops.ts:42-47`:

```
// ⚠ AND THE NOTE NEVER GOES INTO `audit.meta` (D-72). `meta` is a durable jsonb column under an
// explicit no-secrets/no-PII rule; an operator's free text can contain anything they typed about a
// person. The taxonomy SENTENCE is an enum value and may go there; the note stays in the domain
// column, which is where the host reads it from anyway.
```

**A real D-72-compliant `meta`, from a shipped allow-branch write** (`ops-review.ts:427`):

```typescript
    meta: { userId: parsed.data.userId, provider: check.provider, result: check.result },
```

Every value is an **id** or an **enum-shaped literal**. The reveal's row is
`{ actorId: staff.id, action: "ops_reveal_host_contact", outcome: "ok", meta: { userId } }` —
**never the email, never the phone, never an address.**

And the `AuditEntry` contract itself (`src/lib/audit.ts:32-51`):

```typescript
export interface AuditEntry {
  /** The authenticated identity performing the action (server-resolved, never client-supplied). */
  actorId: string;
  /** The privileged action name, e.g. "activateHosting". */
  action: string;
  /** Result of the privileged attempt. */
  outcome: AuditOutcome;   // "ok" | "denied" | "error" | "needs_attention"
  /**
   * Optional structured context (e.g. `{ reason: "rate_limit" }`). Must not contain secrets/PII — and
   * that rule now binds a STORED COLUMN, not only a log line: this value lands in `audit.meta`, a durable
   * jsonb column (D-72: "not in this audit meta, not in any log line, not in any column").
   */
  meta?: Record<string, unknown>;
}
```

**The explicit-columns projection style the `OpsHostContact` type must adopt** —
`src/lib/ops/review-queue.ts:29-34`:

```
// Every column is named EXPLICITLY. There is no `select()` over a whole table anywhere in this file,
// for the reason `listUnresolvedAlerts` states in one line: because the row TYPE has no field for a
// column, re-exporting it is a TYPE ERROR at every consumer rather than something a reviewer has to
// catch.
```

---

### 10. `src/components/ops/ops-contact-reveal.tsx` (component · client island) — **NEW**

**Analogs:** `ops-decision-actions.tsx:315-333` (touch controls + the one refusal region — excerpted in
§ 6) · `ops-queue-row.tsx:190-200` (the "Not provided" precedent) · `:280` (the `<Fact>` call site).

**The `addressOf()` precedent D-271 copies for a missing phone** — `ops-queue-row.tsx:189-203`:

```typescript
/** The listing's street address, assembled from the parts that are actually present. */
function addressOf(row: OpsQueueListingRow): string {
  const parts = [ … ].filter((part): part is string => typeof part === "string" && part.trim().length > 0);
  // An address a host has not finished is a REAL state a reviewer should see as such — never a blank
  // cell, which reads as "fine" rather than as "there is nothing here to check".
  return parts.length > 0 ? parts.join(", ") : "No address on the listing";
}
```

**The class constants the reveal reuses** — `ops-queue-row.tsx:91, 94, 97`:

```typescript
const ROW_VALUE_CLASS = "text-label";
/** A money value — the same role as every other value, plus the figure treatment. Derived, never retyped. */
const ROW_MONEY_CLASS = `${ROW_VALUE_CLASS} tabular-nums`;
/** The muted term class every `<dt>` carries. */
const ROW_TERM_CLASS = "text-label text-muted-foreground";
```

⚠ **`ROW_MONEY_CLASS` is for money and counts.** UI-SPEC type rule 3: **no `tabular-nums` on a phone
number** — it is an identifier, not a figure to compare down a column.

**The `<Fact>` call-site shape** — `ops-queue-row.tsx:280`:

```tsx
            <Fact term="Address">{addressOf(row)}</Fact>
```

⚠ **The revealed values live in the island's React state after the action returns — never in props from
the RSC.** That is what makes the reveal auditable, and it is why the queue projections do not move.

---

### 11. `src/lib/host/resend-verification.ts` (utility · client-safe) — **NEW**

**Analog: `wizard.tsx:682-692`, the function being extracted, verbatim:**

```tsx
  async function resendVerification() {
    try {
      await authClient.sendVerificationEmail({
        email: hostEmail,
        callbackURL: "/host/listings",
      });
      toast.success(`Verification email sent to ${hostEmail}.`);
    } catch {
      toast.error("Couldn't send the email. Try again in a moment.");
    }
  }
```

and the checklist row it drives — `wizard.tsx:779-784`, the shape D-269 reuses:

```tsx
    {
      label: "Verified email",
      done: emailVerified,
      step: null,
      action: emailVerified ? undefined : resendVerification,
    },
```

with `const publishEligible = checklist.every((c) => c.done);` at `:785`.

⚠ **`callbackURL` is the ONE thing that differs — a parameter, never a branch.** UI-SPEC: *"The two
toast strings must have exactly one owner."* The wizard's local function becomes a call into this module.
⚠ **Not a reversal of D-07** — `src/lib/auth.ts` keeps `requireEmailVerification: false`. The gate is
local to the action. ⚠ **The server-side gate is independent of this UI**: the submission action
re-reads `user.emailVerified` and refuses. The checklist row is a hint.

---

### 12. `src/inngest/functions/didit-reconcile.ts` (job · cron · batch) — **NEW (FINDING F-4)**

**Analog: `src/inngest/functions/payment-reconcile.ts` — the whole file.** It is the exact precedent:
a sweep that exists because a webhook may never arrive.

Opening + the argument to reproduce (`:1-18`):

```typescript
import "server-only";

// ════════════════════════════════════════════════════════════════════════════════════════════════════════
// THE PAYMENT RECONCILE SWEEP (13.1-CONTEXT D-104…D-111) — money coming IN, the mirror of payout-reconcile.
// ════════════════════════════════════════════════════════════════════════════════════════════════════════
//
// … A booker's payment reached FitOut over exactly ONE transport — a `checkout_session.
// payment.paid` webhook, an HTTP request over the public internet — and if that request is lost to a deploy,
// a process recycle or a provider incident, the money is captured and the booking stays `pending` forever
// with nothing anywhere failing to say so.
//
// That is not hypothetical. On 2026-08-21 every `pending` booking holding a `checkout_session_id` was probed
// against PayMongo and TWO had taken the customer's money and never confirmed; the second had sat unnoticed
// for THREE DAYS. …
```

**One domain over:** Didit retries **twice and then drops permanently**, and with D-262 there is no
operator to notice. A `pending` `host_verification` row with a `vendor_ref` and a stale `updated_at` is
the exact analogue of the pending-booking-with-a-checkout-session.

**Named constants, not literals** — `payment-reconcile.ts:82, 100, 140, 143-158`. Copy the discipline
including the cron-offset rule:

```typescript
export const RECONCILE_INTERVAL_MINUTES = 5;
export const RECONCILE_MIN_AGE_MINUTES = 2;
export const RECONCILE_BATCH_LIMIT = 50;
```

```
 * The minute the sweep's schedule starts on. Not 0, 15, 30, 45 or 50 — the five slots the existing crons
 * … Pitfall 4's offset discipline: crons that tick together contend for the same database.
```

```
 * The cron trigger string, BUILT from the constants above rather than typed out.
```

⚠ Free-tier GET limit is **10/min** — batch accordingly, and say so at the constant.
⚠ **It must share `didit-verdict.ts` and the write module with the webhook.** Never a second road.

---

### 13. Tests

**(a) `tests/ops/host-verification-submit.test.ts` and `tests/ops/host-contact-reveal.test.ts`**

**Analog: `tests/ops/ops-audit.test.ts` — its harness is the one to copy for any new DB-backed,
session-driven action test.** The full setup, verbatim (`:46-58, 155-215`):

```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach, vi } from "vitest";
import { and, eq } from "drizzle-orm";

import { setupTestDb, teardownTestDb, type TestDb } from "../helpers/db";
import { makeTestAuth, signUp, type TestAuth } from "../helpers/auth";
import { audit, hostVerification, listing, listingReview, user } from "@/lib/db/schema";
import type { RateLimitOptions, RateLimitResult } from "@/lib/rate-limit";
import { HOST_REJECT_REASONS, LISTING_REJECT_REASONS } from "@/lib/validation/ops";

const sessionHeaders: { cookie: string } = { cookie: "" };
vi.mock("next/headers", () => ({
  headers: async () => new Headers({ cookie: sessionHeaders.cookie }),
}));
```

```typescript
beforeAll(async () => {
  testDb = await setupTestDb();
  testAuth = makeTestAuth(testDb);

  await signUp(testAuth, { email: STAFF_EMAIL, password: PASSWORD, name: "Ops Staff",
                           firstName: "Ola", intent: "book" });
  …
  const ids = await testDb.db.select({ id: user.id, email: user.email }).from(user);
  staffId = ids.find((r) => r.email === STAFF_EMAIL)!.id;

  // The CLI's privileged Drizzle flip — never `auth.api.updateUser`, which `input: false` makes
  // structurally incapable of writing this field (tests/auth/ops-role.test.ts measures that).
  await testDb.db.update(user).set({ role: "staff" }).where(eq(user.id, staffId));

  vi.doMock("@/lib/auth", () => ({ auth: testAuth }));
  vi.doMock("@/lib/db", () => ({ db: testDb.db }));
  vi.doMock("next/navigation", () => ({
    // The real `notFound` raises a Next-internal digest only the framework can interpret; the shipped
    // test idiom (tests/ops/staff-guard.test.ts:101) replaces it with a NAMED error so a case can
    // tell "the gate refused" apart from any other failure.
    notFound: () => { throw new Error(NOT_FOUND); },
  }));
  vi.doMock("@/lib/rate-limit", () => ({ rateLimit: fakeRateLimit }));
  vi.doMock("next/cache", () => ({ revalidatePath: () => {} }));
  vi.resetModules();
  ({ approveHost, rejectHost, … } = await import("@/app/actions/ops-review"));
}, 120_000);

afterAll(async () => {
  vi.doUnmock("@/lib/auth");
  vi.doUnmock("@/lib/db");
  vi.doUnmock("next/navigation");
  vi.doUnmock("@/lib/rate-limit");
  vi.doUnmock("next/cache");
  await teardownTestDb(testDb);
});
```

The observable rate-limit stub (`:67-72`) — required so a file making ~20 calls as one identity does
not start measuring the limiter instead of the action:

```typescript
const rateLimitCalls: Array<{ key: string; opts: RateLimitOptions }> = [];
let rateLimitAllows = true;
const fakeRateLimit = (key: string, opts: RateLimitOptions): RateLimitResult => {
  rateLimitCalls.push({ key, opts });
  return rateLimitAllows ? { ok: true } : { ok: false, retryAfter: 42 };
};
```

**The audit-read-back INSTRUMENT** (`:132-138`) — the single most reusable helper in the file:

```typescript
/** THE INSTRUMENT. Every OPS-03 claim in this file goes through here, never through a return value. */
async function trailRows(action: string, outcome: "ok" | "denied" | "needs_attention") {
  return testDb.db
    .select()
    .from(audit)
    .where(and(eq(audit.action, action), eq(audit.outcome, outcome)));
}
```

**The allow-branch assertion** (`:218-230`) and **the deny-branch assertion** (`:328-339`):

```typescript
    // The decision landed on the DOMAIN table…
    expect(await verificationStatus(host)).toBe("approved");
    // …and — the OPS-03 claim — a trail row exists carrying the AUTHENTICATED staff id.
    const rows = await trailRows("ops_approve_host", "ok");
    const row = rows.find((r) => (r.meta as { userId?: string })?.userId === host);
```

```typescript
    expect(res.ok).toBe(false);
    expect(await verificationStatus(host)).toBe("approved"); // untouched
    const rows = await trailRows("ops_approve_host", "denied");
    const row = rows.find((r) => (r.meta as { userId?: string })?.userId === host);
    expect(row, "the refusal left no trail row").toBeDefined();
    expect(row!.actorId).toBe(staffId);
    expect((row!.meta as { reason?: string }).reason).toBe("not_applicable");
```

**The D-72 serialised-meta assertion the reveal test must copy** —
`tests/ops/reject-reason.test.ts:300-325`:

```typescript
  it("case 8 — meta carries the taxonomy sentence and a flag, and NEVER the note", async () => {
    const host = await seedHost("pending");
    // A note that is unmistakable if it leaks, and shaped like the PII this rule exists for.
    const note = "Spoke to Maria Santos on 0917 555 0110; the address is her home.";
    …
    // Serialise the WHOLE row, so the assertion cannot be defeated by the note landing in a field
    // the test forgot to look at (the tests/ops/grant-cli.test.ts case-3 idiom).
    const serialised = JSON.stringify(row);
    expect(serialised).not.toContain("Maria Santos");
    expect(serialised).not.toContain("0917 555 0110");
    expect(serialised).not.toContain(note);
```

**The shared seeding helper the ~12 `createDraftListing` fixtures need** — `ops-audit.test.ts:99-116`
is the shape to lift into `tests/helpers/`:

```typescript
/** A host row at a chosen verification status. `null` ⇒ user only, no verification row at all. */
async function seedHost(
  id: string,
  status: "unverified" | "pending" | "approved" | "rejected" | "grandfathered" | "suspended" | null,
): Promise<string> {
  await testDb.db.insert(user).values({
    id, name: id, email: `${id}@fitout.test`, firstName: "Seed",
    emailVerified: true, canHost: true,
  });
  if (status !== null) {
    await testDb.db.insert(hostVerification).values({ userId: id, status, provider: "manual" });
  }
  return id;
}
```

⚠ Mirror it in `e2e/helpers/booker-seed.ts`, which already holds the repo's only
`INSERT INTO host_verification` at `:600`.

**The isolated-schema mechanism itself** — `tests/helpers/db.ts:105-140`. Read it once; the property
that matters is that **migrations are replayed from `drizzle/*.sql` into a per-file schema**, so a
column declared in `schema.ts` but never migrated type-checks green and fails every integration test:

```typescript
/**
 * Reset an isolated test schema and apply all generated migrations against it. Returns a
 * Drizzle db instance bound to that schema. Call once per integration test file (beforeAll).
 */
export async function setupTestDb(): Promise<TestDb> {
```

**(b) `tests/verification/didit-webhook.test.ts`**

**Analog: `tests/paymongo/webhook-payment-paid.test.ts`.** Three things to copy:

*The captured-fixture builder* (`:157-189`) — a real event body, typed, with the exact nesting asserted:

```typescript
/** A PayMongo checkout_session.payment.paid event body (A1 nesting: data.attributes.data.attributes.*). */
function paidEventBody(opts: { eventId: string; bookingId: string; paymentId?: string | null; method?: string }): string {
  …
  return JSON.stringify({ data: { id: opts.eventId, attributes: { type: "checkout_session.payment.paid",
    data: { id: "cs_test_paid", attributes: { reference_number: opts.bookingId, … } } } } });
}
```

*The self-signing POST driver* (`:191-201`):

```typescript
function post(body: string, signature?: string): Promise<Response> {
  const sig = signature ?? mockPayMongo.signWebhook(body, SECRET, TS);
  return POST(
    new Request("http://localhost/api/paymongo/webhook", {
      method: "POST",
      headers: { "content-type": "application/json", "paymongo-signature": sig },
      body,
    }),
  );
}
```

with the helper at `tests/helpers/mocks.ts:243-256`, which also ships `badSignature()` (all-zero hex)
for the 400 path. **Add a `signDiditWebhook` + `badDiditSignature` pair there, beside it.**

*The per-test secret* (`:200-202`):

```typescript
  prevSecret = process.env.PAYMONGO_WEBHOOK_SECRET;
  process.env.PAYMONGO_WEBHOOK_SECRET = SECRET;
```

⚠ **A self-signed mock proves only that the code agrees with itself.** Research is explicit: the
sandbox transcript in `18.1-EVIDENCE.md` is what proves the signature scheme is right.

**(c) `tests/verification/didit-verdict.test.ts`** — a **DB-free unit** file. Analog:
`tests/ops/verification-port.test.ts`, whose case 13 reads comment-stripped source and whose
prototype-shaped-name cases (`"constructor"`, `"toString"`, `"valueOf"`, `"__proto__"`) must **stay
proving the same properties** once every `runVerification(...)` becomes `await`ed.

---

## Shared Patterns

### The audit row — both branches, read back out, D-72-clean

**Sources:** `src/lib/audit.ts:32-51` (the entry shape) · `:83-90` (the swallow) ·
`ops-review.ts:359-366` (deny) + `:423-428` (allow) · `tests/ops/ops-audit.test.ts:1-16` (why every
assertion is a `SELECT`)
**Apply to:** `requestHostVerification`, `revealHostContact`, the webhook's verdict write, the sweep

```
// THE SWALLOW IS DELIBERATE. recordAudit is awaited at 57 call sites and many of them sit inside
// `catch` blocks on money paths … A thrown INSERT there would convert an ALREADY-HANDLED failure into an
// unhandled 500 and stop the webhook returning 200 … Losing an audit ROW is acceptable; losing the ACK is not.
```

```
// `recordAudit` SWALLOWS ITS OWN INSERT FAILURE BY DESIGN … So an action returning `{ ok: true }` is
// evidence that the DECISION was recorded and evidence of NOTHING about the trail row. OPS-03 is a
// claim about the row, so every case here reads the row back out of the table.
```

### The guarded UPDATE as the durable cap AND the idempotency

**Source:** `ops-review.ts:396-421` · `re-review.ts`'s discipline, quoted in 18.1-RESEARCH:
*"every source state lives in the UPDATE's own WHERE, so a 0-row result is the single calm no-op and
there is no branch a future edit can forget"*
**Apply to:** the submission upsert (D-264/D-266), the webhook verdict (idempotency), the reveal's read

⚠ **`$onUpdate` does NOT fire through `db.execute(sql\`…\`)`.** Every shipped ops write spells
`updated_at = now()` (`ops-review.ts:404, 495`). FINDING F-1 adds `created_at = now()` on the
resubmission branch. ⚠ **A JS `Date` cannot be bound through raw `db.execute`** — bind an ISO-8601
string with an explicit `::timestamptz` cast (`ops-review.ts:390-401`).

⚠ **The 0-row branch is ONE calm sentence for five conditions on purpose** (UI-SPEC): it is also a
privacy property — a suspended host must not be distinguishable, by the shape of a refusal, from a host
whose cooldown has not elapsed.

### The server-only guard on the implementation, never the port

**Source:** `providers/manual.ts:1` + `:34-46` · `port.ts:44-60` · `fees.ts:1-30`'s rule
**Apply to:** `providers/didit.ts` (line 1), `frozen-payouts.ts` (server module),
`inngest/functions/didit-reconcile.ts` (`import "server-only";` on line 1, per `payment-reconcile.ts:1`)

⚠ **NEVER on `port.ts`** — `verification-port.test.ts` case 13 asserts zero occurrences over
comment-stripped source, and `tests/design/server-only-guards.test.ts:140-150` records that collision as
*"this repo's DEFAULT outcome rather than an edge case."*

### Rate limiting on a session-gated escalation

**Source:** `capability.ts:48-51, 63-73` · `src/lib/rate-limit.ts:10-15, 29-32`
**Apply to:** `requestHostVerification` (5/60s, `ACTIVATE_RATE_LIMIT` shape), `revealHostContact`
(the ops budget, `OPS_ACTION_RATE_LIMIT = { window: 60, max: 30 }`, `ops-review.ts:139`)

*"Callers MUST key on the AUTHENTICATED identity … never the client IP."* Test seams:
`__rateLimitBucketCount()`, `__resetRateLimit()`.

### Calm typed refusals, never a throw

**Source:** `listing.ts:61-63` · `ops-review.ts:39-49, 96-120`
**Apply to:** every new action

```
// `{ ok: true } | { ok: false, error }` … Every refusal goes through one of the NAMED constants below
// so two paths cannot return two slightly different sentences for the same condition. Nothing on this
// path raises …
```

### Copy lives in a module; the component decides presentation

**Source:** `hosting-paused-notice.tsx:7-11` · `requests-signal.ts:52-75` · `review-signal.ts:190-243`
**Apply to:** `verification-signal.ts`, `verification-panel.tsx`, the frozen-session sentence

*"A sentence typed into three page files is the shape `requests-signal.ts`'s header describes by name."*

### Describe the forbidden literal; never quote it

**Source:** `hosting-paused-notice.tsx:31-34` · `review-queue.ts:24-26` · `port.ts:47-52` ·
`re-review.ts:38-46` · `drizzle/0021`'s rule
**Apply to:** every new comment in `port.ts`, `re-review.ts`, `review-queue.ts`, and anywhere in `src/`

Four live traps touch this phase. **This repository has tripped it seven times by its own count.**

---

## No Analog Found

| File / concern | Role | Data Flow | Reason & fallback |
|---|---|---|---|
| `src/lib/verification/didit-verdict.ts` — a **total map over an EXTERNAL vocabulary** | utility | transform | Partial. Every shipped total `Record` in the repo is keyed on a **pgEnum-derived union**, so `tsc` does the census (`ops-queue-row.tsx:170-186`, `payout-state-badge.tsx:53-62`). Didit's ten statuses are vendor strings — `tsc` cannot enumerate them. **Fallback:** compose `refund-rail.ts`'s declared-allow-list + fail-closed shape with a `const` tuple of the ten and a **table-driven test over all ten in both casings** as the census. The explicit unknown branch (stay `pending`, write an audit row) is the fail-closed default `port.ts:133-139` argues for. |
| `X-Timestamp` ±300s replay window | route handler | event-driven | **No in-repo precedent.** PayMongo has no equivalent, so this is a *strengthening*, not a copy. Nearest reasoning analog: the two-independent-controls argument in 18.1-RESEARCH § Known threat patterns (timestamp window **plus** the guarded `WHERE status='pending'`). Write the reason at the site and pin the `>300s` rejection with its own case. |
| The **inverted** freeze query in `frozen-payouts.ts` | service (SQL) | CRUD (read) | Partial — same problem 18-PATTERNS flagged for `alertStuckHeld`. `payout-sweep.ts:115-167` has every join this needs, but it *excludes* the rows; the new module *selects* them, and there is no shipped SELECT that does. **Budget real new SQL, not a one-word edit**, and keep the predicate spelled so a divergence from `queryDuePayouts` is visible. |
| The Didit Console configuration (application, workflow, webhook destination, signing secret) | live service config | — | **Not in git and not expressible in code.** Nearest analog: `paymongo.ts:14-17`'s BETA NOTE (*"real hosted onboarding is exercised in MANUAL UAT"*). **Document the exact workflow composition in the plan** — ID Verification + Liveness + Face Match; **no AML, no KYC-expiry** — so it is reproducible and a silent Console edit is detectable. Open with a `checkpoint:human` task. |

---

## Metadata

**Analog search scope:** `src/app/actions/**`, `src/app/api/**`, `src/app/(host)/**`, `src/lib/**`,
`src/components/host/**`, `src/components/ops/**`, `src/inngest/functions/**`, `tests/ops/**`,
`tests/paymongo/**`, `tests/design/**`, `tests/helpers/**`
**Files read in full or in targeted ranges:** 31
**Upstream consumed:** `18.1-CONTEXT.md`, `18.1-RESEARCH.md`, `18.1-UI-SPEC.md`, `18-PATTERNS.md`
**Pattern extraction date:** 2026-09-02
**Line numbers valid as of:** working tree, 2026-09-02. Re-resolve before executing if the tree moves.
