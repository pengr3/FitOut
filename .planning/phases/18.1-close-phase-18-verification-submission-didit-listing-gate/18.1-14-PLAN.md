---
phase: 18.1-close-phase-18-verification-submission-didit-listing-gate
plan: 14
type: execute
wave: 7
depends_on: ["18.1-09", "18.1-12", "18.1-13"]
files_modified:
  - .planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-EVIDENCE.md
  - .planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-VALIDATION.md
  - e2e/skeleton-geometry.spec.ts
  - src/lib/design/measurements.ts
  - .planning/ROADMAP.md
autonomous: false
requirements: [HVER-06, HVER-07, OPS-06]
must_haves:
  truths:
    - "SC1 is proved HONESTLY, not only by a self-signed mock: a real Didit SANDBOX session is driven end to end with sandbox_scenario 'approve' and then 'decline_document_expired', and the actual webhook delivery, its headers, the verified signature and the resulting row transition are recorded in 18.1-EVIDENCE.md with retrieval dates. A self-signed mock proves only that the code agrees with itself."
    - "The Phase 17 responsive/a11y convention is honoured: /host/verify and the revealed /ops row are hand-measured at 320 and 1280 in BOTH themes, because no tests/design/** gate can see a picture."
    - "OPS_QUEUE_ROW_HEIGHT is re-MEASURED rather than assumed. The reveal adds one <dl> row before reveal and two after; if the measured height moved, the constant moves WITH ITS DERIVATION REWRITTEN — never a guess."
    - "Assumption A8 is settled by walking one sandbox session on a phone: the Didit hosted flow is not FitOut's UI, is not specified by the UI-SPEC, and is on the dominant PH device class."
    - "The evidence file follows 18-EVIDENCE.md § P1's pattern: a ONE-TIME AUDIT paired with a NAMED structural test as the ongoing pin, so nobody mistakes a transcript for a gate."
    - "SUCCESS CRITERION 8 IS DISCHARGED DELIBERATELY: Phase 18's roadmap checkbox is ticked, because the thing it was held open for — a verification path a host can actually reach — now exists. Phase 18 shipped the console that decides and never shipped the thing that submits to it; the checkbox was held until 18.1 shipped the code, and this is where that is recorded."
  artifacts:
    - path: ".planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-EVIDENCE.md"
      provides: "The sandbox transcript, the hand-measure record, and the named ongoing pins"
      min_lines: 60
    - path: "e2e/skeleton-geometry.spec.ts"
      provides: "The re-pinned ops queue row geometry, measured with the Contact fact present"
  key_links:
    - from: ".planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-EVIDENCE.md"
      to: "tests/verification/didit-webhook.test.ts"
      via: "the transcript names the structural test that is the ongoing pin"
      pattern: "didit-webhook"
---

<objective>
Close the phase honestly.

Three things no automated gate in this repository can do. **First**, prove the Didit signature scheme
against the real vendor — a self-signed mock proves only that the code agrees with itself, and B3 is
the highest-severity boundary in the phase. **Second**, hand-measure the two new surfaces at 320 and
1280 in both themes, which is the Phase 17 convention and is a picture no `tests/design/**` gate can
see. **Third**, re-measure `OPS_QUEUE_ROW_HEIGHT`, which the reveal may have invalidated.

⚠ **This plan has blocking checkpoints and is not autonomous.** Two of its three tasks need a human
with a phone, a browser and a Didit sandbox account.

Purpose: the phase's strongest claims stop being self-referential.
Output: one evidence file, one re-pinned measurement, one signed-off validation contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-VALIDATION.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-UI-SPEC.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-RESEARCH.md
@.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk the Didit sandbox end to end and record the transcript</name>
  <files>.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/18-host-verification-listing-review-fitout-ops/18-EVIDENCE.md` § P1 — the
      pattern this file follows: a ONE-TIME AUDIT, with retrieval dates, paired with a NAMED
      structural test stated as the ongoing pin.
    - `18.1-RESEARCH.md` § R1 § Sandbox — the 22 scenario slugs, the two simulate endpoints, the
      "Try Webhook" Console feature, and the fact that `sandbox_scenario` is rejected on live
      applications.
    - `18.1-RESEARCH.md` § R1 § Webhooks § Retry policy — Didit **blocks private/localhost URLs**, so
      the local walk needs a tunnel. There is no Didit CLI.
    - Project memory: the recorded ngrok recipe used for the PayMongo webhook UAT, including the
      webhook-URL/env steps.
    - `18.1-VALIDATION.md` § Success Criterion 1 § layer 2, and § Manual-Only Verifications.
  </read_first>
  <what-built>
    Everything. The submission path (18.1-07), the Didit adapter (18.1-05), the shared verdict mapper
    (18.1-06), the webhook (18.1-08) and the reconciliation sweep (18.1-09) are all shipped and green
    against self-signed mocks. This task is the one thing those mocks cannot do.
  </what-built>
  <how-to-verify>
    1. Start the app locally and expose the webhook endpoint through a tunnel (ngrok/cloudflared —
       Didit blocks private/localhost URLs and has no CLI). Register the tunnel URL as the webhook
       destination on the **SANDBOX** Didit application and set `DIDIT_WEBHOOK_SECRET` to that
       application's signing secret.
    2. **The approve walk.** Sign in as a host with a confirmed email, go to `/host/verify`, enter a
       phone, press **Start the check**. Confirm you are sent to the hosted flow. Force the pass —
       either by creating the session with `sandbox_scenario: "approve"` or by calling the simulate
       endpoint on the created session.
    3. Capture, verbatim: the outbound create-session request body, the 201 response, the inbound
       webhook's **headers** (`X-Signature`, `X-Timestamp`, `User-Agent`), the raw body, the response
       status your route returned, and the resulting `host_verification` row **before and after**
       (`status`, `result`, `checked_at`, `provider`, `vendor_ref`). ⚠ **Redact the secret and the
       API key. Do not paste the host's real email or phone.**
    4. **The decline walk.** Repeat with `decline_document_expired`. Capture the same set, plus the
       stored `reason` string, and confirm it is non-empty, bounded, and contains no risk code, no
       `node_id` and no `additional_data`.
    5. **The replay check.** Re-deliver the captured approve event (Console "Try Webhook" or a manual
       repost with a fresh signature). Confirm the response is **200** and that the row did **not**
       change.
    6. **The tamper check.** Repost with one byte of the body changed and the original signature.
       Confirm a **400**, and confirm the server log carries no unhandled exception.
    7. **Assumption A8** — walk one sandbox session on a **real phone browser** and record whether
       the Didit hosted flow is usable at that width. It is not FitOut's UI and no gate can see it.
    8. Write it all into `18.1-EVIDENCE.md` with retrieval dates, in `18-EVIDENCE.md § P1`'s shape,
       and state plainly at the top that this is a **ONE-TIME AUDIT** whose ongoing pin is
       `tests/verification/didit-webhook.test.ts`. Capture the real webhook bytes as a fixture for
       that test if they differ in any way from the fixture currently in the repo.
  </how-to-verify>
  <action>
    Pause for the operator's sandbox walk. When they resume, write everything they captured into
    `18.1-EVIDENCE.md` in `18-EVIDENCE.md § P1`'s shape — verdict first, retrieval dates on every
    reading, redactions applied — and state at the top that this is a ONE-TIME AUDIT whose ongoing
    pin is `tests/verification/didit-webhook.test.ts`. If the captured webhook bytes differ in any
    way from the fixture currently in that test file, replace the fixture with the real bytes and
    re-run it.
  </action>
  <verify>
    <human-check>Both sandbox walks, the replay 200, the tamper 400 and the A8 phone reading are recorded with dates and no secrets</human-check>
    <automated>npx vitest run tests/verification/didit-webhook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `18.1-EVIDENCE.md` exists and contains both walks with retrieval dates
    - The approve walk records a webhook whose signature VERIFIED and whose row transitioned to `approved` with `vendor_ref` unchanged
    - The decline walk records a non-empty stored `reason` containing no risk code, no `node_id` and no `additional_data`
    - The replay attempt is recorded as a 200 with no row change
    - The tamper attempt is recorded as a 400 with no unhandled exception
    - The A8 phone walk is recorded with a verdict, even if the verdict is "unusable"
    - No secret, API key, real email or real phone appears anywhere in the file
    - The file names `tests/verification/didit-webhook.test.ts` as the ongoing pin and says out loud that a transcript is not a gate
  </acceptance_criteria>
  <resume-signal>Type "approved" once the transcript is written, or describe what the sandbox would not do</resume-signal>
  <done>The signature scheme, the verdict path, the replay guard and the tamper refusal are proved against the real vendor, once, on the record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hand-measure the two new surfaces at 320 and 1280, both themes</name>
  <files>.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-EVIDENCE.md</files>
  <read_first>
    - `18.1-UI-SPEC.md` § Surface 1 § Layout (the 320px expectations), § Surface 4 § The interaction
      (focus moves to the email anchor on success), and § NOT COVERED — *"This document proves nothing
      about pixels."*
    - `18.1-VALIDATION.md` § Manual-Only Verifications, row 2.
    - `e2e/axe-sweep.spec.ts` and `e2e/overflow-320.spec.ts` — the rows plan 18.1-12 added for
      `/host/verify`. ⚠ **PROJECT D-24: these are NOT in CI**, so this human run is the only time they
      are sampled.
  </read_first>
  <what-built>
    `/host/verify` with its six states, its two-gate form and its one refusal region; and the `/ops`
    queue row with the Contact fact and the reveal island. Both are covered by build-blocking
    structural gates; neither is covered by anything that can see a picture.
  </what-built>
  <how-to-verify>
    1. Run `npm run test:e2e` once, in full. Record pass/fail per spec — this is the only sampling
       PROJECT D-24 permits.
    2. Load `/host/verify` at **320px** and at **1280px**, in **court** and in **grove**, in the
       `unverified`, `pending` and `rejected` states. For each: no horizontal overflow; the form
       stacks; both controls are full-width and stay 44px; focus order is sensible; and the
       state / reason / way-out sentence is present and legible.
    3. Load `/ops` as a staff member at **320px** and **1280px**, both themes. Press **Show contact**
       on a host row and on a listing row. Confirm: the button reaches 44px; the row does not
       overflow before or after reveal; the `Email` and `Phone` facts render as two `<dl>` rows;
       **focus lands on the email anchor** after a successful reveal; and a host with no phone shows
       **"Not provided"** rather than a blank cell.
    4. Confirm no support address appears anywhere on either surface, and that nothing on
       `/host/verify` offers a route to a person.
    5. Record every reading — including anything that looked wrong — in `18.1-EVIDENCE.md` under a
       hand-measure section with the date and the browser/OS.
  </how-to-verify>
  <action>
    Pause for the operator's hand-measure. When they resume, append a hand-measure section to
    `18.1-EVIDENCE.md` recording every reading with the date, the browser and the OS — including any
    reading that looked wrong. ⚠ Write defects down rather than fixing them silently; a defect found
    here belongs in the phase's verification output, not in an unplanned edit.
  </action>
  <verify>
    <human-check>Eight /host/verify readings and four /ops readings are recorded, plus the per-spec result of one full `npm run test:e2e`</human-check>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/live-regions.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` was run once and its per-spec result is recorded
    - Eight `/host/verify` readings are recorded (3 states x 2 widths, plus both themes at each width for at least the `unverified` and `rejected` states)
    - Four `/ops` readings are recorded (2 widths x 2 themes), each covering before-reveal and after-reveal
    - The focus-lands-on-the-email-anchor behaviour is confirmed or recorded as failing
    - The "Not provided" empty-phone rendering is confirmed
    - Any defect found is written down rather than fixed silently
  </acceptance_criteria>
  <resume-signal>Type "approved" with the readings, or describe what failed</resume-signal>
  <done>Both new surfaces are measured by a person at the two widths and both themes, and the e2e suites are sampled once.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the queue row geometry and sign off the validation contract</name>
  <files>e2e/skeleton-geometry.spec.ts, src/lib/design/measurements.ts, .planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-VALIDATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - `src/lib/design/measurements.ts:1040` — `OPS_QUEUE_ROW_HEIGHT = "h-132 lg:h-211"` and its
      derivation comment. ⚠ **The reveal adds one `<dl>` row before reveal and two after.**
    - `e2e/skeleton-geometry.spec.ts` — the instrument. Read how it measures a row and compares it to
      the declared constant.
    - `18.1-UI-SPEC.md` § Spacing (the `OPS_QUEUE_ROW_HEIGHT` ⚠ MEASURE AGAIN note), § Gate
      Amendments row 11, and § NOT COVERED (the constant is a shipped measurement this phase may
      invalidate).
    - `18.1-VALIDATION.md` § Validation Sign-Off — the seven checkboxes and the `nyquist_compliant`
      frontmatter flag.
    - `.planning/ROADMAP.md` § Phase 18 — its `**Plans:** 14/14 plans complete` line, its § Progress
      row reading *"checkbox held until 18.1 ships the code"*, and § Phase 18.1's eight success
      criteria, of which **criterion 8 is the checkbox itself**.
  </read_first>
  <action>
    Add a `/ops` row to `e2e/skeleton-geometry.spec.ts` that measures the queue row **with the Contact
    fact present**, at both widths, and compare the measurement against `OPS_QUEUE_ROW_HEIGHT`.

    ⚠ **MEASURE; DO NOT ASSUME.** If the measured height still matches `"h-132 lg:h-211"`, leave
    `src/lib/design/measurements.ts` **byte-unchanged** and record the measurement in the SUMMARY as a
    confirmed non-move. If it moved, update the constant **and rewrite its derivation comment** so the
    new value is derived rather than guessed — that comment is the only thing standing between a
    measured constant and a number somebody typed.

    Then sign off `18.1-VALIDATION.md`: tick the seven Validation Sign-Off checkboxes that the phase
    now satisfies, set `nyquist_compliant: true` and `wave_0_complete: true` in its frontmatter, and
    set `status` to reflect the executed contract. Fill the **File Exists** column of the per-task map
    for the eight Wave 0 artifacts that now exist, naming the plan that created each. ⚠ **Do not tick
    a box the phase does not satisfy** — if `npm run test:e2e` failed in task 2, record that instead.

    Finally, discharge **Success Criterion 8**: tick Phase 18's roadmap checkbox in
    `.planning/ROADMAP.md` and update its § Progress row so it no longer reads *"checkbox held until
    18.1 ships the code"*. Replace that clause with a one-line record naming Phase 18.1 and the date.
    ⚠ **Only if every gate is green and both checkpoints above resumed with an approval.** The
    checkbox was held open for a measured reason — a console that decides with nothing that submits
    to it — and ticking it while a gate is red would make the roadmap lie about exactly the thing
    this phase exists to fix.

    ⚠ **Do not modify any `tests/design/**` count in this plan.** Every count this phase moves was
    moved, with its reason, in the plan that caused it.
  </action>
  <acceptance_criteria>
    - `e2e/skeleton-geometry.spec.ts` contains a `/ops` row measuring the queue row with the Contact fact, at both widths
    - Either `git diff src/lib/design/measurements.ts` is EMPTY and the SUMMARY records the confirming measurement, or the constant moved AND its derivation comment was rewritten in the same commit
    - `18.1-VALIDATION.md` frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`
    - Every ❌ in `18.1-VALIDATION.md`'s File Exists column is resolved to a real path with the creating plan named
    - `git diff tests/design/` is EMPTY — no design count moved in this plan
    - `.planning/ROADMAP.md`'s Phase 18 entry reads `- [x]` and its § Progress row no longer contains the phrase about the checkbox being held
    - `npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The one shipped measurement this phase could invalidate is re-measured, and the validation contract records what was actually run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **B3** | Didit -> `POST /api/didit/webhook`, exercised for real. This task is the only evidence the signature scheme is right rather than merely self-consistent. |
| B2 | FitOut -> Didit sandbox. Real credentials cross to a third party during the walk. |
| evidence file -> git | A transcript committed to the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-1401 | Spoofing | a signature scheme that is self-consistent but wrong | mitigate | **This plan is that mitigation.** A real sandbox delivery is verified against the shipped route, and the tamper and replay attempts are driven for real. The transcript names `tests/verification/didit-webhook.test.ts` as the ongoing pin so a one-time audit is never mistaken for a gate. |
| T-18.1-1402 | Information disclosure | secrets, API keys, a real email or a real phone pasted into a committed transcript | mitigate | Explicit redaction instruction in the checkpoint, and an acceptance criterion asserting none appears. The sandbox application's key is separate from live, and `.env.local` is git-ignored. |
| T-18.1-1403 | Spoofing | a sandbox hook reaching the live application | mitigate | Didit **rejects `sandbox_scenario` on live applications**, and sandbox and live are separate applications with separate keys whose traffic never mixes. The adapter has no `sandbox_scenario` code path at all (plan 18.1-05). |
| T-18.1-1404 | Information disclosure | the tunnel exposing the local app to the internet during the walk | accept | Short-lived, human-supervised, sandbox credentials only, and the webhook route refuses everything it cannot verify. Didit blocks private/localhost URLs, so a tunnel is the only way to walk this at all. Shut the tunnel down and de-register the URL when finished. |
| T-18.1-1405 | Repudiation | a pixel claim that no gate can check being asserted rather than measured | mitigate | Task 2 is a blocking human checkpoint with an enumerated reading list, and every reading — including failures — is written into the evidence file rather than resolved silently. |
| T-18.1-1406 | Tampering | a shipped measurement being guessed rather than re-derived | mitigate | `OPS_QUEUE_ROW_HEIGHT` is measured by `e2e/skeleton-geometry.spec.ts` with the Contact fact present; the constant either stays byte-unchanged with the confirming measurement recorded, or moves WITH ITS DERIVATION REWRITTEN. |
| T-18.1-14-SC | Tampering | npm installs | mitigate | Zero package installs. `package.json` and the lockfile are not in `files_modified`. |
</threat_model>

<verification>
Run each gate ALONE — never chained:

1. `npx tsc --noEmit`
2. `npm test`
3. `npm run test:design`
4. `npm run build`

Human-run once (PROJECT D-24 keeps these out of CI):
- `npm run test:e2e`
- `npx playwright test e2e/skeleton-geometry.spec.ts`
</verification>

<success_criteria>
- `18.1-EVIDENCE.md` records both sandbox walks, the replay 200, the tamper 400 and the A8 phone reading, with retrieval dates and no secrets
- The evidence file names its ongoing structural pin and says out loud that a transcript is not a gate
- `/host/verify` and the revealed `/ops` row are hand-measured at 320 and 1280 in both themes, with every reading written down
- `OPS_QUEUE_ROW_HEIGHT` is confirmed unmoved or moved with a rewritten derivation
- `18.1-VALIDATION.md` is signed off with `nyquist_compliant: true` and no ❌ left in its File Exists column
- No `tests/design/**` count moved in this plan
- Phase 18's roadmap checkbox is ticked and its Progress row records why (Success Criterion 8)
- All four gates green, each invoked separately
</success_criteria>

<output>
Create `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-14-SUMMARY.md` when done
</output>
