---
phase: 18.1-close-phase-18-verification-submission-didit-listing-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/lib/payments/fees.ts
  - src/lib/ops/cancel-impact.ts
  - tests/payments/ops-cancel.test.ts
autonomous: true
requirements: [HVER-06, HVER-07, HVER-08, LVER-05, OPS-06, ENF-03]
must_haves:
  truths:
    - "The five Phase 18.1 requirement IDs HVER-06, HVER-07, HVER-08, LVER-05 and OPS-06 exist in .planning/REQUIREMENTS.md as checkbox bullets in the shipped D-239 style, each carrying its *(SC-n · D-nnn)* citation, plus one Traceability row each — so every later plan in this phase cites a real ID."
    - "D-236: an ops-forced cancellation refunds the booker the FULL booking amount. OPS_CANCEL_REFUNDS_SERVICE_FEE is the literal true; FitOut retains no service fee, the host is still paid nothing, and no host-cancel fee is charged."
    - "D-236 is proved as a ONE-LINE flip: withFlippedConstant still exists and still exercises the opposite branch, so both refund bases are measured rather than asserted."
    - "The fees.ts docblock and cancel-impact.ts's opsRefundBasisCents comment record that the PM SETTLED the D-209/D-236 conflict on 2026-09-01 in favour of true. The argument is inverted and dated, never deleted."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "A `## Phase 18.1` requirements section (5 bullets) plus 5 Traceability rows"
      contains: "HVER-06"
    - path: "src/lib/payments/fees.ts"
      provides: "OPS_CANCEL_REFUNDS_SERVICE_FEE = true, with the inverted+dated docblock above it"
      contains: "OPS_CANCEL_REFUNDS_SERVICE_FEE"
    - path: "src/lib/ops/cancel-impact.ts"
      provides: "opsRefundBasisCents with the same inverted+dated D-236 argument"
      contains: "opsRefundBasisCents"
    - path: "tests/payments/ops-cancel.test.ts"
      provides: "Case 1 = SHIPPED (true), case 2 = FLIPPED (false), case 3's two expectation sets swapped, withFlippedConstant mocking false"
      contains: "withFlippedConstant"
  key_links:
    - from: "src/lib/ops/cancel-impact.ts"
      to: "src/lib/payments/fees.ts"
      via: "opsRefundBasisCents reads OPS_CANCEL_REFUNDS_SERVICE_FEE"
      pattern: "OPS_CANCEL_REFUNDS_SERVICE_FEE"
    - from: "tests/payments/ops-cancel.test.ts"
      to: "src/lib/payments/fees.ts"
      via: "case 1 asserts the shipped literal; withFlippedConstant mocks the other branch"
      pattern: "OPS_CANCEL_REFUNDS_SERVICE_FEE"
---

<objective>
Open the phase's requirement ledger and discharge the first of the three carried PM decisions.

Two things, both prerequisites for everything after them. **First**, write `HVER-06`, `HVER-07`,
`HVER-08`, `LVER-05` and `OPS-06` into `.planning/REQUIREMENTS.md` so the nine plans that follow can
cite real IDs (the D-239 convention Phase 18 used — these IDs are written at plan time, not at
milestone time). **Second**, flip `OPS_CANCEL_REFUNDS_SERVICE_FEE` to `true` per **D-236**.

⚠ **D-236 is NOT a one-line change**, despite three phase documents saying so. Production is one line;
the test half is four coordinated edits, and `tests/payments/ops-cancel.test.ts` case 1 **fails by
design** carrying its own change instruction. Read the failure messages before changing any assertion.

Purpose: a rejected booking cancelled by ops now returns the whole charge, which is what the PM
decided on 2026-09-01, and the phase gets its requirement IDs.
Output: 5 requirement bullets + 5 traceability rows; one production literal; two inverted docblocks;
four test edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-CONTEXT.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-RESEARCH.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the five Phase 18.1 requirement IDs into REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — the `## Phase 18` section shows the exact shipped bullet
      style (`- [ ] **ID** — sentence *(SC-n · D-nnn)*`) and the § Traceability table's column shape.
    - `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-RESEARCH.md`
      § Phase Requirements — the five sentences are ALREADY DRAFTED there with their SC and D
      citations. Use them; do not re-author them.
    - `.planning/ROADMAP.md` § Phase 18.1 — the eight success criteria the SC-n citations point at.
  </read_first>
  <action>
    Add a new `## Phase 18.1 — Close Phase 18: the verification path FitOut is legally required to have`
    section immediately after the existing `## Phase 18` section and before `---` / `## Traceability`.

    Mirror Phase 18's sub-grouping: put `HVER-06`, `HVER-07`, `HVER-08` under a `### HVER — Host
    identity verification` heading, `LVER-05` under `### LVER — Listing review`, `OPS-06` under
    `### OPS — Staff identity, ops console & audit`. Each bullet is `- [ ]` (unchecked — this phase has
    not shipped yet), bold ID, the sentence lifted verbatim from 18.1-RESEARCH § Phase Requirements,
    then the `*(SC-n · D-nnn)*` citation that table already carries.

    Add a short scope note under the section heading stating that these five IDs were written at plan
    time following the D-239 convention, and that `D-2xx` numbers referenced live in
    `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-CONTEXT.md`
    (D-255..D-271) or in the Phase 18 `18-CONTEXT.md` for numbers at or below D-254.

    Add five rows to the § Traceability table, one per ID, in the shipped column shape
    (`| Requirement | Phase | Status |`), with Phase reading `Phase 18.1 · <plan numbers>` and Status
    reading `Planned` — the executor of the later plan updates it to Complete.

    Update the count line under the table so it names Phase 18.1's five alongside Phase 18's
    seventeen, rather than leaving the "17 requirements" sentence reading as the whole file.

    ⚠ Do NOT touch the `## Deferred` list in this task — plan 18.1-03 owns removing the
    title/description entry from it, and two plans editing the same list is a merge conflict.
  </action>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*HVER-06\*\*\|^- \[ \] \*\*HVER-07\*\*\|^- \[ \] \*\*HVER-08\*\*\|^- \[ \] \*\*LVER-05\*\*\|^- \[ \] \*\*OPS-06\*\*" .planning/REQUIREMENTS.md` returns 5
    - `grep -c "^| HVER-06 \|^| HVER-07 \|^| HVER-08 \|^| LVER-05 \|^| OPS-06 " .planning/REQUIREMENTS.md` returns 5
    - Every one of the five bullets contains a `*(` … `D-` … `)*` citation
    - `grep -c "Title/description as material-edit fields" .planning/REQUIREMENTS.md` still returns 1 (this task does not touch § Deferred)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "HVER-06\|HVER-07\|HVER-08\|LVER-05\|OPS-06" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Five new requirement bullets and five traceability rows exist; later plans can cite real IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Flip OPS_CANCEL_REFUNDS_SERVICE_FEE and invert both docblocks</name>
  <files>src/lib/payments/fees.ts, src/lib/ops/cancel-impact.ts</files>
  <read_first>
    - `src/lib/payments/fees.ts` — the whole file. Lines 47-81 are the ~35-line D-209/D-236 docblock;
      line 82 is the constant. Its header at :1-30 states the A-LITERAL-NOT-`process.env` rule.
    - `src/lib/ops/cancel-impact.ts` — `opsRefundBasisCents` and its comment, which `fees.ts:71-75`
      names as *"the one expression both `cancelBookingAsOps` and `loadOpsCancelImpact` call"* and
      which carries the D-236 argument in full with the precedent quoted verbatim.
    - `tests/design/money-path-invariants.test.ts` — a build-blocking design gate over money-path
      properties. Read it BEFORE the flip so a red is recognised rather than debugged.
    - `18.1-RESEARCH.md` § `src/lib/payments/fees.ts:82` + `tests/payments/ops-cancel.test.ts` —
      the six-edit table with line numbers.
  </read_first>
  <action>
    Change `src/lib/payments/fees.ts:82` from `export const OPS_CANCEL_REFUNDS_SERVICE_FEE = false;`
    to `= true;`.

    Rewrite the docblock above it (currently ~:47-81) so it records that the PM SETTLED the
    D-209/D-236 conflict on **2026-09-01** in favour of `true`, and states the settled reading: an
    ops-forced cancellation returns the full booking amount to the booker, FitOut retains no service
    fee, the host is paid nothing, and no host-cancel fee is charged. **Do not delete the argument —
    invert its conclusion and date it.** The paragraph that currently reasons that `false` makes
    FitOut less generous to a defrauded booker than to one whose host merely flaked becomes the
    reason the flip happened, not a live objection.

    Keep three things byte-identical inside that docblock: the paragraph explaining this is A LITERAL
    and NOT a `process.env` read (a behaviour change must be a reviewed code change, never an env var
    that can flip silently); the clause recording that it is deliberately absent from `.env.example`;
    and the pointer to `opsRefundBasisCents` in `src/lib/ops/cancel-impact.ts`.

    Give `opsRefundBasisCents`'s comment in `src/lib/ops/cancel-impact.ts` the same treatment: same
    inversion, same date, the quoted precedent preserved. Both comments must agree — they are the two
    places a reader looks, and a stale one is worse than none.

    ⚠ Do not touch the fee arithmetic, the host-cancel fee path, or anything `deriveBookable` reads.
    ENF-03's other two clauses (host paid nothing, no host-cancel fee) are already correct and
    unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "export const OPS_CANCEL_REFUNDS_SERVICE_FEE = true;" src/lib/payments/fees.ts` returns 1
    - `grep -c "process.env" src/lib/payments/fees.ts` is unchanged from before the edit (the literal-not-env paragraph survives as prose; the file reads no env var)
    - `src/lib/payments/fees.ts` still contains the docblock pointer to `opsRefundBasisCents` and the `.env.example` clause: `grep -c "opsRefundBasisCents" src/lib/payments/fees.ts` >= 1 and `grep -c "env.example" src/lib/payments/fees.ts` >= 1
    - Both `src/lib/payments/fees.ts` and `src/lib/ops/cancel-impact.ts` contain the string `2026-09-01`
    - `npx vitest run --config vitest.design.config.ts tests/design/money-path-invariants.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/money-path-invariants.test.ts</automated>
  </verify>
  <done>The constant is `true`, both docblocks record the settlement and its date, and the money-path design gate is green.</done>
</task>

<task type="auto">
  <name>Task 3: Swap the four coordinated edits in tests/payments/ops-cancel.test.ts</name>
  <files>tests/payments/ops-cancel.test.ts</files>
  <read_first>
    - `tests/payments/ops-cancel.test.ts` — read cases 1 (~:374-403), 2 (~:405-431), 3 (~:433-459) and
      `withFlippedConstant` (~:243-265) IN FULL, including their failure messages and docblocks. Case
      1's message literally says *"swap cases 1 and 2 if D-236 is re-decided"*; `withFlippedConstant`'s
      docblock says *"THIS IS WHAT MAKES 'ONE LINE FLIPS IT' MEASURED… the ONLY thing this helper
      changes is that literal."*
    - `src/lib/payments/fees.ts` (already edited in task 2) for the constants `HOURLY`, `SERVICE_FEE`,
      `TOTAL` the cases compute against.
    - `18.1-RESEARCH.md` § fees.ts table rows 3-6 — the exact before/after for each edit.
  </read_first>
  <action>
    Make the four edits the test file's own messages instruct, in this order:

    1. **Case 1 becomes the SHIPPED (`true`) case.** Its guard assertion becomes `.toBe(true)` and its
       message is rewritten to say case 1 asserts the SHIPPED (`true`) basis and to keep the
       swap-cases-1-and-2 instruction pointing the other way. Its expectations become the full charge:
       `refundCents: TOTAL`, `row.refundCents === 105000`, `TOTAL - refundCents === 0`, and
       `createRefund` called with `amountCents: 105000`.
    2. **Case 2 becomes the FLIPPED (`false`) case**, still running through `withFlippedConstant`,
       expecting the space price: `HOURLY` / `100000` / retained `5000`. **Keep its "every other
       consequence is unchanged" block verbatim** — `cancelledBy === "ops"`, `retainedSpaceCents === 0`,
       zero debit rows. That block is what makes the one-line claim measured.
    3. **`withFlippedConstant`'s mock literal becomes `false`.** ⚠ **DO NOT DELETE THIS HELPER.**
       Exercising both branches is the whole reason the D-236 flip can be called one line.
    4. **Case 3's two expectation sets swap.** `shipped` becomes `refundTotal = formatMoney(2 * TOTAL)`
       / `retainedTotal = formatMoney(0)`; `flipped` becomes `formatMoney(2 * HOURLY)` /
       `formatMoney(2 * SERVICE_FEE)`. `cancellableCount`, `hostPaid: "Nothing"` and
       `notCancellableCount: 0` do not move.

    Cases 4-16 read the constant nowhere and must not be touched. Do not add a case; do not rename a
    describe block.
  </action>
  <acceptance_criteria>
    - `grep -c "withFlippedConstant" tests/payments/ops-cancel.test.ts` is >= 3 (the helper still exists and is still called)
    - `grep -c "OPS_CANCEL_REFUNDS_SERVICE_FEE: false" tests/payments/ops-cancel.test.ts` returns 1 (the mock now flips to false)
    - `npx vitest run tests/payments/ops-cancel.test.ts` passes with the same case count as before the edit
    - Deliberate mutation check: temporarily setting `src/lib/payments/fees.ts:82` back to `false` reddens case 1 and case 3's `shipped` set; restore to `true` and both go green again
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/payments/ops-cancel.test.ts</automated>
  </verify>
  <done>All 16 cases pass with the constant at `true`; both refund bases are still exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops staff → `cancelBookingAsOps` | Already gated by `requireStaff()` (Phase 18, OPS-02/OPS-03). This plan changes the amount that moves, not who may move it. |
| FitOut → PayMongo refund API | The refund amount crosses to the payment provider. It is derived server-side from one named constant, never from a request field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-0101 | Tampering | `OPS_CANCEL_REFUNDS_SERVICE_FEE` | mitigate | The value stays a source LITERAL, never `process.env` — a behaviour change is a reviewed code change and cannot flip at runtime. The literal-not-env paragraph in `fees.ts` is preserved verbatim by task 2. |
| T-18.1-0102 | Elevation of Privilege | ops cancel-and-refund reachable by any staff member | accept | D-215 stands: one staff role, no tiers. The authenticated `audit` row (OPS-03) is the control. Unchanged by this plan. |
| T-18.1-0103 | Repudiation | refund amount disagreeing between the impact preview and the money moved | mitigate | `opsRefundBasisCents` is the single expression both `cancelBookingAsOps` and `loadOpsCancelImpact` call; case 3 of `tests/payments/ops-cancel.test.ts` asserts the preview figures agree with the money moved under BOTH branches. |
| T-18.1-01-SC | Tampering | npm installs | mitigate | Zero package installs in this plan. `package.json` and the lockfile are not in `files_modified`. |
</threat_model>

<verification>
Run each gate ALONE — never chained:

1. `npx vitest run tests/payments/ops-cancel.test.ts`
2. `npx tsc --noEmit`
3. `npm test`
4. `npm run test:design`
5. `npm run build`
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` carries five new `- [ ]` bullets (HVER-06/07/08, LVER-05, OPS-06) with SC and D citations, and five traceability rows
- `OPS_CANCEL_REFUNDS_SERVICE_FEE === true` in `src/lib/payments/fees.ts`
- `withFlippedConstant` still exists and now mocks `false`
- `tests/payments/ops-cancel.test.ts` is green with no case deleted
- All four gates green, each invoked separately
</success_criteria>

<output>
Create `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-01-SUMMARY.md` when done
</output>
