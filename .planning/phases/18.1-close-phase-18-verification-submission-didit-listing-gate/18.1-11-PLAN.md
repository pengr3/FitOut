---
phase: 18.1-close-phase-18-verification-submission-didit-listing-gate
plan: 11
type: execute
wave: 5
depends_on: ["18.1-07", "18.1-10"]
files_modified:
  - src/components/host/verification-panel.tsx
  - src/app/(host)/host/verify/page.tsx
  - src/app/(host)/host/verify/loading.tsx
  - src/components/host/host-signals.tsx
  - src/lib/design/live-regions.ts
  - tests/design/live-regions.test.tsx
  - tests/design/loading-coverage.test.ts
  - tests/design/card-pattern-coverage.test.ts
  - tests/design/host-tone-census.test.ts
  - tests/host/verification-surface.test.ts
autonomous: true
requirements: [HVER-06, HVER-08]
must_haves:
  truths:
    - "A host can ASK to be verified from a surface under /host: /host/verify renders one of six states, and the unverified and cooled-down-rejected states carry the submission form."
    - "D-269: the form's first gate is a confirmed email, on the shipped PublishChecklistRow grammar — a label, a done boolean, and an action present only when unmet — reusing the extracted resend with callbackURL pointing at /host/verify."
    - "D-268: the form's second gate is a phone number, a required tel input whose helper says FitOut keeps it to reach the host about their listings and that bookers never see it."
    - "D-264: when the cooldown has not elapsed the panel renders NO control and one sentence carrying an ABSOLUTE server-formatted timestamp. When it has, it renders the retry control and the form."
    - "D-266: the suspended branch renders the shipped HostingPausedNotice VERBATIM and draws NO submit control — the machine cannot reverse a named staff member's enforcement decision, so the product does not draw the button that would try."
    - "D-263: no control anywhere on the surface implies a person will look at the case, and no support address renders."
    - "The Didit callback carries NO AUTHORITY: /host/verify reads ZERO search params, its default export declares no searchParams parameter, and the page re-reads the row. PROJECT D-57's lesson one domain over."
    - "The design ledger moves with the surface, never behind it: loading-coverage 34/22/12 becomes 35/23/12, card-pattern-coverage 22 becomes 23, live-regions 29 becomes 30 with its compile alias renamed, host-tone-census gains two rows at exactly zero, and EXPECTED_ALARM_OCCURRENCES, ACCENT_USES, SUCCESS_HUE_SITES and BOUNDARIES all stay where they are."
  artifacts:
    - path: "src/components/host/verification-panel.tsx"
      provides: "The six-state panel composing PanelCard tone=muted, the two-gate form, and one named role=status refusal region"
      min_lines: 120
    - path: "src/app/(host)/host/verify/page.tsx"
      provides: "The RSC — async, reads loadHostVerification, takes no searchParams"
      min_lines: 30
    - path: "src/app/(host)/host/verify/loading.tsx"
      provides: "The plate — HOST_PANEL_SHELL + PageHeader + exactly one PanelSkeleton, no box class of its own"
    - path: "tests/host/verification-surface.test.ts"
      provides: "The structural searchParams scan and the total-map assertion"
  key_links:
    - from: "src/app/(host)/host/verify/page.tsx"
      to: "src/lib/host/verification-status.ts"
      via: "loadHostVerification — the fifth reader of the same helper, no new query"
      pattern: "loadHostVerification"
    - from: "src/components/host/verification-panel.tsx"
      to: "src/app/actions/host-verification.ts"
      via: "the form submits to requestHostVerification and renders its sentence verbatim"
      pattern: "requestHostVerification"
    - from: "src/components/host/host-signals.tsx"
      to: "/host/verify"
      via: "a fourth conditional advisory row — the way IN, rendered for unverified | pending | rejected only"
      pattern: "/host/verify"
---

<objective>
Ship `/host/verify` — the surface a host uses to ask to be checked.

`18.1-UI-SPEC.md` § Surface 1 and § Surface 2 are **APPROVED 6/6** and binding: the route, the six
states, the copy, the two gates, the one refusal region, the plate, and the eleven named gate
amendments. This plan builds exactly that and moves exactly those counts.

⚠ **The route is NOT in `HOST_NAV`.** Every `NavLink` carries a mandatory `why` because *"primary nav
is the scarcest surface in the app… it competes with the mode switch and the bell for 226px at
320px."* No honest `why` exists for a once-ever destination that is dead weight the moment it
succeeds. The way in is a fourth `HostSignals` advisory row.

⚠ **The page reads ZERO search params.** Didit's `callback` is a browser return the host controls and
carries no authority. There is no "thanks, you're approved!" screen driven by the redirect — for
almost every real return the page shows `pending`, because that is what the row says.

Purpose: SC1's host-facing half, and the destination the listing gate (plan 18.1-12) redirects to.
Output: one panel, one route, one plate, one advisory row, five moved gate counts, one structural
test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-UI-SPEC.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-PATTERNS.md
@.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The verification panel — six states, two gates, one refusal region</name>
  <files>src/components/host/verification-panel.tsx</files>
  <read_first>
    - `src/components/host/hosting-paused-notice.tsx` — the whole 77-line file. The exact shape to
      follow: `PanelCard tone="muted"` with the STATE as `title` and the REASON as a
      `<p className="text-body text-muted-foreground">`.
    - `src/lib/host/verification-signal.ts` (plan 18.1-10) — every string this component renders.
      **Do not type a sentence into this file.**
    - `src/app/actions/host-verification.ts` (plan 18.1-07) — the action and its five named refusal
      sentences. The component renders the action's sentence **verbatim**; no client re-authoring.
    - `src/components/ops/ops-decision-actions.tsx:315-333` — the shipped refusal region
      (`role="status"` + an author `aria-label` + ordinary ink) and the `disabled` + `aria-disabled`
      in-flight idiom. ⚠ `:330` is the exact author-named-region shape to reproduce.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx:779-784` — the `PublishChecklistRow` shape
      (label / done / `step: null` / `action` present only when unmet) and
      `src/components/host/publish-checklist.tsx`.
    - `src/lib/host/resend-verification.ts` (plan 18.1-10) — called with `callbackURL: "/host/verify"`.
    - `src/lib/design/live-regions.ts` — read `AUTHOR_NAMED_REGIONS` and the VoiceOver reason:
      *"a named live region can be announced BY ITS NAME INSTEAD OF ITS CONTENT."*
    - `18.1-UI-SPEC.md` § Surface 1 § The six states, § Surface 2 in full (both gates, the control,
      the hand-off line, the return, the one refusal region), § Color (why `Start the check` is a
      NEUTRAL SOLID and not `variant="brand"`), § Typography (the three type rules), § Spacing (the
      44px touch exception).
  </read_first>
  <action>
    Create `src/components/host/verification-panel.tsx`. It **composes** `PanelCard` and never opens a
    raw `<Card>`. It imports every sentence from `verification-signal.ts`.

    Render the state's copy per the UI-SPEC's six-state table. The `suspended` branch **imports and
    renders `HostingPausedNotice` unchanged** and draws no submit control (D-266). `approved` and
    `grandfathered` are terminal panels with a link to `/host/listings`.

    The form renders inside the `unverified` panel and inside the `rejected` panel **once the cooldown
    has elapsed**. It is ONE `<form>`, two gates, one control:
    - Gate 1, **confirmed email** — the `PublishChecklistRow` grammar: a label, a done boolean, and
      an `action` present only when unmet. When unmet, the control calls the extracted resend with
      `callbackURL: "/host/verify"`, and a line names the address the link goes to.
    - Gate 2, **phone number** — `<Label>` plus the vendored `<Input type="tel" name="phone" required>`
      with the helper line from the UI-SPEC.
    - The control is `<Button type="submit" size="touch">` **Start the check** — a NEUTRAL SOLID,
      **never `variant="brand"`**. Two reasons, either sufficient: the panel has exactly one control
      so it needs no hue to be found, and this is the button that hands a person's identity document
      to a third party, which is not a decision to paint in the conversion colour. In flight it is
      `disabled` **and** `aria-disabled` with the label **Starting…**.
    - The retry control on the `rejected` panel is `variant="outline" size="touch"` — **Ask for
      another check**.
    - Beneath the control, as a `text-label text-muted-foreground` line read BEFORE the press: the
      hand-off sentence. ⚠ It names the vendor's **existence** and **no vendor** — "our checking
      partner", not "Didit", no logo, no link. Naming the vendor puts a third party's brand on
      FitOut's trust surface and turns a vendor swap into a copy change, which is what the port exists
      to avoid.

    When the cooldown has **not** elapsed: **no control at all**, and the single absolute-timestamp
    sentence from `verification-signal.ts`'s composer.

    **One refusal region**, and only one: a `<p role="status" aria-label={HOST_VERIFICATION_REGION_NAME}
    className="text-label">` carrying the server's sentence verbatim. Ordinary ink, **no alarm
    colour**, no retry affordance of its own (the form's control is the retry), and **not a toast** —
    a refusal that leaves the surface on screen does not belong in a dismissible, timed,
    unaddressable overlay. Toasts stay for the resend outcome only, where the shipped wizard already
    uses them.

    ⚠ **ONE component tree at every width.** No `matchMedia`, no `useMediaQuery`, no
    viewport-conditional JSX. ⚠ **No `data-testid`** — every interactive element is reachable by
    `getByRole` and every container carries a declared id from the pattern it composes.
    ⚠ **Zero coral, zero success hue, zero check glyph, zero new lucide glyph.**
  </action>
  <acceptance_criteria>
    - `grep -c "<Card" src/components/host/verification-panel.tsx` returns 0 and `grep -c "PanelCard" src/components/host/verification-panel.tsx` returns at least 1
    - `grep -c "variant=\"brand\"" src/components/host/verification-panel.tsx` returns 0
    - `grep -Ec "tone=\"positive\"|CheckCircle2|--success" src/components/host/verification-panel.tsx` returns 0
    - `grep -Ec "matchMedia|useMediaQuery|data-testid" src/components/host/verification-panel.tsx` returns 0
    - `grep -c "role=\"status\"" src/components/host/verification-panel.tsx` returns exactly 1
    - `grep -ci "didit" src/components/host/verification-panel.tsx` returns 0
    - Every host-visible sentence is imported from `@/lib/host/verification-signal` or returned by the action — `grep -Ec "\"[A-Z][a-z]+ [a-z]+ [a-z]+" src/components/host/verification-panel.tsx` finds no prose literal
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/one-tree.test.ts</automated>
  </verify>
  <done>Six states render from the copy module, the form has two gates and one control, and exactly one named region carries every refusal.</done>
</task>

<task type="auto">
  <name>Task 2: The route, its plate, and the way in from /host</name>
  <files>src/app/(host)/host/verify/page.tsx, src/app/(host)/host/verify/loading.tsx, src/components/host/host-signals.tsx</files>
  <read_first>
    - `src/app/(host)/host/earnings/page.tsx:140-185` — the host RSC shape, and
      `src/app/(host)/host/earnings/loading.tsx` — the plate shape.
    - `src/app/(host)/host/loading.tsx:52-58` and `:69` — the discipline note (*"the count is
      measured here rather than…"*) and `HOST_PANEL_SHELL`'s use.
    - `src/lib/design/measurements.ts:479` (`HOST_PANEL_SHELL`), `:230` (`PANEL_MIN_HEIGHT`), `:251`
      (`TEXT_BAR_HEIGHT`), `:94` (`RowSkeletonHeight`). ⚠ **This file DOES NOT MOVE in this plan.**
    - `src/components/patterns/panel-skeleton.tsx` — it supplies `role="status" aria-busy="true"`,
      the `aria-label` NAME and the `sr-only` CONTENT (two mechanisms, both required) and its bars are
      `aria-hidden`.
    - `src/components/host/host-signals.tsx:109-119` — the shipped conditional-advisory shape
      (`PanelCard tone="muted"` + a sentence + an inline underlined `<Link>`); `:114` is the shipped
      precedent for a conditional route.
    - `src/lib/nav.ts:55-115` — `HOST_NAV_IDS = ["earnings","requests"]` and the mandatory `why`.
      ⚠ **Do not add a row.**
    - `src/app/(host)/host/error.tsx` — the inherited boundary. ⚠ `BOUNDARIES` does not move.
    - `18.1-UI-SPEC.md` § Surface 1 § Layout and its `loading.tsx` block, verbatim.
  </read_first>
  <action>
    Create `src/app/(host)/host/verify/page.tsx` as an **async** default export. It uses
    `HOST_PANEL_SHELL` (a single-panel host surface, like `/host` and
    `/host/listings/[id]/availability`), renders `PageHeader` with the UI-SPEC's title and lede, and
    beneath an `mt-8` data-region offset renders `HostingPausedNotice` for `suspended` or
    `VerificationPanel` otherwise. It reads `loadHostVerification` — the **fifth** reader of the same
    helper, no new query — and passes the retry instant through the copy module's composer so the
    component performs no formatting.

    ⚠ **The default export declares NO `searchParams` parameter and the file contains ZERO
    occurrences of `searchParams`.** Write the reason at the site: Didit's `callback` is a browser
    return the host controls and carries no authority; the page re-reads the row. This is PROJECT
    D-57's lesson one domain over, the same shape `pending-payment-state.tsx` exists for on the money
    path.

    Create `src/app/(host)/host/verify/loading.tsx` exactly as the UI-SPEC specifies: `HOST_PANEL_SHELL`,
    `PageHeader` carrying **the same expressions the page carries, character for character**, an
    `mt-8` wrapper, and **exactly one** `PanelSkeleton` with the declared label. ⚠ **It writes NO box
    measurement of its own** — `loading-coverage.test.ts` fails a `loading.tsx` that does.

    Add a **fourth conditional advisory row** to `src/components/host/host-signals.tsx` on the shipped
    `:109-119` shape, linking to `/host/verify`. ⚠ Rendered for `unverified | pending | rejected`
    **only** — `approved` and `grandfathered` are silent because nothing is owed, and `suspended`
    already renders `HostingPausedNotice` above it, so a second row there would be two panels about
    one state.

    ⚠ **Do NOT add `/host/verify` to `HOST_NAV`.**
  </action>
  <acceptance_criteria>
    - `grep -c "searchParams" src/app/\(host\)/host/verify/page.tsx` returns 0
    - The default export of `src/app/(host)/host/verify/page.tsx` is `async`
    - `grep -c "PanelSkeleton" src/app/\(host\)/host/verify/loading.tsx` returns 1 and `grep -Ec "min-h-|h-[0-9]|max-w-(?!3xl)" src/app/\(host\)/host/verify/loading.tsx` finds no box class beyond `HOST_PANEL_SHELL`
    - The `PageHeader` title and lede expressions in `loading.tsx` are character-identical to the page's
    - `git diff src/lib/nav.ts` is empty
    - `git diff src/lib/design/measurements.ts` is empty
    - `git diff tests/design/error-boundaries.test.ts` is empty — `BOUNDARIES` stays at 6 rows
    - `grep -c "/host/verify" src/components/host/host-signals.tsx` returns at least 1, inside a branch covering exactly `unverified`, `pending` and `rejected`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/error-boundaries.test.ts</automated>
  </verify>
  <done>The route exists with its plate, the way in is a conditional advisory, and neither the nav nor the measurements moved.</done>
</task>

<task type="auto">
  <name>Task 3: Move the design ledger — five counts, each with its written reason</name>
  <files>src/lib/design/live-regions.ts, tests/design/live-regions.test.tsx, tests/design/loading-coverage.test.ts, tests/design/card-pattern-coverage.test.ts, tests/design/host-tone-census.test.ts, tests/host/verification-surface.test.ts</files>
  <read_first>
    - `tests/design/loading-coverage.test.ts:290-292` — `EXPECTED_PAGES = 34`, `EXPECTED_QUALIFYING = 22`,
      `EXPECTED_NON_QUALIFYING = 12`, and the header requiring an enumerated verdict paragraph for
      each pinned number.
    - `tests/design/card-pattern-coverage.test.ts:612` — `EXPECTED_SURFACES = 22`, the inventory rows'
      shape, the mandatory >40-character reason, and `ALLOWED_RAW_CARD`.
    - `src/lib/design/live-regions.ts:352` (`LIVE_REGION_FILES`), `:1798` (the alias history
      paragraph), `:1917` (`export type DeclaredFileCountIsTwentyNine`), plus `LIVE_REGION_IDS`,
      `LIVE_REGIONS` and `AUTHOR_NAMED_REGIONS`. Also `tests/design/live-regions.test.tsx:231` and
      `:549` — the second pin.
    - `tests/design/host-tone-census.test.ts` — `DECLARED_NEUTRAL_SIGNALS`, `EXPECTED_ALARM_OCCURRENCES`
      (an equality map in BOTH directions) and `TREES`.
    - `18.1-UI-SPEC.md` § Gate Amendments rows 5, 6, 7, 8 and the § DOES NOT MOVE table in full.
  </read_first>
  <action>
    Move exactly five counts, each in the same commit as its reason. ⚠ **MEASURE THE REDS ONE AT A
    TIME** — watch each gate fail at its old number before writing the new one, and record the reds
    in the SUMMARY. A silently bumped constant is the exact failure these gates exist to prevent.

    1. **`tests/design/loading-coverage.test.ts`** — `EXPECTED_PAGES` **34 -> 35**,
       `EXPECTED_QUALIFYING` **22 -> 23**, `EXPECTED_NON_QUALIFYING` **12 -> 12, UNCHANGED**. The one
       new page is `src/app/(host)/host/verify/page.tsx`, whose default export is `async` (it awaits
       `loadHostVerification`), so it **qualifies** and its `loading.tsx` ships in the same commit as
       the route. Add the enumerated verdict paragraph the file's header requires.
    2. **`tests/design/card-pattern-coverage.test.ts`** — `EXPECTED_SURFACES` **22 -> 23** plus one
       inventory row for `src/components/host/verification-panel.tsx` mapping to `panel-card`,
       `status: "adopted"`, with a **>40-character** reason. ⚠ **`ALLOWED_RAW_CARD` UNCHANGED** — the
       panel composes the pattern from its first commit, so there is no allow-list row to delete.
    3. **`src/lib/design/live-regions.ts`** — `LIVE_REGION_FILES` **29 -> 30**, adding
       `src/components/host/verification-panel.tsx`. `LIVE_REGION_IDS` **+1**. `LIVE_REGIONS` **+1
       row**, `kind: "status"`, **rule 1** (the result of something the user did).
       `AUTHOR_NAMED_REGIONS` **+1**: the host verification region name, with its reason. Rename the
       compile alias `DeclaredFileCountIsTwentyNine` -> `DeclaredFileCountIsThirty` and move the
       second pin at `tests/design/live-regions.test.tsx:231`.
       ⚠ **The UI-SPEC states 29 -> 31 in one move because it describes the whole phase.** This plan
       moves it to **30**; plan **18.1-13** moves it to **31** when the ops reveal island exists. The
       two files cannot be declared in one step because the gate requires each declared file to exist
       and to contain its region. Extend the alias-history paragraph at `:1798` to record BOTH steps
       so the sequence reads as a decision.
       ⚠ The module's **membership rule** was widened in Phase 18 to name the internal ops surface; it
       must now also cover a **host** surface outside the audited booker/supply journeys. Widen it
       **in words**, in the same paragraph.
    4. **`tests/design/host-tone-census.test.ts`** — `DECLARED_NEUTRAL_SIGNALS` gains **two rows at
       exactly zero**: `src/lib/host/verification-signal.ts` and
       `src/components/host/verification-panel.tsx`, each with a `why` naming D-262/D-264's states as
       normal lifecycle states. ⚠ **`EXPECTED_ALARM_OCCURRENCES` DOES NOT MOVE** and **`TREES` does
       not move.** This amendment is strictly stronger: nothing is relaxed, no exclusion is added,
       two more files are policed.
    5. Create **`tests/host/verification-surface.test.ts`** with two structural cases:
       - the page file contains **zero** occurrences of `searchParams` and its default export
         declares no such parameter — the ops-guard census's syntactic-walk idiom applied to one file;
       - `VERIFICATION_SIGNAL` covers all six `host_verification_status` values, asserted at runtime
         as well as by `tsc`, so a partial map cannot ship behind a cast.

    ⚠ **DO NOT TOUCH**, and add a criterion for each: `src/lib/design/accent-uses.ts` (stays 10, a
    compile constraint), `tests/design/brand-recipe.test.ts` (total stays 20),
    `tests/design/status-vocab.test.ts` `SUCCESS_HUE_SITES` (stays 5), `src/lib/design/status-tones.ts`
    (four tones), `src/lib/design/selector-contract.ts` (this phase adds no `data-testid`),
    `src/lib/design/visual-baselines.ts` (78 baselines — court is the single product theme and this is
    a once-ever host utility surface; record it as a DECISION, not an oversight),
    `tests/design/empty-state-adoption.test.ts` (5 / 10 / 11 — this phase declares no `EmptyState`
    adopter), `tests/design/trust-signals.test.ts` (this phase ships no booker-facing signal).
  </action>
  <acceptance_criteria>
    - `grep -c "EXPECTED_PAGES = 35" tests/design/loading-coverage.test.ts` returns 1; `EXPECTED_QUALIFYING = 23`; `EXPECTED_NON_QUALIFYING = 12`
    - `grep -c "EXPECTED_SURFACES = 23" tests/design/card-pattern-coverage.test.ts` returns 1 and the new inventory row's reason is longer than 40 characters
    - `grep -c "DeclaredFileCountIsThirty\b" src/lib/design/live-regions.ts` returns 1 and `DeclaredFileCountIsTwentyNine` returns 0
    - `LIVE_REGION_FILES` has 30 members; `AUTHOR_NAMED_REGIONS` gained exactly one entry with a non-empty reason
    - `git diff tests/design/host-tone-census.test.ts` shows two added `DECLARED_NEUTRAL_SIGNALS` rows and NO change to `EXPECTED_ALARM_OCCURRENCES` or `TREES`
    - `git diff src/lib/design/accent-uses.ts src/lib/design/status-tones.ts src/lib/design/selector-contract.ts src/lib/design/visual-baselines.ts tests/design/brand-recipe.test.ts tests/design/status-vocab.test.ts tests/design/empty-state-adoption.test.ts tests/design/trust-signals.test.ts` is EMPTY
    - `npx vitest run tests/host/verification-surface.test.ts` passes with both structural cases
    - Each of the five moved counts has a recorded RED at its old value in the SUMMARY
    - `npm run build` succeeds (the design gates are build-blocking)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/loading-coverage.test.ts tests/design/card-pattern-coverage.test.ts tests/design/live-regions.test.tsx tests/design/host-tone-census.test.ts</automated>
  </verify>
  <done>Five counts moved with their reasons, eight inventories provably did not, and the searchParams refusal is structurally pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **B4** | Didit's `callback` redirect -> `/host/verify`. Untrusted in — a browser return the host fully controls. |
| B1 (client half) | Host browser -> the submission form. The gates rendered here are hints; the action is the gate. |
| B5 | Vendor-authored rejection sentence -> the rendered panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-1101 | Spoofing | **a host asserting their own approval via the callback redirect** — e.g. `/host/verify?status=approved` | mitigate | The page reads ZERO search params: the default export declares no `searchParams` and the file contains zero occurrences of the identifier, asserted structurally in `tests/host/verification-surface.test.ts`. The page re-reads `host_verification`. PROJECT D-57's lesson: the webhook is the authority, not the forgeable browser return. |
| T-18.1-1102 | Elevation of Privilege | the client-side email/phone gates being mistaken for the gate | mitigate | Both are HINTS. `requestHostVerification` re-reads `user.emailVerified` and re-validates the phone server-side and refuses independently (plan 18.1-07). Stated at both sites. |
| T-18.1-1103 | Elevation of Privilege | a suspended host reaching a resubmit control | mitigate | **D-266 made visual**: the `suspended` branch renders the shipped `HostingPausedNotice` and draws NO submit control. The server refuses structurally anyway (`suspended` absent from the UPDATE's WHERE), so the UI and the action agree without either depending on the other. |
| T-18.1-1104 | Information disclosure | a refusal's shape distinguishing a suspended host from one whose cooldown has not elapsed | mitigate | ONE named region renders the action's ONE calm 0-row sentence verbatim; the client does not re-author or branch on the refusal. |
| T-18.1-1105 | Tampering | XSS via the vendor's rejection sentence | mitigate | Rendered as a React text node through the shipped escaped path (T-18-1301). No raw-HTML escape hatch anywhere in `src/`, and its name is not spelled in any new comment. |
| T-18.1-1106 | Repudiation | a display countdown disagreeing with the server's cooldown | mitigate | The instant is `updated_at + 24h`, composed SERVER-SIDE by the copy module and passed in FINISHED; the component performs no arithmetic and no formatting. PROJECT D-130 / GATE-05 is the named gate. |
| T-18.1-1107 | Information disclosure | the vendor's brand appearing on FitOut's trust surface | mitigate | The hand-off line names the vendor's existence and no vendor. `grep -ci "didit"` over the panel returns 0. A vendor swap stays a registration, not a copy change. |
| T-18.1-11-SC | Tampering | supply chain — a copied UI block from an unvetted registry | mitigate | `components.json` declares `"registries": {}`; **no block is fetched from any registry** and no primitive is vendored. Every component used (`button`, `input`, `label`) is already in `src/components/ui/**`. Zero npm installs; `package.json` and the lockfile are not in `files_modified`. |
</threat_model>

<verification>
Run each gate ALONE — never chained:

1. `npx vitest run tests/host/verification-surface.test.ts`
2. `npx vitest run --config vitest.design.config.ts tests/design/loading-coverage.test.ts`
3. `npx vitest run --config vitest.design.config.ts tests/design/card-pattern-coverage.test.ts`
4. `npx vitest run --config vitest.design.config.ts tests/design/live-regions.test.tsx`
5. `npx vitest run --config vitest.design.config.ts tests/design/host-tone-census.test.ts`
6. `npx tsc --noEmit`
7. `npm test`
8. `npm run test:design`
9. `npm run build`

Manual, deferred to plan 18.1-14: `/host/verify` hand-measured at **320 and 1280, both themes** in
the `unverified`, `pending` and `rejected` states — overflow, focus order, and that the
state/reason/way-out sentence is present. This is the Phase 17 convention and no `tests/design/**`
gate can see it.
</verification>

<success_criteria>
- `/host/verify` renders all six states; `suspended` renders the shipped notice with no submit control
- The form has two gates and one neutral-solid touch-sized control; in flight it is `disabled` + `aria-disabled`
- The cooldown-not-elapsed branch renders no control and one absolute-timestamp sentence
- Exactly one `role="status"` region carries every refusal, verbatim from the action
- The page contains zero occurrences of `searchParams`
- `loading-coverage` reads 35 / 23 / 12; `card-pattern-coverage` reads 23; `live-regions` reads 30
- `EXPECTED_ALARM_OCCURRENCES`, `ACCENT_USES` (10), the brand total (20), `SUCCESS_HUE_SITES` (5), `BOUNDARIES` (6), `measurements.ts`, `nav.ts` and `selector-contract.ts` all unchanged
- All four gates green, each invoked separately
</success_criteria>

<output>
Create `.planning/phases/18.1-close-phase-18-verification-submission-didit-listing-gate/18.1-11-SUMMARY.md` when done
</output>
