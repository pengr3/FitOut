---
phase: 24-search-bar-rework
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/lib/validation/booking.ts
  - src/lib/search/query.ts
  - tests/validation/booking-schemas.test.ts
  - tests/search/party-size-filter.test.ts
autonomous: true
requirements: [D-06, D-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-06, partySize is an optional scalar URL value that the server coerces to a whole number only within 1..MAX_OPEN_CAPACITY; omitted partySize keeps legacy and cold-browse URLs valid."
    - "Once a valid partySize marks an engaged Phase 24 search, retired date, time, price, radius-override, and relaxation inputs cannot alter the server query; canonical category, paired coordinates, display label, sorting, and paging remain available."
    - "Per D-06, the stage-one database query applies listing.max_occupancy >= partySize before LIMIT and OFFSET for both exclusive and open_capacity listings."
    - "A supplied partySize excludes NULL and below-request capacities and includes equality and above-request capacities; an omitted partySize applies no capacity predicate."
    - "Per D-07, capacity remains a predicate rather than result data: SearchResultRow does not gain maximum capacity, and a no-date search does not infer or claim remaining places."
  prohibitions:
    - statement: "The server does not accept a fractional, repeated, nonnumeric, zero, negative, or over-bound party size."
      status: resolved
      verification: explicit
    - statement: "The capacity predicate does not branch by occupancy mode or consult booking/admission counts."
      status: resolved
      verification: explicit
    - statement: "Maximum capacity is not added to the SELECT list, RawRow, SearchResultRow, or result cards."
      status: resolved
      verification: explicit
    - statement: "A Phase 24 party-size search does not invoke date-specific availability or derive remaining capacity."
      status: resolved
      verification: explicit
  artifacts:
    - path: src/lib/validation/booking.ts
      provides: "Server-owned bounded optional partySize parsing and engaged-search normalization on the existing searchParamsSchema/SearchParams contract."
      contains: "partySize"
    - path: src/lib/search/query.ts
      provides: "Parameterized pre-pagination maximum-capacity predicate in the existing searchListings stage-one SQL."
      contains: "max_occupancy"
    - path: tests/validation/booking-schemas.test.ts
      provides: "String coercion, optionality, scalar-shape, boundary, and engaged legacy-key normalization coverage."
    - path: tests/search/party-size-filter.test.ts
      provides: "Database-backed NULL/below/equal/above, occupancy-mode, no-date, and pre-pagination capacity coverage."
  key_links:
    - from: src/lib/validation/booking.ts
      to: src/lib/search/query.ts
      via: "SearchParams is inferred from searchParamsSchema, so searchListings receives only an optional bounded integer partySize."
      pattern: "partySize"
    - from: src/lib/search/query.ts
      to: src/lib/db/schema.ts
      via: "The stage-one SQL compares the existing listing.max_occupancy column with a Drizzle-bound partySize parameter before ordering and pagination."
      pattern: "max_occupancy"
    - from: tests/search/party-size-filter.test.ts
      to: src/lib/search/query.ts
      via: "An isolated Postgres schema proves filtering semantics against persisted exclusive/open_capacity rows rather than a mocked query."
      pattern: "searchListings"
---

<objective>
Harden the D-06/D-07 server contract for exact party size: bounded optional URL validation, safe normalization of retired refinements after Phase 24 engagement, and a parameterized maximum-capacity predicate that runs before pagination for both occupancy modes.

Purpose: Make the client journey introduced by 24-01 non-bypassable at the server/database boundary while preserving old browse URLs and keeping date-specific availability out of this search phase.
Output: A hardened searchParamsSchema/SearchParams contract, the final stage-one SQL predicate, focused schema tests, and one database-backed party-size integration suite.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-PLAN-OUTLINE.md
@.planning/phases/24-search-bar-rework/24-01-SUMMARY.md

<interfaces>
- The installed Next.js page guide states that page searchParams is a Promise of a plain object whose values may be string, string[], or undefined; src/app/(public)/page.tsx remains the async Server Component that safe-parses this untrusted object.
- searchParamsSchema in src/lib/validation/booking.ts is the sole public-search URL parser, and SearchParams is inferred from its output rather than maintained as a parallel type.
- Phase 24 engagement is identified server-side by a valid partySize because every completed new journey emits partySize, including For me as 1; missing partySize remains the legacy/cold-browse compatibility path.
- MAX_OPEN_CAPACITY is exported by src/lib/validation/listing.ts as 1_000 and is already the persisted listing-capacity validation ceiling; do not duplicate the bound.
- searchListings(db, params, now?, options?) in src/lib/search/query.ts owns stage-one SQL. SearchListingsOptions.fetchLimit can shrink the LIMIT and page size for a focused proof that filtering precedes pagination.
- src/lib/db/schema.ts already maps listing.maxOccupancy to max_occupancy and occupancyMode to the exclusive/open_capacity enum. This plan reads those columns but changes no schema declaration.
- PostgreSQL comparison against NULL is not true, so the same greater-than-or-equal predicate naturally excludes an unknown maximum whenever partySize is supplied.
- D-07 keeps date-aware spots.remaining in the existing availability stage only; a party-size search neither derives admissions nor exposes configured capacity.
</interfaces>
</context>

## Artifacts this phase produces

- Existing `searchParamsSchema` gains the `partySize` field and its engaged-search normalization; the inferred `SearchParams` type gains `partySize?: number`. No parallel parser or hand-maintained input type is created.
- Existing `searchListings` gains one optional Drizzle SQL fragment comparing `l.max_occupancy` with validated `partySize` before `ORDER BY`, `LIMIT`, and `OFFSET`.
- Existing `tests/validation/booking-schemas.test.ts` gains named party-size scalar/boundary/legacy-normalization cases.
- New path `tests/search/party-size-filter.test.ts` contains the isolated-database fixture matrix and the named suites `searchListings — exact party capacity` and `searchListings — party capacity precedes pagination`.
- No new endpoint, external API integration, exported production helper, SearchResultRow field, database column, migration, package, environment variable, or operator action is introduced.

## Spec-less probe fallback

Skipped visibly because ROADMAP.md maps no Phase 24 requirement IDs and records `Requirements: TBD`; no requirement-derived probe predicate is invented. D-06, D-07, the locked prohibitions, and the explicit database outcomes in this plan are the acceptance contract.

## Schema-push detection

Not triggered. `src/lib/db/schema.ts` and `drizzle/` are read-only inputs to this plan; the existing `listing.maxOccupancy` and `listing.occupancyMode` columns already provide every persisted value the predicate needs. Do not run or plan a schema push.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Exact party size safely participates in the progressive activity/location/party search | 24-03 Tasks 1-2 | COVERED | The URL value is validated and reaches one server-owned database predicate. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD and REQUIREMENTS.md has no Phase 24 mapping; no ID is invented. |
| RESEARCH | Optional bounded scalar partySize with legacy browse compatibility | 24-03 Task 1 | COVERED | Uses the shared capacity ceiling and rejects repeated/non-scalar input. |
| RESEARCH | New interaction drops retired keys while server parsing remains authoritative | 24-01 plus 24-03 Task 1 | COVERED | 24-01 emits the canonical URL; this plan prevents retired refinements from regaining query authority when partySize is present. |
| RESEARCH | Parameterized pre-pagination max_occupancy predicate for both modes | 24-03 Task 2 | COVERED | The DB suite proves NULL/below/equal/above and LIMIT ordering. |
| CONTEXT | D-06 exact party count and maximum-capacity filtering | 24-03 Tasks 1-2 | COVERED | The costly URL/query contract is hardened at both server boundaries. |
| CONTEXT | D-07 capacity-only semantics with no remaining/date claim | 24-03 Task 2 | COVERED | No capacity projection, admission count, or date inference is added. |
| EXCLUSION | Date availability, remaining places, party bands, and client-only filtering | — | EXCLUDED | Explicitly out of scope in CONTEXT/RESEARCH. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound partySize and normalize retired refinements after engagement</name>
  <reversibility rating="costly">Per D-06, changing the public partySize shape or its engagement semantics later requires coordinated URL, Server Component, query, and shared-link changes; this plan keeps that cost visible without adding a blocking checkpoint.</reversibility>
  <files>src/lib/validation/booking.ts, tests/validation/booking-schemas.test.ts</files>
  <behavior>
    - Next-delivered scalar strings `1` and the decimal spelling of MAX_OPEN_CAPACITY parse to integers at the inclusive boundaries.
    - Missing partySize remains valid and undefined so a cold browse or old shared URL is not forced through capacity filtering.
    - Zero, negative, fractional, over-MAX_OPEN_CAPACITY, empty, nonnumeric, boolean, and repeated string-array values fail safeParse without throwing.
    - With a valid partySize, supplied date/start/end/priceMax/radius/relax values are normalized to their retired/default state before SearchParams reaches searchListings; category, paired coordinates, locationLabel, sort, and page retain their validated values.
    - Without partySize, the existing legacy URL fields retain their current validation behavior for compatibility until the user engages the Phase 24 journey.
  </behavior>
  <read_first>
    - AGENTS.md — requires bundled Next.js guidance before relying on route conventions.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — current async plain-object searchParams shape, including repeated string[] values.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-06/D-07 exact-count authority and prohibited date/remaining semantics.
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — implemented partySize/locationLabel fields and the canonical client URL emitted by the tracer.
    - src/lib/validation/booking.ts — existing searchParamsSchema coercion, defaults, coordinate-pair refine, and inferred SearchParams type.
    - src/lib/validation/listing.ts — existing MAX_OPEN_CAPACITY authority.
    - tests/validation/booking-schemas.test.ts — current Next-string fixtures and safeParse boundary-test style.
  </read_first>
  <action>
    Start RED in `tests/validation/booking-schemas.test.ts`. Import `MAX_OPEN_CAPACITY`; extend the valid Next-string fixture with partySize and assert numeric coercion. Add focused cases for both inclusive boundaries, omission, every malformed/out-of-range shape listed in behavior, and a repeated one-element string array so JavaScript numeric coercion cannot accidentally authorize a repeated URL parameter. Add a paired compatibility test: the same valid retired refinements retain their present parsed behavior when partySize is absent, but when partySize is present they cannot survive as active date/time/price/radius-override/relaxation inputs. Preserve positive assertions for category, paired lat/lng, bounded locationLabel, sort, page, and partySize so normalization cannot erase canonical data along with retired data.

    Harden the existing `searchParamsSchema` rather than adding an ad-hoc page parser. Import the shared `MAX_OPEN_CAPACITY` and define partySize as an optional, scalar-only coerced integer bounded inclusively from one through that constant. Reject arrays, booleans, null, objects, and other non-string/non-number input before coercion; missing undefined remains optional. After the existing coordinate-pair validation, normalize parsed output only when partySize is present: remove the retired date, start, end, and price ceiling values; restore radius and relaxation to their established defaults instead of honoring URL overrides; and retain only the already-validated canonical answers plus supported sort/page state. Do not change the no-partySize branch, because safe old browse URLs are a locked compatibility allowance. Keep locationLabel display-only and keep SearchParams inferred from the schema output.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/validation/booking-schemas.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, accepts a malformed/repeated/out-of-range party value, rejects either inclusive boundary or omission, lets a retired refinement remain authoritative after partySize is present, or changes legacy parsing when partySize is absent.</fails_when>
  </verify>
  <acceptance_criteria>The focused schema suite exits 0 and proves D-06 partySize is optional, scalar, coerced, integer, shared-bound, and fail-closed while an engaged D-07 search cannot reactivate retired date/time/price/radius/relaxation behavior.</acceptance_criteria>
  <done>The URL boundary has one server-owned party-size contract that accepts exact safe counts, preserves untouched legacy browse URLs, and normalizes every engaged Phase 24 request before query execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the parameterized capacity predicate before pagination for both occupancy modes</name>
  <precondition>`docker info` exits 0 so the repository's isolated Postgres test harness can create and drop its test schema.</precondition>
  <reversibility rating="costly">Per D-06, the predicate defines public result semantics for shared partySize URLs; revising it requires coordinated server-query and expectation changes, but no irreversible data migration.</reversibility>
  <files>src/lib/search/query.ts, tests/search/party-size-filter.test.ts</files>
  <behavior>
    - With requested party size N, exclusive and open_capacity rows at NULL or N-1 are absent, while rows at N and N+1 are present.
    - Without partySize, the same otherwise-bookable NULL-capacity rows remain eligible, proving optional compatibility rather than an unconditional sell-gate change.
    - A bounded fetch whose newest rows are all ineligible still returns a full eligible page and truthful hasMore, proving the capacity predicate executes before LIMIT/OFFSET rather than filtering fetched rows in application code.
    - Both occupancy modes use the same configured maximum-capacity comparison; no booking, pass, RSVP, remaining-admission, or date calculation participates.
    - Returned rows do not expose a maximum-capacity property, and no-date open_capacity rows retain spots=null per D-07.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-06 maximum-capacity filter and D-07 ban on remaining/date inference.
    - .planning/phases/24-search-bar-rework/24-RESEARCH.md — exact SQL fragment, NULL semantics, and no-projection guidance.
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — tracer implementation and current SearchParams/query handoff.
    - src/lib/search/query.ts — SearchResultRow/RawRow projection, searchListings signature, stage-one SQL, fetchLimit/pageSize derivation, and date-aware stage-two boundary.
    - src/lib/db/schema.ts — existing listing.maxOccupancy and occupancyMode mappings; read only.
    - src/lib/validation/booking.ts — Task 1 validated SearchParams.partySize output.
    - tests/search/open-capacity-search.test.ts — isolated DB lifecycle, verified-host setup, occupancy-mode fixture, and spots=null no-date assertion.
    - tests/search/filters.test.ts — compact searchListings helper and category-isolated result assertions.
    - tests/helpers/seed.ts — makeVerifiedHost contract and required published/bookable fixture fields.
  </read_first>
  <action>
    Begin RED by creating `tests/search/party-size-filter.test.ts` with the established `setupTestDb`/`teardownTestDb` lifecycle and `makeVerifiedHost`. Seed published, review-approved listings with operating hours under one verified/payout-enabled host; every capacity case must set its value explicitly. Under one exact catalogue category, create NULL, N-1, N, and N+1 rows for each of `exclusive` and `open_capacity`, supplying the appropriate existing price fields but no bookings and no date. Assert the D-06 matrix for partySize=N, then omit partySize and prove both NULL rows return. Inspect returned no-date rows to prove `spots` remains null and the result objects have no configured-capacity field.

    Add a second category-isolated fixture set for the ordering trap: create at least five newer ineligible rows followed by at least five older eligible rows with explicit `createdAt` values. Call `searchListings` with partySize and `{ fetchLimit: 5 }`; assert four eligible rows plus `hasMore=true`. This must fail if capacity is checked after fetching, because all five pre-filter candidates would be ineligible. Keep the test database-backed and deterministic; do not mock Drizzle or duplicate the query as test SQL.

    In `searchListings`, destructure validated partySize and insert exactly one optional Drizzle-template fragment adjacent to the other stage-one predicates: compare `l.max_occupancy` greater than or equal to the bound parameter when partySize is defined. Place it before `ORDER BY`, `LIMIT`, and `OFFSET`, apply it identically regardless of `l.occupancy_mode`, and rely on PostgreSQL NULL comparison semantics to fail closed for unknown capacity. Do not concatenate SQL text, post-filter mapped rows, select max_occupancy, add it to RawRow/SearchResultRow, inspect bookings/admissions, or alter the date-aware stage-two loop. `locationLabel` remains absent from query destructuring and predicates.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/party-size-filter.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, either occupancy mode disagrees with the NULL/below/equal/above matrix, omission hides a NULL-capacity legacy row, the bounded page is not full/hasMore, a result exposes configured capacity, or a no-date row gains a remaining-spots value.</fails_when>
  </verify>
  <acceptance_criteria>The database-backed suite exits 0 and proves a bound SQL parameter enforces exact D-06 capacity before pagination for exclusive and open_capacity listings while D-07 keeps configured capacity and date-specific remaining places out of result data.</acceptance_criteria>
  <done>Server search applies one occupancy-mode-neutral maximum-capacity predicate at the database boundary, with NULL, equality, upper-side, omission, pagination-order, and no-date semantics pinned by real Postgres tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser URL to Next.js Server Component/Zod | partySize and every legacy query key arrive as attacker-controlled string, repeated string array, or malformed value. |
| Validated SearchParams to Drizzle/Postgres | Only the bounded scalar partySize may become the capacity comparison parameter. |
| Database row to public result model | Persisted capacity is query authority but must not cross into cards as availability or inventory data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-11 | Tampering | searchParamsSchema partySize input | high | mitigate | Reject non-scalars and nonnumeric/fractional/out-of-bound values, import MAX_OPEN_CAPACITY, and safe-parse before searchListings receives the value. |
| T-24-12 | Tampering | engaged URL legacy refinements | high | mitigate | When validated partySize marks Phase 24 engagement, normalize retired date/time/price/radius/relaxation fields before query execution while preserving the validated canonical answers. |
| T-24-13 | Tampering / Injection | searchListings stage-one SQL | high | mitigate | Bind partySize through Drizzle's SQL template and execute the comparison inside WHERE before pagination; concatenate no request value into SQL. |
| T-24-14 | Information Disclosure | RawRow/SearchResultRow projection | medium | mitigate | Use max_occupancy only in the WHERE clause, leave it out of SELECT and public row types, and assert its absence on returned objects. |
| T-24-15 | Spoofing | open-capacity availability meaning | medium | mitigate | Treat both occupancy modes identically for configured maximum capacity and leave date-aware remaining admissions solely in the existing availability stage. |
| T-24-16 | Denial of Service | partySize/pagination input | medium | mitigate | Enforce the shared finite integer ceiling before SQL and retain the existing bounded fetch/page calculations. |
| T-24-SC | Tampering | package supply chain | low | accept | The plan performs no package-manager action and changes no dependency declaration. |
</threat_model>

<verification>
  <automated>node node_modules/vitest/vitest.mjs run tests/validation/booking-schemas.test.ts</automated>
  <fails_when>The command exits non-zero, reports zero tests, or the optional scalar/boundary/engaged-normalization contract is not enforced.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run tests/search/party-size-filter.test.ts</automated>
  <fails_when>The command exits non-zero, reports zero tests, or the database does not prove both occupancy modes, NULL/equality boundaries, optionality, pre-pagination filtering, non-projection, and no-date semantics.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or emits a diagnostic for the transformed SearchParams type, query destructuring, Drizzle SQL fragment, or new integration fixtures.</fails_when>
</verification>

<success_criteria>
- D-06 partySize is optional for legacy/cold browse, but when supplied it is a server-validated scalar whole number from 1 through MAX_OPEN_CAPACITY.
- Retired date/time/price/radius/relaxation values cannot influence an engaged Phase 24 request even when manually appended to its URL.
- One parameterized stage-one predicate excludes NULL/below capacity and includes equality/above capacity before pagination for both exclusive and open_capacity listings.
- Omitting partySize preserves existing browse eligibility rather than silently converting configured capacity into a global sell gate.
- D-07 remains honest: no max-capacity projection, remaining-place arithmetic, booking/admission lookup, or inferred date behavior is added.
- No schema-relevant file is modified, so no migration or schema push is required.
</success_criteria>

<output>
Create .planning/phases/24-search-bar-rework/24-03-SUMMARY.md when done.
</output>
