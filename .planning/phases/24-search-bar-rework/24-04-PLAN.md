---
phase: 24-search-bar-rework
plan: 04
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/app/(public)/page.tsx
  - src/app/(public)/loading.tsx
  - src/components/search/search-results.tsx
  - tests/search/search-results-states.test.tsx
autonomous: true
requirements: [D-01, D-02, D-07, D-08]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, the hard-navigation streaming fallback reserves the progressive search pill's geometry with inert server-rendered markup and never mounts a second interactive search tree."
    - "Per D-08, a completed populated search and a calm completed no-results state both remain inside the same SearchExperience and therefore expose the same activity, location, and party Edit chips."
    - "Per D-02, each chip remains a direct route back to one retained answer; the empty state does not replace those edits with a reset or broadening control."
    - "The public page performs one validated canonical activity/location/party query and never invokes the retired relaxation ladder or passes date/time/price/radius refinement state into results."
    - "Search errors remain honest generic failures with one retry, and both soft-navigation and hard-navigation loading states have one named loading owner without leaking database details."
    - "Per D-07, the public route does not pass a searched date window, request date-aware availability, expose configured maximum capacity, or render a remaining-places claim."
  prohibitions:
    - statement: "The public route renders no date, time, price, radius, relaxation band, Undo, broaden-radius, clear-filters, or nearby-spaces refinement control."
      status: resolved
      verification: explicit
    - statement: "The streaming fallback contains no button, form, input, combobox, client coordinator, event handler, or journey live region."
      status: resolved
      verification: explicit
    - statement: "A no-results state cannot claim that the search was widened or suggest changing a day that Phase 24 never asked for."
      status: resolved
      verification: explicit
    - statement: "Cards receive neither configured capacity nor a date-scoped remaining count from the Phase 24 public page."
      status: resolved
      verification: explicit
  artifacts:
    - path: src/app/(public)/page.tsx
      provides: "Canonical validated public-query composition around SearchExperience, with relaxation execution and retired refinements disconnected."
      contains: "SearchExperience"
    - path: src/components/search/search-results.tsx
      provides: "Populated, calm empty, error, soft-loading, and cold-browse result states without legacy relaxation props or controls."
      exports: [SearchResults]
    - path: src/app/(public)/loading.tsx
      provides: "Server-rendered inert pill shell plus the shared named card-grid skeleton for route streaming."
      contains: "search-idle-pill-shell"
    - path: tests/search/search-results-states.test.tsx
      provides: "Focused state-composition, shared-chip, canonical-navigation, error, and inert-loading coverage."
  key_links:
    - from: src/app/(public)/page.tsx
      to: src/components/search/search-experience.tsx
      via: "The Server Component passes only validated serializable answers and wraps every resolved result state in the single client journey coordinator."
      pattern: "SearchExperience"
    - from: src/components/search/search-experience.tsx
      to: src/components/search/search-results.tsx
      via: "SearchExperience owns the three answer chips outside its rendered child, so populated, empty, error, and pending result branches cannot drift to different edit controls."
      pattern: "children"
    - from: src/app/(public)/page.tsx
      to: src/lib/search/query.ts
      via: "The page constructs the active server-search input from validated canonical Phase 24 fields and supplies no retired date/time/price override to searchListings."
      pattern: "searchListings"
    - from: src/app/(public)/loading.tsx
      to: src/components/patterns/card-grid-skeleton.tsx
      via: "The route fallback reserves search-pill space with inert markup and delegates the only loading announcement to CardGridSkeleton."
      pattern: "CardGridSkeleton"
---

<objective>
Recompose D-01/D-02/D-07/D-08 populated, empty, error, soft-loading, and route-streaming states around the one progressive journey and its shared editable answer chips, while disconnecting every public legacy refinement and relaxation surface.

Purpose: Make all result outcomes feel like one honest continuation of the three-answer conversation, prevent old controls or auto-broadening from contradicting those answers, and ensure Next.js streaming reserves layout without duplicating interactivity.
Output: A canonical public Server Component composition, a narrowed SearchResults state shell, an inert geometry-matched loading pill, and rewritten focused result-state coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-PLAN-OUTLINE.md
@.planning/phases/24-search-bar-rework/24-01-SUMMARY.md

<interfaces>
- The installed Next.js 16.2.7 loading guide defines loading.tsx as a parameterless Server Component Suspense fallback that streams and is automatically replaced by the resolved page. Keep it free of `use client` and interaction.
- The installed page guide defines searchParams as an awaited plain object. src/app/(public)/page.tsx remains the server validation/query boundary and passes only serializable validated answers into SearchExperience.
- 24-01 creates SearchExperience with initialAnswers, hasCompletedSearch, and children. It owns the activity/location/party answer-chip buttons outside the rendered server child; this plan must not duplicate chip markup or URL-edit handlers inside SearchResults.
- SearchResults owns sort, Load more, soft-pending skeleton, generic fetch error, populated grid, completed-search empty state, and cold-browse empty state. It receives queryString from the server rather than reading unvalidated browser parameters.
- CardGridSkeleton is the shared loading geometry/status owner for both loading.tsx and SearchResults soft navigation.
- SearchResultCard supports an optional searchedWindow and date-aware spots for pre-Phase-24 callers/tests, but the public Phase 24 page must neither supply a searched window nor run the date-aware search stage. Do not change that shared component in this Wave 2 plan.
- The targeted census establishes that page.tsx is the only runtime caller of runRelaxationLadder, SearchResults is the only runtime caller of RelaxBand, and loading.tsx is the remaining runtime SearchBar caller after 24-01. Tests/design inventories and the dev theme still reference old modules and are migrated or retired by 24-05.
</interfaces>
</context>

## Artifacts this phase produces

- Existing `src/app/(public)/page.tsx` gains a local canonical active-search value derived from the server-validated Phase 24 fields; no new exported route symbol is introduced.
- Existing `SearchResultsProps` drops `currentRadius`, `searchedWindow`, `relaxation`, and `relaxExhausted`; existing export `SearchResults` remains the result-state boundary.
- Existing `PublicLoading` replaces the live `SearchBar` with an inert element marked `data-testid="search-idle-pill-shell"`, using the settled pill's width, height, radius, border, and padding geometry.
- Existing `tests/search/search-results-states.test.tsx` gains the local `renderCompletedSearch` harness that composes `SearchExperience` around `SearchResults`, plus direct cases for populated, calm empty, error, soft loading, cold browse, and `PublicLoading`.
- The execution SUMMARY records the targeted census in two buckets: runtime surfaces disconnected by 24-04, and tracked browser/design/dead-module contracts assigned to 24-05 retirement.
- No new path, public API, endpoint, schema field, migration, dependency, environment variable, or capacity projection is created.

## Spec-less probe fallback

Skipped visibly because ROADMAP.md maps no Phase 24 requirement IDs and records `Requirements: TBD`; no requirement-derived probe predicate is invented. D-01, D-02, D-07, D-08, the result-state matrix, and the explicit prohibitions in this plan are the acceptance contract.

## Usage-census boundary

Before editing, census `SearchBar`, its retired field labels/ids, `RelaxBand`, `runRelaxationLadder`, the relaxation props, and the three zero-result hatch labels across tracked `src/`, focused component tests, browser specs, and design inventories. Remove every live public-page/loading/results import, invocation, prop, and rendered control in this plan. Do not delete the now runtime-disconnected `search-bar.tsx`, `relax-band.tsx`, `relaxation.ts`, their exact design inventory rows, or browser contracts here: those tracked consumers are the explicit 24-05 migration set, and editing them in Wave 2 would overlap 24-02's design files or pre-empt 24-05's full-journey retirement. Record both census buckets and exact paths in 24-04-SUMMARY.md.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Progressive search remains simple through every result/loading outcome | 24-04 Tasks 1-2 | COVERED | One coordinator surrounds resolved outcomes; hard loading is inert. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD and REQUIREMENTS.md has no Phase 24 mapping; no ID is invented. |
| RESEARCH | Remove page relaxation invocation and old result escape hatches after census | 24-04 Task 1 | COVERED | Runtime composition is disconnected now; tracked obsolete suites are enumerated for 24-05. |
| RESEARCH | Use the same answer chips for populated and zero-result states | 24-04 Task 1 | COVERED | SearchExperience owns one chip row around either SearchResults branch. |
| RESEARCH | Match route-loading geometry without a second active search | 24-04 Task 2 | COVERED | Parameterless server fallback renders inert pill geometry and one skeleton owner. |
| CONTEXT | D-01 compact engaged-only pill | 24-04 Task 2 | COVERED | The fallback reserves its shape without an actionable control. |
| CONTEXT | D-02 retained direct correction rather than restart | 24-04 Task 1 | COVERED | Result outcomes preserve the coordinator and its direct Edit chips. |
| CONTEXT | D-07 no remaining/date availability claim | 24-04 Task 1 | COVERED | Page execution strips the date-aware stage and card handoff. |
| CONTEXT | D-08 same chips in populated/empty; remove legacy controls/relaxation | 24-04 Task 1 | COVERED | State tests pin both positive composition and old-control absence. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recompose result and error states around one validated answer-chip owner</name>
  <files>src/app/(public)/page.tsx, src/components/search/search-results.tsx, tests/search/search-results-states.test.tsx</files>
  <behavior>
    - A completed populated search renders the three D-08 Edit chips once, a truthful count/sort row, and one card per server result.
    - A completed zero-result search renders the identical three chip buttons and calm `No spaces match those answers` copy, with no alternative result grid, automatic widening claim, or refinement hatch.
    - A completed fetch error preserves the three chips, renders one generic alert and one Try again action, calls router.refresh once, and reveals no thrown/database text.
    - Sort and Load more mutate only the server-supplied canonical category/coordinates/locationLabel/partySize query plus their own sort/page key; no retired key is introduced or carried.
    - A cold browse remains distinguishable from a completed no-result search and keeps its existing liquidity-floor copy beneath the D-01 idle coordinator.
    - The public page never hands SearchResults a searched window or relaxation result, and SearchResultCard receives no date-aware input from this route.
  </behavior>
  <read_first>
    - AGENTS.md — requires installed Next.js guidance instead of remembered App Router behavior.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — async plain-object searchParams and Server Component route contract.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — locked D-02/D-07/D-08 result correction and honesty rules.
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — implemented SearchExperience props, shared answer chips, validated initial answers, and canonical query handoff.
    - src/app/(public)/page.tsx — current server validation, cumulative paging, error logging, relaxation ladder, and component composition.
    - src/components/search/search-experience.tsx — read-only 24-01 coordinator/chip contract; do not modify in this plan.
    - src/components/search/search-results.tsx — current prop surface, router transition, result/error/loading branches, relaxation band, hatches, and cold-start copy.
    - src/components/search/search-result-card.tsx — read-only optional searchedWindow/spots behavior that the public route must stop feeding.
    - src/lib/search/relaxation.ts and src/components/search/relax-band.tsx — census authorities to disconnect from runtime, not edit.
    - tests/search/search-results-states.test.tsx — jsdom router mocks, makeRow/baseProps fixture style, and guard-the-guard result pin.
  </read_first>
  <action>
    First run the targeted usage census described above and preserve its runtime-versus-24-05 classification in the plan summary. Then start RED by replacing the relaxation-centric cases in `tests/search/search-results-states.test.tsx` with a `renderCompletedSearch` harness that supplies a validated complete `initialAnswers` tuple to `SearchExperience` and renders `SearchResults` as its child. Cover populated, calm empty, fetch-error, soft-pending navigation, and cold-browse branches. In populated and empty/error cases assert exactly one activity, location, and party Edit button; assert the empty copy exactly; assert the generic alert/retry behavior; preserve a positive result-card guard; and prove the old relaxation band, Undo, three hatches, date/time/price/radius controls, widened/day copy, and extra result grid are absent. Exercise sort and Load more through the captured router and inspect the target URL for retained canonical answers, reset/incremented page behavior, and absence of retired keys. Keep assertions semantic; jsdom cannot prove pixel geometry.

    In `page.tsx`, preserve the installed Next.js async Server Component contract and 24-01's `SearchExperience` composition. After safeParse, derive the public search input from validated category, paired coordinates, bounded locationLabel, optional partySize, sort, and bounded page, allowing the schema's fixed internal defaults but not forwarding URL-supplied date/start/end/price/radius override/relaxation state to `searchListings`. Use that same canonical object for `hasCompletedSearch`, initial answers, cumulative server queries, heading, and `activeQueryString`; keep `MAX_PAGES`, early exhaustion, and operation/error-code-only logging. Remove relaxation imports, rung counting, fan-out queries, effective/default filter state, legacy SearchBar wiring, and all relaxation/window props. Pass `SearchResults` only server rows, paging/sort/origin/query state, heading/city, canonical queryString, and the boolean fetchError. This makes a legacy retired key safe to parse but unable to alter `/` invisibly.

    In `search-results.tsx`, remove the validation-radius, RelaxBand, relaxation-type, and searched-window imports; delete the relaxation/currentRadius/searchedWindow prop branches and their URL handlers. Keep one `pushWith` based on the trusted server-supplied queryString for sort and Load more only, a single soft-pending CardGridSkeleton branch, the populated grid/pager, the permanent generic inline error with retry, the completed-search empty state, and the cold-browse liquidity floor. Render result cards without searchedWindow. The completed empty state uses exact title `No spaces match those answers` and calm body `Edit an answer above to try a different search.` with `actions={null}`, because the actual actions are the shared chips immediately above in SearchExperience. Do not render capacity, spots, or any new fallback query client-side.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/search-results-states.test.tsx</automated>
    <fails_when>The command exits non-zero, reports zero tests, populated/empty/error states do not share exactly three Edit chips, a retired refinement or relaxation affordance renders, empty copy implies widening/date work, retry leaks or fails to refresh, canonical sort/paging loses an answer or carries a retired key, or the positive card guard fails.</fails_when>
  </verify>
  <acceptance_criteria>The focused result-state suite exits 0 and proves D-02/D-08 correction parity across populated, empty, and error outcomes while the server page has no live relaxation/date-refinement path and D-07 data never reaches a public card.</acceptance_criteria>
  <done>All resolved public outcomes remain inside one validated answer-chip coordinator, and SearchResults owns only honest result, empty, error, pending, sorting, and paging behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the streaming SearchBar with an inert pill-shaped server fallback</name>
  <files>src/app/(public)/loading.tsx, tests/search/search-results-states.test.tsx</files>
  <behavior>
    - PublicLoading remains a parameterless Server Component with the same main/header/grid geometry as the resolved page.
    - The slot between the header and CardGridSkeleton contains one aria-hidden element whose outer dimensions, border radius, border, padding, and width behavior match the D-01 idle pill from SearchExperience.
    - The inert shell contains representative non-announced visual strokes only and exposes no form, button, input, link, combobox, handler, focus target, or Search progress live region.
    - CardGridSkeleton remains the only named loading status and keeps label `Loading spaces`.
    - The resolved page continues to own the only interactive SearchExperience; streaming replacement cannot leave two active search trees in the DOM.
  </behavior>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md — installed parameterless Server Component fallback, Suspense nesting, streaming, and automatic replacement behavior.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-01 engaged-only pill and D-07 honest loading boundary.
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — final idle-pill geometry and coordinator test selectors from the tracer.
    - src/components/search/search-experience.tsx — read-only resolved idle-pill outer geometry to mirror; do not import this Client Component into loading.tsx.
    - src/app/(public)/page.tsx — resolved outer container and static header geometry.
    - src/app/(public)/loading.tsx — current live SearchBar fallback that creates a duplicate interactive tree.
    - src/components/patterns/card-grid-skeleton.tsx — shared named loading-region owner.
    - tests/search/search-results-states.test.tsx — Task 1 harness and the target location for the parameterless loading render assertions.
  </read_first>
  <action>
    Start RED in `tests/search/search-results-states.test.tsx` by rendering `PublicLoading`. Assert the static heading/lede, one `search-idle-pill-shell`, and the named `Loading spaces` skeleton. Within the shell assert zero interactive/focusable elements and no status/alert/live attribute; across the full fallback assert the skeleton is the sole named loading status. Pin the shell's agreed D-01 geometry classes from the implemented 24-01 idle pill so removing its width, minimum height, radius, border, or padding fails. Keep pixel-level parity and transient dual-tree timing for 24-05's real browser/visual checks.

    Remove the `SearchBar` import and all stale commentary that justifies a live form in `loading.tsx`. Render a lightweight `aria-hidden="true"` pill shell at the same page position with the same outer geometry tokens as 24-01's idle pill, plus neutral skeleton strokes that do not look actionable. Keep `PublicLoading` parameterless and server-only; do not import SearchExperience, add `use client`, attach event handlers, use a semantic input/button/link role, duplicate answer text, or create a second announcement. Preserve the page container, static heading/lede, and `CardGridSkeleton label="Loading spaces"` so streaming replacement reserves the resolved layout without representing an answer or search result that the fallback cannot know.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/search-results-states.test.tsx</automated>
    <fails_when>The command exits non-zero, reports zero tests, the fallback lacks the geometry-pinned inert shell or named grid skeleton, exposes any focusable/search control or duplicate live region, imports the client coordinator, or changes the resolved page's outer/header placement.</fails_when>
  </verify>
  <acceptance_criteria>The focused suite exits 0 with a geometry-pinned D-01 loading shell, zero fallback interactivity, and one named CardGridSkeleton status, matching the installed Next.js streaming convention without a duplicate client tree.</acceptance_criteria>
  <done>Hard navigation reserves the final search-and-grid layout through server-rendered inert markup, while the resolved page remains the only place the progressive search can be engaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/searchParams to public Server Component | Retired and canonical query keys are attacker-controlled until schema validation and canonical projection. |
| Server query/error to public result shell | Database rows and failures cross into user-visible result, empty, and error states. |
| Next.js streaming fallback to hydrated page | Temporary fallback markup is replaced by the resolved interactive tree and must not create a second actionable search. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-17 | Tampering | page.tsx canonical public query | high | mitigate | Safe-parse the awaited plain object, project only canonical Phase 24 answers plus supported sort/page, and keep retired fields out of searchListings and activeQueryString. |
| T-24-18 | Spoofing | SearchExperience answer chips | high | mitigate | Build chip labels/retained edits only from server-validated serializable initialAnswers and render one coordinator around every result branch. |
| T-24-19 | Information Disclosure | search error state/logging | high | mitigate | Pass only fetchError to the client, render fixed generic copy, and log operation plus database/cause codes without raw error messages or connection details. |
| T-24-20 | Spoofing / Information Disclosure | capacity/availability result claims | high | mitigate | Run no date-aware public query, pass no searchedWindow, project no configured capacity, and render no remaining-place claim for Phase 24 results. |
| T-24-21 | Denial of Service | zero-result relaxation fan-out | medium | mitigate | Remove runRelaxationLadder from the public route so one completed search performs only the existing bounded cumulative page reads. |
| T-24-22 | Elevation of Privilege / Spoofing | loading.tsx fallback | medium | mitigate | Keep the parameterless fallback server-only and inert with no handlers, focus targets, form semantics, client coordinator, or duplicate status owner. |
| T-24-SC | Tampering | package supply chain | low | accept | The plan performs no package-manager action and changes no dependency declaration. |
</threat_model>

<verification>
  <automated>node node_modules/vitest/vitest.mjs run tests/search/search-results-states.test.tsx</automated>
  <fails_when>The command exits non-zero, reports zero tests, the shared-chip/result-state matrix fails, a retired public refinement remains, error/loading ownership is duplicated or misleading, or the route fallback is interactive.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or emits a diagnostic for the narrowed SearchResults props, SearchExperience server composition, canonical query projection, PublicLoading markup, or rewritten fixtures.</fails_when>
</verification>

<success_criteria>
- D-08 populated and calm empty states expose the same one activity/location/party Edit-chip set; error and soft-pending states retain that coordinator rather than replacing it.
- D-01 hard-loading markup matches the idle pill's outer geometry but has no interactive or announced search tree.
- Page execution, SearchResults props, and rendered markup contain no live date/time/price/radius/relaxation refinement path.
- No empty-state sentence claims automatic widening or suggests changing a date that the journey did not collect.
- D-07 result cards receive no searched window, configured maximum capacity, or date-scoped remaining count from the public route.
- Fetch failures expose fixed public copy and one retry while server logs retain only operation/error codes.
- The runtime/test census is recorded, and every obsolete tracked browser/design/dead-module contract is explicitly assigned to 24-05 rather than silently retained as a public capability.
- No schema-relevant file, external API, or dependency declaration changes.
</success_criteria>

<output>
Create .planning/phases/24-search-bar-rework/24-04-SUMMARY.md when done.
</output>
