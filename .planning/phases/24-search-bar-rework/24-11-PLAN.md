---
phase: 24-search-bar-rework
plan: 11
type: execute
wave: 9
depends_on: [24-10]
files_modified:
  - src/components/search/progressive-search-overlay.tsx
  - src/components/search/search-experience.tsx
  - tests/search/progressive-search.test.tsx
autonomous: true
gap_closure: true
requirements: [D-09]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-09, selecting the idle pill or an answer chip presents one current question in a desktop pill-anchored overlay and a mobile full-screen sheet without moving page content."
    - "The existing activity → location → party order, direct edit, Back, Cancel, focus, and status contracts keep working inside the responsive presentation."
  artifacts:
    - path: src/components/search/progressive-search-overlay.tsx
      provides: "One controlled progressive-question host using installed Radix wrappers for desktop anchoring and a mobile full-screen modal presentation."
    - path: src/components/search/search-experience.tsx
      provides: "Reducer-owned question state wired into the overlay host instead of normal document flow."
    - path: tests/search/progressive-search.test.tsx
      provides: "Viewport-specific structural and focus regressions for a single mounted question tree."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: src/components/search/progressive-search-overlay.tsx
      via: "The same reducer-owned current step is supplied to one responsive portal host; client state is not duplicated by breakpoint."
      pattern: "ProgressiveSearchOverlay"
    - from: src/components/search/progressive-search-overlay.tsx
      to: src/components/ui/popover.tsx
      via: "Desktop content uses the existing anchored Popover primitive and trigger geometry."
      pattern: "Popover"
---

<objective>
Implement the protected D-09 presentation contract: question content is portalled above the page as an overlay anchored to the desktop search pill and as a true full-screen mobile sheet.

Purpose: Advancing one question must never reflow the home or result content, while preserving the existing one-question progressive journey and its accessibility behavior.
Output: A search-specific responsive overlay host, migrated coordinator composition, and focused desktop/mobile component coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@src/components/patterns/responsive-dialog.tsx
@src/components/ui/dialog.tsx
@src/components/ui/popover.tsx
@src/components/search/search-experience.tsx
@tests/search/progressive-search.test.tsx
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Reveal the search experience only after engagement with smooth, progressive transitions | 24-11 Tasks 1–2 | COVERED |
| REQ | ROADMAP Phase 24 requirement field is `TBD` | D-09 is the operative acceptance ID | COVERED |
| RESEARCH | Reuse installed Radix overlay primitives and existing z/motion tokens; add no overlay dependency | 24-11 Task 1 | COVERED |
| CONTEXT | D-09 desktop pill anchor, mobile full-screen sheet, and no content push | 24-11 Tasks 1–2 | COVERED |
| CONTEXT | D-01–D-08/D-12 remain existing functional contracts | 24-01–24-10 | PRESERVED |
| CONTEXT | D-10/D-11 geographic authority | 24-12 and 24-13 | COVERED ELSEWHERE |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Portal one active progressive question from the pill without reflowing the route</name>
  <files>src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - Test 1: at desktop width, the idle pill opens one portalled question host anchored to that trigger rather than inserting the question below the section.
    - Test 2: at 375 px, the same reducer-owned question content opens in one full-viewport modal sheet.
    - Test 3: each viewport has exactly one active question tree and one named progress status owner.
  </behavior>
  <action>Implement a search-specific controlled overlay host using existing Radix wrappers only. At `sm` and above, compose the idle pill as the Popover trigger and render the current question in a portalled, pill-aligned desktop overlay. Below `sm`, render that same current question through a controlled Dialog portal whose content is fixed to the dynamic viewport on all four edges, has no desktop max-width or centering transform, and carries an explicit test id for the full-screen sheet. Select exactly one host at runtime so desktop and mobile never mount duplicate activity/location/party trees. In SearchExperience, keep the reducer, canonical keyed coordinator, URL serialization, child results, and answer chips as their current owners; move only the question host and Back/Cancel controls into the overlay. Derive open from the active screen, route Escape/outside dismissal through the existing destructive Cancel boundary, and retain focus transfer to the current step heading plus normal return to the pill/chip trigger. Use existing motion and z-index variables; do not edit UI primitives, global CSS, design registries, snapshots, or dependencies.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx src/components/search/search-experience.tsx tests/search/progressive-search.test.tsx</automated>
  </verify>
  <done>The first activity question opens above rather than inside page flow, the 375 px host fills the viewport, and one reducer-controlled question tree remains accessible at either width.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve correction, dismissal, and focus behavior inside the responsive question host</name>
  <files>src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - Test 1: activity selection, address resolution, party choice, Back, and direct result-chip Edit keep their existing order while the host stays open for the active step.
    - Test 2: Cancel, Escape, and desktop outside dismissal clear draft state and return to the cold browse URL; focus returns to the initiating pill or edit chip.
    - Test 3: reduced-motion classes and the existing single status-region contract remain intact in both presentations.
  </behavior>
  <action>Expand the focused test matrix around the new host before finalizing behavior. Keep question content free of a second focus trap or hand-rolled backdrop: the selected installed primitive owns modality and dismissal. Ensure direct result-chip edits open the appropriate step in the same host, while Back preserves retained answers and falls back to the pill only when its existing history reaches idle. Keep the desktop overlay’s anchor relation after a step changes, and ensure mobile does not retain scrollable page interaction behind the sheet. Do not change activity filtering, address lookup ownership, party bounds, canonical URL keys, SQL queries, or browser test snapshots in this task.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/live-regions.test.tsx tests/design/motion-budget.test.ts</automated>
  </verify>
  <done>All existing correction and accessibility paths function through the overlay/sheet, without duplicate progressive content or a normal-flow question branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| search trigger → portalled controls | A public click opens an interactive, focus-owning layer above public content. |
| overlay dismissal → URL/navigation | Close actions must preserve the existing Cancel semantics rather than leave unseen draft state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-11-01 | Spoofing | responsive overlay | medium | mitigate | The portalled layer has one explicit accessible owner and reuses Radix focus/dismissal primitives rather than a custom non-modal imitation. |
| T-24-11-02 | Denial of service | focus and page interaction | medium | mitigate | Mobile modal composition traps focus and prevents background interaction; tests prove one active tree and one status owner. |
| T-24-11-03 | Tampering | dismissal routing | medium | mitigate | Dismissal delegates to the reducer’s existing Cancel path and constant same-origin navigation. |
| T-24-11-SC | Tampering | package installation | low | accept | Installed Radix wrappers are reused; no package-manager action is allowed. |
</threat_model>

<verification>
Run the focused component, design, and target-ESLint commands. Browser geometry proof belongs to 24-13, where it can measure rendered boxes at both target widths.
</verification>

<success_criteria>
The normal-flow step branches are replaced by one responsive portalled host that is desktop-anchored and full-screen at 375 px, with all existing progressive state behavior preserved.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-11-SUMMARY.md` when done.
</output>
