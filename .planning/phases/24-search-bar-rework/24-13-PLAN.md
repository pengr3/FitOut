---
phase: 24-search-bar-rework
plan: 13
type: execute
wave: 10
depends_on: [24-11, 24-12]
files_modified:
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
requirements: [D-09, D-10, D-11]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "At 1280 px, the progressive question is visibly anchored to the search pill and opening it leaves the underlying route geometry unchanged."
    - "At 375 px, the question is a full-screen sheet with one focusable progressive flow, not a normal-flow expansion."
    - "A real browser journey from a selected Mandaluyong coordinate includes nearby Makati results in nearest-first order but excludes a matching listing outside 25 km."
  artifacts:
    - path: e2e/progressive-search.spec.ts
      provides: "Chromium browser proof for overlay geometry, mobile sheet geometry, and the coordinate-owned fixed-reach search result behavior."
  key_links:
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/progressive-search-overlay.tsx
      via: "Viewport-specific bounding-box and role assertions exercise the rendered desktop anchor and mobile sheet."
      pattern: "progressive-search"
    - from: e2e/progressive-search.spec.ts
      to: src/app/(public)/page.tsx
      via: "A seeded browser journey proves the public page supplies the fixed server radius to actual results."
      pattern: "25"
---

<objective>
Prove the repaired overlay/sheet and fixed 25 km search contract in Chromium without regenerating visual snapshots or weakening the established progressive browser journey.

Purpose: Component and database checks cannot establish the rendered anchor, viewport coverage, or browser-to-server geographic seam on their own.
Output: Two deterministic Chromium cases that measure desktop/mobile geometry and coordinate-based nearby result behavior.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-11-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-12-SUMMARY.md
@e2e/progressive-search.spec.ts
@e2e/helpers/booker-seed.ts
@playwright.config.ts
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Smooth, one-question search journey in real desktop and mobile browsers | 24-13 Tasks 1–2 | COVERED |
| REQ | ROADMAP Phase 24 requirements are `TBD` | D-09/D-10/D-11 are the operative acceptance IDs | COVERED |
| RESEARCH | Browser location/address paths require deterministic fixtures, not live Photon | 24-13 Tasks 1–2 | COVERED |
| RESEARCH | Visual appearance requires runtime observation beyond source/component checks | 24-13 Task 2 human check | COVERED |
| CONTEXT | D-09 anchored desktop overlay and mobile full-screen sheet without content push | 24-13 Task 1 | COVERED |
| CONTEXT | D-10 selected coordinates, 25 km reach, nearest-first, no municipal hard boundary | 24-13 Task 2 | COVERED |
| CONTEXT | D-11 server authority and no silent broadening | 24-13 Task 2 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Measure the desktop anchored overlay and mobile full-screen sheet in Chromium</name>
  <files>e2e/progressive-search.spec.ts</files>
  <precondition>Playwright can start the configured application server on its expected local origin, with no unrelated process holding that port.</precondition>
  <behavior>
    - Test 1: at 1280 px, opening the search pill leaves the results-region top coordinate unchanged and places the active overlay adjacent to the trigger.
    - Test 2: at 375 px, the active sheet box covers the viewport and the route has exactly one active progressive question tree.
    - Test 3: activity, location, party, Back, and Cancel remain usable through the same browser flow in both presentations.
  </behavior>
  <action>Extend the existing progressive-search browser spec, retaining its seeded fixtures, deterministic Photon route mock, named role selectors, and desktop/375 viewport matrix. Add explicit test ids or roles only through the production overlay implementation where necessary, then measure element bounding boxes after the overlay settles: desktop content must be associated with the pill rather than a normal-flow result below it, and the underlying result-region Y coordinate must not move; mobile sheet must cover the dynamic viewport. Assert one active question flow and preserve the established URL/focus checks. Do not use a screenshot-update flag, modify e2e/visual files or PNG baselines, bypass the journey with a result URL, or replace browser geometry assertions with source scans.</action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
  </verify>
  <done>Chromium proves that search questions no longer push the route, remain pill-anchored on desktop, and occupy a full-screen sheet on a 375 px viewport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive a cross-municipality fixed-reach search through the public browser route</name>
  <files>e2e/progressive-search.spec.ts</files>
  <precondition>Playwright can start the configured application server and the local test database accepts the existing seeded-listing helper writes.</precondition>
  <behavior>
    - Test 1: a mocked selected Mandaluyong address returns the closest seeded Mandaluyong listing before an eligible nearby Makati listing.
    - Test 2: the nearby Makati listing is present despite its distinct city label, while a matching eligible row seeded beyond 25 km is absent.
    - Test 3: the submitted URL carries selected coordinates and no radius key, so the browser cannot choose or broaden the server reach.
  </behavior>
  <action>Seed and clean up the additional listings within progressive-search.spec.ts using the existing seedBookableListing connection pattern; set explicit PostGIS points and city labels through its returned SQL connection rather than changing shared seed helpers. Add one deterministic address mock for a Mandaluyong origin, retain the normal activity → address → For me interaction, and assert result order from the rendered result cards or links. Include a matching city-different Makati row inside the fixed reach and a matching row outside it; require inclusion of the former and exclusion of the latter. Assert the canonical browser URL has no radius parameter. Keep existing capacity assertions, route helper behavior, and browser suite coverage intact; do not add a radius UI, fixture-only route shortcut, dependency, migration, release configuration change, or snapshot regeneration.</action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "fixed 25 km cross-municipality reach"</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <human-check>At 1280 px, open the pill and verify the overlay reads as visually attached to it with no page jump. At 375 px, verify the sheet fills the screen, its controls are neither clipped nor hidden behind browser chrome, and normal/reduced-motion transitions remain calm.</human-check>
  </verify>
  <done>A browser-selected coordinate proves the live public route uses the one fixed 25 km server reach, ranks nearby results by distance across city labels, and never exposes a mutable radius.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Playwright browser → public route | Browser-generated canonical URL and viewport interactions exercise the public client/server seam. |
| deterministic fixture → search result | Test-only seeded coordinates must prove real server filtering without calling a live geocoder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-13-01 | Tampering | browser geographic query | high | mitigate | E2E asserts the browser emits no radius key and demonstrates that the server excludes a beyond-reach fixture. |
| T-24-13-02 | Spoofing | address provider fixture | medium | mitigate | Route Photon requests are deterministically intercepted; tests assert coordinates and actual rendered result behavior rather than trusting external service output. |
| T-24-13-03 | Denial of service | visual regression proof | low | accept | Focused Chromium cases are serialized with one worker and do not alter visual baseline infrastructure. |
| T-24-13-SC | Tampering | package installation | low | accept | This plan changes only the existing Playwright spec and uses installed browser tooling. |
</threat_model>

<verification>
Run the named focused Chromium cases, then the full progressive-search Chromium spec. Perform the recorded human visual check after automated geometry passes; do not regenerate snapshots as a substitute for inspection.
</verification>

<success_criteria>
Both target viewports and the public geographic route have automated Chromium evidence for D-09/D-10/D-11, plus one bounded manual visual confirmation of the responsive presentation.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-13-SUMMARY.md` when done.
</output>
