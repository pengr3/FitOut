---
phase: 24-search-bar-rework
plan: 12
type: execute
wave: 9
depends_on: [24-10]
files_modified:
  - src/lib/search/public-search-contract.ts
  - src/app/(public)/page.tsx
  - tests/search/public-search-contract.test.ts
  - tests/search/public-search-geography.test.ts
autonomous: true
gap_closure: true
requirements: [D-10, D-11]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-10, submitted coordinates are the geographic origin, candidates are limited to 25 km, nearest sort is retained, and a nearby listing in another municipality is eligible."
    - "Per D-11, the public Server Component alone supplies the fixed 25 km value; a URL radius value cannot change a completed progressive search and no radius control is restored."
  artifacts:
    - path: src/lib/search/public-search-contract.ts
      provides: "Named server-owned public-search reach and a pure normalized query-input derivation."
    - path: src/app/(public)/page.tsx
      provides: "Public RSC consumer that validates URL input then passes the fixed 25 km contract to searchListings."
    - path: tests/search/public-search-geography.test.ts
      provides: "Database integration proof of 25 km inclusion, exclusion, cross-municipality eligibility, and nearest-first ordering."
  key_links:
    - from: src/app/(public)/page.tsx
      to: src/lib/search/public-search-contract.ts
      via: "Validated parsed URL state is normalized to the fixed public server reach before searchListings executes."
      pattern: "PUBLIC_SEARCH_RADIUS_KM"
    - from: src/lib/search/public-search-contract.ts
      to: src/lib/search/query.ts
      via: "The route passes 25 to the existing parameterized ST_DWithin and distance ordering query without adding a client authority."
      pattern: "radius"
---

<objective>
Restore D-10/D-11’s server-authoritative geographic contract: selected coordinates search a fixed 25 km reach, rank nearest first, and do not stop at municipal borders or accept a URL-selected radius.

Purpose: Search location must mean proximity to the chosen point, not a city filter or an unexposed mutable refinement.
Output: A named public-route reach contract, server-route proof, and database-backed geography regression suite.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@src/app/(public)/page.tsx
@src/lib/validation/booking.ts
@src/lib/search/query.ts
@tests/search/party-size-filter.test.ts
@tests/helpers/db.ts
@tests/helpers/seed.ts
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Location answer leads to honest, server-executed nearby results | 24-12 Tasks 1–2 | COVERED |
| REQ | ROADMAP Phase 24 requirements are `TBD` | D-10/D-11 are the operative acceptance IDs | COVERED |
| RESEARCH | RSC validates async parameters; client does not own SQL reach | 24-12 Task 1 | COVERED |
| RESEARCH | PostGIS geography query already provides metric `ST_DWithin` and `distance_m` ordering | 24-12 Task 2 | COVERED |
| CONTEXT | D-10 selected coordinates, 25 km Metro Manila reach, nearest-first, no municipal boundary | 24-12 Tasks 1–2 | COVERED |
| CONTEXT | D-11 product-owned fixed reach with neither picker nor broadening | 24-12 Task 1 | COVERED |
| CONTEXT | D-09 visual presentation | 24-11 and 24-13 | COVERED ELSEWHERE |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Give the public Server Component one immutable 25 km search contract</name>
  <files>src/lib/search/public-search-contract.ts, src/app/(public)/page.tsx, tests/search/public-search-contract.test.ts</files>
  <behavior>
    - Test 1: a completed public search with any validated legacy or crafted radius input receives exactly 25 km at the route-to-query boundary.
    - Test 2: category, paired coordinates, optional display label, party size, sort, and bounded page survive normalization while the generated public query string omits radius.
    - Test 3: cold browse retains its existing behavior and does not create a coordinate search authority.
  </behavior>
  <action>Per D-10 and D-11, add a small server-safe public-search contract module that exports a named 25 km constant and a pure derivation of the normalized public query inputs. Have the public page call it after searchParamsSchema safeParse and before every searchListings invocation, replacing the existing private 10 km assignment. The derivation must retain only validated category, paired coordinates, presentation-only label, party size, sort, and bounded page inputs, set radius from its own fixed constant regardless of a URL radius value, and preserve the existing cold-browse distinction. Keep the constant out of the client graph and keep activeQueryString free of radius. Add a direct unit test of the pure derivation rather than source-text inspection; do not loosen RADIUS_PRESETS for legacy callers, add a picker, or move validation/database work into a Client Component.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/public-search-contract.test.ts</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/lib/search/public-search-contract.ts src/app/(public)/page.tsx tests/search/public-search-contract.test.ts</automated>
  </verify>
  <done>Every completed public search reaches searchListings with a named fixed 25 km reach that URL input cannot override, while canonical public URLs still omit any radius parameter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the 25 km point-radius query crosses city labels and ranks by distance</name>
  <files>tests/search/public-search-geography.test.ts</files>
  <precondition>Docker is reachable and the isolated `tests/helpers/db.ts` harness can create its test schema before this database-backed task runs.</precondition>
  <behavior>
    - Test 1: a Mandaluyong-origin search includes an eligible nearer Mandaluyong row and an eligible farther Makati row within 25 km, ordered by distance.
    - Test 2: an otherwise eligible row beyond 25 km is absent even when its city text is present and category/capacity match.
    - Test 3: the cross-city row remains eligible because the query relies on coordinates, not a municipal predicate.
  </behavior>
  <action>Follow the isolated database lifecycle and verified-host/listing fixtures in party-size-filter.test.ts. Create a focused geography suite with explicit SRID 4326 coordinates and operating-hours rows for one origin, two eligible Metro Manila listings carrying different city labels inside the fixed reach, and one eligible matching listing outside it. Invoke searchListings through the public contract’s 25 km value with category and partySize so the existing capacity predicate remains active. Assert returned IDs and distance order rather than SQL source text; include the cross-municipality Makati result and exclude the outside-radius row. Do not change query.ts, mutate persisted schema, assert a city equality, or introduce a fallback radius if the nearby set is empty.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/public-search-geography.test.ts</automated>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/public-search-contract.test.ts tests/search/public-search-geography.test.ts</automated>
  </verify>
  <done>The server route’s fixed input is proven against real PostGIS behavior: closest eligible rows rank first, nearby city boundaries do not block results, and farther rows are not silently admitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public URL → Server Component | Untrusted coordinate and legacy-radius parameters arrive as request-time strings. |
| public route → PostGIS query | Only server-normalized values are supplied to the parameterized geographic predicate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-12-01 | Tampering | public search radius | high | mitigate | The server contract overwrites URL-derived radius with the named 25 km product value before search execution. |
| T-24-12-02 | Tampering | coordinate/category query inputs | high | mitigate | Existing searchParamsSchema validation and Drizzle parameter binding remain the sole input boundary. |
| T-24-12-03 | Spoofing | location label | medium | mitigate | The label remains presentation-only; database assertions prove coordinates determine inclusion and ordering. |
| T-24-12-SC | Tampering | package installation | low | accept | The work uses current Vitest, Drizzle, and PostGIS test infrastructure with no package installation. |
</threat_model>

<verification>
Run the pure route-contract test, the isolated geography integration test, and target ESLint. If the precondition fails, report Docker readiness rather than replacing the database test with a mock.
</verification>

<success_criteria>
The public RSC gives every completed progressive search exactly 25 km, and an isolated database suite proves coordinate-driven cross-city inclusion, nearest-first ranking, and outside-reach exclusion.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-12-SUMMARY.md` when done.
</output>
