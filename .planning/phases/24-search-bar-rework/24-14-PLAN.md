---
phase: 24-search-bar-rework
plan: 14
type: execute
wave: 11
depends_on: [24-11, 24-13]
files_modified:
  - src/components/search/progressive-search-overlay.tsx
  - src/components/search/search-experience.tsx
  - tests/search/progressive-search.test.tsx
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
requirements: [D-02, D-08, D-09]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-08 and D-09, editing a completed desktop result answer opens one visible Popover that is positioned from the chip the booker selected, rather than from an absent trigger."
    - "Per D-02, the visible Cancel control still clears the journey, returns to cold browse, and restores normal focus behavior without duplicating the question tree or progress status."
    - "The active desktop question remains within the available viewport height, so its initial Back and Cancel controls can be reached without page reflow."
  artifacts:
    - path: src/components/search/progressive-search-overlay.tsx
      provides: "A controlled desktop Popover that accepts a direct-edit virtual anchor and constrains its scrollable content to Radix's reported available height."
    - path: src/components/search/search-experience.tsx
      provides: "The coordinator records the actual answer-chip element that started a direct edit and supplies it to the existing overlay host."
    - path: tests/search/progressive-search.test.tsx
      provides: "Focused component proof that direct result-chip edits retain one accessible desktop question host and Cancel path."
    - path: e2e/progressive-search.spec.ts
      provides: "Chromium proof that the populated-result Activity chip opens a viewport-reachable, chip-connected desktop popover."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: src/components/search/progressive-search-overlay.tsx
      via: "The exact HTMLElement received by editAnswer is retained as the desktop direct-edit anchor and passed to the controlled overlay."
      pattern: "desktopAnchor"
    - from: src/components/search/progressive-search-overlay.tsx
      to: src/components/ui/popover.tsx
      via: "The search host composes the installed PopoverAnchor virtual-ref API and PopoverContent available-height variable; it does not change the shared primitive."
      pattern: "virtualRef"
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/progressive-search-overlay.tsx
      via: "The populated-result browser journey measures the desktop overlay and its Cancel control through the stable overlay test id."
      pattern: "progressive-search-desktop-overlay"
---

<objective>
Close the measured desktop direct-edit regression: a completed answer chip opens the controlled Popover without any trigger or anchor, so Radix retains the content at its off-screen measuring position and the Cancel control cannot be clicked.

Purpose: D-08's answer chips must be a usable correction entry point under D-02, and D-09 requires that correction to remain an anchored overlay without moving route content.
Output: A local virtual-anchor handoff for direct edits, focused component proof, and one additive Chromium viewport regression. No shared primitive, route, schema, fixture, snapshot, dependency, migration, or configuration change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-REVIEW.md
@.planning/phases/24-search-bar-rework/24-11-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-13-PLAN.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
@src/components/search/progressive-search-overlay.tsx
@src/components/search/search-experience.tsx
@src/components/ui/popover.tsx
@tests/search/progressive-search.test.tsx
@e2e/progressive-search.spec.ts

<interfaces>
From src/components/search/progressive-search-overlay.tsx: ProgressiveSearchOverlay is a controlled client host with `open`, optional `trigger`, `returnFocus`, `onOpenAutoFocus`, `onDismiss`, and question `children`; desktop currently uses Popover while mobile uses Dialog.

From src/components/search/search-experience.tsx: `editAnswer(step, trigger: HTMLElement)` already receives the clicked answer-chip element, stores it for focus restoration, and dispatches the reducer-owned D-02 Edit transition.

From src/components/ui/popover.tsx: the exported PopoverAnchor forwards the installed Radix Popover anchor API, which supports a virtual reference; PopoverContent exposes `--radix-popover-content-available-height` for collision-aware height bounds.
</interfaces>
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One smooth progressive journey that reveals only on engagement | 24-01–24-13 establish it; 24-14 Tasks 1–2 repair its desktop correction path | COVERED |
| REQ | ROADMAP Phase 24 requirements are `TBD` | D-02/D-08/D-09 are the operative acceptance IDs for this additive gap | COVERED |
| RESEARCH | Keep interactive state inside the existing Client Component boundary and reuse installed Radix overlay behavior with no dependency | 24-14 Task 1 | COVERED |
| RESEARCH | Focus, one status owner, and reduced-motion behavior remain part of the state machine | 24-14 Task 1 | COVERED |
| CONTEXT | D-02 retained corrections and destructive Cancel behavior | 24-14 Tasks 1–2 | COVERED |
| CONTEXT | D-08 result answer chips directly edit their exact step | 24-14 Tasks 1–2 | COVERED |
| CONTEXT | D-09 desktop anchored overlay without content push | 24-14 Tasks 1–2 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Anchor a direct desktop answer edit to its initiating chip</name>
  <files>src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - Test 1: selecting the populated-result Activity chip opens one desktop question host with its active heading, Back, and Cancel controls available through the existing reducer-owned flow.
    - Test 2: the direct-edit Cancel path still sends the booker to the cold route, returns focus according to the current dismissal contract, and leaves one progress status owner.
    - Test 3: opening from the idle pill remains trigger-anchored, while a direct edit has its own current chip reference rather than a body-position fallback.
  </behavior>
  <action>Implement the local direct-edit anchor handoff per D-08 and D-09. Keep the idle pill as the existing PopoverTrigger. When editAnswer receives a result-chip HTMLElement, retain that exact current element alongside the existing return-focus origin and pass it to ProgressiveSearchOverlay as a desktop-only virtual anchor. In the desktop Popover branch, compose the installed PopoverAnchor with that virtual reference whenever the idle trigger is absent, so Radix measures from the selected chip instead of leaving content at its unplaced measuring transform. Bound the desktop PopoverContent with its supplied available-height custom property and make its question area scrollable within that bound, preserving the initial Back and Cancel controls in the visible viewport. Keep the one reducer-owned question tree, one status region, focus transfer, Escape/outside dismissal to Cancel, mobile Dialog sheet, and route serialization unchanged. Add focused component coverage before the production correction. Do not edit the shared Popover/Dialog wrappers, global CSS, server route, schema, browser fixtures, dependencies, or snapshots.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx src/components/search/search-experience.tsx tests/search/progressive-search.test.tsx</automated>
  </verify>
  <done>At desktop width, both idle-pill entry and each result-chip entry produce one correctly owned question overlay with a reachable Cancel action; mobile behavior and existing D-02 progression remain unchanged.</done>
  <fails_when>A direct result-chip edit leaves PopoverContent unanchored, exposes duplicate question/status owners, regresses the cold-route Cancel behavior, or alters the mobile Dialog presentation.</fails_when>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure populated-result direct-edit reachability in Chromium</name>
  <precondition>Playwright can boot the configured local application on port 3000, with no unrelated process holding that port.</precondition>
  <files>e2e/progressive-search.spec.ts</files>
  <behavior>
    - Test 1: after a normal desktop activity → address → For me journey, selecting the populated-result Activity chip opens the desktop overlay connected to that chip.
    - Test 2: the measured Cancel button lies entirely inside both the measured overlay and the 1280 px browser viewport, then returns the route to cold browse when activated.
    - Test 3: the full progressive-search Chromium spec retains the separate fixed-25-km cross-municipality test currently awaiting final suite confirmation.
  </behavior>
  <action>Add one separately named desktop regression to the existing progressive-search spec, using the established seeded listing, mocked Photon lookup, normal journey helper, stable Activity-chip role selector, and desktop overlay test id. After a populated result opens the Activity editor, measure the chip, overlay, and Cancel bounding boxes. Assert a collision-aware adjacent anchor relationship, that Cancel is contained by the rendered overlay, and that its box is fully within the viewport before clicking it and asserting the established cold-browse route and pill recovery. Run the complete file after the named regression. Preserve the existing overlay/mobile geometry test and the uncommitted fixed-25-km Mandaluyong/Makati test verbatim; do not change shared seed helpers, add a result-URL shortcut, regenerate screenshots, or edit visual baselines/configuration.</action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "direct-edit desktop popover reachability"</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
  </verify>
  <done>A real 1280 px result-correction journey proves that the selected answer chip anchors the Popover and that Cancel is visible and operable before it clears the search.</done>
  <fails_when>The direct-edit Popover is detached from its initiating chip, any portion of Cancel is outside the overlay or viewport, the browser can no longer return to cold browse, or the preserved fixed-reach scenario fails.</fails_when>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| result answer chip → portalled desktop Popover | A public interaction supplies the DOM element that positions a focus-owning correction overlay. |
| Popover dismissal → public route | Escape, outside click, and the visible Cancel action pass through the existing destructive reset/navigation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-14-01 | Spoofing | direct-edit Popover | medium | mitigate | Use the currently clicked answer-chip HTMLElement as Radix's virtual anchor and prove its measured relationship in Chromium, preventing an unplaced overlay from appearing as an unrelated control. |
| T-24-14-02 | Tampering | dismissal state | medium | mitigate | Preserve the reducer-owned D-02 Cancel route and assert the browser recovers the cold pill after the visible control is clicked. |
| T-24-14-03 | Denial of service | viewport reachability | medium | mitigate | Limit PopoverContent to Radix's collision-aware available height and make only its question body scrollable; the browser test measures the actual Cancel box. |
| T-24-14-SC | Tampering | package installation | low | accept | The correction reuses installed Radix wrappers and existing test runners; no package-manager action is authorized. |
</threat_model>

<verification>
Run the focused component and ESLint checks before the named Chromium regression. Then run the complete progressive-search Chromium file, which is the final regression check for the already-preserved fixed-25-km case as well as the new direct-edit case.
</verification>

<success_criteria>
Desktop direct edits are anchored to the initiating result chip, their controls are visible and operable in a real browser, and the original pill/mobile/one-tree/Cancel contracts remain intact without changing shared primitives or browser fixtures.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-14-SUMMARY.md` when done.
</output>
