---
phase: 24-search-bar-rework
plan: 07
type: execute
wave: 5
depends_on:
  - 24-06
files_modified:
  - e2e/zero-result-relax.spec.ts
  - src/components/search/search-bar.tsx
  - src/components/search/relax-band.tsx
  - src/lib/search/relaxation.ts
  - tests/search/relaxation-ladder.test.ts
  - src/app/dev/theme/page.tsx
autonomous: true
requirements:
  - D-01
  - D-08
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh tracked-file census proves that the old expanded SearchBar and zero-result relaxation ladder have no supported runtime, browser, or unit-test consumer after Plans 24-04 and 24-06."
    - "The five obsolete expanded-search/relaxation paths are absent rather than retained as compatibility shims, alternate entry points, or dead tests, per D-01 and D-08."
    - "The dev theme gallery presents the final capacity-honest result and calm answer-editing empty-state language without date/time/radius refinement copy."
    - "All residual design-registry, visual-driver, and PNG references are enumerated for Plan 24-08 instead of being silently deleted in this plan."
  artifacts:
    - path: src/app/dev/theme/page.tsx
      provides: Final Phase 24 result-card and empty-state examples with no legacy refinement promise
    - path: .planning/phases/24-search-bar-rework/24-07-SUMMARY.md
      provides: Fresh usage census, five-path retirement ledger, residual design-owner ledger, and focused verification evidence
  key_links:
    - from: .planning/phases/24-search-bar-rework/24-06-SUMMARY.md
      to: e2e/zero-result-relax.spec.ts
      via: The migrated dependent-browser ledger proves the contradictory suite can be retired without losing retained booking, parity, one-tree, or axe coverage
      pattern: search-and-book|one-tree|price-parity|axe-sweep
    - from: .planning/phases/24-search-bar-rework/24-07-SUMMARY.md
      to: .planning/phases/24-search-bar-rework/24-08-PLAN.md
      via: Exact residual design names, selectors, counts, and snapshot rows become the bounded reconciliation input
      pattern: live-regions|selector-contract|visual-baselines|brand-recipe|elevation-z
    - from: src/app/dev/theme/page.tsx
      to: src/components/search/search-result-card.tsx
      via: The gallery renders the result card without a searched-window claim and pairs it with the final calm empty-state copy
      pattern: SearchResultCard|No spaces match those answers
---

<objective>
Retire the runtime-disconnected expanded-search and zero-result-relaxation implementation only after a fresh usage census, then remove its stale dev-theme presentation.

Purpose: Make D-01 and D-08 irreversible in the tracked runtime and focused test surface before fixed design registries and visual evidence are reconciled in Plan 24-08.
Output: Five deleted legacy paths, one corrected dev-theme path, and a precise census ledger that bounds the final design/baseline plan.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-04-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-06-SUMMARY.md

<interfaces>
- `24-04-SUMMARY.md` must prove the public page, results composition, and loading shell no longer import or invoke SearchBar, RelaxBand, or the relaxation ladder.
- `24-06-SUMMARY.md` must prove search-and-book, one-tree, price-parity, and axe-sweep now enter through the canonical progressive journey.
- The five retirement targets are `e2e/zero-result-relax.spec.ts`, `src/components/search/search-bar.tsx`, `src/components/search/relax-band.tsx`, `src/lib/search/relaxation.ts`, and `tests/search/relaxation-ladder.test.ts`.
- Plan 24-08 owns fixed design registries, deterministic visual drivers, and the workflow-managed PNG directory; this plan records those residual references but does not edit them.
</interfaces>
</context>

## Artifacts this phase produces

- Deleted `e2e/zero-result-relax.spec.ts` — the contradictory browser contract is retired after the retained dependent suites migrate.
- Deleted `src/components/search/search-bar.tsx` — the always-expanded D-01-incompatible form is absent after all runtime and browser consumers move.
- Deleted `src/components/search/relax-band.tsx` and `src/lib/search/relaxation.ts` — the D-08-incompatible automatic relaxation surface and ladder are absent after the census proves no supported consumer.
- Deleted `tests/search/relaxation-ladder.test.ts` — obsolete ladder behavior is no longer preserved as a unit contract.
- Updated `src/app/dev/theme/page.tsx` — result and empty examples use the final capacity-honest Phase 24 presentation.
- `24-07-SUMMARY.md` — exact pre-delete census, deletion ledger, allowed residual design references, and focused verification results.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Replace always-expanded controls with the progressive activity/location/party journey | 24-07 Tasks 1-2 | COVERED | The contradictory implementation and gallery language are removed after their consumers migrate. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP assigns Phase 24 no formal requirement IDs; decision IDs remain the executable contract. |
| RESEARCH | Repeat the usage census before deleting generic relaxation utilities | 24-07 Task 1 | COVERED | Deletion is conditional on the fresh supported-consumer result and a residual-owner ledger. |
| RESEARCH | Replace or retire the old zero-result browser contract and expanded-search tests | 24-07 Task 1 | COVERED | All five dedicated legacy paths retire together after Plan 24-06. |
| RESEARCH | Remove stale date/time/radius recovery language from the dev theme | 24-07 Task 2 | COVERED | The gallery uses the same final result/empty truth as the public route. |
| CONTEXT | D-01 compact engaged pill, not an always-expanded form | 24-07 Task 1 | COVERED | The old SearchBar cannot remain as a second supported entry. |
| CONTEXT | D-08 editable answer chips replace exposed legacy refinement | 24-07 Tasks 1-2 | COVERED | Relaxation code/tests and stale gallery recovery actions are removed. |
| EXCLUSION | Date-aware availability, free text, fuzzy inference, NLP price, party bands | — | EXCLUDED | Deferred by CONTEXT.md and not introduced here. |

<tasks>

<task type="tracer">
  <name>Task 1: Census and retire the five expanded-search and relaxation owners</name>
  <precondition>24-04-SUMMARY.md proves runtime disconnection, and 24-06-SUMMARY.md proves the four independent dependent-browser suites are migrated and green.</precondition>
  <reversibility rating="reversible">The deleted files remain recoverable from git and expose no external data or schema transition.</reversibility>
  <files>e2e/zero-result-relax.spec.ts, src/components/search/search-bar.tsx, src/components/search/relax-band.tsx, src/lib/search/relaxation.ts, tests/search/relaxation-ladder.test.ts</files>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-01 and D-08 are the non-negotiable retirement boundary.
    - .planning/phases/24-search-bar-rework/24-RESEARCH.md — resolved census-before-delete rule and required existing-test census.
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — executed runtime-disconnection ledger.
    - .planning/phases/24-search-bar-rework/24-06-SUMMARY.md — executed dependent-browser migration ledger.
    - e2e/zero-result-relax.spec.ts — obsolete browser assertions and any helper exports before deletion.
    - src/components/search/search-bar.tsx, src/components/search/relax-band.tsx, and src/lib/search/relaxation.ts — exported values and import names used by the census.
    - tests/search/relaxation-ladder.test.ts — ladder-only contract and any shared harness import before deletion.
  </read_first>
  <action>
    Run a fresh `git grep`/`git ls-files` census for the five target paths, their exports, the legacy band selector, and the old browser/unit suite names across tracked `src/`, `e2e/`, `tests/`, workflow, and design files. Classify every hit as: already removed by Plan 24-04, migrated by Plan 24-06, dedicated legacy owner being deleted here, dev-theme presentation owned by Task 2, or fixed design/visual/snapshot residue owned by Plan 24-08. Record the exact paths and names in `24-07-SUMMARY.md`.

    If any supported runtime or retained browser/unit flow still imports an exported value from the five targets, stop immediately and report that consumer; do not preserve the legacy behavior with a shim, copied helper, hidden route, or skipped assertion. When the census contains only the declared retirement set plus the bounded Task-2/Plan-24-08 residues, delete all five files together. Preserve the progressive-search, validation, result-state, booking, price-parity, one-tree, and axe contracts that supersede them per D-01 and D-08.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const retired=['e2e/zero-result-relax.spec.ts','src/components/search/search-bar.tsx','src/components/search/relax-band.tsx','src/lib/search/relaxation.ts','tests/search/relaxation-ladder.test.ts'];const left=retired.filter((p)=&gt;fs.existsSync(p));if(left.length)throw new Error('retired paths remain: '+left.join(', '));"</automated>
    <fails_when>The command exits non-zero, prints `retired paths remain`, or names any one of the five retirement targets as still present.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/search-results-states.test.tsx tests/validation/booking-schemas.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing test path, or prints a failed result-state or booking-schema assertion.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The command exits non-zero or prints a TypeScript diagnostic, unresolved deleted-module import, or missing-path error.</fails_when>
  </verify>
  <fails_when>A precondition summary is absent or red; the fresh census finds an undeclared supported consumer; any retirement target remains; a replacement test regresses; TypeScript still imports a deleted module; or an assertion is weakened to make deletion pass.</fails_when>
  <acceptance_criteria>The census is complete and recorded, all five paths are absent, the superseding focused tests pass, TypeScript exits 0, and every remaining legacy-named design/visual hit is assigned explicitly to Plan 24-08.</acceptance_criteria>
  <done>The obsolete expanded-search and automatic-relaxation implementation has no surviving runtime or focused test owner.</done>
</task>

<task type="auto">
  <name>Task 2: Clean the dev theme result and empty-state examples</name>
  <precondition>Task 1's census identifies `src/app/dev/theme/page.tsx` only as a stale presentation owner, not a runtime consumer of a deleted module.</precondition>
  <reversibility rating="reversible">The internal dev-gallery copy and props are local presentation changes recoverable from git.</reversibility>
  <files>src/app/dev/theme/page.tsx</files>
  <read_first>
    - AGENTS.md — read the repository's Next.js rule before touching the App Router page.
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md — installed-version Server Component guidance required by AGENTS.md.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-01 and D-08 result/empty-state truth and explicit date-specific exclusion.
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — final result-card and empty-state composition/copy.
    - src/app/dev/theme/page.tsx — current result-card fixture, searched-window fixture, empty-state copy, and recovery action.
    - src/components/search/search-result-card.tsx — optional searched-window prop and capacity-honest default rendering.
  </read_first>
  <action>
    Update only the search result and search-empty examples in the internal theme page. Render the result-card example without a searched-window fixture so the gallery does not imply date-specific places or availability that Phase 24 never checked. Rename its note to describe a capacity-honest submitted result. Change the stale filter-oriented empty title/body to the final `No spaces match those answers` and `Edit an answer above to try a different search.` contract, and remove the theme-only time/radius/clear-filter recovery action because the public answer chips own recovery per D-08. Delete any fixture/import made unused by those edits. Do not mount the interactive progressive journey in the gallery, change unrelated theme sections, or alter runtime search/result modules.
  </action>
  <verify>
    <automated>node node_modules/eslint/bin/eslint.js src/app/dev/theme/page.tsx</automated>
    <fails_when>The command exits non-zero, reports an ESLint error, or reports that the declared theme-page path is missing or ignored unexpectedly.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The command exits non-zero or prints a TypeScript diagnostic, unused-import error, or missing-path error caused by the theme cleanup.</fails_when>
  </verify>
  <fails_when>The gallery still instructs widening time/radius or clearing legacy filters; the result example still supplies a searched window; an unrelated theme section changes; an import becomes unused; ESLint fails; or TypeScript fails.</fails_when>
  <acceptance_criteria>The theme page shows the final capacity-honest result and calm answer-editing empty state, contains no legacy refinement action/copy in that section, and passes focused lint plus TypeScript.</acceptance_criteria>
  <done>The internal gallery no longer teaches the retired expanded-search or relaxation experience.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fresh tracked-file census → deletion decision | Incomplete enumeration could remove a module that a supported runtime or retained test still consumes. |
| Migrated browser contracts → obsolete suite retirement | Deleting the old suite must not erase booking, parity, one-tree, or accessibility coverage that Plan 24-06 promised to preserve. |
| Dev-theme fixture → developer-facing product contract | Stale gallery copy can reintroduce misleading date/radius/capacity expectations during later UI work. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-32 | Tampering | Usage census and five-path deletion | high | mitigate | Require 24-04/24-06 execution ledgers, repeat the tracked-file census, classify every hit, and halt on any undeclared supported consumer. |
| T-24-33 | Repudiation | Legacy-retirement evidence | high | mitigate | Record exact census hits, deleted paths, residual design owners, and focused command outcomes in 24-07-SUMMARY.md. |
| T-24-34 | Spoofing | Dev-theme search examples | high | mitigate | Use the final capacity-honest result/empty contract and remove searched-window and hidden-refinement implications. |
| T-24-35 | Denial of Service | Deleted imports and replacement coverage | high | mitigate | Run superseding focused tests and TypeScript immediately after deletion; do not defer broken imports to Plan 24-08. |
| T-24-SC | Tampering | Package supply chain | high | mitigate | Authorize no package install or manifest/lockfile edit and fail the plan on an unexpected dependency change. |
</threat_model>

<verification>
  <automated>node -e "const fs=require('node:fs');const retired=['e2e/zero-result-relax.spec.ts','src/components/search/search-bar.tsx','src/components/search/relax-band.tsx','src/lib/search/relaxation.ts','tests/search/relaxation-ladder.test.ts'];const left=retired.filter((p)=&gt;fs.existsSync(p));if(left.length)throw new Error('retired paths remain: '+left.join(', '));"</automated>
  <fails_when>Any of the five retired paths still exists.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run tests/search/search-results-states.test.tsx tests/validation/booking-schemas.test.ts</automated>
  <fails_when>The superseding result-state or URL-validation contract fails or reports zero tests.</fails_when>
  <automated>node node_modules/eslint/bin/eslint.js src/app/dev/theme/page.tsx</automated>
  <fails_when>The dev-theme cleanup has a lint error.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>A deleted import, stale type, or other TypeScript diagnostic remains.</fails_when>
</verification>

<success_criteria>
- The plan owns exactly six paths: five legacy deletions and one dev-theme cleanup, with no overlap with Plan 24-06 or Plan 24-08.
- A fresh census recorded in `24-07-SUMMARY.md` proves no supported consumer was deleted and enumerates every residual fixed-design/visual owner for Plan 24-08.
- The always-expanded SearchBar, RelaxBand, relaxation ladder, contradictory browser suite, and obsolete unit suite are absent per D-01 and D-08.
- The dev theme shows a capacity-honest result and the final answer-editing empty-state contract without legacy date/time/radius recovery.
- Focused replacement tests, dev-theme lint, and TypeScript are green; package manifests and lockfiles are unchanged.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-07-SUMMARY.md` when done.
</output>
