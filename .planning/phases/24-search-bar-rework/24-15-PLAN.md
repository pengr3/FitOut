---
phase: 24-search-bar-rework
plan: 15
type: execute
wave: 12
depends_on: [24-14]
files_modified:
  - src/components/search/search-experience.tsx
  - tests/search/progressive-search.test.tsx
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
requirements: [D-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-05, a browser-geolocation success callback released after Back at 375 px cannot write Current location or advance from activity to party."
    - "Per D-05, after cancelling an in-flight location attempt with Back, the activity heading regains focus, the cold browse URL remains unchanged, and the booker can select an address on the next visit to location."
    - "The existing explicit current-location success, address resolution, Cancel, direct Edit, focus, status, desktop Popover, and mobile Dialog contracts remain reducer-owned and functional."
  artifacts:
    - path: src/components/search/search-experience.tsx
      provides: "Reducer-owned active browser-location attempt identity and guarded callback events that invalidate atomically at journey boundaries."
    - path: tests/search/progressive-search.test.tsx
      provides: "Focused red-green component coverage for mobile Back invalidation and the preserved location success/failure lifecycle."
    - path: e2e/progressive-search.spec.ts
      provides: "Deterministic 375 px Chromium proof that releases the browser-geolocation callback only after Back has restored activity focus."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: tests/search/progressive-search.test.tsx
      via: "A callback event carrying its attempt identity is accepted only while the reducer still owns the matching active location attempt on the location screen."
      pattern: "activeLocationAttempt"
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/search-experience.tsx
      via: "The mobile semantic journey starts browser location, dispatches Back, then releases its captured callback and observes focus, route, and next address selection through the shipped Dialog host."
      pattern: "stale location callback cannot advance"
---

<objective>
Close the final D-05 browser lifecycle gap: an in-flight geolocation callback must be unable to re-open or advance the mobile progressive journey after the booker has used Back.

Purpose: The Phase 24 browser release gate remains blocked because the current 375 px Dialog path can finish a browser-location attempt after the booker has returned to activity.
Output: Reducer-authoritative location-attempt invalidation, focused component proof, and a deterministic Chromium regression that exercises the real mobile sheet.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-02-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-05-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-11-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-14-SUMMARY.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md
@src/components/search/search-experience.tsx
@src/components/search/location-step.tsx
@src/components/search/progressive-search-overlay.tsx
@tests/search/progressive-search.test.tsx
@e2e/progressive-search.spec.ts

<interfaces>
- `SearchExperienceCoordinator` is the sole owner of `ProgressiveSearchState`, the canonical client navigation path, focus effect, and browser-geolocation initiation; `LocationStep` only renders the controlled action and address resolver.
- The existing attempt ref invalidates callbacks outside the reducer, but `progressiveSearchReducer` currently accepts every `RESOLVE_LOCATION` event. This plan makes the reducer the validity authority for browser callback events while retaining address resolution and canonical URL serialization in their existing owners.
- `ProgressiveSearchOverlay` chooses one existing desktop Popover or 375 px Dialog presentation. This plan must not fork its tree, modify its positioning/focus primitives, or alter Plan 24-11/24-14 presentation behavior.
- The progressive E2E suite already owns the deterministic Photon route, 375 px viewport, real seeded result journey, and named stale-location scenario. It must release the captured browser callback after an observed Back transition rather than depend on a short wall-clock timeout.
</interfaces>
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | A one-question-at-a-time activity → location → party experience with smooth, recoverable transitions | 24-15 Tasks 1–2 repair the failed location-to-Back transition | COVERED |
| REQ | Phase 24 ROADMAP requirements are `TBD` | D-05 is the operative gap contract; no new requirement ID is invented | COVERED |
| RESEARCH | Explicit browser-location request, invalid/late callback recovery, one Client Component state owner, and deterministic Playwright location proof | 24-15 Tasks 1–2 | COVERED |
| CONTEXT | D-05 address or successfully resolved current location advances directly to party, while a cancelled attempt cannot supply an answer | 24-15 Tasks 1–2 | COVERED |
| CONTEXT | D-01–D-04 and D-06–D-12 | Existing Phase 24 artifacts; Tasks 1–2 preserve their reducer, URL, focus/status, and responsive-host contracts | PRESERVED |
| DEFERRED | Date-aware availability and other deferred search ideas | Not part of the failed callback lifecycle | EXCLUDED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make Back invalidate one in-flight browser-location attempt end to end</name>
  <reversibility rating="reversible">The active attempt representation is internal client state with no persisted, URL, server, or schema contract; it can be changed locally if a later browser API requires a different adapter.</reversibility>
  <files>src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx, e2e/progressive-search.spec.ts</files>
  <behavior>
    - At 375 px, selecting Use my location begins one explicit browser request; clicking Back returns to the activity heading before that request's captured success callback is released.
    - Releasing that callback after Back leaves the activity screen focused, creates no Current location answer, does not reveal party choice, and preserves the cold browse URL.
    - A normal current-location success while the matching request remains active still advances once to party, and address selection remains the separate controlled resolver path.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-VERIFICATION.md — exact failed truth, observed 375 px state, and required named/full Chromium proof.
    - .planning/phases/24-search-bar-rework/24-02-SUMMARY.md — established monotonic attempt-token intent and D-05 outcome copy.
    - .planning/phases/24-search-bar-rework/24-11-SUMMARY.md and 24-14-SUMMARY.md — one reducer-owned Popover/Dialog host, current focus handling, and protected desktop correction behavior.
    - src/components/search/search-experience.tsx and tests/search/progressive-search.test.tsx — current reducer events, attempt ref, component geolocation mocks, status owner, and matchMedia harness.
    - src/components/search/search-bar.tsx — established client-side explicit browser-geolocation and canonical URL ownership conventions to preserve rather than reintroduce in a new owner.
    - tests/search/search-results-states.test.tsx — repository component-test router capture, cleanup, rendering, and positive-state assertion conventions for the focused lifecycle cases.
    - src/components/search/location-step.tsx and src/components/search/progressive-search-overlay.tsx — controlled Use my location props and the existing single mobile Dialog presentation that must stay intact.
    - e2e/progressive-search.spec.ts — existing mobile stale-location scenario, Photon fixture, semantic selectors, and post-Back address recovery.
    - e2e/search-and-book.spec.ts — seeded browser-journey setup and teardown conventions that the progressive regression continues to follow without a fixture or configuration fork.
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md and node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md — installed Next.js 16 Client Component/browser API and same-origin router contracts.
  </read_first>
  <action>
    Start RED by adding one reducer/component case and one 375 px browser case for D-05. In the component harness, capture the browser success callback, start current location from the mobile presentation, activate Back, wait for the activity heading focus, and invoke the captured success only afterward. Assert the reducer cannot advance to party, cannot retain the browser label, and cannot navigate. In the Playwright scenario with the same name, replace the fixed short callback timer with a test-owned captured callback released only after the semantic Back/focus assertion; keep the existing follow-up exact activity selection and mocked address resolution so recovery remains a real journey rather than a terminal assertion. Follow `src/components/search/search-bar.tsx` for explicit client geolocation and canonical URL ownership, `tests/search/search-results-states.test.tsx` for focused component-test router/cleanup conventions, and `e2e/search-and-book.spec.ts` for seeded browser-journey setup and teardown; reuse those patterns without creating another location owner, fixture protocol, or Playwright configuration path.

    In `search-experience.tsx`, make browser-location callback validity reducer-owned per D-05. Add an active location-attempt identity to `ProgressiveSearchState` and event payloads for request start, browser-location success, and browser-location failure. A local monotonic ref may mint an identity, but only the reducer may mark it active, clear it, or accept a callback. The success/failure reducer branches must require both a matching active identity and the location screen; otherwise they are no-ops. Back must clear the active identity in the same state transition that restores history. Address resolution, Cancel, direct Edit, correction, and a replacement browser request must likewise invalidate an older browser attempt before their own state change. Derive the pending button state from the active reducer value, retain the current finite latitude/longitude bounds and Current location label, preserve the calm address fallback message, and route every accepted browser callback through the reducer rather than a direct callback-time state write. Keep explicit-click permission behavior, one Search progress owner, heading focus, URL serialization, desktop Popover, and mobile Dialog untouched.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "stale location callback cannot advance"</automated>
    <fails_when>A captured callback can advance after Back, focus remains on the location/party screen, a browser label or query value appears after invalidation, normal current-location success fails to reach party, the named Chromium run reports zero tests, or either command exits non-zero.</fails_when>
  </verify>
  <done>The named 375 px real-browser path proves that a callback released after Back cannot mutate the reducer-owned journey, while a matching active browser-location response still performs D-05's normal advance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand invalidation-boundary proof and close the full Chromium regression</name>
  <precondition>Playwright's configured local server can bind port 3000 and connect to its isolated test database; the executor must halt and report the verified conflicting process or unavailable database if that is false.</precondition>
  <reversibility rating="reversible">This extends local reducer and browser regressions around an internal callback lifecycle; it introduces no public or persisted interface.</reversibility>
  <files>src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx, e2e/progressive-search.spec.ts</files>
  <behavior>
    - Cancel, a direct result-chip Edit, an address resolution, a newer browser request, and a correction transition each invalidate an older browser callback without changing their established Back/Cancel or focus/status semantics.
    - Browser permission denial, unavailable APIs, invalid coordinates, and a matching success remain recoverable on the location question with the address control enabled and no false URL write.
    - The complete Chromium progressive-search file remains green across the previously repaired desktop direct-edit Popover, mobile Dialog geometry, fixed 25 km reach, accessibility, and this stale-location scenario.
  </behavior>
  <read_first>
    - src/components/search/search-experience.tsx and tests/search/progressive-search.test.tsx — Task 1's reducer identity contract and focused D-05 test harness.
    - e2e/progressive-search.spec.ts — named mobile regression plus protected desktop direct-edit, overlay geometry, fixed-reach, and accessibility cases.
    - .planning/phases/24-search-bar-rework/24-05-SUMMARY.md and 24-14-SUMMARY.md — established semantic E2E fixtures and assertions that must remain present.
    - .planning/phases/24-search-bar-rework/24-VERIFICATION.md — all-green Chromium requirement and final human-only visual follow-up.
  </read_first>
  <action>
    Extend the focused reducer/component matrix before finalizing Task 1's implementation. Deliver delayed success and failure callbacks after Cancel, direct Edit, address resolution, correction, and a subsequent current-location request; assert only the currently active matching callback may alter progress or advance. Preserve the existing denial, unavailable-API, invalid-coordinate, address, and normal-success expectations, and add focused checks that the pending control is restored and AddressAutocomplete stays operable after every rejected or invalidated callback. Do not alter the reducer's retained-answer Back history, literal Cancel navigation, focus effect, status-region ownership, result chips, location-step props, URL schema, server query, overlay component, or copy outside the established D-05 outcomes.

    Run the complete `progressive-search.spec.ts` Chromium file after the named mobile test. Keep the earlier desktop direct-edit Popover geometry, mobile full-screen Dialog geometry, cross-municipality fixed-reach, axe, and seeded-listing flows exactly as their current assertions require. Do not change shared seed helpers, Playwright configuration, snapshots, dependencies, migrations, route/server code, or planning/state/roadmap artifacts.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/search-experience.tsx tests/search/progressive-search.test.tsx e2e/progressive-search.spec.ts</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <fails_when>An invalidated callback changes screen, status, pending state, answers, or URL; a current active request loses its valid completion; address recovery becomes disabled; the complete Chromium suite reports zero tests or any failed case; or ESLint/Vitest exits non-zero.</fails_when>
  </verify>
  <done>D-05 is protected at every invalidation boundary and the complete progressive Chromium suite is green without regressing the completed responsive overlay, direct-edit, geographic, or accessibility contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser geolocation callback → client journey reducer | An asynchronous browser callback can arrive after the booker has changed journey state and must not become a selected location. |
| Address resolver → client journey reducer | A resolved address replaces a location answer and must invalidate any in-flight browser callback without taking over URL or search authority. |
| Client journey → public route | Only a currently confirmed activity/location/party tuple may reach the existing same-origin serializer and server validation path. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-15-01 | Spoofing / Tampering | browser geolocation callback | medium | mitigate | Accept a browser callback only through the reducer when its identity matches the active location-screen attempt; invalidate it atomically on Back and all replacement boundaries, then prove the mobile browser ordering. |
| T-24-15-02 | Information Disclosure | location answer/URL handoff | medium | mitigate | Rejected, stale, invalid, denied, and unavailable attempts do not write coordinates or label to retained answers or the URL; focused and Chromium tests observe the cold route. |
| T-24-15-03 | Denial of Service | pending location action and recovery | medium | mitigate | Derive pending state from the reducer's active attempt and prove invalidation restores the address resolver and current-location control without duplicate status ownership. |
| T-24-15-04 | Tampering | direct-edit and Cancel lifecycle | medium | mitigate | Preserve the reducer's established history, constant browse Cancel route, and existing Popover/Dialog host; run the full progressive Chromium matrix after the local fix. |
| T-24-15-SC | Tampering | package supply chain | low | accept | This plan installs no package and changes no dependency, lockfile, or configuration. |
</threat_model>

<verification>
Run the focused component suite and named 375 px Chromium stale-callback regression before the full Chromium progressive-search suite. The full file is the Phase 24 all-green browser truth and includes the earlier direct-edit, overlay, geographic, and accessibility evidence.
</verification>

<success_criteria>
- A callback deliberately released after Back in the actual 375 px Dialog journey cannot advance to party or create a browser-selected location.
- A matching active browser-location success still advances exactly once, while every rejected/invalidated path leaves address entry usable and leaves the URL untouched.
- The focused component suite, scoped ESLint, named mobile browser regression, and complete progressive Chromium suite all exit successfully without source/configuration scope expansion.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-15-SUMMARY.md` when done.
</output>
