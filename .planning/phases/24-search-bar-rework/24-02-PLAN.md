---
phase: 24-search-bar-rework
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/components/search/search-experience.tsx
  - src/components/search/activity-step.tsx
  - src/components/search/location-step.tsx
  - src/components/search/party-step.tsx
  - tests/search/progressive-search.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-02, one idle pill opens one explicit activity → location → party journey, and the same DOM flow reflows at desktop and 375 px widths."
    - "Per D-02, Back returns to the previous visited screen without clearing confirmed answers, while Cancel clears every draft answer and navigates to the browse URL `/`."
    - "Per D-02 and D-03, choosing an answer-chip Edit opens that exact step with all three submitted answers retained, and a replacement answer resumes the fixed progression."
    - "Per D-04, catalogue typing only filters the complete known list; only an exact option event advances, and zero visible options render `No matching activity or type`."
    - "Per D-05, address and explicitly requested browser location success advance to party, while unsupported, denied, stale, and failed location attempts remain recoverable on the same step."
    - "Per D-03 and D-06, For me submits one immediately; For a group accepts one exact bounded whole-person count and enables See spaces only when that value is valid."
    - "Each step transition has deliberate focus, visible Step N of 3 context, one stable status owner, and motion that uses the repository duration/easing tokens and existing reduced-motion reset."
  prohibitions:
    - statement: "Typing an unmatched or approximate activity cannot become a stored or submitted category."
      status: resolved
      verification: explicit
    - statement: "Cancel cannot retain a hidden submitted answer or preserve the results URL."
      status: resolved
      verification: explicit
    - statement: "The group control cannot offer party-size bands or submit a fractional, nonnumeric, below-two, or above-bound value."
      status: resolved
      verification: explicit
    - statement: "The client journey cannot add date, time, price, radius, remaining-capacity, or availability semantics."
      status: resolved
      verification: explicit
    - statement: "Responsive presentation cannot create separate desktop and mobile search trees."
      status: resolved
      verification: explicit
  artifacts:
    - path: src/components/search/activity-step.tsx
      provides: "Controlled full-catalogue filter and exact option-selection step."
      exports: [ActivityStep, ActivityStepProps]
    - path: src/components/search/location-step.tsx
      provides: "Controlled address and explicit browser-geolocation step with recoverable outcomes."
      exports: [LocationStep, LocationStepProps]
    - path: src/components/search/party-step.tsx
      provides: "Controlled solo/group choice and exact bounded group entry."
      exports: [PartyStep, PartyStepProps, parseGroupPartySize]
    - path: src/components/search/search-experience.tsx
      provides: "Reducer-owned navigation history, retained answers, edit origin, Cancel, focus, status, and canonical submit wiring."
      exports: [ProgressiveSearchScreen, ProgressiveSearchState, ProgressiveSearchEvent, progressiveSearchReducer]
    - path: tests/search/progressive-search.test.tsx
      provides: "Focused component proof of the complete transition, correction, group, location, focus, and status contract."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: src/components/search/activity-step.tsx
      via: "The reducer passes the retained category and accepts only a CatalogueOption selected from SPACE_TYPES or ACTIVITY_TAGS."
      pattern: "ActivityStep"
    - from: src/components/search/search-experience.tsx
      to: src/components/search/location-step.tsx
      via: "Resolved address or successful browser coordinates dispatch one location answer and push the party screen."
      pattern: "LocationStep"
    - from: src/components/search/search-experience.tsx
      to: src/components/search/party-step.tsx
      via: "Solo and parsed group values call the canonical submit function created in 24-01; neither step owns URL or query authority."
      pattern: "PartyStep"
    - from: src/components/search/party-step.tsx
      to: src/lib/validation/listing.ts
      via: "The client affordance imports MAX_OPEN_CAPACITY while the 24-01 searchParamsSchema remains authoritative."
      pattern: "MAX_OPEN_CAPACITY"
---

<objective>
Complete the D-01/D-02/D-03/D-04/D-05/D-06 client-side progressive journey on top of the 24-01 tracer: retained Back and direct Edit answers, Cancel-to-browse, exact catalogue and group input, recoverable location, deliberate focus/status ownership, responsive tokenized motion, and focused component proof.

Purpose: Turn the proven solo tracer into the full recoverable conversation without moving validation, search execution, capacity filtering, or result truth into the browser.
Output: Three controlled step components, an explicit tested journey reducer, full correction and group behavior, and accessibility/motion coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-01-PLAN.md
@.planning/phases/24-search-bar-rework/24-01-SUMMARY.md

<interfaces>
- Plan 24-01 creates SearchExperience, SearchExperienceInitialAnswers, SearchAnswerKey, the canonical same-origin submit path, the `partySize`/`locationLabel` SearchParams fields, and server-composed result children.
- The installed Next.js 16.2.7 Server/Client guide keeps state, event handlers, effects, router use, and navigator.geolocation inside Client Components while allowing rendered Server Component children to cross the boundary as serializable output.
- The installed `useRouter` contract adds history with `push`; this journey uses local reducer history for step Back and a literal `router.push("/")` for Cancel rather than trusting or reconstructing a caller-supplied pathname.
- SPACE_TYPES and ACTIVITY_TAGS remain the only selectable catalogue values; MAX_OPEN_CAPACITY remains the shared party ceiling.
- AddressAutocomplete retains Photon debounce/cancellation and owns address-lookup announcements; LocationStep composes it and owns only journey/geolocation outcomes.
- StepperControl is a controlled read-only increment/decrement analog, but D-06 requires practical exact group entry through the repository Input primitive across the full 2..MAX_OPEN_CAPACITY range.
</interfaces>
</context>

## Artifacts this phase produces

- New path `src/components/search/activity-step.tsx` with exported `ActivityStep` and `ActivityStepProps`.
- New path `src/components/search/location-step.tsx` with exported `LocationStep` and `LocationStepProps`.
- New path `src/components/search/party-step.tsx` with exported `PartyStep`, `PartyStepProps`, and pure `parseGroupPartySize`.
- New exports `ProgressiveSearchScreen`, `ProgressiveSearchState`, `ProgressiveSearchEvent`, and `progressiveSearchReducer` from `src/components/search/search-experience.tsx`.
- New reducer fields `screen`, `answers`, `history`, `editOrigin`, and `groupDraft`, plus explicit `ENGAGE`, `SELECT_ACTIVITY`, `RESOLVE_LOCATION`, `CHOOSE_SOLO`, `CHOOSE_GROUP`, `BACK`, `EDIT`, `CANCEL`, and `HYDRATE_RESULTS` events.
- New component-test cases in `tests/search/progressive-search.test.tsx`; no endpoint, URL field, database query, schema migration, dependency, or external-service integration is added.

## Spec-less probe fallback

Skipped visibly because ROADMAP.md still maps no Phase 24 requirement IDs and records `Requirements: TBD`; the locked CONTEXT decisions and explicit `must_haves` prohibitions supply this plan's testable contract without inventing requirement-derived predicates.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | One-question-at-a-time progressive client journey | 24-02 Tasks 1-2 | COVERED | Completes the interaction paths above the proven 24-01 server tracer. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD and REQUIREMENTS.md has no Phase 24 mapping. |
| RESEARCH | Explicit state/history, exact catalogue, exact bounded group entry | 24-02 Task 1 | COVERED | Uses controlled components and the existing authorities. |
| RESEARCH | Geolocation race recovery, focus/status ownership, tokenized responsive motion | 24-02 Task 2 | COVERED | Uses one DOM and the global reduced-motion contract. |
| CONTEXT | D-01, D-02, D-03 | 24-02 Task 1 | COVERED | Pill, fixed order, Back/Edit retention, Cancel, auto-advance, and both party actions. |
| CONTEXT | D-04 | 24-02 Task 1 | COVERED | Full deterministic catalogue with exact-only selection and locked no-match copy. |
| CONTEXT | D-05 | 24-02 Task 2 | COVERED | Address and explicit current-location success plus recoverable failures. |
| CONTEXT | D-06 | 24-02 Task 1 | COVERED | Solo and exact group choice share the 24-01 validated party contract. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete Back, Edit, Cancel, exact catalogue, and exact party transitions</name>
  <reversibility rating="costly">Per D-06, changing the group-entry semantics later requires coordinated client, canonical URL, schema, query, chip, and browser-contract changes; the decision is already locked, so no new checkpoint is required.</reversibility>
  <files>src/components/search/search-experience.tsx, src/components/search/activity-step.tsx, src/components/search/location-step.tsx, src/components/search/party-step.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - Engage enters activity; selection enters location; location success enters party; Back pops the actual visited screen and retains every confirmed answer.
    - Edit from submitted results opens the named activity, location, or party screen with all answers retained; replacement activity/location continues through the fixed following questions with retained values prefilled.
    - Cancel from any in-progress or results-edit screen clears category, location, party, group draft, and history, then pushes exactly `/` and restores the idle pill.
    - Activity filtering is deterministic over every SPACE_TYPES and ACTIVITY_TAGS label; an input string cannot dispatch SELECT_ACTIVITY.
    - For me submits 1 immediately; For a group reveals one exact numeric entry, and See spaces submits only a base-10 whole number between 2 and MAX_OPEN_CAPACITY inclusive.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — executed tracer exports and deviations; stop if it is absent because depends_on has not completed.
    - src/components/search/search-experience.tsx and tests/search/progressive-search.test.tsx — 24-01 coordinator and focused harness to extend rather than replace.
    - src/lib/listing-vocab.ts — complete closed SPACE_TYPES/ACTIVITY_TAGS authority.
    - src/lib/validation/booking.ts and src/lib/validation/listing.ts — server-owned party contract and shared maximum.
    - src/components/ui/command.tsx and src/components/listing/address-autocomplete.tsx — accessible exact-option and resolver composition patterns.
    - src/components/ui/input.tsx, src/components/booking/pass-stepper.tsx, and src/components/booking/stepper-control.tsx — numeric input and controlled headcount analogs.
    - tests/search/search-results-states.test.tsx — router capture, cleanup, within(), and positive-rendering test conventions.
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md and node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md — installed client boundary and router safety contracts.
  </read_first>
  <action>
    Start RED by expanding `tests/search/progressive-search.test.tsx` with a table over every reducer event and screen. Assert fixed forward order, visited-screen Back behavior, first-step Back to idle, all answer retention, each answer-chip Edit target, re-confirmation after a replacement answer, and D-02 Cancel clearing both local state and the submitted URL through one literal `/` push. Add exact-catalogue tests proving the full activity and space-type groups render, label filtering is deterministic and case-insensitive, arbitrary input never commits, and zero visible options renders exactly `No matching activity or type`. Add party tests for immediate For me submission and For a group values at 2 and MAX_OPEN_CAPACITY, plus blank, 1, fractional, signed, exponent, nonnumeric, and over-bound strings that keep See spaces disabled.

    Extract controlled `ActivityStep`, `LocationStep`, and `PartyStep` leaves from the 24-01 coordinator, leaving SearchExperience as the sole navigation/answer owner. Implement one explicit reducer whose `screen` does not infer progress from missing values; store a visited-screen history so Back and result-origin edits are deterministic. EDIT opens exactly its named step without clearing any answer. A newly confirmed activity or location replaces that answer, retains the others, and proceeds through the fixed next question. CANCEL invalidates pending browser work, clears answers/history/groupDraft, restores idle, and calls only `router.push("/")`; do not use browser `router.back()` for intra-journey correction.

    In `PartyStep`, present the two D-06 decisions `For me` and `For a group`. The group branch owns a raw text draft rendered through the repository Input with `type="number"`, `inputMode="numeric"`, `min={2}`, and `max={MAX_OPEN_CAPACITY}`. Make `parseGroupPartySize` accept only trimmed ASCII base-10 digits whose Number value is a safe integer in that inclusive range; do not clamp, round, accept exponent notation, or convert invalid input into a band. Enable `See spaces` only when this parser returns a number, then pass that exact number to the canonical submit function from 24-01, which remains subordinate to server validation and SQL capacity authority.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <fails_when>The command exits non-zero, reports zero tests, reducer order/history differs from the fixed journey, Back or Edit loses an answer, Cancel preserves state or a query string, arbitrary catalogue text commits, or any invalid group string enables See spaces.</fails_when>
  </verify>
  <acceptance_criteria>The focused component file exits 0 and proves D-01/D-02/D-03/D-04/D-06 with exact event-to-state assertions, router capture, the locked no-match string, both boundary group values, and every named invalid group class.</acceptance_criteria>
  <done>The progressive flow is fully correctable and cancellable, and both solo and exact group submission paths feed the unchanged 24-01 canonical/server contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make both location paths race-safe, focus-owned, announced once, and responsive</name>
  <files>src/components/search/search-experience.tsx, src/components/search/activity-step.tsx, src/components/search/location-step.tsx, src/components/search/party-step.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - Address resolution and current-location success dispatch one retained location and advance once; neither typed address text nor a stale browser callback can advance.
    - Unsupported, denied, or failed geolocation stays on location, keeps AddressAutocomplete operable, restores the action, and announces one actionable message.
    - Every screen presents visible Step 1 of 3, Step 2 of 3, or Step 3 of 3 context and moves focus to its heading or first control only after that screen mounts.
    - AddressAutocomplete owns its lookup status; Search progress owns only journey transitions and browser-geolocation outcomes, starts empty, and does not announce static hints.
    - One DOM tree uses tokenized opacity/translate transitions and responsive layout classes; the global reduced-motion rule collapses those durations without a second motion system.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-01-SUMMARY.md — actual AddressAutocomplete audience and Search progress inventory decisions.
    - src/components/search/search-experience.tsx, src/components/search/activity-step.tsx, src/components/search/location-step.tsx, and src/components/search/party-step.tsx — Task 1 state and controlled leaves.
    - src/components/listing/address-autocomplete.tsx and tests/listing/address-autocomplete.test.tsx — Photon cancellation, resolver feedback, and the status region that must not be duplicated.
    - src/components/search/search-bar.tsx — shipped browser-geolocation action and calm fallback copy.
    - src/app/globals.css — `--motion-fast`, `--motion-base`, `--motion-slow`, `--motion-ease-standard`, and the global reduced-motion media rule.
    - tests/design/motion-budget.test.ts and tests/design/live-regions.test.tsx — automatic transition budget and one-region inventory checks.
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md — navigator, state, effects, and event handlers remain inside the installed-version Client Component boundary.
  </read_first>
  <action>
    Add RED component cases for address-selection success, successful geolocation after an explicit click, unsupported browser API, permission denial, error callback, and a late success arriving after Back, Cancel, or a newer attempt. Mock AddressAutocomplete only as a controlled resolver and mock navigator.geolocation per case. Assert there is one `Search progress` status owner whose first render is empty, address lookup text is not duplicated there, each geolocation outcome is announced once, and a failed/stale callback leaves the URL untouched. Cover pointer and keyboard catalogue selection, each step's exact visible progress label, post-render focus target, retained focus-visible control, and one interaction tree at a 375 px container width.

    Keep `LocationStep` controlled by the coordinator. Start geolocation only from the visible `Use my location` button, disable and relabel that action while a request is pending, and use a monotonically increasing attempt token so Back, Cancel, Edit, or a newer attempt invalidates every older callback. On success, accept only finite latitude/longitude within their geographic bounds, retain the display label `Current location`, clear the location error, and dispatch RESOLVE_LOCATION once. On unavailable, invalid, denied, or error outcomes, remain on location with the address resolver enabled and one calm message that tells the booker to type an address instead; never expose browser error details.

    Give each step a stable heading ref and visible `Step N of 3` text. After the reducer changes screen, focus the mounted heading with `tabIndex={-1}` or the first appropriate input in a post-render effect; do not use positive tabindex and do not steal focus for ordinary typing, validation, or status changes. Keep the existing stable, initially empty `Search progress` region for transition/geolocation outcomes and leave Photon lookup outcomes in AddressAutocomplete. Render one semantic DOM order for every width, using responsive flex/grid classes only. Apply opacity/translate transition utilities with `duration-(--motion-base)` and `ease-(--motion-ease-standard)`; add no CSS keyframe, motion dependency, JavaScript timer, duplicate mobile tree, or second reduced-motion rule.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <fails_when>The command exits non-zero, reports zero tests, geolocation fires without activation, a stale/error callback advances or navigates, focus remains on a removed control or body, a status is duplicated, or the 375 px case renders more than one journey tree.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/motion-budget.test.ts tests/design/live-regions.test.tsx</automated>
    <fails_when>The command exits non-zero, reports zero tests, a transition exceeds the 320 ms budget, the search region is absent or multiply owned, a live region is populated on arrival, or an empty scanner can satisfy the inventory.</fails_when>
  </verify>
  <acceptance_criteria>The component and design commands exit 0; D-05 success/failure/stale cases, keyboard and pointer focus, exact step labels, singular status ownership, one responsive DOM, tokenized motion, and reduced-motion compatibility are each asserted by named tests.</acceptance_criteria>
  <done>The complete progressive client journey is recoverable, keyboard/focus coherent, honestly announced, and responsive without weakening the server-side search authority established in 24-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Typed catalogue/group input to retained client answers | Browser text is untrusted and cannot become a confirmed enum or party value without exact local parsing followed by 24-01 server validation. |
| Browser geolocation callback to journey state | Permission, coordinate values, callback order, and callback timing are outside application control. |
| Client journey to Next.js navigation | Draft answers become a request only through the 24-01 constant-path canonical serializer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-06 | Tampering | activity-step.tsx and party-step.tsx | high | mitigate | Commit catalogue values only from known option objects; parse group input as bounded base-10 safe integers and retain 24-01 server revalidation as authority. |
| T-24-07 | Spoofing | search-experience.tsx navigation | high | mitigate | Cancel uses literal `/`; submission delegates to the 24-01 literal-path URLSearchParams builder and never accepts a user-provided href. |
| T-24-08 | Tampering | location-step.tsx geolocation callbacks | medium | mitigate | Require explicit activation, validate finite geographic bounds, and ignore callbacks whose attempt token is no longer current. |
| T-24-09 | Repudiation | Back/Edit/Cancel state transitions | medium | mitigate | Use one explicit reducer/event table and component assertions so retention and destructive cancellation cannot be inferred inconsistently by separate leaves. |
| T-24-10 | Denial of Service | repeated geolocation and transition work | low | accept | Disable the action while current work is pending, invalidate stale callbacks, and use CSS transitions within the existing 320 ms budget; remaining local event cost is bounded. |
| T-24-SC | Tampering | package supply chain | low | accept | This plan runs no package-manager action and changes no dependency declaration. |
</threat_model>

<verification>
  <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
  <fails_when>The command exits non-zero, reports zero tests, or any D-01 through D-06 client transition, retention, cancellation, exact-selection, location, group, focus, status, or responsive assertion fails.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/motion-budget.test.ts tests/design/live-regions.test.tsx</automated>
  <fails_when>The command exits non-zero, reports zero tests, or motion/live-region source contracts reject the completed journey.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or emits a diagnostic for the new controlled-step props, reducer events, retained answers, group parser, or 24-01 coordinator interface.</fails_when>
</verification>

<success_criteria>
- D-01 through D-06 are fully represented in the client flow while the 24-01 Zod and SQL layers remain the only request/query authorities.
- Back and every direct Edit retain answers; Cancel always abandons them and restores `/` browse.
- Only exact catalogue options and exact valid group integers can advance or submit.
- Address and current-location successes converge; every unsupported, denied, invalid, stale, or failed browser-location outcome stays recoverable.
- Focus, step context, status announcements, motion, reduced motion, and 375 px behavior have focused automated coverage over one DOM tree.
- No date, time, price, radius, fuzzy/free-text search, remaining-capacity claim, package, endpoint, schema migration, or authentication work is introduced.
</success_criteria>

<output>
Create .planning/phases/24-search-bar-rework/24-02-SUMMARY.md when done.
</output>
