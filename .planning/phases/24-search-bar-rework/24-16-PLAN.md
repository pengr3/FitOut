---
phase: 24-search-bar-rework
plan: 16
type: execute
wave: 13
depends_on: [24-15]
files_modified:
  - src/components/search/progressive-search-overlay.tsx
  - tests/search/progressive-search.test.tsx
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
requirements: [D-09]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-09, immediately after opening at 375 px and on the following animation frame, the mobile progressive-search Dialog occupies the dynamic viewport within the established one-pixel geometry bounds."
    - "Per D-09, the desktop pill and result-chip journeys retain the existing anchored Popover; the mobile correction changes neither that host nor document-flow geometry."
    - "The one reducer-owned question tree, heading focus, sole polite status region, Back, Cancel, and cold-browse recovery continue to work inside the full-screen mobile sheet."
  artifacts:
    - path: src/components/search/progressive-search-overlay.tsx
      provides: "A search-local mobile Dialog presentation that overrides the shared centred dialog's scale animation as well as its centring geometry below sm, without changing shared primitives."
    - path: tests/search/progressive-search.test.tsx
      provides: "Focused mobile-host regression coverage for the local full-screen presentation and preserved one-tree accessibility contract."
    - path: e2e/progressive-search.spec.ts
      provides: "Chromium geometry evidence at 375 px on the first rendered sheet frame and the next animation frame, retaining the existing exact viewport bounds."
  key_links:
    - from: src/components/search/progressive-search-overlay.tsx
      to: src/components/ui/dialog.tsx
      via: "The mobile DialogContent locally neutralizes the shared centred content's transform and zoom presentation only below sm."
      pattern: "max-sm:data-open:zoom-in-100"
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/progressive-search-overlay.tsx
      via: "The named geometry journey measures the rendered progressive-search-mobile-sheet instead of inferring viewport coverage from class strings."
      pattern: "progressive-search-mobile-sheet"
---

<objective>
Close the measured D-09 mobile-sheet geometry regression: the 375 px progressive Dialog currently opens at an X offset of 1.8507912158966064 px, exceeding the established full-viewport bound.

Purpose: A full-screen mobile question must be a real viewport sheet from its first rendered frame, not a centred Dialog briefly scaled by inherited entry motion.
Output: A search-local mobile presentation correction, focused component regression, and strengthened Chromium proof with no primitive, route, reducer, configuration, snapshot, migration, or dependency change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-11-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-13-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-14-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-15-PLAN.md
@node_modules/next/dist/docs/01-app/01-getting-started/11-css.md
@src/components/search/progressive-search-overlay.tsx
@src/components/search/search-experience.tsx
@src/components/ui/dialog.tsx
@src/components/ui/popover.tsx
@src/components/patterns/responsive-dialog.tsx
@tests/search/progressive-search.test.tsx
@e2e/progressive-search.spec.ts

<interfaces>
- `ProgressiveSearchOverlay` is the sole responsive presentation boundary. It selects one controlled Radix Dialog below `(max-width: 639px)` and one Popover otherwise; its `open`, `onDismiss`, focus callbacks, and `children` come unchanged from `SearchExperienceCoordinator`.
- The shared `DialogContent` starts centred and applies open/closed zoom motion. This plan changes only the search overlay's mobile class composition so that a full-viewport Dialog does not inherit the centred-scale visual state; `src/components/ui/dialog.tsx` and `src/components/ui/popover.tsx` remain byte-unchanged.
- `SearchExperienceCoordinator` remains the only owner of reducer state, heading focus, status, Back, Cancel, URL navigation, and the exact direct-edit HTMLElement. This plan neither changes that interface nor creates a breakpoint-specific question tree.
- The existing `overlay and sheet geometry anchor the progressive flow without route reflow` case already measures real 1280 px and 375 px boxes. Strengthen that same browser journey; do not replace it with snapshots, source scans, a result URL shortcut, or looser bounds.
</interfaces>
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Progressive activity → location → party questions appear only after engagement, with smooth transitions | 24-16 Tasks 1–2 make the already-selected mobile presentation geometrically true without changing progression | COVERED |
| REQ | Phase 24 ROADMAP requirements are `TBD` | D-09 is the operative locked presentation requirement; no new requirement identifier is invented | COVERED |
| RESEARCH | Reuse installed Radix wrappers, retain one client-side state owner, preserve focus/status and reduced-motion behavior | 24-16 Task 1 keeps the local Dialog wrapper; Task 2 proves live browser geometry | COVERED |
| CONTEXT | D-09 requires a desktop floating overlay and a small-screen full-screen sheet without pushing page or result content | 24-16 Tasks 1–2 | COVERED |
| CONTEXT | D-01–D-08 and D-10–D-12 | Existing Phase 24 behavior; both tasks preserve their coordinator, Popover, route, geographic, and accessibility contracts | PRESERVED |
| DEFERRED | Date-aware availability, free-text search, fuzzy matching, and natural-language price parsing | Outside the measured presentation defect | EXCLUDED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the mobile Dialog a full-viewport sheet on its opening frame</name>
  <reversibility rating="reversible">The change is a search-local responsive class composition behind the existing Dialog interface; undoing it touches no persisted, route, public, or shared-primitive contract.</reversibility>
  <files>src/components/search/progressive-search-overlay.tsx, tests/search/progressive-search.test.tsx</files>
  <behavior>
    - At the existing mobile media query, opening the idle pill mounts exactly one `progressive-search-mobile-sheet`, one current activity heading, and one Search progress status owner.
    - The mobile sheet's local presentation explicitly cancels the shared centred Dialog's entry and exit scale, as well as its desktop centring, while retaining the dynamic-viewport, four-edge, unrounded, scrollable sheet behavior.
    - At desktop width, the existing pill-anchored and direct-edit Popover path is selected instead; Back, Cancel, focus restoration, and the reducer-owned child slot retain their current behavior.
  </behavior>
  <read_first>
    - AGENTS.md and node_modules/next/dist/docs/01-app/01-getting-started/11-css.md — installed Next.js CSS guidance and the project requirement to inspect local Next.js 16 documentation.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-09's locked no-content-push/full-screen-sheet requirement and deferred boundaries.
    - .planning/phases/24-search-bar-rework/24-11-SUMMARY.md, 24-13-SUMMARY.md, 24-14-SUMMARY.md, and 24-15-PLAN.md — the one-host decision, browser geometry proof, desktop direct-edit protection, and immediately preceding reducer scope to leave intact.
    - src/components/search/progressive-search-overlay.tsx and src/components/ui/dialog.tsx — the local mobile class list and the shared DialogContent centring/zoom classes it must override only for this consumer.
    - src/components/patterns/responsive-dialog.tsx — the repository precedent for a local responsive Dialog override that neutralizes inherited mobile scale motion without editing the primitive.
    - src/components/search/search-experience.tsx and tests/search/progressive-search.test.tsx — the existing one-tree, focus, status, Back, Cancel, and matchMedia test contracts to preserve.
  </read_first>
  <action>
    Start RED by extending the focused mobile-host test around the existing full-screen-sheet case. Assert the same mobile Dialog content node retains one reducer-owned activity heading and one named status owner while its local class composition declares all four dynamic-viewport edges, neutralizes the shared centring translate, and explicitly uses unit scale for both opening and closing motion. Keep the test's browser API/matchMedia harness; do not use layout stubs or claim that jsdom proves pixels.

    In `ProgressiveSearchOverlay`, correct only the below-`sm` `DialogContent` presentation per D-09. Keep it fixed to the dynamic viewport on explicit top/right/bottom/left edges, width/height and max-size unconstrained, square-edged, and internally scrollable. Retain the existing fade and reduced-motion behavior, but locally override the shared DialogContent's open and closed zoom states to scale one-to-one so the inherited centred Dialog animation cannot create a temporary inset or X offset. Preserve the current desktop Popover classes and virtual-anchor behavior exactly, keep one runtime-selected Radix host and one children slot, and leave `SearchExperience`, `src/components/ui/dialog.tsx`, `src/components/ui/popover.tsx`, global CSS, design registries, snapshots, configuration, dependencies, and migrations unchanged.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx tests/search/progressive-search.test.tsx</automated>
    <fails_when>The mobile test no longer has exactly one Dialog question tree and status owner, its local presentation omits the explicit unit-scale override, desktop selects a Dialog rather than the existing Popover, or either focused command exits non-zero.</fails_when>
  </verify>
  <done>The mobile sheet no longer inherits a centred-scale entry state, while the desktop anchored Popover and all reducer/focus/status/Back/Cancel owners remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove viewport geometry on consecutive mobile browser frames and re-run the suite</name>
  <precondition>Playwright's configured server can exclusively bind localhost port 3000 and the existing test database is reachable; if either fact is false, halt and report the verified process or database condition before changing the test or product code.</precondition>
  <reversibility rating="reversible">This strengthens a local Chromium regression around an existing test id and does not alter production routes, persisted data, browser configuration, or visual baselines.</reversibility>
  <files>e2e/progressive-search.spec.ts</files>
  <behavior>
    - The named 375 px geometry journey samples the visible mobile sheet immediately after opening and after one requestAnimationFrame boundary; each box meets the existing one-pixel X/Y and dynamic-viewport width/height limits.
    - The existing 1280 px assertion still proves the results-region Y coordinate does not move and the active Popover is adjacent to the initiating pill.
    - The same mobile browser journey can select activity, resolve an address, use Back, and Cancel back to the cold route; the complete progressive Chromium file also retains direct-edit, fixed-reach, accessibility, and stale-callback coverage.
  </behavior>
  <read_first>
    - e2e/progressive-search.spec.ts — existing viewport constants, seeded setup, Photon mock, strict geometry case, semantic journey helpers, and full-file coverage that must remain intact.
    - playwright.config.ts — port-3000 server ownership, no-adoption posture, and Chromium project contract; do not modify it.
    - src/components/search/progressive-search-overlay.tsx — Task 1's mobile-only scale and positioning correction.
    - .planning/phases/24-search-bar-rework/24-13-SUMMARY.md, 24-14-SUMMARY.md, and 24-15-PLAN.md — the protected geometry, direct-edit, fixed-reach, and stale-callback browser paths.
  </read_first>
  <action>
    Start RED by strengthening the existing named overlay-and-sheet geometry case, not by adding a second route, fixture, browser project, or screenshot baseline. After the mobile sheet is visible, retain its current dynamic `window.innerWidth`/`window.innerHeight` measurement and exact one-pixel edge/dimension bounds. Sample the sheet at the first rendered assertion point and again after one `requestAnimationFrame` boundary; require both samples to satisfy the same strict viewport geometry. Retain the desktop result-region and pill-adjacency measurements, the one-tree/focus assertions, and the real activity → address → Back → Cancel recovery in the case. Do not weaken any bound, add a fixed timeout, regenerate a snapshot, or use a source-class assertion as a substitute for the rendered browser check.

    Run the named geometry case before the complete Chromium progressive-search file. Then run the focused component suite, scoped ESLint, and the complete Vitest suite using the existing direct Node launchers. Preserve the uncommitted Plan 24-15 stale-callback test additions, the existing direct-edit Popover, mobile Dialog, fixed 25 km, axe, and test-database setup; make no change to seed helpers, Playwright configuration, routes, reducer, shared primitives, planning/state/roadmap files, dependencies, migrations, or snapshots.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx tests/search/progressive-search.test.tsx e2e/progressive-search.spec.ts</automated>
    <automated>node node_modules/vitest/vitest.mjs run</automated>
    <fails_when>Either measured mobile frame is offset or inset beyond the established one-pixel bound, the desktop route reflows or loses its anchored Popover, the named browser command reports zero tests, the complete Chromium run has any failed case, the full Vitest run has any failure, or any required command exits non-zero.</fails_when>
  </verify>
  <done>Chromium proves the mobile sheet fills the viewport without a transient offset, the full progressive browser suite remains green, and the focused plus full Vitest checks pass without widening scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mobile viewport and Dialog animation state → progressive question surface | Shared Dialog presentation state is composed with search-local responsive classes; an inherited transform can make controls visually unavailable during interaction. |
| Responsive presentation boundary → journey coordinator | The selected Dialog or Popover must present the one reducer-owned question tree without duplicating state, focus, status, or dismissal behavior. |
| Browser geometry regression → shipped visual behavior | jsdom can confirm local composition, but Chromium measurements are the authority for actual viewport coordinates and size. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-16-01 | Tampering | mobile DialogContent presentation | medium | mitigate | In `progressive-search-overlay.tsx`, explicitly neutralize the inherited centred scale state only below sm and retain fixed four-edge dynamic-viewport geometry; focused and Chromium tests protect it. |
| T-24-16-02 | Denial of Service | mobile progressive controls | medium | mitigate | Consecutive-frame Chromium bounding boxes enforce viewport reachability while retaining real Back, Cancel, focus, and address-recovery interactions. |
| T-24-16-03 | Tampering | responsive host selection | medium | mitigate | Preserve the existing single runtime-selected Dialog/Popover host and one reducer-owned children slot; component assertions retain one heading and one progress status owner. |
| T-24-16-04 | Information Disclosure | search navigation and selected answers | low | accept | The local styling and browser-geometry work neither adds data collection nor changes URL serialization, routing, geolocation, or server search authority. |
| T-24-16-SC | Tampering | package supply chain | low | accept | This plan installs no package and changes no dependency, lockfile, build configuration, or shared primitive. |
</threat_model>

<verification>
Run the named geometry case before the full progressive Chromium file, then run the focused component/lint checks and full Vitest suite. The existing strict geometry bounds are retained and extended to a second animation frame; no visual snapshot update substitutes for that proof.
</verification>

<success_criteria>
- At 375 px, the mobile sheet's X/Y and width/height meet the existing one-pixel viewport bounds on its first observed frame and the following animation frame.
- At 1280 px, the Popover remains pill-adjacent and route content does not move; direct-edit behavior remains protected by the full Chromium file.
- The source correction is local to the progressive overlay, and all focused, complete browser, scoped lint, and full Vitest commands pass without modifying configuration, data contracts, snapshots, or the preceding Plan 24-15 work.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-16-SUMMARY.md` when done.
</output>
