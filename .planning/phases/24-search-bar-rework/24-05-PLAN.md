---
phase: 24-search-bar-rework
plan: 05
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04]
files_modified:
  - e2e/progressive-search.spec.ts
  - e2e/helpers/booker-seed.ts
  - e2e/search-and-book.spec.ts
  - e2e/zero-result-relax.spec.ts
  - e2e/one-tree.spec.ts
  - e2e/price-parity.spec.ts
  - e2e/axe-sweep.spec.ts
  - src/components/search/search-bar.tsx
  - src/components/search/relax-band.tsx
  - src/lib/search/relaxation.ts
  - tests/search/relaxation-ladder.test.ts
  - src/app/dev/theme/page.tsx
  - src/lib/design/visual-baselines.ts
  - src/lib/design/live-regions.ts
  - src/lib/design/selector-contract.ts
  - src/lib/design/accent-uses.ts
  - tests/design/brand-recipe.test.ts
  - tests/design/elevation-z.test.ts
  - tests/design/live-regions.test.tsx
  - e2e/helpers/visual-drive.ts
  - e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-location-step-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-location-step-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-party-step-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-party-step-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-results-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-results-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-empty-375-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-empty-1280-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-results-320-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-results-768-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-relax-band-320-court-visual-linux.png
  - e2e/visual/surfaces.spec.ts-snapshots/search-relax-band-1280-court-visual-linux.png
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-01 through D-03, desktop and 375 px browsers each prove one idle pill, the fixed activity → location → party order, retained Back/Edit answers, Cancel-to-browse, immediate solo submission, and explicit valid-group submission."
    - "Per D-04, both viewport journeys use only the complete exact catalogue, keep unmatched typing local, and render the calm `No matching activity or type` state without navigating."
    - "Per D-05, a deterministic selected-address path and an explicitly requested Playwright geolocation path both reach party choice; denied or stale geolocation remains recoverable and submits no false coordinates."
    - "Per D-06 and D-07, solo and exact-group URLs survive reload and a fresh shared-page navigation, return only capacity-eligible rows, and reveal neither configured maximum nor date-scoped remaining places."
    - "Per D-08, populated and calm empty results retain the same three direct-edit chips at both widths, while no expanded date/time/price/radius control or automatic relaxation hatch remains reachable."
    - "Keyboard order, post-transition focus, one status owner, reduced-motion behavior, axe conformance, and one responsive DOM tree are exercised in the real browser rather than inferred from component tests alone."
    - "Pinned Linux visual inventory contains desktop and 375 px baselines for the idle pill, all three questions, populated results, and calm empty results; obsolete expanded-search and relaxation baselines are absent."
  prohibitions:
    - statement: "No browser or visual contract reintroduces a date, time, price, radius, fuzzy, inferred, or free-text public-search control."
      status: resolved
      verification: explicit
    - statement: "No result card, chip, status, empty state, or screenshot exposes configured maximum capacity or promises remaining places without a booking date."
      status: resolved
      verification: explicit
    - statement: "No test reaches Photon over the public network or treats a denied/stale geolocation callback as a selected place."
      status: resolved
      verification: explicit
    - statement: "No legacy search test is made green by weakening its booking, price-parity, reachability, one-tree, or axe assertion."
      status: resolved
      verification: explicit
    - statement: "No package, schema declaration, migration, endpoint, environment variable, or runtime search implementation owned by Plans 24-01 through 24-04 is added here."
      status: resolved
      verification: explicit
  artifacts:
    - path: e2e/progressive-search.spec.ts
      provides: "Deterministic desktop and 375 px full-journey, correction, URL persistence, result-state, keyboard, focus, reduced-motion, and axe proof."
    - path: e2e/helpers/booker-seed.ts
      provides: "Canonical Phase 24 search-to-listing helper for booking suites that are not themselves progressive-search tests."
    - path: src/lib/design/visual-baselines.ts
      provides: "Declared visual surfaces and exact 375/1280 rows for every progressive-search state."
    - path: e2e/helpers/visual-drive.ts
      provides: "Deterministic state drivers for activity, location, and party baseline captures."
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-empty-375-court-visual-linux.png
      provides: "Pinned narrow calm-empty reference; the sibling declared PNG rows cover the other five states and desktop width."
    - path: src/components/search/search-bar.tsx
      provides: "Removed legacy expanded public-search module after the Wave 2 census proves no supported runtime consumer remains."
    - path: src/lib/search/relaxation.ts
      provides: "Removed automatic relaxation ladder and its obsolete unit/browser/design contracts."
  key_links:
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/search-experience.tsx
      via: "Semantic pointer and keyboard actions exercise the shipped reducer, location paths, direct edits, focus, and canonical navigation at 1280 px and 375 px."
      pattern: "SearchExperience"
    - from: e2e/progressive-search.spec.ts
      to: src/app/(public)/page.tsx
      via: "Reload and a fresh page visit replay the canonical URL through the async server searchParams parser and the database-backed result composition."
      pattern: "partySize"
    - from: e2e/helpers/booker-seed.ts
      to: e2e/search-and-book.spec.ts
      via: "Booking-focused suites enter the seeded listing through one canonical category/coordinates/locationLabel/partySize URL instead of retired expanded-form selectors."
      pattern: "openSeededListing"
    - from: src/lib/design/visual-baselines.ts
      to: e2e/helpers/visual-drive.ts
      via: "Every interaction-state surface has a reachability hook and a deterministic drive; plain result URLs contain the complete canonical answer tuple."
      pattern: "search-party-step"
    - from: src/lib/design/visual-baselines.ts
      to: e2e/visual/surfaces.spec.ts
      via: "The generic visual runner reads the declared rows and compares the exact Linux PNG names regenerated only by the sanctioned baseline workflow."
      pattern: "VISUAL_BASELINES"
---

<objective>
Close the D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-08 search journey with deterministic desktop and 375 px browser proof, reload/share persistence, honest populated and calm empty results, accessibility and motion coverage, pinned visual references, and explicit migration or retirement of every legacy expanded-search and relaxation test contract.

Purpose: Make the progressive search releasable as one verified public contract, while preserving the independent booking, price, responsive-tree, and accessibility guarantees that previously entered through the old form.
Output: A full two-viewport Playwright journey, canonical search helpers and migrated suites, retired dead relaxation/search artifacts, updated design inventories, and stable Linux visual baselines for every Phase 24 state.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-PLAN-OUTLINE.md
@.planning/phases/24-search-bar-rework/24-02-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-03-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-04-SUMMARY.md

<interfaces>
- Plan 24-02 supplies the final SearchExperience semantic names, reducer screens, answer-chip Edit actions, explicit Cancel path, stale-geolocation token, focus targets, and one Search progress status owner; use its SUMMARY as executed truth rather than guessing selectors from the plan.
- Plan 24-03 supplies the final scalar partySize parser, shared MAX_OPEN_CAPACITY bound, engaged-search normalization, and pre-pagination SQL behavior; browser assertions must treat cards/results as the observable result, never read configured capacity into the UI.
- Plan 24-04 supplies the final SearchResults populated/empty/error contract, `No spaces match those answers` copy, canonical queryString behavior, `search-results-region`, and inert `search-idle-pill-shell`; the loading shell is not an alternate interaction tree.
- `seedBookableListing(options?)` in e2e/helpers/booker-seed.ts owns verified-host, published-listing, capacity, coordinates, operating-hours, teardown, and unique-title fixtures. Preserve its ordered teardown and the explicit rule that e2e/price-parity.spec.ts keeps its byte-independent venue-time math.
- `expectAxeClean(page, title)` in e2e/helpers/axe.ts owns the repository WCAG tag set and vacuity guards; Phase 24 state scans call it rather than constructing another axe configuration.
- playwright.config.ts collects root `e2e/*.spec.ts` only in project `chromium`, collects `e2e/visual/**/*.spec.ts` only in project `visual` on Linux, fixes updateSnapshots to none, and delegates baseline writing exclusively to .github/workflows/baselines.yml.
- The installed Next.js 16.2.7 page guide defines searchParams as an awaited plain object, loading.tsx as a parameterless Server Component swapped out after streaming, and useRouter.push as a history-adding Client Component API whose target must not be an untrusted URL. The browser suite must observe those shipped boundaries rather than add alternate routing logic.
</interfaces>
</context>

## Artifacts this phase produces

- Expanded `e2e/progressive-search.spec.ts` with a serial, per-run seeded matrix at 1280 × 800 and 375 × 812 covering Back, direct Edit, Cancel, exact/no-match catalogue behavior, selected address, current location, solo and group submission, reload/share, populated/empty outcomes, keyboard/focus, reduced motion, axe, and absence of retired refinements.
- Updated `openSeededListing` in `e2e/helpers/booker-seed.ts` that enters a seeded result through the complete canonical Phase 24 URL, leaving progressive interaction coverage to the dedicated spec and booking assertions to their owning suites.
- Migrated `e2e/search-and-book.spec.ts`, `e2e/one-tree.spec.ts`, `e2e/price-parity.spec.ts`, and `e2e/axe-sweep.spec.ts`; retired `e2e/zero-result-relax.spec.ts` and `tests/search/relaxation-ladder.test.ts`.
- Removed runtime-disconnected `src/components/search/search-bar.tsx`, `src/components/search/relax-band.tsx`, and `src/lib/search/relaxation.ts`, with all fixed design/selector/live-region inventories reconciled rather than weakened.
- Updated `src/lib/design/visual-baselines.ts` and `e2e/helpers/visual-drive.ts` declaring six Phase 24 states at 375 px and 1280 px, plus twelve exact `*-court-visual-linux.png` references generated by the repository's sanctioned workflow.
- Removed obsolete `search-results-320`, `search-results-768`, and both `search-relax-band` Linux PNGs after their inventory rows cease to exist.
- No new public search field, search predicate, runtime component behavior, endpoint, package, schema declaration, migration, external service, or date-specific availability surface is created by this plan.

## Spec-less probe fallback

Skipped visibly because ROADMAP.md still records Phase 24 `Requirements: TBD` and the phase has no mapped REQUIREMENTS.md IDs. The locked D-01 through D-08 decisions, their explicit exclusions, and the browser/design evidence in this plan are the acceptance contract; no requirement-derived predicate is invented.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Engaged activity → location → party journey with smooth transitions | 24-05 Tasks 1 and 3 | COVERED | Real browsers prove behavior and fixed visual references prove the six settled states at both required widths. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD and REQUIREMENTS.md has no Phase 24 mapping; no ID is invented. |
| RESEARCH | Deterministic address and browser-geolocation paths without a live third party | 24-05 Task 1 | COVERED | Address requests are intercepted; geolocation uses Playwright permissions/coordinates and explicit failure/stale cases. |
| RESEARCH | Reload/share, populated/empty correction parity, keyboard/focus/reduced-motion/axe coverage | 24-05 Task 1 | COVERED | One seeded matrix exercises the final Wave 2 contracts at desktop and 375 px. |
| RESEARCH | Migrate search-and-book, zero-result-relax, one-tree, price-parity, axe, helper, and relevant server-test contracts | 24-05 Task 2 | COVERED | Contradictory suites are retired; independent assertions are preserved and pointed through the canonical helper. |
| RESEARCH | Replace obsolete search/relaxation visual rows and reconcile design inventories | 24-05 Task 3 | COVERED | Six progressive states replace the expanded form/band and dead-file registries are removed with positive inventory checks intact. |
| CONTEXT | D-01 compact engaged-only pill | 24-05 Tasks 1 and 3 | COVERED | Both widths start from one pill; idle baselines pin its final geometry. |
| CONTEXT | D-02 retained Back/Edit and clearing Cancel | 24-05 Task 1 | COVERED | Each path is exercised at both widths against retained answers and literal browse URL restoration. |
| CONTEXT | D-03 auto-advance, immediate For me, valid group See spaces | 24-05 Task 1 | COVERED | Browser events and URL/result assertions distinguish the two submission paths. |
| CONTEXT | D-04 full exact catalogue and calm no-match | 24-05 Task 1 | COVERED | Keyboard filtering/selection and unmatched text are tested without fuzzy/free-text fallthrough. |
| CONTEXT | D-05 address and explicit current-location paths | 24-05 Task 1 | COVERED | Both successes converge on party; failure/stale callbacks remain recoverable. |
| CONTEXT | D-06 exact capacity-backed party count | 24-05 Task 1 | COVERED | Eligible/ineligible rows plus reload/share prove the client/server/query contract from the user's view. |
| CONTEXT | D-07 capacity-only honesty | 24-05 Tasks 1-3 | COVERED | Browser text, axe scans, helper URLs, dev/design examples, and screenshots expose no maximum or remaining-place claim. |
| CONTEXT | D-08 identical editable chips; no legacy refinement/relaxation surface | 24-05 Tasks 1-3 | COVERED | Populated/empty parity is browser-proven and every contradictory code/test/design/baseline contract is retired. |
| EXCLUSION | Date-specific availability, fuzzy/free text, NLP price, bands, radius controls | — | EXCLUDED | Explicitly outside CONTEXT and never introduced by this plan. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the complete progressive journey at desktop and 375 px</name>
  <precondition>The Docker daemon is reachable and the repository Playwright web server can connect to its configured isolated test database.</precondition>
  <reversibility rating="reversible">This task adds browser evidence and one test helper around the locked contract; undoing it changes no public URL, schema, or persisted data.</reversibility>
  <files>e2e/progressive-search.spec.ts, e2e/helpers/booker-seed.ts</files>
  <behavior>
    - At 1280 × 800 and 375 × 812, one idle pill opens activity, exact selection opens location, either location success opens party, and no second progressive or streaming interaction tree exists.
    - Back and each result-chip Edit retain every confirmed answer; Cancel from a draft or result edit clears state and returns to `/` browse.
    - Unmatched catalogue typing renders `No matching activity or type`, leaves focus in the filter, and creates neither navigation nor stored category.
    - For me immediately submits partySize=1; a valid exact group submits only from See spaces, while invalid group input cannot navigate.
    - A completed URL contains the canonical answer tuple, survives page.reload(), and produces the same chips/cards when opened in a fresh page as a shared URL.
    - Populated and capacity-empty outcomes show the same three Edit chips; neither outcome exposes configured maximum/remaining inventory or any retired refinement/relaxation action.
    - Pointer and keyboard flows land focus on the newly mounted question, one status owner announces state, reduced motion remains operable, and axe reports no violation with its vacuity guards satisfied.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-02-SUMMARY.md — executed reducer events, semantic names, group parser, focus/status ownership, responsive classes, and geolocation cancellation token.
    - .planning/phases/24-search-bar-rework/24-03-SUMMARY.md — executed partySize bound, canonical normalization, and capacity predicate behavior to observe through results.
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — executed populated/empty/error/loading composition, chip labels, inert shell, and the runtime-versus-retirement census.
    - e2e/progressive-search.spec.ts — 24-01 tracer fixture, route interception, teardown, and named happy path to expand rather than replace.
    - e2e/helpers/booker-seed.ts — seedBookableListing, explicit maximum capacity, coordinates, SQL handle, ordered teardown, and openSeededListing callers.
    - e2e/helpers/axe.ts — repository axe tags, exclusions, node-floor, and document reachability guards.
    - e2e/search-and-book.spec.ts — legacy search-to-book interaction whose independent booking assertions Task 2 preserves.
    - playwright.config.ts — chromium collection, baseURL, server/database environment, retry, and worker behavior.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — installed async plain-object searchParams and repeated-value contract.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md — installed streamed-fallback replacement contract used by the one-tree assertion.
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md — installed history and same-origin navigation safety contract.
  </read_first>
  <action>
    Start RED by expanding `e2e/progressive-search.spec.ts` into a serial table for `desktop` at 1280 × 800 and `mobile-375` at 375 × 812, retaining 24-01's real database/Server Component tracer rather than replacing it with route HTML. Seed uniquely titled published listings with explicit equal/above and below/NULL maximum capacities under the same known catalogue value, and tear them down in dependency order even after failure. Intercept the existing address resolver request with one deterministic valid response near the seeded coordinates; do not call the live geocoder. For current location, grant permission only for the success case, set known valid coordinates on the Playwright context, and add denied plus late-callback coverage that proves the address control remains usable and the URL is untouched.

    At each width drive the fixed D-01/D-02/D-03 progression with semantic roles and the shipped Plan 24-02 names: engage, filter the complete D-04 catalogue, prove unmatched text cannot advance, keyboard-select an exact option, resolve the D-05 address or explicit geolocation branch, move Back through visited questions with values intact, reopen each D-08 result chip directly, and Cancel from an edit to the literal browse URL. Cover both party branches: For me navigates immediately with one; group entry keeps See spaces disabled for invalid strings and submits one exact in-range count only after the explicit action. Assert the URL's exact canonical key/value set after each submission and the absence of retired public-search keys.

    Prove D-06/D-07 from rendered results: the eligible listing appears, undersized/NULL listings do not, and no visible/accessibility text or attribute reveals a maximum, computes a remainder, or implies a date-specific place count. Capture a populated URL, reload it, then open its string in a fresh page and assert the same activity/location/party chips plus result titles. Edit the group answer to an exact count above the seeds to produce the calm empty state; prove the identical three Edit chips remain and no automatic widening, fallback grid, or removed control appears.

    Run a pointer path and a keyboard-only path at both widths. After each auto-advance assert `document.activeElement` is the shipped question heading or first control, never body; assert the single Search progress region and AddressAutocomplete lookup region do not duplicate one outcome. Re-run the state path with Playwright reduced motion emulation and call the existing `expectAxeClean` helper on idle, each question, populated results, and calm empty results with a positive reachability assertion before every scan. Update `openSeededListing` so booking-focused callers navigate through one canonical Phase 24 category/coordinates/locationLabel/partySize URL and click the unique title; it must not re-drive the full wizard, restore the removed selectors, or migrate price-parity's protected venue-time arithmetic.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, either viewport is absent, a correction/location/party branch is skipped, reload/share changes answers or results, an ineligible listing renders, a chip disappears from the empty state, focus/status/reduced-motion/axe checks fail, or any retired control or misleading capacity copy is observable.</fails_when>
  </verify>
  <acceptance_criteria>The focused Chromium file exits 0 with named desktop and 375 px cases proving D-01 through D-08 across the real client, Next.js server, validation, SQL, result, accessibility, history, and streaming boundaries.</acceptance_criteria>
  <done>The complete progressive journey is deterministic, correctable, shareable, capacity-honest, accessible, and responsive in both required browser sizes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate independent browser contracts and retire the expanded-search relaxation stack</name>
  <precondition>Plans 24-02 through 24-04 are complete, and 24-04-SUMMARY.md records no supported runtime consumer for SearchBar, RelaxBand, or runRelaxationLadder.</precondition>
  <reversibility rating="reversible">The removed modules and contradictory tests are recoverable from git; no persisted row, public schema, or external contract is deleted.</reversibility>
  <files>e2e/search-and-book.spec.ts, e2e/zero-result-relax.spec.ts, e2e/one-tree.spec.ts, e2e/price-parity.spec.ts, e2e/axe-sweep.spec.ts, src/components/search/search-bar.tsx, src/components/search/relax-band.tsx, src/lib/search/relaxation.ts, tests/search/relaxation-ladder.test.ts</files>
  <behavior>
    - Search-and-book and price-parity still reach their uniquely seeded listing and retain every booking/price assertion, but enter through the canonical helper rather than old form controls.
    - The one-tree search row settles on the resolved result surface and proves one interaction/result tree at its declared widths without waiting for a duplicate SearchBar to disappear.
    - The axe inventory still covers the public home document, while progressive-search.spec owns axe scans of interactive question/result states.
    - The old zero-result relaxation browser suite and ladder unit suite are deleted because their promised surface contradicts D-08; their cold/empty honesty and surviving-category concerns are covered by the new result/browser suites.
    - Runtime-disconnected SearchBar, RelaxBand, and relaxation ladder modules are deleted after a fresh symbol census; no supported import or test references them afterward.
    - Legacy no-partySize server filter tests remain intentionally valid compatibility checks and are not rewritten to imply that the Phase 24 journey exposes their retired controls.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — authoritative runtime/dead-module census and any deviations from the planned SearchResults prop cleanup.
    - e2e/helpers/booker-seed.ts — Task 1 canonical openSeededListing contract.
    - e2e/search-and-book.spec.ts — old search selector section and the booking/hold/confirmation assertions that must remain equally strong.
    - e2e/price-parity.spec.ts — protected independent fixture/time math and only the old search-entry section that may change.
    - e2e/zero-result-relax.spec.ts — contradictory band/Undo/radius/date contract to delete, with its cold-state and category-survival concerns mapped before removal.
    - e2e/one-tree.spec.ts — settleSearchHome and the search surface row; preserve the fresh-navigation/per-width cardinality mechanism.
    - e2e/axe-sweep.spec.ts — public home row, footer reachability tell, inventory equality, and axe vacuity guards.
    - tests/search/search-results-states.test.tsx and tests/search/progressive-search.test.tsx — replacement browser-suite concerns already pinned at component level.
    - tests/search/filters.test.ts and tests/search/open-capacity-search.test.ts — server-only category/legacy price and no-date spots contracts to classify deliberately, not confuse with public controls.
    - src/components/search/search-bar.tsx, src/components/search/relax-band.tsx, src/lib/search/relaxation.ts, and tests/search/relaxation-ladder.test.ts — exact dead runtime and contradictory-test units to retire only after the census.
  </read_first>
  <action>
    Begin with the exact 24-04 census symbols and legacy selectors across tracked `src`, `tests`, and `e2e`. Classify every hit in the execution summary as `migrated`, `retired`, or `intentionally retained server compatibility`, naming its path and the surviving owner. Stop if SearchBar, RelaxBand, or runRelaxationLadder still has a supported runtime caller, because deleting a live consumer would violate the Wave 2 dependency rather than close it. Do not count comments as runtime callers, but update stale comments so future censuses are not self-invalidating.

    In `search-and-book.spec.ts`, replace only the expanded-form search and radius-origin setup with Task 1's canonical helper/URL, including partySize=1 and the display-only location label where distance is asserted. Preserve the exact unique-card cardinality, displayed rate, distance, listing, venue-time, hold, countdown, confirmation, and expiry checks. In `price-parity.spec.ts`, replace only its local old-form interaction with an independent canonical URL/navigation that keeps the spec deliberately off `booker-seed.ts`'s venue-time calculations; do not move or rederive its price/time fixture. Neither migration may add a date/time/price/radius control to the Phase 24 journey.

    In `one-tree.spec.ts`, replace the obsolete duplicate-SearchBar settle with a positive resolved-page tell and keep its fresh navigation per width, exact `search-results-region` count, result-card reachability guard, and one-tree failure meaning. In `axe-sweep.spec.ts`, remove stale two-SearchBar reasoning while preserving the public page row, exact surface inventory, footer/resolved reachability, tag set, node floor, and both declared viewport scans; reference `progressive-search.spec.ts` as the owner for transient question states rather than silently claiming the cold page scans them.

    Delete `zero-result-relax.spec.ts` and `relaxation-ladder.test.ts` after mapping their still-valid concerns to the Phase 24 progressive/result-state tests: cold browse renders no hatch, category remains exact, populated/empty chips match, and retired URL keys cannot influence an engaged request. Delete runtime-disconnected `search-bar.tsx`, `relax-band.tsx`, and `relaxation.ts`. Do not delete `tests/search/filters.test.ts` or `tests/search/open-capacity-search.test.ts`: their server-only no-partySize price/category and no-date spots coverage remains a deliberate compatibility boundary, and the Phase 24 browser never exposes those retired inputs.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/search-and-book.spec.ts e2e/one-tree.spec.ts e2e/price-parity.spec.ts e2e/axe-sweep.spec.ts --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, a migrated suite still waits for an old selector, search-and-book loses a booking/result assertion, price-parity changes its independent money/time proof, one-tree loses exact cardinality, or the public axe row becomes absent/vacuous.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx tests/search/search-results-states.test.tsx tests/search/party-size-filter.test.ts tests/search/filters.test.ts tests/search/open-capacity-search.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, replacement empty/correction/capacity coverage is missing, a retained legacy server contract breaks, or removal of the ladder leaves a dangling imported suite.</fails_when>
  </verify>
  <acceptance_criteria>Both focused commands exit 0, every research-census path has a named migration/retirement/retention disposition, all independent booking/price/one-tree/axe guarantees remain live, and no tracked runtime or test import references the removed search/relaxation modules.</acceptance_criteria>
  <done>The repository has one public search interaction contract, while every independent downstream guarantee still reaches the same product behavior without obsolete selectors or relaxation promises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile design inventories and mint stable progressive-search baselines</name>
  <precondition>The execution checkout is the tracked `dev` branch, GitHub CLI authentication can dispatch workflows, and the sanctioned baselines workflow can run its pinned Linux container and database fixture.</precondition>
  <reversibility rating="costly">Changing a committed visual reference affects every later comparison and requires another sanctioned regeneration, but the PNG and inventory changes remain recoverable through git.</reversibility>
  <files>src/app/dev/theme/page.tsx, src/lib/design/visual-baselines.ts, src/lib/design/live-regions.ts, src/lib/design/selector-contract.ts, src/lib/design/accent-uses.ts, tests/design/brand-recipe.test.ts, tests/design/elevation-z.test.ts, tests/design/live-regions.test.tsx, e2e/helpers/visual-drive.ts, e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-location-step-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-location-step-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-party-step-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-party-step-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-results-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-results-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-empty-375-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-empty-1280-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-results-320-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-results-768-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-relax-band-320-court-visual-linux.png, e2e/visual/surfaces.spec.ts-snapshots/search-relax-band-1280-court-visual-linux.png</files>
  <behavior>
    - Brand, elevation, live-region, selector, and accent inventories remove only the deleted SearchBar/RelaxBand contracts and retain positive guards for SearchExperience, Search progress, result errors, and all unrelated surfaces.
    - The dev theme gallery demonstrates the progressive search's calm empty/result language and no longer presents the public retired refinement or a searched-window availability claim.
    - Visual inventory declares idle, activity, location, party, populated-results, and calm-empty surfaces at exactly 375 and 1280 widths, each with a reachability hook that fails on the prior state or loading shell.
    - Activity/location/party visual drives use deterministic local catalogue and intercepted address data, and every result/empty URL contains the canonical completed answer tuple without a retired key.
    - The pinned Linux workflow produces exactly the declared PNG set, removes the obsolete 320/768 expanded-results and relaxation-band references, and a repeat dispatch makes no baseline change.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-02-SUMMARY.md and .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — final geometry, hooks, status owners, calm copy, and old-design census.
    - src/lib/design/visual-baselines.ts — SurfaceId, DocumentSurface, BaselineRow, current search-results/search-relax-band definitions, fixed membership counts, and baseline filename contract.
    - e2e/helpers/visual-drive.ts — SurfaceDrive/newDrive registry, fixture URL contract, deterministic media/clock behavior, and cleanup requirements.
    - e2e/visual/surfaces.spec.ts — generic reachability-first capture loop and baselineArg naming; do not duplicate its runner in another spec.
    - .github/workflows/baselines.yml — sole authorized baseline writer, workflow_dispatch trigger, pinned Linux image, fixture seed, update-snapshots invocation, commit/push behavior, and second-dispatch no-change contract.
    - scripts/seed-baseline-fixtures.ts — fixed near/far listing categories, coordinates, explicit maximum capacities, and teardown-safe fixture IDs used by result/empty URLs.
    - src/lib/design/live-regions.ts and tests/design/live-regions.test.tsx — two-sided file/name/count inventory to update when RelaxBand is removed while Search progress remains.
    - src/lib/design/selector-contract.ts and tests/design/selector-contract.test.ts — exact selector registry and positive duplicate/reachability guards.
    - tests/design/brand-recipe.test.ts, tests/design/elevation-z.test.ts, and src/lib/design/accent-uses.ts — fixed legacy SearchBar membership/comments that must be reconciled without lowering unrelated counts.
    - src/app/dev/theme/page.tsx — current searched-window card and old filter-empty example to align with D-07/D-08 design truth.
    - e2e/visual/theme-swap.spec.ts — fixed D-138 four-surface allow-list; `search-results` stays a member and must retain identical-content reachability after its canonical URL changes.
  </read_first>
  <action>
    Reconcile every design registry affected by Task 2's deletions. Remove the old SearchBar and RelaxBand file/name/selector/accent/elevation entries and their fixed counts, while retaining and positively asserting the SearchExperience/Search progress/result-error contracts created by Plans 24-01/24-02. Update explanatory comments and the dev theme gallery so they describe the progressive pill, calm answer-editing empty state, and non-date result card. Do not lower a global design threshold, loosen selector uniqueness, remove an unrelated live region, or preserve a nonexistent file merely to keep a count unchanged.

    Replace `search-relax-band` in `visual-baselines.ts` with six Phase 24 document surfaces: `search-idle-pill`, `search-activity-step`, `search-location-step`, `search-party-step`, `search-results`, and `search-empty`. Give each a reachability hook that is absent in the immediately preceding state and cannot be satisfied by `PublicLoading`; completed result URLs must include a fixture-backed exact category, paired coordinates, bounded location label, and partySize, with no date/time/price/radius/relaxation key. Declare exactly two court rows per surface at 375 × 812 and 1280 × 800. Keep `search-results` in the fixed D-138 theme-swap allow-list, updating only its canonical URL/hook rationale.

    Add deterministic `visual-drive.ts` factories for the three interactive question states. Idle is a plain settled navigation; activity engages the pill; location engages and selects the exact fixture catalogue option; party additionally resolves the repository address request from an intercepted fixed response. Each drive waits on its state-specific hook and performs no live network/geolocation request, random text, clock-derived label, or database mutation. Result and calm-empty surfaces remain plain canonical navigations against the fixed baseline fixture, using a known near category for results and a known no-match category/location tuple for empty. Preserve the generic `surfaces.spec.ts` reachability-first runner and baselineArg convention.

    Remove the obsolete search-results 320/768 and relaxation-band PNGs from the declared set. After the inventory/drive/design changes are green, create their atomic task commit and push it to the tracked `dev` branch so the remote runner sees the exact code under capture. Dispatch `.github/workflows/baselines.yml` through its sole workflow_dispatch trigger, wait for the `generate-baselines` job to succeed and push its Linux PNG commit, then fast-forward the local branch. Dispatch the same workflow a second time at that resulting ref and require its log to report no baseline changes; if it creates another PNG diff, treat the baseline as nondeterministic and fix the drive rather than accepting the second image. Record both run URLs and the final twelve tracked filenames in 24-05-SUMMARY.md.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/brand-recipe.test.ts tests/design/elevation-z.test.ts tests/design/live-regions.test.tsx tests/design/selector-contract.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, a deleted legacy file/name/selector remains declared, SearchExperience or Search progress loses its positive inventory guard, or an unrelated design threshold/count was weakened.</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=baselines</automated>
    <fails_when>The command exits non-zero, the workflow is no longer dispatch-only, its pinned image/permission/snapshot-write invariant changes, or a second sanctioned run cannot establish a no-change baseline set.</fails_when>
  </verify>
  <acceptance_criteria>The design and workflow checks exit 0, the successful pinned-Linux workflow commit leaves exactly the twelve declared progressive-search PNGs at 375/1280, the second run produces no diff, and every obsolete search/relaxation design or snapshot contract is absent without weakening unrelated gates.</acceptance_criteria>
  <done>The design system and visual evidence now describe only the progressive search, with stable references for all six states at both required widths and no retired form or relaxation residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser events and URL to public Client/Server Components | Catalogue text, group size, location labels, coordinates, repeated keys, and history state are untrusted until the shipped client and server contracts validate them. |
| Browser geolocation/geocoder to retained location answer | Permission, coordinates, callback order, and address responses can fail, race, or be spoofed; the journey must advance only on one valid current result. |
| Database result model to public cards/chips/empty state | Capacity is an inclusion authority but configured maximum and date-scoped remaining inventory must not cross into public copy without a booking date. |
| Legacy tests/design inventories to release confidence | A green suite can be deceptive if old selectors are deleted without preserving the independent booking, responsive, pricing, reachability, and accessibility assertions they carried. |
| Baseline workflow to committed PNG references | A capture can permanently bless a loading shell, wrong state, stale CSS, live-network result, or nondeterministic layout unless hooks, fixed fixtures, and repeat-run stability fail closed. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-23 | Tampering | progressive-search URL/reload/share browser path | high | mitigate | Assert the exact canonical answer tuple at both widths, replay it through reload and a fresh page, and fail on retired/repeated/unvalidated authority reaching results. |
| T-24-24 | Spoofing / Information Disclosure | address and navigator.geolocation paths | high | mitigate | Intercept address data, grant geolocation only after the explicit action, use fixed bounded coordinates, invalidate stale callbacks, and prove denied/error paths submit nothing. |
| T-24-25 | Tampering / Information Disclosure | capacity-filtered public results | high | mitigate | Seed eligible plus undersized/NULL listings, assert rendered inclusion/exclusion, and scan visible/accessibility output for no configured maximum, remainder, or date-specific claim. |
| T-24-26 | Repudiation | legacy suite migration | high | mitigate | Preserve positive booking, price, one-tree, reachability, and axe assertions; produce a per-path migration ledger and keep server-only legacy compatibility suites explicitly classified. |
| T-24-27 | Tampering | retired search/relaxation modules and selectors | high | mitigate | Delete disconnected modules and contradictory tests only after the dependency SUMMARY census, then run functional and design suites so no dangling import or silent substitute survives. |
| T-24-28 | Tampering / Spoofing | visual baseline capture | high | mitigate | Use state-specific non-loading hooks, fixed local data, the sole pinned-Linux writer, exact filename inventory, and a second no-change dispatch before accepting references. |
| T-24-29 | Denial of Service | full browser matrix and external services | medium | mitigate | Serialize shared DB fixtures, use unique titles and ordered teardown, intercept geocoding, use fixed geolocation, and avoid live third-party waits. |
| T-24-30 | Elevation of Privilege | public search accessibility state | medium | mitigate | Exercise keyboard-only focus order and the repository axe helper at every state; add no auth/session behavior or privileged endpoint. |
| T-24-SC | Tampering | package supply chain | low | accept | This plan runs no package-manager installation and changes no dependency declaration. |
</threat_model>

<verification>
  <automated>node node_modules/vitest/vitest.mjs run</automated>
  <fails_when>The command exits non-zero, reports zero tests, or any unit/component/database suite regresses after the legacy modules and contracts are removed.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts</automated>
  <fails_when>The command exits non-zero, reports zero tests, or the final source/design/snapshot inventories are inconsistent, weakened, or still name retired artifacts.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or emits a diagnostic for the final SearchExperience/SearchResults interfaces, migrated browser fixtures, removed modules, or design registries.</fails_when>
  <automated>node node_modules/@playwright/test/cli.js test --project=chromium --workers=1</automated>
  <fails_when>The command exits non-zero, reports zero tests, either required viewport/path is absent, a migrated dependent flow regresses, an axe/one-tree guard becomes vacuous, or an obsolete public-search contract remains reachable.</fails_when>
</verification>

<success_criteria>
- D-01 through D-08 are exercised through real Chromium at desktop and 375 px, including Back/Edit/Cancel, exact/no-match activity, address/geolocation, solo/group, reload/share, populated/empty, keyboard/focus, reduced motion, and axe.
- Every completed Phase 24 browser URL carries only the canonical answer tuple plus supported sort/page when relevant; appending retired keys cannot change an engaged search.
- Populated and calm empty results show one identical activity/location/party Edit set and no automatic relaxation, date/time/price/radius control, capacity maximum, or remaining-place promise.
- Search-and-book, price-parity, one-tree, and axe suites retain their original independent assertions after their entry/wait logic moves off the expanded form.
- The zero-result relaxation browser suite, ladder unit suite, disconnected SearchBar/RelaxBand/relaxation modules, and every matching design/selector/live-region/baseline entry are retired with no dangling tracked reference.
- The visual registry and pinned Linux PNG directory agree on six progressive states × two widths; every capture has a non-vacuous state hook and a repeat sanctioned dispatch produces no changes.
- Repository Vitest, design Vitest, TypeScript, and Chromium Playwright gates are green with no new dependency, schema file, migration, endpoint, external runtime integration, or date-specific search capability.
</success_criteria>

<output>
Create .planning/phases/24-search-bar-rework/24-05-SUMMARY.md when done.
</output>
