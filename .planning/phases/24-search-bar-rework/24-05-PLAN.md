---
phase: 24-search-bar-rework
plan: 05
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04]
files_modified:
  - e2e/progressive-search.spec.ts
  - e2e/helpers/booker-seed.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 through D-03, desktop and 375 px browsers each prove one idle pill, the fixed activity → location → party order, retained Back/Edit answers, Cancel-to-browse, immediate solo submission, and explicit valid-group submission."
    - "Per D-04, both viewport journeys use only the complete exact catalogue, keep unmatched typing local, and render `No matching activity or type` without navigating."
    - "Per D-05, a deterministic selected-address path and an explicitly requested browser-geolocation path both reach party choice; denied or stale geolocation remains recoverable and submits no false coordinates."
    - "Per D-06 and D-07, solo and exact-group URLs survive reload and a fresh shared-page navigation, return only capacity-eligible rows, and reveal neither configured maximum nor date-scoped remaining places."
    - "Per D-02 and D-08, populated and calm empty results retain the same three direct-edit chips at both widths, and Cancel from a submitted-result edit clears Phase 24 search state and returns to browse `/`."
    - "Keyboard order, post-transition focus, one status owner, reduced-motion behavior, axe conformance, and one responsive DOM tree are exercised in the real browser."
  prohibitions:
    - statement: "No journey path creates a date, time, price, radius, fuzzy, inferred, or free-text public-search control."
      status: resolved
      verification: explicit
    - statement: "No result card, chip, status, or empty state exposes configured maximum capacity or promises remaining places without a booking date."
      status: resolved
      verification: explicit
    - statement: "No test reaches Photon over the public network or treats a denied or stale geolocation callback as a selected place."
      status: resolved
      verification: explicit
  artifacts:
    - path: e2e/progressive-search.spec.ts
      provides: "Deterministic desktop and 375 px full-journey, correction, URL persistence, result-state, keyboard, focus, reduced-motion, and axe proof."
    - path: e2e/helpers/booker-seed.ts
      provides: "Canonical Phase 24 search-to-listing helper for dependent booking suites without duplicating the progressive journey."
  key_links:
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/search-experience.tsx
      via: "Semantic pointer and keyboard actions exercise the shipped reducer, location paths, direct edits, focus, and canonical navigation at 1280 px and 375 px."
      pattern: "SearchExperience"
    - from: e2e/progressive-search.spec.ts
      to: src/app/(public)/page.tsx
      via: "Reload and a fresh page visit replay the canonical URL through server validation and the database-backed result composition."
      pattern: "partySize"
    - from: e2e/helpers/booker-seed.ts
      to: e2e/search-and-book.spec.ts
      via: "Plan 24-06 can migrate booking-focused entry through one category/coordinates/locationLabel/partySize helper while preserving its independent assertions."
      pattern: "openSeededListing"
---

<objective>
Close D-01 through D-08 on the shipped progressive journey itself with deterministic desktop and 375 px browser proof, reload/share persistence, capacity-honest populated and calm empty outcomes, correction/cancellation, and real accessibility behavior.

Purpose: Prove the complete user-facing interaction before dependent legacy suites and design evidence are migrated in ordered follow-on plans.
Output: A focused progressive-search Playwright matrix and a canonical seeded-listing entry helper.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-02-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-03-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-04-SUMMARY.md

<interfaces>
- Plan 24-02 supplies the final SearchExperience semantic names, reducer screens, answer-chip Edit actions, explicit Cancel path, stale-geolocation token, focus targets, and one Search progress status owner; its SUMMARY is execution truth.
- Plan 24-03 supplies the final scalar partySize parser, shared MAX_OPEN_CAPACITY bound, engaged-search normalization, and pre-pagination SQL behavior; browser assertions observe cards/results and never expose configured capacity.
- Plan 24-04 supplies the final SearchResults populated/empty/error contract, `No spaces match those answers` copy, canonical queryString behavior, `search-results-region`, and inert `search-idle-pill-shell`.
- `seedBookableListing(options?)` owns verified-host, published-listing, capacity, coordinates, operating-hours, teardown, and unique-title fixtures; preserve ordered teardown.
- `expectAxeClean(page, title)` owns the repository WCAG tag set and vacuity guards; use it instead of constructing another axe configuration.
- The installed Next.js 16.2.7 page, loading, and router guides define the async searchParams, streaming replacement, history, and same-origin navigation boundaries these browser tests observe.
</interfaces>
</context>

## Artifacts this phase produces

- Expanded `e2e/progressive-search.spec.ts` with a serial seeded matrix at 1280 × 800 and 375 × 812 covering Back, direct Edit, Cancel-to-`/`, exact/no-match catalogue behavior, both location paths, solo/group submission, reload/share, populated/empty outcomes, keyboard/focus, reduced motion, axe, and retired-control absence.
- Updated `openSeededListing` in `e2e/helpers/booker-seed.ts` that enters a seeded result through the complete canonical Phase 24 URL while leaving progressive interaction coverage to the dedicated spec.
- No runtime component, endpoint, package, schema declaration, migration, environment variable, external-service integration, or date-specific search capability is created.

## Spec-less probe fallback

Skipped visibly because ROADMAP.md records Phase 24 `Requirements: TBD` and REQUIREMENTS.md maps no Phase 24 requirement IDs. Locked D-01 through D-08 and the explicit browser outcomes are the acceptance contract.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Engaged activity → location → party journey with smooth transitions | 24-05 Tasks 1-2 | COVERED | Both required browser widths exercise the shipped progressive flow. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD; no ID is invented. |
| RESEARCH | Deterministic address/geolocation, reload/share, correction, capacity and result parity | 24-05 Task 1 | COVERED | Fixed browser/provider data drives the real server-backed route. |
| RESEARCH | Keyboard/focus/status/reduced-motion/axe/one-tree behavior | 24-05 Task 2 | COVERED | Real-browser checks complement component/design gates. |
| CONTEXT | D-01, D-02, D-03, D-04 | 24-05 Tasks 1-2 | COVERED | Pill, order, retention, clear-to-browse Cancel, auto-advance, exact catalogue, solo/group. |
| CONTEXT | D-05 | 24-05 Task 1 | COVERED | Address and explicit current-location successes plus failure/stale recovery. |
| CONTEXT | D-06, D-07 | 24-05 Task 1 | COVERED | Exact party values reach pre-pagination configured-capacity filtering without availability claims. |
| CONTEXT | D-08 | 24-05 Task 1 | COVERED | Populated/empty chips match and retired controls remain unreachable. |
| EXCLUSION | Date-specific availability, free text, NLP price, bands, radius controls | — | EXCLUDED | Explicitly deferred or outside CONTEXT. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove correction, both location paths, both party paths, and reload-safe results at both widths</name>
  <precondition>The Docker daemon is reachable and the repository Playwright web server can connect to its configured isolated test database.</precondition>
  <reversibility rating="reversible">This task adds browser evidence and a test helper around the locked contract; it changes no persisted or public production schema.</reversibility>
  <files>e2e/progressive-search.spec.ts, e2e/helpers/booker-seed.ts</files>
  <behavior>
    - At 1280 × 800 and 375 × 812, one idle pill opens activity, an exact option opens location, either successful location route opens party, and no second interaction tree remains.
    - Back and every result-chip Edit retain confirmed answers; Cancel from a draft or submitted-result edit clears Phase 24 state and returns to `/` per D-02.
    - Unmatched activity text renders `No matching activity or type`, keeps focus in the filter, and creates neither navigation nor stored category.
    - For me immediately emits partySize=1; valid group entry submits only through See spaces, while malformed or out-of-range input cannot navigate.
    - Canonical URLs contain the confirmed answer tuple, survive reload, and reproduce identical chips/cards on a fresh shared-page visit.
    - Populated and capacity-empty outcomes show the same three Edit chips and reveal neither configured maximum/date-scoped remaining inventory nor a retired refinement hatch.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-02-SUMMARY.md — executed reducer, names, group parsing, focus/status, and geolocation cancellation behavior.
    - .planning/phases/24-search-bar-rework/24-03-SUMMARY.md — executed URL validation and configured-capacity predicate.
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — executed result/empty/loading contracts and runtime census.
    - e2e/progressive-search.spec.ts — 24-01 tracer fixture, provider interception, teardown, and named happy path to expand.
    - e2e/helpers/booker-seed.ts — seedBookableListing and openSeededListing contracts.
    - playwright.config.ts — project collection, server/database environment, retry, and worker behavior.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md — installed async searchParams contract.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md — installed streamed-fallback replacement contract.
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-router.md — installed history and same-origin navigation contract.
  </read_first>
  <action>
    Start RED by expanding `e2e/progressive-search.spec.ts` into a serial table for desktop and mobile-375 while retaining the real database/Server Component tracer. Seed uniquely titled published listings with explicit equal/above and below/NULL maximum capacities under one known catalogue value, and tear them down in dependency order even after failure. Intercept the address resolver with one deterministic valid response near the seed; never call the live geocoder. Grant fixed geolocation only for the explicit success case, and add denied plus late-callback coverage that proves address entry remains usable and the URL is untouched.

    At each width drive the D-01/D-02/D-03 order using the executed Plan 24-02 semantic names. Prove D-04 unmatched typing cannot advance, keyboard-select one exact option, resolve both D-05 branches, move Back with values intact, reopen each D-08 result chip, and Cancel from a submitted-result edit to the literal browse pathname while clearing retained answers. Cover immediate solo submission and explicit valid-group submission, inspect the exact URL key/value set, then reload and open the captured URL in a fresh page.

    Assert D-06/D-07 through rendered results: eligible listings appear, undersized/NULL listings do not, and visible/accessibility output contains no configured maximum, remainder, or date-specific place count. Edit the party answer above seeded capacity to reach the calm empty state and prove the identical three Edit chips remain without an automatic widening or removed control.

    Update `openSeededListing` so dependent booking suites can navigate through one canonical category/lat/lng/locationLabel/partySize URL and click the unique title. Keep the helper free of progressive-step duplication and do not absorb price-parity's protected venue-time calculation.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "journey|reload|share|Cancel|group|location"</automated>
    <fails_when>The command exits non-zero, reports zero matching tests, either viewport or location/party branch is absent, Cancel preserves submitted state, reload/share changes answers, an ineligible listing renders, a chip disappears from empty results, or any retired control/capacity claim is observable.</fails_when>
  </verify>
  <acceptance_criteria>The focused Chromium subset exits 0 with named desktop and 375 px cases proving D-01 through D-08 across the real client, Next.js server, validation, SQL, and result boundaries.</acceptance_criteria>
  <done>The full functional journey is deterministic, correctable, clearable to browse, shareable, capacity-honest, and responsive in both required browser sizes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove keyboard focus, status ownership, reduced motion, axe, and single-tree behavior</name>
  <precondition>Task 1's deterministic seed/provider harness is green and the Docker-backed Playwright server remains available.</precondition>
  <reversibility rating="reversible">This task strengthens browser evidence without changing runtime behavior or persisted data.</reversibility>
  <files>e2e/progressive-search.spec.ts, e2e/helpers/booker-seed.ts</files>
  <behavior>
    - Pointer and keyboard-only flows focus the newly mounted question rather than body after each auto-advance and direct Edit.
    - Search progress and AddressAutocomplete each announce only their owned outcome, and the browser contains one journey/result tree after streaming settles.
    - The idle, three question, populated, and empty states remain operable with reduced motion at both widths.
    - The existing repository axe helper reports no violation only after a positive state-reachability assertion at every scanned surface.
  </behavior>
  <read_first>
    - e2e/progressive-search.spec.ts and e2e/helpers/booker-seed.ts — Task 1 matrix, fixtures, and canonical helper.
    - e2e/helpers/axe.ts — repository WCAG tags, exclusions, node floor, and vacuity guards.
    - .planning/phases/24-search-bar-rework/24-02-SUMMARY.md — executed focus targets, status ownership, and one-DOM responsive contract.
    - .planning/phases/24-search-bar-rework/24-04-SUMMARY.md — executed result-region and inert-loading selectors.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md — the installed streaming replacement contract observed by the single-tree assertion.
  </read_first>
  <action>
    Extend the matrix with explicit pointer and keyboard-only cases at both widths. After every auto-advance and chip Edit, assert the executed heading/control owns focus and `document.activeElement` is not body. Assert the single Search progress region and AddressAutocomplete lookup region never repeat the same outcome, and that the settled document has one journey and one result region rather than preserving the inert streaming shell as a second tree.

    Re-run the complete state path under Playwright reduced-motion emulation. Before each axe scan, assert the state-specific heading/hook and a non-vacuous document node count, then call `expectAxeClean` on idle, activity, location, party, populated results, and calm empty results. Reuse the repository helper and its tag set; do not add another axe configuration or weaken its guards.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, a required width/state is absent, focus lands on body, a status outcome is duplicated, reduced motion makes a state unreachable, an axe guard becomes vacuous, or the settled page exposes multiple journey/result trees.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The command exits non-zero or emits a diagnostic for the progressive browser matrix or canonical helper.</fails_when>
  </verify>
  <acceptance_criteria>The full progressive-search spec and TypeScript gate exit 0, with named browser assertions for keyboard/pointer focus, singular status ownership, reduced motion, axe reachability, and one settled responsive tree at both widths.</acceptance_criteria>
  <done>The dedicated journey spec owns complete functional and accessibility evidence without absorbing dependent booking/design contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser events and URL to public Client/Server Components | Catalogue text, group size, labels, coordinates, repeated keys, and history state are untrusted until shipped client and server validation accepts them. |
| Geocoder/geolocation to retained location answer | Provider data, permission, callback order, and coordinates can fail, race, or be spoofed. |
| Database result model to public results | Configured capacity is an inclusion authority but must not cross into public availability copy. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-23 | Tampering | progressive-search URL/reload/share path | high | mitigate | Assert the exact canonical tuple at both widths and replay it through reload/fresh navigation; fail on retired or repeated authority reaching results. |
| T-24-24 | Spoofing / Information Disclosure | address and geolocation paths | high | mitigate | Intercept address data, request fixed geolocation only explicitly, invalidate stale callbacks, and prove denial/error submits nothing. |
| T-24-25 | Tampering / Information Disclosure | capacity-filtered public results | high | mitigate | Seed eligible, undersized, and NULL listings; assert rendered inclusion/exclusion and absence of configured/remainder/date claims. |
| T-24-26 | Elevation of Privilege | accessibility state | medium | mitigate | Exercise keyboard focus and the existing axe helper at every state without adding auth/session behavior. |
| T-24-27 | Denial of Service | browser matrix/provider waits | medium | mitigate | Serialize unique fixtures, perform ordered teardown, intercept geocoding, and use fixed geolocation rather than third-party waits. |
| T-24-SC | Tampering | package supply chain | low | accept | This plan installs no package and changes no dependency declaration. |
</threat_model>

<verification>
  <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
  <fails_when>The command exits non-zero, reports zero tests, any D-01 through D-08 journey path is absent, or functional/accessibility checks become vacuous.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or emits a diagnostic in either modified browser file.</fails_when>
</verification>

<success_criteria>
- D-01 through D-08 are exercised through real Chromium at desktop and 375 px, including Back/Edit/Cancel, exact/no-match activity, address/geolocation, solo/group, reload/share, populated/empty, keyboard/focus, reduced motion, and axe.
- Cancel from submitted results clears Phase 24 search state and returns to browse `/`; Back preserves answers.
- Every completed Phase 24 URL carries only the canonical answer tuple plus supported sort/page where relevant.
- Populated and empty results show the same activity/location/party Edit set and no capacity maximum, remaining-place promise, or retired control.
- The focused browser plan modifies two files and leaves dependent suite migration, dead-module retirement, and visual evidence to Plans 24-06 and 24-07.
</success_criteria>

<output>
Create .planning/phases/24-search-bar-rework/24-05-SUMMARY.md when done.
</output>
