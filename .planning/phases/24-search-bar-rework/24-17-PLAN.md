---
phase: 24-search-bar-rework
plan: 17
type: execute
wave: 14
depends_on: [24-16]
files_modified:
  - src/components/search/progressive-search-overlay.tsx
  - src/components/search/search-experience.tsx
  - src/components/search/party-step.tsx
  - tests/search/progressive-search.test.tsx
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-24-1]
requirements: [D-01, D-02, D-09]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and G-24-1, at 1280 px the idle search pill is approximately two-thirds of the public content width (48rem maximum) with a deliberate 56px desktop touch height, rather than spanning the entire max-w-6xl container."
    - "Per D-09 and G-24-1, the active desktop Popover is pill/direct-chip anchored, unclipped, and up to the same 48rem design width without moving the results region."
    - "Per D-02 and D-09, at 375 px the full-viewport sheet has exactly one visible dismissal affordance (Cancel), and Back plus Cancel occupy a non-overlapping lower action region while preserving Escape, focus, and cold-browse cancellation."
    - "Per D-06 and G-24-1, the short party-size question is visually compact on desktop while retaining the exact solo/group and bounded group-entry behavior."
    - "D-03 through D-05, D-07 through D-08, and D-10 through D-12 continue to use the existing reducer, address, URL, server-query, answer-chip, and route-synchronization contracts unchanged."
  artifacts:
    - path: src/components/search/progressive-search-overlay.tsx
      provides: "One responsive Radix host whose desktop Popover and mobile Dialog use intentional, breakpoint-specific presentation dimensions without changing the shared primitives."
    - path: src/components/search/search-experience.tsx
      provides: "A coordinator-owned desktop pill, presentation profile, and responsive Back/Cancel action layout that retain existing reducer callbacks."
    - path: src/components/search/party-step.tsx
      provides: "A compact desktop party decision surface that remains full-width and usable in the mobile sheet."
    - path: tests/search/progressive-search.test.tsx
      provides: "Focused structural and behavioral coverage for one mobile exit affordance, its lower action region, and the compact party presentation."
    - path: e2e/progressive-search.spec.ts
      provides: "Rendered Chromium evidence for desktop width hierarchy, compact final step, and 375px action separation alongside the existing geometry journey."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: src/components/search/progressive-search-overlay.tsx
      via: "The reducer-owned current screen supplies the selected desktop presentation profile and a single responsive children slot; it does not create viewport-specific question trees."
      pattern: "ProgressiveSearchOverlay"
    - from: src/components/search/progressive-search-overlay.tsx
      to: src/components/ui/dialog.tsx
      via: "The search-local DialogContent opts out of the shared default close button while preserving the primitive's controlled open-change and focus lifecycle."
      pattern: "showCloseButton={false}"
    - from: e2e/progressive-search.spec.ts
      to: src/components/search/search-experience.tsx
      via: "The existing browser journey measures rendered desktop and mobile controls, then drives semantic Back and Cancel rather than inferring responsive behavior from class strings."
      pattern: "progressive-search-mobile-sheet"
---

<objective>
Close G-24-1 by giving the progressive-search host a balanced responsive visual hierarchy: a deliberately sized desktop pill and Popover, a compact final party question, and a single lower mobile action region.

Purpose: The current fixed 30rem Popover, full-width idle trigger, shared short-step shell, and duplicate mobile close controls make the journey feel visually unbalanced even though its functional contracts pass.
Output: Local search presentation changes with focused and rendered-browser regression coverage; no shared primitive, URL, query, dependency, migration, snapshot, or external-service change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-UAT.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/debug/phase24-search-visual-ux.md
@.planning/phases/24-search-bar-rework/24-11-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-13-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-14-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-15-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-16-SUMMARY.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
@node_modules/next/dist/docs/01-app/01-getting-started/11-css.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
@src/components/search/progressive-search-overlay.tsx
@src/components/search/search-experience.tsx
@src/components/search/party-step.tsx
@src/components/ui/dialog.tsx
@tests/search/progressive-search.test.tsx
@e2e/progressive-search.spec.ts

<interfaces>
- `ProgressiveSearchOverlay` remains the sole runtime responsive boundary: it mounts one controlled Popover on desktop or one controlled Dialog below `(max-width: 639px)`, receives one child slot, and sends close requests to `onDismiss`.
- `SearchExperienceCoordinator` remains the exclusive owner of `progressiveSearchReducer`, canonical navigation, answer retention, heading focus, the named polite status region, direct-edit virtual anchor, Back, and Cancel. The presentation work must not fork that owner.
- `DialogContent` supplies a shared `showCloseButton` prop. This plan consumes that prop only in the progressive-search mobile Dialog; `src/components/ui/dialog.tsx` stays unchanged so other dialogs retain their existing close behavior.
- `PartyStep` keeps its public callback and validation interface. A presentation-profile input may be added only if it keeps all existing `For me`, `For a group`, bounded input, and submit callback semantics unchanged.
- The existing `overlay and sheet geometry anchor the progressive flow without route reflow` Chromium case is the rendered-geometry authority. Extend it and its helpers rather than replacing it with snapshots, source scans, timeout-based checks, a second route, or a duplicate mobile tree.
</interfaces>
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | A compact progressive activity → location → party journey appears only after engagement and transitions smoothly | 24-17 Tasks 1–2 correct the visual hierarchy without changing the completed journey | COVERED |
| REQ | ROADMAP Phase 24 requirements are `TBD` | D-01, D-02, and D-09 remain the operative requirements; no synthetic REQ identifier is introduced | COVERED |
| RESEARCH | Keep client interaction local, use existing Tailwind/Radix wrappers, preserve one owner/focus/status/motion contract, and add no package | 24-17 Tasks 1–2 make only local Client Component composition and rendered-browser assertions | COVERED |
| CONTEXT | D-01 compact engagement pill | Task 1 sets the desktop idle pill to the deliberate 48rem/56px hierarchy while retaining full mobile width | COVERED |
| CONTEXT | D-02 retained order and destructive Cancel-to-browse | Task 1 moves the existing callbacks into the responsive mobile footer without changing their reducer or route effects | COVERED |
| CONTEXT | D-06 exact party-size presentation | Task 2 gives only the short final decision a compact desktop profile; no party input or submission behavior changes | COVERED |
| CONTEXT | D-09 anchored desktop Popover, full-screen mobile sheet, no page push, one question at a time | Tasks 1–2 keep one controlled host, align the desktop width hierarchy, suppress duplicate mobile close control, and prove rendered geometry | COVERED |
| CONTEXT | D-03–D-05, D-07–D-08, D-10–D-12 | Existing verified behavior outside G-24-1; both tasks preserve the coordinator, existing child contracts, URL/query flow, answer chips, and browser journeys | PRESERVED |
| DEFERRED | Date-aware availability, free-text search, fuzzy matching, and natural-language price parsing | Unrelated to the responsive presentation gap | EXCLUDED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Establish the balanced desktop host and one mobile action region</name>
  <reversibility rating="reversible">The decision changes only local responsive layout composition and an existing Dialog prop; it does not alter persisted data, URL semantics, shared primitives, or server behavior.</reversibility>
  <files>src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx, e2e/progressive-search.spec.ts</files>
  <behavior>
    - At 1280 px, the idle `Start your search` pill has a 48rem maximum width and a 56px desktop height, and the opened standard Popover uses the same maximum available width while remaining pill/direct-chip anchored and leaving the results region in place.
    - At 375 px, the responsive host remains one full-viewport Dialog and exposes no `Close` icon button; it keeps the existing visible Back and Cancel controls in a named lower action region, with separate reachable boxes and their established callbacks.
    - Back still retains answers and focuses the active step, Cancel still clears through the existing reducer then navigates to `/`, Escape still reaches the controlled `onOpenChange` dismissal boundary, and normal/reduced-motion behavior remains governed by the existing token classes.
  </behavior>
  <read_first>
    - AGENTS.md plus the bundled Next.js 16 Server/Client, use-client, and CSS guides named in this plan's context — retain the existing narrow Client Component boundary and Tailwind utility approach.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md and 24-UAT.md — D-01, D-02, D-09, the reported visual gap, and deferred scope fences.
    - .planning/debug/phase24-search-visual-ux.md — the confirmed 30rem/full-width/duplicate-close root cause, including alternatives already eliminated.
    - .planning/phases/24-search-bar-rework/24-11-SUMMARY.md, 24-13-SUMMARY.md, 24-14-SUMMARY.md, 24-15-SUMMARY.md, and 24-16-SUMMARY.md — the protected one-host, direct-edit, focus, stale-callback, and consecutive-frame contracts.
    - src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, and src/components/ui/dialog.tsx — current controlled host, target 30rem/full-width classes, reducer callbacks, and the shared default Close contract.
    - tests/search/progressive-search.test.tsx and e2e/progressive-search.spec.ts — existing matchMedia, one-tree/focus assertions, test ids, strict geometry journey, and semantic interaction helpers.
  </read_first>
  <action>
    Start RED by adding focused component assertions and rendered Chromium assertions for the revised hierarchy. The browser proof must measure the idle pill and standard desktop Popover at 1280px, require their widths to be at most 48rem and materially larger than the former 30rem host where viewport space permits, retain the existing pill adjacency and results-region Y checks, and validate the 375px action region through real control boxes. On mobile, assert no accessible Close icon button exists, Back and Cancel are both visible inside one named action group near the lower viewport, their boxes do not intersect, and both retain semantic recovery behavior. Keep the existing exact one-tree, focus, status, reduced-motion, direct-edit, and frame-geometry assertions; do not replace live measurements with source-class expectations or snapshot updates.

    In `SearchExperience`, implement the user-directed desktop hierarchy per D-01 and G-24-1: keep the pill full width below `sm`, cap it at 48rem above `sm`, and make its desktop height 56px so the narrower width retains a calm, touchable visual anchor. Use the same standard desktop width ceiling in `ProgressiveSearchOverlay` in place of the fixed 30rem Popover width, retaining the existing viewport cap, available-height cap, overflow handling, anchor positioning, and direct-edit virtual reference per D-09. Do not alter the desktop host selection, Popover side/alignment, route layout, or results owner.

    In the mobile `DialogContent` call, set the existing `showCloseButton` prop false instead of editing `src/components/ui/dialog.tsx`, so Cancel is the sole visible dismissal affordance. Move the existing Back and Cancel buttons into a named action group that stays in the lower portion of the 375px full-sheet layout: the question content remains first, the controls are separated by a top border and use the existing touch-safe buttons, and desktop keeps its compact top row. Reuse the present Back and Cancel handlers; do not add a second cancellation path, route action, state owner, focus effect, dialog primitive, CSS file, dependency, migration, configuration change, or breakpoint-specific question tree. Preserve D-02 through D-05, D-07 through D-08, and D-10 through D-12 behavior unchanged.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx src/components/search/search-experience.tsx tests/search/progressive-search.test.tsx e2e/progressive-search.spec.ts</automated>
  </verify>
  <done>At the target desktop and mobile viewports, the standard question host has a proportionate, anchored 48rem hierarchy; the mobile sheet has no duplicate Close control and puts non-overlapping Back/Cancel controls in its lower action region without regressing existing journey, focus, or motion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compact the final party decision and prove the complete responsive journey</name>
  <precondition>Playwright's configured server can exclusively bind localhost port 3000 and the existing test database is reachable; if either is false, halt and report the verified process or database condition before changing product or test code.</precondition>
  <reversibility rating="reversible">The compact party profile is a local presentational distinction for an existing short step and can be removed without changing its callbacks, public URL, data, or server contract.</reversibility>
  <files>src/components/search/progressive-search-overlay.tsx, src/components/search/search-experience.tsx, src/components/search/party-step.tsx, tests/search/progressive-search.test.tsx, e2e/progressive-search.spec.ts</files>
  <behavior>
    - On desktop, the party screen uses a compact presentation profile rather than reusing the broad activity/location host; its card fits its short initial choices with balanced padding and no needless blank host area.
    - On mobile, the party decision and optional group input remain comfortably full-width above the lower action region; `For me` immediately submits and a valid exact group size still enables `See spaces`.
    - Direct party-chip editing receives the compact desktop profile, while activity and location retain the standard width; full Chromium coverage still proves direct-edit anchoring, fixed reach, accessibility, stale-location recovery, reduced motion, and one tree.
  </behavior>
  <read_first>
    - src/components/search/party-step.tsx and src/components/ui/button.tsx — existing controlled party API, `MAX_OPEN_CAPACITY` authority, and touch-size recipe to retain.
    - src/components/search/search-experience.tsx and src/components/search/progressive-search-overlay.tsx after Task 1 — state screen selection, presentation boundary, current desktop/mobile layout, and retained direct-edit anchor.
    - tests/search/progressive-search.test.tsx and e2e/progressive-search.spec.ts after Task 1 — exact group/solo tests plus the newly added 1280px and 375px visual-geometry contracts.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-02, D-03, D-06, D-07, D-09 and deferred boundaries that the short-step presentation must not weaken.
  </read_first>
  <action>
    Start RED by extending the focused and Chromium coverage through the actual activity → address → party sequence and the direct party-answer chip. Require the desktop party host and its decision card to be measurably narrower than the standard 48rem activity host while remaining anchored and viewport-bounded; require the mobile party content to remain above the lower Back/Cancel action group, without horizontal clipping or control overlap. Retain the exact `For me`, `For a group`, invalid-input, and valid group-submit assertions rather than substituting a visual-only check.

    Add a screen-derived presentation profile at the existing `SearchExperience` → `ProgressiveSearchOverlay` interface. Use it only to select a compact desktop Popover width for `party`; standard activity and location screens retain the 48rem ceiling from Task 1. Keep the existing viewport/available-height safeguards and direct-edit virtual anchor. In `PartyStep`, use a compact, centered desktop card with proportionate padding and a stable comfortable minimum width for its two initial choices, but preserve full-width behavior below `sm` so the optional number input and `See spaces` action remain readable above the mobile action region. Keep every callback, `parseGroupPartySize`, `MAX_OPEN_CAPACITY`, label, immediate solo submission, group validation, server-owned capacity behavior, and one-question sequence per D-02, D-03, D-06, and D-07 unchanged. Do not add a party-size band, a date/availability claim, any new form state, a selector-only visual baseline, shared primitive changes, or an additional screen-specific tree.

    Run the named geometry regression before the complete Chromium progressive-search file, then run the focused component suite, scoped lint, and full Vitest suite. Preserve the current browser route, seed helper, Playwright configuration, test database lifecycle, D-05 stale-callback scenario, direct-edit Popover, mobile full-sheet frames, 25km result behavior, and accessibility checks. Do not modify snapshots, global styles, planning/state/roadmap files, dependencies, migrations, or unrelated source.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1</automated>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/progressive-search-overlay.tsx src/components/search/search-experience.tsx src/components/search/party-step.tsx tests/search/progressive-search.test.tsx e2e/progressive-search.spec.ts</automated>
    <automated>node node_modules/vitest/vitest.mjs run --reporter=dot --silent</automated>
  </verify>
  <done>The party step looks intentionally compact on desktop and remains usable at 375px, while the complete progressive-search Chromium and Vitest suites prove all existing responsive, accessibility, navigation, direct-edit, location, and public-search behavior still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Responsive presentation → reducer-owned journey | The same untrusted browser interaction selects a Popover or Dialog; the presentation must not create a second state/focus/status owner or change cancellation semantics. |
| Shared Dialog primitive → local mobile search composition | A shared default close control is composed into a search-specific full sheet and can compete with the product-owned Cancel action. |
| Rendered browser geometry → production visual behavior | jsdom proves structural ownership; Chromium is required to prove that controls fit, separate, and remain attached at real desktop/mobile bounds. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-17-01 | Tampering | Responsive host composition | medium | mitigate | Keep one runtime-selected Popover/Dialog and one coordinator children slot; focused tests retain one heading/status owner and Chromium drives the normal journey. |
| T-24-17-02 | Denial of Service | Mobile journey controls | medium | mitigate | Suppress the redundant shared Close only in the progressive Dialog, render the existing Back/Cancel callbacks in a measured lower action region, and assert reachable non-overlapping boxes at 375px. |
| T-24-17-03 | Tampering | Desktop Popover geometry | medium | mitigate | Use local standard/compact profile widths with the existing viewport and available-height caps; Chromium preserves anchoring, route non-reflow, and direct-edit reachability. |
| T-24-17-04 | Information Disclosure | Search answers and navigation | low | accept | This presentation-only scope adds no data collection and leaves the established URL, server validation, query, geolocation, and result projection contracts unchanged. |
| T-24-17-SC | Tampering | Package supply chain | low | accept | The plan installs no package and changes no dependency, lockfile, build configuration, or shared primitive. |
</threat_model>

<verification>
Run the targeted Chromium geometry journey after each behavior addition, then the complete progressive-search Chromium file, focused component tests, scoped ESLint, and full Vitest suite. The required rendered checks retain the existing no-reflow/full-sheet/one-tree proofs and add measurable desktop width, compact-party, and mobile action-separation assertions; no snapshot update substitutes for them.
</verification>

<success_criteria>
- At 1280px, the idle pill and standard active Popover use a deliberate 48rem maximum hierarchy, remain anchored without page reflow, and the short party presentation is materially compact rather than inheriting the broad host.
- At 375px, one full-viewport question tree is present, no top-right Close icon competes with cancellation, and the existing Back/Cancel controls appear separately in a measured lower action region through activity, location, and party.
- Back, Cancel, Escape, heading focus, the sole status owner, reduced-motion behavior, direct edit, exact party submission, address/geolocation recovery, fixed reach, and route synchronization remain covered by passing focused and complete browser/unit checks.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-17-SUMMARY.md` when done.
</output>
