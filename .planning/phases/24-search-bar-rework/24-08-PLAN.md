---
phase: 24-search-bar-rework
plan: 08
type: execute
wave: 6
depends_on:
  - 24-07
files_modified:
  - src/lib/design/visual-baselines.ts
  - src/lib/design/live-regions.ts
  - src/lib/design/selector-contract.ts
  - tests/design/brand-recipe.test.ts
  - tests/design/elevation-z.test.ts
  - tests/design/live-regions.test.tsx
  - e2e/helpers/visual-drive.ts
  - e2e/visual/surfaces.spec.ts-snapshots/
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Fixed live-region, selector, brand, and elevation contracts remove exactly the owners retired in Plan 24-07 while retaining positive counts, uniqueness/reachability checks, SearchExperience, Search progress, result errors, and every unrelated surface."
    - "Per D-01 through D-05, the visual inventory declares the settled idle pill and activity, location, and party questions at exactly 375 and 1280 widths with deterministic hooks that cannot match an earlier step or loading shell."
    - "Per D-06 through D-08, populated and calm empty states use canonical party-aware inputs, identical editable chips, and no configured-capacity, remaining-place, date-specific, or legacy-refinement claim."
    - "The sanctioned pinned-Linux workflow owns exactly twelve progressive-search PNGs, removes the four obsolete 320/768 result and 320/1280 relaxation references, and a same-ref repeat run creates no baseline diff."
    - "The final repository unit, design, TypeScript, lint, production-build, and Chromium gates are green after all ordered Phase 24 work."
  artifacts:
    - path: src/lib/design/live-regions.ts
      provides: Exact post-retirement live-region file/name/count inventory with Search progress retained
    - path: src/lib/design/selector-contract.ts
      provides: Exact post-retirement semantic selector inventory with uniqueness and reachability guards retained
    - path: src/lib/design/visual-baselines.ts
      provides: Six progressive-search surfaces with exact 375/1280 rows and state-specific reachability
    - path: tests/design/live-regions.test.tsx
      provides: Two-sided post-retirement live-region contract
    - path: tests/design/brand-recipe.test.ts
      provides: Exact brand membership after the expanded SearchBar deletion
    - path: tests/design/elevation-z.test.ts
      provides: Exact elevation membership after the expanded SearchBar deletion
    - path: e2e/helpers/visual-drive.ts
      provides: Deterministic activity, location, party, result, and empty-state drives
    - path: e2e/visual/surfaces.spec.ts-snapshots/
      provides: Workflow-managed Phase 24 Linux snapshot set as one atomic ownership boundary
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-375-court-visual-linux.png
      provides: Pinned narrow idle-pill reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-idle-pill-1280-court-visual-linux.png
      provides: Pinned desktop idle-pill reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-375-court-visual-linux.png
      provides: Pinned narrow activity-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-activity-step-1280-court-visual-linux.png
      provides: Pinned desktop activity-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-location-step-375-court-visual-linux.png
      provides: Pinned narrow location-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-location-step-1280-court-visual-linux.png
      provides: Pinned desktop location-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-party-step-375-court-visual-linux.png
      provides: Pinned narrow party-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-party-step-1280-court-visual-linux.png
      provides: Pinned desktop party-step reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-results-375-court-visual-linux.png
      provides: Pinned narrow populated-result reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-results-1280-court-visual-linux.png
      provides: Pinned desktop populated-result reference regenerated for Phase 24
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-empty-375-court-visual-linux.png
      provides: Pinned narrow calm-empty reference
    - path: e2e/visual/surfaces.spec.ts-snapshots/search-empty-1280-court-visual-linux.png
      provides: Pinned desktop calm-empty reference
  key_links:
    - from: .planning/phases/24-search-bar-rework/24-07-SUMMARY.md
      to: src/lib/design/live-regions.ts
      via: The exact retirement/residual ledger controls which fixed rows and count aliases change
      pattern: search-bar|relax-band|Search progress
    - from: src/lib/design/visual-baselines.ts
      to: e2e/helpers/visual-drive.ts
      via: Every declared interaction state has one deterministic production-path drive and a non-vacuous settled-state hook
      pattern: search-party-step|search-results|search-empty
    - from: src/lib/design/visual-baselines.ts
      to: e2e/visual/surfaces.spec.ts-snapshots/
      via: The generic runner derives exact Linux PNG names from the six-state two-width inventory
      pattern: VISUAL_BASELINES|baselineArg
    - from: .github/workflows/baselines.yml
      to: e2e/visual/surfaces.spec.ts-snapshots/
      via: The pinned Linux workflow is the sole writer and a second identical-ref dispatch proves repeatability
      pattern: generate-baselines|workflow_dispatch
---

<objective>
Reconcile Phase 24's fixed design registries, replace the visual inventory with deterministic progressive-search states, mint stable pinned-Linux references, prove repeat-run stability, and close with the final repository-wide release gate.

Purpose: Make D-01 through D-08's progressive search the only fixed design and committed visual contract after Plan 24-07 retires the legacy implementation.
Output: Eight bounded ownership paths, twelve explicitly named responsive PNGs, repeat-run evidence, and a complete green release result.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-05-SUMMARY.md
@.planning/phases/24-search-bar-rework/24-07-SUMMARY.md

<interfaces>
- `24-07-SUMMARY.md` supplies the final five-file deletion ledger, surviving SearchExperience/Search progress owners, dev-theme cleanup, and exact residual fixed-design/visual references.
- `src/lib/design/visual-baselines.ts` owns SurfaceId, DocumentSurface, BaselineRow, fixed membership counts, URLs/hooks, viewport rows, and filename derivation.
- `e2e/helpers/visual-drive.ts` owns deterministic drive registration and state transitions; the generic `e2e/visual/surfaces.spec.ts` runner remains read-only.
- `.github/workflows/baselines.yml` remains read-only and is the sole authorized snapshot writer; it pins Linux, seeds deterministic fixtures, and commits workflow-generated PNGs while local Playwright keeps snapshot updates disabled.
- `scripts/seed-baseline-fixtures.ts` remains read-only and supplies fixed categories, coordinates, explicit maximum capacities, and teardown-safe identifiers.
- `src/lib/design/accent-uses.ts` remains read-only: repository grounding found only a historical SearchBar comment, not an active retired membership. The executor confirms that fact against the 24-07 census instead of expanding this plan beyond eight ownership paths.
- The snapshot directory is one frontmatter ownership path because the sanctioned job creates/deletes its PNG set atomically; exact expected created, regenerated, and retired names are enumerated below.
</interfaces>
</context>

## Artifacts this phase produces

- Updated `src/lib/design/live-regions.ts`, `src/lib/design/selector-contract.ts`, and `tests/design/live-regions.test.tsx` — exact post-retirement live-region and selector membership with positive guards retained.
- Updated `tests/design/brand-recipe.test.ts` and `tests/design/elevation-z.test.ts` — measured post-deletion brand/elevation membership without unrelated threshold reduction.
- Updated `src/lib/design/visual-baselines.ts` and `e2e/helpers/visual-drive.ts` — idle, activity, location, party, populated-results, and calm-empty surfaces at 375 × 812 and 1280 × 800, each with deterministic production-path reachability.
- Workflow-managed `e2e/visual/surfaces.spec.ts-snapshots/` as one atomic ownership path, containing these twelve Phase 24 references: `search-idle-pill-375-court-visual-linux.png`, `search-idle-pill-1280-court-visual-linux.png`, `search-activity-step-375-court-visual-linux.png`, `search-activity-step-1280-court-visual-linux.png`, `search-location-step-375-court-visual-linux.png`, `search-location-step-1280-court-visual-linux.png`, `search-party-step-375-court-visual-linux.png`, `search-party-step-1280-court-visual-linux.png`, `search-results-375-court-visual-linux.png`, `search-results-1280-court-visual-linux.png`, `search-empty-375-court-visual-linux.png`, and `search-empty-1280-court-visual-linux.png`.
- Retired from that directory: `search-results-320-court-visual-linux.png`, `search-results-768-court-visual-linux.png`, `search-relax-band-320-court-visual-linux.png`, and `search-relax-band-1280-court-visual-linux.png`. The existing `search-results-1280-court-visual-linux.png` is regenerated in place as one of the twelve final references.
- `24-08-SUMMARY.md` — exact registry deltas, both workflow run URLs/commit identities, final PNG inventory, image-inspection findings, and every final release command/result.

## Source audit

| Source | Item | Plan/task | Status | Notes |
|---|---|---|---|---|
| GOAL | Progressive activity/location/party search is visually and behaviorally releasable | 24-08 Tasks 1-3 | COVERED | Fixed design contracts, deterministic evidence, and the full repository gate close the goal. |
| REQ | Phase requirement IDs | — | N/A | ROADMAP says TBD; no formal ID is invented. |
| RESEARCH | Reconcile dead-file live-region/selector/brand/elevation contracts after retirement | 24-08 Tasks 1-2 | COVERED | Changes are driven by 24-07's ledger and measured AST membership, not arbitrary count edits. |
| RESEARCH | Six progressive states × 375/1280 with deterministic drives and canonical inputs | 24-08 Task 2 | COVERED | Every hook fails on prior/loading states and every drive avoids live providers. |
| RESEARCH | Pinned-Linux writer, obsolete PNG retirement, repeat stability, and final release gate | 24-08 Task 3 | COVERED | Exact first/repeat workflow evidence and all repository lanes run last. |
| CONTEXT | D-01, D-02, D-03 | 24-08 Tasks 2-3 | COVERED | Idle engagement, fixed order, retained/cancel behavior, and immediate transitions are represented. |
| CONTEXT | D-04, D-05 | 24-08 Tasks 2-3 | COVERED | The closed catalogue and deterministic resolved-address/current-location paths drive the question states. |
| CONTEXT | D-06, D-07, D-08 | 24-08 Tasks 1-3 | COVERED | Party-aware result/empty states expose editable chips without availability or legacy-refinement claims. |
| EXCLUSION | Date-aware availability, free text, fuzzy inference, NLP price, party bands | — | EXCLUDED | Deferred/out of scope and absent from registries, drives, and baselines. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile fixed live-region and selector registries from the retirement ledger</name>
  <precondition>24-07-SUMMARY.md records all five deleted paths and the exact remaining design/visual references.</precondition>
  <reversibility rating="reversible">Registry and guard edits are recoverable and do not change public runtime behavior.</reversibility>
  <files>src/lib/design/live-regions.ts, src/lib/design/selector-contract.ts, tests/design/live-regions.test.tsx</files>
  <behavior>
    - Fixed inventories remove only rows, names, paths, and count aliases owned solely by the deleted SearchBar/RelaxBand implementation.
    - SearchExperience, the singular initially-empty Search progress region, result-error ownership, selector uniqueness/reachability, guard-the-guard assertions, and unrelated members remain positively enforced.
    - Count-bearing aliases and expectations equal measured post-retirement AST membership; empty, duplicate, or unreadable sources cannot satisfy the checks.
    - `src/lib/design/accent-uses.ts` remains unchanged because the census confirms no active retired membership.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-07-SUMMARY.md — exact deletion and residual-owner ledger.
    - src/lib/design/live-regions.ts and tests/design/live-regions.test.tsx — two-sided file/name/count inventory and positive checks.
    - src/lib/design/selector-contract.ts and tests/design/selector-contract.test.ts — selector rows, source reachability, and duplicate guards.
    - src/lib/design/accent-uses.ts — read-only confirmation that the remaining legacy reference is historical commentary rather than registry membership.
    - .planning/phases/24-search-bar-rework/24-CONTEXT.md — D-01 and D-08 retirement semantics plus D-03/D-06 result states.
  </read_first>
  <action>
    Begin RED by changing exact test expectations for the Plan 24-07 deletion ledger before updating the fixed registries. Remove only the deleted SearchBar/RelaxBand file, region, and selector memberships; rename count-bearing aliases and change counts only to values measured by the existing AST/scanner output. Preserve SearchExperience, singular Search progress ownership, result errors, selector uniqueness/reachability, non-vacuity and duplicate checks, and all unrelated entries. Confirm the accent registry has no active retired row and leave it untouched. Do not lower a global threshold, relax a duplicate/read failure, remove a positive assertion, or invent a replacement owner merely to retain the previous count.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/live-regions.test.tsx tests/design/selector-contract.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing config/test path, or prints a failed live-region, selector-membership, uniqueness, or reachability assertion.</fails_when>
  </verify>
  <fails_when>The command exits non-zero or reports zero tests; a deleted path/name/selector remains active; SearchExperience/Search progress/result errors lose a positive guard; an unrelated member disappears; the accent registry gains an unnecessary edit; or an empty/duplicate scanner can pass.</fails_when>
  <acceptance_criteria>The focused design checks exit 0 with exact post-retirement membership, singular Search progress ownership, selector uniqueness/reachability, unchanged unrelated guard strength, and no edit outside the three declared paths.</acceptance_criteria>
  <done>The fixed accessibility and selector registries describe the surviving progressive search rather than retired modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare and deterministically drive six progressive-search visual states</name>
  <precondition>Task 1 is green and 24-05-SUMMARY.md records the final semantic hooks, labels, canonical query contract, and supported viewport sizes.</precondition>
  <reversibility rating="reversible">Inventory, drive, and fixed test edits are recoverable before external PNG generation.</reversibility>
  <files>src/lib/design/visual-baselines.ts, e2e/helpers/visual-drive.ts, tests/design/brand-recipe.test.ts, tests/design/elevation-z.test.ts</files>
  <behavior>
    - Visual inventory contains idle, activity, location, party, populated-result, and calm-empty surfaces, each at exactly 375 × 812 and 1280 × 800.
    - Every surface has a semantic hook absent from its prior state and the inert loading shell; completed inputs contain category, paired coordinates, bounded locationLabel, and partySize without a retired refinement key.
    - Activity/location/party drives use the closed catalogue and intercepted deterministic address or browser-location data, never live network/geolocation, randomness, clock-derived labels, or mutable database writes.
    - Populated and empty drives use fixed fixtures; result cards reveal neither configured maximum nor remaining/date-specific availability.
    - Search results remain in the fixed theme-swap allow-list with identical-content reachability after canonical input changes.
  </behavior>
  <read_first>
    - .planning/phases/24-search-bar-rework/24-05-SUMMARY.md — executed states, hooks, labels, canonical inputs, and viewport contract.
    - .planning/phases/24-search-bar-rework/24-07-SUMMARY.md — retired surfaces and fixed-design residual ledger.
    - src/lib/design/visual-baselines.ts — existing search rows, SurfaceId/count schema, hooks, URLs, and filename derivation.
    - e2e/helpers/visual-drive.ts — drive registry and deterministic media/clock/network contract.
    - e2e/visual/surfaces.spec.ts — read-only generic reachability-first capture loop.
    - scripts/seed-baseline-fixtures.ts — read-only fixed categories, coordinates, capacities, and no-match fixtures.
    - tests/design/brand-recipe.test.ts and tests/design/elevation-z.test.ts — fixed deleted SearchBar membership and unrelated positive guards.
    - e2e/visual/theme-swap.spec.ts — read-only D-138 allow-list and identical-content contract.
  </read_first>
  <action>
    Start RED in the design tests by replacing the expanded-results/relax-band expectations with exact six-state membership, two widths per state, canonical supported inputs, non-loading reachability hooks, deterministic drive ownership, and measured post-SearchBar brand/elevation membership. Remove only rows owned by the deleted paths and retain unrelated counts and positive guards.

    Replace the search visual inventory with `search-idle-pill`, `search-activity-step`, `search-location-step`, `search-party-step`, `search-results`, and `search-empty`. Declare exactly 375 × 812 and 1280 × 800 court rows for each. Idle waits for settled navigation; activity engages the pill; location additionally chooses an exact fixture catalogue option; party additionally resolves fixed intercepted address data. Result and empty states use deterministic fixture-backed canonical inputs. Keep `search-results` in the D-138 theme-swap allow-list and leave the generic visual runner, workflow, and fixture seeder unchanged.

    Update `e2e/helpers/visual-drive.ts` so each interaction state is reached through production controls, each completed result state settles on an explicit result/empty hook, and no drive fabricates DOM or relies on a live provider. The twelve expected outputs are: `search-idle-pill-375-court-visual-linux.png`, `search-idle-pill-1280-court-visual-linux.png`, `search-activity-step-375-court-visual-linux.png`, `search-activity-step-1280-court-visual-linux.png`, `search-location-step-375-court-visual-linux.png`, `search-location-step-1280-court-visual-linux.png`, `search-party-step-375-court-visual-linux.png`, `search-party-step-1280-court-visual-linux.png`, `search-results-375-court-visual-linux.png`, `search-results-1280-court-visual-linux.png`, `search-empty-375-court-visual-linux.png`, and `search-empty-1280-court-visual-linux.png`.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/brand-recipe.test.ts tests/design/elevation-z.test.ts tests/design/live-regions.test.tsx tests/design/selector-contract.test.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing config/test path, or prints a failed registry, count, baseline-membership, or reachability assertion.</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=baselines</automated>
    <fails_when>The command exits non-zero, reports a missing workflow/script path, or prints a baseline workflow trigger, pinned-image, permission, fixture, or snapshot-write invariant failure.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The command exits non-zero or prints a TypeScript diagnostic, missing generated interface, or unresolved retired-module path.</fails_when>
  </verify>
  <fails_when>A command exits non-zero or reports zero tests; six-state/two-width membership differs; a hook matches the prior/loading state; a completed input carries a retired key; a drive uses uncontrolled data/timing; a result reveals capacity/availability; an unrelated design guard weakens; or a read-only workflow/runner/fixture file changes.</fails_when>
  <acceptance_criteria>Design, workflow, and type checks exit 0 with six exact progressive surfaces, twelve rows, deterministic production-path drives, canonical inputs, non-vacuous hooks, capacity-honest results, and unchanged generic runner/workflow/theme-swap guarantees.</acceptance_criteria>
  <done>The repository can deterministically reach every final Phase 24 state that the pinned-Linux workflow must capture.</done>
</task>

<task type="auto">
  <name>Task 3: Mint stable Linux references and execute the final repository gate</name>
  <precondition>The checkout is the tracked `dev` branch, GitHub CLI authentication can dispatch workflows, and the sanctioned baselines workflow can run its pinned Linux container and deterministic database fixture.</precondition>
  <reversibility rating="costly">Committed visual references require another sanctioned workflow regeneration to change, but remain recoverable through git.</reversibility>
  <files>e2e/visual/surfaces.spec.ts-snapshots/</files>
  <behavior>
    - The workflow atomically writes the twelve exact progressive-search PNGs enumerated in the plan artifacts and Task 2 action.
    - `search-results-320-court-visual-linux.png`, `search-results-768-court-visual-linux.png`, `search-relax-band-320-court-visual-linux.png`, and `search-relax-band-1280-court-visual-linux.png` are absent after their inventory rows retire.
    - A second dispatch from the resulting ref succeeds without creating a PNG diff.
    - Each changed image is inspected for correct state, visible focus, unclipped geometry, readable content, and absence of stale controls or capacity/date-specific claims.
    - Unit, design, TypeScript, lint, production build, and full Chromium suites pass after the workflow-generated commit is present locally.
  </behavior>
  <read_first>
    - .github/workflows/baselines.yml — read-only sole writer, workflow_dispatch trigger, pinned Linux image, fixtures, commit/push, and repeat-run contract.
    - src/lib/design/visual-baselines.ts and e2e/helpers/visual-drive.ts — Task 2's exact rows, hooks, and drives.
    - e2e/visual/surfaces.spec.ts — read-only baselineArg filename mapping and reachability-first capture.
    - e2e/visual/surfaces.spec.ts-snapshots/ — current tracked PNG inventory and four obsolete Phase 24 predecessors.
    - package.json — exact repository scripts; final commands use installed direct CLIs to avoid wrapper ambiguity.
    - .planning/phases/24-search-bar-rework/24-VALIDATION.md — final lane inventory and evidence obligations.
  </read_first>
  <action>
    Remove the four obsolete PNGs only after their inventory rows are gone; retain `search-results-1280-court-visual-linux.png` for sanctioned regeneration into the new result state. Commit and push Tasks 1-2 to the tracked `dev` branch so the remote runner captures the exact source. Dispatch `.github/workflows/baselines.yml`, join the `generate-baselines` job by commit/ref identity, require success, and fast-forward the workflow's Linux PNG commit locally.

    Verify the workflow-managed directory contains all twelve exact names declared in this plan and none of the four retired names. Inspect every changed PNG at original detail before accepting it: the idle image has one settled trigger; activity/location/party images show the correct question and focus/geometry; result/empty images show identical answer-chip structure and no configured maximum, remaining-place, date-specific, or legacy-refinement promise. Reject clipping, hidden overflow, wrong state, loading-shell capture, animation smear, live-provider content, or unrelated snapshot changes.

    Dispatch the same workflow again from the resulting ref, join it by commit/ref identity, and require success with no snapshot commit/diff. If any PNG changes, return to Task 2 and remove the nondeterministic driver input rather than accepting another baseline. Record both run URLs, source/result commit identities, exact twelve-file inventory, four absent names, and image-inspection findings in `24-08-SUMMARY.md`.

    With the workflow commit present locally, run the complete unit, design, TypeScript, lint, production build, and full Chromium lanes below. Do not omit a red lane, narrow the final gate to Phase 24 files, weaken an assertion, or update an unrelated baseline to obtain green.
  </action>
  <verify>
    <automated>node scripts/verify-workflows.mjs --section=baselines</automated>
    <fails_when>The command exits non-zero, reports a missing workflow/script path, or prints a baseline workflow trigger, pinned-image, permission, fixture, snapshot-write, or repeat-run invariant failure.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing test/config path, or prints any failed unit or integration assertion.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing design config, or prints any failed design, registry, workflow, or snapshot-inventory assertion.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The command exits non-zero or prints a TypeScript diagnostic, unresolved deleted-module import, or missing generated interface.</fails_when>
    <automated>node node_modules/eslint/bin/eslint.js .</automated>
    <fails_when>The command exits non-zero, reports an ESLint error, or reports that the repository target cannot be resolved.</fails_when>
    <automated>node node_modules/next/dist/bin/next build</automated>
    <fails_when>The command exits non-zero, prints a compile/type/build diagnostic, fails to emit the public route, or reports a missing Next build entry.</fails_when>
    <automated>node node_modules/@playwright/test/cli.js test --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing Chromium project, or prints a failed progressive/dependent browser, accessibility, or visual assertion.</fails_when>
  </verify>
  <fails_when>A command exits non-zero or reports zero tests; the first workflow run is not tied to the intended ref; the twelve-name inventory differs; a retired PNG remains; any changed image is uninspected or incorrect; the repeat run changes a PNG; an unrelated snapshot changes; or a full repository lane is omitted or narrowed.</fails_when>
  <acceptance_criteria>The first pinned-Linux dispatch produces the exact twelve declared progressive-search references, all changed images pass inspection, the repeat dispatch produces no diff, all four obsolete PNGs are absent, and every final repository lane exits 0.</acceptance_criteria>
  <done>Deterministic committed visual evidence and a complete green repository gate close Phase 24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 24-07 retirement ledger → fixed design registries | Incorrect row/count edits can hide a dead member or weaken unrelated design guarantees. |
| Visual inventory/driver → rendered public state | A hook or drive can bless the loading shell, wrong question, stale control, or uncontrolled provider/result data. |
| Pinned workflow → committed PNG references | Remote capture can be stale or nondeterministic unless source identity, fixed fixtures, exact filenames, image inspection, and a repeat run fail closed. |
| Final suite → release decision | Narrowing or skipping a lane can conceal cross-contract regressions after deletions and generated commits. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-36 | Tampering | Fixed design registries | high | mitigate | Reconcile exact ledger members, measure count aliases from the AST/scanner, and preserve positive duplicate/reachability/non-vacuity guards. |
| T-24-37 | Tampering / Spoofing | Visual inventory and drives | high | mitigate | Require prior/loading-state-negative hooks, canonical inputs, closed catalogue data, intercepted location data, and fixed fixtures. |
| T-24-38 | Information Disclosure | Result and empty screenshots | high | mitigate | Use synthetic fixtures only and reject provider-sensitive, configured-capacity, remaining-place, or date-specific content. |
| T-24-39 | Tampering / Repudiation | Baseline workflow output | high | mitigate | Join runs by source/ref identity, assert exact filenames, inspect every changed image, and require a second no-change dispatch with recorded URLs. |
| T-24-40 | Repudiation | Final release gate | high | mitigate | Run every declared repository lane after the generated commit and record exact commands/results in 24-08-SUMMARY.md. |
| T-24-SC | Tampering | Package supply chain | high | mitigate | Authorize no package installation or dependency-file edit; preserve the audited lockfile and fail on an unexpected manifest change. |
</threat_model>

<verification>
  <automated>node scripts/verify-workflows.mjs --section=baselines</automated>
  <fails_when>The sanctioned writer loses its dispatch-only, pinned-image, permission, fixture, or snapshot-write invariant.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run</automated>
  <fails_when>The command exits non-zero or reports zero tests after legacy retirement.</fails_when>
  <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts</automated>
  <fails_when>The command exits non-zero, reports zero tests, or design/visual/snapshot inventories disagree.</fails_when>
  <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
  <fails_when>The command exits non-zero or any deleted-module/generated-interface diagnostic remains.</fails_when>
  <automated>node node_modules/eslint/bin/eslint.js .</automated>
  <fails_when>The command exits non-zero or reports an ESLint error.</fails_when>
  <automated>node node_modules/next/dist/bin/next build</automated>
  <fails_when>The command exits non-zero or production compilation fails.</fails_when>
  <automated>node node_modules/@playwright/test/cli.js test --project=chromium --workers=1</automated>
  <fails_when>The command exits non-zero, reports zero tests, a migrated browser flow regresses, or an obsolete public-search contract remains reachable.</fails_when>
</verification>

<success_criteria>
- D-01 through D-08 are represented by six deterministic visual states at both required widths and by exact fixed design owners.
- Every retired SearchBar/RelaxBand design, selector, live-region, brand, and elevation membership is reconciled while unrelated thresholds and positive guards remain intact.
- The visual registry and workflow-managed directory agree on six states × two widths; every capture has a non-vacuous settled-state hook and passes original-detail inspection.
- The four obsolete PNGs are absent, the twelve explicitly named progressive-search PNGs are present, and the second sanctioned run creates no change.
- Unit, design, TypeScript, lint, production build, and full Chromium gates are green after all Phase 24 plans.
- `files_modified` declares eight ownership paths, below the 15-file blocker; the snapshot directory is atomic while every individual created/regenerated/deleted PNG name is explicit.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-08-SUMMARY.md` when done.
</output>
