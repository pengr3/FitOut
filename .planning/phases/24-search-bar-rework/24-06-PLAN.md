---
phase: 24-search-bar-rework
plan: 06
type: execute
wave: 4
depends_on:
  - 24-05
files_modified:
  - e2e/helpers/booker-seed.ts
  - e2e/search-and-book.spec.ts
  - e2e/one-tree.spec.ts
  - e2e/price-parity.spec.ts
  - e2e/axe-sweep.spec.ts
autonomous: true
requirements:
  - D-02
  - D-06
  - D-07
  - D-08
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every retained browser suite enters the booking flow through the Phase 24 progressive search journey rather than a legacy pre-seeded or relaxation route."
    - "Price parity and accessibility assertions remain intact after the entry-path migration."
    - "Dependent suites reuse one deterministic listing seed and preserve reset, cancel, result-selection, and booking-continuation semantics from D-02 and D-06 through D-08."
  artifacts:
    - path: e2e/helpers/booker-seed.ts
      provides: Canonical deterministic listing entry contract shared by dependent browser suites
    - path: e2e/search-and-book.spec.ts
      provides: Booking journey through submitted Phase 24 results
    - path: e2e/one-tree.spec.ts
      provides: One-tree browser coverage through the progressive entry path
    - path: e2e/price-parity.spec.ts
      provides: Price-parity coverage after deterministic result selection
    - path: e2e/axe-sweep.spec.ts
      provides: Accessibility sweep of the final progressive search states
  key_links:
    - from: e2e/search-and-book.spec.ts
      to: e2e/helpers/booker-seed.ts
      via: Shared progressive-search seed and result-selection helper
      pattern: openSeededListing|seed
    - from: e2e/price-parity.spec.ts
      to: e2e/helpers/booker-seed.ts
      via: The same seeded result drives browse and booking price assertions
      pattern: price|listing|select
    - from: e2e/axe-sweep.spec.ts
      to: app progressive search DOM
      via: Axe scans each reachable submitted state rather than a fabricated legacy surface
      pattern: axe|search|results|empty
---

<objective>
Migrate the four retained dependent browser suites onto the production-quality progressive search journey proven by Plan 24-05.

Purpose: Keep booking, one-tree, price-parity, and accessibility coverage meaningful while removing their dependency on the obsolete relaxation-era entry contract.
Output: Five focused browser-support paths sharing a deterministic D-02/D-06/D-07/D-08 journey, with all original suite-specific assertions retained.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VALIDATION.md
@.planning/phases/24-search-bar-rework/24-05-SUMMARY.md
@e2e/helpers/booker-seed.ts
@e2e/search-and-book.spec.ts
@e2e/one-tree.spec.ts
@e2e/price-parity.spec.ts
@e2e/axe-sweep.spec.ts
</context>

## Artifacts this phase produces

- `e2e/helpers/booker-seed.ts` — canonical deterministic helper contract usable by all retained dependent suites.
- `e2e/search-and-book.spec.ts`, `e2e/one-tree.spec.ts` — booking-focused suites migrated to submitted Phase 24 results.
- `e2e/price-parity.spec.ts` — unchanged parity guarantees exercised after result selection.
- `e2e/axe-sweep.spec.ts` — accessibility sweeps for progressive form, result, empty, cancel, and booking states.
- `24-06-SUMMARY.md` — exact migration ledger and focused Playwright evidence consumed by Plan 24-07.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Search and booking browser paths use the real progressive same-page flow | Tasks 1–2 migrate all four retained dependent suites. |
| REQ | D-02 | Tasks 1–2 use Cancel as cleared Phase 24 state returning to browse `/`, never a permanent results route. |
| REQ | D-06 | Both tasks submit real progressive forms and select deterministic strict-filter results; the axe sweep also reaches the explicit empty state. |
| REQ | D-07 | The shared helper and retained assertions verify edit reset semantics before re-submission where applicable. |
| REQ | D-08 | Booking and parity suites continue from selected submitted results into the existing booking flow. |
| RESEARCH | Migrate dependent browser contracts before deleting legacy runtime/test surfaces | Tasks 1–2 provide the green prerequisite and ledger consumed by Plan 24-07. |
| CONTEXT | Preserve suite-specific assertions; no relaxation UI or permanent results route | Tasks 1–2 change only entry/setup mechanics and retain each suite's independent contract. |

<tasks>

<task type="tracer">
  <name>Task 1: Migrate booking and one-tree suites through the canonical seeded result</name>
  <files>e2e/helpers/booker-seed.ts, e2e/search-and-book.spec.ts, e2e/one-tree.spec.ts</files>
  <read_first>24-CONTEXT.md D-02 and D-06 through D-08; 24-05-SUMMARY.md for the proven journey and helper contract; the three target files; e2e/progressive-search.spec.ts</read_first>
  <action>Start with the `search-and-book` happy path as the tracer: open the header search, complete the sequential fields, submit strict filters, select the deterministic seeded listing, and continue into the existing booking flow per D-06 and D-08. Reuse the Plan 24-05 helper contract; change `e2e/helpers/booker-seed.ts` only where a migration-facing typed option or assertion hook is necessary for the dependent callers, preserving its canonical seeded tuple and avoiding a second wizard implementation. Then migrate `one-tree` through the same entry path. Preserve all booking and one-tree assertions that are independent of setup. Where either suite exercises edits or cancel, assert D-07 invalidation and D-02 clear-and-return-to-browse `/`; do not translate those behaviors into URL state or a results route.</action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/search-and-book.spec.ts e2e/one-tree.spec.ts --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing Chromium project/spec path, or prints a failed booking or one-tree assertion.</fails_when>
  </verify>
  <fails_when>A suite bypasses the production form, duplicates the progressive workflow instead of reusing the helper, loses an independent booking/one-tree assertion, enters through a relaxation surface, or the focused Playwright command fails.</fails_when>
  <acceptance_criteria>Both suites reach their original assertions through one shared deterministic submitted-result path, and the focused command is green.</acceptance_criteria>
  <done>The booking and one-tree contracts now prove the Phase 24 entry path without weakening downstream behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate price-parity and accessibility sweeps without weakening their contracts</name>
  <files>e2e/price-parity.spec.ts, e2e/axe-sweep.spec.ts</files>
  <read_first>24-CONTEXT.md D-02 and D-06 through D-08; 24-05-SUMMARY.md; 24-06 Task 1 changes to e2e/helpers/booker-seed.ts; both target files; e2e/progressive-search.spec.ts</read_first>
  <action>Replace legacy setup in `price-parity` with the shared seeded progressive journey, retaining its complete browse-to-booking price comparison per D-08. Update `axe-sweep` to reach the sequential form, validation, deterministic loading/results, explicit zero-result empty state, reset path, cancel-to-browse `/`, and booking continuation through production controls per D-02, D-06, D-07, and D-08. Preserve existing accessibility thresholds, exclusions, and violation reporting. Do not mute failures, broaden ignores, fabricate DOM, or keep a relaxation-era route merely to make the sweep pass.</action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/price-parity.spec.ts e2e/axe-sweep.spec.ts --project=chromium --workers=1</automated>
    <fails_when>The command exits non-zero, reports zero tests, reports a missing Chromium project/spec path, or prints a failed price-parity or accessibility assertion.</fails_when>
  </verify>
  <fails_when>Price parity no longer compares the same selected listing across browse and booking; accessibility thresholds or assertions weaken; final states are unreachable through production controls; or the focused Playwright command fails.</fails_when>
  <acceptance_criteria>Price-parity and axe suites pass through the canonical Phase 24 journey with every suite-specific assertion and threshold retained.</acceptance_criteria>
  <done>All four dependent browser suites are migrated and green, producing the prerequisite for census-driven legacy retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixtures → browser UI | Synthetic seed data crosses into the production search and booking surfaces. |
| Browser selectors → application DOM | Retained suites must target real accessible controls and results without spoofed legacy surfaces. |
| Search result → booking flow | The selected listing identity and price cross from browse state into downstream booking assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-28 | Spoofing | Shared seeded result selection | high | mitigate | Assert the seeded listing identity before selection and again in the downstream booking/parity contract. |
| T-24-29 | Tampering | Migrated suite assertions | high | mitigate | Preserve independent expectations and accessibility thresholds; permit changes only to entry/setup mechanics. |
| T-24-30 | Information Disclosure | Browser fixtures | high | mitigate | Keep all listing and location data synthetic and bounded; no provider or user secrets enter traces. |
| T-24-31 | Denial of Service | Deterministic waits | high | mitigate | Wait on explicit form/result states with bounded Playwright expectations, not fixed sleeps or unbounded polling. |
| T-24-SC | Tampering | npm package supply chain | high | mitigate | No package installation is authorized; fail on unexpected manifest or lockfile changes. |
</threat_model>

<verification>
1. `node node_modules/@playwright/test/cli.js test e2e/search-and-book.spec.ts e2e/one-tree.spec.ts --project=chromium --workers=1` exits 0 and reports at least one test; a non-zero exit or zero tests fails.
2. `node node_modules/@playwright/test/cli.js test e2e/price-parity.spec.ts e2e/axe-sweep.spec.ts --project=chromium --workers=1` exits 0 and reports at least one test; a non-zero exit or zero tests fails.
3. `git diff -- package.json package-lock.json` is empty.
4. `24-06-SUMMARY.md` lists all four migrated suites and any shared-helper contract change for the Plan 24-07 census.
</verification>

<success_criteria>
- The five declared files form one focused dependent-browser migration slice.
- All retained browser suites enter through the real progressive same-page flow.
- Booking, one-tree, price-parity, and accessibility assertions remain at least as strong as before migration.
- The migration ledger is sufficient for Plan 24-07 to prove that legacy relaxation modules have no remaining consumers.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-06-SUMMARY.md` when done.
</output>
