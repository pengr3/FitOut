---
phase: 24-search-bar-rework
plan: 18
type: execute
wave: 15
depends_on: [24-17]
files_modified:
  - src/components/search/search-experience.tsx
  - e2e/progressive-search.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-24-2]
requirements: [D-01]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and G-24-2, at a 1280px viewport the idle 48rem search pill is centered horizontally rather than beginning at the public-content edge."
    - "Opening, cancelling, and reopening the desktop search journey retain the centered idle pill, the existing Popover anchor, and the unchanged 375px mobile presentation."
  artifacts:
    - path: src/components/search/search-experience.tsx
      provides: "A desktop-level flex display override on the local idle trigger that makes its existing responsive auto margins center the 48rem pill without changing the shared Button recipe."
    - path: e2e/progressive-search.spec.ts
      provides: "Rendered Chromium regression evidence for initial and post-cancel desktop pill centering alongside the existing width, Popover-anchor, no-reflow, and mobile-sheet checks."
  key_links:
    - from: src/components/search/search-experience.tsx
      to: e2e/progressive-search.spec.ts
      via: "The locally overridden desktop trigger display makes its existing `sm:mx-auto` and `sm:max-w-3xl` layout resolve to the viewport center; Chromium measures the resulting x-coordinate before and after the Cancel route recovery."
      pattern: "Start your search"
---

<objective>
Close G-24-2 by making the idle desktop search pill a flex-level layout item at the desktop breakpoint, so its existing 48rem maximum width and automatic horizontal margins visibly center it at 1280px.

Purpose: The shared Button remains `inline-flex`, which leaves a correctly sized pill at the left public-content edge despite its `sm:mx-auto` utility. The gap needs a local display override and a rendered centering assertion, not a shared primitive change.
Output: One local responsive class adjustment and one focused Chromium geometry regression test; no changes to the shared Button, Popover/Dialog host, reducer, navigation, URL contract, packages, migrations, configuration, or mobile layout.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-UAT.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-17-SUMMARY.md
@.planning/debug/g-24-2-desktop-pill-centering.md
@node_modules/next/dist/docs/01-app/01-getting-started/11-css.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
@src/components/search/search-experience.tsx
@src/components/ui/button.tsx
@e2e/progressive-search.spec.ts

<interfaces>
- `SearchExperienceCoordinator` remains the only owner of reducer state, navigation, focus restoration, the idle trigger, and the controlled `ProgressiveSearchOverlay` props.
- The shared `Button` base recipe intentionally remains `inline-flex`; this closure adjusts only the idle trigger's desktop class list because the defect is local to that caller.
- The existing `overlay and sheet geometry anchor the progressive flow without route reflow` Chromium case is the geometry authority. Extend it with live box measurements rather than a source scan, snapshot, duplicate route, or new test file.
</interfaces>
</context>

## Source audit

| Source | Item | Coverage | Status |
| --- | --- | --- | --- |
| GOAL | The engaged progressive search begins with a calm, visually balanced idle pill | Task 1 centers the existing desktop invitation without altering its progressive flow | COVERED |
| REQ | ROADMAP Phase 24 requirements are `TBD` | D-01 remains the applicable acceptance contract; no synthetic requirement identifier is introduced | COVERED |
| RESEARCH | Retain the existing Client Component boundary, Tailwind utilities, local Radix composition, and zero dependency installs | Task 1 changes one local responsive utility; both tasks retain existing rendered-browser coverage | COVERED |
| CONTEXT | D-01 requires a compact search pill that opens only on engagement | Tasks 1-2 preserve the trigger and prove it is centered at the desktop viewport | COVERED |
| CONTEXT | D-02–D-12 retain the journey, one host, anchoring, state, URL, server-query, and result contracts | Tasks 1-2 change neither callbacks nor data flow and run the existing route-recovery/mobile geometry checks | PRESERVED |
| DEFERRED | Date-aware availability and free-text/fuzzy/natural-language discovery | Outside the local desktop-layout correction | EXCLUDED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Center the idle desktop pill through its local display context</name>
  <reversibility rating="reversible">The correction is one responsive utility on a local client-side trigger and a browser assertion; it changes no persisted data, shared primitive, URL, or server contract.</reversibility>
  <files>src/components/search/search-experience.tsx, e2e/progressive-search.spec.ts</files>
  <behavior>
    - At 1280px, the idle `Start your search` trigger remains no wider than 768px and its horizontal midpoint is within one rendered CSS pixel of the viewport midpoint.
    - The same trigger opens the existing desktop Popover at the current anchor, and the results region keeps its prior Y-coordinate while the Popover is open.
    - At 375px, the existing full-sheet journey remains the only mobile host and its current geometry assertions continue to pass.
  </behavior>
  <read_first>
    - Read AGENTS.md and `node_modules/next/dist/docs/01-app/01-getting-started/11-css.md` before editing the Next.js client component; heed the installed Next.js guidance and retain the repository's Tailwind approach.
    - Read `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`, `src/components/search/search-experience.tsx`, and `src/components/ui/button.tsx` to preserve the existing client boundary and confirm why only the local trigger needs a desktop display override.
    - Read `.planning/debug/g-24-2-desktop-pill-centering.md`, `.planning/phases/24-search-bar-rework/24-UAT.md`, and `e2e/progressive-search.spec.ts` to preserve the confirmed root cause, exact reported viewport, and existing geometry helpers.
  </read_first>
  <action>
    Per D-01 and G-24-2, start RED by extending the existing `overlay and sheet geometry anchor the progressive flow without route reflow` Chromium case. At its existing 1280px viewport, use the live viewport width and idle trigger bounding box to require that the trigger midpoint is within one CSS pixel of the viewport midpoint. Keep its current rendered width, Popover adjacency, and results-region no-reflow assertions; do not replace them with class-string checks or snapshots.

    Then change only the idle `Start your search` Button class list in `SearchExperienceCoordinator`: add the desktop flex-display utility that overrides the shared `inline-flex` recipe above `sm`, while retaining the existing full mobile width, `sm:mx-auto`, `sm:max-w-3xl`, height, `justify-between`, accessible name, and engage handler. The flex-level display is required because the current child alignment relies on flex justification and automatic inline margins only center a non-inline layout item. Do not edit `src/components/ui/button.tsx`, `ProgressiveSearchOverlay`, the Popover alignment, any reducer event, route navigation, or mobile breakpoint behavior.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/search-experience.tsx e2e/progressive-search.spec.ts</automated>
  </verify>
  <done>The initial 1280px idle pill is centered at its existing 48rem ceiling, opens the existing anchored Popover without moving results, and the present 375px sheet geometry remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Pin centering across Cancel-to-idle recovery</name>
  <files>e2e/progressive-search.spec.ts</files>
  <action>
    Expand the same rendered Chromium geometry case after its existing desktop Cancel action returns the route to `/`. Verify the restored `Start your search` trigger is visible, remeasure its bounding box against the live viewport, and require the same one-pixel midpoint tolerance used for the initial idle state. Then leave the existing test's mobile transition, one-sheet, lower-action, compact-party, and focus-recovery checks in place. This protects the local centering fix across the real cold-browse recovery path without adding a second journey, modifying production code, or changing D-02 cancellation semantics.
  </action>
  <verify>
    <automated>node node_modules/@playwright/test/cli.js test e2e/progressive-search.spec.ts --project=chromium --workers=1 --grep "overlay and sheet geometry"</automated>
    <automated>node node_modules/eslint/bin/eslint.js e2e/progressive-search.spec.ts</automated>
  </verify>
  <done>At 1280px, both the first idle render and the post-Cancel cold-browse render measure centered; existing desktop-anchor and 375px responsive checks stay active in the same browser scenario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser-rendered search trigger → CSS layout | This closure changes only local presentation; no new user data, endpoint, authority, or persistence boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-24-18-01 | Tampering | `SearchExperienceCoordinator` trigger presentation | low | mitigate | Keep the display override local to the idle trigger; retain the coordinator, shared Button, navigation, and Popover contracts, then assert the rendered geometry through Chromium. |
| T-24-18-02 | Denial of service | Search interaction path | low | accept | No request, dependency, database, or algorithm changes occur; the existing public-search limits and server validation remain outside this layout-only correction. |
| T-24-18-SC | Tampering | Package-manager surface | low | accept | This plan adds no package or package-manager operation; lockfiles and dependency manifests are outside its file set. |
</threat_model>

<verification>
Run the scoped Chromium geometry case on one worker and ESLint on both modified files. The browser case must cover its original desktop anchoring/results-position checks, the new initial and recovered pill-center measurements, and the existing 375px sheet/action checks.
</verification>

<success_criteria>
At a 1280px viewport, the idle desktop search pill's midpoint aligns with the viewport midpoint within one rendered CSS pixel on both initial load and after Cancel returns the cold-browse state. The existing 48rem width, Popover anchoring, no-reflow result layout, and 375px mobile journey remain covered by the focused browser test.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-18-SUMMARY.md` when done.
</output>
