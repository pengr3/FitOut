---
phase: 24-search-bar-rework
plan: 10
type: execute
wave: 8
depends_on: [24-09]
files_modified:
  - src/components/listing/address-autocomplete.tsx
  - tests/listing/address-autocomplete.test.tsx
  - src/components/search/search-experience.tsx
  - tests/search/progressive-search.test.tsx
  - tests/search/search-results-states.test.tsx
autonomous: true
gap_closure: true
requirements: [D-05, D-08]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-05, changing address text invalidates an already-started Photon lookup immediately, so a result for the prior query can never be selected for the current query."
    - "A selected address whose natural display text exceeds the canonical 120-character bound still advances from location to party and produces the same coordinate-backed URL."
    - "Per D-08, a completed shared URL with valid category, coordinates, and party size but no locationLabel renders a meaningful editable location answer."
  artifacts:
    - path: src/components/listing/address-autocomplete.tsx
      provides: "Abortable, generation-guarded debounced Photon lookup lifecycle."
    - path: tests/listing/address-autocomplete.test.tsx
      provides: "Delayed A-to-B lookup regression that proves stale choices cannot resolve a location."
    - path: src/components/search/search-experience.tsx
      provides: "Bounded address-label serialization, location-step recovery, and completed-chip fallback."
    - path: tests/search/progressive-search.test.tsx
      provides: "Long-label submission and label-omitted shared-state regressions."
  key_links:
    - from: src/components/listing/address-autocomplete.tsx
      to: src/components/search/location-step.tsx
      via: "Only the current Photon request may call the existing resolved-address callback."
      pattern: "AbortController|generation"
    - from: src/components/search/search-experience.tsx
      to: src/lib/validation/booking.ts
      via: "The presentation label is bounded before searchParamsSchema validates the canonical URL; coordinates remain unchanged."
      pattern: "locationLabel"
---

<objective>
Close the three proven state-integrity gaps in the address/result path: stale lookup results, overlong display labels, and blank shared-link location chips.

Purpose: A booker must never select a prior address, lose a valid selected location at party submission, or receive an unexplained location answer after opening a valid shared URL.
Output: A race-safe resolver, bounded presentation labels with location recovery, meaningful chip fallback, and focused regressions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-search-bar-rework/24-CONTEXT.md
@.planning/phases/24-search-bar-rework/24-RESEARCH.md
@.planning/phases/24-search-bar-rework/24-PATTERNS.md
@.planning/phases/24-search-bar-rework/24-VERIFICATION.md
@.planning/phases/24-search-bar-rework/24-REVIEW.md
@.planning/phases/24-search-bar-rework/24-09-SUMMARY.md
@src/components/listing/address-autocomplete.tsx
@src/components/search/search-experience.tsx
@tests/listing/address-autocomplete.test.tsx
@tests/search/progressive-search.test.tsx
</context>

## Source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Engaged progressive activity → location → party journey remains reliable on the public route | 24-10 through 24-13 | COVERED |
| REQ | ROADMAP declares `TBD`; there are no separately defined Phase 24 requirement IDs | D-05/D-08 are the operative decision IDs in this plan | COVERED |
| RESEARCH | Retain the existing Photon resolver contract while defending its cancellation boundary | 24-10 Task 1 | COVERED |
| RESEARCH | `locationLabel` is display-only, bounded, and must never affect SQL | 24-10 Task 2 | COVERED |
| CONTEXT | D-01–D-04, D-06–D-07 were delivered by 24-01 through 24-09 and are not reopened | Existing plans | ALREADY DELIVERED |
| CONTEXT | D-05 address resolution is safe after input changes | 24-10 Task 1 | COVERED |
| CONTEXT | D-08 completed answers stay meaningful and editable | 24-10 Task 2 | COVERED |
| CONTEXT | D-09 desktop overlay/mobile sheet | 24-11 and 24-13 | COVERED |
| CONTEXT | D-10 fixed-coordinate 25 km nearest-first search | 24-12 and 24-13 | COVERED |
| CONTEXT | D-11 server-owned 25 km with no radius control/broadening | 24-12 and 24-13 | COVERED |
| CONTEXT | D-12 canonical route reversal | 24-09 | ALREADY DELIVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the live address lookup safe from an A-to-B response race</name>
  <files>src/components/listing/address-autocomplete.tsx, tests/listing/address-autocomplete.test.tsx</files>
  <behavior>
    - Test 1: after query A has started, changing the input to B immediately aborts and invalidates A before B's debounce expires.
    - Test 2: resolving A after B is typed cannot render A's option, clear B's pending feedback, or call onResolved; B alone may become selectable after its own response.
  </behavior>
  <action>Per D-05, keep the existing three-character threshold, 250 ms debounce, six-result Photon mapping, and host/search copy behavior, but assign every effect run its own AbortController and monotonic request generation. In the effect cleanup, clear the timer, invalidate the generation, and abort an already-started request; guard every asynchronous results, error, and loading write against that active generation. Clear stale visible suggestions when a new searchable query is typed so address A cannot be clicked while B is current. Add a real-component fake-timer/deferred-fetch regression: begin A, advance past debounce, type B, resolve A before B starts, and prove no A label or selection survives; then resolve B and prove the B option remains functional. Do not rely only on an abort-spy assertion because a mocked or misbehaving transport can still settle after abort.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/listing/address-autocomplete.test.tsx</automated>
  </verify>
  <done>A query change immediately makes every older Photon result inert, while the current lookup keeps the existing accessible loading, failure, and selection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve valid long addresses and label-less shared locations through the completed flow</name>
  <files>src/components/search/search-experience.tsx, tests/search/progressive-search.test.tsx, tests/search/search-results-states.test.tsx</files>
  <behavior>
    - Test 1: a resolved address whose natural rendered label exceeds 120 characters submits a canonical URL with the bounded nonempty label and unchanged coordinates.
    - Test 2: if canonical submission fails after an address is selected, recovery returns the booker to location with actionable progress rather than silently restarting activity.
    - Test 3: completed initial answers with category, coordinates, and party size but no locationLabel render an editable neutral location chip in populated and empty states.
  </behavior>
  <action>Per D-05 and D-08, make addressLabel create the existing comma-separated presentation string, trim it to the searchParamsSchema maximum without altering lat/lng, and keep that bound in the client only as a mirror of the server schema. When the canonical candidate cannot be submitted, retain the category and coordinates, return the reducer to the location correction step, and announce a concise location correction message; do not redirect a valid selected address to activity. Derive one nonempty neutral chip label for completed answers that lack locationLabel (for example, `Selected location`), use it only for displayed/editable answer text, and keep coordinates as the only location-search authority. Add regressions through the coordinator and result-state fixture paths; do not reverse-geocode, relax the schema maximum, add URL fields, or make label text part of query predicates.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/search/progressive-search.test.tsx tests/search/search-results-states.test.tsx</automated>
    <automated>node node_modules/eslint/bin/eslint.js src/components/search/search-experience.tsx tests/search/progressive-search.test.tsx tests/search/search-results-states.test.tsx</automated>
  </verify>
  <done>Long valid address selections reach party and navigate with a schema-valid label, and every valid completed result URL exposes a meaningful location edit chip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| typed address → Photon response | An external response is allowed to create a selectable location only for the current typed query. |
| browser answer → canonical URL validation | A presentation label accompanies coordinates but must remain bounded and non-authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-10-01 | Tampering | address-autocomplete lookup lifecycle | high | mitigate | Effect-local abort and generation checks prevent an earlier Photon response from creating a selectable answer after input changes. |
| T-24-10-02 | Spoofing | completed location chip | medium | mitigate | Use neutral fallback text only; coordinates remain the server query authority and no reverse-geocoded claim is manufactured. |
| T-24-10-03 | Denial of service | long address labels | medium | mitigate | Bound presentation text to the existing schema maximum before canonical navigation and recover at location on failure. |
| T-24-10-SC | Tampering | package installation | low | accept | This plan adds no package-manager operation or dependency. |
</threat_model>

<verification>
Run the two focused Vitest commands and changed-file ESLint. Do not substitute the broken npm launcher for the pinned node-module CLIs.
</verification>

<success_criteria>
The stale A-to-B lookup, long-label submission, and label-omitted shared-link regressions are covered by executable focused tests and all pass without modifying the schema bound or SQL location predicate.
</success_criteria>

<output>
Create `.planning/phases/24-search-bar-rework/24-10-SUMMARY.md` when done.
</output>
