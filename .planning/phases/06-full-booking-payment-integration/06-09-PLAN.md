---
phase: 06-full-booking-payment-integration
plan: 09
type: execute
wave: 6
depends_on: [06-04, 06-05, 06-06, 06-07, 06-08]
files_modified: []
autonomous: false
requirements: [HOST-01, BOOK-05, BOOK-06, PAY-05]

must_haves:
  truths:
    - "A human confirms the end-to-end pay-on-approval flow works against PayMongo test mode: request → host approve → pay-now email link → hosted checkout → payment.paid webhook confirms → confirmation email + on-screen 'Booking confirmed' (PAY-05, BOOK-06)"
    - "A human confirms the /host/requests inbox renders venue-local windows + a ticking expiry countdown, and Approve/Decline behave (approve → awaiting-payment; decline → confirm dialog → slot frees) (HOST-01, D-65)"
    - "A human confirms the booker requested ('Request sent — awaiting host', no charge) and approved ('pay now' + payment-window countdown + coral Pay now) states render per the UI contract (BOOK-05/BOOK-06, D-66)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the request-to-book lifecycle that the automated integration harness cannot fully drive: the off-site PayMongo hosted-checkout redirect + real webhook round-trip (pay-on-approval), the client-side expiry countdown rendering venue-local, and the visual correctness of the host inbox + the new booker confirmation states.

Purpose: The full pay-on-approval path crosses an external hosted checkout and a live webhook delivery (VALIDATION.md Manual-Only) — mockPayMongo covers the server contract, not the redirect round-trip. This checkpoint closes that gap and confirms the UI contract before phase verification.
Output: human confirmation (or a punch list of issues to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-VALIDATION.md
@.planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md
@$HOME/.claude/projects/C--Users-Admin-Roaming-FitOut/memory/local-env-and-uat-seed.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the pay-on-approval flow + host inbox + booker states end-to-end</name>
  <files>none — verification only (no file changes)</files>
  <action>
    Present the how-to-verify steps below to the developer and pause for their sign-off. All implementation landed in Waves 1-5; this task performs NO code changes. Ensure the automated suites are green first (`npm test`), then bring up the local stack (app + Docker Postgres + Inngest Dev Server + a PayMongo webhook tunnel in test mode) and walk the developer through the request-to-book lifecycle end-to-end. On "approved", record the confirmation in the SUMMARY; on issues, capture the step number + screenshot into a punch list for a `/gsd:plan-phase --gaps` closure plan.
  </action>
  <what-built>
    Phase 6 wired the request-to-book fork: placeHold branches on listing.bookingMode; a request holds the slot with no charge; /host/requests lets the host approve/decline within the SLA; approval emails the booker a pay-now link that reuses the exact Phase-5 hosted checkout; the payment.paid webhook confirms (single writer) and emails the booker; an Inngest cron auto-declines/auto-releases lapsed requests. The double-booking guarantee was extended to the new requested/approved states (widened GiST EXCLUDE + all occupancy predicates).
  </what-built>
  <how-to-verify>
    Prereqs: bring up the app + DB locally (see the local-env-and-uat-seed memory — `npm run db:up`, `npm run db:migrate`, `npm run dev`) and start the Inngest Dev Server + a PayMongo webhook tunnel (ngrok/cloudflared → the dashboard webhook URL) in PayMongo test mode.

    1. As a host, edit a listing → Step 5 → set "Request to book" (confirm the copy says the change applies to new bookings only). Publish.
    2. As a booker, book a slot on that listing. Confirm: you land on /bookings/[id] showing "Request sent — awaiting host", the total labeled "You'll pay if approved" (NOT charged), no pay CTA. Confirm a "request received" email (dev log or Resend) and a host "new request" email fired.
    3. As the host, open /host/requests. Confirm: the row shows the booker, space, requested window in venue-local time, quoted price, and an "Expires in {N}h {M}m" countdown that ticks (per-minute). Confirm the pending-count nudge appears in the dashboard action row + the (host) header.
    4. Click Approve. Confirm the request moves to awaiting-payment (no dialog) and a "approved — pay now" email arrives carrying the checkout link.
    5. As the booker, follow the pay-now link → /bookings/[id] shows "Your request was approved" with a payment-window countdown + coral "Pay now" → the reserve/Confirm & pay page → complete the PayMongo test checkout.
    6. Confirm the payment.paid webhook flips the booking to "Booking confirmed" on-screen (the ?paid=1 interstitial self-resolves) and a "booking confirmed" email arrives.
    7. Decline path: create another request, click Decline → confirm the neutral (not red) confirm dialog, decline, and confirm the slot frees (bookable again) + the booker "declined" email fires.
    8. Expiry path (optional, DB-clock): force a requested row's expires_at into the past and let the request-expiry cron run (minute 15) → confirm it flips to declined + the slot frees.
    9. Confirm an instant-book listing still confirms immediately on payment (BOOK-04 unregressed).
  </how-to-verify>
  <verify>
    <human-check>Report "approved" if every step passes, or list the specific failures (screenshot + step number) to feed a gap-closure plan.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer has walked all nine steps and reported "approved", OR a punch list of specific failures (step number + evidence) is recorded for gap closure.
    - On-screen "Booking confirmed" appears ONLY after the real payment.paid webhook lands (not from the browser ?paid=1 return) — the D-57 single-writer authority holds end-to-end.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues found.</resume-signal>
  <done>Human has confirmed the pay-on-approval flow, host inbox, decline, expiry, and instant-book paths — or filed a punch list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booker browser → hosted checkout → webhook | The off-site redirect + live webhook is the one path the integration harness cannot drive; human verification closes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-26 | Spoofing | browser ?paid=1 vs real confirm | mitigate | The human check confirms the on-screen "Booking confirmed" only appears AFTER the real payment.paid webhook lands (not from the browser return) — the D-57 single-writer authority holds end-to-end. |
</threat_model>

<verification>
- Human confirms all nine steps (or files a punch list).
- All automated suites already green from Waves 1-5 (`npm test`).
</verification>

<success_criteria>
- End-to-end pay-on-approval confirmed on-screen + by email against PayMongo test mode.
- Host inbox approve/decline + venue-local countdown verified.
- Booker requested/approved states verified; instant-book unregressed.
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-09-SUMMARY.md` when done.
</output>
