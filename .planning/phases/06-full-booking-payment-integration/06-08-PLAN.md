---
phase: 06-full-booking-payment-integration
plan: 08
type: execute
wave: 5
depends_on: [06-01, 06-07]
files_modified:
  - src/components/booking/request-countdown.tsx
  - src/components/host/request-row.tsx
  - src/app/(host)/host/requests/page.tsx
  - src/app/(host)/host/page.tsx
  - src/app/(host)/host/layout.tsx
  - src/app/bookings/[id]/page.tsx
autonomous: true
requirements: [HOST-01, BOOK-05, BOOK-06]

must_haves:
  truths:
    - "A host has an owner-scoped /host/requests inbox listing pending (requested) bookings — booker, space, requested window (venue-local), quoted price, an expiry countdown, and Approve/Decline actions wired to the 06-07 server actions (HOST-01, D-65)"
    - "The inbox owner-scopes its read to WHERE listing.host_id = session.user.id — a host never sees another host's requests (Security V4; the route group is NOT the gate)"
    - "A pending-count nudge (neutral secondary badge, hidden at 0) appears in the (host) dashboard action row and the (host) header nav, mirroring the Earnings nav (D-65)"
    - "The booker confirmation page /bookings/[id] renders the new requested ('Request sent — awaiting host', no pay CTA, 'you haven't been charged') and approved ('Your request was approved' + payment-window countdown + coral Pay now) states (BOOK-06, D-66); confirmed/pending-interstitial/reversed are unchanged"
    - "RequestCountdown is an hours-scale display-only clone of hold-countdown.tsx (per-minute tick, DB now() is the sole authority) reused on the host SLA and the booker payment-window"
  artifacts:
    - path: "src/app/(host)/host/requests/page.tsx"
      provides: "owner-scoped host requests inbox RSC"
      min_lines: 60
    - path: "src/components/booking/request-countdown.tsx"
      provides: "hours-scale expiry countdown client component"
      contains: "role=\"timer\""
    - path: "src/app/bookings/[id]/page.tsx"
      provides: "requested + approved booker states"
      contains: "approved"
  key_links:
    - from: "src/app/(host)/host/requests/page.tsx"
      to: "booking joined to listing WHERE listing.host_id = session.user.id AND booking.status='requested'"
      via: "owner-scoped read (mirror /host/earnings)"
      pattern: "host_id|hostId"
    - from: "src/components/host/request-row.tsx RequestActions"
      to: "approveRequest / declineRequest (src/app/actions/host-requests.ts)"
      via: "Approve inline + Decline confirm-dialog; sonner toasts + revalidatePath"
      pattern: "approveRequest|declineRequest"
    - from: "src/app/bookings/[id]/page.tsx approved state"
      to: "coral Pay now → /listings/[id]/book?hold=<id>"
      via: "the pay-on-approval conversion CTA"
      pattern: "book\\?hold"
---

<objective>
Build the host-facing request inbox (`/host/requests`) and the booker-facing confirmation states (`requested` / `approved`) — the visible request-to-book surface (HOST-01, BOOK-05, BOOK-06). Clone `/host/earnings` for the inbox, clone `hold-countdown.tsx` at hours scale for the expiry countdown, add the pending-count nudge, and branch `/bookings/[id]` on the new statuses per the approved UI contract.

Purpose: 06-07 shipped the approve/decline actions; this plan gives the host somewhere to call them and the booker the "request sent" / "approved — pay now" surfaces. All lifecycle states are calm/neutral (never red, never green until confirmed); the one coral CTA is the booker `Pay now`.
Output: RequestCountdown, /host/requests inbox (RSC + RequestRow + RequestActions), pending-count nudge (dashboard + header), and the requested/approved states on /bookings/[id].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md

<interfaces>
<!-- Clone sources (verified 2026-07-20). -->

src/app/(host)/host/earnings/page.tsx — the clone template for /host/requests: the defense-in-depth session + canHost re-check (45-54); the OWNER-SCOPED read joined to booking+listing WHERE hostId = session.user.id (56-78); the venue-tz display discipline `tz(r.timezone)` + `format(..., { in: inTz })` (89-108); the desktop shadcn Table (real th scope=col) + mobile stacked cards (155-214); container `mx-auto w-full max-w-4xl px-4 py-10`. For requests: filter booking.status='requested', columns Space · When · Guest · Guest pays · Expires · Actions.

src/components/host/payout-row.tsx — the mobile row-card template for RequestRow (PayoutRowData shape, formatMoney display-only, no arithmetic).

src/components/booking/hold-countdown.tsx — the RequestCountdown clone source: setInterval-only setState (44-55), onExpireRef ref-synced in its own effect (38-41), suppressHydrationWarning on the digits (76), role="timer" aria-live="off". Retune to HOURS scale: per-MINUTE tick (60s, not per-second — the horizon is 24h), format "Expires in {N}h {M}m" (host) / "Pay within {N}h {M}m" (booker), under 1h → "{M}m", optional --destructive numerals in the final hour, announce only at a threshold + expiry.

src/app/(host)/host/page.tsx:34-38,63-75 — the count() idiom + the action-row Button list. Add a pending-request count (booking joined to listing WHERE hostId=session.user.id AND status='requested') + a neutral outline "Requests" Button with a secondary count badge (hidden at 0).
src/app/(host)/host/layout.tsx:49-61 — the header nav links (Earnings/Profile). Add a neutral "Requests" Link + count badge between Earnings and Profile (same text-sm font-medium underline-offset-4 hover:underline).

src/app/bookings/[id]/page.tsx:74-87 — the status branch. placeHold's request branch (06-04) redirects here; a `requested` row currently falls to notFound() (87). Add a `requested` branch (Request sent — awaiting host; Awaiting host badge secondary+Hourglass/Clock; show window + "You'll pay if approved" total; NO pay CTA) and an `approved` branch (Your request was approved; Approved badge outline+CalendarCheck — NOT success; payment-window RequestCountdown; coral Pay now → /listings/[listingId]/book?hold=<id>) BEFORE notFound(). Optionally render calm declined/expired landings (muted badge + coral "Find another space") instead of a bare 404. confirmed / pending-interstitial / reversed branches unchanged.

Actions to wire: approveRequest / declineRequest (src/app/actions/host-requests.ts, 06-07). Approve = inline neutral default solid button (disable-on-click → "Approving…"); Decline = neutral outline button opening a confirm dialog (existing `dialog`) before firing (D-65 discretionary lock). Toasts via sonner; row refresh via revalidatePath. Config: APPROVAL_SLA_HOURS / APPROVAL_PAYMENT_WINDOW_HOURS (06-01) for deadline copy. No coral on the inbox (mirror /host/earnings). formatMoney / DISPLAY_CURRENCY (src/lib/money.ts), venueTzNote (src/lib/venue-time.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestCountdown + /host/requests inbox (page + RequestRow + RequestActions)</name>
  <files>src/components/booking/request-countdown.tsx, src/components/host/request-row.tsx, src/app/(host)/host/requests/page.tsx</files>
  <read_first>
    - src/components/booking/hold-countdown.tsx (the timer discipline to clone at hours scale)
    - src/app/(host)/host/earnings/page.tsx (the owner-scoped RSC + table/cards clone template)
    - src/components/host/payout-row.tsx (the mobile row-card template)
    - src/app/actions/host-requests.ts (approveRequest/declineRequest to wire — 06-07)
    - src/lib/money.ts (formatMoney/DISPLAY_CURRENCY), src/lib/venue-time.ts (venueTzNote), src/lib/payments/config.ts (APPROVAL_* hours)
    - .planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md (the inbox copy + RequestCountdown contract + status treatments)
  </read_first>
  <action>
    Create src/components/booking/request-countdown.tsx as a direct structural clone of hold-countdown.tsx retuned to an hours horizon: per-MINUTE setInterval (60s), format "Expires in {N}h {M}m" / "Pay within {N}h {M}m" (label passed by prop), under 1h drop the hours, optional --destructive numerals in the final hour, keep the exact timer discipline (setInterval-only setState, onExpireRef ref-synced, suppressHydrationWarning digits, role="timer" aria-live="off", threshold+expiry-only announcements). Display-only — the DB now() vs expires_at guard (06-07 approve / 06-06 sweep) is the authority. Create src/components/host/request-row.tsx = RequestRow (mobile card mirroring PayoutRow: space title 600 + "{date}, {time} ({City} time)" meta + booker/"A guest" + "Guest pays {₱total}" tabular-nums + RequestCountdown "Expires in …") + a "use client" RequestActions (Approve = neutral default solid, disable-on-click → "Approving…"; Decline = neutral outline opening a confirm dialog before firing; both call the 06-07 actions, toast via sonner, and rely on revalidatePath for freshness; owner-scoped/SLA-guarded server-side). Create src/app/(host)/host/requests/page.tsx by cloning /host/earnings: session + canHost re-check (defense in depth), the OWNER-SCOPED read (booking joined to listing WHERE listing.hostId = session.user.id AND booking.status='requested', soonest-expiring first), venue-tz-safe window + expiry-deadline display, desktop shadcn Table (th scope=col: Space · When · Guest · Guest pays · Expires · Actions) + mobile RequestRow cards, the "No requests right now" empty state, container max-w-4xl, NO coral. Money is server-frozen quoted_total_cents via formatMoney (zero arithmetic).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/booking/request-countdown.tsx src/components/host/request-row.tsx "src/app/(host)/host/requests/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "host_id\|hostId" "src/app/(host)/host/requests/page.tsx"` shows the owner-scoped WHERE (host can only see own requests).
    - `grep -n "role=\"timer\"" src/components/booking/request-countdown.tsx` and a per-minute (60000 / 60_000) setInterval are present.
    - `grep -n "approveRequest\|declineRequest" src/components/host/request-row.tsx` shows the actions wired; Decline goes through a confirm dialog.
    - `npx tsc --noEmit` exits 0 and eslint is clean on the three files.
  </acceptance_criteria>
  <done>RequestCountdown + owner-scoped /host/requests inbox + RequestRow/RequestActions built; tsc+eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Pending-count nudge (dashboard + header) + booker requested/approved states</name>
  <files>src/app/(host)/host/page.tsx, src/app/(host)/host/layout.tsx, src/app/bookings/[id]/page.tsx</files>
  <read_first>
    - src/app/(host)/host/page.tsx (count() idiom 34-38, action-row Buttons 63-75)
    - src/app/(host)/host/layout.tsx (header nav links 49-61)
    - src/app/bookings/[id]/page.tsx (the status branch 74-87; the confirmed render 123-186 for the visual scale)
    - src/components/booking/request-countdown.tsx (Task 1 — reuse for the approved payment-window)
    - src/lib/payments/config.ts (APPROVAL_* hours for the copy), src/lib/venue-time.ts (venueTzNote)
    - .planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md (the new confirmation-state copy + status badges + accent rules)
  </read_first>
  <action>
    Add the pending-count nudge: in src/app/(host)/host/page.tsx compute a pending-request count (booking joined to listing WHERE hostId=session.user.id AND status='requested') with the existing count() idiom and add a neutral outline "Requests" Button (asChild Link → /host/requests) with a neutral secondary count badge (hidden when 0, aria-label "{n} requests to review") to the action row beside Your listings / Earnings; in src/app/(host)/host/layout.tsx add a neutral "Requests" nav Link (same underline styling) with the count badge between Earnings and Profile. Then branch src/app/bookings/[id]/page.tsx on the new statuses BEFORE the final notFound(): a `requested` branch rendering "Request sent" (Awaiting host secondary+Hourglass badge; window in venue-local; total labeled "You'll pay if approved" NOT "Total charged"; no pay CTA; the "you haven't been charged / usually within {SLA_HOURS} hours" reassurance) and an `approved` branch rendering "Your request was approved" (Approved outline+CalendarCheck badge — NOT --success; a payment-window RequestCountdown "Pay within {N}h {M}m"; a coral Pay now Button → /listings/${bk.listingId}/book?hold=${bk.id}). Optionally render calm declined/expired landings (muted badge + coral "Find another space" → /) instead of a bare 404. Keep confirmed / pending+?paid interstitial / cancelled+?paid reversed branches unchanged. Every displayed time names the venue timezone (SC#2); all money is server-frozen (zero arithmetic).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "src/app/(host)/host/page.tsx" "src/app/(host)/host/layout.tsx" "src/app/bookings/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "requested" "src/app/(host)/host/page.tsx" "src/app/(host)/host/layout.tsx"` shows the pending-count query + nudge in both.
    - `grep -n "book?hold\|book\\?hold" "src/app/bookings/[id]/page.tsx"` shows the approved-state coral Pay now routes to the reserve page; the requested branch has NO pay CTA.
    - The approved badge uses outline/CalendarCheck (NOT bg-success); requested uses secondary/Hourglass; both are icon + text (never color-only).
    - `npx tsc --noEmit` exits 0 and eslint is clean on the three files.
  </acceptance_criteria>
  <done>Pending-count nudge in dashboard + header; booker requested/approved states rendered per contract; tsc+eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (host) route group → inbox data | The layout gates canHost, but the DATA read must be owner-scoped (IDOR). |
| booker → confirmation page | The owner-gate (bookerId===userId), not the route group, protects the booking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-23 | Elevation | /host/requests read | mitigate | Owner-scope the read `WHERE listing.host_id = session.user.id`; re-check session + canHost in the RSC (defense in depth). The route group is NOT the gate (mirror /host/earnings, Security V4). |
| T-06-24 | Information Disclosure | booker confirmation states | mitigate | /bookings/[id] keeps the existing owner-gate (bookerId===userId → same 404 for missing vs not-mine); the new requested/approved branches read the same owner-gated row. |
| T-06-25 | Spoofing | approved-state Pay now | mitigate | The coral Pay now routes to the existing reserve/Confirm & pay page; confirmation remains the webhook's sole authority (06-05). The client never fabricates a confirmed state; the countdown is display-only. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the six files.
- `npx eslint` clean on the new/modified components + pages.
- `npx vitest run tests/booking tests/paymongo tests/payments` still green (no regressions).
- Visual/interaction verification is deferred to the 06-09 human-verify checkpoint.
</verification>

<success_criteria>
- Owner-scoped /host/requests inbox with Approve/Decline wired, venue-local window + expiry countdown, no coral.
- Pending-count nudge (dashboard + header), hidden at 0.
- /bookings/[id] renders requested + approved states per the UI contract (approved carries the coral Pay now + payment-window countdown; requested has no pay CTA and says "you haven't been charged").
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-08-SUMMARY.md` when done.
</output>
