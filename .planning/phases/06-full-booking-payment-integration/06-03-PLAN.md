---
phase: 06-full-booking-payment-integration
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/email.ts
  - src/app/actions/listing.ts
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - tests/listing/crud.test.ts
autonomous: true
requirements: [BOOK-04, BOOK-06]

must_haves:
  truths:
    - "src/lib/email.ts exports five thin plain-HTML lifecycle sends (booking confirmed, request received, request approved-pay-now, request declined/expired, new-request-to-host) — the first non-auth emails in the repo (D-66)"
    - "Every interpolated user field (space title, booker name, pay link) is escapeHtml'd before entering the markup (WR-01 injection sink closed)"
    - "createDraftListing mints a listing with bookingMode='instant' — the D-62 demand-first default flip (was 'request')"
    - "The listing-editor Step-5 mode toggle FormDescription surfaces the D-61 new-bookings-only rule (a mode change applies to new bookings; in-flight requests keep their creation-time mode)"
  artifacts:
    - path: "src/lib/email.ts"
      provides: "five lifecycle email sends"
      exports: ["sendBookingConfirmed", "sendRequestReceived", "sendRequestApproved", "sendRequestDeclined", "sendNewRequestToHost"]
    - path: "src/app/actions/listing.ts"
      provides: "createDraftListing default flipped to instant"
      contains: "instant"
  key_links:
    - from: "src/lib/email.ts sends"
      to: "the existing send() + escapeHtml() helpers"
      via: "extend, never rebuild; fire-and-forget at call sites (void send...)"
      pattern: "escapeHtml"
    - from: "src/app/actions/listing.ts createDraftListing"
      to: "listing.bookingMode = 'instant'"
      via: "D-62 create-code default (independent of the DB SET DEFAULT)"
      pattern: "bookingMode:\\s*\"instant\""
---

<objective>
Ship the five thin lifecycle emails over the existing `src/lib/email.ts` (D-66 — the first non-auth emails in the repo) and apply the D-62 create-code default flip (`createDraftListing` `request` → `instant`) plus the D-61 mode-toggle copy clarification in the listing editor.

Purpose: These are leaf providers consumed by the fork (06-04), approve/decline (06-07), the expiry sweep (06-06), and the webhook confirm (06-05). Landing them early in Wave 2 lets every consumer import stable, tested send functions. The D-62 default flip is grouped here because it is the demand-first "instant by default" behavior change (the DB SET DEFAULT lives in 06-01's migration; this is the create-code default).
Output: five new email exports (plain HTML, escapeHtml, fire-and-forget), createDraftListing default = instant, wizard Step-5 FormDescription copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md
@.planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md

<interfaces>
<!-- Existing patterns to mirror (verified 2026-07-20). -->

src/lib/email.ts — send(to, subject, html) + escapeHtml(s) + the two existing exports sendVerificationEmail/sendResetPassword (54-62). Dev fallback logs the body when RESEND_API_KEY unset; prod withholds the link. Fire-and-forget at call sites (auth.ts:73-81 uses `void sendResetPassword(...)`).
tests/helpers/mocks.ts — mockResend.sent() / .lastLink() capture every send (assert against these).

src/app/actions/listing.ts:70-83 — createDraftListing inserts { id, hostId, status:"draft", bookingMode:"request" }. Flip to "instant" (D-62); update the doc comment at 66-69 ("bookingMode to 'request'").

src/app/(host)/host/listings/[id]/edit/wizard.tsx:668-709 — the bookingMode RadioGroup (Instant book / Request to book, selected card border-primary). FormDescription at 707 currently "You can change this anytime." — expand to the D-61 rule. The default selection comes from the loaded listing.bookingMode (191), so no structural change is needed beyond copy.

## Email contract (06-UI-SPEC Lifecycle email contract + 06-RESEARCH Pattern 5)
Each send: plain HTML string (a heading line + body line + one anchor CTA), escapeHtml on space title / booker name / pay link, always name the venue timezone on times, amounts = server-frozen formatMoney. Signatures (keep args explicit + escaped):
- sendBookingConfirmed(to, spaceTitle, whenLabel, reference, bookingUrl)   — booker; subject "Your FitOut booking is confirmed — {space}"; CTA "View your booking" → /bookings/{id}
- sendRequestReceived(to, spaceTitle, whenLabel, bookingUrl)               — booker; "We sent your request — {space}"; CTA → /bookings/{id}; body notes SLA hours + "you haven't been charged"
- sendRequestApproved(to, spaceTitle, whenLabel, totalLabel, payUrl)       — booker; "Approved — pay to confirm {space}"; CTA "Pay now" → /listings/{listingId}/book?hold={id}; body notes payment-window hours
- sendRequestDeclined(to, spaceTitle, whenLabel, opts?: {expired?: boolean}) — booker; "Your request for {space} wasn't available"; declined vs expired body; CTA "Find another space" → /
- sendNewRequestToHost(to, spaceTitle, whenLabel, bookerLabel, totalLabel, requestsUrl) — host; "New booking request — {space}"; CTA "Review request" → /host/requests; body notes SLA hours
Reference APPROVAL_SLA_HOURS / APPROVAL_PAYMENT_WINDOW_HOURS (config.ts, 06-01) by value, as hours — never internal names. NEVER install React Email (not present; keep the send thin, D-66).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five lifecycle email sends to src/lib/email.ts</name>
  <files>src/lib/email.ts</files>
  <read_first>
    - src/lib/email.ts (send, escapeHtml, sendVerificationEmail/sendResetPassword — the exact shape to mirror)
    - tests/helpers/mocks.ts (mockResend.sent()/.lastLink() — the test capture surface)
    - src/lib/payments/config.ts (APPROVAL_SLA_HOURS / APPROVAL_PAYMENT_WINDOW_HOURS from 06-01 — reference by value)
    - .planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md (the Lifecycle email contract table — subjects, key body lines, CTAs, tone)
  </read_first>
  <action>
    Extend src/lib/email.ts with the five exports per the email contract in the interfaces block, each mirroring the sendVerificationEmail shape (a `send(to, subject, html)` call with a minimal heading + body line + one anchor CTA). Call escapeHtml on EVERY interpolated user field — space title, booker name/label, and the pay/booking URL (WR-01: an unescaped url in an href is an injection sink; mirror the existing `const safe = escapeHtml(url)` idiom). Reference the SLA / payment-window as their config-constant VALUES rendered as hours (never the internal names) and always name the venue timezone in any time label the caller passes. Keep the HTML minimal (no design-heavy templates — hardening/branding is Phase 7, WR-04). Do NOT change the dev-fallback / prod-withhold behavior of `send`. These are the sole email transport for BOOK-06 and the request lifecycle.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "export const send(BookingConfirmed|RequestReceived|RequestApproved|RequestDeclined|NewRequestToHost)" src/lib/email.ts` returns 5.
    - Every new export interpolates user data through escapeHtml (no raw `${spaceTitle}` / `${payUrl}` inside an href or HTML text without escaping).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Five lifecycle sends exported, all fields escaped, thin plain HTML, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: D-62 create-default flip + D-61 wizard mode-toggle copy + test</name>
  <files>src/app/actions/listing.ts, src/app/(host)/host/listings/[id]/edit/wizard.tsx, tests/listing/crud.test.ts</files>
  <read_first>
    - src/app/actions/listing.ts (createDraftListing 66-83 — the bookingMode:"request" default + doc comment)
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx (the bookingMode RadioGroup 668-709 + FormDescription 707)
    - tests/listing/crud.test.ts (the existing createDraftListing test to extend)
  </read_first>
  <action>
    Change createDraftListing's insert `bookingMode: "request"` → `bookingMode: "instant"` (D-62 demand-first default; existing listings unaffected — no production rows to backfill) and update its doc comment at 66-69 accordingly. In wizard.tsx, replace the Step-5 FormDescription "You can change this anytime." with the D-61 new-bookings-only rule copy from 06-UI-SPEC: "You can change this anytime. It applies to new bookings — any requests already in progress keep the mode they started under." (No structural change to the RadioGroup; the selected default derives from the loaded listing which is now instant by default.) Extend tests/listing/crud.test.ts with an assertion that createDraftListing produces a row with bookingMode === 'instant'.
  </action>
  <verify>
    <automated>npx vitest run tests/listing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'bookingMode: "instant"' src/app/actions/listing.ts` matches the createDraftListing insert.
    - `grep -n "applies to new bookings" "src/app/(host)/host/listings/[id]/edit/wizard.tsx"` shows the D-61 FormDescription copy.
    - `npx vitest run tests/listing` passes, including the new instant-default assertion.
  </acceptance_criteria>
  <done>createDraftListing defaults to instant (D-62); wizard copy states the D-61 rule; listing test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → email transport | Interpolated space title / booker name / pay link are untrusted content entering HTML markup. |
| server action → email send | An awaited/throwing send could block a webhook ACK or a server action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | email HTML interpolation | mitigate | escapeHtml every interpolated field (space title, booker name, pay/booking url) before it enters an href or HTML text (WR-01; mirror email.ts:25 pattern). |
| T-06-07 | Denial of Service | email send at call sites | mitigate | Callers (06-04/05/06/07) invoke these fire-and-forget (void send...); a Resend failure never rejects a 200 ACK or blocks an action (contract enforced in the consuming plans; documented here). |
| T-06-08 | Information Disclosure | dev-log fallback in prod | accept | Unchanged existing behavior — send() already withholds the link in production and only logs in dev/test (email.ts:40-48). Not modified this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after the email + listing edits.
- `npx vitest run tests/listing` green (instant-default assertion).
- `npx vitest run tests/booking tests/paymongo tests/payments` still green (no regressions to email-mocking suites).
</verification>

<success_criteria>
- Five lifecycle email exports present, all user fields escaped, plain-HTML thin sends.
- createDraftListing default = instant (D-62); wizard Step-5 copy states the D-61 new-bookings-only rule.
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-03-SUMMARY.md` when done.
</output>
