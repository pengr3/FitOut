---
phase: 06-full-booking-payment-integration
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/inngest/functions/request-expiry.ts
  - src/app/api/inngest/route.ts
  - tests/booking/request-expiry.test.ts
autonomous: true
requirements: [BOOK-05, PAY-05]

must_haves:
  truths:
    - "An Inngest cron requestExpirySweep auto-declines a requested hold past its SLA (requested → declined + slot freed + booker 'declined/expired' email) and auto-releases an approved hold past its payment window (approved → cancelled + slot freed) — the DB clock now() is the SOLE expiry authority (D-64, BOOK-05, PAY-05)"
    - "The lazy read predicates (06-02) ALSO free a lapsed requested/approved slot instantly between sweep ticks; the cron only drives the VISIBLE status flip + the email side-effect (Research Pattern 3 — do both)"
    - "The cron is a 2-arg createFunction singleton (concurrency:1) offset from the two payout crons (minute 15, vs 0 and 30) and mounted in the /api/inngest serve() functions[] alongside payoutSweep + payoutReconcile (Pitfall 4)"
    - "Nothing is ever refunded or voided on decline/expiry/release — the freed states (declined/cancelled) are simply non-occupying (D-63)"
  artifacts:
    - path: "src/inngest/functions/request-expiry.ts"
      provides: "queryExpired + expireOne + requestExpirySweep cron"
      contains: "requestExpirySweep"
    - path: "src/app/api/inngest/route.ts"
      provides: "serve() mounting requestExpirySweep"
      contains: "requestExpirySweep"
  key_links:
    - from: "src/inngest/functions/request-expiry.ts queryExpired"
      to: "requested/approved rows past expires_at (DB clock now())"
      via: "SELECT ... WHERE (status='requested' AND expires_at<=now()) OR (status='approved' AND expires_at<=now())"
      pattern: "expires_at <= now\\(\\)"
    - from: "src/app/api/inngest/route.ts serve()"
      to: "functions: [payoutSweep, payoutReconcile, requestExpirySweep]"
      via: "code-registered cron (Inngest re-syncs on deploy)"
      pattern: "requestExpirySweep"
---

<objective>
Add the dual-timer expiry sweep (D-64): an Inngest hourly cron that flips `requested` holds past their SLA to `declined` (+ booker email) and `approved` holds past their payment window to `cancelled`, freeing the slot in both cases. Mount it in the `/api/inngest` serve() alongside the payout crons. The lazy read predicates (06-02) already free the slot between ticks — this cron drives the visible flip + the email.

Purpose: BOOK-05 requires auto-expiry if the host doesn't respond; PAY-05 requires the slot to free on non-payment. Nothing is ever refunded or voided (D-63) — the freed states are simply non-occupying. Mirror payout-sweep.ts exactly (2-arg createFunction, DB-clock now(), per-row step.run).
Output: src/inngest/functions/request-expiry.ts (near-clone of payout-sweep), the serve() mount, and DB-clock-manipulation expiry tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md

<interfaces>
<!-- The cron to mirror (verified 2026-07-20). -->

src/inngest/functions/payout-sweep.ts — the near-clone source:
  - queryDuePayouts(dbConn) (69-92): a DB-clock now() sweep with an explicit dbConn param (so a test injects an isolated schema) + LIMIT batch. Mirror its shape for queryExpired.
  - payoutSweep createFunction (192-205): 2-arg form `createFunction({ id, concurrency:1, triggers:[{ cron:"TZ=Asia/Manila 0 * * * *" }] }, async ({ step }) => {...})` with a per-row step.run. (inngest 4.13.0 — the cron lives in options.triggers; the 3-arg skeleton in older docs is wrong, Pitfall 4.)

src/app/api/inngest/route.ts:31-34 — `serve({ client: inngest, functions: [payoutSweep, payoutReconcile] })`; import requestExpirySweep and append it. The fail-closed prod INNGEST_SIGNING_KEY guard (23-27) stays.

New request-expiry.ts shape:
  - queryExpired(dbConn): `SELECT id, status, listing_id AS "listingId", booker_id AS "bookerId" FROM booking WHERE (status='requested' AND expires_at <= now()) OR (status='approved' AND expires_at <= now()) ORDER BY expires_at ASC LIMIT 100`.
  - expireOne(dbConn, row): requested → `UPDATE booking SET status='declined', expires_at=NULL WHERE id=$id AND status='requested'` (idempotent; freeing is automatic — declined is non-occupying) + `void sendRequestDeclined(bookerEmail, title, whenLabel, { expired:true })`; approved → `UPDATE booking SET status='cancelled', expires_at=NULL WHERE id=$id AND status='approved'` (payment-window auto-release; optional email — Open Q1, fold under declined/expired or leave silent). To email you need booker email + listing title — one extra read (join booking→user + listing). Use `console.error("[request-expiry] ...")` in the payout-alert style if a send/read fails; never throw out of the step for an email failure.
  - requestExpirySweep = createFunction({ id:"request-expiry-sweep", concurrency:1, triggers:[{ cron:"TZ=Asia/Manila 15 * * * *" }] }, async ({ step }) => { const due = await step.run("find-expired", () => queryExpired(db)); for (const r of due) await step.run(`expire-${r.id}`, () => expireOne(db, r)); return { swept: due.length }; }).

Config: APPROVAL_SLA_HOURS / APPROVAL_PAYMENT_WINDOW_HOURS (06-01) are consumed at WRITE time (06-04/06-07 set expires_at); the sweep only reads expires_at <= now(). Email: sendRequestDeclined (src/lib/email.ts, 06-03).
Test: tests/booking/request-expiry.test.ts (scaffolded by 06-02; fill here). Mirror tests/payments/payout-sweep.test.ts (DB-clock manip: UPDATE ... expires_at = now() - interval).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: request-expiry.ts cron + /api/inngest serve() mount</name>
  <files>src/inngest/functions/request-expiry.ts, src/app/api/inngest/route.ts</files>
  <read_first>
    - src/inngest/functions/payout-sweep.ts (queryDuePayouts 69-92, payOne 109-184 for the per-row step pattern, payoutSweep createFunction 192-205 — the 2-arg form to mirror)
    - src/app/api/inngest/route.ts (serve() mount 31-34 + the prod INNGEST_SIGNING_KEY guard 23-27)
    - src/lib/email.ts (sendRequestDeclined from 06-03)
    - src/inngest/client.ts (the shared inngest client)
  </read_first>
  <action>
    Create src/inngest/functions/request-expiry.ts mirroring payout-sweep.ts: `queryExpired(dbConn)` selecting requested/approved bookings past expires_at (DB clock now(), explicit dbConn param for test injection, LIMIT 100, ORDER BY expires_at ASC); `expireOne(dbConn, row)` that, keyed off the row's status, either flips requested → declined (expires_at=NULL, scoped `AND status='requested'` so it is idempotent) and fires `void sendRequestDeclined(...)` with the expired variant, or flips approved → cancelled (scoped `AND status='approved'`; the payment-window auto-release — optional email per Open Q1, fold under the declined/expired template or leave silent). Load the booker email + listing title in expireOne (one extra read) for the send; a send/read failure logs `console.error("[request-expiry] ...")` and never throws out of the step. Export `requestExpirySweep = inngest.createFunction` in the 2-arg form with `id:"request-expiry-sweep"`, `concurrency:1`, and `triggers:[{ cron:"TZ=Asia/Manila 15 * * * *" }]` (minute 15 so it never contends with the payout crons at 0 and 30, Pitfall 4), iterating per-row with step.run. Then edit src/app/api/inngest/route.ts to import requestExpirySweep and append it to the serve() functions[] array (leave the fail-closed prod guard + runtime='nodejs' unchanged). Create route.ts's edit LAST-safe: the import resolves because request-expiry.ts already exists.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "expires_at <= now()" src/inngest/functions/request-expiry.ts` shows the DB-clock sweep predicate for both requested and approved.
    - `grep -n "TZ=Asia/Manila 15" src/inngest/functions/request-expiry.ts` shows the offset cron (minute 15).
    - `grep -n "requestExpirySweep" src/app/api/inngest/route.ts` shows it appended to the serve() functions[] array.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>request-expiry cron authored (2-arg, DB-clock, per-row step) + mounted in serve() alongside the payout crons.</done>
</task>

<task type="auto">
  <name>Task 2: SLA auto-decline + payment-window auto-release tests</name>
  <files>tests/booking/request-expiry.test.ts</files>
  <read_first>
    - tests/booking/request-expiry.test.ts (the 06-02 scaffold with it.todo placeholders)
    - tests/payments/payout-sweep.test.ts (the DB-clock manip idiom: UPDATE ... expires_at = now() - interval; setupTestDb; mockResend)
    - src/inngest/functions/request-expiry.ts (queryExpired/expireOne from Task 1)
  </read_first>
  <action>
    Fill tests/booking/request-expiry.test.ts (mirroring payout-sweep.test.ts): seed a `requested` booking, force its expires_at into the past (UPDATE ... expires_at = now() - interval), run queryExpired + expireOne, and assert it flips to `declined`, the slot is freed (a subsequent overlapping createPendingHold/insert succeeds — declined is non-occupying), and the booker declined/expired email fires (mockResend.sent()). Seed an `approved` booking past its window, run the sweep, and assert it flips to `cancelled` and the slot frees. Assert idempotency: a second sweep pass over an already-terminal row is a 0-row no-op (never re-sends / re-flips). Assert a NON-expired requested/approved row is NOT swept.
  </action>
  <verify>
    <automated>npx vitest run tests/booking/request-expiry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts requested→declined+email+slot-freed, approved→cancelled+slot-freed, sweep idempotency, and non-expired rows untouched.
    - `npx vitest run tests/booking/request-expiry.test.ts` passes.
  </acceptance_criteria>
  <done>Expiry sweep tests green — SLA auto-decline + payment-window auto-release + idempotency + selectivity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client countdown → expiry decision | The DB clock now() vs expires_at is the sole authority; the client countdown is a display cue only. |
| Inngest → serve endpoint | Prod requires INNGEST_SIGNING_KEY (fail-closed boot guard, unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering | expiry authority | mitigate | The sweep keys off SQL now() vs expires_at (DB clock), never a JS/client clock; every UPDATE is status-scoped + idempotent so a re-run is a 0-row no-op. |
| T-06-17 | Denial of Service | email/read failure in a step | mitigate | A send/read failure logs [request-expiry] and never throws out of the step; the status flip is the durable side-effect (lazy read predicates already free the slot regardless). |
| T-06-18 | Information Disclosure | Inngest serve endpoint | accept | Unchanged — the prod fail-closed INNGEST_SIGNING_KEY guard already gates the endpoint; dev/test tolerate its absence. |
</threat_model>

<verification>
- `npx vitest run tests/booking/request-expiry.test.ts` — SLA auto-decline + payment-window release + idempotency + selectivity green.
- `npx tsc --noEmit` clean.
- `npx vitest run tests/booking tests/paymongo tests/payments` still green (no cron/serve regressions to the payout crons).
</verification>

<success_criteria>
- requestExpirySweep flips requested→declined (+email) past SLA and approved→cancelled past the payment window, DB-clock-authoritative, idempotent.
- Mounted in serve() functions[] at cron minute 15 (offset from the payout crons).
- Nothing refunded/voided on expiry (freed states are non-occupying).
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-06-SUMMARY.md` when done.
</output>
