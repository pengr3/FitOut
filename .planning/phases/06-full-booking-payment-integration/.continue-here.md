---
context: phase
phase: 06-full-booking-payment-integration
task: 0
total_tasks: 0
status: ready_to_plan
last_updated: 2026-07-19T23:22:15.420Z
---

# Continue Here — Phase 6: Full Booking + Payment Integration

_No blocking constraints were discovered this session — the pause is a clean session boundary before planning, not a mid-task interruption._

<current_state>
Paused at the **pre-planning boundary**. A `/gsd-plan-phase 6 --research` run this session produced RESEARCH + VALIDATION, then correctly **halted at the UI design-contract gate** because Phase 6 is a frontend phase with no UI-SPEC. The user chose to generate the UI-SPEC first; `/gsd-ui-phase 6` then produced and verified `06-UI-SPEC.md` (6/6 dimensions).

All four planning-prep artifacts are committed on `dev`. **No PLAN.md files exist yet.** The working tree is clean; there are no blockers, no background processes, and no pending human actions.

Resume by running `/gsd-plan-phase 6` (no `--research`). The UI gate will now pass, and RESEARCH/VALIDATION will be reused.
</current_state>

<completed_work>

Completed this session (all committed on `dev`):
- **Research** — `06-RESEARCH.md` (`543b240`). Resolved the `requested`/`approved` state model, the 8-site occupancy-predicate audit, the 3-file migration sequence, the 5 lifecycle emails, the Inngest dual-timer sweep, and the security domain. Zero new dependencies.
- **Validation strategy** — `06-VALIDATION.md` (`8eee68b`). Nyquist test map; Wave-0 test files; the concurrent-double-book race on a held/approved slot is the non-negotiable correctness gate. (The "TBD" task-IDs are intentional placeholders — planning fills them in.)
- **UI design contract** — `06-UI-SPEC.md` (`f2e5043`), verified 6/6 and marked approved (`bd5f8d1`). Design system inherited byte-identical from phases 02–05.
</completed_work>

<remaining_work>

- **Plan** — `/gsd-plan-phase 6` → planner writes PLAN.md files from CONTEXT + RESEARCH + VALIDATION + UI-SPEC.
- **Verify** — plan-checker loop (max 3 iterations).
- **Coverage gates** — requirements (BOOK-04, BOOK-05, BOOK-06, PAY-05, HOST-01) + decisions (D-61..D-66). Handle manually — the `check.*` SDK subcommands don't exist here (see Required Reading).
- **Record** — planning completion in STATE.md + ROADMAP wave-dependency annotation (edit files directly).
- **Execute** — `/gsd-execute-phase 6`.
</remaining_work>

<decisions_made>

- **Generate the UI-SPEC before planning** (vs `--skip-ui`) — Phase 6 is a frontend phase and every prior frontend phase (02–05) has a UI-SPEC; the UI safety gate enforces it. User confirmed.
- **Request lifecycle = two new `booking_status` enum values `requested` + `approved`** (keep `pending` only for the 15-min instant hold) — unambiguous per-status TTLs; one-line webhook confirm WHERE widening.
- **Decline behind a confirm dialog, Approve inline, both neutral** (not destructive-red) — a decline is an expected outcome, matching the Phase-5 badge precedent. Locked the D-65 UI discretion item.
- **Pay-on-approval is locked (D-63)** — do NOT re-open the authorize/capture-vs-pay-on-approval debate; it is settled and the ROADMAP/REQUIREMENTS are corrected to match.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-full-booking-payment-integration/06-CONTEXT.md` — the authoritative user decisions (D-61..D-66) + the "Claude's Discretion" items.
2. `.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md` — **§ Occupancy-Predicate Audit is the load-bearing artifact** (the 8 sites to widen in lockstep) + the 3-file migration sequence.
3. `.planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md` — the locked visual/interaction contract for the planner.
4. `.planning/phases/06-full-booking-payment-integration/06-VALIDATION.md` — the test map the planner must satisfy.
5. Memory `gsd-tools-invocation` — the GSD SDK here is `node C:/Users/franc/.claude/get-shit-done/bin/gsd-tools.cjs <cmd>`; many workflow-assumed subcommands (`check.*`, `roadmap.*`, `state.planned-phase`, `agent-skills`, `config-get`, `gap-analysis`) do NOT exist — read fields from `init plan-phase 6` JSON and handle the coverage/state/annotate gates by editing files directly.

## Critical Anti-Patterns (do NOT repeat these)
- **Widening the occupancy predicate at only some sites** → widen ALL 8 (RESEARCH § Occupancy-Predicate Audit) in lockstep, or you get a silent double-book or a paid-but-unconfirmed booking.
- **Altering the EXCLUDE constraint's WHERE in place / adding an enum value and using it in the same transaction** → use the 3-file migration sequence (0010 enum → 0011 columns → 0012 EXCLUDE recreate).
- **Adding a second confirm writer for request-to-book** → the `payment.paid` webhook is the sole confirm authority; widen its WHERE to `IN ('pending','approved')`.
- **Re-adding a QRPh guard on request-to-book** → under pay-on-approval nothing reverses, so all rails work; the old QRPh exclusion is dissolved.

## Infrastructure State
- Git: branch `dev`, clean working tree. Ahead of `origin/dev` by 5 commits before this WIP handoff commit. **User requested a push to origin after pausing** — pushed to `origin/dev`.
- No running dev servers / watchers / background jobs from this session.
- No new env vars needed yet (planning adds `APPROVAL_SLA_HOURS` + `APPROVAL_PAYMENT_WINDOW_HOURS` with defaults during execution).

<context>
This was a well-behaved plan-phase run that stopped exactly where it should (the UI gate). The heavy thinking is already captured in the four committed artifacts, so resuming is low-risk: just run the planner. The one thing to guard obsessively during planning and execution is the double-booking guarantee — every new slot-holding state (`requested`, `approved`) must be in the GiST exclusion's occupying set and every occupancy read predicate, or the core value of the whole product breaks.
</context>

<next_action>
Start with: `/clear`, then `/gsd-plan-phase 6` (no `--research`). Confirm the UI gate passes (it should find `06-UI-SPEC.md`) and the planner proceeds to write PLAN.md files.
</next_action>
