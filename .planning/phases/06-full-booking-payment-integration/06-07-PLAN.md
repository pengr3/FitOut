---
phase: 06-full-booking-payment-integration
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - src/app/actions/host-requests.ts
  - tests/booking/request-lifecycle.test.ts
autonomous: true
requirements: [HOST-01, BOOK-05, PAY-05]

must_haves:
  truths:
    - "A host can approve a pending request within the SLA: an owner-gated, atomic, DB-clock-guarded UPDATE (status='approved', expires_at=now()+APPROVAL_PAYMENT_WINDOW_HOURS WHERE status='requested' AND expires_at>now()) — a host clicking approve one second past the SLA is refused server-side (HOST-01, D-64)"
    - "On approval the booker is emailed a pay-now link to /listings/[id]/book?hold=<id> (the pay-on-approval trigger); on decline the booker is emailed and the slot frees automatically (declined is non-occupying) (PAY-05, BOOK-05, D-66)"
    - "Approve/decline re-verify listing.hostId === session.user.id BEFORE any UPDATE (IDOR / Security V4) — the (host) route group is NOT the gate; a missing row and a cross-host row return the SAME calm denial (leak nothing)"
    - "The /host/requests owner-scoped READ (WHERE listing.host_id = session.user.id AND booking.status='requested') is committed to an automated cross-host isolation assertion here (host-A sees only A's requested rows) — the same predicate 06-08's inbox page consumes (Security V4; the read-path IDOR, not just the action-path)"
    - "A 0-row approve/decline (already actioned or lapsed) returns a calm 'no longer pending' result, never a 500"
  artifacts:
    - path: "src/app/actions/host-requests.ts"
      provides: "approveRequest + declineRequest owner-gated server actions"
      exports: ["approveRequest", "declineRequest"]
  key_links:
    - from: "src/app/actions/host-requests.ts approveRequest"
      to: "atomic SLA-guarded UPDATE + sendRequestApproved (pay link)"
      via: "owner-gate join listing.hostId first, then AND status='requested' AND expires_at>now()"
      pattern: "expires_at > now\\(\\)"
    - from: "approve/decline"
      to: "revalidatePath('/host/requests') + revalidatePath('/host')"
      via: "D-65 freshness (no websockets/polling)"
      pattern: "revalidatePath"
    - from: "tests/booking/request-lifecycle.test.ts owner-scope READ case"
      to: "the owner-scoped inbox SELECT (WHERE listing.host_id = <A> AND status='requested')"
      via: "committed cross-host isolation assertion (Security V4; the predicate 06-08 reuses)"
      pattern: "host_id|hostId"
---

<objective>
Add the owner-gated, atomic, SLA-guarded `approveRequest` / `declineRequest` server actions (HOST-01) that drive the pay-on-approval fork: approve → `approved` + booker pay-now email; decline → `declined` + booker email + slot freed. The DB clock is the sole SLA authority; the `(host)` route group is never the gate. Commit the `/host/requests` owner-scope READ isolation to an automated test here (the predicate 06-08's inbox page consumes).

Purpose: This is the host side of BOOK-05/PAY-05. It clones the ownership-gate skeleton from blocks.ts and the atomic-guard idiom from confirmBooking. Freeing the slot on decline is automatic (declined is non-occupying, 06-02). The pay-now email link routes to the /book page (06-04 made it accept an approved hold).
Output: src/app/actions/host-requests.ts (approve/decline) + approve/decline/owner-gate/SLA + the non-optional owner-scope READ isolation tests in request-lifecycle.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-CONTEXT.md
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md
@.planning/phases/06-full-booking-payment-integration/06-UI-SPEC.md

<interfaces>
<!-- Analogs to clone (verified 2026-07-20). -->

src/app/actions/blocks.ts:1-54 — the "use server" header, the SECURITY CONTRACT comment block, requireUserId() (37-40), assertOwnership(listingId, userId) (46-54, returns the listing row or null), the AvailabilityResult discriminated union (32-34). Clone this skeleton but join booking→listing for the ownership check.
src/app/actions/booking.ts:172-184 — confirmBooking's owner-gate idiom: load the row, `if (!bk || bk.bookerId !== userId) return { ...denied }` (same calm denial for missing vs not-mine). Mirror for host ownership: `if (!row || row.hostId !== userId) return { ...denied }`.
src/app/actions/booking.ts:203-212 — the rate-limit + recordAudit(WR-06) idiom for a money-adjacent action (reuse on the approve — planner's call, recommended).

Approve (atomic + SLA-guarded, mirror booking.ts:228-230):
  UPDATE booking SET status='approved', expires_at = now() + make_interval(hours => ${APPROVAL_PAYMENT_WINDOW_HOURS})
  WHERE id = ${requestId} AND status = 'requested' AND expires_at > now() RETURNING id
  0 rows → calm "this request is no longer pending" (never 500). ≥1 row → void sendRequestApproved(bookerEmail, title, whenLabel, totalLabel, `${base}/listings/${listingId}/book?hold=${requestId}`).
Decline:
  UPDATE booking SET status='declined', expires_at=NULL WHERE id=${requestId} AND status='requested' RETURNING id
  ≥1 row → void sendRequestDeclined(bookerEmail, title, whenLabel). Freeing the slot is automatic (declined non-occupying).
After each mutation: revalidatePath('/host/requests') + revalidatePath('/host') (D-65 freshness).

Owner-scope READ (the /host/requests inbox predicate, tested here, consumed by 06-08's page.tsx):
  SELECT booking.* FROM booking JOIN listing ON listing.id = booking.listing_id
  WHERE listing.host_id = ${hostId} AND booking.status = 'requested' ORDER BY booking.expires_at ASC
  The cross-host isolation test asserts host-A's read returns ONLY A's rows (never host-B's). 06-08's inbox page MUST use this exact predicate.

Config: APPROVAL_PAYMENT_WINDOW_HOURS (06-01). Emails: sendRequestApproved / sendRequestDeclined (06-03, fire-and-forget). base = process.env.BETTER_AUTH_URL ?? "http://localhost:3000" (the established app-URL convention, booking.ts:234).
Owner-gate needs booker email + listing title/hostId/timezone — one joined read (booking → listing → user) BEFORE the UPDATE.
Test: tests/booking/request-lifecycle.test.ts (created 06-02, extended 06-04; extend again here — DIFFERENT WAVE from 06-04 so no same-wave conflict).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: approveRequest + declineRequest owner-gated server actions</name>
  <files>src/app/actions/host-requests.ts</files>
  <read_first>
    - src/app/actions/blocks.ts (the owner-gate skeleton 1-54: use-server header, requireUserId, assertOwnership, the result union)
    - src/app/actions/booking.ts (the owner-gate idiom 172-184; the atomic-UPDATE idiom 228-230; the rate-limit + recordAudit WR-06 idiom 203-212)
    - src/lib/email.ts (sendRequestApproved, sendRequestDeclined from 06-03)
    - src/lib/payments/config.ts (APPROVAL_PAYMENT_WINDOW_HOURS from 06-01)
    - src/lib/db/schema.ts (booking + listing join fields — hostId, title, timezone, bookerId)
  </read_first>
  <action>
    Create src/app/actions/host-requests.ts ("use server") with a discriminated-union result type (ok:true | ok:false with a calm reason/error, mirroring AvailabilityResult). Both actions: requireUserId() (session gate) → load the request row JOINED to its listing (and the booker's email) in one read → owner-gate `if (!row || row.hostId !== userId) return the SAME calm denial` (missing vs cross-host indistinguishable — IDOR / Security V4; the (host) route group is NOT the gate). approveRequest: run the atomic SLA-guarded UPDATE (status='approved', expires_at = now() + make_interval(hours => APPROVAL_PAYMENT_WINDOW_HOURS), WHERE id=$id AND status='requested' AND expires_at > now(), RETURNING id) — 0 rows → calm "this request is no longer pending" (already actioned/lapsed, never a 500); on ≥1 row `void sendRequestApproved(bookerEmail, title, whenLabel, totalLabel, `${base}/listings/${listingId}/book?hold=${requestId}`)`. declineRequest: UPDATE status='declined', expires_at=NULL WHERE id=$id AND status='requested' RETURNING id → on ≥1 row `void sendRequestDeclined(...)` (freeing the slot is automatic — declined is non-occupying). After each mutation revalidatePath('/host/requests') + revalidatePath('/host') (D-65). Add rate-limit + recordAudit on the approve (money-adjacent, WR-06 — mirror confirmBooking). All emails fire-and-forget. Nothing is ever refunded/voided (D-63).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hostId !== userId\|row.hostId" src/app/actions/host-requests.ts` shows the owner-gate re-check before any UPDATE.
    - `grep -n "expires_at > now()" src/app/actions/host-requests.ts` shows the atomic SLA guard on approve.
    - `grep -cE "revalidatePath\('/host/requests'\)|revalidatePath\('/host'\)" src/app/actions/host-requests.ts` shows both revalidations.
    - `grep -n "sendRequestApproved\|sendRequestDeclined" src/app/actions/host-requests.ts` shows the fire-and-forget lifecycle emails.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>approveRequest + declineRequest are owner-gated, atomic, SLA-guarded, email + revalidate; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Approve/decline atomicity + SLA guard + owner-gate + owner-scope READ tests</name>
  <files>tests/booking/request-lifecycle.test.ts</files>
  <read_first>
    - tests/booking/request-lifecycle.test.ts (the harness from 06-02/06-04 — extend; seed a host-A request + a second host-B + a booker)
    - src/app/actions/host-requests.ts (approveRequest/declineRequest from Task 1)
    - tests/booking/state-machine.test.ts (the vi.doMock action-driving + mockResend idioms)
  </read_first>
  <action>
    Extend tests/booking/request-lifecycle.test.ts with: (a) approve on a valid `requested` row → status='approved', expires_at ≈ now()+24h, and sendRequestApproved fires with a pay link containing hold=<id> (mockResend.lastLink()); (b) SLA guard — force the request's expires_at into the past, call approve → 0 rows / calm "no longer pending", status unchanged (a host cannot approve a lapsed request); (c) decline → status='declined', slot freed (a subsequent overlapping hold succeeds), sendRequestDeclined fires; (d) owner-gate (Security V4) — host-B (a DIFFERENT host) calling approve/decline on host-A's request produces NO state change and the SAME calm denial as a missing id; (e) idempotency — a second approve/decline on an already-actioned row is a calm 0-row no-op; (f) REQUIRED, NON-OPTIONAL — the /host/requests owner-scope READ isolation (Security V4): seed host-A with a `requested` row and host-B with a `requested` row, run the exact owner-scoped inbox SELECT (`SELECT booking.* FROM booking JOIN listing ON listing.id = booking.listing_id WHERE listing.host_id = <A> AND booking.status = 'requested'`), and assert it returns ONLY host-A's row (never host-B's) — the read that 06-08's inbox page consumes. This closes the read-path info-disclosure IDOR (the action-layer owner-gate in (d) closes the state-change IDOR). Maps to VALIDATION.md row "`/host/requests` owner-scope: host A sees only A's requests".
  </action>
  <verify>
    <automated>npx vitest run tests/booking/request-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts approve-happy (+pay-link email), SLA-guard refusal on a lapsed request, decline (+email+slot-freed), cross-host owner-gate denial (no state change), idempotent 0-row no-op, AND the non-optional /host/requests owner-scope READ isolation (host-A's owner-scoped inbox SELECT returns only A's requested rows, never host-B's).
    - `grep -n "host_id\|hostId" tests/booking/request-lifecycle.test.ts` shows the committed owner-scope READ assertion.
    - `npx vitest run tests/booking/request-lifecycle.test.ts` passes with the 06-02/06-04 cases still green.
  </acceptance_criteria>
  <done>Approve/decline/SLA-guard/owner-gate/idempotency + the non-optional /host/requests owner-scope READ isolation tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host client → approve/decline action | The host may act on any request id; the action must re-verify ownership (IDOR). |
| host client → /host/requests read | The inbox read must be owner-scoped; a route group is not the gate (read-path IDOR). |
| host client countdown → approve | The DB clock is the SLA authority; a lapsed approve must be refused server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Elevation | approve/decline IDOR | mitigate | Load the request joined to its listing and verify listing.hostId === session.user.id BEFORE any UPDATE; missing vs cross-host return the SAME calm denial (leak nothing). The (host) route group is NOT the gate (Security V4). |
| T-06-19b | Information Disclosure | /host/requests read IDOR | mitigate | The owner-scoped inbox READ (`WHERE listing.host_id = session.user.id`) is committed to a NON-optional cross-host isolation test here (host-A sees only A's requested rows) — the same predicate 06-08's page consumes; worst-case leak of another host's request row is closed at the read path, not just the action path. |
| T-06-20 | Tampering | approve a lapsed request | mitigate | The atomic UPDATE carries `AND status='requested' AND expires_at > now()` (DB clock) — a host acting one second past the SLA claims 0 rows → calm "no longer pending", never a confirm. |
| T-06-21 | Tampering | double-action / replay | mitigate | Every UPDATE is status-scoped (`AND status='requested'`) so a second approve/decline is a 0-row no-op; the approve is rate-limited + audited (WR-06). |
| T-06-22 | Denial of Service | email send blocking the action | mitigate | sendRequestApproved/Declined are fire-and-forget (void); a Resend failure never blocks or fails the action. |
</threat_model>

<verification>
- `npx vitest run tests/booking/request-lifecycle.test.ts` — approve/decline/SLA/owner-gate/idempotency + owner-scope READ isolation green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- approveRequest: owner-gated, atomic, SLA-guarded (expires_at>now()), sets approved + 24h window, emails pay-now link.
- declineRequest: owner-gated, sets declined, frees the slot, emails the booker.
- Cross-host approve/decline is denied with no state change; lapsed approve refused.
- The /host/requests owner-scope READ isolation is proven by a committed (non-optional) cross-host test (host-A sees only A's requested rows).
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-07-SUMMARY.md` when done.
</output>
