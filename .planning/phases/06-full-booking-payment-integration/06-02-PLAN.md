---
phase: 06-full-booking-payment-integration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/availability/read-model.ts
  - src/lib/availability/units.ts
  - tests/booking/request-lifecycle.test.ts
  - tests/booking/request-expiry.test.ts
autonomous: true
requirements: [BOOK-05, PAY-05]

must_haves:
  truths:
    - "Every occupancy read predicate treats requested + approved (unexpired) as occupying — calendar, search, and the hold-probe all see a request-held slot as taken (D-63 correctness note; the occupancy-predicate fan-out)"
    - "A concurrent overlapping insert against a requested OR approved slot is rejected at the DB (23P01/40P01) — exactly one row survives (the non-negotiable phase correctness gate; BOOK-05/BOOK-03)"
    - "createPendingHold is PARAMETERIZED by { holdStatus, ttlMs, bookingMode } with pending/15-min defaults so the instant branch is byte-for-byte unchanged and the request branch can mint a requested hold (D-63)"
    - "The in-tx stale-hold sweep frees lapsed requested/approved rows so a fresh hold can reclaim a just-lapsed request's slot in the same transaction"
  artifacts:
    - path: "src/lib/availability/units.ts"
      provides: "parameterized createPendingHold + widened findOwnActiveHold/pickLowestFreeUnit/in-tx sweep"
      contains: "holdStatus"
    - path: "tests/booking/request-lifecycle.test.ts"
      provides: "Wave-0 scaffold + the concurrent-double-book race on requested/approved slots"
      contains: "makeRacingClients"
    - path: "tests/booking/request-expiry.test.ts"
      provides: "Wave-0 scaffold for the SLA/payment-window sweep tests"
      contains: "expires_at"
  key_links:
    - from: "src/lib/availability/read-model.ts:108"
      to: "the widened occupancy predicate"
      via: "status IN ('pending','requested','approved') AND expires_at > now()"
      pattern: "requested.*approved"
    - from: "src/lib/availability/units.ts createPendingHold"
      to: "CreatePendingHoldInput.holdStatus/ttlMs"
      via: "parameterized status + TTL (defaults keep instant unchanged)"
      pattern: "holdStatus"
---

<objective>
Widen the occupancy-predicate fan-out (the single most dangerous cross-cutting concern of the phase) so `requested` and `approved` are slot-holding states everywhere the codebase reads occupancy, and parameterize `createPendingHold` so the request branch (06-04) can mint a `requested` hold without forking the intricate SAVEPOINT/retry transaction. Land the Wave-0 test scaffolds, including the non-negotiable concurrent-double-book race on a requested/approved slot.

Purpose: With 06-01's live EXCLUDE now including all four statuses, the application-side lazy-read predicates (read-model + units) must widen in LOCKSTEP — miss one and the calendar/search show a held slot as free (silent collision). The race test proves the DB-level guarantee holds for the new states.
Output: widened read-model.ts + units.ts, parameterized createPendingHold, two Wave-0 test files with the race gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md
@.planning/phases/06-full-booking-payment-integration/06-VALIDATION.md

<interfaces>
<!-- The exact sites to widen (Occupancy-Predicate Audit, verified 2026-07-20). -->

src/lib/availability/read-model.ts:108 — occupancy predicate inside getAvailability (reused by search Stage-2):
  was: (status = 'confirmed' OR (status = 'pending' AND expires_at > now()))
  →    (status = 'confirmed' OR (status IN ('pending','requested','approved') AND expires_at > now()))
  Keep the comment (100-104) truthful: requested/approved now occupy; cancelled/declined/completed never do. Widening this ONE site covers search transitively.

src/lib/availability/units.ts:154 (findOwnActiveHold) and :178 (pickLowestFreeUnit) — same time-bounded predicate as read-model:108; widen both identically.
src/lib/availability/units.ts:221-224 (in-tx stale sweep) — was: status = 'pending' AND expires_at <= now(); widen the status set to IN ('pending','requested','approved') so a just-lapsed request frees within the same tx.
src/lib/availability/units.ts:72 (createBooking legacy probe, IN ('pending','confirmed')) — DORMANT (no live caller). Widen for defense-in-depth/consistency or leave a one-line note; not load-bearing.

src/lib/availability/units.ts CreatePendingHoldInput (122-131) — add optional { holdStatus?: 'pending'|'requested'; ttlMs?: number; bookingMode?: 'instant'|'request' }. Defaults: holdStatus 'pending', ttlMs = HOLD_TTL_MS (15 min). The insert at 238-250 hardcodes status:"pending" (245) + expiresAt = now()+HOLD_TTL_MS (228) — swap to the parameters (default preserves instant behavior). Persist bookingMode into the booking.bookingMode snapshot column (06-01) when provided.
HoldResult / HoldSuccess (134-135) unchanged.

Test harness to clone: tests/booking/state-machine.test.ts (real actions via vi.doMock, mockPayMongo, mockResend, redirect-capture). tests/payments/payout-sweep.test.ts (DB-clock manip UPDATE ... expires_at = now() - interval). Helpers: tests/helpers/db.ts (setupTestDb / makeRacingClients — independent connections; makeClient is max:1 and serializes, proving nothing for a race). tests/helpers/mocks.ts (mockPayMongo, mockResend.sent()/.lastLink()).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — the concurrent-double-book race on requested/approved slots</name>
  <files>tests/booking/request-lifecycle.test.ts, tests/booking/request-expiry.test.ts</files>
  <read_first>
    - tests/booking/state-machine.test.ts (the vi.doMock harness, redirect-capture, seeding a bookable listing + host + booker)
    - tests/payments/payout-sweep.test.ts (the makeRacingClients + DB-clock-manip idioms to mirror)
    - tests/helpers/db.ts (setupTestDb, makeRacingClients — independent connections for a genuine race; the isolated schema replays drizzle/0005..0012)
    - tests/helpers/mocks.ts (mockPayMongo, mockResend)
    - src/lib/availability/units.ts (createPendingHold, the 23P01/40P01 mapping)
  </read_first>
  <behavior>
    - A direct INSERT of a `requested` booking on unit 1, then a concurrent overlapping INSERT (via makeRacingClients) on the same listing/unit/window → the second is rejected (23P01 exclusion_violation OR 40P01 deadlock_detected); exactly one row survives. (BOOK-05/BOOK-03 — the non-negotiable gate.)
    - Same for an `approved` slot.
    - A `requested` (unexpired) row makes getAvailability report the slot occupied (not free); a `requested` row past expires_at reads as FREE (lazy expiry).
    - createPendingHold with { holdStatus:'requested', ttlMs: 24h } mints a booking row with status='requested' and expires_at ≈ now()+24h; the default call (no params) still mints status='pending' expires_at ≈ now()+15min.
  </behavior>
  <action>
    Create tests/booking/request-lifecycle.test.ts and tests/booking/request-expiry.test.ts. In request-lifecycle.test.ts, clone the state-machine.test.ts harness (seed a bookable request-mode listing, a host, a booker; the vi.doMock harness is reused by 06-04/06-07 which extend this file in later waves). Write the correctness-gate tests NOW: (a) concurrent overlapping insert on a `requested` slot rejected at the DB — assert the loser raises 23P01 or 40P01 and exactly one booking row exists for the window (use makeRacingClients, NOT makeClient); (b) the same on an `approved` slot; (c) getAvailability sees an unexpired `requested` slot as occupied and a past-`expires_at` `requested` slot as free; (d) createPendingHold parameterization — a { holdStatus:'requested', ttlMs } call mints status='requested' with the longer TTL, and the default call is unchanged pending/15-min. Scaffold request-expiry.test.ts with the setupTestDb boilerplate + `it.todo` placeholders for the SLA auto-decline + payment-window auto-release sweeps (06-06 fills them). These tests are RED until Task 2 widens the predicates + parameterizes createPendingHold.
  </action>
  <verify>
    <automated>npx vitest run tests/booking/request-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "makeRacingClients" tests/booking/request-lifecycle.test.ts` shows the race harness is used (not the serializing makeClient).
    - The file asserts exactly-one-survivor on a requested AND an approved slot (two race cases).
    - Both test files exist and the suite is collectable (no import errors); the race/param tests are RED before Task 2 and GREEN after.
  </acceptance_criteria>
  <done>request-lifecycle.test.ts carries the requested+approved race gate + createPendingHold-param tests; request-expiry.test.ts scaffolded.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the occupancy fan-out + parameterize createPendingHold</name>
  <files>src/lib/availability/read-model.ts, src/lib/availability/units.ts</files>
  <read_first>
    - src/lib/availability/read-model.ts (occupancy predicate 100-110)
    - src/lib/availability/units.ts (findOwnActiveHold 147-163, pickLowestFreeUnit 171-184, in-tx sweep 219-224, createPendingHold 191-282, CreatePendingHoldInput 122-131, HOLD_TTL_MS 117, the insert 236-250)
    - drizzle/0012_booking_exclusion_v2.sql (confirm the live EXCLUDE now matches the app predicates)
  </read_first>
  <action>
    Widen read-model.ts:108 to `(status = 'confirmed' OR (status IN ('pending','requested','approved') AND expires_at > now()))` and keep the 100-104 comment truthful (requested/approved occupy; cancelled/declined/completed do not). Widen the identical predicate in units.ts findOwnActiveHold (154) and pickLowestFreeUnit (178). Widen the in-tx stale sweep (221-224) status set to `IN ('pending','requested','approved') AND expires_at <= now()`. Widen (or add a dormancy note to) the dormant createBooking probe (72). Then parameterize createPendingHold: extend CreatePendingHoldInput with optional `holdStatus?: 'pending'|'requested'`, `ttlMs?: number`, `bookingMode?: 'instant'|'request'`; default holdStatus to 'pending' and ttlMs to HOLD_TTL_MS so the existing instant call site (booking.ts placeHold) is unchanged; use the parameters in the insert (status + expiresAt = now()+ttlMs) and persist bookingMode into the booking.bookingMode snapshot column when provided. Do NOT fork the SAVEPOINT/40P01-outer-retry/idempotency machinery — parameterize in place (Research "Don't Hand-Roll"). Keep all binds as ISO strings for raw sql (Pitfall 3, established convention).
  </action>
  <verify>
    <automated>npx vitest run tests/booking tests/paymongo tests/payments</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requested" src/lib/availability/read-model.ts src/lib/availability/units.ts` shows the widened predicates in both files.
    - `grep -n "holdStatus" src/lib/availability/units.ts` shows the parameter is declared and used in the insert.
    - The race + param tests from Task 1 are GREEN; the full quick suite (`tests/booking tests/paymongo tests/payments`) stays green (instant-hold behavior unregressed).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All lazy-read occupancy predicates widened in lockstep; createPendingHold parameterized; race gate green; instant flow unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application read predicates → occupancy truth | A read predicate that omits requested/approved shows a held slot as free → booker collides. |
| concurrent bookers → the same slot | Two racing inserts must not both win — the DB EXCLUDE, not app code, arbitrates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | read-model.ts + units.ts occupancy predicates | mitigate | Widen all lazy-read predicates to include requested/approved (unexpired) in lockstep with 06-01's EXCLUDE; prove with the getAvailability occupied/free assertions. |
| T-06-04 | Tampering | concurrent double-book on requested/approved | mitigate | The GiST EXCLUDE (widened in 06-01) rejects the loser; the makeRacingClients race test asserts exactly-one-survivor — the non-negotiable phase gate. |
| T-06-05 | Tampering | createPendingHold parameterization | mitigate | Parameterize in place (never fork) so the intricate SAVEPOINT/40P01-retry/idempotency logic is not re-implemented and previously-solved races (04-04 findings) are not re-opened. |
</threat_model>

<verification>
- `npx vitest run tests/booking/request-lifecycle.test.ts` — requested + approved race gates green.
- `npx vitest run tests/booking tests/paymongo tests/payments` — full quick suite green (instant flow unregressed).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- read-model.ts:108 + units.ts (154/178/221-224) widened to include requested/approved.
- createPendingHold accepts { holdStatus, ttlMs, bookingMode } with pending/15-min defaults.
- The concurrent-double-book race on requested AND approved slots rejects the loser (exactly one survivor).
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-02-SUMMARY.md` when done.
</output>
