---
phase: 06-full-booking-payment-integration
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - src/app/api/paymongo/webhook/route.ts
  - tests/paymongo/webhook-payment-paid.test.ts
autonomous: true
requirements: [BOOK-06, PAY-05]

must_haves:
  truths:
    - "The checkout_session.payment.paid webhook — the SINGLE confirm writer (D-57) — confirms BOTH an instant hold (from pending) and a pay-on-approval request (from approved): the confirm UPDATE WHERE widens from status='pending' to status IN ('pending','approved') (PAY-05)"
    - "On a successful confirm (≥1 row) the webhook fires the BOOK-06 booking-confirmed email to the booker, fire-and-forget, outside the 200 ACK path (covers instant AND pay-on-approval)"
    - "A payment.paid for a released/declined request (0-row confirm) still routes to the UNCHANGED handleGoneSlot D-58 backstop (auto-refund on refundable rails / operator-alert on QRPh) — the pay-after-release race, never a silent money retention (Pitfall 3)"
  artifacts:
    - path: "src/app/api/paymongo/webhook/route.ts"
      provides: "widened confirm WHERE + confirmed-email send"
      contains: "IN ('pending','approved')"
  key_links:
    - from: "src/app/api/paymongo/webhook/route.ts confirm UPDATE"
      to: "status IN ('pending','approved')"
      via: "single writer — widen the WHERE, never add a second confirm path"
      pattern: "IN \\('pending','approved'\\)"
    - from: "the ≥1-row confirm branch"
      to: "sendBookingConfirmed (booker)"
      via: "void send... outside the ACK path (one extra read for email + title)"
      pattern: "sendBookingConfirmed"
---

<objective>
Widen the single-writer `checkout_session.payment.paid` confirm authority (D-57) so a pay-on-approval request confirms through the SAME handler as an instant booking (PAY-05), and fire the BOOK-06 booking-confirmed email on a successful confirm. No second confirm writer; the D-58 gone-slot backstop is unchanged and already covers the pay-after-release race.

Purpose: Pay-on-approval is "the Phase-5 checkout gated behind host approval" (D-63). The only webhook change is one predicate widening (`pending` → `IN ('pending','approved')`) plus the confirmed email. Everything else — HMAC verify, event dedupe, handleGoneSlot, refund events — stays exactly as Phase 5 built it.
Output: widened confirm WHERE, confirmed-email send, and extended webhook tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-booking-payment-integration/06-RESEARCH.md
@.planning/phases/06-full-booking-payment-integration/06-PATTERNS.md
@.planning/phases/05-payments-payouts/05-CONTEXT.md

<interfaces>
<!-- The single confirm writer (verified 2026-07-20). -->

src/app/api/paymongo/webhook/route.ts:
  - The confirm UPDATE (306-308): `UPDATE booking SET status = 'confirmed', expires_at = NULL, payment_id = ${paymentId} WHERE id = ${bookingId} AND status = 'pending' RETURNING id`. Widen the WHERE to `status IN ('pending','approved')` (instant pays from pending, request from approved; both → confirmed). Do NOT re-impose the expires_at>now() guard (Pitfall 4 — a legitimately-paid-but-lapsed hold must still confirm; the GiST EXCLUDE, not the TTL, is the double-confirm authority).
  - On rows.length >= 1 (after 308): fetch the booker email + listing title (one extra read — booking joined to user + listing) and `void sendBookingConfirmed(...)`. Keep it fire-and-forget, OUTSIDE the critical ACK path (a Resend failure must never reject the 200).
  - handleGoneSlot (123-175) is UNCHANGED — a 0-row confirm on a released/declined request routes here and auto-refunds (refundable rail) / operator-alerts (QRPh) exactly as instant-book (this IS the D-58 pay-after-release backstop; do NOT add request-specific refund logic).
  - verifySignature / paymongo_event dedupe / handleRefund all UNCHANGED.

Email: sendBookingConfirmed(to, spaceTitle, whenLabel, reference, bookingUrl) (src/lib/email.ts, 06-03) — fire-and-forget.
bookingReference(id) (src/lib/booking/reference.ts) — the FIT- display reference for the email.
Test: tests/paymongo/webhook-payment-paid.test.ts (extend — the existing confirm/replay/badSig/card-refund/QRPh-alert cases; add approved→confirmed + confirmed-email-fires).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the confirm WHERE + fire the BOOK-06 confirmed email</name>
  <files>src/app/api/paymongo/webhook/route.ts</files>
  <read_first>
    - src/app/api/paymongo/webhook/route.ts (the checkout_session.payment.paid branch 295-314 incl. the confirm UPDATE 306-308; handleGoneSlot 123-175; the dedupe + ACK 283-340)
    - src/lib/email.ts (sendBookingConfirmed from 06-03)
    - src/lib/booking/reference.ts (bookingReference — the FIT- display reference)
  </read_first>
  <action>
    Widen the confirm UPDATE WHERE from `status = 'pending'` to `status IN ('pending','approved')` so a pay-on-approval request (paid from the `approved` state) confirms through the SAME single writer as an instant booking; keep the confirm on-status ALONE (do NOT re-impose expires_at>now(), Pitfall 4). On a ≥1-row confirm, load the booker email + listing title + window (one extra read: booking joined to user + listing) and `void sendBookingConfirmed(bookerEmail, title, whenLabel, bookingReference(bookingId), `${base}/bookings/${bookingId}`)` — fire-and-forget, placed AFTER the confirm UPDATE and OUTSIDE the 200 ACK critical path so a Resend failure can never reject the webhook. Leave handleGoneSlot, verifySignature, the paymongo_event dedupe, and handleRefund untouched (the 0-row path already auto-refunds/operator-alerts the pay-after-release race, D-58/Pitfall 3 — do NOT add request-specific refund logic).
  </action>
  <verify>
    <automated>npx vitest run tests/paymongo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "IN ('pending','approved')" src/app/api/paymongo/webhook/route.ts` matches the confirm UPDATE WHERE.
    - `grep -n "sendBookingConfirmed" src/app/api/paymongo/webhook/route.ts` shows the confirmed email fired fire-and-forget on the ≥1-row branch.
    - handleGoneSlot is unchanged (no new request-specific refund branch); `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Confirm WHERE widened to pending|approved; confirmed email fires on success; gone-slot backstop untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the webhook tests (approved→confirmed, pay-after-release, confirmed email)</name>
  <files>tests/paymongo/webhook-payment-paid.test.ts</files>
  <read_first>
    - tests/paymongo/webhook-payment-paid.test.ts (the existing seeded-event harness, badSig/replay/card-refund/QRPh-alert cases, mockPayMongo, mockResend)
    - src/app/api/paymongo/webhook/route.ts (the widened branch from Task 1)
  </read_first>
  <action>
    Extend tests/paymongo/webhook-payment-paid.test.ts with: (a) a payment.paid whose reference_number is an `approved` booking id → the booking flips to 'confirmed' and payment_id is captured (mirrors the existing pending→confirmed case); (b) the confirmed email fires on a successful confirm for BOTH an instant (pending) and a pay-on-approval (approved) confirm — assert via mockResend.sent(); (c) pay-after-release: a payment.paid whose request was already released/`cancelled` claims 0 rows → handleGoneSlot fires (auto-refund on a refundable rail / operator-alert on QRPh) exactly as the existing gone-slot case, and the booking stays terminal → PaymentReversedState (do NOT expect a stuck interstitial, Pitfall 3). Keep the existing badSig-400 / replay-once assertions passing.
  </action>
  <verify>
    <automated>npx vitest run tests/paymongo/webhook-payment-paid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file asserts approved→confirmed, confirmed-email-fires (instant + approval), and pay-after-release→handleGoneSlot.
    - `npx vitest run tests/paymongo/webhook-payment-paid.test.ts` passes with all prior cases still green.
  </acceptance_criteria>
  <done>Webhook tests cover approved→confirmed + confirmed email + pay-after-release; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PayMongo → webhook | The Paymongo-Signature (HMAC) is the authentication; the browser ?paid=1 is never proof (D-57). |
| webhook → booking confirm | The single writer; a widened WHERE must not create a second confirm path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Spoofing | confirm authority | mitigate | The HMAC-verified payment.paid webhook stays the SOLE confirm writer; request-to-book confirms by WIDENING the WHERE, never a parallel confirm path. bookingId is derived from the verified reference_number, never a client field. |
| T-06-14 | Tampering | pay-after-release race | mitigate | A 0-row confirm routes to the UNCHANGED handleGoneSlot (D-58) — auto-refund on refundable rails / operator-alert on QRPh; never silent retention (Pitfall 3). No request-specific refund logic added. |
| T-06-15 | Denial of Service | confirmed-email send | mitigate | sendBookingConfirmed is fire-and-forget outside the ACK path; a Resend failure never rejects the 200 (a rejected ACK would make PayMongo retry forever). |
</threat_model>

<verification>
- `npx vitest run tests/paymongo` — approved→confirmed, confirmed email (both modes), pay-after-release green; prior badSig/replay/refund cases still green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Confirm UPDATE WHERE = IN ('pending','approved'); instant + pay-on-approval both confirm via the one writer.
- BOOK-06 confirmed email fires on a successful confirm, fire-and-forget.
- handleGoneSlot unchanged; pay-after-release still auto-refunds/alerts.
</success_criteria>

<output>
Create `.planning/phases/06-full-booking-payment-integration/06-05-SUMMARY.md` when done.
</output>
