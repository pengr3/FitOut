---
phase: 04-booking-core-search-no-payment
plan: 06
type: execute
wave: 3
depends_on: [04-04, 04-02]
files_modified:
  - src/app/actions/booking.ts
  - src/components/booking/price-breakdown.tsx
  - src/components/booking/hold-countdown.tsx
  - src/components/booking/hold-expired-state.tsx
  - src/components/booking/reserve-actions.tsx
  - tests/booking/state-machine.test.ts
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03]
must_haves:
  truths:
    - "Clicking Book (signed-in canBook user) places the hold via a POST server action and redirects to the reserve page"
    - "Confirm flips the hold pending→confirmed only after re-checking expires_at > now server-side"
    - "A hold that expired before confirm yields a graceful 'just taken/expired' response, never a silent confirm"
    - "The reserve page shows a live 15-minute countdown and a server-frozen price breakdown"
  artifacts:
    - path: "src/app/actions/booking.ts"
      provides: "placeHold + confirmBooking server actions"
      exports: ["placeHold", "confirmBooking"]
    - path: "src/components/booking/price-breakdown.tsx"
      provides: "fee-extensible breakdown consuming the frozen quote (PHP)"
    - path: "src/components/booking/hold-countdown.tsx"
      provides: "client 15-min countdown from expiresAt (role=timer)"
    - path: "src/components/booking/hold-expired-state.tsx"
      provides: "calm expiry interstitial with recovery CTA"
    - path: "src/components/booking/reserve-actions.tsx"
      provides: "Confirm button that disables on click (idempotent double-click)"
  key_links:
    - from: "src/app/actions/booking.ts"
      to: "createPendingHold"
      via: "placeHold after session+canBook+deriveBookable gate"
      pattern: "createPendingHold"
    - from: "src/app/actions/booking.ts"
      to: "confirm expiry re-check"
      via: "UPDATE pending→confirmed only if expires_at > now()"
      pattern: "expires_at > now\\(\\)|status.*pending"
---

<objective>
Wire the booking mutations and the reserve-page interaction pieces. `placeHold` (a POST server action, NOT a GET side-effect) gates on a signed-in `canBook` user (D-41) + a server re-derivation of `deriveBookable` (V4 — the route group is not the gate), calls `createPendingHold`, and redirects to the reserve page. `confirmBooking` re-checks `expires_at > now() AND status='pending'` server-side (never trusting the client countdown), flips `pending → confirmed` (D-40 — the Phase-5 payment seam), and redirects to the confirmation page. The client pieces: a fee-extensible PriceBreakdown (frozen quote), a live HoldCountdown, a calm HoldExpiredState, and a disable-on-click Confirm control (D-42/D-44).

Purpose: BOOK-01/02/03 action layer + reserve UI atoms. The pending hold is placed on ENTERING checkout (SC#3), and the state machine is proven end-to-end (pending→confirmed; pending→cancelled on expiry).
Output: `actions/booking.ts`, the four booking components, and the state-machine integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-booking-core-search-no-payment/04-CONTEXT.md
@.planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md
@.planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md

<interfaces>
From src/app/actions/blocks.ts (the canonical server-action skeleton to CLONE — session → gate → Zod → mutate → revalidate):
```typescript
"use server";
async function requireUserId(): Promise<string|null> { const s = await auth.api.getSession({ headers: await headers() }); return s?.user?.id ?? null; }
export type AvailabilityResult = { ok:true; id?:string } | { ok:false; error:string; fieldErrors?:Record<string,string[]> };
// body: requireUserId early-return → guard → schema.safeParse → mutate → revalidatePath(`/listings/${id}`) + return
```
Booking differs: the guard is CAPABILITY (`canBook`) + a server re-derivation of `deriveBookable` for the listing — NOT host-ownership. Then `redirect(...)` (from next/navigation).
From src/lib/availability/units.ts (Plan 04): `createPendingHold(db, input): Promise<HoldResult>`, `mapBookingError(e)`.
From src/lib/validation/booking.ts (Plan 02): `bookingCreateSchema` (listingId + window + optional idempotencyKey).
From src/lib/bookability.ts: `deriveBookable(listing:{status}, host:{emailVerified, payoutsEnabled})` — re-derive server-side before the hold.
From src/lib/db/schema.ts: `booking` (now with expiresAt/quotedTotalCents/currency/idempotencyKey); `bookingStatus` enum.
From src/lib/money.ts: `formatMoney`, `DISPLAY_CURRENCY` ("php").
From src/components/listing/listing-card.tsx:96-107 (the button-disable idiom for reserve-actions): `disabled={pending}` + label swap on click.
From src/components/availability/availability-calendar.tsx:78-139 (the "use client" state-shell) + :184-204 (the dashed interstitial) + :233-268 RailSelectionSummary (the tabular-nums/formatMoney/muted display template PriceBreakdown formalizes).
UI-SPEC (04-UI-SPEC.md): PriceBreakdown = a fee-extensible line-item list (`{₱rate}/hr × {N} hours = {subtotal}` OR `{₱rate}/day × 1 day`, a RESERVED empty slot for Phase-5 fee rows that renders nothing now, a separator, `Total` Heading-600 tabular-nums), consuming the server-frozen `quotedTotalCents`+`currency`; sub-line `Final price. No booking or service fees in this step.` HoldCountdown = lucide `Clock` + tabular-nums, counts the 15-min TTL from `expiresAt`, neutral by default (optional `--destructive` numerals final 60s), `role="timer"` `aria-live="off"`, flips to the expiry state at 0 (does not silently vanish). HoldExpiredState = calm `card` + lucide `TimerOff` + coral `Back to availability` / `Search other spaces` (NEVER red). Confirm copy `Confirm booking` → `Confirming…`; reassurance `You won't be charged yet.` "Just taken"/"expired" are neutral, never red.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: placeHold + confirmBooking server actions + state-machine test</name>
  <files>src/app/actions/booking.ts, tests/booking/state-machine.test.ts</files>
  <read_first>
    - src/app/actions/blocks.ts (the whole file — the SECURITY CONTRACT header :5-20, requireUserId :37-40, the mutation body shape :81-142, revalidatePath :139-140)
    - src/lib/availability/units.ts (Plan 04 createPendingHold + mapBookingError)
    - src/lib/validation/booking.ts (bookingCreateSchema) + src/lib/bookability.ts (deriveBookable)
    - .planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md (§ actions/booking.ts — the exact placeHold/confirmBooking bodies; § Shared Patterns session+capability gate; § Server re-derives price)
    - .planning/phases/04-booking-core-search-no-payment/04-RESEARCH.md (§ Pattern 5 hold-on-entry POST; § Security V4 owner/capability gate; § Anti-Pattern trusting the client countdown)
    - tests/listing/status-gate.test.ts (the vi.doMock harness for testing real actions :22-41 — mock next/headers, @/lib/auth, @/lib/db, next/cache, then import the action)
  </read_first>
  <behavior>
    - placeHold: an unauthenticated caller → `{ok:false}` sign-in result (D-41); a signed-in non-canBook user → an activate-booking result; a signed-in canBook user for a bookable listing → creates a pending hold and returns/redirects with the hold id; a listing that fails a server `deriveBookable` re-check → refused (route group is not the gate).
    - confirmBooking: a pending hold with `expires_at > now()` owned by the caller → flips to `confirmed` and redirects to `/bookings/{id}`; a hold whose `expires_at <= now()` → NOT confirmed, returns a graceful "just taken/expired" (never a silent confirm); a hold owned by a different user → refused.
    - a double-submit of placeHold (same idempotency/window) returns the SAME booking (delegated to createPendingHold).
  </behavior>
  <action>
    Create `src/app/actions/booking.ts` (`"use server"`) cloning the `blocks.ts` skeleton. `placeHold(input)`: (1) `requireUserId()` → sign-in result on null (D-41, return-to-checkout is handled by the CTA in Plan 07); (2) load the user's `canBook` and the listing+host, and re-derive `deriveBookable` server-side — refuse if not bookable or `!canBook` (V4, the route group is NOT the gate); (3) `bookingCreateSchema.safeParse(input)` → fieldErrors on failure; (4) `createPendingHold(db, { listingId, bookerId: userId, startsAt, endsAt, unitCount, idemKey })`; (5) on the mapped error return the calm result, else `revalidatePath('/listings/${listingId}')` + `revalidatePath('/')` (so the pending hold immediately reflects in calendar + search) and `redirect('/listings/${listingId}/book?hold=${holdId}')`. It MUST be a POST server action, never a GET side-effect (Pattern 5 / Pitfall 2 — a GET duplicates holds on prefetch/refresh). `confirmBooking(holdId)`: `requireUserId()` gate; load the booking; owner-gate `booking.bookerId === userId` (else refuse); re-check `status='pending' AND expires_at > now()` server-side (never trust the client countdown); if expired/taken return the graceful "just taken/expired" copy via `mapBookingError`; else `UPDATE ... SET status='confirmed'` and `redirect('/bookings/${id}')`. Write `tests/booking/state-machine.test.ts` using the `status-gate.test.ts` vi.doMock harness to drive the REAL actions: pending→confirmed happy path, expiry-re-check refusal, and the owner-gate refusal.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/booking/state-machine.test.ts` exits 0 with pending→confirmed, expired-refusal, and owner-gate cases green.
    - `booking.ts` grep: `createPendingHold`, `deriveBookable`, `redirect`, and a confirm re-check of `expires_at`/`status='pending'`; `placeHold` is a server action (no GET route creates the hold).
    - The expired-confirm case asserts the booking is NOT flipped to confirmed and a graceful result is returned.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/booking/state-machine.test.ts</automated>
  </verify>
  <done>placeHold (POST, canBook+deriveBookable gated) mints the hold and redirects; confirmBooking re-checks expiry server-side and flips pending→confirmed or fails gracefully; the state-machine test proves the transitions + the two must-not-skip guards.</done>
</task>

<task type="auto">
  <name>Task 2: Booking components — PriceBreakdown, HoldCountdown, HoldExpiredState, reserve-actions</name>
  <files>src/components/booking/price-breakdown.tsx, src/components/booking/hold-countdown.tsx, src/components/booking/hold-expired-state.tsx, src/components/booking/reserve-actions.tsx</files>
  <read_first>
    - src/components/availability/availability-calendar.tsx (RailSelectionSummary display template :233-268; the "use client" state-shell :78-139; the dashed interstitial :184-204)
    - src/components/listing/listing-card.tsx:96-107 (the disable-on-click button idiom for reserve-actions)
    - src/lib/money.ts (formatMoney + DISPLAY_CURRENCY)
    - src/app/actions/booking.ts (Task 1 confirmBooking — reserve-actions wraps it)
    - .planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md (§ Price breakdown contract; § Component Inventory HoldCountdown/HoldExpiredState/ReservePage; § Color interaction recipes; § Accessibility — role=timer, aria-live)
    - .planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md (§ hold-countdown + hold-expired-state + reserve-actions)
  </read_first>
  <action>
    Create `src/components/booking/price-breakdown.tsx` — render the frozen quote as a fee-extensible line-item list: `{₱rate}/hr × {N} hours = {subtotal}` (or `{₱rate}/day × 1 day`), a RESERVED empty slot for Phase-5 fee/commission rows (renders nothing in Phase 4 but leaves the layout so nothing shifts later — document the seam), a `separator`, then `Total` (Heading weight 600, `tabular-nums`). Consume the server-frozen `quotedTotalCents` + `currency` via `formatMoney(_, DISPLAY_CURRENCY)` — NEVER a client recompute. Sub-line: `Final price. No booking or service fees in this step.` Create `src/components/booking/hold-countdown.tsx` (`"use client"`) — a `setInterval` countdown of the 15-min TTL from `expiresAt`: lucide `Clock` + `tabular-nums` `Held for {mm:ss}`, neutral by default (optional `--destructive` numerals in the final 60s, icon+label persist), `role="timer"` `aria-live="off"` (announce only the final-minute threshold); on reaching 0 it calls an `onExpire` callback / flips the page into the expiry state (does NOT silently vanish). Create `src/components/booking/hold-expired-state.tsx` — a calm interstitial (`card` + lucide `TimerOff`): `Your hold expired` + body + a coral `Back to availability` and a neutral `Search other spaces`. NEVER red (occupancy/expiry is not an error). Create `src/components/booking/reserve-actions.tsx` (`"use client"`) — the `Confirm booking` control wrapping `confirmBooking`: `disabled` + label → `Confirming…` on click (D-42 double-click no-op), `aria-disabled` while submitting; the DB-level idempotency backstop is in createPendingHold. Include the `You won't be charged yet.` reassurance line.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` exit 0 for all four components.
    - PriceBreakdown consumes `quotedTotalCents`/`currency` (grep: `DISPLAY_CURRENCY`; no client price arithmetic) and has a documented reserved fee slot.
    - HoldCountdown has `role="timer"` and derives from `expiresAt`; reserve-actions disables the button on click and swaps the label to `Confirming…`.
    - No component uses `--destructive`/red for the expired or "just taken" state (only the optional final-60s numerals).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>The reserve-page atoms exist: a frozen fee-extensible breakdown, a role=timer 15-min countdown that flips to the calm expiry state, and an idempotent disable-on-click Confirm — all neutral, coral only on the recovery/confirm CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → placeHold/confirmBooking | Untrusted; the actions re-check session, canBook, deriveBookable, ownership, and expiry server-side |
| client countdown → confirm | The client timer is a display cue; the server re-checks expires_at > now() as the sole expiry authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-BOOKCAP | Elevation | placeHold capability | mitigate | Re-check `canBook` AND re-derive `deriveBookable` server-side in the action (route group is not the gate, Security V4, D-41) |
| T-04-COUNTDOWNBYPASS | Tampering | confirmBooking expiry | mitigate | Confirm re-checks `status='pending' AND expires_at > now()` server-side; expired mid-confirm → graceful "just taken/expired", never a silent confirm |
| T-04-HOLDIDOR | Information disclosure / Elevation | confirmBooking ownership | mitigate | Owner-gate `booking.bookerId === session.userId` before any state transition (IDOR) |
| T-04-GETHOLD | Tampering | hold creation boundary | mitigate | Hold is created by a POST server action + redirect, never a GET render side-effect (Pattern 5 / Pitfall 2 — no duplicate holds on prefetch/refresh) |
| T-04-SC | Tampering | package installs | accept | No new packages; all shadcn primitives already installed |
</threat_model>

<verification>
- `npx vitest run tests/booking/state-machine.test.ts` green (transitions + expiry re-check + owner-gate).
- `npx tsc --noEmit` + `npm run lint` clean for the action + four components.
- Grep: placeHold uses createPendingHold + deriveBookable; confirmBooking re-checks expiry; HoldCountdown role=timer; reserve-actions disables on click.
</verification>

<success_criteria>
- Book → placeHold (POST, gated) → redirect to reserve; Confirm → confirmBooking (server expiry re-check) → pending→confirmed → redirect to confirmation (BOOK-01/02, D-40/D-41).
- The reserve atoms (breakdown, countdown, expiry, confirm) are ready for Plan 07 to assemble, all neutral/calm and server-frozen.
- The two must-not-skip controls (server price/window re-derivation is in Plan 04's quote; the confirm expiry re-check + capability gate here) hold.
</success_criteria>

<output>
Create `.planning/phases/04-booking-core-search-no-payment/04-06-SUMMARY.md` when done.
</output>
