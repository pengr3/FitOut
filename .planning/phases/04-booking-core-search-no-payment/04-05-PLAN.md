---
phase: 04-booking-core-search-no-payment
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-02]
files_modified:
  - src/components/search/search-result-card.tsx
  - src/components/search/search-results.tsx
  - src/components/search/search-bar.tsx
  - src/app/page.tsx
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05]
must_haves:
  truths:
    - "The homepage is a search/browse home: a search bar over a list of result cards"
    - "A result card shows photo, name, price (₱/hr · ₱/day), and distance when an origin is set"
    - "The default no-query view shows all bookable listings in the launch city, sensibly sorted"
    - "A zero-result search shows a friendly empty state with escape hatches (broaden radius / clear filters / nearby)"
    - "Results paginate via Load more (not infinite scroll)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "search home RSC (replaces the Next.js scaffold) — awaits searchParams, runs searchListings"
      contains: "searchParams"
    - path: "src/components/search/search-bar.tsx"
      provides: "location autocomplete + activity/date/price/radius filters serialized to the URL"
    - path: "src/components/search/search-result-card.tsx"
      provides: "SearchResultCard (photo, title-600, price, distance, whole-card Link)"
    - path: "src/components/search/search-results.tsx"
      provides: "results grid + sort Select + Load more + skeleton + empty states"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/search/query.ts"
      via: "RSC awaits searchParams → searchParamsSchema.safeParse → searchListings"
      pattern: "searchListings"
    - from: "src/components/search/search-bar.tsx"
      to: "URL"
      via: "serialize filters to query string (shareable/SEO, Back works)"
      pattern: "useRouter|URLSearchParams"
---

<objective>
Replace the Next.js scaffold at `/` with the demand-side front door (D-29): an Airbnb-style search bar (location autocomplete + activity/type + date/optional-time + price + radius, serialized to the URL) over a list of result cards (photo, name, ₱ price, distance). Default no-query view = all bookable city listings (D-30); zero-result = a friendly empty state with escape hatches (D-31); Load-more pagination (D-32); nearest/price sort (D-37).

Purpose: SEARCH-01..05 UI. The search bar is the entry focal point; the single coral Search button is the only accent on the page; result cards are uniform and neutral. Every displayed time names the venue timezone (SC#2). Consumes `searchListings` (Plan 03) and `searchParamsSchema` (Plan 02).
Output: `search-result-card.tsx`, `search-results.tsx`, `search-bar.tsx`, and the replaced `app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-booking-core-search-no-payment/04-CONTEXT.md
@.planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md
@.planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md

<interfaces>
From src/components/listing/listing-card.tsx (extend → SearchResultCard): presentational core = `Card` + `AspectRatio ratio={4/3}` cover `img` (alt=title) + title + primary type + price. Reuse the price-parts builder (`${formatMoney(hourlyRateCents, currency)}/hr` · `${formatMoney(dayRateCents, currency)}/day`). The card already guards `hasActions` — OMIT `editHref`/`onUnlist`/`onDelete` so NO host controls render; OMIT the status badge (every search result is bookable, D-16/D-30). Title weight: `font-medium` (500) → normalize to `font-semibold` (600) per UI-SPEC. Use `DISPLAY_CURRENCY` ("php") from money.ts, NOT `listing.currency` ('usd').
From src/lib/search/query.ts (Plan 03): `searchListings(db, params): Promise<{ results: SearchResultRow[]; hasMore: boolean }>`; each row carries id, title, primary_space_type, hourly/day rate, timezone, city, coverUrl, distance_m (nullable).
From src/lib/validation/booking.ts (Plan 02): `searchParamsSchema` (lat, lng, radius, priceMax, date, start, end, type, activity, sort, page).
From src/components/listing/address-autocomplete.tsx: reuse verbatim for the location field — lifts `{lat,lng}` via `onResolved` (address-autocomplete.tsx:89-97), debounced + graceful-degrade, no key needed.
From src/app/listings/[id]/page.tsx (the public RSC pattern): async `params`/`searchParams` are Promises → `const sp = await searchParams`; `formatMoney(cents, DISPLAY_CURRENCY)`; `gmtLabelFor`/`cityLabelFor` for the venue-tz label; `SPACE_TYPE_LABELS` from listing-vocab.
UI-SPEC locks (04-UI-SPEC.md): radius presets 2/5/10/25 (default 10); sort `Select` top-right (`Nearest first` default / `Price: low to high`; "Nearest" disabled with a hint when no origin); Load more neutral `secondary` (hidden when exhausted); skeleton loading (availability-calendar.tsx:178-183 idiom); empty-state block (availability-calendar.tsx:184-204 idiom) with `Broaden radius`/`Clear filters`/`Show nearby spaces`; copy in § Copywriting/§ Empty states. Card order: cover → title(600) → primary type → `₱X/hr · ₱Y/day` → distance ("2.3 km away", muted, origin-only) → optional searched-window line. Whole card = Link to `/listings/[id]?date=&start=&end=`.
All installed shadcn primitives (input-group, command, popover, calendar, select, toggle-group, skeleton, button, card, aspect-ratio) — reuse, do not re-specify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchResultCard + SearchResults (grid, sort, Load more, skeleton, empty states)</name>
  <files>src/components/search/search-result-card.tsx, src/components/search/search-results.tsx</files>
  <read_first>
    - src/components/listing/listing-card.tsx (extend — the presentational core :153-183; the price-parts builder :131-137; the hasActions guard :129,185-225)
    - src/components/availability/availability-calendar.tsx (the skeleton idiom :178-183 and the dashed empty-state block :184-204)
    - src/lib/money.ts (DISPLAY_CURRENCY, formatMoney) + src/lib/listing-vocab.ts (SPACE_TYPE_LABELS)
    - .planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md (§ Discretionary card layout; § Copywriting Load more/empty; § Empty/zero-result states; § Color — no coral on cards; § Typography — title 600)
    - .planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md (§ search-result-card + § search-results)
  </read_first>
  <action>
    Create `src/components/search/search-result-card.tsx` by extending the `listing-card.tsx` presentational core: cover `AspectRatio ratio={4/3}` + `img` (alt=title) → title (`font-semibold`, weight 600) → primary space type (muted) → price `₱X/hr · ₱Y/day` (`tabular-nums`, via `formatMoney(_, DISPLAY_CURRENCY)`) → distance line (`{km} km away`, muted, rendered ONLY when `distance_m != null`) → optional searched-window line (`Available {t} on {date}`, naming the venue tz, SC#2). OMIT host-action props and the status badge. The WHOLE card is a `<Link href={/listings/${id}?date=&start=&end=}>` carrying the searched window; focus ring `--ring`; hover = subtle `--muted`/shadow, NOT coral. Create `src/components/search/search-results.tsx` (client where needed for Load more) rendering: a results header (`N spaces near {area}` or `Browse spaces in {City}` default, D-30) with a compact sort `Select` (`Nearest first` default / `Price: low to high`; disable "Nearest" with the hint `Enter a location to sort by distance` when no origin); a 1/2/3-col grid (gutter 16/24px); a `Load more` `secondary` button driven off `hasMore` (label `Loading…` while fetching, hidden when exhausted); a `Skeleton` card grid for loading; and the zero-result empty state (`No spaces match those filters` + `Broaden radius`/`Clear filters`/`Show nearby spaces`) plus the cold-start empty state (`No spaces are bookable here yet`) per UI-SPEC copy. Occupancy/empty states are neutral, never red.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` exit 0 for the new components.
    - SearchResultCard renders NO host-action controls and NO status badge; title uses `font-semibold`; distance renders only when an origin distance is present; the card is a single `Link`.
    - SearchResults exposes loading (skeleton), populated (grid + Load more), exhausted (no Load more), zero-result, and cold-start states; the sort "Nearest first" option is disabled when no origin.
    - Grep: `search-result-card.tsx` contains `DISPLAY_CURRENCY` and `font-semibold` and NOT `font-medium`; contains no `Badge` status import.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>A neutral, host-control-free result card (photo/title-600/price/distance/window) and a results shell with sort, Load-more, skeletons, and both empty states — all from existing tokens, coral only on the future Search button.</done>
</task>

<task type="auto">
  <name>Task 2: SearchBar — location autocomplete + filters serialized to the URL</name>
  <files>src/components/search/search-bar.tsx</files>
  <read_first>
    - src/components/listing/address-autocomplete.tsx (reuse verbatim for the location field; the onResolved {lat,lng} lift :89-97; debounced-fetch/abort :107-141)
    - src/components/host/*wizard* or any RHF form in the repo (the `zodResolver` + `Form` wiring pattern)
    - src/lib/validation/booking.ts (searchParamsSchema — the resolver + the exact serialized param names)
    - src/lib/listing-vocab.ts (SPACE_TYPE_LABELS/ACTIVITY_TAG_LABELS for the activity control)
    - .planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md (§ Component Inventory SearchBar contract; § Discretionary radius presets + price min/max popover + "Near me"; § Copywriting Search/Use my location)
    - .planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md (§ search-bar)
  </read_first>
  <action>
    Create `src/components/search/search-bar.tsx` (`"use client"`) as an Airbnb-style filter form: RHF + `zodResolver(searchParamsSchema)`. Compose the location field from `AddressAutocomplete` (reuse verbatim; capture `{lat,lng}` via onResolved); an activity/type control (`command`/`popover` or `select` over `SPACE_TYPE_LABELS`/`ACTIVITY_TAG_LABELS`); a date (`calendar`) + optional time-range (`select`/`toggle-group`); a price popover with min/max `input` (₱/hr, default unbounded — the price axis is hourly rate); and a radius `select` (presets 2/5/10/25, default 10; radius applies only when a location origin is set). On submit, serialize the fields to the URL query string (`?type&activity&date&start&end&priceMax&radius&sort&page&lat&lng`) via `useRouter().push` so search is shareable/SEO-friendly and the Back button works (D-32) — the RSC reads these params. The single coral `Search` button is the one primary CTA (lucide `Search`). Optionally add a neutral `Use my location` (`navigator.geolocation` → same `{lat,lng}`; acceptable to defer as a fast-follow, D-33). Desktop = horizontal pill; mobile = condensed bar that expands to a `dialog`. All fields ≥44px hit area; each field has an associated `<label>`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run lint` exit 0.
    - Submitting the form pushes a URL carrying the filter params (grep: `useRouter` or `URLSearchParams` present; the serialized param names match `searchParamsSchema`).
    - The location field reuses `AddressAutocomplete`; radius presets are 2/5/10/25 with default 10; there is exactly ONE coral primary button (`Search`).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>A filter form whose fields (validated by searchParamsSchema) serialize to a shareable URL; location via the reused autocomplete; one coral Search CTA.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the scaffold — search home RSC at app/page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (the Next.js scaffold being fully replaced, D-29)
    - src/app/listings/[id]/page.tsx (the public RSC pattern: async searchParams await; formatMoney(_, DISPLAY_CURRENCY); gmtLabelFor/cityLabelFor; server data fetch)
    - src/lib/search/query.ts (Plan 03 searchListings) + src/lib/validation/booking.ts (searchParamsSchema) + src/lib/db (db)
    - .planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md (§ app/page.tsx REPLACE) + 04-UI-SPEC.md (§ Screen contract Search home states; § Visual hierarchy — search bar is the focal point)
  </read_first>
  <action>
    Replace `src/app/page.tsx` with the search home RSC (D-29). Signature: `export default async function Home({ searchParams }: { searchParams: Promise<Record<string,string|string[]|undefined>> })`. `const sp = await searchParams`; `searchParamsSchema.safeParse(sp)` → on failure fall back to the default city view (D-30, never crash). Call `searchListings(db, parsed)` server-side (the RSC is the Stage-1/Stage-2 executor). Compose `<SearchBar defaultValues={parsed} />` above `<SearchResults results hasMore origin={parsed.lat/lng} sort={parsed.sort} searchedWindow=... />`. Header reads `Browse spaces in {City}` when no query is set (D-30) else `N spaces near {area}`. `deriveBookable` inclusion is enforced inside the search SQL (not per-card). Every displayed time names the venue tz (reuse gmtLabelFor/cityLabelFor). Keep coral to the single Search button only.
  </action>
  <acceptance_criteria>
    - `npm run build` compiles the `/` route with no type/lint errors.
    - `app/page.tsx` awaits `searchParams`, calls `searchParamsSchema.safeParse`, and calls `searchListings` (grep confirms all three); the Next.js scaffold markup is gone.
    - An invalid/garbage searchParam does NOT crash the page (safeParse fallback to default city view).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -q "searchListings" src/app/page.tsx</automated>
  </verify>
  <done>`/` is the search/browse home: a search bar over result cards, default city view when no query, driven by the two-stage searchListings; the scaffold is fully replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → RSC | The RSC re-validates via searchParamsSchema (Plan 02) before querying; the client cannot inject unbounded params |
| search page → public | Renders public listing data only; no auth surface, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-PARAMTAMPER | Tampering | page.tsx searchParams | mitigate | `searchParamsSchema.safeParse` server-side with a default-city fallback; no unvalidated param reaches searchListings (Security V5) |
| T-04-XSSCARD | Tampering | SearchResultCard render | mitigate | React auto-escapes; listing text/photo URLs are host-provided public data already stored server-side; `img alt` is the listing title |
| T-04-ENUM | Information disclosure | search results | accept | Search exposes only public bookable listings (D-16); no booking/booker data on this surface |
| T-04-SC | Tampering | package installs | accept | No new packages; all shadcn primitives already installed (UI-SPEC Registry Safety — official registry only) |
</threat_model>

<verification>
- `npm run build` compiles `/`; `npx tsc --noEmit` + `npm run lint` clean.
- `app/page.tsx` grep: `searchParams`, `searchParamsSchema`, `searchListings`.
- Cards carry no host controls/status badge; one coral Search button; Load-more + empty states present.
- Visual behavior (search → cards, empty states, sort-disabled-without-origin) is confirmed in the Plan-08 E2E + human-verify.
</verification>

<success_criteria>
- `/` is a working search/browse home (SEARCH-01..05 UI): location + filters → list of cards with photo/name/price/distance.
- Default city view (D-30), zero-result escape hatches (D-31), Load more (D-32), nearest/price sort (D-37) all present.
- Filters serialize to a shareable URL; the RSC re-validates and runs the two-stage search.
</success_criteria>

<output>
Create `.planning/phases/04-booking-core-search-no-payment/04-05-SUMMARY.md` when done.
</output>
