---
phase: 04-booking-core-search-no-payment
plan: 08
type: execute
wave: 5
depends_on: [04-05, 04-07]
files_modified:
  - e2e/search-and-book.spec.ts
autonomous: false
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, BOOK-01, BOOK-02, BOOK-03]
must_haves:
  truths:
    - "A full search → filter → card → listing → Book → reserve (countdown) → Confirm → confirmation flow works end-to-end"
    - "An abandoned hold expires and the reserve page shows the graceful expiry state"
    - "The confirmation page is durable across a refresh"
  artifacts:
    - path: "e2e/search-and-book.spec.ts"
      provides: "full-flow Playwright E2E incl. the expiry UX"
      contains: "search-and-book"
  key_links:
    - from: "e2e/search-and-book.spec.ts"
      to: "the seeded bookable set + UAT host"
      via: "beforeAll direct dev-DB seed (D-38)"
      pattern: "beforeAll|seed"
---

<objective>
Prove the whole Phase-4 promise end-to-end and gate the phase on a human visual check. A single Playwright spec drives the real app: search → apply filters → click a result card → open the listing → Book → land on the reserve page with a live countdown + price breakdown → Confirm → reach the durable confirmation page; plus the abandoned-hold expiry UX. Then a human verifies the visual/interaction contract (search home, reserve countdown/expiry, confirmation durability) per the UI-SPEC.

Purpose: SC#1–SC#4 as a single lived flow, and the UI hint (yes) visual sign-off. This plan reads the LIVE DB, so it depends on the [BLOCKING] migration (Plan 01) already being applied.
Output: `e2e/search-and-book.spec.ts` + a human-verify sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md
@.planning/phases/04-booking-core-search-no-payment/04-VALIDATION.md
@.planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md

<interfaces>
From e2e/availability.spec.ts (the E2E harness to mirror): `test.beforeAll` seeds directly into the dev Postgres (public schema — the same DB the Playwright webServer's dev app reads) via raw postgres.js; unique `randomUUID()` ids; `test.afterAll` cascade-delete (bookings FIRST — `booker_id` is ON DELETE RESTRICT); `page.goto(BASE + ...)`; `getByRole` assertions; venue-tz target-day navigation (`selectTargetDay`, availability.spec.ts:144-157); `DATABASE_URL ?? "postgresql://fitout:fitout@localhost:5432/fitout"`; VENUE_TZ = "Asia/Manila".
From scripts/seed.ts + tests/helpers/seed.ts (Plan 02): the D-38 bookable set + known coords; reuse for the E2E seed (or reuse the UAT seed host `host@fitout.test` per MEMORY local-env-and-uat-seed.md).
From the built surfaces: `/` (search home, Plan 05); `/listings/[id]` (CTA, Plan 07); `/listings/[id]/book` (reserve + countdown, Plan 07); `/bookings/[id]` (confirmation, Plan 07).
UI-SPEC copy anchors (for getByText/getByRole): `Search`, `Load more`, `Book this space`, `Held for`, `Confirm booking`, `Booking confirmed`, `Booking reference`, `Your hold expired`, `Back to availability`, `That time was just taken.`, `You won't be charged yet.`, tz note `Times shown in … (GMT+8)`.
NOTE: a signed-in `canBook` session is required at Book (D-41) — set up the E2E's authenticated context the same way the existing logged-in E2E specs do (or seed + sign in the booker), since Book is gated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-flow E2E — search → book → confirm + expiry UX</name>
  <files>e2e/search-and-book.spec.ts</files>
  <read_first>
    - e2e/availability.spec.ts (the seed/teardown + navigation harness to mirror; venue-tz day selection; cascade-delete order)
    - e2e/public-listing.spec.ts + e2e/login-persistence.spec.ts (the signed-in context pattern for the Book gate, D-41)
    - scripts/seed.ts + tests/helpers/seed.ts (Plan 02 D-38 set + coords) and MEMORY local-env-and-uat-seed.md (UAT host `host@fitout.test`)
    - .planning/phases/04-booking-core-search-no-payment/04-VALIDATION.md (the SEARCH-05 + full-flow row) + 04-UI-SPEC.md (the copy anchors + Screen contract states) + 04-PATTERNS.md (§ e2e/search-and-book.spec.ts)
  </read_first>
  <action>
    Create `e2e/search-and-book.spec.ts` mirroring `e2e/availability.spec.ts`. In `beforeAll`, seed a bookable set into the dev DB (reuse `scripts/seed.ts`/`tests/helpers/seed.ts` helpers or the UAT host) at known Manila coordinates with operating hours and a free target day within the horizon; establish a signed-in `canBook` booker context (Book is gated, D-41). Test the happy path: `goto('/')` → enter a location + a filter (activity/price) → `Search` → assert result cards render (photo/name/price/distance) → click a card → land on `/listings/[id]` → pick a window in the venue-tz calendar → `Book this space` → assert the reserve page shows the selected window (tz note `(GMT+8)`), a `PriceBreakdown` total in ₱, and a `Held for mm:ss` countdown → `Confirm booking` → assert `/bookings/[id]` shows `Booking confirmed` + `Booking reference FIT-…` → reload the page and assert the confirmation still renders (durable, D-43). Add the expiry-UX case: enter checkout to place a hold, then drive the reserve page to its expired state (e.g. seed/advance a hold whose `expires_at` is already past, or configure a short TTL) and assert `Your hold expired` + `Back to availability` (the graceful state, D-44 — never a red error). Teardown deletes bookings first, then listings/users (ON DELETE RESTRICT). Keep ids unique per run.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e -- search-and-book` exits 0.
    - The spec asserts each hop: search results present → card click → listing → Book → reserve (countdown + ₱ breakdown + tz note) → Confirm → confirmation (reference) → durable after reload.
    - The expiry case asserts the `Your hold expired` graceful state with a recovery CTA (not a red error).
    - Teardown removes all seeded rows (bookings before users).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- search-and-book</automated>
  </verify>
  <done>One Playwright spec proves the full search→book→confirm flow plus the abandoned-hold expiry UX against the real app and the D-38 seed, with clean teardown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual sign-off — search home, reserve countdown/expiry, confirmation</name>
  <files>(none — human verification of the running dev app; no files modified)</files>
  <action>
    This is a checkpoint, not a code task — Claude has already automated the build + full-flow E2E (Task 1). Pause and present the numbered steps in `<how-to-verify>` for the user to visually verify the Phase-4 UI contract against the running dev app, then wait for the resume signal. Do not advance to the phase gate until the user approves.
  </action>
  <what-built>
    The full booking-core & search UI: the search home at `/` (search bar + result cards + sort + Load more + empty states), the reserve page at `/listings/[id]/book` (window + price breakdown + live "Held for mm:ss" countdown + Confirm), the graceful hold-expiry state, and the durable owner-gated confirmation at `/bookings/[id]`. Prices display in PHP; occupancy/expiry states are calm and neutral (never red); coral appears once per screen.
  </what-built>
  <how-to-verify>
    1. Ensure the dev app is running (`npm run dev`) and the DB is seeded (`npx tsx scripts/seed.ts`).
    2. Visit http://localhost:3000/ — confirm the search home replaced the Next.js scaffold: a search bar over a list of bookable Manila listing cards (photo, name, ₱/hr · ₱/day, distance). The single coral control is `Search`. Try a filter (activity/price/date) and confirm results narrow; try a location with a tiny radius to reach the zero-result state and confirm the escape hatches (`Broaden radius` / `Clear filters` / `Show nearby spaces`). Confirm `Load more` appears only when there are more results, and the sort `Nearest first` is disabled until a location is entered.
    3. Open a listing → pick an hourly window → `Book this space`. On the reserve page confirm: the selected window shows in Manila time with a `(GMT+8)` note; the price breakdown reads `₱rate/hr × N hours = ₱total` (no fee line); a `Held for 14:59…` countdown ticks down; `You won't be charged yet.` is present.
    4. Let a hold expire (or use a short TTL) and confirm the reserve page shows a calm `Your hold expired` state with `Back to availability` — NOT a red error.
    5. Place another hold and `Confirm booking` — confirm the button disables to `Confirming…`, then the confirmation page shows `Booking confirmed`, a `--success` Confirmed badge, and `Booking reference FIT-XXXXXXXX`. Refresh — the confirmation persists. In a different account (or signed out), visiting that `/bookings/[id]` URL yields a 404/gated message.
    6. Spot-check: all prices are in ₱ (PHP), every time names the venue tz, and no surface uses red for occupancy/"just taken"/expiry.
  </how-to-verify>
  <verify>
    <human-check>User confirms the search home, reserve countdown + expiry state, and durable owner-gated confirmation match the UI-SPEC: PHP prices, venue-tz labels, calm neutral occupancy/expiry (never red), one coral CTA per screen.</human-check>
  </verify>
  <done>User has replied "approved" (or all reported visual/interaction issues are fixed and re-approved).</done>
  <resume-signal>Type "approved" or describe the visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E seed → dev DB | The spec writes/reads the shared dev DB; unique ids + cascade-correct teardown prevent cross-run corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-E2ESEED | Denial of Service | dev-DB test data | mitigate | Unique `randomUUID()` ids per run + afterAll cascade delete (bookings first, ON DELETE RESTRICT) — no leftover rows corrupt later runs |
| T-04-E2EGATE | Elevation | Book requires canBook | verify | The E2E establishes a signed-in canBook context; confirms the D-41 gate is exercised, not bypassed |
| T-04-SC | Tampering | package installs | accept | No new packages; Playwright + Vitest already installed |
</threat_model>

<verification>
- `npm run test:e2e -- search-and-book` green (full flow + expiry + durability).
- Before the phase gate: full `npm test` (Vitest integration) AND `npm run test:e2e` green (per 04-VALIDATION.md sampling — phase gate).
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- The full search→book→confirm flow works end-to-end incl. the abandoned-hold expiry UX (SC#1–SC#4 lived once).
- The confirmation survives refresh and is owner-gated (D-43).
- A human has signed off on the visual/interaction contract (UI hint = yes): PHP prices, venue-tz labels, calm neutral occupancy/expiry, one coral CTA per screen.
</success_criteria>

<output>
Create `.planning/phases/04-booking-core-search-no-payment/04-08-SUMMARY.md` when done.
</output>
