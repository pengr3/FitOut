# Phase 4 — Deferred Items (out-of-scope discoveries)

Items found during execution that are OUT OF SCOPE for the plan that found them (SCOPE BOUNDARY rule).
Logged, not fixed. Pick up in a dedicated quick-task or the phase code-review.

| Found in | Item | Evidence | Status |
|----------|------|----------|--------|
| 04-02 | `tests/auth/secret-config.test.ts > "throws at boot in production when BETTER_AUTH_SECRET is missing"` fails in the FULL parallel `vitest run` but PASSES when `tests/auth/` runs in isolation (28/28). Pre-existing cross-file state pollution: another test file imports/boots `@/lib/auth` (or leaves `NODE_ENV` stubbed) before this file, so the `vi.stubEnv` + module-cache reset no longer triggers the expected fail-closed throw. | Reproduced with plan 04-02's new test EXCLUDED: full suite = 222 passed / 1 failed (the same 223-total baseline), the auth test still failing → NOT caused by 04-02 (booking validation / seed touch no auth code). Isolated `tests/auth/` = 28/28 green. | OPEN — unrelated to 04-02 booking/search work; belongs to auth test-isolation. Suggest a quick-task: make `secret-config.test.ts` robust to prior module state (`vi.resetModules()` + re-import under stubbed env, or run in its own pool/isolate). |
| 04-05 | `npm run build` fails at "Collecting page data" for `/host/payouts/refresh` (a Phase-2 host route) with `PAYMONGO_SECRET_KEY is not set. Refusing to boot in production without the PayMongo secret key`. The guard lives in `src/lib/paymongo.ts` (+ `src/lib/auth.ts` for Better Auth's Google keys warning), both untouched by 04-05. | Build **Compiled successfully (15.8s)** and **Finished TypeScript (17.8s)** — both green, validating ALL routes incl. `/`; failure is a runtime module-eval env precondition on an unrelated route, not a type/lint error. My 4 plan files reference no PayMongo code (`grep PAYMONGO src/app/page.tsx src/components/search/` = none). A build on the prior commit would fail identically. 04-05's own gate (`npx tsc --noEmit && npm run lint && grep searchListings src/app/page.tsx`) is clean. | OPEN — pre-existing local-build env gap (PayMongo/Better-Auth secrets absent from `.env.local`), unrelated to the search-home UI. The `/` route itself compiles + typechecks clean. Set `PAYMONGO_SECRET_KEY` (+ Google OAuth keys) in the build env, or guard the payout route's module eval to be build-safe. |
| 04-05 | Whole-project `npm run lint` exits 1 on 1 ERROR (`react-hooks/set-state-in-effect`) at `src/components/listing/address-autocomplete.tsx:110` (`setResults([])` synchronously in an effect) plus 4 warnings (`react-hooks/incompatible-library` `form.watch()` in `signup/page.tsx` + `wizard.tsx`; unused vars in `tests/helpers/mocks.ts`). All in Phase-1/2 files untouched by 04-05. | `git diff --stat HEAD~2 -- address-autocomplete.tsx` = EMPTY (unchanged); linting that file alone reproduces the error (intrinsic to the file, independent of my imports). My 4 plan files lint clean in isolation (`npx eslint src/app/page.tsx src/components/search/*` exit 0). Newer `eslint-plugin-react-hooks` (React-Compiler rules) surfaces these on pre-existing code. | OPEN — pre-existing project-lint baseline is red on Phase-1/2 code, NOT 04-05. Suggest a quick-task to fix `address-autocomplete.tsx` (move the guard out of the effect body / use a cleanup+early-return pattern) and quiet the `form.watch()` warnings via `useWatch` (the 04-05 SearchBar already uses `useWatch` for exactly this reason). |

_Note: this does not affect any 04-02 deliverable — `tests/validation/booking-schemas.test.ts` is 19/19 green and `npx tsc --noEmit` is clean._
