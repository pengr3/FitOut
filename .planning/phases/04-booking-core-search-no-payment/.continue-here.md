---
context: phase
phase: 04-booking-core-search-no-payment
task: 0
total_tasks: 8
status: ready-to-execute
last_updated: 2026-07-14T10:11:41.435Z
---

# Phase 4 — Paused at the plan → execute boundary

**Phase 4 (Booking Core & Search, no payment) is fully PLANNED, verified, and committed. Execution has NOT started.** Resume with `/gsd:execute-phase 4`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| PostGIS distance-unit trap | `ST_DWithin`/`ST_Distance` on `geometry(Point,4326)` measure in **degrees**, so a km radius silently matches everything unless BOTH operands are cast `::geography` (RESEARCH Pitfall 1 — the highest-risk executor trap this phase) | advisory | Plans 04-03 already specify `::geography` on both operands + x=lng/y=lat axis order; the radius test asserts within-N-km vs beyond-N-km against seeded known coordinates |
| WR-03 savepoint vs deadlock | A `SAVEPOINT` rescues only a `23P01` unique/exclusion violation; a `40P01` deadlock aborts the WHOLE tx and needs an OUTER retry loop — a savepoint cannot rescue it | advisory | Plan 04-04 encodes the outer-retry + per-unit-savepoint structure per RESEARCH Pattern 2; pending-hold.test.ts proves both paths |
| Decision-coverage gate scans only `must_haves` | The §13a gate counts `D-NN` citations only in PLAN `must_haves` frontmatter, NOT the body (despite its message). Cost a revision round-trip this session; already fixed + saved to memory `gsd-decision-coverage-gate` | advisory | Not relevant to execution; relevant only when re-planning phases 5–8 |

<current_state>
Planning for Phase 4 finished this session. All artifacts committed through `6532222`. No mid-task execution state exists — this is a clean phase-boundary pause. The only uncommitted file is `.planning/config.json` (an ephemeral `_auto_chain_active` flag GSD toggled in plan-phase §15; not a planning artifact).
</current_state>

<completed_work>

This session (planning only):
- 04-RESEARCH.md — PostGIS true-availability search SQL, WR-03 SAVEPOINT/retry/sweep tx, hold expiry, idempotency; + Validation Architecture + Security Domain (commit f82e227)
- 04-VALIDATION.md — Nyquist per-requirement test map + Wave-0 gaps (9fb1f61)
- 04-PATTERNS.md — 32 files → closest analogs with file:line excerpts (1dec7f7)
- 04-01..04-08 PLAN.md — 8 plans, 5 waves (69e07fc)
- Verification: plan-checker revision loop — iter 1 fixed **1 blocker** (D-35 activity-tag-only search no-op) **+ 6 warnings**; iter 2 re-check clean (0 blockers); doc-sync applied; 17/17 decision-coverage citations added (6532222)
</completed_work>

<remaining_work>

Execute Phase 4 (nothing done yet):
- **Wave 1** — 04-01 (booking hold columns `expiresAt`/`quotedTotalCents`/`currency`/`idempotencyKey` + **[BLOCKING] `npm run db:migrate`** of `drizzle/0006_booking_hold.sql` + read-model lazy-expiry) · 04-02 (searchParamsSchema/bookingCreateSchema + PHP `DISPLAY_CURRENCY` + D-38 `scripts/seed.ts` + geo-seed helper)
- **Wave 2** — 04-03 (two-stage search query + search tests) · 04-04 (`createPendingHold` WR-03 tx + `quoteWindow` pricing + `FIT-` reference)
- **Wave 3** — 04-05 (search home at `/`, replacing the scaffold) · 04-06 (`placeHold`/`confirmBooking` actions + reserve components)
- **Wave 4** — 04-07 (reserve page + owner-gated confirmation page + listing Book-CTA wiring)
- **Wave 5** — 04-08 (full-flow E2E + expiry UX + **HUMAN-VERIFY checkpoint**)
</remaining_work>

<decisions_made>

- Research-first (vs skip) — Phase 4's two correctness-critical unknowns (true-availability search SQL, WR-03 tx) rewarded investigation.
- Blocker fix: collapsed `type`/`activity` → one `category` param checked against BOTH `primary_space_type::text` and the activity tag (the `::text` cast avoids `22P02` since the vocabularies are disjoint). Spans 04-02 → 04-03 → 04-05.
- Added bare `D-NN` citations to `must_haves.truths` to pass the §13a Decision Coverage Gate (the gate ignores body citations).
</decisions_made>

<blockers>
- None. (In-scope-but-later: Wave 5's 04-08 has a human-verify checkpoint that will pause execution for a manual sign-off.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-booking-core-search-no-payment/04-01-PLAN.md` … `04-08-PLAN.md` — the executable plans (frontmatter waves/depends_on, `<read_first>`/`<acceptance_criteria>`/`<action>`, `<threat_model>`)
2. `.planning/phases/04-booking-core-search-no-payment/04-RESEARCH.md` — Pattern 1 (two-stage search SQL), Pattern 2 (`createPendingHold` WR-03 shape), Pitfalls, Security Domain
3. `.planning/phases/04-booking-core-search-no-payment/04-CONTEXT.md` — the 21 locked decisions D-29..D-49 + out-of-scope fences
4. `.planning/phases/04-booking-core-search-no-payment/04-UI-SPEC.md` — the visual/interaction contract (also the human-verify reference for 04-08)
5. `.planning/phases/04-booking-core-search-no-payment/04-PATTERNS.md` — analog files + excerpts for concrete `<read_first>`

## Infrastructure State
- **Docker Postgres**: bring up with `npm run db:up` before executing Wave 1 — the [BLOCKING] migration (04-01) and all DB-reading tests need it.
- **Migrations**: this project uses `npm run db:generate` → `npm run db:migrate` (numbered SQL in `drizzle/`, hand-authored EXCLUDE), NOT `drizzle-kit push`. Latest applied = `0005_booking_exclusion.sql`; Phase 4 adds `0006_booking_hold.sql`.
- **`.planning/config.json`**: shows modified (ephemeral `_auto_chain_active: false` flag from plan-phase §15) — safe to leave or discard.
- **Tests**: Vitest (per-worker isolated schema; `makeRacingClients` for concurrency proofs) + Playwright E2E; UAT seed host `host@fitout.test` (see memory `local-env-and-uat-seed`).

<context>
The plan set is checker-approved (0 blockers, 12/12 dimensions). Correctness spine to preserve during execution: the DB `booking_no_overlap` EXCLUDE constraint is the SOLE double-booking authority — never app-level query-then-insert; `timestamptz` UTC + venue-local at the edges; server re-derives price/window (never trust client). Two-stage search reuses `getAvailability` so search and the listing calendar cannot diverge. All 8 requirements + 17/17 tracked decisions are covered.
</context>

<next_action>
Start with: `npm run db:up`, then `/gsd:execute-phase 4` (begins Wave 1 — 04-01 migration substrate + 04-02 schemas/seed).
</next_action>
