---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 10
type: execute
wave: 3
depends_on: ["19.1-02"]
files_modified:
  - e2e/avatar-crop.spec.ts
  - e2e/overflow-320.spec.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 — the four upload-dependent failures are diagnosed before any is written off, and the branch is taken from plan 02's measured verdict rather than from a hypothesis. If they pass on this machine the CI failures are an environment-capability gap; if they fail here too they are broken specs and every one of them is repaired."
    - "CI-01 SC4 — the obvious fix is refused on two grounds and the second is the one that matters. Giving the workflow a media-provider credential would turn the checker RED by design, because a shipped invariant requires zero secret references across every job; and it would put a live third-party credential into a repository that is about to become public. The credential route is closed and this plan records why at the site."
    - "CI-01 SC4 — where the capability genuinely cannot exist in CI, the product path is still exercised against a stub rather than skipped. A request intercept over the upload endpoint returning a canned response lets the product's own upload path run end to end; the specs already use request interception extensively and tear it down explicitly, so the idiom exists and is not being invented."
    - "CI-01 SC4 — an intercept WEAKENS what those cases prove, and the specs say so out loud rather than silently. Each intercepted case gains a docblock stating exactly which part of the original assertion is no longer exercised — the real network round trip and the provider's own response shape — and naming the test that still covers it."
    - "CI-01 SC4 — the photos-step failure is repaired by seeding, not by intercepting. Its assertion is on the cover-preview heading, which the step renders once the listing has at least one photo; a photo row pointing at a committed local asset satisfies it without any upload, and the file already establishes that precedent for a local rather than remote asset."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — an intercept that matched nothing must be RED, not silently green. Each intercepted case asserts the handler was actually invoked at least once before asserting anything about the result, because an intercept whose pattern stopped matching produces exactly the same passing run as one whose product path never ran."
    - "CI-01 encoding (spec-less edge probe, verification: backstop) — { statement: 'the canned upload response is matched and returned by content type and body shape rather than by byte-identical payload comparison, and the intercepted case states which fields of the real response it reproduces and which it does not', verification: backstop }"
    - "D-01 / D-03 — any case that ends up annotated instead of repaired carries a written reason naming the specific capability, the invariant that forbids supplying it with its file and line, and the test that covers the behaviour elsewhere. A reason that says only that the environment differs is not a reason."
  artifacts:
    - e2e/avatar-crop.spec.ts
    - e2e/overflow-320.spec.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt
  key_links:
    - "the zero-secret-references invariant in `scripts/verify-workflows.mjs` to every proposed fix for these four. It is what makes the credential route not merely inadvisable but self-defeating, and plans 01 and 05 spent this phase strengthening the job that runs it."
    - "the existing request-interception and teardown helpers in `e2e/avatar-crop.spec.ts` to the new intercepts. The file already installs handlers and drops them explicitly in its teardown; a new intercept that does not route through that teardown leaves handlers holding server actions open."
    - "the committed local asset under the public assets directory to the photos-step repair. The precedent is already recorded in `e2e/overflow-320.spec.ts` around `:2740`, which states that the asset is local rather than remote and is served by the dev server."
    - "any annotated case to plan 11's pinned count and reason assertions. Every entry this plan creates is one plan 11 must count and must find a reason on."
  prohibitions:
    - "A media-provider credential must never be added to the CI workflow. It would turn the checker red by design and it would place a live third-party credential in a repository about to become public."
    - "An intercept must never be installed outside the file's existing teardown path. A handler left holding every server action open on a gate the case never releases is a documented failure mode in this file."
    - "An intercepted case must never claim to prove what it no longer proves. The docblock names what is no longer exercised and where that coverage now lives."
    - "An annotation reason must never be generic. Name the capability, name the invariant that forbids supplying it with its file and line, and name the test that covers the behaviour elsewhere."
    - "The upload assertion must never be deleted to make a case pass. A deleted assertion is an allowlist entry with no entry."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `empty` explicitly (an intercept that matched nothing must be red) and `encoding` as a backstop marker (which fields of the provider response the canned one reproduces). `adjacency` and `ordering` are owned by plans 01, 03, 06, 07, 08 and 09; none is dropped."
    - "A3 (RESEARCH.md) — whether these cases pass locally is settled by plan 02, not re-litigated here. This plan reads that verdict and branches on it."
    - "The three avatar cases may not all take the same route. One may be interceptable, another may need seeding, a third may be annotatable; the verdict is per case and recorded per case."
    - "RESEARCH.md route 1 notes that intercepting weakens what the three cases prove. That is accepted deliberately when the capability cannot exist in CI, and it is recorded rather than hidden — but it is NOT accepted for a case that turns out to be simply broken."
---

<objective>
Settle and close the four failures that depend on a media-upload capability CI structurally cannot have:
three avatar cases and the listing-photos step at 320px.

Purpose: the fastest way to make these pass is to give CI a provider credential, and that is the one
security-shaped trap specific to SC4 — it would turn this phase's own checker red by design, and it would
put a live third-party credential into a repository about to become public. Both halves are reasons to
refuse it and the second is the one that matters.

Output: each of the four with a recorded route — repaired, intercepted with a stated weakening, seeded, or
annotated with a specific written reason — and no credential anywhere near the workflow.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read plan 02's verdict, choose a route per case, and record why the credential route is closed</name>

  <read_first>
    - `.planning/phases/19.1-.../evidence/tnum-phase17-probe.txt` — plan 02's avatar verdict and the
      transcript it rests on. This decides whether these are broken specs or an environment gap.
    - `e2e/avatar-crop.spec.ts:322-329` — the file header stating that three cases perform a real upload
      and that the upload is unavoidable rather than convenient in each. Read the reason it gives.
    - `e2e/avatar-crop.spec.ts:360-375` — the teardown that drops installed request handlers, and the
      comment describing what happens when a handler is left holding server actions open.
    - `e2e/avatar-crop.spec.ts:980-990` and `:1620-1640` — the existing request-interception call sites,
      for the idiom a new intercept must copy.
    - `e2e/overflow-320.spec.ts` around `:2740` — the note establishing that a committed local asset is
      used rather than a remote one, and that the dev server serves it.
    - `e2e/overflow-320.spec.ts` around `:3400` — the failing row, its assertion on the cover-preview
      heading, and the condition under which the photos step renders that heading.
    - `tests/listing/photos.test.ts:434` — the sentence naming the absent-credential condition as the state
      every CI run is in.
    - `scripts/verify-workflows.mjs:538-546` — the zero-secret-references invariant, with its exact line
      reference, which every annotation reason must cite.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Cause B", the three routes in preference order and the
      security note beneath them.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt
  </files>

  <action>
Open `evidence/triage-upload-capability.txt` with plan 02's avatar verdict quoted verbatim, including the
transcript lines it was read from. Then decide a route for each of the four cases individually and record
each decision as its own line beginning `ROUTE`, with the case's file and line, the chosen route, and the
reason.

The permitted routes, in preference order:

  1. REPAIR — if plan 02's verdict says the case fails locally too, it is a broken spec. Repair it. This
     route takes precedence over every other and an intercept is NOT an acceptable substitute for it.
  2. SEED — the assertion does not actually require an upload, only the state an upload would have
     produced. Seed that state from a committed local asset. This is the expected route for the
     photos-step row, whose assertion is on the cover-preview heading rather than on the upload.
  3. INTERCEPT — the capability genuinely cannot exist in CI and the product path is worth exercising
     against a stub. Permitted only with a docblock stating what is no longer proven.
  4. ANNOTATE — none of the above applies. The written reason must name the specific capability, cite the
     zero-secret-references invariant by file and line as the reason the capability cannot be supplied, and
     name the test that covers the behaviour elsewhere.

Then write a section headed WHY THE CREDENTIAL ROUTE IS CLOSED, stating both grounds with their evidence:
the invariant's file and line and what it asserts, and the fact that the repository is about to become
public under D-07 and D-08 so a credential added now would be published. State that the second ground is
the one that matters. This section is what stops the next reader re-proposing it.

Do not edit any spec in this task.
  </action>

  <acceptance_criteria>
    - `evidence/triage-upload-capability.txt` quotes plan 02's avatar verdict verbatim with its transcript
      lines.
    - It contains exactly four lines beginning `ROUTE`, one per failing case, each naming a file, a line
      number, a route from the permitted list and a reason.
    - No case is routed to INTERCEPT or ANNOTATE if plan 02's verdict says it fails locally.
    - The WHY THE CREDENTIAL ROUTE IS CLOSED section cites the invariant's file and line and names the
      publication consequence.
    - `git status --porcelain -- e2e` produces no output.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^ROUTE' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt</automated>
    <fails_when>output is any number other than 4 — one of the four upload-dependent cases has no recorded route, or a route was recorded twice</fails_when>
    <automated>grep -c 'verify-workflows.mjs:538' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt</automated>
    <fails_when>output is 0 — the invariant that closes the credential route is not cited by file and line, so the closure rests on assertion rather than evidence</fails_when>
    <automated>git status --porcelain -- e2e</automated>
    <fails_when>any output — a spec was edited during the routing task</fails_when>
  </verify>

  <done>
    Each of the four cases has a route chosen against plan 02's measured verdict, and the credential route
    is closed in writing with the invariant and the publication consequence behind it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the routes for the three avatar cases</name>

  <precondition>
    Docker Desktop is running with `fitout-db-1` up, the database is migrated and seeded, the build cache
    directory has been removed, and TCP port 3000 is free. The media-provider credential must be in
    whatever state plan 02's verdict was measured under, so this task's runs are comparable to it.
  </precondition>

  <read_first>
    - `evidence/triage-upload-capability.txt` — Task 1's four `ROUTE` lines.
    - `e2e/avatar-crop.spec.ts:322-329` and `:355-380` — the file header and the teardown, in full.
    - `e2e/avatar-crop.spec.ts:980-990` and `:1620-1640` — the existing interception call sites and the
      comment block around `:1480` explaining why the bytes are taken from the request rather than from the
      blob. A new intercept must be consistent with that reasoning.
    - `e2e/avatar-crop.spec.ts` around `:974`, `:2675` and `:2747` — the three failing cases and what each
      asserts after the upload.
    - `tests/listing/photos.test.ts` — the test named as covering the behaviour elsewhere, so the
      annotation reason can name it precisely.
  </read_first>

  <behavior>
    - Under an INTERCEPT route: the product's upload path runs end to end against a canned response, the
      handler is asserted to have been invoked at least once, and the case's original post-upload assertion
      still runs.
    - Under an INTERCEPT route: an intercept whose pattern stops matching is RED, because the invocation
      assertion fires before any result assertion.
    - Under an INTERCEPT route: the handler is dropped by the file's existing teardown; no case leaves a
      handler installed.
    - Under a REPAIR route: the case passes without any intercept and without any credential.
    - Under an ANNOTATE route: the case is marked as an expected failure conditional on the CI environment,
      with a reason string naming the capability, the invariant with its file and line, and the covering
      test — and it still runs and still asserts locally.
  </behavior>

  <files>
    e2e/avatar-crop.spec.ts
  </files>

  <action>
Execute each avatar case's `ROUTE` line exactly.

FOR AN INTERCEPT: install the handler through the file's established interception idiom, so the existing
teardown drops it. Return a canned response for the upload endpoint with the content type and the response
fields the product's own upload path reads — determine those from the product code, not from memory.
Before any assertion about the result, assert the handler was invoked at least once, with a message stating
that an intercept that matched nothing produces exactly the same passing run as a product path that never
ran. Add a docblock above the case stating plainly what the intercept means the case NO LONGER proves — the
real network round trip and the provider's own response shape — and naming the test that still covers it.

FOR A REPAIR: fix the case so it passes with no credential and no intercept, and record in the docblock
what was actually wrong, since plan 02's verdict established it was not the environment.

FOR AN ANNOTATE: mark the case as an expected failure conditional on the CI environment, with a reason
string that names the capability, cites the zero-secret-references invariant by file and line as the reason
the capability cannot be supplied, and names the covering test. The annotation takes a condition and a
reason; never a bare call. The case must still run and still assert locally.

Under no route may the upload assertion be deleted. A deleted assertion is an allowlist entry with no
entry.

After the edits, run the spec locally and capture the transcript into
`evidence/triage-upload-capability.txt` under a POST-ROUTE heading, recording pass, expected-failure and
failure counts per case.
  </action>

  <acceptance_criteria>
    - Every avatar case named in a `ROUTE` line has been changed according to that route and no other.
    - Every intercepted case asserts handler invocation before asserting on the result, and carries a
      docblock naming what it no longer proves and the covering test.
    - Every intercept is dropped by the file's existing teardown; the file installs no handler outside that
      path.
    - Every annotated case takes a condition and a reason string; the reason names a capability, a file and
      line, and a covering test.
    - No upload assertion was deleted — the count of upload-related assertions in the file is not lower
      than at HEAD.
    - `npx playwright test e2e/avatar-crop.spec.ts --project=chromium` reports zero unexpected failures.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>npx playwright test e2e/avatar-crop.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or the summary block reporting a non-zero "failed" count, or the summary reporting any unexpectedly-passing test</fails_when>
    <automated>grep -c 'unrouteAll' e2e/avatar-crop.spec.ts</automated>
    <fails_when>output is less than 1 — the teardown that drops installed handlers is gone, and a handler left holding server actions open is a documented failure mode in this file</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output — a workflow edit was made to supply a credential</fails_when>
  </verify>

  <done>
    The three avatar cases each took their recorded route, every intercept proves it was actually invoked
    and states what it stopped proving, and no credential exists anywhere near the workflow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seed the photos step from a committed local asset</name>

  <precondition>
    Docker Desktop is running with `fitout-db-1` up, the database is migrated and seeded, the build cache
    directory has been removed, and TCP port 3000 is free.
  </precondition>

  <read_first>
    - `e2e/overflow-320.spec.ts` around `:3400` — the failing row, the walk it performs, and its assertion
      on the cover-preview heading, including the sentence stating that only the photos step renders that
      heading and only once the listing has at least one photo.
    - `e2e/overflow-320.spec.ts` around `:2740` — the existing note that the asset used is local and
      committed rather than remote, and that the dev server serves it. This is the precedent.
    - `public/vrt/photo-0.svg` — the committed asset the precedent points at.
    - the listing-photo seeding path used elsewhere in the suite — find it from the helpers this spec
      already imports, and reuse it rather than writing a second seeder.
    - `evidence/triage-upload-capability.txt` — Task 1's `ROUTE` line for this case.
  </read_first>

  <behavior>
    - The listing under test has at least one photo row before the photos step is reached, so the
      cover-preview heading renders and the walk continues.
    - The seeded photo points at a committed local asset served by the dev server, not at a remote URL.
    - The seed is torn down with the rest of the fixture; no photo row survives the run.
    - The case still fails if the cover-preview heading does not render — the seeding removes the
      precondition gap, not the assertion.
  </behavior>

  <files>
    e2e/overflow-320.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt
  </files>

  <action>
Seed a listing-photo row for the listing this row measures, pointing at a committed local asset under the
public assets directory, using the seeding helper this spec's fixtures already use. Do not write a second
seeder and do not upload anything.

Add a comment at the seed site stating what was MEASURED: the assertion is on the cover-preview heading,
which the photos step renders only once the listing has at least one photo; the CI environment has no
media-provider credential and structurally cannot have one; and seeding the state an upload would have
produced exercises the surface this row measures without touching the upload path. Point at the existing
local-asset precedent in the same file by line reference.

Ensure the seeded row is removed by the same teardown that removes the rest of the fixture.

Then run the row locally and capture the transcript into `evidence/triage-upload-capability.txt` under the
POST-ROUTE heading.

Finally prove the assertion still bites: temporarily remove the seed on the TRACKED file, run the row, and
capture the RED transcript carrying the heading-not-found message; restore the seed and record the
checksum pair and `git status --porcelain` as the revert proof. Close the evidence file with a line
beginning `SUMMARY` listing each of the four cases and its final state — repaired, seeded, intercepted or
annotated — so plan 11 can count annotations without reopening this file.
  </action>

  <acceptance_criteria>
    - `e2e/overflow-320.spec.ts` seeds a listing-photo row from a committed local asset path that resolves
      under `git ls-files`, using an existing seeding helper.
    - A comment at the seed site names the measurement, the capability gap and the local-asset precedent by
      line reference.
    - The seeded row is torn down with the rest of the fixture.
    - `evidence/triage-upload-capability.txt` contains a post-route transcript, a RED transcript from the
      temporary seed removal with the heading-not-found message, a revert proof, and a `SUMMARY` line
      covering all four cases.
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium` reports zero unexpected failures.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>grep -oE 'public/vrt/[a-z0-9-]+\.svg' e2e/overflow-320.spec.ts | sort -u | while read -r p; do git ls-files --error-unmatch "$p" >/dev/null || { echo "UNTRACKED $p"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or any line beginning "UNTRACKED " — the spec references a local asset path that is not a tracked file, so the dev server would not serve it in CI</fails_when>
    <automated>npx playwright test e2e/overflow-320.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or the summary block reporting a non-zero "failed" count — the temporary seed removal was left applied or the row still fails</fails_when>
    <automated>grep -c '^SUMMARY' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-upload-capability.txt</automated>
    <fails_when>output is 0 — the four cases have no consolidated final-state line, and plan 11 would have to re-derive the annotation count</fails_when>
  </verify>

  <done>
    The photos step reaches the surface it measures without any upload, the seeded state is torn down with
    the fixture, the assertion is proved still able to fail, and all four upload-dependent cases have a
    recorded final state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a third-party media provider → the CI job's environment | The capability the specs want is a live credential; supplying it crosses a boundary the repository is invariant-forbidden from crossing. |
| a request intercept → what a test claims to prove | An intercepted case proves less than an unintercepted one, and the difference must be stated rather than absorbed. |
| the phase's evidence directory → the public repository | Everything recorded here is published under D-07 and D-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-42 | Information Disclosure | a media-provider credential added to the workflow to make specs pass | high | mitigate | Prohibited on two recorded grounds; Task 1 writes the closure with the invariant's file and line, and Task 2's verify runs the checker to confirm no workflow edit was made. |
| T-19.1-43 | Spoofing | an intercept that matched nothing, passing silently | high | mitigate | Every intercepted case asserts handler invocation before asserting on the result, with a message naming the indistinguishable-outcomes problem. |
| T-19.1-44 | Repudiation | a case claiming coverage an intercept removed | high | mitigate | Each intercepted case carries a docblock naming what it no longer proves and the test that still covers it. |
| T-19.1-45 | Tampering | a leaked handler holding server actions open across cases | medium | mitigate | Every intercept routes through the file's existing teardown; the verify asserts the teardown is still present. |
| T-19.1-46 | Tampering | deleting an upload assertion instead of routing the case | high | mitigate | Prohibited; the acceptance criteria require the upload-assertion count not to fall. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; RESEARCH.md's audit records zero proposed packages. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed symbols in `e2e/avatar-crop.spec.ts`:** per-case route execution — new interception handlers
routed through the existing teardown with invocation assertions and weakening docblocks, or direct repairs,
or conditional expected-failure annotations with reason strings naming a capability, an invariant file and
line, and a covering test.

**Changed symbols in `e2e/overflow-320.spec.ts`:** a listing-photo seed from a committed local asset for
the photos-step row, with a comment naming the measurement and the local-asset precedent, torn down with
the fixture.

**New evidence file:** `evidence/triage-upload-capability.txt` — plan 02's verdict quoted, four `ROUTE`
lines, a WHY THE CREDENTIAL ROUTE IS CLOSED section, post-route transcripts, a seed-removal RED with revert
proof, and a `SUMMARY` line covering all four cases.

**Consumed by plan 11:** the `SUMMARY` line's annotation entries, if any.

**New npm scripts:** none. **No production source file is modified.**
</artifacts_this_phase_produces>

<verification>
- `npx playwright test e2e/avatar-crop.spec.ts e2e/overflow-320.spec.ts --project=chromium` reports zero
  unexpected failures and zero unexpected passes.
- `node scripts/verify-workflows.mjs` is green — no credential reached the workflow.
- Every local asset path referenced by the specs is a tracked file.
- `npx tsc --noEmit` exits 0.
- The evidence file carries four routes, a closure section and a four-case summary line.
</verification>

<success_criteria>
All four upload-dependent failures are closed without a credential: each took a route chosen against a
measured verdict, every intercept proves it ran and says what it stopped proving, the photos step is
satisfied by seeded state pointing at a committed local asset, and any remaining annotation carries a
reason specific enough to act on.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-10-SUMMARY.md` when done.
List the four cases and their final states, and state the exact number of annotation entries this plan
created so plan 11's pinned count is a sum rather than a survey.
</output>
