---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-workflows.mjs
  - tests/design/workflow-invariants.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-pre-fix.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-post-fix.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC1 — the job that executes this repository's workflow checker is itself checked. `scripts/verify-workflows.mjs` gains a `gate-db-free` block modelled structurally on the `gate-e2e` block at `:725-866`: a hard stop when the job key is absent, a display-name pin, and an invariant asserting the checker step exists, is anchored by its exact `name:` string, carries the exact no-argument invocation, and declares no key outside `[name, run]`. CR-03 reproduced all three of its mutations at 50 green; each is a named FAIL line after this plan."
    - "CI-01 / SC1 — the display name `gate-db-free (lint + design + build + workflow parse)` is pinned as a string, and the pin's comment states WHY: GitHub matches a required status check on `name:`, so a `name:` edit would silently unbind the gate SC5 is about to make required (D-05) while leaving the job perfectly intact. That is the `:741-751` rationale transferred to the second job that carries it."
    - "CI-01 adjacency (spec-less edge probe, verification: explicit) — `node scripts/verify-workflows.mjs` and `node scripts/verify-workflows.mjs <anything>` land on OPPOSITE sides of the checker-step invariant. The conjunct is an exact-equality comparison after `.trim()`, never a containment test, because `run` is a PERMITTED key and the allow-list sees key sets rather than values: only exact equality can see an appended argument. This is the WR-01 defect being fixed elsewhere in this phase, and writing the new predicate as containment would ship it in a new place on the day it is closed."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — when several steps of `gate-db-free` could compare equal under a loose anchor, the selected step is specified and stable: it is the unique step whose `name:` equals `Verify the workflow invariants (parse, not grep)`. A decoy step inserted FIRST whose `run:` merely mentions the checker path does NOT capture the anchor, and a standing positive-control case asserts that decoy leaves the checker GREEN. `gate-db-free`'s step 1 already carries multi-line free text, so a substring anchor in this job would have been capturable on arrival."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — the new block cannot pass over nothing. An absent `gate-db-free` job hits a `hardStop` rather than being universally quantified into a vacuous pass (the `:725-739` precedent, and the reason that precedent is deliberately NOT a counted invariant), and the checker-step invariant is false rather than vacuously true when `stepsOf(checker)` yields no matching step, because the conjunct asserts the found step is defined before reading its `run`."
    - "CI-01 encoding (spec-less edge probe, verification: explicit) — whose definition of equality applies is stated at the site. The checker compares the `run` scalar the YAML parser produced, normalised only by `.trim()`; no comment-stripping and no whitespace collapsing, because a `run:` body is executable text and any further normalisation would make two different commands compare equal. On the test side `ci.yml` is checked out CRLF, so every inserted mutation line uses `eolOf(text)` and never a hard-coded newline."
    - "CI-01 / SC1 — the invariant total moves in all THREE spellings in the same commit: the checker's own printed summary line, `.github/workflows/ci.yml:362`'s quoted copy of it, and `tests/design/workflow-invariants.test.ts:100`'s `EXPECTED_GREEN`. The control case (case 1) is what makes a silent count change impossible, and it is also what turns a half-done bookkeeping edit red for a reason unrelated to the real signal — so all three move together or none do."
    - "CI-01 / SC1 — every predicate this plan adds is proved TWICE: by a hand mutation on the TRACKED file captured GREEN before the fix and RED after it, with `md5sum` and `git status --porcelain` revert proof in `evidence/guards-01-*.txt`; AND by a standing case in `tests/design/workflow-invariants.test.ts` that spawns the shipped checker against a mutated copy. Phase 19 rounds 1-3 did the first and skipped the second, which is exactly why round N+1 began with no memory of round N."
    - "CI-01 — the standing suite grows from twelve cases to fifteen and every new case names what was MEASURED, not what is being tested: deleting the checker step is red, appending an argument to its invocation is red, and a decoy `run:` mentioning the checker path ahead of it stays GREEN. The third is the only new case that asserts green and it carries its own justification comment saying so."
    - "Nothing executable in `.github/workflows/ci.yml` moves. The only bytes that change in that file are comment lines — the invariant-count paragraph and a new `gate-db-free` bullet list beside the existing `gate-e2e` one — proven by a `git diff -U0` in which every added and removed line begins with `#`. `gate-db-free`'s `name:`, steps, `run:` bodies and `env:` map are asserted, never edited."
  artifacts:
    - scripts/verify-workflows.mjs
    - tests/design/workflow-invariants.test.ts
    - .github/workflows/ci.yml
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-pre-fix.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-post-fix.txt
  key_links:
    - "`.github/workflows/ci.yml`'s `gate-db-free` job → a `ci` section `check()` in `scripts/verify-workflows.mjs`. Before this plan NOTHING in the checker read this job, so deleting the two-line step that runs the checker left `All 50 invariants hold` and exit 0 — reproduced twice, by the reviewer and independently by the verifier (CR-03)."
    - "`gate-db-free`'s `name:` string → the required-status-check context SC5 configures on `main`. The pin added here is what stops a `name:` edit detaching the gate from branch protection; plan 15 consumes the same string verbatim."
    - "the checker's printed summary line → `.github/workflows/ci.yml:362` → `tests/design/workflow-invariants.test.ts:100`'s `EXPECTED_GREEN`. Three spellings of one number; the control case is the instrument that binds them."
    - "the exact step `name:` → `stepsOf(checker).find(...)`. This is the anchor rule (PATTERNS.md §F) applied on arrival rather than retrofitted after a decoy defeats it, which is the entire content of CR-02 one job over."
  prohibitions:
    - "A positive assertion must never LOCATE its subject by a substring of a `run:` body. Substring matching is safe only in the deny direction, where over-matching produces a false red and therefore fails closed; used to find the thing being asserted about, it hands the anchor to whoever writes the next step."
    - "No `continue-on-error:` may be introduced anywhere in `.github/workflows/ci.yml`, at any level. It is the single most self-defeating edit available in this phase: it turns the unconditional invariant red by design, and on `gate-db-free` it would make the job this plan is constraining report success while doing nothing."
    - "No `secrets.` reference may be introduced anywhere in `.github/workflows/ci.yml`. A shipped invariant requires zero of them across every job, and the three build-only placeholders in `gate-db-free` are literals precisely so that promoting them goes red."
    - "A documented invariant count must never outlive the count it documents. A stale total is a false claim of exactly the class this phase exists to delete, and it is the cheapest one in the file to detect."
    - "An invariant must never be proved only by a hand-applied mutation. The hand mutation proves the fix today; the standing case is what remembers it, and a fix with no standing case is a fix the next round will not know happened."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this phase has no SPEC.md, so the four-item probe report was authored rather than lifted. All four items are RESOLVED with verification: explicit and appear above as plain truths: adjacency (exact-equality vs containment on the invocation), ordering (anchor by exact `name:`, decoy control asserts green), empty (hard stop plus a defined-step conjunct, no vacuous pass), encoding (`.trim()` only, `eolOf` for CRLF). Zero items dismissed, zero silent drops. Plans 06 and 11 re-derive ordering and encoding for their own subjects rather than citing this one."
    - "PROHIBITION PROBE — five bespoke must-NOTs authored descriptor-less above. Routine-engineering candidates were dropped (must not leave a temp directory behind; must not hard-code a newline). The `--update-snapshots` and visual-baseline prohibitions are minted in plan 12, which owns that subject; the allowlist prohibitions in plan 11."
    - "A10 (RESEARCH.md Assumptions Log) — the invariant total after this plan is estimated at 52 (`ci` 31 → 33: one for the display-name pin, one for the checker-step invariant; the hard stop is deliberately uncounted). THE NUMBER IS WHATEVER THE CHECKER PRINTS. Take it from the run, never from this estimate, and if it differs, move all three spellings to the printed value and note the delta in the SUMMARY."
    - "ASSUMPTION DELTA — recorded below in `<assumption_delta_decision>`: primary noun UNCHANGED, decision `no-change`."
---

<objective>
Constrain `gate-db-free` — the job that actually executes `scripts/verify-workflows.mjs`, `lint`,
`test:design` and `next build` — so that deleting or softening its checker step turns the checker RED.

This is the phase's TRACER plan. It carries ONE complete SC1 invariant through every layer the rest of
the phase repeats: checker constants → a `ci` section predicate → the three-place count bookkeeping →
a standing mutation case in the design suite → a watched-red/watched-green evidence pair on the tracked
file. Everything in plans 05 and 06 is expansion out from this proven slice.

Purpose: Phase 19 rounds 2-4 each hardened the guard INSIDE `gate-e2e` while the job that runs the guard
sat unprotected. CR-03 reproduced three separate one-line defeats of it, twice. Until this closes, every
other invariant in the file is enforced by a step nothing asserts the existence of.

Output: a `gate-db-free` block in the checker, three new standing cases, a moved invariant total in three
spellings, and the phase's `evidence/` directory with its first watched-red pair.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md
</context>

<assumption_delta_decision>
The assumption-delta detector fired `detected: true` on two prose connectives — the word "also" in
"*presence* **also** asserts the absence of the modifiers…" and "alongside" in "ships an attack recipe
**alongside** the code it applies to".

- Primary noun: **unchanged**
- Decision: **`no-change`**
- Rationale: **detector matched prose connectives, not an identity-model transition; no abstraction
  gains or loses a monopoly in this phase.**

No identity-model redesign is opened. Neither match is a singular→plural, required→optional, or
derived→chosen transition.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — "deleting gate-db-free's checker step turns the checker RED", one path only</name>

  <read_first>
    - `scripts/verify-workflows.mjs:85-96` — the `CI_E2E_JOB` / `CI_E2E_CONTEXT` constant pair and the
      comment distinguishing the YAML job KEY from the `name:` a required status check matches on. New
      constants go here, as siblings.
    - `scripts/verify-workflows.mjs:143-149` — `MAIL_STEP_ALLOWED_KEYS` and the "THIS IS AN ALLOW-LIST AND
      NOT A DENY-LIST" rationale block above it. Copy the shape and the reasoning, not the values.
    - `scripts/verify-workflows.mjs:725-751` — the `gate-e2e` hard stop and display-name pin. This is the
      structural template for the whole block; read the hard stop's body text, which explains why every
      other assertion in the section would pass vacuously without it.
    - `scripts/verify-workflows.mjs:885-950` — Invariant A: the exact-`name:` anchor at `:890`, the
      `Object.keys(...).sort()` allow-list filter at `:897`, and the exact-invocation conjunct at `:938`.
      This is the predicate shape to replicate.
    - `.github/workflows/ci.yml:675-700` — `gate-db-free`'s job header, `name:` and step list. Read the
      exact `name:` strings; they are the constants, not paraphrases of them.
    - `.github/workflows/ci.yml:340-372` — the invariant-count paragraph and the `gate-e2e` bullet list,
      including the `⚠ THE LIST IS EIGHT AND THE TOTAL IS 50` line and the verbatim summary quote at :362.
    - `tests/design/workflow-invariants.test.ts:85-100` — `CHECKER` resolution, `withMutatedWorkflows`
      preamble and `EXPECTED_GREEN`.
    - `tests/design/workflow-invariants.test.ts:134-176` — `withMutatedWorkflows` and its
      differs-from-input assertion, and `expectRed`.
    - `tests/design/workflow-invariants.test.ts:288-310` — case 1 (the green control) and case 2, for the
      exact `it(...)` and docblock idiom.
    - `.planning/phases/19.1-.../19.1-PATTERNS.md` §1 and §2 — the constants, hard-stop, display-name,
      allow-list, step-anchor and exact-invocation patterns quoted with their line numbers.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt` —
      the raw-transcript evidence convention this plan reproduces.
  </read_first>

  <files>
    scripts/verify-workflows.mjs,
    tests/design/workflow-invariants.test.ts,
    .github/workflows/ci.yml,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-pre-fix.txt,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-post-fix.txt
  </files>

  <action>
Create the phase evidence directory
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/`.

STEP 1 — capture the defect before fixing it. On the TRACKED `.github/workflows/ci.yml`, delete the
two-line step whose `name:` is `Verify the workflow invariants (parse, not grep)` from the `gate-db-free`
job. Run `node scripts/verify-workflows.mjs` and capture the full stdout plus the exit code into
`evidence/guards-01-pre-fix.txt` as a raw console transcript (not prose), under a heading naming the
mutation. It must show the green summary line and exit 0 — that is CR-03 reproduced. Then restore the
file with `git checkout -- .github/workflows/ci.yml` and append `md5sum .github/workflows/ci.yml` before
and after plus `git status --porcelain` to the same evidence file as the revert proof.

STEP 2 — author the block in `scripts/verify-workflows.mjs`. Beside `CI_E2E_JOB` and `CI_E2E_CONTEXT` at
`:95-96`, declare `CI_CHECKER_JOB = "gate-db-free"`, `CI_CHECKER_CONTEXT` set to the job's exact `name:`
string `gate-db-free (lint + design + build + workflow parse)`, `CHECKER_STEP_NAME` set to
`Verify the workflow invariants (parse, not grep)`, `CHECKER_RUN` set to
`node scripts/verify-workflows.mjs`, and `CHECKER_STEP_ALLOWED_KEYS` set to the two-element array
`name`, `run` — byte-identical to that step's parsed key set. Carry the job-KEY-versus-`name:` comment
forward to the new pair.

In the `ci` section, after the `gate-e2e` block, add:

(a) A `hardStop` when `doc?.jobs[CI_CHECKER_JOB]` is absent, modelled on `:725-739`, with a body that
states the same argument in its own words: every other assertion in the section is universally quantified
over the jobs that remain, so all of them would still pass; a genuine retirement is a decision to record,
so remove the hard stop in the same commit and say why. This is NOT a counted invariant, matching the
`gate-e2e` precedent.

(b) One counted `check()` asserting the checker step. Find it with
`stepsOf(checker).find((s) => String(s?.name ?? "") === CHECKER_STEP_NAME)` — the `:890` idiom, anchored
by exact `name:` and never by a substring of any `run:` body. The conjuncts are: the found step is
defined; its `run` compared with `.trim()` for EXACT equality against `CHECKER_RUN`; and the sorted
`Object.keys` of the step filtered against `CHECKER_STEP_ALLOWED_KEYS` is empty. Do not write the
invocation conjunct as a containment test — `run` is a permitted key, the allow-list sees key sets and
not values, so exact equality is the only conjunct that can see an appended argument. The printed name
must state all three properties, and the evidence string must print the found step's name, the trimmed
run, and the offending extra keys (never a value from `env:`).

STEP 3 — move the count. Run the checker, read the printed summary line, and write those exact bytes into
BOTH `.github/workflows/ci.yml:362`'s verbatim quote and `tests/design/workflow-invariants.test.ts`'s
`EXPECTED_GREEN`. Do not compute the number; take it from the run. Add a `gate-db-free` bullet list to
`ci.yml`'s comment block as a SIBLING beside the `gate-e2e` list at `:352-372`, in the same style, and
update the count paragraph's prose so it names both lists. The `⚠ THE LIST IS EIGHT` figure counts
`gate-e2e` bullets and does not move.

STEP 4 — the standing case. Add case 13 to `tests/design/workflow-invariants.test.ts` in the file's
existing `it(...)` shape: a mutation that removes the checker step's two lines from the mutated copy, and
`expectRed` naming a stable fragment of the new invariant's printed name. Declare that fragment as a
`const` beside the existing fragments at `:274-282`. Every inserted or joined line must use `eolOf(text)`
because `ci.yml` is checked out CRLF.

STEP 5 — capture the fix. Re-apply the STEP 1 mutation to the TRACKED file, run the checker, and capture
the RED output with the FAIL line naming the new invariant into `evidence/guards-01-post-fix.txt`, then
revert and record `md5sum` before/after plus `git status --porcelain` in the same file.
  </action>

  <acceptance_criteria>
    - `node scripts/verify-workflows.mjs` exits 0 against the unmutated tree and prints a summary line
      whose invariant total is strictly greater than 50.
    - The exact bytes of that summary line appear in `.github/workflows/ci.yml` and as the value of
      `EXPECTED_GREEN` in `tests/design/workflow-invariants.test.ts` — a three-way byte comparison passes.
    - `grep -c 'CI_CHECKER_JOB\|CHECKER_STEP_NAME\|CHECKER_STEP_ALLOWED_KEYS' scripts/verify-workflows.mjs`
      is at least 3.
    - The new checker-step predicate region contains zero containment calls: extracting the region between
      the new `check(` opening and its closing `);` and counting containment-method occurrences yields 0.
    - `evidence/guards-01-pre-fix.txt` contains a transcript in which the checker printed its green summary
      line and exited 0 while the checker step was absent from `gate-db-free`.
    - `evidence/guards-01-post-fix.txt` contains a transcript in which the same mutation produced a
      non-zero exit and a line beginning `FAIL` that names the new invariant, plus `git status --porcelain`
      producing no line for `.github/workflows/ci.yml` after the revert.
    - `tests/design/workflow-invariants.test.ts` contains an `it("case 13` block and the whole design file
      passes.
    - `git diff -U0 -- .github/workflows/ci.yml` shows every added and removed line beginning with `#`.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output, or the final summary line still reads a total of 50</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reports fewer than 13 passing cases, or any line containing "case 13" appears under a failed-test heading</fails_when>
    <automated>git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#' || true</automated>
    <fails_when>output is any number other than 0 — a non-comment line changed in ci.yml, which this task forbids</fails_when>
  </verify>

  <done>
    Deleting `gate-db-free`'s checker step from the tracked workflow file turns
    `node scripts/verify-workflows.mjs` red with a named FAIL line, the same mutation is remembered by a
    standing case in the design suite, and both the pre-fix green and post-fix red transcripts are on disk
    with revert proof.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin gate-db-free's display name — the string branch protection matches on</name>

  <read_first>
    - `scripts/verify-workflows.mjs:741-751` — the `gate-e2e` display-name pin and its rationale comment
      ("GitHub matches branch protection on `name:`, so a `name:` edit detaches the gate from branch
      protection while leaving the job … perfectly intact"). That argument transfers unchanged.
    - `scripts/verify-workflows.mjs` — the `gate-db-free` block written in Task 1; the new check is a
      sibling inside it.
    - `.github/workflows/ci.yml:675-676` — the job key and the exact `name:` string. Do not edit either.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"The exact payload" — the four required-status-check
      contexts, of which this string is one. Plan 15 consumes it verbatim.
    - `tests/design/workflow-invariants.test.ts:288-310` — the case idiom and the docblock convention.
  </read_first>

  <behavior>
    - Renaming `gate-db-free`'s `name:` to any other string turns the checker RED with a FAIL line naming
      the display-name invariant.
    - An unmutated tree stays GREEN — the pin compares against the string the file actually declares.
    - The evidence line prints both the observed `name:` and the expected one as JSON, so a red is
      diagnosable without opening the workflow.
  </behavior>

  <files>
    scripts/verify-workflows.mjs,
    tests/design/workflow-invariants.test.ts,
    .github/workflows/ci.yml,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-pre-fix.txt,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-post-fix.txt
  </files>

  <action>
Capture the pre-fix state first: on the TRACKED `.github/workflows/ci.yml`, change `gate-db-free`'s
`name:` value to `gate-db-free (renamed)`, run `node scripts/verify-workflows.mjs`, and append the green
transcript plus exit code to `evidence/guards-01-pre-fix.txt` under a heading naming the mutation. Revert
and record `md5sum` before/after and `git status --porcelain`.

Add one counted `check()` inside the `gate-db-free` block asserting
`String(checker?.name ?? "") === CI_CHECKER_CONTEXT`. Copy the `:741-751` rationale comment and rewrite it
for this job, stating the load-bearing consequence explicitly: this string becomes a required status check
on `main` in plan 15, GitHub matches on it rather than on the YAML job key, and `gate-db-free` is the job
that holds lint, the design suite, the Next build and this checker — so an unbound `gate-db-free` undoes
SC1 at the branch level. The evidence string prints the observed and expected values with `JSON.stringify`.

Move the invariant total again in all three spellings, taking the number from the checker's own printed
output rather than computing it.

Add case 14 to `tests/design/workflow-invariants.test.ts`: mutate the copy's `gate-db-free` `name:` value
and `expectRed` on a stable fragment of the display-name invariant's printed name, declared as a `const`
beside the other fragments. Use `eolOf` for any inserted line.

Finally re-apply the rename mutation to the tracked file, capture the RED transcript with its FAIL line
into `evidence/guards-01-post-fix.txt`, revert, and record the revert proof.
  </action>

  <acceptance_criteria>
    - A `check()` in `scripts/verify-workflows.mjs` compares `checker?.name` against `CI_CHECKER_CONTEXT`
      by string equality, and the string `gate-db-free (lint + design + build + workflow parse)` appears
      exactly once as a constant declaration in that file.
    - `node scripts/verify-workflows.mjs` exits 0 and its summary total is strictly greater than the total
      Task 1 left behind.
    - The summary line's bytes match `.github/workflows/ci.yml`'s quoted copy and `EXPECTED_GREEN`.
    - `evidence/guards-01-pre-fix.txt` shows the rename mutation leaving the checker green and exit 0
      BEFORE this task's predicate existed; `evidence/guards-01-post-fix.txt` shows the same rename
      producing a non-zero exit and a FAIL line naming the display-name invariant.
    - `tests/design/workflow-invariants.test.ts` contains an `it("case 14` block and the design suite
      passes.
    - `git diff -U0 -- .github/workflows/ci.yml` shows every added and removed line beginning with `#`.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit, or any line containing "case 14" appears under a failed-test heading</fails_when>
    <automated>grep -c 'gate-db-free (lint + design + build + workflow parse)' scripts/verify-workflows.mjs</automated>
    <fails_when>output is 0 (the context string is not declared) or greater than 1 (a second spelling of the same string was introduced)</fails_when>
  </verify>

  <done>
    Renaming `gate-db-free`'s `name:` turns the checker red with a named FAIL line, a standing case
    remembers it, and the string plan 15 will register as a required status check is now pinned in exactly
    one place in the checker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the anchor and the invocation — the appended-argument red and the decoy-step green</name>

  <read_first>
    - `scripts/verify-workflows.mjs:930-950` — Invariant A's exact-invocation conjunct at `:938` and its
      surrounding comment explaining why the allow-list is blind to an appended argument by construction.
      This task's red case is the same property one job over.
    - `scripts/verify-workflows.mjs` — the checker-step `check()` written in Task 1. This task adds no new
      predicate; it proves the one already there along two more axes.
    - `.github/workflows/ci.yml:675-700` — `gate-db-free`'s step 1, whose `run:` body is multi-line free
      text containing `git config` lines. That step is why a substring anchor in this job would have been
      capturable on arrival.
    - `tests/design/workflow-invariants.test.ts:288-303` — case 1, the ONLY existing case that asserts
      green. The decoy control must copy that shape, including asserting the summary line is present.
    - `tests/design/workflow-invariants.test.ts:262-271` — `insertAfterLineContaining`, and `:155-166`, the
      differs-from-input assertion every mutation builder must route through.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §SC1, the standing-cases table — the decoy row is
      flagged as the only green-asserting case and as the one requiring its own justification comment.
  </read_first>

  <behavior>
    - Appending any argument to `node scripts/verify-workflows.mjs` in `gate-db-free`'s checker step turns
      the checker RED — exact equality after `.trim()`, so the two commands land on opposite sides.
    - Inserting a decoy step FIRST in `gate-db-free` whose `run:` merely mentions the checker path leaves
      the checker GREEN and the summary line intact — the anchor selects by exact `name:`, so the decoy
      cannot capture it.
    - Both cases route through `withMutatedWorkflows`, so a drifted anchor lands on the differs-from-input
      assertion rather than silently measuring an unmutated copy.
  </behavior>

  <files>
    tests/design/workflow-invariants.test.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-pre-fix.txt,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-01-post-fix.txt
  </files>

  <action>
Add case 15 (RED): mutate the copy so `gate-db-free`'s checker step's `run:` becomes the same command with
` /tmp/decoy.mjs` appended, and `expectRed` on the checker-step invariant's name fragment. The docblock
above it states what was measured: `run` is a PERMITTED key, so the allow-list is blind here by
construction and only the exact-equality conjunct can see this — the WR-01 property, asserted for
`gate-db-free` on the day it is fixed for `gate-e2e`.

Add case 16 (GREEN POSITIVE CONTROL): insert a new step at the TOP of `gate-db-free`'s step list whose
`name:` is something other than the checker step's name and whose `run:` is an echo mentioning the checker
path, then assert exit 0 AND that the stdout contains `EXPECTED_GREEN` — the case-1 shape. Its docblock
must carry its own justification: this is the only new case in the plan that asserts green, and it is
what proves the CR-02 defect class is CLOSED rather than moved, because a decoy that would capture a
substring anchor cannot capture an exact-`name:` anchor. Note in the same docblock that `gate-db-free`'s
own step 1 already contains multi-line free text, so this is not a hypothetical decoy.

Both mutation builders must return the input unchanged when their anchor is absent, so a drifted anchor
lands on `withMutatedWorkflows`'s differs-from-input assertion. Join every inserted block with
`eolOf(text)`.

Apply BOTH mutations by hand to the TRACKED `.github/workflows/ci.yml` in turn and record the outcomes:
the appended-argument mutation goes into `evidence/guards-01-post-fix.txt` as a RED transcript with its
FAIL line; the decoy-step mutation goes into the same file as a GREEN transcript with exit 0, labelled as
the anchor control. Revert after each and record `md5sum` before/after plus `git status --porcelain`.
  </action>

  <acceptance_criteria>
    - `tests/design/workflow-invariants.test.ts` contains `it("case 15` and `it("case 16` blocks.
    - Case 16 asserts `result.status` is 0 and that stdout contains `EXPECTED_GREEN`; it is the only case
      added by this plan that asserts a zero exit.
    - The design suite passes with at least 16 cases in the workflow-invariants file.
    - `evidence/guards-01-post-fix.txt` contains a transcript in which the appended-argument mutation
      produced a non-zero exit and a line beginning `FAIL` naming the checker-step invariant, and a second
      transcript in which the decoy-step mutation produced exit 0 and the green summary line.
    - `git status --porcelain` reports no modification to `.github/workflows/ci.yml` at task end.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit, or any line containing "case 15" or "case 16" appears under a failed-test heading</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any line containing "error" from eslint, or the vitest design run reporting any failed file</fails_when>
    <automated>git status --porcelain -- .github/workflows/ci.yml scripts/verify-workflows.mjs</automated>
    <fails_when>any output line whose status field is not "M" — an untracked or deleted workflow/checker file means a mutation was left applied</fails_when>
  </verify>

  <done>
    The checker-step invariant is proved along all three axes SC1 names — deletion, softening by argument,
    and decoy capture — with two standing cases and a hand-mutation transcript for each, and the decoy case
    documents that the anchor rule is the reason it stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit author → `.github/workflows/ci.yml` | Anyone with commit access controls the document the checker parses. The workflow file is untrusted input to the checker. |
| `.github/workflows/ci.yml` → GitHub Actions runner | The YAML decides what executes, with what interpreter, under what conditions. |
| `gate-db-free`'s `name:` → GitHub's required-status-check matcher | A string in the repository binds an enforcement decision made outside it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-01 | Tampering | `gate-db-free` checker step in `.github/workflows/ci.yml` | high | mitigate | Task 1's counted invariant plus the hard stop: deletion of the step or of the job is a named FAIL, not a vacuous pass. |
| T-19.1-02 | Tampering | the checker step's `run:` value | high | mitigate | Task 1's exact-equality conjunct after `.trim()`; an appended argument that would change what the step actually runs is red (proved by case 15). |
| T-19.1-03 | Spoofing | step anchoring within `gate-db-free` | high | mitigate | Anchor by exact `name:`. A decoy step whose `run:` impersonates the checker cannot capture the anchor; case 16 is the standing positive control. |
| T-19.1-04 | Repudiation | `gate-db-free`'s `name:` string | high | mitigate | Task 2's display-name pin: a rename that would silently detach the job from the ruleset plan 15 installs is a named FAIL. |
| T-19.1-05 | Tampering | the invariant total across three files | medium | mitigate | All three spellings move in the same commit; case 1 (the control) is the instrument that makes a silent divergence impossible. |
| T-19.1-06 | Information Disclosure | evidence transcripts in `.planning/` | medium | mitigate | The evidence files record step NAMES, key names and exit codes only. No `env:` value and no credential shape is echoed into a transcript, per the rule `scripts/refuse-mail-credential.mjs:40-41` states about itself. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. RESEARCH.md's Package Legitimacy Audit records zero proposed packages; if a later task reaches for one, the legitimacy gate runs then. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New symbols in `scripts/verify-workflows.mjs`:**
`CI_CHECKER_JOB`, `CI_CHECKER_CONTEXT`, `CHECKER_STEP_NAME`, `CHECKER_RUN`,
`CHECKER_STEP_ALLOWED_KEYS`, one `hardStop` for the absent `gate-db-free` job, and two counted `check()`
invariants (the checker-step invariant and the display-name pin).

**New symbols in `tests/design/workflow-invariants.test.ts`:**
invariant-name fragment constants for the two new invariants; `it("case 13 …")` (checker step deleted →
RED), `it("case 14 …")` (display name renamed → RED), `it("case 15 …")` (argument appended → RED),
`it("case 16 …")` (decoy `run:` step inserted first → GREEN, the anchor control). `EXPECTED_GREEN` is
re-valued, not renamed.

**New comment blocks in `.github/workflows/ci.yml`:**
a `gate-db-free` invariant bullet list sibling to the `gate-e2e` list, and an updated verbatim quote of
the checker's summary line at the count paragraph.

**New directory and evidence files:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/` containing
`guards-01-pre-fix.txt` and `guards-01-post-fix.txt`.

**New npm scripts:** none. **New allowlist manifest:** none (plan 11 owns it).
</artifacts_this_phase_produces>

<verification>
- `node scripts/verify-workflows.mjs` exits 0 with a summary total greater than 50.
- That summary line's bytes appear identically in `.github/workflows/ci.yml` and as `EXPECTED_GREEN`.
- `npm run build` passes (lint + full design suite + next build).
- Each of the four mutations — step deleted, name renamed, argument appended, decoy inserted — has a
  transcript in `evidence/guards-01-pre-fix.txt` or `evidence/guards-01-post-fix.txt` with `md5sum` and
  `git status --porcelain` revert proof.
- `git status --porcelain` shows no unexpected modification to `.github/workflows/ci.yml`.
</verification>

<success_criteria>
Deleting `gate-db-free`'s checker step, renaming the job's display name, or appending an argument to the
checker invocation each turn `node scripts/verify-workflows.mjs` RED with a FAIL line naming the specific
invariant; a decoy step impersonating the checker in a `run:` body leaves it GREEN; all four outcomes are
remembered by standing cases in `tests/design/workflow-invariants.test.ts`; and the invariant total is the
same bytes in all three places that spell it.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-01-SUMMARY.md` when done.
Record the ACTUAL invariant total the checker printed (RESEARCH.md A10 estimates 52; the run is
authoritative), and note whether the decoy control needed any adjustment to stay green.
</output>
