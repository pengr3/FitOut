---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
verified: 2026-09-05T19:22:17Z
status: gaps_found
score: 3/5 success criteria verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "SC4 — the ci workflow run is not red on every push"
    status: failed
    reason: >
      gate-e2e and gate-visual are RED at the last real CI run (33976831607, headSha c42cc3a,
      2026-09-05T16:06:35Z) and the phase's own artifacts (19.1-13-SUMMARY.md, ROADMAP.md) say so
      without softening. Three e2e specs remain genuinely failing (a real hydration defect on every
      host route reachable from host-headings.spec.ts and overflow-320.spec.ts, plus one flaky
      collision/confirmation pair partially settled), and four gate-visual baselines
      (listing-detail x3 + collision-notice-1280) are deliberately left red under D-19.1-D because
      their content bakes in the render-time wall clock. The route to closing SC4 is three
      not-yet-created follow-up plans (19.1-16/-17/-18) that the phase itself defers to its own
      gap-planning step — confirmed absent from .planning/phases/ at verification time.
    artifacts:
      - path: "e2e/host-headings.spec.ts"
        issue: "still red — real hydration defect on host routes, unrepaired"
      - path: "e2e/overflow-320.spec.ts"
        issue: "still red — same hydration defect family, plus 24 tests it blocks from running at all"
      - path: "e2e/visual/surfaces.spec.ts-snapshots/*listing-detail*, *collision-notice-1280*"
        issue: "4 baselines deliberately un-minted back to a stale reference (D-19.1-D) — today-ring and host-since-month are wall-clock-derived, so any regeneration expires at the next day rollover until the venue-local-today fix (decided, not yet implemented) lands"
    missing:
      - "19.1-16 (host-layout hydration; owns host-headings + overflow-320 + 24 blocked tests)"
      - "19.1-17 (duplicated booker mounts — confirmation-decay)"
      - "19.1-18 (the lost day click)"
      - "The venue-local-today implementation in src/components/availability/availability-calendar.tsx that D-19.1-D point (a) already decided but explicitly did not implement"
  - truth: "SC5 — the repository is public and gate-e2e is a required status check on main"
    status: failed
    reason: >
      Verified live against GitHub: `gh repo view pengr3/FitOut --json visibility` returns
      `{"visibility":"PRIVATE"}`, and `gh api repos/pengr3/FitOut/rulesets` returns
      `403 Upgrade to GitHub Pro or make this repository public to enable this feature`. Both
      match the phase's own claims exactly. The PM's recorded `hold` decision (2026-09-05,
      evidence/preflight-public.md) is a legitimate, correctly-sequenced D-08 outcome, not an
      execution failure — but SC5's literal text ("the repository is public... is a required
      status check") is not true today by design, and 19.1-15 cannot even attempt its work while
      the 403 stands.
    artifacts:
      - path: ".planning/phases/19.1-.../19.1-15-PLAN.md"
        issue: "no SUMMARY.md exists — plan never started, blocked by construction on the 403"
    missing:
      - "Publication (PM action, held pending SC4)"
      - "19.1-15's ruleset application, read-back, and direct-push probe"
deferred: []
human_verification: []
---

# Phase 19.1: CI Signal Becomes Real — Verification Report

**Phase Goal:** A `ci` run means something. Green means the suite passed; red names something that
actually broke. The job that runs the gates is itself gated, and the checker's own blind spots are
closed.

**Verified:** 2026-09-05T19:22:17Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Summary Up Front

This is a self-aware, honestly-documented gap. The phase's own ROADMAP.md, 19.1-13-SUMMARY.md, and
19.1-14-SUMMARY.md all state plainly that SC4 and SC5 are open, and every claim I could check against
the live codebase and live GitHub API matched what those artifacts say. **I found no instance where a
SUMMARY overstated what the code does.** The three closed criteria (SC1–SC3) are backed by real,
passing, independently-run mutation tests against the actual shipped checker — not by narrative. The
two open criteria (SC4–SC5) are open for the reasons stated, verified independently, not merely
asserted. This report also surfaces one thing none of the fourteen SUMMARYs or the code review named:
**the local `dev` branch is 8 commits ahead of `origin/dev`**, meaning the second code-review repair
pass (WR-01/WR-02/WR-03/WR-05, plus the CR-01/WR-04/IN-01 first pass) that touches `.github/workflows/ci.yml`
itself has never been exercised by an actual GitHub Actions run — only by the local mutation harness.

## Goal Achievement

### Observable Truths (by Success Criterion)

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| SC1 | `gate-db-free` is itself constrained — deleting/softening its checker step turns the checker RED | ✓ VERIFIED | `node scripts/verify-workflows.mjs` run live: "All 57 invariants hold across 3 section(s) (baselines=11, ci=38, cross=8)." Cases 13, 18–24 in `tests/design/workflow-invariants.test.ts` (deletion, `continue-on-error` at job/Actions-expression/quoted-string spellings, a per-step `if:` condition, a job-level `defaults:`, an appended build-invocation argument, build-step deletion) all run and pass locally (47/47 in the file). Grepped `.github/workflows/ci.yml` for `continue-on-error`/`secrets.` — every match is a comment, zero live directives. |
| SC2 | Every predicate is audited for axis-vs-property error (presence vs. absence-of-neutering-modifier); every anchor uses exact `name:` | ✓ VERIFIED | Cases 25–36 cover CR-01 (nothing-matching branch filter, everything-matching path-ignore, missing default branch on push — all red) and CR-02 (decoy `run:` bodies ahead of/after the real step, anchor-by-substring capture prevented, positive controls proving legitimate decoys stay green). |
| SC3 | The standing test covers the predicate it was built for — reverting the `.trim() === MAIL_REFUSAL_RUN` fix to `.includes(...)` turns the suite RED | ✓ VERIFIED | Case 17 ("WR-01, SC3: appending an argument to the mail refusal invocation is red") exercises exactly the exact-equality-vs-containment property named in the roadmap text; ran it live, passes. Source line confirmed live at `scripts/verify-workflows.mjs:1343`. |
| SC4 | `ci` is not red on every push (14 e2e failures repaired or allowlisted; allowlist itself asserted) | ✗ FAILED (OPEN) | Last real CI run 33976831607: `gate-e2e` and `gate-visual` both red. `tests/design/e2e-known-failures.test.ts` verified live (9/9 passing) — the allowlist is pinned at exactly 2 entries (`e2e/avatar-crop.spec.ts:1125`, `e2e/tabular-figures.spec.ts:416`), matching `PINNED_ENTRY_TOTAL = 2`. `e2e/cancel.spec.ts` (the refund path, D-01-protected) carries zero `test.fail`/`test.fixme` — it was repaired, not quarantined, matching the claim. But 3 e2e failures and 4 visual baselines remain genuinely red, routed to three plans (19.1-16/-17/-18) that do not exist yet (confirmed: no such phase/plan directories on disk). |
| SC5 | Repository is public; `gate-e2e` is a required status check on `main` | ✗ FAILED (OPEN) | Verified live: `gh repo view pengr3/FitOut --json visibility` → `PRIVATE`. `gh api repos/pengr3/FitOut/rulesets` → `403 Upgrade to GitHub Pro or make this repository public`. Both match the phase's claims exactly. This is a designed, PM-held stop (D-08), not an execution defect — but the criterion's literal text is false today. |

**Score:** 3/5 success criteria verified (SC4 and SC5 open, as the phase's own tracking already states)

### CI-01 Requirement Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| CI-01 | All 15 plans (19.1-01 … 19.1-15) declare `requirements: [CI-01]` | Functional Playwright specs run in CI as a new job; carried into 19.1 specifically because "its literal SC4 text is satisfied, but the invariant suite meant to keep it true is not yet undriftable" | ⚠ PARTIAL — cannot be marked SATISFIED | The new-job structure and the invariant suite that guards it (SC1–SC3) are real and verified. But CI-01's own carry-forward language ties it to SC4/SC5 closing, and both remain open. `REQUIREMENTS.md` line 248 is still unchecked (`[ ]`), which is correct given the phase's own status — updating it to closed here would be the exact vacuous-green this phase exists to prevent. |

No orphaned requirement IDs found: `REQUIREMENTS.md`'s only CI section entry is CI-01, and it is declared by every plan in the phase.

### Artifact and Key-Link Verification

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/verify-workflows.mjs` | `gate-db-free` block, exact-invocation conjunct, CR-01/CR-02 audit fixes | ✓ VERIFIED | Live run prints `All 57 invariants hold`; matches `EXPECTED_GREEN` in the test file and the `ci.yml` comment byte-for-byte |
| `tests/design/workflow-invariants.test.ts` | Standing mutation suite, cases 1–47 (SC1/SC2/SC3 + review fixes) | ✓ VERIFIED | 47/47 tests pass in an isolated run of this file alone |
| `tests/design/e2e-known-failures.test.ts` | Checked-in, pinned, growth-guarded allowlist | ✓ VERIFIED | 9/9 tests pass; `PINNED_ENTRY_TOTAL = 2` matches the 2 live `test.fail(` sites found by grep, both in the pinned spec paths |
| `.github/workflows/ci.yml` | No `continue-on-error`, no `secrets.*`, `gate-db-free` structurally hardened | ✓ VERIFIED | Grep confirms zero live directives of either kind; only comment-line matches |
| `e2e/cancel.spec.ts` | Refund path repaired (WR-01/WR-02), never allowlisted (D-01) | ✓ VERIFIED | Poll-based branch selection present at lines ~310–345; zero `test.fail`/`test.fixme` in the file |
| 19.1-16/-17/-18 (the route out of SC4) | Follow-up plans that close the remaining 3 e2e failures + 4 visual baselines | ✗ MISSING | No such phase/plan files exist on disk — confirmed by directory listing |
| `main` branch ruleset | Applied, required checks configured | ✗ MISSING | Blocked by construction — private-repo 403, confirmed live |

### Behavioral Spot-Checks (run live during this verification)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Workflow checker reports 57 invariants | `node scripts/verify-workflows.mjs` | `All 57 invariants hold across 3 section(s) (baselines=11, ci=38, cross=8).` | ✓ PASS |
| SC1–SC3 standing mutation suite | `npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts` | 47/47 passed | ✓ PASS |
| Known-failures allowlist growth guard | `npx vitest run --config vitest.design.config.ts tests/design/e2e-known-failures.test.ts` | 9/9 passed | ✓ PASS |
| Full design suite | `npm run test:design` | 81 files / 1431 passed \| 3 skipped | ✓ PASS — matches measured-state claim exactly |
| Full unit suite | `npm test` | 219 files / 2721 passed \| 5 skipped | ✓ PASS — matches measured-state claim exactly |
| Lint | `npm run lint` | 0 errors / 30 warnings | ✓ PASS — matches claim exactly |
| Type-check | `npx tsc --noEmit` | 7 pre-existing errors, all in `tests/design/mail-credential-refusal.test.ts` and `tests/design/workflow-invariants.test.ts` | ✓ PASS — confirmed pre-existing, not a new gap |
| Repository visibility (live GitHub API) | `gh repo view pengr3/FitOut --json visibility` | `{"visibility":"PRIVATE"}` | Confirms SC5 open |
| Ruleset API (live GitHub API) | `gh api repos/pengr3/FitOut/rulesets` | `403 Upgrade to GitHub Pro or make this repository public` | Confirms SC5 blocked by construction |

### Anti-Patterns Found

None. Debt-marker scan (`TBD`/`FIXME`/`XXX`) across all phase-modified files (`ci.yml`, `verify-workflows.mjs`, `workflow-invariants.test.ts`, `e2e-known-failures.test.ts`, `cancel.spec.ts`, `calendar-plate-month.test.tsx`, `loading.tsx`) returned zero matches.

### A Finding None of the Fourteen SUMMARYs Named

**`dev` is 8 commits ahead of `origin/dev`.** `git status --short --branch` shows `## dev...origin/dev [ahead 8]`. The 8 unpushed commits include the orchestrator-directed code-review repair passes (CR-01/WR-04/IN-01, then WR-03/WR-05/WR-01/WR-02) and the 19.1-14 hold record — and they touch `.github/workflows/ci.yml` (61 changed lines), `scripts/verify-workflows.mjs` (128 lines), `e2e/cancel.spec.ts` (113 lines), and three `tests/design/` files. None of this has ever been exercised by an actual GitHub Actions run: the last real `ci` run (33976831607) is against `origin/dev`'s current tip (`c42cc3a`), which predates all eight commits.

This does not invalidate SC1–SC3: this phase's own established methodology (and Phase 19's before it) proves those properties by spawning the shipped checker script against mutated copies of the workflow file locally — a real GitHub Actions run was never the verification mechanism for that layer, and the local method is sound because `verify-workflows.mjs` is a pure YAML-parsing script whose behavior does not depend on the runner. But it does mean the specific claim "SC1 is constrained end-to-end" has not yet been proven by an actual push-triggered `ci` run against the current `ci.yml` — only against an earlier version of it. Recommend pushing these 8 commits and confirming one real `gate-db-free` run before treating SC1's closure as fully load-bearing at the branch-protection layer SC5 will eventually enforce.

### Gaps Summary

SC1, SC2, and SC3 are genuinely closed: the checker code exists, is wired into `gate-db-free`, and every claimed mutation-red / control-green behavior reproduces live under an independent run of the actual test file (47/47) plus a direct invocation of the actual checker script. SC4 and SC5 are genuinely open, exactly as the phase's own ROADMAP.md, 19.1-13-SUMMARY.md and 19.1-14-SUMMARY.md say — verified independently against a live CI run and a live GitHub API call rather than taken on the SUMMARYs' word. The route out (19.1-16/-17/-18) does not exist yet. This phase should not be marked complete; the correct next step is the gap-planning step the phase's own ROADMAP entry already names, to create those three follow-up plans, after which SC4 closing lifts the PM's publication hold and unblocks 19.1-15.

---

_Verified: 2026-09-05T19:22:17Z_
_Verifier: Claude (gsd-verifier)_
