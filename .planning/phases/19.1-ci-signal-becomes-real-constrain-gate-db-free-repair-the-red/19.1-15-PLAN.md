---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 15
type: execute
wave: 7
depends_on: ["19.1-14"]
files_modified:
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05 — the four BEHAVIOUR jobs are required status checks on the default branch: the functional end-to-end suite, the checker-and-build job, the database job and the price-parity job. The pixel-comparison job reports on every run but does NOT block, because it cannot be allowed to wedge a merge over antialiasing or a legitimate one-pixel design change."
    - "D-05 — the checker-and-build job being required is the specific point of the four. It carries lint, the design suite, the Next build and the workflow checker this phase spent SC1 hardening; leaving it non-required would undo SC1 at the branch level, because the job that enforces every invariant would itself be optional at the moment a merge happens."
    - "D-06 — protection applies to the DEFAULT BRANCH ONLY. The development branch stays unprotected: direct commits keep working, the workflow still runs on every push there so the result is visible, and only the merge into the default branch has to pass the four checks. Protecting both was rejected because it would force a pull request and a full-suite wait on every commit and roughly halve execution pace."
    - "CI-01 / SC5 — the required contexts are verified against the check-run names GitHub actually produced, not merely against the workflow file. GitHub matches a required status check on the run's name, and a context that matches nothing is a required check that can never be satisfied — the failure mode that turns a merge gate into a permanent block with no diagnostic."
    - "CI-01 / SC5 — the live configuration is captured into the repository. The ruleset lives in GitHub's database rather than in git, so without a checked-in payload and a read-back this phase's most important outcome would exist nowhere in the repository. The payload plan 14 authored and the read-back this plan captures are together that record."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — the read-back is compared to the submitted payload field by field with the context list SORTED on both sides, so a difference in the order GitHub returns the contexts is not read as a difference in the contexts themselves — and conversely a genuine substitution cannot hide behind a reordering."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — an empty ruleset list, an empty rules array or an empty context list is a RED, not a quiet pass. The read-back asserts the ruleset list is non-empty, that the status-check rule is present, and that it names exactly four contexts, because a submission that silently created nothing returns the same shape as one that created an unenforced ruleset."
    - "D-06 — the unresolved mechanic is PROBED and recorded rather than planned around. Whether a required-status-checks ruleset also blocks a direct push to the default branch, as opposed to only a merge, is not stated unambiguously in the documentation; the answer is read from the live configuration after the flip and written down, because it decides whether the development-branch workflow is genuinely unaffected."
    - "CI-01 / SC5 — the bypass list is empty. A ruleset's bypass is explicit, and administrator status alone does not grant it, so an empty list means the four checks apply to every actor including the repository owner."
  artifacts:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt
  key_links:
    - "the four job display names to the four required contexts to the check-run names GitHub produces. All three must be the same strings; plan 01 pinned the first link with an invariant, plan 14 copied them into the payload, and this plan verifies the third against a real run."
    - "the checker-and-build job being required to SC1's durability. Every invariant this phase added runs in that job, so requiring it is what makes SC1 hold at the moment that matters."
    - "the checked-in payload and this read-back to the repository's record of a setting that lives outside it. Without both, the phase's most important outcome would be invisible to anyone reading the repository."
    - "the direct-push probe to D-06's workflow guarantee. If a ruleset blocks direct pushes to the default branch, that is a fact the development-branch workflow must be checked against rather than discovered later."
  prohibitions:
    - "The pixel-comparison job must never be added to the required checks. It reports and does not block, by decision, because a one-pixel design change must not be able to hold a merge."
    - "The development branch must never be brought under the ruleset. It would force a pull request and a full-suite wait on every commit."
    - "The bypass list must never be populated. An explicit bypass would let the actor most likely to be in a hurry skip the four checks the phase exists to install."
    - "A required context must never be typed by hand. It is copied from the workflow file and verified against a real check-run name; a typed context that matches nothing is a merge gate that can never go green."
    - "The up-to-date-branch policy must never be enabled without re-costing it. It would mean a full suite re-run every time the default branch moves between a green check and the merge click."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `ordering` and `empty` explicitly for its own subject (sorted field-by-field read-back comparison; empty list, rule and context checks). `adjacency` and `encoding` are owned by plans 01, 06, 07, 09, 11, 12 and 14; none is dropped."
    - "A6 (RESEARCH.md) — whether the ruleset blocks direct pushes to the default branch is genuinely unresolved. This plan probes and records the answer; it does not plan around either outcome, and the answer is written into the evidence file for the next reader."
    - "The settings API is expected to respond now that the repository is public. If it still returns a permission error, the precondition is unmet and the plan halts rather than working around it."
    - "The plan does not open a pull request to test the gate end to end. The context names are verified against the check-run names of a real workflow run, which is the same string comparison GitHub performs, at no cost and with no throwaway branch."
---

<objective>
Install the branch ruleset the phase has been building toward: the four behaviour jobs required on the
default branch, the pixel-comparison job reporting but not blocking, the development branch untouched — and
capture the live configuration back into the repository so the phase's most important outcome is not
invisible to anyone reading it.

Purpose: the ruleset is the only artefact of this phase that lives in GitHub's database rather than in git.
Requiring the checker-and-build job is what makes SC1 hold at the moment a merge happens, because that job
carries every invariant this phase added.

Output: an applied ruleset, a field-by-field read-back, a verification that the four contexts match real
check-run names, and a recorded answer to the direct-push question.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the checked-in ruleset payload and read it back field by field</name>

  <precondition>
    The repository's visibility reads public — plan 14's decision checkpoint completed and its HANDOFF
    RESULT section records the read-back. The settings API returns a permission error while the repository
    is private, so this task cannot run before the PM has performed the flip. Halt if the visibility still
    reads private.
  </precondition>

  <read_first>
    - `.planning/phases/19.1-.../evidence/ruleset-main.json` — the payload plan 14 authored, which is
      submitted unchanged.
    - `.planning/phases/19.1-.../evidence/preflight-public.md` section (d) — the read-back commands and the
      leading-slash note.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Part 3 — SC5": the two shell gotchas (this shell
      rewrites a leading-slash API path, and the read-only subcommand cannot create a ruleset so the
      creation goes through the raw API), the exact payload's field notes, and the up-to-date-branch
      recommendation with its cost argument.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-05 and D-06 in full.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt
  </files>

  <action>
Confirm the precondition first: read the repository's visibility and record it. If it is not public, halt
and say so — plan 14's checkpoint has not completed.

Submit `evidence/ruleset-main.json` unchanged through the raw settings API. Write the API path WITHOUT a
leading slash; this shell rewrites a leading-slash path into a filesystem path and the call fails with an
error naming a directory that does not exist, which reads like a broken command rather than a shell
quirk. Note also that the read-only ruleset subcommand cannot create a ruleset — creation is a raw API
call.

Capture into `evidence/ruleset-readback.txt`, each with its command line above it and unedited output
below it:

  - the visibility read-back confirming the precondition;
  - the submission call and its response;
  - a list of all rulesets on the repository with their identifiers, names and enforcement states;
  - the full read-back of the ruleset just created.

Then write a COMPARISON section that checks the read-back against the submitted payload FIELD BY FIELD:
target, enforcement state, the branch condition, the rule type, the context list, the up-to-date-branch
policy, the bypass list. Sort the context list on BOTH sides before comparing, so a difference in the order
GitHub returns them is not read as a difference in the contexts — and so a genuine substitution cannot hide
behind a reordering.

Assert three emptiness checks explicitly, each as its own recorded line, because a submission that silently
created nothing returns the same shape as one that created an unenforced ruleset: the ruleset list is not
empty; the rules array contains a status-check rule; and that rule names exactly four contexts. Also record
that the bypass list is empty.

Close the section with a line beginning `APPLIED` naming the ruleset identifier, its enforcement state and
the four contexts.
  </action>

  <acceptance_criteria>
    - `evidence/ruleset-readback.txt` records the repository visibility as public before any submission.
    - It contains the submission command with an API path carrying no leading slash, and its response.
    - It contains a ruleset list and a full read-back of the created ruleset.
    - Its comparison section covers all seven named fields, with the context lists sorted on both sides.
    - It records the three emptiness checks and the empty bypass list as separate lines.
    - It ends with a line beginning `APPLIED` naming the identifier, the enforcement state and four
      contexts.
    - The pixel-comparison job's display name does not appear among the required contexts.
  </acceptance_criteria>

  <verify>
    <automated>gh repo view --json visibility --jq .visibility</automated>
    <fails_when>the output is anything other than PUBLIC — the precondition is unmet and the settings API will return a permission error</fails_when>
    <automated>gh api repos/pengr3/FitOut/rulesets --jq 'length'</automated>
    <fails_when>non-zero exit, or the printed number is 0 — no ruleset exists, so the submission silently created nothing</fails_when>
    <automated>grep -c '^APPLIED' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is 0 — the applied state is not recorded in an assertable line, so the live configuration has no record in the repository</fails_when>
    <automated>grep -c 'gate-visual' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt || true</automated>
    <fails_when>output is any number other than 0 — the pixel-comparison job appears in the read-back, meaning it was made a required check, which D-05 forbids</fails_when>
  </verify>

  <done>
    The ruleset is live on the default branch, its read-back matches the checked-in payload field by field
    with sorted contexts, the three emptiness checks pass, the bypass list is empty, and the whole live
    configuration is captured in the repository.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the four contexts match real check-run names, and probe the direct-push question</name>

  <read_first>
    - `.github/workflows/ci.yml` — the five jobs' display-name strings.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the `HANDOFF` line and the final run
      identifier from plan 13, whose check-run names are the comparison target.
    - `scripts/verify-workflows.mjs` — the display-name pins plans 01 and 05 added, which are the reason
      these strings cannot drift silently.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"The unresolved mechanic" — the ambiguity about direct
      pushes and the recommended read-only probe.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-06 — the development-branch guarantee this probe
      checks.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt
  </files>

  <action>
VERIFY THE CONTEXTS AGAINST REALITY. List the check-run names GitHub produced for the most recent workflow
run on the development branch, and compare that set against the four required contexts. This is the same
string comparison GitHub performs at merge time, so a context that matches nothing here is a required check
that can never be satisfied — a permanent merge block with no diagnostic. Record the command, the full
check-run name list, and a per-context match result. Record a line beginning `CONTEXTS` stating how many of
the four matched.

Also record the display name of the pixel-comparison job in the same list, and note that it is present as a
check run and absent from the required set — that is D-05 working, and it is worth showing rather than
asserting.

PROBE THE DIRECT-PUSH QUESTION. Use the read-only ruleset check against the default branch to report which
rules would apply, and read the ruleset's own rule list for anything that would block a push as opposed to
a merge. Record the command and its unedited output, then write a line beginning `DIRECTPUSH` stating the
answer the evidence supports and naming the evidence. If the evidence is inconclusive, say inconclusive and
say what would settle it — do not guess.

CONFIRM THE DEVELOPMENT BRANCH IS UNTOUCHED. Run the same read-only check against the development branch
and record its output. Write a line beginning `DEVBRANCH` stating whether any rule applies to it. D-06's
whole point is that direct commits there keep working and the workflow still runs so the result is visible;
if any rule now applies to it, that is a defect in the branch condition and it is fixed before this plan
closes.
  </action>

  <acceptance_criteria>
    - `evidence/ruleset-readback.txt` contains the check-run name list for a named real run and a
      per-context match result.
    - It contains a line beginning `CONTEXTS` stating a match count.
    - It shows the pixel-comparison job present as a check run and absent from the required set.
    - It contains the read-only rule check for the default branch with unedited output and a line beginning
      `DIRECTPUSH` stating the answer or an explicit inconclusive with what would settle it.
    - It contains the read-only rule check for the development branch and a line beginning `DEVBRANCH`.
    - No rule applies to the development branch.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^CONTEXTS' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is 0 — the contexts were never checked against real check-run names, so a required check that can never be satisfied would go unnoticed until the first merge</fails_when>
    <automated>grep -c '^DIRECTPUSH' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is 0 — the unresolved mechanic was not probed, leaving the next reader to rediscover it</fails_when>
    <automated>grep -c '^DEVBRANCH' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is 0 — D-06's guarantee that the development branch stays unprotected is not verified anywhere</fails_when>
    <automated>gh ruleset check dev</automated>
    <fails_when>the output lists any rule as applying to the development branch — D-06 requires it to stay unprotected so direct commits keep working</fails_when>
  </verify>

  <done>
    All four required contexts are proved to match check-run names GitHub actually produced, the
    pixel-comparison job is shown reporting-but-not-blocking, the direct-push question has a recorded
    answer, and the development branch is confirmed unprotected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close SC5 and state the phase's final position</name>

  <read_first>
    - `.planning/ROADMAP.md` §"Phase 19.1" — SC5's exact wording, so the closure is stated against the
      criterion rather than against a summary of it, and D-05's note that the roadmap's literal SC5 text
      named only one job while the recorded decision requires four.
    - `evidence/ruleset-readback.txt` — the `APPLIED`, `CONTEXTS`, `DIRECTPUSH` and `DEVBRANCH` lines.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the SC4 closure line and the merge-wait
      duration.
    - `.planning/phases/19.1-.../evidence/preflight-public.md` — the criteria table, which this task
      completes.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` — the deferred items, so the carry list is accurate.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt
  </files>

  <action>
Write a closure section in `evidence/ruleset-readback.txt` stating, against SC5's exact wording:

  - the repository is public, with the read-back that shows it;
  - the required status checks on the default branch, named verbatim, with the note that the recorded
    decision requires four behaviour jobs where the roadmap's literal text named one, and why: the
    checker-and-build job holds every invariant this phase added, so leaving it non-required would undo
    SC1 at the branch level;
  - the pixel-comparison job reports and does not block, with the evidence from Task 2;
  - the development branch is unprotected, with the evidence from Task 2;
  - the merge wait this installs, taken from plan 13's measured green-tree duration, and its headroom
    against the job's time cap.

Close with exactly one of two lines:

  - `SC5 MET` — followed by the ruleset identifier and the four contexts.
  - `SC5 OPEN` — followed by what is missing and what must own it.

Then write a CARRY section listing what this phase deliberately did not close, so the next reader inherits
a list rather than a surprise: the deferred suite-sharding conversation and the time cap that must not be
raised; the absence of a forcing function for baseline drift now that the pixel job is green but not
required; whichever review findings the SC2 audit recorded as carried; the deferred flake-failure
enforcement and the wave it belongs to; and any plan proposed under D-02 during the re-measurement.

Finally, mark the criteria table in `evidence/preflight-public.md` complete by filling in SC5's row.
  </action>

  <acceptance_criteria>
    - `evidence/ruleset-readback.txt` contains a closure section addressing all five points against SC5's
      wording.
    - It ends with exactly one of `SC5 MET` or `SC5 OPEN`, and under the open line names what must own the
      remainder.
    - It contains a CARRY section listing every deliberately-unclosed item with a one-line reason.
    - `evidence/preflight-public.md`'s criteria table has a completed SC5 row.
    - `node scripts/verify-workflows.mjs` is green and `npm run build` passes.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE '^SC5 (MET|OPEN)' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is any number other than 1 — SC5 has no closure line, or has both, and the criterion must land on exactly one verdict</fails_when>
    <automated>grep -c '^CARRY' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-readback.txt</automated>
    <fails_when>output is 0 — nothing was recorded about what this phase deliberately left open, so the next reader inherits a surprise instead of a list</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any eslint error line, or the design vitest run reporting a failed file</fails_when>
  </verify>

  <done>
    SC5 has exactly one recorded verdict stated against its own wording with the live configuration behind
    it, and everything the phase deliberately left open is on a list rather than in someone's memory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the merge action → the default branch | The ruleset is the only thing standing between an unverified change and the branch this project releases from. |
| a required context string → the check run it must match | A context matching nothing is a merge gate that can never go green; a context matching the wrong run is a gate that goes green for the wrong reason. |
| GitHub's settings database → the repository's record of itself | The ruleset lives outside git, so without a checked-in payload and a read-back the phase's most important outcome is invisible to anyone reading the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-71 | Elevation of Privilege | merging an unverified change into the default branch | high | mitigate | Four required behaviour checks with an empty bypass list, so administrator status alone does not grant a skip. |
| T-19.1-72 | Spoofing | a required context that matches no check run | high | mitigate | Task 2 compares all four contexts against the check-run names of a real run — the same string comparison GitHub performs at merge time. |
| T-19.1-73 | Tampering | the checker-and-build job left non-required | high | mitigate | D-05 requires it explicitly; the read-back and closure both name it, and its display name is pinned by an invariant plan 01 added. |
| T-19.1-74 | Repudiation | a live setting with no record in the repository | high | mitigate | The payload is checked in by plan 14 and the read-back is captured here, compared field by field with sorted context lists. |
| T-19.1-75 | Denial of Service (self-inflicted) | the development branch brought under the ruleset | high | mitigate | D-06 scopes the condition to the default branch; Task 2 probes the development branch and the verify fails if any rule applies to it. |
| T-19.1-76 | Denial of Service (self-inflicted) | a pixel-comparison job wedging a merge | medium | mitigate | It is deliberately absent from the required set; Task 2 shows it present as a check run and absent from the required list. |
| T-19.1-77 | Spoofing | a submission that silently created nothing | high | mitigate | Three explicit emptiness checks — non-empty ruleset list, status-check rule present, exactly four contexts. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New evidence file:** `evidence/ruleset-readback.txt` — the visibility precondition read-back, the
submission call and response, the ruleset list, the full read-back, a seven-field comparison with sorted
context lists, three emptiness checks, an empty-bypass confirmation, and the lines `APPLIED`, `CONTEXTS`,
`DIRECTPUSH`, `DEVBRANCH`, exactly one of `SC5 MET` or `SC5 OPEN`, and a `CARRY` section.

**Live configuration created:** a repository ruleset on the default branch requiring four status checks,
with an empty bypass list and the up-to-date-branch policy disabled.

**Completed artefact:** the SC5 row of `evidence/preflight-public.md`'s criteria table.

**New symbols, npm scripts or source changes:** none.
</artifacts_this_phase_produces>

<verification>
- The repository's visibility reads public and the settings API responds.
- Exactly one ruleset exists on the default branch, actively enforced, requiring exactly four contexts,
  with an empty bypass list.
- All four contexts match check-run names produced by a real workflow run.
- No rule applies to the development branch.
- The evidence file carries `APPLIED`, `CONTEXTS`, `DIRECTPUSH`, `DEVBRANCH`, one SC5 closure line and a
  `CARRY` section.
- `node scripts/verify-workflows.mjs` is green and `npm run build` passes.
</verification>

<success_criteria>
A merge into the default branch cannot land without the functional suite, the checker-and-build job, the
database job and the price-parity job all green; the pixel-comparison job reports without blocking; the
development branch is untouched so direct commits keep working; the live configuration is recorded in the
repository as both a submitted payload and a read-back; and the direct-push question has an answer on disk
rather than an assumption.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-15-SUMMARY.md` when done.
Quote the `APPLIED` line, state the direct-push answer, state the merge wait this installs with its
headroom against the time cap, and reproduce the `CARRY` list so the phase's open items travel with the
summary rather than only with the evidence file.
</output>
