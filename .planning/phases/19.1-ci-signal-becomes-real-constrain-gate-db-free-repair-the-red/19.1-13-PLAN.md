---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 13
type: execute
wave: 5
depends_on: ["19.1-11", "19.1-12"]
files_modified:
  - e2e/confirmation-decay.spec.ts
  - tests/design/e2e-known-failures.test.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC4 — the criterion is checked against a REAL run of all five jobs, not against the arithmetic of what was repaired. The prior measurement recorded fourteen failed, two flaky, fifty-two skipped, twenty-five never executed and three hundred and sixty-seven passed; this plan produces the same shape of measurement after the repairs and compares the two side by side."
    - "D-02 — the twenty-five tests that never executed are executed for the first time, and whatever they surface is triaged rather than absorbed. A serial group stops after a failure, so repairing the failures RELEASES tests nobody has ever seen run in CI. D-02 authorises the phase to grow for whatever they find rather than pushing real bugs behind a green check."
    - "CI-01 / SC4 — the two tests recorded as flaky are addressed before the gate becomes required, because once it is required a flaky test is a merge blocker on most runs. Their treatment is decided on the re-measurement's evidence and recorded, and the decision to enforce flake-failure is deliberately NOT taken in the same wave that turns the suite green, because it would mask which change did what."
    - "CI-01 / SC4 — the wall-clock is re-measured rather than inherited. The prior forty-four minutes included one hundred and thirty-five retry executions, so it is an upper bound for a RED tree and not the cost of a green one. The new number is what the merge wait actually costs once the gate is required, and it is the input to any future sharding conversation."
    - "D-13 — the ninety-minute cap on the functional job is NOT raised. If the re-measured run approaches it, that is a finding handed to the deferred sharding conversation, never a reason to move the number. The cap has already been raised once."
    - "D-03 — if the re-measurement changes the allowlist, the pinned count moves deliberately and the entry docblock moves with it. A count that drifts to match the tree is the mechanism working backwards."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — a run that collected fewer tests than the prior run is a RED, not an improvement. The re-measurement compares the total collected count against the prior measurement's total and treats a shortfall as tests having stopped being discovered, which looks identical to tests passing if only the failure count is read."
    - "CI-01 ordering (spec-less edge probe, verification: backstop) — { statement: 'the re-measurement records which serial groups released previously-unexecuted tests and in what order those tests then ran, because a test that only passes when it runs after a particular sibling is an ordering dependency and not a pass', verification: backstop }"
  artifacts:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt
    - tests/design/e2e-known-failures.test.ts
    - e2e/confirmation-decay.spec.ts
  key_links:
    - "the repaired failures to the twenty-five tests that never ran. Releasing a serial group is the mechanism by which repairing fourteen failures can produce new ones, and it is why this re-measurement wave exists at all."
    - "the re-measured wall-clock to the merge wait plan 15 installs. Once the functional job is a required check its duration is the cost of every merge, and the deferred sharding conversation needs a green-tree number rather than a red-tree one."
    - "the allowlist's pinned count to whatever this run changes. Plan 11 built the mechanism; this plan is the first thing that tests whether it holds under a real change."
    - "the flake policy to plan 15's required-check decision. A flaky required check blocks merges on most runs, so the treatment is decided here even if enforcement is deferred."
  prohibitions:
    - "The functional job's time cap must never be raised. It has been raised once already, and an expiry once the job is required is a finding for the deferred sharding conversation rather than a number to move."
    - "Flake-failure enforcement must never be switched on in the same wave that turns the suite green. It would mask which change produced which effect, and the point of this wave is a clean comparison."
    - "A newly surfaced failure must never be added to the allowlist without the same triage every earlier failure received. The allowlist is not the place tests go when a wave is running late."
    - "The allowlist's pinned count must never be edited to match the tree. The number is the record of a decision; the entry and its reason are written first."
    - "A drop in the total collected count must never be read as an improvement. Fewer tests discovered and more tests passing produce the same-looking summary if only the failure count is read."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `empty` explicitly (a shortfall in collected tests is red) and `ordering` as a backstop marker (recording which serial groups released and in what order the released tests ran). `adjacency` and `encoding` are owned by plans 01, 06, 07, 09, 11 and 12; none is dropped."
    - "Open Question 2 (RESEARCH.md) — how many of the twenty-five previously-unexecuted tests fail once unblocked is genuinely unknown. This plan measures it. If the number is large, D-02 authorises the phase to grow and the SUMMARY proposes the split."
    - "The re-measured wall-clock is expected to fall because the prior figure included one hundred and thirty-five retry executions, but that is a prediction and the run is the measurement. Plan 02's answer about whether expected failures consume retries is one input; if it said they do, the predicted saving does not exist."
    - "If a newly surfaced failure is a real product bug, this plan does NOT repair it inline. It records the verdict and the SUMMARY proposes the plan that owns it, so the repair gets the same triage discipline as the original fourteen."
---

<objective>
Re-measure the whole suite against a real run of all five jobs, execute the twenty-five tests that have
never run in CI, triage whatever they surface, settle the two flaky tests, and confirm SC4: the workflow
run is not red on every push.

Purpose: repairing the fourteen failures releases serial groups that stopped early, so tests nobody has
ever seen run in CI will execute for the first time. Declaring SC4 met on the arithmetic of what was
repaired would be exactly the vacuous green this phase exists to delete.

Output: a side-by-side measurement against the prior run, a verdict for every newly surfaced failure, a
re-measured wall-clock for the merge wait plan 15 installs, and either SC4 closed or a named proposal for
the plan that closes it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-11-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all five jobs and measure against the prior run, term by term</name>

  <read_first>
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"The measurement, from the real job log" — the prior
      run's summary block verbatim, the single-worker fact, the retry configuration, the retry-execution
      count, and the note that a serial group marks its remainder as not run after a failure.
    - `.planning/phases/19.1-.../evidence/testfail-behaviour.txt` — plan 02's measured answers about
      retries and serial groups, which decide what the new numbers mean.
    - `.planning/phases/19.1-.../19.1-11-SUMMARY.md` — the allowlist's final entry count and the caveat
      about what an annotation does and does not reveal.
    - `.planning/phases/19.1-.../19.1-12-SUMMARY.md` — the visual job's state after regeneration.
    - `.github/workflows/ci.yml` — the five job names, which are the rows of the measurement, and the
      functional job's time cap.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt
  </files>

  <action>
Push to the development branch and let the whole workflow run. Do not run only the functional job; SC4 is a
property of the RUN, and all five jobs are in scope.

Capture into `evidence/suite-remeasurement.txt`, as raw output with command lines above it:

  - the run identifier and the branch;
  - each of the five jobs by its display name, with its conclusion and its duration;
  - the functional job's full summary block verbatim — failed, flaky, skipped, not run, passed, and the
    wall-clock;
  - the count of retry executions in the run, read from the log rather than estimated.

Then write a COMPARISON table with one row per term of the summary block: the prior value, the new value,
and the delta. The prior values come from RESEARCH.md's recorded measurement, quoted with its run
identifier so the comparison is against a named run rather than a memory.

Check three things explicitly and record each as its own line:
  - `COLLECTED` — the total collected count in the new run against the prior run's total. A SHORTFALL is a
    red: it means tests stopped being discovered, which looks identical to tests passing if only the
    failure count is read. Investigate any shortfall before continuing.
  - `RELEASED` — which serial groups previously stopped early, how many of the twenty-five
    previously-unexecuted tests now ran, and the order they ran in within their groups.
  - `WALLCLOCK` — the new duration against the ninety-minute cap, with the headroom stated. If the run
    approaches the cap, say so; the cap is not raised.

List every test that failed in the new run and was NOT in the original fourteen. Those are the newly
surfaced failures Task 2 triages.

Do not repair anything in this task.
  </action>

  <acceptance_criteria>
    - `evidence/suite-remeasurement.txt` names a run identifier, lists all five jobs with conclusions and
      durations, and contains the functional job's summary block verbatim.
    - It contains a comparison table with one row per summary term, quoting the prior run's identifier.
    - It contains lines beginning `COLLECTED`, `RELEASED` and `WALLCLOCK`, each with numbers.
    - It lists every newly surfaced failure by spec path and line, or states explicitly that there are none.
    - The functional job's time cap in `.github/workflows/ci.yml` is byte-identical to HEAD.
    - `git status --porcelain -- e2e src tests` produces no output.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE '^(COLLECTED|RELEASED|WALLCLOCK)' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt</automated>
    <fails_when>output is less than 3 — one of the three explicit checks was not recorded, and a missing collected-count check is how a shrinking suite reads as an improving one</fails_when>
    <automated>git diff -- .github/workflows/ci.yml</automated>
    <fails_when>any output — the workflow was edited during a measurement task, and the time cap in particular must not move</fails_when>
    <automated>git status --porcelain -- e2e src tests</automated>
    <fails_when>any output — a spec, source or test file was edited during the measurement, which would make the run being measured describe a tree that no longer exists</fails_when>
  </verify>

  <done>
    A named real run of all five jobs is measured term by term against the prior run, the previously
    unexecuted tests have executed, and every newly surfaced failure is listed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Triage everything the re-measurement surfaced, and settle the two flaky tests</name>

  <read_first>
    - `evidence/suite-remeasurement.txt` — Task 1's list of newly surfaced failures and the released-groups
      record.
    - `.planning/phases/19.1-.../evidence/triage-harness.md` — plan 02's reproduction sequence, which each
      triage uses.
    - `e2e/confirmation-decay.spec.ts` around `:151` — the flaky case and its failure message about a
      zero-height measurement in a viewport that is not zero, which indicates a measurement taken before
      layout settled.
    - `e2e/helpers/booker-seed.ts:118-160` — the established retry-with-post-condition shape, if a settle
      is the correct treatment.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"The two flaky" — both entries and the warning about
      what a flaky test costs once the gate is required, plus the instruction not to enable flake-failure
      enforcement in the same wave that turns the suite green.
    - `tests/design/e2e-known-failures.test.ts` — the pinned count and the entry docblock, in case an entry
      genuinely has to be added.
  </read_first>

  <behavior>
    - The zero-height measurement case measures a settled layout, so a repeated run produces a consistent
      non-zero height.
    - Every newly surfaced failure has a recorded verdict drawn from the same vocabulary the earlier triage
      used: repair here, repair in a new plan, seed, intercept, or annotate with a specific written reason.
    - If an annotation is added, the pinned count moves deliberately and the entry docblock gains its row.
  </behavior>

  <files>
    e2e/confirmation-decay.spec.ts,
    tests/design/e2e-known-failures.test.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt
  </files>

  <action>
For EACH newly surfaced failure, run the plan 02 reproduction sequence, reproduce it locally, and record a
line beginning `VERDICT` naming the spec path, the line, the root cause and the treatment. The treatment
vocabulary is the same one the earlier triage used, and the same rules apply: a real product bug is
repaired, not annotated; an annotation carries a specific written reason naming the capability or finding
and the test that covers it elsewhere; and nothing on a money path is annotated at all.

If a newly surfaced failure is a REAL PRODUCT BUG that is not a small localised fix, do not repair it
inline. Record the verdict, name the component and the behaviour, and state in the SUMMARY that the phase
grows by a plan under D-02 that owns it. Repairing a product bug in a re-measurement wave gives it less
triage discipline than the original fourteen received, which is the inconsistency this phase exists to
stop.

Settle the two previously flaky tests:
  - the collision case's court variant was addressed by plan 04; confirm from the new run whether it is
    still flaky and record the result;
  - the zero-height measurement case is a measurement taken before layout settled. Add a settle in the
    established shape — assert the observable state the measurement depends on before measuring — with a
    docblock stating what was measured and why the wait is a requirement rather than a hedge. Do not widen
    a timeout and do not lower the expected height.

Do NOT enable flake-failure enforcement. Record it as a deliberate deferral with its reason: switching it
on in the same wave that turns the suite green would mask which change did what. Name it as a candidate
for the wave after the gate is required.

If any treatment adds an allowlist entry, write the entry and its reason FIRST, then move the pinned count
and add its row to the entry docblock. The number is the record of the decision, never the mechanism for
making it.
  </action>

  <acceptance_criteria>
    - `evidence/suite-remeasurement.txt` contains one line beginning `VERDICT` for every newly surfaced
      failure listed in Task 1, or a statement that there were none.
    - No newly surfaced failure on a money path is annotated.
    - The zero-height measurement case has a settle in the established shape with a docblock, no widened
      timeout and no lowered expected height.
    - If the allowlist changed, the pinned count and the entry docblock moved together and the census
      passes.
    - The evidence file records the flake-enforcement deferral with its reason.
    - `npm run build` passes.
  </acceptance_criteria>

  <verify>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any eslint error line, or the design vitest run reporting a failed file — most likely the allowlist census disagreeing with a moved count</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/e2e-known-failures.test.ts</automated>
    <fails_when>non-zero exit, or any test appearing under a failed heading — the pinned count and the tree disagree</fails_when>
    <automated>npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --retries=2 --reporter=list</automated>
    <fails_when>non-zero exit, or the summary block reporting a non-zero "failed" or "flaky" count</fails_when>
    <automated>git diff -U0 -- e2e/confirmation-decay.spec.ts | grep -E '^\+' | grep -oE 'timeout: *[0-9_]+' | sort -u</automated>
    <fails_when>the output lists any timeout value larger than one already present at HEAD in that file — a widened timeout is the shortcut this plan forbids</fails_when>
  </verify>

  <done>
    Every failure the re-measurement surfaced has a verdict from the same vocabulary the original fourteen
    received, both flaky tests are settled or explained, and flake enforcement is deferred on the record
    rather than forgotten.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close SC4 against a clean run, or name the plan that closes it</name>

  <read_first>
    - `evidence/suite-remeasurement.txt` — the comparison table and every verdict.
    - `.planning/ROADMAP.md` §"Phase 19.1" — SC4's exact wording, so the closure is stated against the
      criterion rather than against a summary of it.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-02 — the authorisation for the phase to grow rather
      than push real bugs behind a green check.
    - `tests/design/e2e-known-failures.test.ts` — the final pinned count and entry list.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt
  </files>

  <action>
Push again after Task 2's changes and let the whole workflow run. Capture the final run identifier and the
conclusion of each of the five jobs into `evidence/suite-remeasurement.txt` under a FINAL heading, with the
functional job's summary block verbatim and its wall-clock.

Then write a closure section stating, against SC4's exact wording:

  - whether the run is red, and if so which job and why;
  - the count of failures that were repaired versus recorded in the allowlist, with the allowlist's final
    pinned number;
  - that the allowlist is checked in and asserted, naming the census file that asserts it;
  - the final wall-clock and the headroom against the ninety-minute cap.

Close with one of exactly two lines:

  - `SC4 MET` — the run is not red, every failure is repaired or allowlisted with a written reason, and
    the allowlist is guarded. Follow it with the run identifier.
  - `SC4 OPEN` — the run is still red. Name every remaining failure, its verdict, and the plan that must
    own it. Under D-02 the phase grows; propose the plan and what it owns. Do not soften the criterion.

Also record a line beginning `HANDOFF` carrying the two numbers plan 15 needs: the functional job's
duration, which becomes the merge wait once it is required, and the exact display names of all five jobs,
which are the required-status-check contexts.
  </action>

  <acceptance_criteria>
    - `evidence/suite-remeasurement.txt` contains a FINAL section with a run identifier and all five job
      conclusions.
    - It contains a closure section addressing all four points against SC4's wording.
    - It ends with exactly one of the two closure lines, and under the open line every remaining failure
      has a verdict and a named owning plan.
    - It contains a `HANDOFF` line carrying the functional job's duration and the five display names
      verbatim.
    - The functional job's time cap is byte-identical to HEAD.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE '^SC4 (MET|OPEN)' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt</automated>
    <fails_when>output is any number other than 1 — SC4 has no closure line, or has both, and the criterion must land on exactly one verdict</fails_when>
    <automated>grep -c '^HANDOFF' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/suite-remeasurement.txt</automated>
    <fails_when>output is 0 — plan 15 has no recorded source for the required-check context names and the merge-wait duration</fails_when>
    <automated>git diff -- .github/workflows/ci.yml</automated>
    <fails_when>any output — the workflow changed in this plan, and the time cap in particular must not move</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
  </verify>

  <done>
    SC4 has exactly one recorded verdict backed by a named run of all five jobs, the allowlist's final
    state is stated with the file that guards it, and plan 15 has the two numbers it needs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the repaired suite → the tests a serial group had been hiding | Repairing a failure releases tests nobody has seen run; their results are new information, not a confirmation. |
| a summary block → the reader's conclusion | Fewer collected tests and more passing tests produce a similar-looking summary if only the failure count is read. |
| a flaky required check → every merge | Once the gate is required, a test that passes two runs in three blocks merges one run in three. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-60 | Repudiation | SC4 declared met on arithmetic rather than a run | high | mitigate | Task 3 requires a named run of all five jobs and exactly one closure line stated against the criterion's own wording. |
| T-19.1-61 | Spoofing | a shrinking suite reading as an improving one | high | mitigate | The `COLLECTED` check compares totals against the prior run and treats a shortfall as red. |
| T-19.1-62 | Tampering | a newly surfaced failure annotated to close the wave on time | high | mitigate | Prohibited; every newly surfaced failure gets the same triage vocabulary, and adding an entry requires the reason and the docblock row before the count moves. |
| T-19.1-63 | Denial of Service (self-inflicted) | a flaky test becoming a merge blocker | high | mitigate | Both flaky tests are settled or explained here, before the gate becomes required; enforcement is deferred deliberately and recorded. |
| T-19.1-64 | Tampering | the functional job's time cap raised to fit a slow run | high | mitigate | Prohibited by D-13 and by this plan; the workflow diff is asserted empty in two tasks. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New evidence file:** `evidence/suite-remeasurement.txt` — the prior-versus-new comparison table, lines
beginning `COLLECTED`, `RELEASED` and `WALLCLOCK`, one `VERDICT` line per newly surfaced failure, a FINAL
section with a named run, exactly one `SC4 MET` or `SC4 OPEN` line, and a `HANDOFF` line.

**Changed symbols:** a settle with a docblock in `e2e/confirmation-decay.spec.ts`; possibly a moved pinned
count and a new entry row in `tests/design/e2e-known-failures.test.ts`.

**Possible downstream output:** a proposed plan under D-02 for any newly surfaced product bug that is not a
small localised fix.

**New npm scripts:** none. **No production source file is modified.**
</artifacts_this_phase_produces>

<verification>
- A named workflow run exists in which all five jobs are recorded with their conclusions.
- The total collected count is not lower than the prior run's.
- Every newly surfaced failure has a verdict; none on a money path is annotated.
- `npm run build` passes, including the allowlist census.
- The functional job's time cap is unchanged.
- The evidence file carries exactly one SC4 closure line and a `HANDOFF` line.
</verification>

<success_criteria>
SC4 is decided by a real run of all five jobs rather than by arithmetic: the tests that had never executed
have executed, everything they surfaced has a verdict from the same triage vocabulary as the original
fourteen, the flaky pair is settled, the allowlist's final state is guarded by a build-blocking census, and
the wall-clock that becomes the merge wait is measured on a green tree.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-13-SUMMARY.md` when done.
State the SC4 verdict, the before-and-after summary terms side by side, the final allowlist count, the
green-tree wall-clock and its headroom against the cap, and — if SC4 is open — the exact plan proposed
under D-02 and what it owns.
</output>
