---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 17
type: execute
wave: 9
depends_on: ["19.1-16"]
files_modified:
  - e2e/confirmation-decay.spec.ts
  - e2e/avatar-crop.spec.ts
  - src/app/(app)/bookings/[id]/page.tsx
  - src/components/booking/confirmation-moment.tsx
  - src/components/profile/avatar-field.tsx
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt
autonomous: true
requirements: [CI-01]
gap_closure: true

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC4 — the booker surfaces put exactly one copy of each of two elements in the document at the moment the suite measures them: the confirmation section on a paid booking, and the file picker on the profile surface. Both currently resolve to two, one of them hidden, with the identical in-main / not-in-main signature; the second was measured on a real run and reproduces on this box under compile pressure."
    - "CI-01 / SC4 — the SECOND copy's provenance is READ, not inferred. Before anything is repaired, both matches are dumped with their full ancestor chain to the document body, each ancestor's tag, identifier, class list and hidden state, whether the node sits inside the page's main landmark, and whether the second copy disappears on its own within a bounded wait. Those readings are what decide whether this is a duplicated mount, an artefact of the route's own streaming commit window, or a tree React regenerated — three causes with three different owners."
    - "CI-01 / SC4 — the question plan 13 left open is answered first: are this and the host-layout defect ONE defect? The two surfaces are measured on the tree BEFORE and AFTER the preceding plan's repair, and the answer is written down either way. The booker layout streams only a box-reserving skeleton and the booker checkout route was measured emitting no regeneration, so the null result is a real possible outcome and is recorded as a finding rather than treated as a failed attempt."
    - "CI-01 adjacency (spec-less edge probe, verification: explicit) — the confirmation case's count assertion and its visibility assertion are ADJACENT statements, and the defect appears BETWEEN them: the count reads one, and two statements later the strict-mode resolution reports two. The window between the two assertions is therefore the measurement's subject, and it is instrumented directly — the count is sampled repeatedly across that window rather than once at each end, so the transcript says when the second copy arrives rather than only that it did."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — the reproduction is ordered before the repair and the compile-pressure condition is ordered before the at-rest one, because the defect is intermittent at rest and deterministic under compile pressure. Both conditions are captured: a cold build cache, and a cache left dirty by a production build. A single at-rest green would be a draw, not a measurement."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — no assertion in this plan may pass against a page that never rendered the surface. Both cases keep a presence assertion ahead of every measurement, and the profile case keeps the precondition that the crop stage is visible before any focus reading. A run that failed to reach the surface fails on presence, never silently on an absence."
    - "CI-01 encoding (spec-less edge probe, verification: explicit) — the ancestor dumps are serialised as one line per ancestor with a leading marker at column zero, so a grep over the transcript cannot be satisfied by the prose around it, and the hidden state is recorded as the attribute's presence rather than as a computed style, because a node inside a hidden container computes as hidden without carrying the attribute itself. Nothing here writes a newline into a checked-out file."
    - "CI-01 / SC4 — whatever is repaired, the instrument that named the defect still goes RED against the defect. The repair is proved by re-introducing a genuine second copy on a mutated tree and watching the case fail; a repair that cannot be shown to still fail is a green that has deleted its own evidence."
    - "CI-01 — the confirmation case's two measured constants do not move: the minimum height stays the viewport minus the header, and the ordinary page's top edge still has to start at or past the viewport. No timeout is widened and no expectation is lowered; those two are how a requirement's adjective became a measurement in the first place."
    - "CI-01 / SC4 — this plan is read on its OWN real push-triggered `ci` run, identified by the pushed head SHA, in the identical shape plan 16 established one wave earlier. Both booker specs must be absent from that run's `gate-e2e` failure list, AND plan 16's two host specs must still be absent — a wave that closes two failures while silently reopening two is a net of nothing, and only a real run can see that. Per D-04 all five job conclusions are recorded, because the criterion is a property of the run and not of one job."
    - "CI-01 / SC4 — a `cancelled` conclusion is discarded by identity and counted, never read as green or red. The workflow's concurrency group cancels in progress on the next push, and two runs in this phase (`33971557439`, `33971883942`) already died that way. Per D-06 the push itself needs no approval: `dev` is unprotected by decision and `ci` runs on every push to it so the result is visible."
  artifacts:
    - e2e/confirmation-decay.spec.ts
    - e2e/avatar-crop.spec.ts
    - src/app/(app)/bookings/[id]/page.tsx
    - src/components/booking/confirmation-moment.tsx
    - src/components/profile/avatar-field.tsx
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt
  key_links:
    - "the confirmation section's single mount site in the booking detail route → the two copies the runner reported. The component is imported once and rendered once, so a second copy in the document is a runtime fact about how the route is delivered, not about how it is written — which is why the ancestor chain is the reading that matters."
    - "the profile surface's file picker → the helper that hands a fixture to it. That helper's own docblock records that the picker is unambiguous by construction because it is the only one in the source tree, and states that strict mode is the assertion keeping it that way. The strict-mode violation is therefore the assertion firing, not a test that needs relaxing."
    - "the preceding plan's host-layout repair → these two surfaces. If they share a cause, this plan closes on that plan's tree with no further edit; if they do not, the null result is what stops a repair here being credited to the wrong change."
    - "`src/app/(app)/layout.tsx`'s streaming bell slot → the box-reserving skeleton it already uses. This is the booker layout's control: it does not mount a live overlay in a fallback, which is why the booker composition is not the host composition's defect and why the search has to look at the route rather than the layout."
    - "the pushed head SHA → the `ci` run identity → the `gate-e2e (functional Playwright suite)` job's failure list. The SHA is the join key: `gh run list --commit <sha>` is what makes the run being read provably the run that measured this tree, and the head-SHA read-back is what stops a run the concurrency group cancelled being recorded as this plan's verdict."
  prohibitions:
    - "Neither locator may be scoped to the page's main landmark, and the box helper may not be handed the visible one of the two. Either move turns the suite green and deletes the only instrument that has ever named this defect — the explicit prohibition already written at the confirmation case's own site."
    - "No timeout may be widened and no measured expectation may be lowered in either spec. The height floor and the fold position are the two numbers that make the confirmation requirement a measurement instead of an adjective."
    - "Neither case may be recorded in the known-failures allowlist. One is a duplicate-element defect on a surface a booker reaches after paying, and the other is the assertion that a picker stayed unique; quarantining either records a false reason and moves a pin this plan has no mandate to move."
    - "A repair may not be credited to the preceding plan's change without the before-and-after pair that shows it. Two surfaces with the same shape and different layout trees is the exact situation where a shared cause is assumed and never checked."
    - "The failure message of either case may not be left asserting a cause the measurement refuted. This phase has already had to repair two messages that named the wrong thing, and the second was written by the plan that then had to correct it."
  assumptions:
    - "SPEC-LESS EDGE PROBE — authored by hand in plan 19.1-01's shape; this phase has no SPEC.md and the installed tooling exposes no coverage verb. All four items are RESOLVED with verification: explicit and appear above as plain truths: adjacency (the window between the count and the visibility assertion, sampled rather than sampled twice), ordering (compile pressure before at rest; reproduce before repair), empty (a presence assertion ahead of every measurement), encoding (one line per ancestor with a column-zero marker, hidden recorded as an attribute rather than a computed style). Zero items dismissed, zero silent drops."
    - "PROHIBITION PROBE — five bespoke must-NOTs above, descriptor-less. Routine candidates were dropped in the shape plan 01 established (must not leave a scratch file behind; must not hard-code a newline). The allowlist prohibition is restated rather than cited because both of this plan's cases would otherwise look like annotation candidates on their face."
    - "THE CAUSE IS OPEN. Three readings are consistent with what has been observed so far — a genuinely duplicated mount, a commit-window artefact of the route's own streaming, and a client-regenerated tree — and plan 13 settled none of them. The plan therefore branches on Task 1's reading and states the constraint set each branch must satisfy, rather than prescribing a repair that would be a guess wearing an instruction's costume."
    - "THE SHARED-CAUSE QUESTION IS OPEN. Plan 13 wrote that these two and the host-layout defect have the same shape and different layout trees and that it did not settle whether they are one. A null result in Task 1 is a legitimate and useful outcome."
    - "ASSUMPTION DELTA — primary noun UNCHANGED, decision `no-change`. The subject is the same two booker cases plan 13 named."
---

<objective>
Close the two booker-surface failures where one element resolves to two: the confirmation section on a
paid booking, and the file picker on the profile surface. Both carry the identical signature — one copy
inside the page's main landmark, one outside it, one of them hidden.

The confirmation case is a hard failure on the runner rather than a flake, and it became one because the
settle added in plan 13 made the second copy visible to the reporter. The defect is older than that plan;
what changed is that it is named. This plan is where it stops being named and starts being fixed.

Purpose: two of the four remaining `gate-e2e` failures are these, and one of them sits on the surface a
booker lands on immediately after paying.

Output: a read of where the second copy actually comes from, an answer to whether it shares a cause with
the host-layout defect, a repair chosen from that read, both cases green with their instruments proved
still able to fail, and — last — a named, real, push-triggered `ci` run in which neither booker spec appears
in `gate-e2e`'s failure list and plan 16's two host specs have not come back.

That last step is this wave's own reading, in the shape plan 16 established one wave earlier and for the
same reason: SC4 is a property of a RUN, every other measurement here is local, and a wave that closes two
failures while quietly reopening two nets nothing that only a real run would have shown.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-13-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-16-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — read where the second copy comes from, on one surface, and answer the shared-cause question</name>

  <read_first>
    - `e2e/confirmation-decay.spec.ts` — the whole file header and the case that measures the moment's box.
      Read the long comment above the visibility assertion in full: what was observed first, the hypothesis
      it refuted, the strict-mode resolution it printed, the reproduction conditions at rest and under
      compile pressure, and the two prohibitions it states. Read the box helper and note that it takes the
      document's first match.
    - `e2e/avatar-crop.spec.ts` — the fixture-handing helper and its docblock's claim that the picker is
      unambiguous by construction, and the focus-indicator case that calls it.
    - `src/components/booking/confirmation-moment.tsx` — the component and the identifier it carries.
    - `src/app/(app)/bookings/[id]/page.tsx` — its single import and single render of that component, and
      the surrounding streaming structure.
    - `src/components/profile/avatar-field.tsx` — the picker element and the file's own record that it is
      the only one in the source tree.
    - `src/app/(app)/layout.tsx` — the streaming bell slot and the box-reserving skeleton it uses. This is
      the control: the booker layout does not mount a live overlay in a fallback.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the flake-that-was-not-a-flake section
      and the proposal block for this plan, including its two first instructions.
    - `.planning/phases/19.1-.../19.1-16-SUMMARY.md` — what the preceding plan's reading actually named and
      what it repaired. This is the input to the shared-cause question.
    - `.planning/phases/19.1-.../evidence/triage-harness.md` — the bring-up sequence and the cold-cache step.
  </read_first>

  <files>
    e2e/confirmation-decay.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt
  </files>

  <action>
STEP 1 — REPRODUCE UNDER BOTH CONDITIONS, ON THE CONFIRMATION SURFACE ONLY.

Bring the app up per the triage harness. Reproduce twice and record both: once with a cold build cache,
once with a cache left dirty by a production build. The recorded shape of this defect is intermittent at
rest and deterministic under compile pressure, so a single green at rest settles nothing. Run at the
runner's own shape, one worker and two retries.

Write raw transcripts into `evidence/triage-duplicate-mount.txt` with these line-anchored markers at
column zero: `CONDITION=cold` or `CONDITION=dirty`, `COUNT-AT-ASSERTION=<n>`, and `EXIT=<code>`.

STEP 2 — DUMP BOTH MATCHES' PROVENANCE. THIS IS THE MEASUREMENT THE WHOLE PLAN TURNS ON.

Inside the reproduction, when the element resolves to more than one, capture for EACH match a walk from the
node up to the document body, one ancestor per line, each line beginning with the marker `ANCESTOR ` at
column zero and carrying: the index of the match it belongs to, the ancestor's tag name, its identifier if
any, its class list, whether it declares the hidden attribute itself, and whether that ancestor is the
page's main landmark. Record any comment nodes that sit as siblings of the outermost differing ancestor —
the delivery mechanism leaves markers there, and they are the difference between a duplicated mount and a
commit-window artefact.

Then sample the count across the window the defect lives in. The case's count assertion passes and two
statements later the resolution reports two, so the arrival is between them. Sample repeatedly from just
before the count assertion to just after the visibility assertion and record one line per sample beginning
`SAMPLE ` with the elapsed milliseconds and the count. Close with a line beginning `ARRIVAL-MS=` giving
when the count first exceeded one, and a line beginning `PERSISTS=` recording whether the second copy is
still present after a bounded wait or whether it goes away on its own.

Those two lines are the discriminator, and the branch they select is stated here so the reading is not
re-argued later:
  - the second copy goes away on its own and its ancestor chain carries the delivery mechanism's own
    markers → it is a commit-window artefact, and the defect is that the measurement runs before the
    surface has committed;
  - the second copy persists and both chains are ordinary page structure → it is a genuine duplicated
    mount, and the defect is in the route;
  - the browser also reported that it had to regenerate the tree on this route → it is the same class as
    the preceding plan's defect, and the shared-cause question is answered yes.

STEP 3 — ANSWER THE SHARED-CAUSE QUESTION, WITH A PAIR.

Repeat Step 1's dirty-cache reproduction on the tree WITHOUT the preceding plan's repair — restore that
plan's source file from a byte copy, never a checkout, because the working tree is mixed-EOL and a checkout
silently rewrites a line-feed file while the status reads clean. Record `SHARED-CAUSE=yes` or
`SHARED-CAUSE=no` with the counts from both sides, and restore the repaired file from its byte copy with a
checksum pair and a porcelain status in the same file. A null result is a finding; write it as one.

STEP 4 — REPAIR THE BRANCH THE READING SELECTED, INSIDE THE CONSTRAINT SET.

Whichever branch Step 2 selected, all of the following must hold afterwards, and the transcript must say
how each was satisfied:

  (a) The locator stays unscoped, and the box helper is not handed the visible one of the two. Both moves
      are prohibited at the case's own site with the reason written there.
  (b) No timeout is widened. The case's own budget and the default expectation timeout both stay as they
      are.
  (c) The height floor and the fold position stay exactly as they are.
  (d) The case still fails when a genuine second copy is present after the surface has settled. This is
      Task 3's proof and it is the reason a commit-window repair must assert the commit rather than wait
      out the window with a sleep.
  (e) If the repair is in production source, the confirmation component's single mount site stays single,
      and nothing in the route's streaming structure is moved above a gate.

Record the chosen repair and the rejected alternatives, each with the constraint that decided it.
  </action>

  <acceptance_criteria>
    - `evidence/triage-duplicate-mount.txt` contains a line beginning `CONDITION=cold` and a line beginning
      `CONDITION=dirty`, each followed by a `COUNT-AT-ASSERTION=` line.
    - The same file contains at least four lines beginning `ANCESTOR ` at column zero, covering both
      matches, and each such line names a tag.
    - The same file contains at least three lines beginning `SAMPLE `, exactly one beginning `ARRIVAL-MS=`
      and exactly one beginning `PERSISTS=`.
    - The same file contains exactly one line beginning `SHARED-CAUSE=` whose value is `yes` or `no`, with
      the counts from both trees printed beneath it.
    - The same file contains a checksum pair for the preceding plan's restored source file, and the two
      values are equal.
    - `npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2`
      reports zero failed and zero flaky.
    - The case's height floor expression and its fold-position expression are byte-unchanged from this
      plan's start.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count, or the output contains a strict-mode resolution naming more than one element</fails_when>
    <automated>grep -c '^ANCESTOR ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt</automated>
    <fails_when>output is less than 4 — fewer than two ancestors were dumped for each of the two matches, so the provenance was not read and any repair below it is a guess</fails_when>
    <automated>grep -c '^SHARED-CAUSE=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt</automated>
    <fails_when>output is any number other than 1 — the question plan 13 handed forward was either never answered or answered twice</fails_when>
    <automated>git diff -U0 -- e2e/confirmation-decay.spec.ts | grep -E '^\+' | grep -cE 'timeout: *[0-9_]+'</automated>
    <fails_when>output is any number other than 0 — a timeout was widened, which this plan forbids</fails_when>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
    <fails_when>output is greater than 7 — a new type error was introduced beyond the seven pre-existing ones in tests/design/</fails_when>
  </verify>

  <done>
    Both matches' ancestor chains are on the record, the arrival time and the persistence of the second copy
    are measured, the shared-cause question carries a one-word answer backed by a before-and-after pair, and
    the confirmation case is green with its locator unscoped, its box helper unchanged in which match it
    takes, and both of its measured constants byte-identical.
  </done>

  <reversibility rating="reversible">
    Whichever branch is taken, the change is one route's delivery structure or one spec's precondition, in a
    single commit, with the pre-change bytes captured as a copy in the same task.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: The same signature on the profile surface — measured, then closed by the same reading or given its own</name>

  <read_first>
    - `e2e/avatar-crop.spec.ts` — the fixture-handing helper, the focus-indicator case that calls it, the
      seeded-avatar helper plan 19.1-10 added, and the one annotated case in the file with its condition and
      its raised budget. Read plan 19.1-10's note that the three case positions in this file have already
      moved once.
    - `src/components/profile/avatar-field.tsx` — the picker element and the file's record that it is the
      only one in the source tree.
    - `.planning/phases/19.1-.../evidence/triage-upload-capability.txt` — the routing table for the four
      upload-dependent cases and the prohibition line, so the case this task touches is not confused with
      the annotated one.
    - `.planning/phases/19.1-.../19.1-10-SUMMARY.md` — the measured finding that an expected-failure
      annotation does not cover a timeout, and the resulting rule about a case's own budget.
    - This plan's own `evidence/triage-duplicate-mount.txt` as written by Task 1 — the branch its reading
      selected and the constraint set it satisfied.
  </read_first>

  <files>
    e2e/avatar-crop.spec.ts,
    src/components/profile/avatar-field.tsx,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt
  </files>

  <action>
STEP 1 — MEASURE THE PROFILE SURFACE THE SAME WAY, BEFORE ASSUMING IT IS THE SAME DEFECT.

Reproduce the two-element resolution on the profile surface under both cache conditions, and dump both
matches' ancestor chains with the same `ANCESTOR ` marker shape Task 1 used, under a heading naming the
surface. Record `ARRIVAL-MS=` and `PERSISTS=` for this surface too.

Then compare, explicitly, and write one line beginning `SAME-BRANCH=` with `yes` or `no` and one sentence
naming the term that decided it. Two surfaces with the same visible signature and different delivery
structures is exactly the situation where a shared cause is assumed and never checked — this line is the
check.

STEP 2 — CLOSE IT.

If the branch is the same, apply the same repair shape Task 1 chose, in this surface's own idiom, and say
at the site that it is the same repair and where the reading lives.

If the branch is different, repair what this surface's own reading names, under the same constraint set:
the fixture-handing helper keeps its unqualified locator so strict mode remains the assertion that the
picker stayed unique; no timeout is widened; and nothing about the crop stage's own preconditions is
relaxed. The helper's docblock is updated to record what was measured — the claim that the picker is
unambiguous by construction is true of the source tree and was false of the delivered document, and that
correction belongs at the site rather than only in a transcript.

Do not touch the file's one annotated case, its condition or its raised budget. That case is a separate,
credential-shaped verdict with its own written reason, and the pinned entry total does not move here.

STEP 3 — RUN THE WHOLE FILE.

The three cases this phase has already touched in this spec sit in a file of many, and plan 19.1-10
recorded that running it beside another spec at this box's default worker count fails on unrelated rows
through compile contention on one shared server. Run it alone, and run it at one worker. Record the
transcript with an `EXIT=` marker.
  </action>

  <acceptance_criteria>
    - `evidence/triage-duplicate-mount.txt` contains a heading naming the profile surface, at least four
      further lines beginning `ANCESTOR `, and one line each beginning `ARRIVAL-MS=` and `PERSISTS=` under
      that heading.
    - The same file contains exactly one line beginning `SAME-BRANCH=` whose value is `yes` or `no`.
    - `e2e/avatar-crop.spec.ts` still contains exactly one expected-failure annotation, and its condition
      and raised budget are byte-unchanged.
    - The fixture-handing helper's locator is still unqualified by any landmark.
    - `npx playwright test e2e/avatar-crop.spec.ts --project=chromium --workers=1` reports zero failed and
      zero flaky.
    - `tests/design/e2e-known-failures.test.ts` passes with its pinned entry total still reading two.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/avatar-crop.spec.ts --project=chromium --workers=1</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count, or the output contains a strict-mode resolution naming more than one element</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/e2e-known-failures.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reports fewer than nine passing cases, or any message in the output reports an entry count other than two</fails_when>
    <automated>grep -c '^SAME-BRANCH=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt</automated>
    <fails_when>output is any number other than 1 — the second surface was either never compared against the first or compared twice with two answers</fails_when>
    <automated>git diff -U0 -- e2e/avatar-crop.spec.ts | grep -E '^\+' | grep -cE 'timeout: *[0-9_]+'</automated>
    <fails_when>output is any number other than 0 — a timeout was widened in a file whose one annotated case already depends on a deliberately raised budget</fails_when>
  </verify>

  <done>
    The profile surface has its own ancestor reading rather than an inherited assumption, a one-word answer
    on whether it is the same branch as the confirmation surface, a repair chosen accordingly, and the whole
    spec green at one worker with the file's single annotation and the pinned entry total both untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove both instruments can still fail — reintroduce the duplicate and watch red</name>

  <behavior>
    - With a genuine second copy of the confirmation section injected into the settled document, the
      confirmation case fails, and its message names the duplicate rather than a height.
    - With a genuine second file picker injected into the settled profile document, the focus-indicator
      case fails at the fixture-handing helper.
    - With neither injection, both cases pass — and the transcript records that pair, not just the reds.
    - Injecting a copy that the delivery mechanism would itself have removed does NOT redden either case,
      which is what separates "the instrument still catches the defect" from "the instrument catches any
      transient".
  </behavior>

  <read_first>
    - `tests/design/workflow-invariants.test.ts` — the mutate-a-copy-then-run idiom and the assertion that
      the mutated text differed from its input, which is what catches a mutation that silently did not
      apply.
    - `.planning/phases/19.1-.../evidence/eol-mutation-builders.txt` — the record of a mutation that did not
      apply and reported a case surviving the deletion of its own conjunct, and the three controls that
      settled it. Its lesson is why every injection below carries an applied count.
    - `.planning/phases/19.1-.../19.1-08-SUMMARY.md` — the technique of watching the GUARD red when the
      original failure does not reproduce, and the requirement that the evidence file say explicitly that
      the red simulates the state rather than the cause.
    - This plan's own `evidence/triage-duplicate-mount.txt` — both surfaces' readings and both repairs.
  </read_first>

  <files>
    e2e/confirmation-decay.spec.ts,
    e2e/avatar-crop.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt
  </files>

  <action>
STEP 1 — INJECT A GENUINE DUPLICATE AND WATCH BOTH CASES RED.

For each surface, add a second copy of the element to the SETTLED document from inside a throwaway probe —
cloning the existing node after the page has finished settling, so the copy is not one the delivery
mechanism would remove on its own. Run the shipped case against it and capture the RED with its message.

Record, per injection, at column zero: `INJECT=<surface>`, `APPLIED=<count>` where the count is the number
of nodes actually added as read back from the document, and `RESULT=red` or `RESULT=green`. An injection
whose applied count is zero is not evidence; it is the failure mode this phase has already paid for once.

Then the controls, in the same file: with no injection, both cases green; and with a copy injected in the
form the delivery mechanism itself produces and then removes, neither case red. The second control is what
distinguishes a repair that asserts the surface has committed from one that merely waits.

Delete the throwaway probes in this same task and prove the deletion by comparing the collected test count
before and after — the probe-and-delete idiom this phase established.

STEP 2 — CORRECT THE MESSAGES TO WHAT WAS MEASURED.

Both cases currently carry long notes written from earlier readings. The confirmation case's note states a
refuted hypothesis, the measurement that refuted it, and two prohibitions; the picker helper's docblock
states that the element is unambiguous by construction. Update each to carry what THIS plan measured — the
provenance of the second copy, the branch it selected, the repair, and where the transcript lives. Keep
every prohibition that is still true and keep the record of the refuted hypothesis, because a note that
reads as a history is what stopped the wrong fix being applied twice already. Delete nothing that a future
reader would need in order to not repeat this.

STEP 3 — CLOSE THE TRANSCRIPT.

Append a closing section carrying: both surfaces' branch verdicts, the shared-cause answer, both repairs
with their rejected alternatives, the injection results with their applied counts, both controls, and one
line beginning `SC4-TERM=` stating in one sentence what this plan closed and what it did not.
  </action>

  <acceptance_criteria>
    - `evidence/triage-duplicate-mount.txt` contains at least two lines beginning `INJECT=`, each paired
      with an `APPLIED=` line whose value is greater than zero and a `RESULT=` line.
    - At least two of those `RESULT=` lines read `RESULT=red`, and at least two further `RESULT=green` lines
      record the two controls.
    - The same file contains exactly one line beginning `SC4-TERM=`.
    - The confirmation case's note still contains its two prohibitions, and the picker helper's docblock
      still states why strict mode is the assertion rather than an inconvenience.
    - `git status --porcelain` shows no throwaway probe file left in the tree, and the collected test count
      matches the count recorded before the probes existed.
    - `npm test` and `npm run test:design` both pass.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/confirmation-decay.spec.ts e2e/avatar-crop.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count, or any test is reported as "did not run"</fails_when>
    <automated>grep -c '^RESULT=red' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt</automated>
    <fails_when>output is less than 2 — one of the two instruments was never shown to still fail against the defect it exists to catch, which is a green that has deleted its own evidence</fails_when>
    <automated>grep -c '^APPLIED=0$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt</automated>
    <fails_when>output is greater than 0 — an injection recorded zero nodes actually added, so its result measured an unmutated document</fails_when>
    <automated>npx playwright test --project=chromium --list 2>&1 | tail -2</automated>
    <fails_when>the printed collected total differs from the total recorded before the throwaway probes existed — a probe was left in the tree</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or a passing total below 2721, or any failed file in the reporter's summary</fails_when>
  </verify>

  <done>
    Both cases are red against a genuine injected duplicate and green against the transient form and against
    no injection at all, every injection carries a non-zero applied count, both notes now state what this
    plan measured while keeping their prohibitions, and the throwaway probes are gone with the collected
    count to prove it.
  </done>
</task>

<task type="auto">
  <name>Task 4: Read this wave on a REAL push-triggered `ci` run — both booker specs closed, and plan 16's two still closed</name>

  <read_first>
    - `.planning/phases/19.1-.../19.1-16-PLAN.md`, its terminal task — the shape this task repeats: push, join
      the run to the pushed head SHA rather than to recency, discard a cancelled conclusion by identity, record
      all five job conclusions, and give each owned spec a one-word verdict against the run's failure list.
      Repeat the convention; do not invent a second one.
    - `.planning/phases/19.1-.../evidence/ci-run-after-16.txt` — the previous wave's reading. Its `OWNED ` lines
      are the baseline this task's regression check is measured against, and its `STILL-RED ` lines predicted
      exactly which failures this wave was supposed to remove.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — plan 19.1-13's capture, with the command
      line above each block: the `gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url`
      identity read, the `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
      per-job read, and the `gh run view --job <jobId> --log` line that reaches the functional job's output.
      Read its `COLLECTED` block: a shortfall in the collected total looks identical to tests passing if only
      the failure count is read.
    - `.github/workflows/ci.yml` — the five `name:` strings, which are the rows of this measurement;
      `gate-e2e (functional Playwright suite)` is the job both this plan's specs run in. And the `concurrency:`
      block, keyed on workflow, ref and event with `cancel-in-progress: true`.
    - `.planning/ROADMAP.md` §"Phase 19.1", Waves 8–11 — why each of these four plans is its own wave: every one
      closes by reading a real run, and the next push cancels the previous plan's. Runs `33971557439` and
      `33971883942` died that way inside this phase.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-04 and D-06 — D-04, that "not red on every push" is a
      property of the RUN and not of one job, which is why all five conclusions are recorded; D-06, that `dev`
      is unprotected by decision and GSD commits to it directly, which is why this push takes no checkpoint.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt
  </files>

  <action>
This is the wave's ONE push. Nothing earlier in this plan pushes and nothing after it does: a second push
cancels this run through the concurrency group.

STEP 1 — PUSH, AND RECORD THE HEAD THE RUN WILL BE JOINED TO.

Push the development branch to its remote with every commit this plan produced. Create
`evidence/ci-run-after-17.txt` and write, at column zero, one line `CI-HEAD-SHA=<sha>` carrying the full
forty-character SHA that is now the remote head, with the commit subjects this push carried beneath it.

STEP 2 — SELECT THE RUN BY THAT SHA, NOT BY RECENCY.

`gh run list --workflow ci.yml --commit <sha> --json databaseId,headSha,event,status,conclusion,createdAt --limit 10`.
If this build of the CLI does not accept the commit filter, fall back to
`gh run list --workflow ci.yml --branch dev --json databaseId,headSha,event,status,conclusion,createdAt --limit 20`
and select on head-SHA equality. Position in the list decides nothing.

Wait for the selected run to leave the in-progress state, then read its identity with
`gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url`, the command
line above its JSON. Record `CI-RUN-ID=<id>`, `CI-EVENT=<event>` and `CI-CONCLUSION=<conclusion>` at column
zero. The event must be the push event.

STEP 3 — A CANCELLED RUN IS NOT A RESULT.

If the selected run concluded cancelled, the concurrency group killed it and it measured nothing; recording
it as green or red would be manufacturing a verdict out of an interruption. Record its id on a line beginning
`DISCARDED-RUN ` with the reason, then measure again — `gh run rerun <id>` for the same SHA, or push this
plan's next commit and select the run for the new head SHA. The join key stays the head SHA and the new
identity replaces the discarded one in `CI-RUN-ID=`. Close with exactly one line `CANCELLED-DISCARDED=<n>`;
zero is a legitimate value and it still gets written.

STEP 4 — FIVE JOBS, THE FAILURE LIST, AND FOUR ACCOUNTABILITY LINES.

Read every job with `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
and write one line per job beginning `JOB ` at column zero with the display name, its conclusion and its
duration. All five, per D-04.

Then `gh run view --job <jobId> --log` for the functional job, and record its summary block verbatim — failed,
flaky, skipped, did not run, passed, wall-clock — followed by the full list of failing specs and titles. Add
one line `GATE-E2E=<conclusion>` at column zero.

Then FOUR lines beginning `OWNED ` at column zero, each carrying a spec path, a vertical bar, and one of the
two words — absent, if no case from that file appears in this run's failure list; present, if any does:

  - `e2e/confirmation-decay.spec.ts` and `e2e/avatar-crop.spec.ts`, the two this plan owns;
  - `e2e/host-headings.spec.ts` and `e2e/overflow-320.spec.ts`, plan 16's two, which must STILL be absent.
    A repair in this wave that reopens the previous wave's is a net of nothing, and the only instrument that
    can see it is a run that executes both.

If any of the four reads as still failing, THIS TASK IS NOT FINISHED and the plan does not claim its gap
closed. If it is one of this plan's own, return to the branch its reading selected with the run's log as the
new instrument and push again, recording the superseded identity beside the new one. If it is one of plan 16's,
that is a regression this wave caused and it is repaired here, not handed on — and the prohibitions still
hold: no locator is scoped, no timeout widened, no entry joins the allowlist.

Also compare the collected total against the figure recorded in `evidence/ci-run-after-16.txt` and write one
line beginning `COLLECTED ` with both numbers and the delta. A shortfall is a red dressed as an improvement.

STEP 5 — SAY WHAT IS STILL RED AND WHO OWNS IT.

`gate-e2e` may still be red for the day-click failure plan 18 owns, and `gate-visual` stays red until plan 19
mints. Write one line beginning `STILL-RED ` per remaining failing spec, each naming the plan that owns it,
and close with one line `SC4-RUN-TERM=` stating in one sentence what this run proved and what it did not.
  </action>

  <acceptance_criteria>
    - `evidence/ci-run-after-17.txt` contains exactly one line beginning `CI-HEAD-SHA=` carrying a full
      forty-character SHA, and exactly one line each beginning `CI-RUN-ID=`, `CI-EVENT=` and `CI-CONCLUSION=`.
    - `CI-EVENT=` names the push event, and the recorded run's own head SHA equals the recorded
      `CI-HEAD-SHA=` value.
    - The file contains at least five lines beginning `JOB `, one per job in `.github/workflows/ci.yml`, each
      carrying a conclusion, and exactly one line beginning `GATE-E2E=` with the functional job's summary
      block verbatim beneath the command line that produced it.
    - The file contains exactly four lines beginning `OWNED ` — this plan's two and plan 16's two — and none
      is recorded as still present in the failure list.
    - The file contains exactly one line beginning `CANCELLED-DISCARDED=`, and one `DISCARDED-RUN ` line for
      each run that value counts.
    - The file contains one line beginning `COLLECTED ` carrying this run's total, the previous wave's total
      and the delta, and one line beginning `STILL-RED ` per remaining failing spec, each naming an owning
      plan, and exactly one line beginning `SC4-RUN-TERM=`.
    - `git log origin/dev..HEAD --oneline` produces no output.
  </acceptance_criteria>

  <verify>
    <automated>gh run view $(grep -m1 '^CI-RUN-ID=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt | cut -d= -f2) --json event,conclusion,headSha --jq '[.event,.conclusion,.headSha]|@tsv'</automated>
    <fails_when>non-zero exit — the recorded id does not resolve to a real run; or the first field is not "push" — a dispatch or pull-request run was read instead of the push-triggered run SC4 is a property of; or the second field is "cancelled" — the concurrency group killed it and it measures nothing, so measure again rather than recording that value; or the third field does not equal the CI-HEAD-SHA= line in the same file — the run that was read is not the run that measured this tree</fails_when>
    <automated>grep -c '^JOB ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt</automated>
    <fails_when>output is less than 5 — fewer than the workflow's five jobs were recorded with a conclusion, so a claim about the RUN rests on a reading of one job, which is what D-04 forbids</fails_when>
    <automated>grep -c '^OWNED ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt</automated>
    <fails_when>output is any number other than 4 — this plan's two specs and plan 16's two were not each given a verdict against the real run's failure list, so a repair here that reopened the previous wave's would be invisible</fails_when>
    <automated>grep -cE '^OWNED .+\|present$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt</automated>
    <fails_when>output is greater than 0 — a spec this wave is accountable for still appears in the real run's failure list; if it is one of this plan's own the repair did not hold on the runner, and if it is one of plan 16's this wave caused a regression, and either way the task returns to its diagnosis and pushes again</fails_when>
    <automated>grep -c '^CANCELLED-DISCARDED=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt</automated>
    <fails_when>output is any number other than 1 — the concurrency-group accounting is missing or doubled, and without it a cancelled run can be silently substituted for a measured one</fails_when>
    <automated>git log origin/dev..HEAD --oneline</automated>
    <fails_when>any output — a commit this plan made never reached the remote, so the run that was read measured a tree that is not this one</fails_when>
  </verify>

  <done>
    A named, push-triggered `ci` run selected by the pushed head SHA carries all five job conclusions, the
    functional job's summary block verbatim, and four `OWNED ` verdicts — both booker specs this plan closed
    and both host specs plan 16 closed, all absent from the failure list; every cancelled run is discarded by
    identity with a count; the collected total is compared against the previous wave's; and every failure
    still standing is named with the plan that owns it.
  </done>

  <reversibility rating="reversible">
    One evidence file and a push to the unprotected development branch. Nothing is published and no protected
    ref moves; per D-06 `dev` is exactly the branch GSD commits to directly.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paid booker → the booking detail route | The surface a booker lands on immediately after money has moved; what it renders is the confirmation that the booking is real. |
| uploaded file → the profile surface's picker | The one file input in the application; a second one in the delivered document is an ambiguity about which control a person is operating. |
| commit author → `e2e/` | The instruments that named both defects live here, and weakening either is a quieter change than changing the product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-80 | Spoofing | the profile surface's file picker | high | mitigate | Two pickers in one document means a person can operate one while the application reads the other. The repair keeps the helper's unqualified locator so strict mode remains the assertion that the picker stayed unique, and Task 3 proves that assertion can still fail. |
| T-19.1-81 | Tampering | `e2e/confirmation-decay.spec.ts`'s measured constants | high | mitigate | The height floor and the fold position are asserted byte-unchanged by a task verify, and a widened-timeout grep runs on the diff of both specs. |
| T-19.1-82 | Repudiation | the injection evidence | high | mitigate | Every injection records an applied count read back from the document; a zero applied count fails a verify outright, because a probe that silently did not apply has already produced a false survival claim in this phase. |
| T-19.1-83 | Tampering | `tests/design/e2e-known-failures.test.ts` | high | mitigate | Untouched and run as a verify; neither of this plan's cases may be recorded, and the pinned entry total stays at two. |
| T-19.1-84 | Information Disclosure | evidence transcripts under `.planning/` | medium | mitigate | The dumps record tag names, identifiers, class lists and counts. No seeded credential, no session cookie and no database URL is echoed; the booker fixtures mint synthetic addresses and those are not reproduced in the transcript. |
| T-19.1-85 | Denial of Service | a repair that waits rather than asserts | medium | mitigate | The second control in Task 3 — a transient copy must NOT redden the case — is what forbids a repair that merely sleeps past the window and would therefore mask a real duplicate later. |
| T-19.1-86 | Repudiation | the real `ci` run this plan is read on | high | mitigate | The run is selected by the pushed head SHA rather than by recency, its identity is re-read as a task verify, a `cancelled` conclusion is discarded by identity and counted rather than read as a verdict, and `git log origin/dev..HEAD` asserts the measured tree is this one. |
| T-19.1-87 | Tampering | the previous wave's closed failures | high | mitigate | Plan 16's two specs get their own `OWNED ` verdict on this wave's run, so a repair here that reopens a repair there fails a task verify instead of netting to zero unnoticed; the collected total is compared against the previous wave's for the same reason. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan and no new dependency is introduced. If a task reaches for one, the legitimacy gate runs then. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed production source, conditional on Task 1's reading:** the delivery structure of the booking detail
route around its confirmation section, and/or the profile surface's picker element. Which of the two, and
whether either, is decided by the ancestor reading and recorded with the rejected alternatives.

**Changed instruments:** `e2e/confirmation-decay.spec.ts`'s precondition and its note, rewritten around what
this plan measured while keeping both prohibitions and the record of the refuted hypothesis;
`e2e/avatar-crop.spec.ts`'s fixture-handing helper docblock, corrected from "unambiguous by construction" to
what the delivered document actually contained.

**New evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-duplicate-mount.txt`
— `CONDITION=` / `COUNT-AT-ASSERTION=` pairs for both cache conditions, `ANCESTOR ` dumps for both matches on
both surfaces, `SAMPLE ` lines with `ARRIVAL-MS=` and `PERSISTS=`, one `SHARED-CAUSE=`, one `SAME-BRANCH=`,
`INJECT=` / `APPLIED=` / `RESULT=` triples with two controls, and one `SC4-TERM=` line.

**Second new evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-17.txt`
— the real push-triggered `ci` run this wave is read on: `CI-HEAD-SHA=`, `CI-RUN-ID=`, `CI-EVENT=`,
`CI-CONCLUSION=`, five `JOB ` lines with conclusions and durations, `GATE-E2E=` with the functional job's
summary block verbatim, four `OWNED ` lines (this plan's two booker specs and plan 16's two host specs),
`CANCELLED-DISCARDED=` with a `DISCARDED-RUN ` line per discarded identity, one `COLLECTED ` line against the
previous wave's total, `STILL-RED ` lines naming the plan that owns each remaining failure, and one
`SC4-RUN-TERM=` line.

**New npm scripts:** none. **New dependencies:** none. **New allowlist entries:** none — the pinned entry
total stays at two, and the one existing annotation in the avatar spec is byte-unchanged. **New counted
workflow invariants:** none.
</artifacts_this_phase_produces>

<verification>
- `npx playwright test e2e/confirmation-decay.spec.ts e2e/avatar-crop.spec.ts --project=chromium --workers=1 --retries=2` reports zero failed, zero flaky and zero not-run.
- `npm test` and `npm run test:design` both pass at or above the totals recorded at this phase's head.
- `node scripts/verify-workflows.mjs` exits 0 with fifty-seven invariants.
- `npx tsc --noEmit` produces no `error TS` line beyond the seven pre-existing ones in `tests/design/`.
- `evidence/triage-duplicate-mount.txt` carries every marker the acceptance criteria name, including two reds and two green controls with non-zero applied counts.
- `evidence/ci-run-after-17.txt` names one push-triggered `ci` run whose head SHA equals the pushed head, carries all five job conclusions, and records all four accountable specs — this plan's two and plan 16's two — as absent from that run's `gate-e2e` failure list.
- `git log origin/dev..HEAD --oneline` produces no output.
- `git status --porcelain` shows no leftover throwaway probe, and the collected test count is back to what it was before the probes existed.
</verification>

<success_criteria>
Both booker cases resolve their element to exactly one node and pass at the runner's own shape; the second
copy's provenance is on the record as an ancestor walk rather than an inference; the shared-cause question
plan 13 handed forward carries a one-word answer backed by a before-and-after pair; both instruments are
proved still able to fail against a genuine injected duplicate and proved NOT to fail against the transient
form; neither locator was scoped, neither timeout widened, neither measured constant moved, and no entry
was added to the allowlist; and a NAMED, real, push-triggered `ci` run selected by the pushed head SHA
records both booker specs AND plan 16's two host specs as absent from `gate-e2e`'s failure list, with every
cancelled run discarded by identity rather than read as a verdict.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-17-SUMMARY.md` when done.

State plainly: (1) where the second copy came from, in the ancestor walk's own terms; (2) the shared-cause
answer, with the counts from both trees; (3) whether the two surfaces took the same branch; (4) the repair
chosen on each and what was rejected; (5) the injection results; (6) the identity of the real `ci` run this
wave was read on — its id, its head SHA, its conclusion and the `gate-e2e` job's conclusion — with all four
`OWNED ` verdicts, the collected delta against the previous wave, and every run discarded as cancelled. If
either case is still red, record the verdict and propose the follow-up in the D-02 shape — and do not
annotate it.
</output>
