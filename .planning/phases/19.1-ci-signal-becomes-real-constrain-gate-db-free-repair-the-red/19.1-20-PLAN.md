---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 20
type: execute
wave: 12
depends_on:
  - "19.1-19"
files_modified:
  - e2e/host-route-reachability.spec.ts
  - playwright.config.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-20.txt
autonomous: true
requirements:
  - CI-01
gap_closure: true
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: the seven measured host-wizard/edit-route failures are treated as a functional cluster and repaired at the measured shared cause; none is hidden as drift or flake."
    - "Route reachability is the first discriminator: anonymous requests to /host/listings, /host/listings/new, /host/listings/route-reachability/edit, and /host/listings/route-reachability/availability each return 307 with redirects disabled, while 404 remains evidence that the router did not reach the module."
    - "The clean-runner diagnostic records the running server mode and the actual host edit/availability entries in .next/dev/server/app-paths-manifest.json before any product-source edit or causal claim."
    - "The authenticated edit arrival renders exactly one [data-testid='wizard-step-rail'] and the step-one Get started control; a heading, loading shell, or 404 does not count as arrival."
    - "All six downstream property groups remain independently asserted and pass: axe at 320 and 1280, heading hierarchy, the 14-stop keyboard sequence, one-tree ownership, 320px overflow, and route reachability."
    - "The exact-SHA push run finishes gate-e2e successfully with all seven owned failures absent from both failed and flaky results and no owned case left not-run; gate-db-free, gate-db, and gate-price-parity stay green."
    - "D-04: Plan 19.1-19 retains exclusive ownership of visual diagnosis and repair, and the exact-SHA closure run must preserve its result as gate-visual=success; a red visual job stops this plan and routes back to Plan 19.1-19 without visual interpretation or edits here."
    - "UI consideration E1/empty: an unfilled draft remains the existing editable form with unselected controls and unmet checklist rows; no substitute empty screen is introduced."
    - "UI consideration E1/loading: PanelSkeleton labelled Loading your listing remains until the wizard rail exists, and neither a heading nor the skeleton satisfies arrival."
    - "UI consideration E1/error: existing ErrorState and Save-state refusals remain intact, and a router-level 404 is not converted into an editable-page error panel."
    - "UI consideration E1/partial: missing fields remain editable with not-done checklist rows; no value is defaulted and no incomplete row or step is hidden or skipped."
    - "UI consideration E1/overflow: at 320px, min-w-0 wrapping, navigation, and the collapsed checklist remain inside the viewport without changing the layout contract."
    - "UI consideration E1/long-text: labels and refusal text wrap without truncation or horizontal scrolling, and the save control remains adjacent to its status."
    - "UI consideration E2/empty: no substitute empty state is introduced and exactly one checklist state exists even when it has zero rows."
    - "UI consideration E2/loading: the rail and checklist remain unresolved while loading; the skeleton persists until exactly one rail can render."
    - "UI consideration E2/error: existing ErrorState remains distinct from any partial rail/checklist state, and save errors remain form status."
    - "UI consideration E2/populated: markers, rows, placements, labels, and current/visited/future semantics remain byte-for-byte and structurally unchanged."
    - "UI consideration E2/partial: unmet items stay not-done, only visited steps are actionable, and future steps remain inert."
    - "UI consideration E2/overflow: at 320px the rail, collapsed disclosure, labels, targets, and focus indicators stay inside the viewport with no second tree."
    - "UI consideration E2/zero-one-many: the rail renders the established eight-of-nine marker model exactly once, and a variable checklist still has exactly one node at 320, 768, and 1280 widths."
    - "UI consideration E2/long-text: listing names and checklist text reflow without truncation or horizontal scrolling."
    - "UI consideration E3/loading: Get started is absent until the authenticated route resolves, so loading cannot report the arrival landmark."
    - "UI consideration E3/error: the arrival control is not synthesized for error or not-found states; those states remain distinct and keep the reachability discriminator red."
    - "UI consideration E3/long-text: Get started keeps its exact byte-identical visible and accessible name without truncation, and no other copy substitutes for it."
  artifacts:
    - "e2e/host-route-reachability.spec.ts exposes runner-visible route-manifest diagnostics without weakening the four status assertions."
    - "playwright.config.ts contains the measured server-mode repair only if the clean matrix selects it."
    - ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt records watched-red evidence, manifest entries, the clean server-mode matrix, and downstream proof."
    - ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-20.txt records exact-SHA job and owned-case conclusions."
  key_links:
    - "playwright.config.ts webServer.command -> the same Next server whose .next/dev/server/app-paths-manifest.json state is reported by e2e/host-route-reachability.spec.ts."
    - "anonymous edit 307 -> authenticated owner arrival -> unchanged six-suite property assertions."
    - "git rev-parse HEAD -> GitHub push run headSha -> gate-e2e job and seven owned Playwright case outcomes."
  assumptions:
    - statement: "CI-01's spec-less deterministic edge probe returned exactly one unclassified item with probe 'unclassified — review manually', status unresolved, verification null, coverage applicable 1/resolved 0/unresolved 1. The item is flagged for manual review and is not treated as resolved by this plan."
      category: unclassified
      status: unresolved
      verification: null
      flagged: true
    - statement: "The noun is unchanged from Plan 19.1-18: the remaining red is the seven-case host-wizard/edit-route cluster. A shared server-mode cause is a hypothesis only until the exact clean-runner manifest/request matrix measures it."
      status: measured-before-action
      flagged: true
  prohibitions:
    - statement: "The functional CI badge must not be made green by suppressing, quarantining, retrying, increasing timeouts for, or allowlisting any of the seven owned failures; their independent assertions remain active."
      category: safety
      status: unverified
      flagged: true
    - statement: "A local/container pass, a merely recent run, or a run for another commit must not be presented as closure; only a completed push run whose head SHA equals the pushed repair SHA qualifies."
      category: transparency
      status: unverified
      flagged: true
    - "No visual redesign, layout adjustment, copy change, component extraction, or UI cleanup."
    - "Do not weaken the edit page's session-before-database sequence, host-role redirect, ownership predicate, or notFound IDOR boundary."
    - "Do not add retries, timeout increases, quarantine, test.skip, test.fixme, or known-failure entries for the seven owned cases."
    - "Do not create or update visual baselines; Plan 19.1-19 owns visual closure."
    - "Do not change checkout, hold-countdown, booking-confirmation decay, or deriveBookable behavior."
    - "Do not add runtime dependencies, schema or migration changes, CI jobs, credentials, or environment variables."
    - "Do not edit product source before the route/module probe and watched-red evidence; this plan does not authorize edits beneath src/app/(host)/host/listings/[id]/edit/."
---

<objective>
Close the seven-case host-wizard/edit-route functional cluster that remains red after Plan 19.1-18, while preserving the approved UI and authorization contracts.

Purpose: CI-01 is not credible until the functional Playwright lane reaches the edit route on a clean GitHub runner and executes the six independently valuable wizard property groups without suppression.
Output: a measured runner/server-mode diagnosis, the narrow route-harness repair selected by that diagnosis, unchanged-contract local proof, and an exact-SHA real-run evidence artifact.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-18-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-19-PLAN.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
</context>

<source_coverage_audit>

- GOAL: the ROADMAP's functional-CI signal and Phase 19.1 success criterion 4 are covered here; success criterion 5 remains covered by the already-existing CI publication plans and is not reopened.
- REQ: CI-01 is covered by retaining the separate gate-e2e job and proving its existing functional Playwright specs on a real exact-SHA run.
- RESEARCH: watched-red diagnosis, clean runtime/manifests, narrow same-layer repair, no new package, no schema work, and real GitHub-run evidence are all represented.
- CONTEXT: D-01, D-02, and D-03 govern the no-suppression functional repair; D-04 keeps visuals in Plan 19.1-19. D-05 through D-08 remain implemented by existing Plans 14 and 15 and are not replanned. Deferred sharding, visual refresh, and remaining review items are excluded.
- UI-SPEC: every UI Considerations row is represented in `must_haves.truths`; every no-change rule remains in `must_haves.prohibitions` and in task acceptance.

</source_coverage_audit>

<assumption_delta>

- Unchanged: the measured gap is the seven host-wizard/edit-route failures from run 34008844133; Plan 19.1-18's checkout day-click issue is closed.
- Newly constrained: the first discriminator is runner route/module reachability, not an inferred wizard rendering defect.
- Still unasserted: default Turbopack development mode is only a candidate shared cause. The plan selects the `--webpack` repair only if the clean runner/default-dev versus Webpack/production matrix proves that direction; otherwise execution stops with the captured evidence for replanning rather than editing product code speculatively.
- Flagged: CI-01's descriptor-less spec probe remains unresolved and visible in frontmatter; the plan does not auto-dismiss it.

</assumption_delta>

<external_api_coverage>

No product external API integration is introduced or changed. GitHub's API/CLI is used only as evidence transport for the repository's existing CI run, so a fabricated endpoint/request-response matrix would be inapplicable. The relevant integrity boundary is instead covered by exact workflow, event, branch, and head-SHA selection.

</external_api_coverage>

<schema_push_detection>

No schema-relevant file is in `files_modified`, the research identifies no data migration, and the plan adds no schema push or migration task.

</schema_push_detection>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Measure the clean runner route boundary and repair the Playwright server mode</name>
  <files>e2e/host-route-reachability.spec.ts, playwright.config.ts, .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt</files>
  <read_first>
    - AGENTS.md — the installed Next version has breaking conventions.
    - node_modules/next/dist/docs/01-app/03-api-reference/06-cli/next.md — authoritative `next dev --webpack` and production-server CLI behavior.
    - node_modules/next/dist/docs/01-app/03-api-reference/08-turbopack.md — default development bundler behavior and diagnostics.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md — the installed version's dynamic segment rules.
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md — the installed version's route-group URL behavior.
    - e2e/host-route-reachability.spec.ts — preserve all four anonymous 307 assertions and its existing failure contract.
    - playwright.config.ts — the single Playwright webServer boundary this task may repair.
    - next.config.ts — confirm that no existing configuration explains or should absorb the route discrepancy.
    - .github/workflows/ci.yml — preserve the separate gate-e2e job, current environment, and 90-minute cap.
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt — exact watched-red case names and runner SHA.
  </read_first>
  <behavior>
    - Red: on the clean GitHub runner, the anonymous edit request is non-307 and the diagnostic prints the actual development server mode plus whether the edit and availability route keys exist in the live app-paths manifest.
    - Green: the same four requests return 307 with redirects disabled, and route-manifest diagnostics remain available on any future failure.
    - Guard: the selected repair changes only the Playwright-owned server launch boundary; route UI, auth order, notFound ownership enforcement, assertions, retries, and timeouts are unchanged.
  </behavior>
  <action>
Per D-01 and D-02, start from the real red rather than the product UI. Preserve a runner-visible watched-red record from the exact failing gate-e2e run, then add a focused `readHostRouteManifestState` diagnostic in `e2e/host-route-reachability.spec.ts`. On a non-307 response it must report the running server command/mode and the actual top-level and per-route state for the edit and availability keys in `.next/dev/server/app-paths-manifest.json`; it must not turn manifest absence into an allowed result or expose environment values.

Run a clean discriminator matrix with the same four redirect-disabled requests against (a) the configured default development server, (b) `next dev --webpack`, and (c) a clean `next build` plus `next start`. Record commands, clean-state preparation, response statuses, manifest locations, exact edit/availability entries, and process termination in `triage-host-wizard-route.txt`. If local/container default mode cannot reproduce the runner result, push the instrumentation-only commit and capture these values on the clean GitHub runner before choosing a repair.

Only when the measured evidence shows the default development server missing/failing the edit route while Webpack and the production build both expose it and return the required status, change `playwright.config.ts` `webServer.command` to exactly `npm run dev -- --webpack`. This is a reversible harness repair at the measured boundary, not a product-route change. If the matrix does not select that branch, stop with the evidence artifact and report that the assumption was contradicted; do not edit the edit page, wizard, Next config, route guards, assertions, or unrelated source. Do not change the gate-e2e timeout, dependencies, job definition, credentials, or environment.
  </action>
  <verify>
    <automated>npx playwright test e2e/host-route-reachability.spec.ts --project=chromium --list</automated>
    <fails_when>The tracked route-reachability spec cannot be collected as part of the Chromium functional suite.</fails_when>
    <automated>npx playwright test e2e/host-route-reachability.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>Any of the four anonymous requests is not a 307 with redirects disabled, or the failure output lacks concrete live-manifest state.</fails_when>
    <automated>$lines = @(npx tsc --noEmit 2&gt;&amp;1); $errors = @($lines | Where-Object { $_ -match 'error TS' }); $touched = @($errors | Where-Object { $_ -match '^(e2e[/\\]host-route-reachability\.spec\.ts|playwright\.config\.ts)\(' }); if ($errors.Count -gt 7 -or $touched.Count -gt 0) { $errors; exit 1 }; exit 0</automated>
    <fails_when>The established seven-error tests/design baseline grows, or an error names e2e/host-route-reachability.spec.ts or playwright.config.ts.</fails_when>
  </verify>
  <acceptance_criteria>
    - Watched-red evidence precedes the repair and identifies the runner's actual status, server mode, and edit/availability manifest entries.
    - The clean three-mode matrix is recorded and selects the implemented branch; no cause is asserted before those observations.
    - All four anonymous routes return 307 after the repair with redirects disabled.
    - Product route/UI files, auth/notFound behavior, retries, timeouts, test disposition, dependencies, workflow jobs, credentials, and environment are unchanged.
  </acceptance_criteria>
  <done>The runner's first failing boundary is observable, the evidence-selected Playwright server mode reaches all four routes, and the vertical route-to-harness slice passes without weakening a product or test contract.</done>
</task>

<task type="auto">
  <name>Task 2: Prove owner arrival and the six frozen wizard property groups</name>
  <files>.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt</files>
  <read_first>
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md — frozen wizard, route, responsive, and no-change contract.
    - src/app/(host)/host/listings/[id]/edit/page.tsx — verify the immutable session/role/ownership/notFound sequence without editing it.
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx — verify the established rail and Get started landmarks without editing it.
    - src/app/(host)/host/listings/[id]/edit/loading.tsx — distinguish PanelSkeleton from owner arrival without editing it.
    - e2e/axe-sweep.spec.ts — the 320/1280 accessibility property.
    - e2e/host-headings.spec.ts — the wizard heading-hierarchy property.
    - e2e/host-route-reachability.spec.ts — the four-route first discriminator.
    - e2e/keyboard-composites.spec.ts — the 14-stop keyboard property.
    - e2e/one-tree.spec.ts — the single semantic-tree property.
    - e2e/overflow-320.spec.ts — the narrow-viewport overflow property.
  </read_first>
  <action>
Using the repaired Playwright launch boundary from Task 1, execute the authenticated owner path first and record that it reaches exactly one `[data-testid="wizard-step-rail"]` and the step-one `Get started` control. Record separate negative observations that the page is not still the labelled loading skeleton, the heading alone was not used as proof, and the result is not a not-found page.

Then run the six existing specs together against Chromium and append their exact case results to `triage-host-wizard-route.txt`. Preserve all UI-SPEC rows and frozen contracts: accessibility at both widths, heading hierarchy, the 14-stop keyboard sequence, single tree ownership, 320px overflow, route reachability, the eight-of-nine rail model, checklist cardinality, responsive one-tree behavior, exact labels/copy, loading/error/partial/populated semantics, and auth/IDOR behavior. Per D-01/D-03, do not edit, skip, quarantine, allowlist, retry, soften, rename, or combine these assertions. If any downstream property remains red after owner arrival, record the distinct failure and stop for a new measured gap plan instead of changing the frozen UI surface.
  </action>
  <verify>
    <automated>npx playwright test e2e/axe-sweep.spec.ts e2e/host-headings.spec.ts e2e/host-route-reachability.spec.ts e2e/keyboard-composites.spec.ts e2e/one-tree.spec.ts e2e/overflow-320.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>Owner arrival is not proven or any accessibility, heading, route, keyboard, one-tree, or 320px overflow assertion fails or is not executed.</fails_when>
    <automated>npm run test:design</automated>
    <fails_when>The unchanged design-system contract fails after the harness repair.</fails_when>
  </verify>
  <acceptance_criteria>
    - Owner-authenticated arrival has exactly one wizard rail and the exact Get started landmark, with loading, heading-only, and not-found outcomes rejected.
    - All seven originally owned case titles across the six specs pass in one targeted Chromium execution.
    - Every UI consideration in frontmatter remains true; no frozen wizard, loading, design-token, CSS, primitive, checklist, route-auth, or ownership source is modified.
    - The assertions retain independent failure value and no retry, timeout, skip, fixme, quarantine, or allowlist mechanism is added.
  </acceptance_criteria>
  <done>The real owner path reaches the unchanged wizard once, and all six downstream property groups pass without a UI or assertion change.</done>
</task>

<task type="auto">
  <name>Task 3: Close the functional gap on an exact-SHA push run</name>
  <files>.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-20.txt</files>
  <read_first>
    - .github/workflows/ci.yml — select the existing CI workflow and preserve separate job boundaries.
    - scripts/verify-workflows.mjs — run the repository's structural workflow checker before dispatch.
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt — retain the exact seven owned case names and evidence vocabulary.
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt — ensure the pushed repair is exactly the measured local result.
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-19-PLAN.md — keep visual ownership and interpretation separate.
  </read_first>
  <action>
Run the workflow contract checks, push the completed Plan 20 repair commit, and select the CI workflow run by `event=push`, the current target branch, and an exact `headSha` match to `git rev-parse HEAD`. Ignore and replace cancelled/superseded runs rather than presenting them as evidence. Wait for the matched run to complete, require its overall conclusion to be `success`, and inspect the matched gate-e2e job's own list-reporter log rather than a local rerun or prose summary.

Write `ci-run-after-20.txt` with exact machine-readable identity lines `CI-HEAD-SHA=`, `CI-RUN-ID=`, `CI-EVENT=push`, `CI-BRANCH=`, and `CI-CONCLUSION=success`; the run URL and timing; exactly one `JOB gate-*=success` line for each of the five jobs; Playwright collected/passed/failed/flaky/skipped/not-run counts; and `FAILED-OWNED=0`, `FLAKY-OWNED=0`, and `OWNED-NOT-RUN=0`. Add exactly seven rows in the form `OWNED-CASE=<stable-id>|<spec path>|<exact case title>|passed|RUN=<run id>|SHA=<head SHA>` for `axe-320`, `axe-1280`, `host-headings`, `host-route-reachability`, `keyboard-composites`, `one-tree`, and `overflow-320`. Derive every row from a non-retry positive list-reporter line in that matched gate-e2e job log; a missing, skipped, failed, retried/flaky, or did-not-run case cannot produce a row.

Per D-04, require `gate-visual=success` and the whole matched workflow run green as preservation checks even though Plan 19.1-19 exclusively owns visual diagnosis and repair. If gate-visual is red, stop Plan 20, record only the matched run identity and `VISUAL-HANDOFF=19.1-19`, and route diagnosis/repair back to Plan 19.1-19; do not inspect the diff as a visual verdict, change baselines or visual specs, or close SC4 from the functional result alone.
  </action>
  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>The existing CI workflow no longer satisfies the repository-wide workflow contract.</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=ci</automated>
    <fails_when>The separate gate-e2e job, its job boundary, or another CI-specific invariant fails validation.</fails_when>
    <automated>$sha = (git rev-parse HEAD).Trim(); if ($LASTEXITCODE -ne 0) { exit 1 }; $branch = (git branch --show-current).Trim(); if ($LASTEXITCODE -ne 0 -or -not $branch) { exit 1 }; $candidateJson = @(gh run list --workflow CI --branch $branch --event push --limit 20 --json databaseId,headSha,status,conclusion); if ($LASTEXITCODE -ne 0) { exit 1 }; $runs = @(ConvertFrom-Json -InputObject ($candidateJson -join "`n")); $matches = @($runs).Where({ $_.headSha -eq $sha -and $_.status -eq 'completed' }); if ($matches.Count -eq 0) { exit 1 }; $candidate = $matches[0]; $runJson = @(gh run view $candidate.databaseId --json databaseId,headBranch,headSha,event,status,conclusion,url,jobs); if ($LASTEXITCODE -ne 0) { exit 1 }; $run = ConvertFrom-Json -InputObject ($runJson -join "`n"); if ($run.headSha -ne $sha -or $run.headBranch -ne $branch -or $run.event -ne 'push' -or $run.status -ne 'completed' -or $run.conclusion -ne 'success') { exit 1 }; $jobNames = @('gate-db-free (lint + design + build + workflow parse)','gate-db (vitest against PostGIS 18)','gate-price-parity (DB-vs-DOM price, 1 spec)','gate-visual (GATE-01 visual regression)','gate-e2e (functional Playwright suite)'); foreach ($name in $jobNames) { $job = @($run.jobs).Where({ $_.name -eq $name }); if ($job.Count -ne 1 -or $job[0].conclusion -ne 'success') { exit 1 } }; $e2e = @($run.jobs).Where({ $_.name -eq 'gate-e2e (functional Playwright suite)' })[0]; $logLines = @(gh run view $run.databaseId --job $e2e.databaseId --log); if ($LASTEXITCODE -ne 0) { exit 1 }; $log = $logLines -join "`n"; $cases = @([pscustomobject]@{ id='axe-320'; file='e2e/axe-sweep.spec.ts'; title='/host/listings/[id]/edit · wizard · court · 320px' },[pscustomobject]@{ id='axe-1280'; file='e2e/axe-sweep.spec.ts'; title='/host/listings/[id]/edit · wizard · court · 1280px' },[pscustomobject]@{ id='host-headings'; file='e2e/host-headings.spec.ts'; title='wizard · whole space, every step' },[pscustomobject]@{ id='host-route-reachability'; file='e2e/host-route-reachability.spec.ts'; title='/host/listings/route-reachability/edit resolves to its module (anonymous ⇒ 307)' },[pscustomobject]@{ id='keyboard-composites'; file='e2e/keyboard-composites.spec.ts'; title='/host/listings/[id]/edit · the wizard at 320 — 14 stops, in order' },[pscustomobject]@{ id='one-tree'; file='e2e/one-tree.spec.ts'; title='wizard · /host/listings/[id]/edit' },[pscustomobject]@{ id='overflow-320'; file='e2e/overflow-320.spec.ts'; title='/host/listings/[id]/edit · court' }); $evidence = Get-Content -Raw '.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-20.txt'; $evidenceLines = @($evidence -split "`r?`n"); $ownedRows = @($evidenceLines).Where({ $_ -like 'OWNED-CASE=*' }); if ($ownedRows.Count -ne 7) { exit 1 }; $markers = @("CI-HEAD-SHA=$sha","CI-RUN-ID=$($run.databaseId)",'CI-EVENT=push',"CI-BRANCH=$branch",'CI-CONCLUSION=success','JOB gate-db-free=success','JOB gate-db=success','JOB gate-price-parity=success','JOB gate-visual=success','JOB gate-e2e=success','FAILED-OWNED=0','FLAKY-OWNED=0','OWNED-NOT-RUN=0'); foreach ($marker in $markers) { if (@($evidenceLines).Where({ $_ -ceq $marker }).Count -ne 1) { exit 1 } }; $separator = [char]124; foreach ($case in $cases) { $passed = @($log -split "`r?`n").Where({ $_.Contains($case.file) -and $_.Contains($case.title) -and $_ -match '✓' -and $_ -notmatch '\(retry #' }); if ($passed.Count -ne 1) { exit 1 }; $expected = 'OWNED-CASE=' + $case.id + $separator + $case.file + $separator + $case.title + $separator + 'passed' + $separator + 'RUN=' + $run.databaseId + $separator + 'SHA=' + $sha; if (@($ownedRows).Where({ $_ -ceq $expected }).Count -ne 1) { exit 1 } }; exit 0</automated>
    <fails_when>The exact-SHA push run is not wholly green, any required job including gate-visual is not successful, any named owned case lacks exactly one non-retry positive result in the matched gate-e2e log, or the evidence identity and seven rows do not exactly match that run.</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence run is a completed, wholly successful push run whose head SHA exactly equals the pushed Plan 20 repair commit.
    - gate-e2e succeeds, and the matched job log contains exactly one non-retry positive result for each of the seven named owned cases; none is failed, flaky, skipped, or not run.
    - gate-db-free, gate-db, gate-price-parity, and gate-visual all succeed, preserving the phase's existing CI boundaries and D-04's whole-run closure requirement.
    - The evidence file records the same SHA, run ID, branch, event, run conclusion, five job conclusions, and exact seven-case set that the automated check reads from GitHub.
    - If gate-visual is red, Plan 20 stops and hands the run back to Plan 19.1-19 without interpreting or repairing the visual failure.
  </acceptance_criteria>
  <done>The exact repair SHA has one wholly green push workflow, all five jobs including gate-visual succeed, and the matched gate-e2e log plus identity-bound evidence prove all seven owned cases passed once without retry or suppression.</done>
</task>

</tasks>

<threat_model>

Security enforcement: active. Target: OWASP ASVS Level 1. Any unmitigated high or critical item blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser request -> Next route/auth guard | Anonymous and authenticated traffic crosses routing, session, role, and listing-ownership enforcement. |
| Playwright harness -> Next development server | The test-owned launch mode determines which route manifest and modules the functional suite exercises. |
| GitHub runner -> durable evidence | Remote job/report data must be bound to the exact repair commit before it can support closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19.1-20-01 | Tampering | Functional assertions and Playwright disposition | high | mitigate | Tasks 1-2 forbid assertion edits, retries, timeout increases, skips, quarantine, and allowlists; acceptance requires all seven independent cases to execute and pass. |
| T-19.1-20-02 | Spoofing | GitHub run identity | high | mitigate | Task 3 selects event=push, current branch, and exact headSha; requires the completed run and all five jobs to conclude success; and binds each evidence row to the retrieved run ID and SHA. |
| T-19.1-20-03 | Elevation of privilege | Edit route session/role/ownership/notFound sequence | high | mitigate | Product route source is frozen; Task 2 verifies anonymous redirect and authenticated owner arrival without weakening the notFound IDOR boundary. |
| T-19.1-20-04 | Information disclosure | Runner manifest diagnostics | medium | mitigate | Diagnostics are restricted to server mode and route-key presence; environment values, credentials, session data, and database records are not emitted. |
| T-19.1-20-05 | Repudiation | Claimed functional closure | medium | mitigate | The automated verifier reads seven exact non-retry positive case lines from the matched gate-e2e log and requires seven byte-exact evidence rows carrying the same run ID and SHA; marker text alone cannot close the plan. |
| T-19.1-20-06 | Denial of service | CI runtime | low | accept | The existing 90-minute gate-e2e cap remains; the plan adds no retry, sharding, or timeout increase and runs only focused local diagnostics before one authoritative push run. |
| T-19.1-20-SC | Tampering | npm package supply chain | high | mitigate | No package install or dependency change is planned; the existing lockfile/install boundary remains untouched, so no new package-legitimacy checkpoint is applicable. |

</threat_model>

<verification>

1. Confirm Task 1's evidence orders observations as watched-red -> live manifest/server-mode discriminator -> repair -> four-route green, with no product source edit before measurement.
2. Run `npx playwright test e2e/axe-sweep.spec.ts e2e/host-headings.spec.ts e2e/host-route-reachability.spec.ts e2e/keyboard-composites.spec.ts e2e/one-tree.spec.ts e2e/overflow-320.spec.ts --project=chromium --reporter=list`; all owned cases must execute and pass.
3. Run `npm run test:design`, both workflow checker commands, and Task 1's TypeScript non-regression command; TypeScript may retain at most the established seven `tests/design/` errors but may name neither touched Plan 20 TypeScript file.
4. Run Task 3's exact-SHA verifier; it must retrieve a wholly successful push run, require all five jobs including gate-visual to succeed, find exactly one non-retry positive log line for each named owned case, and match the evidence SHA/run ID/case rows byte-for-byte.
5. Review the diff: only the four frontmatter `files_modified` paths may change, and no schema, migration, runtime dependency, workflow, product edit-route, frozen UI, checkout, or visual-baseline file may appear.

</verification>

<success_criteria>

- The four anonymous host route probes return 307 with redirects disabled on the same Playwright launch path used by CI.
- Authenticated owner arrival exposes exactly one wizard rail and the exact Get started control, not a loading, heading-only, or not-found substitute.
- These seven run-34008844133 failures execute and pass: axe sweep at wizard court 320; axe sweep at wizard court 1280; host headings across wizard steps; anonymous edit-route 307; keyboard composites at wizard 320; one-tree wizard; overflow-320 edit court.
- The exact-SHA push run is wholly green: gate-e2e, gate-db-free, gate-db, gate-price-parity, and gate-visual all succeed; failed-owned=0, flaky-owned=0, owned-not-run=0, and the seven exact owned rows are bound to that run's ID and SHA.
- No UI, auth/IDOR, assertion, workflow-job, dependency, schema, credential, checkout, or visual-baseline contract is weakened or broadened.

</success_criteria>

## Artifacts this phase produces

- New test helper symbol: `readHostRouteManifestState` in `e2e/host-route-reachability.spec.ts`.
- New/changed harness flag: `--webpack` in `playwright.config.ts` `webServer.command`, only after the clean discriminator selects it.
- New evidence file: `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt`.
- New evidence file: `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-20.txt`.
- New product API, UI surface, schema object, migration, runtime dependency, environment variable, CI job, or credential: None.

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-20-SUMMARY.md` when done.
</output>
