---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 20
type: execute
wave: 12
depends_on:
  - 19.1-19
files_modified:
  - e2e/host-headings.spec.ts
  - e2e/overflow-320.spec.ts
  - e2e/helpers/booker-seed.ts
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - src/app/actions/listing.ts
  - tests/host/verification-panel.test.tsx
  - src/components/host/verification-panel.tsx
  - scripts/verify-plan-20-triage.mjs
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt
autonomous: true
requirements:
  - CI-01
gap_closure: true
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01 / D-02: both stable wizard-walk failures and the independent verification-panel failure are reproduced, causally diagnosed, and repaired without writing any off as environmental noise."
    - "The whole-space heading walk advances beyond step 3, `Where is it?`, and completes all nine steps without changing the frozen sequence, rail, headings, save contract, or destination."
    - "The 320px court edit walk reaches the photos step and `How your cover appears` while retaining its real checklist entry, seeded photo, overflow, focus, and target assertions."
    - "The verification case finds `Start the check`, passes the typed phone to the action, exposes the existing locked pending control while unresolved, and returns through the existing refusal behavior."
    - "The route diagnostic stays standing evidence: no Webpack/server-mode change occurs, and `readHostRouteManifestState` plus four anonymous one-attempt 307 probes remain active."
    - "Axe at 320/1280, heading hierarchy, the fourteen-stop keyboard route, one served tree, 320px overflow, and route reachability remain six independent active property groups."
    - "D-04: visual ownership remains with Plan 19; no baseline changes occur, and any visual red stops and routes back without interpretation here."
    - "After all repair/code/causal-evidence commits, tracked `HEAD` is clean and locally green; its 40-character SHA is derived only from Git at the later Plan 21 checkpoint, never embedded in tracked Plan 20 content."
    - "Plan 20 completion accounting advances `.planning/STATE.md` `completed_plans` from 62 to 63 exactly once; exact-SHA approval, push, CI evidence, and final closure remain exclusively in Plan 21."
  artifacts:
    - path: "e2e/host-headings.spec.ts"
      provides: "Unchanged nine-step heading contract and any narrowly measured harness repair."
    - path: "e2e/overflow-320.spec.ts"
      provides: "Court edit/photos-step arrival and 320px contract."
    - path: "tests/host/verification-panel.test.tsx"
      provides: "Focused red/green proof for typed-phone pending behavior."
    - path: ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt"
      provides: "Focused, full-order, causal, and post-fix evidence for all three owned reds."
    - path: "scripts/verify-plan-20-triage.mjs"
      provides: "Cross-platform fail-closed validation and deterministic negative fixtures for substantive Plan 20 causal evidence."
  key_links:
    - from: "e2e/host-headings.spec.ts"
      to: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      via: "`walkWizard` observes each save request and heading transition."
    - from: "e2e/overflow-320.spec.ts"
      to: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      via: "`openWizardPhotosStep` enters through the real checklist and proves arrival with the cover heading."
    - from: "tests/host/verification-panel.test.tsx"
      to: "src/components/host/verification-panel.tsx"
      via: "The focused case drives the real form, action mock, React transition, and refusal region."
    - from: "scripts/verify-plan-20-triage.mjs"
      to: ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt"
      via: "Self-tested structural and semantic validation rejects non-substantive or mismatched preflight, probe, cause, and fix-site records."
---

<objective>
Repair what the exact clean-runner evidence measured: causally diagnose the two stable wizard walks and independent verification-panel red, make only the measured repairs, and finish with committed causal evidence plus a clean locally proven `HEAD` for Plan 21 to approve.

Purpose: D-01 requires evidence before repair and D-02 keeps every real red in this phase. The completed route instrumentation falsified the former Webpack/server-mode hypothesis, so state, seed, request, transition, and runner order are measured before a causal edit is selected. Approval, push, and remote CI closure are deliberately deferred to dependent Plan 21.

Output: one structured causal triage record, the narrow measured repairs, local static/unit/design/workflow/E2E proof on committed code, and one 62-to-63 Plan 20 completion increment without recording the post-closeout SHA in tracked content.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-19-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
</context>

<completed_evidence prerequisite="true">
The former Task 1 is completed evidence, not a runnable task here. Instrumentation commit `809f948` and exact push run `34076948628` proved CI uses `npm run dev` with default Turbopack, the live manifest has the exact edit and availability entries, and four anonymous reachability requests returned 307 without retry in 100/183/273/482 ms. Default Turbopack, explicit Webpack, and production build/start all exposed the routes locally. Commit `9f6bb87` records that contradiction. Preserve `e2e/host-route-reachability.spec.ts`, `readHostRouteManifestState`, and `evidence/triage-host-wizard-route.txt`; do not rerun the completed investigation, delete the helper, or change `playwright.config.ts` to select Webpack.

The exact run measured `gate-e2e` at 2 failed, 2 flaky, 386 passed, 52 skipped, and 19 not run. Its stable owned reds were the whole-space heading walk at `Where is it?` and the court edit/photos-step walk before `How your cover appears`. It measured `gate-db` at 1 failed, 2725 passed, and 5 skipped: the typed-phone/pending-control verification test could not find `Start the check`. The other three jobs passed.
</completed_evidence>

<assumption_delta>
The server-mode/route-availability assumption is removed because exact evidence falsified it. Shared state, seed lifecycle, wizard navigation, request ordering, test order, and React transition cleanup remain hypotheses only until Task 1 proves a direction. No product external API integration is introduced; GitHub is evidence transport only. There are no package additions, schema changes, migrations, new jobs, new environment variables, auth-policy changes, or ownership changes.
</assumption_delta>

<ui_spec_contract>
- R1 remains the standing four-route anonymous 307 diagnostic with one attempt per request.
- R2 keeps route order, authentication, ownership, redirect, and `notFound` behavior unchanged.
- R3 keeps the owner arrival rail and existing `Get started` path; no direct-step shortcut replaces real wizard navigation.
- R4 keeps six separate property groups: axe at 320/1280, heading hierarchy, fourteen-stop keyboard navigation, one served tree, 320px overflow, and route reachability.
- E1 keeps empty, loading, error, partial, overflow, and long-text considerations. E2 keeps empty, loading, error, populated, partial, overflow, zero/one/many, and long-text considerations. E3 keeps loading, error, and long-text considerations.
- The wizard retains nine whole-space steps, eight drop-in steps, progress, rail, headings, checklist, forms, save/loading/refusal states, navigation, and one rendered tree. Verification retains its existing idle, pending, refusal, and accessible-region copy/semantics.
- No UI redesign or copy drift, auth-guard change, retry/timeout/quarantine/skip/fixme, allowlist growth, visual-baseline update, checkout switch, dependency/schema/migration/job/environment change, or `deriveBookable` rewrite is permitted. Product source is edited only after Task 1 measures it as the failing layer.
</ui_spec_contract>

## Artifacts this phase produces

- New file `evidence/triage-plan-20-owned-failures.txt`: six structured probe rows (three cases by two probe types), exact commands, first divergent state, directional result, owned file/symbol, causal boundary, selected edit, and post-fix outcomes.
- New file `scripts/verify-plan-20-triage.mjs`: cross-platform schema/content validator with deterministic self-tests proving marker-only, dummy, duplicate, untracked, and mismatched records are rejected.
- New product symbols: none mandated. Reuse existing seams. If measurement earns a file-local harness helper or diagnostic symbol, name it in the triage artifact and summary; do not add a public API speculatively.
- Existing `readHostRouteManifestState`: preserved standing evidence, not newly produced or deleted.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reproduce all three owned reds and locate their causal boundaries</name>
  <files>scripts/verify-plan-20-triage.mjs, .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt</files>
  <precondition>An isolated runner whose loopback port 3000 is namespace-local is available, so Playwright boots its fresh server without touching the user-owned localhost:3000 process; commits 809f948 and 9f6bb87 are ancestors of HEAD.</precondition>
  <read_first>
    - `AGENTS.md` — installed Next 16 docs must be consulted before any later Next source edit.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md` — D-01, D-02, D-03, and D-04.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md` — frozen wizard counts, rail, headings, checklist, form/save states, navigation, one-tree rule, and E1/E2/E3 states.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-wizard-route.txt` — disproven server-mode hypothesis.
    - `e2e/host-headings.spec.ts` — `pressAdvance`, `walkWizard`, save witness, and nine-step case.
    - `e2e/overflow-320.spec.ts` — `openWizardPhotosStep`, `PHASE_14_ROWS`, fixture lifecycle, and court row.
    - `e2e/helpers/booker-seed.ts` — seed/teardown and order-diagnostic patterns; it is not presumed causal.
    - `tests/host/verification-panel.test.tsx` — exact failing case and neighboring lifecycle tests.
    - `src/components/host/verification-panel.tsx` — form, transition, labels, refusal, and action boundary.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` — `persist`, `saveAndContinue`, and step advancement.
    - `src/app/actions/listing.ts` — `saveListingStep` auth, ownership, validation, and persistence outcomes.
  </read_first>
  <behavior>
    - "Each focused selector first has exactly one successful collection row naming exactly one selected test; a missing/renamed test, no-tests-found result, command-launch failure, Playwright webServer boot failure, or absent reporter result is an infrastructure/contract failure rather than a watched red."
    - "All three focused case probes and all three runner-faithful-order case probes execute before repair even when an executed test assertion exits non-zero; each records exact command, exit code, output path/summary, selected identity, and resultKind `expected-red` or `not-reproduced-green`."
    - "Heading RED records step, request path/status or absence, action outcome, before/after heading, listing/form identity, and seed identity at the step-3 stall."
    - "Overflow RED records checklist selection, click result, URL, current step/rail, photo count, heading, and fixture identity when the cover heading is absent."
    - "If either mode is green, its prediction is recorded as falsified and contrasted with the other mode; if focused is green but full order is red, a predecessor-prefix reduction preserves order until the smallest reproducible contaminating prefix is measured."
    - "DB RED distinguishes initial render/copy absence, unresolved-transition lifecycle, mock leakage, cleanup, and assertion timing."
    - "Every substantive JSON string is rejected when `/(?:^|[^A-Za-z0-9_$])(?:todo|tbd|placeholder|unknown|n\/?a|none|xxx|fill[- _]?me|sample|example)(?=$|[^A-Za-z0-9_$])/i` matches anywhere inside it."
  </behavior>
  <action>
Create `scripts/verify-plan-20-triage.mjs` as a dependency-free Node ESM validator with an exported pure record-validation function, a normal evidence-file mode, and a `--self-test` mode. The self-test must construct one valid in-memory three-case/two-probe fixture, then independently mutate it for every forbidden-token position, equal expected/observed values, equal before/after values, invalid or short owned symbols, missing reporter results, untracked/missing files, duplicate/missing/extra cases, and cause/fix file-symbol-boundary mismatches; each mutation must be observed to throw, while the valid fixture must pass. Keep the command-line entry point cross-platform by using Node file/process APIs and `execFileSync('git', ...)`, with no shell syntax.

Create a temporary tracked-tree copy from HEAD inside a runner whose network namespace owns its own loopback port 3000; publish no host port and never inspect, adopt, stop, or signal the user's localhost:3000 process. Start no test server from the shared checkout. Create the triage artifact with HEAD, runner OS, Node/npm/Playwright/Vitest versions, `CI=true`, database identity, fresh-server command, and resolved temporary-copy path. Capture each command's stdout/stderr to a distinct output path and its numeric exit code without chaining watched-red commands through a shell success operator.

Before any probe, collect each focused selector with exactly these commands: `npx playwright test e2e/host-headings.spec.ts --project=chromium --grep "wizard · whole space, every step" --list`; `npx playwright test e2e/overflow-320.spec.ts --project=chromium --grep "/host/listings/\[id\]/edit · photos step · court" --list`; and `npx vitest list tests/host/verification-panel.test.tsx -t "hands the typed phone to the action and locks the control while the press is out" --json`. Write exactly three `PREFLIGHT_JSON=` records keyed by case with `case`, `exactCommand`, `exitCode`, `outputPath`, `outputSummary`, `selectedTestIdentity`, and `collectedCount`. Require exit code zero, exactly one collected test, and the expected file plus full title in the emitted identity. Stop the task before any repair if collection fails, a selector is absent/renamed/ambiguous, or no parseable collection record exists.

After all preflights pass, run every watched probe even when an executed assertion is red: `npx playwright test e2e/host-headings.spec.ts --project=chromium --grep "wizard · whole space, every step"`; `npx playwright test e2e/overflow-320.spec.ts --project=chromium --grep "/host/listings/\[id\]/edit · photos step · court"`; `npx vitest run tests/host/verification-panel.test.tsx -t "hands the typed phone to the action and locks the control while the press is out" --reporter=verbose`; the runner-faithful `npx playwright test --project=chromium` whose one reporter output supplies separate heading and overflow case rows; and the runner-faithful `npm test` for the verification case. A non-zero exit caused by a reporter-confirmed assertion failure is diagnostic data and does not prevent later watched probes. Stop immediately, before repair, only for an unexpected launch/collection failure, Playwright webServer boot failure, no-tests-found result, crash without the selected test executing, or absent/unparseable reporter result. Temporary logs may exist only in the isolated tracked-tree copy and must be removed after their required paths and summaries are captured.

Write exactly six `PROBE_JSON=` NDJSON records: the Cartesian product of cases `host-heading-whole-space`, `overflow-edit-photos-court`, and `verification-panel-phone-pending` with probes `focused` and `runner-faithful-order`. Every record must contain `case`, `probe`, `selectedTestIdentity`, `testExecuted: true`, numeric `exitCode`, `outputPath`, `outputSummary`, `resultKind`, `firstDivergentState`, `exactCommand`, `directionalResult`, `ownedFile`, `ownedSymbol`, and a structured `reporterResult` with target status and execution/attempt counts. Set `resultKind=expected-red` only when the reporter proves the selected target had an assertion failure, including a retry that exposes the watched red; set `resultKind=not-reproduced-green` only when the reporter proves it executed without an assertion failure. A green in one mode records `prediction=falsified` and becomes contrast evidence; it is never suppressed or omitted. If both modes are green for a case, retain both rows and stop before repair because no causal boundary has been measured.

Parse `firstDivergentState` strictly as `expected=<observation>; observed=<observation>` and `directionalResult` strictly as `prediction=(confirmed|falsified); before=<observation>; after=<observation>`. Require each observation to be at least 12 trimmed characters, expected and observed to differ after whitespace/case normalization, and before and after to differ the same way. Apply the forbidden-token expression from `<behavior>` recursively to every string in all structured records. Require every preflight/probe `exactCommand` to equal its case/probe command named above, each `outputPath` to be non-empty, each `outputSummary` to contain at least 24 trimmed characters, and each probe identity to equal the corresponding successful preflight identity. Require `ownedFile` to exist and pass `git ls-files --error-unmatch -- <ownedFile>`. Require `ownedSymbol` to be at least six characters and match a JS/TS dotted identifier (`segment` or `segment.segment`) rather than a dummy value.

Write exactly three `CAUSE_JSON=` and three `FIX_SITE_JSON=` records, one of each per case and no extra/duplicate case. Each carries `case`, `ownedFile`, `ownedSymbol`, and `observedBoundary`; CAUSE adds `mechanism`, FIX_SITE adds `rationale`. Independently require every cause/fix file to exist and be Git-tracked, the file/symbol pair to equal at least one probe row for that case, `observedBoundary` to equal the parsed observed side of at least one of that case's probe rows, mechanism/rationale to be at least 48 trimmed characters, unique across cases, and to name both the owned symbol and exact observed boundary. A shared-state, seed, navigation, request-order, transition-cleanup, or harness cause is accepted only when a directional probe changes the failure as predicted. Do not begin Task 2/3 repair or any green gate until all three preflights, all six probes, and all six cause/fix records pass the validator and every case has a measured in-scope fix site. Preserve all six property groups per D-01/D-02.
  </action>
  <verify>
    <automated>node scripts/verify-plan-20-triage.mjs --self-test .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt</automated>
  </verify>
  <fails_when>An infrastructure/collection/launch/server/reporter failure does not stop immediately, an ordinary reporter-confirmed assertion red prevents a later watched probe, the three-preflight or six-probe matrix is incomplete/duplicated, the substantive self-tests do not observe rejection, a case lacks a measured causal direction/fix site, localhost:3000 is touched, Webpack is proposed, or the DB red is dismissed as unrelated.</fails_when>
  <acceptance_criteria>
    - Exactly three successful selector preflights and three cases by two probe types exist; all six watched case probes run before repair even when an executed assertion exits non-zero.
    - Each row carries the verbatim command, exit code, output path/summary, selected test identity, reporter-confirmed result kind, first divergent state, directional result, and tracked existing owned file with a meaningful owned symbol.
    - Exactly three CAUSE_JSON and three FIX_SITE_JSON records correlate one-to-one with the measured probe boundary and owned file/symbol; embedded forbidden tokens, dummy values, duplicates, omissions, and mismatches fail deterministic self-tests.
    - Each fix site is justified by an observable state, request/action, fixture, predecessor, or React-lifecycle difference; a green mode records falsification/contrast rather than disappearing.
    - Config, standing route evidence, auth/ownership, assertions, retries, timeouts, skips, allowlists, and baselines are untouched.
  </acceptance_criteria>
  <done>Each owned red has a measured causal boundary sufficient for a narrow repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the measured wizard-walk causes without changing the UI contract</name>
  <files>e2e/host-headings.spec.ts, e2e/overflow-320.spec.ts, e2e/helpers/booker-seed.ts, src/app/(host)/host/listings/[id]/edit/wizard.tsx, src/app/actions/listing.ts</files>
  <precondition>Task 1 names reproducible E2E fix sites within this task's owned paths.</precondition>
  <read_first>
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt` — measured causes only.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md` — R2/R3/R4, E1/E2/E3, and frozen UI contract.
    - `e2e/host-headings.spec.ts` — preserve heading-size and one-h1 assertions.
    - `e2e/overflow-320.spec.ts` — preserve real checklist, cover heading, overflow, focus, and target assertions.
    - `e2e/helpers/booker-seed.ts` — use only if Task 1 measured this seam.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` — edit only for a measured product defect and after the AGENTS.md docs gate.
    - `src/app/actions/listing.ts` — edit only for a measured action defect; keep auth/ownership invariant.
  </read_first>
  <behavior>
    - "The nine-step whole-space walk issues one observed save per advance, leaves `Where is it?`, visits every frozen heading once, and reaches the established destination."
    - "The court row enters through the real checklist, retains its seeded photo, reaches `How your cover appears`, and executes the same 320px assertions."
    - "Focused and full-order repetitions have zero failed/flaky/not-run owned E2E cases and all six property groups remain active."
  </behavior>
  <action>
Retain or add a focused assertion exposing Task 1's cause, run it RED, then repair only that measured seam: product code for a real save/navigation defect or harness seed/lifecycle/navigation code for measured contamination. Although five files are owned as possible sites, edit only the smallest evidenced subset. Preserve nine whole-space steps, eight drop-in steps, progress, rail, headings/copy, checklist, form states, save behavior, destination, one tree, and loading/empty/error/partial/overflow/long-text states.

Never add retries, timeout increases, skips/fixme/quarantine, allowlist entries, weaker assertions, direct step URLs, persistence bypasses, baseline updates, auth/ownership weakening, alternate copy, or redesign. Do not edit `playwright.config.ts`. Run both focused cases, the six property-group files together, and the full chromium suite in Task 1's isolated runner/order; stop immediately on the first non-zero command and do not run a later command to mask it. Append exact failed/flaky/not-run owned counts. A visual red stops and returns to Plan 19 per D-04 without baseline interpretation.
  </action>
  <verify>
    <automated>npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --project=chromium --grep "wizard · whole space, every step|/host/listings/\[id\]/edit · photos step · court" &amp;&amp; npx playwright test e2e/axe-sweep.spec.ts e2e/host-headings.spec.ts e2e/keyboard-composites.spec.ts e2e/one-tree.spec.ts e2e/overflow-320.spec.ts e2e/host-route-reachability.spec.ts --project=chromium &amp;&amp; npx playwright test --project=chromium</automated>
  </verify>
  <fails_when>Any verification command fails without an immediate stop, an owned walk fails/flakes/is not run, a property group is omitted, full order disagrees with focused green, UI/copy/state drifts, suppression appears, or visual becomes red.</fails_when>
  <acceptance_criteria>
    - Both exact E2E cases are green focused and in full order with failed=0, flaky=0, and not-run=0.
    - All six independent groups execute with existing assertions.
    - Every changed line maps to Task 1's cause and no prohibited config/auth/UI/baseline/gate change exists.
  </acceptance_criteria>
  <done>The measured E2E causes are repaired without weakening gates or changing the frozen experience.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repair the verification-panel cause and finish one clean locally proven repair head</name>
  <files>tests/host/verification-panel.test.tsx, src/components/host/verification-panel.tsx, .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt</files>
  <precondition>Task 1 names a reproducible DB fix site within this task's paths and Task 2 is full-order green.</precondition>
  <read_first>
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt` — exact RED and measured boundary.
    - `tests/host/verification-panel.test.tsx` — typed-phone case, neighboring states, mock reset, promise release, and cleanup.
    - `src/components/host/verification-panel.tsx` — form, transition, labels, refusal region, and action call.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md` — verification UI/copy remains unchanged.
    - `.github/workflows/ci.yml` — exact five job names and commands.
    - `scripts/verify-workflows.mjs` — workflow topology guards.
  </read_first>
  <behavior>
    - "Focused RED reproduces the absent idle control or measured predecessor/lifecycle fault."
    - "GREEN renders existing `Start the check`, calls once with typed phone, and exposes the existing disabled pending control while unresolved."
    - "Refusal returns through the existing named region and neighboring states remain green."
    - "After all repair and causal-evidence commits, clean `HEAD` has green lint, design, Vitest, workflow, focused E2E, six-group, and full-order evidence without a tracked self-reference to its own SHA."
  </behavior>
  <action>
Preserve the exact focused RED before repair. Repair a measured mock/promise/cleanup boundary while still exercising the real form, or repair `VerificationForm` after the AGENTS.md installed-doc gate if the product transition/render boundary is causal. Keep idle and pending labels, refusal copy, named region, form semantics, and action contract. Do not call the action directly, weaken role assertions, delete pending proof, or wait longer.

Run the exact case, whole file, and `npm test`; then lint, design tests, workflow guards, Task 2's focused cases, six groups, and full chromium order in the isolated runner. Stop immediately on the first non-zero command and do not continue to a later gate. Append the exact commands, directional RED/GREEN evidence, six property-group rows, and failed/flaky/not-run counts to the tracked triage artifact, then commit the narrow repair plus causal/local-proof evidence atomically. After that commit, require `git status --porcelain=v1 --untracked-files=no` to be empty and print `git rev-parse HEAD` only to the executor/checkpoint transcript. Do not append, amend, or otherwise store that post-commit SHA in the triage artifact, summary, state, roadmap, or any other tracked content, because a commit cannot contain its own identity. Do not push. Plan 21 will derive the approvable candidate afresh from clean `HEAD` after Plan 20's normal closeout commit.
  </action>
  <verify>
    <automated>npx vitest run tests/host/verification-panel.test.tsx -t "hands the typed phone to the action and locks the control while the press is out" --reporter=verbose &amp;&amp; npx vitest run tests/host/verification-panel.test.tsx --reporter=verbose &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run test:design &amp;&amp; node scripts/verify-workflows.mjs &amp;&amp; npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --project=chromium --grep "wizard · whole space, every step|/host/listings/\[id\]/edit · photos step · court" &amp;&amp; npx playwright test e2e/axe-sweep.spec.ts e2e/host-headings.spec.ts e2e/keyboard-composites.spec.ts e2e/one-tree.spec.ts e2e/overflow-320.spec.ts e2e/host-route-reachability.spec.ts --project=chromium &amp;&amp; npx playwright test --project=chromium &amp;&amp; node -e "const{execFileSync}=require('child_process');const d=execFileSync('git',['status','--porcelain=v1','--untracked-files=no'],{encoding:'utf8'}).trim();if(d)throw Error('tracked tree is dirty');const h=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();if(!/^[0-9a-f]{40}$/.test(h))throw Error('invalid clean HEAD');console.log('clean local repair HEAD '+h)"</automated>
  </verify>
  <fails_when>Any command fails without an immediate stop, the case is absent or green only after weakening, the full file/Vitest is red, lifecycle state leaks, UI semantics drift, a local gate is red, tracked changes remain after the repair/evidence commit, a post-commit SHA is written into tracked content, or a push occurs.</fails_when>
  <acceptance_criteria>
    - Focused verification RED and GREEN are recorded at one measured cause, and all Vitest tests pass.
    - Existing accessible idle/pending/refusal behavior is tested through the real form.
    - Local static, unit, design, workflow, focused E2E, six-group, and full-order checks are green.
    - All repair/code/causal-evidence changes are committed, the tracked tree is clean, and `git rev-parse HEAD` prints a 40-character unpushed candidate only outside tracked content.
  </acceptance_criteria>
  <done>All three failures are repaired and locally proven on one clean committed head; no tracked file claims to contain that commit's SHA.</done>
</task>

</tasks>

<threat_model>
Security enforcement: OWASP ASVS Level 1. Any unresolved high-severity threat blocks closure.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Runner to wizard/server action | Test form state, cookies, listing IDs, and navigation cross into authenticated actions. |
| Vitest DOM to verification action mock | Synthetic interaction and deferred promises cross the client/action seam. |
| Repair checkout to tracked causal evidence | Diagnostic output becomes committed evidence only after secrets and user-owned process data are excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19.1-20-01 | Tampering | E2E/Vitest harness | high | mitigate | Block suppression, weaker assertions, allowlist/baseline growth, and direct-action bypasses; require RED/GREEN and full order. |
| T-19.1-20-02 | Elevation of Privilege | `saveListingStep` and wizard | high | mitigate | Freeze auth/ownership and accept product edits only at a measured causal symbol. |
| T-19.1-20-03 | Repudiation | Diagnosis/census | medium | mitigate | Require three selector preflights, six reporter-confirmed probe rows, one-to-one causal/fix records, and deterministic semantic-validator rejection tests. |
| T-19.1-20-05 | Information Disclosure | Diagnostics | medium | mitigate | Redact cookies, credentials, phones, database URLs, and secret headers. |
| T-19.1-20-06 | Denial of Service | Full probes | low | accept | Bounded full-order probes run in isolation without retry or timeout inflation. |
| T-19.1-20-SC | Tampering | Package surface | high | mitigate | No package install or dependency change is allowed. |
</threat_model>

<verification>
- `git ls-files --error-unmatch` succeeds for every referenced existing project path; only the new Plan 20 triage evidence and its new validator script are absent before execution.
- Standing route evidence remains and no diff touches `playwright.config.ts` or deletes `readHostRouteManifestState`.
- Task 1 supplies exactly three cases by two probe types, with a substantive first divergence, verbatim command, directional result, and owned file/symbol in every row.
- Tasks 2/3 supply focused/full-order greens with six groups active and no suppression or UI/auth/visual drift.
- After the repair/evidence commit, the tracked tree is clean and the SHA is printed only outside tracked content; Plan 20 performs no approval, push, or remote-run selection.
- Normal Plan 20 closeout creates its summary and advances `completed_plans` from 62 to 63 exactly once; Plan 21 owns the next increment.
</verification>

<success_criteria>
1. The whole-space walk completes unchanged nine-step navigation beyond `Where is it?`.
2. The court edit row reaches unchanged `How your cover appears` and retains 320px assertions.
3. The verification case finds the existing idle control, passes the typed phone once, and proves pending lock.
4. No config/Webpack, suppression, assertion, baseline, auth/ownership, redesign, or copy-drift workaround enters.
5. Local static, unit, design, workflow, focused E2E, six-group, and full-order commands are green and stop immediately on any red.
6. D-04 stays with Plan 19; Plan 20 performs no push and completion advances 62 to 63 exactly.
</success_criteria>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | CI run means something; diagnosis/repair/local-proof half of SC4 closure | Tasks 1-3; dependent Plan 21 owns exact-SHA CI closure |
| REQ | CI-01 functional Playwright specs are enforced | Tasks 2-3 preserve six groups and full order |
| RESEARCH | Step-3 stall is unverified until request/save/navigation are measured | Tasks 1-2 |
| RESEARCH | Runner-order contamination cannot be inferred from isolated behavior | Task 1 full-order and prefix probe |
| CONTEXT | D-01 triage first and repair real cause | Tasks 1-3 |
| CONTEXT | D-02 all real bugs stay in phase | All three cases remain owned through local proof and dependent Plan 21 |
| CONTEXT | D-03 guarded allowlist | Tasks 2-3 prohibit growth and preserve assertions |
| CONTEXT | D-04 visual is SC4 and regressions return to Plan 19 | Tasks 2-3 |
| CONTEXT | D-05 through D-08 branch/public decisions | Preserved, not reopened; GitHub is transport only |
| CONTEXT | Deferred sharding/unrelated product work | Excluded |
| UI-SPEC | R1-R4, E1-E3, frozen wizard/UI/copy, six groups | Prerequisite and Tasks 1-3 |
</source_coverage_audit>

<output>
After Task 3, create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-20-SUMMARY.md`. Name all three measured causes/fix sites, six structured probe rows, local green commands/censuses, six property groups, D-04 disposition, changed files, absence of any push, and the exact 62-to-63 transition. Do not write the post-closeout `HEAD` SHA into the summary or any other tracked file; dependent Plan 21 derives it fresh from Git.
</output>
