---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 04
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified:
  - src/components/availability/availability-calendar.tsx
  - src/components/availability/date-pass-picker.tsx
  - tests/design/availability-tz-note-id.test.ts
  - e2e/collision-in-place.spec.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 / D-02 / CI-01 SC4 — a real product bug found by triage is REPAIRED, not written off. Two components declare the same literal DOM id and the comment beside one of them asserts the collision is impossible; the CI run falsifies that comment. The responsive booking sheet mounts a second booking view on the listing route, so while that sheet is open the document holds two elements carrying one id. D-02 says the phase grows rather than the allowlist, and this is the growth."
    - "CI-01 SC4 — the id becomes unique per component INSTANCE, identically in both files, and the accessibility wiring moves with it. The paragraph carrying the timezone note and the control that references it through its describedby attribute stay bound to each other inside each instance; a fix that made the id unique while leaving a stale describedby target would trade a duplicate-id defect for a broken screen-reader association."
    - "CI-01 SC4 — the comment that the CI run falsified is replaced by the measurement that falsified it. The claim that exactly one of the two surfaces renders per listing is deleted and replaced with a sentence naming the responsive sheet as the mechanism by which both mount in one document, citing the helper that records it."
    - "CI-01 SC4 — the repair carries a standing assertion so the class cannot come back. A new design test performs a static census over the two component sources and is RED if either declares a module-level string literal that is used directly as a DOM id attribute; the census proves it scanned something before it reports a clean result, so a broken walk cannot report a clean tree."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — the new census cannot pass over nothing. It asserts a non-zero count of describedby-bound id sites across the two files before asserting anything about their shape; a zero there means the scan or the pattern stopped working, not that the tree is clean."
    - "CI-01 encoding (spec-less edge probe, verification: backstop) — { statement: 'the census strips comments before matching, and asserts that stripping was load-bearing by comparing the stripped and unstripped hit counts, because a docblock discussing the id constant would otherwise be read as a declaration of it', verification: backstop }"
    - "CI-01 SC4 — the second, distinct defect the same spec may carry is triaged rather than absorbed. The court variant of `e2e/collision-in-place.spec.ts:240` was recorded flaky with a DIFFERENT message — the notice on screen while the grid beneath it still offers the lost hours — so the spec is triaged twice, and if the second defect survives the id repair it gets its own recorded verdict rather than being swept into the first."
  artifacts:
    - src/components/availability/availability-calendar.tsx
    - src/components/availability/date-pass-picker.tsx
    - tests/design/availability-tz-note-id.test.ts
    - e2e/collision-in-place.spec.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt
  key_links:
    - "`src/components/availability/availability-calendar.tsx` and `src/components/availability/date-pass-picker.tsx` to each other. The two files are each other's analog and declare the same id constant identically; the repair is applied identically in both or the defect simply moves."
    - "the timezone-note paragraph to the control that references it by describedby, inside each component instance. This is the link that must not break while the id is being made unique."
    - "the responsive booking sheet on the listing route to both components being mounted in one document. `e2e/helpers/booker-seed.ts:86-88` records the mechanism, and it is the mechanism the falsified comment did not know about."
    - "the same timezone-note locator to `pickWindow` in `e2e/helpers/booker-seed.ts:175-185`, which asserts on it. Plan 08 hardens that helper; the strict-mode ambiguity this plan removes is what would otherwise make that assertion nondeterministic."
  prohibitions:
    - "The accessibility association must never be left pointing at an id that no longer exists. Making an id unique without moving the reference that targets it converts a validity defect into a screen-reader defect, which is strictly worse because nothing observes it."
    - "A comment a measurement has falsified must never be left standing beside the code it describes. A description that outlives what it describes is the defect class this repository keeps recording, and here the description is the reason the bug shipped."
    - "The two components must never diverge in how they derive the id. Two spellings of one convention in one directory is a second source of truth wearing the costume of a convention."
    - "A static census must never report a clean result without proving it scanned something. A zero-hit scan and a clean tree are indistinguishable outputs, and only the non-vacuity guard separates them."
    - "Neither variant of this spec may be recorded in the known-failures allowlist. The grove variant is a real product bug with a known repair, and the court variant's verdict is recorded before any eligibility question is asked."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `empty` explicitly (the census non-vacuity guard) and `encoding` as a backstop marker (comment-stripping and its load-bearing proof). `adjacency` and `ordering` are owned by plans 01, 03, 06 and 11; none is dropped."
    - "A9 (RESEARCH.md) — a second duplicate-id instance is documented at `src/lib/design/visual-baselines.ts:505` for a different id. Whether that one is still live is NOT settled by this plan and it is not repaired here; it is named in the SUMMARY as a carried observation so the class is not claimed closed more broadly than it is."
    - "The court variant's second message may be a missing settle rather than a product defect. Task 3 records whichever it is with the transcript it is read from; if it is a product defect the phase grows per D-02 and it gets its own plan rather than being folded in here."
---

<objective>
Repair a real product bug the CI run found: `src/components/availability/availability-calendar.tsx` and
`src/components/availability/date-pass-picker.tsx` both declare the DOM id `availability-tz-note` as a
module-level literal, and the responsive booking sheet mounts both surfaces in one document — so the id
appears twice, a Playwright strict-mode locator resolves to two elements, and the accessibility
association is ambiguous.

Purpose: this is one of the two failures RESEARCH.md classifies as a genuine product defect rather than
drift or an environment gap. D-01 forbids writing it off and D-02 says the phase grows to fix it.

Output: a per-instance id in both components, the accessibility wiring moved with it, the falsified
comment replaced by the measurement, a static census that keeps the class closed, and a triage transcript
covering BOTH of the spec's possible defects.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce both of collision-in-place's defects and record two verdicts</name>

  <precondition>
    Docker Desktop is running with `fitout-db-1` up, the database is migrated and seeded, the build cache
    directory has been removed, and TCP port 3000 is free. Plan 02's `evidence/triage-harness.md` holds the
    sequence.
  </precondition>

  <read_first>
    - `e2e/collision-in-place.spec.ts` around `:240` — the test, its court and grove variants, and the
      locator that resolved to two elements in CI.
    - `e2e/helpers/booker-seed.ts:86-88` — the note recording that the responsive booking sheet mounts a
      SECOND booking view on the listing route, so the document holds two month grids while it is open.
    - `src/components/availability/availability-calendar.tsx:470`, `:620`, `:625` — the id constant, the
      paragraph that carries it, and the describedby reference.
    - `src/components/availability/date-pass-picker.tsx:71-73`, `:241`, `:246` — the same three sites plus
      the comment asserting the collision is impossible.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Cause D — real product bugs", the D1 subsection with
      the verbatim strict-mode violation and the note that the court variant carried a different message.
    - `.planning/phases/19.1-.../evidence/triage-harness.md` — plan 02's bring-up sequence.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt
  </files>

  <action>
Run the plan 02 bring-up sequence, then run the spec unmodified with CI's retry shape and capture the
command line and unedited transcript into `evidence/triage-collision-in-place.txt` under a PRE-REPAIR
heading:

`npx playwright test e2e/collision-in-place.spec.ts --project=chromium --retries=2 --reporter=list`

Two distinct outcomes are being looked for and both must be recorded separately:

DEFECT 1 — the grove variant's strict-mode violation, in which the timezone-note locator resolves to two
elements. Quote the two resolved elements from the transcript and confirm both carry the same id
attribute. Then confirm, by reading the two component sources, that the id is declared as the same literal
in both files and that one of them carries a comment asserting only one surface can render per listing.
Record that the comment is falsified and name the mechanism from the seed helper that falsifies it.

DEFECT 2 — the court variant. Look for the second message recorded in Phase 19's wall-clock evidence: the
cancellation notice on screen while the grid beneath it still offers the hours the booker just lost. If it
appears, record it as a SEPARATE finding with its own transcript excerpt. If the court variant passes,
record that too — an absence measured is a finding; an absence assumed is not.

Close with two lines beginning `VERDICT`, one per defect, each naming the transcript lines it is read
from. Do not edit any source file in this task.
  </action>

  <acceptance_criteria>
    - `evidence/triage-collision-in-place.txt` contains a command line and an unedited transcript.
    - The transcript contains a strict-mode violation naming two resolved elements.
    - The file contains exactly two lines beginning `VERDICT`, one for the duplicate-id defect and one for
      the court variant's stale-grid behaviour, each quoting transcript text.
    - `git status --porcelain -- src/components/availability e2e/collision-in-place.spec.ts` produces no
      output.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^VERDICT' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt</automated>
    <fails_when>output is any number other than 2 — the spec was triaged once rather than twice, and RESEARCH.md records that it may carry two distinct defects</fails_when>
    <automated>grep -ci 'strict mode violation' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt</automated>
    <fails_when>output is 0 — the duplicate-id failure was not reproduced, so the repair that follows would be aimed at a defect nobody measured on this machine</fails_when>
    <automated>git status --porcelain -- src/components/availability e2e/collision-in-place.spec.ts</automated>
    <fails_when>any output — a source file was edited during the reproduction task</fails_when>
  </verify>

  <done>
    Both possible defects in this spec have a recorded verdict backed by transcript text, and the falsified
    comment is identified by the mechanism that falsifies it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the timezone-note id unique per instance in both components, and pin it</name>

  <read_first>
    - `src/components/availability/availability-calendar.tsx:460-480` and `:610-630` — the id constant, the
      paragraph, and the describedby reference in context.
    - `src/components/availability/date-pass-picker.tsx:65-80` and `:235-250` — the same three sites plus
      the comment to be replaced.
    - `tests/design/listing-reuse-predicate-census.test.ts:104-107`, `:144-165`, `:170-178`, `:300-306`,
      `:333` — the file-read helper, the comment-stripping block with its load-bearing proof, the recursive
      source walk, the non-vacuity guard and the pinned-count assertion. This is the census analog.
    - `tests/helpers/source-text.ts:57` — `stripComments`, the shared helper the census must use rather
      than rolling its own.
    - `tests/design/workflow-invariants.test.ts:63-70` — the rule that a new file in `tests/design/` is
      collected automatically and that editing the design vitest config is not permitted.
    - `.planning/phases/19.1-.../19.1-PATTERNS.md` §5 — the repair pattern and the precedent for the defect
      class.
  </read_first>

  <behavior>
    - Two booking surfaces mounted in one document produce two DIFFERENT ids for their timezone notes.
    - Inside each instance the describedby attribute of the control targets that instance's own note id.
    - Rendering a single surface alone still associates the control with its note — the repair is not
      conditional on the sheet being open.
    - The new census is RED if either component reintroduces a module-level string literal used directly as
      a DOM id attribute.
    - The census is RED if its own scan finds zero describedby-bound id sites across the two files.
  </behavior>

  <files>
    src/components/availability/availability-calendar.tsx,
    src/components/availability/date-pass-picker.tsx,
    tests/design/availability-tz-note-id.test.ts
  </files>

  <action>
In BOTH components, replace the module-level id literal with a per-instance derivation: generate a stable
unique identifier inside the component with React's id hook and compose the note id from it, or accept an
explicit id-prefix prop supplied by the mount site. Choose ONE mechanism and apply it identically in both
files — two spellings of one convention in one directory is the drift class this repository keeps paying
for. Move the describedby reference to the derived value in the same edit, at both sites, so the paragraph
and the control that describes it stay bound inside each instance.

Delete the comment in `date-pass-picker.tsx` asserting that exactly one of the two surfaces renders per
listing and that the id can therefore never appear twice. Replace it with a sentence naming the
measurement that falsified it: the responsive booking sheet mounts a second booking view on the listing
route, so both surfaces are in one document while that sheet is open, and the id is derived per instance
for that reason. Cite `e2e/helpers/booker-seed.ts:86-88` as the record of the mechanism.

Create `tests/design/availability-tz-note-id.test.ts` — a static census over the two component sources,
modelled on `tests/design/listing-reuse-predicate-census.test.ts`. It must:

1. Read both files through a shared read helper and strip comments with `stripComments` from
   `tests/helpers/source-text.ts`.
2. Assert a non-zero count of sites where an id attribute is bound to a describedby reference across the
   two files, with a failure message stating that a zero here means the walk or the pattern stopped
   working and that this property is passing over nothing.
3. Assert that neither file declares a module-level string-literal constant that is used directly as the
   value of an id attribute — the shape that produced the duplicate. The failure message must name both
   files, state what would have to be true, and state the correct response (derive per instance, do not
   rename the literal).
4. Carry a case proving the comment-stripping is load-bearing rather than decorative, by comparing the
   stripped and unstripped hit counts and failing if stripping removed nothing.
5. Carry a header docblock in the lettered watched-red convention used by `tests/design/e2e-email-silence.test.ts:33-51`,
   listing the mutations that drove it red.

Do not edit `vitest.design.config.ts`; a new file in that directory is collected automatically and adding
a setup or global-setup key is what would reintroduce the database dependency that config exists to
exclude.
  </action>

  <acceptance_criteria>
    - Neither component file contains a module-level constant whose string value is used directly as an id
      attribute; both derive the note id per instance using the same mechanism.
    - Both files bind the describedby reference to the derived value, and the count of describedby bindings
      in each file is unchanged from HEAD.
    - The comment in `date-pass-picker.tsx` asserting single-surface rendering is gone and is replaced by a
      sentence naming the responsive sheet and citing the seed helper.
    - `tests/design/availability-tz-note-id.test.ts` exists, is collected by the design config without any
      config edit, and passes.
    - The census contains a non-vacuity assertion whose failure message states that a zero means the scan
      stopped working.
    - `git diff -- vitest.design.config.ts` is empty.
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/availability-tz-note-id.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reporting 0 tests collected, or any test named for the comment-stripping proof appearing under a failed heading</fails_when>
    <automated>git diff --stat -- vitest.design.config.ts</automated>
    <fails_when>any output — the design vitest config was edited, which this plan and the design suite's own header forbid</fails_when>
    <automated>npm run test:design</automated>
    <fails_when>non-zero exit, or any file reported as failed in the vitest summary</fails_when>
  </verify>

  <done>
    Two mounted booking surfaces produce two different timezone-note ids, each control describes its own
    note, the falsified comment is replaced by the measurement, and a build-blocking census keeps the shape
    from coming back.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run both variants and settle the court variant's separate verdict</name>

  <read_first>
    - `evidence/triage-collision-in-place.txt` — Task 1's two verdicts, which this task closes or carries.
    - `e2e/collision-in-place.spec.ts` around `:240` — the assertions in both variants.
    - `e2e/helpers/booker-seed.ts:118-160` — `openBookingSheet`'s retry-with-post-condition pattern, which
      is the established shape if the court variant turns out to need a settle rather than a product fix.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Cause D1", the paragraph flagging the court variant's
      different message and instructing that the spec be triaged twice.
  </read_first>

  <files>
    e2e/collision-in-place.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt
  </files>

  <action>
Re-run the spec with CI's retry shape and append the POST-REPAIR transcript to
`evidence/triage-collision-in-place.txt`. The grove variant must now pass and the strict-mode violation
must be absent.

Then close the court variant's verdict with evidence:

- If it passes on all attempts, record that with the transcript and state that the stale-grid message did
  not reproduce after the id repair, naming the run count it was observed over.
- If the stale-grid message appears, decide between the two readings on evidence, not on convenience: a
  missing settle in the spec (the assertion measured the grid before the server round trip landed) versus
  a product defect (the grid genuinely keeps offering hours the booker lost). Read the trace to decide. If
  it is a missing settle, add a post-condition in the established shape — assert the observable state the
  next step depends on before proceeding, with a docblock stating that the wait is a measured requirement
  and not a hedge. If it is a product defect, DO NOT repair it here: record the verdict, name the component
  and the state that is stale, and state in the SUMMARY that the phase grows by one plan per D-02.

Update both `VERDICT` lines in the evidence file to their final state, each quoting the transcript text it
rests on. If a post-condition was added, capture a red-then-green pair for it: remove the post-condition,
show the original timeout-shaped failure, restore it, show the named failure or the pass, and record the
checksum and porcelain revert proof.
  </action>

  <acceptance_criteria>
    - `evidence/triage-collision-in-place.txt` contains a post-repair transcript in which no strict-mode
      violation appears.
    - Both `VERDICT` lines are updated to a final state and each quotes transcript text.
    - If a post-condition was added to `e2e/collision-in-place.spec.ts`, the file contains a docblock
      stating the wait is a measured requirement, and the evidence file carries the red-then-green pair.
    - If the court variant's second defect is a product defect, the evidence file names the component and
      the stale state, and no product file outside `src/components/availability/` is modified by this plan.
    - `npx playwright test e2e/collision-in-place.spec.ts --project=chromium` reports zero failures, OR the
      evidence file records a product-defect verdict with the failing transcript and the SUMMARY states the
      phase grows.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/collision-in-place.spec.ts --project=chromium --retries=2 --reporter=list</automated>
    <fails_when>the summary block reports a non-zero "failed" count for the grove variant, or any line containing "strict mode violation" appears in the output</fails_when>
    <automated>grep -ci 'strict mode violation' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-collision-in-place.txt</automated>
    <fails_when>output is 0 — the pre-repair transcript that proves the defect existed is missing, so the post-repair pass proves nothing</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>

  <done>
    The grove variant passes with no strict-mode violation, and the court variant's separate behaviour has
    a final recorded verdict backed by a transcript rather than an assumption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a rendered document → assistive technology | A duplicated id makes the describedby association ambiguous; the browser resolves it, the user does not choose. |
| a component's stated invariant (in a comment) → the routes that mount it | The comment asserted an invariant the routing layer does not enforce. |
| the two availability components → each other | They are twins; a fix applied to one and not the other moves the defect rather than closing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-14 | Tampering | duplicate DOM id across two mounted availability surfaces | high | mitigate | Per-instance id derivation applied identically in both components, with the describedby reference moved in the same edit; the new census is build-blocking. |
| T-19.1-15 | Repudiation | the comment asserting the collision is impossible | high | mitigate | The falsified comment is deleted and replaced by the measurement and the mechanism, citing the helper that records it. A stale claim beside the code is how this shipped. |
| T-19.1-16 | Tampering | the accessibility association during the repair | high | mitigate | The behaviour block requires the control to describe its own instance's note; the census asserts the describedby binding count is non-zero and unchanged. |
| T-19.1-17 | Tampering | `vitest.design.config.ts` | medium | mitigate | The plan forbids editing it and the verify asserts an empty diff; adding a setup key would reintroduce the database dependency the design suite exists to exclude. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New test file:** `tests/design/availability-tz-note-id.test.ts` — a static census over the two
availability components, with a non-vacuity guard, a comment-stripping load-bearing proof, and a lettered
watched-red header docblock.

**Changed symbols:** the timezone-note id in `src/components/availability/availability-calendar.tsx` and
`src/components/availability/date-pass-picker.tsx` changes from a module-level literal to a per-instance
derived value; the describedby reference at both sites moves with it. The falsified comment in
`date-pass-picker.tsx` is replaced.

**Possibly changed:** a post-condition helper call inside `e2e/collision-in-place.spec.ts`, only if Task 3
measures a missing settle.

**New evidence file:** `evidence/triage-collision-in-place.txt` with two `VERDICT` lines.

**New npm scripts:** none.
</artifacts_this_phase_produces>

<verification>
- `npm run test:design` passes with the new census file collected.
- `npx playwright test e2e/collision-in-place.spec.ts --project=chromium` reports zero failures for the
  grove variant and a recorded verdict for the court variant.
- `npx tsc --noEmit` and `npm run lint` exit 0.
- `git diff -- vitest.design.config.ts` is empty.
- `node scripts/verify-workflows.mjs` is still green.
</verification>

<success_criteria>
Two availability surfaces mounted in one document carry two different timezone-note ids, each bound to its
own control; the comment that claimed the collision was impossible is replaced by the measurement that
disproved it; a build-blocking census keeps the module-level-literal shape from returning; and the second
behaviour the same spec may carry has a recorded verdict rather than being absorbed into the first.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-04-SUMMARY.md` when done.
State both verdicts, state whether the phase grows by a plan for the court variant, and carry forward the
observation about the second documented duplicate id at `src/lib/design/visual-baselines.ts:505` without
claiming it closed.
</output>
