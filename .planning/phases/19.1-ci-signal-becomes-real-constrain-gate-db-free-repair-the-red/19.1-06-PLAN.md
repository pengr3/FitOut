---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 06
type: execute
wave: 3
depends_on: ["19.1-05"]
files_modified:
  - scripts/verify-workflows.mjs
  - tests/design/workflow-invariants.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-pre-fix.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/sc2-audit-inventory.md
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC2 — every existing predicate is audited for the axis-versus-property error, and the audit's verdict for EVERY site is written down, including the sites deliberately left alone. Ten anchoring and presence sites are inventoried; each carries a verdict of repaired, correct-as-is, or carried-with-a-reason, so a later reader can tell an audited site from an unvisited one."
    - "CI-01 / SC2 / CR-01 — a check asserting a trigger key's PRESENCE now also denies the modifiers that neuter it. Restricting the pull-request trigger to a branch list that matches nothing, or adding a path-ignore filter that matches everything, detaches every gate in the file from pull requests while leaving the key in place; both are RED. The two triggers are asserted along DIFFERENT properties because this file's push filter is deliberate: the pull-request trigger must be unfiltered, while the push trigger must list both the development and the default branch — a missing default branch detaches every gate from the branch the D-05 ruleset protects."
    - "CI-01 / SC2 / WR-02 — the trigger accessor understands the documented sequence form. Before this repair a correct file written with the sequence spelling produced trigger names that were array indices, so the fix's own filter conjunct would have passed while the membership conjunct failed — a correct file, red, for the wrong reason, which is the failure mode that gets a check deleted rather than repaired. A standing positive control asserts the sequence form stays GREEN."
    - "CI-01 / SC2 / CR-02 — every step anchor identifies its step the SAME way. The refusal ordering predicate located its subject a SECOND time by a substring of a run body, so a decoy step whose run merely mentions the refusal script captured the anchor; it now derives the index from the already-name-anchored step object, and additionally asserts the preceding-step list is non-empty so the ordering claim cannot hold vacuously."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — when several steps compare equal under a loose anchor, the selected one is now specified and stable everywhere it matters. Ordering predicates in both the e2e job and the visual job derive their indices from name-anchored step objects rather than from the first run body that happens to contain a substring, so inserting a decoy earlier in the job cannot shift an index into satisfying an ordering claim while the real step runs later."
    - "CI-01 encoding (spec-less edge probe, verification: explicit) — whose definition of equality applies is stated at each repaired site. Step identity is exact equality over the parsed `name:` scalar; invocation identity is exact equality over the trimmed `run:` scalar; trigger identity is exact key membership over a form-aware accessor that normalises the scalar, sequence and mapping spellings to the same list of names. No site compares raw bytes and no site normalises further than that."
    - "CI-01 — the SAFE direction of substring matching is preserved deliberately and said so. The counter that requires zero snapshot-update occurrences over every run command is a deny-direction count where over-matching produces a false red and therefore fails closed; it is inventoried as correct-as-is with that reason, so a future audit does not 'fix' it into a locator."
    - "CI-01 — every repaired predicate is proved twice: a hand mutation on the TRACKED workflow file, GREEN before and RED after with checksum and porcelain revert proof, AND a standing case. The two positive controls — the sequence-form trigger spelling and a decoy run body that must NOT capture an anchor — are the cases that prove each defect class is CLOSED rather than moved."
  artifacts:
    - scripts/verify-workflows.mjs
    - tests/design/workflow-invariants.test.ts
    - .github/workflows/ci.yml
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-pre-fix.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/sc2-audit-inventory.md
  key_links:
    - "the workflow's trigger block to the `ci` section's trigger check. The check previously read key membership only, so a filter that matched nothing left it green — and CI-01's own requirement text is about specs running on pull requests, which a nothing-matching filter makes false with the checker green."
    - "the name-anchored refusal step object to the ordering predicate that used to find it again by substring. One identity per step, derived once, used everywhere — this is what makes the decoy control meaningful."
    - "the ordering indices in the visual job to the same repair. Its steps all carry exact names, so the fix transfers verbatim rather than needing a new shape."
    - "the build-job locator to the build-step invariant plan 05 added. The invariant supersedes the locating half, so the locator's remaining job is narrower and its verdict says so."
  prohibitions:
    - "A substring over a run body must never be used to LOCATE the subject of a positive assertion. It is safe only in the deny direction, where over-matching produces a false red and fails closed."
    - "The push trigger must not be asserted unfiltered. This file's push filter is deliberate, and an unfiltered assertion would redden a correct file — the failure mode that gets a check deleted rather than repaired."
    - "An ordering claim must never be allowed to hold vacuously. A predicate asserting one step precedes another must also assert the preceding set is non-empty, or an empty step list satisfies it."
    - "The deny-direction substring counter must not be converted into a locator. Its over-matching is the property, not a defect."
    - "A predicate's printed name must never be broader than its conjunct list. If only one mutation violates the stated property, the property was written too narrowly and the name is a claim the code does not make."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `ordering` and `encoding` explicitly, re-derived for its own subject (index derivation from name-anchored objects; the three trigger spellings normalised to one list). `adjacency` and `empty` are owned by plans 01, 02, 03, 04 and 11; none is dropped."
    - "RESEARCH.md inventory row 5 (the ordering indices inside the e2e job) is flagged as NOT YET REPRODUCED. Task 2 reproduces it before deciding; if the decoy does not in fact shift the index, the row's verdict becomes correct-as-is WITH the reproduction attached, and the repair is not made on suspicion."
    - "IN-04 — the mail environment-key scan not walking a container or service environment block — is carried, not closed, unless Task 2's audit absorbs it cheaply. It is mitigated today by the raw-text scan in the cross section. The inventory records the carry and the mitigation rather than dropping the row."
    - "A count change is NOT expected: CR-01, WR-02 and CR-02 all TIGHTEN existing predicates rather than adding new ones. If any repair genuinely needs a new counted check, all three spellings of the total move in the same commit, taken from the checker's printed output."
---

<objective>
Carry out SC2: audit every anchoring and presence site in `scripts/verify-workflows.mjs` for the
axis-versus-property error, repair the three confirmed instances, reproduce the one suspected instance
before deciding, and write down a verdict for every site — including the ones deliberately left alone.

Purpose: rounds 2, 3 and 4 of Phase 19 each closed the mutation last measured and were each defeated one
token further out. The defect is not any single predicate; it is the authoring habit of writing a check
from the reproduction rather than from the property in the check's own printed name. An audit that
produces no written verdict per site reproduces that habit at a larger scale.

Output: a repaired trigger predicate that denies its neutering modifiers, a form-aware trigger accessor, a
single identity per anchored step, two positive controls, and an inventory document with ten verdicts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The trigger property — deny the neutering modifiers, and understand all three spellings</name>

  <read_first>
    - `scripts/verify-workflows.mjs:217-218` — `triggersOf`, which handles the scalar and mapping spellings
      and returns array indices for the documented sequence spelling. This is WR-02.
    - `scripts/verify-workflows.mjs:487-494` — the `ci` trigger check as it stands: key membership only.
    - `.github/workflows/ci.yml`'s `on:` block — the push trigger's deliberate branch filter and the bare
      pull-request trigger. Read the actual values; the repair must pass against this file unchanged.
    - `.planning/phases/19-.../19-REVIEW.md` CR-01 — the finding, its reproduction and its fix sketch.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"SC2", the inventory rows 1 and 2 and the paragraph
      headed "The trigger fix, with the nuance CR-01's sketch does not state" — including the warning that
      the form-aware accessor is a prerequisite for the fix and not an optional tidy.
    - `tests/design/workflow-invariants.test.ts:288-303` — the green-control shape the sequence-form case
      must copy, and `:421` — the existing deleted-trigger case, which this repair must not break.
  </read_first>

  <behavior>
    - Restricting the pull-request trigger to a branch list that matches no branch turns the checker RED.
    - Adding a path-ignore filter that matches everything under the pull-request trigger turns it RED.
    - Removing the default branch from the push trigger's branch list turns it RED.
    - The push trigger keeping its existing deliberate branch filter stays GREEN — the repair does not
      assert push is unfiltered.
    - A file written with the sequence spelling of the trigger block stays GREEN — the accessor normalises
      all three spellings to the same list of trigger names.
    - The existing deleted-trigger case stays RED.
  </behavior>

  <files>
    scripts/verify-workflows.mjs,
    tests/design/workflow-invariants.test.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-pre-fix.txt
  </files>

  <action>
FIRST reproduce the two vectors against the current tree. On the TRACKED `.github/workflows/ci.yml`, in
turn: (a) give the pull-request trigger a branch list naming a branch that does not exist; (b) give it a
path-ignore filter matching everything. Run the checker after each, capture the GREEN transcript with exit
0 into `evidence/guards-06-pre-fix.txt` under a heading naming the mutation, revert, and record the
checksum pair and `git status --porcelain`. Also capture a third pre-fix observation: rewrite the trigger
block into the sequence spelling and record what the checker does with it today, which is the WR-02
false-red.

Repair `triggersOf` first — it is the prerequisite. It must return the list of trigger NAMES for all three
documented spellings: a bare scalar, a sequence of names, and a mapping of names to filter objects. State
at the site which three spellings it handles and that an unhandled spelling would make a correct file red
for a reason unrelated to its content.

Then repair the trigger check so it asserts the PROPERTY rather than the axis. Two triggers, two different
properties, each with its reason at the site:
  - The pull-request trigger must be present AND unfiltered — its parsed filter value must be absent or
    null. A branch list that matches nothing and a path-ignore filter that matches everything both make
    every gate in the file unreachable from a pull request, which falsifies CI-01's own requirement text
    while leaving the key in place.
  - The push trigger must be present AND its branch list must contain both the development branch and the
    default branch. Do NOT assert it is unfiltered: this file's push filter is deliberate, and an
    unfiltered assertion would redden a correct file. A missing default branch detaches every gate from the
    branch the ruleset in plan 15 protects.
The evidence line prints the parsed trigger list in the document's own key order and the parsed filter
values, rendering an explicit marker when the list is empty so a red is diagnosable against the file as
written.

Add standing cases in the file's idiom, numbered after plan 05's: a nothing-matching branch list on the
pull-request trigger (RED), an everything-matching path-ignore filter (RED), the default branch removed
from the push list (RED), and the sequence spelling of the whole trigger block (GREEN POSITIVE CONTROL,
asserting exit 0 and the presence of the pinned summary line, with a docblock stating it is the WR-02
control and why it asserts green).

Re-apply each mutation to the TRACKED file after the repair and capture the RED transcripts with their
FAIL lines into `evidence/guards-06-post-fix.txt`, with revert proof for each.
  </action>

  <acceptance_criteria>
    - `triggersOf` returns trigger names for the scalar, sequence and mapping spellings, and a comment at
      the site names all three.
    - The trigger check asserts the pull-request filter is absent or null AND that the push branch list
      contains both named branches; it does not assert the push trigger is unfiltered.
    - `node scripts/verify-workflows.mjs` exits 0 against the unmutated tree.
    - Four new standing cases exist; exactly one of them asserts a zero exit and the presence of the pinned
      summary line.
    - `evidence/guards-06-pre-fix.txt` contains three pre-repair observations with revert proofs, and
      `evidence/guards-06-post-fix.txt` contains three post-repair RED transcripts with FAIL lines.
    - `git diff -U0 -- .github/workflows/ci.yml` shows no non-comment line changed.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit, or any case name appearing under a failed-test heading, or the existing deleted-trigger case appearing as skipped</fails_when>
    <automated>git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#' || true</automated>
    <fails_when>output is any number other than 0 — a non-comment line changed in the workflow file</fails_when>
    <automated>grep -c '^FAIL' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt</automated>
    <fails_when>output is less than 3 — fewer than three trigger vectors have a post-fix red on record</fails_when>
  </verify>

  <done>
    A trigger filter that neuters the key without deleting it is RED; a correct file written in any of the
    three documented spellings is GREEN; and both outcomes are remembered by standing cases including one
    positive control.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One identity per anchored step — repair the substring locators, reproduce the suspected one first</name>

  <read_first>
    - `scripts/verify-workflows.mjs:885-900` — the name-anchored refusal step object, which is the identity
      every predicate about that step should use.
    - `scripts/verify-workflows.mjs:994-1008` — the ordering predicate that locates the same step a SECOND
      time by a run substring (CR-02), and the preceding-uses prefix test beside it.
    - `scripts/verify-workflows.mjs:874-878` and `:1003-1005` — the migrate, seed and playwright ordering
      indices inside the e2e job, found by run substring. RESEARCH.md inventory row 5, flagged NOT YET
      REPRODUCED.
    - `scripts/verify-workflows.mjs:1069-1086` — the visual job's project-run detection and its three
      ordering indices, the same shape one job over. Its steps carry exact names.
    - `scripts/verify-workflows.mjs:696-707` — the build-job locator, whose locating half is superseded by
      the build-step invariant plan 05 added.
    - `scripts/verify-workflows.mjs:527-532` — the snapshot-update counter, the SAFE direction of substring
      matching. Read the reason before touching anything nearby.
    - `.github/workflows/ci.yml` — the visual job's step list and the e2e job's step list, to confirm every
      step this task anchors on actually declares an exact `name:`.
    - `.planning/phases/19-.../19-REVIEW.md` CR-02 and its fix sketch.
    - `.planning/phases/19.1-.../19.1-PATTERNS.md` §1, the step-anchor pattern block with the model and the
      counter-example quoted side by side.
  </read_first>

  <behavior>
    - A decoy step inserted ahead of the refusal step whose run merely mentions the refusal script does NOT
      capture the ordering anchor; the checker's verdict is unchanged by its presence.
    - Moving the real refusal step to a position after the steps it must precede turns the checker RED.
    - An ordering predicate over an empty preceding-step list is RED, not vacuously true.
    - For the e2e ordering indices: whichever way the reproduction goes, the behaviour after this task is
      the behaviour the reproduction demonstrated, not the behaviour it was assumed to have.
  </behavior>

  <files>
    scripts/verify-workflows.mjs,
    tests/design/workflow-invariants.test.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-pre-fix.txt,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt
  </files>

  <action>
FIRST reproduce, on the TRACKED `.github/workflows/ci.yml`:

(a) CR-02: insert a decoy step ahead of the refusal step whose run mentions the refusal script path, and
capture the checker's GREEN transcript proving the decoy captured the anchor.
(b) Inventory row 5, which RESEARCH.md flags as NOT YET REPRODUCED: insert a decoy earlier in the e2e job
whose run mentions the seed command, and observe whether the seed index shifts such that an ordering claim
is satisfied while the real seed step runs after the suite. Capture whatever happens — a green here
confirms the vector, a red here refutes it, and both are findings.

Record both under headings in `evidence/guards-06-pre-fix.txt` with revert proofs.

Then repair, applying ONE rule everywhere: a step that is anchored by exact `name:` is anchored once, and
every predicate about that step derives from the resulting object.

- The refusal ordering predicate takes its index from the position of the already-name-anchored step
  object in the job's step list, not from a search over run bodies. Add a conjunct asserting the
  preceding-step list is non-empty, so the ordering claim cannot hold over an empty set.
- The e2e job's migrate, seed and playwright indices: if the reproduction confirmed the vector, anchor each
  by its step's exact `name:` and derive the index from the object; if it refuted the vector, leave the
  code and record the refutation with its transcript in the inventory. Do not repair on suspicion.
- The visual job's project-run detection and its three ordering indices: apply the same name-anchored
  derivation. RESEARCH.md records that this job's steps all carry exact names, so the fix transfers
  without a new shape.
- The build-job locator: its locating half is superseded by plan 05's build-step invariant. Narrow its
  remaining responsibility and state at the site what now owns the locating job, or record it as
  correct-as-is with that reason. Do not delete an assertion to simplify a locator.

Do NOT touch the snapshot-update counter. Its over-matching is the property.

Add standing cases: a decoy run body ahead of the refusal step that must leave the checker GREEN (the
CR-02 positive control, with a docblock saying it is the case that proves the class is closed rather than
moved), and the refusal step moved after the steps it must precede, which must be RED.

Re-apply the reproduced mutations after the repair and capture the results in
`evidence/guards-06-post-fix.txt` with FAIL lines and revert proofs.
  </action>

  <acceptance_criteria>
    - No predicate in `scripts/verify-workflows.mjs` locates a step for a POSITIVE assertion by searching
      run bodies for a substring, except where the inventory records a reproduction that refuted the
      vector, with the transcript attached.
    - The refusal ordering predicate derives its index from the name-anchored step object and asserts a
      non-empty preceding-step list.
    - The snapshot-update counter at `:527-532` is byte-unchanged.
    - Two new standing cases exist: one decoy-ahead case asserting GREEN and one reordering case asserting
      RED.
    - `evidence/guards-06-pre-fix.txt` contains the CR-02 reproduction and the row-5 reproduction attempt,
      each with a revert proof; `evidence/guards-06-post-fix.txt` contains the corresponding post-repair
      results.
    - `node scripts/verify-workflows.mjs` exits 0 and `npm run build` passes.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>git diff -- scripts/verify-workflows.mjs | grep -c '^-.*update-snapshots' || true</automated>
    <fails_when>output is any number other than 0 — a line mentioning the snapshot-update counter was removed, and its over-matching is the property this plan protects</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit, or any case name appearing under a failed-test heading</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any eslint error line, or the design vitest run reporting a failed file</fails_when>
  </verify>

  <done>
    Every step this checker asserts something positive about is identified exactly once, by exact name; a
    decoy that would have captured a substring anchor now leaves the verdict unchanged; and the one
    suspected vector was reproduced before it was repaired or refuted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the audit inventory — a verdict for every site, including the untouched ones</name>

  <read_first>
    - `scripts/verify-workflows.mjs` — as repaired by Tasks 1 and 2.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"SC2", the ten-row inventory table with its per-row
      verdicts and the asymmetry paragraph about the safe and dangerous directions of substring matching.
    - `.planning/phases/19-.../19-REVIEW.md` — the IN-series findings, specifically the ones plan 05's
      summary recorded as still open.
    - `evidence/guards-06-pre-fix.txt` and `evidence/guards-06-post-fix.txt` — the transcripts each verdict
      cites.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt` —
      the evidence-document voice.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/sc2-audit-inventory.md,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt
  </files>

  <action>
Author `evidence/sc2-audit-inventory.md`: one row per anchoring or presence site in
`scripts/verify-workflows.mjs`, with columns for the site's line reference, its current shape, its verdict,
and the evidence the verdict rests on.

The verdict vocabulary is exactly three values and every row carries one:
  - REPAIRED — with the transcript reference proving green before and red after;
  - CORRECT AS IS — with the REASON it is correct, not merely the assertion that it is. The
    deny-direction snapshot counter, the concurrency-group value test and the preceding-uses prefix test
    are the rows this applies to, and each reason must be the property, not the shape;
  - CARRIED — with what remains open, why it was not closed here, and what mitigates it today. The mail
    environment-key scan that does not walk a container or service environment block is the row this
    applies to; name the raw-text scan in the cross section as the current mitigation.

Include a row for every one of the ten sites RESEARCH.md inventoried, plus any additional site the audit
found. A site with no row is an unvisited site, and this document is the only thing that distinguishes
those two states for the next reader.

Then add a HOW THIS AUDIT WAS CARRIED OUT section, three short paragraphs: the property was written down
before the fix in each case; at least three distinct mutations were enumerated for each repaired property
and each has a standing case; and where only one mutation could be constructed, the property was rewritten
rather than shipped narrow. State the pitfall this guards against by name — fixing the axis that was
measured — and state its warning sign: a check whose printed name is broader than its conjunct list.

Finally, append to `evidence/guards-06-post-fix.txt` a closing table mapping each of CR-01, WR-02 and
CR-02 to the invariant that now catches it and to the standing case that remembers it, plus the two
positive controls and what each proves.
  </action>

  <acceptance_criteria>
    - `evidence/sc2-audit-inventory.md` contains at least ten rows, each with a line reference and a
      verdict drawn from the three-value vocabulary.
    - Every CORRECT AS IS row states a reason that is a property, and every CARRIED row names both what
      remains open and what mitigates it.
    - The HOW THIS AUDIT WAS CARRIED OUT section names the pitfall and its warning sign.
    - `evidence/guards-06-post-fix.txt` ends with a table mapping CR-01, WR-02 and CR-02 to an invariant
      and a standing case each, and naming the two positive controls.
    - `node scripts/verify-workflows.mjs` is green and `npm run build` passes.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE 'REPAIRED|CORRECT AS IS|CARRIED' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/sc2-audit-inventory.md</automated>
    <fails_when>output is less than 10 — fewer than ten sites carry a verdict, so the audit left sites indistinguishable from unvisited ones</fails_when>
    <automated>grep -cE 'CR-01|WR-02|CR-02' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt</automated>
    <fails_when>output is less than 3 — one of the three named findings has no closing mapping to an invariant and a standing case</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or any eslint error line, or the design vitest run reporting a failed file</fails_when>
    <automated>git status --porcelain -- .github/workflows/ci.yml</automated>
    <fails_when>any output line whose status field is not "M" — a hand mutation was left applied</fails_when>
  </verify>

  <done>
    Every anchoring and presence site in the checker has a written verdict with its evidence, and the three
    named findings each map to the invariant that catches them and the case that remembers them.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit author → `.github/workflows/ci.yml` | Every trigger filter and every inserted step is an attacker-controlled edit to the document the checker validates. |
| a step's `run:` body → the checker's step selection | Free text inside a run body must never be able to decide which step a positive assertion is about. |
| the checker's own predicate names → the reader | A printed name broader than the conjunct list is a claim the code does not make, and it is what three rounds of review believed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-24 | Tampering | trigger filters that neuter a present trigger key | high | mitigate | Task 1's repaired predicate denies a nothing-matching branch list and an everything-matching path-ignore filter, and requires the default branch in the push list. |
| T-19.1-25 | Spoofing | a decoy step capturing an ordering anchor by run substring | high | mitigate | Task 2's single-identity rule: index derived from the name-anchored step object, plus a standing decoy control asserting the verdict is unchanged. |
| T-19.1-26 | Tampering | an ordering claim satisfied vacuously by an empty preceding set | medium | mitigate | Task 2 adds a non-empty conjunct to the ordering predicate. |
| T-19.1-27 | Denial of Service (self-inflicted) | a correct file reddened by an unhandled trigger spelling | high | mitigate | Task 1's form-aware accessor plus the sequence-form positive control; a check that reddens correct files is a check that gets deleted. |
| T-19.1-28 | Repudiation | an audited site indistinguishable from an unvisited one | high | mitigate | Task 3's inventory: a verdict per site, with a reason for every correct-as-is and a mitigation for every carry. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed symbols in `scripts/verify-workflows.mjs`:** `triggersOf` gains sequence-form handling; the `ci`
trigger check gains filter-denial conjuncts and a push-branch membership conjunct; the refusal ordering
predicate derives its index from the name-anchored step object and gains a non-empty conjunct; the visual
job's project detection and ordering indices become name-anchored; the build-job locator is narrowed or
recorded correct-as-is.

**New symbols in `tests/design/workflow-invariants.test.ts`:** six new cases — a nothing-matching
pull-request branch list (RED), an everything-matching path-ignore filter (RED), a push list missing the
default branch (RED), the sequence spelling of the trigger block (GREEN control), a decoy run body ahead
of the refusal step (GREEN control), and the refusal step reordered after its dependents (RED); plus any
new invariant-name fragment constants.

**New evidence files:** `evidence/guards-06-pre-fix.txt`, `evidence/guards-06-post-fix.txt`,
`evidence/sc2-audit-inventory.md` (ten-plus rows, three-value verdict vocabulary).

**New npm scripts:** none.
</artifacts_this_phase_produces>

<verification>
- `node scripts/verify-workflows.mjs` exits 0 against the unmutated tree.
- `npm run build` passes.
- Each of CR-01, WR-02 and CR-02 has a pre-fix and post-fix transcript and a standing case.
- Both positive controls assert green and carry a docblock saying why.
- `evidence/sc2-audit-inventory.md` carries a verdict for every inventoried site.
- No non-comment line of `.github/workflows/ci.yml` differs from HEAD.
</verification>

<success_criteria>
Every existing predicate has been audited against the property in its own printed name rather than the
mutation last measured; the three confirmed instances of the axis-versus-property error are repaired and
proved twice; the one suspected instance was reproduced before a decision; the safe direction of substring
matching is preserved deliberately and documented; and the audit's verdict for every site is on disk.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-06-SUMMARY.md` when done.
Record whether inventory row 5's vector reproduced, whether the invariant total moved, and which IN-series
findings remain carried after this audit.
</output>
