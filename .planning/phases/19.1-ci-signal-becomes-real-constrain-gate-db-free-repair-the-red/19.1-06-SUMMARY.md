---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 06
subsystem: infra
tags: [ci, github-actions, workflow-checker, mutation-testing, vitest, audit, branch-protection]

# Dependency graph
requires:
  - phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
    provides: "plan 01's harness idiom (constFromChecker, withMutatedWorkflows, expectRed's FAIL-line fragment) and its exact-name step-anchor model; plan 05's isolation discipline, its parameterised withJobKey, its three-spelling count discipline, and the D-19.1-C specimen this plan was chartered to audit"
  - phase: 19-host-listing-surfaces-gates-that-actually-run
    provides: "19-REVIEW.md findings CR-01, CR-02 and WR-02, and the IN-series findings whose disposition this audit had to record rather than drop"
provides:
  - "A trigger predicate that denies the modifiers which neuter a present key: pull_request must be UNFILTERED, push must REACH both the development and the default branch"
  - "triggersOf handling all three documented spellings of `on:` (scalar, sequence, mapping) plus triggerFilterOf normalising the filter across them — WR-02's false red closed"
  - "stepNamed / stepIndexNamed / runOfStep — one identity per anchored step, and every ordering index on the FULL-step scale"
  - "Six substring locators repaired: the refusal ordering anchor, the gate-e2e ordering indices, gate-visual's project detection and ordering indices, baselines.yml's staging tripwire, and the cross-section byte-identity comparisons"
  - "A non-empty conjunct on the refusal ordering predicate, so an ordering claim can no longer hold vacuously over an empty preceding set"
  - "withMutatedWorkflowPair — the harness can now mutate baselines.yml, and both files at once, with each mutator asserted separately"
  - "Twelve new standing cases (25-36), taking the design suite from 24 to 36, of which THREE assert GREEN"
  - "evidence/sc2-audit-inventory.md — 24 rows, three-value verdict vocabulary, a reason on every correct-as-is and a mitigation on every carry"
  - "evidence/guards-06-pre-fix.txt and guards-06-post-fix.txt — thirteen watched-green/watched-red pairs plus six per-conjunct memory measurements"
affects: [19.1-11, 19.1-12, 19.1-15, branch-protection, ci-signal, gate-visual, baselines]

actuals:
  tokens: 51394
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "One identity per anchored step: a step anchored by exact `name:` is anchored ONCE, and every predicate about it derives from the resulting object — never found a second time by a substring"
    - "Reachability, not shape: where two files are both correct along an axis, the property is what they have in common (push REACHES both branches), not the spelling one of them happens to use (push LISTS both branches)"
    - "The safe direction of substring matching is preserved DELIBERATELY and documented at the site, with the reason stated as the property — so a later audit does not 'fix' a deny-direction counter into a locator"
    - "A verdict per audited site, from a fixed three-value vocabulary, is what distinguishes an audited site from an unvisited one — the only thing four review rounds could not recover from the code"
    - "Per-conjunct memory measurement: loosen ONE repaired conjunct back to its prior shape, confirm the checker still exits 0 on the unmutated tree (so the revert is faithful), and require exactly one named case to fail"
    - "A mutation harness is subject to the same anchoring rule as the checker it measures — an ambiguous anchor in the harness produces reds attributed to vectors they have nothing to do with"

key-files:
  created:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-pre-fix.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/guards-06-post-fix.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/sc2-audit-inventory.md
  modified:
    - scripts/verify-workflows.mjs
    - tests/design/workflow-invariants.test.ts
    - .github/workflows/ci.yml
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md

key-decisions:
  - "The push trigger is asserted by REACHABILITY, not by branch-list membership. The plan's literal conjunct ('the branch list contains both names') and the plan's own WR-02 green control are mutually inconsistent: the sequence spelling has no branch list at all. An unfiltered push reaches every branch, so it reaches both; a filter must name both and may carry no other key. Reachability admits both correct files; membership admits one."
  - "RESEARCH.md inventory row 5 REPRODUCES. It was flagged NOT YET REPRODUCED and was reproduced before any decision: a decoy naming db:seed with the real seed moved after the suite left the checker at exit 0 with all 55 holding and the seed invariant printed `ok`. Repaired on the measurement."
  - "Row 6's project-detection half is the worst single result in the plan: a decoy naming --project=visual with gate-visual's real invocation switched to --project=chromium left ALL 55 REPORTED HOLDING while GATE-01's comparison did not run at all."
  - "The audit was carried out as a SCAN of the whole checker for the shape, not a walk of RESEARCH.md's ten rows. That found two more sites (rows 11 and 12), both outside the sections this plan's task text names, both reproduced, both repaired."
  - "D-19.1-C is CARRIED, not repaired, and the suggested fix in deferred-items.md was explicitly NOT applied. Its over-matching fails CLOSED, and re-anchoring it by job key would defeat the purpose its own heading states."
  - "The plan's decoy-ahead POSITIVE control cannot exist as specified: a decoy ahead of the refusal step legitimately violates the correct WR-05 predecessor allow-list. The honest control places the same decoy where it is legitimate."
  - "The invariant total does NOT move. Every repair tightened an existing predicate; no counted check() was added. 55 (baselines=11, ci=36, cross=8), read from the checker's printed line."

patterns-established:
  - "One identity per anchored step, derived once and used by every predicate about it"
  - "Reachability over shape when more than one file is correct along the axis"
  - "Three positive controls, each stating in its docblock which defect class it proves CLOSED rather than MOVED"
  - "Per-conjunct memory measurement with a faithfulness precondition (checker still green on the unmutated tree)"
  - "A failed measurement is written up rather than discarded — both the scale-mixing revert and the false refutation are in the evidence"

requirements-completed: [CI-01]

coverage:
  - id: D1
    description: "A pull-request trigger filter that neuters the key without deleting it — a nothing-matching branch list, or a path-ignore matching everything — turns the checker RED"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 25 (CR-01): a nothing-matching branch list on the pull-request trigger is red"
        status: pass
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 26 (CR-01): an everything-matching path-ignore filter on the pull-request trigger is red"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTORS CR-01(a) and (b) (exit 0, all 55 holding) vs guards-06-post-fix.txt (exit 1, 1 of 55, named)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Removing the default branch from the push trigger's branch list — detaching every gate from the branch plan 15's ruleset protects — turns the checker RED, while the file's deliberate push filter stays GREEN"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 27 (CR-01): removing the default branch from the push trigger's branch list is red"
        status: pass
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 1 (control): an unmutated copy is green, and pins the invariant total"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTOR CR-01(c) (exit 0, all 55 holding) vs guards-06-post-fix.txt (exit 1, named)"
        status: pass
    human_judgment: false
  - id: D3
    description: "WR-02 closed — a correct file written in the documented sequence spelling of `on:` is GREEN at the pinned summary line, where it used to be RED"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 28 (WR-02, POSITIVE CONTROL): the sequence spelling of the trigger block stays GREEN"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTOR WR-02 (CHECKER-EXIT=1, 1 of 55 FAILED, naming this invariant) vs guards-06-post-fix.txt (exit 0, All 55 hold)"
        status: pass
    human_judgment: false
  - id: D4
    description: "CR-02 closed — a decoy run body can no longer capture the refusal step's ordering anchor, and an ordering claim can no longer hold vacuously over an empty preceding set"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 29 (CR-02): a decoy run body ahead of the refusal step is red"
        status: pass
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 31 (CR-02): the refusal step moved after the steps it must precede is red"
        status: pass
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 34 (CR-02): the refusal step promoted to FIRST is red"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTORS CR-02-DECOY-ONLY, CR-02 and VACUOUS-PRECEDE (all exit 0, all 55 holding) vs guards-06-post-fix.txt (all RED, named)"
        status: pass
    human_judgment: false
  - id: D5
    description: "RESEARCH.md inventory row 5 reproduced and repaired — a decoy naming the seed command can no longer satisfy the seed-before-suite ordering claim while the real seed runs after the suite"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 32 (row 5): a decoy mentioning the seed command, with the real seed moved after the suite, is red"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTOR ROW-5 — exit 0, all 55 holding, with the seed invariant printed `ok` (the reproduction the research flagged as missing)"
        status: pass
    human_judgment: false
  - id: D6
    description: "RESEARCH.md inventory row 6 reproduced and repaired — gate-visual's project detection and its three ordering indices are name-anchored, so a decoy cannot leave GATE-01's comparison unrun under a green check"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 33 (row 6): a decoy naming the visual project, with the real step switched to another project, is red"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTOR VISUAL-PROJECT (exit 0, ALL 55 REPORTED HOLDING with --project=chromium in gate-visual) and VECTOR VISUAL-ORDER"
        status: pass
    human_judgment: false
  - id: D7
    description: "Two sites the research inventory did not list — baselines.yml's staging tripwire and the cross-section byte-identity comparisons — found by scanning for the shape, reproduced, and repaired"
    requirement: CI-01
    verification:
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 35 (row 11): renaming the staging step, with a decoy copying its command, is red"
        status: pass
      - kind: unit
        ref: "tests/design/workflow-invariants.test.ts#case 36 (row 12): identical decoys in BOTH visual jobs, with the real migrate commands made to differ, is red"
        status: pass
      - kind: other
        ref: "evidence/guards-06-pre-fix.txt VECTORS STAGE-GLOB and CROSS-PICK (both exit 0, all 55 holding) vs guards-06-post-fix.txt (both RED)"
        status: pass
    human_judgment: false
  - id: D8
    description: "Every anchoring and presence site in the checker carries a written verdict, so an audited site is distinguishable from an unvisited one"
    requirement: CI-01
    verification:
      - kind: other
        ref: "grep -cE 'REPAIRED|CORRECT AS IS|CARRIED' evidence/sc2-audit-inventory.md -> 30 (24 inventory rows plus the vocabulary table and prose)"
        status: pass
      - kind: other
        ref: "evidence/sc2-audit-inventory.md — 8 REPAIRED, 14 CORRECT AS IS with a property-shaped reason each, 2 CARRIED with what is open and what mitigates it"
        status: pass
    human_judgment: false
  - id: D9
    description: "Every repaired conjunct is remembered by exactly one standing case — proved by loosening each conjunct alone and watching precisely one case fail"
    requirement: CI-01
    verification:
      - kind: other
        ref: "evidence/guards-06-post-fix.txt LOOSENINGS A-F — six loosenings, checker still exit 0 on the unmutated tree each time, 1 failed | 33-35 passed, one named case each"
        status: pass
    human_judgment: false

# Metrics
duration: 1h 10m
completed: 2026-09-05
status: complete
---

# Phase 19.1 Plan 06: The axis-versus-property audit Summary

**Six predicates that decided WHICH step they were about by searching free text can no longer be impersonated by an `echo`; a present-but-neutered trigger key is RED and a correct file written in the sequence spelling is GREEN; and every anchoring site in the checker now carries a written verdict, including the eleven deliberately left alone.**

## Performance

- **Duration:** 1h 10m
- **Started:** 2026-09-05T15:47:00Z
- **Completed:** 2026-09-05T16:57:00Z
- **Tasks:** 3
- **Files modified:** 7 (4 modified, 3 evidence files created)

## Accomplishments

- **CR-01 closed, along a property rather than an axis.** Membership was never the property: three edits that leave both trigger keys exactly where they are — a nothing-matching pull-request branch list, an everything-matching `paths-ignore`, and the default branch dropped from the push list — each left the checker at **exit 0 with all 55 reported holding**. The first two make every gate in the file unreachable from a pull request, which falsifies CI-01's own requirement text. All three are now RED on one predicate whose printed name states what it enforces.
- **WR-02 closed, and it is the opposite kind of defect.** `on: [push, pull_request]` — a **correct** file, in a spelling docs.github.com documents — made the checker exit 1, because `Object.keys` over an array returns its indices. A check that reddens correct files is the check that gets deleted, and its three real reds go with it. `triggersOf` now normalises all three spellings and case 28 is a standing GREEN control.
- **CR-02 closed, in its sharpest form.** The refusal step was anchored by exact `name:` in one place and then **found again** by a run substring in the ordering predicate. A decoy `echo` mentioning the script captured that second anchor — and because the preceding set was sliced at the decoy's own index, **the decoy excluded itself from the set it violates**. With the real step then moved to the end of the job: exit 0, all 55 holding, with the only control protecting the current run firing after the whole suite.
- **Both rows the research could not decide from the code were REPRODUCED, not assumed.** Row 5 was flagged NOT YET REPRODUCED and reproduces. Row 6 reproduces in both halves, and its project-detection half is the worst single result here: a decoy naming `--project=visual` with `gate-visual`'s real invocation switched to `--project=chromium` left **ALL 55 REPORTED HOLDING** while GATE-01's comparison did not run at all and 52 committed baselines went uncompared.
- **The audit was a scan, not a walk of the list — and that found two more sites.** `baselines.yml`'s staging tripwire (a decoy merely *printing* the staging command satisfied it while the real step staged a glob matching nothing) and the `cross` byte-identity comparisons (identical decoys in **both** jobs make the comparison hold between two strings that are neither job's real command — the machine that COMPARES diverging from the machine that SHOT, i.e. D-27 exactly). Both reproduced, both repaired.
- **Six repaired conjuncts, six bound cases, one-to-one.** Each conjunct was loosened **alone** back to its pre-19.1-06 shape, with the checker confirmed still exiting 0 on the unmutated tree so the revert is known faithful — and exactly one named case failed each time.
- **The suite went from 24 cases to 36**, of which three assert GREEN and each says in its docblock which defect class it proves CLOSED rather than MOVED. **The invariant total did not move: 55** (`baselines=11, ci=36, cross=8`). Every repair tightened an existing predicate.
- **`npx tsc --noEmit` went 9 → 7 errors.** The third `spawnSync<string>` annotation this plan needed would have added two more of a pre-existing class, so the construct is named once as `CheckerRun` instead of three times.

## Task Commits

1. **Task 1: the trigger property — deny the neutering modifiers, understand all three spellings** — `3e87c8d` (fix)
2. **Task 2: one identity per anchored step — repair the substring locators** — `971bd30` (fix)
3. **Task 3: the audit inventory, plus the two sites the research did not list** — `6a63c57` (docs)

## Files Created/Modified

- `scripts/verify-workflows.mjs` — `triggersOf` (three spellings) and `triggerFilterOf`; `CI_DEV_BRANCH`/`CI_DEFAULT_BRANCH`; the repaired trigger check with filter-denial and push-reachability conjuncts; `stepNamed`/`stepIndexNamed`/`runOfStep`; six repaired locators; a non-empty conjunct on the refusal ordering predicate; six new step-name constants; and a 20-line audited-and-kept rationale block on T-11-DBFREE's locator.
- `tests/design/workflow-invariants.test.ts` — `withMutatedWorkflowPair` (mutates `baselines.yml`, and both files at once, each mutator asserted separately); the job-scoped step primitives (`jobKeyAt`, `stepSpan`, `withStepAfterInJob`, `withStepBeforeInJob`, `withStepMovedAfterInJob`, `withStepMovedFirstInJob`, `withStepRunReplacedInJob`, `withRenamedStepInJob`, `withAppendedRunArgumentInJob`, `firstRunLineOf`); the trigger builders; eleven new constants read through `constFromChecker`; four new invariant-name fragments; the `CheckerRun` alias; cases 25–36.
- `.github/workflows/ci.yml` — **comment lines only**, proven by `git diff -U0 … | grep -cv '^[+-][[:space:]]*#'` returning 0 across all three task commits: the trigger bullet's tightening paragraph, a new file-level bullet on the step-anchor repair with all five measured vectors, the two extra sites, the safe-direction note, and the standing-case count.
- `evidence/guards-06-pre-fix.txt` (817 lines) — thirteen hand mutations on the tracked tree with md5 pairs and revert proofs, and two written-up measurement failures.
- `evidence/guards-06-post-fix.txt` (467 lines) — the same mutations red, the two positive controls green, six per-conjunct memory measurements, and a closing map from each finding to its invariant and its case.
- `evidence/sc2-audit-inventory.md` (138 lines) — 24 rows, three-value verdict vocabulary, and the HOW THIS AUDIT WAS CARRIED OUT section naming the pitfall and its warning sign.
- `deferred-items.md` — D-19.1-C answered as CARRIED, with the suggested fix explicitly withdrawn.

## Decisions Made

- **The push trigger is asserted by REACHABILITY, not by branch-list membership.** See deviation 1 — the plan's two requirements were mutually inconsistent, and reachability is the form that admits both correct files.
- **`pull_request` must carry NO filter at all**, rather than a deny-list of the two modifiers that were measured. The modifier surface is an open set (`types:` is a third), and the file's own `MAIL_STEP_ALLOWED_KEYS` block already argues why enumerating against an open set loses.
- **Every ordering index is on the FULL-step scale.** Mixing it with `runsOf` indices is finding WR-05's defect, and it bit during this plan's own measurement — see deviation 3.
- **D-19.1-C is CARRIED and its suggested fix was explicitly NOT applied.** Its over-matching fails closed; re-anchoring it by job key would defeat the purpose its own heading states.
- **The snapshot-update counter is byte-unchanged**, verified (`git diff | grep -c '^-.*update-snapshots'` → 0), and nine deny-direction sites are recorded CORRECT AS IS with the reason stated as a property so a later audit does not convert them into locators.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The plan's two push-trigger requirements cannot both hold**

- **Found during:** Task 1
- **Issue:** The plan requires "the push branch list must contain both the development branch and the default branch" **and** that the sequence spelling `on: [push, pull_request]` be a GREEN positive control. The sequence form cannot express a filter, so it has no branch list at all — a literal membership conjunct reddens the very file the control requires to be green. Implementing the plan's text verbatim would have made case 28 fail.
- **Fix:** The conjunct asserts **reachability**: an absent push filter reaches every branch and is GREEN; a present filter must name both branches and — allow-list, never deny-list — may carry no other key, so `paths-ignore: ['**']` on push is red too. The site states both halves and why push is deliberately not asserted unfiltered.
- **Files modified:** `scripts/verify-workflows.mjs`
- **Verification:** Case 27 red, case 28 green at the pinned summary line, tracked file green.
- **Committed in:** `3e87c8d`

**2. [Rule 1 - Bug] The plan's decoy-ahead POSITIVE control cannot exist as specified**

- **Found during:** Task 2
- **Issue:** The plan asks for "a decoy run body ahead of the refusal step that must leave the checker GREEN". Measured: it is **RED after the repair**, and correctly so — the WR-05 predecessor allow-list says only checkout, setup-node and `npm ci` may precede that step, and a decoy `echo` is a fourth thing. The plan's control would have demanded the checker ignore a real violation.
- **Fix:** The decoy-ahead mutation became a **RED** case (29) and it is the sharpest statement of CR-02 in the file: before the repair the decoy captured the anchor and was therefore sliced out of its own preceding set. The **positive control** (case 30) places the same decoy where it is legitimate — after everything the refusal must precede — proving the repair did not degenerate into "no step may mention the script", which would be a predicate broader than its printed name.
- **Files modified:** `tests/design/workflow-invariants.test.ts`, `evidence/guards-06-post-fix.txt`
- **Verification:** Case 29 red, case 30 green; the evidence line for case 29 shows the decoy now appearing in `precedes=[…]`.
- **Committed in:** `971bd30`

**3. [Rule 2 - Missing Critical] The first memory measurement mixed index scales and measured nothing**

- **Found during:** Task 2
- **Issue:** All four repaired conjuncts were loosened at once. That left `iE2eSeed` on the run-command scale while `iE2ePlay` stayed on the step scale, so the checker exited 1 on the **unmutated** tree and eight cases failed — including the three that assert GREEN. None of those failures said anything about memory. A revert that does not reproduce the prior *behaviour* is not a revert.
- **Fix:** Each conjunct is loosened **alone**, and the faithfulness of every revert is proved by the checker still exiting 0 on the unmutated tree before the suite runs. Six loosenings, six failures, one named case each. The failed first attempt is written up in the evidence rather than discarded.
- **Files modified:** `evidence/guards-06-post-fix.txt`
- **Verification:** LOOSENINGS A–F, each `CHECKER-EXIT=0` on the unmutated tree and `1 failed`.
- **Committed in:** `971bd30`, `6a63c57`

**4. [Rule 1 - Bug] The mutation harness itself had this plan's own defect**

- **Found during:** Task 2 (first reproduction run)
- **Issue:** The hand-mutation helper anchored on `- name: Migrate the database` with an unscoped `indexOf`. That string appears in **three** jobs of `ci.yml` and the first is `gate-price-parity`, so two decoys landed in the wrong job and produced three reds attributed to vectors they had nothing to do with. It was caught only because the FAIL lines named invariants the vectors were not about.
- **Fix:** Every anchor in both the hand harness and the standing-case builders is scoped to a job key first (`jobKeyAt` → `stepSpan`). The builders' shared docblock records the incident, because a mutation harness is subject to the same rule as the checker it measures.
- **Files modified:** `tests/design/workflow-invariants.test.ts`, `evidence/guards-06-pre-fix.txt`
- **Verification:** All seven Task 2 vectors produce FAIL lines naming their own invariants.
- **Committed in:** `971bd30`

**5. [Rule 1 - Bug] A false refutation of row 11, caught before it became a verdict**

- **Found during:** Task 3
- **Issue:** The first `STAGE-GLOB` decoy was a plain YAML scalar with backslash-escaped quotes, so the string reaching the parser was `git add -- \"*-visual-linux.png\"` — which does not contain the needle. The checker went red and the row looked **REFUTED**. The harness was broken, not the vector. Writing that verdict down would have left a live hole recorded as audited-and-safe, which is the exact failure mode this document exists to prevent.
- **Fix:** The decoy is a **block** scalar, and the parsed run body was verified to contain the needle *before* the verdict was written. The vector then reproduced: exit 0, all 55 holding. The false refutation is recorded in the pre-fix evidence with the reason it happened.
- **Files modified:** `evidence/guards-06-pre-fix.txt`, `scripts/verify-workflows.mjs`, `tests/design/workflow-invariants.test.ts`
- **Verification:** `node -e` printed `decoy run contains the exact needle? true` before the measurement; case 35 red.
- **Committed in:** `6a63c57`

**6. [Rule 2 - Missing Critical] Two anchoring sites outside the plan's task list**

- **Found during:** Task 3
- **Issue:** Task 2's acceptance criterion is scoped to the whole file — "no predicate in `scripts/verify-workflows.mjs` locates a step for a POSITIVE assertion by searching run bodies" — and a scan found two the research inventory had not listed, both confirmed by measurement. Recording them as findings without repairing them would have failed the criterion this plan set itself.
- **Fix:** Both repaired (rows 11 and 12), with standing cases 35 and 36 and per-conjunct memory measurements E and F. `withMutatedWorkflowPair` was added because row 12 **requires** mutating both workflow files — a single-file version of it is green against the old code for the right reason and would have proved the site safe.
- **Files modified:** `scripts/verify-workflows.mjs`, `tests/design/workflow-invariants.test.ts`, both evidence files, `.github/workflows/ci.yml`
- **Verification:** Both vectors red post-fix; total unchanged at 55; `npm run build` exit 0.
- **Committed in:** `6a63c57`

**7. [Rule 3 - Blocking] The new harness signature added two `tsc` errors of a pre-existing class**

- **Found during:** Task 3
- **Issue:** `withMutatedWorkflowPair` needed a third `ReturnType<typeof spawnSync<string>>` annotation. That construct does not typecheck under this repository's `@types/node` — it is 4 of the 9 pre-existing errors — so the count went 9 → 11. The bar is "contributes no NEW `error TS` lines".
- **Fix:** The construct is named **once**, as `type CheckerRun`, and the three call sites use the alias. The count goes 9 → **7**: two fewer than the pre-19.1 baseline, none added. The alias is deliberately not `any` — the call sites still read `status`, `stdout` and `stderr` as strings.
- **Files modified:** `tests/design/workflow-invariants.test.ts`
- **Verification:** `npx tsc --noEmit | grep -c "error TS"` → 7; suite still 36/36.
- **Committed in:** `6a63c57`

---

**Total deviations:** 7 auto-fixed (4 bugs, 2 missing critical, 1 blocking)
**Impact on plan:** Two are corrections to the plan's own internally-inconsistent requirements, caught because the plan demanded a measurement before the fix. Three are corrections to *this executor's own measurements*, caught because each measurement was checked against what it claimed rather than against what it was expected to show. None weakened a predicate. No scope creep beyond the two extra sites, which the plan's own acceptance criterion required.

## Issues Encountered

- **The plan's `<verify>` for Task 1 (`grep -c '^FAIL'`) has plan 05's known shape problem** — the checker indents its FAIL lines by two spaces. Handled the same way plan 05 did: each evidence file carries a section quoting the FAIL lines **verbatim and unindented** under a heading stating they are quotes and not paraphrases. `grep -c '^FAIL'` → 3.
- **`npm run build` failed once with 1 test failure, then passed twice.** The failure was not reproducible; `npm run test:design` in isolation passed 80/80 immediately afterwards, and two subsequent full `npm run build` runs were exit 0 with 1406 passed. This is **D-19.1-A**, the already-logged cold-module-cache flake in `tests/design/e2e-email-silence.test.ts`. Not in this plan's files; not touched.
- **`npx tsc --noEmit` still exits 2**, now on **7** pre-existing errors rather than 9. All are in `tests/design/mail-credential-refusal.test.ts` (5) and the two remaining `spawnSync<string>` sites (2). Pre-existing from phase 19-14; the achievable bar remains "contributes no new `error TS` lines", and this plan removed two.
- **`.planning/STATE.md`'s progress bar could not be recalculated** — `state.update-progress` reports "Progress field not found in STATE.md". This is D-19.1-B's STATE.md size/shape problem, logged by plan 02 and still open.

## Known Stubs

None. No placeholder, no `TODO`, no unwired path was introduced. Every predicate added or repaired runs on every `node scripts/verify-workflows.mjs`; all twelve cases added run inside `npm run build`.

## What This Plan Did NOT Close

Named rather than dropped, and this is the list plan 11 and the phase verifier inherit:

- **IN-01** — one third of Invariant B is unreachable through the `cwd`-only harness, and the file's header does not say so. **OPEN.** Untouched here.
- **IN-02** — `withJobKey`'s silent fallback. **CLOSED by plan 19.1-05.**
- **IN-03** — cases 4, 5, 6 and 8 do not isolate the predicate they name. **STILL OPEN for those four cases.** This plan did not modify them; it did apply the isolation discipline to all twelve of its own new cases (32 and 33 place their decoys specifically so the red is the invariant they name).
- **IN-04** — the mail env-key scan walks neither `container.env` nor `services.*.env`. **CARRIED**, with a written verdict this time: inventory row 10 records what is open, why it was not closed (it is a scan-coverage question, not an anchoring one, and would need its own watched-red pair) and what mitigates it (the `cross` raw-text scan, which is strictly broader for this token — what the parse scan adds is a *diagnosable location*, and it is the diagnosis that is incomplete).
- **IN-05** — `REQUIREMENTS.md` flipped `CI-01` to `Complete` against a standard CR-01 falsifies. **OPEN.** CR-01 is now closed, which removes the falsifier, but the flip itself was made without evidence and is the verifier's call.
- **D-19.1-C** — **CARRIED with a verdict**, and its suggested fix explicitly withdrawn. See `deferred-items.md` and inventory row 7.

## The three questions the plan asked this summary to answer

1. **Did inventory row 5's vector reproduce?** **Yes.** A decoy naming `db:seed` after the refusal step, with the real seed step moved after the Playwright step, left the checker at exit 0 with all 55 reported holding and the seed-ordering invariant printed `ok`. It was repaired on that measurement, not on suspicion. Case 32 is its memory.
2. **Did the invariant total move?** **No. It is still 55** (`baselines=11, ci=36, cross=8`). Every change tightened a predicate that already existed; no counted `check()` was added or removed. All three spellings are byte-identical and case 1 pins them.
3. **Which IN-series findings remain carried?** **IN-01, IN-03 and IN-05 remain OPEN**; **IN-04 is CARRIED with a written mitigation**; **IN-02 was closed by plan 19.1-05**. **D-19.1-C is CARRIED with a reason**, and is now a deliberate decision rather than an outstanding item.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Ready for plan 11.** It inherits `withMutatedWorkflowPair`, the job-scoped step primitives, the `CheckerRun` alias, and a 24-row audit it can read instead of re-deriving. IN-03's four non-isolating cases are still its to take if it wants them.
- **Ready for plan 15.** This is the plan that matters most for the ruleset: before it, the push trigger could drop the default branch and every gate would detach from the branch the ruleset protects, under a green check. A required status check can only be required on a branch the workflow actually runs on, and that is now an invariant.
- **`gate-visual` is materially safer than it was this morning.** Its Playwright invocation could have been switched to a different project — running the functional suite instead of GATE-01's comparison, comparing nothing, reporting success — with the checker printing all 55 holding. That is closed, with a standing case.
- **One number to carry forward:** the total is **55** (`baselines=11, ci=36, cross=8`). Any plan adding a counted `check()` must move all three spellings in the same commit; case 1 is the instrument that makes forgetting impossible. The standing suite is **36 cases**, three of which assert GREEN.
- **No blockers.**

---
*Phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red*
*Completed: 2026-09-05*

## Self-Check: PASSED

All key files exist on disk; all three task commits (`3e87c8d`, `971bd30`, `6a63c57`) are reachable in git. Plan-level `<verification>` re-run at close-out: `node scripts/verify-workflows.mjs` exit 0 at 55 invariants with zero FAIL lines; `npm run build` exit 0 (80 design files, 1406 tests passed, 0 lint errors); the design suite is 36 cases, all green; `git diff -U0 -- .github/workflows/ci.yml` shows zero non-comment lines changed across all three commits; `git diff -- scripts/verify-workflows.mjs | grep -c '^-.*update-snapshots'` → 0 (the snapshot counter is byte-unchanged); `grep -c '^FAIL'` over the post-fix evidence → 3; `grep -cE 'CR-01|WR-02|CR-02'` over it → 18; `grep -cE 'REPAIRED|CORRECT AS IS|CARRIED'` over the inventory → 30 across 24 rows; `npx tsc --noEmit` → 7 errors, all pre-existing, two fewer than the baseline.
