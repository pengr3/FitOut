---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 02
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified:
  - e2e/__scratch-known-failure.spec.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/testfail-behaviour.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-harness.md
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/tnum-phase17-probe.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 / D-03 — the allowlist mechanism is chosen on measured behaviour, not on documentation that does not say. Two behaviours of `test.fail(condition, reason)` that change the allowlist design if they are wrong are settled by one scratch-spec run: whether an EXPECTED failure still consumes its configured retries, and whether an expected failure inside a `describe.serial` group still skips the remainder of that group. RESEARCH.md logs both as ASSUMED (A1, A2) and both are answered before plan 11 pins a count."
    - "D-01 — triage precedes any write-off, and this plan is what makes triage cheap for the plans that follow. A single reproduction recipe is captured once — database up, migrate, seed, build cache removed, port 3000 free, retries raised to two to reproduce CI's shape — so plans 07 through 11 spend their context on the defect rather than on rediscovering the harness."
    - "CI-01 — RESEARCH.md's Open Question 1 is answered inside a task rather than escalated. Whether Phase 17 closed the Geist tabular-figure finding decides whether `e2e/tabular-figures.spec.ts:361` is a stale spec to REPAIR or a known open finding to annotate; plan 11 branches on the verdict recorded here and does not re-litigate it."
    - "CI-01 — RESEARCH.md's A3 is answered before plan 10 chooses a route for the three `avatar-crop` failures. If they fail on this laptop too, they are broken specs and the verdict changes from environment gap to repair; if they pass, the capability reading holds and the route-intercept or allowlist choice is the live one."
    - "CI-01 empty (spec-less edge probe, verification: backstop) — { statement: 'the scratch experiment proves its own harness before its result is trusted: each transcript must report a non-zero count of collected tests, because a run that collected nothing reports the same clean exit as one whose expected failures behaved as hoped', verification: backstop }"
    - "CI-01 — every artefact this plan produces is a raw captured transcript with its command line above it, matching Phase 19's `evidence/guards-pre-fix.txt` convention, so a later reader can tell a measurement from a recollection."
    - "The scratch spec is a probe and leaves no residue. `e2e/__scratch-known-failure.spec.ts` is created, run, transcribed into the evidence file and DELETED in the same task; the chromium project's collected spec-file count at task end equals the count at task start."
  artifacts:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/testfail-behaviour.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-harness.md
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/tnum-phase17-probe.txt
  key_links:
    - "the retry answer to plan 11's wall-clock claim. If an expected failure does NOT consume retries, annotating `e2e/hold-countdown.spec.ts:443` alone returns roughly six of the forty-four minutes; if it does, that saving does not exist and plan 13's re-measurement must not assume it."
    - "the serial-group answer to plan 11's allowlist honesty. If an expected failure still stops its serial group, an allowlist entry would keep hiding the 25 tests recorded as `did not run`, and the allowlist would mask more than it declares — which would make the in-file annotation the wrong mechanism."
    - "the tabular-figure verdict to plan 11's branch for `e2e/tabular-figures.spec.ts:361`: repair a stale spec, or annotate a known open finding with the reason the source already carries."
    - "the local reproduction recipe to the first task of plans 07, 08, 09 and 10, each of which reproduces before repairing."
  prohibitions:
    - "A triage verdict must never be recorded from a job log alone when a local run can settle it. Four of the fourteen failures carry hypotheses the log cannot separate, and a wrong root cause costs a whole plan."
    - "A local reproduction must never run against a stale build cache. A week-old bundler cache fakes route 404s and hydration mismatches, and both have been misfiled as product bugs in this repository before."
    - "A probe file must never be left under `e2e/`. Anything matching the chromium project's glob becomes part of the suite this phase is trying to make meaningful."
    - "A scan or experiment that found nothing must never be recorded as a clean result without evidence that it scanned something. A vacuous green is the exact defect class this phase exists to delete."
    - "The real-mail opt-in flag must never be set for a local triage run. This machine's `.env.local` holds a live transactional-mail key, and the config's injected empty value is the only thing standing between a triage run and real outbound email."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan carries the `empty` item as a backstop marker (the non-vacuity guard on its own experiment). `adjacency`, `ordering` and `encoding` are owned and authored explicitly by plans 01, 06 and 11 for their own subjects; they are not restated here, and none is dropped."
    - "A5 (RESEARCH.md) — `e2e/hold-countdown.spec.ts:443`'s root cause is NOT settled by this plan. Plan 08 owns the trace run. This plan records only the harness that makes that run reproducible."
    - "RESEARCH.md Open Question 3 (raising the Playwright worker count above one) is OUT OF SCOPE and is named here rather than dropped: it is not sharding, so D-13 does not forbid it, but it is a behaviour change to the gate in the same phase that is trying to make the gate trustworthy. Carried to the deferred list beside D-13."
---

<objective>
Settle by measurement the four questions that would otherwise be guessed inside a later plan: the two
undocumented expected-failure behaviours that decide the allowlist mechanism, whether Phase 17 closed the
tabular-figure finding, whether the three `avatar-crop` cases fail on this machine too, and what the
reproducible local triage recipe actually is.

Purpose: RESEARCH.md leaves four items at MEDIUM confidence and flags each as changing a downstream plan
if wrong. This is the Wave 0 that stops plans 07-11 spending their context re-deriving the harness or
committing to a mechanism on an assumption.

Output: three evidence transcripts under the phase `evidence/` directory. No production file changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the two expected-failure behaviours the allowlist design depends on</name>

  <precondition>
    Docker Desktop is running with the container `fitout-db-1` up (`docker ps` lists it), and TCP port 3000
    is free — `playwright.config.ts` sets `reuseExistingServer: false`, so an occupied port aborts the run
    naming the port rather than falling back to the existing server.
  </precondition>

  <read_first>
    - `playwright.config.ts` — the retry expression keyed on the CI environment variable, the unset worker
      count, the chromium project's spec glob, the `webServer` command and its `env` map (which injects an
      empty transactional-mail key unless a named opt-in flag is set), and `reuseExistingServer: false`.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"D-03 — the known-failures allowlist": the options
      table A through E, the recommendation, and the two behaviours listed beneath it as ASSUMED.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Assumptions Log" rows A1 and A2, including what each
      costs if it is wrong.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt` — the
      raw-transcript evidence convention: the command line, then unedited output, environment banners
      included, never a prose summary in place of the output.
  </read_first>

  <files>
    e2e/__scratch-known-failure.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/testfail-behaviour.txt
  </files>

  <action>
Write a throwaway spec at `e2e/__scratch-known-failure.spec.ts` containing exactly four tests and no
product interaction — no page navigation, no fixtures, no database access. It contains:

1. A standalone test annotated as an expected failure with a true condition and a reason string, whose
   body throws. This measures whether an expected failure consumes its configured retries (A1).
2. A standalone test annotated as an expected failure with a true condition and a reason string, whose
   body PASSES. This measures the unexpected-pass behaviour, which is the stale-entry guard the whole
   allowlist recommendation rests on.
3. A serial describe group of three tests where the FIRST is annotated as an expected failure and throws,
   and the second and third are ordinary passing tests. This measures whether an expected failure still
   marks the remainder of the group as not run (A2).
4. One plain passing test outside both, so the run has a non-vacuity anchor: a collected count of zero
   must be distinguishable from "everything behaved as hoped".

Every expected-failure annotation takes a condition and a reason string; none is called bare.

Run the spec twice and capture BOTH transcripts, each preceded by its exact command line, into
`evidence/testfail-behaviour.txt`:

- `npx playwright test e2e/__scratch-known-failure.spec.ts --project=chromium --reporter=list`
- the same command with `--retries=2` appended, which reproduces CI's retry shape locally.

Read Playwright's own summary block in each transcript and write a verdict section at the END of the
evidence file answering, in order: does an expected failure consume its retries; does an unexpected pass
fail the run; does an expected failure stop its serial group; and what the collected-test count was. Quote
the summary line each answer is read from — the answer is the quote, not your reading of it.

Then DELETE `e2e/__scratch-known-failure.spec.ts` and confirm the deletion.
  </action>

  <acceptance_criteria>
    - `evidence/testfail-behaviour.txt` contains two command lines and two unedited Playwright transcripts.
    - Each transcript contains a summary block reporting a non-zero total of collected tests.
    - The evidence file ends with a verdict section answering all four questions, each quoting the summary
      line it is read from.
    - `e2e/__scratch-known-failure.spec.ts` does not exist on disk and is not tracked at task end.
    - `npx playwright test --project=chromium --list` reports the same spec-file count as before the task.
  </acceptance_criteria>

  <verify>
    <automated>test ! -e e2e/__scratch-known-failure.spec.ts</automated>
    <fails_when>non-zero exit — the scratch spec is still on disk and would join the suite this phase is making meaningful</fails_when>
    <automated>grep -c 'passed\|failed\|did not run' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/testfail-behaviour.txt</automated>
    <fails_when>output is 0 — the evidence file holds no Playwright summary text, meaning no run was transcribed</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
  </verify>

  <done>
    The evidence file records, from two real runs, whether an expected failure consumes retries, whether an
    unexpected pass fails the run, and whether an expected failure stops its serial group — and the scratch
    spec is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the local reproduction recipe the repair plans each run first</name>

  <read_first>
    - `playwright.config.ts` — the `webServer` command and `env` map, `reuseExistingServer: false`, and the
      `visual` project's platform guard together with the banner it prints when it is not collected.
    - `package.json` scripts — the database bring-up, migrate, seed, design-test and build entries.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Environment Availability", the "Local run recipe for
      SC4 triage" block and its four warning bullets.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Common Pitfalls" Pitfall 7 — a stale build cache fakes
      route 404s and hydration mismatches.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Triage of the 14" — Causes B, D2 and F and item #11,
      the four items whose verdicts are unresolved and which each need one local run, including the exact
      observation RESEARCH.md names as settling each.
    - `e2e/skeleton-geometry.spec.ts:909-958` — the narrower grep the file's own header recommends, and the
      instruction to re-measure and move the constant rather than the tolerance.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-harness.md
  </files>

  <action>
Author `evidence/triage-harness.md` as an operational note, not an essay. It contains four sections.

(a) THE SEQUENCE. The exact commands, in order, that bring this machine to a state where a single spec can
be reproduced: bring the Docker Postgres up, migrate, seed, remove the build cache directory, confirm port
3000 is free, then run one spec with the chromium project and tracing on. Give the commands literally, in
the shell form that works on this box.

(b) THE FOUR WARNINGS, each with the consequence of ignoring it: an occupied port 3000 aborts the run
naming the port because the config does not reuse an existing server; the local retry count is zero while
CI uses two, so a CI-only three-attempt failure presents as a single failure locally and `--retries=2` is
what reproduces CI's shape; the dev server receives an empty transactional-mail key from the config's
`webServer.env` unless a named opt-in flag is set, and that flag must not be set because this machine's
`.env.local` holds a live key; and the visual project is not collected off Linux at all, so a visual
reproduction needs the pinned container and is plan 12's subject rather than a local run.

(c) PER-ITEM REPRODUCTION LINES — one for each of the four unresolved triage items, each naming the spec
path, the narrowest run command for it (including the file's own grep where its describe group is serial),
and the ONE observation that settles its verdict:
  - `e2e/skeleton-geometry.spec.ts:1807` — which table cell wrapped at 1280px, the When cell or the Space
    cell. Repeat the spec's own instruction verbatim: re-measure and move the constant, never the
    tolerance.
  - `e2e/hold-countdown.spec.ts:443` — whether, in the trace's DOM snapshot at the timeout, a day is
    selected in the grid and whether the 2:00 PM hour button is absent or present-but-disabled.
  - `e2e/host-headings.spec.ts:964` — what the wizard's location-step server action returned, and whether
    the same refusal happens locally.
  - `e2e/avatar-crop.spec.ts` and `e2e/overflow-320.spec.ts:3400` — whether the upload path succeeds
    locally at all, which is what distinguishes an environment gap from a broken spec.

(d) A CLOSING LINE stating that this file is the harness and not a verdict: no triage conclusion for any
of the fourteen failures is recorded here, and each repair plan writes its own `triage-<spec>.txt`
transcript.
  </action>

  <acceptance_criteria>
    - `evidence/triage-harness.md` exists and its sequence section names the database bring-up, the
      migration, the seed, a build-cache removal and a chromium-project invocation.
    - It contains four distinct warning entries, each naming a consequence rather than only a fact.
    - It contains one reproduction line for each of the four unresolved items, each naming a spec path
      under `e2e/` that resolves under `git ls-files`.
    - It records no triage verdict for any of the fourteen failures.
  </acceptance_criteria>

  <verify>
    <automated>grep -c 'e2e/skeleton-geometry.spec.ts' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-harness.md</automated>
    <fails_when>output is 0 — the harness note names no reproduction line for the host-bookings row-height item</fails_when>
    <automated>for f in e2e/skeleton-geometry.spec.ts e2e/hold-countdown.spec.ts e2e/host-headings.spec.ts e2e/avatar-crop.spec.ts e2e/overflow-320.spec.ts; do grep -q "$f" .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-harness.md || { echo "MISSING $f"; exit 1; }; git ls-files --error-unmatch "$f" >/dev/null || exit 1; done</automated>
    <fails_when>non-zero exit, or any line beginning "MISSING " in the output — a named spec is absent from the note or is not a tracked path</fails_when>
  </verify>

  <done>
    A single operational note on disk gives every later repair plan the exact bring-up sequence, the four
    environment traps, and the one observation that settles each unresolved verdict.
  </done>
</task>

<task type="auto">
  <name>Task 3: Probe the two open verdicts — the Phase 17 tabular-figure finding and the local avatar upload</name>

  <precondition>
    Docker Desktop is running with `fitout-db-1` up and port 3000 free, as in Task 1. The avatar half of
    this task drives a real browser against the local dev server.
  </precondition>

  <read_first>
    - `e2e/tabular-figures.spec.ts` around `:361` — the assertion and its full error message, including the
      sentence handing the finding to Phase 17 and the instruction not to strip any class in response.
    - `e2e/avatar-crop.spec.ts:322-329` — the file header stating that three cases perform a real upload
      and that the upload is unavoidable rather than convenient in each.
    - `tests/listing/photos.test.ts:434` — the sentence naming the absent-credential condition as the state
      every CI run is in.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Cause B" and §"Cause E", and Assumptions Log rows A3
      and the Open Questions section item 1.
    - `evidence/triage-harness.md` — written in Task 2; run its sequence before the avatar probe.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/tnum-phase17-probe.txt
  </files>

  <action>
PART 1 — the tabular-figure verdict. Search `.planning/` for the prior finding and for any Phase 17
artefact that resolves it: grep the planning tree for the font-feature term, for the assumption
identifier the spec's message cites, and for the font family name, across ROADMAP, REQUIREMENTS, the
Phase 17 directory and any SUMMARY that mentions a font change. Capture the grep command lines and their
unedited output into `evidence/tnum-phase17-probe.txt`. Then write a one-line VERDICT: either Phase 17
shipped a change that makes the assertion satisfiable — in which case `e2e/tabular-figures.spec.ts:361` is
a STALE SPEC and plan 11 must REPAIR it — or it did not, in which case the finding is still open and plan
11 annotates it with the reason the source already carries. Name the artefact the verdict rests on. If no
Phase 17 artefact exists at all, say that explicitly; absence is a finding, not a blank.

PART 2 — the local avatar verdict. Run the Task 2 bring-up sequence, then run
`npx playwright test e2e/avatar-crop.spec.ts --project=chromium --reporter=list` and capture the command
line and unedited transcript into the same evidence file. Write a second VERDICT line: if the three cases
at `:974`, `:2675` and `:2747` pass locally, the CI failures are an environment-capability gap and plan 10
chooses between a request intercept, a locally-served asset and an annotation; if they fail locally too,
they are broken specs and plan 10 REPAIRS them. Quote the failing or passing lines the verdict is read
from.

Record ONLY names, counts and outcomes. Do not print any credential value, any environment-variable value
or any signed URL into the evidence file — the file is about to be published, and the rule
`scripts/refuse-mail-credential.mjs` states about its own diagnostics binds every diagnostic this phase
writes.
  </action>

  <acceptance_criteria>
    - `evidence/tnum-phase17-probe.txt` contains at least one grep command line over `.planning/` and its
      unedited output.
    - It contains a line beginning `VERDICT` for the tabular-figure question that names either a Phase 17
      artefact path or the explicit absence of one.
    - It contains a Playwright command line for `e2e/avatar-crop.spec.ts` and its unedited transcript with
      a summary block.
    - It contains a second line beginning `VERDICT` for the avatar question, quoting the transcript lines
      it is read from.
    - The file contains no value of any environment variable and no signed upload URL.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^VERDICT' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/tnum-phase17-probe.txt</automated>
    <fails_when>output is less than 2 — one or both open verdicts were not recorded</fails_when>
    <automated>grep -ciE 'api_secret|api_key=|sk_(test|live)_|BETTER_AUTH_SECRET=|PAYMONGO_SECRET_KEY=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/tnum-phase17-probe.txt || true</automated>
    <fails_when>output is any number other than 0 — a credential-shaped value reached an evidence file that is about to be published</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
  </verify>

  <done>
    Both open verdicts are on disk with the evidence they rest on, so plans 10 and 11 branch on a
    measurement rather than on RESEARCH.md's hypothesis.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.env.local` → the dev server started by Playwright | This machine holds live third-party keys; a triage run boots a real server with a real environment. |
| triage output → `.planning/evidence/` → the public repository | Everything written here is published by D-07/D-08 at the end of the phase. |
| the scratch spec → the chromium project's spec glob | A file left under `e2e/` silently joins the gate this phase is making meaningful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-07 | Information Disclosure | `evidence/tnum-phase17-probe.txt` and `evidence/testfail-behaviour.txt` | high | mitigate | Transcripts record names, counts and outcomes only. Task 3's verify greps the file for credential shapes and fails on any hit, before D-07 publishes the directory. |
| T-19.1-08 | Information Disclosure | outbound transactional email from a triage run | high | mitigate | The real-mail opt-in flag is prohibited by this plan; the config's injected empty key is left in place, and the prohibition is stated at the top of the harness note so later plans inherit it. |
| T-19.1-09 | Tampering | `e2e/__scratch-known-failure.spec.ts` left behind | medium | mitigate | Task 1 deletes it and the verify asserts its absence; a collected-count comparison catches a rename. |
| T-19.1-10 | Repudiation | a triage verdict with no evidence behind it | high | mitigate | Every verdict line quotes the transcript line it is read from; a verdict with no quote is not a verdict. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs. RESEARCH.md's Package Legitimacy Audit records zero proposed packages; if a later task reaches for an image-diff or reporter package, the legitimacy gate runs then. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New evidence files:**
`evidence/testfail-behaviour.txt` (two transcripts plus a four-answer verdict section),
`evidence/triage-harness.md` (the bring-up sequence, four warnings, four per-item reproduction lines),
`evidence/tnum-phase17-probe.txt` (two `VERDICT` lines with their evidence).

**Transient file, created and deleted within Task 1:** `e2e/__scratch-known-failure.spec.ts`.

**New production symbols:** none. **New npm scripts:** none. **No source file is modified.**
</artifacts_this_phase_produces>

<verification>
- Three evidence files exist under the phase `evidence/` directory, each containing raw command lines and
  unedited output rather than prose summaries.
- `e2e/__scratch-known-failure.spec.ts` is gone and the chromium project's collected spec-file count is
  unchanged.
- No credential-shaped string appears in any evidence file this plan writes.
- `node scripts/verify-workflows.mjs` is still green and `git status --porcelain` reports no change under
  `src/`, `scripts/` or `.github/`.
</verification>

<success_criteria>
Plans 07 through 11 can each open with a reproduction rather than a hypothesis: the expected-failure
mechanics are measured, the tabular-figure verdict is recorded with the artefact it rests on, the avatar
verdict is recorded with the transcript it is read from, and the bring-up recipe is one file away.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-02-SUMMARY.md` when done.
State the two verdicts in the summary body so plans 10 and 11 do not have to open the evidence file to
learn which branch they are on, and state whether the retry answer changes plan 13's wall-clock budget.
</output>
