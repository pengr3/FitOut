---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 18
type: execute
wave: 10
depends_on: ["19.1-17"]
files_modified:
  - e2e/helpers/booker-seed.ts
  - e2e/hold-countdown.spec.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
autonomous: true
requirements: [CI-01]
gap_closure: true

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC4 — the checkout path's day selection lands on the runner. The failure is green on this laptop over the whole file at the runner's own worker and retry counts and red on the runner, which is precisely the claim shape the pinned image exists to settle; two plans this phase used it and both changed their verdict."
    - "CI-01 / SC4 — the reproduction happens inside `mcr.microsoft.com/playwright:v1.60.0-noble` BEFORE any repair, and 19.1-08's refusal to add a retry on a mechanism that merely FITS is inherited rather than revisited. A retry is added only if the runner's own call log names the clicked node's detachment. If it does not, no retry is added and the verdict says so — an unobserved retry makes the next red unreadable."
    - "CI-01 / SC4 — the helper's existing post-condition stays and is not weakened. It already turns a lost day click from a three-minute timeout on an unrelated locator into a named failure in about a second, and that conversion is the reason the failure is now legible at all. Any repair sits beside it, never in place of it."
    - "CI-01 / SC4 — the tests the file's serial ordering could not reach are named individually, by file and FULL TITLE, before and after. The file is serial at file scope, so one failure stops everything declared after it, and the released set is what the criterion actually buys."
    - "CI-01 adjacency (spec-less edge probe, verification: explicit) — the day click and its observable effect are adjacent in time and were measured 683 milliseconds apart, with the helper returning before its own effect existed. The plan measures that interval again in the container rather than assuming the laptop's figure transfers: a two-core runner under full-suite load is the condition the number has to be true of, and the interval is what decides whether the existing two-second budget is the right one."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — the container reproduction is ordered before the repair, and the whole-file run is ordered before the single-case run, because the failure's own position in a file-scoped serial group is part of what is being measured. Running the one case alone would report a green that says nothing about the group."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — no assertion here may pass against a calendar that never rendered. The helper's day locator is already an intersection of three conditions, and an intersection that matches nothing is a silent zero rather than a failure; the plan therefore keeps a positive count assertion on the enabled in-month day before the click, so an empty grid fails as an empty grid."
    - "CI-01 encoding (spec-less edge probe, verification: explicit) — the selected-day attribute is READ off the rendered document and off the component, never guessed. The library emits no aria-selected here and the styling hook is a data attribute whose exact spelling was dumped from thirty-three live day buttons; the accessible name also gains a suffix on selection, which the label matcher survives only because it is unanchored. Any new matcher in this plan is checked against both facts."
    - "CI-01 — the money-path spec's own copy of the day-selection walk still lacks this post-condition, and that divergence is written down with a disposition rather than silently widened. One spelling of a walk in two files is a known drift surface; the plan records which file owns which copy and why the second was not edited here."
    - "CI-01 / SC4 — THIS IS THE PLAN WHERE `gate-e2e` HAS TO COME BACK GREEN ON A REAL RUN. It is the last of the three e2e-repair plans, so the functional half of SC4 rests here: a named, push-triggered `ci` run, selected by the pushed head SHA, in which the `gate-e2e (functional Playwright suite)` job's own conclusion reads success and none of the five specs the three plans own appears in its failure list. Every measurement before this one is local or containerised, and this phase has twice watched a laptop verdict and a runner verdict disagree."
    - "CI-01 / SC4 (D-04) — all five job conclusions are recorded from that one run, because \"not red on every push\" is a property of the RUN and not of one job. `gate-visual` is expected still red here and that is stated rather than smoothed: plan 19 owns it, and until it mints, SC4 is closed on its functional half only. This plan does not claim the whole criterion."
    - "CI-01 / SC4 — a `cancelled` conclusion is discarded by identity and counted, never read as green or red. The concurrency group cancels in progress on the next push and two runs in this phase (`33971557439`, `33971883942`) already died that way. Per D-06 the push needs no approval: `dev` is unprotected by decision and `ci` runs on every push to it so the result is visible."
    - "Nothing in this plan annotates. The failure is on the checkout path a booker takes to pay, its two blocked siblings include the countdown announcement, and the allowlist stays pinned at two entries."
  artifacts:
    - e2e/helpers/booker-seed.ts
    - e2e/hold-countdown.spec.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
  key_links:
    - "the day-selection helper → the hour-chip click in the window picker → the hold placement → the checkout the countdown cases measure. A day that never became selected renders no hours, so the failure surfaces three minutes later on an unrelated locator in a different helper — which is what the post-condition already converted into a named one-second failure."
    - "the file's file-scoped serial configuration → the two cases declared after the failing one. One red stops them executing, so the failure's cost is not one test but three."
    - "the sibling trigger helper in the same file → the precedent for a retried click. Its retry exists because its lost click was OBSERVED with the runner's own call log naming the detachment; that observation is the condition this plan has to meet before copying the shape."
    - "the preceding plan's reading of a second copy of a booking surface in one document → this helper's own docblock, which cites a measurement of the listing route streaming a pending shell and a resolved copy of the very subtree the day button lives in. If that reading closed the class, this failure may already be gone on arrival, and Task 1's first run is what says so."
    - "the pushed head SHA → the `ci` run identity → the `gate-e2e (functional Playwright suite)` job's own conclusion → the functional half of SC4. This is the last link in the chain the three e2e-repair plans form: 16 and 17 each recorded their own owned specs absent on their own run, and this run is the one that has to read the job itself as success. `gh run list --commit <sha>` is what makes it provably this tree's run rather than whichever run the concurrency group left standing."
  prohibitions:
    - "No retry may be added on a mechanism that merely fits the evidence. The condition is the runner's own call log naming the clicked node's detachment, and it is inherited from the plan that refused the same retry for the same reason."
    - "The existing selected-day post-condition may not be removed, weakened, or given a longer budget in order to make a red go away. Its budget may move only if the container measures the interval and the transcript carries both numbers."
    - "The hour-chip helper's four named grid states may not be collapsed back into one message. Each names a different likely cause in a different file, and the distinction was bought by a measurement that the discriminator everyone expected could not make."
    - "Neither the failing case nor either of its blocked siblings may be recorded in the known-failures allowlist. This is the path a booker takes to pay, and the pinned entry total does not move in this plan."
    - "The money-path spec's own copy of the day-selection walk may not be edited as a side effect of this plan. If it should carry the same post-condition, that is a separate decision with its own watched red, and it is recorded rather than performed."
  assumptions:
    - "SPEC-LESS EDGE PROBE — authored by hand in plan 19.1-01's shape. All four items are RESOLVED with verification: explicit and appear above as plain truths: adjacency (the interval between the click and its observable effect, re-measured in the container), ordering (container before repair; whole file before single case), empty (a positive count on the enabled in-month day so a matched-nothing intersection fails loudly), encoding (the selected-day attribute read off the document and the component, and the unanchored label matcher's survival of the selection suffix). Zero items dismissed, zero silent drops."
    - "PROHIBITION PROBE — five bespoke must-NOTs above, descriptor-less. Routine candidates dropped in plan 01's shape (must not leave a throwaway config behind; must not hard-code a newline). The allowlist prohibition is restated rather than cited because a timeout-shaped failure is exactly the kind that gets quarantined, and this phase already measured that an expected-failure annotation does not cover a timeout at all."
    - "THE FAILURE MAY BE GONE ON ARRIVAL. The preceding two plans repaired a regenerated tree on the host composition and a duplicated element on the booker surfaces, and this helper's own docblock cites the same class on the listing route. A first container run that is green is a legitimate outcome; it is recorded with the run identity and the tree, and the plan then closes on the released siblings and the standing proof rather than inventing a repair."
    - "THE BLOCKED COUNT IS AN ESTIMATE. Plan 13 recorded two siblings blocked by this case's position. The file is serial at file scope and carries further describes after the two named, so the run's own not-run list is authoritative and any delta is written down."
    - "ASSUMPTION DELTA — primary noun UNCHANGED, decision `no-change`. The subject is the same helper and the same spec plan 13 named."
---

<objective>
Settle the day click that does not select on the checkout path — the failure that is green on this laptop
and red on the runner, and whose position in a file-scoped serial group stops the countdown cases from
executing at all.

Its first step is named and inherited: reproduce inside `mcr.microsoft.com/playwright:v1.60.0-noble` before
touching anything, because 19.1-08 refused to add a retry without a trace showing the click genuinely lost,
and that refusal is carried forward rather than re-argued.

Purpose: this is the last of the four `gate-e2e` failures 19.1-13 left with a named owner, and it sits on
the path a booker walks to pay.

Output: a container reproduction with the runner's own call log, a repair justified by what that log named
or a written verdict that no repair is warranted, the released siblings listed by name, a proof that the
guard still fails when the day genuinely cannot be selected, and — last — a named, real, push-triggered
`ci` run in which the `gate-e2e (functional Playwright suite)` job's own conclusion reads success.

That last task is where the functional half of SC4 actually closes. This is the last of the three
e2e-repair plans: 16 and 17 each read their own real run for the specs they owned, and this one has to read
the JOB green, with all five specs the three plans own absent from its failure list. `gate-visual` stays red
here by construction — plan 19 owns it — so this plan closes SC4's functional half and says so, and claims
nothing about the other half.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-08-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-09-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-13-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — reproduce the lost day click in the runner's own image, then close one path through to a live checkout</name>

  <read_first>
    - `e2e/helpers/booker-seed.ts` — the day-selection helper in full: the scope parameter's stated reason,
      the three-condition intersection that picks the enabled in-month occurrence, the post-condition
      19.1-08 added with its measured 683-millisecond interval, its explicit refusal to retry and the
      condition under which a retry would become justified, and the note that the selected-day attribute was
      read off thirty-three live day buttons rather than guessed. Then read the sibling trigger helper below
      it — the one that DOES retry — and its call-log excerpt naming the detachment.
    - `e2e/helpers/booker-seed.ts` — the window-picker function that calls the day helper, the hour-chip
      helper's four named grid states, and the timezone-note assertion that waits for the streamed boundary
      to resolve to one.
    - `e2e/hold-countdown.spec.ts` — the file-scoped serial configuration, the checkout-reaching helper, the
      header-stability describe that carries the failing case, and the two describes declared after it.
    - `.planning/phases/19.1-.../deferred-items.md` — the container reproduction harness recipe in the host
      geometry entry: host-side dev server and database, the pinned image, an in-container loopback
      forwarder because a helper hardcodes the localhost origin, and a throwaway config removing only the
      web-server block. Do not flip the reuse-existing-server setting.
    - `.planning/phases/19.1-.../evidence/triage-hold-countdown.txt` — the canonical run, the four probes,
      the three hypotheses recorded as dead, and the finding that the discriminator everyone nominated could
      not separate the two live hypotheses.
    - `.planning/phases/19.1-.../19.1-17-SUMMARY.md` — whether the duplicated-element class on the booker
      surfaces was closed, and on which reading. This helper's docblock cites the same class on the listing
      route.
  </read_first>

  <files>
    e2e/helpers/booker-seed.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt
  </files>

  <action>
STEP 1 — BUILD THE HARNESS, THROWAWAY, AND ASSERT THE TREE IT MEASURES.

Follow the recipe in `deferred-items.md`. Build it, use it, delete it in this same task, and prove the
deletion by comparing the collected test count before and after the throwaway files existed.

Before believing any run, record what it measured: the porcelain status over the source, spec and test
directories is empty, and the checked-out head is captured. Write, at column zero, in
`evidence/triage-day-click-container.txt`: `IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble`,
`TREE=<head sha>`, and `CONTAINER-EXIT=<code>` for every container invocation.

STEP 2 — RUN THE WHOLE FILE IN THE CONTAINER, AT THE RUNNER'S OWN SHAPE, WITH TRACING ON.

One worker, two retries, trace on. The whole file, not the one case: the failure's cost is its position in
a file-scoped serial group, and a single-case run reports a green that says nothing about the group.

Record, per attempt, one line beginning `ATTEMPT ` carrying the attempt number, the file and the FULL TITLE
of each case and its outcome. Then the two questions that decide everything below:

  - Did the day click LAND? Record `SELECTED=<yes|no>` from the helper's own post-condition, and if no,
    record the failure message verbatim under a `POSTCOND-BEGIN` / `POSTCOND-END` pair.
  - Was the click LOST, or refused? Copy the runner's own call log for the click verbatim under a
    `CALLLOG-BEGIN` / `CALLLOG-END` pair, and record `DETACHED=<yes|no>` according to whether that log
    names the element being detached or the click being retried against a moving node. This single word is
    the condition 19.1-08 set for a retry and it is the whole reason this task exists before any repair.

Also re-measure the interval the laptop measured at 683 milliseconds: from the helper's click returning to
the day carrying the selected attribute, and from there to the first hour chip existing. Record
`INTERVAL-SELECTED-MS=` and `INTERVAL-HOURS-MS=`. The existing post-condition budget is two seconds; those
two numbers are what say whether that budget is right on a two-core runner.

If the file is GREEN in the container on the first run, that is a legitimate outcome and it is recorded as
one: write `REPRODUCED=no` with the run's identity and tree, state that the preceding plans may have closed
it, and go straight to Task 2's before-and-after accounting. Do not invent a repair for a failure that no
longer reproduces.

STEP 3 — REPAIR ONLY WHAT THE LOG JUSTIFIES, AND CLOSE ONE PATH END TO END.

If `DETACHED=yes`: add the retried-click shape this repository already documents for a lost click on a
streamed surface, in the sibling trigger helper's idiom — poll the click until the post-condition holds,
with the existing post-condition as the polled predicate rather than a bare re-click, and with the docblock
stating that the retry is now warranted, quoting the call log that warranted it and naming this transcript.
The guard is not weakened by the retry: a day that opens nothing still fails, with this helper's own message
rather than a generic locator timeout — that clause is copied from the sibling and it is Task 3's proof.

If `DETACHED=no` and `SELECTED=no`: the click was received and refused, which is a different defect. Record
a verdict line beginning `VERDICT ` naming the surviving cause and the file that owns it, and repair there.
Do not add a retry.

If `SELECTED=yes` and the case still failed: the day selection is not the cause at all. Record the verdict,
name what the attempt actually failed on, and repair or hand on accordingly.

Whichever branch: if the post-condition's budget moves, the transcript carries both measured intervals and
the new number is derived from them, never chosen. Re-run the whole file in the container and record the
paired `AFTER` attempt lines with a fresh `CONTAINER-EXIT=`.
  </action>

  <acceptance_criteria>
    - `evidence/triage-day-click-container.txt` contains at least two lines beginning
      `IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble` and at least two beginning `CONTAINER-EXIT=`.
    - The same file contains exactly one line beginning `REPRODUCED=`, exactly one beginning `DETACHED=` and
      exactly one beginning `SELECTED=`.
    - The same file contains a `CALLLOG-BEGIN` line and a `CALLLOG-END` line with at least one line between
      them.
    - The same file contains one line beginning `INTERVAL-SELECTED-MS=` and one beginning
      `INTERVAL-HOURS-MS=`, both carrying a number.
    - If a retry was added, the helper's docblock quotes the call log that warranted it; if none was added,
      the file carries a line beginning `VERDICT ` saying why.
    - The existing selected-day post-condition is still present and its failure message still names the day
      and the attribute.
    - `git status --porcelain` shows no throwaway container config, forwarder script or scratch copy.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/hold-countdown.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" count, or any test is reported as "did not run", or the output contains a line beginning "Test timeout of"</fails_when>
    <automated>grep -c '^DETACHED=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is any number other than 1 — the condition 19.1-08 set for a retry was either never answered or answered twice</fails_when>
    <automated>grep -cE '^INTERVAL-(SELECTED|HOURS)-MS=[0-9]+$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is less than 2 — one of the two intervals was not measured on the runner's image, so the post-condition budget rests on a laptop figure</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>any line whose path is not under .planning/ and whose status is "??" — a throwaway harness file was left behind</fails_when>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
    <fails_when>output is greater than 7 — a new type error was introduced beyond the seven pre-existing ones in tests/design/</fails_when>
  </verify>

  <done>
    The whole spec has been run in the pinned image at the runner's own shape with tracing on, the call log
    is on the record and answers in one word whether the click was lost, the two intervals are measured on
    that image rather than inherited from this laptop, and either a repair justified by that log is in place
    or a verdict says why none was warranted — with a live checkout reached end to end either way.
  </done>

  <reversibility rating="reversible">
    The change is one helper's click shape in one test-side file, in a single commit. No production source
    and no workflow is touched, and the pre-change bytes are captured in the same task.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Account for the siblings the serial group could not reach, and record the divergent second copy of the walk</name>

  <read_first>
    - `e2e/hold-countdown.spec.ts` — every describe in the file and its declaration order, so the not-run set
      can be read against the group's own structure rather than against an expectation.
    - `e2e/helpers/booker-seed.ts` — the header's copy note stating that the day-selection walk exists
      verbatim in the money-path spec and that the post-condition was deliberately not copied back into it,
      with the reason.
    - `e2e/price-parity.spec.ts` — its own copy of the day-selection walk, and the settle plan 19.1-13 added
      before the category click with the byte offsets and the transcript recorded at the site. Read it; do
      not edit it.
    - `.planning/phases/19.1-.../19.1-13-SUMMARY.md` — the pattern that two runs are compared on file and
      full title and never on `file:line`, and the reason: six specs moved their line numbers inside this
      phase.
    - `.planning/phases/19.1-.../deferred-items.md` — the entry format: what was found, where, why it was not
      fixed here, the suggested disposition, and a severity.
  </read_first>

  <files>
    e2e/hold-countdown.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
  </files>

  <action>
STEP 1 — THE RELEASED SET, BY NAME.

From Task 1's before and after container runs, compute the set of cases that did not run before and did run
after, keyed on file and FULL TITLE. Write one line beginning `RELEASED ` per case, in run order, and one
line beginning `REGRESSED ` per case that ran before and did not after. Close with `RELEASED-TOTAL=` and a
line beginning `DELTA-VS-13=` comparing the count against the two siblings plan 13 recorded, stating what
accounts for any difference. The run wins; the earlier figure is not edited into agreement.

For every released case, record its first-attempt outcome and its position in declaration order — a case
that only passes on a retry, or only when it runs at a particular position, is a different finding from a
case that simply now executes, and this phase has already recorded one such distinction.

STEP 2 — SAY AT THE SITE WHAT A RED HERE NOW MEANS.

Add a comment block at the failing case in `e2e/hold-countdown.spec.ts` recording what the container
measured, which of the three branches Task 1 took, and what a future red at this row does and does not
imply. Follow the shape plan 19.1-10 used on the photos-step row: what was measured, why the row's own
earlier accusation was wrong or incomplete, and what the correct response is. Change no assertion, no
constant and no fixture in this step.

STEP 3 — RECORD THE DIVERGENT SECOND COPY, DO NOT WIDEN IT.

The money-path spec carries its own copy of the day-selection walk without the post-condition, by an
explicit decision recorded in the helper's header. If Task 1 established that the click can be lost on the
runner, that spec has the same exposure with none of the instrumentation — and it is the one functional spec
that has been a gate since before this phase.

File it in `deferred-items.md` in that file's own entry format: found during this plan, the exact divergence,
the measurement that makes it matter, why it is not fixed here (the walk lives in two files by a recorded
decision, editing the money-path gate spec's walk needs its own watched red, and this plan's subject is the
checkout path's helper), a suggested disposition, and a severity. Do not edit that spec.

STEP 4 — RUN THE WHOLE FILE ON THIS BOX TOO.

The laptop run is not the verdict, but a regression introduced here would show up on it first and cheapest.
Run the whole spec at one worker and record the transcript with an `EXIT=` marker.
  </action>

  <acceptance_criteria>
    - `evidence/triage-day-click-container.txt` contains exactly one line beginning `RELEASED-TOTAL=` and
      exactly one beginning `DELTA-VS-13=`.
    - Every line beginning `RELEASED ` carries a spec path ending in `.spec.ts` and a quoted full title, and
      records a first-attempt outcome.
    - `e2e/hold-countdown.spec.ts` gained a comment block at the previously failing case, and
      `git diff -U0` over that file shows every added and removed line beginning with a comment marker.
    - `deferred-items.md` gained one entry naming the money-path spec's divergent copy of the walk, carrying
      a suggested disposition and a severity.
    - `e2e/price-parity.spec.ts` is byte-unchanged.
    - `npx playwright test e2e/hold-countdown.spec.ts --project=chromium --workers=1` reports zero failed,
      zero flaky and zero not-run.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/hold-countdown.spec.ts e2e/price-parity.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count, or any test is reported as "did not run"</fails_when>
    <automated>git diff --exit-code -- e2e/price-parity.spec.ts</automated>
    <fails_when>non-zero exit — the money-path spec was edited, which this plan records rather than performs</fails_when>
    <automated>git diff -U0 -- e2e/hold-countdown.spec.ts | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*[/*]' || true</automated>
    <fails_when>output is any number other than 0 — a non-comment line changed in the spec during a task whose only edit there is a recorded measurement</fails_when>
    <automated>grep -c '^RELEASED ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is 0 and the same file's REPRODUCED line reads yes — a reproduced failure was repaired but no case was recorded as released, which cannot both be true</fails_when>
  </verify>

  <done>
    Every case the serial group could not reach is named individually with its first-attempt outcome and its
    declaration position, the total carries a stated delta against plan 13's figure, the failing row now
    records what the container measured and what a future red there means, and the money-path spec's
    divergent copy of the walk is filed with a disposition rather than quietly widened.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the guard both ways — a lost click is recovered, an unselectable day still fails</name>

  <behavior>
    - When the day button is replaced under the pointer once and then settles, the helper reaches a selected
      day and the caller proceeds — if and only if Task 1 added a retry.
    - When the day can never become selected, the helper fails at its own line within its own budget, with
      its message naming the day and the attribute — not three minutes later on an hour-chip locator in a
      different helper.
    - When the calendar renders no enabled in-month day at all, the failure names the empty grid rather than
      reporting a matched-nothing intersection as a lost click.
    - The hour-chip helper still distinguishes its four grid states, each naming its own likely cause.
  </behavior>

  <read_first>
    - `e2e/helpers/booker-seed.ts` — the sibling trigger helper's clause that the guard is not weakened by
      the retry: a trigger that opens nothing still fails with the function's own message rather than a
      generic locator timeout. That clause is the property this task proves for the day helper.
    - `.planning/phases/19.1-.../19.1-08-SUMMARY.md` — how that plan watched the guard red when the original
      failure did not reproduce, by making a press that could never be received, and its requirement that
      the evidence state the red simulates the state rather than the cause.
    - `.planning/phases/19.1-.../evidence/eol-mutation-builders.txt` — the record of a mutation that silently
      did not apply, and why every mutation below carries an applied count.
    - This plan's own `evidence/triage-day-click-container.txt` — which branch Task 1 took, since that is what
      decides whether the first behaviour above is in scope or is recorded as not applicable.
  </read_first>

  <files>
    e2e/helpers/booker-seed.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt
  </files>

  <action>
STEP 1 — WATCH THE GUARD RED, IN BOTH DIRECTIONS THAT MATTER.

From a throwaway probe under the spec directory, deleted in this same task:

  (a) THE UNSELECTABLE DAY. Make the selection impossible without removing the click — intercept the
      selection so the attribute never lands — and run a case that calls the helper. Record the failure's
      elapsed time, the line it fired at, and its message. It must fire at the helper's own line inside its
      own budget, and its message must name the day and the attribute. Record `GUARD=red` with the elapsed
      milliseconds.
  (b) THE EMPTY GRID. Remove every enabled in-month day and run the same case. Record what the failure
      names; if it reports a lost click rather than an empty grid, that is a defect in the helper's own
      reporting and it is repaired here, because a matched-nothing intersection is a silent zero and the
      whole point of the post-condition was to stop one failure wearing another's message.
  (c) THE RECOVERED CLICK — only if Task 1 added a retry. Replace the day button once immediately after the
      first click and let it settle. Record `RECOVERED=yes` with the attempt count the poll took. If no
      retry was added, record `RECOVERED=n/a` with the branch Task 1 took as the reason.

Every probe records `APPLIED=<count>` read back from the document, so a probe that silently did not apply
cannot be mistaken for a result. Delete the probes in this task and prove it with a collected-count
comparison before and after.

STEP 2 — THE FOUR GRID STATES STAY FOUR.

Drive the hour-chip helper into each of its four named states and record which message each produced, one
line per state beginning `STATE `. This is the anti-regression proof for the distinction 19.1-08 bought
after finding that the observation everyone nominated could not make it: a helper that collapses back to one
message would send the next reader to the wrong file.

STEP 3 — CLOSE THE TRANSCRIPT.

Append a closing section carrying: the branch Task 1 took and why; the two measured intervals; the guard's
two reds with their elapsed times; the recovery result or its not-applicable reason; the four grid states;
the released set and its total; and one line beginning `SC4-TERM=` stating in one sentence what this plan
closed and what it did not.
  </action>

  <acceptance_criteria>
    - `evidence/triage-day-click-container.txt` contains at least two lines beginning `GUARD=red`, each with
      an elapsed-millisecond figure, and each paired with an `APPLIED=` line whose value is greater than
      zero.
    - The same file contains exactly one line beginning `RECOVERED=` whose value is `yes` or `n/a`, and if
      `n/a` the same line or the one below it names the branch Task 1 took.
    - The same file contains exactly four lines beginning `STATE `.
    - The same file contains exactly one line beginning `SC4-TERM=`.
    - The helper's selected-day post-condition is present, and its failure message names both the day label
      and the attribute.
    - `git status --porcelain` shows no throwaway probe left behind, and the collected test count matches the
      count recorded before the probes existed.
    - `npm test` and `npm run test:design` both pass.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/hold-countdown.spec.ts e2e/collision-in-place.spec.ts e2e/one-tree.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count — the three specs that reach the day helper must all still pass, because a change to a shared helper is measured across its callers and not just its own spec</fails_when>
    <automated>grep -c '^GUARD=red' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is less than 2 — the guard was shown to fail in fewer than the two directions this task requires (an unselectable day and an empty grid), so a repair that weakened it would go unnoticed</fails_when>
    <automated>grep -c '^APPLIED=0$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is greater than 0 — a probe recorded zero nodes actually changed, so its result measured an unmutated document</fails_when>
    <automated>grep -c '^STATE ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt</automated>
    <fails_when>output is any number other than 4 — the hour-chip helper's four named grid states were not each driven and recorded, so a collapse back to one message would pass unnoticed</fails_when>
    <automated>npm run test:design</automated>
    <fails_when>non-zero exit, or any failed file in the reporter's summary, or a passing total below 1431</fails_when>
  </verify>

  <done>
    The day helper is proved to fail in both directions that matter — an unselectable day fails at its own
    line inside its own budget naming the day and the attribute, and an empty grid fails as an empty grid —
    the recovery result is recorded or explicitly marked not applicable with the branch that made it so, the
    hour-chip helper's four states are each driven and named, and every probe carries a non-zero applied
    count with the throwaway files gone.
  </done>
</task>

<task type="auto">
  <name>Task 4: Close the functional half of SC4 — `gate-e2e` reads success on a REAL push-triggered `ci` run</name>

  <read_first>
    - `.planning/phases/19.1-.../19.1-16-PLAN.md` and `19.1-17-PLAN.md`, their terminal tasks — the convention
      this task completes: push, join the run to the pushed head SHA rather than to recency, discard a
      cancelled conclusion by identity, record all five job conclusions, and give each owned spec a one-word
      verdict against the run's failure list. This task adds the one thing neither of those could claim: the
      job's own conclusion.
    - `.planning/phases/19.1-.../evidence/ci-run-after-16.txt` and `evidence/ci-run-after-17.txt` — the two
      previous waves' readings. Their `OWNED ` lines are the four specs that must still be absent here, their
      `STILL-RED ` lines predicted what this wave was left to remove, and their `COLLECTED ` figures are what
      this run's total is compared against.
    - `.planning/phases/19.1-.../19.1-13-PLAN.md` Task 3 and `evidence/suite-remeasurement.txt` — the closure
      section's own shape, and the commands beneath each block: the
      `gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url` identity
      read, the `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
      per-job read, and the `gh run view --job <jobId> --log` line that reaches the functional job's own output.
      Read the `COLLECTED` and `WALLCLOCK` blocks: a shortfall in the collected total looks identical to tests
      passing if only the failure count is read, and the ninety-minute cap is not raised by this plan either.
      Read the `HANDOFF` line — plan 15 takes its required-check context strings and its merge wait from it.
    - `.github/workflows/ci.yml` — the five `name:` strings; `gate-e2e (functional Playwright suite)` is the
      exact display name whose conclusion this task turns on, and it is also the required-status-check context
      D-05 names. And the `concurrency:` block, keyed on workflow, ref and event with `cancel-in-progress: true`.
    - `.planning/ROADMAP.md` §"Phase 19.1" — SC4's exact wording, so the closure is stated against the criterion
      rather than against a summary of it; and Waves 8–11, for why each of these plans is its own wave.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-02, D-04, D-05 and D-06 — D-02, that a real bug is repaired
      rather than pushed behind a green check, which governs what may be written under an open verdict; D-04,
      that the criterion is a property of the RUN, which is why `gate-visual` is reported here even though plan
      19 owns it; D-05, that `gate-e2e` is one of the four blocking contexts, which is why its display name is
      recorded verbatim; D-06, that `dev` is unprotected and pushed to directly, which is why no checkpoint
      guards this push.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
  </files>

  <action>
This is the wave's ONE push, and it is the reading the three e2e-repair plans exist to make possible.

STEP 1 — PUSH, AND RECORD THE HEAD THE RUN WILL BE JOINED TO.

Push the development branch with every commit this plan produced. Create `evidence/ci-run-after-18.txt` and
write, at column zero, one line `CI-HEAD-SHA=<sha>` carrying the full forty-character SHA that is now the
remote head, with this push's commit subjects beneath it.

STEP 2 — SELECT THE RUN BY THAT SHA, NOT BY RECENCY.

`gh run list --workflow ci.yml --commit <sha> --json databaseId,headSha,event,status,conclusion,createdAt --limit 10`.
If this build of the CLI does not accept the commit filter, fall back to
`gh run list --workflow ci.yml --branch dev --json databaseId,headSha,event,status,conclusion,createdAt --limit 20`
and select on head-SHA equality. Position in the list decides nothing.

Wait for the selected run to leave the in-progress state — the functional job took twenty-six minutes at
19.1-13's last reading, so this is a wait, not a poll-once — then read its identity with
`gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url`, the command
line above its JSON. Record `CI-RUN-ID=<id>`, `CI-EVENT=<event>` and `CI-CONCLUSION=<conclusion>` at column
zero. The event must be the push event.

STEP 3 — A CANCELLED RUN IS NOT A RESULT.

If the selected run concluded cancelled, the concurrency group killed it and it measured nothing; reading it
as green or red would manufacture a verdict out of an interruption. Record its id on a line beginning
`DISCARDED-RUN ` with the reason, then measure again — `gh run rerun <id>` for the same SHA, or push this
plan's next commit and select the run for the new head SHA. The join key stays the head SHA and the new
identity replaces the discarded one in `CI-RUN-ID=`. Close with exactly one line `CANCELLED-DISCARDED=<n>`;
zero is a legitimate value and it still gets written.

STEP 4 — THE FIVE JOBS, AND THE ONE CONCLUSION THIS PLAN TURNS ON.

Read every job with `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
and write one line per job beginning `JOB ` at column zero with the display name, its conclusion and its
duration. All five, per D-04.

Then `gh run view --job <jobId> --log` for the functional job, and record its summary block verbatim —
failed, flaky, skipped, did not run, passed, wall-clock — followed by any remaining failing specs and titles.
Write one line `GATE-E2E=<conclusion>` at column zero, taking the value from the job read rather than from
the run's overall conclusion: the run may be red on `gate-visual` while this job is green, and conflating the
two is exactly the error D-04's "property of the run" language is usually misread into.

Then FIVE lines beginning `OWNED ` at column zero, each carrying a spec path, a vertical bar, and one of the
two words — absent, if no case from that file appears in this run's failure list; present, if any does:
`e2e/hold-countdown.spec.ts`, the one this plan owns, plus `e2e/host-headings.spec.ts`,
`e2e/overflow-320.spec.ts`, `e2e/confirmation-decay.spec.ts` and `e2e/avatar-crop.spec.ts` — the four the two
previous waves closed, which must all still be absent. A wave that closes one failure by reopening another is
a net of nothing, and the only instrument that can see it is a run that executes all five.

Also record one line beginning `COLLECTED ` with this run's total, the figure in `evidence/ci-run-after-17.txt`
and the delta, one line beginning `NOT-RUN=` with the count of tests the run reported as never executed, and
one line beginning `WALLCLOCK ` with the functional job's duration and its headroom against the ninety-minute
cap. The cap is not touched by this plan.

If `gate-e2e` does not read success, THIS TASK IS NOT FINISHED and the plan does not claim SC4's functional
half. Every remaining failure gets a line beginning `VERDICT ` naming what it measured, the file that owns it,
and whether it is in this plan's scope to repair. Under D-02 what is in scope is repaired here and pushed
again — recording the superseded run identity beside the new one — and what is not is proposed as a further
plan in the D-02 shape with what it owns and its first instrument. Nothing is annotated: the allowlist stays
pinned at two entries, and a plan whose subject is the checkout path a booker walks to pay is not the plan
that quarantines it.

STEP 5 — STATE THE CLOSURE AGAINST SC4'S OWN WORDING, AND HAND ON THE HALF THAT IS NOT THIS PLAN'S.

Write a closure section addressing, against the criterion's exact text in the ROADMAP: whether the run is
red and if so which job and why; the count of failures repaired versus allowlisted with the allowlist's final
pinned number; that the allowlist is checked in and asserted, naming the census file that asserts it; and the
functional job's wall-clock with its headroom.

Close with exactly one line, and it is about the functional half only:

  - `SC4-E2E MET` — the `gate-e2e` job reads success on the named run, all five owned specs are absent, and
    every failure is repaired rather than quarantined. Follow it with the run identifier.
  - `SC4-E2E OPEN` — it does not. Name every remaining failure, its verdict, and the plan that must own it.
    Do not soften the criterion.

Then one line beginning `SC4-VISUAL ` recording `gate-visual`'s conclusion on this same run and naming plan 19
as its owner, so the reader of this file knows exactly which half of SC4 is still outstanding and where it
closes. Finish with one line beginning `HANDOFF ` carrying the functional job's duration — which becomes the
merge wait once the check is required — and the five job display names verbatim, which are the
required-status-check contexts plan 15 needs.
  </action>

  <acceptance_criteria>
    - `evidence/ci-run-after-18.txt` contains exactly one line beginning `CI-HEAD-SHA=` carrying a full
      forty-character SHA, and exactly one line each beginning `CI-RUN-ID=`, `CI-EVENT=` and `CI-CONCLUSION=`.
    - `CI-EVENT=` names the push event, and the recorded run's own head SHA equals the recorded
      `CI-HEAD-SHA=` value.
    - The file contains at least five lines beginning `JOB `, one per job in `.github/workflows/ci.yml`, each
      carrying a conclusion and a duration.
    - The file contains exactly one line beginning `GATE-E2E=`, whose value is taken from the job read rather
      than from the run's overall conclusion, with the functional job's summary block verbatim beneath the
      command line that produced it.
    - The file contains exactly five lines beginning `OWNED ` — this plan's one spec and the four the two
      previous waves closed — and none is recorded as still present in the failure list.
    - The file contains exactly one line beginning `CANCELLED-DISCARDED=`, and one `DISCARDED-RUN ` line for
      each run that value counts.
    - The file contains one line each beginning `COLLECTED `, `NOT-RUN=` and `WALLCLOCK `, all carrying
      numbers, and the wall-clock line states the headroom against the ninety-minute cap.
    - The file ends its closure with exactly one of the two `SC4-E2E ` lines, followed by one line beginning
      `SC4-VISUAL ` naming plan 19 as the owner of the other half and one line beginning `HANDOFF ` carrying
      the functional job's duration and the five display names verbatim.
    - The functional job's time cap in `.github/workflows/ci.yml` is byte-identical to HEAD.
    - `git log origin/dev..HEAD --oneline` produces no output.
  </acceptance_criteria>

  <verify>
    <automated>gh run view $(grep -m1 '^CI-RUN-ID=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt | cut -d= -f2) --json event,conclusion,headSha --jq '[.event,.conclusion,.headSha]|@tsv'</automated>
    <fails_when>non-zero exit — the recorded id does not resolve to a real run; or the first field is not "push" — a dispatch or pull-request run was read instead of the push-triggered run SC4 is a property of; or the second field is "cancelled" — the concurrency group killed it and it measures nothing, so measure again rather than recording that value; or the third field does not equal the CI-HEAD-SHA= line in the same file — the run that was read is not the run that measured this tree</fails_when>
    <automated>gh run view $(grep -m1 '^CI-RUN-ID=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt | cut -d= -f2) --json jobs --jq '.jobs[] | select(.name == "gate-e2e (functional Playwright suite)") | .conclusion'</automated>
    <fails_when>non-zero exit, or no output at all — the exact display name did not match a job in the run, which means either the name moved or the wrong run was read; or the printed conclusion is anything other than "success" — the functional half of SC4 is not closed, and under D-02 the remaining failures are repaired and pushed again rather than annotated</fails_when>
    <automated>grep -c '^OWNED ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt</automated>
    <fails_when>output is any number other than 5 — the five specs the three e2e-repair plans own were not each given a verdict against this run's failure list, so a repair here that reopened an earlier wave's would be invisible</fails_when>
    <automated>grep -cE '^OWNED .+\|present$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt</automated>
    <fails_when>output is greater than 0 — a spec one of the three e2e-repair plans closed still appears in the real run's failure list, so the chain those three plans form has a hole in it and this task returns to its diagnosis and pushes again</fails_when>
    <automated>grep -cE '^SC4-E2E (MET|OPEN)' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt</automated>
    <fails_when>output is any number other than 1 — the functional half of SC4 has no closure line, or has both, and the criterion must land on exactly one verdict backed by a named run</fails_when>
    <automated>grep -c '^CANCELLED-DISCARDED=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt</automated>
    <fails_when>output is any number other than 1 — the concurrency-group accounting is missing or doubled, and without it a cancelled run can be silently substituted for a measured one</fails_when>
    <automated>git log origin/dev..HEAD --oneline</automated>
    <fails_when>any output — a commit this plan made never reached the remote, so the run that was read measured a tree that is not this one</fails_when>
  </verify>

  <done>
    A named, push-triggered `ci` run selected by the pushed head SHA has the
    `gate-e2e (functional Playwright suite)` job concluding success, all five job conclusions and the
    functional job's summary block on the record, all five specs the three e2e-repair plans own recorded
    absent from its failure list, every cancelled run discarded by identity with a count, and exactly one
    `SC4-E2E ` verdict beside a `SC4-VISUAL ` line handing the other half to plan 19 and a `HANDOFF ` line
    carrying plan 15's two numbers.
  </done>

  <reversibility rating="reversible">
    One evidence file and a push to the unprotected development branch. Nothing is published, no protected ref
    moves, no workflow file is touched; per D-06 `dev` is exactly the branch GSD commits to directly.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booker → the listing route's availability calendar | The first control on the path to paying; a click that is lost here means the booker cannot reach a checkout at all. |
| shared test helper → its three calling specs | One helper serves the checkout, the collision and the one-tree specs; a change to it is a change to all three, and only one of them is this plan's subject. |
| commit author → `e2e/helpers/` | A weakened post-condition here restores a three-minute timeout on an unrelated locator, which reads as a different defect entirely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-90 | Tampering | the day helper's selected-day post-condition | high | mitigate | The post-condition may not be removed or weakened; its budget may move only from two measured intervals, and Task 3 proves it still fails in two directions with recorded elapsed times. |
| T-19.1-91 | Repudiation | a retry added on an unobserved mechanism | high | mitigate | The retry is gated on one recorded word taken from the runner's own call log, and the log is quoted verbatim in the transcript. A retry with no such quote fails the plan's own acceptance criteria. |
| T-19.1-92 | Denial of Service | the file-scoped serial group | medium | mitigate | The released set is computed from before-and-after runs keyed on file and full title, with first-attempt outcomes and declaration positions, so a case that only passes on a retry is not counted as released. |
| T-19.1-93 | Spoofing | a failure wearing another failure's message | high | mitigate | The empty-grid direction in Task 3 exists precisely for this: a matched-nothing intersection must fail as an empty grid, never as a lost click, and the hour-chip helper's four states are re-driven so the distinction does not collapse. |
| T-19.1-94 | Tampering | `e2e/price-parity.spec.ts`, the money-path gate spec | high | mitigate | Asserted byte-unchanged by a task verify. Its divergent copy of the walk is filed in the carry-forward register with a disposition and a severity rather than edited as a side effect. |
| T-19.1-95 | Information Disclosure | evidence transcripts under `.planning/` | medium | mitigate | The transcripts record test titles, attribute names, elapsed times, exit codes and call-log lines. The booker fixtures mint synthetic addresses and no credential, cookie or database URL is echoed. |
| T-19.1-96 | Repudiation | the run the functional half of SC4 is closed on | critical | mitigate | The criterion closes here, so the run's identity is load-bearing: it is selected by the pushed head SHA rather than by recency, its identity and the `gate-e2e` job's conclusion are each re-read as separate task verifies, a `cancelled` conclusion is discarded by identity and counted rather than read as a verdict, and `git log origin/dev..HEAD` asserts the measured tree is this one. |
| T-19.1-97 | Spoofing | the run's overall conclusion standing in for the job's | high | mitigate | `GATE-E2E=` is taken from the per-job read, never from the run's overall conclusion, because the run is expected red on `gate-visual` while this job is green. The second task verify queries the job by its EXACT display name, which is also the required-status-check context D-05 names, so a renamed job fails loudly instead of silently matching nothing. |
| T-19.1-98 | Tampering | the two previous waves' closed failures | high | mitigate | All four specs plans 16 and 17 closed get their own `OWNED ` verdict on this run, so a repair in this wave that reopens an earlier one fails a task verify instead of netting to zero unnoticed. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs and no new dependency is introduced; the container image is the one already pinned in the workflow. If a task reaches for a package, the legitimacy gate runs then. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed test-side source, conditional on Task 1's reading:** the day-selection helper in
`e2e/helpers/booker-seed.ts` gains either a retried-click shape polled on its existing post-condition — with
the call log that warranted it quoted in the docblock — or nothing at all, with a written verdict in its
place. Its empty-grid reporting is repaired if Task 3's second direction shows it wears the wrong message.

**Changed instrument commentary:** a comment block at the previously failing case in
`e2e/hold-countdown.spec.ts` recording what the container measured, which branch was taken, and what a future
red at that row does and does not imply. No assertion, constant or fixture moves there.

**New evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt`
— `IMAGE=` / `TREE=` / `CONTAINER-EXIT=` markers for every container invocation, `ATTEMPT ` lines keyed on
file and full title, the post-condition and call-log excerpt blocks, `REPRODUCED=`, `SELECTED=`, `DETACHED=`,
`INTERVAL-SELECTED-MS=`, `INTERVAL-HOURS-MS=`, `RELEASED `/`REGRESSED ` lines with `RELEASED-TOTAL=` and
`DELTA-VS-13=`, two `GUARD=red` readings with `APPLIED=` counts, one `RECOVERED=`, four `STATE ` lines, and
one `SC4-TERM=` line.

**Second new evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt`
— the real push-triggered `ci` run the functional half of SC4 closes on: `CI-HEAD-SHA=`, `CI-RUN-ID=`,
`CI-EVENT=`, `CI-CONCLUSION=`, five `JOB ` lines with conclusions and durations, `GATE-E2E=` taken from the
per-job read with the functional job's summary block verbatim, five `OWNED ` lines (this plan's spec and the
four the two previous waves closed), `CANCELLED-DISCARDED=` with a `DISCARDED-RUN ` line per discarded
identity, one `COLLECTED ` line against the previous wave's total, one `NOT-RUN=`, one `WALLCLOCK ` with the
headroom against the ninety-minute cap, exactly one `SC4-E2E ` verdict line, one `SC4-VISUAL ` line naming
plan 19 as the owner of the other half, and one `HANDOFF ` line carrying the functional job's duration and
the five display names verbatim.

**New carry-forward entry:** one entry in `deferred-items.md` naming the money-path spec's divergent copy of
the day-selection walk, with a suggested disposition and a severity.

**New npm scripts:** none. **New dependencies:** none. **New allowlist entries:** none — the pinned entry
total stays at two. **New counted workflow invariants:** none.
</artifacts_this_phase_produces>

<verification>
- `npx playwright test e2e/hold-countdown.spec.ts e2e/collision-in-place.spec.ts e2e/one-tree.spec.ts e2e/price-parity.spec.ts --project=chromium --workers=1 --retries=2` reports zero failed, zero flaky and zero not-run.
- `npm test` and `npm run test:design` both pass at or above the totals recorded at this phase's head.
- `node scripts/verify-workflows.mjs` exits 0 with fifty-seven invariants.
- `npx tsc --noEmit` produces no `error TS` line beyond the seven pre-existing ones in `tests/design/`.
- `git diff --exit-code -- e2e/price-parity.spec.ts` exits 0.
- `evidence/triage-day-click-container.txt` carries every marker the acceptance criteria name.
- `evidence/ci-run-after-18.txt` names one push-triggered `ci` run whose head SHA equals the pushed head, in which the `gate-e2e (functional Playwright suite)` job concludes success, all five job conclusions are recorded, all five accountable specs are absent from the failure list, and exactly one `SC4-E2E ` verdict stands beside a `SC4-VISUAL ` line and a `HANDOFF ` line.
- `git log origin/dev..HEAD --oneline` produces no output.
- `git status --porcelain` shows no leftover throwaway harness or probe, and the collected test count is back to what it was before they existed.
</verification>

<success_criteria>
The checkout spec runs to completion at the runner's own shape inside the pinned image, with the two cases
its position previously blocked named individually and a total carrying a stated delta against plan 13's
figure; whether the day click was lost is answered in one word from the runner's own call log, and a retry
exists only if that word is yes; the helper's guard is proved to fail in two directions with recorded elapsed
times; the money-path spec's divergent copy of the same walk is filed with a disposition rather than
edited; and the functional half of SC4 closes on a NAMED, real, push-triggered `ci` run selected by the
pushed head SHA, in which the `gate-e2e (functional Playwright suite)` job's own conclusion reads success and
none of the five specs the three e2e-repair plans own appears in its failure list — with `gate-visual`'s
conclusion recorded on the same run and handed to plan 19 rather than claimed here.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-18-SUMMARY.md` when done.

State plainly: (1) whether the failure reproduced in the pinned image at all, with the run identity and the
tree; (2) what the call log said, verbatim enough to justify or refuse the retry; (3) the two measured
intervals against the laptop's 683 milliseconds and against the post-condition's two-second budget; (4) the
released set and its delta against two; (5) the guard's two reds with their elapsed times; (6) the identity
of the real `ci` run the functional half of SC4 closed on — its id, its head SHA, the `gate-e2e` job's own
conclusion and all five `OWNED ` verdicts — together with the `SC4-E2E ` verdict line verbatim, `gate-visual`'s
conclusion on the same run, and every run discarded as cancelled. If the failure was already gone on arrival,
say which of the preceding plans is the candidate and on what evidence — and do not claim it without the
pair. If `SC4-E2E OPEN` stands, name every remaining failure with its verdict and its owning plan; that is a
complete result, not an incomplete one, and it is what SC4's remaining route out is built from.
</output>