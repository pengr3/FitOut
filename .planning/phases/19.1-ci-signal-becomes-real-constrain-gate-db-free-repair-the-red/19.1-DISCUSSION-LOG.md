# Phase 19.1: CI signal becomes real — constrain gate-db-free, repair the red e2e baseline, and close the checker's own coverage holes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-05
**Phase:** 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
**Areas discussed:** The 14 red tests, gate-visual's fate, What becomes required, Publishing .planning/

---

## Grounding gathered before questions

Questions were written against the live repository rather than from the ROADMAP text alone:

| Fact | Evidence |
|---|---|
| `ci` on `dev` is red in **two** jobs, not one | run 33848750657 — `gate-e2e` ✗ and `gate-visual` ✗; `gate-db-free`, `gate-db`, `gate-price-parity` ✓ |
| `gate-e2e` = 14 failed, 367 passed, 44.1m | 14 tests across 10 spec files |
| `gate-visual` red separately | visual snapshots stale since 2026-08-30; ~36 tracked `*-visual-linux.png` |
| Repo `PRIVATE`, default branch `main`, owner `pengr3` | `gh repo view` — this is why SC5 needs the public flip |

**Area selection.** All four offered gray areas were selected for discussion.

---

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| The 14 red tests | Repair, quarantine, or hybrid — the biggest scope fork in the phase | ✓ |
| gate-visual's fate | SC4 names only the e2e failures, but gate-visual is red too | ✓ |
| What becomes required | Which jobs block a merge to `main` | ✓ |
| Publishing .planning/ | Going public publishes working CI-defeat payloads | ✓ |

**User's choice:** all four.
**Notes:** nothing was left to Claude's discretion at the area level.

---

## The 14 red tests

| Option | Description | Selected |
|--------|-------------|----------|
| Triage, then split | Diagnose all 14 first; repair real product bugs (notably `cancel.spec.ts:232` on the refund path); only genuine drift/flake goes in the allowlist, each with a written reason | ✓ |
| Repair all 14, no allowlist | No quarantine mechanism exists at all. Purest signal, but `hold-countdown` burns 3m per attempt and looks like timing rather than breakage — the phase could not close until even that is fixed | |
| Quarantine all 14 now | Fastest route to a required check, but the allowlist would cover the refund-path test — a green badge over an unverified money path | |

**User's choice:** Triage, then split.
**Notes:** captured as D-01. The refund-path test was the deciding argument against blanket quarantine.

### Follow-up — the open end of "triage then split"

| Option | Description | Selected |
|--------|-------------|----------|
| Fix them all here | However many triage calls real, they get fixed in 19.1; the phase grows to 10–15 plans rather than the allowlist growing | ✓ |
| Fix the important ones, spin the rest | Money-path and a11y here; everything else allowlisted with "Phase 19.2" as owner | |
| Fix the money-path one only | Repair `cancel.spec.ts:232`, quarantine the other 13 | |

**User's choice:** Fix them all here.
**Notes:** captured as D-02. Explicitly accepts a larger phase over a larger allowlist.

---

## gate-visual's fate

| Option | Description | Selected |
|--------|-------------|----------|
| Regenerate baselines here | Whole workflow goes green; a diff that turns out to be a real regression gets fixed, not blessed | ✓ |
| Leave gate-visual red | Scope SC4 to `gate-e2e`, record `gate-visual` as known-red with its own follow-up | |
| Regenerate, then de-list it | Green now, and permanently outside the required set | |

**User's choice:** Regenerate baselines here.
**Notes:** captured as D-04. The argument that carried it: a standing red X trains everyone to ignore
red, which is the habit SC4 exists to break. Note that the *de-listing* half of option 3 was taken
separately in the next area — `gate-visual` is regenerated **and** stays non-blocking.

---

## What becomes required

| Option | Description | Selected |
|--------|-------------|----------|
| Four behaviour jobs | `gate-e2e`, `gate-db-free`, `gate-db`, `gate-price-parity` required; `gate-visual` reports only | ✓ |
| `gate-e2e` only | The ROADMAP's literal SC5 text; leaves lint/design/build/checker unprotected at the branch level | |
| All five required | Strictest; a one-pixel design change blocks every merge until baselines are regenerated | |

**User's choice:** Four behaviour jobs.
**Notes:** captured as D-05. `gate-db-free` being required is the load-bearing part — it carries the
very checker SC1 spends the phase hardening.

### Follow-up — where protection applies

| Option | Description | Selected |
|--------|-------------|----------|
| `main` only | `dev` push workflow unchanged; `ci` still runs on `dev` and is visible, just non-blocking | ✓ |
| `main` and `dev` | Strictest; would force a PR and ~45-minute wait on every GSD commit | |

**User's choice:** `main` only.
**Notes:** captured as D-06. Claude recommended against protecting `dev`; the PM agreed.

---

## Publishing .planning/

| Option | Description | Selected |
|--------|-------------|----------|
| Publish as-is after SC1–SC3 | Once the vectors are closed the documents describe defeated attacks; the planning trail is the point | ✓ |
| Redact the payloads first | Rewrite exact mutation text into prose. Originals stay in git history across 2,157 commits unless rewritten | |
| Keep .planning/ private | Publish code only. Removes the exposure but breaks every in-repo link and gives up the public record | |

**User's choice:** Publish as-is after SC1–SC3.
**Notes:** captured as D-07. Redaction was described to the PM as mostly buying the appearance of
caution, since history retains the originals; they accepted that reading.

### Follow-up — when and by whom

| Option | Description | Selected |
|--------|-------------|----------|
| End of phase, you flip it | Everything lands first; Claude runs a pre-flight checklist and a fresh secret scan, then hands over the command | ✓ |
| After SC1–SC3, you flip it | ROADMAP's literal sequencing; publishes a red badge for the rest of the phase | |
| After SC1–SC3, I flip it | Pre-authorises Claude to run `gh repo edit --visibility public` | |

**User's choice:** End of phase, you flip it.
**Notes:** captured as D-08. This is a deliberate deviation from the ROADMAP's literal SC5 sequencing —
the ROADMAP said "after criteria 1–3", the PM chose "after everything". The security rationale is
unchanged and strictly better served; only the badge optics and the human-in-the-loop position moved.

---

## Closing gate

| Option | Description | Selected |
|--------|-------------|----------|
| Ready for context | Write CONTEXT.md with the eight decisions | ✓ |
| Explore more gray areas | Surface further points, e.g. allowlist expiry dates, the 45-minute merge wait | |

**User's choice:** Ready for context.

---

## Claude's Discretion

No gray area was handed back at the selection step, but the following were stated as Claude's to decide
and are recorded in CONTEXT.md:

- Allowlist representation and its fail-closed assertion mechanism.
- Triage method and ordering; plan/wave decomposition.
- How the SC2 axis-vs-property audit is carried out and each predicate watched red.
- The shape of the SC3 regression case (`.trim() === ` reverted to `.includes(...)`).
- How `gate-db-free` is constrained — invariant shape, anchor rule, mutation coverage.
- Whether a given repair touches product code, the spec, or a fixture.
- The `gh` / API calls and ruleset shape for the required checks.

## Deferred Ideas

- Sharding the Playwright suite (D-13 defers; the 90-minute cap must not be raised again).
- A scheduled or manual visual-baseline refresh, since `gate-visual` will be green but non-required.
- Phase 19's remaining `IN-01`…`IN-05` findings — SC2's audit may absorb some; the rest carry forward.

### Reviewed todos, not folded

Four pending todos matched on keywords only and were rejected as scope creep for a CI phase: host
verification submission path (0.6), Phase 18 PM decision follow-through (0.6), reveal host contact
details in ops queue (0.4), ops staff management surface (0.2).
