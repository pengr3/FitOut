---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 14
subsystem: infra
tags: [security, secret-scanning, gitleaks, github-rulesets, branch-protection, disclosure, checkpoint]

requires:
  - phase: 19.1-01
    provides: "the four behaviour jobs' display-name strings, pinned by invariant so the ruleset payload could copy rather than type them"
  - phase: 19.1-05
    provides: "guards-01-post-fix.txt and guards-05-post-fix.txt — the watched-red evidence that CR-03 and WR-01 are closed, cited by the checklist's section (a)"
  - phase: 19.1-06
    provides: "guards-06-post-fix.txt and sc2-audit-inventory.md — CR-01/CR-02 closure and the two carried rows, cited by section (a)"
  - phase: 19.1-13
    provides: "the SC4 closure line, run 33976831607, the merge-wait number and the five required-status-check context strings quoted in sections (b) and (d)"
provides:
  - "a fresh full-history secret scan over every ref with its non-vacuity proof — the scanner's own reported commit count reconciled against this repository's 2244 commits"
  - "a measured positive control showing the detector missing two of three canaries, which turned the project-specific second pass from a precaution into a necessity (logged as D-19.1-F)"
  - "evidence/ruleset-main.json — the exact declarative payload plan 19.1-15 submits, four contexts copied programmatically from ci.yml, the visual job deliberately absent"
  - "evidence/preflight-public.md — the four-section handover checklist, plus a dated record that the decision came back `hold`"
  - "the PM's `hold` decision on publication, with its reasoning, recorded rather than paraphrased"
  - "the decided answer to D-19.1-D point (a): the today-ring follows venue-local today"
  - "the route by which SC4 closes: 19.1-16, -17 and -18, confirmed as in-phase and owed to gap planning"
affects: [19.1-15, 19.1-16, gap planning for phase 19.1, SC4, SC5, any future secret-scanning control]

actuals:
  tokens: 19300
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Prove a clean scan scanned something before believing it: assert the scanner's OWN reported commit count and reconcile it against the repository's history length, because a scan over an unmounted directory exits clean identically"
    - "Watch the detector detect before trusting its silence — a positive control on a throwaway repository is what separates 'found nothing' from 'cannot find this'"
    - "Record a credential finding by FILE and LINE, never by value, in any diagnostic that is itself about to be published"
    - "Author outward-facing configuration as a checked-in declarative payload reviewed before it is applied, never as a sequence of clicks"
    - "A designed stop is recorded as a stop: `status: halted`, the downstream plan marked BLOCKED with its blocking mechanism named, and the open criteria stated without softening"

key-files:
  created:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md
  modified:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md

key-decisions:
  - "PUBLICATION: HOLD. The PM declined to make the repository public. Stated reasoning: SC4 is open — gate-e2e and gate-visual are red — so publishing today would put a red badge in front of every arrival, the exact cost D-08's ordering exists to avoid. The hold lifts when 19.1-16/-17/-18 land."
  - "Plan 19.1-15 is BLOCKED, not skipped. It installs the branch ruleset, and `gh api repos/pengr3/FitOut/rulesets` returns 403 while the repository is private. It is blocked by construction — by an API that refuses — not by a choice about its content. Its payload and its five measured context strings are prepared and waiting."
  - "D-19.1-D point (a) DECIDED by the PM: the availability calendar's today-ring follows VENUE-LOCAL today, matching startMonth/endMonth/disabled — not the rendering host's clock. Reasoning: the two differ only where server and venue timezones already disagree, which is arguably the bug. Recorded in the ledger; NOT implemented here."
  - "No HANDOFF RESULT section was written into preflight-public.md. There was no flip to read back, and Task 3's acceptance criteria describe a document state that only a completed flip can produce. A dated hold record was written instead."
  - "The three follow-up plans 19.1-13 proposed WILL be created inside phase 19.1 (19.1-16 host-layout hydration and both still-red originals, 19.1-17 duplicated booker mounts, 19.1-18 the lost day click). They are owed to the gap-planning step after this plan closes, and were not written by this plan."
  - "requirements-completed is EMPTY. CI-01's remaining criterion SC5 is not met — the repository is private and no ruleset is installed. Marking it complete here would be the vacuous green this phase exists to delete."

patterns-established:
  - "A halt is a first-class outcome with its own record: status: halted, the halted task named, the blocked downstream plan named with its blocking mechanism, and every open criterion stated plainly"
  - "When a plan's own task text is falsified by a later measurement, say so in the artefact the human reads rather than letting them discover it — plan 14's checklist opens by contradicting plan 14's Task 3 context block"
  - "Record a human decision with the human's reasoning quoted, not paraphrased into agreement with the executor's prior position"

requirements-completed: []

coverage:
  - id: D1
    description: "A fresh secret scan over every ref, with its full unedited output, its exit code, and a proof that it scanned this repository rather than nothing"
    requirement: CI-01
    verification:
      - kind: other
        ref: "grep -c '^SCAN' evidence/secret-scan-pre-public.txt => 1 (verdict line present)"
        status: pass
      - kind: other
        ref: "SCAN commits=2244 reconciled against `git log --all --oneline | wc -l` and `git rev-list --all --count`, which agree; the 27 commits gitleaks did not read are accounted for by four measured no-added-text classes"
        status: pass
      - kind: other
        ref: "credential-shaped-value grep over the evidence file (sk_test_/sk_live_/re_/BETTER_AUTH_SECRET=/CLOUDINARY_API_SECRET=) => 0 — no value reached a file that was about to be published"
        status: pass
      - kind: other
        ref: "positive control on a throwaway repo: gitleaks v8.30.1 found 1 of 3 canaries; the project-specific pass 2 read ZERO on all three real key shapes across 2244 commits"
        status: pass
    human_judgment: false
  - id: D2
    description: "The declarative ruleset payload plan 19.1-15 will submit — four required contexts copied from ci.yml, the visual job absent, default branch only, empty bypass list"
    requirement: CI-01
    verification:
      - kind: other
        ref: "node -p JSON.parse(...).rules.find(type==='required_status_checks').parameters.required_status_checks.length => 4"
        status: pass
      - kind: other
        ref: "grep -c 'gate-visual' evidence/ruleset-main.json => 0 — D-05's prohibition asserted, not assumed"
        status: pass
      - kind: other
        ref: "node scripts/verify-workflows.mjs — no FAIL line; ci.yml was read, never touched"
        status: pass
      - kind: other
        ref: "each of the four contexts matches a job display name in .github/workflows/ci.yml byte for byte (copied programmatically, not typed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The four-section pre-flight checklist justifying as-is publication, with per-vector closure citations and per-criterion verdicts"
    requirement: CI-01
    verification:
      - kind: manual_procedural
        ref: "evidence/preflight-public.md sections (a)-(d); section (a) cites guards-01/05/06-post-fix.txt per vector, section (b) gives a verdict and proving artefact per SC, section (c) quotes the SCAN line, section (d) carries the flip and read-back commands with the leading-slash gotcha"
        status: pass
    human_judgment: true
    rationale: "Whether the cited closures actually justify publishing this repository's planning trail as-is is a disclosure judgement about a one-way act. The instruments prove each vector has a named invariant, a standing case and an evidence file showing it red; they cannot prove that the resulting document is safe to publish. That judgement is the PM's, and it was exercised — the answer was hold."
  - id: D4
    description: "The publication decision itself — Task 3's checkpoint, handed to the PM"
    requirement: CI-01
    verification: []
    human_judgment: true
    rationale: "D-08 reserves this act for the PM and explicitly rejected pre-authorising the executor. It is one-way and outward-facing; no automated verification can substitute for the decision. Outcome: HOLD — the repository remains PRIVATE, read back with `gh repo view --json visibility --jq .visibility`."

# Metrics
duration: 10min
completed: 2026-09-06
status: halted
halted_at_task: 3
halted_reason: "PM decision `hold` at Task 3's blocking-human checkpoint — the repository was not made public. Blocks plan 19.1-15 by construction (rulesets API returns 403 while private)."
---

# Phase 19.1 Plan 14: SC5 pre-flight — scan, payload, checklist, and a `hold` Summary

**A clean full-history secret scan proven non-vacuous against 2244 commits, the four-context branch ruleset payload checked in, and the publication checklist handed to the PM — who answered `hold`, leaving SC4 and SC5 OPEN and plan 19.1-15 blocked by a 403.**

## Status: HALTED (designed stop)

This plan **did everything it owed except the one act D-08 reserves for a human — and that human said
no.** That is a designed stop, not a failure, and the distinction is the point of this section.

| Task | What it owed | Outcome |
|---|---|---|
| 1 | Fresh full-history secret scan + non-vacuity proof | **COMPLETE** — `3a3639b` |
| 2 | Ruleset payload + as-is-publication checklist | **COMPLETE** — `a75e1ce` |
| 3 | Hand over the flip; the PM makes the repository public | **HALTED** — the PM answered `hold` |

Task 3 was a `checkpoint:decision` carrying `gate="blocking-human"`. It is the one task in this phase
the executor is *forbidden* from performing, by D-08, which rejected pre-authorising the executor
specifically. It presented, it stopped, and the decision came back `hold`. **No flip command was run
— not by the executor, and not by the PM.** Nothing outward-facing happened.

## Performance

- **Duration:** ~10 min across two sessions (Tasks 1–2: 2026-09-06T00:57–01:02 +08:00; the halt
  record: 2026-09-06 01:11 +08:00)
- **Tasks:** 2 of 3 complete, 1 halted
- **Files created:** 3 · **Files modified:** 1 (plus tracking)

## Accomplishments

- **A secret scan that proves it scanned.** gitleaks v8.30.1 (`git` subcommand, not the deprecated
  `detect`, with `--log-opts="--all"` so it walks every ref) over **2244 commits**, with the mount
  confirmed against a separate image *before* any result was read. The scanner's own reported count
  (2217 read) reconciles against the repository's history with the 27-commit shortfall accounted for
  by four measured no-added-text classes, running one commit in the conservative direction. **A scan
  that finds nothing because it scanned nothing is the exact vacuous green this phase exists to
  delete**, and the count is what forecloses it.
- **The detector was watched detecting — and watched missing.** On a throwaway repository it found a
  secret that existed only in history after deletion from the working tree, **but it missed two of
  three canaries**, including this project's own `re_` mail-key prefix. That measurement converted the
  project-specific second pass from a precaution into a necessity, and became `D-19.1-F`.
- **Ten findings, zero credentials.** The three patterns that would name a real credential — the
  payment provider's `sk_test_`/`sk_live_` prefixes and the mail `re_` prefix — read **ZERO** across
  every ref. One environment file was ever added to history (`.env.example`, all secret slots empty).
  Every finding is recorded by **file and line, never by value** — the rule
  `scripts/refuse-mail-credential.mjs` states about its own diagnostics, binding hardest on an
  evidence file that was about to be published.
- **The ruleset payload, checked in as reviewable configuration.** Four required contexts copied
  *programmatically* out of `ci.yml` rather than typed; `gate-visual` deliberately absent (D-05, so a
  pixel comparison cannot wedge a merge over antialiasing); scope `~DEFAULT_BRANCH` symbolically so it
  survives a rename; `dev` untouched (D-06); `bypass_actors` empty; up-to-date-branch policy disabled
  with its cost reasoning recorded in the checklist rather than as an unknown JSON field that risks a
  422.
- **A checklist that contradicts its own plan where the plan was wrong.** Plan 14's Task 3 `<context>`
  block asserts *"the workflow run is green in all five jobs"* and *"Everything the phase owed is
  done."* Both were false. Rather than presenting a decision on a false premise, the checklist opens
  by naming the delta against run `33976831607` so the PM did not have to notice it themselves. The
  `hold` was taken **on that corrected premise** — which is what made it the right call.

## Task Commits

1. **Task 1: Fresh full-history secret scan + non-vacuity proof** — `3a3639b` (docs)
2. **Task 2: Ruleset payload + as-is-publication checklist** — `a75e1ce` (docs)
3. **Deviation: log `D-19.1-F`** — `86fa0f0` (docs)
4. **Task 3: HALTED** — no commit. A decision checkpoint produces no code.

**Halt record + plan metadata:** see the final commit of this plan.

## Files Created/Modified

- `evidence/secret-scan-pre-public.txt` — three passes with full unedited output, the positive
  control transcript, the exit code, the reported commit count, per-pattern finding counts, and the
  `SCAN` verdict line.
- `evidence/ruleset-main.json` — the exact payload 19.1-15 submits. **Prepared and waiting.**
- `evidence/preflight-public.md` — four sections plus, now, a dated `hold` record.
- `deferred-items.md` — `D-19.1-F` added (Task 1's deviation); `D-19.1-D` updated with the PM's
  venue-local decision.

## The decisions recorded (not re-litigated)

### 1. Publication: `hold`

**The PM's reasoning, quoted rather than paraphrased:** SC4 is open — `gate-e2e` and `gate-visual`
are red — so publishing today would put **a red badge in front of every arrival**, the exact cost
D-08's ordering exists to avoid.

That takes the argument the checklist itself makes in its point 3 and treats it as decisive rather
than as a caveat. D-08's *stated* reason for sequencing the flip last was that "the first thing anyone
sees is a green badge rather than a red one"; today that benefit is unavailable, so the ordering's own
rationale argues for waiting. **The hold stands until 19.1-16/-17/-18 land.**

The pre-flight is not thereby invalidated. The scan, the vector closure and the payload are all
independent of whether the suite is green — which is why the checklist is explicitly reusable, with
section (b) flagged for re-measurement and section (c) flagged for a fresh re-run (D-08 requires the
scan be *fresh*, not referenced).

### 2. The three follow-up plans are in-phase

The PM confirmed that the three plans 19.1-13 proposed under D-02 **will be created inside phase
19.1**:

| plan | owns |
|---|---|
| **19.1-16** | host-layout hydration — **both still-red originals** (`host-headings`, `overflow-320`) plus the 24 blocked tests |
| **19.1-17** | the duplicated booker mounts |
| **19.1-18** | the lost day click |

**This is the route by which SC4 closes, and therefore the route by which the publication hold
lifts.** None of the three exists yet; they are owed to the **gap-planning step that runs after this
plan closes**. This plan did not write them and was not asked to.

### 3. `D-19.1-D` point (a): venue-local today

**Decided:** the availability calendar's today-ring follows **venue-local today** — the value
`startMonth`, `endMonth` and `disabled` already use — not the rendering host's clock. Concretely,
point (a) resolves to its `if yes` branch: pass `today={todayStart}`.

**The PM's reasoning:** the two clocks differ *only* where the server and venue timezones already
disagree, and that divergence is arguably the bug itself.

The ledger entry has been updated so whichever follow-up plan inherits it inherits the **answer**
rather than re-opening the question. **The six-point spec is retained unchanged** — points (b)–(f)
(the seam note, the pinned `created_at`, the `&today=` parameter and its assertion, the
clock-independence test, the post-mint crop) are all still owed. **Not implemented here:**
`availability-calendar.tsx` is production source with a product consequence and is outside this plan's
`files_modified`; it belongs to a planned plan, expected to be 19.1-16.

## Plan 19.1-15: BLOCKED by this halt

**Not skipped. Not deferred by choice. Blocked by construction.**

19.1-15's whole subject is installing the branch ruleset, and
`gh api repos/pengr3/FitOut/rulesets` returns **`403 "Upgrade to GitHub Pro or make this repository
public to enable this feature"`** while the repository is private — re-probed and still standing on
2026-09-05. **That 403 is the single hard sequencing constraint in the phase**, and 19.1-15 states the
flip as an assertable precondition rather than assuming it. With the repository private, that
precondition is unmet and the plan cannot start.

**Its inputs are prepared and waiting, not missing:**

- The payload is checked in at `evidence/ruleset-main.json` — four contexts, validated, `gate-visual`
  absent.
- **The five context strings and their measured statuses** are in `evidence/preflight-public.md`
  section (d), measured against run `33976831607`: three `success`, `gate-e2e` **`failure`**,
  `gate-visual` **`failure`** (reports only, not required).
- The caveat 19.1-15 must take with it is already written down: **one of the four checks it makes
  required is red today**, so installing the ruleset in this state would block every merge into `main`
  until 19.1-16/-17/-18 land. That is 19.1-15's call with the number in front of it — which is a
  further reason the hold is coherent rather than merely cautious.

When the hold lifts, 19.1-15 runs against these unchanged.

## Deviations from Plan

### Auto-fixed / recorded

**1. [Rule 2 — Missing Critical] Positive control added to the secret scan, and its result logged as `D-19.1-F`**
- **Found during:** Task 1
- **Issue:** The plan required a fresh scan and a project-specific second pass, but nothing established
  that the detector *could* detect. "Found nothing" and "cannot find this" produce identical output.
- **Fix:** A positive control on a throwaway repository. gitleaks found 1 of 3 canaries — missing this
  project's `re_` mail prefix and a bare AWS key id. The scan that ran is unaffected (pass 2 read ZERO
  on all three real shapes), but the blind spot binds anyone who reaches for gitleaks next, so it was
  logged as `D-19.1-F` with a disposition: any future secret-scanning control needs a `.gitleaks.toml`
  with explicit rules **and a watched-red case per rule**.
- **Committed in:** `86fa0f0`

**2. [Task 3 acceptance criteria unsatisfiable — recorded, not worked around] No `HANDOFF RESULT` section written**
- **Found during:** Task 3, on the `hold`
- **Issue:** Task 3's `<action>` and `<acceptance_criteria>` instruct appending a `HANDOFF RESULT`
  section recording the flip's read-back and timestamp, and its `<verify>` asserts
  `grep -c 'HANDOFF RESULT' >= 1` and `gh repo view --visibility == PUBLIC`. **Both describe a document
  state only a completed flip can produce.** The plan's acceptance criteria assume the `publish`
  branch; the `hold` branch it explicitly offered has no corresponding output spec.
- **Resolution:** No `HANDOFF RESULT` was written. Writing one would be a **fabricated record of an
  irreversible outward-facing act** — a worse defect than an unmet criterion, and precisely the class
  of vacuous-green this phase exists to delete. A dated **hold record** was written into
  `preflight-public.md` instead, so a later reader cannot mistake a decided checklist for a pending
  one.
- **Verification:** `gh repo view --json visibility --jq .visibility` => `PRIVATE`. Task 2's own
  `<verify>` asserts exactly this and still passes — the executor did not flip anything.
- **⚠ A near-miss worth recording.** The first draft of the hold note *denied* the handover in prose,
  spelling the section heading out in order to say it was absent. Task 3's `<verify>` is
  `grep -c '<that heading>' evidence/preflight-public.md`, so the denial would have **satisfied the
  check that exists to prove the flip happened** — a green produced by a sentence saying the opposite.
  That is this phase's own subject in miniature. The heading string now appears **nowhere** in
  `preflight-public.md` (`grep -c` => 0, asserted in the self-check below), and the file explains why
  it is not written even as a negation. The string appears in *this* SUMMARY, which is not the grep's
  target and is the correct place for the record.

**3. [Rule 2 — Missing Critical] The checklist opens by contradicting its own plan's Task 3 context**
- **Found during:** Task 2
- **Issue:** Plan 14 was authored before 19.1-13's re-measurement and asserts the run is green in all
  five jobs and that everything the phase owed is done. Run `33976831607` measured the opposite.
- **Fix:** A `⚠ READ THIS BEFORE THE FOUR SECTIONS — SC4 IS OPEN` block at the top of the checklist,
  quoting the plan's own false sentences beside what was measured. **This is why the `hold` was taken
  on a true premise.**
- **Committed in:** `a75e1ce`

---

**Total deviations:** 3 (2 missing-critical, 1 unsatisfiable-criterion recorded rather than
worked around). **Impact:** no scope creep. Two hardened the evidence; the third refused to fabricate
a record.

## Criteria state at phase end — SC4 and SC5 are OPEN

**Stated plainly, because a SUMMARY that read as a clean close would be the same defect in the record
that this phase exists to remove from the code.** The whole subject of phase 19.1 is that a green
badge must mean something.

| # | criterion | verdict |
|---|---|---|
| SC1 | `gate-db-free` is itself constrained | **MET** |
| SC2 | every predicate audited for the axis-vs-property error | **MET**, 2 carried with written verdicts |
| SC3 | the standing test covers the predicates it was built for | **MET** |
| **SC4** | the `ci` workflow run is **not red on every push** | **OPEN** |
| **SC5** | the repository is **public** and the agreed checks are **required** on the default branch | **OPEN — both halves** |

- **SC4 is OPEN.** Run `33976831607` has overall conclusion `failure`: `gate-e2e` and `gate-visual`
  are red. 19.1-13 measured this and declined to soften the criterion — "not red on every push" is a
  property of the RUN, and three-of-five green does not satisfy it. **This plan does not soften it
  either.** It closes via 19.1-16/-17/-18.
- **SC5 is OPEN in both halves.** The repository is `PRIVATE` (the PM held), and no ruleset is
  installed (19.1-15 is blocked by the 403 that follows from the hold).
- **`requirements-completed` is therefore empty.** CI-01 is not discharged by this plan. Marking it
  complete against two open criteria would be exactly the satisfiable-green this phase spent fourteen
  plans deleting.

## Issues Encountered

- **The plan's own premise was stale** (deviation 3) — surfaced in the artefact the human reads rather
  than left for them to discover.
- **Task 3's acceptance criteria cover only one branch of the decision it offers** (deviation 2). The
  plan defines `hold` as a valid option with pros and cons, then specifies outputs only for `publish`.
  Recorded here as a planning lesson: **a checkpoint that offers N options owes an output spec for
  each N.**

## Next Phase Readiness

**Phase 19.1 is NOT complete.** Two of five criteria are open and this plan halted at a designed stop.

**The sequence out:**

1. **Gap planning** produces 19.1-16, -17 and -18 (confirmed in-phase by the PM). Not this plan's work.
2. **19.1-16/-17/-18 execute** → SC4 closes. 19.1-16 additionally inherits the decided `D-19.1-D`
   point (a) and the retained points (b)–(f).
3. **The publication decision is re-taken** using `evidence/preflight-public.md` — section (a) durable,
   **section (b) re-measured**, **section (c) re-run fresh** per D-08, section (d) durable.
4. **If the answer is `publish`**, 19.1-15 unblocks — the 403 lifts with the flip — and installs the
   waiting payload. SC5 closes.

**Blockers:** the publication hold (a decision, held by the PM, waiting on SC4) and, downstream of it,
the rulesets API 403 (a mechanism, waiting on the flip).

**Nothing in this plan is wasted by the hold.** The scan is replayable as written, the payload is
final, and three of the checklist's four sections are durable.

## Self-Check: PASSED

Every claim above re-verified against disk and git rather than against memory:

| check | result |
|---|---|
| `evidence/secret-scan-pre-public.txt` exists | FOUND |
| `evidence/ruleset-main.json` exists | FOUND |
| `evidence/preflight-public.md` exists | FOUND |
| `deferred-items.md` exists | FOUND |
| commit `3a3639b` (Task 1) | FOUND |
| commit `a75e1ce` (Task 2) | FOUND |
| commit `86fa0f0` (D-19.1-F) | FOUND |
| `grep -c '^status: halted'` this file | 1 |
| `grep -c '^requirements-completed: \[\]'` this file | 1 |
| `grep -c` the handover heading in `preflight-public.md` | **0** — no fabricated handover, and no false-positive denial |
| `grep -c 'DECIDED — point (a) is answered'` in `deferred-items.md` | 1 |
| `gh repo view --json visibility --jq .visibility` | **`PRIVATE`** — the executor flipped nothing |
| `STATE.md` `completed_plans` | **58** — corrected twice; the SDK bumped it to 59 on both `record-metric` and a later write, which would have counted a halted plan as a completed one |

**No task-3 commit exists, and that is correct** — a decision checkpoint that returned `hold` produces
a record, not a change.

---
*Phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red*
*Halted: 2026-09-06 — Task 3, PM decision `hold`*
*SC4 and SC5 remain OPEN. Phase 19.1 is not complete.*
