---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 07
type: execute
wave: 3
depends_on: ["19.1-01"]
files_modified:
  - e2e/cancel.spec.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 — the money-path test is REPAIRED and never quarantined. `e2e/cancel.spec.ts:232` is the test the PM named explicitly as the reason blanket quarantine was rejected: a green badge sitting on top of an unverified refund behaviour is the exact failure mode this phase exists to prevent. It is repaired here and it is named in plan 11's forbidden-file list so the prohibition survives this phase."
    - "D-01 / CI-01 SC4 — the diagnosis is that the PRODUCT is right and the SPEC is incomplete. The booking detail page composes the in-transit refund sentence only on the dispatch-succeeded path; when the payments call raises, an audit row is written and the manual-return notice renders INSTEAD, because telling the booker a refund is on its way would be false on those paths. CI has no payments credential and structurally cannot have one, so the manual-return branch is the CORRECT render there."
    - "D-01 / CI-01 SC4 — the repaired assertion is STRONGER than the one it replaces, not weaker. It asserts a named disjunction — either the in-transit sentence or the manual-return notice is on screen — and in the manual-return branch it additionally asserts the audit row exists carrying one of the four attention-needing actions. A bare either-or with no branch-specific consequence would be a weakening dressed as a repair; the branch assertion is what stops it being one."
    - "D-01 — the existing database assertion beneath the DOM assertion is unchanged. The refund amount comparison already passes in both environments and stays exactly as it is; this repair adds a second truth beside it rather than trading one for another."
    - "CI-01 adjacency (spec-less edge probe, verification: explicit) — the two legitimate money truths are adjacent but not interchangeable, and the repaired spec keeps them distinguishable. It does not assert 'one of these two strings is somewhere on the page'; it identifies WHICH branch rendered and asserts the consequence that branch owes, so a page rendering neither, or rendering the wrong one for its audit state, is RED."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — the disjunction cannot be satisfied vacuously. A page that renders neither the in-transit sentence nor the manual-return notice is RED with a message naming both branches and stating that a cancelled booking with a positive refund must land in exactly one of them; and in the manual-return branch, an audit query returning zero rows is RED rather than treated as an absent-therefore-fine result."
    - "CI-01 — the repair is proved by a watched-red pair. A transcript captures the failure against the unrepaired spec with the missing-locator message verbatim, a second captures it passing after the repair, and a third proves the repaired assertion is still RED when the branch it selects is forced not to render."
  artifacts:
    - e2e/cancel.spec.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt
  key_links:
    - "the payments dispatch outcome to which of the two money sentences renders. `src/lib/booking/refund-dispatch.ts` and `src/app/(app)/bookings/[id]/page.tsx:1092-1122` own the branch; the spec previously knew about only one side of it."
    - "the manual-return branch to the audit row it writes. This is the link that makes the branch assertion meaningful: the notice on screen without the audit row would be a silent money failure, and the repaired spec now asserts both together."
    - "this spec to plan 11's forbidden-file list. D-01's prohibition is encoded as a named constant with the decision identifier beside it, so a future round cannot annotate this file without deleting a build-blocking assertion."
  prohibitions:
    - "This spec must never be recorded in the known-failures allowlist. It sits on the refund path, and D-01 names it as the specific test whose quarantine was rejected."
    - "A payments credential must never be added to the CI workflow to make this pass. A shipped invariant requires zero secret references across every job, and the second reason matters more than the first: it would put a live third-party credential into a repository that is about to become public."
    - "The database assertion beneath the DOM assertion must not be relaxed, reordered or removed. It passes today in both environments and it is the half of this test that no rendering decision can affect."
    - "A disjunction must never be asserted as a text search that either string satisfies. Identify the branch, then assert what that branch owes; anything less is a weakening presented as a repair."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `adjacency` and `empty` explicitly for its own subject (two adjacent money truths that must stay distinguishable; a page rendering neither being red). `ordering` and `encoding` are owned by plans 01, 06 and 11; none is dropped."
    - "The four attention-needing audit actions are read out of `src/lib/booking/refund-dispatch.ts` at authoring time rather than typed from RESEARCH.md. A copied literal would be a second source of truth wearing the costume of a constant — the rule this repository states at `scripts/verify-workflows.mjs:118-133`."
    - "If the local run shows the in-transit sentence rendering (because this machine's environment DOES carry a payments credential), the disjunction is still the correct repair and the branch assertion is exercised on the other side. Record which branch the local run took; the spec must pass on both."
---

<objective>
Repair `e2e/cancel.spec.ts:232` — the refund-path test D-01 forbids quarantining — by asserting the named
disjunction the product actually implements: either the in-transit refund sentence or the manual-return
notice is on screen, and in the manual-return case the audit row exists with one of the four
attention-needing actions.

Purpose: the spec asserts one of two legitimate money truths. CI has no payments credential and cannot be
given one, so the dispatch raises and the manual-return branch is the correct render — the product is
right and the spec is incomplete. The repair is a stronger assertion that holds in both environments.

Output: a repaired spec, a triage transcript with the audit-row evidence, and a recorded verdict feeding
plan 11's forbidden-file constant.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@src/lib/booking/refund-dispatch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the manual-return branch and read the audit row it writes</name>

  <precondition>
    Docker Desktop is running with `fitout-db-1` up, the database is migrated and seeded, the build cache
    directory has been removed, and TCP port 3000 is free. The payments secret key must be UNSET in the
    environment the dev server inherits, so the dispatch raises and the manual-return branch is the one
    under test — that is the state every CI run is in and the state this reproduction must match.
  </precondition>

  <read_first>
    - `e2e/cancel.spec.ts` around `:225-250` — the sibling test above at `:225-230` (the established shape:
      a DOM assertion and a database row assertion side by side), the test under repair, and the assertion
      at `:249` that looks for the in-transit sentence.
    - `src/app/(app)/bookings/[id]/page.tsx:1092-1122` — where the refund line is composed, the condition
      under which the manual-return notice renders instead, and the comment stating why the in-transit
      sentence would be false on those paths.
    - `src/lib/booking/refund-dispatch.ts` — the audit-row reader and the four attention-needing actions.
      Read the action values from this file; do not type them from research.
    - `src/components/booking/manual-return-notice.tsx` — the notice's rendered text, which is what the
      repaired disjunction's second branch locates.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Cause C" — the verbatim failure, the verdict, and the
      named repair shape.
    - `.planning/phases/19.1-.../evidence/triage-harness.md` — plan 02's bring-up sequence.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt
  </files>

  <action>
Run the plan 02 bring-up sequence with the payments secret key unset, then run the spec unmodified and
capture the command line and unedited transcript into `evidence/triage-cancel-refund.txt` under a
PRE-REPAIR heading:

`npx playwright test e2e/cancel.spec.ts --project=chromium --reporter=list`

The transcript must show the in-transit locator resolving to no elements. Record which test line failed.

Then read the state the product actually produced. Query the audit table for the seeded booking used by
that test and capture the query and its result rows — the action column and the booking reference only,
never a credential, never a full row dump containing payment identifiers. Confirm the row carries one of
the four attention-needing actions declared in `src/lib/booking/refund-dispatch.ts`, and quote those four
action values from the source with their line reference.

Append a DIAGNOSIS block stating four things: the payments call raised because no credential is present;
the page therefore rendered the manual-return notice rather than the in-transit sentence; an audit row was
written with a named action; and the spec asserts only one of the two legitimate outcomes, so the product
is correct and the spec is incomplete. Close with a line beginning `VERDICT` stating that this test is
repaired, never allowlisted, and citing D-01.

Do not edit `e2e/cancel.spec.ts` in this task.
  </action>

  <acceptance_criteria>
    - `evidence/triage-cancel-refund.txt` contains a command line and an unedited transcript showing the
      in-transit locator resolving to no elements.
    - It contains an audit-table query and its result, showing one of the four attention-needing actions.
    - It quotes the four action values with the source file and line reference they are read from.
    - It contains a DIAGNOSIS block and a `VERDICT` line citing D-01 and stating repair rather than
      allowlist.
    - The file contains no payment credential, no secret key value and no signed URL.
    - `git status --porcelain -- e2e/cancel.spec.ts` produces no output.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^VERDICT' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt</automated>
    <fails_when>output is 0 — no verdict was recorded for the money-path test D-01 protects</fails_when>
    <automated>grep -ciE 'sk_(test|live)_|PAYMONGO_SECRET_KEY=|api_secret' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt || true</automated>
    <fails_when>output is any number other than 0 — a credential-shaped value reached an evidence file that is about to be published</fails_when>
    <automated>git status --porcelain -- e2e/cancel.spec.ts</automated>
    <fails_when>any output — the spec was edited during the reproduction task</fails_when>
  </verify>

  <done>
    The manual-return branch is reproduced on this machine, the audit row it writes is read and quoted, and
    the verdict — repair, never allowlist — is on disk with the decision it cites.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert the named disjunction, with the audit row as the manual-return branch's consequence</name>

  <read_first>
    - `e2e/cancel.spec.ts:225-230` — the sibling test's shape: a DOM assertion and a database row assertion
      side by side. The repair copies this shape rather than inventing one.
    - `e2e/cancel.spec.ts` around `:232-260` — the test under repair in full, including the refund-amount
      comparison beneath the assertion being replaced.
    - `src/app/(app)/bookings/[id]/page.tsx:1092-1122` — the two branches and their exact rendered text.
    - `src/components/booking/manual-return-notice.tsx` — the notice's own copy, so the second branch is
      located by what the component actually renders.
    - `src/lib/booking/refund-dispatch.ts` — the four action values, imported or read rather than typed.
    - `evidence/triage-cancel-refund.txt` — Task 1's diagnosis and the audit query that works.
    - `.planning/phases/19.1-.../19.1-PATTERNS.md` §6 — the assertion being replaced, the product code that
      owns the other branch, and the repair shape.
  </read_first>

  <behavior>
    - With no payments credential, the manual-return notice is on screen AND an audit row exists carrying
      one of the four attention-needing actions — both asserted, the second only in that branch.
    - With a payments credential present, the in-transit refund sentence is on screen and the test passes
      without requiring an audit row.
    - A page rendering NEITHER branch is RED with a message naming both and stating that a cancelled
      booking with a positive refund must land in exactly one of them.
    - The manual-return notice on screen with NO audit row is RED — the notice without its record is a
      silent money failure.
    - The existing refund-amount comparison still runs and still passes, unchanged.
  </behavior>

  <files>
    e2e/cancel.spec.ts
  </files>

  <action>
Replace the single in-transit visibility assertion with a branch-identifying disjunction, in the sibling
test's established shape.

First determine which branch rendered, by checking for the manual-return notice's own text using a
non-throwing count rather than a visibility assertion, so the test can branch rather than fail. Then:

- IF the manual-return notice rendered: assert it is visible, and query the audit table for the booking
  under test and assert exactly that a row exists whose action is one of the four attention-needing
  values. Read those four values from `src/lib/booking/refund-dispatch.ts` — import the declaration if it
  is exported, otherwise reference it by name and cite the line — never type them as literals in the spec.
  Assert the row count is greater than zero explicitly, with a message stating that a notice on screen
  without its audit record is a silent money failure.
- ELSE IF the in-transit sentence rendered: assert it is visible, matching today's behaviour.
- ELSE: fail with a message naming BOTH branches, stating that a cancelled booking with a positive refund
  must land in exactly one of them, naming the product file and lines that own the branch, and stating the
  correct response — investigate which branch the dispatch took, never relax this assertion.

Leave the refund-amount comparison beneath it exactly as it is: same expression, same position, same
message.

Add a docblock above the repaired assertion stating what was MEASURED: the CI environment has no payments
credential and structurally cannot have one, so the dispatch raises and the manual-return branch is the
correct render there; the spec previously asserted only one of two legitimate money truths; and this test
is on the refund path and is never eligible for the known-failures allowlist, citing D-01.
  </action>

  <acceptance_criteria>
    - `e2e/cancel.spec.ts` no longer asserts the in-transit sentence unconditionally; it identifies the
      branch first and asserts that branch's consequence.
    - The manual-return branch asserts an audit row exists with one of the four attention-needing actions,
      and those values are read from `src/lib/booking/refund-dispatch.ts` rather than typed as literals in
      the spec.
    - The neither-branch path fails with a message naming both branches and the product file that owns
      them.
    - The refund-amount comparison beneath is byte-identical to HEAD.
    - A docblock cites D-01 and states this test is never allowlist-eligible.
    - `npx playwright test e2e/cancel.spec.ts --project=chromium` passes with zero failures.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>npx playwright test e2e/cancel.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or the summary block reporting a non-zero "failed" count, or a summary reporting 0 collected tests</fails_when>
    <automated>git diff -- e2e/cancel.spec.ts | grep -c '^-.*refund_cents' || true</automated>
    <fails_when>output is any number other than 0 — the database refund-amount assertion was altered or removed, and this plan requires it byte-unchanged</fails_when>
    <automated>grep -c 'D-01' e2e/cancel.spec.ts</automated>
    <fails_when>output is 0 — the docblock does not cite the decision that forbids quarantining this test, so the prohibition has no anchor in the file it protects</fails_when>
  </verify>

  <done>
    The spec asserts both legitimate money outcomes, identifies which one rendered, holds the
    manual-return branch to its audit record, and passes in an environment with no payments credential.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the repaired disjunction can still fail, and record the allowlist prohibition</name>

  <read_first>
    - `e2e/cancel.spec.ts` — the repaired test from Task 2.
    - `evidence/triage-cancel-refund.txt` — Task 1's pre-repair transcript and verdict, which this task
      completes.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt` —
      the watched-red convention: mutate, capture, revert, prove the revert.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"D-03 — the known-failures allowlist", the
      forbidden-file requirement, so the constant plan 11 declares can be quoted here exactly.
  </read_first>

  <files>
    e2e/cancel.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt
  </files>

  <action>
Append the POST-REPAIR transcript to `evidence/triage-cancel-refund.txt`, showing the spec passing and
naming which branch it took on this machine.

Then prove the repaired assertion is not a guard that cannot fail, twice:

(a) Temporarily narrow the branch-selection check on the TRACKED spec so neither branch is recognised,
run the test, and capture the RED transcript — it must carry the neither-branch message naming both
branches and the product file. Revert with `git checkout --` and record the checksum pair and
`git status --porcelain`.

(b) Temporarily change the audit-row assertion to require an action value that does not exist, run the
test, and capture the RED transcript — it must carry the message about a notice without its record.
Revert and record the proof the same way.

Close the evidence file with a line beginning `PROHIBITION` stating, in the words plan 11 will encode as a
named constant: this spec path is never eligible for the known-failures allowlist because it sits on the
refund path, and the decision identifier that establishes it. Plan 11 declares that constant; this line is
what it quotes.
  </action>

  <acceptance_criteria>
    - `evidence/triage-cancel-refund.txt` contains a post-repair transcript naming which branch rendered.
    - It contains two RED transcripts from temporary narrowings, each with a distinct message and each with
      a checksum pair and `git status --porcelain` revert proof.
    - It ends with a line beginning `PROHIBITION` naming the spec path and the decision identifier.
    - `git status --porcelain -- e2e/cancel.spec.ts` shows the file modified relative to HEAD only by Task
      2's repair, with no residue of either narrowing.
    - `npx playwright test e2e/cancel.spec.ts --project=chromium` passes.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^PROHIBITION' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-cancel-refund.txt</automated>
    <fails_when>output is 0 — the allowlist prohibition plan 11 must encode has no recorded source</fails_when>
    <automated>npx playwright test e2e/cancel.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or the summary block reporting a non-zero "failed" count — a temporary narrowing was left applied</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>

  <done>
    The repaired disjunction is proved to still fail when neither branch renders and when the audit record
    is missing, both narrowings are reverted with checksum proof, and the allowlist prohibition is on disk
    in the exact words plan 11 will encode.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the payments provider → the booking detail page | The dispatch may succeed or raise, and the page tells the booker a different money truth in each case. |
| the rendered money sentence → the booker | A false in-transit claim is a money statement the product would be making without a refund behind it. |
| triage output → `.planning/evidence/` → the public repository | Audit-row output must never carry a credential or a payment identifier that survives publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-29 | Repudiation | a green CI badge over an unverified refund behaviour | high | mitigate | The test is repaired rather than annotated (D-01), and plan 11 encodes its path in a build-blocking forbidden-file constant so a future round cannot annotate it silently. |
| T-19.1-30 | Tampering | the manual-return notice rendering without its audit record | high | mitigate | Task 2 asserts the audit row in that branch and Task 3 proves the assertion can fail; a notice with no record would be a silent money failure. |
| T-19.1-31 | Information Disclosure | a payments credential added to CI to make this pass | high | mitigate | Prohibited by this plan on two grounds — a shipped invariant requires zero secret references in the workflow, and the repository is about to become public. |
| T-19.1-32 | Information Disclosure | the audit query's output in an evidence file | high | mitigate | The query captures the action column and the booking reference only; the verify greps the evidence file for credential shapes and fails on any hit. |
| T-19.1-33 | Tampering | the database refund-amount assertion weakened during the repair | medium | mitigate | The verify asserts no removed line mentions the refund-amount column; the assertion is required byte-unchanged. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed symbol:** the cancelled-booking refund assertion in `e2e/cancel.spec.ts` — a single visibility
check becomes a branch-identifying disjunction with a per-branch consequence, plus a docblock citing D-01
and stating the test is never allowlist-eligible.

**New evidence file:** `evidence/triage-cancel-refund.txt` — pre-repair red, the audit-row query and its
result, the four action values quoted from source, post-repair green, two narrowed-guard reds with revert
proofs, a `VERDICT` line and a `PROHIBITION` line.

**Consumed by plan 11:** the `PROHIBITION` line's wording becomes the forbidden-file constant's comment.

**New npm scripts:** none. **No production source file is modified.**
</artifacts_this_phase_produces>

<verification>
- `npx playwright test e2e/cancel.spec.ts --project=chromium` passes with no payments credential present.
- `npx tsc --noEmit` exits 0.
- The refund-amount database assertion is byte-identical to HEAD.
- The evidence file carries four transcripts, a `VERDICT` line and a `PROHIBITION` line, and contains no
  credential-shaped value.
- `node scripts/verify-workflows.mjs` is still green.
</verification>

<success_criteria>
The refund-path test passes in the environment CI actually has, by asserting the money truth the product
actually renders plus the audit record that branch owes — a stronger assertion than the one it replaces,
proved able to fail in two directions, and named as permanently ineligible for the known-failures
allowlist.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-07-SUMMARY.md` when done.
State which branch the local run took, quote the `PROHIBITION` line verbatim so plan 11 can encode it
without reopening this file, and confirm the refund-amount assertion is unchanged.
</output>
