---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 16
type: execute
wave: 8
depends_on: ["19.1-13"]
files_modified:
  - src/app/(host)/host/layout.tsx
  - src/components/patterns/auth-slot-skeleton.tsx
  - e2e/shell.spec.ts
  - e2e/host-headings.spec.ts
  - e2e/overflow-320.spec.ts
  - e2e/host-listing-grid.spec.ts
  - tests/design/suspense-fallback-overlay.test.ts
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt
autonomous: true
requirements: [CI-01]
gap_closure: true

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC4 — every `(host)` route loads without React reporting that it had to regenerate the tree on the client. The defect is in production source: the host layout puts a live nav in its streaming fallback AND in the resolved child, and both sides mount the app's one overlay primitive, whose Radix-generated attribute the two renders cannot agree on. Plan 19.1-13 reproduced the message locally with a cold compile cache and CONTROLLED it — the booker checkout route emits none — so the mechanism is real and is not an over-unification."
    - "CI-01 / SC4 — the repair is chosen from what React's own error detail NAMES, not from the component chain the previous plan inferred. 19.1-13 recorded the chain and the attribute as the LEADING CANDIDATE and said in writing that the causal link to the two lost presses was not observed. This plan reads the mismatching node and the differing attribute out of the browser's own diagnostic before it edits anything, and records the reading verbatim."
    - "CI-01 / SC4 — `e2e/host-headings.spec.ts` and `e2e/overflow-320.spec.ts` are measured in the pinned runner image `mcr.microsoft.com/playwright:v1.60.0-noble` BEFORE and AFTER the repair, at the runner's own `--workers=1 --retries=2` shape. Both are green on this laptop and red on the runner, which is exactly the claim shape 19.1-09 built that image harness to settle, and two plans this phase changed their verdict once they used it."
    - "CI-01 / SC4 — the tests the `AC#36` serial group could not reach are identified BY FILE AND FULL TITLE and in run order, never by `file:line`. Six specs moved their line numbers inside this phase and line-keyed matching reports a repaired test as a newly surfaced one — 19.1-13's own recorded pattern. The released count is taken from the run; where it disagrees with the 24 that plan recorded, the run wins and the delta is written down."
    - "CI-01 adjacency (spec-less edge probe, verification: explicit) — a streaming fallback and its resolved child are ADJACENT renders of the same slot, and the question this plan settles is whether they may both mount an element whose identity attribute is generated per render. The two are separated by asserting the property on the SERVED page rather than on the components: a full load of the host composition collects the browser's console and page-error channels from before the navigation, because the mismatch is thrown during the first client render and a listener attached after the navigation resolves can miss the only message it wants (`e2e/public-listing.spec.ts`'s own recorded reason for the same ordering)."
    - "CI-01 ordering (spec-less edge probe, verification: explicit) — the repair is ordered AFTER the reproduction and the reproduction is ordered after a cold-compile-cache control. This repository has recorded a stale build cache faking both a route 404 and a hydration failure, so a warm-cache reproduction is not a reproduction. The ordering is enforced by the evidence file itself: the pre-repair transcript is captured and committed before the source edit, and the post-repair transcript is the same command against the same route."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — the new end-to-end assertion cannot pass over nothing. Before it asserts the absence of a regeneration message it first proves the page HYDRATED, by driving a control that only live client JavaScript can satisfy — the pattern `e2e/public-listing.spec.ts` records as the third sighting of this trap in this repository, where an absence assertion silently measured the pre-hydration document. A run that never reached the host composition fails on the presence assertion, not on the absence one."
    - "CI-01 encoding (spec-less edge probe, verification: explicit) — the transcripts are raw captured console output in the convention `19.1-PATTERNS.md` §A fixes, with line-anchored markers so a grep over them cannot be satisfied by surrounding prose. Nothing in this plan writes a newline into a checked-out file, so the mixed-EOL hazard that shipped six broken mutation builders earlier in this phase has no surface here; where a file must be restored it is restored from a byte copy with a checksum pair, never with a checkout that silently rewrites line endings."
    - "CI-01 / SC4 — the repaired property is REMEMBERED by a standing test that spawns against a mutated copy, not by the one-time run that proved it today. A hand mutation proves a fix today; a standing case is what stops the next round arriving with no memory of this one — the rule this phase has paid for four times."
    - "CI-01 — the two host-surface flakes this phase's final run produced, in `e2e/shell.spec.ts` and `e2e/host-listing-grid.spec.ts`, are re-measured after the repair and each is given a written outcome. They are LEADS, not owned failures: 19.1-13 named them as adjacent to the layout the mismatch was measured on and did not attribute them."
    - "CI-01 / SC4 — the repair is finally read on a REAL push-triggered `ci` run, not only on this laptop and not only in a locally built copy of the runner image. SC4's text is a property of a RUN, and per D-04 it is a property of the whole run rather than of one job, so all five job conclusions are recorded from one named run identified BY THE PUSHED HEAD SHA. The two specs this plan owns must be absent from that run's `gate-e2e` failure list; the job as a whole may still be red for the failures plans 17 and 18 own, and that is recorded rather than smoothed over."
    - "CI-01 / SC4 — the run that is read is the run that measured THIS tree. The workflow's concurrency group is keyed on workflow, ref and event with `cancel-in-progress: true`, and two runs already died that way inside this phase (`33971557439`, `33971883942`). A `cancelled` conclusion is therefore neither green nor red: it is discarded by identity and the measurement is taken again on the next run, with every discarded identity written down."
    - "CI-01 / SC4 (D-06) — pushing `dev` needs no approval and gets none. `dev` is unprotected by decision, GSD commits to it directly, and `ci` runs on every `dev` push precisely so the result is visible; only the merge into `main` is gated. This plan's push is the wave's ONE push, because a second one would cancel the first's run."
    - "Nothing in `.github/workflows/ci.yml` or `scripts/verify-workflows.mjs` moves. The invariant total stays at fifty-seven in all three of its spellings, and the known-failures allowlist stays pinned at two entries — this plan repairs, it does not annotate."
  artifacts:
    - src/app/(host)/host/layout.tsx
    - src/components/patterns/auth-slot-skeleton.tsx
    - e2e/shell.spec.ts
    - e2e/host-headings.spec.ts
    - e2e/overflow-320.spec.ts
    - e2e/host-listing-grid.spec.ts
    - tests/design/suspense-fallback-overlay.test.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt
  key_links:
    - "the host layout's streaming nav slot → `SiteNav` → `NavDrawer` → `ResponsiveDialog` → the app's one Radix dialog. The fallback and the resolved child both traverse this chain, and the layout's own comment asserts the two are byte-identical — true of the links and the badge, false of the generated attribute. That sentence is the falsified claim this plan replaces with a measurement."
    - "React's regeneration of the host tree → the advance control in `e2e/host-headings.spec.ts`'s wizard walk. `pressAdvance` already names this mechanism in its own failure message — a control replaced under the pointer while React finishes, so the press is LOST rather than queued — and the region reading it prints is a state a refusal cannot produce. The causal link is the thing Task 2 measures rather than assumes."
    - "the same regeneration → the publish-checklist `Fix` press in `e2e/overflow-320.spec.ts`, whose failure alone stops the rest of the `AC#36` serial group from executing. That block is the largest single term in the tests the suite never reaches."
    - "`src/app/(host)/host/layout.tsx`'s two blocking gates → `tests/design/blocking-session-gate.test.ts`, which asserts over the AST that neither redirect sits inside a streaming subtree. Any reshaping of the boundary in this plan has to leave that assertion green, and it is the reason the boundary may be narrowed but never widened upward."
    - "the pushed head SHA → the `ci` run identity → the `gate-e2e (functional Playwright suite)` job's conclusion. The SHA is the join key, not the clock: `gh run list --commit <sha>` is what makes the run being read provably the run that measured this tree, and it is the only defence against the concurrency group having cancelled the one a bare latest-run read would have returned."
  prohibitions:
    - "The `<Suspense>` boundary in the host layout may be NARROWED but never moved upward, and neither the session read nor the capability redirect may end up inside a streamed subtree. Both gates are the hosting surface's security boundary and the file's own header states that nothing below them may move above them."
    - "A failing end-to-end case may not be repaired by weakening the locator that named the failure. The advance control's press-received assertion and the publish-checklist walk's arrival assertion both stay; if the failure survives the repair, what changes is the diagnosis, never the instrument."
    - "No retry may be added to any press in this plan unless the lost click was OBSERVED, with the runner's own call log naming the detachment. An inferred retry makes the next red unreadable, and it is the refusal 19.1-08 recorded and this plan inherits."
    - "No entry may be added to the known-failures allowlist by this plan. Both specs it owns are barred from it by written verdicts, and a plan whose subject is a production defect that reproduces cannot also be the plan that quarantines the tests which found it."
    - "The falsified sentence in the layout's nav comment may not be deleted silently. It is replaced by what was measured, in the same edit, so the next reader inherits the correction rather than an absence."
    - "A container reproduction may not be believed without its own applied-proof: the run must show the image it ran in and the tree it measured, because a probe that silently did not apply has already reported a case surviving the deletion of its own conjunct in this phase."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this phase has no SPEC.md and the installed tooling has no coverage verb, so the four-item probe was authored by hand in the shape plan 19.1-01 established. All four items are RESOLVED with verification: explicit and appear above as plain truths: adjacency (fallback versus resolved child, separated on the served page with listeners armed before navigation), ordering (cold cache, then reproduce, then repair, enforced by the evidence file's own sequence), empty (a hydration proof before any absence assertion), encoding (raw line-anchored transcripts; byte-copy restores, never a checkout). Zero items dismissed, zero silent drops."
    - "PROHIBITION PROBE — six bespoke must-NOTs authored descriptor-less above. Routine-engineering candidates were dropped as they were in plan 01 (must not leave a temp directory behind; must not commit a throwaway config). The allowlist prohibition is restated here rather than cited because this plan owns two specs whose written verdicts bar them by name."
    - "THE CAUSAL LINK IS AN ASSUMPTION, NOT A FINDING. 19.1-13 recorded the regeneration message as the leading candidate for the two lost presses and stated explicitly that the link to those specific presses was not observed. Task 1 is justified on the defect's own terms — a production route that cannot hydrate is a defect whether or not it explains those two — and Task 2 is what settles the link. If the two specs stay red after the repair, that is a finding to record and repair at the site the new measurement names, NOT a reason to revisit Task 1."
    - "THE RELEASED COUNT IS AN ESTIMATE. 19.1-13 recorded twenty-four tests blocked by the publish-checklist row. The group holds thirteen rows across two themes and the row is the fifth, so a straightforward reading of the serial-abort rule gives a smaller tail. The NUMBER IS WHATEVER THE RUN PRINTS, keyed on file and full title; a delta from twenty-four is written into the evidence file and the SUMMARY rather than reconciled by argument."
    - "ASSUMPTION DELTA — primary noun UNCHANGED, decision `no-change`. The subject is the same host layout and the same two specs 19.1-13 named; no abstraction gains or loses a monopoly here."
---

<objective>
Repair the hydration failure React reports on every `(host)` route, and settle — by measurement in the
runner's own image — whether it is what stops the wizard's advance press and the publish-checklist's
`Fix` press from landing on the runner.

This is the plan that owns the two failures no earlier plan closed: `e2e/host-headings.spec.ts` and
`e2e/overflow-320.spec.ts`, both green on this laptop and red on all three attempts of every CI run
measured, and the second of which alone stops the rest of its serial group from executing at all.

Purpose: SC4 says the `ci` run is not red on every push. It is red, and this is the largest term.
Two of the four remaining `gate-e2e` failures live here, together with the biggest block of tests the
suite never reaches.

Output: a host layout whose streaming nav slot no longer produces a tree React has to throw away, an
end-to-end case that says so on the served page, a standing test that remembers the property, a
before/after transcript pair taken inside `mcr.microsoft.com/playwright:v1.60.0-noble`, and — last — a
named, real, push-triggered `ci` run in which the two specs this plan owns no longer appear in
`gate-e2e`'s failure list.

That last step is not decoration. SC4 is a property of a RUN. Every measurement above it is local or
containerised, and this phase has twice watched a laptop verdict and a runner verdict disagree; the plan
does not get to claim its two failures closed on a copy of the runner when the runner itself is one push
away. This plan reads its own run; plan 18 is where `gate-e2e` as a whole has to come back green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-PATTERNS.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-13-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-08-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-10-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one host route hydrates without regeneration, reproduced cold, repaired, and watched red</name>

  <read_first>
    - `src/app/(host)/host/layout.tsx` — the whole file. Read the header block that states both gates are
      blocking and nothing below them may move above them, and read the comment on the nav slot that
      claims the fallback is byte-identical to the resolved zero-count markup. That sentence is the one
      the measurement falsifies.
    - `src/components/patterns/ambient-notifications.tsx` — the file header's account of why the nav and
      the bell are two exports rather than one, and the `AmbientHostNav` export itself.
    - `src/components/patterns/site-chrome.tsx` — the `SiteNav` and `NavDrawer` exports and the docblock
      above `NavDrawer` explaining why the drawer composes the one overlay primitive rather than adding a
      second mechanism.
    - `src/components/patterns/responsive-dialog.tsx` — the header's record of the one-focus-trap decision
      and the trigger's `asChild` wiring. This is the primitive whose generated attribute is the suspect.
    - `src/components/patterns/auth-slot-skeleton.tsx` — `AuthSlotSkeleton` and `BellSlotSkeleton`. This is
      the in-repo precedent for a streaming fallback that reserves a slot's exact box without mounting the
      live control, and the host layout's bell slot already uses it.
    - `tests/design/blocking-session-gate.test.ts` — the AST assertion that neither gate sits inside a
      streamed subtree. Any reshaping of the boundary has to leave it green.
    - `e2e/public-listing.spec.ts` — the case named "a full load of the not-payable listing raises no
      hydration mismatch". Read all of it: the listeners armed BEFORE the navigation with its stated
      reason, the interactive proof that the page hydrated before any absence is asserted, and the
      failure message that names React's own remedy. This is the shape the new case copies.
    - `e2e/shell.spec.ts` — the `COMPOSITIONS` table, the `signUp` helper and the two describes that
      already drive the host composition (the header-height and the header-does-not-move cases). The new
      case belongs beside them, in a non-serial describe.
    - `.planning/phases/19.1-.../deferred-items.md` — the entry headed with the host geometry constants,
      whose closing paragraph carries the container reproduction harness recipe in full.
    - `.planning/phases/19.1-.../evidence/triage-harness.md` — the local bring-up sequence and the
      cold-compile-cache step.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the section carrying the local
      hydration transcript and its booker-route control, and the proposal block naming what this plan owns.
  </read_first>

  <files>
    src/app/(host)/host/layout.tsx,
    src/components/patterns/auth-slot-skeleton.tsx,
    e2e/shell.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt
  </files>

  <action>
STEP 1 — REPRODUCE BEFORE ANYTHING IS EDITED, AND CONTROL IT.

Bring the app up per the triage harness: database up, `.next` removed so the compile cache is cold, dev
server booted fresh. Sign a host up, load `/host`, and capture the browser's console and page-error
channels from BEFORE the navigation. Write the raw transcript into
`evidence/triage-host-hydration.txt` under a heading naming the route, with these line-anchored markers,
one per line, at column zero: `IMAGE=local`, `COLD-CACHE=yes`, `HYDRATION-COUNT=<n>` where `<n>` is the
number of regeneration messages collected, and `EXIT=<code>`.

Capture React's OWN error detail, not just the first line of the message. React 19 prints the mismatching
subtree with the differing attribute; copy those lines verbatim into the transcript under a marker
`DETAIL-BEGIN` / `DETAIL-END`. **Name the node and the attribute the browser named.** Do not carry
forward 19.1-13's identification of the component chain as if it were this plan's measurement — it was
recorded there as a leading candidate, and this step is what turns it into a reading or refutes it.

Then the control, in the same file: repeat the identical capture on the booker checkout route, which
19.1-13 measured as emitting none, and record `HYDRATION-COUNT=` for it too. A mechanism that fires
everywhere explains nothing; the control is what makes this a `(host)` finding.

Then the second control, which is what separates a real defect from this repository's recorded stale-cache
artefact: repeat the host capture against a `.next` left dirty by a production build, and record both
readings. Both are data; a disagreement between them is itself the finding and must be written down.

STEP 2 — REPAIR THE SITE THE MEASUREMENT NAMED, INSIDE A FIXED CONSTRAINT SET.

The repair goes in production source. Choose the shape from what Step 1 read, and record in the transcript
both the chosen shape and the shapes rejected, each with the constraint that decided it. Every one of the
following must still hold afterwards, and each is a real assertion somewhere in the tree:

  (a) The session read and the capability redirect both run to completion before any JSX is returned, and
      neither ends up inside a streamed subtree. `tests/design/blocking-session-gate.test.ts` is the
      instrument.
  (b) The navigation landmark count is exactly one at every width. `e2e/shell.spec.ts` already asserts it
      for the host composition at the 320px floor and at 1280.
  (c) The header box and the brand box are byte-identical across the slot's pending-to-resolved swap, in
      three widths and two themes. `e2e/shell.spec.ts` already asserts it; a fallback that reserves a
      different box reddens it, which is why any placeholder must reserve the control's own box the way
      the bell slot's skeleton already does.
  (d) The pending-request badge still hides at zero and still appears when the count is non-zero.
  (e) The app keeps exactly one overlay mechanism and one focus trap; no second overlay component is
      introduced, and the vendored dialog is not edited.
  (f) The host shell keeps its own wordmark and its own surface tint — the two compositions are not merged.

Candidate shapes, to be weighed against the reading rather than picked by preference: give the fallback a
placeholder that reserves the drawer trigger's box without mounting the overlay primitive, as the bell slot
already does; or narrow the boundary so that only the badge-bearing part of the nav streams and the overlay
mounts once, outside it; or make the generated identity the two renders disagree about a value both sides
derive from the same stable source. Whichever is chosen, the falsified sentence in the layout's nav comment
is REPLACED in the same edit with what Step 1 measured — the claim, why it was true of the links and the
badge and false of the attribute, and the transcript's location.

If the placeholder route is taken, add the new export beside `BellSlotSkeleton` in
`src/components/patterns/auth-slot-skeleton.tsx` rather than inventing a home for it, and carry that file's
own convention for why a skeleton reserves a box.

STEP 3 — THE END-TO-END ASSERTION, ON THE SERVED PAGE.

Add one case to `e2e/shell.spec.ts`, in a non-serial describe beside the existing host-composition cases,
modelled clause for clause on `e2e/public-listing.spec.ts`'s no-mismatch case:

  - console and page-error listeners armed BEFORE the navigation, with the reason restated at the site:
    the error is thrown during the first client render, so a listener registered after the navigation
    resolves can miss the only message it wants;
  - a presence assertion proving the host composition actually rendered, so the absence assertion below it
    is not measuring an empty page;
  - a proof that the page HYDRATED — drive a control only live client JavaScript can satisfy on this
    surface (opening and closing the nav drawer below the medium breakpoint is the cheapest one, and it is
    the very subtree under suspicion) — before asserting any absence;
  - the absence assertion itself, whose message states what React's remedy is and why a regenerated tree
    is the duplicate-node mechanism this repository has chased before, and prints the collected messages.

STEP 4 — WATCH IT RED.

Revert only the Step 2 source edit in the working tree, using a byte copy taken before the edit rather than
a checkout (the working tree is mixed-EOL and a checkout silently rewrites a line-feed file to carriage
returns while the status stays clean). Run the new case. Capture the RED transcript into the evidence file
under a heading naming the reversion, with `HYDRATION-COUNT=` at column zero. Restore the edit from the
byte copy and record the checksum pair before and after plus the porcelain status, in the same file.
  </action>

  <acceptance_criteria>
    - `.planning/phases/19.1-.../evidence/triage-host-hydration.txt` contains at least four lines beginning
      `HYDRATION-COUNT=` at column zero — the cold host reading, the booker control, the dirty-cache host
      reading, and the post-repair reading — and at least one of them reads exactly `HYDRATION-COUNT=0`.
    - The same file contains a `DETAIL-BEGIN` line and a `DETAIL-END` line, with at least one line between
      them, and the text between them names an element and an attribute.
    - The same file contains a checksum pair for `src/app/(host)/host/layout.tsx` taken before and after the
      Step 4 reversion, and the two values are equal.
    - `e2e/shell.spec.ts` declares one more test than it did at this plan's start, and the new case's body
      registers its console listener before its first navigation call.
    - `npx playwright test e2e/shell.spec.ts --project=chromium --workers=1` reports zero failed and zero
      flaky.
    - `npx vitest run --config vitest.design.config.ts tests/design/blocking-session-gate.test.ts` passes.
    - `git status --porcelain -- .github scripts` produces no output — no workflow and no checker file moved.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/shell.spec.ts --project=chromium --workers=1</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" or "flaky" count, or the summary's passed count is not greater than the count the same command printed before this task</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/blocking-session-gate.test.ts</automated>
    <fails_when>non-zero exit, or the reporter prints a line beginning with a multiplication sign naming a case in this file — the boundary was reshaped in a way that put a gate inside a streamed subtree</fails_when>
    <automated>grep -c '^HYDRATION-COUNT=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt</automated>
    <fails_when>output is less than 4 — one of the four required readings (cold host, booker control, dirty-cache host, post-repair) was never captured, so the reproduction is incomplete</fails_when>
    <automated>grep -c '^HYDRATION-COUNT=0$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt</automated>
    <fails_when>output is 0 — no reading in the transcript records a clean load, so nothing in the file shows the repair took effect</fails_when>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
    <fails_when>output is greater than 7 — a new type error was introduced; seven is the pre-existing count in tests/design/ that this phase has recorded at every plan</fails_when>
  </verify>

  <done>
    A cold-cache load of a `(host)` route is on the record as reporting a regeneration, with React's own
    detail naming the node and the attribute; the booker control on the same record reports none; the
    production source is repaired inside the stated constraint set with the falsified comment replaced by
    the measurement; a case in `e2e/shell.spec.ts` asserts the clean load on the served page after proving
    the page hydrated; and reverting the repair turns that case red with the transcript to show it.
  </done>

  <reversibility rating="reversible">
    The repair is one boundary's shape in one layout file plus, at most, one new skeleton export. Reverting
    it is a single revert of a single commit, and the byte-copy restore proof is captured in the same task.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Settle the two owned failures in the runner's own image — before, after, and the tests that were released</name>

  <read_first>
    - `e2e/host-headings.spec.ts` — the `pressAdvance` helper in full, including its docblock's account of
      why the save request rather than the transient UI state is the press-received signal, and its
      explicit refusal to retry. Read `expectSurface` and the wizard walk that calls them.
    - `e2e/overflow-320.spec.ts` — the `openWizardPhotosStep` helper and its docblock's account of why the
      publish checklist's `Fix` control is the only seam to the photos step, the `PHASE_14_ROWS` table, and
      the `AC#36` describe with its serial configuration.
    - `.planning/phases/19.1-.../deferred-items.md` — the container reproduction harness recipe: a host-side
      dev server and database, the pinned image, an in-container loopback forwarder because a helper
      hardcodes the localhost origin, and a throwaway config that removes only the web-server block. Do not
      flip the reuse-existing-server setting.
    - `.planning/phases/19.1-.../19.1-09-SUMMARY.md` — how that harness was built and deleted in the same
      task, and the collected-count comparison that proved the deletion.
    - `.planning/phases/19.1-.../19.1-08-SUMMARY.md` — the two outcomes that plan predicted for the next run
      of the wizard walk, and the note that neither occurred.
    - `.planning/phases/19.1-.../19.1-10-SUMMARY.md` — the measured finding that an expected-failure
      annotation does not cover a timeout, and the note that line numbers in the avatar spec had already
      moved. This is why every comparison below is keyed on file and full title.
    - `.planning/phases/19.1-.../evidence/triage-host-headings.txt` and `evidence/triage-upload-capability.txt`
      — the two prior verdicts on exactly these rows, including the prohibition barring the photos-step row
      from the allowlist by name.
  </read_first>

  <files>
    e2e/host-headings.spec.ts,
    e2e/overflow-320.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt
  </files>

  <action>
STEP 1 — BUILD THE CONTAINER HARNESS, THROWAWAY, AND PROVE IT MEASURED THE TREE YOU THINK IT DID.

Follow the recipe recorded in `deferred-items.md`: host-side dev server and database, the pinned image
mounted at the repository root, a small in-container loopback forwarder for the two host ports, and a
throwaway config importing the real Playwright config with only its web-server block removed. Build it,
use it, delete it in this same task, and record the collected test count before and after the throwaway
files existed so the deletion is proved rather than asserted.

Before believing any run, assert the tree it measured, as 19.1-13 did: the porcelain status over the
source, spec and test directories is empty, and the checked-out head is recorded. Write
`IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble`, `TREE=<head sha>` and `CONTAINER-EXIT=<code>` as
line-anchored markers at column zero in the evidence file for every container invocation.

STEP 2 — BEFORE. Check out the pre-repair state of the one source file Task 1 edited, as a byte copy in a
scratch location, apply it, and run BOTH owned specs in the container at the runner's own shape —
one worker, two retries. Capture the raw reporter output. Record, per spec, a line beginning `BEFORE `
carrying the file and the FULL TITLE of every case that failed and every case reported as not run.
Restore the repaired file from its byte copy and record the checksum pair.

If the pre-repair run does NOT reproduce the two failures in the container, that is the finding and it is
written down as such: the container is then not the instrument that separates this claim, and the plan's
verdict rests on the CI run instead. Say so in the transcript rather than reaching for a second theory.

STEP 3 — AFTER. Run the same two specs, the same way, on the repaired tree. Record a line beginning
`AFTER ` for every case that failed and every case reported as not run, in the same file-and-full-title
form. Then compute the released set by comparing the two `did not run` sets ON FILE AND FULL TITLE, never
on `file:line` — six specs moved their line numbers inside this phase, and line-keyed matching reports a
repaired test as a newly surfaced one. Write one line beginning `RELEASED ` per test that ran after and did
not run before, in run order, and one line beginning `REGRESSED ` per test that ran before and did not run
after. Close with a line beginning `RELEASED-TOTAL=` carrying the count.

19.1-13 recorded twenty-four blocked tests for the photos-step row. Compare the printed total against it
explicitly and write one line beginning `DELTA-VS-13=` stating the difference and, if there is one, what
accounts for it. The run wins; the earlier figure is not edited into agreement.

STEP 4 — IF EITHER SPEC IS STILL RED, DIAGNOSE FROM WHAT IT MEASURED.

Read what the failure MEASURED, not what its message says. The wizard walk now distinguishes a press that
was never received from a press that was refused, and it prints the save-state region's contents; the
photos-step row prints which of the walk's two assertions did not hold. Take the printed state as the
starting point and record a verdict line beginning `VERDICT ` per still-red case, naming the surviving
cause and the file that owns it.

A repair at that site is in scope for this task, subject to the plan's prohibitions: the instrument that
named the failure is not weakened, and a retry is added only if the container run's own call log names the
detachment of the clicked node. If the log names it, the retry is the one this repository already documents
for a lost click on a streamed surface, applied in that helper's idiom with a post-condition rather than a
bare re-click. If the log does not name it, no retry is added and the verdict says why.

If a still-red case turns out to be neither a product defect nor a spec defect this task can close, it is
written up as a proposed follow-up with what it owns and its first instrument — the D-02 shape 19.1-13
used — and it is NOT quarantined. The two specs are barred from the allowlist by name.
  </action>

  <acceptance_criteria>
    - The evidence file contains at least two lines beginning `IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble`
      at column zero and at least two lines beginning `CONTAINER-EXIT=`.
    - The evidence file contains at least one line beginning `BEFORE ` and at least one beginning `AFTER `,
      and every such line carries both a spec path ending in `.spec.ts` and a quoted full test title.
    - The evidence file contains exactly one line beginning `RELEASED-TOTAL=` and exactly one beginning
      `DELTA-VS-13=`.
    - Every case still reported as failing after the repair has a line beginning `VERDICT ` naming it.
    - `tests/design/e2e-known-failures.test.ts` is unmodified and its census passes, with the pinned entry
      total still reading two.
    - `git status --porcelain` shows no throwaway container config, no forwarder script and no scratch byte
      copy left in the tree.
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --project=chromium --workers=1 --retries=2</automated>
    <fails_when>non-zero exit, or the reporter's summary line carries a non-zero "failed" count, or the summary reports any test as "did not run"</fails_when>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/e2e-known-failures.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reports fewer than nine passing cases — the census is the guard that the allowlist did not grow, and a shrunken run means it stopped scanning</fails_when>
    <automated>grep -c '^RELEASED-TOTAL=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt</automated>
    <fails_when>output is any number other than 1 — either the released set was never computed, or it was computed twice and the plan has two answers</fails_when>
    <automated>grep -c '^IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt</automated>
    <fails_when>output is less than 2 — fewer than two runs in the pinned image were recorded, so there is no before-and-after pair on the runner's own rasteriser</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>any line whose path is not under .planning/ and whose status is "??" — a throwaway harness file was left behind</fails_when>
  </verify>

  <done>
    Both owned specs have a recorded outcome from a named run in the pinned image, taken before and after
    the repair on the same tree; the tests the serial group could not reach are listed by file and full
    title with a printed total and a stated delta against the figure plan 13 recorded; and every case still
    red carries a verdict naming the surviving cause and the file that owns it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remember the class — a standing test, and a verdict on the two host-surface flakes</name>

  <behavior>
    - Restoring the pre-repair shape of the host layout's nav slot on a mutated copy turns the new standing
      test RED, with a message naming the layout, the slot and the property.
    - An unmutated copy leaves it GREEN, and the green case says in its own docblock why it is the only
      case in the file that asserts green.
    - A second layout that legitimately streams a slot with a box-reserving placeholder stays GREEN — the
      property is about mounting the overlay primitive on both sides of a boundary, not about streaming.
    - The census cannot pass over nothing: with zero layouts discovered it fails, naming the walk rather
      than reporting a clean tree.
  </behavior>

  <read_first>
    - `tests/design/calendar-plate-month.test.tsx` — the three-way proof shape this repository uses for a
      "this is a pure function of its prop" claim: behavioural under two clocks, structural by census, and a
      real server-render-then-hydrate pass carrying a control required to fail. Read the control case
      especially; it is what stops the green being a green a decoy can counterfeit.
    - `tests/design/blocking-session-gate.test.ts` — the AST walk over a layout file, which is the closest
      analog for a structural assertion about a `<Suspense>` subtree in this repository.
    - `tests/design/listing-reuse-predicate-census.test.ts` — the recursive source walk, the comment-stripping
      that is itself proved load-bearing, the non-vacuity guard that stops a broken scan reporting a clean
      tree, and the pinned-count-with-named-constant shape.
    - `tests/design/workflow-invariants.test.ts` — the mutate-a-copy-then-spawn idiom and the assertion that
      the mutation actually differed from its input, which is what catches a drifted anchor.
    - `e2e/shell.spec.ts` — the host-composition case at the header-does-not-move describe, which is one of
      the two flakes this plan carries as a lead.
    - `e2e/host-listing-grid.spec.ts` — the case 19.1-13 recorded as the second host-surface flake.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the paragraph recording that the flaky
      set is redrawn on every run and that two of the newest were host surfaces.
  </read_first>

  <files>
    tests/design/suspense-fallback-overlay.test.ts,
    e2e/host-listing-grid.spec.ts,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt
  </files>

  <action>
STEP 1 — THE STANDING TEST.

Create `tests/design/suspense-fallback-overlay.test.ts`. A new file in that directory is collected
automatically; no config edit is needed and none is permitted.

The property it pins is the one Task 1 actually repaired, stated in the terms Task 1's transcript named —
not a paraphrase written from this plan. In the shape the measurement most likely supports: for every
layout under the app directory, a streaming boundary's fallback and its resolved child must not both
mount the component subtree that composes the app's one overlay primitive, because that primitive carries
an identity attribute generated per render which the two sides cannot agree on.

Build it in the census idiom:
  - a recursive walk over the tracked source, comment-stripped with the shared helper, with a case proving
    the stripping is load-bearing rather than decorative (the unstripped read must be observably different);
  - a non-vacuity guard: zero discovered layouts is a failure naming the walk, never a clean tree;
  - a pinned count of the discovered set in a named constant, with the docblock explaining why each member
    is in it;
  - failure messages that state what would have to be true, what the correct response is, and what the
    wrong response would be.

STEP 2 — PROVE IT BY PUTTING THE DEFECT BACK.

Restore the pre-repair shape of the nav slot on a MUTATED COPY, run the shipped test against it, and
capture the RED with its message into the evidence file under a heading naming the mutation, with an
`APPLIED=` marker at column zero recording that the mutated text differed from its input. A mutation that
silently did not apply has already, in this phase, reported a case surviving the deletion of its own
conjunct — the applied-count marker is what stops that.

Then two green controls, each with its own docblock saying why it asserts green: an unmutated copy, and a
copy whose boundary streams a slot with a box-reserving placeholder rather than the live control. The
second is what stops the property being read as "do not stream a nav slot".

STEP 3 — THE TWO LEADS, MEASURED AND GIVEN AN OUTCOME.

Run the two host-surface flakes 19.1-13 named — the header-does-not-move case in `e2e/shell.spec.ts` and
the case in `e2e/host-listing-grid.spec.ts` — at the runner's own shape, one worker and two retries,
repeated enough times to say something about a flake rather than about one draw. Record a line beginning
`LEAD ` per case with the observed attempt outcomes and a verdict: closed by this repair, unaffected, or
still unsettled with what would settle it. They are leads, not owned failures; a verdict of "unaffected" is
a legitimate outcome and is preferable to attributing them.

If either is still unsettled, record it in `deferred-items.md`'s shape as a proposed follow-up with what it
owns and its first instrument. Do not annotate either.

STEP 4 — CLOSE THE TRANSCRIPT.

Append a closing section to `evidence/triage-host-hydration.txt` carrying: the chosen repair shape and the
rejected ones with the constraint that decided each; the four hydration readings; the before/after
container results; the released set and its total; the leads' verdicts; and one line beginning
`SC4-TERM=` stating in one sentence what this plan closed and what it did not.
  </action>

  <acceptance_criteria>
    - `tests/design/suspense-fallback-overlay.test.ts` exists, is collected by the design suite, and contains
      at least one case whose name marks it as a control asserting green.
    - The evidence file contains a heading for the put-the-defect-back mutation, a line beginning `APPLIED=`
      at column zero, and the RED message the shipped test printed under that mutation.
    - The evidence file contains at least two lines beginning `LEAD ` at column zero, each carrying a verdict
      word.
    - The evidence file contains exactly one line beginning `SC4-TERM=`.
    - `npm run test:design` passes and its total passing count is greater than the count recorded at this
      phase's head.
    - `npm test` passes with no new failures.
    - `node scripts/verify-workflows.mjs` exits 0 and its summary still reports fifty-seven invariants —
      this plan adds no counted invariant and must not move that number.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/suspense-fallback-overlay.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reports fewer than four passing cases — the file must carry the property, its load-bearing-stripping proof, its non-vacuity guard and at least one green control</fails_when>
    <automated>npm run test:design</automated>
    <fails_when>non-zero exit, or any failed file in the reporter's summary, or a passing total not greater than 1431</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or a passing total below 2721, or any failed file in the reporter's summary</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or a summary line whose invariant total is not 57</fails_when>
    <automated>grep -c '^SC4-TERM=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt</automated>
    <fails_when>output is any number other than 1 — the plan either did not state what it closed, or stated it twice</fails_when>
  </verify>

  <done>
    Putting the pre-repair shape back on a mutated copy turns a standing design test red with a named
    message, two green controls stop the property being read as a ban on streaming, both host-surface flakes
    carry a written verdict, and the transcript closes with one sentence naming what this plan closed and
    what it left open.
  </done>
</task>

<task type="auto">
  <name>Task 4: Read the repair on a REAL push-triggered `ci` run, identified by the pushed head SHA</name>

  <read_first>
    - `.planning/phases/19.1-.../19.1-13-PLAN.md`, Task 1 and Task 3 — this phase's established recipe for
      measuring SC4 against a real run: push the development branch, let the WHOLE workflow run, and capture
      the run identifier, all five jobs with their conclusions and durations, and the functional job's summary
      block verbatim. This task reuses those commands rather than starting a second convention.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the recorded shape of that capture, with
      the command line above each block: the `gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url`
      identity read, the `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
      per-job read, and the `gh run view --job <jobId> --log` line that reaches the functional job's own
      output. Read the `COLLECTED` block too: a shortfall in the collected total looks identical to tests
      passing if only the failure count is read.
    - `.github/workflows/ci.yml` — the five `name:` strings, which are the rows of this measurement and are
      also the required-status-check contexts; `gate-e2e (functional Playwright suite)` is the one this plan's
      two owned specs run in. And the `concurrency:` block, whose group is keyed on the workflow, the ref and
      the event with `cancel-in-progress: true`.
    - `.planning/ROADMAP.md` §"Phase 19.1", Waves 8–11 — the recorded reason each of these four plans is its
      own wave: every one closes by reading a real run, and the next push cancels the previous plan's. Runs
      `33971557439` and `33971883942` died exactly that way inside this phase.
    - `.planning/phases/19.1-.../19.1-VERIFICATION.md` §"A Finding None of the Fourteen SUMMARYs Named" — that
      `dev` stood eight commits ahead of `origin/dev`, so the hardened workflow file SC1 spent this phase on
      had never been exercised by a real run. If those commits are still unpushed, this push is the first that
      carries them, and `gate-db-free`'s conclusion here is the first real reading of that work.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-04 and D-06 — D-04, that "not red on every push" is a
      property of the RUN and not of one job, which is why all five conclusions are recorded even though this
      plan owns specs in only one of them; D-06, that `dev` stays unprotected and GSD commits to it directly
      with `ci` running on every push so the result is visible, which is why this push is taken without a
      checkpoint.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt
  </files>

  <action>
This is the wave's ONE push. Nothing earlier in this plan pushes, and nothing after it does either: a second
push cancels this run through the concurrency group, which is how two runs already died in this phase.

STEP 1 — PUSH EVERYTHING THIS PLAN PRODUCED, AND RECORD WHAT WENT.

Push the development branch to its remote. Before and after, record what was carried: the count of commits
that were ahead of the remote before the push, their subjects, and the resulting head SHA. Write into
`evidence/ci-run-after-16.txt`, at column zero, a line `CI-HEAD-SHA=<sha>` carrying the full forty-character
SHA that is now the remote head. Everything below is joined to this value; it is the key, and the clock is
not.

If commits predating this plan were still unpushed, say so in one line and name the files they touch. The
workflow file this phase hardened is the one that matters: this run may be its first real exercise, and that
is a fact about what this run measures.

STEP 2 — FIND THE RUN FOR THAT SHA. DO NOT READ "THE LATEST RUN".

Ask for runs by commit rather than by recency:
`gh run list --workflow ci.yml --commit <sha> --json databaseId,headSha,event,status,conclusion,createdAt --limit 10`.
If this build of the CLI does not accept the commit filter, fall back to
`gh run list --workflow ci.yml --branch dev --json databaseId,headSha,event,status,conclusion,createdAt --limit 20`
and select the entries whose head SHA equals the recorded one. Either way the selection is by SHA equality,
never by position in the list.

Wait for the selected run to leave the in-progress state, then read its identity with the same command
19.1-13 used: `gh run view <id> --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url`,
with the command line above its JSON output. Record `CI-RUN-ID=<id>`, `CI-EVENT=<event>` and
`CI-CONCLUSION=<conclusion>` at column zero. The event must be the push event; a dispatch or a pull-request
run is not the thing SC4 is about.

STEP 3 — A CANCELLED RUN IS NOT A RESULT. IT IS A REASON TO MEASURE AGAIN.

If the selected run concluded cancelled, it measured nothing: the concurrency group killed it, and reading it
as either a green or a red would be inventing a verdict out of an interruption. Discard it by identity, record
its id on a line beginning `DISCARDED-RUN ` with the reason, and take the measurement again: re-run the
workflow for the same SHA with `gh run rerun <id>` and select the new run for that same head SHA, or if this
plan still has a commit to make, push it and select the run for the new head SHA. Either way the join key
stays the head SHA and the newly selected identity replaces the discarded one in `CI-RUN-ID=`.

Close this step with exactly one line `CANCELLED-DISCARDED=<n>` carrying the count of runs discarded this way
— zero is a legitimate value and it still gets written.

STEP 4 — RECORD ALL FIVE JOBS, THEN THE FAILURE LIST THIS PLAN IS ACCOUNTABLE FOR.

Read every job with `gh run view <id> --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'`
and write one line per job beginning `JOB ` at column zero carrying the display name, its conclusion and its
duration. All five, per D-04 — this plan owns specs in one job, but the criterion is about the run.

Then reach the functional job's own output with `gh run view --job <jobId> --log` and record its summary block
verbatim — failed, flaky, skipped, did not run, passed, wall-clock — followed by the full list of failing
specs and titles. Add one line `GATE-E2E=<conclusion>` at column zero.

Then the accountability lines. For each of the two specs this plan owns — `e2e/host-headings.spec.ts` and
`e2e/overflow-320.spec.ts` — write one line beginning `OWNED ` at column zero carrying the spec path, a
vertical bar, and one of the two words: absent, if no case from that file appears in the run's failure list;
present, if any does.

If either reads as still failing, THIS TASK IS NOT FINISHED and the plan does not claim its gap closed. Go
back to Task 2's step 4 with the run's own log as the instrument that named it, repair at the site that log
names, and push again — recording the superseded run's identity beside the new one, so the file reads as a
sequence of measurements rather than as a single claim. The prohibitions still hold: the instrument is not
weakened and no entry joins the allowlist.

STEP 5 — SAY WHAT IS STILL RED AND WHO OWNS IT.

`gate-e2e` may still be red on this run for the failures plans 17 and 18 own, and `gate-visual` is expected
red until plan 19 mints. That is the expected shape at this wave, and it is written down rather than left for
a reader to infer: one line beginning `STILL-RED ` per remaining failing spec, each naming the plan that owns
it. Close with one line `SC4-RUN-TERM=` stating in one sentence what this run proved and what it did not.
  </action>

  <acceptance_criteria>
    - `evidence/ci-run-after-16.txt` contains exactly one line beginning `CI-HEAD-SHA=` carrying a full
      forty-character SHA, and exactly one line each beginning `CI-RUN-ID=`, `CI-EVENT=` and `CI-CONCLUSION=`.
    - `CI-EVENT=` names the push event, and the recorded run's own head SHA equals the recorded `CI-HEAD-SHA=`
      value.
    - The file contains at least five lines beginning `JOB ` at column zero, one per job in
      `.github/workflows/ci.yml`, each carrying a conclusion.
    - The file contains exactly one line beginning `GATE-E2E=` and the functional job's summary block verbatim
      beneath the command line that produced it.
    - The file contains exactly two lines beginning `OWNED `, one per spec this plan owns, and neither is
      recorded as still present in the failure list.
    - The file contains exactly one line beginning `CANCELLED-DISCARDED=`, and one line beginning
      `DISCARDED-RUN ` for each run that value counts.
    - The file contains one line beginning `STILL-RED ` per remaining failing spec, each naming an owning
      plan, and exactly one line beginning `SC4-RUN-TERM=`.
    - `git log origin/dev..HEAD --oneline` produces no output — everything this plan committed is in the tree
      the run measured.
  </acceptance_criteria>

  <verify>
    <automated>gh run view $(grep -m1 '^CI-RUN-ID=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt | cut -d= -f2) --json event,conclusion,headSha --jq '[.event,.conclusion,.headSha]|@tsv'</automated>
    <fails_when>non-zero exit — the recorded id does not resolve to a real run; or the first field is not "push" — a dispatch or pull-request run was read instead of the push-triggered run SC4 is a property of; or the second field is "cancelled" — the concurrency group killed it and it measures nothing, so push again and read the next run rather than recording that value; or the third field does not equal the CI-HEAD-SHA= line in the same file — the run that was read is not the run that measured this tree</fails_when>
    <automated>grep -c '^JOB ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt</automated>
    <fails_when>output is less than 5 — fewer than the workflow's five jobs were recorded with a conclusion, so a claim about the RUN rests on a reading of one job, which is exactly what D-04 forbids</fails_when>
    <automated>grep -c '^OWNED ' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt</automated>
    <fails_when>output is any number other than 2 — the two specs this plan owns were not each given a verdict against the real run's failure list, or one was given two</fails_when>
    <automated>grep -cE '^OWNED .+\|present$' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt</automated>
    <fails_when>output is greater than 0 — a spec this plan owns still appears in the real run's failure list, so the repair that measured green on this laptop and in the local image did not hold on the runner; return to Task 2's diagnosis with this run's log and push again</fails_when>
    <automated>grep -c '^CANCELLED-DISCARDED=' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt</automated>
    <fails_when>output is any number other than 1 — the concurrency-group accounting is missing or doubled, and without it a cancelled run can be silently substituted for a measured one</fails_when>
    <automated>git log origin/dev..HEAD --oneline</automated>
    <fails_when>any output — a commit this plan made never reached the remote, so the run that was read measured a tree that is not this one</fails_when>
  </verify>

  <done>
    A named, push-triggered `ci` run selected by the pushed head SHA carries all five job conclusions in the
    evidence file, its functional job's summary block is recorded verbatim, both specs this plan owns are
    recorded absent from that run's failure list, every cancelled run is discarded by identity with a count,
    and every failure still standing is named with the plan that owns it.
  </done>

  <reversibility rating="reversible">
    The task writes one evidence file and pushes commits to the unprotected development branch. Nothing is
    published, no protected ref moves, and per D-06 `dev` is exactly the branch GSD commits to directly.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → `(host)` layout | The layout's session read and capability redirect are the hosting surface's real gate; the optimistic middleware and the UI mode switch can both be bypassed with a direct URL. |
| streamed boundary → hydrated client tree | The server's HTML and the client's first render must agree; where they do not, React discards the server tree, which changes which nodes exist and which handlers are attached. |
| commit author → `e2e/` and `tests/design/` | Anyone with commit access can weaken the instrument that reports a defect, which is a quieter change than weakening the product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-70 | Elevation of Privilege | `src/app/(host)/host/layout.tsx` — the two blocking gates | critical | mitigate | Constraint (a) in Task 1 forbids either gate moving into a streamed subtree, and `tests/design/blocking-session-gate.test.ts` is run as a task verify rather than assumed. |
| T-19.1-71 | Tampering | the failing specs this plan owns | high | mitigate | Prohibition: the instrument that named the failure is never weakened; Task 2 requires a verdict naming the surviving cause instead. |
| T-19.1-72 | Repudiation | the container reproduction | high | mitigate | Every container invocation records the image, the measured tree and the exit code as line-anchored markers, and the throwaway harness's deletion is proved by a collected-count comparison. |
| T-19.1-73 | Tampering | `tests/design/e2e-known-failures.test.ts` | high | mitigate | Untouched by this plan and run as a verify; the pinned entry total stays at two and both owned specs are barred from the list by written verdicts. |
| T-19.1-74 | Denial of Service | the `(host)` subtree's error boundary | medium | accept | A missing public configuration value still takes the whole host subtree down through the error boundary, in production as well as in CI. Recorded in `deferred-items.md` by plan 19.1-10 as a deployment-risk judgement; it is not this plan's subject and is not silently absorbed into it. |
| T-19.1-75 | Information Disclosure | evidence transcripts under `.planning/` | medium | mitigate | The transcripts record route paths, element names, attribute names, counts and exit codes. No session cookie value, no database URL and no environment value is echoed into a transcript. |
| T-19.1-76 | Repudiation | the real `ci` run this plan is read on | high | mitigate | The run is selected by the pushed head SHA rather than by recency, its identity is re-read as a task verify, a `cancelled` conclusion is discarded by identity and counted rather than read as a verdict, and `git log origin/dev..HEAD` asserts the measured tree is this one. Without the SHA join, the concurrency group's cancellation makes "the latest run" a different tree's verdict wearing this plan's name. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. No new dependency is introduced; the container image is the one already pinned in the workflow. If a task reaches for a package, the legitimacy gate runs then. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Changed production source:** the streaming nav slot in `src/app/(host)/host/layout.tsx`, reshaped to the
form Task 1's reading names, with the falsified byte-identity sentence replaced by the measurement. If the
placeholder route is taken, one new export beside `BellSlotSkeleton` in
`src/components/patterns/auth-slot-skeleton.tsx`.

**New end-to-end case:** one case in `e2e/shell.spec.ts` asserting a clean load of the host composition on
the served page, with its listeners armed before the navigation and a hydration proof before the absence.

**New design test:** `tests/design/suspense-fallback-overlay.test.ts` — the property, a proof that its
comment-stripping is load-bearing, a non-vacuity guard, a pinned discovered-set constant, and at least two
green controls.

**New evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-host-hydration.txt`
— four `HYDRATION-COUNT=` readings, React's own detail block, the chosen and rejected repair shapes, the
paired container transcripts with `IMAGE=` / `TREE=` / `CONTAINER-EXIT=` markers, `BEFORE `/`AFTER `/
`RELEASED `/`REGRESSED ` lines keyed on file and full title, `RELEASED-TOTAL=`, `DELTA-VS-13=`, the
`VERDICT ` lines, the `LEAD ` lines, and one `SC4-TERM=` line.

**Second new evidence file:**
`.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-16.txt`
— the real push-triggered `ci` run this plan is read on: `CI-HEAD-SHA=`, `CI-RUN-ID=`, `CI-EVENT=`,
`CI-CONCLUSION=`, five `JOB ` lines with conclusions and durations, `GATE-E2E=` with the functional job's
summary block verbatim, two `OWNED ` lines (one per spec this plan owns), `CANCELLED-DISCARDED=` with a
`DISCARDED-RUN ` line per discarded identity, `STILL-RED ` lines naming the plan that owns each remaining
failure, and one `SC4-RUN-TERM=` line.

**New npm scripts:** none. **New dependencies:** none. **New allowlist entries:** none — the pinned entry
total stays at two. **New counted workflow invariants:** none — the total stays at fifty-seven.
</artifacts_this_phase_produces>

<verification>
- `npx playwright test e2e/shell.spec.ts e2e/host-headings.spec.ts e2e/overflow-320.spec.ts e2e/host-listing-grid.spec.ts --project=chromium --workers=1 --retries=2` reports zero failed, zero flaky and zero not-run.
- `npm run test:design` and `npm test` both pass, with the design total above the 1431 recorded at this phase's head.
- `node scripts/verify-workflows.mjs` exits 0 with fifty-seven invariants.
- `npx tsc --noEmit` produces no `error TS` line beyond the seven pre-existing ones in `tests/design/`.
- `evidence/triage-host-hydration.txt` carries every marker the acceptance criteria name, and the checksum pair proving the Step 4 reversion was restored byte-for-byte.
- `evidence/ci-run-after-16.txt` names one push-triggered `ci` run whose head SHA equals the pushed head, carries all five job conclusions, and records both owned specs as absent from that run's `gate-e2e` failure list.
- `git log origin/dev..HEAD --oneline` produces no output.
- `git status --porcelain` shows no leftover throwaway harness file and no modification under `.github/` or `scripts/`.
</verification>

<success_criteria>
A `(host)` route loads without React reporting that it regenerated the tree, proved on the served page by a
case that first proves the page hydrated; reverting the repair turns that case red; `e2e/host-headings.spec.ts`
and `e2e/overflow-320.spec.ts` each carry a recorded before-and-after outcome from the pinned runner image;
the tests the serial group could not reach are named individually with a printed total and a stated delta
against the figure plan 13 recorded; the repaired property is remembered by a standing design test that
goes red when the defect is put back; and both owned specs are recorded absent from the `gate-e2e` failure
list of a NAMED, real, push-triggered `ci` run selected by the pushed head SHA — with every cancelled run
discarded by identity rather than read as a verdict.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-16-SUMMARY.md` when done.

State plainly, in the SUMMARY's own words: (1) what React's error detail actually named, and whether it
matched the component chain plan 13 recorded as the leading candidate or refuted it; (2) whether the repair
closed either owned spec on the runner's image, with the numbers; (3) the released set's total and the delta
against twenty-four; (4) the verdicts on the two leads; (5) the identity of the real `ci` run this plan was
read on — its id, its head SHA, its conclusion and the `gate-e2e` job's conclusion — together with the two
`OWNED ` verdicts and every run discarded as cancelled. If a still-red case survives, propose the follow-up
plan in the D-02 shape with what it owns and its first instrument — and do not annotate it.
</output>
