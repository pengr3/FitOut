GUARDS — PLAN 19.1-06, PRE-FIX MEASUREMENTS
===========================================

WHAT THIS FILE IS. Every mutation below was applied BY HAND to the TRACKED
`.github/workflows/ci.yml`, the SHIPPED `scripts/verify-workflows.mjs` was run against it, and the
result was captured verbatim. Then the file was reverted and the revert was PROVED — md5 before and
after, plus `git status --porcelain`. A mutation is a measurement, and a measurement left in the tree
is a defect.

WHY IT EXISTS. This plan's subject is the axis-versus-property error: a predicate that asserts a
key's PRESENCE without denying the modifiers that neuter it, or that LOCATES its subject by a
substring of a `run:` body. Both classes are invisible to a reading of the code — plan 19.1-05
demonstrated that by loosening the checker's own conjunct at `:982` and watching the standing suite
report 16 of 16 GREEN. So each site is MEASURED before it is repaired, and a site whose vector does
not reproduce is recorded as refuted rather than repaired on suspicion.

ELISION RULE, STATED SO AN OMISSION CANNOT BE MISTAKEN FOR A SELECTION. The checker closes each
section with a `parsed values` block — four-space-indented lowercase key lines dumping the whole
parsed tree. Those lines are IDENTICAL in every capture except where a mutation changed the value
they print, and they are hundreds of columns wide. They are filtered out of every transcript below by
`grep -vE '^ {4}[a-z]'`. NOTHING ELSE IS REMOVED: every heading, every FAIL line, every summary line
and every exit code is verbatim.

THE THREE TRIGGER VECTORS (Task 1) COME FIRST, THEN THE TWO STEP-ANCHOR VECTORS (Task 2).

================================================================================
TASK 1 / VECTOR CR-01(a) — the pull-request trigger restricted to a branch that does not exist
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. The key `pull_request` is still present, so a membership test cannot see this. Every gate in the file becomes unreachable from a pull request.

$ md5sum .github/workflows/ci.yml            # tracked file, at HEAD
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml

$ node <apply mutation "pr-branches" to the TRACKED file>
applied pr-branches

$ md5sum .github/workflows/ci.yml            # MUTATED
7a7489479669bac626801db7fcb8c037 *.github/workflows/ci.yml

$ git diff --unified=1 -- .github/workflows/ci.yml
@@ -711,2 +711,3 @@ on:
   pull_request:
+    branches: [does-not-exist]
 

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
verify-workflows — parse-based invariants over .github/workflows/
sections            baselines, ci, cross
@playwright/test    1.60.0 (installed)
expected image      mcr.microsoft.com/playwright:v1.60.0-noble

── baselines — .github/workflows/baselines.yml (job "generate-baselines") ──────────────────────
  ok    the ONLY trigger is workflow_dispatch (no push, no pull_request, no schedule)
          triggers=[workflow_dispatch]
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    job "generate-baselines" permissions.contents is 'write'
          job permissions={"contents":"write"}
  ok    exactly ONE job in this workflow holds contents: write
          jobs with contents:write=[generate-baselines]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    container image tag is pinned and equals the installed @playwright/test version
          image=mcr.microsoft.com/playwright:v1.60.0-noble  expected=mcr.microsoft.com/playwright:v1.60.0-noble  (@playwright/test=1.60.0)
  ok    container options carry --ipc=host
          container.options=--ipc=host
  ok    exactly ONE run command contains --update-snapshots
          run commands=8  carrying --update-snapshots=1
  ok    zero `secrets.` references in any env / run / with VALUE
          scanned workflow env, job env, container env, service env, and every step run/env/with — 0 hits
  ok    every `uses:` is a first-party actions/* action
          uses=[actions/checkout@v4, actions/setup-node@v4]
  ok    a run command stages exactly the *-visual-linux.png glob
          found: git add -- "*-visual-linux.png"

  parsed values (baselines):

── ci — .github/workflows/ci.yml (5 job(s)) ────────────────────────────────────────────────────
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    NO job in ci.yml holds contents: write — the write path is baselines.yml alone
          jobs with contents:write=[(none)]  jobs=[gate-db-free, gate-db, gate-price-parity, gate-visual, gate-e2e]
  ok    ci.yml runs on BOTH push and pull_request — CI-01's requirement text is about opening a pull request
          triggers=[push, pull_request]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    every `uses:` across every job is a first-party actions/* action
          uses=[gate-db-free:actions/checkout@v4, gate-db-free:actions/setup-node@v4, gate-db:actions/checkout@v4, gate-db:actions/setup-node@v4, gate-price-parity:actions/checkout@v4, gate-price-parity:actions/setup-node@v4, gate-visual:actions/checkout@v4, gate-visual:actions/setup-node@v4, gate-e2e:actions/checkout@v4, gate-e2e:actions/setup-node@v4]
  ok    ZERO run commands in ci.yml carry --update-snapshots — this file can never mint a baseline
          run commands=21 across 5 job(s)  carrying the flag=0
  ok    zero `secrets.` references in any env / run / with VALUE, across every job
          scanned workflow env, every job env, every container env, every service env, and every step run/env/with — 0 hits
  ok    no job, step or workflow env KEY declares RESEND_API_KEY — the e2e suite must send zero real email
          hits=[(none)]  scanned 5 job(s), their steps, and the workflow env  prefix=RESEND
  ok    every job declaring a container: pins the exact installed-Playwright image
          containerized=[gate-db-free=mcr.microsoft.com/playwright:v1.60.0-noble, gate-price-parity=mcr.microsoft.com/playwright:v1.60.0-noble, gate-visual=mcr.microsoft.com/playwright:v1.60.0-noble, gate-e2e=mcr.microsoft.com/playwright:v1.60.0-noble]  expected=mcr.microsoft.com/playwright:v1.60.0-noble
  ok    every containerized job carries --ipc=host
          options=[gate-db-free="--ipc=host", gate-price-parity="--ipc=host", gate-visual="--ipc=host", gate-e2e="--ipc=host"]
  ok    every service image is postgis/postgis: with a tag qualified beyond the bare major
          services=[gate-db.postgres=postgis/postgis:18-3.6, gate-price-parity.postgres=postgis/postgis:18-3.6, gate-visual.postgres=postgis/postgis:18-3.6, gate-e2e.postgres=postgis/postgis:18-3.6]
  ok    addressing rule — "gate-db-free" declares no services:, so DATABASE_URL must be the unreachable sentinel
          job="gate-db-free"  services=[]  container=true  authority=127.0.0.1:59999  expected=127.0.0.1:59999
  ok    addressing rule — "gate-db" runs ON the runner, so the host must be localhost and every service MUST declare ports:
          job="gate-db"  container=false  host=localhost  ports=[postgres=["5432:5432"]]
  ok    addressing rule — "gate-price-parity" runs IN a container, so the host must be a service LABEL and that service must declare NO ports:
          job="gate-price-parity"  container=true  host=postgres  service labels=[postgres]  ports=null
CHECKER-EXIT=0

$ git checkout -- .github/workflows/ci.yml
$ md5sum .github/workflows/ci.yml            # REVERTED — must equal the first md5 above
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml
$ git status --porcelain -- .github/workflows/ci.yml
(no line above = the working tree is clean; the measurement left nothing behind)

================================================================================
TASK 1 / VECTOR CR-01(b) — a path-ignore filter matching EVERYTHING under the pull-request trigger
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. Same shape, different modifier: the key survives, and `paths-ignore: ['**']` excludes every path there is.

$ md5sum .github/workflows/ci.yml            # tracked file, at HEAD
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml

$ node <apply mutation "pr-paths-ignore" to the TRACKED file>
applied pr-paths-ignore

$ md5sum .github/workflows/ci.yml            # MUTATED
8699e36a8d582b68cbc5468bfda6d1de *.github/workflows/ci.yml

$ git diff --unified=1 -- .github/workflows/ci.yml
@@ -711,2 +711,3 @@ on:
   pull_request:
+    paths-ignore: ['**']
 

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
verify-workflows — parse-based invariants over .github/workflows/
sections            baselines, ci, cross
@playwright/test    1.60.0 (installed)
expected image      mcr.microsoft.com/playwright:v1.60.0-noble

── baselines — .github/workflows/baselines.yml (job "generate-baselines") ──────────────────────
  ok    the ONLY trigger is workflow_dispatch (no push, no pull_request, no schedule)
          triggers=[workflow_dispatch]
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    job "generate-baselines" permissions.contents is 'write'
          job permissions={"contents":"write"}
  ok    exactly ONE job in this workflow holds contents: write
          jobs with contents:write=[generate-baselines]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    container image tag is pinned and equals the installed @playwright/test version
          image=mcr.microsoft.com/playwright:v1.60.0-noble  expected=mcr.microsoft.com/playwright:v1.60.0-noble  (@playwright/test=1.60.0)
  ok    container options carry --ipc=host
          container.options=--ipc=host
  ok    exactly ONE run command contains --update-snapshots
          run commands=8  carrying --update-snapshots=1
  ok    zero `secrets.` references in any env / run / with VALUE
          scanned workflow env, job env, container env, service env, and every step run/env/with — 0 hits
  ok    every `uses:` is a first-party actions/* action
          uses=[actions/checkout@v4, actions/setup-node@v4]
  ok    a run command stages exactly the *-visual-linux.png glob
          found: git add -- "*-visual-linux.png"

  parsed values (baselines):

── ci — .github/workflows/ci.yml (5 job(s)) ────────────────────────────────────────────────────
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    NO job in ci.yml holds contents: write — the write path is baselines.yml alone
          jobs with contents:write=[(none)]  jobs=[gate-db-free, gate-db, gate-price-parity, gate-visual, gate-e2e]
  ok    ci.yml runs on BOTH push and pull_request — CI-01's requirement text is about opening a pull request
          triggers=[push, pull_request]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    every `uses:` across every job is a first-party actions/* action
          uses=[gate-db-free:actions/checkout@v4, gate-db-free:actions/setup-node@v4, gate-db:actions/checkout@v4, gate-db:actions/setup-node@v4, gate-price-parity:actions/checkout@v4, gate-price-parity:actions/setup-node@v4, gate-visual:actions/checkout@v4, gate-visual:actions/setup-node@v4, gate-e2e:actions/checkout@v4, gate-e2e:actions/setup-node@v4]
  ok    ZERO run commands in ci.yml carry --update-snapshots — this file can never mint a baseline
          run commands=21 across 5 job(s)  carrying the flag=0
  ok    zero `secrets.` references in any env / run / with VALUE, across every job
          scanned workflow env, every job env, every container env, every service env, and every step run/env/with — 0 hits
  ok    no job, step or workflow env KEY declares RESEND_API_KEY — the e2e suite must send zero real email
          hits=[(none)]  scanned 5 job(s), their steps, and the workflow env  prefix=RESEND
  ok    every job declaring a container: pins the exact installed-Playwright image
          containerized=[gate-db-free=mcr.microsoft.com/playwright:v1.60.0-noble, gate-price-parity=mcr.microsoft.com/playwright:v1.60.0-noble, gate-visual=mcr.microsoft.com/playwright:v1.60.0-noble, gate-e2e=mcr.microsoft.com/playwright:v1.60.0-noble]  expected=mcr.microsoft.com/playwright:v1.60.0-noble
  ok    every containerized job carries --ipc=host
          options=[gate-db-free="--ipc=host", gate-price-parity="--ipc=host", gate-visual="--ipc=host", gate-e2e="--ipc=host"]
  ok    every service image is postgis/postgis: with a tag qualified beyond the bare major
          services=[gate-db.postgres=postgis/postgis:18-3.6, gate-price-parity.postgres=postgis/postgis:18-3.6, gate-visual.postgres=postgis/postgis:18-3.6, gate-e2e.postgres=postgis/postgis:18-3.6]
  ok    addressing rule — "gate-db-free" declares no services:, so DATABASE_URL must be the unreachable sentinel
          job="gate-db-free"  services=[]  container=true  authority=127.0.0.1:59999  expected=127.0.0.1:59999
  ok    addressing rule — "gate-db" runs ON the runner, so the host must be localhost and every service MUST declare ports:
          job="gate-db"  container=false  host=localhost  ports=[postgres=["5432:5432"]]
  ok    addressing rule — "gate-price-parity" runs IN a container, so the host must be a service LABEL and that service must declare NO ports:
          job="gate-price-parity"  container=true  host=postgres  service labels=[postgres]  ports=null
CHECKER-EXIT=0

$ git checkout -- .github/workflows/ci.yml
$ md5sum .github/workflows/ci.yml            # REVERTED — must equal the first md5 above
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml
$ git status --porcelain -- .github/workflows/ci.yml
(no line above = the working tree is clean; the measurement left nothing behind)

================================================================================
TASK 1 / VECTOR CR-01(c) — the DEFAULT branch removed from the push trigger's branch list
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. `push` is still a key and still filtered; it simply no longer reaches the branch plan 15's ruleset protects.

$ md5sum .github/workflows/ci.yml            # tracked file, at HEAD
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml

$ node <apply mutation "push-no-main" to the TRACKED file>
applied push-no-main

$ md5sum .github/workflows/ci.yml            # MUTATED
453934ef656e23d835a365b641347462 *.github/workflows/ci.yml

$ git diff --unified=1 -- .github/workflows/ci.yml
@@ -709,3 +709,3 @@ on:
   push:
-    branches: [dev, main]
+    branches: [dev]
   pull_request:

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
verify-workflows — parse-based invariants over .github/workflows/
sections            baselines, ci, cross
@playwright/test    1.60.0 (installed)
expected image      mcr.microsoft.com/playwright:v1.60.0-noble

── baselines — .github/workflows/baselines.yml (job "generate-baselines") ──────────────────────
  ok    the ONLY trigger is workflow_dispatch (no push, no pull_request, no schedule)
          triggers=[workflow_dispatch]
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    job "generate-baselines" permissions.contents is 'write'
          job permissions={"contents":"write"}
  ok    exactly ONE job in this workflow holds contents: write
          jobs with contents:write=[generate-baselines]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    container image tag is pinned and equals the installed @playwright/test version
          image=mcr.microsoft.com/playwright:v1.60.0-noble  expected=mcr.microsoft.com/playwright:v1.60.0-noble  (@playwright/test=1.60.0)
  ok    container options carry --ipc=host
          container.options=--ipc=host
  ok    exactly ONE run command contains --update-snapshots
          run commands=8  carrying --update-snapshots=1
  ok    zero `secrets.` references in any env / run / with VALUE
          scanned workflow env, job env, container env, service env, and every step run/env/with — 0 hits
  ok    every `uses:` is a first-party actions/* action
          uses=[actions/checkout@v4, actions/setup-node@v4]
  ok    a run command stages exactly the *-visual-linux.png glob
          found: git add -- "*-visual-linux.png"

  parsed values (baselines):

── ci — .github/workflows/ci.yml (5 job(s)) ────────────────────────────────────────────────────
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    NO job in ci.yml holds contents: write — the write path is baselines.yml alone
          jobs with contents:write=[(none)]  jobs=[gate-db-free, gate-db, gate-price-parity, gate-visual, gate-e2e]
  ok    ci.yml runs on BOTH push and pull_request — CI-01's requirement text is about opening a pull request
          triggers=[push, pull_request]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    every `uses:` across every job is a first-party actions/* action
          uses=[gate-db-free:actions/checkout@v4, gate-db-free:actions/setup-node@v4, gate-db:actions/checkout@v4, gate-db:actions/setup-node@v4, gate-price-parity:actions/checkout@v4, gate-price-parity:actions/setup-node@v4, gate-visual:actions/checkout@v4, gate-visual:actions/setup-node@v4, gate-e2e:actions/checkout@v4, gate-e2e:actions/setup-node@v4]
  ok    ZERO run commands in ci.yml carry --update-snapshots — this file can never mint a baseline
          run commands=21 across 5 job(s)  carrying the flag=0
  ok    zero `secrets.` references in any env / run / with VALUE, across every job
          scanned workflow env, every job env, every container env, every service env, and every step run/env/with — 0 hits
  ok    no job, step or workflow env KEY declares RESEND_API_KEY — the e2e suite must send zero real email
          hits=[(none)]  scanned 5 job(s), their steps, and the workflow env  prefix=RESEND
  ok    every job declaring a container: pins the exact installed-Playwright image
          containerized=[gate-db-free=mcr.microsoft.com/playwright:v1.60.0-noble, gate-price-parity=mcr.microsoft.com/playwright:v1.60.0-noble, gate-visual=mcr.microsoft.com/playwright:v1.60.0-noble, gate-e2e=mcr.microsoft.com/playwright:v1.60.0-noble]  expected=mcr.microsoft.com/playwright:v1.60.0-noble
  ok    every containerized job carries --ipc=host
          options=[gate-db-free="--ipc=host", gate-price-parity="--ipc=host", gate-visual="--ipc=host", gate-e2e="--ipc=host"]
  ok    every service image is postgis/postgis: with a tag qualified beyond the bare major
          services=[gate-db.postgres=postgis/postgis:18-3.6, gate-price-parity.postgres=postgis/postgis:18-3.6, gate-visual.postgres=postgis/postgis:18-3.6, gate-e2e.postgres=postgis/postgis:18-3.6]
  ok    addressing rule — "gate-db-free" declares no services:, so DATABASE_URL must be the unreachable sentinel
          job="gate-db-free"  services=[]  container=true  authority=127.0.0.1:59999  expected=127.0.0.1:59999
  ok    addressing rule — "gate-db" runs ON the runner, so the host must be localhost and every service MUST declare ports:
          job="gate-db"  container=false  host=localhost  ports=[postgres=["5432:5432"]]
  ok    addressing rule — "gate-price-parity" runs IN a container, so the host must be a service LABEL and that service must declare NO ports:
          job="gate-price-parity"  container=true  host=postgres  service labels=[postgres]  ports=null
CHECKER-EXIT=0

$ git checkout -- .github/workflows/ci.yml
$ md5sum .github/workflows/ci.yml            # REVERTED — must equal the first md5 above
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml
$ git status --porcelain -- .github/workflows/ci.yml
(no line above = the working tree is clean; the measurement left nothing behind)

================================================================================
TASK 1 / VECTOR WR-02 — the trigger block rewritten in the DOCUMENTED SEQUENCE spelling
================================================================================
EXPECTED BEFORE THE REPAIR: RED — AND THIS ONE IS THE FALSE RED. `on: [push, pull_request]` is a CORRECT file. `triggersOf` does `Object.keys` over an array, so the trigger names come back as the array INDICES "0" and "1" and the membership conjunct fails. A check that reddens a correct file is the check that gets deleted rather than repaired, which is why the form-aware accessor is a PREREQUISITE for the CR-01 fix and not an optional tidy.

$ md5sum .github/workflows/ci.yml            # tracked file, at HEAD
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml

$ node <apply mutation "sequence-form" to the TRACKED file>
applied sequence-form

$ md5sum .github/workflows/ci.yml            # MUTATED
6d1408f8ff050dae5ed916201573f1ac *.github/workflows/ci.yml

$ git diff --unified=1 -- .github/workflows/ci.yml
@@ -707,6 +707,3 @@ name: ci
 
-on:
-  push:
-    branches: [dev, main]
-  pull_request:
+on: [push, pull_request]
 

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
verify-workflows — parse-based invariants over .github/workflows/
sections            baselines, ci, cross
@playwright/test    1.60.0 (installed)
expected image      mcr.microsoft.com/playwright:v1.60.0-noble

── baselines — .github/workflows/baselines.yml (job "generate-baselines") ──────────────────────
  ok    the ONLY trigger is workflow_dispatch (no push, no pull_request, no schedule)
          triggers=[workflow_dispatch]
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    job "generate-baselines" permissions.contents is 'write'
          job permissions={"contents":"write"}
  ok    exactly ONE job in this workflow holds contents: write
          jobs with contents:write=[generate-baselines]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    container image tag is pinned and equals the installed @playwright/test version
          image=mcr.microsoft.com/playwright:v1.60.0-noble  expected=mcr.microsoft.com/playwright:v1.60.0-noble  (@playwright/test=1.60.0)
  ok    container options carry --ipc=host
          container.options=--ipc=host
  ok    exactly ONE run command contains --update-snapshots
          run commands=8  carrying --update-snapshots=1
  ok    zero `secrets.` references in any env / run / with VALUE
          scanned workflow env, job env, container env, service env, and every step run/env/with — 0 hits
  ok    every `uses:` is a first-party actions/* action
          uses=[actions/checkout@v4, actions/setup-node@v4]
  ok    a run command stages exactly the *-visual-linux.png glob
          found: git add -- "*-visual-linux.png"

  parsed values (baselines):

── ci — .github/workflows/ci.yml (5 job(s)) ────────────────────────────────────────────────────
  ok    workflow-level permissions.contents is 'read'
          workflow permissions={"contents":"read"}
  ok    NO job in ci.yml holds contents: write — the write path is baselines.yml alone
          jobs with contents:write=[(none)]  jobs=[gate-db-free, gate-db, gate-price-parity, gate-visual, gate-e2e]
  FAIL  ci.yml runs on BOTH push and pull_request — CI-01's requirement text is about opening a pull request
          triggers=[0, 1]
  ok    github.event_name is in the concurrency group
          concurrency.group=${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  ok    every `uses:` across every job is a first-party actions/* action
          uses=[gate-db-free:actions/checkout@v4, gate-db-free:actions/setup-node@v4, gate-db:actions/checkout@v4, gate-db:actions/setup-node@v4, gate-price-parity:actions/checkout@v4, gate-price-parity:actions/setup-node@v4, gate-visual:actions/checkout@v4, gate-visual:actions/setup-node@v4, gate-e2e:actions/checkout@v4, gate-e2e:actions/setup-node@v4]
  ok    ZERO run commands in ci.yml carry --update-snapshots — this file can never mint a baseline
          run commands=21 across 5 job(s)  carrying the flag=0
  ok    zero `secrets.` references in any env / run / with VALUE, across every job
          scanned workflow env, every job env, every container env, every service env, and every step run/env/with — 0 hits
  ok    no job, step or workflow env KEY declares RESEND_API_KEY — the e2e suite must send zero real email
          hits=[(none)]  scanned 5 job(s), their steps, and the workflow env  prefix=RESEND
  ok    every job declaring a container: pins the exact installed-Playwright image
          containerized=[gate-db-free=mcr.microsoft.com/playwright:v1.60.0-noble, gate-price-parity=mcr.microsoft.com/playwright:v1.60.0-noble, gate-visual=mcr.microsoft.com/playwright:v1.60.0-noble, gate-e2e=mcr.microsoft.com/playwright:v1.60.0-noble]  expected=mcr.microsoft.com/playwright:v1.60.0-noble
  ok    every containerized job carries --ipc=host
          options=[gate-db-free="--ipc=host", gate-price-parity="--ipc=host", gate-visual="--ipc=host", gate-e2e="--ipc=host"]
  ok    every service image is postgis/postgis: with a tag qualified beyond the bare major
          services=[gate-db.postgres=postgis/postgis:18-3.6, gate-price-parity.postgres=postgis/postgis:18-3.6, gate-visual.postgres=postgis/postgis:18-3.6, gate-e2e.postgres=postgis/postgis:18-3.6]
  ok    addressing rule — "gate-db-free" declares no services:, so DATABASE_URL must be the unreachable sentinel
          job="gate-db-free"  services=[]  container=true  authority=127.0.0.1:59999  expected=127.0.0.1:59999
  ok    addressing rule — "gate-db" runs ON the runner, so the host must be localhost and every service MUST declare ports:
          job="gate-db"  container=false  host=localhost  ports=[postgres=["5432:5432"]]
  ok    addressing rule — "gate-price-parity" runs IN a container, so the host must be a service LABEL and that service must declare NO ports:
          job="gate-price-parity"  container=true  host=postgres  service labels=[postgres]  ports=null
CHECKER-EXIT=1

$ git checkout -- .github/workflows/ci.yml
$ md5sum .github/workflows/ci.yml            # REVERTED — must equal the first md5 above
925db7b91bccc2d1ab1220b1441e1e58 *.github/workflows/ci.yml
$ git status --porcelain -- .github/workflows/ci.yml
(no line above = the working tree is clean; the measurement left nothing behind)


################################################################################
# TASK 2 — THE STEP-ANCHOR VECTORS
#
# The checker used for every capture in THIS SECTION is the one committed by Task 1 (3e87c8d) —
# i.e. WITH the trigger repair and WITHOUT the step-anchor repair. It was restored with
# `git checkout HEAD -- scripts/verify-workflows.mjs` and its md5 is printed above this block in
# the session transcript. Task 2's working-tree changes were set aside for the duration and put
# back afterwards; the checker's md5 was compared before and after and is identical.
#
# ⚠ A NOTE ON THE HARNESS ITSELF, RECORDED BECAUSE IT IS THE SAME DEFECT CLASS THIS PLAN IS ABOUT.
# The first run of these mutations used an UNSCOPED `indexOf("      - name: Migrate the database")`.
# That string appears in THREE jobs of `ci.yml`, and the first is `gate-price-parity` — so two
# decoys landed in the wrong job and produced three reds attributed to vectors they had nothing to
# do with. It was caught because the FAIL lines named invariants the vectors were not about. Every
# anchor below is scoped to a job key first. A mutation harness that locates its subject by an
# ambiguous string is measuring something other than what it claims, exactly as a checker is.
################################################################################

================================================================================
TASK 2 / VECTOR CR-02-DECOY-ONLY — a decoy step, ahead of the refusal step, whose run merely MENTIONS the refusal script
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. This is the sharpest form of CR-02. The predicate finds the refusal step with `findIndex(s => run.includes(<script path>))` — the FIRST step whose run body mentions the script — so the decoy BECOMES the anchor. The preceding set is then `slice(0, <decoy index>)`, which does not contain the decoy: THE DECOY EXCLUDES ITSELF FROM THE SET IT VIOLATES. The invariant's printed name says only checkout, setup-node and `npm ci` may precede the refusal step, and here is a fourth thing preceding it under a green check.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "cr02-decoy-only">
applied cr02-decoy-only

$ md5sum .github/workflows/ci.yml            # MUTATED
008ee50829bd91d692c4ee6e3c977041 *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1606,0 +1607,2 @@ jobs:
+      - name: A decoy step whose run merely mentions the refusal script
+        run: echo "this step mentions scripts/refuse-mail-credential.mjs and executes nothing"

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
CHECKER-EXIT=0

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / VECTOR CR-02 — the same decoy, WITH the real refusal step moved to the END of the job
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. The consequence of the above, stated as damage: the only control that protects the CURRENT run now fires AFTER migrate, seed and the entire Playwright suite — i.e. after the mail this invariant exists to prevent has already been sent. Invariants A and B stay green throughout, because the step they anchor still exists and still carries the exact invocation. It is in the wrong PLACE, and this is the only predicate that was supposed to notice.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "cr02-decoy-and-move">
applied cr02-decoy-and-move

$ md5sum .github/workflows/ci.yml            # MUTATED
9be45b3a73f579dbb407c89ea536e1cc *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1607,2 +1606,0 @@ jobs:
-      - name: Refuse to run the suite with a live mail credential in the environment
-        run: node scripts/refuse-mail-credential.mjs
@@ -1612,0 +1611,2 @@ jobs:
+      - name: A decoy step whose run merely mentions the refusal script
+        run: echo "this step mentions scripts/refuse-mail-credential.mjs and executes nothing"
@@ -1633,0 +1634,2 @@ jobs:
+      - name: Refuse to run the suite with a live mail credential in the environment
+        run: node scripts/refuse-mail-credential.mjs

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
CHECKER-EXIT=0

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / CONTROL — the refusal step moved to the end WITHOUT a decoy
================================================================================
EXPECTED BEFORE THE REPAIR: RED, already, before any repair. Nothing captures the anchor, so the real step's own late index fails the comparison honestly. Recorded so the two captures above cannot be misread as "the ordering predicate does nothing": it works, and a decoy is what disables it.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "refusal-moved-last">
applied refusal-moved-last

$ md5sum .github/workflows/ci.yml            # MUTATED
0988816794a89fd68eb896d7e853538b *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1607,2 +1606,0 @@ jobs:
-      - name: Refuse to run the suite with a live mail credential in the environment
-        run: node scripts/refuse-mail-credential.mjs
@@ -1633,0 +1632,2 @@ jobs:
+      - name: Refuse to run the suite with a live mail credential in the environment
+        run: node scripts/refuse-mail-credential.mjs

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  FAIL  "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
1 of 55 invariant(s) FAILED:
CHECKER-EXIT=1

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / VECTOR ROW-5 — a decoy mentioning the seed command, WITH the real seed step moved after the suite
================================================================================
EXPECTED BEFORE THE REPAIR: UNKNOWN AT CAPTURE TIME. 19.1-RESEARCH.md's inventory flags row 5 as NOT YET REPRODUCED, so this capture is the reproduction and its result decides whether the row is repaired or recorded as refuted. A green confirms the vector; a red refutes it. Both are findings and neither was assumed.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "row5-decoy-and-move">
applied row5-decoy-and-move

$ md5sum .github/workflows/ci.yml            # MUTATED
3933293b9d637d3d14afa5a41cd4b222 *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1608,0 +1609,2 @@ jobs:
+      - name: A decoy step whose run merely mentions the seed command
+        run: echo "this step mentions npm run db:seed and seeds nothing"
@@ -1621,2 +1622,0 @@ jobs:
-      - name: Seed the demo catalogue
-        run: npm run db:seed
@@ -1633,0 +1634,2 @@ jobs:
+      - name: Seed the demo catalogue
+        run: npm run db:seed

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
CHECKER-EXIT=0

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / VECTOR VISUAL-ORDER — the same shape one job over: a decoy mentioning the baseline seed, with gate-visual's real seed step moved after its Playwright step
================================================================================
EXPECTED BEFORE THE REPAIR: UNKNOWN AT CAPTURE TIME (inventory row 6). Watch WHICH invariant reddens, not merely whether one does.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "visual-decoy-and-move">
applied visual-decoy-and-move

$ md5sum .github/workflows/ci.yml            # MUTATED
61f1818ba4d495bff0912f803fc55287 *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1411,0 +1412,2 @@ jobs:
+      - name: A decoy step whose run merely mentions the baseline seed
+        run: echo "this step mentions scripts/seed-baseline-fixtures.ts and seeds nothing"
@@ -1426,2 +1427,0 @@ jobs:
-      - name: Seed the baseline fixtures
-        run: npx tsx scripts/seed-baseline-fixtures.ts
@@ -1436,0 +1437,2 @@ jobs:
+      - name: Seed the baseline fixtures
+        run: npx tsx scripts/seed-baseline-fixtures.ts

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
  FAIL  the two visual jobs' seed run commands are byte-identical and both name scripts/seed-baseline-fixtures.ts
1 of 55 invariant(s) FAILED:
CHECKER-EXIT=1

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / VECTOR VISUAL-PROJECT — a decoy naming the visual project, with gate-visual's REAL Playwright step switched to a different project
================================================================================
EXPECTED BEFORE THE REPAIR: UNKNOWN AT CAPTURE TIME (inventory row 6, second half). gate-visual exists to run GATE-01's comparison against 52 committed baselines.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "visual-project-decoy">
applied visual-project-decoy

$ md5sum .github/workflows/ci.yml            # MUTATED
92275b4a4afe2073f6e81586d2027561 *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1427,0 +1428,4 @@ jobs:
+      - name: Playwright — visual regression (GATE-01)
+        run: npx playwright test --project=chromium
+      - name: A decoy step whose run merely mentions the visual project
+        run: echo "this step mentions npx playwright test --project=visual and runs nothing"
@@ -1435,2 +1438,0 @@ jobs:
-      - name: Playwright — visual regression (GATE-01)
-        run: npx playwright test --project=visual

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
CHECKER-EXIT=0

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)

================================================================================
TASK 2 / VECTOR VACUOUS-PRECEDE — the refusal step promoted to the FIRST step of the job
================================================================================
EXPECTED BEFORE THE REPAIR: UNKNOWN AT CAPTURE TIME. Not on RESEARCH.md's inventory at all; found while reading the predicate for the CR-02 repair. `[].every(…)` is TRUE, so a step with NOTHING before it may satisfy "only these three may precede it" by having nothing precede it — and the refusal step first means it runs before `actions/checkout`, so the script it invokes is not on disk yet.

$ md5sum .github/workflows/ci.yml            # working tree (comment-only diff from HEAD)
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml

$ node <apply mutation "refusal-first">
applied refusal-first

$ md5sum .github/workflows/ci.yml            # MUTATED
71ed0c5036b0310300cec84ee4930e8c *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/ci.yml   # the edit, in full
@@ -1566,0 +1567,2 @@ jobs:
+      - name: Refuse to run the suite with a live mail credential in the environment
+        run: node scripts/refuse-mail-credential.mjs
@@ -1607,2 +1608,0 @@ jobs:
-      - name: Refuse to run the suite with a live mail credential in the environment
-        run: node scripts/refuse-mail-credential.mjs

$ node scripts/verify-workflows.mjs   # (parsed-values block elided — see ELISION RULE)
  ok    "gate-e2e" declares the exact display name branch protection matches on
  ok    "gate-e2e" runs the functional project BY NAME (--project=chromium)
  ok    "gate-e2e" is unconditional — no if:, no continue-on-error: AT ALL, on the JOB or on ANY STEP
  ok    "gate-e2e" runs its steps with the runner's DEFAULT interpreter — NO defaults: block at workflow level or on the job
  ok    "gate-e2e" seeds the demo catalogue BEFORE it runs the suite
  ok    "gate-e2e"'s mail refusal reads the REAL process environment — no ${{ }} expression, no env: map, exact invocation, NO ARGUMENTS, and NO KEY OUTSIDE [name, run]
  ok    "gate-e2e" refuses a live mail credential BEFORE it migrates, seeds or boots the suite — and ONLY actions/checkout@, actions/setup-node@ and `npm ci` may precede it, over the FULL step list
  ok    "gate-visual" runs IN the pinned Playwright container
  ok    "gate-visual" declares a `postgres` service
  ok    "gate-visual" holds no permissions: block — it inherits the workflow default contents: read
  ok    "gate-visual" runs the visual project BY NAME (--project=visual)
  ok    "gate-visual" runs migrate, then seed, then playwright — IN THAT ORDER
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
CHECKER-EXIT=0

$ <restore ci.yml>  && md5sum
2a74342bea34f2ca733efa150979b5ea *.github/workflows/ci.yml
$ git diff -U0 -- .github/workflows/ci.yml | grep -E '^[+-][^+-]' | grep -cv '^[+-][[:space:]]*#'
0
(0 = only comment lines differ from HEAD; the measurement left nothing behind)


################################################################################
# TASK 3 — THE TWO SITES THE RESEARCH INVENTORY DID NOT LIST
#
# 19.1-RESEARCH.md inventoried TEN anchoring and presence sites. This plan's audit was carried out
# as a SCAN of the whole checker for the SHAPE — every `.includes(`, `.some(`, `.find(`,
# `.findIndex(`, `.startsWith(` in the file — rather than as a walk of those ten. It found two more,
# both in sections the plan's task text never mentions, and both reproduce.
#
# ⚠ THE FIRST ATTEMPT AT VECTOR STAGE-GLOB WAS A FALSE REFUTATION, AND IT IS RECORDED RATHER THAN
# DISCARDED. The decoy was written as a plain YAML scalar containing backslash-escaped quotes, so
# the string that reached the parser was `git add -- \"*-visual-linux.png\"` — which does NOT
# contain the needle. The checker went red and the row looked REFUTED. It was the harness that was
# broken, not the vector. The decoy below is a BLOCK scalar and the parsed run body was verified to
# contain the needle before the verdict was written. A refutation is a finding, and a finding has to
# be right; "the mutation did not fire" and "the mutation did not apply" look identical from the
# outside, which is what `withMutatedWorkflows`'s differs-from-input assertion exists to separate,
# and what a hand measurement has to check by hand.
################################################################################

================================================================================
TASK 3 / VECTOR STAGE-GLOB (audit row 11) — a decoy PRINTING the staging command, with the real staging step changed to a glob that matches nothing
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN. The invariant reads `runs.some(r => r.includes(<the glob>))` — an existential over EVERY run body in the regeneration job, which any echo satisfies. The job would stage nothing, commit nothing and report success, and this tripwire's entire purpose is to notice what that commit contains.

$ md5sum .github/workflows/*.yml
01c1dce48118f62d4151e196a67de7fa *.github/workflows/baselines.yml
9c515f96e51ff058d74550a157288364 *.github/workflows/ci.yml

$ python <apply mutation "stage-glob-decoy">
applied stage-glob-decoy to baselines.yml (decoy prints the glob; the REAL step now stages a glob that matches nothing)

$ md5sum .github/workflows/*.yml            # MUTATED
e8be137ae28966bb11e5108299680b19 *.github/workflows/baselines.yml
9c515f96e51ff058d74550a157288364 *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/
diff --git a/.github/workflows/baselines.yml b/.github/workflows/baselines.yml
@@ -369,0 +370,3 @@ jobs:
+      - name: A decoy step that merely prints the staging command
+        run: |
+          echo git add -- "*-visual-linux.png"
@@ -373 +376 @@ jobs:
-          git add -- "*-visual-linux.png"
+          git add -- "*-NOTHING-AT-ALL.png"

$ node scripts/verify-workflows.mjs
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
  ok    a run command stages exactly the *-visual-linux.png glob
  ok    the two visual jobs' migrate run commands are byte-identical
CHECKER-EXIT=0

$ git checkout -- .github/workflows/ ; git status --porcelain -- .github/workflows/
(no line above = clean revert; the measurement left nothing behind)

================================================================================
TASK 3 / VECTOR CROSS-PICK (audit row 12) — IDENTICAL decoy steps in BOTH visual jobs, with the two REAL migrate commands made to differ
================================================================================
EXPECTED BEFORE THE REPAIR: GREEN, AND THIS IS THE NASTIEST VECTOR IN THE PLAN. The comparison locates each command with `runs.find(r => r.includes("db:migrate"))` on BOTH sides. A decoy in ONE job makes the picks differ and goes red — which is why this site looked safe. IDENTICAL decoys in BOTH make the picks identical TO EACH OTHER, and the byte-identity comparison then holds between two strings that are neither job's real command. The machine that COMPARES would migrate differently from the machine that SHOT: exactly and only what D-27 exists to prevent, and what run 32216145319 already cost once.

$ md5sum .github/workflows/*.yml
01c1dce48118f62d4151e196a67de7fa *.github/workflows/baselines.yml
9c515f96e51ff058d74550a157288364 *.github/workflows/ci.yml

$ python <apply mutation "cross-pick-decoy">
applied cross-pick-decoy: IDENTICAL decoy steps in BOTH jobs, and the two REAL migrate commands made to differ

$ md5sum .github/workflows/*.yml            # MUTATED
b2cfb3bfa2c199a3ffdf8f7c3e7f9817 *.github/workflows/baselines.yml
04d72c3486b7536b3c27d26829c1bdaf *.github/workflows/ci.yml

$ git diff --unified=0 -- .github/workflows/
diff --git a/.github/workflows/baselines.yml b/.github/workflows/baselines.yml
@@ -324,0 +325,2 @@ jobs:
+      - name: A decoy step that merely prints the migrate command
+        run: echo npm run db:migrate
@@ -326 +328 @@ jobs:
-        run: npm run db:migrate
+        run: npm run db:migrate --baselines-only
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
@@ -1442,0 +1443,2 @@ jobs:
+      - name: A decoy step that merely prints the migrate command
+        run: echo npm run db:migrate
@@ -1444 +1446 @@ jobs:
-        run: npm run db:migrate
+        run: npm run db:migrate --ci-only

$ node scripts/verify-workflows.mjs
All 55 invariants hold across 3 section(s) (baselines=11, ci=36, cross=8).
  ok    a run command stages exactly the *-visual-linux.png glob
  ok    the two visual jobs' migrate run commands are byte-identical
CHECKER-EXIT=0

$ git checkout -- .github/workflows/ ; git status --porcelain -- .github/workflows/
(no line above = clean revert; the measurement left nothing behind)
