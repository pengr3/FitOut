19.1-16 — THE (host) HYDRATION FAILURE: REPRODUCED COLD, CONTROLLED, REPAIRED, AND WATCHED RED
================================================================================================

Convention: `19.1-PATTERNS.md` §A — the command line above its own unedited output, line-anchored
markers at column zero so a grep over this file cannot be satisfied by the prose around it.

Box: Windows 11, Chromium via Playwright 1.60.0, dev server on :3000, postgis/postgis:18-3.6 in
`fitout-db-1`. Date: 2026-09-06.

THE PROBE. A throwaway spec `e2e/zz-hydration-probe.spec.ts`, created for STEP 1 and deleted in the
same task. It arms `page.on("console")` and `page.on("pageerror")` BEFORE the navigation — a
hydration error is thrown during the FIRST client render, so a listener registered after `goto`
resolves can miss the only message it wants (`e2e/public-listing.spec.ts:329-330`'s own recorded
reason) — signs a host up through the UI, loads `/host` at 375px, and then drives the nav drawer open
and closed so the reading is taken on a page that provably HYDRATED rather than on a pre-hydration
document.

------------------------------------------------------------------------------------------------
STEP 1 · READING 1 — /host, COLD COMPILE CACHE
------------------------------------------------------------------------------------------------

$ npm run db:migrate && npm run db:seed
$ rm -rf .next
$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list

IMAGE=local
COLD-CACHE=yes
ROUTE=/host
HYDRATION-COUNT=1
EXIT=0

React's own error detail, verbatim, as the browser printed it:

DETAIL-BEGIN
Hydration failed because the server rendered HTML didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:

- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

https://react.dev/link/hydration-mismatch

  ...
    <div className="mx-auto fl...">
      <LinkComponent>
      <nav data-testid="site-nav" className="flex items...">
        <Suspense fallback={[...]}>
          <div>
          <div className="md:hidden">
            <NavDrawer>
              <ResponsiveDialog title="Menu" hideTitle={true} trigger={<button>}>
                <Dialog open={undefined} onOpenChange={undefined}>
                  <Dialog data-slot="dialog" open={undefined} onOpenChange={undefined}>
                    <DialogProvider scope={undefined} triggerRef={{current:null}} contentRef={{current:null}} ...>
                      <DialogTrigger asChild={true}>
                        <DialogTrigger data-slot="dialog-tri..." asChild={true}>
                          <Primitive.button type="button" aria-haspopup="dialog" aria-expanded={false} ...>
                            <Primitive.button.Slot type="button" aria-haspopup="dialog" aria-expanded={false} ...>
                              <Primitive.button.SlotClone type="button" aria-haspopup="dialog" aria-expanded={false} ...>
+                               <button
+                                 data-slot="button"
+                                 data-variant="ghost"
+                                 data-size="icon"
+                                 className={"group/button inline-flex shrink-0 items-center justify-center rounded-l..."}
+                                 aria-label="Menu"
+                                 type="button"
+                                 aria-haspopup="dialog"
+                                 aria-expanded={false}
+                                 aria-controls="radix-_R_ad5ritulb_"
+                                 data-state="closed"
+                                 onClick={function handleEvent}
+                                 ref={function}
+                               >
                      ...
      ...
DETAIL-END

WHAT THE BROWSER NAMED, IN ITS OWN WORDS AND NOT IN PLAN 13's.

  THE NODE:      `<button data-slot="button" data-variant="ghost" data-size="icon"
                 aria-label="Menu" type="button">` — the nav drawer's trigger.
  THE ATTRIBUTE: `aria-controls="radix-_R_ad5ritulb_"`, a Radix identifier generated per render from
                 React's own id hook.
  THE POSITION:  under `<nav data-testid="site-nav">` → `<Suspense fallback={[...]}>` →
                 `<div className="md:hidden">` → `NavDrawer` → `ResponsiveDialog` → `Dialog` →
                 `DialogProvider` → `DialogTrigger` → `Primitive.button.Slot`.
  THE SIGN:      every line of the node is prefixed `+`. In React's diff `+` is CLIENT-ONLY — the
                 client rendered this button at a position the server's HTML did not put it at.

MATCHED OR REFUTED? MATCHED. 19.1-13 recorded the chain
`host layout → SiteNav → NavDrawer → ResponsiveDialog → Radix DialogTrigger` and the attribute
`aria-controls` as the LEADING CANDIDATE, and said in writing that it had not been read as this
plan's own measurement. It is now read: the element and the attribute the browser names are the
element and the attribute 13 named. Nothing in the reading was inherited — the block above is this
run's output, and the id value (`radix-_R_ad5ritulb_`) is this run's own.

------------------------------------------------------------------------------------------------
STEP 1 · READING 2 — THE BOOKER CONTROL, SAME RUN, SAME COLD CACHE
------------------------------------------------------------------------------------------------

The booker CHECKOUT route (`/listings/[id]/book`), reached through a real seeded listing and a real
placed hold, which is the route 19.1-13 measured as emitting none. Identical capture: listeners armed
before the navigation, same filter, same probe.

$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list
  (test 2 of the same invocation)

IMAGE=local
COLD-CACHE=yes
ROUTE=/listings/[id]/book
HYDRATION-COUNT=0
EXIT=0

  @@@PROBE-BEGIN booker checkout /listings/[id]/book 375px
  @@@HYDRATION-COUNT 0
  @@@PROBE-END booker checkout /listings/[id]/book 375px
  ok 2 [chromium] › e2e\zz-hydration-probe.spec.ts:65:5 › probe booker checkout control (17.0s)

  2 passed (1.5m)

WHY THE CONTROL IS THE WHOLE ARGUMENT. A mechanism that fires everywhere explains nothing. `(app)`
streams too — `src/app/(app)/layout.tsx` wraps its bell in a boundary — but its fallback is
`<BellSlotSkeleton />`, a plain `aria-hidden` box that mounts no Radix subtree at all, and the route
emits zero. The difference between the two boundaries is not "streaming"; it is WHAT THE FALLBACK
MOUNTS. That single in-repo contrast is what selects the repair shape in STEP 2.

------------------------------------------------------------------------------------------------
STEP 1 · READING 3 — /host AGAINST A `.next` LEFT DIRTY BY A PRODUCTION BUILD
------------------------------------------------------------------------------------------------

The second control, and the one this repository has actually been burned by: `evidence/triage-harness.md`
warning 4 records a stale Turbopack cache faking both a route 404 and a hydration mismatch, so a
warm-cache reading is not a reproduction on its own — and neither is a cold one, on its own, evidence
that the cache is irrelevant. Both are taken.

$ npm run build
BUILD-EXIT=0
$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list --grep "probe host route"

IMAGE=local
COLD-CACHE=no
ROUTE=/host
HYDRATION-COUNT=1
EXIT=0

  @@@PROBE-BEGIN host /host 375px
  @@@HYDRATION-COUNT 1
  ...
  @@@PROBE-END host /host 375px
  ok 1 [chromium] › e2e\zz-hydration-probe.spec.ts:49:5 › probe host route (8.3s)

  1 passed (15.5s)

THE TWO READINGS AGREE — 1 and 1. There is no disagreement to record, and therefore no cache
artefact: the defect survives both a cache removed seconds earlier and a `.next` a production build
has just rewritten. The same `aria-controls="radix-…"` node is named in the dirty-cache transcript
(3 occurrences of `aria-controls="radix-` in that run's output).

------------------------------------------------------------------------------------------------
STEP 2 · THE FIRST REPAIR ATTEMPT, AND THE MEASUREMENT THAT REFUTED THE INHERITED DIAGNOSIS
------------------------------------------------------------------------------------------------

The shape plan 19.1-13 named, and the one this plan's own must_haves name, is: *the fallback and the
resolved child BOTH mount the overlay primitive, whose Radix-generated attribute the two renders
cannot agree on.* The first repair applied exactly that shape — a new `NavSlotSkeleton` beside
`BellSlotSkeleton` reserving the drawer trigger's `size-8` box without mounting `ResponsiveDialog`,
with the host layout's fallback switched to it.

$ rm -rf .next
$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list

IMAGE=local
COLD-CACHE=yes
ROUTE=/host
ATTEMPT=fallback-placeholder-only
HYDRATION-COUNT=1
EXIT=0

UNCHANGED. Same count, same node, and — decisively — the SAME GENERATED VALUE,
aria-controls="radix-_R_ad5ritulb_", byte-for-byte with the pre-repair run. Two renders that
disagreed about a generated id would not produce the same id after one of them stopped existing.

WARNING: THE INHERITED DIAGNOSIS IS THEREFORE REFUTED AS A CAUSE, AND CONFIRMED ONLY AS A
DESCRIPTION. 19.1-13 recorded it as the LEADING CANDIDATE and said the causal link had not been
observed; this is what observing it produced. The node and the attribute React names ARE the ones 13
named. What is false is the mechanism: it is not two renders disagreeing about an id. It is ONE
RENDER MISSING.

------------------------------------------------------------------------------------------------
STEP 2 · WHAT THE SERVED BYTES SAID, WHICH IS WHAT ACTUALLY NAMED THE SITE
------------------------------------------------------------------------------------------------

The probe was extended to read the SERVED document for /host rather than the hydrated DOM.

$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list --grep "probe host route"

  @@@SERVED-BEGIN len=68373
  @@@SERVED-ARIA aria-controls="radix-_R_bd5ritulb_"
  @@@SERVED-MENU-BUTTONS 0
  @@@SERVED-SEGMENTS ["id=\"S:2\"","id=\"S:1\"","id=\"S:0\"","id=\"S:3\""]
  @@@SERVED-BOUNDARIES ["id=\"B:0\"","id=\"B:1\"","id=\"B:2\""]
  @@@SERVED-S0 id="S:0"><div class="hidden md:flex"><div class="flex gap-3 items-center"><a class="text-sm font-medium underline-offset-4 hover:underline" href="/host/earnings">Earnings</a><a class="text-sm font-medium underline-offset-4 hover:underline" href="/host/requests">Requests</a></div></div><div class="md:hidden"></div></div><script>$RC("B:0","S:0")</script>

THREE FACTS IN THOSE FIVE LINES, AND THEY SETTLE IT.

  1. @@@SERVED-MENU-BUTTONS 0 — the served HTML for /host contains NO aria-label="Menu" button
     ANYWHERE. Not in the fallback, not in the resolved boundary.
  2. S:0, the resolved nav boundary's own completion segment, ends with
     <div class="md:hidden"></div> — the drawer placement is served EMPTY.
  3. The ONE aria-controls in the whole document belongs to the NOTIFICATION BELL's popover trigger
     (aria-label="Notifications, 0 unread", in S:1), not to the drawer. So the "two ids disagree"
     reading had nothing to disagree about: the drawer had no server id at all.

That is precisely what React's + meant, and what this plan's Step 1 wrote down without yet knowing
why: CLIENT-ONLY. The client renders <button aria-label="Menu"> into a div the server left empty.

WHY THE SERVER LEFT IT EMPTY. ResponsiveDialog forwards its `trigger` prop into
`DialogTrigger asChild`, and `asChild` is Radix's `Slot` — which CLONES an element rather than
rendering one. `patterns/site-chrome.tsx` is a SERVER COMPONENT (its own header says so, of every
export in it), so `NavDrawer` created the trigger element on the server and handed it across the RSC
boundary into a client component, where it could not be cloned during the SSR pass.

THE CENSUS THAT MAKES THIS THE SITE RATHER THAN A STORY — every adopter of ResponsiveDialog in src/,
with the first line of its module:

$ grep -rln "ResponsiveDialog" src/ | while read f; do printf "%s | %s\n" "$(head -1 "$f")" "$f"; done

  "use client";  | src/components/availability/blocks-editor.tsx
  "use client";  | src/components/availability/copy-hours-dialog.tsx
  "use client";  | src/components/booking/booking-sticky-bar.tsx
  "use client";  | src/components/host/request-row.tsx
  "use client";  | src/components/listing/photo-lightbox.tsx
  "use client";  | src/components/ops/ops-reject-dialog.tsx
  "use client";  | src/components/profile/avatar-field.tsx
  "use client";  | src/components/profile/image-crop-dialog.tsx
  // SHELL-01 — ONE header geometry, THREE compositions…  | src/components/patterns/site-chrome.tsx

EIGHT CLIENT MODULES AND ONE SERVER MODULE. One exception in the tree, one route in the app
reporting the mismatch. The census is the argument.

------------------------------------------------------------------------------------------------
STEP 2 · THE CHOSEN SHAPE, AND THE SHAPES REJECTED WITH THE CONSTRAINT THAT DECIDED EACH
------------------------------------------------------------------------------------------------

CHOSEN — move the CLONED ELEMENT, and only it, to the client side of the boundary.
  `src/components/patterns/nav-drawer-shell.tsx`, a new "use client" module holding the
  <Button aria-label="Menu"> trigger and composing the same ResponsiveDialog. `NavDrawer` stays in
  `site-chrome.tsx`, stays a Server Component, and passes `NavLinks` down as ordinary children —
  children cross the boundary unchanged and are never cloned, so only the trigger had to move.
  Constraint (e) holds exactly: ONE overlay mechanism, ONE focus trap, `ui/sheet.tsx` still absent,
  the vendored dialog untouched. Constraint (f) holds: no composition merged.

REJECTED — a placeholder fallback ALONE.  Decided by: THE MEASUREMENT ABOVE. It leaves
  HYDRATION-COUNT at 1. It is KEPT as a second, smaller change (below) but it is not the repair.

REJECTED — turn `asChild` off and let Radix render its own trigger button.  Decided by: the drawer's
  accessible name and its AUTH_SLOT_ICON box are the caller's, and Radix's own button carries
  neither. That trades a hydration failure for a WCAG 4.1.2 failure and a reflow.

REJECTED — wrap the trigger in a <span class="contents"> so Slot has something real to clone.
  Decided by: the aria wiring (aria-haspopup, aria-expanded, aria-controls) would land on the span
  rather than on the button. Same category of trade as above.

REJECTED — make `site-chrome.tsx` itself a client module.  Decided by: `SiteChrome` and `NavLinks`
  are Server Components by design and the file's header states it; converting them would ship the
  whole header inventory to the browser to fix one cloned element.

REJECTED — edit the vendored `ui/dialog.tsx`.  Decided by: the plan's prohibition, and RESP-01's own
  record that nothing under src/components/ui/** is edited by this layer.

KEPT ALONGSIDE — `NavSlotSkeleton`. It is not the repair and is not claimed to be. It is kept on its
own merits, stated narrowly: while the boundary is pending there is now NO button[aria-label="Menu"]
in the document at all, where before there was one that React then replaced — and a control that is
absent and then appears is one Playwright's auto-waiting handles, whereas a control that is present
and then swapped is the LOST-CLICK shape `e2e/helpers/booker-seed.ts:118-160` records.

------------------------------------------------------------------------------------------------
STEP 2 · READING 4 — /host AFTER THE REPAIR
------------------------------------------------------------------------------------------------

$ rm -rf .next
$ npx playwright test e2e/zz-hydration-probe.spec.ts --project=chromium --workers=1 --reporter=list

IMAGE=local
COLD-CACHE=yes
ROUTE=/host
HYDRATION-COUNT=0
EXIT=0

  @@@SERVED-BEGIN len=68248
  @@@SERVED-MENU-BUTTONS 1
  @@@SERVED-S0 id="S:0">…<div class="md:hidden"><button data-slot="dialog-trigger" data-variant="ghost" data-size="icon" class="group/button inline-flex shrink-0 items-center justify-center rounded-lg …

  ok 1 [chromium] › e2e\zz-hydration-probe.spec.ts:49:5 › probe host route
  ok 2 [chromium] › e2e\zz-hydration-probe.spec.ts:86:5 › probe booker checkout control
  2 passed (39.8s)

The drawer placement is no longer served empty, the trigger is in the server HTML, and the route
reports zero regenerations. The booker control stays at zero, unmoved by any of this.

$ npx tsc --noEmit | grep -c "error TS"
7                     the same seven pre-existing tests/design/ errors. No new error.

------------------------------------------------------------------------------------------------
STEP 3 · THE END-TO-END ASSERTION, AND THE RACE THAT MADE IT TWO CLAUSES
------------------------------------------------------------------------------------------------

One case added to `e2e/shell.spec.ts`, in a NON-SERIAL describe beside the other host-composition
cases: listeners armed before the navigation, a presence assertion, an interactive hydration proof
(open and close the nav drawer below md: — the very subtree under suspicion), then the absence.

WARNING: ONE THING THE PLAN DID NOT ANTICIPATE, MEASURED HERE RATHER THAN ASSUMED. The message
clause ALONE IS RACED. React does not hydrate a Suspense boundary that is still pending when
hydration begins — it client-renders the contents when they arrive, and a client render has nothing
to disagree with. So on a cold compile cache the same broken tree reports ZERO mismatches:

  reverted tree, rm -rf .next, message clause only     -> PASSED   (a green for the wrong reason)
  reverted tree, route already compiled                -> FAILED   1 mismatch
  a warm-up navigation added before the measured load  -> still PASSED cold; NOT deterministic

So the case carries a SECOND clause, on the served bytes, which reads the CAUSE rather than the
symptom: the served document must contain no EMPTY drawer placement <div class="md:hidden"></div>.
Pre-repair that string is present whether the boundary resolved or not (the fallback and the resolved
child are both served empty); post-repair neither is (the fallback holds the reserved box, the
resolved child holds the button). Both clauses ship; the deterministic one is what makes the red
reliable and the symptom clause is what names what a user would see.

------------------------------------------------------------------------------------------------
STEP 4 · WATCHED RED — THE REPAIR REVERTED FROM BYTE COPIES, NEVER FROM A CHECKOUT
------------------------------------------------------------------------------------------------

The working tree is mixed-EOL and a checkout silently rewrites a line-feed file while `git status`
stays clean, so the reversion is `cp` from byte copies taken before the edit.

$ sha256sum "src/app/(host)/host/layout.tsx" src/components/patterns/site-chrome.tsx   # BEFORE
CHECKSUM-BEFORE src/app/(host)/host/layout.tsx = 00a6d8e4844b4b8727cfa0b13f74b098d0cb57f731f510210c0c974f982b2480
CHECKSUM-BEFORE src/components/patterns/site-chrome.tsx = 148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af

$ cp /tmp/19116/layout.pre.tsx "src/app/(host)/host/layout.tsx"
$ cp /tmp/19116/site-chrome.pre.tsx src/components/patterns/site-chrome.tsx
$ mv src/components/patterns/nav-drawer-shell.tsx /tmp/19116/nav-drawer-shell.parked.tsx
$ git status --porcelain -- src
 M src/components/patterns/auth-slot-skeleton.tsx
                      <- the two reverted files are byte-identical to HEAD again, which is itself the
                         proof the byte copies restored the pre-repair bytes exactly, EOLs included.

$ rm -rf .next
$ npx playwright test e2e/shell.spec.ts --project=chromium --workers=1 --reporter=list --grep "regenerates no tree"

IMAGE=local
COLD-CACHE=yes
ROUTE=/host
STATE=reverted
HYDRATION-COUNT=1
EXIT=1

  Error: the served /host document holds an EMPTY drawer placement <div class="md:hidden"></div>.
  The nav drawer's trigger is passed to ResponsiveDialog's `trigger` prop and forwarded into
  `DialogTrigger asChild` — Radix's `Slot`, which CLONES an element rather than rendering one. An
  element created in a SERVER module cannot be cloned during the SSR pass, so the button is absent
  from the server HTML and appears only on the client, and React throws the tree away and regenerates
  it. …
  1 failed

…and the symptom clause, from the warm-cache run of the same reverted tree, verbatim, for the record:

  Error: /host reported 1 hydration mismatch(es). … Messages: ["Hydration failed because the server
  rendered HTML didn't match the client. As a result this tree will be regenerated on the client. …
  <nav data-testid=\"site-nav\" …><Suspense fallback={[...]}><div><div className=\"md:hidden\">
  <NavDrawer><ResponsiveDialog title=\"Menu\" hideTitle={true} trigger={<button>}> … <DialogTrigger
  asChild={true}> … <Primitive.button.SlotClone …> +<button data-slot=\"button\" … aria-label=\"Menu\"
  … aria-controls=\"radix-_R_ad5ritulb_\" …>"]

$ cp /tmp/19116/layout.post.tsx "src/app/(host)/host/layout.tsx"    # restored from the byte copy
$ cp /tmp/19116/site-chrome.post.tsx src/components/patterns/site-chrome.tsx
$ mv /tmp/19116/nav-drawer-shell.parked.tsx src/components/patterns/nav-drawer-shell.tsx
$ sha256sum "src/app/(host)/host/layout.tsx" src/components/patterns/site-chrome.tsx   # AFTER
CHECKSUM-AFTER src/app/(host)/host/layout.tsx = 00a6d8e4844b4b8727cfa0b13f74b098d0cb57f731f510210c0c974f982b2480
CHECKSUM-AFTER src/components/patterns/site-chrome.tsx = 148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af

THE PAIR IS EQUAL. The repair came back byte-for-byte.

$ git status --porcelain -- src
 M src/app/(host)/host/layout.tsx
 M src/components/patterns/auth-slot-skeleton.tsx
 M src/components/patterns/site-chrome.tsx
?? src/components/patterns/nav-drawer-shell.tsx

$ rm -rf .next
$ npx playwright test e2e/shell.spec.ts --project=chromium --workers=1 --reporter=list
  1 skipped
  25 passed (2.4m)          zero failed, zero flaky; 24 passed before this task, 25 after — the new
                            case is the +1, and the file declares one more test than at plan start.

$ npx vitest run --config vitest.design.config.ts tests/design/blocking-session-gate.test.ts
  Tests  17 passed (17)     the two gates did not move; neither sits inside a streamed subtree.

$ git status --porcelain -- .github scripts
                            (no output — no workflow and no checker file moved.)

$ rm e2e/zz-hydration-probe.spec.ts
                            the throwaway probe, deleted in the task that created it.

================================================================================================
TASK 2 — THE TWO OWNED SPECS IN THE RUNNER'S OWN IMAGE, BEFORE AND AFTER
================================================================================================

------------------------------------------------------------------------------------------------
STEP 1 · THE CONTAINER HARNESS: BUILT, USED, AND DELETED IN THIS TASK
------------------------------------------------------------------------------------------------

Built to 19.1-09's recipe as `deferred-items.md` records it: the dev server and Postgres on the
HOST; the pinned image mounted at the repository root; a ~15-line in-container Node loopback
forwarder mapping 127.0.0.1:3000 and :5432 to host.docker.internal (because
`e2e/helpers/served-document.ts:39` and the seed helpers hardcode localhost); and a throwaway config
importing the real `playwright.config.ts` with ONLY its `webServer` block removed.
`reuseExistingServer` was NOT flipped ([17-D24] / [17-D28]).

Two throwaway files, both at the repository root so the mount reaches them:
  pw-container.config.ts     - `const { webServer, ...rest } = base; export default rest;`
  container-forward.cjs      - two `net.createServer` pipes, no dependencies

THE DELETION IS PROVED, NOT ASSERTED — the collected count on either side of their existence:

$ npx playwright test --project=chromium --list | tail -1        # before they existed
Total: 461 tests in 39 files
$ rm -f pw-container.config.ts container-forward.cjs
$ npx playwright test --project=chromium --list | tail -1        # after they were deleted
Total: 461 tests in 39 files

$ git status --porcelain
 M .planning/STATE.md
?? .gsd/
?? .planning/milestone.lock
?? .planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-PATTERNS.md
?? .planning/state.json
                            no throwaway config, no forwarder, no scratch byte copy. The four
                            untracked entries predate this plan and are not its files.

THE TREE EVERY CONTAINER RUN MEASURED, asserted before believing any of them:

$ git status --porcelain -- src e2e tests      (before the BEFORE run, with the pre-repair copies applied)
 M src/app/(host)/host/layout.tsx
 D src/components/patterns/nav-drawer-shell.tsx
 M src/components/patterns/site-chrome.tsx
                            exactly the three files the reversion touches and nothing else.

------------------------------------------------------------------------------------------------
STEP 2 · BEFORE — THE PRE-REPAIR TREE, IN THE PINNED IMAGE, AT THE RUNNER'S OWN SHAPE
------------------------------------------------------------------------------------------------

The one source file Task 1 edited turned out to be three, so all three were applied from the byte
copies taken before the edit (never a checkout — the tree is mixed-EOL).

$ docker run --rm --ipc=host --add-host=host.docker.internal:host-gateway -v "C:/Users/Admin/Roaming/FitOut:/repo" -w /repo -e PLAYWRIGHT_BROWSERS_PATH=/ms-playwright -e DATABASE_URL=... mcr.microsoft.com/playwright:v1.60.0-noble bash -lc 'node container-forward.cjs & sleep 2; npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --config=pw-container.config.ts --project=chromium --workers=1 --retries=2 --reporter=list'

IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble
TREE=282073c1c10bf3484f62ce48de20d5a19f804e00 (+ the three files reverted to their pre-repair bytes)
STATE=pre-repair
CONTAINER-EXIT=0

  [forward] 127.0.0.1:3000 -> host.docker.internal:3000
  [forward] 127.0.0.1:5432 -> host.docker.internal:5432
  ✓    12 [chromium] › e2e/host-headings.spec.ts:1052:7 › AC#35 … › wizard · whole space, every step (16.1s)
  ✓    13 [chromium] › e2e/host-headings.spec.ts:1056:7 › AC#35 … › wizard · drop-in, every step (10.0s)
  ✓    96 [chromium] › e2e/overflow-320.spec.ts:3434:11 › AC#36 … › /host/listings/[id]/edit · photos step · court (2.3s)
  ✓    97 [chromium] › e2e/overflow-320.spec.ts:3434:11 › AC#36 … › /host/listings/[id]/edit · photos step · grove (2.2s)

  7 skipped
  118 passed (4.8m)

⚠⚠ THE PRE-REPAIR RUN DOES NOT REPRODUCE EITHER OWNED FAILURE IN THE CONTAINER. ZERO FAILED.

That is the finding, and this plan's STEP 2 named it in advance as a finding rather than a problem:
the container is NOT the instrument that separates this claim, and the plan's verdict on the two
owned specs therefore rests on the REAL `ci` RUN in Task 4. It is written down here rather than
reached for a second theory.

Both cases the two owned specs are named for pass on the runner's own rasteriser, at the runner's own
`--workers=1 --retries=2`, on the tree the runner has been failing them on. The wizard walk
(`host-headings:1052`) completes all nine steps; the publish-checklist seam
(`overflow-320:3434 · photos step`) arrives, in both themes, first attempt, no retry line.

BEFORE e2e/host-headings.spec.ts | FAILED: none
BEFORE e2e/overflow-320.spec.ts | FAILED: none
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: not completed · court"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: not completed · grove"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, AUTOMATIC branch · court"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, AUTOMATIC branch · grove"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, MANUAL branch · court"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, MANUAL branch · grove"
BEFORE e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › D-83 — the support control sits above the coral primary in the reversed manual branch"

$ sha256sum "src/app/(host)/host/layout.tsx" src/components/patterns/site-chrome.tsx   # restored from the byte copies
CHECKSUM-AFTER src/app/(host)/host/layout.tsx = 00a6d8e4844b4b8727cfa0b13f74b098d0cb57f731f510210c0c974f982b2480
CHECKSUM-AFTER src/components/patterns/site-chrome.tsx = 148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af
                            equal to the pair recorded in Task 1 STEP 4. The repaired tree came back
                            byte-for-byte a second time.

------------------------------------------------------------------------------------------------
STEP 3 · AFTER — AND THE FALSE RED THAT CAME FIRST, RECORDED RATHER THAN QUIETLY RE-RUN
------------------------------------------------------------------------------------------------

FIRST AFTER RUN, AGAINST THE DEV SERVER THAT HAD BEEN LIVE ACROSS BOTH MUTATIONS:

IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble
TREE=282073c1c10bf3484f62ce48de20d5a19f804e00 (repaired)
STATE=post-repair, dev server NOT restarted
CONTAINER-EXIT=1

  ✘  104 … AC#36 … › /host/listings · court (1.0m)          all three attempts
  ✘  … AC#36 … › /host/listings · grove
  2 failed / 7 skipped / 106 passed (9.0m)   — plus 10 AC#36 cases the serial abort left unrun

  Error: /host/listings · court · 320px: the route rendered no
  `a[href^="/host/listings/"][href$="/availability"]` … 122 × locator resolved to 0 elements

⚠ THIS IS THE WEDGED-DEV-SERVER TRAP, NOT THE REPAIR, AND IT IS WRITTEN DOWN BECAUSE IT WOULD HAVE
BEEN VERY EASY TO REPORT AS A REGRESSION. `src/components/patterns/nav-drawer-shell.tsx` was DELETED
and RECREATED under a live Turbopack session — the reversion moved it out for the BEFORE run and
moved it back for the AFTER one — and `evidence/triage-harness.md` warning 4 plus `[17-D24]` both
record this repository serving stale or broken output from exactly that. The failing route is not one
this repair touches, and the failing assertion is about listing CARDS.

THE SECOND AFTER RUN, ON A FRESH SERVER WITH A COLD CACHE — the correct measurement:

$ taskkill the dev server ; rm -rf .next ; npm run dev            (fresh boot, cold Turbopack cache)
$ docker run … mcr.microsoft.com/playwright:v1.60.0-noble bash -lc 'node container-forward.cjs & sleep 2; npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --config=pw-container.config.ts --project=chromium --workers=1 --retries=2 --reporter=list'

IMAGE=mcr.microsoft.com/playwright:v1.60.0-noble
TREE=282073c1c10bf3484f62ce48de20d5a19f804e00 (repaired)
STATE=post-repair, fresh dev server, cold cache
CONTAINER-EXIT=0

  7 skipped
  118 passed (4.7m)             zero failed, zero flaky, zero `✘` in the whole transcript

AFTER e2e/host-headings.spec.ts | FAILED: none
AFTER e2e/overflow-320.spec.ts | FAILED: none
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: not completed · court"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: not completed · grove"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, AUTOMATIC branch · court"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, AUTOMATIC branch · grove"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, MANUAL branch · court"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › payment state: reversed, MANUAL branch · grove"
AFTER e2e/overflow-320.spec.ts | did not run: "AC#30 / AC#22 — every Phase-13 surface at 320px, in both themes › D-83 — the support control sits above the coral primary in the reversed manual branch"

THE RELEASED SET, COMPUTED ON FILE AND FULL TITLE AND NEVER ON `file:line`:

$ diff <(sorted BEFORE did-not-run set) <(sorted AFTER did-not-run set)
                            (no output — the two sets are IDENTICAL, seven members each)

  All seven are `AC#30 / AC#22` payment-state cases that this repository skips by construction, not
  by serial abort: they are the same seven in both runs and their `-` markers carry no `(retry #N)`.
  Nothing was released in the container, because nothing was blocked in the container.

RELEASED-TOTAL=0
DELTA-VS-13=-24 — plan 19.1-13 recorded 24 tests blocked by the publish-checklist row, measured on
the RUNNER. The container blocked ZERO, because the row it names never failed there. The delta is
not a disagreement about arithmetic and is not reconciled by argument: it is a disagreement about
WHERE, and the run wins. 19.1-13's figure stands as a figure about `gate-e2e`; this plan's figure
stands as a figure about the pinned image on this box, and the two are measurements of different
things. The runner's own released set is read in Task 4, not here.

------------------------------------------------------------------------------------------------
STEP 4 · THE VERDICTS
------------------------------------------------------------------------------------------------

No case failed in the AFTER run, so there is no still-red case to diagnose in this instrument. Both
owned specs get a verdict anyway, because "the container said nothing" is itself the verdict and it
has to be written where the next reader will look.

VERDICT e2e/host-headings.spec.ts (O11, `wizard · whole space, every step`) — NOT SEPARATED BY THE
VERDICT   CONTAINER. Green in `mcr.microsoft.com/playwright:v1.60.0-noble` at `--workers=1 --retries=2`
VERDICT   on the pre-repair tree AND on the repaired tree, 16.1s and 15.9s, first attempt both times.
VERDICT   The instrument 19.1-09 built to settle "green here, red in CI" does not reproduce this one,
VERDICT   which is a fact about the instrument's reach rather than about the defect: the runner is a
VERDICT   2-core GitHub host under a whole-suite load, and this box is neither. NO REPAIR IS MADE AT A
VERDICT   SITE NO MEASUREMENT NAMED — the prohibition against weakening the instrument and 19.1-08's
VERDICT   refusal to add an unobserved retry both bind here, and neither is revisited. The surviving
VERDICT   cause is unnamed and the file that owns it is unknown; Task 4's real run is the instrument.
VERDICT   NOT AN ALLOWLIST CANDIDATE — production source, D-02, barred by written verdict.

VERDICT e2e/overflow-320.spec.ts (O12, `AC#36 … /host/listings/[id]/edit · photos step`) — NOT
VERDICT   SEPARATED BY THE CONTAINER, on the same evidence: green both themes, both trees, first
VERDICT   attempt, 2.0-3.7s. The `Fix` press lands and `openWizardPhotosStep` arrives. The 24 tests
VERDICT   19.1-13 recorded this row as blocking are not blocked here because the row does not fail
VERDICT   here. NOT AN ALLOWLIST CANDIDATE, and barred BY NAME:
VERDICT   `evidence/triage-upload-capability.txt` § PROHIBITION — "`e2e/overflow-320.spec.ts:3400` is
VERDICT   likewise ineligible, and for a stronger reason — the capability it needs is not one CI lacks."

WHAT THE REPAIR DID AND DID NOT BUY, STATED PLAINLY. It removed a real production defect that
reproduces deterministically on every `(host)` route (Task 1, four readings and the served bytes). It
did NOT produce a before/after difference in these two specs in the container, because there was no
difference to produce: they were green in the container on both trees. Anyone reading this file for
"did the repair fix O11 and O12" must read Task 4, and Task 4 must be read on a real `ci` run.

------------------------------------------------------------------------------------------------
TASK 2 · THE LOCAL AND CENSUS VERIFICATIONS
------------------------------------------------------------------------------------------------

$ npx playwright test e2e/host-headings.spec.ts e2e/overflow-320.spec.ts --project=chromium --workers=1 --retries=2
  7 skipped
  118 passed (3.1m)           zero failed, zero flaky

$ npx vitest run --config vitest.design.config.ts tests/design/e2e-known-failures.test.ts
  Tests  9 passed (9)         the census ran; `PINNED_ENTRY_TOTAL = 2` is untouched

$ git diff --stat HEAD -- tests/design/e2e-known-failures.test.ts
                            (no output — the allowlist file is unmodified by this plan.)
