19.1-17 — THE "ONE ELEMENT RESOLVES TO TWO" DEFECT ON THE TWO BOOKER SURFACES, READ RATHER THAN INFERRED
=======================================================================================================

Convention: `19.1-PATTERNS.md` §A — the command line above its own unedited output, line-anchored
markers at column zero. Raw excerpts from the throwaway probe are INDENTED by two spaces so that a
grep over a marker cannot be satisfied by a block that merely quotes one; the markers this plan's
acceptance criteria count are the un-indented ones.

MARKER BUDGET, STATED SO A LATER READER IS NOT SURPRISED. Task 1 writes exactly one `ARRIVAL-MS=` and
exactly one `PERSISTS=` (the confirmation surface). Task 2 writes one more of each under its own
heading, because its acceptance criterion requires them for the profile surface. So the finished file
carries two of each, one per surface, and that is by design rather than a duplicate answer. There is
exactly ONE `SHARED-CAUSE=` and exactly ONE `SAME-BRANCH=` in the whole file, and those two are the
questions that may only be answered once.

-------------------------------------------------------------------------------------------------
TASK 1 — STEP 1. REPRODUCED UNDER BOTH CACHE CONDITIONS, ON THE CONFIRMATION SURFACE ONLY.
-------------------------------------------------------------------------------------------------

Bring-up per `evidence/triage-harness.md` §(a): `npm run db:up` (already up — `fitout-db-1`
postgis/postgis:18-3.6, 15h), `npm run db:migrate`, `npm run db:seed`, `rm -rf .next`, port 3000 free.
Run at the runner's own shape: one worker, two retries.

The compile-pressure condition is reported first, per this plan's ordering truth; the cold run is
below it and was executed first in wall-clock order, which is recorded rather than tidied.

CONDITION=dirty
$ npm run build            # leaves .next as a production build — the compile-pressure condition
  BUILD_EXIT=0
$ npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2

  Error: court · 320×568: the moment must resolve to exactly ONE laid-out section before anything
  below measures its box. …
  Error: strict mode violation: getByTestId('confirmation-moment') resolved to 2 elements:
      1) <section data-testid="confirmation-moment" class="min-h-[calc(100svh-3.5rem)] …">…</section>
         aka getByRole('main').getByTestId('confirmation-moment')
      2) <section data-testid="confirmation-moment" class="min-h-[calc(100svh-3.5rem)] …">…</section>
         aka getByTestId('confirmation-moment').nth(1)
  1 flaky
  3 passed (38.7s)

COUNT-AT-ASSERTION=2
EXIT=0

CONDITION=cold
$ rm -rf .next
$ npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2

  Error: grove · 375×667: the moment must resolve to exactly ONE laid-out section …
  Error: strict mode violation: getByTestId('confirmation-moment') resolved to 2 elements:
      1) … aka getByRole('main').getByTestId('confirmation-moment')
      2) … aka getByTestId('confirmation-moment').nth(1)
  1 flaky
  3 passed (51.3s)

COUNT-AT-ASSERTION=2
EXIT=0

BOTH CONDITIONS REPRODUCE, and the exit code is zero in both because the retry passes — which is the
whole reason CI has only ever reported this as flaky. The shape the case's own note recorded holds:
intermittent at rest, deterministic under compile pressure (5 of 6 loads below, versus 1 of 6 on the
warm-at-rest probe run). The viewport and theme it lands on MOVE between runs — court·320×568 under
compile pressure, grove·375×667 cold — which is a timing signature, not a viewport defect.

-------------------------------------------------------------------------------------------------
TASK 1 — STEP 2. BOTH MATCHES' PROVENANCE. THIS IS THE MEASUREMENT THE WHOLE PLAN TURNS ON.
-------------------------------------------------------------------------------------------------

Instrument: a throwaway probe (`e2e/zz-probe-dup-mount.spec.ts`, deleted in Task 3, never committed)
that installs a document-start script before navigation. The script observes the whole document with a
MutationObserver plus a 4ms interval, records one sample per count change, and — the first time the
count exceeds one — walks BOTH matches to `<html>`, one ancestor per line, recording each ancestor's
tag, identifier, class list, whether it declares the `hidden` attribute ITSELF (the attribute's
presence, never a computed style, because a node inside a hidden container computes as hidden without
carrying the attribute), and whether it is the page's main landmark. It also records the comment-node
siblings of the outermost DIVERGING ancestor.

The dump below is `grove · 375×667` under the dirty-cache condition. Eleven of the twelve loads across
two probe runs produced a byte-identical chain shape; the twelfth never exceeded one.

  $ PROBE_OUT=probe-conf-dirty.txt npx playwright test e2e/zz-probe-dup-mount.spec.ts --project=chromium --workers=1
    1 passed (33.9s)

  ### SURFACE=confirmation THEME=grove VIEWPORT=375x667
  MAX-COUNT=2

SAMPLE 0ms count=0
SAMPLE 155ms count=1
SAMPLE 193ms count=2
SAMPLE 338ms count=1
SAMPLE 757ms count=1
SAMPLE 1173ms count=1
SAMPLE 2005ms count=1
SAMPLE 3237ms count=1

ARRIVAL-MS=193
PERSISTS=no (count after a 3000ms bounded wait: 1)

ANCESTOR match=1 depth=0 tag=section id=(none) class="min-h-[calc(100svh-3.5rem)] sm:min-h-[calc(100svh-4rem)] flex flex-col items-center justify-center gap-6 text-center" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=1 tag=div id=(none) class="mx-auto w-full max-w-2xl px-4 py-8 sm:py-12" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=2 tag=main id=(none) class="flex flex-1 flex-col" hiddenAttr=no isMain=yes
ANCESTOR match=1 depth=3 tag=div id=(none) class="flex min-h-full flex-col" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=4 tag=body id=(none) class="min-h-full flex flex-col" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=5 tag=html id=(none) class="geist_… geist_mono_… h-full antialiased" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=0 tag=section id=(none) class="min-h-[calc(100svh-3.5rem)] sm:min-h-[calc(100svh-4rem)] flex flex-col items-center justify-center gap-6 text-center" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=1 tag=div id=(none) class="mx-auto w-full max-w-2xl px-4 py-8 sm:py-12" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=2 tag=div id=S:1 class="(none)" hiddenAttr=yes isMain=no
ANCESTOR match=2 depth=3 tag=body id=(none) class="min-h-full flex flex-col" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=4 tag=html id=(none) class="geist_… geist_mono_… h-full antialiased" hiddenAttr=no isMain=no

  DIVERGE-AT match=1 tag=div parent=body
  DIVERGE-AT match=2 tag=div parent=body
  (no comment-node siblings at the divergence point: the two chains diverge at `<body>`'s OWN
   children, and the delivery mechanism's marker here is not a comment — it is the identifier and the
   attribute on the diverging element itself, `div#S:1[hidden]`.)

THE READING, IN THE WALK'S OWN TERMS.

The two chains are identical for two levels — `section[data-testid=confirmation-moment]` inside
`div.mx-auto.w-full.max-w-2xl` — and then diverge at `<body>`:

    match 1 :  … → MAIN.flex.flex-1.flex-col → div.flex.min-h-full.flex-col → body
    match 2 :  … → DIV#S:1[hidden]                                          → body

`div#S:1[hidden]` is React's OUT-OF-ORDER STREAMING STAGING AREA. Content for a Suspense boundary that
resolves after the shell has been flushed is written at the END of `<body>` inside a hidden div with a
generated `S:n` identifier, and an inline script then commits those children into the boundary and
drops the staging div. The two "copies" are therefore ONE section in two places for one frame: staged
and committed.

THE BRANCH THIS SELECTS, from the three this plan stated in advance:
  • the second copy goes away on its own — `PERSISTS=no`, measured on every load, gone within ~150ms
    of arriving (193ms → 338ms in the samples above);
  • its ancestor chain carries the delivery mechanism's own marker — `div#S:1`, `hidden`, a direct
    child of `<body>`, which no page composition in this repository produces;
  • the browser reported NOTHING about regenerating the tree on this route — `HYDRATION-MSGS=0` on
    both trees (see Step 3), so the third branch is excluded by measurement and not by argument.

→ IT IS A COMMIT-WINDOW ARTEFACT. It is NOT a duplicated mount, and it is NOT a regenerated tree. The
defect is that the measurement runs before the surface has committed.

AND IT EXPLAINS THE ONE THING NO DUPLICATE-MOUNT THEORY EVER DID — the original `0px tall` reports.
While only the STAGED copy exists the count really is one, `boxOf`'s `document.querySelector` takes it,
and a node inside a `hidden` container has a zero rect. The case's note read that as "the box of the
WRONG section", which was right about the section and wrong about why there were two. Both readings
are kept at the site; see Task 3.

WHY THE COUNT ASSERTION PASSES AND THE VISIBILITY ASSERTION THEN FINDS TWO — the window, named. The
samples say the count is 0 → 1 → 2 → 1. `toHaveCount(1)` retries until it sees ONE, and one is exactly
what the document holds while the staged copy is present and the committed one is not. Two statements
later the commit lands, the staged div has not yet been dropped, and the strict-mode resolution reports
both. The window is not between two renders; it is between the flush and the commit.

-------------------------------------------------------------------------------------------------
TASK 1 — STEP 3. THE SHARED-CAUSE QUESTION PLAN 13 HANDED FORWARD, ANSWERED WITH A PAIR.
-------------------------------------------------------------------------------------------------

Method: byte copy out, byte copy back — never a checkout, because this working tree is mixed-EOL and a
checkout silently rewrites a line-feed file while `git status` reads clean.

  $ cp src/components/patterns/site-chrome.tsx  <scratch>/site-chrome.repaired.bytes
  $ sha256sum src/components/patterns/site-chrome.tsx
    148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af

CHECKSUM-BEFORE=148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af

  $ git cat-file -p 282073c^:src/components/patterns/site-chrome.tsx > src/components/patterns/site-chrome.tsx
  $ grep -c "NavDrawerShell" src/components/patterns/site-chrome.tsx
    0                     # plan 16's repair is out of the tree
  $ sha256sum src/components/patterns/site-chrome.tsx
    347e543c3c958f9b99534c73946cfe18339dc995efd53040eee1b1bc4f2dca68

Both sides measured with the SAME probe, at the same six theme×viewport loads, on a dirty cache:

  WITH plan 16's repair (HEAD)          : loads reaching count 2 = 5 of 6 ; HYDRATION-MSGS=0
  WITHOUT plan 16's repair (282073c^)   : loads reaching count 2 = 6 of 6 ; HYDRATION-MSGS=0
  match-2 ancestor chain, both trees    : section → div.mx-auto → div#S:1[hidden] → body → html
                                          (byte-identical shape on both sides)

SHARED-CAUSE=no
  counts, both trees: repaired 5/6 loads at count 2, ARRIVAL-MS 193–273ms; pre-repair 6/6 loads at
  count 2, ARRIVAL-MS 198–1883ms. The defect is present in equal measure on BOTH sides of plan 16's
  repair, with the same ancestor chain, so plan 16's repair neither caused it nor closes it.

AND THE STRUCTURAL READING AGREES WITH THE MEASUREMENT, which is why this null result is a finding and
not a failed attempt. Plan 16's defect is a trigger element crossing the RSC boundary into Radix's
`asChild` clone, inside `NavDrawer` → `NavDrawerShell`. `NavDrawer` is reached only through `SiteNav`,
and `SiteNav` is reached only through `SiteChrome`'s `nav` prop — which `src/app/(app)/layout.tsx` does
NOT pass. It passes `actions` only. So the booker shell never renders the component plan 16 repaired,
and `HYDRATION-MSGS=0` on the booker route on BOTH trees is that structural fact measured rather than
argued. The two defects share a visible signature — one match in `main`, one outside it, one hidden —
and nothing else.

  $ cp <scratch>/site-chrome.repaired.bytes src/components/patterns/site-chrome.tsx
  $ sha256sum src/components/patterns/site-chrome.tsx
    148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af
  $ git status --porcelain -- src/components/patterns/site-chrome.tsx
    (empty)

CHECKSUM-AFTER=148cd8ccbc388507eb270de932fe46a454cc9fe352fbaccd971cb7a63e2432af
CHECKSUM-EQUAL=yes

-------------------------------------------------------------------------------------------------
TASK 1 — STEP 4. THE REPAIR THE READING SELECTED, AND THE CONSTRAINT SET IT SATISFIES.
-------------------------------------------------------------------------------------------------

CHOSEN: a precondition in `e2e/confirmation-decay.spec.ts` that ASSERTS THE COMMIT — a new helper
`expectStreamCommitted(page, testId, where)` that polls a CLASSIFICATION of every match: a match with a
`[hidden]` ancestor is STAGED, one without is COMMITTED, and the precondition is *zero staged and at
least one committed*. It runs on the default expect budget and is called immediately after each
`page.goto` on this surface, ahead of the count assertion.

  This is a repair to the INSTRUMENT and not to the product, and the reading is why: nothing in the
  delivered page is wrong. A booker never sees two confirmation sections; the staged copy is hidden,
  belongs to the framework's delivery mechanism, and is gone within ~150ms. What was wrong is that the
  measurement started before the surface had committed.

REJECTED, each with the constraint that decided it:

  1. Scope the locator to the main landmark — PROHIBITED at the case's own site, and rightly: it
     deletes the only instrument that has ever named this defect. Also wrong on the reading: it would
     hide a genuine duplicate outside `main` forever.
  2. Hand `boxOf` the visible match — PROHIBITED at the same site, and it would silently pass over a
     staged copy rather than notice one.
  3. Widen the expect budget, or the case's own budget — forbidden by this plan, and it would not have
     worked: the count is ONE during the window, so a longer wait on `toHaveCount(1)` returns sooner,
     not later.
  4. `waitForTimeout` past the window — the constraint that kills it is (d). A sleep long enough to
     outlast the commit also outlasts a genuine duplicate injected afterwards, and the case stops being
     able to see the thing it exists to see. This is the difference between asserting the commit and
     waiting it out, and it is why Task 3's second control exists.
  5. Change the route's streaming structure so the moment is never streamed — this is production
     source, it moves a surface above a gate for a test's convenience, and the measurement names no
     product defect to justify it. Constraint (e) and the plan's own prohibition on crediting a repair
     to the wrong change.

THE CONSTRAINT SET, EACH SATISFIED AND HOW:

  (a) The case's locator is untouched and unscoped; `boxOf` still takes `document.querySelector`'s
      first match. The helper classifies matches, it does not select one for the case.
        $ git diff -- e2e/confirmation-decay.spec.ts | grep -cE '^\+.*getByRole\(.main.\)'   ->  0
  (b) No timeout widened. The helper takes no explicit budget; `expect.poll` uses the configured
      expect default, the same one `toBeVisible()` above it already used.
        $ git diff -U0 -- e2e/confirmation-decay.spec.ts | grep -E '^\+' | grep -cE 'timeout: *[0-9_]+'  ->  0
  (c) The height floor (`height - 64`) and the fold position (`>= height`) are byte-unchanged.
  (d) The case still fails against a genuine second copy present after the surface has settled: the
      precondition passes (`staged=0`, `committed=2`) and `toHaveCount(1)` then reddens. Watched in
      Task 3.
  (e) No production source changed in this task. The confirmation component's single mount site is
      still single and nothing in the route's streaming structure moved.

VERIFICATION, BOTH CONDITIONS, AFTER THE REPAIR:

  $ npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2
    (dirty cache)  4 passed (28.3s)   — 0 failed, 0 flaky, first attempt
  $ rm -rf .next && npx playwright test e2e/confirmation-decay.spec.ts --project=chromium --workers=1 --retries=2
    (cold cache)   4 passed (43.9s)   — 0 failed, 0 flaky, first attempt
EXIT=0

=================================================================================================
TASK 2 — THE PROFILE SURFACE: MEASURED ON ITS OWN BEFORE ANYTHING WAS INHERITED
=================================================================================================

SURFACE=profile (`/profile`, `src/components/profile/avatar-field.tsx`'s `input[type="file"]`)

Instrument: a second throwaway probe (`e2e/zz-probe-dup-picker.spec.ts`, deleted in Task 3, never
committed) with the same document-start observer and the same `ANCESTOR ` marker shape as Task 1's,
plus a served-bytes read. It signs up WITHOUT visiting `/profile`, so the first read below lands on a
route the dev server has never compiled — the condition CI is permanently in.

  $ rm -rf .next            # cold
  $ PROBE_OUT=probe-picker-cold2.txt npx playwright test e2e/zz-probe-dup-picker.spec.ts --project=chromium --workers=1
    1 passed (36.6s)

  SERVED-BYTES total=64372 occurrences-of-type-file=1 offsets=11665
  SERVED-STAGING-DIVS=2

  ⚠ THE SERVED-BYTES READ DID NOT ANSWER IT, AND THAT IS RECORDED RATHER THAN DROPPED. A buffered
  read of the whole response collapses the flush ORDER, so a single occurrence there is consistent
  with both a boundary emitted inline and one emitted into a staging div. The DOM observer is the
  instrument that answered; the byte read is kept because it rules out the other shape this
  repository has measured before — `/`'s `loading.tsx` renders a SECOND copy of a control
  (`booker-seed.ts:481-495`), which would have shown TWO occurrences here. It shows one, so
  `profile/loading.tsx` is NOT a second mount site. VERDICT N2's open question — "why two mount" —
  is answered: they do not. One mounts, and it is momentarily in two places.

  ### SURFACE=profile PASS=2   (six passes; PASS=2 is the one that caught it — the shape is
                                intermittent at rest here exactly as the confirmation surface's is)
  MAX-COUNT=2

  SAMPLE 0ms count=0
  SAMPLE 312ms count=1
  SAMPLE 347ms count=2
  SAMPLE 421ms count=1

ARRIVAL-MS=347
PERSISTS=no (count after a 2000ms bounded wait: 1)

ANCESTOR match=1 depth=0 tag=input id=(none) class="hidden" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=5 tag=form id=(none) class="space-y-6" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=7 tag=div id=(none) class="mx-auto w-full max-w-2xl px-4 py-8 sm:py-12" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=8 tag=main id=(none) class="flex flex-1 flex-col" hiddenAttr=no isMain=yes
ANCESTOR match=1 depth=9 tag=div id=(none) class="flex min-h-full flex-col" hiddenAttr=no isMain=no
ANCESTOR match=1 depth=10 tag=body id=(none) class="min-h-full flex flex-col" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=0 tag=input id=(none) class="hidden" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=5 tag=form id=(none) class="space-y-6" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=7 tag=div id=(none) class="mx-auto w-full max-w-2xl px-4 py-8 sm:py-12" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=8 tag=div id=S:1 class="(none)" hiddenAttr=yes isMain=no
ANCESTOR match=2 depth=9 tag=body id=(none) class="min-h-full flex flex-col" hiddenAttr=no isMain=no
ANCESTOR match=2 depth=10 tag=html id=(none) class="geist_… geist_mono_… h-full antialiased" hiddenAttr=no isMain=no

  (depths 1–4 and 6 are identical on both matches — `div.space-y-1`, `div.flex.items-center.gap-4`,
   the card's two divs, `div.space-y-8` — and are elided here only for width. The full twelve- and
   eleven-line walks are in the probe output; nothing differs below the divergence point.)

  DIVERGE-AT match=1 tag=div parent=body
  DIVERGE-AT match=2 tag=div parent=body
  (no comment-node siblings at the divergence point, same as the confirmation surface.)

  HYDRATION-MSGS=0 — the browser reported nothing about regenerating the tree on this route either.

SAME-BRANCH=yes
  The term that decided it: the DIVERGENCE POINT. On both surfaces the two chains are identical from
  the element up through the page's own container and then split at `<body>`'s own children — one
  through `MAIN`, one through `DIV#S:1[hidden]` — and on both surfaces `PERSISTS=no`. Two surfaces
  with the same visible signature and DIFFERENT delivery structures is exactly the situation where a
  shared cause gets assumed and never checked; this one was checked, on this surface's own walk,
  before any repair was carried across. Both routes carry a `loading.tsx`, which is the boundary the
  staging area belongs to.

THE REPAIR ON THIS SURFACE — the same shape, in this file's idiom.

`expectPickerCommitted(page)` in `e2e/avatar-crop.spec.ts`, called from `pick()` ahead of
`setInputFiles`. Same classification (a match with a `[hidden]` ancestor is staged), same default
expect budget, no explicit budget anywhere. `class="hidden"` on the input itself is a CSS class and
NOT the attribute, so it is correctly not counted as staged — confirmed by the walk above, which
records `hiddenAttr=no` on the input in both matches.

  • The fixture-handing helper's locator is still `page.locator('input[type="file"]')`, unqualified
    by any landmark. Strict mode remains the assertion that the picker stayed unique.
  • The docblock is corrected AT THE SITE: the "unambiguous by construction" sentence is kept and
    labelled as a claim about the SOURCE, with the measurement of what the DOCUMENT held beside it.
  • `src/components/profile/avatar-field.tsx` carries the same correction beside its own "the only
    one in `src/`" comment, because that is the sentence the spec was citing.
  • The file's ONE expected-failure annotation, its `CI` condition and its raised budget are
    byte-unchanged, and no entry was added to the known-failures allowlist.

  $ npx playwright test e2e/avatar-crop.spec.ts --project=chromium --workers=1
    33 passed (1.9m)
EXIT=0

=================================================================================================
TASK 3 — BOTH INSTRUMENTS WATCHED RED AGAINST A GENUINE DUPLICATE, AND GREEN AGAINST THE TRANSIENT
=================================================================================================

⚠ THE RED SIMULATES THE STATE, NOT THE CAUSE — 19.1-08's requirement, stated here explicitly. Nothing
below reproduces the route delivering two committed sections; each injection PUTS the document into the
state that would produce, and then runs the SHIPPED case against it. That is what "the instrument can
still fail" means and it is all it means.

METHOD: mutate-a-copy-then-run, the idiom of `tests/design/workflow-invariants.test.ts`. A script
copies the shipped spec, inserts one line at a single unique anchor, and REFUSES TO WRITE THE COPY if
the mutated text equals its input — `evidence/eol-mutation-builders.txt` records what a silently
unapplied mutation once cost this phase (a case reported as surviving the deletion of its own
conjunct). Every injection additionally reports `APPLIED=`, the number of nodes actually added as read
back from the document, so a probe that ran and changed nothing cannot be mistaken for a result. The
copies are `e2e/zz-mutant-*.spec.ts` and they are deleted below.

-------------------------------------------------------------------------------------------------
(1) A GENUINE SECOND COPY, CLONED INTO THE SETTLED DOCUMENT
-------------------------------------------------------------------------------------------------

Injected AFTER the commit precondition, as a sibling of the original with NO hidden ancestor — so it
is not a copy the delivery mechanism would ever remove on its own.

  $ node mutate.mjs confirmation genuine
    MUTATED e2e/confirmation-decay.spec.ts -> e2e/zz-mutant-confirmation.spec.ts (genuine);
    text-differs=yes; bytes 36864 -> 37291
  $ npx playwright test e2e/zz-mutant-confirmation.spec.ts --project=chromium --workers=1 --grep "the moment is a full screen"
    PROBE-APPLIED=1
    Error: court · 320×568: the moment must be in the document exactly once. ZERO means it did not
    render on ?paid=1 + a confirmed booking … MORE THAN ONE means a GENUINE second copy, because the
    precondition above has already established that nothing is left staged in React's streaming area;
    two committed confirmation sections on the surface a booker lands on after paying is a defect in
    the route, not a race. Do not make either direction pass by scoping the locator.
        14 × locator resolved to 2 elements
    1 failed

INJECT=confirmation-genuine
APPLIED=1
RESULT=red

  ⚠ AND THE MESSAGE IS THE POINT, NOT A DETAIL. Before Task 3 this same red printed *"the moment did
  not render … either a broken mount or a 404 from the owner gate"* — a sentence that names a cause the
  document refutes, on a document holding TWO of the thing it says did not render. It now names the
  duplicate, in both directions, and it does not name a height. That correction is the plan's
  prohibition against leaving a failure message asserting a refuted cause, applied to the case that
  needed it.

  $ node mutate.mjs picker genuine
    MUTATED e2e/avatar-crop.spec.ts -> e2e/zz-mutant-picker.spec.ts (genuine);
    text-differs=yes; bytes 170579 -> 170949
  $ npx playwright test e2e/zz-mutant-picker.spec.ts --project=chromium --workers=1 --grep "undiluted indicator"
    PROBE-APPLIED=1
    Error: locator.setInputFiles: Error: strict mode violation: locator('input[type="file"]')
    resolved to 2 elements:
        at pick (…/e2e/zz-mutant-picker.spec.ts:226:3)
    1 failed

INJECT=profile-genuine
APPLIED=1
RESULT=red

  It fails AT THE FIXTURE-HANDING HELPER, which is where the behaviour said it must: strict mode is
  still the assertion that the picker stayed unique, and it is still unqualified by any landmark.

-------------------------------------------------------------------------------------------------
(2) CONTROL A — THE TRANSIENT FORM. A copy the delivery mechanism itself produces AND REMOVES.
-------------------------------------------------------------------------------------------------

Injected BEFORE the commit precondition, inside a hidden staging div appended to the body with the
generated identifier — the shape the walk in Task 1 measured — and removed 400ms later, as the
mechanism removes its own. This is what separates "the instrument still catches the defect" from "the
instrument catches any transient", and it is the control that forbids a repair which merely sleeps.

  $ node mutate.mjs confirmation transient    # text-differs=yes; bytes 36864 -> 37472
  $ npx playwright test e2e/zz-mutant-confirmation.spec.ts … --grep "the moment is a full screen"
    PROBE-APPLIED=1  (×6, one per theme×viewport load)
    1 passed (24.8s)

INJECT=confirmation-transient
APPLIED=1
RESULT=green

  $ node mutate.mjs picker transient          # text-differs=yes; bytes 170579 -> 171130
  $ npx playwright test e2e/zz-mutant-picker.spec.ts … --grep "undiluted indicator"
    PROBE-APPLIED=1
    1 passed (13.9s)

INJECT=profile-transient
APPLIED=1
RESULT=green

-------------------------------------------------------------------------------------------------
(3) CONTROL B — NO INJECTION AT ALL. Both shipped cases, together, at the runner's shape.
-------------------------------------------------------------------------------------------------

  $ npx playwright test e2e/confirmation-decay.spec.ts e2e/avatar-crop.spec.ts --project=chromium --workers=1 --retries=2
    37 passed (2.3m)          — 0 failed, 0 flaky, 0 did not run

RESULT=green   (control B, confirmation surface — no injection)
RESULT=green   (control B, profile surface — no injection)

-------------------------------------------------------------------------------------------------
(4) THE PROBES ARE GONE, AND THE COLLECTED COUNT IS THE PROOF
-------------------------------------------------------------------------------------------------

Four throwaway files existed during this plan and none was ever committed:
`e2e/zz-probe-dup-mount.spec.ts`, `e2e/zz-probe-dup-picker.spec.ts`,
`e2e/zz-mutant-confirmation.spec.ts`, `e2e/zz-mutant-picker.spec.ts`.

  $ npx playwright test --project=chromium --list | tail -1
    Total: 461 tests in 39 files          # recorded BEFORE the probes existed
    Total: 461 tests in 39 files          # after they were deleted
  $ git status --porcelain
    (no e2e/ entry beyond the shipped specs this plan modified)

-------------------------------------------------------------------------------------------------
CLOSING — BOTH SURFACES, BOTH REPAIRS, WHAT WAS REJECTED, AND WHAT THIS DID NOT CLOSE
-------------------------------------------------------------------------------------------------

BRANCH VERDICTS      : confirmation = commit-window artefact ; profile = commit-window artefact.
SHARED-CAUSE (vs 16) : no. Measured on both sides of 19.1-16's repair, same chain, HYDRATION-MSGS=0 on
                       the booker route on both trees; `(app)/layout.tsx` passes no `nav`, so the
                       component 19.1-16 repaired never renders on this shell.
SAME-BRANCH (17↔17)  : yes, decided by the divergence point, measured on each surface's own walk.
REPAIRS              : `expectStreamCommitted` in `e2e/confirmation-decay.spec.ts`;
                       `expectPickerCommitted` in `e2e/avatar-crop.spec.ts`. Both assert the commit.
                       Both notes rewritten to what was measured, both prohibitions kept, the refuted
                       "missing settle" hypothesis kept as history.
REJECTED             : scoping to `main`; handing `boxOf` the visible match; widening a budget;
                       sleeping past the window; changing the route's streaming structure. Each with
                       the constraint that decided it, in Task 1 Step 4 above.
PRODUCTION SOURCE    : one comment in `src/components/profile/avatar-field.tsx`, correcting a sentence
                       the spec was citing. No behaviour changed, no dependency added, no migration.
ALLOWLIST            : untouched. PINNED_ENTRY_TOTAL still 2; `tests/design/e2e-known-failures.test.ts`
                       9 passed. `e2e/avatar-crop.spec.ts`'s one annotation, its `CI` condition and its
                       raised budget are byte-unchanged.

SC4-TERM=19.1-17 closed the two booker "one element resolved to two" failures by measuring that neither is a duplicate mount — both are the same React streaming commit window, staged in a hidden div at the end of the body and gone on their own — and by giving each case a precondition that ASSERTS the commit, proved still able to redden against a genuine injected duplicate and proved NOT to redden against the transient form; it did NOT close SC4, which stays open on gate-e2e's remaining failures and on gate-visual, and it did not touch the two host specs 19.1-16 owns or the wizard-advance cause they are waiting on.
