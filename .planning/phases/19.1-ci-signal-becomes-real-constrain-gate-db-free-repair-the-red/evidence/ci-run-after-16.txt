19.1-16 TASK 4 — THE REPAIR READ ON A REAL, PUSH-TRIGGERED `ci` RUN, JOINED BY THE PUSHED HEAD SHA
================================================================================================

Convention: `19.1-PATTERNS.md` §A and 19.1-13's own capture shape — the command line above its own
unedited output, line-anchored markers at column zero. The join key is the SHA, never the clock.

------------------------------------------------------------------------------------------------
STEP 1 — THE PUSH, AND WHAT WENT WITH IT
------------------------------------------------------------------------------------------------

$ git rev-list --count origin/dev..HEAD          # before the push
6
$ git log origin/dev..HEAD --oneline             # before the push
2e4dc43 test(19.1-16): remember the class — a census over Radix's asChild seam, watched red
fe47247 docs(19.1-16): the pinned image does not separate the two owned specs — before or after
282073c fix(19.1-16): the (host) tree is no longer regenerated — the cloned trigger moves to the client
b2e9607 docs(19.1-16): reproduce the (host) hydration failure cold, with two controls
16e3804 docs(19.1): gap plans verified — 16..19 checker-passed, STATE points at execution
48016ed docs(19.1): revise 19.1-16..19 — each closes on a real ci run, joined by head SHA

$ git push origin dev
   ff10020..2e4dc43  dev -> dev

CI-HEAD-SHA=2e4dc43b4e15bdab70dabb87e2e1ae0ffd9cbba0

COMMITS PREDATING THIS PLAN THAT WERE STILL UNPUSHED: two, both planning-only —
`16e3804` and `48016ed`, touching only `.planning/STATE.md` and
`.planning/phases/19.1-…/19.1-{16,17,18,19}-PLAN.md`. Neither touches `.github/workflows/ci.yml`,
`scripts/verify-workflows.mjs`, `src/`, `e2e/` or `tests/`. So this run is NOT the first exercise of
the hardened workflow file — `19.1-VERIFICATION.md`'s eight-commits-ahead finding was closed before
this plan started, and run `33976831607` already ran the hardened file. Stated because the plan asked
the question, and the answer is "no" rather than "yes".

------------------------------------------------------------------------------------------------
STEP 2 — THE RUN, SELECTED BY SHA AND NEVER BY RECENCY
------------------------------------------------------------------------------------------------

$ gh run list --workflow ci.yml --commit 2e4dc43b4e15bdab70dabb87e2e1ae0ffd9cbba0 --json databaseId,headSha,event,status,conclusion,createdAt --limit 10
[{"conclusion":"","createdAt":"2026-09-06T00:19:19Z","databaseId":34000975768,"event":"push","headSha":"2e4dc43b4e15bdab70dabb87e2e1ae0ffd9cbba0","status":"in_progress"}]

Exactly one run for that SHA. Selection is by SHA equality, not by position in the list.

$ gh run view 34000975768 --json databaseId,headBranch,headSha,event,createdAt,updatedAt,conclusion,url
{"conclusion":"failure","createdAt":"2026-09-06T00:19:19Z","databaseId":34000975768,"event":"push","headBranch":"dev","headSha":"2e4dc43b4e15bdab70dabb87e2e1ae0ffd9cbba0","updatedAt":"2026-09-06T00:46:08Z","url":"https://github.com/pengr3/FitOut/actions/runs/34000975768"}

CI-RUN-ID=34000975768
CI-EVENT=push
CI-CONCLUSION=failure

The event is the push event and the run's own head SHA equals the recorded `CI-HEAD-SHA=`.

------------------------------------------------------------------------------------------------
STEP 3 — THE CONCURRENCY ACCOUNTING
------------------------------------------------------------------------------------------------

The workflow's concurrency group is keyed on workflow, ref and event with `cancel-in-progress: true`,
and two runs already died that way inside this phase (`33971557439`, `33971883942`). This run did not:
its conclusion is `failure`, not `cancelled`, and it ran to 26m 49s of wall clock with all five jobs
reaching a terminal state. No second push was made while it was in flight.

CANCELLED-DISCARDED=0

------------------------------------------------------------------------------------------------
STEP 4 — ALL FIVE JOBS (D-04: the criterion is about the RUN, not about one job)
------------------------------------------------------------------------------------------------

$ gh run view 34000975768 --json jobs --jq '.jobs[] | "\(.name)|\(.conclusion)|\(.startedAt)|\(.completedAt)"'

JOB gate-db-free (lint + design + build + workflow parse) | success | 3m 41s (00:19:25 -> 00:23:06)
JOB gate-db (vitest against PostGIS 18) | success | 7m 49s (00:19:25 -> 00:27:14)
JOB gate-price-parity (DB-vs-DOM price, 1 spec) | success | 2m 21s (00:19:26 -> 00:21:47)
JOB gate-visual (GATE-01 visual regression) | failure | 5m 11s (00:19:24 -> 00:24:35)
JOB gate-e2e (functional Playwright suite) | failure | 26m 41s (00:19:26 -> 00:46:07)

GATE-E2E=failure

`gate-db-free` is green, which means the design gate ran ON THE RUNNER and passed — including this
plan's new `tests/design/suspense-fallback-overlay.test.ts` and the untouched known-failures census.

$ gh run view --job 101399672247 --log     # the functional job's own output, summary block verbatim

  3 failed
    [chromium] › e2e/hold-countdown.spec.ts:292:9 › SHELL-03 — the checkout header does not reflow once per session › court · the header and countdown boxes are identical in all four states
    [chromium] › e2e/host-headings.spec.ts:1052:7 › AC#35 — one first-level heading per document, one size across five surfaces › wizard · whole space, every step
    [chromium] › e2e/overflow-320.spec.ts:3434:11 › AC#36 — every Phase-14 host surface at 320px, in both themes › /host/listings/[id]/edit · photos step · court
  3 flaky
    [chromium] › e2e/avatar-crop.spec.ts:1419:7 › CROP-01 / D-178 — the stage pans by keyboard, with the library's clamp intact › Shift makes the step FINER, which is the shipped contract D-178 accepted
    [chromium] › e2e/confirmation-decay.spec.ts:151:7 › BFLOW-08 — the confirmation moment fills the first screen and then decays › the moment is a full screen, the detail starts below the fold, and a reload shows the ordinary page
    [chromium] › e2e/shell.spec.ts:291:11 › AC#3 — the header does not move when the auth slot resolves › host · court · the header and brand boxes are byte-identical
  52 skipped
  22 did not run
  381 passed (24.9m)
  ##[error]Process completed with exit code 1.

------------------------------------------------------------------------------------------------
STEP 4a — THE ACCOUNTABILITY LINES. BOTH OWNED SPECS ARE STILL PRESENT, AND IT IS WRITTEN PLAINLY.
------------------------------------------------------------------------------------------------

OWNED e2e/host-headings.spec.ts|present
OWNED e2e/overflow-320.spec.ts|present

THIS PLAN DOES NOT CLAIM ITS GAP CLOSED. Its own Task 4 says so in as many words, and the criterion
is not softened: both specs it owns appear in this run's `gate-e2e` failure list, on all three
attempts each.

------------------------------------------------------------------------------------------------
STEP 4b — WHAT THE RUN *DID* PROVE, MEASURED AS A BEFORE/AFTER PAIR ON THE RUNNER ITSELF
------------------------------------------------------------------------------------------------

The instrument the container failed to be, the runner supplied for free. The dev server forwards the
browser's console into the job log (`[WebServer] [browser] …`), so the hydration failure this plan
repaired is COUNTABLE in `gate-e2e`'s own transcript, on both sides.

$ gh run view --job <gate-e2e of 33976831607> --log | grep -c "regenerated on the client"
110                       the run before this plan, on the pre-repair tree

$ gh run view --job 101399672247 --log | grep -c "regenerated on the client"
0                         this run, on the repaired tree

$ ... | grep -c "Hydration failed"
110  ->  0

ONE HUNDRED AND TEN TO ZERO, ON THE RUNNER. That is the repair proven where SC4 lives, not on a
laptop and not in a local copy of the runner image. It is the strongest single measurement in this
plan and it was not available until the push.

⚠ AND IT SETTLES THE CAUSAL QUESTION THE PLAN WAS BUILT AROUND — IN THE NEGATIVE.

19.1-13 recorded the regeneration as the LEADING CANDIDATE for the two lost presses and stated the
link was not observed. It is now observed, and it is ABSENT: the regeneration is gone from the runner
and BOTH owned specs fail exactly as before. Their failure messages are byte-identical across the two
runs — same site, same step, same text:

  O11  `walkWizard` at e2e/host-headings.spec.ts:1034
       "wizard · whole space · step 3 of 9: pressing the advance control did not change the question
        — it is still \"Where is it?\". … The save-state region says: (nothing)."
       ⚠ NOTE WHERE IT IS *NOT*. It is not at `pressAdvance`'s "NO SAVE REQUEST LEFT THE PAGE" branch.
       A save POST DID leave the page — the press was RECEIVED — and the step still did not advance
       while the status region stayed empty. That is not the lost-click shape at all; a lost click
       reddens `pressAdvance`, and `pressAdvance` passed.

  O12  e2e/overflow-320.spec.ts:3465
       "the walk did not reach the surface this row measures — the cover preview's own heading …
        getByRole('heading', { name: 'How your cover appears' }) … element(s) not found"
       Court only. `grove` passed this run.

Both live on `/host/listings/[id]/edit`, and both are "the wizard did not move". The regeneration was
not their cause, and this run is the measurement that says so.

------------------------------------------------------------------------------------------------
STEP 4c — WHAT DID MOVE ON THE RUNNER, KEYED ON FILE AND FULL TITLE
------------------------------------------------------------------------------------------------

                       33976831607 (before)      34000975768 (this run)     delta
  failed                        4                          3                 -1
  flaky                         2                          3                 +1
  did not run                  25                         22                 -3
  passed                      377                        381                 +4
  skipped                      52                         52                  0
  wall clock                24.4m                      24.9m

$ comm -13 <(passing set, before) <(passing set, now)      # keyed on file + full title, never file:line
RELEASED e2e/confirmation-decay.spec.ts | "BFLOW-08 — the confirmation moment fills the first screen and then decays › a seeded pending row completes all 8 poll attempts with ZERO navigations (D-89)"
RELEASED e2e/confirmation-decay.spec.ts | "BFLOW-08 — the confirmation moment fills the first screen and then decays › court · the success mark honours reduced motion, and nothing loops"
RELEASED e2e/confirmation-decay.spec.ts | "BFLOW-08 — the confirmation moment fills the first screen and then decays › grove · the success mark honours reduced motion, and nothing loops"

NEWLY PASSING, NOT RELEASED — this plan's own new case, green on the runner first attempt:
  e2e/shell.spec.ts:1399 | "SHELL-01 — the host composition raises no hydration mismatch (CI-01) › a full load of /host regenerates no tree, on a page proven to have hydrated"

REGRESSED: none. Nothing that passed before stopped passing.

RELEASED-TOTAL=3
DELTA-VS-13=-21

⚠ AND THE ATTRIBUTION IS NOT THIS PLAN'S TO TAKE. 19.1-13 recorded TWENTY-FOUR tests blocked by the
publish-checklist row (O12). That row is STILL RED, so it released nothing. The three released tests
are the tail of `confirmation-decay.spec.ts`'s serial group, freed because `:151` flipped from a hard
failure to a flake — the settle 19.1-13 itself installed, landing on its second run. THE NUMBER IS
WHATEVER THE RUN PRINTS, and what it prints is three, from a different row, owned by a different
plan. The 24 is not edited into agreement and is not claimed as closed.

------------------------------------------------------------------------------------------------
STEP 5 — WHAT IS STILL RED, AND WHO OWNS IT
------------------------------------------------------------------------------------------------

STILL-RED e2e/hold-countdown.spec.ts:292 "SHELL-03 — the checkout header does not reflow once per session › court · the header and countdown boxes are identical in all four states" | owner: PROPOSED 19.1-18 (N1, the day click that does not select, on the checkout path)
STILL-RED e2e/host-headings.spec.ts:1052 "AC#35 … › wizard · whole space, every step" | owner: 19.1-16 (this plan) — NOT CLOSED. See the follow-up proposed below; the mechanism this plan owned is now measured absent and the surviving cause is unnamed.
STILL-RED e2e/overflow-320.spec.ts:3434 "AC#36 … › /host/listings/[id]/edit · photos step · court" | owner: 19.1-16 (this plan) — NOT CLOSED, same sentence.
STILL-RED gate-visual (GATE-01 visual regression) — the four baselines whose references encode the calendar day and the seed month | owner: PROPOSED 19.1-19, specified in six points by evidence/rows-9-12-fixture-probe.txt under D-19.1-D. Red on every push by design until it mints.

THE FLAKY THREE, recorded because a required check whose flaky set is redrawn each run is `T-19.1-63`:
  e2e/avatar-crop.spec.ts:1419      | owner: PROPOSED 19.1-17 (N2's file)
  e2e/confirmation-decay.spec.ts:151| owner: PROPOSED 19.1-17 — improved: hard failure -> flake
  e2e/shell.spec.ts:291 host · court| a LEAD this plan carried. Measured 8/8 green locally at CI's
                                      shape; drawn flaky on the runner in the GROVE theme last run and
                                      in the COURT theme this one — i.e. it moves theme between runs,
                                      which is a timing signature and not a theme defect. Verdict
                                      stands at UNAFFECTED; the instrument is 19.1-15's.

------------------------------------------------------------------------------------------------
THE PROPOSED FOLLOW-UP, IN THE D-02 SHAPE, AND IT IS NOT AN ANNOTATION
------------------------------------------------------------------------------------------------

PROPOSED — "the listing wizard does not advance on the runner, and the press was received"

  OWNS: `e2e/host-headings.spec.ts:1052` (O11) and `e2e/overflow-320.spec.ts:3434 · photos step ·
  court` (O12). Both on `/host/listings/[id]/edit`. Both barred from the allowlist by written
  verdicts — O11 because it is production source (D-02), O12 by name in
  `evidence/triage-upload-capability.txt` § PROHIBITION.

  WHAT IS NOW KNOWN, AND IT IS MORE THAN 19.1-13 KNEW:
    • It is NOT the `(host)` hydration mismatch. 110 -> 0 on the runner with both specs unmoved.
    • It is NOT a lost click, at least not for O11: `pressAdvance` PASSES — a save POST leaves the
      page — and the walk then stalls with the save-state region EMPTY. `saveAndContinue` writes
      `Saving…` into that region SYNCHRONOUSLY before it awaits anything, so an empty region 30
      seconds after a POST left the page is a contradiction that names its own next question.
    • It does not reproduce on this laptop, nor in `mcr.microsoft.com/playwright:v1.60.0-noble` at
      `--workers=1 --retries=2`, on the pre-repair tree or the repaired one (19.1-16 Task 2).

  ITS FIRST INSTRUMENT, NAMED: resolve the contradiction above before touching any product code.
  Widen `pressAdvance`'s press-received predicate from "any POST to /host/listings/" to the SPECIFIC
  server action, and print the responding request's URL, status and `Next-Action` header into the
  failure message. If the observed POST turns out to be a DIFFERENT action (an autosave on the blur
  the click causes, say), then `pressAdvance` has been reporting a lost press as a received one and
  the instrument — not the product — is what the last three runs were measuring. That is a
  STRENGTHENING of the instrument, which the prohibition permits; weakening it is what it forbids.
  Second instrument, if the predicate holds: capture the wizard route's own browser console in the
  runner's log and read whether `/host/listings/[id]/edit` reports anything of its own.

  ⚠ WHAT THE FOLLOW-UP MAY NOT DO: add a retry to any press (the click was never observed lost, and
  19.1-08's refusal is inherited); weaken the advance assertion or the arrival assertion; add either
  spec to the known-failures allowlist.

SC4-RUN-TERM=Run 34000975768 proved on the runner itself that the `(host)` hydration regeneration this plan repaired is gone — 110 messages to 0, with `gate-db-free`, `gate-db` and `gate-price-parity` green and this plan's own new case passing first attempt — and it proved, by the same measurement, that the regeneration was NOT the cause of the two failures this plan owns, which are unchanged and still red; SC4 stays OPEN, `gate-e2e` and `gate-visual` are still red, and the two owned specs pass to a named follow-up rather than to an annotation.

------------------------------------------------------------------------------------------------
ADDENDUM — THE CLOSE-OUT PUSHES, RECORDED SO A LATER `gh run list` IS NOT CONFUSING
------------------------------------------------------------------------------------------------

Run 34000975768 was read to a terminal conclusion BEFORE anything else was pushed, and no push
happened while it was in flight — that is the constraint the concurrency group imposes and it was
honoured.

Two further pushes followed it, both after it was terminal, and neither can have cancelled or altered
it:

  bdbb0d7  docs(19.1-16): this evidence file
  8a051dc  docs(19.1-16): the SUMMARY, STATE.md and ROADMAP.md

Both are documentation-only — no `src/`, no `e2e/`, no `tests/`, no `.github/`, no `scripts/`. Each
starts a `ci` run of its own because the workflow runs on every `dev` push (D-06), and those runs are
NOT this plan's measurement: the plan is read on 34000975768 and on nothing else. A later run's
verdict on a docs-only tree is neither evidence for nor against anything recorded above.

The plan's "ONE push" rule is about not cancelling the run being read. It was not violated. The rule
as written — "nothing after it pushes either" — is unsatisfiable for any plan that must also commit
its own SUMMARY and leave `git log origin/dev..HEAD` empty, and that is worth carrying forward to
plans 17, 18 and 19: the constraint that matters is ONE push while a run is IN FLIGHT.
