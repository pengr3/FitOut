---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 14
type: execute
wave: 6
depends_on: ["19.1-13"]
files_modified:
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md
autonomous: false
requirements: [CI-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-08 — the public flip happens at the END of the phase, after every criterion has landed, and THE USER RUNS IT, NOT CLAUDE. The last thing this plan does is produce a pre-flight checklist plus a fresh full-history secret scan, hand over the exact command, and stop. Two consequences follow deliberately: the first thing anyone sees is a green badge rather than a red one, and the irreversible outward-facing act stays with the PM."
    - "D-07 — the planning directory is published AS IS. There is no redaction pass over the review and verification documents, because once SC1 through SC3 have closed the vectors those documents describe DEFEATED attacks, which is ordinary security writing. Redaction was rejected as theatre: the original payload text stays in git history across the whole commit range unless the history is rewritten, so it would buy the appearance of caution at the cost of damaging the record."
    - "D-07 / D-08 — the sequencing is load-bearing and this plan VERIFIES it rather than assuming it. Before the handover, the checklist confirms with cited evidence that each documented vector is now caught by a named invariant with a standing case; publishing while any of them is open would ship an attack recipe alongside the code it applies to."
    - "D-08 — the secret scan is FRESH, not a reference to the earlier one. It walks every ref rather than the current branch, its full output is captured regardless of outcome because the exit code alone is not the record, and a second independent pass greps history for this project's own credential spellings because a detector tuned to limit false positives can miss a project-specific one."
    - "CI-01 empty (spec-less edge probe, verification: explicit) — a scan that found nothing because it scanned nothing is the exact vacuous green this project's whole checker exists to delete. The scan asserts a non-zero commit count that the scanner itself reports, and that number is compared against this repository's actual history length before any clean result is accepted."
    - "CI-01 encoding (spec-less edge probe, verification: backstop) — { statement: 'the second independent pass matches this project s own credential prefixes case-sensitively against the diff text of every ref, and the checklist names every spelling searched, because a generic detector and a project-specific grep answer different questions and neither subsumes the other', verification: backstop }"
    - "D-05 / D-06 — the ruleset payload is authored and checked in BEFORE it is applied, as declarative configuration rather than as a sequence of clicks. Its four required contexts are the four behaviour jobs' display-name strings copied from the workflow file; the visual job is deliberately absent so a pixel comparison cannot wedge a merge over antialiasing or a legitimate one-pixel design change; and the scope is the default branch only, so the development branch stays unprotected and direct commits keep working."
    - "D-08 — the handover is a checkpoint, not a suggestion. The plan stops at a decision checkpoint carrying the exact command, the reason the act is irreversible, and the state to confirm afterwards. Pre-authorising the executor to run it was explicitly rejected."
  artifacts:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md
  key_links:
    - "each documented vector to the invariant that now catches it and the standing case that remembers it. This mapping is the precondition for D-07's as-is publication, and it is what turns the review document from a recipe into ordinary security writing."
    - "the four job display names to the four required status-check contexts. GitHub matches on the display name rather than the job key, which is why plan 01 pinned those strings; the payload consumes exactly what the checker asserts."
    - "the secret scan's commit count to the credibility of its clean result. A scan over an unmounted directory produces the same clean exit as a scan over a clean repository."
    - "this plan's checkpoint to plan 15's precondition. Plan 15 cannot run until a human has flipped the repository, and it states that as an assertable precondition rather than assuming the ordering."
  prohibitions:
    - "The repository must never be made public by the executor. The act is irreversible and outward-facing and it is held by the PM by decision."
    - "No redaction pass may be made over the planning directory. The original text remains in history regardless, so redaction damages the record without reducing the exposure."
    - "A credential VALUE must never be printed into an evidence file. If a scan produces a hit, record the file and the line, never the value. This is the rule the repository's own refusal script states about itself, and it binds every diagnostic this phase writes — especially one that is about to be published."
    - "A clean scan result must never be accepted without evidence that the scan had something to scan. Assert the commit count and confirm the mount before reading the exit code."
    - "The visual job must never be added to the required status checks. A pixel comparison cannot be allowed to wedge a merge over antialiasing or a legitimate one-pixel design change."
    - "Branch protection must never be extended to the development branch. It would force a pull request and a full suite wait on every commit and roughly halve execution pace."
  assumptions:
    - "SPEC-LESS EDGE PROBE — this plan authors `empty` explicitly (the scan's non-vacuity guard) and `encoding` as a backstop marker (the case-sensitive project-specific credential spellings). `adjacency` and `ordering` are owned by plans 01, 06, 07, 09, 11 and 13; none is dropped."
    - "The earlier scan's finding that only the example environment file was ever committed is RE-ESTABLISHED here rather than restated. D-08 requires a fresh scan, and a restated finding is not one."
    - "A6 (RESEARCH.md) — whether a required-status-checks ruleset also blocks direct pushes to the default branch is unresolved and is NOT planned around. Plan 15 probes it after the flip and records the answer."
    - "The scanning container image is pulled from the project's own published registry named in its README. No package-manager install occurs and no dependency is added to the repository, so no legitimacy-gate entry is created."
---

<objective>
Produce everything the public flip needs and then stop: a fresh full-history secret scan with its
non-vacuity proof, a checked-in ruleset payload, a pre-flight checklist confirming with cited evidence that
the documented vectors are closed, and a decision checkpoint handing the PM the exact command.

Purpose: D-08 sequences the flip last and holds it with the PM, and D-07 makes it irreversible —
publishing puts the planning directory and its full history in front of crawlers and mirrors, and
un-publishing does not retract what was fetched or indexed. The one thing that makes as-is publication
correct is that the vectors those documents describe are closed, and this plan verifies that rather than
assuming it.

Output: three evidence artefacts and a stop.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-RESEARCH.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a fresh full-history secret scan, and prove it scanned something</name>

  <precondition>
    Docker Desktop is running. Neither of the two common native secret scanners is installed on this
    machine, so the scan runs from a container image; this shell rewrites leading-slash paths, which also
    mangles a mount path, so the mount must be confirmed before the result is read.
  </precondition>

  <read_first>
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"D-08 — the pre-flight secret scan": the recommended
      command, the registry the image comes from, the note that the older subcommand was deprecated in
      favour of the current one, the flag that makes it walk every ref, the exit-code semantics, the mount
      warning specific to this shell, and the recommended second independent pass with its pattern list.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` §Specific Ideas — the requirement that the scan be fresh
      rather than a reference to the earlier one, and the earlier scan's recorded result.
    - `scripts/refuse-mail-credential.mjs` — the rule the file states about its own diagnostics: it prints
      names and counts, never values, because a credential echoed into a log is a wider disclosure than the
      send the control prevents. That rule binds this evidence file, which is about to be published.
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Security Domain" — the note that the scan reads for
      credentials and must never print one.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt
  </files>

  <action>
PASS 1 — the detector. Run the scanning container against this repository, walking EVERY ref rather than
the current branch, using the current subcommand rather than the deprecated one. Confirm the mount worked
BEFORE reading any result: have the scanner report the number of commits it processed, and compare that
number against this repository's actual history length. A scan that found nothing because it scanned
nothing is the exact vacuous green this project's whole checker exists to delete.

Capture the full output into `evidence/secret-scan-pre-public.txt` with the command line above it and the
exit code below it, regardless of the outcome. The exit code alone is not the record.

PASS 2 — the project-specific grep. Search the diff text of every ref for this project's OWN credential
spellings: the payment provider's test and live secret-key prefixes, the transactional-mail key prefix, and
the assignment forms of the payment secret, the auth secret and the media-provider secret environment
variables. Record the command and a per-pattern result count, naming every pattern searched. A generic
detector tuned to limit false positives can miss a project-specific spelling, and the two passes answer
different questions.

PASS 3 — the environment-file history. List every file ever ADDED to history whose name matches an
environment-file pattern, and record the command and its output. Re-establish the earlier finding rather
than restating it.

If ANY pass produces a hit: record the FILE and the LINE, never the value, state at the TOP of the file
that the flip is BLOCKED pending a decision about history, and stop.

Close the evidence file with a line beginning `SCAN` carrying the commit count the scanner reported, the
finding count from each pass, and a clean-or-blocked verdict.
  </action>

  <acceptance_criteria>
    - `evidence/secret-scan-pre-public.txt` contains three command lines with their full unedited outputs,
      plus the exit code of the first.
    - It records the commit count the scanner itself reported and states how the mount was confirmed.
    - It records a per-pattern result count for the project-specific pass and names every pattern searched.
    - It records the environment-file history result with the command that produced it.
    - It ends with a line beginning `SCAN` carrying the commit count, per-pass finding counts and a verdict.
    - It contains no credential value — only file names, line numbers and counts.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^SCAN' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt</automated>
    <fails_when>output is 0 — the scan has no verdict line, so its clean-or-blocked result is not stated anywhere assertable</fails_when>
    <automated>grep -ciE 'sk_(test|live)_[A-Za-z0-9]{8}|re_[A-Za-z0-9]{16}|BETTER_AUTH_SECRET=[^[:space:]]|CLOUDINARY_API_SECRET=[^[:space:]]' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/secret-scan-pre-public.txt || true</automated>
    <fails_when>output is any number other than 0 — a credential-shaped VALUE reached an evidence file that is about to be published, which is a wider disclosure than the one the scan exists to prevent</fails_when>
    <automated>git log --all --oneline | wc -l</automated>
    <fails_when>the number printed differs from the commit count the evidence file's SCAN line records — the container scanned a different tree than this repository, and its clean result would be about something else</fails_when>
  </verify>

  <done>
    A fresh scan over every ref has run, its full output is on disk with its exit code, its reported commit
    count matches this repository's history, an independent project-specific pass ran beside it, and the
    verdict is stated in one assertable line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the ruleset payload and the checklist that justifies as-is publication</name>

  <read_first>
    - `.planning/phases/19.1-.../19.1-RESEARCH.md` §"Part 3 — SC5": the probed repository state and the
      permission error that blocks the settings API while the repository is private, the two shell gotchas,
      the three reasons for choosing a declarative ruleset over the legacy protection surface, the exact
      payload with its field-by-field notes, and the recommendation on the up-to-date-branch policy with
      its cost argument.
    - `.github/workflows/ci.yml` — the five jobs' display-name strings verbatim. These are the contexts and
      they must be copied from the file, never typed.
    - `.planning/phases/19.1-.../evidence/suite-remeasurement.txt` — the `HANDOFF` line carrying the display
      names and the merge-wait duration, and the SC4 closure line.
    - `.planning/phases/19.1-.../evidence/guards-01-post-fix.txt`,
      `.planning/phases/19.1-.../evidence/guards-05-post-fix.txt`,
      `.planning/phases/19.1-.../evidence/guards-06-post-fix.txt` — the closing mappings from each
      documented vector to the invariant that catches it and the case that remembers it. The checklist
      cites these by name.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-05, D-06, D-07 and D-08 in full.
    - `.planning/ROADMAP.md` §"Phase 19.1" — the disclosure-sequencing warning, quoted verbatim in the
      checklist.
  </read_first>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json,
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md
  </files>

  <action>
Author `evidence/ruleset-main.json` as the exact declarative payload plan 15 will submit. It targets
branches; it is actively enforced; its condition selects the default branch through the symbolic reference
rather than a literal branch name, so it survives a future rename; and its single rule requires four status
checks. The four contexts are the display-name strings of the four behaviour jobs, COPIED out of the
workflow file. The visual job is deliberately absent. The up-to-date-branch policy is disabled. Leave the
bypass list empty and omit the optional integration identifier.

Record the up-to-date-branch reasoning in a sibling note file or as a top-level comment field the API
ignores — whichever keeps the payload valid: with a single-developer repository there is no collaborator to
race, and enabling that policy would cost a full suite re-run every time the default branch moves between a
green check and the merge click.

Then author `evidence/preflight-public.md`, the document that is handed over. Four sections:

(a) WHY AS-IS PUBLICATION IS CORRECT. One row per documented vector: the vector identifier, the invariant
that now catches it, the standing case that remembers it, and the evidence file showing it red. Cite the
guard evidence files by path. Then state D-07's reasoning in one paragraph: once the vectors are closed
those documents describe defeated attacks; redaction is theatre because the original text remains in
history regardless; and moving the planning directory to a private mirror was rejected because it breaks
every in-repository link into it.

(b) CRITERIA STATE. One row per success criterion, its verdict and the artefact that proves it, ending with
the SC4 closure line quoted from the re-measurement evidence together with the run identifier behind it.

(c) THE SCAN. The `SCAN` verdict line from Task 1 quoted, the commit count, and an explicit statement that
a hit anywhere blocks the flip pending a decision about history.

(d) THE FLIP. The exact command for the PM to run, and the exact read-back commands to confirm it
afterwards. Note the shell gotcha: the settings API path is written WITHOUT a leading slash, because this
shell rewrites a leading-slash path into a filesystem path and the call fails with a confusing error naming
a directory that does not exist. State that the ruleset is applied AFTER the flip, by plan 15, because the
settings API returns a permission error while the repository is private.

Do NOT run the flip command. Do NOT submit the ruleset — it would fail with the recorded permission error,
and plan 15 owns it.
  </action>

  <acceptance_criteria>
    - `evidence/ruleset-main.json` parses as JSON, targets branches, is actively enforced, selects the
      default branch symbolically, and its status-check rule lists exactly four contexts.
    - Each of the four contexts appears verbatim as a job display name in `.github/workflows/ci.yml`, and
      the visual job's display name does not appear in the payload.
    - The up-to-date-branch policy is disabled and its reasoning is recorded.
    - `evidence/preflight-public.md` contains all four sections; section (a) has one row per documented
      vector citing an invariant, a standing case and an evidence file path; section (b) has one row per
      success criterion with a proving artefact; section (c) quotes the scan verdict; section (d) carries
      the flip command, the read-back commands and the leading-slash note.
    - The repository is still private — no flip command was run and no ruleset was submitted.
  </acceptance_criteria>

  <verify>
    <automated>node -p "JSON.parse(require('fs').readFileSync('.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json','utf8')).rules.find(r=>r.type==='required_status_checks').parameters.required_status_checks.length"</automated>
    <fails_when>non-zero exit, or the printed number is anything other than 4 — the payload does not require exactly the four behaviour jobs</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output — the workflow was touched while authoring the payload</fails_when>
    <automated>grep -c 'gate-visual' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ruleset-main.json || true</automated>
    <fails_when>output is any number other than 0 — the visual job appears in the required-check payload, which D-05 forbids because a pixel comparison must not be able to wedge a merge</fails_when>
    <automated>gh repo view --json visibility --jq .visibility</automated>
    <fails_when>the output is anything other than PRIVATE — the repository was flipped by the executor, which D-08 reserves for the PM</fails_when>
  </verify>

  <done>
    The exact payload plan 15 will submit is checked in and its four contexts match four job display names
    byte for byte; the checklist states, with cited evidence, why publishing the planning directory as is
    is correct; and the repository is still private.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Hand over the flip — the PM makes the repository public</name>

  <reversibility rating="one-way">
    Publishing puts the planning directory and its full history in front of crawlers and mirrors.
    Un-publishing does not retract what was fetched or indexed. This is the act that makes D-07
    irreversible, which is why it is sequenced last and held by a human.
  </reversibility>

  <read_first>
    - `.planning/phases/19.1-.../evidence/preflight-public.md` — the four-section checklist from Task 2.
    - `.planning/phases/19.1-.../evidence/secret-scan-pre-public.txt` — the `SCAN` verdict line.
    - `.planning/phases/19.1-.../19.1-CONTEXT.md` D-07 and D-08 in full.
  </read_first>

  <decision>
    Make the FitOut repository public now, or hold. This is the phase's last act and the only one the
    executor is forbidden from performing.
  </decision>

  <context>
    Everything the phase owed is done: the job that runs the gates is itself gated, every predicate has been
    audited against the property in its own printed name, the standing suite remembers the fix it was built
    for, and the workflow run is green in all five jobs.

    D-08 places this act with you: publishing is irreversible and outward-facing, so the click stays with
    the PM rather than being pre-authorised to the executor. D-07 governs what gets published: the planning
    directory goes out AS IS. Once the vectors are closed, the review and verification documents describe
    DEFEATED attacks, which is ordinary security writing; redaction was rejected as theatre because the
    original text remains in git history regardless, and a private mirror was rejected because it breaks
    every in-repository link into the planning trail.

    Sequencing it now rather than after criteria one through three means the first thing anyone sees is a
    green badge rather than a red one.

    Before you run it:
    1. Read `evidence/preflight-public.md` — four short sections: why as-is publication is correct, with one
       row per closed vector naming the invariant that catches it and the standing case that remembers it;
       the state of every success criterion with its proving artefact; the secret scan's verdict; and the
       flip itself.
    2. Confirm the `SCAN` line in `evidence/secret-scan-pre-public.txt` reads clean and that the commit
       count it reports matches this repository's history length. A hit anywhere blocks the flip.
    3. Run the flip command written in section (d) of the checklist, then the two read-back commands beside
       it, and confirm the visibility reads public.

    Plan 15 then applies the branch ruleset, which is unreachable until now because the settings API
    returns a permission error on a private repository.
  </context>

  <options>
    <option id="publish">
      <name>Publish now — run the flip command</name>
      <pros>The phase closes with a green badge visible from the first moment; the planning trail, which is most of what makes this repository worth reading, goes public; and the branch ruleset becomes reachable so the four required checks can be installed.</pros>
      <cons>One-way. The planning directory and its full history go in front of crawlers and mirrors, and un-publishing does not retract what was fetched or indexed.</cons>
    </option>
    <option id="hold">
      <name>Hold — do not publish yet</name>
      <pros>Nothing is exposed; the decision can be taken later with the same checklist, which does not go stale.</pros>
      <cons>The branch ruleset cannot be installed, so criterion five stays open and merges into the default branch remain ungated. The four required checks are the point of the phase.</cons>
    </option>
  </options>

  <resume-signal>
    Reply "publish" once you have run the flip and the read-back reads public, or "hold" with what you want
    changed first.
  </resume-signal>

  <files>
    .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md
  </files>

  <action>
Present the checklist and stop. Do not run the flip command under any circumstance, including if the PM
says to proceed without running it themselves — D-08 places the act with them specifically, and a
pre-authorisation was the option that was rejected.

Once the PM reports the flip is done, append a HANDOFF RESULT section to `evidence/preflight-public.md`
recording: the read-back output showing the new visibility, the timestamp, and the fact that the settings
API is now reachable. That section is plan 15's precondition evidence.
  </action>

  <acceptance_criteria>
    - `evidence/preflight-public.md` was presented in full and no flip command was executed by the
      executor.
    - After the PM's action, the document carries a HANDOFF RESULT section with the read-back output and a
      timestamp.
    - The repository's visibility reads public.
  </acceptance_criteria>

  <verify>
    <human-check>The PM has read the pre-flight checklist and run the flip command themselves.</human-check>
    <automated>gh repo view --json visibility --jq .visibility</automated>
    <fails_when>the output is anything other than PUBLIC — the flip has not happened, and plan 15's precondition is unmet</fails_when>
    <automated>grep -c 'HANDOFF RESULT' .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/preflight-public.md</automated>
    <fails_when>output is 0 — the flip's read-back was not recorded, so plan 15 has no evidence its precondition holds</fails_when>
  </verify>

  <done>
    The PM has read the checklist, run the flip themselves, and the read-back is recorded — the repository
    is public and the settings API is reachable for plan 15.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the repository → the public internet | Crossing this boundary is one-way; nothing published can be reliably retracted. |
| git history → the published repository | History carries every version of every file, so a redaction of the working tree changes nothing about what is exposed. |
| an evidence file → the published repository | Everything this phase wrote into the planning directory is published minutes after the flip. |
| the executor → an irreversible outward-facing act | The decision to publish is reserved for the PM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19.1-65 | Information Disclosure | publishing while a documented vector is still open | high | mitigate | Section (a) of the checklist maps every vector to the invariant that catches it and the case that remembers it, citing the evidence file that shows it red; the flip is sequenced after those land. |
| T-19.1-66 | Information Disclosure | a credential in git history exposed by publication | high | mitigate | A fresh scan over every ref plus an independent project-specific pass plus an environment-file history check; any hit blocks the flip. |
| T-19.1-67 | Spoofing | a clean scan result from a scan that scanned nothing | high | mitigate | The scanner's own reported commit count is compared against this repository's history length before the result is accepted. |
| T-19.1-68 | Information Disclosure | a credential value printed into an evidence file about to be published | high | mitigate | Prohibited; the verify greps the evidence file for credential-shaped values and fails on any hit. |
| T-19.1-69 | Elevation of Privilege | the executor performing an irreversible outward-facing act | high | mitigate | The flip is a decision checkpoint; the executor is forbidden from running it even on verbal authorisation, and the verify asserts the repository is still private at the end of Task 2. |
| T-19.1-70 | Denial of Service (self-inflicted) | a pixel-comparison job able to wedge a merge | medium | mitigate | D-05: the visual job is deliberately absent from the required contexts, asserted by grepping the payload. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs. The scanner runs from a container image pulled from the project's own published registry named in its README; no repository dependency is added, so no legitimacy-gate entry is created. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New evidence files:**
`evidence/secret-scan-pre-public.txt` — three passes with full unedited output, the scanner's reported
commit count, per-pattern finding counts and a `SCAN` verdict line;
`evidence/ruleset-main.json` — the exact declarative payload plan 15 submits, with four contexts copied
from the workflow file and the visual job deliberately absent;
`evidence/preflight-public.md` — four sections (vector closure with citations, criteria state with proving
artefacts, the scan verdict, the flip and read-back commands) plus a HANDOFF RESULT section written after
the PM acts.

**New symbols, npm scripts or source changes:** none. This plan produces documents and a checkpoint.
</artifacts_this_phase_produces>

<verification>
- The secret scan's `SCAN` line reads clean and its commit count matches this repository's history length.
- No credential value appears in any evidence file.
- The ruleset payload parses, requires exactly four contexts, and each is a job display name in the
  workflow file; the visual job is absent.
- The pre-flight checklist maps every documented vector to an invariant, a standing case and an evidence
  file.
- The repository is private at the end of Task 2 and public only after the PM acts.
- `node scripts/verify-workflows.mjs` is still green.
</verification>

<success_criteria>
The PM can make an informed irreversible decision from one document: every vector closed with its evidence,
every criterion's state with its proving artefact, a fresh scan that demonstrably scanned the whole history,
and the exact command — and the executor did not run it.
</success_criteria>

<output>
Create `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-14-SUMMARY.md` when done.
Quote the `SCAN` verdict line, state the commit count scanned, list the four required contexts verbatim,
and record the timestamp of the flip so plan 15's precondition has a dated source.
</output>
