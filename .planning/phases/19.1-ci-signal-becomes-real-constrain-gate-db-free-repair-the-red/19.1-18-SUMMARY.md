---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 18
subsystem: testing
tags: [playwright, ci, fake-clock, calendar, host-wizard]

requires:
  - phase: 19.1-08
    provides: the selected-day post-condition and the rule that a click retry requires observed detachment
  - phase: 19.1-13
    provides: the full-suite baseline and the list of remaining functional failures
  - phase: 19.1-17
    provides: the preceding real-run reading and the four earlier-wave specs this plan must protect
provides:
  - "The checkout day-click failure's measured cause: `page.clock.install()` covered availability selection and shifted venue-day arithmetic one day early; the click landed and was refused, so no retry was added"
  - "A runner-image proof that all four hold-countdown cases pass on their first attempt after installing the fake clock only before checkout navigation"
  - "Two distinct day-helper guards: empty enabled/in-month grid at the pre-condition and a clicked day that never selects at the post-condition"
  - "A real push-triggered run (34008844133) where `e2e/hold-countdown.spec.ts` is absent from failures, but `gate-e2e` remains red on seven host-wizard cases"
  - "A precise follow-up boundary: the checkout-path defect is closed; the host-wizard route-resolution cluster needs a separate plan and Plan 19 still owns visual baselines"
affects: [19.1-19, proposed-19.1-20, 19.1-verification]

actuals:
  tokens: 0
  tasks: 4
  commits: 5

tech-stack:
  added: []
  patterns:
    - "Install a fake page clock at the narrowest boundary that needs it; calendar availability must keep the real venue-local day"
    - "A received-but-refused click and a detached/lost click are different branches; retry only the latter, and only when the call log proves it"
    - "A matched-nothing locator intersection needs a positive-count pre-condition or its click timeout misreports an empty grid as an interaction failure"

key-files:
  created:
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-day-click-container.txt
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-18.txt
  modified:
    - e2e/helpers/booker-seed.ts
    - e2e/hold-countdown.spec.ts
    - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/deferred-items.md

key-decisions:
  - "No retry was added: the runner trace says the element was visible, enabled, stable and the click action completed; `DETACHED=no` and `SELECTED=no` select the received-but-refused branch"
  - "The fake clock moved from before signup navigation to immediately before checkout navigation, keeping countdown determinism without corrupting calendar-day arithmetic"
  - "The 2,000ms selected-day budget stayed fixed: runner-image worst cases were 350ms click-to-selected and 315ms selected-to-first-hour"
  - "Task 4's required `gate-e2e=success` result is not softened. Run 34008844133 is a failure, so this plan is `halted` even though all four tasks executed and its own checkout-path case is closed"
  - "The seven failures are recorded as a host-wizard route-resolution cluster requiring a separate follow-up; their shared cause is not asserted before the route probe measures it"

patterns-established:
  - "Fake-clock boundary: install after availability selection, before the checkout state whose countdown needs deterministic time"
  - "Guard both zero matches and failed state transition: pre-condition names an empty grid; post-condition names a day whose selected attribute never lands"

requirements-completed: []

coverage:
  - id: D1
    description: "The runner-image call log decides whether the day click was detached or received and refused"
    requirement: "CI-01"
    verification:
      - kind: other
        ref: "evidence/triage-day-click-container.txt — DETACHED=no, SELECTED=no, call log and interval measurements"
        status: pass
    human_judgment: false
  - id: D2
    description: "The repaired checkout path releases every sibling in the file-scoped serial group without retries"
    verification:
      - kind: e2e
        ref: "Runner image and local whole-file runs: 4/4 first-attempt passes; RELEASED-TOTAL=3"
        status: pass
    human_judgment: false
  - id: D3
    description: "The day helper reports an empty grid and an unselectable clicked day at different assertions, and its four hour-grid states remain distinct"
    verification:
      - kind: e2e
        ref: "Throwaway guard probe: 6 passed; two GUARD=red records with APPLIED>0; four distinct STATE records; probe deleted"
        status: pass
    human_judgment: false
  - id: D4
    description: "The functional Playwright gate reads success on a real push-triggered run and all five owned specs are absent from failures"
    requirement: "CI-01"
    verification:
      - kind: other
        ref: "evidence/ci-run-after-18.txt — run 34008844133, gate-e2e=failure; host-headings and overflow-320 remain present"
        status: fail
    human_judgment: false

status: halted
completed: 2026-09-06
---

# Phase 19.1 Plan 18: Checkout day-click repair and real-run reading Summary

**The checkout failure was repaired at its measured fake-clock cause and disappeared on the runner; the plan halts because the same real run exposed seven host-wizard failures and `gate-e2e` did not become green.**

## Performance

- **Duration:** ~75 min from first implementation commit through the measured CI result; metadata recovered later
- **Tasks executed:** 4/4
- **Task acceptance:** Tasks 1–3 pass; Task 4 fails its `gate-e2e=success` criterion
- **Files created:** 3, including this summary
- **Files modified:** 3 implementation/evidence files plus STATE and ROADMAP in the metadata close-out

## Accomplishments

- Reproduced the checkout failure in `mcr.microsoft.com/playwright:v1.60.0-noble` with the runner's shape and read the click's own trace. The click landed; the installed fake clock shifted venue-local calendar arithmetic, so the selected attribute could never land on the requested date.
- Moved clock installation to the checkout boundary. The whole four-case serial spec then passed on the first attempt in the container and locally, with the selected-day post-condition and its 2-second budget intact.
- Named all three cases released by the repair and recorded the deliberately divergent money-path copy of the day-selection walk without widening it.
- Proved the guard red in both useful directions. The empty-grid branch found and fixed a reporting defect; the unselectable-day branch still fails at the helper's own post-condition and message.
- Joined GitHub Actions run `34008844133` to the exact pushed SHA. The plan's own `hold-countdown` failure is absent, but the functional job reports seven failed host-wizard cases, two flakes, 52 skipped, 26 not run, and 374 passed.

## Task Commits

1. **Task 1: Reproduce and repair the day click** — `41b7153`
2. **Task 2: Account for released siblings and divergent copy** — `eea83ad`
3. **Task 3: Prove both guards and the four grid states** — `2d944c0`
4. **Task 4: Record the SHA-matched real-run verdict** — manual recovery metadata commit

The separate STATE/ROADMAP commit completes the five-commit plan record without changing application code.

## Files Created/Modified

- `e2e/helpers/booker-seed.ts` — positive empty-grid pre-condition and retained selected-day post-condition.
- `e2e/hold-countdown.spec.ts` — fake clock installed at the checkout boundary; measurement note at the former failing case.
- `deferred-items.md` — the money-path spec's uninstrumented copy is recorded with scope and disposition.
- `evidence/triage-day-click-container.txt` — container reproduction, call log, timings, released set, red guards, and grid-state proof.
- `evidence/ci-run-after-18.txt` — exact pushed SHA, run identity, five job conclusions, functional summary, owned-spec verdicts, and handoff.

## Decisions Made

- No retry: `DETACHED=no` is the explicit condition that rules it out.
- No timeout increase: the measured intervals leave ample headroom and a wider wait cannot repair wrong calendar arithmetic.
- No quarantine or allowlist growth: the real run stays red and the remaining failures are handed to a measured follow-up.
- No claim that the seven host-wizard failures share one cause: route reachability is the first discriminator for the proposed repair plan.

## Deviations from Plan

### Recovery deviation: terminal evidence and SUMMARY were missing after the CI run

- **Found during:** safe resume
- **Issue:** all three implementation/test commits and the real CI run existed, but `ci-run-after-18.txt` and this SUMMARY had not been written.
- **Resolution:** reconstructed the terminal record from commit history and immutable GitHub Actions run `34008844133`, then preserved the failed acceptance criterion as `status: halted`.
- **Scope impact:** documentation/state only; no production or test code changed during recovery.

## Issues Encountered

- The functional suite remained red after this plan's owned checkout failure disappeared. Its seven failures all touch the host listing wizard or its edit-route reachability, but a shared cause remains a hypothesis until a follow-up begins with the route/module probe.
- `confirmation-decay.spec.ts` re-entered the flaky list, and `shell.spec.ts` also flaked. Neither changes the seven-failure verdict, but both are preserved in the evidence.

## User Setup Required

None.

## Next Phase Readiness

- Plan 19.1-18 is closed out as **HALTED**, not complete.
- Plan 19.1-19 still owns the known visual-baseline work, but Phase 19.1 cannot close while `gate-e2e` is red.
- A new measured follow-up (proposed as 19.1-20) must own the host-wizard route-resolution cluster before SC4 can be claimed.
- Plan 19.1-15 remains blocked while the repository stays private and the publication hold remains in force.

---
*Phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red*
*Completed: 2026-09-06 — halted on terminal acceptance*
