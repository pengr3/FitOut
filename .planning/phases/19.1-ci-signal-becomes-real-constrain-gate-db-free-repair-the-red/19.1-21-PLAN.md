---
phase: 19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red
plan: 21
type: execute
wave: 13
depends_on:
  - 19.1-20
files_modified:
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt
  - .planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-21-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - CI-01
gap_closure: true
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The approvable repair candidate is derived directly from a clean tracked `git rev-parse HEAD` after Plan 20 completes; its SHA is presented to the human without first writing that SHA into tracked content."
    - "Only the exact 40-character SHA named in `approve push <SHA>` is pushed to `refs/heads/dev`; any intervening commit or tracked change invalidates approval and repeats the checkpoint."
    - "The selected GitHub Actions run is a `push` run on `dev` whose `headSha` equals the approved SHA, and exactly gate-db-free, gate-db, gate-e2e, gate-visual, and gate-price-parity conclude success."
    - "Both repaired wizard walks and the verification-panel case report failed=0, flaky=0, not-run=0, while axe-320-1280, heading, keyboard-14-stop, one-tree, overflow-320, and route-reachability remain active."
    - "D-04 remains enforced: any visual red stops closure and returns to Plan 19 without baseline interpretation or regeneration here."
    - "Only after the approved SHA's run completes successfully are CI evidence, the Plan 21 summary, state, and roadmap written/committed; those later documentation commits are explicitly distinct from the tested repair SHA."
    - "Plan 20 has already advanced `completed_plans` 62 to 63; Plan 21 advances 63 to 64 exactly once and does not claim Phase 19.1 or SC5 complete."
  artifacts:
    - path: ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt"
      provides: "Approved/remote/run SHA identity, exact run metadata, five job conclusions, three owned-case censuses, six property groups, and D-04 disposition."
    - path: ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-21-SUMMARY.md"
      provides: "Plan 21 closeout that distinguishes the tested repair SHA from later evidence/bookkeeping commits."
    - path: ".planning/STATE.md"
      provides: "Exact 63-to-64 completed-plan transition after CI evidence exists."
    - path: ".planning/ROADMAP.md"
      provides: "Plan 21 execution status and SC4 evidence without lifting the D-08 publication hold or claiming SC5."
  key_links:
    - from: "clean Plan 20 HEAD"
      to: "human approval response"
      via: "The checkpoint prints fresh `git rev-parse HEAD`; no tracked SHA record is created before approval."
    - from: "approved SHA"
      to: "refs/heads/dev"
      via: "An explicit SHA refspec pushes only the approved commit after branch/clean-tree/head rechecks."
    - from: "GitHub push run"
      to: ".planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt"
      via: "Run selection binds event, branch, and head SHA before job conclusions/censuses are recorded."
---

<objective>
Approve, push, and close remote CI for the exact clean repair produced by Plan 20, then record evidence and bookkeeping only after that exact SHA's five jobs succeed.

Purpose: CI-01 and SC4 require remote proof, but approval must remain bound to one immutable repair commit. Separating the push boundary from Plan 20 removes the impossible self-referential SHA record and keeps external side effects behind a fresh human-action gate.

Output: one fresh exact-SHA approval, one exact-ref push, one SHA-bound five-job CI transcript, one Plan 21 summary, roadmap closeout for this gap plan, and one 63-to-64 completion increment.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-CONTEXT.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-UI-SPEC.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-19-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-20-SUMMARY.md
@.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt
</context>

<completed_evidence prerequisite="true">
Plan 20 has reproduced and causally repaired `host-heading-whole-space`, `overflow-edit-photos-court`, and `verification-panel-phone-pending`; run local lint, design, workflow, Vitest, focused E2E, six-property-group, and full chromium proof; committed all repair/code/causal evidence; created its summary; and advanced `completed_plans` from 62 to 63. Its standing evidence preserves default Turbopack, rejects Webpack, keeps `readHostRouteManifestState` and the four anonymous one-attempt 307 diagnostics, and never touches the user-owned localhost:3000 process.
</completed_evidence>

<assumption_delta>
No product external API integration is added: GitHub CLI/API is used only to transport the approved commit and read CI evidence. There are no product schema changes, migrations, dependencies, new jobs, new environment variables, auth/ownership changes, UI/copy changes, or visual-baseline changes. Plan 21 changes no application or test source. After the CI-evidence commit, local `HEAD` may legitimately be ahead of `origin/dev`; `origin/dev..HEAD` emptiness is not a closure condition because the tested repair SHA remains identified separately.
</assumption_delta>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Derive and approve the exact clean repair SHA without tracking it</name>
  <files>None; checkpoint only.</files>
  <precondition>Plan 20 has a completed summary, `.planning/STATE.md` reads `completed_plans: 63`, current branch is `dev`, and the tracked tree is clean.</precondition>
  <read_first>
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-20-SUMMARY.md` — local proof, three causes/fix sites, six groups, no-push disposition, and 62-to-63 transition.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt` — six structured probe rows and local RED/GREEN evidence.
    - `e2e/host-headings.spec.ts` — final whole-space diff if changed.
    - `e2e/overflow-320.spec.ts` — final photos-step diff if changed.
    - `tests/host/verification-panel.test.tsx` — final verification diff if changed.
    - `src/components/host/verification-panel.tsx` — read only if Plan 20 changed it.
    - `src/app/(host)/host/listings/[id]/edit/wizard.tsx` — read only if Plan 20 changed it.
    - `src/app/actions/listing.ts` — read only if Plan 20 changed it.
  </read_first>
  <action>
Run `git branch --show-current`, `git status --porcelain=v1 --untracked-files=no`, and `git rev-parse HEAD` in that order; stop immediately if a command fails, the branch is not `dev`, the tracked status is non-empty, or the SHA is not 40 lowercase hexadecimal characters. Treat the fresh stdout value from `git rev-parse HEAD` as the candidate; do not write it into any tracked file, create/amend a commit, or mutate state/roadmap/evidence before approval.

Present the full candidate SHA, clean status, Plan 20 changed paths, the three one-to-one `CAUSE_JSON=`/`FIX_SITE_JSON=` pairs, six structured probe pairs, local green commands, three-case census, six-group activity, standing route diagnostics, default-Turbopack/explicit-Webpack-rejection evidence, D-04 disposition, and confirmation of no config/auth/UI/baseline/retry/timeout/skip/fixme/quarantine/allowlist change. Ask for exact response `approve push <candidate-SHA>`. Approval of 809f948, 9f6bb87, an earlier Plan 20 commit, this plan, or earlier discussion does not authorize the fresh candidate. Any subsequent commit or tracked change invalidates approval and repeats this checkpoint.
  </action>
  <instructions>
Review the displayed exact clean SHA, changed-path list, causal evidence, local gate results, owned-case census, six property groups, route diagnostics, and prohibited-change audit. If and only if that exact candidate is authorized for the external push, reply `approve push <candidate-SHA>` with the displayed 40-character SHA substituted verbatim. Do not run the push yourself.
  </instructions>
  <verification>The executor compares the exact approval text with a fresh current `HEAD`, branch, and tracked status immediately before Task 2; any mismatch invalidates approval.</verification>
  <resume-signal>Reply `approve push <candidate-SHA>` with the displayed full SHA, or describe the rejected evidence/change.</resume-signal>
  <verify>
    <automated>node -e "const{execFileSync}=require('child_process');const branch=execFileSync('git',['branch','--show-current'],{encoding:'utf8'}).trim();if(branch!=='dev')throw Error('branch must be dev');const dirty=execFileSync('git',['status','--porcelain=v1','--untracked-files=no'],{encoding:'utf8'}).trim();if(dirty)throw Error('tracked tree is dirty');const s=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();if(!/^[0-9a-f]{40}$/.test(s))throw Error('invalid SHA');console.log('candidate for human approval '+s)"</automated>
  </verify>
  <fails_when>Any command fails without an immediate stop, the human has not approved the displayed full SHA verbatim, branch/status/HEAD changes, the SHA is written into tracked content before approval, evidence omits a case/group, or the reviewed diff contains a prohibited change.</fails_when>
  <acceptance_criteria>
    - Candidate SHA comes directly from fresh clean `HEAD`, not from tracked evidence.
    - Approval text names that exact current 40-character SHA.
    - Any later commit/change explicitly invalidates approval and repeats the checkpoint.
  </acceptance_criteria>
  <done>The human explicitly authorizes pushing exactly the unchanged clean repair SHA, with no tracked self-reference.</done>
</task>

<task type="auto">
  <name>Task 2: Push only the approved SHA and capture its five-job CI result</name>
  <files>.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt</files>
  <precondition>Task 1 contains explicit `approve push &lt;SHA&gt;` for the current unchanged clean `dev` HEAD.</precondition>
  <read_first>
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/triage-plan-20-owned-failures.txt` — approved local causal proof and six structured probe rows.
    - `.github/workflows/ci.yml` — exact five job names, push trigger, and commands.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-19-SUMMARY.md` — D-04 ownership and visual closure.
    - `e2e/host-route-reachability.spec.ts` — standing route diagnostic and `readHostRouteManifestState`.
  </read_first>
  <action>
Parse the approved 40-character SHA from the exact Task 1 response into transient executor state. Immediately before push run `git rev-parse HEAD`, `git branch --show-current`, and `git status --porcelain=v1 --untracked-files=no`; stop immediately if any command fails, HEAD differs from the approved SHA, branch is not `dev`, or tracked status is non-empty. Push only that commit with `git push origin "<approved-SHA>:refs/heads/dev"`; stop immediately on a non-zero result. Read `git ls-remote origin refs/heads/dev` and stop unless the remote SHA equals the approved SHA.

Use `gh run list --workflow ci.yml --branch dev --event push --json databaseId,headSha,headBranch,event,status,conclusion,url` only to select a run whose event is `push`, branch is `dev`, and `headSha` equals the approved SHA. A bounded wait for that identity to appear may repeat the read-only listing, but no workflow rerun/dispatch is allowed. Run `gh run watch <RUN_ID> --exit-status`, then `gh run view <RUN_ID> --json databaseId,headSha,headBranch,event,status,conclusion,jobs,url`; every command or identity mismatch stops immediately. A non-success job stops closure. A `gate-visual` red returns to Plan 19 per D-04 with no interpretation, baseline change, or rerun. Never substitute another run/SHA or rerun to erase a red.

Only after the exact run completes, write `ci-run-after-21.txt` with `APPROVED_SHA`, `REMOTE_SHA`, `RUN_HEAD_SHA`, run ID/URL/event/branch, and conclusions for exactly gate-db-free, gate-db, gate-e2e, gate-visual, and gate-price-parity. Inspect the matched run's logs/artifacts and record `PROPERTY-GROUP=` rows for axe-320-1280, heading, keyboard-14-stop, one-tree, overflow-320, and route-reachability; `OWNED-CASE=` rows for both E2E walks and the DB case; explicit E2E/DB failed/flaky/not-run zeros; the standing route-helper result; and D-04 disposition. Redact cookies, credentials, phones, database URLs, and secret headers. Run the same content/SHA checks expressed in `<verify>` against the uncommitted artifact, stop immediately on failure, then commit the validated run evidence; the `<verify>` gate additionally proves the committed tracked artifact. This evidence commit is intentionally later than the tested repair SHA, is not pushed in this plan, and must not be checked with an `origin/dev..HEAD` emptiness gate.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),{execFileSync}=require('child_process');const p='.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt';const s=fs.readFileSync(p,'utf8');for(const x of ['EVENT=push','BRANCH=dev','JOB=gate-db-free CONCLUSION=success','JOB=gate-db CONCLUSION=success','JOB=gate-e2e CONCLUSION=success','JOB=gate-visual CONCLUSION=success','JOB=gate-price-parity CONCLUSION=success','PROPERTY-GROUP=axe-320-1280','PROPERTY-GROUP=heading','PROPERTY-GROUP=keyboard-14-stop','PROPERTY-GROUP=one-tree','PROPERTY-GROUP=overflow-320','PROPERTY-GROUP=route-reachability','OWNED-CASE=host-heading-whole-space','OWNED-CASE=overflow-edit-photos-court','OWNED-CASE=verification-panel-phone-pending','E2E_OWNED_FAILED=0','E2E_OWNED_FLAKY=0','E2E_OWNED_NOT_RUN=0','DB_OWNED_FAILED=0','DB_OWNED_FLAKY=0','DB_OWNED_NOT_RUN=0','ROUTE_HELPER=readHostRouteManifestState','D-04=preserved'])if(!s.includes(x))throw Error('missing '+x);const a=s.match(/APPROVED_SHA=([0-9a-f]{40})/),b=s.match(/REMOTE_SHA=([0-9a-f]{40})/),c=s.match(/RUN_HEAD_SHA=([0-9a-f]{40})/);if(!a||!b||!c||a[1]!==b[1]||a[1]!==c[1])throw Error('SHA mismatch');execFileSync('git',['ls-files','--error-unmatch',p],{stdio:'ignore'})"</automated>
  </verify>
  <fails_when>Any command fails without an immediate stop, current/approved/remote/run identity differs, the matched run is not push/dev, a job is non-success, an owned case is failed/flaky/not-run, a property group is inactive, visual output is interpreted here, evidence is written before run completion, or a substitute/rerun is used.</fails_when>
  <acceptance_criteria>
    - Approved SHA, remote dev SHA, and push-run head SHA are identical.
    - Exactly the five named jobs succeed on that run with no substitute run/SHA.
    - Three owned cases report failed=0, flaky=0, not-run=0 and all six property groups are active.
    - D-04 and the route helper remain intact; evidence is committed only after the run completes.
  </acceptance_criteria>
  <done>The exact human-approved repair SHA has one identity-bound all-green CI transcript committed after the tested run.</done>
</task>

<task type="auto">
  <name>Task 3: Close Plan 21 bookkeeping without rewriting tested-SHA history</name>
  <files>.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-21-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <precondition>Task 2's committed evidence proves the approved SHA's exact push run completed with all five jobs successful, and `.planning/STATE.md` still reads `completed_plans: 63`.</precondition>
  <read_first>
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/evidence/ci-run-after-21.txt` — sole remote-run identity and conclusion source.
    - `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-20-SUMMARY.md` — 62-to-63 predecessor closeout and local proof.
    - `.planning/STATE.md` — over-bump incident history and current 63 precondition.
    - `.planning/ROADMAP.md` — Phase 19.1 plan list, SC4/SC5 status, and D-08 publication hold.
  </read_first>
  <action>
Create `19.1-21-SUMMARY.md` from the committed CI evidence, naming the tested/approved SHA, run ID/URL, five conclusions, three owned censuses, six property groups, standing route diagnostic, D-04 disposition, and changed files. State explicitly that the evidence/summary/state/roadmap commits came after the tested repair SHA and are not claimed to be CI-tested repair commits.

Update the Phase 19.1 roadmap plan list/status so Plan 20 and Plan 21 are represented in strict Waves 12 and 13 and Plan 21 is checked complete, while SC5, the public flip, ruleset enforcement, and Phase 19.1 completion remain open per D-05 through D-08. Advance `.planning/STATE.md` `completed_plans` from 63 to 64 exactly once using the normal closeout flow. For every state/roadmap command, stop immediately on non-zero; diff against a pre-mutation copy, then re-read `completed_plans` after the final state/roadmap mutation. If the established verb over-bumps, make one scoped metadata correction back to 64 and record the incident in the summary. Commit summary/state/roadmap together. Require a clean tracked tree after that commit, but do not require `origin/dev..HEAD` to be empty and do not push these later documentation commits as part of the tested repair run.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),{execFileSync}=require('child_process');const st=fs.readFileSync('.planning/STATE.md','utf8');if(!/^\s*completed_plans:\s*64\s*$/m.test(st))throw Error('completed_plans must be 64');const rp=fs.readFileSync('.planning/ROADMAP.md','utf8');if(!/\[x\]\s+19\.1-21-PLAN\.md/.test(rp))throw Error('Plan 21 not checked complete');const sp='.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-21-SUMMARY.md';const s=fs.readFileSync(sp,'utf8');for(const x of ['completed_plans: 63 -> 64','gate-db-free','gate-db','gate-e2e','gate-visual','gate-price-parity'])if(!s.includes(x))throw Error('summary missing '+x);const d=execFileSync('git',['status','--porcelain=v1','--untracked-files=no'],{encoding:'utf8'}).trim();if(d)throw Error('tracked tree is dirty')"</automated>
  </verify>
  <fails_when>Any bookkeeping command fails without an immediate stop, CI evidence is not already committed, summary conflates later docs with the tested SHA, completed_plans is not exactly 64, roadmap claims SC5/public/phase completion, an `origin/dev..HEAD` emptiness condition is imposed, tracked changes remain after commit, or another push occurs.</fails_when>
  <acceptance_criteria>
    - Summary distinguishes the approved/tested repair SHA from later evidence/bookkeeping commits.
    - Roadmap records Plan 21 complete in Wave 13 but keeps SC5/publication/phase completion open.
    - `completed_plans` moves exactly 63 to 64 after all final mutations and is re-read from disk.
    - Closeout ends tracked-clean without asserting local HEAD equals remote/tested repair SHA.
  </acceptance_criteria>
  <done>Plan 21 is closed with exact-SHA CI evidence and one 63-to-64 increment, while later documentation commits remain honestly distinct from the tested repair SHA.</done>
</task>

</tasks>

<threat_model>
Security enforcement: OWASP ASVS Level 1. Any unresolved high-severity threat blocks closure.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Clean local repair to human approval | A local Git identity becomes externally authorized without a tracked self-reference. |
| Approved commit to GitHub push | The exact reviewed SHA crosses to `refs/heads/dev`; any intervening mutation must invalidate approval. |
| GitHub output to tracked CI evidence | Remote metadata/logs are untrusted until event, branch, run ID, and head SHA are bound. |
| Tested repair to later documentation commits | Post-run evidence/bookkeeping must not be represented as part of the tested repair SHA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19.1-21-01 | Spoofing | Approval/push/run identity | high | mitigate | Derive candidate from clean HEAD, require exact human response, recheck immediately, push explicit SHA refspec, and bind remote/run SHAs. |
| T-19.1-21-02 | Tampering | CI result selection | high | mitigate | Select only push/dev/exact-head run; forbid substitutes, reruns, suppression, baseline changes, and partial job success. |
| T-19.1-21-03 | Repudiation | CI evidence and closeout | medium | mitigate | Record run ID/URL/event/branch, five conclusions, three censuses, six groups, D-04, and explicit tested-vs-later commit distinction. |
| T-19.1-21-04 | Information Disclosure | GitHub logs/evidence | medium | mitigate | Redact cookies, credentials, phones, database URLs, and secret headers before commit. |
| T-19.1-21-05 | Denial of Service | Long CI watch | low | accept | Watch the single approved push run; bounded identity listing is read-only and no rerun/dispatch is allowed. |
| T-19.1-21-SC | Tampering | Package surface | high | mitigate | No package install or dependency change is allowed. |
</threat_model>

<verification>
- Plan 21 depends only on 19.1-20 and is Wave 13; Plan 20 remains Wave 12 dependent on 19.1-19.
- Task 1 derives the candidate SHA directly from clean Git state and writes no tracked SHA before exact human approval.
- Task 2 rechecks approval/head/branch/clean state immediately, pushes only the approved refspec, and binds approved/remote/run identities.
- All five named jobs succeed; three owned cases have zero failed/flaky/not-run; all six property groups and standing route diagnostics are present.
- Any visual red stops and returns to Plan 19 per D-04; no baseline interpretation or repair happens here.
- Only after the run completes are evidence, summary, state, and roadmap committed; later local commits need not equal or be pushed as the tested repair SHA.
- Plan 20 accounts for 62-to-63; Plan 21 accounts for 63-to-64 exactly, with SC5 and Phase 19.1 still open.
</verification>

<success_criteria>
1. A human approves the fresh clean `HEAD` SHA verbatim without that SHA first appearing in tracked content.
2. Current, approved, remote-dev, and push-run identities match at the push boundary.
3. gate-db-free, gate-db, gate-e2e, gate-visual, and gate-price-parity all conclude success on that exact run.
4. Three owned cases report failed=0, flaky=0, not-run=0 and six independent property groups remain active.
5. No suppression, retry/timeout increase, allowlist growth, Webpack/config change, auth/ownership weakening, schema/API change, UI/copy drift, baseline change, or localhost:3000 interaction occurs.
6. Evidence/bookkeeping is written only after CI completion, `completed_plans` ends at 64, and no false remote-HEAD equality is required after closeout.
</success_criteria>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | CI run means something; SC4 closes on exact-SHA all-green evidence | Tasks 1-3 |
| REQ | CI-01 functional Playwright specs are enforced | Task 2 binds gate-e2e and the six groups to the approved SHA |
| RESEARCH | Runner-order/local proof requires real push-run confirmation | Task 2 reads the exact push run, never recency/substitute evidence |
| CONTEXT | D-01 triage first and repair real causes | Satisfied by prerequisite Plan 20; Tasks 1-2 preserve its causal evidence |
| CONTEXT | D-02 all real bugs stay in phase | All three Plan 20 cases remain required in Task 2's remote census |
| CONTEXT | D-03 guarded allowlist | Tasks 1-2 prohibit growth/suppression and preserve assertions |
| CONTEXT | D-04 visual is SC4 and regressions return to Plan 19 | Tasks 2-3 block closure on visual red without interpreting it |
| CONTEXT | D-05 through D-08 branch/public decisions | Task 3 keeps SC5/publication open; GitHub use is CI transport only |
| CONTEXT | Deferred sharding/unrelated product work | Excluded |
| UI-SPEC | R1-R4, E1-E3, frozen wizard/UI/copy, six groups | Task 2 requires standing route diagnostics and all six groups; Plan 21 edits no UI/test source |
</source_coverage_audit>

<output>
After Task 3, the committed `.planning/phases/19.1-ci-signal-becomes-real-constrain-gate-db-free-repair-the-red/19.1-21-SUMMARY.md` is the closeout artifact. It must name the approved/tested SHA separately from later evidence/bookkeeping commits, the exact run and five conclusions, three owned censuses, six property groups, D-04 disposition, changed files, exact 63-to-64 transition, and the still-open SC5/publication state.
</output>
