---
phase: 17-cross-cutting-audit-themes-responsive-a11y-baselines
plan: 07
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - e2e/axe-sweep.spec.ts
  - src/lib/design/contrast-pairs.ts
  - tests/design/pair-drift.test.ts
  - e2e/helpers/theme.ts
  - src/components/theme/theme-provider.tsx
autonomous: true
requirements: [GATE-02]

must_haves:
  truths:
    - "The axe sweep runs in court only, at the five conformance tags, at 320 and 1280, over every surface in the route table, and reports zero violations (AC#16)"
    - "No disableRules anywhere; every .exclude() carries a committed reason and global-error is the only route-level exclusion (AC#17)"
    - "Each axe run asserts non-zero scanned nodes AND non-zero passed rules before asserting an empty violation list (AC#18)"
    - "The axe table's row set equals the declared surface set, so a surface cannot be silently absent (AC#2)"
    - "The four stale 'two-theme axe pass' comments — six sentences across four files — say court-only with the D-138 limitation named (AC#24)"
  artifacts:
    - path: "e2e/axe-sweep.spec.ts"
      provides: "The GATE-02 conformance sweep, table-driven, rows derived from the shipped inventories"
      min_lines: 150
      contains: "expectAxeClean"
    - path: "e2e/helpers/theme.ts"
      provides: "Three amended two-theme sentences at :3, :7 and :28"
      contains: "court"
    - path: "src/lib/design/contrast-pairs.ts"
      provides: "The amended NOT-COVERED entry at ~:27"
  key_links:
    - from: "e2e/axe-sweep.spec.ts"
      to: "e2e/helpers/axe.ts"
      via: "the single shared axe configuration and its vacuity guard"
      pattern: "from \"./helpers/axe\""
    - from: "e2e/axe-sweep.spec.ts"
      to: "src/lib/design/visual-baselines.ts"
      via: "VISUAL_SURFACES rows supply each route's url + hook + hookWhy as the sweep's tell"
      pattern: "VISUAL_SURFACES"
---

<objective>
Build GATE-02's automated accessibility sweep, run it, triage what it finds against the UI-SPEC's two
remediation tables, and amend the four shipped comments that still promise a two-theme pass D-138
retired.

Purpose: `package.json` contained no `axe` at any version, so GATE-02's *"an automated axe pass"* had
no instrument at all. It now has one (plan 17-01). This plan is where it gets a route table, a first
run, and a verdict — and where the phase's three riskiest assumptions get falsified rather than
assumed: that excluding the `nextjs-portal` host also removes its **open** shadow subtree from axe's
scope (A1), that `heading-order` can be enabled alongside a tag filter (A4), and the predicted list of
rules likely to fire (A5, explicitly a prediction and not a measurement).

Output: one table-driven spec, one triage record, six amended sentences.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-CONTEXT.md
@.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md

<!-- ⚠ `.planning/STATE.md` is 499KB. Never read it whole — Grep for a phrase, then Read with
     offset/limit around the hit. -->

<interfaces>
From `e2e/helpers/axe.ts` (shipped by plan 17-01) — import, never re-derive:
  export const AXE_TAGS = ["wcag2a","wcag2aa","wcag21a","wcag21aa","wcag22aa"] as const;
  export function makeAxe(page: Page): AxeBuilder;              // ONE .options(), .exclude("nextjs-portal")
  export async function expectAxeClean(page: Page, where: string): Promise<void>;

From `src/lib/design/visual-baselines.ts` — the reachability source (RESEARCH Pattern 4):
  export type SurfaceRow = {
    readonly kind: SurfaceKind;
    readonly url: string | null;
    readonly hook: string;      // a CSS selector that MUST match before any measurement
    readonly hookWhy: string;   // why that selector proves the surface rendered ITS OWN subject
    readonly blocked: string | null;
  };
  export const VISUAL_SURFACES: readonly SurfaceRow[]   // 44 rows, each with url + hook + hookWhy
  export const THEME_SWAP_EXCLUSIONS                    // length 1: global-error, with its reason

From `e2e/overflow-320.spec.ts` — the row shape, the tell guard and the named-skip idiom to copy:
  :296  type RouteRow = { name; path; skip?; tell; served?; open?; scope? }
  :695  async function expectReachable(page, row, where)
  :719  test.skip(title, () => { throw new Error(`unreachable: ${row.skip}`) })   // NAMED, never silent

From `e2e/helpers/theme.ts`:
  export async function seedTheme(context, theme)   // on the CONTEXT, before the first goto (D-06)

The four stale two-theme sentences — ALL VERIFIED PRESENT, six sentences in four files:
  src/lib/design/contrast-pairs.ts        ~:27            1 sentence  (inside a NOT COVERED list)
  tests/design/pair-drift.test.ts         ~:69            1 sentence  (inside a NOT COVERED list)
  e2e/helpers/theme.ts                    :3, :7, :28     3 sentences
  src/components/theme/theme-provider.tsx :20-21          1 sentence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/axe-sweep.spec.ts — the table-driven court sweep at 320 and 1280</name>
  <files>e2e/axe-sweep.spec.ts</files>
  <read_first>
    - `e2e/overflow-320.spec.ts` :296-340 (the `RouteRow` type — every field carries prose, `skip` is mandatory whenever `path` is null), :372-430 (worked rows), :663-686 (four worked `skip` strings — a skip is a paragraph, not a phrase), :689-707 (`expectReachable` and its 15s measured timeout), :709-776 (the loop, the named skip that throws its reason into the run's output, the `document.fonts.ready` wait, the `row.open` ordering)
    - `src/lib/design/visual-baselines.ts` :94-121 (the `SurfaceRow` type and the Trap-1 argument behind `hook`) and the 44 `VISUAL_SURFACES` rows with their `url`/`hook`/`hookWhy`
    - `e2e/helpers/theme.ts` :38-71 — `THEME_STORAGE_KEY` and `seedTheme`, and why seeding happens on the context before the first `goto`
    - `e2e/helpers/axe.ts` — the helper plan 17-01 shipped
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-RESEARCH.md` § Open Questions 1 (a new file that IMPORTS its rows rather than retyping them) and 2 (enable `heading-order` explicitly AND land the bespoke walk — they cover different route sets), § Common Pitfalls 1-4, and § GATE-02 — Measured Baseline (the ranked prediction of rules likely to fire, tagged `[ASSUMED — A5]`)
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-PATTERNS.md` § `e2e/axe-sweep.spec.ts` — the five patterns assigned to this file
  </read_first>
  <action>
    Create a NEW spec rather than a fourth table inside `overflow-320.spec.ts` — that file is already
    2,092 lines and its AC#29 half is deliberately seed-free, and mixing an axe pass into it would couple
    the cheapest gate in the suite to the flakiest fixtures.

    **Derive the rows; do not retype them.** Build the table from `VISUAL_SURFACES`' `url` + `hook` +
    `hookWhy` (the `hook` selectors are exactly the "tell" this sweep needs) and from the route rows
    `overflow-320.spec.ts` already declares. A surface whose hook changes then moves in one place.

    **Assert the table's row set equals the declared surface set.** This assertion is what makes AC#2's
    "zero silent absences" mechanical rather than a promise: compute the declared production surface set
    (the D-201 enumeration — `src/app/**/page.tsx` minus `src/app/dev/**`, plus the route states
    `tests/design/loading-coverage.test.ts` pins) and assert every member appears in this table either
    with a measurement or with a named `skip` reason. The four D-201 exclusions each appear IN the table
    with their reason recorded there, not as absences: `src/app/dev/**` (audit instruments, `NODE_ENV`-gated
    out of production, not audit subjects), email templates (not browser documents; owned by the
    `email-shell` / `email-tokens` suites), OG-image routes (not interactive documents; owned by
    `og-routes.test.ts`), and `global-error` (the one declared route-level exclusion).

    **`global-error`'s exclusion reason must be corrected, not copied.** The UI-SPEC justifies it by
    axe's `region` rule, but under the declared conformance tags `region` is `best-practice`-tagged and
    never fires, so that justification is moot. The correct reason is: it renders its own document,
    cannot be themed, and is already `THEME_SWAP_EXCLUSIONS`' only entry. Write that reason into the
    row and note in a comment that it supersedes the UI-SPEC's sentence.

    **The loop** is `overflow-320.spec.ts`'s AC#29 loop with three changes: `THEMES` collapsed to
    `["court"]` (D-138 — grove is a probe, never a product theme, so a grove axe pass would audit a
    theme no user can reach); widths `[320, 1280]` (a responsive layout is two different accessibility
    trees, and a desktop-only sweep audits half the product); and `expectNoOverflow` replaced by
    `expectAxeClean`. Keep everything else: `seedTheme` on the context before the first `goto`,
    `document.fonts.ready`, `row.open` before `expectReachable` where the tell is what the interaction
    produces, and `expectReachable` before ANY measurement. Configure a describe-level timeout above the
    default the way AC#29 does (60s), and say why.

    Include each surface's designed loading / empty / error state where a tell can reach it — a skeleton
    has a different accessibility tree from the content it stands in for. Where no tell can reach a
    state, that is a named skip row, not an omission.

    Never call `disableRules` (AC#17). Never add a second `.options()` call or a `.withTags()` — all
    configuration lives in `makeAxe`, in one file, for `expectRing`'s reason.
  </action>
  <acceptance_criteria>
    - `e2e/axe-sweep.spec.ts` exists, imports `expectAxeClean` from `./helpers/axe`, and imports `VISUAL_SURFACES` from `../src/lib/design/visual-baselines`
    - `grep -c 'disableRules\|withTags\|\.options(' e2e/axe-sweep.spec.ts` returns `0`
    - The spec runs court only: `grep -c '"grove"' e2e/axe-sweep.spec.ts` returns `0`
    - Both widths present: the file contains `320` and `1280` as viewport widths
    - The row-set equality assertion exists — the file contains an assertion comparing the table's route names against a derived declared-surface list, and its failure message contains the word `absent`
    - Every row whose path is null has a `skip` string longer than 80 characters: `grep -v '^\s*//' e2e/axe-sweep.spec.ts | grep -c 'skip:'` matches the number of null-path rows
    - The `global-error` row's reason contains `own document` and does NOT justify itself with `region`
    - `expectReachable` (or its equivalent tell guard) appears before every `expectAxeClean` call in the loop body
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run db:up && npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1 --reporter=list</automated>
  </verify>
  <done>A table-driven court-only axe sweep exists whose row set provably equals the declared surface set, whose every row waits on its own tell, and whose configuration cannot diverge from the one shared helper.</done>
</task>

<task type="auto">
  <name>Task 2: First run — falsify A1 and A4, red-watch the vacuity guard, and triage every violation</name>
  <files>e2e/axe-sweep.spec.ts</files>
  <read_first>
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-RESEARCH.md` § Assumptions Log — **A1** (excluding the `nextjs-portal` host also removes its open shadow subtree), **A4** (`heading-order` can be enabled alongside a tag filter), **A5** (the predicted rule list is a prediction, not a measurement)
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md` § Color → the arbitration rule and its three-step procedure (a declared pair wins; an undeclared rendered pairing is a real finding; a token value moving is an escalation), and § Remediation rules → both tables in full
    - `src/lib/design/contrast-pairs.ts` — `CONTRAST_PAIRS` and `EXCLUDED_PAIRS`, and the row shape a measured addition must follow
    - `tests/design/contrast.test.ts` — the colour authority (culori, OKLab→sRGB, 8-bit rounding, gamma-space alpha compositing, `AA_EPSILON = 0.05`); axe uses different rounding and no epsilon, so a 4.51-vs-4.50 disagreement on a DECLARED row is not a finding
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-CONTEXT.md` § D-199 and § D-200 — batch findings; interrupt only for a GATE-06 scope alarm or an unreachable acceptance criterion
  </read_first>
  <action>
    Run the sweep and treat the first run as three measurements, not one verdict.

    **Falsify A1.** Inspect the violation output for any `target` selector beginning `nextjs-portal`. If
    any appear, the host-level `.exclude()` did not remove the open shadow subtree, and the fallback is a
    shadow-piercing exclusion selector or a production-build run — not silently accepting the noise.
    Record what was observed either way, in the source beside the exclusion, in `helpers/focus.ts`'s
    "the partition is ASSERTED, not trusted" spirit.

    **Falsify A4.** Confirm `heading-order` appears in `results.passes` on at least one surface. If it
    does not, the explicit `rules` enable did not survive the tag filter, heading coverage stays
    host-only, and that is a finding to record — plan 17-10's bespoke walk still lands, but the
    non-host surfaces then have no heading-order coverage and the phase must say so rather than imply
    otherwise.

    **Red-watch the vacuity guard** (RESEARCH Pattern 3): point one row temporarily at a URL that 404s
    and confirm the GUARD fires — `passes.length` or `scannedNodes` — and not the violation list. That
    distinction is the whole point: a page that failed to load produces an empty violation list and reads
    exactly like a pass. Restore the row and record the mutation, the command and the observed message in
    a comment.

    **Triage every violation against the two tables, one at a time.** May fix in place, here, now: a
    missing `aria-label` on an icon-only control (the visible action, never "icon"/"button"/a filename);
    `role="status"` + `aria-busy` + an `sr-only` label on a skeleton (it is the CONDITION that makes the
    1.09:1 fill exclusion legal); a heading level corrected to close a skipped step; a `data-testid` on a
    structural container the sweep must scope to; a measured row added to `contrast-pairs.ts` for a
    rendered pairing the inventory does not declare. Must escalate, recorded and NOT fixed: any token
    value moving, an 11th `ACCENT_USES` entry, a fourth card pattern or fifth type role or new spacing
    value, any product copy change, any booking/payment/capacity/availability logic change or
    computation moved client-side (D-130), a schema migration, removing a control or disclosure or state
    to make a measurement pass, or reversing a recorded decision.

    For `color-contrast` specifically, follow § Color's procedure in order: if the pair is in
    `CONTRAST_PAIRS` or `EXCLUDED_PAIRS`, `contrast.test.ts` wins and it is NOT a finding (record the
    disagreement as data if it is within 0.1 of the bar); if it is a genuinely undeclared rendered
    pairing, add the MEASURED row to `contrast-pairs.ts` with its reason — never fix it by editing a
    colour at the call site; if the remediation needs a token value to move, escalate.

    Batch the escalate-class findings for plan 17-13 rather than interrupting (D-199). Interrupt
    immediately only for the two named exceptions: a fix that appears to need a schema migration
    (GATE-06 scope alarm), or a finding that makes an acceptance criterion unreachable.

    Record every mechanical fix taken, with its rule id and its surface, in the plan SUMMARY, and note
    that A5's predicted rule list is replaced by this measurement.
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1` reports zero violations, OR every remaining violation is enumerated in the SUMMARY with its rule id, surface, width and disposition against the two remediation tables
    - A1 is recorded: the spec contains a comment stating whether any violation target began `nextjs-portal` on the first run, and what was done
    - A4 is recorded: the SUMMARY states whether `heading-order` appeared in `results.passes`, naming a surface
    - Red-watch evidence: a comment in `e2e/axe-sweep.spec.ts` names the 404 mutation, the command, and which assertion fired (the guard, not the violation list)
    - No violation was suppressed: `grep -c 'disableRules' e2e/axe-sweep.spec.ts` returns `0`, and the count of `.exclude(` calls in the sweep is unchanged apart from the declared `global-error` route exclusion
    - Any `contrast-pairs.ts` addition carries a measured ratio and a reason; `npx vitest run --config vitest.design.config.ts tests/design/contrast.test.ts` exits 0
    - `git status --porcelain drizzle/` prints nothing — no fix reached for a migration
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run db:up && npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1 --reporter=list && npm run build</automated>
    <note>Runtime tradeoff (recorded): both halves are minutes — a seeded e2e sweep across two widths,
    then `npm run build` (95s test:design + lint + next build). Kept, and NOT lightened: this task's
    triage step may edit product source (an `aria-label`, a heading level, a skeleton's `role="status"`, a
    `data-testid`, a measured row in `contrast-pairs.ts`), so `next build` and the contrast/leak gates
    inside `test:design` are the only things that prove those edits did not break something else. Fast
    inner-loop signal while triaging a single violation: re-run the sweep filtered to the one row
    (`--grep`) before re-running the whole chain.</note>
  </verify>
  <done>The sweep has a real verdict, the three assumptions are measurements rather than assumptions, the vacuity guard was observed firing on an empty page, and every violation is either mechanically fixed or recorded with its disposition.</done>
</task>

<task type="auto">
  <name>Task 3: AC#24 — amend the six stale "two-theme axe pass" sentences to court-only, with the limitation named</name>
  <files>src/lib/design/contrast-pairs.ts, tests/design/pair-drift.test.ts, e2e/helpers/theme.ts, src/components/theme/theme-provider.tsx</files>
  <read_first>
    - `e2e/helpers/theme.ts` :1-40 — all three two-theme sentences (:3, :7, :28) AND the amendment idiom at :17-28, which is this tree's worked example of rewriting a stale paragraph in place with the history preserved rather than deleting it
    - `src/lib/design/contrast-pairs.ts` around :20-35 — the sentence at ~:27 and the "NOT COVERED — real blind spots" list it sits inside
    - `tests/design/pair-drift.test.ts` around :60-80 — the sentence at ~:69 and its surrounding NOT-COVERED list, plus the docblock stating this file does not cover cross-element pairings
    - `src/components/theme/theme-provider.tsx` :15-30 — the sentence at :20-21
    - `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-UI-SPEC.md` § Color → "The court-only narrowing, and what it costs", which is the exact content each amended sentence must carry
  </read_first>
  <action>
    Amend six sentences across four files. Note the count: `e2e/helpers/theme.ts` has **three**
    two-theme sentences (:3, :7, :28), not one — the UI-SPEC's "four sites" is four files.

    Use the amendment idiom already worked in `e2e/helpers/theme.ts:17-28`: rewrite in place with the
    history preserved, never delete. Each amended passage states what the sentence used to promise, that
    D-138 (2026-08-23) postdates it and makes the pass court-only, and the honest consequence — in
    `court` the cross-element residue is fully covered by the axe sweep on the rendered DOM; in `grove`
    it is covered only by the four-surface theme-swap probe (`search-results`, `auth-login`, `terms`,
    `root-not-found`) plus the 24-name key-set parity check and the two-theme declared-pair table, so a
    cross-element pairing that is legal in court and sub-bar in grove, on a surface outside those four,
    **is not detected**. Say that this is acceptable and is the point of D-138 — grove is a probe, not a
    shippable theme, and no user can reach it — and that it becomes a real gap the day grove or any
    second theme becomes shippable, which is a DSFUT-02 (dark mode) concern.

    `contrast-pairs.ts`'s and `pair-drift.test.ts`'s sentences sit inside **"NOT COVERED — real blind
    spots"** lists. The amended text stays in those lists. Do not promote it to a promise — the point of
    those lists is that they enumerate what is not covered, and moving a half-covered item out of them
    is the same defect in the other direction.

    Change no behaviour in any of the four files: no assertion, no export, no runtime value. This task
    edits comments only.
  </action>
  <acceptance_criteria>
    - `grep -rc 'two-theme axe pass\|TWO-THEME AXE PASS' src/lib/design/contrast-pairs.ts tests/design/pair-drift.test.ts e2e/helpers/theme.ts src/components/theme/theme-provider.tsx` returns `0` for each file, OR each surviving occurrence is inside a preserved-history paragraph explicitly marked as superseded
    - Each of the four files contains `D-138`
    - Each of the four files contains the word `court` in the amended passage
    - `e2e/helpers/theme.ts` was amended in three places: `git diff e2e/helpers/theme.ts` shows changes near lines 3, 7 and 28
    - `contrast-pairs.ts` and `pair-drift.test.ts` amendments are still inside their NOT-COVERED lists — the list heading still precedes the amended sentence in each file
    - Comments only: `git diff --stat` for these four files shows no change to any exported symbol; `npx tsc --noEmit` exits 0 and `npm run test:design` exits 0
    - `npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1` still green (a comment edit in `e2e/helpers/theme.ts` changed no behaviour)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:design && npx tsc --noEmit</automated>
    <note>Runtime tradeoff (recorded): `npm run test:design` is 95s measured. Kept because the amended
    sentences live in files the design suite reads (`contrast-pairs.ts`, `pair-drift.test.ts`, `theme.ts`,
    `theme-provider.tsx`) and a comment or docblock edit that accidentally moves a declared pair or a swap
    surface is only caught by the whole suite. Fast inner-loop signal: `npx vitest run --config
    vitest.design.config.ts tests/design/pair-drift.test.ts tests/design/contrast.test.ts` (seconds).</note>
  </verify>
  <done>All six sentences say court-only and name the grove limitation, the two NOT-COVERED lists keep their entries, the history is preserved rather than deleted, and no behaviour changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next dev-server chrome → audit scope | `<nextjs-portal>`'s shadow root is `mode:"open"` and axe walks open shadow trees by design, so the dev server's own furniture is inside the audit unless excluded |
| Axe exclusion mechanism → phase verdict | An `.exclude()` or a `disableRules` is the cheapest way to turn a red green, and both are invisible in a passing run |
| Rendered colour → declared inventory | Two instruments measure colour with different rounding; without a stated arbitration order the first disagreement becomes an argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-30 | Repudiation | `.exclude()` used to make a red green | mitigate | Exactly two exclusions: the component-level `nextjs-portal` in `helpers/axe.ts` with its committed reason, and the route-level `global-error` row with its corrected reason. `disableRules` asserted absent by grep. Every skip is a named `skip` string that throws its reason into the run's output. |
| T-17-31 | Repudiation | axe scan over an unresolved surface | mitigate | `expectReachable` on the row's own tell runs before every `expectAxeClean`; the vacuity guard (`passes.length > 0`, `scannedNodes > 0`) is red-watched against a URL that 404s, confirming the GUARD fires rather than the violation list. |
| T-17-32 | Repudiation | a surface silently absent from the table | mitigate | The row-set-equals-declared-surface-set assertion makes AC#2 mechanical; the four D-201 exclusions appear IN the table with their reasons rather than as absences. |
| T-17-33 | Tampering | product source reshaped to clear a violation | mitigate | Every violation triaged against the UI-SPEC's two remediation tables before any edit; escalate-class findings are recorded for 17-13, not absorbed. `git status --porcelain drizzle/` asserted empty (GATE-06 scope alarm is a D-199 immediate-escalation exception). |
| T-17-34 | Spoofing | axe `color-contrast` overruling the declared inventory | mitigate | § Color's arbitration order is followed explicitly: a declared pair wins (`contrast.test.ts` is the D-12 authority); an undeclared rendered pairing gets a MEASURED row in `contrast-pairs.ts`; a token value moving is escalated. Never a colour edited at a call site. |
| T-17-35 | Information disclosure | axe results attached to CI artifacts | accept | Results contain DOM selectors and rendered text from a locally seeded fixture DB on `localhost`; no production data, no secrets. The suite has no network egress beyond the dev server. |
</threat_model>

<verification>
- `npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1` reports zero violations, or every survivor is enumerated with its disposition
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `grep -rc 'disableRules' e2e/` returns 0
- `git status --porcelain drizzle/` prints nothing (GATE-06)
</verification>

<success_criteria>
- AC#16, AC#17, AC#18 and AC#2 hold, each mechanically rather than by review
- Assumptions A1 and A4 are replaced by measurements, and A5's prediction is replaced by the first run's actual rule set
- The vacuity guard was observed firing against a page that failed to load
- AC#24 holds: six sentences amended across four files, court-only with the grove limitation named, history preserved, no behaviour changed
- Zero schema migrations proposed or absorbed (AC#32)
</success_criteria>

<output>
Create `.planning/phases/17-cross-cutting-audit-themes-responsive-a11y-baselines/17-07-SUMMARY.md` when done
</output>
