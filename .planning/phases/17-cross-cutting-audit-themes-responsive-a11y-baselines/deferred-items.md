# Phase 17 — deferred items

Out-of-scope discoveries made while executing this phase's plans (D-199: findings are BATCHED — one
list at phase end). Nothing here was fixed by the plan that found it; each row names the plan that
found it, the measurement, the owner file, why it was left, and the cheapest correct fix.

| Found by | Item | Why deferred |
|---|---|---|
| 17-05 | **`e2e/avatar-crop.spec.ts`'s `pick()` helper is an unguarded strict-mode locator, and it flakes.** Measured 2026-08-29 on `chromium --workers=1`: `pick()` at `:159` is a bare `page.locator('input[type="file"]').setInputFiles(...)` with no count assertion. In one full-file run of three, `corrupt.jpg is refused before the dialog` (`:742`) failed with `strict mode violation: locator('input[type="file"]') resolved to 2 elements` — one inside `main`, one outside it. The same test passes **in isolation** (`-g "corrupt.jpg is refused"`, green) and the same full file passed on the run immediately before the D-197 edit and on the run immediately after, so the file is 33/33 green either side of it. This is the `[16-D2]` shared-fixture/contention class Pitfall 9 names, met at the one helper in this file with no count guard. | It is **not caused by the D-197 change** — a slider attribute cannot mint a second file input, the failing test never opens the crop dialog, and the failure reproduces on neither the pre- nor the post-change tree deterministically. It is also **not 17-05's file to restructure**: `avatar-crop.spec.ts` is in this plan's `<files>` only for the two zoom-state assertions, and the helper is used by ~30 cases across the file. The cheapest correct fix is one line — assert `await expect(page.locator('input[type="file"]')).toHaveCount(1)` inside `pick()` before `setInputFiles`, which converts a nondeterministic strict-mode error into a named failure that says *which* surface rendered a second input. Whichever plan next opens this file should take it; **17-13** is the likely owner. Recorded here so a later red on `:742` is read as this row rather than as a Phase-17 regression. |
| 17-06 | **D-196's `p-1.5` moves committed visual baselines, and no run on this machine can say which.** The padding widens `ProfileLink` by 12px (16→28 below `sm:`, and by the same 12px at every width above it — `p-1.5` carries no breakpoint), and `ProfileLink` renders in all three signed-in compositions (`(app)`, `(host)`, `public-header`). At least one **shootable** row renders that header: `visual-baselines.ts` says of `booking-not-found` — *"it renders no booking, no money, no date and no identity — an `EmptyState` inside the signed-in shell — so it is shot."* Twenty of the signed-in rows are `blocked` on the fixture re-point, so the exposed set is small, but it is not empty. | The `visual` Playwright project **is not constructed off Linux** (D-27/D-29), so this cannot be measured, regenerated or even enumerated on this machine — `playwright.config.ts` prints that refusal on every local run. Regenerating is also the wrong instinct: `13-16`'s row in the Phase-13 ledger records that *"a phase can COMPLETE with GATE-01 red, and nothing notices"*, and `15-11`'s records ten references re-minted without anyone reading the diff. The cheapest correct fix is a **comparison** dispatch (not a generation one) on this phase's head commit, and a read of the diff for any signed-in-shell surface: a 12px-wider Profile control is the expected delta and anything else is a finding. Owner: the phase verifier, or **17-13** if it is carried. |
| 17-07 | **`e2e/helpers/axe.ts`'s docblock states that axe's SC 2.5.8 target rule cannot run, and at axe-core 4.13.0 it does.** The constant's header says the rule is *"DISABLED BY DEFAULT in axe-core … and a tag filter does not enable a disabled rule. `wcag22aa` in this list therefore buys nothing on its own, and nothing here may be read as covering the 24px target floor."* Measured 2026-08-29 by a throwaway probe reading `results.passes` on three surfaces under the shipped configuration: the rule id is present on **all three** — `/terms` (22 passing rules), `/` (26), root not-found (13). A rule that is disabled does not appear in `passes` at all, so it ran and it passed. The scan is therefore covering slightly MORE than its own header claims — the benign direction, but a false statement in a gate's own docblock is the defect class this phase exists to repair (AC#24 is the same defect in a different file). | **The correction cannot be written in the file that needs it.** Plan 17-01's acceptance criterion for `e2e/helpers/axe.ts` is `grep -c 'withTags\|withRules\|disableRules\|target-size' e2e/helpers/axe.ts` returning `0`, so writing the rule id into that docblock breaks a committed gate; the file already records that it spells forbidden identifiers descriptively for exactly this reason. `helpers/axe.ts` is also outside 17-07's declared `files_modified`. The measurement is recorded in full in `e2e/axe-sweep.spec.ts`'s first-run block instead. **No policy changes and none should:** `expectTargets` (`overflow-320.spec.ts`, `TARGET_FLOOR_PX = 24`) stays the authority on the floor, because axe's rule honours SC 2.5.8's spacing and inline exceptions and reports no named offenders, and two definitions of one floor is the drift the one-import-site rule exists to stop. Cheapest correct fix: whichever plan next opens `helpers/axe.ts` rewrites claim 2 of the `AXE_TAGS` docblock to say the rule DOES run at 4.13.0 while `expectTargets` remains the authority — spelling the rule id descriptively, as that file already does for three other identifiers — and amends 17-01's grep in the same change. **17-13** is the likely owner. |
| 17-07 | **A dev-only hydration mismatch on the mobile nav drawer trigger, on every route that renders the signed-in shell.** Observed on the console of every host and booker row of the first GATE-02 sweep (2026-08-29, `next dev`): *"Hydration failed because the server rendered HTML didn't match the client"*, with React's diff pointing at `site-nav` → `NavDrawer` → `ResponsiveDialog` → `DialogTrigger`, and the client-only attribute in the `+` block being `aria-controls="radix-_R_ad5ritulb_"`. Radix mints the controlled element's id on the client; the server render has no `aria-controls` at all, so the two trees disagree on that one attribute and React regenerates the subtree. It is a WARNING, not a failure: all 48 measured rows scanned clean around it, and **axe reported nothing** — the regenerated tree carries a valid `aria-controls` by the time a scan reads it. | **Pre-existing and not this plan's**: no file 17-07 touches is in that subtree, and the warning reproduces on rows this plan did not change. It is also **not an accessibility finding by measurement** — the sweep's whole purpose is to say what the rendered tree looks like, and it looks correct. What it costs is a real but different thing: a subtree that regenerates on hydration is a subtree whose first paint is thrown away, and a console full of hydration noise is how a genuine mismatch later gets ignored. Cheapest correct fix: give the drawer's `Dialog` a stable `id` so Radix derives the same `aria-controls` on both sides, or render the trigger's `aria-controls` server-side. Owner file: `src/components/patterns/site-chrome.tsx` (the `NavDrawer` composition). Recorded rather than fixed because it is a shell change with a blast radius across every signed-in route, which is the class D-199 says to batch. |
| 17-09 | **`AvailabilityCalendar` forks on occupancy mode and only ONE branch carries the declared container id, so RESP-04 AC#12 cannot be ASKED of the drop-in calendar.** `src/components/availability/availability-calendar.tsx:551-582` (OPEN-02's Phase-9 fork) takes an early return for an `open_capacity` listing and renders a **fragment** wrapping `CollisionNotice` + `DatePassPicker`; `data-testid="availability-calendar"` sits only on the exclusive surface's root at `:619`. Measured 2026-08-29: the count is 1 on an `exclusive` listing at 320/768/1280 and **0** on a drop-in one — and a 0 is indistinguishable from a calendar that failed to render, which is why the row is a named skip rather than a second measurement. Found the expensive way: the calendar row originally drove *whichever listing is first in the catalogue* and went red once in three full runs, on the run that landed on a drop-in listing. | **The fix is not one attribute.** The fragment is deliberate — its own comment records choosing it over a wrapper div *"so the drop-in tree keeps its box exactly"* — so hanging the id there means introducing a wrapper element into a shipped surface, i.e. a `src/` change outside 17-09's declared `files_modified` that also reverses a recorded decision. It would additionally want a `SELECTOR_CONTRACT` review, since one id would then name two structurally different subtrees. Cheapest correct fix: whichever plan next opens the drop-in surface adds the wrapper **and** re-measures the box it was chosen to protect, then deletes the skipped row in `e2e/one-tree.spec.ts` and replaces it with a measurement. Until then RESP-04's AC#12 is closed for five of six families and the calendar family is closed for the exclusive surface only. |
| 17-09 | **The only navigation landmark on `/listings/[id]` is `react-day-picker`'s, named "Navigation bar".** Measured 2026-08-29 at 320, 768 and 1280: `getByRole("navigation")` resolves to exactly 1 on the listing detail route, and the element is `<nav class="rdp-nav" aria-label="Navigation bar">` — the month grid's prev/next control inside the availability calendar. The app's own contribution is 0, because the public composition passes no `nav` prop to `SiteChrome` (`site-chrome.tsx:205-209` renders the landmark only when there is navigation). So on the product's highest-intent public page, the one thing announced to a screen-reader user as *navigation* is a vendor's month stepper with a generic name. | **Not a duplication defect**, so it is not AC#13's failure: the count is 1 and the tree is valid. It is a naming/semantics finding, and the fix is inside a third-party component's markup — `rdp-nav` is react-day-picker's own element, reachable only through `src/components/ui/calendar.tsx`'s overrides. Cheapest correct fix: give the month nav a specific `aria-label` (e.g. *"Calendar months"*) through the existing `components`/`classNames` override seam, so the label names what it moves through; and consider whether a month stepper should be a landmark at all. Owner: whichever plan next opens `ui/calendar.tsx`. `e2e/one-tree.spec.ts` PINS the current state (`landmarks: 1`, `siteNavs: 0`, with the vendor named in the row's reason), so the day this changes the gate says so by name rather than silently. |
| 17-09 | **`e2e/one-tree.spec.ts` counts the booking sheet SHUT, never open.** `listings/[id]/(detail)/page.tsx` mounts `BookingPanel` twice — the rail placement, in the document at every width, and the sheet placement, inside a portal that is not in the document until the sticky bar is tapped. Measured: `booking-panel` is 1 on the resting page at 320/768/1280 and goes to 2 with the sheet open. | **By design, and RESP-02's sanctioned arrangement** (one component, two placements, one provider, one fetch), asserted by `e2e/mobile-booker-path.spec.ts`. AC#12's claim is about the resting document and is made there. Recorded so that a later reader who opens the sheet and counts 2 reads this row rather than filing a duplication defect. No fix owed unless RESP-02's arrangement changes. |
