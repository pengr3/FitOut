---
phase: 01-auth-accounts
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - drizzle.config.ts
  - docker-compose.yml
  - .env.local
  - .env.example
  - .gitignore
  - vitest.config.ts
  - playwright.config.ts
  - tests/setup.ts
  - tests/helpers/db.ts
  - tests/helpers/mocks.ts
  - src/lib/db/index.ts
  - components.json
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
user_setup:
  - service: docker
    why: "Local Postgres 18 (postgis/postgis:18) for dev + integration tests"
    dashboard_config:
      - task: "Ensure Docker Desktop is running before `docker compose up -d db`"
        location: "Docker Desktop (already installed, v29.4)"

must_haves:
  truths:
    - "A Next.js 16 App Router + TypeScript project builds (`npx next build` succeeds) without clobbering CLAUDE.md or .planning/"
    - "A local Postgres 18 (postgis/postgis:18) container starts and accepts connections on 5432"
    - "Drizzle is wired to the DB via postgres.js and drizzle.config.ts points at src/lib/db/schema.ts"
    - "Vitest runs (`npx vitest run` exits 0 on a trivial smoke test) and Playwright is installed"
    - "A test DB strategy exists so integration tests run migrations against an isolated Postgres before assertions"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency set (Next 16.2.7, better-auth 1.6.14, drizzle 0.45.2, zod 4.4, etc.)"
      contains: "better-auth"
    - path: "docker-compose.yml"
      provides: "Local postgis/postgis:18 Postgres"
      contains: "postgis/postgis:18"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config (schema path + DATABASE_URL)"
      contains: "./src/lib/db/schema.ts"
    - path: "src/lib/db/index.ts"
      provides: "Drizzle db instance over postgres.js"
      contains: "drizzle"
    - path: "vitest.config.ts"
      provides: "Vitest unit/integration config"
      contains: "test"
    - path: "playwright.config.ts"
      provides: "Playwright E2E config"
      contains: "defineConfig"
    - path: "tests/helpers/db.ts"
      provides: "Test DB setup/teardown + migrate-before-tests"
      contains: "migrate"
  key_links:
    - from: "drizzle.config.ts"
      to: "src/lib/db/schema.ts"
      via: "schema path"
      pattern: "src/lib/db/schema"
    - from: "src/lib/db/index.ts"
      to: "DATABASE_URL"
      via: "postgres(process.env.DATABASE_URL)"
      pattern: "DATABASE_URL"
---

<objective>
Stand up the entire greenfield project scaffold that every later plan and phase depends on: a Next.js 16 App Router + TypeScript app, a Dockerized PostgreSQL 18 (postgis/postgis:18) local DB, Drizzle ORM wired over postgres.js, Tailwind v4 + shadcn/ui, and the Vitest + Playwright test infrastructure (none exists yet). This is Wave 0 — nothing else can run until it is green.

Purpose: This phase is the first implementation phase of a greenfield repo (only CLAUDE.md + .planning/ exist). The scaffold is the load-bearing foundation for auth (Plan 02), auth UI flows (Plan 03), and profile/capabilities (Plan 04), plus all of Phases 2-8.

Output: A buildable Next.js project with pinned deps, a running local Postgres, a Drizzle DB instance, shadcn/ui initialized, and a runnable test harness with a real-Postgres integration-test strategy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-auth-accounts/01-RESEARCH.md
@.planning/phases/01-auth-accounts/01-VALIDATION.md

<interfaces>
<!-- Concrete config the executor must produce. Copied verbatim from 01-RESEARCH.md. -->

Pinned versions (verified npm 2026-06-03 — these OVERRIDE CLAUDE.md drift):
- next 16.2.7, react/react-dom 19.2.7, typescript 5.7+
- drizzle-orm 0.45.2, drizzle-kit 0.31.10, postgres (postgres.js) 3.4.9
- better-auth 1.6.14 (installed here, configured in Plan 02)
- resend 6.12.4, cloudinary 2.10.0
- react-hook-form 7.77.0, zod 4.4.3, @hookform/resolvers 5.4.0
- tailwindcss 4.3.0
- DEV/TEST: vitest 4.1.8, @playwright/test 1.60.0, @testing-library/react 16.3.2, @vitejs/plugin-react
- DO NOT install jose (Apple deferred from Phase 1)

Drizzle db instance (src/lib/db/index.ts):
```typescript
import { drizzle } from "drizzle-orm/postgres-js";
import postgres from "postgres";
import * as schema from "./schema";
export const db = drizzle(postgres(process.env.DATABASE_URL!), { schema });
```

drizzle.config.ts:
```typescript
import { defineConfig } from "drizzle-kit";
export default defineConfig({
  schema: "./src/lib/db/schema.ts",
  out: "./drizzle",
  dialect: "postgresql",
  dbCredentials: { url: process.env.DATABASE_URL! },
});
```

docker-compose.yml:
```yaml
services:
  db:
    image: postgis/postgis:18
    environment:
      POSTGRES_USER: fitout
      POSTGRES_PASSWORD: fitout
      POSTGRES_DB: fitout
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }
```

DATABASE_URL for local dev: postgresql://fitout:fitout@localhost:5432/fitout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 + install the pinned dependency set without clobbering CLAUDE.md/.planning/</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, src/app/ (scaffolded)</files>
  <read_first>
    - .planning/phases/01-auth-accounts/01-RESEARCH.md (§"Installation (scaffold sequence)", Pitfall 8, §"Standard Stack" version table)
    - CLAUDE.md (§"Technology Stack" — confirm Next/Drizzle/Postgres/Tailwind are the locked stack; note version drift: use the RESEARCH.md pins, NOT CLAUDE.md's older numbers)
  </read_first>
  <action>
    The repo is NOT empty (contains CLAUDE.md, .planning/, .git, .gitattributes). DO NOT clobber CLAUDE.md or .planning/.

    1. Scaffold Next.js into the current directory:
       `npx create-next-app@latest . --typescript --tailwind --app --eslint --src-dir --import-alias "@/*" --no-turbopack`
       When prompted about the non-empty directory, proceed but VERIFY afterward that CLAUDE.md and .planning/ are intact (`git status` must still show them unmodified). If create-next-app refuses, scaffold into a temp dir (`/tmp/fitout-scaffold`) and copy the generated files in, skipping any that would overwrite CLAUDE.md/.planning/.

    2. Pin Next/React to the researched versions if the scaffold installed different ones:
       `npm install next@16.2.7 react@19.2.7 react-dom@19.2.7`

    3. Install data layer: `npm install drizzle-orm@0.45.2 postgres@3.4.9` and `npm install -D drizzle-kit@0.31.10`

    4. Install auth + integrations (configured in later plans): `npm install better-auth@1.6.14 resend@6.12.4 cloudinary@2.10.0`

    5. Install forms + validation: `npm install react-hook-form@7.77.0 zod@4.4.3 @hookform/resolvers@5.4.0`

    6. DO NOT install jose (Apple OAuth is DEFERRED from Phase 1 — see RESEARCH.md "Apple Sign In Recommendation").

    7. Confirm Tailwind v4 is present (create-next-app --tailwind installs it). If it installed an older major, `npm install -D tailwindcss@4.3.0`.

    8. Ensure .gitignore contains `.env.local`, `.env*.local`, `node_modules`, `.next`, `/drizzle/meta` is NOT ignored (keep migrations tracked). Add `.env.local` explicitly if missing.

    Confirm `git status` still lists CLAUDE.md and .planning/ unchanged.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; const need=['next','react','drizzle-orm','postgres','better-auth','resend','cloudinary','react-hook-form','zod','@hookform/resolvers']; const missing=need.filter(k=>!p[k]); if(missing.length){console.error('MISSING deps:',missing);process.exit(1)} if(p.jose){console.error('jose must NOT be installed (Apple deferred)');process.exit(1)} console.log('deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` includes: next, react, react-dom, drizzle-orm, postgres, better-auth, resend, cloudinary, react-hook-form, zod, @hookform/resolvers
    - package.json `devDependencies` includes: drizzle-kit
    - package.json does NOT contain a `jose` dependency
    - `git status` shows CLAUDE.md and .planning/ are unmodified (not deleted/overwritten)
    - `src/app/` directory exists (App Router, --src-dir)
  </acceptance_criteria>
  <done>Next.js 16 App Router project scaffolded with all pinned deps installed; CLAUDE.md and .planning/ untouched; jose absent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Dockerized Postgres 18 + Drizzle + env, and initialize shadcn/ui</name>
  <files>docker-compose.yml, .env.local, .env.example, src/lib/db/index.ts, src/lib/db/schema.ts, drizzle.config.ts, package.json, components.json, src/lib/utils.ts</files>
  <read_first>
    - .planning/phases/01-auth-accounts/01-RESEARCH.md (§"Code Examples" — Drizzle DB instance, drizzle.config.ts, docker-compose.yml; §"Environment Availability")
    - package.json (confirm drizzle-kit + postgres installed by Task 1)
  </read_first>
  <action>
    1. Create `docker-compose.yml` exactly as in the interfaces block (image: postgis/postgis:18, user/pass/db = fitout, port 5432, pgdata volume).

    2. Create `.env.local` with:
       ```
       DATABASE_URL=postgresql://fitout:fitout@localhost:5432/fitout
       BETTER_AUTH_SECRET=dev-secret-change-me-32chars-minimum-aaaa
       BETTER_AUTH_URL=http://localhost:3000
       # Filled in later plans / by user:
       GOOGLE_CLIENT_ID=
       GOOGLE_CLIENT_SECRET=
       RESEND_API_KEY=
       EMAIL_FROM=FitOut <onboarding@resend.dev>
       CLOUDINARY_CLOUD_NAME=
       CLOUDINARY_API_KEY=
       CLOUDINARY_API_SECRET=
       ```
       Also create `.env.example` mirroring the keys with empty values (committed; `.env.local` is gitignored).

    3. Create `src/lib/db/index.ts` exactly as in the interfaces block (drizzle over postgres.js, importing `* as schema from "./schema"`).

    4. Create a placeholder `src/lib/db/schema.ts` with a single line `export {};` (Better Auth CLI in Plan 02 will populate the real auth tables here — DO NOT hand-write auth tables now; see RESEARCH Pitfall 1). This placeholder just makes the import in index.ts resolve.

    5. Create `drizzle.config.ts` exactly as in the interfaces block.

    6. Add npm scripts to package.json: `"db:up": "docker compose up -d db"`, `"db:generate": "drizzle-kit generate"`, `"db:migrate": "drizzle-kit migrate"`, `"db:studio": "drizzle-kit studio"`.

    7. Start the DB and confirm it is reachable: `docker compose up -d db` then poll until ready (`docker compose exec -T db pg_isready -U fitout`).

    8. Initialize shadcn/ui: `npx shadcn@latest init` (accept defaults; Tailwind v4, "@/*" alias). This creates `components.json` and `src/lib/utils.ts`. Add the primitives the auth/profile forms need: `npx shadcn@latest add button input label card form dialog dropdown-menu avatar textarea`.
  </action>
  <verify>
    <automated>docker compose exec -T db pg_isready -U fitout && node -e "const fs=require('fs'); for(const f of ['docker-compose.yml','drizzle.config.ts','src/lib/db/index.ts','src/lib/db/schema.ts','.env.local','components.json']){if(!fs.existsSync(f)){console.error('MISSING',f);process.exit(1)}} console.log('files OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `postgis/postgis:18`
    - `docker compose exec -T db pg_isready -U fitout` reports the server is accepting connections
    - drizzle.config.ts contains `./src/lib/db/schema.ts`
    - src/lib/db/index.ts contains `drizzle` and `DATABASE_URL`
    - .env.local contains `DATABASE_URL=postgresql://fitout:fitout@localhost:5432/fitout`
    - .env.example exists and is NOT gitignored
    - components.json exists (shadcn/ui initialized)
  </acceptance_criteria>
  <done>Local Postgres 18 container is running and reachable; Drizzle db instance + config point at schema.ts; env vars scaffolded; shadcn/ui initialized with base primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Install + configure Vitest + Playwright with a real-Postgres integration-test strategy and shared mocks</name>
  <files>vitest.config.ts, playwright.config.ts, tests/setup.ts, tests/helpers/db.ts, tests/helpers/mocks.ts, tests/smoke.test.ts, package.json</files>
  <read_first>
    - .planning/phases/01-auth-accounts/01-VALIDATION.md (§"Test Infrastructure", §"Wave 0 Requirements")
    - .planning/phases/01-auth-accounts/01-RESEARCH.md (§"Validation Architecture" — Test Framework, Wave 0 Gaps; the full Req→Test map listing every tests/**/*.test.ts path)
    - src/lib/db/index.ts (the db instance integration tests will exercise)
  </read_first>
  <action>
    1. Install test deps: `npm install -D vitest@4.1.8 @playwright/test@1.60.0 @testing-library/react@16.3.2 @vitejs/plugin-react jsdom` then `npx playwright install chromium`.

    2. Create `vitest.config.ts` using `@vitejs/plugin-react`, `environment: "jsdom"` for component tests but `environment: "node"` selectable for integration tests (use a single config with `test.environmentMatchGlobs` or default node + jsdom per-file pragma). Set `test.setupFiles: ["tests/setup.ts"]`, `test.globals: true`, and `test.include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"]`. NO watch mode in the run command.

    3. Create `playwright.config.ts` with `defineConfig` — `testDir: "e2e"`, `webServer` that runs `npm run dev` on port 3000 with `reuseExistingServer: true`, `baseURL: "http://localhost:3000"`, chromium project.

    4. Create `tests/setup.ts` — loads `.env.local` (via dotenv or Next's loader), and registers the Resend/Cloudinary mocks (below) globally.

    5. Create `tests/helpers/db.ts` — the integration-test DB strategy: connect to the same local Postgres but to an **isolated test schema or test database** so integration tests don't pollute dev data. Provide `setupTestDb()` that (a) ensures a clean schema, (b) runs `drizzle-kit migrate` (or applies the generated SQL) against it BEFORE tests, and (c) `teardownTestDb()`. Export a `db`-equivalent bound to the test schema. NOTE: real auth tables are generated in Plan 02 — this helper must run whatever migrations exist in `./drizzle` at test time, so it stays correct once Plan 02 adds them. Include a comment: "migrations are applied here; Plan 02 generates the auth migration."

    6. Create `tests/helpers/mocks.ts` — exports: a `mockResend` that captures sent emails (and exposes the last reset/verification link) so reset/verify integration tests can read the link without real delivery; a `mockCloudinary` whose `upload_stream` resolves a fake `{ secure_url, public_id }`; a `mockGoogleProvider` stub returning a profile with `emailVerified: true` (for the D-08 test in Plan 03). These are the fixtures the Plan 02/03/04 tests import.

    7. Add a trivial smoke test `tests/smoke.test.ts` asserting `1+1===2` and that `process.env.DATABASE_URL` is defined, so `npx vitest run` is green now (before any feature tests exist).

    8. Add npm scripts: `"test": "vitest run"`, `"test:e2e": "playwright test"`.
  </action>
  <verify>
    <automated>npx vitest run tests/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/smoke.test.ts` exits 0
    - vitest.config.ts contains `setupFiles` referencing `tests/setup.ts`
    - playwright.config.ts contains `defineConfig` and `testDir: "e2e"`
    - tests/helpers/db.ts contains `migrate` and exports `setupTestDb`
    - tests/helpers/mocks.ts exports `mockResend`, `mockCloudinary`, and `mockGoogleProvider`
    - No `--watch` flag appears in package.json test scripts
    - `@playwright/test` and `vitest` present in devDependencies
  </acceptance_criteria>
  <done>Vitest + Playwright installed and configured; a real-Postgres integration-test strategy (isolated schema, migrate-before-tests) exists; shared Resend/Cloudinary/Google mocks exported for downstream plans; smoke test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify scaffold builds, Postgres is up, and tests run</name>
  <action>HUMAN VERIFICATION (no autonomous action). The executor pauses here after Tasks 1-3 complete; the human runs the steps in how-to-verify and approves. Claude does not perform additional work in this task.</action>
  <what-built>Greenfield scaffold: Next.js 16 App Router project, Dockerized Postgres 18 (postgis/postgis:18) running on 5432, Drizzle wired, shadcn/ui initialized, and Vitest + Playwright configured with a real-Postgres integration strategy.</what-built>
  <how-to-verify>
    1. Confirm Docker Desktop is running, then run `npm run db:up` — `docker ps` should show a `postgis/postgis:18` container on 0.0.0.0:5432.
    2. Run `npm run dev` and visit http://localhost:3000 — the default Next.js page should render with Tailwind styles.
    3. Run `npm run test` — the smoke test should pass (exit 0).
    4. Confirm `git status` still shows CLAUDE.md and .planning/ present and unmodified.
  </how-to-verify>
  <resume-signal>Type "approved" if the app builds, Postgres is up, and tests run — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Docker Postgres | Local DB credentials live in .env.local (gitignored); container exposed only on localhost:5432 |
| Repo → version control | Secrets must never be committed (.env.local gitignored; .env.example holds empty placeholders) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local secrets (DATABASE_URL, future API keys) committed to git | mitigate | .gitignore includes `.env.local`/`.env*.local`; only `.env.example` (empty values) is committed. Verified in Task 1 acceptance. |
| T-01-02 | Tampering | Scaffold tooling overwrites CLAUDE.md / .planning/ planning artifacts | mitigate | Task 1 scaffolds carefully (temp-dir fallback) and verifies `git status` shows CLAUDE.md + .planning/ unmodified. |
| T-01-03 | Information Disclosure | Local Postgres exposed beyond localhost | accept | Dev-only container bound to localhost:5432 with throwaway creds; never used in prod. Low risk for a single-dev local DB. |
| T-01-04 | Tampering | Integration tests run against the dev database and corrupt real data | mitigate | tests/helpers/db.ts isolates tests to a dedicated test schema/DB and migrates it separately (Task 3). |
</threat_model>

<verification>
- `npm run db:up` starts a postgis/postgis:18 container reachable on 5432
- `npx next build` (or `npm run dev`) compiles the scaffolded app
- `npx vitest run` is green (smoke test)
- CLAUDE.md and .planning/ remain unmodified after scaffolding
- No secrets committed: `git check-ignore .env.local` returns `.env.local`
</verification>

<success_criteria>
- Buildable Next.js 16 App Router + TypeScript project with all pinned deps
- Local Postgres 18 (postgis/postgis:18) running and reachable
- Drizzle db instance + drizzle.config.ts pointing at src/lib/db/schema.ts (placeholder)
- shadcn/ui initialized with base form primitives
- Vitest + Playwright configured; isolated-Postgres integration-test strategy + shared mocks in place; smoke test green
- Foundation ready for Plan 02 (Better Auth) to populate schema.ts and run the migration workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-accounts/01-01-SUMMARY.md` documenting: scaffold structure, pinned versions actually installed, the test DB strategy chosen (test schema vs separate DB), shadcn primitives added, and any deviations.
</output>
