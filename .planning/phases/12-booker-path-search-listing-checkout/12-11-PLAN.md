---
phase: 12-booker-path-search-listing-checkout
plan: 11
type: execute
wave: 9
depends_on: [12-10]
files_modified:
  - src/components/ui/collapsible.tsx
  - src/components/booking/price-disclosure.tsx
  - src/components/booking/checkout-sticky-bar.tsx
  - src/components/booking/reserve-view.tsx
  - src/components/booking/reserve-actions.tsx
  - src/app/listings/[id]/book/page.tsx
  - src/lib/design/selector-contract.ts
  - tests/booking/reserve-actions.test.tsx
  - tests/design/dark-scope.test.ts
  - tests/design/elevation-z.test.ts
  - e2e/mobile-booker-path.spec.ts
  - e2e/overflow-320.spec.ts
autonomous: true
requirements: [BFLOW-06, BFLOW-07]

must_haves:
  truths:
    - "D-50: on mobile checkout the ITEMISED LINES collapse and the Total never does — the booker never opens anything to see what they are paying, only to see how it was built"
    - "D-51: the PayMongo redirect is INLINE — no dialog, no interstitial. A confirm dialog inserts a second tap between a decided booker and paying, on a surface where a 15-minute hold is expiring"
    - "BFLOW-07's actual gap is the word PayMongo: the pending label says `Taking you to checkout…` today, which names no destination. The pressed label and the at-rest line both name it, plus the amount and the rails"
    - "The collapsible block adds ZERO npm dependencies — radix-ui is installed in its single-package form and re-exports Collapsible. If the add writes a @radix-ui/react-* entry into package.json, STOP and hand-roll: a new dependency in a milestone whose net-new test bars it is a scope alarm, not a convenience"
    - "shadow-sticky finishes this phase with exactly 2 call sites, both bottom-anchored bars, and zero on any header"
  artifacts:
    - path: "src/components/ui/collapsible.tsx"
      provides: "The one new shadcn registry block, vendored, importing only the already-installed radix-ui meta-package"
    - path: "src/components/booking/price-disclosure.tsx"
      provides: "<div data-testid=\"price-disclosure\"> — the `Price details` trigger with aria-expanded/aria-controls, >=44px, hiding the run/fee/surcharge lines and never the Total"
      contains: "price-disclosure"
    - path: "src/components/booking/checkout-sticky-bar.tsx"
      provides: "The checkout bottom bar — Total label over the amount, `Confirm & pay` at 44px, shadow-sticky's second and final call site"
      contains: "checkout-sticky-bar"
  key_links:
    - from: "src/components/booking/reserve-actions.tsx"
      to: "src/app/actions/booking.ts"
      via: "confirmBooking mints the hosted checkout; the client only NAMES the destination"
      pattern: "PayMongo"
    - from: "src/components/booking/price-disclosure.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "the vendored Radix collapsible rather than a hand-rolled aria-expanded/aria-controls pair"
      pattern: "Collapsible"
---

<objective>
Make checkout a single column on mobile with the itemised lines behind a disclosure and a sticky confirm
bar carrying the amount, and tell the booker where they are going before they leave.

Purpose: BFLOW-06 and BFLOW-07 are the last two steps of the path, and BFLOW-07's whole gap is one word.
The disclosure is the only place this phase adds a vendored block, so it carries the milestone's registry
discipline with it.
Output: one new block with three checked conditions, a disclosure, a bar, and a redirect that names its
destination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-booker-path-search-listing-checkout/12-UI-SPEC.md
@.planning/phases/12-booker-path-search-listing-checkout/12-RESEARCH.md
@.planning/phases/12-booker-path-search-listing-checkout/12-PATTERNS.md

<interfaces>
THE CHECKOUT REGIONS (12-UI-SPEC § Checkout):
  header        the shipped minimal SiteChrome — wordmark as a <span> (brandHref={null}), no nav, no
                footer, ZERO <a href> inside site-header. Phase 11 AC#5 and AC#7 survive unchanged.
  header slot   the live hold countdown (plan 12-03, already shipped)
  summary       listing name, city, day, and `{9:00 AM – 11:00 AM · 2 hours · Makati time}`. ALWAYS
                visible; never behind a disclosure.
  disclosure    data-testid="price-disclosure" — trigger `Price details` + an item count, aria-expanded,
                aria-controls, >=44px, caret rotating at --motion-fast. Collapsed content: the run line,
                the fee line, any surcharge line.
  total         NEVER collapses. Outside the disclosure, below it, with the Separator.
  sticky bar    data-testid="checkout-sticky-bar" — same box as the listing bar (STICKY_BAR_HEIGHT,
                shadow-sticky, z-(--z-sticky)). Left: `Total` label over the amount. Right: `Confirm & pay`,
                44px, variant="brand". Below `lg:` only; the desktop rail keeps the action inline.

COPY THAT CHANGES (all of it is shipped copy that some test may pin — where a test pins a string this
phase changes, THE TEST MOVES IN THE SAME COMMIT):
  <h1>                `Review and book`  ->  `Confirm and pay`
  rail lede           `We're holding this for you while you review.`  (no digits — plan 12-03 moved them)
  CTA at rest         `Confirm & pay`  (shipped, unchanged)
  CTA pressed         `Taking you to checkout…`  ->  `Taking you to PayMongo…`
  line beneath        `You'll pay {total} on PayMongo — card, GCash, Maya or QR Ph. We'll bring you
                      straight back.`
  sticky bar left     `Total` over `{amount}`
  disclosure trigger  `Price details` + `{N} items`
  lease refusal       the server's own sentence, rendered inline through role="status", muted, never red —
                      shipped, unchanged. ⚠ DO NOT import CHECKOUT_IN_FLIGHT_MESSAGE: that module pulls the
                      ORM into the client bundle.

THE REGISTRY ADD AND ITS THREE CONDITIONS, each checkable at the moment it lands:
  1. IT MUST NOT ADD AN NPM DEPENDENCY. `radix-ui` is installed at 1.4.3 in its single-package form and
     re-exports Collapsible; the fetched registry item's sole import is from `"radix-ui"`. If
     `npx shadcn@4.10.0 add collapsible` writes a new `@radix-ui/react-*` entry into `package.json`, STOP
     and hand-roll instead.
  2. `tests/design/dark-scope.test.ts` pins the vendored `dark:` occurrence count and the file count. ⚠ THE
     UI-SPEC SAYS 54; MEASURED, the assertions are 44 occurrences across 13 files (54 appears only in that
     file's comments, describing the app-wide history). Re-measure, state the delta, update the pin in the
     SAME commit as the add if and only if it moved. The fetched item contains zero Tailwind classes and
     zero dark variants, so the expected delta is 0 — do not pre-emptively bump either number.
  3. The DS-13 leak gate and the focus-recipe scan run over it. If the block carries a raw hex, an
     arbitrary type size or a half-alpha ring, the fix is the same token substitution Phase 10 applied to
     the other 30 primitives, not an exemption.

  `components.json` declares `"registries": {}` — ZERO third-party. This plan must not add one.

`shadow-sticky` reaches its final count here: EXACTLY 2 call sites, both bottom-anchored bars, ZERO on any
header. That assertion lands in `tests/design/elevation-z.test.ts` in this plan, because this is the plan
in which the second bar exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the collapsible block under its three conditions, and build the disclosure</name>
  <files>src/components/ui/collapsible.tsx, src/components/booking/price-disclosure.tsx, src/lib/design/selector-contract.ts, tests/design/dark-scope.test.ts</files>
  <read_first>
    - .planning/phases/12-booker-path-search-listing-checkout/12-RESEARCH.md § Package Legitimacy Audit and § Standard Stack — both rows verified this session; the block is vendored SOURCE, not an npm install, so `slopcheck` and `npm view` are not applicable and the equivalent verification is the registry-item fetch, which was performed.
    - tests/design/dark-scope.test.ts — find the two pinned assertions and READ THE NUMBERS OFF THE FILE. Do not trust the UI-SPEC's figure; RESEARCH Pitfall 10 records that it is wrong.
    - tests/design/leak.test.ts and tests/design/focus-recipe.test.ts — the two scans that run over every vendored primitive.
    - src/components/ui/popover.tsx and src/components/ui/dialog.tsx — the single-package import form (`import { X as XPrimitive } from "radix-ui"`) the new block must match.
    - src/components/booking/price-breakdown.tsx — the run line, the D-108 surcharge line and the fee line that go inside the disclosure, and the Total that never does.
    - src/lib/design/selector-contract.ts and tests/design/selector-contract.test.ts.
    - components.json — `"registries": {}`; confirm it is still empty after the add.
  </read_first>
  <action>
Run `npx shadcn@4.10.0 add collapsible`. THEN, before writing any consumer, check the three conditions in
`<interfaces>` and record each result in the SUMMARY:

  - `git diff --stat package.json` must be EMPTY. If it is not — if a `@radix-ui/react-*` entry appeared —
    revert the add, hand-roll the disclosure with an explicit `aria-expanded` / `aria-controls` pair, and
    RAISE the dependency as a scope alarm in the SUMMARY. Do not proceed with a new dependency.
  - Re-measure `tests/design/dark-scope.test.ts`'s two pins, state the before/after numbers and the delta,
    and update them only if they actually moved.
  - Run the leak and focus-recipe scans over the new file; fix any finding by token substitution, never by
    adding an exemption.

Create `src/components/booking/price-disclosure.tsx` — a `"use client"` component composing the vendored
collapsible, carrying `data-testid="price-disclosure"` as a STRING LITERAL. Its trigger reads
`Price details` plus the item count, is at least 44px tall, carries `aria-expanded` and `aria-controls`
(Radix wires both — do not hand-write them), and rotates its caret at `--motion-fast`. Its content holds
the run line, the fee line and any surcharge line. THE TOTAL IS NOT INSIDE IT: the Total, its `Separator`
and the fee popover's trigger stay outside and always visible.

Add the `price-disclosure` row to `selector-contract.ts` in this commit with `owner: "12-11"` and a `why`
stating that the assertions hung on it are about the collapsed/expanded STATE of a specific region, which
no role query addresses.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/dark-scope.test.ts tests/design/leak.test.ts tests/design/focus-recipe.test.ts tests/design/selector-contract.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat package.json` is EMPTY, and the SUMMARY records that as condition 1's result (12-UI-SPEC § Registry Safety)
    - `src/components/ui/collapsible.tsx` exists and its only Radix import is from the `"radix-ui"` single package — `grep -n "@radix-ui/react-" src/components/ui/collapsible.tsx` returns nothing
    - `components.json`'s `registries` is still `{}`
    - `tests/design/dark-scope.test.ts`'s before/after pin values and the delta are recorded in the SUMMARY; the pins were changed only if the measurement moved them
    - `data-testid="price-disclosure"` appears exactly once in `src/` as a string literal with its contract row
    - The Total is NOT inside the collapsible content — asserted in Task 3's e2e with the disclosure collapsed (12-UI-SPEC AC#22)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>One new vendored block, three conditions checked at the moment it landed, and a disclosure that hides how the price was built and never what it is.</done>
</task>

<task type="auto">
  <name>Task 2: Single-column checkout and its sticky confirm bar</name>
  <files>src/components/booking/reserve-view.tsx, src/app/listings/[id]/book/page.tsx, src/components/booking/checkout-sticky-bar.tsx, src/lib/design/selector-contract.ts, tests/design/elevation-z.test.ts</files>
  <read_first>
    - src/components/booking/reserve-view.tsx — the whole file after plan 12-03: the `lg:grid-cols-[1fr_360px]` grid, the `PanelCard sticky` rail, the `breakdown` prop it receives as a node, and its expiry swap now reading the hold context.
    - src/app/listings/[id]/book/page.tsx — the whole file: the `<h1>`, where `summary` and `breakdown` are built as SERVER nodes, the D-108 surcharge split, the OC-07 partial grant, the cancellation rungs, `totalLabel`, and the way-back link plan 12-03 added last in the main column.
    - src/components/booking/booking-sticky-bar.tsx — plan 12-10's bar; the checkout bar is the same box with different content, and the two must not diverge in geometry.
    - src/lib/design/measurements.ts — `STICKY_BAR_HEIGHT`, `STICKY_BAR_CLEARANCE`.
    - tests/design/elevation-z.test.ts — its current `shadow-sticky` assertion and its top-header rule. This is the plan that changes the pinned count from the Phase-11 zero to 2.
    - .planning/phases/12-booker-path-search-listing-checkout/12-UI-SPEC.md § Checkout and § Visual Hierarchy (the Total is the checkout's ONE focal point; the countdown is chrome)
  </read_first>
  <action>
Make the checkout a single column below `lg:`: the summary, then the disclosure, then the Total with its
`Separator`, then the cancellation rungs, then the way-back link last. At `lg:` and above the shipped
two-column grid with the `PanelCard sticky` rail is unchanged and keeps its action inline. Reorder with
CSS and placement, not with conditional rendering — one component tree, one `breakdown` node.

Change the `<h1>` to `Confirm and pay` and the rail lede to `We're holding this for you while you review.`
Both are shipped strings: find every test that pins either and move it in this commit. List them in the
SUMMARY.

Create `src/components/booking/checkout-sticky-bar.tsx` — a `"use client"` component rendering the same
box as the listing bar (`STICKY_BAR_HEIGHT`, `bg-card`, `border-t`, `shadow-sticky`, `z-(--z-sticky)`,
`fixed inset-x-0 bottom-0`), with `data-testid="checkout-sticky-bar"` as a STRING LITERAL. Left: the
`Total` label over the amount, `tabular-nums`. Right: `Confirm & pay`, 44px, `variant="brand"`, wired to
the SAME confirm action `ReserveActions` uses — one action, one lease, one guard. Below `lg:` only. Add
`STICKY_BAR_CLEARANCE` to the checkout page container.

Add the `checkout-sticky-bar` row to `selector-contract.ts` with `owner: "12-11"`.

Update `tests/design/elevation-z.test.ts`: `shadow-sticky` now has EXACTLY 2 call sites, both
bottom-anchored bars, and ZERO on any top header. Assert the count as an equality (this is a closed set
this phase declares, unlike the query floors) and keep the header rule. Record the old pinned value and
the new one.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/elevation-z.test.ts tests/design/selector-contract.test.ts tests/design/sticky-offset.test.ts && npx vitest run tests/booking && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "shadow-sticky" src/ | grep -v '^\s*//'` shows exactly 2 call sites, and `tests/design/elevation-z.test.ts` asserts that equality plus zero on any header (12-UI-SPEC AC#47)
    - `data-testid="checkout-sticky-bar"` appears exactly once in `src/` as a string literal with its contract row
    - The `<h1>` reads `Confirm and pay`; every test pinning `Review and book` was moved in this commit and is listed in the SUMMARY
    - The desktop rail is unchanged: `tests/design/sticky-offset.test.ts` passes and the two-column grid classes are byte-identical
    - The bar's confirm action calls the same server action as `ReserveActions` — `grep -n "confirmBooking" src/components/booking/` shows no second call path
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Checkout is one column on a phone with a persistent amount and action, and the desktop layout it grew out of is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Name the destination, and gate the mobile checkout</name>
  <files>src/components/booking/reserve-actions.tsx, tests/booking/reserve-actions.test.tsx, e2e/mobile-booker-path.spec.ts, e2e/overflow-320.spec.ts</files>
  <read_first>
    - src/components/booking/reserve-actions.tsx — the whole 98-line file: the confirm button, the pending label reading `Taking you to checkout…`, the reassurance line beneath, the lease-refusal `role="status"`, and the header warning NOT to import `CHECKOUT_IN_FLIGHT_MESSAGE` because that module pulls the ORM into the client bundle.
    - tests/booking/reserve-actions.test.tsx — the existing cases and which strings they pin.
    - src/app/actions/booking.ts — `confirmBooking`'s `ConfirmResult` union and the fact that the server mints the hosted checkout and redirects. The client only NAMES the destination.
    - e2e/search-and-book.spec.ts — its header recording that `Confirm & pay` opens a hosted checkout and that the tail past the redirect is un-automatable from Playwright.
    - e2e/mobile-booker-path.spec.ts — plan 12-10's spec; this task extends it with the checkout half.
    - .planning/phases/12-booker-path-search-listing-checkout/12-UI-SPEC.md § Copywriting Contract > Checkout
  </read_first>
  <action>
Change `reserve-actions.tsx`'s pending label to `Taking you to PayMongo…` and the at-rest line beneath the
CTA to `You'll pay {total} on PayMongo — card, GCash, Maya or QR Ph. We'll bring you straight back.` The
amount is a pre-formatted string passed in; the component computes nothing. No dialog, no interstitial, no
second tap — D-51 refuses one, and the reason is that a 15-minute hold is expiring while the booker reads
it.

Keep the `disabled` + `aria-disabled` double-click courtesy exactly as it ships, and write at that site
that it IS a courtesy: the real guards are the server-side checkout lease and expire-before-create, and
PayMongo does not honour `Idempotency-Key` on its checkout-session endpoint (T-08-79, probed). Do not
import `CHECKOUT_IN_FLIGHT_MESSAGE`.

Extend `tests/booking/reserve-actions.test.tsx`: the pressed label contains `PayMongo`; the at-rest line
contains `PayMongo`, the formatted total and all four rail names; and — the negative assertion — pressing
the CTA opens no dialog and renders no additional confirmation step.

Extend `e2e/mobile-booker-path.spec.ts` with the checkout half at 375px: `[data-testid="checkout-sticky-bar"]`
is rendered; the `Total` amount is visible with `[data-testid="price-disclosure"]` COLLAPSED; expanding it
reveals the run and fee lines and does not move the Total out of view; and the sticky bar's amount is
byte-equal to the Total. Extend `e2e/overflow-320.spec.ts`'s route table with `/listings/{id}/book` at
320px in both themes with its own `tell`.
  </action>
  <verify>
    <automated>npx vitest run tests/booking/reserve-actions.test.tsx && npx playwright test e2e/mobile-booker-path.spec.ts e2e/overflow-320.spec.ts e2e/price-parity.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - The pressed confirm label and the at-rest line both contain the literal `PayMongo` (12-UI-SPEC AC#28)
    - `grep -n "CHECKOUT_IN_FLIGHT_MESSAGE" src/components/booking/reserve-actions.tsx` returns nothing
    - The negative assertion passes: pressing the CTA renders no `role="dialog"` and no additional confirmation step
    - At 375px the `Total` is visible with the disclosure collapsed, and the sticky bar's amount is byte-equal to it (12-UI-SPEC AC#22)
    - `e2e/overflow-320.spec.ts` measures `/listings/{id}/book` at 320px in both themes with no offender (12-UI-SPEC AC#23, checkout row)
    - `e2e/price-parity.spec.ts` is byte-unmodified and still green — its `price-total` hook is unchanged by the disclosure
    - `npx playwright test --project=chromium` (full suite) exits 0
  </acceptance_criteria>
  <done>The booker knows the amount, the rails and the destination before they leave, and the last screen before the money is one column on a phone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry -> vendored source | A registry block is remote source code entering `src/components/ui/**` |
| client confirm button -> payment authority | The button disable is a courtesy; the server lease is the guarantee |
| checkout copy -> external destination | Naming a destination the app does not actually redirect to is a phishing-shaped trust failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11-SC | Tampering | `npx shadcn add collapsible` | mitigate | RESEARCH's Package Legitimacy Audit dispositioned this Approved: the block is vendored source from the OFFICIAL `ui.shadcn.com` registry (fetched and inspected this session), `components.json` declares zero third-party registries, and it adds NO npm dependency. The three landing conditions are acceptance criteria: empty `package.json` diff (else stop and hand-roll), the re-measured `dark:` pins, and the leak/focus scans |
| T-12-11-DOUBLECHARGE | Tampering | confirm CTA | accept | The `disabled`/`aria-disabled` pair is a courtesy only. The real guards are the server-side compare-and-swap checkout lease and expire-before-create; PayMongo does not honour `Idempotency-Key` on checkout sessions (T-08-79, probed). Unchanged by this plan and stated at the call site |
| T-12-11-ORMBUNDLE | Information disclosure | `reserve-actions.tsx` | mitigate | Renders `result.error`; never imports `CHECKOUT_IN_FLIGHT_MESSAGE`, asserted by grep |
| T-12-11-REDIRECTNAME | Spoofing | the at-rest line | mitigate | The named destination is the one `confirmBooking` actually mints; the client names it and never constructs it. The un-automatable tail is routed to human UAT rather than asserted |
| T-12-11-HIDDENTOTAL | Repudiation | price disclosure | mitigate | The Total sits OUTSIDE the collapsible; asserted visible at 375px with the disclosure collapsed, and byte-equal to the sticky bar's amount |
| T-12-11-STICKYHEADER | Denial of service | `shadow-sticky` | mitigate | `elevation-z.test.ts` asserts exactly 2 call sites, both bottom-anchored, zero on any header — its `-1px` offset is an upward cast |
</threat_model>

<verification>
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `git diff --stat package.json` is EMPTY and `components.json`'s `registries` is still `{}`
- `npm run test:design` green with the re-measured `dark:` pins recorded
- `npx playwright test --project=chromium` green, `e2e/price-parity.spec.ts` unmodified
</verification>

<success_criteria>
- One new vendored block, zero new npm dependencies, three conditions checked as it landed
- Checkout is a single column on mobile with a persistent Total and a 44px confirm action
- The booker is told the amount, the rails and the destination before they leave
</success_criteria>

<output>
Create `.planning/phases/12-booker-path-search-listing-checkout/12-11-SUMMARY.md` when done
</output>
