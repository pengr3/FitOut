# Deferred Items — Phase 12

Out-of-scope discoveries logged during execution. Not fixed by the plan that found them.

- **[12-01] The `/dev/theme` visual baselines are now stale and must be regenerated in the Linux dispatch job.** `RESULT_GRID_GAP` retires the 20px card-grid gutter, and `/dev/theme` section 14 renders BOTH the card-grid skeleton and the resolved grid — so `dev-theme-1280-court-visual-linux.png`, `dev-theme-768-court-visual-linux.png` and `dev-theme-320-court-visual-linux.png` under `e2e/visual/surfaces.spec.ts-snapshots/` no longer match the tree. This is a **consequence of the plan, not a defect**: the gutter change is the plan's whole point, and a baseline that still shows the old gutter is the stale artefact.

  **Not regenerated here, deliberately.** `playwright.config.ts` sets `updateSnapshots: "none"` unconditionally and the `visual` project is not created at all off Linux (D-28 / D-29), so the only sanctioned write path is the `--update-snapshots` dispatch job in `mcr.microsoft.com/playwright:v1.60.0-noble`. Minting a `*-win32.png` locally is exactly the illegal baseline that config exists to prevent. Whoever runs the next dispatch: expect a three-file diff on `dev-theme-*`, confined to the card-grid section's column widths (skeleton cells narrow by 2.67px at 1280 as the gutter goes 20 → 24).

  The functional `chromium` project is unaffected and green — `npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium` is 8 passed after the change.

- **[12-02] `notFound()` on `/listings/[id]` answers HTTP 200 in dev, and `e2e/public-listing.spec.ts`'s two 404 cases are RED because of it — PRE-EXISTING at `6272c8f`, not caused by plan 12-02.** Measured before touching the file: `git checkout -- e2e/public-listing.spec.ts && npx playwright test e2e/public-listing.spec.ts --project=chromium` on a clean tree fails `a draft listing 404s to the public` with `Expected: 404 / Received: 200`, and the unlisted twin never runs (the file is `mode: "serial"`).

  **It is a STATUS-code regression, not a content leak, and that distinction was measured rather than assumed.** `curl -s -o body -w "%{http_code}"` against a real draft listing returns `200` with a body containing `This space isn't available` and `couldn't find that page` — i.e. the `(detail)/not-found.tsx` boundary rendered and the listing did not. T-05-NONPUB's substance (a draft is not viewable by link) holds; what has broken is the status line, almost certainly because Next 16 flushes the streamed shell before `notFound()` resolves, committing 200. `<title>` is the root layout's `FitOut`, not the listing's, confirming the page component's own render never completed.

  **Not fixed here.** It is outside plan 12-02's declared surface, it predates the plan, and the plausible fixes (forcing the route non-streaming, or moving the published gate into `generateMetadata`/a route segment config) are a rendering-strategy change on a public money-path route — a Rule 4 architectural question, not a Rule 1 inline fix. It needs its own decision and its own before/after measurement in `npm run build && npm start` (dev-vs-prod is the discriminator, exactly as `[11-13]`/D-56 requires). **The three new D-59 #1 cases were verified with `--grep "D-59"`**, which does not collect the red pre-existing cases, so their green is real; the whole-file run is red for this reason alone.

- **[12-02] `e2e/availability.spec.ts` and `e2e/price-parity.spec.ts` flake when run in the SAME Playwright invocation.** `npx playwright test e2e/price-parity.spec.ts e2e/availability.spec.ts --project=chromium` failed `published-but-not-payable listing: calendar renders read-only` once (`toBeVisible` timeout on the "Not bookable yet" button); both files pass in full, repeatedly, when invoked separately (`availability` 4 passed, `price-parity` 1 passed). Both seed directly into the same Postgres with their own `postgres({max:1})` client and their own `beforeAll`/`afterAll`, and both are `serial` within themselves but not across files. This is the `write CONNECTION_ENDED` family the spine's item 2 already documents, arriving one level up. Worth a look before anyone adds a fourth DB-seeding spec to a single command line.

- **[12-01] `e2e/skeleton-geometry.spec.ts`'s header measurement table is now one number out of date.** The table recorded at 17 August 2026 lists the card-grid media box at `179.33 × 134.48` in both themes on `/dev/theme` at 1280px. With the gutter at 24px the measured box is `176.66 × 134.48`. The table is prose, not an assertion — the ±2px comparison re-measures on every run and is green — so nothing is broken, but a reader who trusts the table will be 2.67px wrong about the width. Refresh it the next time that file is opened for a real change.
