# Deferred Items — Phase 12

Out-of-scope discoveries logged during execution. Not fixed by the plan that found them.

- **[12-09] RESOLVED — the last booker-path tooltip is gone, and its content did not go with it.** The `[12-08]` entry below names 12-09 as the owner of `slot-picker.tsx:169-211` and states the question: *"where does 'why this hour is unavailable, including the notice requirement' live once the hover is gone — a single day-panel line naming the states present, or the `aria-label` expanded into visible text?"* **Both**, because they answer different halves. The chip's `aria-label` now carries the FULL sentence (`tooltipFor`, notice requirement included) rather than the short reason, and the day panel renders one visible line — `Struck-through times can't be booked — {the distinct states this day contains}` — derived from the same function so the announced sentence and the visible one cannot drift. `grep -rln "components/ui/tooltip" src/components/{search,listing,availability,booking} src/app/listings` now returns **one** file: `src/components/listing/photo-uploader.tsx`, a HOST surface (the listing wizard's photo step), which the criterion's directory glob was always wider than. D-56's *"deletes the last one"* is now true of the booker path.

- **[12-09] `date-pass-picker.tsx`'s calendar did NOT receive BFLOW-05's overrides, and its month read has no loading state at all.** Two findings on one file, both measured while paying off the hit-area debt on the exclusive calendar.

  **(a) The drop-in month grid is still 28 × 28.** `DatePassPicker` mounts the same vendored `Calendar` and the same `CalendarDayButton`, with its own `components={{ DayButton }}` override for the fully-booked treatment — and none of the four overrides 12-09 landed. Measured on the exclusive calendar, the shipped state is a **28 × 28** day cell and **28 × 28** nav buttons at every width; the drop-in one is byte-identical in its class strings, so it is the same number. That is below the WCAG 2.5.8 AA 24px bar's comfortable margin and is the same debt BFLOW-05 names, on the other booker surface. **Not fixed here:** the file is outside plan 12-09's `files_modified`, the four overrides interact (the container width has to move with the cell size or the grid collapses to 25.08px — see `e2e/calendar-hit-area.spec.ts`'s header), and no seeded `open_capacity` listing exists in the local catalogue today (`SELECT … WHERE occupancy_mode='open_capacity'` → 0 rows), so the change could not be MEASURED in a browser the way the exclusive one was. Doing it unmeasured is precisely what this plan exists to refuse. **Owner: whichever plan next opens the drop-in surface**; the four overrides and their derivations are at `availability-calendar.tsx`'s Calendar call site and can be lifted verbatim, but a `boundingBox()` on a seeded drop-in listing is what settles them.

  **(b) The drop-in month read shows nothing while it runs.** `date-pass-picker.tsx:153-172` fetches `getOpenMonthAvailability` on every month change and, until it resolves, renders the new month with the PREVIOUS month's fully-booked set applied — i.e. dates that are sold out look selectable for the duration of the read. That is the stale-but-plausible shape IN-03 refuses, and it is the one genuine "the month's availability is in flight" state in the tree. `CalendarMonthSkeleton` (exported from `availability-calendar.tsx`) is the plate it wants; what it needs first is a decision about whether replacing the grid mid-navigation is acceptable, since doing so removes the nav buttons the booker just used. Recorded rather than fixed for the same reason as (a).

- **[12-01] The `/dev/theme` visual baselines are now stale and must be regenerated in the Linux dispatch job.** `RESULT_GRID_GAP` retires the 20px card-grid gutter, and `/dev/theme` section 14 renders BOTH the card-grid skeleton and the resolved grid — so `dev-theme-1280-court-visual-linux.png`, `dev-theme-768-court-visual-linux.png` and `dev-theme-320-court-visual-linux.png` under `e2e/visual/surfaces.spec.ts-snapshots/` no longer match the tree. This is a **consequence of the plan, not a defect**: the gutter change is the plan's whole point, and a baseline that still shows the old gutter is the stale artefact.

  **Not regenerated here, deliberately.** `playwright.config.ts` sets `updateSnapshots: "none"` unconditionally and the `visual` project is not created at all off Linux (D-28 / D-29), so the only sanctioned write path is the `--update-snapshots` dispatch job in `mcr.microsoft.com/playwright:v1.60.0-noble`. Minting a `*-win32.png` locally is exactly the illegal baseline that config exists to prevent. Whoever runs the next dispatch: expect a three-file diff on `dev-theme-*`, confined to the card-grid section's column widths (skeleton cells narrow by 2.67px at 1280 as the gutter goes 20 → 24).

  The functional `chromium` project is unaffected and green — `npx playwright test e2e/skeleton-geometry.spec.ts --project=chromium` is 8 passed after the change.

- **[12-02] `notFound()` on `/listings/[id]` answers HTTP 200 in dev, and `e2e/public-listing.spec.ts`'s two 404 cases are RED because of it — PRE-EXISTING at `6272c8f`, not caused by plan 12-02.** Measured before touching the file: `git checkout -- e2e/public-listing.spec.ts && npx playwright test e2e/public-listing.spec.ts --project=chromium` on a clean tree fails `a draft listing 404s to the public` with `Expected: 404 / Received: 200`, and the unlisted twin never runs (the file is `mode: "serial"`).

  **It is a STATUS-code regression, not a content leak, and that distinction was measured rather than assumed.** `curl -s -o body -w "%{http_code}"` against a real draft listing returns `200` with a body containing `This space isn't available` and `couldn't find that page` — i.e. the `(detail)/not-found.tsx` boundary rendered and the listing did not. T-05-NONPUB's substance (a draft is not viewable by link) holds; what has broken is the status line, almost certainly because Next 16 flushes the streamed shell before `notFound()` resolves, committing 200. `<title>` is the root layout's `FitOut`, not the listing's, confirming the page component's own render never completed.

  **Not fixed here.** It is outside plan 12-02's declared surface, it predates the plan, and the plausible fixes (forcing the route non-streaming, or moving the published gate into `generateMetadata`/a route segment config) are a rendering-strategy change on a public money-path route — a Rule 4 architectural question, not a Rule 1 inline fix. It needs its own decision and its own before/after measurement in `npm run build && npm start` (dev-vs-prod is the discriminator, exactly as `[11-13]`/D-56 requires). **The three new D-59 #1 cases were verified with `--grep "D-59"`**, which does not collect the red pre-existing cases, so their green is real; the whole-file run is red for this reason alone.

- **[12-02] `e2e/availability.spec.ts` and `e2e/price-parity.spec.ts` flake when run in the SAME Playwright invocation.** `npx playwright test e2e/price-parity.spec.ts e2e/availability.spec.ts --project=chromium` failed `published-but-not-payable listing: calendar renders read-only` once (`toBeVisible` timeout on the "Not bookable yet" button); both files pass in full, repeatedly, when invoked separately (`availability` 4 passed, `price-parity` 1 passed). Both seed directly into the same Postgres with their own `postgres({max:1})` client and their own `beforeAll`/`afterAll`, and both are `serial` within themselves but not across files. This is the `write CONNECTION_ENDED` family the spine's item 2 already documents, arriving one level up. Worth a look before anyone adds a fourth DB-seeding spec to a single command line.

- **[12-01] `e2e/skeleton-geometry.spec.ts`'s header measurement table is now one number out of date.** The table recorded at 17 August 2026 lists the card-grid media box at `179.33 × 134.48` in both themes on `/dev/theme` at 1280px. With the gutter at 24px the measured box is `176.66 × 134.48`. The table is prose, not an assertion — the ±2px comparison re-measures on every run and is green — so nothing is broken, but a reader who trusts the table will be 2.67px wrong about the width. Refresh it the next time that file is opened for a real change.

- **[12-03] `/` and `/bookings/[id]` transiently hold TWO copies of a streamed element, and every spec that addresses one by id or by exact text is racing it.** Measured on `/` while writing `e2e/helpers/booker-seed.ts`: the served document carries `id="search-category"` at byte **11,713** (the `(public)/loading.tsx` fallback's own `SearchBar`) and again at **84,158** (the resolved page), with React's first completion segment at **26,879** between them. Playwright's strict mode fails with `resolved to 2 elements` if the click lands before the boundary resolves. Fixed inside the new helper with an `expect(locator).toHaveCount(1)` wait that states the reason; the shipped specs (`price-parity`, `search-and-book`) use the same bare `#search-category` locator and get away with it only because they happen to arrive late.

  The same shape bites `e2e/search-and-book.spec.ts:306` (*"directly-seeded confirmed booking → the durable confirmation"*) after its `page.reload()`: `getByText('FIT-XXXXXXXX', { exact: true })` intermittently resolves to two identical `<p>` nodes. Observed failing on two runs and passing on a third **with no code change in between**, on a route (`/bookings/[id]`) that plan 12-03 does not touch. **Not fixed here** — it is a shipped spec outside this plan's surface and the correct fix is the same `toHaveCount(1)` wait, applied by whoever next opens that file for a real change. Note that it is in a `serial` describe, so its failure SKIPS the two tests after it; run with `--grep` when that matters.

- **[12-03] `e2e/availability.spec.ts:261` flakes in the FULL-suite invocation, extending the `[12-02]` entry above.** `npx playwright test --project=chromium` failed *"published-but-not-payable listing: calendar renders read-only"* on one full run and passed on the next, and on a third full run it was `e2e/hold-countdown.spec.ts`'s one-countdown case that timed out at 2m instead. Both pass repeatedly when their own file is invoked alone (`hold-countdown` 4 passed, `shell` 19 passed, together 23 passed). Four DB-seeding spec files now share one Postgres with `postgres({ max: 1 })` clients apiece; the ceiling this repository has already met twice (`sorry, too many clients already`) is the likely cause. **MEASURED:** the plan's own verify command `npx playwright test e2e/hold-countdown.spec.ts e2e/shell.spec.ts --project=chromium` went 23 passed -> 1 failed (hold-countdown's court geometry case, timed out at 3m) -> 1 failed (shell.spec's SHIPPED `AC#3 host/court` case, *"the auth slot is STILL holding 1 placeholder(s) in the resolved document"*) -> **23 passed** across four consecutive invocations with NO code change, the last one immediately after `docker restart fitout-db-1` (9 connections after restart). Three different tests, two of them shipped and untouched by this plan, all timing-shaped. Worth a real fix — a shared pool, or `fullyParallel: false` for the seeding specs — before a fifth one lands.

- **[12-06] `e2e/availability.spec.ts:261` has stopped being a flake and is now a STANDING RED, and it is PRE-EXISTING — measured against a reverted tree rather than assumed.** *"published-but-not-payable listing: calendar renders read-only, slots not selectable"* fails `expect(getByRole("button", { name: /not bookable yet/i })).toBeVisible()` on **three consecutive isolated invocations** of its own file (`npx playwright test e2e/availability.spec.ts --project=chromium` → 3 passed / 1 failed, each time). The `[12-02]` and `[12-03]` entries above record it as a cross-file contention flake; it no longer needs company to fail.

  **Not caused by plan 12-06, and that was CHECKED rather than argued.** The plan's five component edits were reverted with `git checkout c801829 -- <the five files>` — leaving only the new `src/lib/design/live-regions.ts`, which no route imports — and the spec was re-run: **same 3 passed / 1 failed, same locator, same 5s timeout.** The five files were then restored with `git checkout HEAD --`. GATE-03's own browser evidence is unaffected: `hold-countdown.spec.ts` (4 passed, including the announce-once `toBe(1)`), `search-and-book.spec.ts:332` (the `HoldExpiredState` case this plan edits), `open-capacity.spec.ts` (the `DatePassPicker` surface this plan edits) and `shell.spec.ts` are all green — 28 passed in one targeted invocation.

  **The symptom points somewhere specific.** The dev-server log carries a React hydration mismatch inside `src/app/listings/[id]/(detail)/page.tsx:692`, on the paragraph whose text is chosen by `bookable`:

  ```
  > 692 |             <p className="text-center text-xs text-muted-foreground">
    693 |               {bookable
    694 |                 ? "You won't be charged yet."
    695 |                 : "You can browse now — booking opens once this space is ready."}
  ```

  A server/client disagreement on `bookable` for the not-payable listing is exactly the shape that would leave the `Not bookable yet` CTA off the hydrated tree. That is a rendering-correctness question on a public money-path route and belongs with the `[12-02]` `notFound()`-status entry, which is the other symptom of the same route's streaming behaviour — plausibly one investigation, and a Rule 4 architectural one either way (dev-vs-prod is the discriminator, per `[11-13]`/D-56: re-measure under `npm run build && npm start` before touching anything).

  **Not fixed here.** `(detail)/page.tsx` is outside this plan's declared surface, the failure predates the plan, and this plan's whole subject is an accessibility inventory.

- **[12-07] The mosaic's RENDERED geometry is measured by nothing, and the collapsed button predicate is measured only as a class string.** `tests/listing/photo-gallery.test.tsx` proves each declared template has exactly as many slots as it renders (`templateSlots()` — area arithmetic over the class strings), which is what makes "no empty cell at 3 and 4 photos" checkable without a browser. What it cannot do is confirm the compiled CSS lays those slots out inside one `MOSAIC_ASPECT` box: jsdom applies no Tailwind (D-131), and `e2e/photo-lightbox.spec.ts` deliberately reads no `boundingBox()`. Likewise the button's `sm:hidden` half — the control appearing at two photos on a phone and disappearing at five on a desktop — is asserted as a CLASS on an element found by role, never as a rendered visibility. **Owner: 12-11 (`e2e/mobile-booker-path.spec.ts`)**, which is already going to drive this route at 375px; one `toBeVisible()` on `Show all {N} photos` at 375 and one `toBeHidden()` at 1280 closes the second half, and a `boundingBox()` on the mosaic against the skeleton's closes the first. Not done here because this plan ships no viewport pass and adding one would duplicate the spec 12-11 owns.

- **[12-08] RESOLVED — `e2e/availability.spec.ts:261` is GREEN, and the `[11-13]` discriminator says the mismatch behind it was DEV-ONLY.** Recorded here so the three entries below that treat it as open are not read as current. Plan 12-08 ran the discriminator D-56 requires — `npm run build && npm start` versus `npm run dev`, same durable seeded not-payable listing, same theme seed, same probe, once with the `TooltipProvider` site RESTORED and once with it deleted:

  | tree | `npm run dev` | `npm run build && npm start` |
  |---|---|---|
  | the `TooltipProvider` + `tabIndex` span present (the shipped state) | `Hydration failed` **× 1** | **0** |
  | the site deleted (12-08) | **0** | **0** |

  So `[11-13]` **was a dev-mode artefact** — the same class as `[11-03]`, `[11-11](a)` and `[11-14]`, and the production build never had it. The fix is still correct on its own merits (the explanation is now readable on touch, on keyboard and to a screen reader without any interaction), but it repaired a **dev-only** symptom and 12-08's SUMMARY says exactly that rather than claiming a production defect. What it DID repair for real is the test: `availability.spec.ts:261` fails in dev because the regenerated tree drops the `Not bookable yet` button, and with the site gone it passes — **4 passed** on its own file, in dev, no flake.

- **[12-08] NOT RESOLVED, and now proven NOT dev-only either — `e2e/public-listing.spec.ts`'s draft-404 red survives a production build.** The same discriminator was pointed at the other standing red on this route, and it came back the other way. Against `npm start` on a production build: `npx playwright test e2e/public-listing.spec.ts --project=chromium` fails `a draft listing 404s to the public` at `Expected: 404 / Received: 200`, identically to dev, and `curl -s -o /dev/null -w "%{http_code}"` returns **200** for a real draft listing in BOTH modes while an unmatched URL (`/nope-not-a-route`) correctly returns **404** in both. The body is the `(detail)/not-found.tsx` boundary in both modes, so T-05-NONPUB's substance still holds and it remains a status-line defect only.

  **This is the finding worth carrying forward:** the `[12-02]` and `[12-06]` entries below both suggested the two reds were plausibly ONE investigation of this route's streaming behaviour. They are not. One disappears in production and one does not, so the notFound()-status defect is a real rendering-strategy question that needs its own decision (force the route non-streaming, or move the published gate into `generateMetadata`/a segment config) and cannot be closed by anything that fixes a hydration boundary. Still a Rule 4 architectural call on a public money-path route, still outside 12-08's declared surface.

  **Consequence for anyone editing `e2e/public-listing.spec.ts`:** the file is `mode: "serial"`, so this red skips every test declared after it. 12-08's two new cases are declared ABOVE it for that reason alone, with the reason at the site; the three D-59 cases below it still only run under `--grep "D-59"`.

- **[12-08] `src/components/availability/slot-picker.tsx` still imports the tooltip primitive, and deleting it is a product decision this plan could not make.** 12-08's acceptance criterion asks that `grep -rln "components/ui/tooltip" src/components/{search,listing,availability,booking} src/app/listings` return nothing. Measured after the listing page's site was deleted, it returns **two** files, with different dispositions:

  - `src/components/listing/photo-uploader.tsx` — a **HOST** surface (the listing wizard's photo step). It is not on the booker path at all; the criterion's directory glob is simply wider than the property it names ("no tooltip on the booker path"). Nothing to do.
  - `src/components/availability/slot-picker.tsx:169-211` — genuinely the booker path, genuinely a tooltip. Every UNAVAILABLE hour chip is wrapped in a `TooltipTrigger` whose content is `tooltipFor(state, mode)`: the short reason PLUS the notice requirement when there is one. **Not deleted here**, for three reasons that compound: the file is outside this plan's `files_modified`; the chips already carry the short reason in their `aria-label`, so a screen reader is served and only sighted pointer users would lose anything; and the notice-requirement half has **nowhere else to go** on a wrapping grid of 44px chips — a static line per chip is not a layout that exists. Replacing it needs a copy/placement decision, which is exactly what a plan whose declared subject is the page's information architecture should not make on another plan's surface.

  **Owner: 12-09**, which already opens this file for `SLOT_CHIP_BOX`. The question to answer there: where does "why this hour is unavailable, including the notice requirement" live once the hover is gone — a single day-panel line naming the states present, or an expansion of the `aria-label` into visible text. Until then, the anti-pattern list's *"D-56 deletes the last one"* is **false**: D-56 deleted the last one on the LISTING PAGE.

- **[12-07] `e2e/availability.spec.ts:261` and `e2e/public-listing.spec.ts:254` are BOTH still standing reds, re-measured in isolation.** `npx playwright test e2e/public-listing.spec.ts e2e/availability.spec.ts --project=chromium` → 4 passed / 2 failed, the two named above, with no other spec in the invocation. Unchanged by this plan and consistent with the `[12-02]` and `[12-06]` entries. Recorded again only because 12-07 edits `photo-gallery.tsx`, which `public-listing.spec.ts` renders — and its *"a published listing is viewable by an anonymous visitor"* case, which drives the new mosaic on a three-photo listing, **passes**, so the draft-404 red is not the gallery's.
