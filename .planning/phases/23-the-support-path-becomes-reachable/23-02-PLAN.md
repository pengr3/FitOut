---
phase: 23-the-support-path-becomes-reachable
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/lib/notifications.ts
  - src/lib/payments/confirm-booking-payment.ts
  - src/inngest/functions/request-expiry.ts
  - src/inngest/functions/reminders.ts
  - src/app/(app)/bookings/[id]/group/page.tsx
  - src/app/actions/booking.ts
  - src/app/actions/cancel-booking.ts
  - src/app/actions/re-request.ts
  - src/app/actions/group.ts
  - src/app/actions/host-requests.ts
  - src/app/actions/paymongo-connect.ts
  - src/lib/verification/providers/didit.ts
  - tests/auth/public-origin-callers.test.ts
autonomous: true
requirements: [STATE-05, TRUST-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Booking, notification, group invite, host request, payout onboarding, and Didit return links use the same canonical or exact Preview public origin.
    - A Vercel Preview cannot emit a localhost callback, email CTA, notification destination, payment return URL, or hosted-verification callback.
    - Public callback receivers remain the existing signed endpoints; no new route, event subscription, secret, or schema is introduced.
  artifacts:
    - path: tests/auth/public-origin-callers.test.ts
      provides: An explicit inventory test for every runtime public-origin caller changed in this phase.
    - path: src/lib/notifications.ts
      provides: Central-origin notification destinations for both in-app and email channels.
    - path: src/app/actions/paymongo-connect.ts
      provides: Canonical return/refresh destinations for PayMongo hosted onboarding.
  key_links:
    - from: src/lib/app-origins.ts
      to: notification, booking, and verification callers
      via: absolutePublicUrl or PUBLIC_APP_ORIGIN imports replace per-file environment fallback logic.
      pattern: absolutePublicUrl
    - from: notification payloads
      to: src/lib/email.ts
      via: One absolute href is projected into both in-app and Resend email channels.
      pattern: href
---

<objective>
Expand the proven origin slice to every runtime public-link caller so emails, payment returns,
notifications, hosted verification, and group invitations cannot drift from the domain topology.

Purpose: D-11 applies to every user-visible or provider-facing URL, not only Better Auth and metadata.
Output: One tested inventory of public-origin consumers and refactored callers that delegate to the
authority delivered by Plan 23-01.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-the-support-path-becomes-reachable/23-CONTEXT.md
@.planning/phases/23-the-support-path-becomes-reachable/23-RESEARCH.md
@.planning/phases/23-the-support-path-becomes-reachable/23-PATTERNS.md
@.planning/phases/23-the-support-path-becomes-reachable/23-01-PLAN.md

<interfaces>
From Plan 23-01:
- PUBLIC_APP_ORIGIN resolves canonical Production configuration or one exact Vercel Preview authority.
- absolutePublicUrl accepts an absolute pathname and returns a public absolute URL.

Existing public-link data flows:
- Notification payload.href is shared by durable in-app notifications and email.
- Booking and PayMongo actions pass absolute success, cancel, refresh, and return URLs to external services.
- Didit starts a hosted session with FitOut callback/redirect destinations.
</interfaces>
</context>

## Artifacts this phase produces

- One source-tested public-origin caller inventory.
- Canonical/Preview-safe booking, notification, hosted-verification, group-invite, and payment-onboarding destinations.
- No new webhook implementation, provider SDK, dependency, or migration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move notification and scheduled-email destinations to the shared public origin</name>
  <files>src/lib/notifications.ts, src/lib/payments/confirm-booking-payment.ts, src/inngest/functions/request-expiry.ts, src/inngest/functions/reminders.ts, tests/auth/public-origin-callers.test.ts</files>
  <behavior>
    - In canonical production mode, every affected notification href begins with https://fitout.live.
    - In exact Preview mode, the same paths begin with https:// plus the configured VERCEL_URL.
    - Notification payloads remain absolute and preserve their existing pathname/query semantics.
  </behavior>
  <read_first>
    - src/lib/app-origins.ts — Plan 23-01 public-origin exports and validation behavior.
    - src/lib/notifications.ts — shared in-app/email payload contract.
    - src/lib/payments/confirm-booking-payment.ts, src/inngest/functions/request-expiry.ts, and src/inngest/functions/reminders.ts — booking and scheduled notification call sites.
    - tests/booking/cancellation.test.ts — absolute notification href contract.
  </read_first>
  <action>
    Begin by adding an inventory test that isolates production and Preview environments, proves the
    shared helper output, and positively names the four callers in this task. Then replace each
    caller's local base construction with the Plan 23-01 authority, retaining its existing path,
    query parameters, event type, transaction ordering, error handling, and notification payload
    shape. Per D-01, D-10, and D-11, canonical production links must use fitout.live while a Preview
    uses only its exact VERCEL_URL. Do not alter payment confirmation ownership, Inngest scheduling,
    or notification retry behavior.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/auth/public-origin-callers.test.ts</automated>
    <fails_when>The command exits non-zero, an inventoried caller bypasses the shared authority, or production and Preview href expectations diverge.</fails_when>
  </verify>
  <acceptance_criteria>Every affected notification and scheduled-email CTA remains absolute and uses the same public-origin decision as Better Auth.</acceptance_criteria>
  <done>The first inventory segment is green and its runtime public destinations cannot obtain a local Preview host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make booking, cancellation, request, and group-page links share the public-origin contract</name>
  <files>src/app/(app)/bookings/[id]/group/page.tsx, src/app/actions/booking.ts, src/app/actions/cancel-booking.ts, src/app/actions/re-request.ts, tests/auth/public-origin-callers.test.ts</files>
  <behavior>
    - Group invitation URLs, checkout success/cancel paths, and emitted booking/request notification links preserve their current route and query values.
    - Each result is canonical in Production and exact-hosted in Preview mode.
    - No user-supplied host value can select the origin.
  </behavior>
  <read_first>
    - src/lib/app-origins.ts — public URL helper signature.
    - src/app/(app)/bookings/[id]/group/page.tsx — owner-scoped bearer invite construction.
    - src/app/actions/booking.ts, cancel-booking.ts, and re-request.ts — payment and notification side-effect ordering.
    - tests/auth/public-origin-callers.test.ts — first inventory segment and environment isolation.
  </read_first>
  <action>
    Extend the inventory test RED with these four call-site families, asserting only their origin is
    replaced while route structure stays as shipped. Refactor them to consume the shared authority
    from Plan 23-01. Per D-01 and D-11, keep canonical public URLs on fitout.live and exact Preview
    URLs on VERCEL_URL; never derive them from request headers, browser input, or a second environment
    parser. Preserve bearer-token confidentiality for group links, all booking status/transaction
    conditions, and the existing post-commit notification order.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/auth/public-origin-callers.test.ts</automated>
    <fails_when>The command exits non-zero, a converted booking/group caller changes its path/query contract, or Preview link output loses the generated deployment authority.</fails_when>
  </verify>
  <acceptance_criteria>Every affected booking and group flow keeps its existing destination semantics while obtaining origin solely from the shared trusted resolver.</acceptance_criteria>
  <done>The inventory includes and verifies all four booking/group caller families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align host, PayMongo onboarding, and Didit hosted-session destinations with production topology</name>
  <files>src/app/actions/group.ts, src/app/actions/host-requests.ts, src/app/actions/paymongo-connect.ts, src/lib/verification/providers/didit.ts, tests/auth/public-origin-callers.test.ts</files>
  <behavior>
    - Host notification links, group email links, PayMongo return/refresh URLs, and Didit callback/redirect URLs share canonical/Preview public-origin behavior.
    - Existing PayMongo and Didit secrets, event scope, signing, request body, and response parsing do not change.
  </behavior>
  <read_first>
    - src/lib/app-origins.ts — exported public-origin API.
    - src/app/actions/group.ts, host-requests.ts, and paymongo-connect.ts — notification and provider return contracts.
    - src/lib/verification/providers/didit.ts — hosted session callback and redirect construction.
    - tests/auth/public-origin-callers.test.ts — completed inventory segments.
  </read_first>
  <action>
    Add RED cases for the remaining public-origin callers, then replace their per-file origin fallbacks
    with the Plan 23-01 contract. Per D-01, D-03, D-10, D-11, and D-12, public routes use the
    canonical host in Production and one exact Preview host outside it; staff-only destinations remain
    on OPS_APP_URL where the existing code already requires them. Preserve PayMongo onboarding policy,
    Didit session semantics, the Didit status.updated webhook scope, provider signing keys, and all
    established logging/redaction rules. Do not create or change a receiver route in this task.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/auth/public-origin-callers.test.ts tests/verification/didit-session.test.ts</automated>
    <fails_when>The command exits non-zero, a remaining runtime caller bypasses the public-origin authority, or the Didit session contract no longer passes its focused behavior test.</fails_when>
  </verify>
  <acceptance_criteria>All discovered runtime public-origin call sites are inventoried and consistently resolve canonical Production or exact Preview destinations.</acceptance_criteria>
  <done>The complete public-link inventory is green without changing provider callback signing, event scope, secrets, dependencies, or schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server actions/jobs to browser and provider URLs | Absolute destinations leave FitOut in email, hosted checkout, and hosted verification flows. |
| Notification payload to two channels | One href reaches both an in-app surface and an email client. |
| Provider callback/return configuration | External providers retain secrets and signed inbound receiver behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-04 | Spoofing | public URL call sites | high | mitigate | Replace local per-file fallback logic with the validated public-origin authority and test every inventoried caller. |
| T-23-05 | Tampering | PayMongo/Didit integrations | high | mitigate | Preserve existing signature, event-scope, and secret contracts while changing only trusted outbound/public destinations. |
| T-23-06 | Information Disclosure | email and invitation URLs | high | mitigate | Keep token-bearing path construction server-side and retain existing redacted logging behavior. |
| T-23-SC | Tampering | package supply chain | low | accept | No package manager, SDK addition, or dependency declaration change is in scope. |
</threat_model>

<verification>
- node node_modules/vitest/vitest.mjs run tests/auth/public-origin-callers.test.ts
- node node_modules/vitest/vitest.mjs run tests/auth/public-origin-callers.test.ts tests/verification/didit-session.test.ts
</verification>

<success_criteria>
- No runtime public link can fall back to localhost in a Vercel Preview.
- All existing booking and provider URL paths retain their route/query contracts.
- Existing signed callback receivers and provider secret ownership remain unchanged.
</success_criteria>

<output>
Create .planning/phases/23-the-support-path-becomes-reachable/23-02-SUMMARY.md when done.
</output>
