---
phase: 23-the-support-path-becomes-reachable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site.ts
  - src/lib/email.ts
  - .env.example
  - tests/auth/email-dev-fallback.test.ts
  - tests/design/site-contacts.test.ts
  - src/lib/app-origins.ts
  - src/app/layout.tsx
  - src/app/(ops)/ops/error.tsx
  - tests/auth/secret-config.test.ts
  - tests/design/scaffold-residue.test.ts
autonomous: true
requirements: [STATE-05, TRUST-01]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - A booker can discover the monitored launch inbox from every already-guarded booking and trust surface.
    - Every existing Resend email has the same support inbox as Reply-To without an address copy in a template.
    - Production resolves the canonical public host and distinct ops host exactly, while a Vercel Preview resolves only its own exact VERCEL_URL authority.
    - Metadata and the ops-to-public escape link cannot expose a localhost URL in a Vercel Preview.
  artifacts:
    - path: src/lib/site.ts
      provides: Sole published support-address owner.
    - path: src/lib/email.ts
      provides: Singleton Resend payload with support-derived Reply-To.
    - path: src/lib/app-origins.ts
      provides: Validated production, Preview, and local public-origin resolution.
    - path: src/app/layout.tsx
      provides: Metadata base from the shared public-origin authority.
  key_links:
    - from: src/lib/site.ts
      to: src/lib/email.ts
      via: SUPPORT_EMAIL supplies replyTo in the sole resend.emails.send payload.
      pattern: SUPPORT_EMAIL
    - from: src/lib/app-origins.ts
      to: src/lib/auth.ts
      via: PUBLIC_APP_ORIGIN, AUTH_ALLOWED_HOSTS, and AUTH_TRUSTED_ORIGINS.
      pattern: AUTH_ALLOWED_HOSTS
    - from: src/lib/app-origins.ts
      to: src/app/layout.tsx
      via: PUBLIC_APP_ORIGIN becomes metadataBase.
      pattern: metadataBase
---

<objective>
Deliver the first production-quality vertical slice: one owned support address flows through the shared
mail transport, and one canonical/Preview origin authority flows through Better Auth and metadata.

Purpose: Prevent support-source drift and Preview localhost leaks before the production Vercel and provider
cutover.
Output: Support/Reply-To implementation, Preview-safe origin resolution, targeted tests, and non-secret
environment documentation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-the-support-path-becomes-reachable/23-CONTEXT.md
@.planning/phases/23-the-support-path-becomes-reachable/23-RESEARCH.md
@.planning/phases/23-the-support-path-becomes-reachable/23-PATTERNS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-02-SUMMARY.md

<interfaces>
- SUPPORT_EMAIL in src/lib/site.ts is the only published support-address owner.
- send(to, subject, html, text, options) in src/lib/email.ts is the only Resend send path.
- Installed Resend 6.12.4 accepts EmailOptions.replyTo as a string or string array.
- src/lib/app-origins.ts exports PUBLIC_APP_ORIGIN, OPS_APP_ORIGIN, exact Better Auth lists, and absolute URL helpers.
</interfaces>
</context>

## Artifacts this phase produces

- A sole support/Reply-To implementation in src/lib/site.ts and src/lib/email.ts.
- A production/Preview origin authority and metadata contract in src/lib/app-origins.ts and src/app/layout.tsx.
- Regression coverage for delivery safety and exact Vercel Preview authority.

## Source audit

| Source | Item | Plan/task | Status |
|---|---|---|---|
| GOAL | Help is reachable from every booking/payment state | 01-01, 03-03 | COVERED |
| REQ | STATE-05 | 01-01, 02-01..03, 03-03 | COVERED |
| REQ | TRUST-01 | 01-01, 03-03 | COVERED |
| RESEARCH | One Resend transport, exact host authority, no dependencies/migrations | 01-01, 01-02, 02-01..03 | COVERED |
| RESEARCH | Vercel/Resend/provider cutover and redacted live proof | 03-01..03 | COVERED |
| CONTEXT | D-01 through D-04 public/support topology | 01-01, 01-02, 03-01 | COVERED |
| CONTEXT | D-07 through D-09 sender, DNS, and Reply-To | 01-01, 03-01 | COVERED |
| CONTEXT | D-10 through D-12 environment and callback configuration | 01-02, 02-01..03, 03-01..02 | COVERED |
| CONTEXT | D-13 through D-15 live proof and inbox replacement | 03-03 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one password-reset email from the support owner through the Resend payload</name>
  <files>src/lib/site.ts, src/lib/email.ts, .env.example, tests/auth/email-dev-fallback.test.ts, tests/design/site-contacts.test.ts</files>
  <behavior>
    - A configured Resend client receives one reset payload whose Reply-To equals SUPPORT_EMAIL.
    - pengr.clmc.3@gmail.com exists only at SUPPORT_EMAIL; site and email-shell consumers continue to import it.
    - The non-null SUPPORT_EMAIL branch exposes every guarded booking, payment-state, and trust-surface support affordance from that sole constant.
    - Production with no Resend key fails safely and logs neither recipient data nor bearer URLs.
  </behavior>
  <read_first>
    - src/lib/site.ts — D-26 single-owner contract.
    - src/lib/email.ts — singleton send helper, staff invitation host validation, and safe failure path.
    - tests/auth/email-dev-fallback.test.ts — module-reset and redacted-log test pattern.
    - tests/design/site-contacts.test.ts — structural support-affordance guard for the non-null source branch.
    - node_modules/resend/dist/index.d.cts — installed replyTo contract.
    - node_modules/next/dist/docs/01-app/02-guides/environment-variables.md — server-only and build-time public values.
  </read_first>
  <action>
    Start RED by extending the existing mocked transport test to capture the sole Resend email payload and
    require Reply-To from SUPPORT_EMAIL, and by extending the existing design guard so its non-null
    SUPPORT_EMAIL branch requires every guarded booking, payment-state, and trust-surface support
    affordance to import the same sole constant. Per D-01 and D-02 for the support decision, set the one
    declaration in src/lib/site.ts to pengr.clmc.3@gmail.com and update its replacement instruction:
    change that declaration, deploy, and repeat the live delivery/reply proof. Import SUPPORT_EMAIL in
    src/lib/email.ts and add the installed SDK's replyTo option only to the existing send call; no
    template may choose an address. Move email CTA construction onto the shared public-origin contract.
    Per D-07 and D-09, keep Resend as the only transport, preserve safe error logging, and retain
    OPS_APP_ORIGIN validation for staff invitations. Document EMAIL_FROM=FitOut &lt;updates@send.fitout.live&gt;
    in .env.example and state that RESEND_API_KEY is server-only; no credential enters the repository.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/site-contacts.test.ts</automated>
    <fails_when>The command exits non-zero or the non-null SUPPORT_EMAIL branch does not expose every guarded booking, payment-state, and trust-surface support affordance from the sole constant.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run tests/auth/email-dev-fallback.test.ts</automated>
    <fails_when>The command exits non-zero, the sole payload lacks the support-derived Reply-To, or the production no-key branch records a bearer token.</fails_when>
  </verify>
  <acceptance_criteria>The monitored Gmail address has one code owner; every existing Resend send inherits it as Reply-To; and every guarded booking, payment-state, and trust-surface support affordance is exposed from that same source.</acceptance_criteria>
  <done>One real application mail path and every guarded support surface are proven from the source-owned support value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make canonical, ops, and Preview origins one validated authority</name>
  <files>src/lib/app-origins.ts, src/app/layout.tsx, src/app/(ops)/ops/error.tsx, tests/auth/secret-config.test.ts, tests/design/scaffold-residue.test.ts</files>
  <behavior>
    - Production resolves https://fitout.live while OPS_APP_URL resolves https://ops.fitout.live and remains distinct.
    - A Preview without production-only public values uses exactly https:// plus VERCEL_URL for public links, Better Auth lists, and metadata.
    - An invalid authority never becomes trusted; non-Vercel local development retains explicit local origins.
    - The client ops-error exit uses Production NEXT_PUBLIC_APP_URL when supplied and its browser authority in Preview mode.
  </behavior>
  <read_first>
    - src/lib/app-origins.ts and src/lib/auth.ts — parser, exact host classification, and Better Auth consumers.
    - src/app/layout.tsx and src/app/(ops)/ops/error.tsx — independent metadata and client escape resolution.
    - tests/auth/secret-config.test.ts, tests/auth/ops-host-routing.test.ts, and tests/design/scaffold-residue.test.ts — cold-import, host/cookie isolation, and metadata test patterns.
    - node_modules/next/dist/docs/01-app/02-guides/environment-variables.md — public-variable build-time behavior.
    - node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md — static metadata export behavior.
  </read_first>
  <action>
    Start with failing production, exact Preview, malformed-authority, and local-development cases. Per
    D-01, D-02, D-03, D-10, and D-11, retain the existing absolute-origin parser and exact host
    lists, but resolve explicit public configuration first, then a valid exact VERCEL_URL Preview
    authority. Permit the local fallback only outside Vercel deployment context; a Vercel deployment
    without a usable public authority must fail configuration instead of generating a local URL. Export
    the resolved public origin, make the root layout use it for metadataBase, and keep the client
    error boundary safe by falling back to its real browser authority only when the production public
    variable is absent. Preserve no wildcard/suffix trust, host-only public/ops cookies, Proxy routing,
    and every server-side staff authorization layer. Extend focused tests across production and Preview
    modes, including the existing host-routing check that asserts public and ops retain host-only session
    configuration rather than a cross-subdomain cookie scope.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/auth/secret-config.test.ts tests/auth/ops-host-routing.test.ts</automated>
    <fails_when>The command exits non-zero, a production or Preview path resolves to localhost, an unallowlisted host becomes trusted, a Preview does not use its exact VERCEL_URL authority, public and ops hosts converge, or cookie configuration permits cross-subdomain session sharing.</fails_when>
    <automated>node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/scaffold-residue.test.ts</automated>
    <fails_when>Either command exits non-zero, Preview resolves an authority other than its exact VERCEL_URL, an unallowlisted host enters Better Auth, or Preview metadata falls back to a local URL.</fails_when>
  </verify>
  <acceptance_criteria>Production, public, and ops remain distinct exact hosts; neither configured code nor cookie configuration enables cross-subdomain session sharing; and Preview uses its intended exact authority without weakening Phase 20 host isolation.</acceptance_criteria>
  <done>Focused tests cover canonical and Preview origin behavior; no dependency or migration was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Deployment configuration to Better Auth/metadata | Variables select browser-facing absolute origins. |
| Application to Resend | Sender and Reply-To affect outbound identity and customer response routing. |
| Browser host to routing/auth | Host is caller-controlled and must match exact configured authorities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-01 | Spoofing | app-origins.ts and Better Auth | high | mitigate | Use validated exact public, ops, and one Preview authority; retain trustedProxyHeaders false and no wildcard matching. |
| T-23-02 | Information Disclosure | email.ts transport/logging | high | mitigate | Set Reply-To in the singleton payload while retaining token-withholding production failures and redacted provider errors. |
| T-23-03 | Spoofing | layout metadata and ops error boundary | medium | mitigate | Use PUBLIC_APP_ORIGIN server-side and current browser authority only for a Preview client fallback. |
| T-23-SC | Tampering | package supply chain | low | accept | This plan runs no package-manager action and changes no dependency declaration. |
</threat_model>

<verification>
- node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/site-contacts.test.ts
- node node_modules/vitest/vitest.mjs run tests/auth/email-dev-fallback.test.ts
- node node_modules/vitest/vitest.mjs run tests/auth/secret-config.test.ts tests/auth/ops-host-routing.test.ts
- node node_modules/vitest/vitest.mjs run --config vitest.design.config.ts tests/design/scaffold-residue.test.ts
</verification>

<success_criteria>
- STATE-05 and TRUST-01 have an honest monitored support source ready for public rendering and mail replies.
- fitout.live, ops.fitout.live, and exact Preview hosts resolve through one tested authority.
- No dependency, migration, mailbox product, inbound-email receiver, or helpdesk is added.
</success_criteria>

<output>
Create .planning/phases/23-the-support-path-becomes-reachable/23-01-SUMMARY.md when done.
</output>
