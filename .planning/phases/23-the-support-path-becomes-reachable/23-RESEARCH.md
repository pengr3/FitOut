# Phase 23: The Support Path Becomes Reachable - Research

**Researched:** 2026-09-11
**Domain:** Vercel production-domain topology, transactional email, origin/callback configuration
**Confidence:** MEDIUM

## User Constraints (from CONTEXT.md)

### Locked Decisions

- **D-01:** `https://fitout.live` is FitOut's canonical public production origin. It is the value for
  customer-facing links, canonical metadata, Better Auth's fallback origin, payment return URLs, and
  provider webhook destinations.
- **D-02:** `www.fitout.live` is assigned to the Vercel project solely to redirect to
  `https://fitout.live`; it is never a second canonical application origin.
- **D-03:** `https://ops.fitout.live` remains a distinct production host for the existing staff
  surface. It must be assigned to the same Vercel project and supplied through `OPS_APP_URL`; it must
  not share marketplace session cookies or become a public-site alias.
- **D-04:** Vercel is the authoritative DNS and deployment control plane for the purchased domain.
  Keep its website-routing records intact while adding only the exact third-party mail-authentication
  records generated by Resend. Do not commit DNS values, provider tokens, or screenshots containing
  credentials.

- **D-01:** `pengr.clmc.3@gmail.com` is the real, monitored launch support address. It is not a
  placeholder: it may be published in the app and receive replies until a dedicated account exists.
- **D-02:** The future dedicated support account replaces that address at the one existing
  `SUPPORT_EMAIL` declaration. The outbound mail reply address must read the same source, so the
  future swap cannot leave the website and transactional mail pointing at different inboxes.

- **D-07:** Resend remains the sole outbound transport. Verify the dedicated sending subdomain
  `send.fitout.live` in Resend and send production mail as `FitOut <updates@send.fitout.live>`.
  This keeps sender reputation separate from the public/ops hosts while remaining visibly FitOut.
- **D-08:** Copy the exact SPF, DKIM, and required MX records that Resend displays for
  `send.fitout.live` into Vercel DNS; add a monitoring DMARC record for the organisational domain if
  it does not conflict with an existing record. Do not invent or copy sample values from a document.
- **D-09:** Every transaction through the shared Resend transport sets `Reply-To` to the monitored
  `SUPPORT_EMAIL`. Gmail is the reply destination; Resend inbound-email webhooks are not a mailbox
  replacement and remain out of scope.

- **D-10:** Configure the public and mail values as Vercel **Production-only** variables:
  `BETTER_AUTH_URL=https://fitout.live`, `NEXT_PUBLIC_APP_URL=https://fitout.live`,
  `OPS_APP_URL=https://ops.fitout.live`, `EMAIL_FROM=FitOut <updates@send.fitout.live>`, and the
  server-only `RESEND_API_KEY`. Production variables never leak through a `NEXT_PUBLIC_` key.
- **D-11:** Preview deployments must retain their exact generated `VERCEL_URL` host behavior and must
  not impersonate `fitout.live`. The plan must remove any localhost fallback that can leak into a
  Vercel preview's metadata, links, or Better Auth origin while preserving exact-host allow-listing and
  the public-default fail-closed routing rule.
- **D-12:** Audit every external configuration that stores a public URL, then update and prove only the
  relevant entries: Google OAuth production origin/callback, PayMongo webhook, Didit webhook, and
  Inngest serve endpoint where configured. Cloudinary and Neon have no domain callback to change.
  The production public host is `fitout.live`; staff-only links remain `ops.fitout.live`.

- **D-13:** Completion requires Vercel domain health, Resend verification, real delivery to the monitored Gmail inbox, and a real reply arriving
  there, not only a green unit test or Resend dashboard status.
- **D-14:** Production proof covers the public auth path, a booking/notification path, staff invitation
  when safely available, and each configured external webhook's authenticated receipt without using a
  bearer URL or real customer data as evidence.
- **D-15:** The manual handoff records the safe future-inbox replacement procedure: create and monitor
  the dedicated inbox, change `SUPPORT_EMAIL` once, deploy, repeat the delivery/reply walk, and only
  then retire the launch Gmail address.

### Codex's Discretion

- Select the smallest focused test set and redacted evidence format that prove the domain topology,
  sender/Reply-To contract, callback destinations, and preview-versus-production origin behavior.
- Choose the exact Vercel UI or CLI sequence, provided no secret is written to the repository and DNS
  records are copied only from the live provider dashboards.

### Deferred Ideas (OUT OF SCOPE)

- A dedicated `support@fitout.live` mailbox or Google Workspace: replace the launch Gmail address using
  D-02 after the account exists.
- Shared-inbox workflows, ticketing, inbound Resend webhooks, and automatic support triage: separate
  support-product work, not needed for a monitored launch address.
- Moving the public canonical host to `www.fitout.live`: Vercel recommends that topology, but this
  phase retains the user-chosen `fitout.live` public address and a one-way `www` redirect.

## Project Constraints (from AGENTS.md)

- This repository uses a breaking-change version of Next.js; read relevant guidance in `node_modules/next/dist/docs/` and heed deprecation notices before changing Next.js code. [VERIFIED: AGENTS.md:1-5]

## Phase Requirements

| ID | Description | Research Support |
|---|---|---|
| STATE-05 | A booker can find a real support path from any booking/payment state; no placeholder may be published. [VERIFIED: .planning/REQUIREMENTS.md:254-256] | Set the one existing constant, retain the guarded surfaces, and make the mail transport reply to it. |
| TRUST-01 | The same real support path is reachable from trust surfaces that owe it. [VERIFIED: .planning/REQUIREMENTS.md:257-258] | The existing source-scan guard and real delivery/reply evidence complete the requirement. |

## Summary

This is an end-to-end production integration phase, not a mailbox build. Assign apex, `www`, and `ops` to the existing Vercel project; preserve website DNS; add only Resend records generated for `send.fitout.live`; set Production-only values; and update the provider URLs that retain FitOut's public origin. Vercel documents domain assignment, redirects, DNS, and separate Production/Preview environments. [CITED: https://vercel.com/docs/domains/working-with-domains] [CITED: https://vercel.com/docs/domains/working-with-domains/deploying-and-redirecting] [CITED: https://vercel.com/docs/environment-variables]

The previous plan covers only support and outbound mail. The code has the correct seams—one support constant, one Resend transport, and an exact-origin model—but Preview currently can reach localhost fallbacks. The replacement plan must make support/Reply-To share one owner, make Preview use only exact `VERCEL_URL`, configure all callback dashboards, and capture redacted production proof.

**Primary recommendation:** Replace the mail-only plan with three ordered plans: code-and-regression coverage; human-operated Vercel/Resend/provider configuration; then production proof and future-inbox handoff. No new dependency, inbound-email system, database change, or migration belongs in Phase 23.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|---|---|---|---|
| Public canonical host, `www` redirect, and ops host | CDN / Static | Frontend Server | Vercel owns domain assignment, DNS, TLS, and redirects; Next/Better Auth consume exact hosts. [CITED: https://vercel.com/docs/domains/working-with-domains/deploying-and-redirecting] |
| Public/ops/preview origin selection | Frontend Server | Browser / Client | Server constructs links/auth URLs; `NEXT_PUBLIC_APP_URL` is build-time metadata. [CITED: https://nextjs.org/docs/app/guides/environment-variables] |
| Transactional From and Reply-To | API / Backend | External email service | Singleton server transport calls Resend; Gmail receives replies. [CITED: https://resend.com/docs/api-reference/emails/send-email] |
| OAuth and signed callbacks | API / Backend | External providers | Better Auth, PayMongo, Didit, and Inngest receive/authenticate at server routes. [CITED: https://better-auth.com/docs/reference/options] [CITED: https://docs.paymongo.com/docs/creating-a-webhook-endpoint] [CITED: https://docs.didit.me/integration/webhooks] [CITED: https://www.inngest.com/docs/deploy/vercel] |

## Standard Stack

### Core

| Component | Purpose | Why Standard |
|---|---|---|
| Existing Next.js application | Public, ops, and metadata surfaces | Next.js documents that public variables are inlined at build time. [CITED: https://nextjs.org/docs/app/guides/environment-variables] |
| Existing Better Auth | Dynamic URL and browser-origin validation | Explicit allowed hosts and trusted origins fit this three-host topology. [CITED: https://better-auth.com/docs/reference/options] |
| Existing Resend transport | Transactional delivery | Resend documents custom `from` and `reply_to`, plus domain SPF/DKIM. [CITED: https://resend.com/docs/api-reference/emails/send-email] [CITED: https://resend.com/docs/dashboard/domains/introduction] |
| Existing Inngest serve route | Durable jobs | Inngest documents `/api/inngest` on Vercel and `INNGEST_SERVE_ORIGIN`. [CITED: https://www.inngest.com/docs/deploy/vercel] |

### Supporting

| Component | Purpose | When to Use |
|---|---|---|
| Vercel Domains/DNS | host assignment, DNS, TLS, redirects | Add all three hosts to the same project and manage mail records in Vercel DNS. [CITED: https://vercel.com/docs/domains/working-with-domains] |
| Google OAuth dashboard | production authorized origin/callback | Amend the live client after the public host is healthy. [CITED: https://better-auth.com/docs/reference/options] |
| PayMongo + Didit dashboards | production webhook destinations | Update existing destination only; retain event scope and secret. [CITED: https://docs.paymongo.com/docs/creating-a-webhook-endpoint] [CITED: https://docs.didit.me/integration/webhooks] |

**Installation:** None. Do not run `npm install`.

## Architecture Patterns

### System Architecture Diagram

```mermaid
flowchart LR
  B[Browser] --> A[fitout.live canonical public app]
  B --> W[www.fitout.live]
  W -->|redirect| A
  S[Staff browser] --> O[ops.fitout.live host-only ops]
  A --> V[Vercel production deployment]
  O --> V
  V --> R[Resend: updates@send.fitout.live]
  R --> M[Recipient mailbox]
  M -->|Reply-To| G[pengr.clmc.3@gmail.com]
  P[PayMongo] -->|signed POST| PM[/api/paymongo/webhook]
  D[Didit] -->|signed POST| DW[/api/didit/webhook]
  I[Inngest] -->|signed request| IW[/api/inngest]
```

### Recommended Project Structure

```text
src/lib/site.ts                    # sole published support address
src/lib/email.ts                   # sole Resend send payload
src/lib/app-origins.ts             # exact public / ops / preview origins
src/app/layout.tsx                 # metadata base
tests/auth/                        # origin and delivery safety tests
tests/design/site-contacts.test.ts # support-affordance guard
```

### Pattern 1: One server-only origin model

Use one `app-origins.ts` resolver for public production, ops production, and the exact Vercel Preview authority. Existing code already has exact allow-listing, but derives public origin with a localhost fallback before considering Preview.

DATA_m5Qx8TbV_START
```ts
export const SUPPORT_EMAIL: string | null = null;
```
DATA_m5Qx8TbV_END

[VERIFIED: src/lib/site.ts:66-70]

DATA_Y8vT2KrL_START
```ts
[publicUrl.host.toLowerCase(), opsUrl.host.toLowerCase(), previewUrl?.host.toLowerCase()]
```
DATA_Y8vT2KrL_END

[VERIFIED: src/lib/app-origins.ts:91-98]

Refactor ordering so a Vercel Preview with `VERCEL_URL` uses that exact HTTPS origin for `PUBLIC_APP_ORIGIN`, auth allow/trust lists, absolute public links, and metadata. Retain localhost only for non-Vercel local development. [VERIFIED: src/lib/app-origins.ts:39-48] [VERIFIED: src/lib/app-origins.ts:79-117] [VERIFIED: src/app/layout.tsx:96-106]

### Pattern 2: One transport payload for Reply-To

Read `SUPPORT_EMAIL` in the existing private `send()` helper and supply it once; do not make individual templates choose an inbox.

DATA_p7Lr4NwQ_START
```ts
const { error } = await resend.emails.send({ from: FROM, to, subject, html, text });
```
DATA_p7Lr4NwQ_END

[VERIFIED: src/lib/email.ts:41-43,76-84]

Resend documents the API parameter as `reply_to`; confirm the installed SDK's camel-case property (`replyTo`) in its type definition before code changes. [CITED: https://resend.com/docs/api-reference/emails/send-email] [ASSUMED]

### Pattern 3: Provider dashboards target deployed receiver routes

| Provider | Production destination | Existing boundary |
|---|---|---|
| Google OAuth | `https://fitout.live` and `https://fitout.live/api/auth/callback/google` | Better Auth owns callback/origin validation. [CITED: https://better-auth.com/docs/reference/options] |
| PayMongo | `https://fitout.live/api/paymongo/webhook` | Route requires signed raw-body input and production secret. [VERIFIED: src/app/api/paymongo/webhook/route.ts:31-41] |
| Didit | `https://fitout.live/api/didit/webhook` | Destination uses its per-destination secret; signed receipt is required. [CITED: https://docs.didit.me/integration/webhooks] |
| Inngest | `INNGEST_SERVE_ORIGIN=https://fitout.live`, existing `/api/inngest` | Existing route exports `GET`, `POST`, `PUT`; production custom origin is documented. [VERIFIED: src/app/api/inngest/route.ts:100-113] [CITED: https://www.inngest.com/docs/deploy/vercel] |

### Anti-Patterns to Avoid

- Do not replace the locked sending subdomain with a root-domain sender.
- Do not apply production-domain variables to Preview or trust wildcard preview origins.
- Do not guess/create additional webhook events or copy sample DNS values.
- Do not duplicate the support inbox in Vercel variables or templates.
- Do not put secrets, bearer links, full email headers, or recipient addresses in evidence.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---|---|---|---|
| Domain redirects/TLS | app-level redirect or second app | Vercel project-domain redirect | Vercel owns domain routing. [CITED: https://vercel.com/docs/domains/working-with-domains/deploying-and-redirecting] |
| Mail DNS | homemade SPF/DKIM/MX values | exact Resend dashboard values | Records are domain/account-specific. [CITED: https://resend.com/docs/dashboard/domains/introduction] |
| Support inbox | inbound webhook/database/helpdesk | monitored Gmail Reply-To | Inbound support product is explicitly deferred. |
| Callback security | new unsigned receiver | existing signed handlers | Preserve the established trust boundary. [CITED: https://docs.paymongo.com/docs/creating-a-webhook-endpoint] [CITED: https://docs.didit.me/integration/webhooks] |

## Common Pitfalls

### Preview localhost leakage

Production-only configuration leaves Preview without `BETTER_AUTH_URL` and `NEXT_PUBLIC_APP_URL`; present fallbacks can yield localhost links/metadata. `NEXT_PUBLIC_` values are frozen at build time and Vercel scopes Preview separately. [CITED: https://nextjs.org/docs/app/guides/environment-variables] [CITED: https://vercel.com/docs/environment-variables]

**Avoid:** test local, production canonical, exact Preview `VERCEL_URL`, and unknown/malformed host modes. Preserve no wildcard/suffix matching.

### Sender verification mistaken for reachability

SPF/DKIM verification, successful API send, inbox delivery, and reply receipt are different checks. Resend requires verified SPF/DKIM and documents optional DMARC. [CITED: https://resend.com/docs/dashboard/domains/introduction]

**Avoid:** verify `send.fitout.live`, deploy the locked `EMAIL_FROM`, inspect a real message, then reply and confirm Gmail receipt.

### Provider dashboard drift

PayMongo requires a public HTTPS receiver; Didit stores a per-destination secret and supports dashboard test sends; Inngest signs serve traffic. [CITED: https://docs.paymongo.com/docs/creating-a-webhook-endpoint] [CITED: https://docs.didit.me/integration/webhooks] [CITED: https://www.inngest.com/docs/platform/signing-keys]

**Avoid:** inventory before mutation; retain the existing event scope and secret; evidence records only time, endpoint class, status, and authenticated receipt.

### Staff invite crosses host boundaries

DATA_R4zP1aVm_START
```ts
if (inviteUrl.origin !== OPS_APP_ORIGIN) {
  throw new Error("invalid staff invitation origin");
}
```
DATA_R4zP1aVm_END

[VERIFIED: src/lib/email.ts:122-128]

**Avoid:** set `OPS_APP_URL=https://ops.fitout.live` in Production and perform staff-invite proof only with a safe test account.

## Code Examples

```ts
import { SUPPORT_EMAIL } from "@/lib/site";

const { error } = await resend.emails.send({
  from: FROM,
  to,
  subject,
  html,
  text,
  replyTo: SUPPORT_EMAIL ?? undefined,
});
```

`replyTo` is [ASSUMED] pending installed-type confirmation; the null-to-undefined form leaves local development non-sending behavior intact while the phase's production value is real. Every other identifier is already in the codebase. [CITED: https://resend.com/docs/api-reference/emails/send-email]

## State of the Art

| Old Approach | Current Approach | Impact |
|---|---|---|
| Static single origin | Better Auth dynamic base URL with explicit allowed hosts | Public, ops, and exact Preview hosts can work without arbitrary-host trust. [CITED: https://better-auth.com/docs/guides/dynamic-base-url] |
| Vercel generated URL only for Inngest | optional `INNGEST_SERVE_ORIGIN` custom production host | Canonical production endpoint while Preview stays generated-host specific. [CITED: https://www.inngest.com/docs/deploy/vercel] |

**Deprecated/outdated:** Do not use `FitOut <onboarding@resend.dev>` in Production after sender verification. It is the current source fallback only. [VERIFIED: src/lib/email.ts:41-43]

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|---|---|---|
| A1 | The installed Resend SDK property is `replyTo`. | Patterns / Code Examples | Typecheck failure or no header; inspect local types before coding. |
| A2 | Account access permits editing existing Vercel/Resend/OAuth/PayMongo/Didit/Inngest configuration. | Control plane | Requires account owner checkpoint, with no code workaround. |
| A3 | `INNGEST_SERVE_ORIGIN` needs setting/correction. | Provider inventory | Audit actual state; do not add blindly. |

## Open Questions

1. **Which provider entries already exist?**
   - What we know: secure receivers exist for PayMongo, Didit, and Inngest. [VERIFIED: src/app/api/paymongo/webhook/route.ts:1-41] [VERIFIED: src/app/api/didit/webhook/route.ts:1-49] [VERIFIED: src/app/api/inngest/route.ts:1-113]
   - Recommendation: record endpoint URL, environment, event scope, and redacted test outcome before/after—never secrets.

2. **Can staff invite be safely proven?**
   - Recommendation: use a disposable staff recipient; otherwise record that narrow proof as pending, never use a real employee/customer or expose the invitation token.

## Environment Availability

| Dependency | Required By | Available | Version / State | Fallback |
|---|---|---|---|---|
| Node.js | code/test verification | ✓ | `v24.13.0` [VERIFIED: local command output] | — |
| npm CLI | test/registry checks | ✗ | local npm shim cannot resolve `npm-cli.js` [VERIFIED: local command output] | project-local executable if needed; installs are out of scope |
| Vercel/Resend/provider dashboards | control-plane changes/proof | unverified | authenticated access not probed | human checkpoint only |

**Missing dependencies with no fallback:** authenticated control-plane access to Vercel, Resend, Google OAuth, PayMongo, Didit, and Inngest.

## Validation Architecture

### Test Framework

| Property | Value |
|---|---|
| Framework | Vitest, with main and design configs. [VERIFIED: vitest.config.ts:1-3] [VERIFIED: vitest.design.config.ts:1-3] |
| Quick run | `.\\node_modules\\.bin\\vitest.cmd run --config vitest.design.config.ts tests/design/site-contacts.test.ts` [VERIFIED: local command output] |
| Full suite | `.\\node_modules\\.bin\\vitest.cmd run` [VERIFIED: local command output] |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|---|---|---|---|---|
| STATE-05 | real support value renders every guarded support affordance | structural | design-suite focused test | ✅ `tests/design/site-contacts.test.ts` |
| STATE-05 | every Resend payload has same Reply-To | unit | email focused test | ❌ add in code plan |
| TRUST-01 | footer/email shell read one owner | structural | design-suite focused test | ✅ `tests/design/site-contacts.test.ts` |
| D-11 | Preview origin/metadata/auth never emit localhost | unit | origin/auth focused tests | ❌ add in code plan |
| D-13/D-14 | domain, delivery/reply, signed callback receipt | manual integration | dashboard evidence | ❌ intentionally manual |

### Wave 0 Gaps

- [ ] Payload assertion: `replyTo` equals `SUPPORT_EMAIL`.
- [ ] Preview test: exact `VERCEL_URL` drives metadata, public links, `AUTH_ALLOWED_HOSTS`, and `AUTH_TRUSTED_ORIGINS`.
- [ ] Test fixtures for `fitout.live`, `ops.fitout.live`, one exact Preview host, and unknown-host denial.

## Security Domain

| ASVS Category | Applies | Standard Control |
|---|---|---|
| V2 Authentication | yes | explicit Better Auth allowed hosts/trusted origins and canonical Google callback. [CITED: https://better-auth.com/docs/reference/options] |
| V3 Session Management | yes | retain host-only public/ops cookies; do not enable cross-subdomain cookies. [VERIFIED: src/lib/auth.ts:71-88] |
| V4 Access Control | yes | ops host routing never replaces existing server-side staff guard. [VERIFIED: src/lib/ops/staff.ts:59-66,130-134] |
| V5 Input Validation | yes | signed existing PayMongo/Didit/Inngest receiver paths only. [VERIFIED: src/app/api/paymongo/webhook/route.ts:31-41] [CITED: https://docs.didit.me/integration/webhooks] |
| V6 Cryptography | yes | provider-issued secrets and existing HMAC/signing validation; no custom signing. [CITED: https://www.inngest.com/docs/platform/signing-keys] |

| Threat | STRIDE | Mitigation |
|---|---|---|
| Host header creates hostile auth/CTA origin | Spoofing | exact configured host list, no wildcard preview, no untrusted proxy headers. [VERIFIED: src/lib/app-origins.ts:109-118] |
| Preview impersonates production | Spoofing | separate Vercel scopes and exact Preview host. [CITED: https://vercel.com/docs/environment-variables] |
| Forged callback | Tampering | signed receiver plus authenticated dashboard test. |
| Secrets/bearer links in proof | Information Disclosure | server-only dashboard values and redacted evidence. [CITED: https://nextjs.org/docs/app/guides/environment-variables] |

## Sources

### Primary (MEDIUM confidence)

- [Vercel domains](https://vercel.com/docs/domains/working-with-domains), [redirects](https://vercel.com/docs/domains/working-with-domains/deploying-and-redirecting), and [environment variables](https://vercel.com/docs/environment-variables).
- [Next.js environment variables](https://nextjs.org/docs/app/guides/environment-variables).
- [Resend domains](https://resend.com/docs/dashboard/domains/introduction) and [send API](https://resend.com/docs/api-reference/emails/send-email).
- [Better Auth options](https://better-auth.com/docs/reference/options) and [dynamic base URL](https://better-auth.com/docs/guides/dynamic-base-url).
- [PayMongo webhooks](https://docs.paymongo.com/docs/creating-a-webhook-endpoint), [Didit webhooks](https://docs.didit.me/integration/webhooks), and [Inngest on Vercel](https://www.inngest.com/docs/deploy/vercel).

### Secondary (VERIFIED codebase)

- `src/lib/site.ts`, `src/lib/email.ts`, `src/lib/app-origins.ts`, `src/lib/auth.ts`, and `src/app/layout.tsx`.
- `src/app/api/paymongo/webhook/route.ts`, `src/app/api/didit/webhook/route.ts`, and `src/app/api/inngest/route.ts`.
- `tests/design/site-contacts.test.ts`, `tests/auth/email-dev-fallback.test.ts`, `tests/auth/secret-config.test.ts`, and `tests/auth/ops-host-routing.test.ts`.

## Metadata

**Confidence breakdown:**

- Standard stack: MEDIUM — official docs and verified integration seams; dashboards were not inspected.
- Architecture: MEDIUM — topology is locked and code is verified; Preview changes need tests.
- Pitfalls: MEDIUM — provider docs corroborate code fallback and signing boundaries.

**Research date:** 2026-09-11
**Valid until:** 2026-09-18
