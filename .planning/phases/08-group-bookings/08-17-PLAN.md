---
phase: 08-group-bookings
plan: 17
type: execute
wave: 9
depends_on: [08-10, 08-11, 08-12, 08-13, 08-14, 08-15, 08-16]
gap_closure: true
closes: [08-VERIFICATION.md human_verification — the pax-pricing surcharge UI walkthrough]
files_modified: []
autonomous: false
requirements: [GROUP-01, GROUP-04, GROUP-05]
must_haves:
  truths:
    - "D-108: a human has driven the per-head-priced booking path end to end with a real extraHeadFee configured — the surface 08-09 step 6 explicitly did NOT exercise"
    - "D-113/D-114: the organizer's headcount counts them exactly once and the over-RSVP nudge fires at the first over-subscription, confirmed on screen"
    - "CR-01: a surcharged HOURLY booking shows its real time range on the invite page, the organizer page and the RSVP email — not 'Full day'"
    - "CR-02: after a re-price, the previously-minted checkout session is no longer payable"
  artifacts: []
  key_links:
    - from: "manual UAT with extra_head_fee configured"
      to: "the shipped PaxStepper → re-quote → checkout → invite → roster → nudge path"
      via: "a real browser against the local stack"
      pattern: "extra_head_fee"
---

<objective>
Run the one open `human_verification` item from `08-VERIFICATION.md`, on the surface that all four of this run's other findings land on.

08-09's optional Step 6 — the pax-pricing surcharge walkthrough — was **explicitly not exercised**, because `uat_listing_bookable.extra_head_fee` was NULL. Its own SUMMARY records it as "NOT human-verified". So the phase's existing sign-off does **not** cover the per-head path, and that is exactly where CR-01 ("Full day" everywhere), CR-02 (a second payable session), CR-03 (unclamped headcount) and WR-03/WR-04 (the organizer counted zero or twice) all live.

This plan configures the fixture that was missing and walks the path with a human at the keyboard. No code changes.

Purpose: close the last gap in `08-VERIFICATION.md` and give `/gsd:verify-work` something to re-verify against.
Output: a human sign-off, or issues logged as the next gap-closure input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-group-bookings/08-VERIFICATION.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md
@.planning/phases/08-group-bookings/08-VALIDATION.md
@$HOME/.claude/projects/C--Users-Admin-Roaming-FitOut/memory/local-env-and-uat-seed.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + build gate, then configure the per-head UAT fixture</name>
  <files></files>
  <read_first>
    - $HOME/.claude/projects/C--Users-Admin-Roaming-FitOut/memory/local-env-and-uat-seed.md (the run recipe, the seeded host login, `uat_listing_bookable`, the RESEND_API_KEY situation, the stale-`.next` 404 trap and the two-process Inngest requirement)
    - .planning/phases/08-group-bookings/08-VERIFICATION.md (the `human_verification` block — the exact test and why a human is required)
    - .planning/phases/08-group-bookings/08-10-SUMMARY.md through 08-16-SUMMARY.md (what shipped in this gap-closure run, so the walkthrough asserts the corrected behaviour rather than the old one)
  </read_first>
  <action>
    First the automated gate, then the fixture.

    **Gate.** Run the full suite with NO `DATABASE_URL` override in the shell (`tests/setup.ts` loads `.env.local` non-overridingly, so a stale shell value wins and breaks every integration test), then a bare `npm run build`. Both must be clean before a human is asked to look at anything. Record the test count and compare it against the 792 baseline at phase-8 close.

    **Fixture.** Bring the local stack up (`npm run db:up`, then `npm run dev` and `npm run dev:inngest` in their own terminals) and patch `uat_listing_bookable` through the containerised database — `docker compose exec -T db psql -U fitout -d fitout -c "..."` — so no throwaway script lands in the repo. The listing needs four values set in one statement:
      - `extra_head_fee` to a visible non-zero amount in integer centavos (₱100 → `10000`) — the field whose NULL is the entire reason step 6 was skipped
      - `included` to `1` (D-108: the organizer's own seat folded into the base rate)
      - `max_occupancy` to `12` — a round number so the WR-03 arithmetic is legible on screen (`capacity_snapshot` must come out as `11`, and the organizer page must read `of 12`)
      - `booking_mode` to `'instant'` — the `PaxStepper` lives on the reserve page, which a request-to-book booker only reaches after host approval (`deferred-items.md` item 2, still open and out of scope); instant mode is what puts the stepper in front of a human
    Also patch `cancellation_policy` to `'standard'` if it is still NULL — the seed predates the D-77 tier gate and a NULL tier blocks checkout.

    Read the row back and confirm all five values, and confirm the listing is `published` with a host who can actually be paid. Paste the SELECT output into the SUMMARY: a walkthrough run against an unpatched fixture is how step 6 got skipped the first time.

    If `RESEND_API_KEY` is unset, say so plainly in the checkpoint below rather than letting the human hunt for an email that only exists in a `[email:dev]` log line.

    Make no repository changes in this task. `git status` must be clean when it finishes.
  </action>
  <acceptance_criteria>
    - `npx vitest run` exits 0; the file/test counts are recorded in the SUMMARY alongside the 792 baseline
    - `npm run build` exits 0, bare, with no dummy-env workaround (a fail-closed boot guard throwing here is a regression to report, not to route around)
    - The `SELECT id, status, booking_mode, max_occupancy, included, extra_head_fee, cancellation_policy FROM listing WHERE id = 'uat_listing_bookable'` output is pasted into the SUMMARY and shows `extra_head_fee = 10000`, `included = 1`, `max_occupancy = 12`, `booking_mode = instant`, `cancellation_policy = standard`, `status = published`
    - `git status --porcelain` is empty at the end of this task
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>The suite and build are green and the UAT listing actually charges per head, so the walkthrough below can exercise the surface it is meant to.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pax-pricing surcharge walkthrough with a real per-head fee (human-verify)</name>
  <files></files>
  <action>
    Pause for the human to run the walkthrough below. Claude makes NO code changes here: it presents the steps, waits for the resume signal, and records the outcome — approved, or the issues, which become the next `/gsd:plan-phase 8 --gaps` input.
  </action>
  <what-built>
    The per-head-priced booking path, end to end, with every fix from this gap-closure run in place: a `PaxStepper` that re-quotes entirely server-side and clamps to the listing's own `maxOccupancy` (CR-03), a re-price that retires the superseded PayMongo checkout session before minting a new one (CR-02), time labels composed from the persisted `booking.full_day` so a surcharged hourly booking no longer reads "Full day" anywhere (CR-01), and an organizer surface that counts the organizer exactly once in the headcount, the roster and the over-RSVP nudge (WR-03 / WR-04).
  </what-built>
  <how-to-verify>
    1. **Stack.** Confirm `npm run dev` (:3000) AND `npm run dev:inngest` (:8288) are both up and the dashboard shows the app's functions. If routes 404 oddly, kill dev, `rm -rf .next`, restart (the stale-Turbopack-cache trap). Confirm `INNGEST_DEV=1` is present in `.env.local`. Note whether `RESEND_API_KEY` is set — without it, emails only appear as `[email:dev]` log lines, which is env config and not a bug.

    2. **The stepper and the surcharge line (D-108, CR-03).** Sign in as the seeded host/booker, open `uat_listing_bookable`, pick a **2-hour hourly** slot (NOT full-day — the hourly case is the one CR-01 broke) and go to the reserve page. Confirm: the `PaxStepper` is present; stepping it up re-renders a NEW server total (the page refreshes rather than doing arithmetic in the browser); an **"Extra guests"** line appears in the breakdown once the count exceeds 1; and the total equals space + service fee with no double-counted centavos. Step it to the maximum — it must stop at **12** and refuse to go higher.

    3. **The time label (CR-01 — the regression this run fixed).** With the surcharge applied, confirm the reserve page and the booking detail show the real **hour range** (e.g. `8:00 AM – 10:00 AM`), **not** "Full day". This is the case that was broken on every email and notification.

    4. **One payable session (CR-02).** Set the headcount to 3 and click **Confirm & pay**. On the PayMongo hosted page, do NOT pay — leave that tab OPEN and use the cancel/back link to return to the reserve page. Step the headcount to 4 and click **Confirm & pay** again. Now switch back to the FIRST PayMongo tab and reload it: it must no longer be payable (expired / unavailable). Only the newest session may accept money. Then pay the newest one and let the webhook confirm the booking.

    5. **The organizer numbers (WR-03 / D-113).** On the now-confirmed booking, click **Invite people**, then open `/bookings/[id]/group`. With nobody having RSVP'd yet, the headcount must read **`1 of 12`** — you, counted once, against the room's rating — and the roster must show exactly one row, `You · organizer`.

    6. **The nudge fires at the first over-subscription (WR-04 / D-114).** The booking was paid for 4 people including you, so 4 RSVP-yes attendees means 5 in the room. Open the invite link in **three separate private windows** and RSVP **yes** as three different guests. The headcount should read `4 of 12`, and no nudge should appear yet (4 attending vs 4 declared). Now RSVP yes as a **fourth** guest: the headcount reads `5 of 12` and the neutral top-up nudge must appear, saying you may owe for **1 extra person**. Confirm it is a calm neutral alert with **no** payment button and no amount.

    7. **The cap holds (SC4 / GROUP-05).** Keep RSVPing yes as new guests until the invite page reports the group is full. It must refuse the **12th** attendee — 11 RSVP-yes plus you is 12, the listing's rating. It must never admit a 12th RSVP.

    8. **The invite page and email time (CR-01, second half).** On the public invite page, confirm the session window again reads the real hour range and not "Full day". If `RESEND_API_KEY` is set, RSVP with a real address you can check and confirm the confirmation email says the same hour range.

    Note: CR-04 (the rate-limit bound and resolve-before-budget) is server-side and fully covered by automated tests — there is no human step for it, and none is expected.
  </how-to-verify>
  <acceptance_criteria>
    - The human records an outcome for each of steps 2 through 8 individually (approved / issue), not one blanket approval — step 6 in particular has a specific expected number (`1 extra person`) and a specific expected silence (no nudge at 4 vs 4)
    - Any step that cannot be run is recorded as NOT VERIFIED with the reason, never as passed by assumption — that is the exact failure mode that let 08-09 step 6 slip
    - Step 4's first-tab reload result is recorded verbatim (what the old PayMongo tab actually said)
    - Steps 3 and 8 record the literal time string that was rendered
  </acceptance_criteria>
  <verify>
    <human-check>The human confirms the PaxStepper re-quotes server-side and clamps at 12, the "Extra guests" line appears, a surcharged hourly booking never reads "Full day" on the reserve page / booking detail / invite page / email, the superseded checkout session is no longer payable after a re-price, the organizer headcount reads `1 of 12` before any RSVP, the top-up nudge stays silent at 4-vs-4 and fires with "1 extra person" at 5-vs-4, and the cap refuses the 12th RSVP.</human-check>
  </verify>
  <done>Human sign-off recorded per step, or issues logged for the next gap-closure run.</done>
  <resume-signal>Type "approved" if steps 2-8 all behaved as described; otherwise describe which step failed and what you saw (it becomes input for the next `/gsd:plan-phase 8 --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a real browser → the live per-head booking path | the production-shaped money path this run changed, exercised for the first time by a human |
| an abandoned PayMongo tab → a superseded checkout session | the CR-02 exposure, checked live in step 4 |
| a real guest browser → the public invite link | the unauthenticated path, re-exercised now that its budget keys and its cap changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-59 | Elevation of Privilege (financial) | two payable checkout sessions | mitigate | Step 4 reloads the abandoned session's tab and requires it to be unpayable — the only way to observe the CR-02 fix against the real PayMongo side rather than against a mock. |
| T-08-60 | Tampering (integrity of a stated fact) | surcharged hourly time labels | mitigate | Steps 3 and 8 require the literal rendered string to be recorded on four surfaces, so "looked fine" cannot stand in for the CR-01 fix. |
| T-08-61 | Tampering (physical-capacity integrity) | the RSVP cap | mitigate | Step 7 fills the group to the cap live and requires the 12th RSVP to be refused, confirming the WR-03 seat reservation end to end rather than by unit test alone. |
| T-08-62 | Repudiation | a skipped step recorded as passing | mitigate | The acceptance criteria require a per-step outcome and forbid assumed passes — 08-09's blanket sign-off over a skipped step 6 is what produced this gap. |
| T-08-63 | Information Disclosure | real email delivery in UAT | accept | Confirming real Resend delivery requires sending to a real inbox; the account-owner address is the only deliverable target in test mode. Unchanged from 08-09. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No packages added or upgraded in this run; nothing to audit. |
</threat_model>

<verification>
- `npx vitest run` full suite exits 0 (no `DATABASE_URL` override in the shell)
- `npm run build` exits 0, bare
- The patched `uat_listing_bookable` row is pasted into the SUMMARY
- A per-step human outcome is recorded for steps 2 through 8
- `git status --porcelain` is empty (this plan modifies no repository file)
</verification>

<success_criteria>
- The open `human_verification` item in `08-VERIFICATION.md` has been genuinely exercised on a listing that actually charges per head, with a per-step record
- Every gap-closure fix from this run that has a visible surface has been seen working by a human, or the failure has been logged
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-17-SUMMARY.md` when done — include the fixture SELECT output, the suite/build results, and the per-step human outcomes verbatim.
</output>
