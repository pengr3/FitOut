---
phase: 08-group-bookings
plan: 08
type: execute
wave: 4
depends_on: [08-06]
files_modified:
  - src/app/invite/[token]/page.tsx
  - src/components/group/rsvp-form.tsx
  - src/components/group/rsvp-confirmation.tsx
autonomous: true
requirements: [GROUP-02, GROUP-03]
must_haves:
  truths:
    - "Pitfall 5: /invite/[token] lives at the ROOT (outside (app)/(host)), header-less like /listings/[id] — a signed-out invitee is NEVER bounced to /login (GROUP-03 'without a full account')"
    - "D-116: the invite page reads the session with auth.api.getSession but does NOT require it — the guest-or-login fork; 'Log in instead' is a ghost link that threads the token via callbackURL so the user returns to the same invite"
    - "D-117: the guest email field is OPTIONAL and framed as a benefit (G1/G2), never a signup gate; a blank-email guest RSVP is a valid pure-headcount entry"
    - "D-117/D-120/G3: the blank-email-guest confirmation states plainly it is the ONLY confirmation and cannot be changed later; accounts + guests-with-email can Change my answer until RSVP closes"
    - "D-120: one RSVP = one person, no +guests (no quantity control); RSVP closes at session start (server/DB-clock evaluated)"
    - "D-112: the UI 'full' state is a courtesy (Yes disabled + calm 'This group is full'); the server seat-claim is the gate — a yes that loses the race resolves to the calm raced copy, never a crash"
    - "Open Q7: unknown = revoked = voided all render the SAME calm 'no longer active' state (no enumeration oracle); times are venue-local + named tz; address respects showExactAddress (D-122/D-09)"
  artifacts:
    - path: "src/app/invite/[token]/page.tsx"
      provides: "the public token-credentialed RSVP RSC (event summary + all states)"
      min_lines: 40
      contains: "getGroupByToken"
    - path: "src/components/group/rsvp-form.tsx"
      provides: "guest-or-login RSVP form (Yes coral / Can't make it neutral) calling submitRsvp"
      contains: "submitRsvp"
  key_links:
    - from: "src/app/invite/[token]/page.tsx"
      to: "getGroupByToken"
      via: "token-scoped read (08-06); unknown=voided → same calm state"
      pattern: "getGroupByToken"
    - from: "src/components/group/rsvp-form.tsx"
      to: "submitRsvp"
      via: "public server action (token credential, no session required)"
      pattern: "submitRsvp"
---

<objective>
Build the first surface in the app reachable without a session and without owning anything: the public `/invite/[token]` RSVP page (GROUP-02/GROUP-03). Header-less at the root, token-credentialed, with the D-116 guest-or-login fork, the D-117 optional-email-as-benefit, one-person RSVP (D-120), and all edge states (full/closed/voided/unknown) rendered calm — the server seat-claim (08-02/08-06) is the real gate.

Purpose: This closes GROUP-03 ("RSVP without a full account") end-to-end. It depends on 08-06's `submitRsvp` action + `getGroupByToken` read. It sits OUTSIDE `(app)` so a guest is never bounced to login (Pitfall 5).
Output: the public invite RSC + `RsvpForm` + `RsvpConfirmation`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md
@.planning/phases/08-group-bookings/08-PATTERNS.md

<interfaces>
<!-- submitRsvp(token, {name, email?, answer}) (08-06). getGroupByToken(db, token) → group + booking/listing display fields OR calm "not active" (08-06). -->
<!-- auth.api.getSession returns null for a guest (auth.ts). Container: mx-auto w-full max-w-lg px-4 py-8 sm:py-12 (narrower than booking detail). -->
<!-- Times: venue-local + named tz via venueTzNote/whenLabel. Address: per listing.showExactAddress (exact or approximate). Wordmark lockup at the top of the card (02-UI-SPEC accent #5). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The public /invite/[token] RSC — root placement, token gate, event summary, all states</name>
  <files>src/app/invite/[token]/page.tsx</files>
  <read_first>
    - src/app/listings/[id]/page.tsx (the ROOT, header-less, session-optional public-route shape to mirror; async params)
    - src/lib/auth.ts (auth.api.getSession — returns null for a guest; do NOT require it)
    - src/lib/group/rsvp.ts getGroupByToken (08-06 — unknown=voided calm state), venueTzNote/whenLabel + listing.showExactAddress idioms (from the public listing page)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §3 (the full invite-page state table) + Open Q2/Q7 (root placement, wordmark, unknown=voided) + RESEARCH §Pitfall 5
  </read_first>
  <action>
    Create `src/app/invite/[token]/page.tsx` at the ROOT (outside `(app)`/`(host)`) so no session redirect applies (Pitfall 5). Await `params`, read the session with `auth.api.getSession` but do NOT require it. Resolve the group via `getGroupByToken`; if the token is unknown/voided/cancelled render the SAME calm "This invite is no longer active. Ask the organizer for the latest link." state (no distinguishable 404, Open Q7). Otherwise render the card: a FitOut wordmark lockup at top, then the event summary — title "You're invited to {space}", venue-local "{date}, {time} ({City} time)" via the shipped tz idiom, and the address per `listing.showExactAddress` (exact or approximate, never leaking a withheld precise address, D-09/D-122) — each with a leading lucide glyph. Compute the display state server-side against the DB clock: `closed` when now() >= booking.startsAt (D-120), `full` when confirmed-yes >= capacity_snapshot (courtesy — D-112), `voided` when voided_at set. Pass the resolved state + a session flag to `RsvpForm` (Task 2). Container `mx-auto w-full max-w-lg px-4 py-8 sm:py-12`.
  </action>
  <acceptance_criteria>
    - the file is at `src/app/invite/[token]/page.tsx` (root — `ls src/app/invite/[token]/page.tsx` succeeds) and there is NO `(app)`/`(host)` segment in its path (a guest is never redirected to /login)
    - it calls `auth.api.getSession` but does NOT `redirect`/`notFound` on a null session (grep: no session-required redirect)
    - unknown token and voided token both render the identical "no longer active" copy (Open Q7 — assert the same string / no distinguishing branch)
    - closed/full states are computed server-side (now()/capacity_snapshot), not client-side; `npx tsc --noEmit` + eslint clean; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The public invite RSC renders at the root without requiring a session, shows the venue-local event summary + address per showExactAddress, and calmly handles unknown/voided/closed/full.</done>
</task>

<task type="auto">
  <name>Task 2: RsvpForm (guest-or-login fork) + RsvpConfirmation, wired to submitRsvp</name>
  <files>src/components/group/rsvp-form.tsx, src/components/group/rsvp-confirmation.tsx</files>
  <read_first>
    - src/app/actions/group.ts submitRsvp (08-06 — the public action + its result shape: ok / full / closed / error)
    - a shipped RHF + zod form (e.g. an auth or reserve form) for the client-form pattern; src/lib/validation/group.ts rsvpSchema (08-06)
    - src/components/booking/hold-expired-state.tsx / pending-payment-state.tsx (calm-state rendering idioms, never red)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §3 (identity block, RSVP choice, confirmation, full/closed/change-answer) + §Copywriting G1/G2/G3 + §Accessibility
  </read_first>
  <action>
    Create `rsvp-form.tsx` ("use client"): the D-116 guest-or-login fork — logged in shows "RSVPing as {name}" (no name field); logged out shows a REQUIRED guest name input + an OPTIONAL email input with the benefit helper "Add your email to get a confirmation and a link to change your answer." (framed as a benefit, never a gate — G1/G2), plus a quiet `Log in instead` ghost link that threads the token via `callbackURL`. RSVP choice = coral "Yes, I'm coming" (the one coral) + neutral outline "Can't make it"; NO quantity control (one RSVP = one person, D-120). Submit calls `submitRsvp(token, {name, email?, answer})` (server-validated via rsvpSchema); a raced/full result resolves to the calm "This group just filled up — all {N} spots are taken." copy (never a crash, D-112). The `full` state disables Yes with a neutral alert (Can't-make-it stays); `closed` shows the neutral "RSVPs have closed" alert with no choice buttons; a returning account/guest-with-email sees their current answer + a neutral "Change my answer" toggle (D-120). Create `rsvp-confirmation.tsx`: the calm post-RSVP state ("You're in — see you there." / "Thanks for letting us know.") retaining the event summary; the BLANK-EMAIL-GUEST variant adds the muted line "This is your only confirmation — we don't have an email to reach you." and states they cannot change it later (D-117/D-120/G3). All states are announced via role=status (accessibility); no destructive-red anywhere.
  </action>
  <acceptance_criteria>
    - `grep -c "submitRsvp" src/components/group/rsvp-form.tsx` >= 1; the email field is optional (not required) and framed as a benefit (grep the helper copy — no "sign up to RSVP")
    - there is NO quantity/+guests control in rsvp-form.tsx (grep 0 for a quantity stepper) — one RSVP = one person (D-120)
    - the blank-email-guest confirmation renders the "only confirmation" line (grep the copy) — G3/D-117
    - `grep -ci "destructive" src/components/group/rsvp-form.tsx src/components/group/rsvp-confirmation.tsx` returns 0; the full/closed states use role=status (accessibility)
    - `npx tsc --noEmit` + eslint clean; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>A guest can RSVP without an account (optional email), an account can RSVP; full/closed/raced states are calm; the blank-email guest is told plainly it is their only confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet → /invite/[token] | unauthenticated, token-credentialed access crosses here |
| guest form input → submitRsvp | attacker-controlled name/email/answer crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-23 | Information Disclosure | token enumeration / oracle | mitigate | unknown=revoked=voided render the same calm state (Open Q7); ~100-bit token (08-02); never logged |
| T-08-24 | Elevation | public route inside (app) bounces guests | mitigate | route is at the ROOT, session read-not-required (Pitfall 5) — GROUP-03 holds |
| T-08-25 | Tampering (integrity) | raced yes beyond cap | mitigate | the server claimSeat is the gate; the UI "full" is a courtesy; a raced yes resolves to calm copy, never a crash (D-112) |
| T-08-26 | Information Disclosure | withheld exact address leak | mitigate | the invite page renders the address per listing.showExactAddress (D-09/D-122) |
| T-08-27 | Input Validation | guest name/email/answer | mitigate | rsvpSchema (Zod 4) server-re-validates; the client is never trusted (V5) |
| T-08-SC | Tampering | npm installs | accept | no packages added; composes from installed shadcn blocks |
</threat_model>

<verification>
- `npx tsc --noEmit` + eslint clean; `npm run build` exits 0; manual cross-session RSVP flow is deferred to the 08-09 human-verify checkpoint.
</verification>

<success_criteria>
- A signed-out invitee can open the root invite link and RSVP yes/no as a guest (optional email) or by logging in, one person per RSVP; full/closed/voided/unknown are calm; the blank-email guest is honestly told it is their only confirmation (GROUP-02/GROUP-03).
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-08-SUMMARY.md` when done.
</output>
