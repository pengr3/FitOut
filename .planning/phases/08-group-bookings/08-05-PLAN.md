---
phase: 08-group-bookings
plan: 05
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - src/lib/validation/listing.ts
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - src/components/booking/price-breakdown.tsx
  - src/components/booking/pax-stepper.tsx
  - src/app/listings/[id]/book/page.tsx
  - src/app/(app)/bookings/[id]/page.tsx
autonomous: true
requirements: [GROUP-01, GROUP-05]
must_haves:
  truths:
    - "D-108: when extraHeadFee>0 the reserve page shows a PaxStepper (min 1, max maxOccupancy) that re-fetches a SERVER-frozen quote on change — the client performs ZERO price arithmetic"
    - "D-108: PriceBreakdown gains ONE conditional 'Extra guests ({K} × {fee})' line, rendered only when the surcharge > 0, from server-computed props (extraHeads, extraHeadCents); the component still sums nothing"
    - "D-108: when extraHeadFee=0 there is ZERO UI change — no stepper, no surcharge line, no declaredPax (existing/flat listings behave exactly as today)"
    - "Pitfall 3: book/page.tsx and (app)/bookings/[id]/page.tsx now read the PERSISTED booking.fullDay column instead of re-deriving fullDay from spacePriceCents (the surcharge would otherwise mislabel every surcharged hourly booking as 'Full day')"
    - "D-113: the PaxStepper label states 'This includes you' (organizer is attendee #1)"
    - "D-109: occupancy_mode has NO host-facing UI in v1 (schema column only); the wizard renders no mode toggle"
  artifacts:
    - path: "src/components/booking/pax-stepper.tsx"
      provides: "input-group +/- declaredPax stepper that re-quotes server-side"
      contains: "maxOccupancy"
    - path: "src/app/(host)/host/listings/[id]/edit/wizard.tsx"
      provides: "optional Group pricing subsection (extraHeadFee + included, no publish requirement, no occupancy_mode control)"
      contains: "extraHeadFee"
  key_links:
    - from: "src/app/(app)/bookings/[id]/page.tsx"
      to: "booking.full_day column"
      via: "persisted read (WR-06 / drizzle 0016) replacing the price-derivation"
      pattern: "fullDay"
    - from: "src/components/booking/price-breakdown.tsx"
      to: "extraHeads / extraHeadCents props"
      via: "conditional Extra guests line (surcharge>0)"
      pattern: "extraHeads"
---

<objective>
Ship the pax-pricing UI: the host wizard's optional "Group pricing" fields (`extraHeadFee` + `included`, D-108), the booker's `PaxStepper` + surcharge breakdown line on the reserve page, and — mandatory with the surcharge (Pitfall 3) — switch BOTH shipped pages that re-derive `fullDay` from price to read the persisted `booking.fullDay` column. When `extraHeadFee = 0` there is zero UI change.

Purpose: This is the human-facing half of the D-108 seam (08-03 built the server math). All money stays server-computed (D-108). `occupancy_mode` gets no UI in v1 (D-109).
Output: wizard fields, PaxStepper, breakdown line, and the two fullDay fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md
@.planning/phases/08-group-bookings/08-PATTERNS.md

<interfaces>
<!-- validation/listing.ts: add to BOTH draftSchema (optional) and publishSchema (optional — NOT a publish requirement): -->
occupancyMode?: z.enum(["exclusive"]).optional() ; included?: z.number().int().positive().optional() ; extraHeadFee?: z.number().int().min(0).optional()
<!-- PriceBreakdown already conditionally renders the service-fee row and sums nothing (price-breakdown.tsx:94-99). Clone that shape for the Extra guests line. -->
<!-- fullDay: read bk.fullDay (persisted, schema.ts:632) instead of the `spacePriceCents !== hourlyRate×hours` derivation (bookings/[id]/page.tsx:206-214, book/page.tsx:136-152). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard Group-pricing fields + listing validation (no occupancy_mode UI)</name>
  <files>src/lib/validation/listing.ts, src/app/(host)/host/listings/[id]/edit/wizard.tsx</files>
  <read_first>
    - src/lib/validation/listing.ts:25-48 (draftSchema — all optional), :55-84 (publishSchema — postalCode:64 is the OPTIONAL-in-publish template to mirror; cancellationPolicy:81 is the REQUIRED template to NOT mirror)
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx (the existing pricing step fields — hourlyRateCents/dayRateCents pattern to mirror for the currency input)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §6 (wizard pricing step contract) + §8 Open Q8 (occupancy_mode has NO v1 control)
  </read_first>
  <action>
    Add `occupancyMode` (optional, single enum value `exclusive`), `included` (optional positive int), and `extraHeadFee` (optional int >= 0, centavos) to BOTH `draftSchema` (optional) and `publishSchema` — but keep both fee fields OPTIONAL in publishSchema (mirror `postalCode` at :64, NOT the required `cancellationPolicy` at :81); they are backward-compatible with defaults, so they are NOT publish requirements and must NOT enter the publish checklist. In the wizard's existing pricing step, add an optional "Group pricing" subsection: `extraHeadFee` as a PHP-prefixed currency input (default ₱0, helper per UI-SPEC §6 "Leave at ₱0 for flat pricing — most spaces do") and `included` as a number input (default 1) shown ONLY when `extraHeadFee > 0` (helper "How many people your rate includes..."). Render NO occupancy_mode control (D-109). Values in centavos (integer).
  </action>
  <acceptance_criteria>
    - `grep -c "extraHeadFee" src/app/(host)/host/listings/[id]/edit/wizard.tsx` >= 1 and `grep -ci "occupancy" src/app/(host)/host/listings/[id]/edit/wizard.tsx` returns 0 (no mode toggle rendered, D-109)
    - extraHeadFee/included are absent from publishSchema's required set (a listing with neither set still publishes — assert via an existing/new listing-schema test that a publish-eligible fixture WITHOUT these fields still passes)
    - `npx vitest run tests/listing/ tests/booking/` GREEN (no publish-gate regression)
    - `npx tsc --noEmit` + eslint clean
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Host can set an optional extra-guest fee + included headcount; neither is a publish requirement; no occupancy_mode UI exists.</done>
</task>

<task type="auto">
  <name>Task 2: PaxStepper + PriceBreakdown surcharge line on the reserve page; fullDay fix on both pages</name>
  <files>src/components/booking/pax-stepper.tsx, src/components/booking/price-breakdown.tsx, src/app/listings/[id]/book/page.tsx, src/app/(app)/bookings/[id]/page.tsx</files>
  <read_first>
    - src/components/booking/price-breakdown.tsx:1-17 (the "sums nothing" contract), :94-99 (the conditional service-fee row to clone for the Extra guests line)
    - src/app/listings/[id]/book/page.tsx:136-152 (the fullDay re-derivation to REPLACE), :215-220 (PriceBreakdown wiring + where extraHeads/extraHeadCents props flow), :55 (session read)
    - src/app/(app)/bookings/[id]/page.tsx:206-214 (the twin fullDay re-derivation to REPLACE with the persisted bk.fullDay), :217-218 (the timeLabel that consumes fullDay)
    - src/app/actions/booking.ts placeHold (declaredPax capture from 08-03 — the stepper submits declaredPax through the same reserve flow)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §5 + Open Q5 (declaredPax re-quotes server-side; client zero math)
  </read_first>
  <action>
    Create `src/components/booking/pax-stepper.tsx` ("use client"): an `input-group` +/− around a number input, min 1 / max `maxOccupancy`, label "How many people are coming?" with helper "This includes you. Up to {maxOccupancy}." (D-113). Changing it re-fetches a SERVER-frozen quote (mirror the reserve page's existing freeze-quote discipline — the client computes NO price, Open Q5); accessible names "Add a guest"/"Remove a guest". Render the stepper on book/page.tsx ONLY when the listing's `extraHeadFee > 0` (fee=0 ⇒ no stepper, unchanged). Extend `price-breakdown.tsx` with ONE conditional line "Extra guests ({extraHeads} × {formatMoney(extraHeadCents)})" → {surcharge}, between the run line and the service fee, rendered only when the surcharge (extraHeads×extraHeadCents) > 0, from server-computed props; the component still performs zero arithmetic. In BOTH book/page.tsx and (app)/bookings/[id]/page.tsx, DELETE the `spacePriceCents !== hourlyRate×hours` fullDay derivation and read the persisted `bk.fullDay` column instead (Pitfall 3 / WR-06); update the stale "fullDay is not persisted" comments.
  </action>
  <acceptance_criteria>
    - `grep -c "hourlyTotal\|hourlyRate.*hours" src/app/(app)/bookings/[id]/page.tsx` for the fullDay derivation returns 0 AND the page reads `bk.fullDay` (persisted column — Pitfall 3 fixed on the detail page)
    - `grep -c "bk.fullDay\|booking.fullDay\|\\.fullDay" src/app/listings/[id]/book/page.tsx` >= 1 (reserve page reads the persisted column)
    - the Extra guests line renders only when surcharge>0 (a fee=0 booking shows the shipped breakdown byte-for-byte — assert via a component test or grep the conditional guard)
    - PaxStepper max is bound to `maxOccupancy` and it submits declaredPax without doing client price math (`grep -c "formatMoney\|\\*\|totalCents" src/components/booking/pax-stepper.tsx` shows no price computation)
    - `npx vitest run tests/booking/` GREEN; `npx tsc --noEmit` + eslint clean; `npm run build` exits 0 (with the documented env placeholders)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/booking/</automated>
  </verify>
  <done>The PaxStepper + surcharge line appear only when extraHeadFee>0 and re-quote server-side; both pages read the persisted fullDay; flat listings render unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client PaxStepper → placeHold | declaredPax + implied price arrive from an untrusted client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | client price recompute | mitigate | PaxStepper re-fetches a server-frozen quote and does zero arithmetic; PriceBreakdown receives frozen props and sums nothing (D-108, Open Q5) |
| T-08-13 | Tampering | fullDay mislabel | mitigate | both pages read the persisted booking.fullDay, so a surcharged hourly booking is never mislabeled "Full day" (Pitfall 3) |
| T-08-SC | Tampering | npm installs | accept | no packages added; every UI surface composes from installed shadcn blocks (UI-SPEC §Registry Safety) |
</threat_model>

<verification>
- `npx vitest run tests/booking/ tests/listing/` green; `npm run build` exits 0; tsc + eslint clean.
</verification>

<success_criteria>
- Host can set an optional extra-guest fee; a booker on a fee>0 listing sees a server-re-quoting PaxStepper + a surcharge line; both shipped pages read the persisted fullDay; a fee=0 listing is unchanged (D-108); no occupancy_mode UI (D-109).
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-05-SUMMARY.md` when done.
</output>
