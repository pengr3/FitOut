---
phase: 08-group-bookings
plan: 02
type: tdd
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/group/token.ts
  - src/lib/group/seat-claim.ts
  - tests/group/seat-claim-race.test.ts
  - tests/group/seat-claim.test.ts
  - tests/group/rsvp-identity.test.ts
autonomous: true
requirements: [GROUP-05, GROUP-03]
must_haves:
  truths:
    - "D-112: concurrent →yes RSVPs never exceed capacity_snapshot — enforced by SELECT capacity_snapshot FOR UPDATE + count under the lock, PROVEN by a two-connection makeRacingClients race (the phase acceptance gate, mutation-verified)"
    - "D-112: the seat-claim is a pessimistic row lock, NOT an app-level count-then-insert (the CLAUDE.md anti-pattern)"
    - "D-113: only a confirmed 'yes' consumes a seat; yes→no frees a seat which is immediately re-claimable; a partial RSVP leaves the booking valid"
    - "D-111: the cap reads capacity_snapshot only, never live listing.maxOccupancy (a host lowering capacity after confirmations cannot over-cap)"
    - "D-116: identity is ONE rsvp row with nullable user_id; de-dup by user_id (account) or normalized email (guest), single-path"
    - "D-118: the invite/manage token reuses the reference.ts Crockford encoder but ~100 bits over 16-20 bytes (a bearer credential, not the 8-char display label)"
  artifacts:
    - path: "src/lib/group/seat-claim.ts"
      provides: "claimSeat(db, {groupId, answer, userId, guestEmailNorm, name}) — the FOR UPDATE transaction returning ClaimResult"
      min_lines: 30
      contains: "FOR UPDATE"
    - path: "src/lib/group/token.ts"
      provides: "crypto-random Crockford invite + manage token minting (~100 bits)"
      contains: "randomBytes"
    - path: "tests/group/seat-claim-race.test.ts"
      provides: "the D-112 two-connection acceptance-gate race test (each racer a FULL transaction via client.begin)"
      contains: "makeRacingClients"
  key_links:
    - from: "src/lib/group/seat-claim.ts"
      to: "booking_group.capacity_snapshot"
      via: "SELECT ... FOR UPDATE row lock (D-112)"
      pattern: "FOR UPDATE"
    - from: "tests/group/seat-claim-race.test.ts"
      to: "claimSeat"
      via: "client.begin per racer (full-tx, not autocommit — Pitfall 1)"
      pattern: "\\.begin\\("
---

<objective>
Build the ONE genuinely new correctness surface of Phase 8 — the pessimistic per-group RSVP seat-claim (D-112, GROUP-05/SC#4) — red-first, and mint the crypto-random invite/manage token (D-118). The seat-claim's two-connection `makeRacingClients` race test is the phase's non-negotiable acceptance gate and MUST be mutation-verified (delete `FOR UPDATE` → red → restore), exactly like Phase 3's exclusion-race test.

Purpose: The row lock on the single `booking_group` row is the atomic no-overflow authority — the app never adjudicates the cap, mirroring the GiST EXCLUDE philosophy. Everything money-related is untouched (D-107).
Output: `src/lib/group/{token,seat-claim}.ts` + three Wave-0 integration test files, all green, the race mutation-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-RESEARCH.md
@.planning/phases/08-group-bookings/08-PATTERNS.md
@.planning/phases/08-group-bookings/08-VALIDATION.md

<interfaces>
<!-- The claimSeat contract (RESEARCH §Pattern 1 drafts the full body). -->
export type ClaimResult = { ok: true; rsvpId: string; status: "yes" | "no" } | { ok: false; reason: "full" }
export async function claimSeat(db: DbConn, args: { groupId: string; answer: "yes" | "no"; userId: string | null; guestEmailNorm: string | null; name: string }): Promise<ClaimResult>
<!-- DbConn is imported from @/lib/availability/read-model (same type createPendingHold binds), so a test can inject an isolated schema. -->
<!-- token.ts: reuse the reference.ts CROCKFORD alphabet + byte%32 encoder, but over randomBytes(16..20); NO "FIT-" prefix (bearer credential). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 seat-claim tests RED (race gate + toggle/free + identity de-dup)</name>
  <files>tests/group/seat-claim-race.test.ts, tests/group/seat-claim.test.ts, tests/group/rsvp-identity.test.ts</files>
  <read_first>
    - tests/availability/exclusion-race.test.ts:10-11 (mutation-verify discipline statement), :61-79 (setupTestDb + fixture inserts), :81-108 (the race assertion skeleton to clone)
    - tests/helpers/db.ts:52-67 (makeRacingClients — independent max:1 pools = true concurrency; the shared testDb.client serializes and proves nothing)
    - .planning/phases/08-group-bookings/08-RESEARCH.md §Pattern 2 + Pitfall 1 (the CRITICAL divergence: each racer must run a FULL transaction via client.begin, NOT an autocommit INSERT)
    - src/lib/db/schema.ts (bookingGroup + rsvp defs from 08-01 — fixture shape)
  </read_first>
  <behavior>
    - seat-claim-race: with capacity_snapshot=1 and 3 racers each running claimSeat as a full transaction via client.begin, exactly ONE 'yes' row survives; re-count via testDb.client asserts count = 1 (no overflow). Racers a/b/c end their pools in finally.
    - seat-claim (toggle/free): a 'yes' then same-identity 'no' frees the seat (count drops); a freed seat is re-claimable by a different identity up to snapshot; an 'already-yes → yes' is a no-op for the count (one rsvp = one row, D-120); a partial RSVP (some yes, some no, some unanswered) leaves the booking row unchanged and valid (D-113).
    - seat-claim (snapshot-not-live): after lowering listing.maxOccupancy below the current yes-count, a new claim still reads capacity_snapshot and rejects only against the snapshot, never the lowered live value (D-111).
    - rsvp-identity: a guest (userId null, guest_email_norm null) inserts a name-only row; an account (userId set) and a guest-with-email (guest_email_norm set) each get exactly one row; a second submit by the same user_id UPDATES the existing row (no duplicate); a second submit by the same normalized email UPDATES; two name-only guests are NOT de-duped (D-117).
  </behavior>
  <action>
    Create the three files under `tests/group/`. Clone the `setupTestDb`/fixture/`teardownTestDb` boilerplate from exclusion-race.test.ts and insert a `user` + `listing` (maxOccupancy small) + a `booking_group` fixture (capacity_snapshot = the test's cap). For seat-claim-race, inline the three seat-claim statements (`SELECT capacity_snapshot ... FOR UPDATE`, `SELECT count(*) ... status='yes'`, conditional INSERT) INSIDE each racer's `client.begin(async (sql) => {...})` so the lock window is genuinely raced — a raw single INSERT would pass against a broken unlocked claim (Pitfall 1). For the other two files, call `claimSeat(testDb.db, {...})` directly. These tests import `claimSeat` from `@/lib/group/seat-claim` (not yet created) and MUST fail (RED) — a missing module / unimplemented behavior.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/group/` runs the three files and they FAIL (RED — claimSeat not yet implemented), NOT error-collecting on a missing test-db (the harness must set up)
    - seat-claim-race.test.ts uses `makeRacingClients` AND `client.begin(` per racer (grep both patterns present) — the full-tx divergence from the autocommit template
    - the race test asserts a final `count(*) ... status='yes'` equal to the snapshot, not merely "one fulfilled promise"
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/ 2>&1 | grep -q "seat-claim" && echo "tests present (expected RED)"</automated>
  </verify>
  <done>Three Wave-0 test files exist and fail for the right reason (behavior not yet implemented); the race harness opens real independent connections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token.ts + seat-claim.ts to GREEN; mutation-verify the race gate</name>
  <files>src/lib/group/token.ts, src/lib/group/seat-claim.ts</files>
  <read_first>
    - src/lib/booking/reference.ts:10-32 (the bias-free CROCKFORD `byte % 32` encoder to reuse; NOTE the contrast at :42-44 — the invite token is a RANDOM access gate like makeBookingReference, never a sha256-DERIVED label)
    - src/lib/availability/units.ts:296, :357-359, :451-497 (the db.transaction / SAVEPOINT / bounded-40P01-retry envelope; bind EVERY statement to `tx`; DbConn import site)
    - src/lib/notifications.ts:144-151 (the `count(*)::int` + `tx.execute(sql\`...\`)` house idiom — count is bigint, cast in SQL never JS)
    - .planning/phases/08-group-bookings/08-RESEARCH.md §Pattern 1 (the full drafted claimSeat body) + §Code Examples
  </read_first>
  <action>
    `token.ts`: export `makeInviteToken()` and `makeManageToken()` that reuse reference.ts's CROCKFORD alphabet and bias-free `byte % 32` mapping over `randomBytes(16)`..`randomBytes(20)` (~100+ bits, D-118 / RESEARCH Pitfall 4), with NO `FIT-` prefix (it is a bearer credential, not a display label). Never log a token. `seat-claim.ts`: implement `claimSeat(db, args): Promise<ClaimResult>` per RESEARCH §Pattern 1 — a `db.transaction` that (1) `SELECT capacity_snapshot FROM booking_group WHERE id=${groupId} FOR UPDATE` (courtesy fail-closed `{ok:false, reason:"full"}` if the group is missing/voided), (2) resolves an EXISTING row by `user_id` (accounts) or `guest_email_norm` (guests-with-email) — name-only guests never match, (3) `SELECT count(*)::int AS yes ... status='yes'` under the lock, (4) rejects a `→yes` that would exceed the snapshot (excluding an already-yes toggling to yes), else UPDATE the existing row or INSERT a new rsvp with a `randomUUID()` id. Bind every statement to `tx`. Keep the bounded outer 40P01-retry envelope from units.ts (contention is on one row so deadlock is rare, but keep the idiom). RETURN a ClaimResult — do NOT emit any notification inside the tx (post-commit is the caller's job, 08-06). After GREEN, mutation-verify the acceptance gate: delete `FOR UPDATE` from the SELECT, confirm seat-claim-race goes RED at snapshot=1 with 3 racers, then restore. Record the mutation result in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/group/` — all three files GREEN
    - MUTATION-VERIFIED: removing `FOR UPDATE` (or the ceiling check) turns seat-claim-race.test.ts RED (2+ committed 'yes' at snapshot=1), then restoring turns it GREEN — recorded in the SUMMARY (the phase acceptance gate)
    - `grep -c "FOR UPDATE" src/lib/group/seat-claim.ts` returns >= 1 (the row lock is present, D-112)
    - `grep -v '^//' src/lib/group/seat-claim.ts | grep -c "emitNotify\|inngest.send\|sendGroup"` returns 0 (no notification emitted inside the seat-claim, PATTERNS post-commit contract)
    - `npx tsc --noEmit` and `npx eslint src/lib/group/` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/</automated>
  </verify>
  <done>claimSeat + token modules exist and pass; the D-112 race gate is mutation-verified; no notification crosses the transaction boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent RSVP writers → one booking_group row | untrusted concurrency crosses here; overflow must be impossible |
| token generation → shareable URL | a bearer credential is minted here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering (integrity) | claimSeat cap | mitigate | `SELECT capacity_snapshot FOR UPDATE` + count under the lock; proven by the two-connection makeRacingClients race, mutation-verified (delete FOR UPDATE → red → restore) |
| T-08-04 | Information Disclosure | invite/manage token | mitigate | ~100-bit `node:crypto` randomBytes over the bias-free Crockford encoder; non-enumerable; never logged (D-118, V6) |
| T-08-05 | Tampering | host lowers capacity after confirmations | mitigate | the claim reads capacity_snapshot only, never live listing.maxOccupancy (D-111); asserted by the snapshot-not-live test |
| T-08-SC | Tampering | npm installs | accept | no packages added this phase |
</threat_model>

<verification>
- `npx vitest run tests/group/` green; the seat-claim-race mutation-verified; `npx tsc --noEmit` + eslint clean.
</verification>

<success_criteria>
- The D-112 seat-claim proves atomic no-overflow under a genuine race (GROUP-05/SC#4); toggle/free/re-claim and snapshot-not-live hold; identity de-dup is single-path (D-116); the invite/manage token is a ~100-bit crypto bearer credential (D-118).
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-02-SUMMARY.md` when done — include the FOR UPDATE mutation-verification result.
</output>
