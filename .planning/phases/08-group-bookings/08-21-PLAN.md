---
phase: 08-group-bookings
plan: 21
type: execute
wave: 12
depends_on: [08-18, 08-19]
gap_closure: true
closes: [08-19 case-4 finding (repeat expire returns HTTP 400 'already expired', NOT the 200-replay 08-18 assumed — a recovery-path livelock + a shipped false comment)]
files_modified:
  - src/lib/paymongo.ts
  - src/app/actions/booking.ts
  - tests/payments/paymongo-calls.test.ts
  - tests/paymongo/checkout-idempotency-real.test.ts
autonomous: true
requirements: [GROUP-01]
must_haves:
  truths:
    - "expireCheckoutSession is IDEMPOTENT: a repeat expire of an already-expired session RESOLVES (its postcondition — the session is not payable — already holds), because PayMongo returns HTTP 400 'already expired' (probed live in 08-19 case 4, NOT the 200-replay 08-12 contract 1 assumed) and the wrapper now tolerates exactly that 400 as success"
    - "The double-charge protection is UNCHANGED for GENUINE failures: a 500, a network error, or any 400 whose detail is NOT 'already expired' still THROWS, so confirmBooking and updateDeclaredPax still fail-closed and refuse to leak a second payable session — only the benign already-expired case is tolerated"
    - "The recovery-path livelock is closed for BOTH callers at the shared primitive: after a createCheckoutSession failure that follows a successful expire, the booker's retry re-expires the now-expired id, expireCheckoutSession resolves (tolerating the 400), and the retry proceeds to mint a fresh session — no permanent fail-closed loop in confirmBooking OR updateDeclaredPax"
    - "No shipped comment asserts the falsified belief: booking.ts's NOT-TRAPPED RECOVERY block and paymongo.ts's expireCheckoutSession doc no longer claim a repeat expire 'replays PayMongo's prior 200 via the stable Idempotency-Key' — they state the probed truth (repeat expire → 400 'already expired', tolerated in the catch; the key is not honored on the expire endpoint either)"
  artifacts:
    - path: "src/lib/paymongo.ts"
      provides: "expireCheckoutSession tolerates HTTP 400 'already expired' as idempotent success; genuine failures still throw"
      contains: "already expired"
    - path: "tests/payments/paymongo-calls.test.ts"
      provides: "fetch-stub unit proof: 400 'already expired' resolves; other 400 / 500 throw — mutation-pinned"
      min_lines: 30
  key_links:
    - from: "src/app/actions/booking.ts confirmBooking (expire catch)"
      to: "expireCheckoutSession tolerating already-expired"
      via: "a repeat expire on an already-expired id resolves, so the catch does not fire on the benign case"
      pattern: "expireCheckoutSession"
    - from: "src/lib/paymongo.ts expireCheckoutSession"
      to: "HTTP 400 'already expired' → resolve { id }"
      via: "try/catch around paymongoFetch matching the 400 already-expired detail"
      pattern: "already expired"
---

<objective>
Close the NEW finding 08-19's live probe surfaced (case 4): **a repeat expire of the same checkout session returns `HTTP 400: "Checkout session is already expired"` and THROWS — it is NOT the "no-op that replays PayMongo's prior 200" that 08-18 assumed.** That falsified assumption left two defects in the just-shipped double-charge fix:

1. **A shipped code comment now contradicts a probe we ran.** `src/app/actions/booking.ts:599-605` (the NOT-TRAPPED RECOVERY block) asserts a repeat expire "RESOLVES (does not throw)"; `src/lib/paymongo.ts:234-239` asserts "expiring one session twice must be a no-op, which the stable key gives us." Both are now proven false. Shipping a comment that asserts disproven provider behaviour is exactly the blocking anti-pattern this whole phase exists to eliminate (`.continue-here.md` → `comment-asserts-unverified-external-behaviour`).

2. **A real recovery-path livelock.** In both `confirmBooking` and `updateDeclaredPax`, the expire is fail-closed: a thrown expire records a `checkout_expire_failed` / `needs_attention` audit and REFUSES. If `createCheckoutSession` fails in the window AFTER a successful expire, the booking row still names the now-expired session id. The booker's retry re-expires that id → **HTTP 400 throw → catch → refuse → every subsequent retry hits the same 400 → the booker can never complete checkout** until an operator clears it by hand. It is fail-closed-SAFE (a denial, never a double-charge — the primary blocker stays fixed), but permanent once triggered.

**The fix belongs at the shared primitive, not the call sites.** Make `expireCheckoutSession` IDEMPOTENT: a repeat expire of an already-expired session should RESOLVE, because the whole postcondition of expire ("this session can never be paid") already holds for an already-expired session. Tolerating exactly the `400 "already expired"` detail — and ONLY that — repairs the livelock in `confirmBooking` AND `updateDeclaredPax` at once, while a 500 / network error / any other 400 still throws so genuine failures still fail-closed. Then correct the two false comments to the probed truth.

**Scope boundary — this does NOT weaken the double-charge fix.** The tolerated case is precisely "the session is already non-payable"; every failure mode that could leave a session STILL payable still throws and still refuses. The accepted residuals stay accepted: the concurrent double-click (T-08-79) and the already-captured qrph double charge (T-08-74).

Purpose: remove a false provider-behaviour comment and a recovery-path livelock from the money path, proven against the same real API that found them.
Output: an idempotent `expireCheckoutSession`, two comments that tell the probed truth, a fetch-stub unit proof, and 08-19's case 4 updated from "documents the throw" to "proves the wrapper tolerates it."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-group-bookings/deferred-items.md
@.planning/phases/08-group-bookings/.continue-here.md
@.planning/phases/08-group-bookings/08-19-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration required. -->

The error shape — src/lib/paymongo.ts:74-101 paymongoFetch throws a PLAIN Error on non-2xx:
```typescript
const detail = (json as { errors?: Array<{ detail?: string }> })?.errors?.[0]?.detail ?? res.statusText;
throw new Error(`PayMongo ${method} ${path} failed (${res.status}): ${detail}`);
```
So a repeat expire throws exactly: `Error("PayMongo POST /v1/checkout_sessions/{id}/expire failed (400): Checkout session is already expired")`. There is NO typed status field — the only signal on the caught error is `err.message`. Do NOT change paymongoFetch (it is on every money-movement path); tolerate the case locally in expireCheckoutSession, matching the message format defined in the same file.

The observed live evidence (08-19 case 4, recorded in 08-19-SUMMARY.md): repeat expire of `cs_84bdb4eb16ef8db016a031b9` → `HTTP 400: Checkout session is already expired`.

The function to change — src/lib/paymongo.ts:245-255 expireCheckoutSession (current — throws through):
```typescript
export async function expireCheckoutSession(id: string): Promise<{ id: string }> {
  const json = await paymongoFetch<{ data: { id: string } }>(
    `/v1/checkout_sessions/${id}/expire`,
    { method: "POST", idempotencyKey: `checkout-expire:${id}` },
  );
  return { id: json.data.id };
}
```

The two false comments to correct:
- src/lib/paymongo.ts:234-239 — "Expiring one session twice must be a no-op, which the stable key gives us." (the session-scoped-key reasoning; the key is NOT honored — 08-19 proved the repeat 400s).
- src/app/actions/booking.ts:599-605 — the NOT-TRAPPED RECOVERY block claiming the repeat "replays PayMongo's prior 200 … so it RESOLVES (does not throw)".

The callers this fix repairs WITHOUT touching them:
- src/app/actions/booking.ts:613-625 (confirmBooking expire-before-create — catch refuses on throw)
- src/app/actions/booking.ts:~421-438 (updateDeclaredPax — same expire-then-catch-refuse shape; the same livelock; the same primitive-level fix repairs it)

The fetch-stub test idiom — tests/payments/paymongo-calls.test.ts drives the REAL paymongo.ts functions with `global.fetch` stubbed (vi.fn / vi.stubGlobal). Reuse it: stub a Response with the 400 body `{ errors: [{ detail: "Checkout session is already expired" }] }` and `ok:false, status:400`. No DB, no live API.

The mock — tests/helpers/mocks.ts mockPayMongo.expireCheckoutSession already RESOLVES (echoes the id); it faithfully models the FIXED wrapper (resolve-on-repeat), so no mock churn is needed and 08-18's confirm-double-submit case (5) stays valid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make expireCheckoutSession idempotent (tolerate 400 'already expired') + correct the two false comments</name>
  <files>src/lib/paymongo.ts, src/app/actions/booking.ts</files>
  <read_first>
    - src/lib/paymongo.ts:74-101 (paymongoFetch — the exact thrown Error message format) AND :223-255 (expireCheckoutSession + its doc comment, the one to correct)
    - src/app/actions/booking.ts:584-625 (the EXPIRE-BEFORE-CREATE block — correct the :599-605 NOT-TRAPPED RECOVERY sub-comment; leave the create-endpoint comments at :585-592 and :627-644, already corrected by 08-18, untouched) AND :415-438 (updateDeclaredPax expire — the second caller this fix silently repairs; do NOT modify its code)
    - .planning/phases/08-group-bookings/08-19-SUMMARY.md (the verbatim observed 400 evidence)
  </read_first>
  <behavior>
    - expireCheckoutSession(id): on 2xx, resolves { id } (unchanged). On a caught error whose message is a 400 that names the session already expired, RESOLVES with { id } (idempotent success — the session is already non-payable). On ANY other error (500, network, a 400 with a different detail), RETHROWS unchanged.
    - No change to confirmBooking / updateDeclaredPax code — they inherit the idempotent primitive.
  </behavior>
  <action>
    **1a. Make expireCheckoutSession tolerate the already-expired 400.** Wrap the paymongoFetch call in try/catch:
    ```typescript
    export async function expireCheckoutSession(id: string): Promise<{ id: string }> {
      try {
        const json = await paymongoFetch<{ data: { id: string } }>(
          `/v1/checkout_sessions/${id}/expire`,
          { method: "POST", idempotencyKey: `checkout-expire:${id}` },
        );
        return { id: json.data.id };
      } catch (err) {
        // IDEMPOTENT EXPIRE (08-19 case 4, probed live). A repeat expire of an already-expired session
        // returns HTTP 400 "Checkout session is already expired" — NOT the 200-replay the session-scoped
        // Idempotency-Key was once believed to give (the key is not honored on this endpoint either). But
        // the POSTCONDITION of expire — this session can never be paid — ALREADY HOLDS for an
        // already-expired session, so tolerating exactly that 400 as success is correct, and it is what
        // makes the confirmBooking / updateDeclaredPax post-expire-then-create-failure RETRY recover
        // instead of livelocking on a permanent fail-closed refusal.
        //
        // ⚠️ ONLY the already-expired 400 is tolerated. A 500, a network error, or any other 400 detail
        // still THROWS — so a session that might STILL be payable is never silently treated as retired, and
        // the callers' fail-closed refusal (the double-charge guard) is fully preserved.
        const msg = err instanceof Error ? err.message : String(err);
        if (/\(400\)/.test(msg) && /already\b.*\bexpired/i.test(msg)) {
          return { id };
        }
        throw err;
      }
    }
    ```

    **1b. Correct the expireCheckoutSession doc comment — src/lib/paymongo.ts:234-239.** The paragraph beginning "⚠️ THE KEY IS SCOPED TO THE SESSION ID …" asserts "Expiring one session twice must be a no-op, which the stable key gives us." Replace that false mechanism with the probed truth — keep the (still-correct) point that a session-scoped key must never be booking-scoped, but state that idempotence comes from tolerating the 400, not from a key replay:
    ```
     * ⚠️ THE KEY IS SCOPED TO THE SESSION ID, NEVER TO THE BOOKING. A booking legitimately owns MORE THAN
     * ONE session over its life (a re-price creates exactly that), so a booking-scoped key would let one
     * session's expire be confused with another's. But the key is NOT what makes a repeat expire safe:
     * PayMongo does NOT honor the Idempotency-Key on this endpoint (08-19 probed it — a repeat expire
     * returns HTTP 400 "already expired", not a replayed 200). Idempotence is provided HERE, in the catch
     * below, which tolerates exactly that already-expired 400 as success because the session is already
     * non-payable. Every other failure still throws.
    ```
    Also update the trailing "Failures THROW … deliberately not swallowed" paragraph (:241-243) so it is no longer absolute — it now reads that GENUINE failures throw while the benign already-expired 400 is tolerated:
    ```
     * GENUINE failures (500, network, any non-already-expired 4xx) THROW through paymongoFetch's descriptive
     * error shape and are NOT swallowed — the caller catches, records a `needs_attention` audit and REFUSES,
     * because proceeding on a session that might still be payable costs an unrefunded double capture. The one
     * tolerated case is the already-expired 400 (see the catch) — retiring an already-retired session is the
     * success the caller wanted, not a failure to refuse over.
    ```

    **1c. Correct the NOT-TRAPPED RECOVERY comment — src/app/actions/booking.ts:599-605.** Replace the falsified block (which claims the repeat "replays PayMongo's prior 200 … so it RESOLVES (does not throw)") with the probed truth:
    ```
    // NOT-TRAPPED RECOVERY: if createCheckoutSession fails AFTER this expire succeeds, the column still names
    // the (now-expired) old id. On the booker's retry, this block expires that id AGAIN — and a repeat expire
    // returns HTTP 400 "already expired" (08-19 case 4, probed live — NOT a replayed 200; the Idempotency-Key
    // is not honored on the expire endpoint either). expireCheckoutSession TOLERATES exactly that 400 as
    // success (the session is already non-payable), so the retry RESOLVES here and proceeds to mint a fresh
    // session — never a permanent fail-closed loop. A genuine expire failure (500 / network / any other 400)
    // still throws and still refuses below.
    ```
  </action>
  <acceptance_criteria>
    - expireCheckoutSession tolerates the already-expired 400: `grep -c "already expired" src/lib/paymongo.ts` returns at least 2 (the catch match-comment + the doc-comment truth), and the code matches BOTH a 400 and an already-expired message — `grep -c "already\\\\b.*\\\\bexpired" src/lib/paymongo.ts` returns at least 1 (the regex).
    - The tolerate branch returns without rethrowing only on the matched case: `grep -c "throw err;" src/lib/paymongo.ts` returns at least 1 (the else-rethrow is present — genuine failures still throw).
    - The two false claims are GONE: `grep -c "must be a no-op, which the stable key gives us" src/lib/paymongo.ts` returns 0; `grep -c "replays PayMongo's prior 200" src/app/actions/booking.ts` returns 0; `grep -c "RESOLVES (does not throw)" src/app/actions/booking.ts` returns 0.
    - The probed truth is stated in booking.ts: `grep -c "returns HTTP 400" src/app/actions/booking.ts` returns at least 1 (the corrected recovery comment).
    - No caller code changed: `git diff src/app/actions/booking.ts` shows ONLY comment lines changed inside confirmBooking's expire block (no change to the `if (bk.checkoutSessionId != null)` logic, no change to updateDeclaredPax code).
    - paymongoFetch is untouched: `git diff src/lib/paymongo.ts` shows no change between lines 74-101.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>expireCheckoutSession resolves on a repeat (already-expired) expire and still throws on every genuine failure; the two false "repeat expire replays a 200" comments state the probed truth; no caller code and no paymongoFetch changed; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetch-stub unit proof of the idempotent expire + update 08-19 case 4 to prove tolerance against the real API</name>
  <files>tests/payments/paymongo-calls.test.ts, tests/paymongo/checkout-idempotency-real.test.ts</files>
  <read_first>
    - tests/payments/paymongo-calls.test.ts (the ENTIRE file — reuse its global.fetch stubbing idiom: how it builds a Response with ok/status/json/text, how it asserts a call throws, and its beforeEach/afterEach reset)
    - src/lib/paymongo.ts:74-101 + the expireCheckoutSession changed in Task 1 (the message format the stub must reproduce so the tolerate-branch fires)
    - tests/paymongo/checkout-idempotency-real.test.ts (08-19 — case 4 currently documents that the repeat expire throws; it becomes a proof that expireCheckoutSession TOLERATES it)
  </read_first>
  <behavior>
    - Unit (no DB, no live API): expireCheckoutSession RESOLVES to { id } when the stubbed fetch returns 400 with detail "Checkout session is already expired"; THROWS when the stub returns 400 with a different detail; THROWS when the stub returns 500; RESOLVES on 200.
    - Real API (gated, opt-in): case 4 asserts a SECOND expireCheckoutSession on the same id RESOLVES (the wrapper tolerates the underlying 400), instead of asserting it throws.
  </behavior>
  <action>
    **2a. Add a fetch-stub describe to tests/payments/paymongo-calls.test.ts** — `describe("expireCheckoutSession — idempotent on already-expired (08-21 / 08-19 case 4)")`, cloning the file's existing global.fetch stub idiom. Open with a MUTATION-VERIFY header:
    ```
    // MUTATION-VERIFY (run before committing): in src/lib/paymongo.ts expireCheckoutSession, delete the
    // tolerate branch (the `if (/\(400\)/… already… expired/) return { id };`) so it rethrows every error →
    // the "resolves on already-expired 400" case goes RED. Restore → GREEN. Proves the tolerance is real,
    // not incidental.
    ```
    Cases (stub `global.fetch` per case; build the Response so paymongoFetch sees `ok:false, status:400/500` and a body `{ errors: [{ detail }] }`):
    - **(a) already-expired 400 RESOLVES.** Stub → `{ ok:false, status:400, text:async()=>JSON.stringify({ errors:[{ detail:"Checkout session is already expired" }] }) }`. Assert `await expireCheckoutSession("cs_x")` resolves to `{ id: "cs_x" }` (the id echoed from the argument, since no 200 body is parsed).
    - **(b) a DIFFERENT 400 THROWS.** Stub → 400 with detail `"amount is invalid"`. Assert `await expect(expireCheckoutSession("cs_y")).rejects.toThrow(/amount is invalid/)`.
    - **(c) a 500 THROWS.** Stub → `{ ok:false, status:500, … detail:"internal server error" }`. Assert it rejects (the double-charge guard is preserved for genuine failures).
    - **(d) a 200 RESOLVES with the provider id.** Stub → `{ ok:true, status:200, text:async()=>JSON.stringify({ data:{ id:"cs_z" } }) }`. Assert `{ id: "cs_z" }`.
    Assert the request shape once: `global.fetch` was called with a path ending `/v1/checkout_sessions/cs_x/expire` and method POST (reuse whatever assertion helper the file already uses).

    **2b. Update 08-19 case 4 in tests/paymongo/checkout-idempotency-real.test.ts.** It currently creates a session, expires once, then documents that the SECOND expire throws (the NEW finding). Now that expireCheckoutSession tolerates the already-expired 400, flip the assertion to PROVE the fix end-to-end against the real API: the second `expireCheckoutSession(s.id)` must RESOLVE.
    ```typescript
    // 08-21: expireCheckoutSession is now IDEMPOTENT — a repeat expire of an already-expired session
    // resolves (the wrapper tolerates PayMongo's 400 "already expired"). This is the live proof of the fix
    // for the livelock 08-19 originally FOUND here.
    await expireCheckoutSession(s.id);                    // first expire — retires it
    await expect(expireCheckoutSession(s.id)).resolves.toBeDefined();  // repeat — tolerated, no throw
    ```
    Keep the case gated behind `runLive` and self-cleaning (s.id is already tracked in `created`). Do NOT un-gate it.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/payments/paymongo-calls.test.ts` exits 0 with the 4 new cases (a-d) passing.
    - The resolve-on-already-expired case is present: `grep -c "already expired" tests/payments/paymongo-calls.test.ts` returns at least 1, and the genuine-failure-throws cases are present: `grep -c "rejects" tests/payments/paymongo-calls.test.ts` returns at least 2 (the different-400 and the 500).
    - MUTATION CHECK (perform, record the RED line in the SUMMARY, revert): delete the tolerate branch from expireCheckoutSession → `npx vitest run tests/payments/paymongo-calls.test.ts` FAILS case (a) [b/c/d stay GREEN]; restore → GREEN.
    - 08-19 case 4 now asserts resolution, not a throw: `grep -c "resolves.toBeDefined" tests/paymongo/checkout-idempotency-real.test.ts` returns at least 1, and `grep -c "THREW\|toThrow" tests/paymongo/checkout-idempotency-real.test.ts` in the case-4 region no longer asserts the repeat throws (the file may still describe the historical finding in a comment).
    - Re-run the OPTED-IN real-API proof to confirm the fix live: `RUN_LIVE_PAYMONGO_PROBE=1` with the real `sk_test_` key → `npx vitest run tests/paymongo/checkout-idempotency-real.test.ts` exits 0 with 4 cases RUN and case 4 now GREEN on resolution. Record the outcome in the SUMMARY. (If skipped, say so — a skipped real-API test is not proof.)
    - Full suite `npx vitest run` (default, no opt-in) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/payments/paymongo-calls.test.ts</automated>
  </verify>
  <done>A fetch-stub unit proof pins that expireCheckoutSession resolves on an already-expired 400 and throws on every genuine failure (mutation-verified), and 08-19's case 4 now PROVES the wrapper tolerates the repeat against the real API instead of documenting the livelock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirmBooking / updateDeclaredPax → expireCheckoutSession | The caller's fail-closed refusal depends on the primitive throwing ONLY on genuine failures |
| expireCheckoutSession → PayMongo /expire | A repeat expire returns 400 "already expired"; a genuine failure returns 500 / network error / other 4xx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-84 | Denial (booker) | post-expire-then-create-failure retry livelock | mitigate | expireCheckoutSession tolerates the already-expired 400 as success, so the retry resolves and proceeds — closing the permanent fail-closed loop in BOTH confirmBooking and updateDeclaredPax at the shared primitive. Proven by the fetch-stub unit case (a) and 08-19's live case 4. |
| T-08-85 | Repudiation / correctness | over-tolerant catch swallows a genuine failure | mitigate | ONLY a message matching BOTH `(400)` AND `already…expired` resolves; a 500, a network error, or any other 400 rethrows. Pinned by unit cases (b) a different 400 and (c) a 500, which must still throw — so a session that might still be payable is never silently treated as retired and the double-charge guard is preserved. |
| T-08-86 | Info-integrity | a shipped comment asserts disproven provider behaviour | mitigate | The two false "repeat expire replays a 200 via the Idempotency-Key" comments (paymongo.ts, booking.ts) are rewritten to the probed truth (400 "already expired", tolerated in the catch). Grep-gated to 0 in Task 1. |
| T-08-79 | Tampering (concurrent) | two near-simultaneous confirmBooking calls | accept | Unchanged by this plan — still the accepted unlocked read-then-act residual from 08-18. |
| T-08-74 | (residual) | already-captured double charge on qrph | accept | Unchanged — this plan prevents a livelock, it does not add a refund rail. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/payments/paymongo-calls.test.ts` exits 0 (4 new cases; mutation-verified).
- `npx vitest run tests/booking/confirm-double-submit.test.ts tests/booking/checkout-session-expire.test.ts` exits 0 (08-18 / CR-02 proofs still hold — the mock already models resolve-on-repeat, so no regression).
- OPTED IN: `RUN_LIVE_PAYMONGO_PROBE=1 PAYMONGO_SECRET_KEY=sk_test_… npx vitest run tests/paymongo/checkout-idempotency-real.test.ts` runs 4 cases green, case 4 now proving tolerance; SUMMARY records it.
- The two false comments are gone and the probed truth is stated (grep gates in Task 1).
- Full suite `npx vitest run` exits 0 (Docker Postgres up; not during a live UAT).
</verification>

<success_criteria>
- expireCheckoutSession is idempotent — a repeat expire of an already-expired session resolves — while every genuine failure still throws, so the double-charge fail-closed guard is fully preserved.
- The recovery-path livelock is closed for both confirmBooking and updateDeclaredPax at the shared primitive, with no caller code changed.
- No shipped comment asserts the falsified "repeat expire replays a 200" belief; both state the probed truth.
- The fix is proven by a mutation-pinned fetch-stub unit test and by 08-19's live case 4 (when opted in), which now proves tolerance instead of documenting the livelock.
</success_criteria>

<output>
After completion, create `.planning/phases/08-group-bookings/08-21-SUMMARY.md` (use the summary template). Record the mutation-verify RED line for unit case (a), whether the opted-in real-API run was performed and case 4's live outcome, and confirm `completed_phases` remains 7 (do not touch phase-closure state).
</output>
</content>
</invoke>
