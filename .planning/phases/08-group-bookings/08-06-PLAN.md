---
phase: 08-group-bookings
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-04]
files_modified:
  - src/lib/validation/group.ts
  - src/lib/group/rsvp.ts
  - src/app/actions/group.ts
  - src/app/actions/cancel-booking.ts
  - tests/group/group-lifecycle.test.ts
  - tests/group/group-owner-scope.test.ts
  - tests/group/guest-email-guard.test.ts
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05]
must_haves:
  truths:
    - "D-119: createGroup is owner-gated — it re-checks status==='confirmed' AND bookerId===session.user.id server-side; a MISSING and a CROSS-USER booking return the SAME calm denial (the route group is never the gate)"
    - "D-111: createGroup snapshots capacity_snapshot = listing.maxOccupancy transactionally at creation (mirrors the D-67 tier snapshot)"
    - "GROUP-04: the organizer roster/headcount read is owner-scoped in the WHERE (a foreign row is UNREADABLE); non-organizer → the SAME bare 404; unknown=revoked=voided render the same calm state (no enumeration oracle)"
    - "D-117: the opt-in email guard — only an address that ACTIVELY submitted an RSVP is emailed, rate-limited, de-duped by user_id/normalized email; blank-email guests get NO send"
    - "D-121: on booking cancel/refund → auto-void all invites AND notify every reachable yes attendee (accounts + guests-with-email); blank-email guests are unreachable by design; organizer can removeAttendee (frees a seat via the D-112 claim) and regenerateLink (kills a leaked link)"
    - "D-120: submitRsvp refuses after RSVP closes (now() >= booking.startsAt, evaluated against the DB clock); one RSVP = one person, no +guests"
    - "notifications emit AFTER the seat-claim tx commits, never inside it (emitNotify + the guest-email fn); accounts → fitout/notify, guests-with-email → fitout/guest-email, blank → on-screen only"
  artifacts:
    - path: "src/app/actions/group.ts"
      provides: "createGroup / submitRsvp / removeAttendee / regenerateLink server actions"
      min_lines: 60
      contains: "claimSeat"
    - path: "src/lib/group/rsvp.ts"
      provides: "owner-scoped roster + headcount reads + group-by-token lookup (isoUtc boundary)"
      contains: "group_id"
    - path: "tests/group/group-owner-scope.test.ts"
      provides: "the IDOR + token-oracle security proof (clone bookings-owner-scope.test.ts, mutation-verified)"
      contains: "bookerId"
  key_links:
    - from: "src/app/actions/group.ts submitRsvp"
      to: "claimSeat"
      via: "the D-112 seat-claim (public path, token is the credential, no session required)"
      pattern: "claimSeat"
    - from: "src/app/actions/cancel-booking.ts"
      to: "booking_group.voided_at"
      via: "auto-void on both cancelBookingAsBooker and cancelBookingAsHost (D-121)"
      pattern: "voided_at"
---

<objective>
Build the group server actions and their lifecycle: `createGroup` (owner-gated, confirmed-only, snapshots the cap — GROUP-01/D-119/D-111), `submitRsvp` (the public token-credentialed path that calls `claimSeat` and emits notifications post-commit under the D-117 opt-in guard — GROUP-03/GROUP-05), `removeAttendee` + `regenerateLink` (organizer management — D-121), the owner-scoped `rsvp.ts` read helpers (roster/headcount/group-by-token — GROUP-04), and the D-121 auto-void-on-cancel wiring into both cancel paths. Prove IDOR + the token oracle with a mutation-verified security test.

Purpose: This is the group logic layer. It depends on the seat-claim (08-02) and the notification infra (08-04); the UI surfaces (management RSC, invite route) that call these actions are 08-07/08-08.
Output: `validation/group.ts`, `rsvp.ts`, `actions/group.ts`, cancel-booking auto-void, and three integration/security tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-RESEARCH.md
@.planning/phases/08-group-bookings/08-PATTERNS.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md

<interfaces>
<!-- claimSeat (08-02): claimSeat(db, {groupId, answer, userId, guestEmailNorm, name}): Promise<ClaimResult>. -->
<!-- token (08-02): makeInviteToken(), makeManageToken(). -->
<!-- notifications: emitNotify(payload) [accounts, fitout/notify]; inngest.send({name: GUEST_EMAIL_EVENT, data}) [guests-with-email, 08-04]. -->
<!-- rate-limit: rateLimit(key, {window, max}) → {ok:true}|{ok:false,retryAfter} (rate-limit.ts:37-57). -->
<!-- validation/group.ts (NEW, Zod 4): rsvpSchema { name: z.string().min(1); email: z.email().optional(); answer: z.enum(["yes","no"]) } ; createGroupSchema { bookingId }. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: validation/group.ts + rsvp.ts owner-scoped reads + group-by-token; group-owner-scope security test (mutation-verified)</name>
  <files>src/lib/validation/group.ts, src/lib/group/rsvp.ts, tests/group/group-owner-scope.test.ts</files>
  <read_first>
    - src/lib/validation/notification.ts:19,:67 (Zod 4 discriminatedUnion / z.email usage), src/lib/validation/listing.ts:25-48 (schema idioms)
    - src/lib/notifications.ts:144-151 (count(*)::int + tx.execute idiom), :164-196 (listRecent — owner scope in the WHERE, the isoUtc timestamptz-as-TEXT boundary, NOTIFICATIONS_MAX_LIMIT clamp)
    - src/lib/booking/bookings-query.ts (isoUtc / to_char boundary — db.execute returns timestamptz as TEXT; hydrate once)
    - tests/security/bookings-owner-scope.test.ts (the crossed A/B fixture + mutation-verify discipline to clone)
    - .planning/phases/08-group-bookings/08-PATTERNS.md (rsvp.ts section + Shared Patterns > Owner gate/IDOR)
  </read_first>
  <action>
    Create `src/lib/validation/group.ts` with `rsvpSchema` (name required, email optional via Zod 4 `z.email().optional()`, answer `z.enum(["yes","no"])`) and `createGroupSchema`. Create `src/lib/group/rsvp.ts` with: `getGroupByToken(db, token)` (token-scoped WHERE — the sole credential; returns the group + parent booking/listing display fields, or the SAME calm "not active" shape for an unknown OR voided token — no enumeration oracle), `getRoster(db, groupId)` and `getHeadcount(db, groupId)` (owner scope enforced by the CALLER's booking.bookerId check; count yes via `count(*)::int`; select timestamps through the isoUtc/to_char mask and hydrate once). Normalize guest email (lowercase/trim) with a shared `normalizeEmail` helper (the de-dup + opt-in-email key, D-117). Write `tests/group/group-owner-scope.test.ts` cloning bookings-owner-scope.test.ts (crossed A hosts/books, B books) proving a non-organizer read returns nothing and an unknown token renders identically to a voided one; MUTATION-VERIFY by deleting the bookerId predicate → red → restore.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/group/group-owner-scope.test.ts` GREEN, and MUTATION-VERIFIED (removing the owner predicate turns it RED, restoring turns it GREEN) — recorded in the SUMMARY
    - `getGroupByToken` returns the SAME shape for an unknown token and a voided token (grep/assert the calm state has no distinguishing field — no oracle)
    - roster/headcount reads select `count(*)::int` and hydrate timestamps once at the boundary (no raw db.execute Date cast); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/group-owner-scope.test.ts</automated>
  </verify>
  <done>Group validation + owner-scoped read helpers + token lookup exist; the IDOR/token-oracle test is mutation-verified.</done>
</task>

<task type="auto">
  <name>Task 2: actions/group.ts — createGroup / submitRsvp / removeAttendee / regenerateLink + opt-in email guard</name>
  <files>src/app/actions/group.ts, tests/group/group-lifecycle.test.ts, tests/group/guest-email-guard.test.ts</files>
  <read_first>
    - src/app/actions/cancel-booking.ts:15-33 (the session→owner-gate→rate-limit→atomic UPDATE→audit→notify→revalidate skeleton), :217-218 (missing==not-mine calm denial), :292 (emitNotify AFTER commit), :335-340 (revalidatePath)
    - src/app/actions/booking.ts:71,:361 (rateLimit(key,OPTS) + recordAudit on denial), placeHold emit-after-commit at :227
    - src/lib/rate-limit.ts:37-57 (rateLimit signature; early-return calm, never throw)
    - src/lib/group/seat-claim.ts (claimSeat — 08-02), src/lib/group/token.ts (makeInviteToken/makeManageToken), src/lib/group/rsvp.ts (getGroupByToken — Task 1)
    - src/inngest/functions/guest-email.ts + GUEST_EMAIL_EVENT (08-04), src/lib/notifications.ts emitNotify (accounts)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §2/§3 (management + RSVP contracts) + §Copywriting (D-117 opt-in framing)
  </read_first>
  <action>
    Create `src/app/actions/group.ts` ("use server"). `createGroup(bookingId)`: session → load booking JOINed to listing → re-check `status==='confirmed'` AND `bookerId===userId` (missing/cross-user → the SAME calm denial, D-119) → rate-limit + audit on denial → INSERT booking_group with `capacity_snapshot = listing.maxOccupancy` read transactionally (D-111) + `access_token = makeInviteToken()` (D-118) → revalidate `/bookings/[id]` + `/bookings/[id]/group`. `submitRsvp(token, {name,email,answer})`: the PUBLIC path — does NOT require a session (guest branch, D-116); resolve group via `getGroupByToken` (voided/unknown → calm "no longer active", no oracle); refuse if `now() >= booking.startsAt` (RSVP closed, D-120, DB clock); read the optional session for `userId`; normalize the optional email; call `claimSeat(db, {...})`; AFTER commit emit post-commit — organizer gets `group_rsvp_received` via emitNotify (account); an account attendee gets `group_rsvp_confirmed` via emitNotify; a guest-with-email gets `inngest.send(GUEST_EMAIL_EVENT,...)` UNDER the D-117 opt-in guard (only email an address that actively submitted this RSVP, rate-limited by normalized email, de-duped); a blank-email guest gets on-screen only (NO send). `removeAttendee(rsvpId)` + `regenerateLink(groupId)`: organizer-owner-gated exactly like cancel; removeAttendee frees a seat through the same claim path; regenerateLink mints a new access_token and the old one stops resolving. Write `group-lifecycle.test.ts` (create-only-on-confirmed-owned, token minted, regenerate kills old token, removeAttendee frees a seat) and `guest-email-guard.test.ts` (only an RSVP-submitting address is emailed; rate-limited; a blank-email guest triggers NO send — assert via mockResend/mock inngest.send call counts).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/group/` GREEN (all group tests including lifecycle + guest-email-guard)
    - guest-email-guard: a blank-email guest RSVP produces ZERO guest-email sends AND zero account notifications for the attendee (assert call counts); a guest-with-email produces exactly one guest-email send; a rate-limited repeat is suppressed
    - createGroup on a NON-confirmed or CROSS-USER booking returns the calm denial and creates NO booking_group row (assert)
    - `grep -c "emitNotify\|inngest.send\|GUEST_EMAIL_EVENT" src/app/actions/group.ts` shows emits present, and they occur AFTER the claimSeat call returns (post-commit, not inside a tx)
    - `npx tsc --noEmit` + eslint clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/</automated>
  </verify>
  <done>All four group actions exist and are owner/token gated; the public RSVP path claims a seat and notifies post-commit under the D-117 opt-in guard; lifecycle + guard tests green.</done>
</task>

<task type="auto">
  <name>Task 3: D-121 auto-void invites + notify attendees on booking cancel (both paths)</name>
  <files>src/app/actions/cancel-booking.ts</files>
  <read_first>
    - src/app/actions/cancel-booking.ts:361 (cancelBookingAsBooker) + :806 (cancelBookingAsHost) — the two post-commit consequence blocks (:292, :1015-1025) where D-121 auto-void hangs off; each already guards consequences so a throw can't 500 a committed cancellation (07-11 pattern)
    - src/lib/group/rsvp.ts getRoster (Task 1 — the reachable-yes attendees to notify), src/lib/group/seat-claim.ts (freeing semantics)
    - src/lib/db/schema.ts booking_group.voided_at (08-01)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §4 (group_cancelled recipient/channel) + §7 (the cancel-review consequence line is 08-07's cancel/page.tsx, NOT this action file)
  </read_first>
  <action>
    In BOTH `cancelBookingAsBooker` and `cancelBookingAsHost`, add a post-commit consequence: if the cancelled booking has a `booking_group`, set its `voided_at = now()` (invites stop accepting RSVPs — D-121) and notify every REACHABLE yes attendee that the group was cancelled — accounts + attendee-account via emitNotify `group_cancelled`, guests-with-email via `inngest.send(GUEST_EMAIL_EVENT, {kind:"group_cancelled",...})`; blank-email guests are unreachable by design (no send). Do this AFTER the existing money/state commit and inside the same guarded post-commit block so a notification failure cannot roll back or 500 the cancellation (07-11 discipline). Do NOT change the refund/money math (the organizer bought the slot; per-attendee refunds do not exist, D-114). This file does NOT touch cancel/page.tsx (the review-copy line is 08-07).
  </action>
  <acceptance_criteria>
    - a test (extend group-lifecycle.test.ts or cancel suite) proves cancelling a group booking sets booking_group.voided_at AND after that a submitRsvp on the token returns the calm "no longer active" state (invites voided)
    - `grep -c "voided_at\|voidedAt" src/app/actions/cancel-booking.ts` >= 1 and the auto-void runs in BOTH cancel functions
    - `npx vitest run tests/booking/ tests/group/` GREEN (no cancellation/refund regression)
    - `npx tsc --noEmit` + eslint clean; `npm run build` exits 0 (with documented env placeholders)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/booking/ tests/group/</automated>
  </verify>
  <done>Cancelling a group booking (booker or host) auto-voids its invites and notifies reachable attendees, without touching the refund math or risking the committed cancellation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public invite link → submitRsvp | unauthenticated, token-credentialed input crosses here |
| organizer → group management actions | owner authority must be re-checked in the action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Elevation / Info Disclosure (IDOR) | roster/headcount/removeAttendee/regenerateLink | mitigate | owner-gate on booking.bookerId in the action + owner scope in the read WHERE; non-owner/missing → same calm 404; mutation-verified (group-owner-scope test) |
| T-08-15 | Spoofing / Elevation | createGroup on someone else's / non-confirmed booking | mitigate | createGroup re-checks status==='confirmed' AND bookerId===userId server-side (D-119) |
| T-08-16 | Denial of Service (spam cannon) | shared invite link → mass RSVP emails | mitigate | D-117 opt-in guard: only email an address that actively submitted an RSVP; rate-limit via rate-limit.ts; de-dup by user_id/normalized email; blank-email guests get no send |
| T-08-17 | Information Disclosure | token oracle | mitigate | getGroupByToken renders unknown=revoked=voided identically (no distinguishable 404) |
| T-08-18 | Tampering | RSVP after session start / +guests | mitigate | submitRsvp refuses when now() >= startsAt (DB clock, D-120); one RSVP = one row, no quantity field |
| T-08-SC | Tampering | npm installs | accept | no packages added this phase |
</threat_model>

<verification>
- `npx vitest run tests/group/ tests/booking/` green; group-owner-scope mutation-verified; `npm run build` exits 0; tsc + eslint clean.
</verification>

<success_criteria>
- createGroup (confirmed-owned, cap snapshotted), submitRsvp (public token path, seat-claim, post-commit notify under the D-117 guard), removeAttendee/regenerateLink, and cancel-time auto-void all work and are owner/token gated; IDOR + token oracle proven.
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-06-SUMMARY.md` when done — include the group-owner-scope mutation-verification result.
</output>
