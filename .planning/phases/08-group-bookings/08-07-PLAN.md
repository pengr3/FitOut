---
phase: 08-group-bookings
plan: 07
type: execute
wave: 4
depends_on: [08-05, 08-06]
files_modified:
  - src/app/(app)/bookings/[id]/page.tsx
  - src/app/(app)/bookings/[id]/group/page.tsx
  - src/app/(app)/bookings/[id]/cancel/page.tsx
  - src/components/group/create-group-button.tsx
  - src/components/group/share-link-box.tsx
  - src/components/group/headcount-meter.tsx
  - src/components/group/attendee-roster.tsx
  - src/components/group/remove-attendee-button.tsx
  - src/components/group/regenerate-link-button.tsx
  - src/components/group/top-up-nudge.tsx
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-04]
must_haves:
  truths:
    - "D-119: the confirmed booking detail shows a coral 'Invite people' entry (confirmed + session-ahead only; pending/requested/approved/cancelled/declined/completed/past do NOT); once a group exists it becomes a neutral 'Manage group · {N} coming' link; 'Find another space' demotes to a ghost link (one primary per surface)"
    - "GROUP-04: /bookings/[id]/group is an owner-gated RSC re-checking booking.bookerId === session.user.id (route group is NOT the gate); a non-organizer gets the SAME bare 404 (no enumeration oracle)"
    - "GROUP-04: the headcount figure '{confirmed} of {capacity_snapshot}' is the Display-scale focal point; the roster shows the organizer as row #1 (You · organizer, D-113), yes rows first, no rows collapsed; only yes rows count"
    - "D-118: the share-link block renders the absolute invite URL in a read-only field with a neutral Copy link (never logs the token)"
    - "D-121: the organizer can Remove attendee (frees a seat) and Regenerate link (kills a leaked link, roster preserved); both neutral outline + confirm dialog, NEVER destructive-red"
    - "D-114 (A3 deferred): the top-up nudge is a NEUTRAL record/nudge shown ONLY when extraHeadFee>0 AND yes>declaredPax — no charge CTA in v1 (the in-app top-up is a fast-follow)"
    - "G6: attendee names render as escaped React text; the roster is organizer-only (attendees never see the guest list)"
  artifacts:
    - path: "src/app/(app)/bookings/[id]/group/page.tsx"
      provides: "owner-gated organizer management RSC (headcount + roster + share + manage)"
      min_lines: 40
      contains: "bookerId"
    - path: "src/components/group/headcount-meter.tsx"
      provides: "{confirmed} of {snapshot} focal figure + full caption"
      contains: "capacity"
  key_links:
    - from: "src/app/(app)/bookings/[id]/page.tsx"
      to: "createGroup / /bookings/[id]/group"
      via: "CreateGroupButton (confirmed branch) → routes to the management RSC"
      pattern: "Invite people"
    - from: "src/app/(app)/bookings/[id]/group/page.tsx"
      to: "getRoster / getHeadcount"
      via: "owner-scoped reads (08-06) + the D-84 router.refresh poller"
      pattern: "getRoster"
---

<objective>
Build the organizer side: the D-119 "Invite people" / "Manage group" entry on the confirmed booking detail, the owner-gated `/bookings/[id]/group` management RSC (headcount focal figure, "who's coming" roster, share-link block, Remove attendee, Regenerate link, the D-114 top-up nudge), and the D-121 group-consequence line on the cancel review. All calm and neutral per the UI-SPEC — the only coral is "Invite people".

Purpose: GROUP-04 ("see the headcount and who's coming") lives here, plus the GROUP-01/GROUP-02 organizer entry + share surface. Depends on 08-06 (the actions + read helpers) and 08-05 (shares the booking-detail file).
Output: the entry point, the management RSC + its composites, and the cancel-review consequence line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md
@.planning/phases/08-group-bookings/08-PATTERNS.md

<interfaces>
<!-- Actions (08-06): createGroup(bookingId), removeAttendee(rsvpId), regenerateLink(groupId). Reads (08-06): getRoster(db,groupId), getHeadcount(db,groupId). -->
<!-- Owner gate (bookings/[id]/page.tsx:161-162): if (!userId) notFound(); if (bk.bookerId !== userId) notFound(). -->
<!-- Poller: clone src/components/booking/pending-payment-state.tsx (D-84 bounded router.refresh, paused on document.hidden). -->
<!-- Containers: management = mx-auto w-full max-w-2xl px-4 py-8 sm:py-12 (bookings/[id]/page.tsx:230). Icons: UserRoundMinus, RefreshCw. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group entry point on the confirmed booking detail + cancel-review consequence line</name>
  <files>src/app/(app)/bookings/[id]/page.tsx, src/app/(app)/bookings/[id]/cancel/page.tsx, src/components/group/create-group-button.tsx</files>
  <read_first>
    - src/app/(app)/bookings/[id]/page.tsx:507 (canCancel gate to mirror for session-ahead), :558-568 (the confirmed-branch forward-action slot where the coral goes; demote Find another space to ghost), :570-583 (the cancel-entry Separator precedent)
    - src/app/(app)/bookings/[id]/cancel/page.tsx (the shipped cancel review — add ONE muted prose line when the booking is a group, above the breakdown; money unchanged)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §1 + Open Q3 (entry point contract; one coral primary) + §7 (cancel consequence line)
  </read_first>
  <action>
    Create `src/components/group/create-group-button.tsx` ("use client"): a coral "Invite people" button (bg-brand) that calls `createGroup(bookingId)` and routes to `/bookings/[id]/group`, with a pending state. In bookings/[id]/page.tsx confirmed branch (:558-568), gate on `bk.status==='confirmed'` AND session-ahead (mirror canCancel at :507) AND occupancy exclusive: if the booking has no group yet, render CreateGroupButton + helper "Invite friends to this booking and track who's coming."; if it already has a group, render a neutral outline "Manage group · {N} coming" link to `/bookings/[id]/group`. DEMOTE the shipped coral "Find another space" to a `ghost` text link so one-primary-per-surface holds (Open Q3). Leave the cancel entry (:570-583) untouched. In cancel/page.tsx, when the booking is a group, add ONE muted prose line above the breakdown: "This also cancels the group — we'll let the {N} people coming know." (D-121, §7) — prose only, no new component, breakdown unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "Invite people" src/components/group/create-group-button.tsx` >= 1 and it is coral (bg-brand); "Find another space" in bookings/[id]/page.tsx is now a ghost/link variant, not bg-brand (grep the confirmed branch)
    - the entry renders ONLY on a confirmed, session-ahead booking (a cancelled/requested/past booking shows no entry — assert via a render or a source-level branch guard)
    - cancel/page.tsx renders the group-consequence prose line only when the booking is a group; the money breakdown lines are unchanged (grep the breakdown block untouched)
    - `npx tsc --noEmit` + eslint clean; `npm run build` exits 0 (documented env placeholders)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The confirmed booking shows one coral Invite-people entry (or a neutral Manage-group link), Find-another-space is demoted, and the cancel review discloses the group consequence.</done>
</task>

<task type="auto">
  <name>Task 2: /bookings/[id]/group management RSC — headcount + roster + share-link + poller</name>
  <files>src/app/(app)/bookings/[id]/group/page.tsx, src/components/group/headcount-meter.tsx, src/components/group/share-link-box.tsx, src/components/group/attendee-roster.tsx</files>
  <read_first>
    - src/app/(app)/bookings/[id]/page.tsx:127-133,:161-162,:230 (async params + session-first owner gate + the container width to reuse)
    - src/app/(app)/bookings/[id]/cancel/page.tsx (the nested-route RSC precedent)
    - src/components/booking/pending-payment-state.tsx (the D-84 bounded router.refresh poller to clone, paused on document.hidden)
    - src/components/host/request-row.tsx / payout-row.tsx (the row-shell to clone for AttendeeRow)
    - src/lib/group/rsvp.ts getRoster/getHeadcount (08-06)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §2 (management layout, headcount focal, roster contract, empty/error states, privacy Open Q4)
  </read_first>
  <action>
    Create `src/app/(app)/bookings/[id]/group/page.tsx`: an owner-gated RSC repeating the booking-detail gate on `booking.bookerId === session.user.id` (no session → notFound; not-mine/missing → the SAME bare 404, no oracle, Security V4); container `mx-auto w-full max-w-2xl px-4 py-8 sm:py-12`. Layout top→bottom: title "Your group" + subhead, HeadcountMeter, ShareLinkBox, AttendeeRoster, (conditional TopUpNudge — wired in Task 3), manage actions (Task 3). `HeadcountMeter`: "{confirmed} of {capacity_snapshot}" at Display 28/600 tabular-nums with an aria-label "{confirmed} of {N} spots filled"; caption "spots filled" → "This group is full." (muted, never red) at the cap. `ShareLinkBox` ("use client"): a read-only full-width input holding the absolute `{origin}/invite/{token}` URL + a neutral Copy link button (navigator.clipboard + sonner toast "Link copied"; never logs the token; copy-fail fallback copy). `AttendeeRoster`/`AttendeeRow`: organizer as row #1 labelled "You · organizer" (D-113), each other row = escaped name (14/600) + neutral secondary Guest/Account tag + answer meta; yes rows first, no rows in a collapsed "Can't make it ({M})" group; unanswered are not rows; the roster is ORGANIZER-ONLY (Open Q4). Add the D-84 bounded router.refresh poller (paused on document.hidden, no spinner). Empty state "No one's RSVP'd yet" with the share block still visible; calm error state (not red).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/group/group-owner-scope.test.ts` still GREEN (the RSC read reuses the owner-scoped helper; the gate matches the mutation-verified predicate)
    - the RSC calls `notFound()` when `bk.bookerId !== userId` (grep the gate) — same bare 404 as the booking detail
    - HeadcountMeter exposes an aria-label beyond the numeral (accessibility, UI-SPEC §Accessibility); the Guest/Account tag is icon+text, never colour-only
    - ShareLinkBox renders the absolute invite URL in a readonly input and does not console.log the token (grep 0 log of token)
    - `npx tsc --noEmit` + eslint clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/group-owner-scope.test.ts</automated>
  </verify>
  <done>The owner-gated management RSC shows the focal headcount, the share link, and the organizer-only roster (organizer row #1, yes-first), refreshing via the bounded poller.</done>
</task>

<task type="auto">
  <name>Task 3: Remove attendee + Regenerate link confirm dialogs + the D-114 top-up nudge</name>
  <files>src/components/group/remove-attendee-button.tsx, src/components/group/regenerate-link-button.tsx, src/components/group/top-up-nudge.tsx</files>
  <read_first>
    - src/components/booking/cancel-request-dialog.tsx (the confirm-dialog + confirm pattern to clone; focus trap, return focus)
    - src/app/actions/group.ts removeAttendee/regenerateLink (08-06)
    - .planning/phases/08-group-bookings/08-UI-SPEC.md §2 (Remove/Regenerate rows), §Color (all neutral outline, NOT destructive-red), §Copywriting > Destructive confirmations (exact copy), §2 top-up nudge + Open Q10 (record/nudge only, no charge CTA)
  </read_first>
  <action>
    Create `remove-attendee-button.tsx` ("use client"): neutral outline button (icon UserRoundMinus + "Remove attendee", never on the organizer's own row) → confirm dialog with the UI-SPEC copy ("Remove {name} from this group? This frees up their spot. They can RSVP again if they still have the link.") → calls removeAttendee; sonner toast + revalidate freshness. Create `regenerate-link-button.tsx` ("use client"): neutral outline (icon RefreshCw + "Regenerate link") → confirm dialog with the UI-SPEC regenerate copy → calls regenerateLink. NEITHER uses a destructive-red variant (UI-SPEC §Color). Create `top-up-nudge.tsx`: a NEUTRAL alert (not destructive) rendered ONLY when `extraHeadFee > 0` AND confirmed-yes > declaredPax — copy "{N} people have RSVP'd, but you booked for {declaredPax}. You may owe a bit more for the extra {K} at check-in." with NO "Pay now" CTA (v1 record/nudge only; the automated top-up charge is the deferred fast-follow, A3/D-114). On a flat listing (extraHeadFee 0) the block never renders. Mount all three in the management RSC (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -ci "destructive" src/components/group/remove-attendee-button.tsx src/components/group/regenerate-link-button.tsx` returns 0 (neutral outline, UI-SPEC §Color)
    - `grep -ci "pay now\|charge\|/checkout" src/components/group/top-up-nudge.tsx` returns 0 (record/nudge only, no charge CTA — A3/D-114)
    - top-up-nudge renders only when extraHeadFee>0 AND yes>declaredPax (grep the double guard); both confirm dialogs trap focus and return focus to the trigger (reuse the shipped dialog)
    - `npx tsc --noEmit` + eslint clean; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Remove/Regenerate are neutral confirm-dialog actions (never red); the top-up nudge is a neutral fee>0-only signal with no charge CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer route → management RSC/actions | owner authority re-checked in the RSC, not the route group |
| roster render → organizer screen | attacker-controlled guest names cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Elevation / Info Disclosure (IDOR) | /bookings/[id]/group RSC | mitigate | RSC re-checks booking.bookerId === session.user.id; non-organizer/missing → same bare 404 (no oracle); reuses the mutation-verified owner-scope read |
| T-08-20 | Tampering (stored XSS) | attendee name in roster | mitigate | names render as auto-escaped React text; no dangerouslySetInnerHTML (G6) |
| T-08-21 | Information Disclosure | invite token in logs | mitigate | ShareLinkBox renders but never logs the token (D-118) |
| T-08-22 | Information Disclosure | guest list exposure | mitigate | the roster is organizer-only; attendees never see it (Open Q4) |
| T-08-SC | Tampering | npm installs | accept | no packages added; every surface composes from installed shadcn blocks |
</threat_model>

<verification>
- `npx vitest run tests/group/group-owner-scope.test.ts` green; `npm run build` exits 0; tsc + eslint clean.
</verification>

<success_criteria>
- The confirmed booking has one coral Invite-people entry; the owner-gated management RSC shows the focal headcount + organizer-only roster + share link + neutral Remove/Regenerate + fee>0-only top-up nudge; the cancel review discloses the group consequence.
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-07-SUMMARY.md` when done.
</output>
