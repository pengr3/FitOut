---
phase: 08-group-bookings
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/booking/pricing.ts
  - src/lib/availability/units.ts
  - src/lib/validation/booking.ts
  - src/app/actions/booking.ts
  - tests/booking/pricing.test.ts
autonomous: true
requirements: [GROUP-01, GROUP-05]
must_haves:
  truths:
    - "D-108: surcharge = max(0, pax − included) × extraHeadFee; extraHeadFee=0 ⇒ byte-identical to today (zero leak, backward-compatible default)"
    - "A1 (RESOLVED YES): the surcharge folds into spacePriceCents — host revenue, the payout basis and the service-fee basis; the Phase-5 hold-until-session rail stays UNCHANGED (D-107)"
    - "D-108: declaredPax is captured server-side at placeHold and drives price ONLY when extraHeadFee>0; on a flat listing there is no declaredPax and no surcharge machinery"
    - "D-108/CLAUDE.md: the client does ZERO price arithmetic — quoteWindow is server-authoritative and freezes the total; a client-supplied surcharge/pax cannot move the charge"
    - "D-113: pricing included=1 folds the organizer's own seat into the base (organizer is attendee #1)"
  artifacts:
    - path: "src/lib/booking/pricing.ts"
      provides: "quoteWindow extended with included/extraHeadFee/declaredPax; returns extraHeads + extraHeadCents for the breakdown"
      contains: "extraHeadFee"
    - path: "src/lib/availability/units.ts"
      provides: "createPendingHold reads listing.included/extra_head_fee, threads declaredPax, folds surcharge into spacePriceCents (payout + fee basis)"
      contains: "declaredPax"
  key_links:
    - from: "src/app/actions/booking.ts placeHold"
      to: "createPendingHold declaredPax"
      via: "bookingCreateSchema.declaredPax (server-validated, fee>0 only)"
      pattern: "declaredPax"
    - from: "src/lib/availability/units.ts"
      to: "booking.space_price_cents"
      via: "quote.totalCents (base + surcharge) — the payout-sweep gross basis (A1)"
      pattern: "spacePriceCents"
---

<objective>
Extend the server-authoritative price quote with the D-108 pax surcharge — `total = baseRate + max(0, pax − included) × extraHeadFee` — backward-compatibly (`extraHeadFee = 0` is byte-identical to today), and thread `declaredPax` from `placeHold` into `createPendingHold` so the surcharge folds into `spacePriceCents` at freeze time. Per the RESOLVED A1 decision, the surcharge is host revenue: it enters `spacePriceCents` (the payout gross basis) and therefore the commission and service-fee basis, and the Phase-5 hold-until-session rail is unchanged (D-107).

Purpose: This is the pure/backend half of the pax-pricing seam; the UI (PaxStepper, breakdown line, fullDay fix) is 08-05. Keeping the math server-side is the core-value money-trust guard (D-108, CLAUDE.md).
Output: extended `quoteWindow` + tests, `createPendingHold` wiring, `bookingCreateSchema.declaredPax`, `placeHold` capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-RESEARCH.md
@.planning/phases/08-group-bookings/08-PATTERNS.md

<interfaces>
<!-- Current quoteWindow (pricing.ts:55-69) is pure, no-I/O, isomorphic. Extend backward-compatibly. -->
quoteWindow(input: QuoteInput & { included?: number; extraHeadFee?: number; declaredPax?: number }): Quote
  where Quote gains: extraHeads: number; extraHeadCents: number   // both 0 when extraHeadFee falsy
  surcharge = (extraHeadFee ?? 0) > 0 ? max(0, (declaredPax ?? 1) − (included ?? 1)) × extraHeadFee : 0
  totalCents = base.totalCents + surcharge
<!-- createPendingHold freezes: spacePriceCents = quote.totalCents (base+surcharge, A1); serviceFeeCents = computeServiceFee(spacePriceCents); quotedTotalCents = space+fee (units.ts:419-482). -->
<!-- bookingCreateSchema (validation/booking.ts:92-98): add declaredPax?: z.coerce.number().int().min(1).optional() -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend quoteWindow with the D-108 surcharge (RED-first) + pricing tests</name>
  <files>src/lib/booking/pricing.ts, tests/booking/pricing.test.ts</files>
  <read_first>
    - src/lib/booking/pricing.ts:1-69 (the whole pure module — the throw-on-missing-rate money guard at :61,:64 must be preserved; integer centavos only)
    - tests/booking/pricing.test.ts (existing D-45 cases — extend, do not rewrite)
    - .planning/phases/08-group-bookings/08-RESEARCH.md §Code Examples (Pax-scaled quote) + §Pitfall 3 (A1 payout/fee-basis note)
  </read_first>
  <behavior>
    - extraHeadFee absent or 0 ⇒ quoteWindow output byte-identical to today: totalCents unchanged, extraHeads=0, extraHeadCents=0 (backward-compat, zero leak — assert against a pre-existing D-45 case's total).
    - extraHeadFee>0, declaredPax > included ⇒ totalCents = base + (declaredPax − included) × extraHeadFee; extraHeads = declaredPax − included; extraHeadCents = extraHeadFee.
    - extraHeadFee>0, declaredPax <= included ⇒ surcharge 0 (max floors at 0); organizer-only (declaredPax=1, included=1) has no surcharge (D-113).
    - integer centavos throughout; no float; the missing-rate throw still fires when the required base rate is null.
  </behavior>
  <action>
    Extend the `QuoteInput`/`Quote` types and `quoteWindow` per the interfaces block: compute `base` via the existing unchanged hourly/day logic, then `included = input.included ?? 1`, `fee = input.extraHeadFee ?? 0`, `pax = input.declaredPax ?? 1`, `extraHeads = fee > 0 ? Math.max(0, pax − included) : 0`, `surchargeCents = extraHeads * fee`; return `{ ...base, totalCents: base.totalCents + surchargeCents, extraHeads, extraHeadCents: fee }`. Keep the module pure/isomorphic (no directive). Extend tests/booking/pricing.test.ts with the four behaviors above (RED first against the un-extended signature, then GREEN).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/booking/pricing.test.ts` GREEN including the four new D-108 cases
    - a case asserts `extraHeadFee=0` yields the SAME totalCents as the shipped D-45 flat case (backward-compat / zero leak)
    - `grep -c "Math.max(0" src/lib/booking/pricing.ts` >= 1 (the floor is present, no negative surcharge)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/booking/pricing.test.ts</automated>
  </verify>
  <done>quoteWindow returns a surcharge-aware total + extraHeads/extraHeadCents, identical to today when fee=0; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Thread declaredPax through placeHold → createPendingHold; fold surcharge into spacePriceCents</name>
  <files>src/lib/validation/booking.ts, src/app/actions/booking.ts, src/lib/availability/units.ts</files>
  <read_first>
    - src/lib/validation/booking.ts:92-100 (bookingCreateSchema — SHAPE-ONLY, invariants re-derived server-side; add declaredPax as an optional coerced int)
    - src/app/actions/booking.ts:106-107, :160-182 (placeHold safeParse + the createPendingHold call to thread declaredPax into)
    - src/lib/availability/units.ts:148-175 (CreatePendingHoldInput — add declaredPax/included/extraHeadFee), :416-483 (the quote + frozen-triple freeze site where spacePriceCents = quote.totalCents; this is the A1 fold point)
    - .planning/phases/08-group-bookings/08-PATTERNS.md (units.ts + booking.ts sections — placeHold captures declaredPax fee>0 only)
  </read_first>
  <action>
    Add `declaredPax` (optional coerced positive int) to `bookingCreateSchema` (SHAPE-ONLY — the real cap enforcement is D-112's seat-claim, and the price re-derivation is server-side). In `placeHold`, read the parsed `declaredPax` and pass it into `createPendingHold`. Extend `CreatePendingHoldInput` with `declaredPax?`, and have `createPendingHold` read `listing.included` and `listing.extra_head_fee` alongside the existing rates INSIDE the tx, then call `quoteWindow({ ..., included, extraHeadFee, declaredPax })`. The surcharge folds into `quote.totalCents`, which is already frozen as `spacePriceCents` (units.ts:480) — so per A1 the surcharge becomes the payout gross basis and `computeServiceFee(quote.totalCents)` applies to it automatically; `quotedTotalCents = space + fee` still holds by construction. Persist `declaredPax` on the booking row (08-01 added the column) when `extraHeadFee > 0`, else leave it NULL (D-108 — no declaredPax on a flat listing). Do NOT change any payment code (D-107).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/booking/` GREEN (no regression to the existing hold/state-machine suites)
    - `grep -c "declaredPax" src/lib/availability/units.ts` >= 1 and `grep -c "declaredPax" src/app/actions/booking.ts` >= 1 (threaded end-to-end)
    - a flat listing (extra_head_fee null/0) freezes the SAME spacePriceCents/quotedTotalCents as before (backward-compat — assert in an existing or new units test)
    - `npx tsc --noEmit` + `npx eslint src/lib/availability/units.ts src/app/actions/booking.ts src/lib/validation/booking.ts` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/booking/</automated>
  </verify>
  <done>placeHold captures declaredPax; createPendingHold folds the surcharge into spacePriceCents (payout + fee basis, A1); flat listings unchanged; no payment code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → placeHold | declaredPax + window arrive from an untrusted client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | client-supplied surcharge/pax/price | mitigate | quoteWindow is server-authoritative and re-derives the surcharge from the listing's own included/extra_head_fee; declaredPax is shape-validated but the PRICE is frozen server-side (D-108, CLAUDE.md); the client never sends a price |
| T-08-07 | Tampering | payout basis leak | mitigate | surcharge folds into spacePriceCents (A1) so the host is paid (base+surcharge)−commission; it never routes to platform revenue (STATE 07-04 payout-basis contract preserved) |
| T-08-SC | Tampering | npm installs | accept | no packages added this phase |
</threat_model>

<verification>
- `npx vitest run tests/booking/` green; backward-compat asserted (fee=0 identical); tsc + eslint clean.
</verification>

<success_criteria>
- The D-108 surcharge is computed and frozen server-side into spacePriceCents (A1), backward-compatible at fee=0, with declaredPax captured only when the fee is set; the Phase-5 rail is untouched (D-107).
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-03-SUMMARY.md` when done.
</output>
