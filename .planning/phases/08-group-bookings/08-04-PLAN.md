---
phase: 08-group-bookings
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/db/schema.ts
  - src/lib/validation/notification.ts
  - src/inngest/functions/notify.ts
  - src/components/notifications/notification-item.tsx
  - src/lib/email.ts
  - src/inngest/functions/guest-email.ts
  - src/app/api/inngest/route.ts
  - drizzle/0018_group_notification_types.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0018_snapshot.json
  - tests/notifications/notify.test.ts
  - tests/notifications/notification-render.test.tsx
autonomous: true
requirements: [GROUP-03, GROUP-04]
must_haves:
  truths:
    - "D-122: group RSVP notification types are added as the FOUR-FILE compile-checked change (pgEnum+TS union, Zod union, sendForType, describeNotification) with NO default: clause in any switch — every omission stays a compile error"
    - "A2 (RESOLVED, Pitfall 2): guests (null user_id) CANNOT flow through fitout/notify (recipientId is a NOT NULL FK to user.id); a NEW email-only Inngest fn (fitout/guest-email) clones the D-83 retry/onFailure envelope but writes NO durable notification row"
    - "D-122: organizer is notified per RSVP (in-app+email); attendee-accounts get in-app+email; guests-with-email get email only; blank-email guests get NO send"
    - "G6: guest-typed names are escaped in every email body (escapeHtml) and every notification payload renders as auto-escaped React text; every group href is absolute (${BETTER_AUTH_URL}/...) and passes safeHref"
    - "T-07-38: the guest-email onFailure audit NEVER logs the guest email address"
  artifacts:
    - path: "src/inngest/functions/guest-email.ts"
      provides: "the email-only Inngest fn (retries:4 + onFailure, single send-email step, NO durable row)"
      contains: "onFailure"
    - path: "src/inngest/functions/notify.ts"
      provides: "sendForType group cases (no default:)"
      contains: "group_rsvp"
    - path: "drizzle/0018_group_notification_types.sql"
      provides: "ALTER TYPE notification_type ADD VALUE IF NOT EXISTS ... (the group values)"
      contains: "ADD VALUE"
  key_links:
    - from: "src/app/api/inngest/route.ts"
      to: "guestEmail"
      via: "serve({ functions: [...] }) mount"
      pattern: "guestEmail"
    - from: "src/lib/validation/notification.ts"
      to: "NotificationPayload"
      via: "_PayloadUnionParity weld (Zod tuple == TS union)"
      pattern: "_PayloadUnionParity"
---

<objective>
Add the group RSVP notification types (organizer RSVP-received, attendee RSVP confirmation, group cancelled — D-122) as the compile-checked four-file change, AND build the separate email-only Inngest function that reaches guest-with-email attendees who have no `user.id` (the single most important RESEARCH finding, Pitfall 2 / RESOLVED A2). Ship migration `0018` (the `ALTER TYPE notification_type ADD VALUE` half of the 55P04-safe split) and mount the new function.

Purpose: This plan builds the notification INFRASTRUCTURE (types, email templates, dispatch, guest channel). The EMIT sites (organizer/attendee sends after the seat-claim commits, with the D-117 opt-in guard) are wired by 08-06, which depends on this plan. No emitter is added here.
Output: four-file group types, `guest-email.ts`, email templates, migration 0018, extended notification tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-group-bookings/08-RESEARCH.md
@.planning/phases/08-group-bookings/08-PATTERNS.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md

<interfaces>
<!-- Recommended group notification type set (RESEARCH §Assumption A5; final payload shape is Claude's discretion per D-86). -->
group_rsvp_received (organizer): { listingTitle; whenLabel; attendeeLabel; answer: "yes"|"no"; href }
group_rsvp_confirmed (attendee-account): { listingTitle; whenLabel; href }
group_cancelled (reachable yes attendee): { listingTitle; whenLabel; href }
<!-- Guest-with-email path (NO durable row): -->
export const GUEST_EMAIL_EVENT = "fitout/guest-email" as const
guestEmail = inngest.createFunction({ id:"guest-email", retries:4, triggers:[{event:GUEST_EMAIL_EVENT}], onFailure }, handler with ONLY a send-email step)
event.data: { to: string; kind: "rsvp_confirmed"|"group_cancelled"; listingTitle; whenLabel; href }
<!-- Icons per UI-SPEC §4: UserRoundCheck (yes), UserRoundX (no), CalendarCheck (attendee confirm), CalendarX2 (cancelled). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-file group notification types + email templates (accounts) + migration 0018</name>
  <files>src/lib/db/schema.ts, src/lib/validation/notification.ts, src/inngest/functions/notify.ts, src/components/notifications/notification-item.tsx, src/lib/email.ts, drizzle/0018_group_notification_types.sql, drizzle/meta/_journal.json, drizzle/meta/0018_snapshot.json</files>
  <read_first>
    - src/lib/db/schema.ts:352-462 (notificationType pgEnum + the NotificationPayload discriminated union + the side/expired copy-variant modeling to mirror)
    - src/lib/validation/notification.ts:19-36, :67, :194-199 (notificationTypeValues tuple, notificationPayloadSchema, the _PayloadUnionParity weld — do NOT touch the weld, let it enforce parity; use Zod 4 z.discriminatedUnion / z.email)
    - src/inngest/functions/notify.ts:76-84 (a sendForType case shape to clone), :195-198 (the never weld — NO default:)
    - src/components/notifications/notification-item.tsx:129-134 (a describeNotification case to clone), :97-108 (safeHref), :214-215 (the never weld)
    - src/lib/email.ts:25-32, :101-118, :244-287 (the sendBooking* family + private send/escapeHtml; every field escapeHtml'd; href absolute via the APP_URL convention at :94)
    - drizzle/0010_booking_request_states.sql (the ALTER TYPE ADD VALUE template — multiple values in one file OK)
    - .planning/phases/08-group-bookings/08-RESEARCH.md §Pitfall 7 (55P04) + §Pitfall 8 (four-file + parity weld)
  </read_first>
  <action>
    Add the group notification values to the `notificationType` pgEnum in schema.ts (`group_rsvp_received`, `group_rsvp_confirmed`, `group_cancelled`) AND add matching members to the `NotificationPayload` union (payload shapes per the interfaces block; each field a pre-composed DISPLAY string per D-86; `href` absolute). Mirror them in validation/notification.ts's `notificationTypeValues` tuple and `notificationPayloadSchema` (the `_PayloadUnionParity` assertion breaks the build if the Zod and TS unions disagree — leave it). Add a `sendForType` case per type in notify.ts (NO default:) calling new thin `email.ts` senders (`sendGroupRsvpReceived`, `sendGroupRsvpConfirmed`, `sendGroupCancelled`) built over the private `send`/`escapeHtml` — escape every interpolated field including the attendee/guest name (G6). Add a `describeNotification` case per type in notification-item.tsx (NO default:) returning `{ Icon, title, body }` with the UI-SPEC §4 icons; all fields render as React text children; href passes `safeHref`. Then `npm run db:generate` → hand-edit the produced `drizzle/0018_group_notification_types.sql` to be exactly `ALTER TYPE "notification_type" ADD VALUE IF NOT EXISTS '<value>'` per value (unqualified type name for harness rewriting; the 55P04 split is honored because this migration ONLY adds values and nothing in any migration USES them — the values are used at runtime INSERT, never in a migration). Run `npm run db:migrate` [BLOCKING].
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the four-file parity + never welds all compile — proves no file was missed)
    - `grep -c "default:" src/inngest/functions/notify.ts` and `grep -c "default:" src/components/notifications/notification-item.tsx` for the group switches: NO new default clause added (the never weld intact)
    - `npm run db:migrate` exits 0 and 0018 SQL contains `ALTER TYPE "notification_type" ADD VALUE IF NOT EXISTS 'group_rsvp_received'` (and the other two)
    - `npx vitest run tests/notifications/` GREEN (extend notify.test.ts to dispatch the three group types; extend notification-render.test.tsx to render them with an escaped guest-name payload and a safeHref-checked href)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/notifications/</automated>
  </verify>
  <done>Three group notification types exist across all four files + email templates; migration 0018 applied and idempotent; render + dispatch tests green; guest name escaped, href absolute + safeHref.</done>
</task>

<task type="auto">
  <name>Task 2: The email-only guest Inngest function (no durable row) + mount</name>
  <files>src/inngest/functions/guest-email.ts, src/app/api/inngest/route.ts, src/lib/email.ts</files>
  <read_first>
    - src/inngest/functions/notify.ts:224-262 (the 2-arg createFunction envelope with retries:4 + onFailure → recordAudit needs_attention; the write-notification step to DROP for guests; onFailure must NEVER log the address, T-07-38 at :224-225,:241)
    - src/app/api/inngest/route.ts:52 (the serve({ functions: [...] }) list to append `guestEmail` to)
    - src/lib/email.ts:34-52 (send takes ANY address + the [email:dev] fallback when RESEND_API_KEY unset — the guest path needs no new integration)
    - .planning/phases/08-group-bookings/08-RESEARCH.md §Code Examples (guest-email fn draft) + §Pitfall 2
  </read_first>
  <action>
    Create `src/inngest/functions/guest-email.ts`: export `GUEST_EMAIL_EVENT = "fitout/guest-email"` and `guestEmail = inngest.createFunction({ id:"guest-email", retries:4, triggers:[{event:GUEST_EMAIL_EVENT}], onFailure: recordAudit needs_attention (log NO email address, T-07-38) }, async ({event, step}) => step.run("send-email", () => sendGuestRsvpEmail(event.data)))`. It has ONLY the send-email step and writes NO notification row (guests have no user.id — routing them through fitout/notify would fail the recipientId FK, Pitfall 2). Add `sendGuestRsvpEmail(d: {to; kind: "rsvp_confirmed"|"group_cancelled"; listingTitle; whenLabel; href})` to email.ts over the private send/escapeHtml (escape every field, G6). Append `guestEmail` to the `serve({ functions: [...] })` list in src/app/api/inngest/route.ts so the fn is registered. Do NOT add the emit call here (08-06 owns the D-117 opt-in-guarded emit).
  </action>
  <acceptance_criteria>
    - `grep -c "write-notification\|insertNotification" src/inngest/functions/guest-email.ts` returns 0 (NO durable row — the defining property of the guest path)
    - `grep -c "guestEmail" src/app/api/inngest/route.ts` returns >= 1 (mounted in serve)
    - guest-email.ts declares `retries: 4` and an `onFailure` handler (D-83 envelope preserved)
    - a test asserts sendGuestRsvpEmail escapes a `<script>`-bearing name (G6) and does not throw when RESEND_API_KEY is unset (dev fallback); `npx tsc --noEmit` + eslint clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/notifications/</automated>
  </verify>
  <done>guestEmail fn exists (email-only, no row), is mounted in the Inngest serve route, keeps the retry/onFailure envelope, and never logs the guest address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest-typed name → email/notification render | attacker-controlled free text crosses here |
| notification payload → in-app render + email CTA | href must be an allow-listed destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (stored XSS) | guest name in email/notification | mitigate | escapeHtml every field in every group email body; notification payloads render as auto-escaped React text; no dangerouslySetInnerHTML (G6) |
| T-08-09 | Information Disclosure | guest email in logs | mitigate | guest-email onFailure records a needs_attention audit but NEVER logs the address (T-07-38 precedent) |
| T-08-10 | Tampering | notification href | mitigate | every group href is absolute (${BETTER_AUTH_URL}/...) and passes the shipped safeHref render allow-list (root-relative or http(s) only) |
| T-08-11 | Denial of Service | guest FK crash loop | mitigate | guests never route through fitout/notify (would fail recipientId FK → retry storm → un-actionable audit); the separate email-only fn writes no row (Pitfall 2 / A2) |
| T-08-SC | Tampering | npm installs | accept | no packages added this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (four-file welds prove completeness); `npm run db:migrate` idempotent; `npx vitest run tests/notifications/` green.
</verification>

<success_criteria>
- Group notification types exist across all four files with no default: clause; the guest-with-email email-only fn is built and mounted (no durable row); migration 0018 applied; guest names escaped, hrefs absolute + safeHref; no address ever logged.
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-04-SUMMARY.md` when done.
</output>
