---
phase: 08-group-bookings
plan: 16
type: execute
wave: 6
depends_on: []
gap_closure: true
closes: [deferred-items.md item 4]
files_modified:
  - tests/group/seat-claim-race.test.ts
  - .planning/phases/08-group-bookings/deferred-items.md
autonomous: true
requirements: [GROUP-05]
must_haves:
  truths:
    - "D-112: deleting FOR UPDATE from the SHIPPED src/lib/group/seat-claim.ts turns tests/group/seat-claim-race.test.ts RED — the production lock now has a regression guard, not only the pattern"
    - "The existing inlined-SQL cases survive unchanged as the pattern proof; the new case is additive"
    - "Each racer drives the real claimSeat bound to its OWN connection, so the lock window is genuinely raced rather than serialized"
  artifacts:
    - path: "tests/group/seat-claim-race.test.ts"
      provides: "at least one racing case routed through the real claimSeat, alongside the existing inlined cases"
      contains: "claimSeat"
  key_links:
    - from: "tests/group/seat-claim-race.test.ts"
      to: "src/lib/group/seat-claim.ts claimSeat"
      via: "drizzle(racingClient) bound as the DbConn argument, one per racer"
      pattern: "claimSeat\\("
---

<objective>
Close **`deferred-items.md` item 4** — the shipped `claimSeat` `FOR UPDATE` has no mutation coverage.

`tests/group/seat-claim-race.test.ts` deliberately INLINES the seat-claim SQL inside each racer's transaction, and the reason is sound: it makes the `FOR UPDATE` lock itself the thing under test, mirroring `exclusion-race.test.ts` testing the EXCLUDE constraint directly. The consequence, which its header does not state, is that the file **never imports `claimSeat`**. This was measured during 08-09: deleting `FOR UPDATE` from `src/lib/group/seat-claim.ts:54` left `tests/group/seat-claim-race.test.ts` GREEN (2/2), the whole of `tests/group/` GREEN (6 files / 60 tests) and the full 792-test suite GREEN.

So the gate proves the *pattern* is race-free; it does not prove the *shipped* claim still contains the lock. Someone deleting that one line ships an over-cap bug behind a fully green suite.

**This is a regression-net gap, not a live defect.** `claimSeat` carries the lock today — byte-verified after the 08-09 mutation was reverted — and `tests/group/seat-claim.test.ts` covers its functional contract. Nothing about the shipped behaviour changes in this plan; only the net under it.

Purpose: make GROUP-05's atomic no-overflow guarantee survive a one-line edit to the production file.
Output: one additive racing case that drives the real `claimSeat`, and a mutation statement that is executable as written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-group-bookings/deferred-items.md
@.planning/phases/08-group-bookings/08-VALIDATION.md
@.planning/phases/08-group-bookings/08-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: race the real claimSeat, one connection per racer</name>
  <files>tests/group/seat-claim-race.test.ts</files>
  <behavior>
    - Three concurrent `claimSeat(..., answer: "yes")` calls against a group with `capacity_snapshot = 1`, each bound to its OWN connection: exactly one returns `{ ok: true, status: "yes" }`, the other two return `{ ok: false, reason: "full" }`, and exactly one `yes` row is committed
    - Four concurrent `claimSeat` →yes calls against a group with `capacity_snapshot = 2`: exactly two succeed and exactly two committed `yes` rows exist — proving the guard tracks the snapshot value rather than a hardcoded 1
    - No racer throws: a loser resolves to the calm `full` result, never a rejected promise
    - The two existing inlined-SQL cases still pass, unchanged
  </behavior>
  <read_first>
    - tests/group/seat-claim-race.test.ts (the whole file — the header at :1-15 explaining the inlining and stating a mutation instruction that is NOT executable as written; the fixture at :34-77; `raceClaim` at :86-106)
    - src/lib/group/seat-claim.ts (the whole file — the `FOR UPDATE` at :54 that is the mutation target, the `DbConn` first parameter, the name-only-guest de-dup branch at :60-65 that matches the `false` predicate and therefore always inserts a fresh row, and the bounded 40P01 retry at :46/:97)
    - src/lib/availability/read-model.ts (line 27 — `DbConn = PostgresJsDatabase<Record<string, unknown>>`, i.e. exactly what `drizzle(client)` returns, which is how a racing connection becomes a valid argument)
    - tests/helpers/db.ts (lines 43-73 — `makeRacingClients` opens n INDEPENDENT postgres.js clients bound to one isolated schema; `setupTestDb` shows the `drizzle(client)` + search_path idiom)
    - tests/availability/exclusion-race.test.ts (the Phase-3 sibling this file's discipline is modelled on)
    - .planning/phases/08-group-bookings/deferred-items.md (item 4 — the exact remediation asked for)
  </read_first>
  <action>
    Add a third and fourth fixture group to the existing `beforeAll` (a distinct booking id per group, since `booking_id` is UNIQUE on `booking_group`, with non-overlapping windows so the `booking_no_overlap` EXCLUDE stays irrelevant to the fixture): one with `capacitySnapshot: 1`, one with `capacitySnapshot: 2`.

    Add a helper alongside `raceClaim` that wraps the REAL claim: take a racing postgres.js client, wrap it with `drizzle(...)` from `drizzle-orm/postgres-js` to produce a `DbConn`, and call `claimSeat` imported from `@/lib/group/seat-claim`. Each racer must get its OWN `drizzle(client)` over its OWN connection — a single shared `testDb.db` (max: 1) serializes and would prove nothing, which is RESEARCH Pitfall 1 and the reason `makeRacingClients` exists.

    Pass each racer a name-only guest identity (`userId: null`, `guestEmailNorm: null`, a distinct `name`), so the de-dup predicate resolves to `false` and every racer genuinely attempts a fresh insert. An identity that de-duped would make a loser return `{ ok: true }` by matching an existing row and the race would test nothing.

    Drive the racers with `Promise.allSettled`, then assert on BOTH the returned results and the committed row count read back through the shared `testDb.client`. `.end()` every racing client in a `finally`, exactly as the existing cases do.

    Keep the two existing inlined cases exactly as they are. Rewrite the file header to describe the two layers honestly: the INLINED cases prove the *pattern* is race-free (and their own `FOR UPDATE` is the thing their mutation targets); the new cases prove the *shipped* `claimSeat` still carries the lock (and `src/lib/group/seat-claim.ts:54` is the thing their mutation targets). Replace the current single MUTATION-VERIFY instruction with both, each naming its own file and its own line — the existing one is not executable as written, which is precisely what 08-09 discovered.
  </action>
  <acceptance_criteria>
    - `grep -n "claimSeat" tests/group/seat-claim-race.test.ts` matches an `import` from `@/lib/group/seat-claim` and at least one call
    - `grep -n "drizzle" tests/group/seat-claim-race.test.ts` matches — each racer is wrapped individually, not sharing `testDb.db`
    - `npx vitest run tests/group/seat-claim-race.test.ts` exits 0 with 4 tests
    - **THE MUTATION, stated as the acceptance criterion (record both outputs verbatim in the SUMMARY):** with `FOR UPDATE` deleted from `src/lib/group/seat-claim.ts:54`, `npx vitest run tests/group/seat-claim-race.test.ts` FAILS (2+ committed `yes` rows at `capacity_snapshot = 1`); with `FOR UPDATE` restored it passes. Confirm the restored file is byte-identical to its committed state (`git diff --exit-code src/lib/group/seat-claim.ts` exits 0) before finishing.
    - The SECOND mutation still holds: deleting `FOR UPDATE` from the test's own inlined `raceClaim` also turns the file RED (the pattern proof is intact), then restore
    - The mutated state is never committed
    - `npx vitest run tests/group/` exits 0 (all files)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/group/seat-claim-race.test.ts</automated>
  </verify>
  <done>A one-line deletion in `src/lib/group/seat-claim.ts` now turns the suite red.</done>
</task>

<task type="auto">
  <name>Task 2: mark deferred item 4 closed</name>
  <files>.planning/phases/08-group-bookings/deferred-items.md</files>
  <read_first>
    - .planning/phases/08-group-bookings/deferred-items.md (item 3 at lines 52-89 — the CLOSED-item formatting convention this must copy: a struck-through heading, a quoted closure note naming the commit and what changed, and the original entry preserved inside a `<details>` block)
    - .planning/phases/08-group-bookings/08-16-SUMMARY.md is not yet written — use the Task-1 commit hash
  </read_first>
  <action>
    Mark item 4 closed using the item-3 convention exactly: strike through the heading, add a dated closure note naming the Task-1 commit, and state in one paragraph what now holds — the race file drives the real `claimSeat` on per-racer connections, so deleting `FOR UPDATE` from `src/lib/group/seat-claim.ts:54` turns the suite RED, while the inlined cases remain as the pattern proof. Preserve the original entry verbatim inside a `<details>` block.

    Do not touch items 1, 2 or 3. Item 1 is closed by plans 08-12/08-13 and is marked there; item 2 (request-mode listings cannot declare a headcount before host approval) stays open and out of scope for this run.
  </action>
  <acceptance_criteria>
    - `.planning/phases/08-group-bookings/deferred-items.md` item 4's heading is struck through and carries a dated closure note naming a real commit hash
    - The original item-4 text is still present inside a `<details>` block (nothing is deleted from the record)
    - Items 1, 2 and 3 are byte-identical to before this task (`git diff` shows changes only within the item-4 section)
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat .planning/phases/08-group-bookings/deferred-items.md</automated>
  </verify>
  <done>The deferred-items record shows item 4 closed, with the original entry preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent RSVP submissions → one `booking_group` row | the read-count-decide-write window the `FOR UPDATE` lock spans |
| a future edit to `src/lib/group/seat-claim.ts` → a green CI run | the boundary this plan actually defends: today a one-line deletion crosses it silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-56 | Tampering (integrity) | `src/lib/group/seat-claim.ts:54` `FOR UPDATE` | mitigate | A racing case that drives the real `claimSeat` on independent connections, whose acceptance criterion IS the mutation: delete the lock → red, restore → green, `git diff --exit-code` clean. |
| T-08-57 | Repudiation | a mutation instruction that cannot be executed as written | mitigate | The file header is rewritten to name two mutation targets — the test's own inlined `FOR UPDATE` (pattern proof) and `src/lib/group/seat-claim.ts:54` (shipped proof) — each against its own file and line, so the next executor of the gate is not misled as 08-09's was. |
| T-08-58 | Denial of Service (false green) | a serialized "race" | mitigate | Each racer gets its own `drizzle(client)` over its own `makeRacingClients` connection and a name-only-guest identity that cannot de-dup, so a loser is genuinely a loser rather than a replayed match. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No packages added or upgraded; `drizzle-orm/postgres-js` and `postgres` are already direct dependencies used by `tests/helpers/db.ts`. |
</threat_model>

<verification>
- `npx vitest run tests/group/` exits 0
- `npx vitest run` full suite exits 0 with no `DATABASE_URL` override in the shell
- Both mutations (production file and test-inlined file) recorded red/green in the SUMMARY
- `git diff --exit-code src/lib/group/seat-claim.ts` exits 0 — no production file is modified by this plan
</verification>

<success_criteria>
- Deleting `FOR UPDATE` from `src/lib/group/seat-claim.ts` makes `npx vitest run tests/group/seat-claim-race.test.ts` fail
- The two pre-existing inlined-SQL cases are unchanged and still pass
- No production source file is modified by this plan
- `deferred-items.md` item 4 is marked closed with its original text preserved
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-16-SUMMARY.md` when done — include both mutation red/green outputs verbatim and the `git diff --exit-code` confirmation for `src/lib/group/seat-claim.ts`.
</output>
