---
context: phase
phase: 08-group-bookings
task: null
total_tasks: null
status: in_progress
last_updated: 2026-07-24T10:09:06.102Z
---

<current_state>
Phase 7 is fully COMPLETE and committed (see below). Phase 8 (Group Bookings) discussion was OPENED via `/gsd-discuss-phase 8` but is paused in a **strategic debate**, not the structured flow — the user twice dismissed the gray-area multiselect to interrogate the idea itself. **No 08-CONTEXT.md exists yet.**

The debate's crux (do NOT lose this): v1 group bookings, as scoped, is a **FREE RSVP coordination shell** layered on a booking the organizer pays 100% for. RSVP never touches occupancy and never becomes per-attendee payment (GPAY-01 cost-splitting + GPAY-02 ticketing are explicitly OUT of v1 scope). So the invite link is essentially **decorative unless attendees pay their share** — its only justifications are (a) foundation for a future paid version, (b) a growth/distribution funnel, (c) a capacity cap a group chat can't enforce. The user's sharp instinct: "if the organizer pays for the whole, sending links doesn't make sense." That's correct *about v1 as a standalone feature*.

**The gate:** the whole feature's worth hinges on one unanswered product question — **does FitOut eventually do per-attendee payment / cost-splitting?** If yes, v1's free shell is a smart down-payment on the rails (invites/RSVP/capacity/identity). If no, the free shell is hard to justify and deferring Phase 8 is the honest move. Resolve THIS before writing any CONTEXT.md.
</current_state>

<completed_work>

This session (all committed on `dev`):
- Phase 7 gap plans 07-18 / 07-19 / 07-20 executed (wave-1 gaps-only), suite green.
- Phase 7 human-UAT T6/T8/T9 driven in a real browser (host + booker DB-session cookies, seeded fixtures) — all pass. 07-HUMAN-UAT.md status=passed; 07-VERIFICATION.md status=passed.
- quick 260724-jo1 (edc10e5): fixed `/host/earnings` "₱300.00in" missing-space (SWC JSX whitespace strips the space after `{formatMoney(...)}`) → extracted `CancellationFeeNotice` with `{" "}` + jsdom regression.
- quick 260724-l1s (35243e1) + e54e416: rewrote the stale Phase-4 search-and-book e2e to the current instant-book flow; serialized public-listing spec to kill a parallel CONNECTION_ENDED flake. Full e2e suite 16/16 green on two consecutive runs.
- quick 260724-lmy (4a97771 / 463d131 / 98b49ab): build-guard NEXT_PHASE exemption (plain `npm run build` passes, guards still fire at runtime), eslint ignores for `.claude/worktrees/**` + `**/.next/**`, and the react-hooks/set-state-in-effect fix in address-autocomplete.
- Documented (94d9681): refund-workflow verification gap + local-Inngest two-process note.
- Phase 8 discussion started; gray areas identified; strategic debate held (in progress, no artifact).
</completed_work>

<remaining_work>

1. **Resolve the payment-model question** (blocking) — per-attendee pay / cost-splitting eventually: yes or no?
2. If proceeding: run the 4-gray-area discussion → write `08-CONTEXT.md`. The four areas:
   - **Invite & RSVP identity** — shareable link vs per-email tokens vs both; what "who's coming" captures; can attendees change RSVP / bring +guests. (REQUIREMENTS flags: "tokenized link / no account" is the v1 default — confirm.)
   - **Headcount cap rules** — does the organizer count? what counts (yes / +guests)? behavior at capacity (block/waitlist/decline)? `listing.max_occupancy` is NULLABLE — fallback when unset? (SC#4 atomic hard-cap; REQUIREMENTS flags capacity-source.)
   - **Group-creation entry point** — opt-in at checkout vs "Invite people" on the confirmed booking; which bookings qualify.
   - **Invite/RSVP lifecycle & edges** — RSVP close time; what happens to invites if the booking is cancelled/refunded; revoke/resend.
3. **PRE-LAUNCH GATE (separate track):** validate the core money loop live vs real PayMongo test mode (payment → confirm → payout → refund). Never run live; only stubbed. Card/GCash rail testable now; QRPh blocked on PayMongo Money Movement. Ready fixture: booking `42132ab1` / `pay_C4PW6fRGtUTNm6GsKCpt4P36`.
</remaining_work>

<decisions_made>

- Build Phase 8 anyway (user's explicit choice), with money-loop validation parked as a pre-launch gate — group bookings is decoupled from the money path so it's low-risk to build; the bigger risk (unvalidated live money loop) is deferred, not dropped.
- Phase 7 declared fully complete (verification passed, e2e green, build/lint clean).
</decisions_made>

<blockers>
- Product decision needed (per-attendee payment / cost-splitting on the roadmap?) before 08-CONTEXT.md is worth writing. No technical workaround — it's a scope call.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — Phase 8 goal + GROUP-01..05 success criteria (RSVP is informational coordination, never per-attendee payment).
2. `.planning/REQUIREMENTS.md` — GROUP-01..05; the OUT-OF-SCOPE list (GPAY-01 cost-splitting, GPAY-02 ticketing); the two flagged open questions (capacity source, RSVP identity).
3. `.planning/phases/07-bookings-management-cancellation-notifications/deferred-items.md` — refund-workflow gap + PayMongo-blocked items + UI/product deferrals (incl. duplicated approve/decline surfaces).
4. This session's Phase-7 contracts (notify/email layer, DB-atomic correctness discipline) — reuse for invites/RSVP/headcount-cap.

## Infrastructure State
- Docker Postgres `fitout-db-1`: UP (localhost:5432).
- Next app `:3000`: **STOPPED** (the build-guard task killed it to avoid a `.next` conflict). Restart with `npm run dev`.
- Inngest dev server `:8288` (PID 11904): running but **0 connected apps** (the app is down). Restart the app, and separately `npm run dev:inngest`. Local Inngest = TWO processes; `npm run dev` does NOT start it.
- UAT DB fixtures still present (labelled `uat-t8-*`, `uat-t9-*`, plus request `02e7e24b` SLA bumped to 2026-07-30) — harmless local-only test data; remove if desired.
- Working tree: clean. Branch `dev`, 37 commits ahead of `origin/dev` (this pause pushes to origin).

<context>
We were in "founder debate" mode, not structured discussion — the user wanted to stress-test whether group bookings is even the right feature (they questioned priority, chose to build it anyway, then questioned the payment model). The honest conclusion reached: free-RSVP group bookings is only compelling as foundation-for-paid-version + growth funnel; its value collapses to "nice coordination tool, marginally better than a group chat" if the paid version never comes. Everything downstream (which gray areas matter, what schema to build) depends on the payment-model answer.
</context>

<next_action>
Start with: ask the user the gate question — "does FitOut eventually do per-attendee payment / cost-splitting?" If YES → resume the 4-gray-area discussion and write 08-CONTEXT.md (treat the free-RSVP rails as the foundation the paid version plugs into). If NO → put the option of deferring Phase 8 on the table before committing scope. Do NOT write 08-CONTEXT.md until this is settled.
</next_action>
