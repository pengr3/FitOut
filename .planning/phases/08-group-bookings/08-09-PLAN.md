---
phase: 08-group-bookings
plan: 09
type: execute
wave: 5
depends_on: [08-07, 08-08]
files_modified: []
autonomous: false
requirements: [GROUP-02, GROUP-03, GROUP-04, GROUP-05]
must_haves:
  truths:
    - "Phase gate (VALIDATION.md): the full suite is green AND the GROUP-05 seat-claim race test is mutation-verified (delete FOR UPDATE → red → restore) before sign-off"
    - "GROUP-02/03/04: a real guest opens a real invite link in a fresh browser and RSVPs without an account; the organizer's headcount + roster reflect it (the cross-session UX the units cannot fully prove)"
    - "D-117/D-122: a guest-with-email actually receives the RSVP confirmation email via real Resend; a blank-email guest receives nothing; a group cancellation notifies reachable attendees"
  artifacts: []
  key_links:
    - from: "manual UAT"
      to: "the shipped invite → RSVP → roster flow"
      via: "fresh-browser guest session + real email delivery"
      pattern: "invite"
---

<objective>
Phase-gate the group-bookings feature: prove the full automated suite is green with the GROUP-05 seat-claim race test mutation-verified, then run the two Manual-Only cross-session verifications from 08-VALIDATION.md (a real guest RSVP in a fresh browser reflected on the organizer roster, and real guest-with-email delivery). These are end-to-end UX + external-side-effect checks the unit/integration tests deliberately do not cover.

Purpose: The seat-claim is enforced server-side and the units prove it; this checkpoint confirms the human-facing invite → RSVP → roster loop and real email delivery before `/gsd:verify-work`.
Output: a human sign-off (or logged issues) — no code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-group-bookings/08-VALIDATION.md
@.planning/phases/08-group-bookings/08-UI-SPEC.md
@$HOME/.claude/projects/C--Users-Admin-Roaming-FitOut/memory/local-env-and-uat-seed.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green + GROUP-05 race mutation-verification (the acceptance gate)</name>
  <files></files>
  <read_first>
    - .planning/phases/08-group-bookings/08-VALIDATION.md (Sampling Rate — the phase gate before verify-work)
    - tests/group/seat-claim-race.test.ts (the acceptance-gate race test)
    - src/lib/group/seat-claim.ts (the FOR UPDATE to mutate then restore)
  </read_first>
  <action>
    Run the full test suite (`npm test`) and confirm it is green. Then perform the non-negotiable acceptance-gate mutation: remove `FOR UPDATE` from `src/lib/group/seat-claim.ts`, run `npx vitest run tests/group/seat-claim-race.test.ts`, confirm it goes RED (2+ committed 'yes' at capacity_snapshot=1), then RESTORE `FOR UPDATE` and confirm GREEN. Record the before/after test output in the SUMMARY. Do not commit the mutated state.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green, above the Phase-7 baseline of 78 files / 655 tests)
    - the GROUP-05 race test is RED with FOR UPDATE removed and GREEN when restored — the exact red/green outputs recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full suite green; the D-112 seat-claim acceptance gate is mutation-verified and restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cross-session guest RSVP flow + real email delivery (human-verify)</name>
  <files></files>
  <action>
    Pause for the human to run the manual cross-session verification described below (the two Manual-Only rows of 08-VALIDATION.md). This is a checkpoint — Claude makes NO code changes here; it presents the steps, waits for the resume signal, and records the outcome (approved, or the issues, which become /gsd:plan-phase 8 --gaps input).
  </action>
  <what-built>The end-to-end group-booking flow: an "Invite people" entry on a confirmed booking → a shareable /invite/[token] link → guest/account RSVP (yes/no) → the organizer's live headcount + "who's coming" roster; plus real transactional email for guests-with-email and group-cancellation notices.</what-built>
  <how-to-verify>
    1. Start both local processes (project memory): `npm run dev` (app :3000) AND `npm run dev:inngest` (:8288). For real email delivery, ensure `RESEND_API_KEY` is set (otherwise sends log to [email:dev]).
    2. Sign in as the seeded host/booker, open a CONFIRMED booking at `/bookings/[id]`, click the coral "Invite people", and copy the invite link from `/bookings/[id]/group`.
    3. In a PRIVATE/incognito window (no session), open the invite link. Confirm you are NOT bounced to /login. RSVP once as a NAME-ONLY guest (no email) — confirm the on-screen "only confirmation" copy. Open the link again in another private window and RSVP as a GUEST-WITH-EMAIL (use a real address you can check) — confirm the confirmation email arrives via real Resend.
    4. Back as the organizer, confirm the `/bookings/[id]/group` headcount and roster reflect both RSVPs (organizer row #1, yes rows first), refreshing on their own via the poller.
    5. Cancel the parent booking; confirm the invite link now shows "no longer active", and the guest-with-email receives a cancellation notice while the name-only guest receives nothing.
    6. (Optional) On a listing with an extra-guest fee set in the wizard, confirm the reserve page PaxStepper re-quotes server-side and the "Extra guests" breakdown line appears; and that the organizer sees the neutral top-up nudge when yes-RSVPs exceed the declared pax.
  </how-to-verify>
  <verify>
    <human-check>The human confirms steps 2–5 behave as described (guest RSVP without login, roster reflects it, real email arrives for the guest-with-email, blank-email guest gets none, cancellation voids the link + notifies reachable attendees).</human-check>
  </verify>
  <done>Human sign-off recorded (approved), or issues logged for gap closure.</done>
  <resume-signal>Type "approved" if the cross-session RSVP flow, roster, and real email delivery all behave as described; otherwise describe the issues (they become gap-closure input for /gsd:plan-phase 8 --gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real guest browser → live invite link | the production-shaped, unauthenticated access path is exercised here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-28 | Tampering (integrity) | live cap overflow | mitigate | the GROUP-05 race test is mutation-verified in Task 1 before the human flow; the live flow confirms the calm "full" UX |
| T-08-29 | Information Disclosure | real email side-effect | accept | manual verification of real Resend delivery is the only way to confirm the external side-effect (VALIDATION.md Manual-Only) |
| T-08-SC | Tampering | npm installs | accept | no packages added this phase |
</threat_model>

<verification>
- `npm test` green; GROUP-05 mutation-verified; human sign-off on the cross-session flow + real email.
</verification>

<success_criteria>
- The full suite is green with the acceptance gate mutation-verified, and a human has confirmed the real guest RSVP → organizer-roster → email loop end-to-end (or logged issues for gap closure).
</success_criteria>

<output>
Create `.planning/phases/08-group-bookings/08-09-SUMMARY.md` when done — include the mutation-verification output and the human-verify result.
</output>
