---
phase: 25-finalize-paymongo-production-payments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
  - .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
autonomous: true
requirements: []
user_setup:
  - service: Local Docker Engine
    why: "The isolated PostgreSQL payment suite cannot run without a reachable Docker daemon."
    dashboard_config:
      - task: "Make the local Docker Engine reachable before the executor starts the database test gate."
        location: "Developer workstation"
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Payment mechanics are checked through the existing checkout, webhook, confirmation, reconciliation, refund, and payout tests only after the isolated test database is available."
    - "Every production payment capability has an explicit initial evidence status rather than an inferred account entitlement."
    - "The phase has a non-secret evidence record that distinguishes passed, blocked, and unverified observations."
  artifacts:
    - path: .planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
      provides: "Complete capability matrix for checkout rails, webhooks, recovery, refunds, linked accounts, transfers, and rollout."
    - path: .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
      provides: "Non-secret evidence and controlled-operation record."
  key_links:
    - from: "Existing payment tests"
      to: "25-PRODUCTION-RUNBOOK.md"
      via: "The exact command, result, and any blocked prerequisite are recorded without credentials or payment payloads."
    - from: "COVERAGE.md"
      to: "later human checkpoints"
      via: "Each release decision resolves a capability row with dated evidence or a concrete approved opt-out."
---

<objective>
Establish a reproducible, non-production evidence baseline for the existing PayMongo payment system.

Purpose: Prove repository mechanics before anyone changes a live account or accepts a controlled transaction, while making the unavailable local database an explicit Wave 0 gate.
Output: An updated capability matrix and a non-secret production-payment runbook template with local verification evidence.
</objective>

## Artifacts this phase produces

- `COVERAGE.md` — exhaustive capability and release-effect matrix.
- `25-PRODUCTION-RUNBOOK.md` — dated, redacted evidence ledger and controlled-operation checklist.

<execution_context>
@C:/Users/Admin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-finalize-paymongo-production-payments/25-RESEARCH.md
@AGENTS.md
@package.json
<interfaces>
The existing payment contract is retained. `confirmPaidBooking({ bookingId, paymentId, paymentMethod })` is the sole status-scoped `pending|approved → confirmed` writer; verified webhook and reconciliation are provider-evidence callers. Checkout creation remains server-owned behind its checkout lease and session retirement.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the existing checkout-to-confirmation and recovery contract through the restored isolated test database</name>
  <read_first>AGENTS.md; package.json scripts; tests/paymongo/checkout-idempotency-real.test.ts; tests/paymongo/webhook-signature.test.ts; tests/paymongo/webhook-payment-paid.test.ts; tests/payments/confirm-idempotency.test.ts; tests/payments/payment-reconcile.test.ts; tests/payments/payout-reconcile.test.ts</read_first>
  <precondition>Docker Engine answers `docker info` without revealing credentials; otherwise stop at the Wave 0 infrastructure gate and do not describe database-backed tests as green.</precondition>
  <files>.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</files>
  <action>Run `npm run db:up`, then `npm run db:test:setup`, before any database-backed payment claim. Run the focused suite named in the Research: webhook signature, paid-webhook, refund-webhook, merchant-activation, checkout-idempotency, checkout-create, confirm-idempotency, payment-reconcile and cadence, payout-sweep, and payout-reconcile. Run the scoped payment ESLint command and the inherited `node node_modules/typescript/bin/tsc --noEmit` command. Run the existing opt-in test-mode Checkout Session probe only when its non-secret opt-in conditions already hold; do not set or print a credential. Create the runbook with command, UTC timestamp, exit result, suite scope, test-mode probe result, and a blocked-not-green record if any prerequisite or command fails. Do not install packages, generate a migration, alter a payment source file, or turn a browser return into confirmation evidence.</action>
  <acceptance_criteria>The runbook records the exact local baseline result; all database-backed green claims occur only after Docker and `fitout_test` setup completed; no credential, wallet value, customer record, or raw provider payload is written.</acceptance_criteria>
  <verify>
    <automated>npm test -- tests/paymongo/webhook-signature.test.ts tests/paymongo/webhook-payment-paid.test.ts tests/paymongo/webhook-refund.test.ts tests/paymongo/webhook-merchant-activated.test.ts tests/paymongo/checkout-idempotency-real.test.ts tests/payments/checkout-create.test.ts tests/payments/confirm-idempotency.test.ts tests/payments/payment-reconcile.test.ts tests/payments/payment-reconcile-cadence.test.ts tests/payments/payout-sweep.test.ts tests/payments/payout-reconcile.test.ts</automated>
    <fails_when>The command exits nonzero, cannot connect to the isolated PostgreSQL test database, or reports a failing focused payment test.</fails_when>
    <automated>node node_modules/eslint/bin/eslint.js src/lib/paymongo.ts src/app/api/paymongo/webhook/route.ts src/lib/payments/confirm-booking-payment.ts src/app/actions/booking.ts src/lib/payments/checkout-probe.ts src/lib/payments/checkout-lease.ts src/lib/payments/retire-checkout.ts src/lib/payments/refund-rail.ts src/lib/payments/cancellation.ts src/inngest/functions/payment-reconcile.ts src/inngest/functions/payout-reconcile.ts src/inngest/functions/payout-sweep.ts src/app/api/inngest/route.ts src/lib/payments/config.ts</automated>
    <fails_when>ESLint exits nonzero for an existing payment-path source file.</fails_when>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The compiler exits nonzero.</fails_when>
  </verify>
  <done>The existing end-to-end test path from server-owned Checkout Session behavior through verified confirmation and recovery has current, accurately scoped local evidence or an explicit Wave 0 blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Make the capability matrix and evidence template complete before any live-money checkpoint</name>
  <read_first>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md; .planning/phases/25-finalize-paymongo-production-payments/25-RESEARCH.md</read_first>
  <files>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md, .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</files>
  <action>Review and complete `COVERAGE.md` as the single capability matrix. Create `25-PRODUCTION-RUNBOOK.md` with sections for authority, environment boundary, provider evidence references, controlled checkout, webhook delivery, provider-probe recovery, refund/manual return, linked-account activation, payout/wallet recovery, alerts, rollout decision, rollback record, and an evidence index. Keep every live account fact at `UNKNOWN` until the authorized checkpoint supplies evidence. Record Research assumptions A1–A4 and open questions Q1–Q5 as unresolved inputs, not conclusions. Include every configured rail, payment event, reconciliation path, refund/manual fallback, merchant/linked-account path, wallet/transfer path, and broad-release gate. Store only redacted references and outcomes; never copy values from protected consoles.</action>
  <acceptance_criteria>The two documents make every required capability visible, distinguish unknown from verified and concrete opt-out states, and give later operators a bounded place to record evidence without recording secrets.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const base='.planning/phases/25-finalize-paymongo-production-payments/'; const checks={ 'COVERAGE.md':['Card hosted checkout','GCash hosted checkout','PayMaya hosted checkout','QR Ph hosted checkout','Paid checkout webhook','Missed-webhook reconciliation','Merchant / linked-account activation','Host payout transfer','Payout reconciliation and recovery','Broad availability authorization'], '25-PRODUCTION-RUNBOOK.md':['Authority','Controlled checkout','Provider-probe recovery','Manual return','Payout','Rollback'] }; for(const [file,terms] of Object.entries(checks)){const text=fs.readFileSync(base+file,'utf8'); for(const term of terms) if(!text.includes(term)) throw new Error(file+' missing '+term);} console.log('payment evidence documents complete');"</automated>
    <fails_when>Either required document is absent or a required capability/evidence section is missing.</fails_when>
  </verify>
  <done>The release evidence surface is complete and intentionally contains no asserted production entitlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local test runner → isolated PostgreSQL | Tests can only demonstrate durable payment behavior when their database setup actually succeeds. |
| tracked evidence documents → human operators | Documentation can mislead a live-money decision if it turns unknown provider state into a claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Denial of service | Local DB-backed payment suite | high | mitigate | Require Docker and `fitout_test` before the suite; record a blocker rather than a passing claim. |
| T-25-02 | Information disclosure | Coverage and runbook documents | high | mitigate | Store evidence references and outcomes only; keep credentials, wallet values, customer data, and raw payloads out of tracked files. |
| T-25-SC | Tampering | Package-manager supply chain | high | mitigate | No package installation is authorized; if a defect suggests one, stop for a Package Legitimacy Audit and blocking review before changing dependencies. |
</threat_model>

<verification>
Run the focused payment suite only after the database gate, then run scoped ESLint and the inherited TypeScript command. A skipped or blocked database suite is a blocker, never a green result.
</verification>

<success_criteria>
The phase starts with a complete, non-secret evidence ledger and a demonstrably accurate local payment baseline or a clearly recorded Wave 0 environment blocker.
</success_criteria>

<output>
Create `.planning/phases/25-finalize-paymongo-production-payments/25-01-SUMMARY.md` when done.
</output>

## Source coverage audit

| Source | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | Gated production-payment evidence and release decision | 01–04 | COVERED | The roadmap placeholder is finalized without asserting a launch. |
| REQ | — | No mapped requirement IDs (`TBD`) | 01–04 | N/A | No requirement ID is fabricated. |
| RESEARCH | — | Restore DB test gate and preserve existing payment invariants | 01 | COVERED | Local contract evidence precedes console changes. |
| RESEARCH | A1–A4, Q1–Q5 | Account, provider, operations, controlled-money, and rollback unknowns | 02–04 | COVERED | Each is an explicit human gate or hold condition. |
| RESEARCH | — | Rails, webhook events, reconciliation, refunds, linked accounts, payouts | 01–04 | COVERED | Full matrix maps each to evidence and fallback. |
| CONTEXT | — | No CONTEXT.md by user choice | 01–04 | N/A | There are no locked decisions or deferred ideas to infer. |
