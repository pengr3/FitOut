---
phase: 25-finalize-paymongo-production-payments
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
  - .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
autonomous: false
requirements: []
user_setup:
  - service: PayMongo production account
    why: "Only an authorized account owner can establish actual live rail, webhook, marketplace, wallet, and account-limit status."
    env_vars:
      - name: PAYMONGO_SECRET_KEY
        source: "Authorized PayMongo/Vercel production-secret management; value is never placed in source or planning artifacts."
      - name: PAYMONGO_WEBHOOK_SECRET
        source: "Authorized PayMongo webhook configuration and Vercel production-secret management; value is never displayed."
      - name: PLATFORM_WALLET_NUMBER
        source: "Authorized provider wallet configuration only if wallet transfers are confirmed enabled."
      - name: PLATFORM_WALLET_NAME
        source: "Authorized provider wallet configuration only if wallet transfers are confirmed enabled."
    dashboard_config:
      - task: "Verify the canonical production webhook destination and only the event set accepted by the existing signed receiver."
        location: "PayMongo protected production console"
  - service: Inngest production app
    why: "The recovery and alert functions are operational only when the protected endpoint has synchronized and signed execution is configured."
    env_vars:
      - name: INNGEST_SIGNING_KEY
        source: "Authorized Inngest/Vercel production-secret management; value is never displayed."
      - name: INNGEST_EVENT_KEY
        source: "Authorized Inngest/Vercel production-secret management; value is never displayed."
  - service: Vercel production project
    why: "Production-only server secrets, canonical hostname, and Preview isolation must be checked without exposing values."
    env_vars:
      - name: OPS_ALERT_EMAIL
        source: "A monitored production mailbox selected by the authorized operations owner."
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An authorized human can show which live PayMongo products and checkout rails are approved, without the plan inferring entitlement."
    - "Production secrets, webhook routing, and Inngest registration are checked by name and scope without exposing values or enabling Preview payments."
    - "A controlled transaction has an explicit authority, amount/rail boundary, abort condition, refund/manual-return owner, and rollback boundary before it can occur."
  artifacts:
    - path: .planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
      provides: "Current evidence state for provider and operating readiness."
    - path: .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
      provides: "Approved, redacted readiness and controlled-transaction boundary record."
  key_links:
    - from: "PayMongo protected production console"
      to: "COVERAGE.md"
      via: "An authorized owner resolves each rail, event, account, and wallet row using a dated evidence reference."
    - from: "Vercel and Inngest protected consoles"
      to: "existing payment webhook and reconciliation paths"
      via: "Secret name/scope and registered-function evidence, not exported secret values."
---

<objective>
Establish authoritative production-account and operational readiness before any real charge can be authorized.

Purpose: Convert Research assumptions A1–A4 and open questions Q1–Q5 into bounded human evidence and a controlled-transaction decision, without making a live-money change by inference.
Output: Redacted account/configuration evidence and a signed-off controlled-transaction boundary in the runbook and matrix.
</objective>

## Artifacts this phase produces

- An updated `COVERAGE.md` with each account and configuration capability resolved as verified, unknown, or a concrete authorized opt-out.
- An updated `25-PRODUCTION-RUNBOOK.md` holding evidence references, accountable roles, transaction bounds, and rollback ownership.

<execution_context>
@C:/Users/Admin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-finalize-paymongo-production-payments/25-RESEARCH.md
@.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md
@.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify live-account, production-secret, webhook, Inngest, and alert readiness with the authorized owners</name><what-built>Redacted readiness evidence for rails, protected configuration, webhook delivery, Inngest registration, and alert ownership.</what-built><how-to-verify>Authorized PayMongo, Vercel, and Inngest owners inspect their protected consoles, provide dated evidence references, and confirm that only redacted outcomes were written.</how-to-verify><resume-signal>Type approved when the evidence is recorded, or describe the missing capability to retain HOLD.</resume-signal>
  <read_first>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md; .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md; .planning/phases/25-finalize-paymongo-production-payments/25-RESEARCH.md</read_first>
  <precondition>The Wave 0 local baseline and the initial capability matrix from Plan 25-01 exist; do not use a protected console review to compensate for an unrecorded test-infrastructure failure.</precondition>
  <files>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md, .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</files>
  <action>At this blocking checkpoint, require an authorized PayMongo account owner plus the authorized Vercel/Inngest production operators to supply dated evidence for each matrix row: live versus test account state; each configured rail's approval and limit; linked-account/merchant and wallet-transfer entitlement; canonical HTTPS production origin; signed webhook destination and subscribed events; production-only presence of the required server-secret names without their values; Preview's credential-free boundary; registered payment/payout reconciliation and alert functions; monitored alert recipient and response owner. Record evidence references, role names, outcomes, and unresolved discrepancies only. For any console configuration performed here, record the operator, configuration class, and reversible rollback boundary, never a value. Do not create a charge, replay a forged webhook, add a client confirmation path, or claim that an unviewed console setting exists.</action>
  <acceptance_criteria>Every readiness row is marked VERIFIED, UNKNOWN, or OPTED OUT with a concrete provider/product reason and an owned fallback; no unknown is converted to approved by prose; no secret or payment data is copied into the repository.</acceptance_criteria>
  <verify>
    <automated>node node_modules/typescript/bin/tsc --noEmit</automated>
    <fails_when>The compiler exits nonzero, so the current application baseline is not safe to use for a protected-console readiness decision.</fails_when>
    <human_check>Authorized owners show the protected-console evidence above and confirm that the runbook contains only redacted evidence references, role names, and outcomes.</human_check>
  </verify>
  <done>Production readiness is evidenced or explicitly held at the exact missing capability, with no live-money action performed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize or hold one bounded controlled transaction</name><decision>Whether one real transaction is authorized within the documented rail, amount, participant, refund, and stop boundaries.</decision><options><option>AUTHORIZE CONTROLLED TRANSACTION — every required bound and provider-side stop method is evidenced by accountable owners.</option><option>HOLD — any required fact, evidence, or rollback boundary is absent; no charge will be created.</option></options><resume-signal>Enter AUTHORIZE CONTROLLED TRANSACTION or HOLD with the accountable approvers.</resume-signal>
  <read_first>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md; .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</read_first>
  <reversibility rating="one-way">A real payment can only be reversed through provider and operational processes, so product and account authority must approve its amount, participant, rail, and stop condition before execution.</reversibility>
  <files>.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</files>
  <action>Present one decision with two permitted outcomes: `AUTHORIZE CONTROLLED TRANSACTION` or `HOLD`. Authorization must name the accountable product decision-maker and account operator, one approved rail, a single bounded amount within the documented provider limit, the test booker/host or operational recipient, expected provider/webhook/reconciliation evidence, refund or manual-return owner, stop-on-first-failure condition, and the provider-side method and authority for stopping new checkout activity. If any field is unknown or the provider-side stop method cannot be evidenced, record `HOLD`; do not invent a feature flag, alter payment code, or broaden traffic. Write the exact outcome and rationale to the runbook without customer data, secret values, wallet identifiers, or raw event data.</action>
  <acceptance_criteria>The runbook contains a clear authorize-or-hold decision with a specific live-money boundary and a rollback authority; broad availability remains unauthorized at this stage.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const text=fs.readFileSync('.planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md','utf8'); for(const heading of ['Authority','Controlled checkout','Rollback']) if(!text.includes(heading)) throw new Error('runbook missing '+heading); console.log('controlled-transaction decision record is structurally present');"</automated>
    <fails_when>The runbook is missing the authority, controlled-checkout, or rollback section needed for an accountable decision.</fails_when>
    <human_check>The accountable product decision-maker and authorized account operator explicitly choose AUTHORIZE CONTROLLED TRANSACTION or HOLD.</human_check>
  </verify>
  <done>A one-way live-money decision is recorded before any transaction is attempted; HOLD is a valid, safe terminal result for this task.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile readiness evidence into the capability matrix without disclosing protected values</name>
  <read_first>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md; .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</read_first>
  <files>.planning/phases/25-finalize-paymongo-production-payments/COVERAGE.md, .planning/phases/25-finalize-paymongo-production-payments/25-PRODUCTION-RUNBOOK.md</files>
  <action>Normalize the checkpoint results into the matrix and runbook. Preserve `UNKNOWN` for missing evidence. Use `OPTED OUT` only when the record names a concrete provider restriction or authorized product decision, its release effect, and the accountable owner. Cross-link every row to a dated evidence reference or an explicit hold. Re-state that the webhook receives raw authenticated bytes before parsing, event IDs deduplicate durably, provider evidence calls the single confirmation writer, session lease/retirement remains active, and browser returns are not confirmation evidence. Do not change a source, test, environment file, package manifest, schema, or console setting in this consolidation task.</action>
  <acceptance_criteria>The written record is internally consistent: each later live action has its prerequisite evidence, every unresolved item has an owner and release effect, and no record contains protected values.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const base='.planning/phases/25-finalize-paymongo-production-payments/'; const coverage=fs.readFileSync(base+'COVERAGE.md','utf8'); const runbook=fs.readFileSync(base+'25-PRODUCTION-RUNBOOK.md','utf8'); for(const term of ['VERIFIED','UNKNOWN','OPTED OUT']) if(!coverage.includes(term)) throw new Error('coverage lacks status vocabulary '+term); for(const term of ['Authority','Environment boundary','Evidence index']) if(!runbook.includes(term)) throw new Error('runbook missing '+term); console.log('readiness evidence vocabulary and runbook sections present');"</automated>
    <fails_when>The matrix cannot represent verified, unknown, and opt-out states or the runbook lacks accountable evidence sections.</fails_when>
  </verify>
  <done>The evidence ledger can gate the controlled transaction without relying on hidden or assumed production facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected PayMongo/Vercel/Inngest consoles → tracked evidence ledger | Account configuration and secret-scoped facts must be summarized without transferring protected values. |
| authorized human decision → live Checkout Session | A real-money charge must not proceed on an inferred entitlement or an unbounded instruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Information disclosure | Production-secret and wallet configuration | critical | mitigate | Verify names and scope only in protected consoles; prohibit values, identifiers, and raw payloads in tracked artifacts. |
| T-25-04 | Tampering | Webhook endpoint and subscribed event configuration | high | mitigate | Require authorized evidence of canonical endpoint, signing setup, and allowed event set before a transaction is authorized. |
| T-25-05 | Repudiation | Controlled-transaction authority | high | mitigate | Record accountable product/account roles, bounds, abort criteria, refund owner, and provider-side rollback authority in the runbook. |
| T-25-SC | Tampering | Package-manager supply chain | high | mitigate | No dependency change is authorized; a proposed package requires a fresh legitimacy audit and blocking review. |
</threat_model>

<verification>
Protected-console facts are verified by authorized humans; automated checks only protect the repository baseline and the structure of the redacted evidence record.
</verification>

<success_criteria>
Either a bounded controlled transaction is explicitly authorized with evidence and rollback authority, or the phase is visibly held at the missing fact without accepting customer money.
</success_criteria>

<output>
Create `.planning/phases/25-finalize-paymongo-production-payments/25-02-SUMMARY.md` when done.
</output>
