---
phase: 02-listings-host-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/components/ui/select.tsx
  - src/components/ui/command.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/aspect-ratio.tsx
  - src/lib/rate-limit.ts
  - src/lib/audit.ts
  - src/app/actions/capability.ts
  - tests/security/rate-limit.test.ts
  - tests/security/audit.test.ts
  - .env.example
autonomous: true
requirements: [LIST-01]
must_haves:
  truths:
    - "FitOut Coral (--brand) and --success tokens exist in light + dark and are exposed via @theme inline (UI-SPEC)"
    - "All 15 UI-SPEC shadcn components are installed from the official registry (no third-party)"
    - "A reusable rate-limiter + audit helper exist and the capability-activate actions are wrapped (WR-06)"
    - "All new Phase-2 npm dependencies (next-cloudinary, @dnd-kit/*) are installed; the PayMongo client is a thin server-side REST wrapper — no official SDK, wired in Plan 06"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--brand/--brand-foreground/--success tokens + @theme inline mappings"
      contains: "--brand:"
    - path: "src/lib/rate-limit.ts"
      provides: "in-memory/per-identity rate limiter for privileged server actions (WR-06)"
      contains: "export function"
    - path: "src/lib/audit.ts"
      provides: "audit-trail helper for capability/payout-activating actions (WR-06)"
      contains: "export"
    - path: "src/app/actions/capability.ts"
      provides: "activateHosting/activateBooking now rate-limited + audited (WR-06 closure)"
      contains: "rateLimit"
  key_links:
    - from: "src/app/actions/capability.ts"
      to: "src/lib/rate-limit.ts + src/lib/audit.ts"
      via: "wrap the privileged flip with a rate-limit check + audit log entry"
      pattern: "rateLimit|audit"
    - from: "src/app/globals.css"
      to: "@theme inline"
      via: "--color-brand: var(--brand) so Tailwind exposes bg-brand/text-brand-foreground"
      pattern: "--color-brand:"
---

<objective>
Lay the shared UI + security foundation that Plans 03–06 consume: install the Phase-2 dependencies,
add the FitOut Coral + success design tokens, install the 15 shadcn official-registry components named
in the UI-SPEC, and CLOSE the Phase-1 deferred WR-06 (rate-limit + audit trail on capability-activate
server actions) by building a reusable rate-limiter + audit helper and wrapping the existing
`activateHosting`/`activateBooking` actions. This satisfies the security carry-forward gate
(security_block_on=high) BEFORE Plan 06 wires PayMongo payouts to the host/`canHost` flow, and gives
Plan 04's Cloudinary sign endpoint and Plan 06's onboarding action the same rate-limit treatment.

Purpose: Phase-1 review deferred WR-06; STATE.md + 02-RESEARCH.md § Security require it closed before
payouts wire to canHost. Doing it here (shared, reusable) means Plans 04/06 just call the helpers.
Output: deps installed, design tokens, 15 UI components, rate-limit.ts + audit.ts, hardened capability.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-listings-host-onboarding/02-UI-SPEC.md
@.planning/phases/02-listings-host-onboarding/02-RESEARCH.md

<interfaces>
<!-- The exact things downstream plans expect from this plan. -->

WR-06 origin (src/app/actions/capability.ts) — the two privileged actions to wrap:
```typescript
export async function activateHosting(): Promise<CapabilityResult> // sets canHost=true (server-side)
export async function activateBooking(): Promise<CapabilityResult>  // sets canBook=true (server-side)
```
The session is resolved via `requireUserId()` (auth.api.getSession). The rate-limit key MUST be the
authenticated user id (not IP) since these are session-gated privileged escalations.

UI-SPEC § "New Tokens to Add" gives the EXACT oklch values (see action). UI-SPEC § Registry Safety:
shadcn official registry ONLY, `components.json` registries: {} (no third-party).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add design tokens, install 15 shadcn components</name>
  <files>package.json, src/app/globals.css, src/components/ui/*.tsx (15 shadcn components), .env.example</files>
  <read_first>
    - package.json (current deps — confirm none of next-cloudinary/@dnd-kit are present yet)
    - src/app/globals.css (the @theme inline block + :root/.dark — add tokens, do NOT redefine --radius or repurpose --primary)
    - 02-UI-SPEC.md § "New Tokens to Add" (lines 241-266 — exact oklch values) + § "To add — official registry" (lines 172-189)
    - .env.example (add the new Phase-2 env vars)
  </read_first>
  <action>
    1. Install runtime deps: `npm install next-cloudinary @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`
       (D-04/D-18: next-cloudinary + dnd-kit). **PayMongo has NO official SDK** (D-20, supersedes D-17): do NOT install
       an SDK-style payments package — the PayMongo client is a thin server-side `fetch` wrapper (or an optional community
       TS lib) added when Plan 06 builds `src/lib/paymongo.ts`. No PayMongo npm dependency is required in this plan.
    2. Add design tokens to `src/app/globals.css`. In `:root` add:
       `--brand: oklch(0.637 0.208 25);` `--brand-foreground: oklch(0.985 0 0);`
       `--success: oklch(0.62 0.17 150);` `--success-foreground: oklch(0.985 0 0);`
       In `.dark` add:
       `--brand: oklch(0.645 0.205 25);` `--brand-foreground: oklch(0.145 0 0);`
       `--success: oklch(0.7 0.16 150);` `--success-foreground: oklch(0.145 0 0);`
       In `@theme inline` add: `--color-brand: var(--brand);` `--color-brand-foreground: var(--brand-foreground);`
       `--color-success: var(--success);` `--color-success-foreground: var(--success-foreground);`
       Do NOT touch `--radius` or repurpose `--primary` (UI-SPEC line 266 — primary stays neutral).
    3. Install the 15 official shadcn components (no third-party registry):
       `npx shadcn@latest add select command popover checkbox switch radio-group badge tabs sonner skeleton alert progress separator tooltip aspect-ratio`
       Confirm each lands under `src/components/ui/`.
    4. Add to `.env.example` (documented, no real values): `PAYMONGO_SECRET_KEY=`, `PAYMONGO_PUBLIC_KEY=`,
       `PAYMONGO_WEBHOOK_SECRET=` (D-20: server-only secret key + client-safe public key + the `Paymongo-Signature`
       webhook secret; PayMongo is PH-native so there is NO country env — D-19 `country=US` is superseded). Cloudinary vars already exist
       from Phase 1; do not duplicate. (Photon/LocationIQ + react-leaflet OSM tiles need NO key — D-18.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>next-cloudinary + @dnd-kit/* in package.json (PayMongo needs no npm SDK — thin REST wrapper, wired in Plan 06); brand/success tokens in :root, .dark, and @theme inline; 15 shadcn components under src/components/ui; new PayMongo env vars in .env.example.</done>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');const d={...p.dependencies};console.log(d['next-cloudinary'], d['@dnd-kit/sortable'])"` prints two non-undefined versions (PayMongo has no npm SDK — verified via env vars + the Plan-06 client wrapper, not package.json)
    - `grep -c -- "--brand:\|--success:\|--color-brand:" src/app/globals.css` ≥ 3
    - All 15 files exist under `src/components/ui/` (select, command, popover, checkbox, switch, radio-group, badge, tabs, sonner, skeleton, alert, progress, separator, tooltip, aspect-ratio)
    - `grep -c "PAYMONGO_SECRET_KEY\|PAYMONGO_WEBHOOK_SECRET\|PAYMONGO_PUBLIC_KEY" .env.example` → 3
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-06 — rate-limit + audit helpers and wrap the capability-activate actions</name>
  <files>src/lib/rate-limit.ts, src/lib/audit.ts, src/app/actions/capability.ts, tests/security/rate-limit.test.ts, tests/security/audit.test.ts</files>
  <read_first>
    - src/app/actions/capability.ts (the file being modified — wrap activateHosting/activateBooking; keep the input:false discipline + structured CapabilityResult)
    - .planning/phases/01-auth-accounts/01-REVIEW.md § WR-06 (the deferred finding: "privileged escalation endpoints with no rate limiting, no audit trail")
    - src/lib/auth.ts § rateLimit (lines 153-174 — the existing Better-Auth rate-limit values to mirror: privileged actions ~5/60s per identity)
    - 02-RESEARCH.md § Security (lines 638, 642 — WR-06 carry-forward; the sign endpoint + onboarding action reuse this)
  </read_first>
  <behavior>
    - rate-limit.test.ts: the same user id calling a guarded action >N times within the window is rejected; a different id is unaffected; the window resets
    - audit.test.ts: invoking activateHosting writes an audit entry with the actor id, action name, and outcome; a failed (unauthenticated) call records nothing privileged
  </behavior>
  <action>
    Create `src/lib/rate-limit.ts` — a small per-identity fixed-window limiter usable from server actions
    and route handlers. Signature: `export function rateLimit(key: string, opts: { window: number; max: number }): { ok: boolean; retryAfter?: number }`.
    Use a module-level `Map<string, { count: number; resetAt: number }>` (in-memory is acceptable for v1 —
    single-region, single instance; document that a distributed store is a future hardening). Mirror the
    Better-Auth privileged-endpoint values (5 per 60s). Export a small wrapper helper
    `requireWithinRateLimit(key, opts)` that returns the structured result so callers can early-return.

    Create `src/lib/audit.ts` — an append-only audit-trail helper. Signature:
    `export async function recordAudit(entry: { actorId: string; action: string; outcome: "ok" | "denied" | "error"; meta?: Record<string, unknown> }): Promise<void>`.
    For v1, write a structured `console.info("[audit]", ...)` line (with timestamp) — do NOT over-build a DB
    table (STATE.md "do not over-build"); leave a doc-comment that a durable audit table is a future
    hardening. The point WR-06 requires is that privileged escalations are observable.

    MODIFY `src/app/actions/capability.ts`: in BOTH `activateHosting` and `activateBooking`, after resolving
    the session id but BEFORE the privileged `db.update`, call `rateLimit("activate:"+userId, { window: 60, max: 5 })`;
    if `!ok`, `recordAudit({ actorId: userId, action: "activateHosting"|"activateBooking", outcome: "denied", meta: { reason: "rate_limit" } })`
    and return `{ ok: false, error: "Too many attempts. Please try again in a moment." }`. On the successful
    flip, `recordAudit({ actorId: userId, action, outcome: "ok" })`. Keep the existing input:false +
    structured-result discipline intact (do not change the CapabilityResult type or the coexistence behavior).

    Update the WR-06 reference in capability.ts's header comment to note the gap is now closed.
  </action>
  <verify>
    <automated>npx vitest run tests/security/rate-limit.test.ts tests/security/audit.test.ts</automated>
  </verify>
  <done>rate-limit.ts + audit.ts exist and are unit-tested; activateHosting/activateBooking call both; exceeding the window returns a denial + an audit entry; the input:false discipline and CapabilityResult are unchanged.</done>
  <acceptance_criteria>
    - `src/lib/rate-limit.ts` contains `export function rateLimit`
    - `src/lib/audit.ts` contains `export async function recordAudit`
    - `grep -c "rateLimit\|recordAudit" src/app/actions/capability.ts` ≥ 4 (both actions, both helpers)
    - `npx vitest run tests/security/rate-limit.test.ts tests/security/audit.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session → privileged capability flip | activateHosting/activateBooking escalate a money-adjacent capability (canHost unlocks PayMongo payouts in Plan 06); must be bounded + observable |
| client bundle → secrets | new PAYMONGO_* secrets must never reach the client (server-only env, no NEXT_PUBLIC except the public key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-WR06 | Elevation of Privilege / DoS | capability-activate server actions | mitigate | WR-06 closure: per-identity rate limit (5/60s) + audit trail on every privileged flip; reused by Plan 04 sign endpoint + Plan 06 onboarding. |
| T-02-AUDIT | Repudiation | privileged escalation actions | mitigate | recordAudit logs actorId + action + outcome so escalations are non-repudiable (v1 structured log; durable table is future hardening). |
| T-02-SECRET | Information Disclosure | PAYMONGO_SECRET_KEY / PAYMONGO_WEBHOOK_SECRET | mitigate | Documented server-only in .env.example; only PAYMONGO_PUBLIC_KEY is client-safe; consumed by Plan 06's fail-closed paymongo.ts. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run tests/security/rate-limit.test.ts tests/security/audit.test.ts` passes
- `npm test` (full suite) still green — the existing 54 tests + capability tests unaffected by the WR-06 wrap
- 15 shadcn components present; brand/success tokens render (`bg-brand` available)
</verification>

<success_criteria>
- Phase-2 dependencies installed (next-cloudinary, @dnd-kit/*; PayMongo client is a thin REST wrapper — no npm SDK, wired in Plan 06)
- FitOut Coral + success tokens added (light + dark + @theme inline) without disturbing existing tokens
- All 15 UI-SPEC official-registry components installed
- WR-06 CLOSED: capability-activate actions are rate-limited + audited (security carry-forward gate satisfied before Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-listings-host-onboarding/02-02-SUMMARY.md`
</output>
