---
phase: 13.1-payment-reconciliation
verified: 2026-08-22T17:00:00Z
status: human_needed
score: 6/6 must-haves verified (0 blockers); 3 residual items require a human/PM decision before phase close
overrides_applied: 0
human_verification:
  - test: "Decide whether `cancelUnpaidHold` (src/app/actions/cancel-booking.ts:855-896) must be wired to the retire policy before this phase closes, or accepted as a documented residual."
    expected: "A PM/human call on whether the fourth session-orphaning path (found and self-reported by 13.1-05, never wired) is acceptable to ship. It is the only orphaning path in the codebase with NO backstop at all: if a booker withdraws an `approved` hold and its checkout session is later paid while the confirming webhook is also lost, the booking stays `cancelled` forever and the payment is never reconciled, refunded, or alerted on — because `expires_at` is nulled in the same UPDATE that releases the hold, which removes the row from `checkout-retire-sweep`'s candidate set (`expires_at <= now()`) in the same statement that orphans the session, and `payment-reconcile`'s candidate set requires `status IN ('pending','approved')`, which a cancelled row is not."
    why_human: "This is a genuine, structural gap outside the literal scope of every one of this phase's 5 roadmap Success Criteria and outside D-113 as literally worded (\"when a hold EXPIRES\" — this path is a voluntary cancel, not an expiry), so it does not fail a stated must-have. But it sits squarely inside the phase's own stated purpose (\"a booker who paid always ends up with a booking... never a silent loss\") and is the only orphaning path in the whole enumeration with zero backstop. Whether to fix it now (one `RETURNING` + one `retireCheckoutsForBookings` call, per deferred-items.md's own prescription) or accept it as bounded/low-probability is a product-risk call, not a code-correctness one — the fix is trivial but the phase's own plans repeatedly declined to make it in scope."
  - test: "Update `.planning/ops/NEEDS-ATTENTION-RUNBOOK.md` §4 to cover the two new alert action names this phase introduces (`webhook_missed`, `checkout_paid_after_lapse`), or confirm the omission is acceptable until the next runbook pass."
    expected: "An operator paging through the daily digest and seeing a `webhook_missed` or `checkout_paid_after_lapse` row should find it in the triage table. Today it is not there — the table still lists only the 13 action names that existed as of 2026-08-10 (quick task 260810-j3z), predating this phase by 12 days. `checkout_expire_failed` (also written by this phase's retire policy) IS already listed, since the re-price path wrote it first."
    why_human: "The alert is mechanically 'loud' — reachable by the existing `listUnresolvedAlerts` predicate and the daily digest with no code change needed, which is what SC3 literally asks for — but the runbook's own stated purpose (\"Audience: whoever holds the ops pager for FitOut. No prior context assumed\") is undermined if the two newest and most money-relevant action names in the system are not in the table an on-call operator is told to consult first. This is a documentation gap, not a code gap, and whether it blocks phase close is a judgement call."
  - test: "Decide whether the Checkout Session response shape's `payments[0].id` placement needs to be captured against a REAL PayMongo paid session before phase close, or remains a standing live-UAT follow-up."
    expected: "One `GET /v1/checkout_sessions/<id>` body for a genuinely PAID session, captured and compared against the envelope-first read `attributes.payments?.[0]?.id ?? attributes.payments?.[0]?.attributes?.id ?? null` that 13.1-01 shipped defensively (both placements read, neither deleted, because the plan's prose and this repo's own fixtures disagreed on where PayMongo puts it)."
    why_human: "This has been an open item since 13-03 (predates this phase) and was carried through all 5 plans of 13.1 without becoming live-verified — every live PayMongo proof in this phase (13.1-04's `lapsed-hold-not-payable-real.test.ts`) deliberately drives a session that is NEVER paid, so it live-verifies `status` but not `payments[0].id`. The cost of being wrong is bounded (`paymentId: null`, the pre-13.1-01 status quo — not a regression), so this is a low-urgency but still-open verification gap that a human running live UAT on the settling screen can close in one real payment."
---

# Phase 13.1: Payment Reconciliation Verification Report

**Phase Goal:** "A booker who paid always ends up with a booking. A lost webhook becomes a delay, never a
silent loss. And when a hold expires, the ability to pay expires with it — FitOut never takes money for a
booking that no longer exists."

**Verified:** 2026-08-22
**Status:** human_needed
**Re-verification:** No — initial verification

## Summary Judgment

**All 5 roadmap Success Criteria and the D-113 hold-expiry guarantee are independently verified true in
the codebase, not merely claimed in SUMMARY.md.** I re-derived the confirm path, the two sweeps, the
inline reclaim wirings, and the D-58/D-105/D-106/D-108/D-111/D-112 invariants directly from source, ran
the full test suite and the design gate myself (not trusting the SUMMARY's numbers), and queried the dev
database directly for the two D-111 evidence fixtures. Every claim I checked held.

**But three residual items are real, and none of them were silently hidden — every one is disclosed in
`deferred-items.md` or was found independently by me and cross-checked against it.** They do not fail any
of the 5 formally-stated Success Criteria or D-113 as literally worded, which is why this report does not
mark any must-have FAILED. They do sit inside the phase's own broader promise ("never a silent loss"), so
I am routing them to a human decision rather than declaring the phase unconditionally closed.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — A booking PayMongo records paid always reaches `confirmed` on a schedule, not only when a human is watching | ✓ VERIFIED | `src/inngest/functions/payment-reconcile.ts` exists, is registered in `src/app/api/inngest/route.ts:84`, and its candidate query (`queryUnconfirmedPaid`) is scoped to `status IN ('pending','approved')` — independently re-derived from source, matching the claim that `pending`-only would miss request-to-book. Cron `TZ=Asia/Manila 2-59/5 * * * *`, derived from the same constant the cadence test asserts against (not hand-typed). |
| 2 | SC2 — Reconciliation routes through the SAME idempotent confirm path the webhook uses; concurrency proof exists and could fail | ✓ VERIFIED | `grep -rn "status = 'confirmed'" src/` returns exactly ONE write: `src/lib/payments/confirm-booking-payment.ts:303`. The webhook route (`src/app/api/paymongo/webhook/route.ts:292`), the sweep (`payment-reconcile.ts:262`), and the fast path (`reconcile-payment.ts:177` via `reconcileOne`) all call `confirmPaidBooking`/`reconcileOne`, never a second UPDATE. `tests/payments/confirm-idempotency.test.ts` case (2) races two INDEPENDENT Postgres connections (`makeRacingClients`) over one row; I confirmed the test genuinely uses independent connections, not a serialized `Promise.all` over a `max:1` client. |
| 3 | SC3 — A missed webhook is LOUD: an operator alert records it, server-side only | ✓ VERIFIED (mechanism); ⚠ see human item 2 | `reconcileOne` writes `recordAudit({ action: "webhook_missed", outcome: "needs_attention" })` ONLY on outcome `"confirmed"` (a genuine transition), never on `"already-confirmed"` or a repeat pass — I confirmed this is structurally single-fire because the confirm's status-scoped claim means only one caller can ever get the "confirmed" outcome for a row. `listUnresolvedAlerts` (`src/lib/ops/alerts.ts`) reads any `needs_attention AND resolved_at IS NULL` row generically, so the existing daily digest reaches it with no code change. **However**, the operator-facing triage runbook was not updated with this action name — see residual below. |
| 4 | SC4 — PROJECT D-57 unweakened: a server-side probe is authoritative, a URL parameter is not | ✓ VERIFIED | `reconcile-payment.ts` reads no `searchParams`, no headers beyond session auth. In `bookings/[id]/page.tsx`, `paid === "1"` is used ONLY to select which UI branch renders (`paidParamDecay`, `showConfirmationMoment`), and `showConfirmationMoment` additionally requires `bk.status === "confirmed"` read from the DB — never combined with a write. `confirm-booking-payment.ts`'s only inputs are three server-derived scalars; no `Request`, header, or query parameter reaches it anywhere in its call graph. |
| 5 | SC5 — No double-charge, double-confirm, or resurrection of a gone slot; D-58 backstop still governs | ✓ VERIFIED | The confirm's `WHERE id = $1 AND status IN ('pending','approved')` is byte-identical to pre-phase logic (moved, not rewritten). `handleGoneSlot` is retained verbatim inside `confirm-booking-payment.ts` (auto-refund on a refundable rail, manual-return alert otherwise), and `git diff --stat origin/dev -- src/lib/payments/refund-rail.ts` is empty across all 5 plans — `REFUNDABLE_RAILS` was never widened. |
| 6 | D-113 (phase goal, 3rd sentence) — when a hold EXPIRES, the ability to pay expires with it | ✓ VERIFIED, and independently confirmed to hold WITHOUT 13.1-05 | `src/lib/payments/retire-checkout.ts` (the policy: probe-first, never expires a `paid` session — a call-count-of-zero, not an unthrown error; structurally cannot throw, one outer `try` covering `recordAudit` too; writes NO `booking` row, confirmed by reading the full function body) + `src/inngest/functions/checkout-retire.ts` (the sweep, registered at `route.ts:85`, candidate set `status IN ('pending','approved') AND expires_at <= now() AND expires_at > now() - 30min`). The sweep's reach is a superset of the three inline reclaim wirings' (`units.ts` ×2 + `request-expiry.ts` ×2 branches), all three of which I confirmed retire POST-COMMIT (outside any transaction/SAVEPOINT) with their own independent `catch` guarding against a PayMongo outage failing a booking. |

**Score:** 6/6 truths verified. 0 BLOCKER-level failures.

### Residual Risks (not gating a specific must-have, but real)

| # | Risk | Reachability | Backstop today | Disposition |
|---|------|-------------|-----------------|-------------|
| A | `cancelUnpaidHold` (`cancel-booking.ts:855-896`) releases an `approved` hold and sets `expires_at = NULL` in the same statement, with no `expireCheckoutSession`/`retireCheckoutsForBookings` call anywhere in the function. **Confirmed by direct code read**, not from the SUMMARY's claim. | Only the booking's own booker (ownership-gated, `booker_id = ${userId}` in the WHERE) — a self-inflicted path, not attacker-reachable. | **NONE.** `queryRetirableSessions`' `expires_at <= now()` cannot select a NULLed `expires_at`, confirmed by reading the predicate directly. `queryUnconfirmedPaid`'s `status IN ('pending','approved')` cannot select a `cancelled` row. If the webhook that would normally catch a late payment via `handleGoneSlot` is ALSO lost, the payment is reconciled by nothing, forever. | See human item 1. |
| B | Two new operator-alert action names (`webhook_missed`, `checkout_paid_after_lapse`) are not documented in `.planning/ops/NEEDS-ATTENTION-RUNBOOK.md` §4's triage table. **Confirmed by grep** — the table's 13 rows all predate this phase (2026-08-10); `checkout_expire_failed` is present only because an earlier phase's re-price path wrote it first. | Any on-call operator following the runbook as instructed ("no prior context assumed"). | The digest still surfaces the row (mechanism VERIFIED) — this is a documentation gap, not a silence. | See human item 2. |
| C | The "Inngest must re-sync before either cron ticks" deployment requirement is recorded only in code comments (`route.ts`) and in `13.1-02`/`13.1-04`'s SUMMARY.md "User Setup Required" sections — not in any operator-facing deployment checklist. **Pre-existing gap** (the same is true of the 4 crons that predate this phase); not a regression, but now covers two crons that ARE this phase's entire guarantee. | Whoever deploys/redeploys the app. | None — "a cron that never ticks raises no error anywhere" is stated as a fact in the code comment itself. | Noted for awareness; folded into human item 2's scope if a documentation pass is done. |
| D | `payments[0].id`'s envelope-vs-`attributes` placement is still not live-verified against a genuinely PAID PayMongo session (only `status` was live-verified, via a session that is deliberately never paid). | N/A — bounded cost (`paymentId: null` on the wrong guess, the pre-13.1-01 status quo). | Both placements are read defensively; neither is deleted. | See human item 3. Low urgency, explicitly disclosed since 13-03. |
| E | `handleGoneSlot`'s alert re-fires on every pass over an already-`cancelled` row (measured: 2 audit rows, not 1, in `confirm-idempotency.test.ts` case 5) | Only via a caller that hands `confirmPaidBooking` a terminal booking id — **no such caller exists in production today** (both the sweep and the fast path are scoped to `pending`/`approved` before delegating). | N/A — unreachable, not a live risk. Money-critical half (`createRefund` count) proven to stay at zero. | Correctly recorded as an intentional non-fix (13.1-01/02), not a regression. No action needed. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/payments/confirm-booking-payment.ts` | D-105's single confirm path | ✓ VERIFIED | Read in full. Exports `confirmPaidBooking`, `ConfirmOutcome`. Sole `UPDATE booking SET status = 'confirmed'` writer in `src/`. |
| `src/inngest/functions/payment-reconcile.ts` | The 5-minute reconciliation sweep | ✓ VERIFIED | Read in full. Registered, correct candidate scope, epoch-bounded, delegates to `confirmPaidBooking`. |
| `src/app/actions/reconcile-payment.ts` | The settling-screen fast path | ✓ VERIFIED | Read in full. Session→rate-limit→ownership→status-scope→epoch, all before any provider call; delegates to `reconcileOne`. |
| `src/lib/payments/retire-checkout.ts` | D-113's retire policy | ✓ VERIFIED | Read in full. Probe-first, never-throw (single outer `try` covering `recordAudit`), writes no `booking` row. |
| `src/inngest/functions/checkout-retire.ts` | D-113's sweep (the guarantee) | ✓ VERIFIED | Read in full. Registered, correct 4-clause candidate predicate, 30-min lookback documented and reasoned. |
| `src/lib/availability/units.ts` (2 call sites) | Post-commit retire wirings | ✓ VERIFIED | Both `retireCheckoutsForBookings` calls confirmed to sit AFTER `await tx.transaction(...)` resolves (i.e., after commit), each with its own `try/catch`. |
| `src/inngest/functions/request-expiry.ts` | Post-commit retire wiring | ✓ VERIFIED | `retireOrphanedSession` reads `checkout_session_id` off the flip's own `RETURNING`, called after the single-statement UPDATE (no explicit transaction to be "inside"). |
| `tests/payments/confirm-idempotency.test.ts` | Concurrency proof | ✓ VERIFIED | Uses `makeRacingClients` (2 independent connections), not a serialized client. |
| `tests/paymongo/lapsed-hold-not-payable-real.test.ts` | Live PayMongo proof | ✓ VERIFIED (present, correct skip behavior) | Confirmed skips cleanly without `RUN_LIVE_PAYMONGO_PROBE=1` (ran it myself: 1 skipped, no live call). Did not re-run the live-flagged version — the SUMMARY's verbatim transcript (session ids, before/after status) is internally consistent with the code path I independently read, and re-running would mint another real PayMongo session for no incremental evidence value. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `webhook/route.ts` | `confirm-booking-payment.ts` | `confirmPaidBooking` | ✓ WIRED | `route.ts:292`, outcome discarded by design (documented, correct per T-06-15 ACK reasoning). |
| `payment-reconcile.ts` | `confirm-booking-payment.ts` | `confirmPaidBooking` | ✓ WIRED | `payment-reconcile.ts:262`, via `reconcileOne`. |
| `reconcile-payment.ts` | `payment-reconcile.ts` | `reconcileOne` | ✓ WIRED | `reconcile-payment.ts:177`, same body, no second alert policy. |
| `pending-payment-state.tsx` | `reconcile-payment.ts` | `reconcilePaymentNow` | ✓ WIRED | Effect fires once, `slow`-gated, latched; `bookingId` passed from `bookings/[id]/page.tsx:680`. |
| `checkout-retire.ts` | `retire-checkout.ts` | `retireCheckoutForBooking` | ✓ WIRED | `checkout-retire.ts:213`. |
| `units.ts` ×2, `request-expiry.ts` | `retire-checkout.ts` | `retireCheckoutsForBookings` | ✓ WIRED | All 3 call sites confirmed post-commit with independent `catch`. |
| `route.ts` | `payment-reconcile.ts` / `checkout-retire.ts` | registration array | ✓ WIRED | Both present in `functions: [...]` at `route.ts:84-85`. |
| `retireCheckoutForBooking` / `reconcileOne` | `src/lib/audit.ts` (`needs_attention`) | `recordAudit` | ✓ WIRED | `listUnresolvedAlerts` predicate is generic (`needs_attention AND resolved_at IS NULL`), reaches all 3 new action names with zero code change. |
| `cancel-booking.ts:cancelUnpaidHold` | `retire-checkout.ts` | *(none)* | ✗ NOT WIRED | Confirmed by direct read — see Residual A. Self-reported by 13.1-05, independently re-confirmed by me. |

### Behavioral Spot-Checks (independently run, not trusting SUMMARY numbers)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm test` | **1687 passed \| 5 skipped** (169 files) | ✓ PASS — matches every SUMMARY's claimed count exactly |
| Design gate | `npm run test:design` | **816 passed \| 3 skipped** (48 files) | ✓ PASS — matches |
| Type check | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| D-112 migration pin | `ls drizzle/*.sql \| tail -1` / count | `0025_audit_resolved_by.sql` / 26 files | ✓ PASS — zero migrations added across all 5 plans |
| D-111 fixtures, queried directly against dev DB | `SELECT ... FROM booking WHERE id IN (...)` via `docker exec fitout-db-1 psql` | Both `09f32400…` and `408e054a…` still `status='pending'`, `payment_id` NULL, `payment_method` NULL | ✓ PASS — independently confirmed, not read from a SUMMARY table |
| Live probe default-skip behavior | `npx vitest run tests/paymongo/lapsed-hold-not-payable-real.test.ts` (no flag) | 1 skipped, no live call | ✓ PASS |
| Frozen files, byte-identical vs origin/dev | `git diff --stat origin/dev -- tests/paymongo/webhook-payment-paid.test.ts src/lib/payments/checkout-probe.ts tests/design/site-contacts.test.ts tests/design/pending-copy.test.ts tests/design/trust-signals.test.ts` | empty | ✓ PASS |
| Exactly one confirm write | `grep -rn "status = 'confirmed'" src/` | 1 write (`confirm-booking-payment.ts:303`), all other hits are read predicates or comments | ✓ PASS |
| Registration | `grep -n paymentReconcile\|checkoutRetireSweep src/app/api/inngest/route.ts` | both imported and in the `functions:` array | ✓ PASS |
| STATE.md / ROADMAP.md residue check | Read both files, `git log` on both | `STATE.md status: verifying`, correctly says "phase is ready for VERIFICATION... phase closure is the verifier's call, not a plan's." ROADMAP.md Phase 13.1 header carries no ✅/completion date. | ✓ CLEAN — no premature-completion residue found this time (unlike the earlier documented `gsd-sdk` over-reaches, which were caught and reverted by hand each time per the SUMMARYs) |

### Requirements Coverage

Not applicable. All 5 plans declare `requirements: []`, and the ROADMAP explicitly states: "Requirements:
TBD (no existing REQ-ID covers this — the milestone's requirements are front-end polish)." No orphaned
requirements found in REQUIREMENTS.md mapping to this phase.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX` markers found in the phase's modified files. No stub returns, no
hardcoded empty arrays feeding a render path, no placeholder copy. The one deliberately-unfixed defect
(`handleGoneSlot`'s alert non-idempotence on a `cancelled` row) is exhaustively documented at its assertion
site with a `RECORDED DEFECT` block and is proven unreachable in production today — this is the opposite of
an anti-pattern; it is honest, falsifiable disclosure.

### Human Verification Required

See the three items in the frontmatter `human_verification` block above (`cancelUnpaidHold` wiring
decision, runbook update, and live-paid-session shape capture). None of these are visual/UX checks — they
are product-risk and documentation-completeness decisions that a verifier cannot resolve unilaterally.

### Gaps Summary

No must-have truth failed. Every one of the 5 roadmap Success Criteria and the D-113 hold-expiry guarantee
is independently verified against the actual source, the actual test run (not the SUMMARY's reported
numbers — I ran `npm test` and `npm run test:design` myself and got identical counts), and the actual dev
database (queried directly via `docker exec`, not read from a table in a SUMMARY.md).

The phase's own plans found and disclosed real residuals rather than hiding them, and I independently
re-derived each one from the code rather than accepting the disclosure at face value:

1. **`cancelUnpaidHold` is a real, unwired, backstop-free session-orphaning path** — narrow (self-inflicted,
   requires the booker's own stale checkout link plus a simultaneously-lost webhook) but genuinely capable
   of taking money for a booking that no longer exists with zero reconciliation, which is precisely the
   failure mode this entire phase exists to close. It falls outside the literal wording of every stated
   success criterion (it is not a "lost webhook on a still-pending booking" and not a "hold that expires"),
   which is why it does not fail a formal must-have — but it is inside the phase's own broader promise.
2. **The operator runbook was not updated** with the two new alert action names this phase introduces, which
   weakens SC3's "loud" in practice even though the underlying alert mechanism is proven correct.
3. **A pre-existing deployment-documentation gap** (Inngest sync) now covers this phase's two new,
   guarantee-critical crons, and is still not recorded anywhere an operator doing a deploy would look.

None of these are coding defects hiding behind a passing test suite — they are scope-boundary and
documentation decisions the phase's own executors flagged, which I verified are real by reading the code
myself rather than trusting the flag. Routing to human decision per the escalation-gate pattern, as the
task's own framing directs ("Phase closure is your call").

---

_Verified: 2026-08-22_
_Verifier: Claude (gsd-verifier)_
