---
phase: 21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit
plan: 08
type: execute
wave: 5
depends_on:
  - 21-07
files_modified:
  - src/components/host/verification-roadmap.tsx
  - e2e/host-dashboard.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-21-1]
requirements:
  - HVER-09
  - HVER-10
  - HVER-14
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-21-1 / HVER-09 / HVER-10 per D-02 and D-03: every roadmap body and advancing CTA remains vertically inside its owning PanelCard at 320px and 1280px in court and grove, including unusually long rejection copy."
    - "The inner roadmap stack participates in intrinsic CardContent height; the outer list-item-to-PanelCard stretch chain, one-column/2×2 grid reflow, and equal desktop peer heights remain intact."
    - "Per D-05, the ready and mixed-portfolio states still replace the four-card roadmap with one compact receipt whose body remains inside its PanelCard."
    - "Existing min-w-0, break-words, overflow-wrap:anywhere, one-action ownership, 44px target, heading order, keyboard focus, and horizontal-overflow checks remain green."
    - "The browser regression reports a failure when a card's body or CTA crosses the card's top or bottom edge even when CSS overflow clipping would otherwise hide the defect."
  artifacts:
    - path: src/components/host/verification-roadmap.tsx
      provides: "Intrinsic-height roadmap content stack beneath the separately rendered PanelCard title."
      exports: [VerificationRoadmapProps, VerificationRoadmap]
    - path: e2e/host-dashboard.spec.ts
      provides: "Real-Chromium vertical containment assertions across the existing court/grove state matrix at both acceptance widths."
  key_links:
    - from: src/components/host/verification-roadmap.tsx
      to: src/components/patterns/panel-card.tsx
      via: "The child flex stack contributes intrinsic height below PanelCard's separate title sibling while the outer list item still stretches the card."
      pattern: "flex min-w-0 flex-col gap-4"
    - from: e2e/host-dashboard.spec.ts
      to: src/components/host/verification-roadmap.tsx
      via: "getBoundingClientRect and scrollHeight/clientHeight measurements on each rendered panel, its body copy, and its action descendants."
      pattern: "data-testid=\"panel-card\""
---

<objective>
Close diagnosed UAT gap G-21-1 by restoring intrinsic vertical sizing inside each verification-roadmap card and making Chromium prove that body copy and CTAs stay inside their cards.

Purpose: Remove the one redundant height claim that clips Card 1 while preserving the shipped roadmap authority, responsive grid, long-token wrapping, and equal desktop rows.
Output: One localized Tailwind-class correction and a state-complete, theme-complete, two-width vertical-containment regression in the existing host-dashboard E2E matrix.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-RESEARCH.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-05-SUMMARY.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-07-SUMMARY.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UAT.md
@.planning/debug/phase-21-roadmap-card-overflow.md
@node_modules/next/dist/docs/01-app/01-getting-started/11-css.md
@node_modules/next/dist/docs/01-app/02-guides/testing/playwright.md
@src/components/host/verification-roadmap.tsx
@src/components/patterns/panel-card.tsx
@src/components/ui/card.tsx
@e2e/host-dashboard.spec.ts
@playwright.config.ts

<interfaces>
- `VerificationRoadmap` is the existing Client Component whose non-ready branch renders one ordered list; each `li` stretches its `PanelCard`, while the roadmap child stack sits below PanelCard's separately rendered title inside CardContent.
- `PanelCard` renders title/description and children as sibling blocks in one `CardContent`; this plan consumes that contract and does not modify the shared pattern.
- `Card` intentionally applies `overflow-hidden`; the component must fit its content rather than neutralize clipping in the shared primitive.
- `assertRoadmapSnapshot(page, width, snapshot)` already drives zero-listing, waiting, stale, rejected-long-reason, and grandfathered snapshots for court and grove at `ROADMAP_WIDTHS = [320, 1280]`, while its surrounding test covers ready and mixed-portfolio receipt states.
- `assertNoHorizontalOverflow` and the existing desktop row-height assertions remain the horizontal/equal-height authorities; vertical containment supplements them rather than replacing them.
</interfaces>
</context>

<assumption_delta_decision>
  <primary_noun>roadmap card sizing model</primary_noun>
  <decision>no-change</decision>
  <reason>The one-variable Chromium counterfactual already proved that removing the inner height claim restores containment at both widths and themes while preserving equal desktop peers.</reason>
  <invariant>One ordered responsive grid and its outer card-stretch chain remain authoritative; only the inner stack's redundant full-height claim is removed.</invariant>
</assumption_delta_decision>

<root_cause>
Classification: execution miss. Plan 21-05 required long copy and controls to remain inside equal-height cards, but `verification-roadmap.tsx` left `h-full` on the child stack beneath PanelCard's separate title sibling, and its E2E helper measured only horizontal overflow, touch height, focus, and peer equality. The child therefore claims the full CardContent height in addition to the title and gap; the shared Card's intentional `overflow-hidden` clips the resulting overrun.
</root_cause>

<prohibitions>
- Do not change `src/components/patterns/panel-card.tsx`, `src/components/ui/card.tsx`, or the shared Card overflow policy.
- Do not remove the list item's `h-full`, its child-PanelCard stretch selector, `min-w-0`, `break-words`, or `[overflow-wrap:anywhere]`.
- Do not add fixed heights, minimum heights, maximum heights, viewport-specific duplicate markup, a second roadmap tree, or screenshot-only evidence.
- Do not change roadmap copy, state derivation, action ownership, payout behavior, ready-receipt authority, dashboard reads, routes, selectors, design tokens, dependencies, schema, or migrations.
- Do not weaken or replace the existing horizontal-overflow, 44px action, keyboard-focus, heading-order, roadmap/receipt XOR, or desktop peer-height assertions.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore one end-to-end roadmap card containment path</name>
  <read_first>
    - AGENTS.md
    - node_modules/next/dist/docs/01-app/01-getting-started/11-css.md
    - node_modules/next/dist/docs/01-app/02-guides/testing/playwright.md
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UAT.md
    - .planning/debug/phase-21-roadmap-card-overflow.md
    - src/components/host/verification-roadmap.tsx
    - src/components/patterns/panel-card.tsx
    - src/components/ui/card.tsx
    - e2e/host-dashboard.spec.ts
    - playwright.config.ts
  </read_first>
  <files>src/components/host/verification-roadmap.tsx, e2e/host-dashboard.spec.ts</files>
  <reversibility rating="reversible">The production edit removes one local utility class, and the browser helper is confined to the existing Phase 21 state matrix.</reversibility>
  <behavior>
    - "Current shipped roadmap plus a real court-theme browser snapshot -> the new geometry assertion fails because at least one body or CTA bottom exceeds its PanelCard bottom."
    - "The same snapshot after the inner height correction -> every measured body and action top/bottom falls within the owning card with at most 1px rendering tolerance."
    - "The corrected card has no vertical scroll-height overrun, while the existing one-column/2×2 reflow and equal 1280px peer heights remain unchanged."
  </behavior>
  <action>
First add a real-browser vertical-containment assertion to the existing roadmap snapshot path and run the focused court matrix before editing production. Measure the owning `[data-testid="panel-card"]` rectangle plus its body-copy and `a[href], button` descendant rectangles with `getBoundingClientRect`; also capture `clientHeight` and `scrollHeight` so the failure message names theme/state/width, card index, descendant kind, and the relevant numeric bounds. Treat more than 1px beyond either vertical card edge, or more than 1px of internal scroll-height overrun, as failure. Visibility alone is insufficient because the shared Card clips overflow.

After observing the assertion fail on the shipped class chain, remove only `h-full` from the inner roadmap content div in `VerificationRoadmap`. Preserve `flex min-w-0 flex-col gap-4`, the outer `li` height/stretch selector, long-token wrapping, `mt-auto` action region, and every shared primitive. Re-run the same focused court browser command and the roadmap component suite. Record the pre-fix failing geometry and post-fix passing command in the execution summary as G-21-1 acceptance evidence.
  </action>
  <acceptance_criteria>
    - "The browser assertion is demonstrated red against the diagnosed shipped class and green after only the inner height utility is removed."
    - "Every measured roadmap body and action in the focused court path remains inside its owning PanelCard within a 1px tolerance, and each measured card's scrollHeight is no more than 1px above clientHeight."
    - "The production diff preserves the outer card stretch chain, intrinsic wrapping classes, CTA push-down region, PanelCard, and shared Card implementation."
    - "Existing component assertions for one tree, 2×2/stack classes, heading order, one action, touch height, and receipt replacement remain green."
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/host-dashboard.spec.ts --project=chromium --grep "court · the roadmap and receipt state matrix" --reporter=list</automated>
    <fails_when>The command exits non-zero after the fix, collects no test, any body/action/card vertical measurement exceeds tolerance, or an existing responsive/focus/horizontal assertion regresses.</fails_when>
    <automated>npm.cmd test -- tests/host/verification-roadmap.test.tsx</automated>
    <fails_when>The command exits non-zero or reports zero passing tests.</fails_when>
  </verify>
  <done>One real court-theme path proves the diagnosed class removal restores visible body and CTA containment without altering roadmap semantics or desktop equalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock vertical containment across every theme, width, and roadmap state</name>
  <read_first>
    - src/components/host/verification-roadmap.tsx
    - e2e/host-dashboard.spec.ts
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UAT.md
    - .planning/debug/phase-21-roadmap-card-overflow.md
  </read_first>
  <files>e2e/host-dashboard.spec.ts</files>
  <reversibility rating="reversible">The expansion adds measurements to an existing isolated E2E fixture matrix and creates no production interface.</reversibility>
  <behavior>
    - "Court and grove × 320px and 1280px × zero-listing, waiting, stale, rejected-long-reason, and grandfathered -> all roadmap bodies and CTAs remain within their owning cards."
    - "Court and grove × 320px and 1280px × ready and mixed portfolio -> the compact receipt replaces the roadmap and every receipt body remains within its PanelCard."
    - "Every 1280px roadmap row retains equal peer heights; every tested viewport remains horizontally contained; actions remain 44px and keyboard-focusable."
    - "An assertion failure identifies the exact theme, snapshot, width, panel index, element type, and measured card/descendant bounds rather than returning an opaque boolean."
  </behavior>
  <action>
Refine the Task 1 measurement into one reusable helper scoped to a supplied roadmap or receipt section. For expanded roadmap snapshots, inspect every PanelCard and every body-copy and action descendant, not only the first card or active action. Invoke it from `assertRoadmapSnapshot`, which already covers both `ROADMAP_WIDTHS` for zero-listing, waiting, stale, long rejected, and grandfathered states inside both theme tests. Invoke the same helper in the ready and mixed-portfolio receipt loops so D-05 replacement states retain body containment too. Keep the existing fixtures and theme loop; do not duplicate database setup or create a second E2E spec.

Retain all existing horizontal-overflow, column-count, equal-desktop-row, state/action, touch-target, focus-ring, and roadmap/receipt exclusivity assertions. Run the exact two-test Chromium matrix selected by its shared title, then run focused component and lint checks. The final `21-08-SUMMARY.md` must record the selected test count, both themes, both widths, the covered snapshot names, the zero vertical-violation result, and confirmation that desktop peer equality still passes.
  </action>
  <acceptance_criteria>
    - "The browser matrix directly checks vertical bounds for every roadmap body and CTA at 320px and 1280px in court and grove, including the long unbroken rejection reason."
    - "Ready and mixed-portfolio receipt bodies are measured inside their PanelCards, and the four-card roadmap remains absent in both states per D-05."
    - "Existing equal desktop row, one-column/2×2, horizontal containment, 44px action, keyboard focus, exact state order, one advancing action, and owed-hook assertions all remain green."
    - "The final diff modifies only `verification-roadmap.tsx` and `host-dashboard.spec.ts`; PanelCard, Card, roadmap model, page reads, packages, lockfile, schema, and migrations remain unchanged."
    - "The execution summary contains reproducible RED/GREEN geometry and full-matrix acceptance evidence for G-21-1."
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/host-dashboard.spec.ts --project=chromium --grep "the roadmap and receipt state matrix reflows at 320px/1280px without overflow" --reporter=list</automated>
    <fails_when>The command exits non-zero, does not collect both court and grove tests, omits either acceptance width/state family, reports a vertical violation, or regresses an existing matrix assertion.</fails_when>
    <automated>npm.cmd test -- tests/host/verification-roadmap.test.tsx</automated>
    <fails_when>The command exits non-zero or reports zero passing tests.</fails_when>
    <automated>npm.cmd exec eslint -- src/components/host/verification-roadmap.tsx e2e/host-dashboard.spec.ts</automated>
    <fails_when>The command exits non-zero.</fails_when>
  </verify>
  <done>The complete existing court/grove roadmap and receipt matrix proves body and CTA containment at both acceptance widths while preserving every shipped responsive and interaction invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-readable server model → clipped browser card | Arbitrarily long stored reason/copy crosses into a fixed visual hierarchy and must remain readable and actionable without changing authority. |
| Browser geometry → acceptance evidence | Measurements must detect content outside an overflow-clipped card rather than certify only visibility or equal outer heights. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08-01 | Tampering | host-readable roadmap copy layout | medium | mitigate | Preserve React text rendering and long-token wrapping; measure every body/action rectangle against its owning card in both themes and widths. |
| T-21-08-02 | Denial of Service | advancing roadmap CTA | medium | mitigate | Remove the redundant inner height claim and require the CTA rectangle plus card scroll extent to remain inside the clipped PanelCard. |
| T-21-08-03 | Spoofing | browser acceptance signal | medium | mitigate | Keep existing action/state assertions and add numeric vertical bounds with contextual failure diagnostics; equal height or element visibility alone cannot pass the gate. |
| T-21-08-SC | Tampering | npm package supply chain | high | mitigate | Install no package and change no manifest or lockfile; use the checked-in Next.js, Tailwind, Vitest, and Playwright toolchain only. |
</threat_model>

<verification>
- Before the production edit, run the focused court Chromium command and retain the failing numeric geometry in the task record; a test that never demonstrates the diagnosed overrun is not a valid regression.
- After Task 1, run the focused court Chromium command and roadmap component suite; both must pass with the outer stretch and equal-height assertions intact.
- After Task 2, run the shared-title Chromium selection and require exactly the court and grove matrix tests to collect and pass across both widths and every existing snapshot family.
- Run focused ESLint and inspect `git diff -- src/components/host/verification-roadmap.tsx e2e/host-dashboard.spec.ts` to confirm the sole production change is the inner height utility and the remaining diff is browser evidence.
- Playwright's configured `reuseExistingServer: false` remains unchanged. If port 3000 is occupied, identify the owning process and stop only a confirmed stale FitOut development server; do not adopt an unknown listener or weaken the configuration to make the run green.
</verification>

<success_criteria>
- G-21-1 is closed with the diagnosed one-variable correction: no roadmap body or CTA crosses its card boundary at 320px or 1280px in court or grove.
- HVER-09/HVER-10 and D-02/D-03 remain intact: one responsive ordered tree, four separate cards, one genuine action, calm waiting, long-copy containment, and equal desktop rows.
- HVER-14's stale-pending `Finish the check` action remains reachable and contained without a second clock, route, or authority.
- D-05 ready and mixed-portfolio states still render only the compact receipt, whose body is also vertically contained.
- Shared Card/PanelCard primitives, state derivation, page reads, dependencies, schema, and migrations are unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID / item | Plan coverage | Status | Notes |
|--------|-----------|---------------|--------|-------|
| GOAL | Host reads standing as state and follows the one action that advances them | 21-01 through 21-07 retain the implemented outcome; 21-08 restores visibility/reachability of the action under the shipped card composition. | COVERED | No authority or workflow is broadened. |
| REQ | HVER-09 | 21-08 Task 1/2 | COVERED | The existing button-shaped state remains visible inside its card at both widths/themes. |
| REQ | HVER-10 | 21-08 Task 1/2 | COVERED | The ordered roadmap preserves one-column/2×2 reflow and gains direct vertical-containment evidence. |
| REQ | HVER-14 | 21-08 Task 2 | COVERED | The stale-pending action remains within its card in the full matrix. |
| REQ | HVER-11, HVER-12, HVER-13, LVER-06, LVER-07, LVER-08, LVER-09 | Existing plans 21-01 through 21-07 | COVERED | 21-08 leaves their server gates, copy authority, review history, deliberate edit path, and receipt authority unchanged. |
| RESEARCH | Presentation over existing authorities; zero query, dependency, schema, or submission changes | 21-08 prohibitions and both tasks | COVERED | The fix is local to the existing Client Component and E2E suite. |
| RESEARCH | Reuse Tailwind component utilities, PanelCard, and Playwright; preserve one CSS-reflowed tree | 21-08 Task 1/2 | COVERED | Shared primitives and responsive grid/stretch semantics remain unchanged. |
| CONTEXT | D-01, D-04 | Existing 21-01/21-05 coverage preserved | COVERED | Dashboard placement and first-bookable account completion do not change. |
| CONTEXT | D-02, D-03 | 21-08 Task 1/2 | COVERED | Four separate responsive cards, explicit order/state, and one real action remain intact and measurable. |
| CONTEXT | D-05 | 21-08 Task 2 | COVERED | Ready/mixed receipt replacement remains exclusive and gains body-bound evidence. |
| CONTEXT | D-06 through D-13 | Existing plans 21-02 through 21-07 | COVERED | Deliberate resubmit and review history are outside G-21-1 and remain untouched. |
| UAT | G-21-1 | 21-08 Task 1/2 | COVERED | Removes the confirmed inner height cause and adds the missing vertical browser gate for both themes and widths. |
| CONTEXT | Deferred appeals and reviewed unrelated todos | Excluded | EXCLUDED | Explicitly deferred or assigned outside Phase 21; this plan adds none of them. |

## Artifacts this phase produces

- Modified `src/components/host/verification-roadmap.tsx`: the inner roadmap stack uses intrinsic height while all outer stretch and wrapping classes remain.
- Modified `e2e/host-dashboard.spec.ts`: contextual vertical body/CTA/card-bound assertions across the existing state matrix.
- No shared primitive, page/query, authority, package, lockfile, schema, migration, route, or generated artifact.

<output>
Create `.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-08-SUMMARY.md` when done.
</output>
