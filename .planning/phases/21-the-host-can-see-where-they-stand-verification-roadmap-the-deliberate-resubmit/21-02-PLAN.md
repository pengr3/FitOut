---
phase: 21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/listing/review-history.ts
  - src/app/(host)/host/listings/page.tsx
  - src/components/listing/listing-card.tsx
  - tests/listing/review-history.test.ts
  - tests/listing/listing-card.test.tsx
  - e2e/host-listing-grid.spec.ts
autonomous: true
requirements: [LVER-07]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "LVER-07 adjacency edge: a normal cycle renders Submitted then Waiting and, only when decided, exactly one adjacent Approved/Not approved/Review ended terminal event; an open pending cycle stops at Waiting and a grandfathered row uses its separate capability sentence."
    - "LVER-07 empty edge: zero cycles means no trigger or dialog shell; nullable `decidedAt` and optional rejected reason omit only the unavailable fact, while an impossible required state/instant pairing fails server composition rather than guessing."
    - "LVER-07 ordering edge: SQL ranks each listing by `submitted_at DESC, id DESC`, reads at most six, serializes the first five newest-first, and uses row six only as the older-data sentinel."
    - "E3 / empty — A listing with zero review cycles renders no `Review history` trigger and no dead dialog shell."
    - "E3 / loading — History data resolves with server-rendered cards; opening the dialog performs no fetch and shows no loading state."
    - "E3 / error — History read failures route through the existing host boundary and never leak SQL, staff, vendor, or row details."
    - "E3 / populated — One to five newest review cycles render as semantic ordered records; a sixth row authorizes only the older-data sentinel."
    - "E3 / partial — Pending cycles may omit `decidedAt`, old rejected rows may omit a blank reason, and grandfathered rows use capability-only copy; no missing fact is guessed."
    - "E3 / zero-one-many — Zero cycles has no trigger, one renders singularly without invented plural copy, and many are server-bounded to five visible cycles plus one sentinel row."
    - { statement: "E3 / overflow — Browser evidence must show five review cycles scrolling inside the dynamic-height dialog while the page and listing card retain zero horizontal overflow.", verification: backstop }
    - { statement: "E3 / long-text — Browser evidence must show long titles and operator reasons as selectable wrapping text with no clamp, ellipsis, action displacement, or horizontal scroll.", verification: backstop }
  artifacts:
    - path: src/lib/listing/review-history.ts
      provides: "One owner-scoped bounded ranked read and host-safe display DTOs"
      exports: [ReviewCycleDisplay, ListingReviewHistory, loadReviewHistoryByListing]
    - path: tests/listing/review-history.test.ts
      provides: "Real-DB owner/deleted scope, deterministic ordering, cap/sentinel, lifecycle, and DTO-shape coverage"
    - path: src/components/listing/listing-card.tsx
      provides: "Conditional accessible Review history dialog using resolved serializable data"
  key_links:
    - from: src/app/(host)/host/listings/page.tsx
      to: src/lib/listing/review-history.ts
      via: "the existing grouped rejection query is replaced by one ranked owner/non-deleted-parent read"
      pattern: "loadReviewHistoryByListing"
    - from: src/app/(host)/host/listings/page.tsx
      to: src/components/listing/listing-card.tsx
      via: "one narrow `ListingReviewHistory` DTO per card; zero-cycle listings receive no dialog"
      pattern: "reviewHistory"
  prohibitions:
    - statement: "Review history must never expose the deciding staff identity, review/host/listing identifiers, or deleted-parent data to the host client."
      status: unverified
      flagged: true
    - statement: "Review history must never replace the operator's stored sentence with a canned reason or internal code."
      status: unverified
      flagged: true
---

<objective>
Give each owner-owned current listing a bounded, truthful review-cycle history beside the card per D-10 through D-13, without adding a query round trip or bringing staff/internal identifiers toward the client.

Purpose: a host can understand submitted → waiting → decided history without an infinite feed, a separate page, or a fetch-on-open state.
Output: one ranked server read, host-safe display DTOs, a conditional accessible dialog, and integration/component/browser coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-RESEARCH.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-PATTERNS.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VALIDATION.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md

<interfaces>
- `/host/listings` already performs one owner/non-deleted listing read followed by one grouped latest-rejection read; this plan replaces the second read rather than appending another.
- `ListingCardData` already carries the current `reviewState` and host-readable rejection reason; it receives only finished serializable additions.
- `listing_review` stores state/reason/submittedAt/decidedAt/decidedByStaffId, but the new projection selects only the host-approved fields plus an internal listing grouping key.
- Existing `Dialog` and `ConfirmDialog` composition in `listing-card.tsx` provides controlled open state, accessible title/description/close, focus trapping, and trigger restoration.
</interfaces>
</context>

## Artifacts this phase produces

- `src/lib/listing/review-history.ts`: `ReviewCycleDisplay`, `ListingReviewHistory`, and `loadReviewHistoryByListing()`.
- `ListingCardData.reviewHistory` as a narrow serializable optional field; internal card components `ReviewHistoryDialog` and `ReviewCycleRecord`.
- `tests/listing/review-history.test.ts`; focused additions to `tests/listing/listing-card.test.tsx` and `e2e/host-listing-grid.spec.ts`.
- No new page, route, script, CLI flag, schema field, migration, dependency, or query round trip.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver one real pending-to-decided history path from ranked SQL to the listing-card dialog</name>
  <reversibility rating="reversible">The query/DTO/dialog are internal read-only presentation and can be changed without migrating data.</reversibility>
  <files>tests/listing/review-history.test.ts, tests/listing/listing-card.test.tsx, src/lib/listing/review-history.ts, src/app/(host)/host/listings/page.tsx, src/components/listing/listing-card.tsx</files>
  <read_first>
    - AGENTS.md
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - src/app/(host)/host/listings/page.tsx
    - src/components/listing/listing-card.tsx
    - src/lib/db/schema.ts
    - src/lib/host/verification-signal.ts
    - src/lib/listing/review-signal.ts
    - tests/listing/material-edit.test.ts
    - tests/listing/listing-card.test.tsx
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-RESEARCH.md
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <behavior>
    - RED: a host with one owner-owned non-deleted listing and one pending/decided cycle receives one Review history control and opens a resolved semantic lifecycle without a fetch.
    - Another host's row and a deleted parent's row never enter the result map or serialized card props.
    - The SQL projection omits `decidedByStaffId`, review id, host id, and raw internal state/code from the client DTO.
  </behavior>
  <action>Write the real-DB and card tests first. Create `loadReviewHistoryByListing(dbConn, hostId)` with an inner PostgreSQL `row_number()` partition ordered by `submittedAt DESC, id DESC`, an outer rank `<= 6`, and the owner plus `listing.deletedAt IS NULL` constraints inside the joined SQL. Select only the internal grouping key plus state, reason, submittedAt, and decidedAt; map rows 1–5 into finished absolute `en-PH`/`Asia/Manila` display strings, row six into `hasOlder`, and the latest applicable reason into the existing card reason input. Replace—not accompany—the page's grouped rejection read and pass a plain serializable per-card DTO. In `ListingCard`, render no shell for zero cycles and otherwise open one controlled Dialog with the UI-SPEC title/description/Close, one outer ordered cycle list, and a nested Submitted → Waiting → terminal sequence. Render stored reasons as ordinary React text.</action>
  <verify>
    <automated>npm.cmd test -- tests/listing/review-history.test.ts tests/listing/listing-card.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - The public loader is owner-scoped and excludes deleted parents in SQL, not after serialization.
    - One grouped query serves the grid and returns no more than six rows per listing; no query exists inside `rows.map` or dialog open.
    - The client DTO contains no staff/internal ids/codes and all Date values are formatted server-side.
    - A pending cycle stops at Waiting; a decided cycle has exactly one terminal event and absolute decision time.
    - Rejection reason is selectable React text exactly once and an approval invents no reason.
  </acceptance_criteria>
  <done>A host can open one current listing's truthful review lifecycle from already-resolved server data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand history to deterministic five-cycle bounds, partial legacy data, and accessible responsive behavior</name>
  <reversibility rating="reversible">The cap, DTO variants, and dialog layout are local read/display choices over unchanged rows.</reversibility>
  <files>src/lib/listing/review-history.ts, src/components/listing/listing-card.tsx, tests/listing/review-history.test.ts, tests/listing/listing-card.test.tsx, e2e/host-listing-grid.spec.ts</files>
  <read_first>
    - src/lib/listing/review-history.ts
    - src/components/listing/listing-card.tsx
    - tests/listing/review-history.test.ts
    - tests/listing/listing-card.test.tsx
    - e2e/host-listing-grid.spec.ts
    - src/components/ui/dialog.tsx
    - src/components/ui/scroll-area.tsx
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <behavior>
    - Tied submitted instants remain deterministic by descending review id; five records render and a sixth authorizes only `Showing the latest five review cycles.`
    - Pending without decidedAt, rejected without a nonblank reason, approved, withdrawn, and grandfathered rows each follow their exact total lifecycle rule; impossible pairings fail the server read to the existing boundary.
    - Dialog focus starts on Close or the first cycle heading, remains trapped, returns to Review history on Escape/overlay/close, and long five-cycle content scrolls internally within 16px viewport insets.
  </behavior>
  <action>Per D-11, expand the tests to zero/one/five/six/more cycles, timestamp ties, all persisted review states, missing optional reason/decidedAt, malformed required combinations, and serialized key allowlisting. Implement a total state-to-display mapping: pending stops at Waiting, approved/rejected/withdrawn append one exact terminal label and absolute time, and grandfathered renders only `Listing was already live when reviews began.`. Per D-12, show the sentinel only when row six exists; never client-slice an unbounded collection. Refine the existing Dialog with `sm:max-w-lg`, `max-w-[calc(100%-2rem)]`, `max-h-[calc(100dvh-2rem)]`, internal vertical scrolling, visible Close, safe initial focus, trigger restoration, and plain wrapping selectable text. Extend browser coverage at 320px/1280px in court/grove for no-trigger, one, five-plus-sentinel, long title/reason, keyboard dismissal, and zero horizontal overflow.</action>
  <verify>
    <automated>npm.cmd test -- tests/listing/review-history.test.ts tests/listing/listing-card.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
    <automated>npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - All three LVER-07 edge-probe items and all six E3 explicit/two E3 backstop truths are directly asserted.
    - Server SQL—not client slicing—enforces five visible plus one sentinel and deterministic newest-first ties.
    - Zero cycles produces neither trigger nor dead dialog; partial optional history omits only the missing truth.
    - The dialog is usable at both acceptance widths/themes, keeps focus deterministic, scrolls internally, and introduces no pagination/load-more/ops link.
    - Reason text remains unescaped markup-free React text; staff and internal identifiers remain absent at the SQL and DTO boundaries.
  </acceptance_criteria>
  <done>LVER-07 is complete for empty, partial, bounded-many, tied-order, responsive, and accessible histories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Session/RSC → history SQL | The authenticated host identity constrains which parent listings and cycles may be read. |
| Database row → client DTO | Review data is minimized before serialization into the interactive listing card. |
| Operator reason → dialog DOM | Untrusted host-readable prose crosses into rendered/selectable text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-06 | Information Disclosure | `loadReviewHistoryByListing` IDOR/deleted rows | high | mitigate | Bind authenticated host id and `deletedAt IS NULL` to the parent join inside SQL; integration-test host A/B and deleted rows. |
| T-21-07 | Information Disclosure | history DTO/RSC payload | high | mitigate | Explicit projection/type allowlist omits staff, review, host, listing ids and raw codes before serialization. |
| T-21-08 | Tampering | operator reason rendering | medium | mitigate | Render reason only as React text and assert exact-once text with markup-like fixtures. |
| T-21-09 | Denial of Service | per-listing history | medium | mitigate | One ranked grouped query, rank <= 6 per parent, no query on dialog open or inside render loops. |
| T-21-02-SC | Tampering | package installs | high | mitigate | Use checked-in PostgreSQL/Drizzle/Dialog primitives and install no package. |
</threat_model>

<verification>
- Run both task-level focused commands and the browser journey.
- Run `npm.cmd run typecheck` and `npm.cmd run test:design`; non-zero exit or a zero-test selection blocks completion.
- Inspect the generated/client DTO type and SQL projection together: no staff/internal id field may exist in either.
</verification>

<success_criteria>
- LVER-07 is satisfied for every current owner-owned listing with one or more cycles.
- D-10 through D-13 hold exactly, including absolute time, five-plus-sentinel, and no deciding staff identity.
- The existing grouped query is replaced, not supplemented; schema and re-review mutation remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-02-SUMMARY.md` when done.
</output>
