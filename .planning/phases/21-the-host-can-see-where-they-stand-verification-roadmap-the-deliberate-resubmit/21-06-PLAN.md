---
phase: 21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit
plan: 06
type: execute
wave: 4
depends_on:
  - 21-04
files_modified:
  - src/lib/listing/review-history.ts
  - tests/listing/review-history.test.ts
  - tests/listing/listing-card.test.tsx
autonomous: true
gap_closure: true
requirements:
  - LVER-06
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-07 and LVER-06, the newest rejected review row is the sole authority for the current rejection explanation even when its reason is null or blank."
    - "An older non-empty rejection reason remains visible only in its historical cycle and never leaks into the current Fix and resubmit dialog."
    - "Per D-10, D-11, D-12, and D-13, the existing owner/deleted scope, newest-first deterministic order, five-cycle display bound plus sentinel, and host-safe DTO remain intact."
    - "Per D-08, src/lib/listing/re-review.ts remains byte-for-byte unchanged."
  artifacts:
    - path: src/lib/listing/review-history.ts
      provides: "A distinct observation sentinel for the newest rejected row, independent of nullable reason content."
    - path: tests/listing/review-history.test.ts
      provides: "Real-database regression coverage for newest null/blank rejection reasons followed by an older non-empty rejection."
    - path: tests/listing/listing-card.test.tsx
      provides: "Visible Fix and resubmit dialog coverage proving historical text is not promoted to current guidance."
  key_links:
    - from: src/lib/listing/review-history.ts
      to: src/app/(host)/host/listings/page.tsx
      via: "ListingReviewHistoryEntry.latestRejectionReason"
      pattern: "latestRejectionReason"
    - from: src/app/(host)/host/listings/page.tsx
      to: src/components/listing/listing-card.tsx
      via: "Nullable rejectionReason prop passed to ListingCard"
      pattern: "rejectionReason"
  assumptions:
    - requirement_id: LVER-06
      category: empty
      status: unresolved
      flagged: true
      statement: "The spec-less probe has no accepted empty-value resolution. This gap plan follows the binding verifier finding and D-07: a newest null/blank reason is a valid observed result and omits the optional current-reason line."
    - requirement_id: LVER-06
      category: encoding
      status: unresolved
      flagged: true
      statement: "The spec-less probe has no accepted encoding resolution. Preserve React text rendering and the host-safe DTO; do not add HTML interpretation or expose staff identifiers."
---

<objective>
Restore current-cycle fidelity for rejected listing guidance so the host sees the newest review outcome, including a deliberately absent reason, rather than an older rejection sentence.

Purpose: Close the sole failed Phase 21 requirement, LVER-06, without weakening the bounded history query or the deliberate-resubmit contract.
Output: Corrected rejection-selection state plus real-database and visible-dialog regressions.
</objective>

<execution_context>
@C:/Users/Admin/Roaming/FitOut/.Codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/Roaming/FitOut/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VERIFICATION.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-REVIEW.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/COVERAGE.md
@src/lib/listing/review-history.ts
@src/app/(host)/host/listings/page.tsx
@src/components/listing/listing-card.tsx
@tests/listing/review-history.test.ts
@tests/listing/listing-card.test.tsx

<interfaces>
- loadListingReviewHistory(db, userId) returns a host-safe record keyed by listing ID; each entry exposes cycles, hasMore, and latestRejectionReason as string or null.
- The SQL result is already owner-scoped, excludes deleted listings, ranks submittedAt descending with id descending as the tie-break, and fetches at most six rows per listing so the DTO can expose five plus hasMore.
- ListingCard accepts a nullable rejectionReason and existing reviewHistory; FixAndResubmitDialog renders the optional current reason only when rejectionReason.trim() is non-empty.
</interfaces>
</context>

<assumption_delta_decision>
  <primary_noun>current rejection explanation</primary_noun>
  <decision>no-change</decision>
  <reason>The verifier exposed an implementation sentinel collision, not a changed product rule or authority.</reason>
  <invariant>The newest rejected row remains authoritative; null or blank content stays null even when an older rejected row has non-empty text.</invariant>
</assumption_delta_decision>

<root_cause>
Classification: execution miss. The loader used nullable latestRejectionReason for both “no rejected row observed” and “newest rejected row observed with no reason,” so the newest-first scan could not preserve a valid empty result. The fix introduces independent observation state instead of treating the older sentence as a display-only symptom.
</root_cause>

<prohibitions>
- Do not edit src/lib/listing/re-review.ts or weaken the material-saved-change predicate.
- Do not add a query, widen row scope, expose staff data, expand the five-cycle display bound, or change the host-safe DTO.
- Do not infer current guidance from older reviewHistory cycles in the page or dialog.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve a newest blank rejection as the current-cycle result</name>
  <read_first>
    - src/lib/listing/review-history.ts
    - tests/listing/review-history.test.ts
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VERIFICATION.md
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
  </read_first>
  <files>src/lib/listing/review-history.ts, tests/listing/review-history.test.ts</files>
  <reversibility rating="reversible">The observation sentinel is private loader state behind the unchanged ListingReviewHistoryEntry DTO.</reversibility>
  <behavior>
    - "Case 1: older rejected reason 'Move the hero photo higher.' followed by the newest rejected row with reason null -> latestRejectionReason is null, newest cycle reason is null, and the older cycle retains its own sentence."
    - "Case 2: the same sequence with a newest whitespace-only reason -> latestRejectionReason is null and the older sentence remains historical only."
    - "Existing owner/deleted scoping, deterministic ordering, five-plus-sentinel bound, and malformed-row tolerance continue to pass."
  </behavior>
  <action>
Add the two-cycle regression to the real-database review-history suite first and run the targeted command to observe the pre-fix failure. In loadListingReviewHistory, separate “a rejected row has been observed for this listing” from the nullable latestRejectionReason content: maintain private per-listing observation state, mark the first rejected row as observed before normalizing its reason, and never allow a later row to become the current reason. Keep the public DTO unchanged. Preserve the existing single bounded SQL query, owner and deleted predicates, submittedAt/id ordering, five-cycle projection, hasMore sentinel, malformed-row tolerance, and per-cycle reason values per D-10, D-11, D-12, and D-13. Do not edit src/lib/listing/re-review.ts per D-08 and do not add schema or package changes.
  </action>
  <acceptance_criteria>
    - "A real database sequence containing an older non-empty rejection and a newest null rejection returns latestRejectionReason null."
    - "The whitespace variant produces the same current value while each historical cycle retains its own normalized reason."
    - "All existing review-history and listing-card tests pass without changing the query scope, ordering, bound, DTO shape, or resubmit helper."
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd test -- tests/listing/review-history.test.ts tests/listing/listing-card.test.tsx</automated>
    <fails_when>The command exits non-zero, the new null/blank cases report the older sentence as latestRejectionReason, or an existing scope/order/bound assertion fails.</fails_when>
    <automated>git diff --exit-code da2232c4 -- src/lib/listing/re-review.ts</automated>
    <fails_when>The command exits non-zero or prints a diff for the frozen deliberate-resubmit helper.</fails_when>
  </verify>
  <done>The loader distinguishes rejected-row observation from nullable reason content, the real-database regression passes, and all protected history and resubmit invariants remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the visible Fix and resubmit dialog never promotes historical text</name>
  <read_first>
    - src/components/listing/listing-card.tsx
    - tests/listing/listing-card.test.tsx
    - src/app/(host)/host/listings/page.tsx
    - src/lib/listing/review-history.ts
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <files>tests/listing/listing-card.test.tsx</files>
  <reversibility rating="reversible">This adds regression coverage around the existing nullable prop and dialog contract without changing a public interface.</reversibility>
  <behavior>
    - "Rejected published listing + latest rejection reason null + history containing an older non-empty reason -> opening Fix and resubmit shows the confirmation checklist and omits the older reason."
    - "Rejected published listing + whitespace-only current reason -> the dialog omits the optional reason line and remains actionable."
    - "The existing non-empty current-reason case still renders that sentence exactly once."
  </behavior>
  <action>
Extend the ListingCard test fixture with a newest missing-reason cycle and an older rejected cycle carrying a unique non-empty sentence. Render the rejected published card with null, and separately whitespace-only, rejectionReason values; click the visible “Fix and resubmit” action; scope assertions to the opened dialog; and prove the older sentence is absent while the D-07 tuple checklist and Continue action remain present. Retain the existing positive assertion for a non-empty current reason. Do not teach the component to search reviewHistory for guidance: the nullable value supplied by the server loader is the only current-reason input. Keep the listings page as the existing Server Component and the dialog as its existing Client Component, passing only serializable nullable text across that boundary. Preserve the deliberate material-edit gate and no-unchanged-submit behavior per D-06 and D-08.
  </action>
  <acceptance_criteria>
    - "The Fix and resubmit dialog opens for newest null and whitespace reasons without showing the older historical sentence."
    - "The confirmation checklist and Continue action remain available when the optional reason is absent."
    - "The non-empty current-reason path still renders its sentence exactly once."
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd test -- tests/listing/review-history.test.ts tests/listing/listing-card.test.tsx</automated>
    <fails_when>The command exits non-zero, the historical sentence appears in the scoped Fix and resubmit dialog, the dialog loses its required checklist/action, or the positive current-reason case regresses.</fails_when>
    <automated>git diff --exit-code da2232c4 -- src/lib/listing/re-review.ts</automated>
    <fails_when>The command exits non-zero or prints a diff for the frozen deliberate-resubmit helper.</fails_when>
  </verify>
  <done>The visible host recovery path treats a newest blank reason as valid absence and never substitutes an older rejection sentence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database review rows -> host-safe history DTO | Stored staff-authored review data is reduced to host-visible state, timestamps, and optional reason text. |
| Server Component -> ListingCard dialog | Nullable current guidance crosses the server/client serialization boundary and becomes visible host text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06-01 | Tampering | loadListingReviewHistory current-cycle selection | high | mitigate | Track first rejected-row observation independently from reason content and lock the newest-null/older-text ordering with a real-database regression. |
| T-21-06-02 | Elevation of Privilege | review-history SQL scope | high | mitigate | Preserve the existing owner predicate and deleted-listing exclusion; rerun the suite's cross-owner and deleted-listing assertions. |
| T-21-06-03 | Information Disclosure | ListingReviewHistoryEntry and dialog text | medium | mitigate | Keep the host-safe DTO shape, omit staff IDs/internal notes, and render the optional reason as React text only. |
| T-21-06-04 | Denial of Service | per-listing history read | medium | mitigate | Preserve the single windowed query and six-row fetch bound rather than adding per-listing or unbounded fallback reads. |
| T-21-06-SC | Tampering | npm package supply chain | high | mitigate | This plan permits no package-manager install, dependency, or lockfile change; use the repository's existing test toolchain only. |
</threat_model>

<verification>
Run the inherited Phase 21 listing-history/card command and the frozen-helper diff gate. Treat the existing browser backstops in 21-VERIFICATION.md as still requiring browser evidence; these source and jsdom regressions do not silently convert them into completed human verification.
</verification>

<success_criteria>
- LVER-06 no longer fails when the newest rejected row has a null or blank reason and an older rejection has text.
- The older sentence remains attached to its historical cycle but is absent from the current Fix and resubmit dialog.
- The owner/deleted scope, deterministic newest-first bound, host-safe DTO, and deliberate-resubmit semantics have no regression.
</success_criteria>

## Flagged Assumptions

- LVER-06 empty and encoding probes remain unresolved because there is no plain Phase SPEC. The executor follows the binding CONTEXT.md, UI-SPEC.md, and VERIFICATION.md contracts stated above without claiming those probes were independently resolved.
- HVER-11/HVER-12 adjacency, empty, and ordering probes and HVER-13 idempotency/concurrency probes remain verification backstops outside this concrete LVER-06 gap. This plan does not alter their shipped behavior or recast them as closed.

## API Coverage

- The existing `COVERAGE.md` conclusion remains unchanged: this phase composes internal authorities and has no external API integration. The history-loader correction adds no endpoint, external call, or contract surface.

## Artifacts this phase produces

- Modified `src/lib/listing/review-history.ts`: private rejected-row observation state; no new export or DTO field.
- Modified `tests/listing/review-history.test.ts`: newest-null/blank versus older-non-empty real-database regression.
- Modified `tests/listing/listing-card.test.tsx`: visible Fix and resubmit dialog regression.
- No schema, migration, API integration, dependency, or generated artifact.

<output>
Create `.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-06-SUMMARY.md` when done.
</output>
