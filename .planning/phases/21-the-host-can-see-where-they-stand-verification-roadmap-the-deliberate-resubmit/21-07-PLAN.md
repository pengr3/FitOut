---
phase: 21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit
plan: 07
type: execute
wave: 4
depends_on:
  - 21-04
files_modified:
  - src/components/host/verification-roadmap.tsx
  - tests/host/verification-roadmap.test.tsx
autonomous: true
gap_closure: true
requirements:
  - HVER-10
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "HVER-10 payout onboarding failures remain inside the roadmap: both a resolved refusal and a rejected promise produce visible, retryable inline feedback."
    - "A rejected payout promise does not create an unhandled rejection or navigate the host away from the dashboard."
    - "A host can retry after a thrown failure; the stale error clears while the retry is pending and the new authoritative result replaces it."
    - "The existing payout Server Action remains the authority for eligibility and authorization, and navigation occurs only for an explicit success result."
  artifacts:
    - path: src/components/host/verification-roadmap.tsx
      provides: "A caught rejected-promise path using the roadmap's existing inline error surface."
    - path: tests/host/verification-roadmap.test.tsx
      provides: "Regression coverage for thrown payout actions, resolved refusals, and a subsequent retry."
  key_links:
    - from: src/components/host/verification-roadmap.tsx
      to: src/app/actions/paymongo-connect.ts
      via: "startPayoutOnboarding() invoked inside beginPayoutOnboarding"
      pattern: "startPayoutOnboarding"
    - from: src/components/host/verification-roadmap.tsx
      to: "window.location.href"
      via: "Navigation guarded by result.ok"
      pattern: "result.ok"
  assumptions:
    - requirement_id: HVER-10
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The spec-less probe contains no accepted classification for HVER-10. This plan closes only WR-01's concrete rejected-promise path and preserves the shipped payout authority and roadmap presentation."
---

<objective>
Keep payout-onboarding failures visible and recoverable inside the host verification roadmap when the Server Action promise rejects unexpectedly.

Purpose: Close the concrete non-blocking WR-01 finding without redesigning payout eligibility, authorization, or the roadmap.
Output: A guarded async client transition and focused rejection/retry regression coverage.
</objective>

<execution_context>
@C:/Users/Admin/Roaming/FitOut/.Codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/Roaming/FitOut/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VERIFICATION.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-REVIEW.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/COVERAGE.md
@src/components/host/verification-roadmap.tsx
@src/app/actions/paymongo-connect.ts
@tests/host/verification-roadmap.test.tsx
@tests/host/verification-roadmap-state.test.ts

<interfaces>
- VerificationRoadmap is the existing Client Component that receives serializable verification, payout, and listing state from the host dashboard Server Component.
- startPayoutOnboarding is an existing independently authorized Server Action that resolves to either an explicit success carrying a destination URL or a refusal carrying user-visible error text; the client must also tolerate a rejected promise.
- beginPayoutOnboarding currently owns the pending transition, inline error state, action invocation, and success-only browser navigation.
</interfaces>
</context>

<assumption_delta_decision>
  <primary_noun>payout onboarding result authority</primary_noun>
  <decision>no-change</decision>
  <reason>WR-01 identifies a missing client failure boundary, not a change to payout eligibility or authorization.</reason>
  <invariant>Only an explicit successful Server Action result may navigate; refusals and thrown failures stay inline and retryable.</invariant>
</assumption_delta_decision>

<root_cause>
Classification: execution miss. The Client Component handled the Server Action's resolved result variants but had no rejection boundary around the awaited promise, allowing a transport/runtime failure to bypass the shipped inline recovery surface.
</root_cause>

<prohibitions>
- Do not move payout authorization, eligibility, or provider orchestration into the Client Component.
- Do not navigate for thrown values or resolved refusal variants.
- Do not add a second payout control, toast-only failure channel, external API integration, dependency, or schema change.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Convert a rejected payout promise into inline recovery</name>
  <read_first>
    - src/components/host/verification-roadmap.tsx
    - src/app/actions/paymongo-connect.ts
    - tests/host/verification-roadmap.test.tsx
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-REVIEW.md
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VERIFICATION.md
  </read_first>
  <files>src/components/host/verification-roadmap.tsx, tests/host/verification-roadmap.test.tsx</files>
  <reversibility rating="reversible">The catch boundary is local to the existing client transition and leaves the Server Action contract unchanged.</reversibility>
  <behavior>
    - "Eligible host clicks the Step 2 payout action and startPayoutOnboarding rejects -> the roadmap shows 'We couldn't start payout setup. Please try again.' inline."
    - "The rejection does not assign window.location.href and does not escape as an unhandled promise rejection."
    - "The payout action remains visible after the failure so the host can retry."
  </behavior>
  <action>
Write the rejected-promise component test first by mocking startPayoutOnboarding to reject, rendering the existing payout-eligible roadmap state, clicking the Step 2 action, and asserting the deterministic fallback appears in the established inline error region with no navigation. Then wrap the awaited Server Action call inside beginPayoutOnboarding's existing transition with a local try/catch. Preserve the explicit success branch and the resolved `{ ok: false, error }` branch; only the thrown/rejected branch uses the exact fallback “We couldn't start payout setup. Please try again.” Keep setError(null) before each attempt so recovery state is fresh. Do not move authorization or eligibility into this Client Component: the existing Server Action must continue to validate them independently under the current Next Server Action security convention. Do not add a new toast, route, package, API, or payout state authority.
  </action>
  <acceptance_criteria>
    - "A mocked rejected payout promise renders the exact inline fallback and the test observes no browser navigation."
    - "The rejected promise is handled inside the transition and the initiating action remains available for retry."
    - "Explicit success and resolved refusal behavior remain unchanged."
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd test -- tests/host/verification-roadmap.test.tsx tests/host/verification-roadmap-state.test.ts</automated>
    <fails_when>The command exits non-zero, the rejection escapes the component, the exact fallback is missing, navigation occurs on failure, or an existing roadmap-state assertion regresses.</fails_when>
  </verify>
  <done>The host receives bounded inline feedback instead of an unhandled payout-onboarding rejection and can initiate another attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the retry transition against stale payout errors</name>
  <read_first>
    - src/components/host/verification-roadmap.tsx
    - tests/host/verification-roadmap.test.tsx
    - src/app/actions/paymongo-connect.ts
  </read_first>
  <files>tests/host/verification-roadmap.test.tsx</files>
  <reversibility rating="reversible">This records the existing retry state contract in component tests without changing a shared interface.</reversibility>
  <behavior>
    - "First payout attempt rejects -> fallback appears; second attempt begins -> stale fallback clears while the promise is pending."
    - "Second attempt resolves with `{ ok: false, error: 'Payout setup is unavailable.' }` -> that authoritative refusal replaces the fallback and no navigation occurs."
    - "Exactly one payout action invocation occurs per click."
  </behavior>
  <action>
Add a focused two-attempt test using a controllable second promise: reject the first invocation, assert the fallback, click the still-available action again, assert the stale fallback clears during the pending transition, then resolve the second invocation with an explicit refusal and assert that refusal becomes the sole inline message. Verify one Server Action invocation per click and no navigation on either failure. Reuse the production behavior introduced in Task 1; do not add parallel local eligibility state or duplicate payout controls. Keep this test in the existing roadmap component suite so it exercises the visible action and inline error surface rather than only a helper.
  </action>
  <acceptance_criteria>
    - "A host can retry after a rejected promise without retaining the stale fallback during the new attempt."
    - "The second resolved refusal replaces the fallback, remains inline, and does not navigate."
    - "The action mock is called exactly twice for two user clicks."
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd test -- tests/host/verification-roadmap.test.tsx tests/host/verification-roadmap-state.test.ts</automated>
    <fails_when>The command exits non-zero, the stale fallback remains during retry, the returned refusal is not shown, a failure navigates, or invocation count differs from one call per click.</fails_when>
  </verify>
  <done>The payout action's thrown and resolved-failure paths share a visible retry contract with no stale error or duplicate invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client roadmap -> payout Server Action | A browser interaction requests an externally linked onboarding session; client eligibility is not trusted. |
| Server Action result -> browser navigation/error state | The client interprets success, refusal, or transport/runtime rejection and either navigates or presents feedback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07-01 | Elevation of Privilege | payout onboarding authorization | high | mitigate | Leave authorization and eligibility in startPayoutOnboarding; the client change handles presentation only and must not create a second authority. |
| T-21-07-02 | Spoofing | success-only redirect branch | high | mitigate | Navigate only when the existing Server Action returns its explicit ok success variant; thrown values and refusal variants can only update inline error state. |
| T-21-07-03 | Denial of Service | rejected promise in startTransition | medium | mitigate | Catch the rejection inside the transition, render a bounded fallback, and retain a single retry control covered by the two-attempt regression. |
| T-21-07-04 | Tampering | stale client failure state | medium | mitigate | Clear the previous error at attempt start and replace it only with the current attempt's authoritative refusal or fallback. |
| T-21-07-SC | Tampering | npm package supply chain | high | mitigate | This plan permits no package-manager install, dependency, or lockfile change; use the repository's existing test toolchain only. |
</threat_model>

<verification>
Run the inherited Phase 21 verification-roadmap command. The behavior-unverified repeated-navigation, concurrent retry-winner, and read-failure-boundary items remain verifier work because they are not gaps in 21-VERIFICATION.md; this plan closes only WR-01 and must not claim those backstops are complete.
</verification>

<success_criteria>
- HVER-10's visible payout action handles both resolved refusals and rejected promises inline.
- No payout failure navigates or escapes as an unhandled rejection, and retry remains available.
- The Server Action remains the sole eligibility/authorization authority and existing roadmap state behavior passes unchanged.
</success_criteria>

## Flagged Assumptions

- HVER-10's spec-less edge probe remains unclassified. The concrete WR-01 contract is sourced from 21-REVIEW.md and 21-VERIFICATION.md; this plan does not fabricate a broader HVER-10 classification.
- The remaining HVER-09, HVER-14, LVER-07, and LVER-08 unclassified probes and HVER-11/HVER-12/HVER-13 adjacency, empty, ordering, idempotency, and concurrency probes stay flagged at their existing verification status unless separately evidenced.

## API Coverage

- The existing `COVERAGE.md` conclusion remains unchanged. `startPayoutOnboarding` is an internal Server Action already in the phase's shipped authority graph; catching its rejected promise does not add an external API integration or a second payout authority.

## Multi-Source Coverage Audit

| Source | Item | Coverage disposition |
|--------|------|----------------------|
| GOAL | Host can see verification, payout, and listing standing and deliberately recover a rejected listing | Existing executed plans 21-01 through 21-05 deliver the phase outcome; 21-06 repairs current rejection fidelity and 21-07 keeps payout initiation failures inside the roadmap. |
| REQ | HVER-09, HVER-11, HVER-12, HVER-13, HVER-14, LVER-07, LVER-08, LVER-09 | Covered by executed plans 21-01 through 21-05; gap plans preserve their predicates and do not claim unresolved browser or spec-less probes are newly resolved. |
| REQ | HVER-10 | Existing payout-roadmap behavior is preserved; 21-07 closes the concrete rejected-promise recovery warning. |
| REQ | LVER-06 | 21-06 closes the verifier's newest-blank-versus-older-text failure through the real DB and visible dialog. |
| RESEARCH | Shipped authorities, bounded history read, host-safe DTO, Next server/client boundary, and no new packages | 21-06 keeps the SQL/DTO bounds and server-to-client nullable text contract; 21-07 keeps payout authority in the Server Action; both plans prohibit installs. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Covered by executed plans 21-01 and 21-02; gap plans leave the roadmap hierarchy and authority model intact. |
| CONTEXT | D-06, D-07, D-08 | Covered by executed plans 21-03 and 21-04; 21-06 repairs D-07 current-reason fidelity and explicitly preserves D-06/D-08 deliberate-resubmit behavior. |
| CONTEXT | D-09, D-10, D-11, D-12, D-13 | Covered by executed plans 21-03 through 21-05; 21-06 preserves the bounded, deterministic, host-safe history contract. |
| VERIFICATION | Sole failed truth and WR-01 | 21-06 closes the failed LVER-06 truth; 21-07 closes the report's concrete non-blocking promise rejection. |
| VERIFICATION | Behavior-unverified items and eight browser backstops | Retained as verifier/human evidence work, not converted into source-only completion claims because the report does not classify them as implementation gaps. |
| REVIEW | UI-review typography, spacing, and in-flight-label advisories | Excluded from this gap plan set: they are advisory findings outside the verifier's actionable gap and WR-01 closure. |
| CONTEXT | Deferred appeals, alternate resubmission routes, staff exposure, and unrelated redesign | Excluded exactly as deferred/out-of-scope ideas; no gap plan implements them. |

## Artifacts this phase produces

- Modified `src/components/host/verification-roadmap.tsx`: local rejected-promise failure boundary; no new export.
- Modified `tests/host/verification-roadmap.test.tsx`: rejected-promise and retry regression coverage.
- No schema, migration, API integration, dependency, or generated artifact.

<output>
Create `.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-07-SUMMARY.md` when done.
</output>
