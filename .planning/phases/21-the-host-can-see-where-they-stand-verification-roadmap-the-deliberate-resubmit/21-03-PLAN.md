---
phase: 21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit
plan: 03
type: execute
wave: 2
depends_on: [21-02]
files_modified:
  - src/lib/listing/re-review-copy.ts
  - src/components/listing/listing-card.tsx
  - src/app/(host)/host/listings/[id]/edit/page.tsx
  - src/app/(host)/host/listings/[id]/edit/wizard.tsx
  - tests/listing/material-edit.test.ts
  - tests/listing/listing-card.test.tsx
  - tests/listing/wizard-save-state.test.tsx
  - e2e/host-listing-grid.spec.ts
autonomous: true
requirements: [LVER-06, LVER-08]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-06, only an exactly rejected listing replaces the ordinary Edit action with `Fix and resubmit`; all other review/listing states retain Edit and the first press opens an explanation without navigation or mutation."
    - "Per D-07/D-08, both pre-edit dialog and persistent rejected notice derive all seven labels from `MATERIAL_FIELDS` through one total `MATERIAL_FIELD_LABELS` record; no second material-field list or unchanged-listing submit exists."
    - "E2 / empty — The material-change collection is total over the seven-entry `MATERIAL_FIELDS` tuple and therefore never renders as an empty explanatory list."
    - "E2 / loading — Opening `Fix and resubmit` performs no fetch or mutation and shows no intermediate spinner; resolved serializable data is already present on the card."
    - "E2 / error — Safe dismissal and `Continue to edit` do not mutate review state; a navigation failure cannot display or fabricate a receipt."
    - "E2 / populated — The dialog renders exactly seven tuple-backed material labels, the stored reason when present, one safe close action, and one forward edit action."
    - "E2 / partial — A missing optional rejection reason omits only that reason line; the seven material labels and edit route remain complete."
    - "E2 / zero-one-many — This collection is fixed at seven labels; zero or variable-length material-field variants are prohibited by the total tuple-backed map."
    - "E4 / empty — Outside authoritative rejected context, neither the rejected notice nor the `Changes received` receipt renders."
    - "E4 / partial — A missing optional rejection reason omits that line while the rejected-state notice, tuple-backed material rule, and guarded receipt authority remain intact."
    - { statement: "E2 / overflow — Browser evidence must show the rejected-listing footer wrapping and the dialog remaining within 16px viewport insets with internal vertical scrolling and no page overflow.", verification: backstop }
    - { statement: "E2 / long-text — Browser evidence must show long listing titles, stored reasons, and material labels wrapping without clipping the safe or forward actions.", verification: backstop }
  artifacts:
    - path: src/lib/listing/re-review-copy.ts
      provides: "One tuple-backed host copy/serializable context authority"
      exports: [MATERIAL_FIELD_LABELS, composeMaterialChangeRule, RejectedListingContext]
    - path: src/components/listing/listing-card.tsx
      provides: "Rejected-only Fix and resubmit dialog and existing edit fallback"
    - path: src/app/(host)/host/listings/[id]/edit/wizard.tsx
      provides: "Persistent server-derived rejected notice across wizard sections"
  key_links:
    - from: src/components/listing/listing-card.tsx
      to: src/lib/listing/re-review.ts
      via: "`MATERIAL_FIELDS.map(field => MATERIAL_FIELD_LABELS[field])` through the new copy module; the mechanism file is import-only and byte-identical"
      pattern: "MATERIAL_FIELDS"
    - from: src/app/(host)/host/listings/[id]/edit/page.tsx
      to: src/app/(host)/host/listings/[id]/edit/wizard.tsx
      via: "owner/deleted-bound server query supplies a narrow persisted `RejectedListingContext`; search params supply no authority"
      pattern: "reReviewContext"
  prohibitions:
    - statement: "Fix and resubmit must never silently become a submit-without-edit, Resubmit now, appeal, dispute, or contest control."
      status: unverified
      flagged: true
    - statement: "No Phase 21 explanation may append a support, contact, or get-in-touch clause while the monitored support address is absent."
      status: unverified
      flagged: true
    - statement: "Explaining material edits must never soften the shipped sell gate or introduce a material-but-still-sellable state."
      status: unverified
      flagged: true
  assumptions:
    - requirement_id: LVER-06
      category: unclassified
      status: unresolved
      flagged: true
      probe: "unclassified — review manually"
      statement: "LVER-06 has no plain SPEC edge classification; rejected-only replacement, explanation-before-navigation, and zero first-press mutation are the explicit assumption boundary."
    - requirement_id: LVER-08
      category: unclassified
      status: unresolved
      flagged: true
      probe: "unclassified — review manually"
      statement: "LVER-08 has no plain SPEC edge classification; tuple equality, exact seven-label order, and persistent direct-entry notice are the explicit assumption boundary."
---

<objective>
Make rejected-listing resubmission a deliberate route into editing per D-06 through D-08: explain the seven material changes before navigation and preserve the authoritative current reason/rule throughout direct entry and refresh.

Purpose: the host understands what will requeue a listing without gaining an unchanged-listing submit or appeal mechanism.
Output: one tuple-backed copy authority, a rejected-only card dialog, a server-derived wizard notice, and component/browser proof.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-CONTEXT.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-RESEARCH.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-PATTERNS.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-VALIDATION.md
@node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
@node_modules/next/dist/docs/01-app/02-guides/forms.md

<interfaces>
- `MATERIAL_FIELDS` and `MaterialField` are exported from byte-frozen `src/lib/listing/re-review.ts`; this plan consumes them without editing that file.
- After Plan 21-02, `ListingCardData` has current `reviewState`, current host-readable rejection reason, and history; the card remains the sole call site for both dialogs.
- `ListingWizard` receives finished server props from its owner/deleted-guarded edit page and keeps client state across wizard sections.
- `Dialog` primitives already provide controlled open state, overlay/Escape dismissal, accessible title/description, close, focus trap, and trigger restoration.
</interfaces>
</context>

## Artifacts this phase produces

- `src/lib/listing/re-review-copy.ts`: `MATERIAL_FIELD_LABELS`, `composeMaterialChangeRule()`, and `RejectedListingContext`.
- `ListingCardData`/`ListingCard` additions for rejected-only `FixAndResubmitDialog`; no new route or mutation.
- `ListingWizard` prop `reReviewContext` plus internal `RejectedListingNotice` presentation.
- Focused additions to `tests/listing/material-edit.test.ts`, `tests/listing/listing-card.test.tsx`, `tests/listing/wizard-save-state.test.tsx`, and `e2e/host-listing-grid.spec.ts`.
- No script, CLI flag, schema field, migration, package, query round trip, standalone resubmit action, or appeal route.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace rejected Edit with a no-mutation explanation dialog and tuple-backed forward route</name>
  <reversibility rating="reversible">The rejected-only control and shared copy module are local UI contracts over the unchanged edit route and mutation.</reversibility>
  <files>src/lib/listing/re-review-copy.ts, src/components/listing/listing-card.tsx, tests/listing/listing-card.test.tsx, tests/listing/material-edit.test.ts</files>
  <read_first>
    - src/lib/listing/re-review.ts
    - src/components/listing/listing-card.tsx
    - src/lib/listing/review-history.ts
    - tests/listing/listing-card.test.tsx
    - tests/listing/material-edit.test.ts
    - src/components/ui/dialog.tsx
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <behavior>
    - Exactly rejected replaces Edit with neutral `Fix and resubmit`; draft, pending, approved, grandfathered, withdrawn, unlisted, and review-null states retain Edit.
    - First press opens a controlled dialog, preserves row state, performs no navigation/fetch/mutation, and places focus on `Keep reviewing changes`.
    - Dialog emits exactly the seven UI-SPEC labels in `MATERIAL_FIELDS` order from a total keyed record, optional current reason once, one safe action, and one `Continue to edit` link.
  </behavior>
  <action>Write rejected/non-rejected interaction tests and a tuple-key equality test first. Create `src/lib/listing/re-review-copy.ts` with a `Record<MaterialField, string>` containing the exact seven UI-SPEC labels, `composeMaterialChangeRule()` derived from `MATERIAL_FIELDS.map(...)`, and the narrow `RejectedListingContext` type; do not add another field array or edit `re-review.ts`. In `ListingCard`, replace only the rejected direct Edit link with a neutral `size="sm"` `PencilIcon` trigger for `FixAndResubmitDialog`. Use the existing controlled Radix pattern, exact title fallback/description/material heading, semantic `<ul>`, optional reason React text exactly once, `Keep reviewing changes` as safe initial-focus close, and neutral Next link `Continue to edit` to the existing `editHref`. Escape, overlay, close icon, and safe button must restore trigger focus and cause no mutation.</action>
  <verify>
    <automated>npm.cmd test -- tests/listing/material-edit.test.ts tests/listing/listing-card.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `Object.keys(MATERIAL_FIELD_LABELS)` and `MATERIAL_FIELDS` match exactly and order comes from mapping the tuple.
    - Only current `rejected` state replaces Edit; all named non-rejected/null states retain the shipped affordance.
    - Opening/dismissing the dialog changes no row, calls no action/fetch, and restores focus.
    - Continue to edit is the sole forward control and is a normal Next link to the owner-scoped wizard.
    - Existing Delete/Unlist dialogs and footer `mt-auto flex-wrap` behavior are unchanged.
  </acceptance_criteria>
  <done>A rejected host can deliberately choose and understand the edit route before leaving the card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive persistent rejected context on the server and keep its notice through the wizard</name>
  <reversibility rating="reversible">The added serializable context prop and notice are internal presentation and do not alter review rows or URLs.</reversibility>
  <files>src/lib/listing/re-review-copy.ts, src/app/(host)/host/listings/[id]/edit/page.tsx, src/app/(host)/host/listings/[id]/edit/wizard.tsx, tests/listing/wizard-save-state.test.tsx, tests/listing/material-edit.test.ts</files>
  <read_first>
    - AGENTS.md
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - node_modules/next/dist/docs/01-app/02-guides/forms.md
    - src/app/(host)/host/listings/[id]/edit/page.tsx
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx
    - src/lib/listing/re-review-copy.ts
    - src/lib/db/schema.ts
    - tests/listing/wizard-save-state.test.tsx
    - tests/listing/material-edit.test.ts
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <behavior>
    - A current rejected owner listing renders `This listing needs changes`, the exact lead/current stored reason when present, and tuple-derived material rule on direct route entry and refresh.
    - A query string cannot create rejected context; non-rejected listings show no notice even with forged parameters.
    - Missing optional reason omits only its line; the notice and seven-field rule persist while moving through wizard sections and failed saves preserve them.
  </behavior>
  <action>Write direct-entry, forged-query, missing-reason, section-navigation, and save-refusal tests first. Strengthen the edit page's existing listing predicate to include both `listing.hostId === session.user.id` and `listing.deletedAt IS NULL` in SQL, and reshape that same query/selection to obtain current review state and latest current rejection reason without another round trip. Return `notFound()` when inaccessible. Pass only the narrow serializable `RejectedListingContext` to `ListingWizard`; ignore search params for authority. Render a flat neutral `RejectedListingNotice` above editable content, using the exact heading/lead, stored reason as text, and `composeMaterialChangeRule()` from the tuple-backed map. Keep it mounted across all sections, with no action of its own and no destructive treatment.</action>
  <verify>
    <automated>npm.cmd test -- tests/listing/wizard-save-state.test.tsx tests/listing/material-edit.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - Ownership and non-deleted status are SQL predicates on the edit page before data crosses to the wizard.
    - Current rejected state—not URL input—controls the notice on direct entry and refresh.
    - Current reason is plain text exactly once when nonblank; absent reason leaves every other explanation intact.
    - Notice remains visible across section changes and action errors, and it exposes no staff/internal ids or codes.
    - The existing page read is reshaped; no additional query round trip is introduced.
  </acceptance_criteria>
  <done>The explanation cannot be bypassed by direct URL/refresh and cannot be forged by a query string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the deliberate entry and persistent notice at both widths and themes</name>
  <reversibility rating="reversible">Responsive/focus refinements and browser scenarios are local and do not modify persisted review semantics.</reversibility>
  <files>src/components/listing/listing-card.tsx, src/app/(host)/host/listings/[id]/edit/wizard.tsx, tests/listing/listing-card.test.tsx, tests/listing/wizard-save-state.test.tsx, e2e/host-listing-grid.spec.ts</files>
  <read_first>
    - src/components/listing/listing-card.tsx
    - src/app/(host)/host/listings/[id]/edit/wizard.tsx
    - tests/listing/listing-card.test.tsx
    - tests/listing/wizard-save-state.test.tsx
    - e2e/host-listing-grid.spec.ts
    - tests/design/one-tree.test.ts
    - src/lib/design/live-regions.ts
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-UI-SPEC.md
  </read_first>
  <behavior>
    - Footer controls remain 28px `size="sm"`, wrap rather than clip/shrink, and the fix dialog stays within 16px viewport insets with internal scrolling.
    - At 320px/1280px in court/grove, long title/reason/labels wrap; safe dismissal and forward navigation preserve deterministic focus and cause no review mutation.
    - Direct wizard entry shows persisted explanation without a dialog/query token and introduces no standalone submission/support path.
  </behavior>
  <action>Extend component/browser tests for all seven non-rejected alternatives, untitled fallback, missing/long reason, long label/title, footer wrapping, dialog focus/dismissal/restoration, first-press row immutability, direct/refresh wizard entry, and a forged query parameter. Refine only the two owned surfaces using `sm:max-w-md`, vendored mobile inset, dynamic viewport cap, internal scrolling, semantic list/text, and existing token/typography roles. Preserve one DOM tree, visible labels, decorative-icon hiding, existing Delete/Unlist dialogs, no new live region, no page horizontal overflow, and exact UI-SPEC copy.</action>
  <verify>
    <automated>npm.cmd test -- tests/listing/listing-card.test.tsx tests/listing/wizard-save-state.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
    <automated>npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - All six E2 truths, both E2 backstops, and assigned E4 empty/partial truths have direct component/browser assertions.
    - Dialog initial focus, all safe dismissals, forward link, and trigger restoration are deterministic and mutation-free.
    - Long explanatory text remains selectable and wrapping; controls remain visible and the viewport does not horizontally scroll.
    - No support/contact clause, unchanged-listing submission, appeal/dispute/contest wording, or softened sell state appears.
    - LVER-06 and LVER-08 unclassified probe items remain explicit flagged assumptions.
  </acceptance_criteria>
  <done>The deliberate entry and persistent warning are understandable, accessible, and truthful across direct/browser paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Listing-card props → navigation dialog | Persisted review state/reason determines whether the host sees the deliberate entry. |
| Edit route/session → listing query | A route id must not disclose or edit another host's/deleted listing. |
| Server-derived context → client wizard | Only narrow current state/reason may authorize the persistent notice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-10 | Spoofing | rejected notice/query parameters | high | mitigate | Derive context only from owner/deleted-bound SQL and test forged/direct/refreshed URLs. |
| T-21-11 | Elevation of Privilege | edit-page listing id | high | mitigate | Bind session user id and `deletedAt IS NULL` inside the existing query; use `notFound()` for inaccessible rows. |
| T-21-12 | Tampering | material-field explanation | medium | mitigate | Total `Record<MaterialField,string>`, render with `MATERIAL_FIELDS.map`, and assert exact tuple/key equality. |
| T-21-13 | Information Disclosure | stored reason/client props | medium | mitigate | Pass only current host-readable reason/state, render as React text, and omit staff/internal fields. |
| T-21-03-SC | Tampering | package installs | high | mitigate | Compose existing Dialog/Card primitives and install no package or registry block. |
</threat_model>

<verification>
- Run all task-level focused suites and the listing-grid Playwright journey.
- Run `git diff --exit-code -- src/lib/listing/re-review.ts` and block on any output/non-zero exit.
- Run `npm.cmd run typecheck` and `npm.cmd run test:design`; any non-zero exit or zero-test selection blocks completion.
</verification>

<success_criteria>
- LVER-06 and LVER-08 are complete exactly as D-06 through D-08 define them.
- The explanation appears before editing and persists from authoritative state after navigation, direct entry, and refresh.
- `src/lib/listing/re-review.ts` is byte-identical; no new mutation, query, schema, package, support, or appeal surface exists.
</success_criteria>

<output>
Create `.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-03-SUMMARY.md` when done.
</output>
