---
phase: 22-ops-decides-with-the-whole-picture-queue-removal-enforcement
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/ops/ops-queue-row.tsx
  - tests/ops/ops-queue-row.test.tsx
  - tests/design/ops-host-invariants.test.ts
  - e2e/ops-queue.spec.ts
autonomous: true
requirements: [OPS-13, OPS-14, OPS-15]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Listing evidence is one conditional section in the existing terminal row, with the single decision widget visibly before it."
    - "Both row kinds remain terminal in collapsed and expanded states, and listing expansion preserves focus and all existing photo/contact accessibility behavior."
    - "At 320px and 1280px in Court and Grove, long evidence wraps without horizontal overflow and the decision controls stay within the viewport after expansion."
  artifacts:
    - path: src/components/ops/ops-queue-row.tsx
      provides: Contract-complete listing evidence layout using existing semantic design primitives.
    - path: tests/ops/ops-queue-row.test.tsx
      provides: jsdom coverage for disclosure state, semantic evidence, terminality, total standing, and retained focus.
    - path: tests/design/ops-host-invariants.test.ts
      provides: Source-level guardrails for staff-bound ops composition and existing action-census behavior.
    - path: e2e/ops-queue.spec.ts
      provides: Court/Grove browser geometry and terminality acceptance matrix at both required widths.
  key_links:
    - from: src/components/ops/ops-queue-row.tsx
      to: tests/ops/ops-queue-row.test.tsx
      via: Named native disclosure and one labelled evidence section tested through real DOM interaction.
      pattern: "Show listing evidence|aria-expanded"
    - from: src/components/ops/ops-queue-row.tsx
      to: e2e/ops-queue.spec.ts
      via: Authenticated Chromium assertions use the same local ops queue implementation at Court/Grove and 320px/1280px.
      pattern: "ops.localhost"
    - from: src/components/ops/ops-queue-row.tsx
      to: tests/design/ops-host-invariants.test.ts
      via: Existing protected ops-action and host-routing invariants remain intact while the UI composes the existing widget once.
      pattern: "requireStaff|OpsDecisionActions"
---

<objective>
Complete the approved inspection contract across all semantic, accessibility, partial-data, terminality, and responsive states after the tracer has proven the architecture.

Purpose: Make the decision boundary safe and legible for every listed evidence state without broadening the ops surface.
Output: Component and design-invariant coverage plus a two-theme/two-viewport browser acceptance matrix.
</objective>

## Phase Goal

**As a** signed-in FitOut staff reviewer, **I want to** inspect a host listing submission, **so that** I can decide its action.

## Artifacts this phase produces

- Contract-complete semantics and accessibility for listing evidence in `OpsQueueRow`.
- A focused design guard for unchanged staff/action authority boundaries.
- Browser evidence covering Court and Grove at 320px and 1280px.

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-RESEARCH.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-VALIDATION.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-PATTERNS.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-01-SUMMARY.md
@.planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-08-SUMMARY.md

<interfaces>
- The client entry remains `OpsQueueRow({ row: OpsQueueRowItem })`; listing rows narrow to `OpsQueueListingRow`, host rows remain `OpsQueueHostRow`.
- `HOST_VERIFICATION_LABEL` is a total `Record<HostVerificationStatus, string>` and the UI-SPEC fixes its six visible phrases.
- `RowCard` stays terminal whenever no navigation prop is supplied; `PhotoGallery`, `OpsContactReveal`, and `OpsDecisionActions` keep their existing accessibility contracts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the exact disclosure semantics, fallbacks, terminality, and one-widget boundary</name>
  <files>src/components/ops/ops-queue-row.tsx, tests/ops/ops-queue-row.test.tsx, tests/design/ops-host-invariants.test.ts</files>
  <read_first>
    - AGENTS.md and node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md — confirm the existing client entry and serializable-prop contract before changing its stateful markup.
    - .planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md — binding order, exact copy, semantic token, fallback, accessibility, and non-navigation rules.
    - src/components/ops/ops-queue-row.tsx, tests/ops/ops-queue-row.test.tsx, and tests/design/ops-host-invariants.test.ts — retain the established fixture, render helper, terminality selectors, and authority/action census.
    - src/components/listing/photo-gallery.tsx, src/components/ops/ops-decision-actions.tsx, src/components/ops/ops-contact-reveal.tsx, src/components/ui/button.tsx, src/components/ui/badge.tsx, and src/lib/listing-vocab.ts — preserve existing interaction primitives rather than reproducing them.
  </read_first>
  <behavior>
    - A listing begins collapsed; activating its named native button retains focus, changes `aria-expanded`, and reveals exactly one evidence section in the specified visual and semantic order.
    - Description wraps as prose, amenities are a semantic list of neutral noninteractive badges, null values render `Not set`, unknown amenity keys remain readable through their raw fallback, and an empty photo collection retains `PhotoGallery`'s `No photos yet` state.
    - Listing collapsed, listing expanded, and unmodified host rows each contain no anchors or link-role elements; only listing rows have the disclosure and listing evidence.
    - The listing has exactly one approve/reject widget outside evidence, and existing contact-reveal, named refusal-status, and queue-zero-state behavior remain intact.
  </behavior>
  <action>Extend the current component tests before finalizing the row markup. Use the existing `listingRow` factory and `renderRow` helper to assert the collapsed state, button type, accessible text, stable focus, `aria-controls`, one conditional section, exact evidence order, a single `PhotoGallery`, its no-photo state, readable description classes, amenity list semantics, known and raw-fallback amenity labels, and both `Not set` cases. Retain a complete status-union matrix and explicitly exercise the selected fail-closed standing. Run the current terminality DOM assertions for listing collapsed, listing expanded, and host rows; prove the host branch has neither disclosure nor listing evidence. Assert one existing decision widget before the evidence boundary, plus unchanged named refusal and contact-reveal contracts. Adjust `OpsQueueRow` only to satisfy these observable requirements: use semantic tokens and existing `Button`/`Badge` primitives, preserve the 16px decision-to-evidence separation, keep controls before evidence in DOM order, keep one responsive tree, and maintain every established host action. Extend the existing design invariant suite with source-level checks that this composition did not weaken staff/page and protected-action coverage and that the existing positive queue-zero copy remains unchanged. Do not create a detail destination, an additional decision control, a dialog, a data-fetching state, an evidence card, a sticky control, a package change, or a migration.</action>
  <verify>
    <automated>npm.cmd test -- tests/ops/ops-queue-row.test.tsx &amp;&amp; npm.cmd run test:design -- tests/design/ops-host-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>Every component and design assertion passes: evidence is accessible and complete when expanded, all partial states and the photo-zero state remain named, the existing queue-zero copy and refusal status remain intact, one decision widget is structurally separate, the activated disclosure retains focus, and both surviving row kinds remain terminal.</acceptance_criteria>
  <done>The evidence layout satisfies the approved UI-SPEC with the existing component tree and protected action surfaces preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove responsive decision visibility and terminality through the Court/Grove browser matrix</name>
  <precondition>`DATABASE_URL` targets a local database host, and the focused `e2e/ops-queue.spec.ts` test asserts that fact before seeding staff, listing, photo, amenity, and verification rows.</precondition>
  <files>src/components/ops/ops-queue-row.tsx, e2e/ops-queue.spec.ts</files>
  <read_first>
    - e2e/ops-queue.spec.ts from Plan 22-01, e2e/ops-auth.spec.ts, e2e/helpers/theme.ts, and playwright.config.ts — extend the same protected local-browser harness and existing Court/Grove theme mechanism.
    - .planning/phases/21-the-host-can-see-where-they-stand-verification-roadmap-the-deliberate-resubmit/21-08-SUMMARY.md — reuse scoped numeric rectangle plus scroll-extent diagnostics for responsive containment rather than visibility-only assertions.
    - .planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md and src/components/ops/ops-queue-row.tsx — exact viewport, wrap, non-sticky, and terminal row criteria.
  </read_first>
  <behavior>
    - The authenticated inspection test runs a Court/Grove matrix at 320px and 1280px with long description and many amenity values.
    - At every matrix point, the existing decision widget and both of its touch-sized controls remain in the viewport after expansion, while evidence remains below the controls and no document scroll width exceeds its client width.
    - Browser DOM checks preserve terminality before and after expansion and find neither a navigation change nor a second decision widget.
  </behavior>
  <action>Expand the focused authenticated browser spec into a state matrix that selects Court and Grove through the existing theme helper before loading `/ops`, then runs at 320px and 1280px against a seeded listing with long wrapped description and enough amenities to exercise the collection. After expansion, measure the listing's scoped controls and evidence using rectangle and scroll-extent diagnostics with theme/viewport context; the approve/reject widget and its controls must remain within the viewport, the evidence must follow it, and the page must have no horizontal overflow. Assert terminal markup in the real browser before and after the disclosure, unchanged `/ops` URL, one native disclosure/evidence section, one decision widget, and the existing lightbox/contact controls. If the matrix exposes a layout defect, correct only `OpsQueueRow` with the existing semantic design tokens and intrinsic wrapping layout; retain the non-sticky control strategy and all prior component guarantees. Keep test seeding local-only, clean up deterministically, and leave Playwright configuration, dependencies, database schema, and other ops routes untouched.</action>
  <verify>
    <automated>npx playwright test e2e/ops-queue.spec.ts --project=chromium --reporter=list</automated>
  </verify>
  <acceptance_criteria>The focused Chromium run passes Court and Grove at both required widths, reporting no horizontal overflow or vertical control loss; the operator can expand evidence, see the singular decision widget, and remain in the terminal queue row in every matrix case.</acceptance_criteria>
  <done>Responsive and terminality acceptance coverage proves OPS-13, OPS-14, and OPS-15 against the real staff route with no routing, action, package, schema, or migration expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → local disclosure | A privileged user activates client-only state beside protected decision controls. |
| row evidence → existing interaction primitives | Existing gallery, contact reveal, and decision controls are composed in a more detailed terminal row. |
| browser matrix → local seeded database | Browser tests create realistic staff/listing data and must not target non-local infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Elevation | `OpsQueueRow` control composition | high | mitigate | Task 1 keeps the existing protected `OpsDecisionActions` exactly once and extends the source-level action/authority invariant suite. |
| T-22-06 | Information Disclosure | expanded listing evidence | high | mitigate | Task 1 limits evidence to the approved listing DTO and keeps host-row/document-shaped evidence absent; component tests cover each row kind. |
| T-22-07 | Spoofing | `e2e/ops-queue.spec.ts` | medium | mitigate | Task 2 signs in through the existing configured ops authority and preserves the local-target assertion before database seeding. |
| T-22-08 | Tampering | row interaction markup | medium | mitigate | Task 1 uses native controls and tests stable focus, terminal markup, and unchanged protected interaction contracts rather than custom click shims. |
</threat_model>

<verification>
- `npm.cmd test -- tests/ops/ops-queue-row.test.tsx`
- `npm.cmd run test:design -- tests/design/ops-host-invariants.test.ts`
- `npx playwright test e2e/ops-queue.spec.ts --project=chromium --reporter=list`
</verification>

<success_criteria>
- The full approved evidence order, fallback copy, and semantic structure hold in jsdom and the browser.
- Both queue-row kinds remain terminal before and after listing expansion.
- Court/Grove at 320px and 1280px prove controls remain in view and long evidence remains contained.
</success_criteria>

## Source coverage audit

| Source | ID | Required outcome | Plan | Status |
|--------|----|------------------|------|--------|
| GOAL | Phase 22 | Signed-in staff inspects and decides from an existing queue row | 22-01, 22-02 | COVERED |
| REQ | OPS-13 | Complete facts behind one in-place disclosure; terminal listing and host rows | 22-01, 22-02 | COVERED |
| REQ | OPS-14 | One visible decision widget separated from evidence at both acceptance widths | 22-01, 22-02 | COVERED |
| REQ | OPS-15 | Fail-closed host standing is a visible listing fact | 22-01, 22-02 | COVERED |
| RESEARCH | explicit DTO, description/amenities, deterministic aggregates, and no document projection | 22-01 Task 1–2 | COVERED |
| RESEARCH | grouped impact read and pure final page map | 22-01 Task 1–2 | COVERED |
| RESEARCH | reuse client row, PhotoGallery, AMENITY_LABELS, existing decisions, and native disclosure | 22-01 Task 1; 22-02 Task 1 | COVERED |
| RESEARCH | retain page staff guard and existing protected actions | 22-01 Task 1; 22-02 Task 1 | COVERED |
| RESEARCH | local-only authenticated Chromium tracer with responsive, terminality, and focus coverage | 22-01 Task 1; 22-02 Task 2 | COVERED |
| UI-SPEC | semantic tokens, exact copy/fallbacks, one tree, one evidence section, Court/Grove responsive states | 22-01 Task 1; 22-02 Task 1–2 | COVERED |
| CONTEXT | none | No CONTEXT.md exists; the approved UI-SPEC and research act as the binding design record | 22-01, 22-02 | COVERED |

<output>
Create `.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-02-SUMMARY.md` when done.
</output>
