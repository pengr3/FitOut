---
phase: 22-ops-decides-with-the-whole-picture-queue-removal-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ops/review-queue.ts
  - src/lib/ops/cancel-impact.ts
  - src/app/(ops)/ops/page.tsx
  - src/components/ops/ops-queue-row.tsx
  - tests/ops/queue-query.test.ts
  - tests/ops/ops-queue-row.test.tsx
  - e2e/ops-queue.spec.ts
autonomous: true
requirements: [OPS-13, OPS-14, OPS-15]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A signed-in staff reviewer can expand one pending listing in the existing /ops queue and inspect its server-loaded evidence without leaving the row."
    - "The listing read model includes nullable description, deterministically ordered amenity keys, ordered photos, and a fail-closed host-verification standing while the host DTO remains free of identity-document data."
    - "The existing approve/reject widget stays singular and all decision-impact values remain server-computed while row shaping reads an already-built impact map."
  artifacts:
    - path: src/lib/ops/review-queue.ts
      provides: Explicit typed listing evidence projection with deterministic amenity normalization.
    - path: src/lib/ops/cancel-impact.ts
      provides: Server-only set-based impact loader returning an OpsCancelImpact map.
    - path: src/app/(ops)/ops/page.tsx
      provides: Staff-gated parallel loading and synchronous queue-row shaping.
    - path: src/components/ops/ops-queue-row.tsx
      provides: Native one-level listing evidence disclosure inside the terminal RowCard tree.
    - path: e2e/ops-queue.spec.ts
      provides: Authenticated browser tracer for staff inspection on the configured ops authority.
  key_links:
    - from: src/lib/ops/review-queue.ts
      to: src/components/ops/ops-queue-row.tsx
      via: OpsQueueListingItem serializable description, amenities, photos, and hostVerificationStatus props.
      pattern: "description|amenities|hostVerificationStatus"
    - from: src/lib/ops/cancel-impact.ts
      to: src/app/(ops)/ops/page.tsx
      via: loadOpsCancelImpacts runs before the final items.map and supplies each listing impact.
      pattern: "loadOpsCancelImpacts"
    - from: src/app/(ops)/ops/page.tsx
      to: src/components/ops/ops-queue-row.tsx
      via: Staff-gated server page passes finished OpsQueueRowItem values to the existing client island.
      pattern: "<OpsQueueRow"
---

<objective>
Deliver the production-quality end-to-end inspection path: a staff member opens a pending listing in the existing queue and reads the evidence needed to decide without a route change or secondary data request.

Purpose: Establish the real server-query-to-terminal-row architecture before expanding its edge-state and responsive coverage.
Output: Typed queue evidence, a set-based impact map, a native disclosure in the existing row, focused integration/component tests, and an authenticated Chromium tracer.
</objective>

## Phase Goal

**As a** signed-in FitOut staff reviewer, **I want to** inspect a host listing submission, **so that** I can decide its action.

## Artifacts this phase produces

- An explicit listing evidence projection and grouped cancellation-impact reader.
- An in-place native disclosure in the existing `OpsQueueRow` client island.
- `e2e/ops-queue.spec.ts`, which seeds and inspects a real pending listing through `ops.localhost`.

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-RESEARCH.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-PATTERNS.md
@.planning/phases/20-ops-gets-its-own-front-door-the-ops-host-sign-in-staff-onboa/20-14-SUMMARY.md

<interfaces>
- `OpsQueueListingItem` in `src/lib/ops/review-queue.ts` is the server-owned serializable DTO; its `hostVerificationStatus` is the existing `HostVerificationStatus` union, and its `photos` are ordered `OpsQueuePhoto[]`.
- `OpsCancelImpact` in `src/lib/ops/cancel-impact.ts` contains finished money strings, booking IDs, and the fixed payout-left reason; `opsRefundBasisCents` remains its single monetary-policy expression.
- `OpsQueueListingRow` in `src/components/ops/ops-queue-row.tsx` composes `OpsQueueListingItem`, finished labels, and required `impact`; `OpsQueueRow` is already the one `use client` boundary.
- `OpsDecisionActions` is the existing protected approve/reject widget; `PhotoGallery` owns ordered-photo rendering, zero-photo state, and its lightbox; `AMENITY_LABELS` is the listing-vocabulary authority.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one authenticated pending listing from explicit query evidence to an in-place disclosure</name>
  <precondition>`DATABASE_URL` targets a local database host and the existing schema through `drizzle/0029_listing_review_cascade.sql` is available; the new browser spec must reject a non-local target before it seeds.</precondition>
  <reversibility rating="costly">Changing the grouped impact reader touches the page-to-row financial-display contract, so reversing it requires coordinated service, page, and equivalence-test changes.</reversibility>
  <files>src/lib/ops/review-queue.ts, src/lib/ops/cancel-impact.ts, src/app/(ops)/ops/page.tsx, src/components/ops/ops-queue-row.tsx, e2e/ops-queue.spec.ts</files>
  <read_first>
    - AGENTS.md and node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md — read before modifying the Server/Client component boundary.
    - .planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md — binding evidence order, copy, terminality, focus, and responsive contract.
    - src/lib/ops/review-queue.ts, src/lib/ops/cancel-impact.ts, src/app/(ops)/ops/page.tsx, and src/components/ops/ops-queue-row.tsx — existing DTO, fail-closed standing, money policy, staff gate, and RowCard composition.
    - src/components/ops/ops-decision-actions.tsx, src/components/listing/photo-gallery.tsx, src/lib/listing-vocab.ts, and src/components/patterns/row-card.tsx — reuse boundaries for controls, gallery, vocabulary, and terminal markup.
    - e2e/ops-auth.spec.ts and playwright.config.ts — local-target assertion, configured ops host/origin, existing authentication endpoint, and browser runner contract.
  </read_first>
  <behavior>
    - First add a focused browser test that seeds one staff account plus one complete pending listing with ordered photos, a description, amenities, and an approved host-verification row; it signs in at the configured ops origin and fails until the evidence is reachable through the disclosure.
    - A click on `Show listing evidence` changes the same native button to `Hide listing evidence`, sets `aria-expanded`, mounts one labelled evidence section, keeps the URL at `/ops`, and leaves the existing approve/reject controls available.
    - Queue-row props cross from the server as serializable values; local disclosure state is the sole client-side behavior and does not invoke a route, request, action, or browser-side money calculation.
  </behavior>
  <action>Implement the established Phase-22 architecture. Extend `OpsQueueListingItem` and its query-row type with `description: string | null` plus `amenities: string[]`; select `listing.description` explicitly and aggregate `listing_amenity` through a deterministic ordered lateral JSON aggregate, then normalize the null aggregate to `[]`. Preserve the explicit-column contract, the existing fail-closed `COALESCE(hv.status::text, 'unverified')`, ordered photo aggregate, and the host DTO's absence of document or identifier fields. Add and export `loadOpsCancelImpacts(dbConn, listingIds): Promise<Map<string, OpsCancelImpact>>` in `cancel-impact.ts`; it must use the current booking/payout predicates, `opsRefundBasisCents`, `PAYOUT_ALREADY_LEFT_REASON`, and finished display strings for each known ID. Keep `loadOpsCancelImpact` as the established single-item contract for equivalence coverage. In `OpsQueuePage`, keep `await requireStaff()` as the first statement, launch the grouped impact reader in the existing pre-map `Promise.all`, and make the final `items.map` a synchronous formatter that reads its required impact from the completed map. In the existing `OpsQueueRow` client island, render the one exact `OpsDecisionActions` invocation in a listing-only control zone directly beneath the header, then a native `Button` disclosure and one conditional evidence section below it. Keep host rows on their existing rendering path. The section order is `PhotoGallery` once, readable description, semantic amenity list, Address, Space type, Capacity, Price, Host standing, Submitted, then the existing contact-reveal facts. Reuse `AMENITY_LABELS` with raw unknown-key fallback, use `Not set` for absent description/amenities, preserve finished page labels and money strings, and retain the `RowCard` without a navigation prop. Add the browser tracer following `e2e/ops-auth.spec.ts`: assert the local database before seed, authenticate with the real ops sign-in endpoint, exercise the disclosure, assert its first full evidence path and unchanged URL, and clean up all seeded rows.</action>
  <verify>
    <automated>npx playwright test e2e/ops-queue.spec.ts --project=chromium --reporter=list</automated>
    <fails_when>The command exits non-zero or Playwright reporter output names a failed ops-queue test because staff sign-in, evidence disclosure, URL preservation, or the one-widget assertion did not hold.</fails_when>
  </verify>
  <acceptance_criteria>One seeded staff reviewer can sign in at the configured ops authority, expand the pending listing without URL navigation, read one mounted evidence section with photos, description, amenities, facts, host standing, and contact facts, and still locate the one existing decision widget.</acceptance_criteria>
  <done>The authenticated tracer is committed, the queue data arrives in the existing client row without a second fetch, and the page builds every listing impact from the grouped server result before synchronous row shaping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove evidence projection, fail-closed standing, and grouped-impact equivalence</name>
  <files>src/lib/ops/review-queue.ts, src/lib/ops/cancel-impact.ts, src/app/(ops)/ops/page.tsx, tests/ops/queue-query.test.ts, tests/ops/ops-queue-row.test.tsx</files>
  <read_first>
    - tests/ops/queue-query.test.ts and tests/ops/ops-queue-row.test.tsx — extend their existing isolated database fixtures and complete row factories rather than introducing a second harness.
    - .planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-PATTERNS.md — query DTO, batch reader, fixture, and terminality analogs.
    - src/lib/ops/review-queue.ts, src/lib/ops/cancel-impact.ts, and src/app/(ops)/ops/page.tsx — compare grouped results against the current single-item financial contract and preserve the page guard/map boundary.
    - src/components/ops/ops-queue-row.tsx and src/lib/listing-vocab.ts — total standing labels and vocabulary fallback contract.
  </read_first>
  <behavior>
    - Query fixtures assert nullable description, deterministic amenity ordering, zero amenities, ordered photos, and the no-verification-row path that resolves to the existing unverified standing.
    - For equivalent isolated listing data, each `loadOpsCancelImpacts` map entry equals `loadOpsCancelImpact` for counts, booking IDs, monetary strings, host-paid text, and payout-left reason.
    - The complete row fixture covers every `HostVerificationStatus` phrase and proves the host-standing fact never renders blank.
  </behavior>
  <action>Extend the established database and component suites before settling remaining implementation details. In `queue-query.test.ts`, assert returned DTO values rather than SQL implementation internals: description, ordered amenity keys, empty amenity normalization, existing ordered photos, and fail-closed standing when the joined verification row is absent. Add the set-based/single-item impact equivalence matrix using the existing isolated `testDb` seed style; compare every `OpsCancelImpact` field so batching cannot alter booking eligibility, frozen-money output, or the non-cancellable explanation. Update `OpsQueuePage` and the readers only where these assertions expose a contract mismatch; keep all money arithmetic server-side, preserve the first `requireStaff()` call, and retain a map lookup in the final synchronous row construction. Extend the row factory with the new required evidence fields and assert that every existing status union member renders its mapped phrase in the Host fact, including the absent-verification fallback. Do not add a document-shaped field, a host-row disclosure, an action, a dependency, a migration, or a schema edit.</action>
  <verify>
    <automated>npm.cmd test -- tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx</automated>
    <fails_when>The command exits non-zero or its Vitest summary names a failing queue-query or ops-queue-row assertion for deterministic evidence, fail-closed standing, or batched impact equivalence.</fails_when>
  </verify>
  <acceptance_criteria>The focused suites prove deterministic complete listing evidence, a total visible standing vocabulary, and equality between batched and established single-listing impact contracts; page row construction remains staff-gated and free of per-row database work.</acceptance_criteria>
  <done>All database-backed data-shape and row-contract tests pass with no identity-document projection, no browser money computation, and no change to package manifests, lockfile, or `drizzle/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff request → `/ops` server page | A browser request reaches privileged listing and host evidence. |
| database read model → client row props | Staff-only data crosses from explicit SQL projection to a serializable client island. |
| browser decision widget → existing Server Functions | Local disclosure state shares a row with protected money-bearing decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Elevation | `src/app/(ops)/ops/page.tsx` | high | mitigate | Task 1 retains `await requireStaff()` as the first page statement and adds no evidence endpoint or route. |
| T-22-02 | Information Disclosure | `src/lib/ops/review-queue.ts` | high | mitigate | Task 1 keeps named SQL columns and the narrow host DTO; Task 2 verifies the added listing evidence without a document or identifier projection. |
| T-22-03 | Denial of Service | `src/lib/ops/cancel-impact.ts` and page map | medium | mitigate | Task 1 replaces page-level per-listing reads with one set-based loader and a completed `Map`; Task 2 proves output equivalence. |
| T-22-04 | Elevation | `OpsDecisionActions` placement | high | mitigate | Task 1 reuses the existing protected decision widget without adding or changing Server Functions; UI state has no authorization role. |
</threat_model>

<verification>
- `npm.cmd test -- tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx`
- `npx playwright test e2e/ops-queue.spec.ts --project=chromium --reporter=list`
</verification>

<success_criteria>
- The first end-to-end staff inspection path works through the real ops host, server read model, existing client row, and existing decision widget.
- Listing evidence is explicit, deterministic, fail-closed, and contains no additional host identity material.
- The page has one batched impact read and a synchronous final row map.
</success_criteria>

<output>
Create `.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-01-SUMMARY.md` when done.
</output>
