---
phase: 22-ops-decides-with-the-whole-picture-queue-removal-enforcement
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - src/lib/ops/review-queue.ts
  - src/components/ops/ops-queue-row.tsx
  - tests/ops/queue-query.test.ts
  - tests/ops/ops-queue-row.test.tsx
  - e2e/ops-queue.spec.ts
autonomous: true
gap_closure: true
gap_ids: [G-22-8]
requirements: [OPS-13, OPS-14, OPS-15]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A signed-in staff reviewer can read the host-set weekly operating hours in the expanded listing evidence before deciding."
    - "Weekly hours show Sunday-first canonical sentences, including ordered multiple windows and explicit closed days; an empty schedule makes every day visibly closed."
    - "The queue remains staff-gated and terminal, with one decision widget before the evidence and the existing responsive Court/Grove behavior."
  artifacts:
    - path: src/lib/ops/review-queue.ts
      provides: "A bounded serializable listing operating-hours DTO projected in deterministic day/window order."
    - path: src/components/ops/ops-queue-row.tsx
      provides: "An Operating hours evidence fact rendered through the canonical week-strip derivation."
    - path: e2e/ops-queue.spec.ts
      provides: "Authenticated responsive staff proof of populated multi-window and closed-day hours evidence."
    - path: tests/ops/queue-query.test.ts
      provides: "Integration coverage for ordered and empty operating-hours queue projection."
    - path: tests/ops/ops-queue-row.test.tsx
      provides: "Component coverage for evidence order, canonical weekly labels, closed days, and an empty schedule."
  key_links:
    - from: src/lib/ops/review-queue.ts
      to: src/components/ops/ops-queue-row.tsx
      via: "OpsQueueListingItem.operatingHours crosses the existing server-to-client row contract as plain day/time values."
      pattern: "operatingHours"
    - from: src/components/ops/ops-queue-row.tsx
      to: src/lib/availability/week-strip.ts
      via: "deriveWeekStrip owns weekday order, time labels, range order, and closed-day wording."
      pattern: "deriveWeekStrip"
    - from: e2e/ops-queue.spec.ts
      to: src/app/(ops)/ops/page.tsx
      via: "A seeded staff session visits the configured ops origin and expands the existing queue row without a navigation or secondary request."
      pattern: "Show listing evidence"
---

<objective>
Close G-22-8 by extending the established queue-evidence path so staff can inspect the host-set weekly schedule in the same expanded terminal row where they decide the listing.

Purpose: Weekly availability is a listing fact needed for a staff decision; the source table already exists, but the Phase 22 projection and evidence renderer omit it.
Output: Ordered queue DTO hours, canonical operating-hours evidence, and focused authenticated/browser plus integration/component proof.
</objective>

## Phase Goal

**As a** signed-in FitOut staff reviewer, **I want to** inspect a host listing submission, **so that** I can decide its action.

<source_audit>

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 22 | Staff can inspect a host listing in place before deciding. | Tasks 1–2 add the omitted weekly-hours fact to that existing inspection path. | COVERED |
| REQ | OPS-13 | Every fact needed to decide is available in one in-place disclosure. | Tasks 1–2 project, display, and verify operating hours. | COVERED |
| REQ | OPS-14 | One visible decision widget remains before evidence. | Task 1 preserves and rechecks the existing decision/evidence geometry in the authenticated matrix. | COVERED |
| REQ | OPS-15 | Host standing remains a visible fact rather than a blank. | Existing 22-01/22-02 coverage remains in place; Task 2 preserves the adjacent ordered Host fact. | COVERED |
| RESEARCH | Phase 22 queue architecture | Explicit server-owned DTO, terminal row, existing local disclosure, and no client fetch. | Task 1 extends that path without a new route or request. | COVERED |
| CONTEXT | — | No Phase 22 CONTEXT.md exists; the binding decision is the diagnosed UAT gap. | Tasks 1–2 cite and close G-22-8 exactly. | COVERED |
</source_audit>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UAT.md
@.planning/debug/g-22-8-operating-hours-evidence.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-01-SUMMARY.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-02-SUMMARY.md
@.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UI-SPEC.md
@src/lib/ops/review-queue.ts
@src/components/ops/ops-queue-row.tsx
@src/lib/availability/week-strip.ts
@e2e/ops-queue.spec.ts
@tests/ops/queue-query.test.ts
@tests/ops/ops-queue-row.test.tsx

<interfaces>
- `OpsQueueListingItem` in `src/lib/ops/review-queue.ts` is the serializable server-owned DTO for one pending listing; its collection fields currently normalize SQL `json_agg` nulls to arrays.
- `deriveWeekStrip(windows)` in `src/lib/availability/week-strip.ts` is the sole owner of Sunday-first weekday names, hour labels, multi-window ordering, and the `closed` sentence. It accepts the existing `{ dayOfWeek, openTime, closeTime }` shape.
- `OpsQueueRow` is the existing client island. Its listing branch renders one `OpsDecisionActions` instance before the disclosure and one conditional `Listing evidence` region; host rows do not receive this evidence surface.
- `e2e/ops-queue.spec.ts` already authenticates a seeded staff user at `ops.localhost`, executes the Court/Grove × 320px/1280px matrix, enforces a literal-loopback database target, and checks row geometry and terminal behavior.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one host-set weekly schedule from queue projection to staff-visible evidence</name>
  <precondition>The local test schema contains the established listing-owned `operating_hours` relation, and the existing browser tracer's database URL satisfies its literal loopback-host assertion before it seeds.</precondition>
  <reversibility rating="costly">Adding a field to the shared server-to-client queue DTO requires the query and row renderer to remain coordinated, although it has no persisted or public wire-format migration.</reversibility>
  <files>src/lib/ops/review-queue.ts, src/components/ops/ops-queue-row.tsx, tests/ops/queue-query.test.ts, tests/ops/ops-queue-row.test.tsx, e2e/ops-queue.spec.ts</files>
  <read_first>
    - AGENTS.md and the relevant `node_modules/next/dist/docs/` client-component guide before changing the server DTO / client-island boundary.
    - .planning/debug/g-22-8-operating-hours-evidence.md and .planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-UAT.md for the exact failed truth and root cause.
    - src/lib/ops/review-queue.ts and src/components/ops/ops-queue-row.tsx for the explicit-column DTO, existing lateral aggregates, normalized arrays, decision-widget placement, and evidence fact order.
    - src/lib/availability/week-strip.ts for the canonical `deriveWeekStrip` contract; do not create another day, time-label, range-order, or closed-day formatter.
    - e2e/ops-queue.spec.ts and playwright.config.ts for the local-only seed guard, real staff sign-in, configured ops origin, and current responsive assertions.
  </read_first>
  <behavior>
    - Begin RED by extending the existing queue-query fixture, row factory, and authenticated browser fixture with a host-set Monday schedule whose windows are inserted out of time order, while Sunday has no record; the focused suite must require the queue DTO field and its rendered canonical Monday multi-window sentence, and the browser matrix must expose that sentence plus Sunday’s closed-day sentence at every current Court/Grove and 320px/1280px point.
    - GREEN projects plain `{ dayOfWeek, openTime, closeTime }` values on `OpsQueueListingItem`, then renders them as an Operating hours fact through `deriveWeekStrip`; the browser reaches that fact only after signing in as staff and opening the existing disclosure on `/ops`.
    - The control zone continues to contain exactly one existing decision widget before the evidence region, and the established URL, overflow, touch-target, contact, terminal-row, and host-row behavior remains intact.
  </behavior>
  <action>Close G-22-8 through the established listing-evidence path. First make the UAT path red in the queue-query fixture, the `listingRow` render factory, and the authenticated browser fixture, then commit the assertions as `test(22-03): add failing operating-hours queue contracts`. The focused assertions must seed an out-of-order Monday schedule, require the plain `operatingHours` DTO values in deterministic day/window order, and require the expanded row to render the canonical Monday sentence through its Operating hours fact; the browser path additionally requires the closed Sunday sentence. Add a narrow serializable operating-hours item type to `src/lib/ops/review-queue.ts`, extend `OpsQueueListingItem` and its SQL row type, and select the listing-owned records with one lateral JSON aggregate ordered by `day_of_week ASC`, `open_time ASC`, then `close_time ASC`; normalize an absent aggregate to `[]` alongside photos and amenities. Keep the queue's explicit named-column/host-privacy contract, current predicates and wait-clock ordering, and existing staff-only page boundary unchanged. In `OpsQueueRow`, import `deriveWeekStrip` and render its returned seven canonical sentences as a semantic list within one `Fact` named `Operating hours`; place that fact after `Price` and before `Host`, yielding the complete evidence order Description, Amenities, Address, Space type, Capacity, Price, Operating hours, Host, Submitted, Contact. An empty projected schedule must still render all seven explicit closed-day sentences, rather than a blank value or a new local formatter. Extend the existing browser seed with the one multi-window schedule and assert the exact canonical sentence strings, while retaining its real ops sign-in, row-only URL, singular controls before evidence, no-overflow, responsive geometry, and terminality checks. Make the implementation pass and commit it as `feat(22-03): show operating hours in ops queue evidence`. Leave `drizzle/`, schema definitions, package manifests, lockfile, routes, server actions, and runtime dependency set untouched.</action>
  <verify>
    <automated>npm.cmd test -- tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx</automated>
    <automated>node node_modules/@playwright/test/cli.js test e2e/ops-queue.spec.ts --project=chromium --reporter=list --workers=1</automated>
  </verify>
  <done>A loopback-only seeded staff session opens one pending listing at the real ops origin and, at every existing theme/viewport point, reads the canonical multi-window and closed-day operating-hours evidence in the one expanded terminal row before the unchanged decision controls.</done>
</task>

<task type="auto">
  <name>Task 2: Lock ordered, empty, closed-day, and row-order operating-hours contracts</name>
  <files>tests/ops/queue-query.test.ts, tests/ops/ops-queue-row.test.tsx</files>
  <read_first>
    - tests/ops/queue-query.test.ts for the isolated-schema fixture helpers and its existing out-of-order photo / amenity projection assertions.
    - tests/ops/ops-queue-row.test.tsx for `listingRow`, `valueFor`, the disclosure helper, and the exact ordered-evidence assertion to extend.
    - src/lib/ops/review-queue.ts, src/components/ops/ops-queue-row.tsx, and src/lib/availability/week-strip.ts to test the real DTO and canonical derivation boundary rather than reimplementing their rules in test helpers.
  </read_first>
  <action>Extend Task 1's happy-path focused assertions into the edge-state proof for G-22-8, then commit the completed coverage as `test(22-03): lock operating-hours evidence contracts`. In `queue-query.test.ts`, retain the established deterministic populated projection assertion and add the listing with zero source rows receiving `[]`. In `ops-queue-row.test.tsx`, expand the evidence and assert the outer terms retain the exact row order with `Operating hours` between Price and Host. Assert canonical Sunday-first sentences for the seeded schedule’s closed day and multiple windows, including the canonical conjunction/order; separately assert `operatingHours: []` presents every weekday as explicitly closed. Retain the current assertions that one decision widget sits outside evidence, rows have no destination/link role before contact reveal, host rows have no disclosure, and Host remains an adjacent visible fact. Do not duplicate weekday labels, time formatting, range joining, or closed wording in production or test helper code; expected sentence literals belong only in behavior assertions. The task changes coverage files only unless Task 1's committed generic implementation demonstrably fails a stated contract.</action>
  <verify>
    <automated>npm.cmd test -- tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx</automated>
  </verify>
  <done>The focused integration and component suites prove deterministic source projection, multi-window display order, closed-day display, the all-closed empty schedule, and the complete ordered evidence list without weakening existing staff, decision, or terminal-row assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff request → `/ops` page | Privileged weekly listing evidence is exposed only after the existing staff request gate. |
| listing `operating_hours` rows → queue DTO | Database values become serialized props crossing into the existing client row. |
| queue DTO → expanded evidence | The client presents server-selected schedule data beside protected approve/reject controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-G8-01 | Information Disclosure | `src/lib/ops/review-queue.ts` and `/ops` | high | mitigate | Task 1 retains the first-statement staff gate and carries hours only through the existing staff page DTO; it adds no endpoint, route, or client fetch. |
| T-22-G8-02 | Tampering | operating-hours projection and display | medium | mitigate | Task 1 uses explicit ordered fields and the existing canonical derivation; Task 2 proves query and rendered ordering against noncanonical inserts. |
| T-22-G8-03 | Denial of Service | per-listing lateral aggregate | low | mitigate | Task 1 extends the existing bounded per-listing queue aggregate and does not introduce an N+1 request or second browser fetch. |
| T-22-G8-04 | Elevation | `OpsDecisionActions` adjacency | high | mitigate | Task 1 preserves the existing protected widget and staff-gated page composition while adding read-only evidence below it. |
| T-22-G8-SC | Tampering | npm package supply chain | high | mitigate | This plan installs no package; Task 1 leaves manifests and lockfile unchanged, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npm.cmd test -- tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx`
- `node node_modules/@playwright/test/cli.js test e2e/ops-queue.spec.ts --project=chromium --reporter=list --workers=1`
- `npm.cmd run test:design -- tests/design/ops-host-invariants.test.ts`
- `node node_modules/eslint/bin/eslint.js src/lib/ops/review-queue.ts src/components/ops/ops-queue-row.tsx tests/ops/queue-query.test.ts tests/ops/ops-queue-row.test.tsx e2e/ops-queue.spec.ts`
- `git diff --check`
</verification>

<success_criteria>
- G-22-8's failed truth passes: an authenticated staff reviewer can inspect host-set weekly hours in the expanded listing row before deciding.
- The row presents canonical ordered windows and explicit closed days for populated and empty schedules, without a competing formatter.
- The staff gate, one-widget-before-evidence layout, terminal row contract, responsive matrix, schema, and dependency set remain preserved.
</success_criteria>

<output>
Create `.planning/phases/22-ops-decides-with-the-whole-picture-queue-removal-enforcement/22-03-SUMMARY.md` when done.
</output>
