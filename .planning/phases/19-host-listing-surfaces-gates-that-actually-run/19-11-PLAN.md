---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 11
type: execute
wave: 11
depends_on: ["19-10"]
gap_closure: true
files_modified:
  - scripts/verify-workflows.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-12 — `gate-e2e` is asserted to RUN THE SUITE, not merely to exist: deleting the job's Playwright run command turns `node scripts/verify-workflows.mjs` red instead of leaving every invariant green over a job that installs npm and stops."
    - "D-12 — `gate-e2e` is asserted to be UNCONDITIONAL: a job-level `if:` or `continue-on-error: true` — either of which silently detaches the gate while leaving every parsed invariant intact — turns the script red."
    - "D-12 — `gate-e2e` is asserted to seed the catalogue BEFORE the suite, by index rather than by presence, mirroring the migrate-then-seed-then-playwright ordering assertion the visual job already carries. Five specs fail against an empty catalogue and say so in their own messages."
    - "CI-01 adjacency — a job that EXISTS and a job that RUNS THE SUITE are not the same property and are not treated as equal: the existence hard stop and the runs-the-suite check answer different questions and both are present."
    - "CI-01 empty — when `gate-e2e` declares zero run commands the new invariants FAIL rather than pass vacuously, because each is an existential over the run-command list and an existential over an empty list is false; the printed evidence is the empty list itself."
    - "CI-01 ordering — the seed and the suite are compared on INDEX within the job's run-command order, so a seed step that exists but runs after the suite is red, not green."
    - "D-14 — the fail-closed mail assertion and its parse-based sibling are untouched by this plan; the new invariants neither weaken nor duplicate them."
    - "`.github/workflows/ci.yml`'s header describes the file it is attached to: five jobs, with the functional Playwright suite named among them, and the retired exclusion claim marked falsified in the file's own established style rather than silently rewritten."
    - "The ci.yml edit is documentation-only: every changed line in that file is a comment line, and every job body, step, `run:` command and `env:` key is byte-unchanged."
    - "`node scripts/verify-workflows.mjs` exits 0 after both tasks, with an invariant count three higher than the count recorded in 19-08-SUMMARY."
  artifacts:
    - scripts/verify-workflows.mjs
    - .github/workflows/ci.yml
  key_links:
    - "`playwright.config.ts`'s `chromium` project carries `testMatch: \"e2e/*.spec.ts\"`, so naming that project in the run command IS naming the whole functional set — which is why the invariant asserts the project BY NAME rather than asserting that some Playwright command exists."
    - "`scripts/verify-workflows.mjs` runs on `gate-db-free`, the cheap job that always runs, so these invariants fire about a minute into a run rather than after a browser boots."
    - "The `gate-visual` job's existing by-name and ordering checks are the shape these three copy; the argument transfers verbatim and the plan says so rather than re-deriving it."
  prohibitions:
    - statement: "A gate must never report green over a job that runs nothing — a check that cannot fail is a report, and this repository has already recorded what it costs to mistake one for the other."
      status: unverified
      verification: flagged
    - statement: "A file must never describe a state it no longer has — a reader auditing the exclusion from this header would conclude Playwright is out of CI, which is no longer true and which other files still depend on being true."
      status: unverified
      verification: flagged
  assumptions:
    - "D-15's required-check flip is OUT OF SCOPE and stays out. Branch protection returns `403 — Upgrade to GitHub Pro or make this repository public`; that is a repository-visibility and billing decision recorded as a PM hold in 19-08-SUMMARY and re-raised as a human-verification item in 19-VERIFICATION. This plan makes the gate harder to detach; it does not and must not attempt to make it blocking, and no task here works around the 403."
    - "D-13's wall-clock measurement is done and recorded in `evidence/gate-e2e-wallclock.txt`; the sharding decision it gates is not this plan's to make and no task here changes the job's timeout, worker count or spec selection."
    - "The 14 pre-existing e2e failures and the stale visual baselines (since 2026-08-30) are OUT OF SCOPE — they predate the phase and are named in the verification report as needing a separate suite-repair phase. No task here repairs a spec or regenerates a baseline."
    - "WR-02, WR-03, WR-04, WR-06, WR-07 and WR-08 from `19-REVIEW.md` are real and are NOT closed by this plan — only WR-01 and WR-05 were assigned to this gap-closure run. They are carried forward, not absorbed and not silently dropped."
    - "The three unresolved HSURF-01 edge-probe rows and the unclassified HSURF-02 row are carried in `19-09-PLAN.md`'s `assumptions` block. CI-01's three probe rows are resolved as explicit truths above. Accounting: seven probe rows, three authored as truths here, four carried as assumptions in 19-09 — no row dropped, none counted twice."
---

<objective>
Close the two CI-01 warnings the verification report flagged: a set of gate invariants that can pass
over a job doing nothing, and a file header that describes a workflow the file no longer contains.

`scripts/verify-workflows.mjs`'s `gate-e2e` block asserts the job exists and that its display name
matches what branch protection would key on. It does not assert that the job RUNS the suite, that it
is unconditional, or that it seeds before running. Delete the Playwright run command and every
invariant still holds. Separately, `.github/workflows/ci.yml`'s header still says the file has four
jobs and that almost the whole e2e set is excluded from CI — both false since `gate-e2e` landed.

Purpose: this is the class the file names about itself twice — a description that outlives what it
describes, and a checker whose subject can be removed without it noticing. Leaving either is how the
next phase inherits a gate that lies.

Output: three new parse-based invariants, each proven by a temporary and reverted mutation of the
workflow, and a header that is true about the file it is attached to.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VERIFICATION.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-08-SUMMARY.md

**Why `depends_on: ["19-10"]`.** Worktrees are OFF and this phase's executed plans form a strict
chain on `dev`; the wave number is this plan's position in that chain. Its real artifact dependency
is `19-08`, which shipped both the `gate-e2e` job and the invariants this plan extends. It shares no
file with plans 19-09 or 19-10.

**Do not modify any of `19-01-PLAN.md` … `19-08-PLAN.md`.** They are executed.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Three invariants that a detached gate cannot survive — each watched RED</name>

  <reversibility rating="reversible">Three `check()` calls appended to one section of one script;
  deleting them restores today's behaviour exactly.</reversibility>

  <read_first>
    - `scripts/verify-workflows.mjs` — the whole `ci` section's `gate-e2e` block: the `CI_E2E_JOB`
      and `CI_E2E_CONTEXT` constants and the comment explaining why they are separate, the hard stop
      for a missing job with its full diagnostic text, and the display-name `check()` that follows it.
    - `scripts/verify-workflows.mjs` — the `gate-visual` block immediately below, in particular the
      `--project=visual` BY NAME check and the migrate-then-seed-then-playwright INDEX ordering check.
      These two are the shape this task copies and the argument transfers verbatim.
    - `scripts/verify-workflows.mjs` — the `runsOf`, `stepsOf`, `check` and `hardStop` helpers, and
      the `counts` map `check` increments, so the new invariants land in the right section total.
    - `.github/workflows/ci.yml` — the `gate-e2e` job body in full: its `name:`, its absence of a
      job-level `if:` and of `continue-on-error`, its `timeout-minutes`, the mail-refusal step, the
      migrate step, the seed step and the Playwright step with its project argument.
    - `playwright.config.ts` — the `chromium` project's `testMatch`, which is what makes naming the
      project equivalent to naming the whole functional set.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` — WR-01 in
      full, including its fix sketch and the three failure shapes it names.
  </read_first>

  <behavior>
    - Deleting the job's Playwright run step from `ci.yml` makes `node scripts/verify-workflows.mjs`
      exit non-zero, naming the runs-the-suite invariant.
    - Adding `continue-on-error: true` to the job makes it exit non-zero, naming the unconditional
      invariant. Adding a job-level `if:` does the same.
    - Moving the seed step after the Playwright step makes it exit non-zero, naming the ordering
      invariant.
    - With `ci.yml` unmodified, the script exits 0 and its printed `ci` count is exactly three higher
      than before this task.
    - A `gate-e2e` job with an empty run-command list fails all three rather than passing any: each
      is an existential over that list.
  </behavior>

  <files>scripts/verify-workflows.mjs</files>

  <action>
Append three `check()` calls to the `ci` section's `gate-e2e` block in
`scripts/verify-workflows.mjs`, immediately after the existing display-name check and before the
`gate-visual` hard stop. Bind `runsOf(e2e)` to a local first, so all three read the same list and the
evidence strings can print it.

**Invariant 1 — it runs the functional project BY NAME.** True when some run command contains both
the Playwright test invocation and the chromium project argument. Not "some Playwright command
exists" — the project name is what selects `testMatch: "e2e/*.spec.ts"`, so naming it is naming the
whole functional set, and a bare invocation would also collect the visual project that job 4 owns
and whose committed baselines this job must never touch. The evidence string prints the full
run-command list, so a red says what the job actually runs. The `gate-visual` block one screen below
makes this identical argument for its own project name; say in the comment that the argument
transfers rather than restating it at length.

**Invariant 2 — it is unconditional.** True when the job declares no job-level `if:` AND does not set
`continue-on-error` to true. Either one detaches the gate while leaving every other parsed invariant
perfectly intact — the same failure shape as the display-name edit the check above it was added for.
The evidence string prints both actual values so a red is actionable. The comment must say that a
deliberate condition is a decision to RECORD, not one to absorb: remove this invariant in the same
commit and say why, exactly as the hard stop above demands for a deliberate job deletion.

**Invariant 3 — it seeds before it runs.** Find the index of the seed command and the index of the
Playwright command within the run-command list, and require both to be present and the seed to come
strictly first. Presence alone is not the property: a seed after the suite is a seed that changed
nothing, and the job's own step comment records that five named specs fail against an empty
catalogue. Print both indices and the list length as evidence, in the shape the `gate-visual`
ordering check already uses.

Write each invariant's failure-facing name in the voice the file uses for the others — a sentence a
reader can act on, naming the job in quotes and the property in plain words.

**Then prove all three by watching them red.** For each of the three mutations below, apply it to
`.github/workflows/ci.yml`, run the script, capture the exact FAIL line, then revert `ci.yml` and
confirm the script is green again before applying the next:

  1. delete the job's Playwright step;
  2. add `continue-on-error: true` to the job, then separately add a job-level `if:` expression;
  3. move the seed step to after the Playwright step.

After the last revert, confirm `git status --porcelain .github/workflows/ci.yml` produces no output
— the mutations are measurements, and a measurement left in the tree is a defect. Record all four
observed FAIL lines verbatim in the SUMMARY; a paraphrase is not evidence that the check fired.

Change nothing else in the script. In particular do not touch the mail-key scan, the addressing rule,
the supply-chain scan or any `baselines` invariant — WR-02, WR-03 and WR-04 name real holes in three
of those and are explicitly carried forward rather than closed here.
  </action>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or the printed `ci` count is not exactly three higher than the count recorded in 19-08-SUMMARY; or any line begins with `FAIL`; or the HARD STOP banner is printed</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=ci</automated>
    <fails_when>non-zero exit, or the three new invariant names are absent from the printed output</fails_when>
    <automated>git status --porcelain .github/workflows/ci.yml</automated>
    <fails_when>any output at all — a mutation used to watch a red was left in the tree</fails_when>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-workflows.mjs` contains three new `check()` calls inside the `gate-e2e` block,
      all placed after the display-name check and before the `gate-visual` hard stop.
    - Each new check's `ok` expression is an existential or an index comparison over `runsOf(e2e)`,
      so a job with an empty run-command list makes all three false — demonstrate by temporarily
      emptying the job's steps, observing three FAIL lines, and reverting.
    - The by-name check requires BOTH the Playwright test invocation and the chromium project
      argument in the same run command, not either one alone.
    - The unconditional check tests the job-level `if:` and `continue-on-error` separately in its
      evidence string, so a red names which one fired.
    - The ordering check compares indices and requires both to be non-negative — a missing seed step
      is red, not green.
    - Four observed FAIL lines (the deleted suite step, `continue-on-error`, a job-level `if:`, the
      reordered seed) are recorded verbatim in the SUMMARY.
    - `git status --porcelain .github/workflows/ci.yml` is empty at the end of this task.
    - `node scripts/verify-workflows.mjs` exits 0 and its `ci` count is exactly three higher than
      before.
  </acceptance_criteria>

  <done>Detaching `gate-e2e` from actually running Playwright — by deletion, by condition, or by
  reordering the seed — now fails the check that runs on every push, and each of those three was
  watched failing rather than reasoned about.</done>
</task>

<task type="auto">
  <name>Task 2: The ci.yml header describes the file it is attached to</name>

  <reversibility rating="reversible">Comment text in one workflow file; no job, step or command
  changes, so the workflow's behaviour is identical before and after.</reversibility>

  <read_first>
    - `.github/workflows/ci.yml` lines 1 through 160 — the whole header: the `THE JOB SPLIT` block
      and its per-job list, the `WHAT IS DELIBERATELY ABSENT` bullets (the e2e-exclusion claim and
      the narrow-exception claim that follows it), and the invariant list that begins `The invariants
      worth re-checking after ANY edit here`.
    - `.github/workflows/ci.yml` — the `gate-e2e` job body, so the header's new entry describes what
      the job actually does rather than what this plan assumes.
    - `.github/workflows/ci.yml` — the two passages the file has ALREADY falsified in place (the
      measured count correction on the e2e bullet, and the paragraph recording that the visual step
      landed on job 1 and later left it). These are the house style for this edit: keep the old
      claim, mark it falsified with the date and the plan that falsified it, and state what replaced
      it. Do not invent a new style.
    - ⚠ `.github/workflows/ci.yml` around the snapshot-update prohibition — the paragraph explaining
      that the forbidden flag is deliberately not spelled anywhere in the file, comments included,
      because the cheapest audit of the rule is a count that a comment would permanently break. Do
      not introduce that token while editing nearby prose.
    - `scripts/verify-workflows.mjs` — the four `gate-e2e` invariants as they stand after Task 1,
      so the header's invariant list names what the parser actually asserts.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` — WR-05, which
      names each false passage with its line range.
  </read_first>

  <files>.github/workflows/ci.yml</files>

  <action>
Four comment-only edits to `.github/workflows/ci.yml`. Nothing outside a comment line may change.

**1. The job split.** The block that enumerates the jobs and splits them by database need currently
counts four and lists four. It counts FIVE, and gains a fifth entry for `gate-e2e` written in the
same register as the four beside it: the functional Playwright suite, a database AND a browser,
running the chromium project by name in the pinned container, landed by plan 19-01/19-08 under D-12
and CI-01.

**2. The exclusion claim.** The bullet asserting that almost the whole e2e set is deliberately not
run here is now false and is the passage a reader auditing D-24 would trust. Falsify it IN PLACE, in
the file's own style: keep the original sentence, mark it falsified with the date and the plan, and
state what is true now — the functional set runs as job 5 on every push and pull request; what
remains outside that job is the `visual` project, which job 4 owns. Keep the flake argument the
bullet makes, because it is still the reason the job was added late and measured before being
trusted, and add the fact that D-13's wall-clock measurement now exists and is recorded in the
phase's evidence file.

**3. The narrow-exception claim.** The sentence declaring job 3 the one exception to that exclusion
is false in its quantifier and true in its content. Correct the quantifier and preserve the content:
job 3 is still the DB-vs-DOM price check and still names its single spec explicitly, and that
constraint is still asserted by the parser — but it is no longer an exception to a broad exclusion,
because the exclusion is retired.

**4. The invariant list.** The list of invariants worth re-checking after any edit predates jobs 4
and 5. Add the `gate-e2e` set as it stands after Task 1: the display name branch protection would key
on, the functional project asserted BY NAME, the job asserted unconditional, and the seed asserted to
precede the suite. Add the D-14 fail-closed mail assertion and name both of its halves — the runtime
step in job 5 and the parse-based key scan on job 1 — and state, because WR-02 measured it and this
plan does not close it, that the parse half does not currently scan container-level or service-level
`env:` blocks. A list that omits a known hole is the same defect as a header that omits a job.

Add one line at the end of the header recording what is NOT true of this gate and why, so a reader
does not have to find it in a planning document: `gate-e2e` is not a required status check, because
branch protection on this repository returns a plan-upgrade refusal, and that is a recorded product
decision rather than an oversight.
  </action>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line begins with `FAIL`; or the HARD STOP banner is printed; or the `ci` count differs from the count Task 1 left</fails_when>
    <automated>git diff -U0 -- .github/workflows/ci.yml</automated>
    <fails_when>any added or removed line (excluding `@@` hunk headers and the `+++`/`---` file headers) whose first non-whitespace character is not `#` — this edit is documentation-only and a changed YAML line means a job was touched</fails_when>
    <automated>node -e "const y=require('yaml');const d=y.parse(require('fs').readFileSync('.github/workflows/ci.yml','utf8'));const j=Object.keys(d.jobs);console.log(j.join(','));if(j.length!==5)process.exit(1)"</automated>
    <fails_when>non-zero exit, or the printed job list does not contain `gate-e2e` — this is the parse the file's own header prescribes instead of a substring match</fails_when>
  </verify>

  <acceptance_criteria>
    - The job-split block states five jobs and its list contains an entry naming `gate-e2e` and
      describing what the job runs.
    - The exclusion bullet carries an in-place falsification marker in the file's existing style,
      with a date and the plan that falsified it, and states that the functional set now runs as
      job 5.
    - The narrow-exception sentence no longer asserts that job 3 is the only exception, and still
      states that job 3 names its single spec explicitly.
    - The invariant list names four `gate-e2e` invariants and both halves of the D-14 mail
      assertion, and records the container/service `env:` gap WR-02 measured.
    - The header carries a line stating that `gate-e2e` is not a required status check and why.
    - `git diff -U0 -- .github/workflows/ci.yml` shows only comment lines changed.
    - The workflow still parses to exactly five jobs and `node scripts/verify-workflows.mjs` exits 0.
    - The snapshot-update flag's spelling was not introduced anywhere in the file by this edit —
      confirm the cheapest audit of that prohibition still returns zero, exactly as the file's own
      paragraph describes.
  </acceptance_criteria>

  <done>A reader auditing this repository's CI from the file's own header learns that five jobs run,
  that one of them is the functional Playwright suite, and what is still known to be uncovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pull request → the CI workflow | Arbitrary contributor-authored YAML changes cross here; the parse-based checker on job 1 is what inspects them before any browser boots. |
| repository/organization secrets → the `gate-e2e` job's environment | A live mail credential reaching this job was measured at fourteen real outbound sends per suite run. |
| `scripts/verify-workflows.mjs` → its own subject | The checker's assertions are only as real as the subject they quantify over; a removed subject makes a universal assertion vacuously true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-11-01 | Tampering | `gate-e2e` job body | high | mitigate | Task 1's three invariants close the three shapes that detach the gate while leaving every existing assertion green: deleting the suite step, conditioning or soft-failing the job, and reordering the seed. Each is proven by a watched, reverted mutation rather than by reading. |
| T-19-11-02 | Repudiation | `.github/workflows/ci.yml` header | medium | mitigate | A header claiming Playwright is excluded from CI causes a future auditor to conclude a control is absent when it is present, and the reverse for the controls it omits. Task 2 falsifies the stale passages in place, in the file's own style, so the correction is legible as a correction. |
| T-19-11-03 | Information disclosure | mail credential reaching the suite (D-14, WR-02) | medium | accept | The runtime step and the parse-based key scan both ship and are unchanged. WR-02 measured that the parse half does not walk `container.env` or `services.*.env`; nothing in this repository sets a mail key at either level today, and widening the scan is outside this gap-closure run's assigned scope. Accepted with the hole NAMED in the header by Task 2 rather than left implicit. |
| T-19-11-04 | Tampering | reusable-workflow delegation and `secrets: inherit` (WR-03) | medium | accept | The supply-chain and credential scans are built on step lists and are blind to a job written as a reusable-workflow call. Real, out of this run's assigned scope, carried forward to a follow-up rather than absorbed here. No job in this file uses that shape today. |
| T-19-11-05 | Elevation of privilege | `gate-e2e` as a merge gate (D-15) | medium | transfer | The gate reports but cannot block, because branch protection is unreachable on this repository's plan. Transferred to the PM as a recorded visibility/billing decision — already held once mid-phase and re-raised as a human-verification item. No task here works around it. |
| T-19-11-SC | Tampering | npm/pip/cargo installs and third-party actions | high | mitigate | This plan installs nothing and adds no `uses:` entry. Only `actions/checkout` and `actions/setup-node` remain, both first-party; the existing supply-chain invariant continues to assert it. No package-legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
1. `node scripts/verify-workflows.mjs` — exit 0, `ci` count three higher than 19-08-SUMMARY's.
2. `node scripts/verify-workflows.mjs --section=ci` — the three new invariant names present in the
   printed output.
3. `git diff -U0 -- .github/workflows/ci.yml` — only comment lines changed.
4. The workflow parses to exactly five jobs.
5. The SUMMARY records FOUR observed FAIL lines verbatim, one per watched mutation.
</verification>

<success_criteria>
- `gate-e2e` cannot be detached from running the functional suite without turning the always-run
  checker red, and each detachment shape was watched failing.
- `ci.yml`'s header states five jobs, names the functional suite job, retires the exclusion claim in
  the file's own falsification style, and records both what the parser now asserts and what it is
  known not to.
- No job, step, command or environment key in `ci.yml` changed.
</success_criteria>

<artifacts_this_phase_produces>
**New workflow invariants** (in `scripts/verify-workflows.mjs`, `ci` section, `gate-e2e` block)
- `gate-e2e` runs the functional project BY NAME.
- `gate-e2e` is unconditional — no job-level `if:`, no `continue-on-error: true`.
- `gate-e2e` seeds the catalogue before the suite, compared by index.

**Modified files**
- `scripts/verify-workflows.mjs` — three appended `check()` calls; section count +3.
- `.github/workflows/ci.yml` — four comment-only header corrections plus one new line recording the
  non-required state.

**New verify commands entering the phase's proven set**
- `node scripts/verify-workflows.mjs --section=ci`
- `git diff -U0 -- .github/workflows/ci.yml` as a comment-only-change gate.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-11-SUMMARY.md` when
done. Record the four observed FAIL lines verbatim, the before and after `ci` invariant counts, and
the exact wording used for each falsification marker so a later reader can find them by search.
</output>
