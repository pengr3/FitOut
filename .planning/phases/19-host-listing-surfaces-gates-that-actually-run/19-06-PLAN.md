---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 06
type: execute
wave: 6
depends_on: ["19-05"]
files_modified:
  - e2e/host-route-reachability.spec.ts
  - src/app/actions/listing.ts
  - tests/listing/crud.test.ts
autonomous: true
requirements: [HSURF-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A host-facing route that stops resolving goes red in about two seconds with a message naming the discriminator, not in sixty seconds with a message about fixtures (D-11)."
    - "The routing guard asserts ROUTING and never authorization: every request is anonymous, so a 404 proves the page module never ran, and the message says so and forbids softening the ownership check (D-10, D-11)."
    - "Pressing Create listing twice, or landing on the creation URL twice, yields ONE listing row for that host (D-02)."
    - "A draft the host has genuinely started editing is NEVER reused: the predicate requires updated_at = created_at AND a null title AND zero photos, and every ambiguity resolves toward tightness (D-02)."
    - "saveListingStep stays UNGATED — the reuse read is additional to createDraftListing and nothing about what the wizard may write changes (D-02 / D-270)."
    - "The reuse read is owner-scoped BY ARGUMENT from the session and takes no id parameter, so one host can never be measured against another's rows."
    - "The reuse read sits AFTER the host-verification gate and BEFORE the insert, so an unverified host cannot discover whether they own a reusable draft."
    - "The docblock states plainly that this makes creation idempotent against a HUMAN RETRY and not against concurrency, and that the residual race yields one surplus empty draft and no data loss."
  artifacts:
    - e2e/host-route-reachability.spec.ts
    - src/app/actions/listing.ts
    - tests/listing/crud.test.ts
  key_links:
    - "createDraftListing has exactly one caller, (host)/host/listings/new/page.tsx:80, so the contract change has one integration point."
    - "updated_at carries the meaning because schema.ts:264-267 declares $onUpdate, which fires on every db.update() through the listing table object — every write path in the app."
    - "listing-photo inserts do NOT touch the listing row (listing-photo.ts:294), which is why the photo NOT EXISTS conjunct closes a real gap rather than a hypothetical one."
    - "The new spec joins the chromium project automatically and rides gate-e2e, moving the spec count from 38 to 39."
---

<objective>
Leave the instrument, and stop the orphan class at its source.

Plan 19-05 left the record. This plan leaves the guard: a two-second spec that goes red — for the
right reason, with the right sentence — if a host-facing route stops resolving. And it makes listing
creation idempotent, so a failed redirect can never mint a fourth empty draft again.

Purpose: D-11 exists because this class has cost real time twice and left nothing behind both times.
D-02 exists because cleaning up orphans after the fact treats the residue rather than the source, and
it is what makes plan 19-04's delete a genuine one-off rather than the first of many.
Output: a routing guard with a diagnostic failure message, and a reuse-then-mint branch in
`createDraftListing` with the three cases that pin it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-PATTERNS.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-05-SUMMARY.md
@src/app/actions/listing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/host-route-reachability.spec.ts — D-11's guard, and watch it go red</name>
  <files>e2e/host-route-reachability.spec.ts</files>
  <read_first>
    - `19-RESEARCH.md § 1.4` — the full spec body at lines 525-559, its docblock, and the four notes the planner must preserve. Copy it rather than paraphrase; its whole value is the failure message.
    - `e2e/axe-sweep.spec.ts:294-315` — `mintDraftListing` and its docblock: the repository's own demonstration that coverage without diagnosis costs sixty seconds and a re-diagnosis. That is the bar this spec exists to beat.
    - `e2e/axe-sweep.spec.ts:1163` — `expect(path).toBeTruthy()`, the assertion nine host rows collapse into when the redirect stops landing, and whose message names a fixture problem rather than a route problem.
    - `src/app/(host)/host/listings/[id]/edit/page.tsx:30-33` — the `/login` redirect that precedes the database read. **Read only; never edit (D-10).**
    - `src/db/schema.ts:202` — `listing.id` is `text`, which is why an arbitrary literal segment routes and this spec needs no fixture, no seed and no database.
    - `playwright.config.ts` — `testMatch: "e2e/*.spec.ts"`, so the new file joins the `chromium` project automatically.
    - `19-PATTERNS.md § 10`.
  </read_first>
  <action>
    Create `e2e/host-route-reachability.spec.ts` from 19-RESEARCH § 1.4's body.

    Four routes, each asserted anonymously with `maxRedirects: 0`, each expecting `307`:
    `/host/listings`, `/host/listings/new`, `/host/listings/route-reachability/edit`,
    `/host/listings/route-reachability/availability`.

    Four properties, every one load-bearing:
    1. **`maxRedirects: 0`.** Playwright's request context follows redirects by default; a followed
       307 reports 200 from the login page and the assertion becomes vacuous.
    2. **A literal id segment, not a seeded row.** `listing.id` is `text`, so an arbitrary string
       routes, and because the request is anonymous the module never reaches the database. No
       fixture, no seed, no database — this is what makes the spec cost about two seconds.
    3. **`request`, not `page`.** No browser context, no cookies, and therefore no risk of an ambient
       session turning the 307 into a 200.
    4. **The failure message IS the deliverable.** It must state that a 404 here does not mean the
       listing is missing and does not mean the ownership check refused — the request carries no
       cookie, so the page redirects to the login route before it reads the database — and that a 404
       therefore means the ROUTER never reached the module. It must print the two manifest `grep`
       commands to run before reading a 404 as an application bug, and it must say that absence from
       the running server's manifest indicates a dev-server routing artifact rather than an
       application defect.

    The docblock must carry the ⚠ that a red here must NOT be "fixed" by softening the ownership
    check at the edit route: that check is shipped, and the 404 it renders is byte-identical to the
    root not-found, which is the exact collision that made this look like an application bug the
    first time.

    ⚠ The docblock must NOT name a cause for the phantom 404 (D-11). It records the discriminator,
    not a diagnosis. Point at `19-RESEARCH § 1` and at `19-FINDING-404.md`; assert nothing beyond
    what those two record.

    **Then watch it go red** (Pattern 4): temporarily delete the `maxRedirects: 0` option, run the
    spec, and confirm it reports `200` rather than `307` — proving the option is what makes the
    assertion non-vacuous. Record the failure line verbatim, then restore the option and confirm
    green.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `e2e/host-route-reachability.spec.ts`.</fails_when>
    <automated>npx playwright test e2e/host-route-reachability.spec.ts --project=chromium</automated>
    <fails_when>Exit code is non-zero. A red reporting `404` on any route means the router did not reach the module — read the failure message, run the two manifest greps it prints, and do NOT touch the ownership check. A red reporting `200` means `maxRedirects: 0` is missing and the assertion is vacuous.</fails_when>
    <automated>grep -c "maxRedirects" e2e/host-route-reachability.spec.ts</automated>
    <fails_when>The count is 0 — the redirect-following option is absent, so every 307 reports as a 200 from the login page and all four assertions are vacuous.</fails_when>
    <automated>ls e2e/*.spec.ts | wc -l</automated>
    <fails_when>The printed count is anything other than 39 — the spec count must move 38 to 39 here, and a drifting number is what the research asked the summary to pin.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/host-route-reachability.spec.ts --project=chromium` exits 0 and reports 4 passing tests.
    - The whole file completes in under 10 seconds, recorded in the summary — the point of the guard is that it is cheap.
    - `grep -c "maxRedirects" e2e/host-route-reachability.spec.ts` returns at least 1.
    - `grep -c "app-paths-manifest" e2e/host-route-reachability.spec.ts` returns at least 1 — the failure message prints the command that discriminates a routing artifact from an application defect.
    - `grep -c "request" e2e/host-route-reachability.spec.ts` returns at least 1, and the file does not import or use a browser `page` fixture.
    - `ls e2e/*.spec.ts | wc -l` returns 39.
    - `git status --porcelain "src/app/(host)/host/listings/[id]/"` produces no output.
    - The watched-red evidence is in the summary: the verbatim failure line produced with the redirect option removed, showing `200` where `307` was expected, and confirmation of the restore.
  </acceptance_criteria>
  <done>A two-second anonymous spec goes red with a diagnostic sentence if any of four host-facing routes stops resolving, its non-vacuity has been proven by removing the load-bearing option, and no cause is named anywhere in it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reuse-then-mint in createDraftListing — the D-02 predicate</name>
  <files>src/app/actions/listing.ts</files>
  <read_first>
    - `src/app/actions/listing.ts:36` — the existing drizzle import (`and`, `eq`, `isNull`, `sql`, `type InferInsertModel`). ⚠ `desc` is NOT in this list and must be added, or the ordering clause has to be written as raw SQL.
    - `src/app/actions/listing.ts:88-103` — `assertOwnership`, the file's owner-scoping idiom.
    - `src/app/actions/listing.ts:105-165` — `createDraftListing` in full: its docblock, the D-255 verification gate ending at `:152`, and the insert at `:154-160`. **The reuse read goes between them.**
    - `src/app/actions/listing.ts:172-190` — `saveListingStep`'s docblock and the D-270 argument. **That function is not touched.**
    - `src/db/schema.ts:199-287` — the `listing` columns, and specifically `:250` (`status` default `draft`), `:260` (`review_state` default `pending`), `:262` (`deleted_at`), `:263` (`created_at` `defaultNow().notNull()`), `:264-267` (`updated_at` `defaultNow().$onUpdate(...).notNull()`).
    - `src/app/actions/listing-photo.ts:294` — the photo insert that does NOT update the listing row. This is the measured gap the photo conjunct closes.
    - `19-RESEARCH.md § 5.1` through `§ 5.5` — the measured fresh-row shape, the recommended predicate, the two asymmetric failure directions, the check-then-act argument, and the placement rule.
    - `19-PATTERNS.md § 6`.
  </read_first>
  <behavior>
    - Calling `createDraftListing()` twice in a row for one verified host returns the SAME id both times, and the `listing` table grows by exactly one row.
    - Calling it, then writing a wizard step to that draft, then calling it again returns a DIFFERENT id and leaves two rows — the host's started work is never adopted.
    - Calling it, then attaching a `listing_photo` row to that draft, then calling it again returns a DIFFERENT id — `updated_at` alone does not close the photo gap, because the photo insert does not touch the listing row.
    - A host with no verification row still gets the existing refusal and no reuse read is performed on their behalf.
    - `saveListingStep` behaves exactly as before on both a fresh and a grandfathered draft.
  </behavior>
  <action>
    Insert the D-02 reuse read into `createDraftListing`, between the end of the verification gate at
    `:152` and the insert at `:154`.

    **Predicate** (19-RESEARCH § 5.2), a Drizzle `select` of `listing.id` from `listing`, ordered by
    `createdAt` descending and limited to 1, where ALL of:
    - `hostId` equals `userId` — owner-scoped **by argument from the session**, never from a
      parameter. `createDraftListing` takes no arguments at all; keep it that way.
    - `deletedAt` is null;
    - `status` equals `draft`;
    - `updatedAt` equals `createdAt` — **the load-bearing term**. `schema.ts:264-267` declares
      `$onUpdate`, which fires on every `db.update()` through this table object, and every write path
      in the app goes through it: the wizard autosave, publish, unlist, soft-delete, the re-review
      flip. So "the host has done literally anything to this row" collapses to one comparison, and it
      does not need to enumerate twenty columns that will drift as the schema grows;
    - `title` is null — belt-and-braces, which `updatedAt = createdAt` should already imply;
    - no `listing_photo` row references this listing — **the real gap**, because
      `listing-photo.ts:294` inserts inside a transaction and does not update the listing row, so a
      host who minted a draft, uploaded a cover photo and abandoned it has `updatedAt = createdAt`
      AND a photo. Without this conjunct a second press would silently adopt their photo into what
      they believe is a new listing.

    If a row is found, return it as the successful result. Otherwise fall through to the existing
    insert, unchanged.

    Add `desc` to the drizzle import at `:36`, or write the ordering as raw SQL.

    **Consider adding the same NOT EXISTS for `operating_hours`.** It is the same class of gap and
    assumption A9 (whether the availability editor writes to the listing row) is unverified. Every
    ambiguity resolves toward tightness, because the two failure directions are NOT symmetric: too
    loose means the host presses Create for their second space, lands in the wizard for their first
    one already half filled in, edits it, overwrites real work and never learns a second listing was
    not created — silent data loss on a host's own content. Too tight means one extra empty draft
    appears in the grid, which is exactly today's behaviour, visible and deletable. Add the conjunct.

    **The docblock must say three things, and the third is the one a later reader will delete:**
    1. what "untouched" means here, defined from the columns that are null or default on a
       freshly-minted row, and why the timestamp comparison carries the meaning;
    2. why the read sits after the verification gate and before the insert — placing it before the
       gate would let an unverified host discover whether they own a reusable draft, and placing it
       in the page would make it bypassable, which is the argument that page's own header already
       makes about the verification gate;
    3. ⚠ **that this is a check-then-act and must not be described as race-free.** Two tabs can both
       find zero reusable drafts and both insert. That is acceptable here and the reason must be
       written down: the failure mode is one extra empty draft, not a double-booked room and not a
       double charge; the observed defect was SEQUENTIAL (four inserts across 46 seconds is a human
       pressing a button, going back and pressing again), which is exactly what a check-then-act does
       fix; and the correct-by-construction alternative is a partial unique index, which is a schema
       migration, and zero schema migrations is a v1.2 invariant. A docblock claiming idempotency
       without that qualifier is the confident-wrong-claim the house rules forbid.

    ⚠ `saveListingStep` stays UNGATED (D-270). The reuse read changes what `createDraftListing`
    RETURNS; it never changes what the wizard may write.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `src/app/actions/listing.ts` — most often an unimported ordering helper.</fails_when>
    <automated>npx vitest run tests/listing/crud.test.ts</automated>
    <fails_when>Exit code is non-zero. A red on "grandfathered SUCCEEDS (FINDING F-7)" means the verification gate's state set was disturbed; a red on the D-270 case means `saveListingStep` was gated, which this task must not do.</fails_when>
    <automated>ls drizzle/*.sql | tail -1</automated>
    <fails_when>The printed filename is anything other than `drizzle/0029_listing_review_cascade.sql` — a migration appeared. D-02 says in as many words: no migration, this is a query plus a branch. A migration here is a milestone scope alarm.</fails_when>
    <automated>git status --porcelain package.json package-lock.json</automated>
    <fails_when>Any output at all — a dependency changed, which violates the v1.2 zero-new-runtime-dependencies invariant.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/listing/crud.test.ts` exits 0 with the existing cases still passing by name.
    - The reuse read appears in `createDraftListing` textually after the verification gate's refusal return and before `db.insert(listing)`.
    - `grep -c "listing_photo" src/app/actions/listing.ts` returns at least 1 — the photo conjunct is present.
    - `grep -c "updatedAt" src/app/actions/listing.ts` returns at least 1 — the load-bearing timestamp term is present.
    - `createDraftListing`'s signature still takes zero arguments: `grep -c "export async function createDraftListing()" src/app/actions/listing.ts` returns 1.
    - `git diff HEAD -- src/app/actions/listing.ts` shows no change inside `saveListingStep`.
    - The docblock contains the check-then-act qualifier in prose: that this is idempotent against a human retry and not against concurrency, and that the residual race costs one surplus empty draft and no data loss.
    - `ls drizzle/*.sql | tail -1` returns `drizzle/0029_listing_review_cascade.sql`.
  </acceptance_criteria>
  <reversibility rating="reversible">One added query and one branch in one server action. Reverting restores mint-always; no data is migrated and no schema changes, so there is nothing to undo beyond the code.</reversibility>
  <done>Creating a listing reuses the host's own untouched empty draft when one exists and mints otherwise, the predicate cannot adopt work a host has started, and the docblock states its own limits rather than overclaiming.</done>
</task>

<task type="auto">
  <name>Task 3: the three D-02 cases, with case 2 watched red</name>
  <files>tests/listing/crud.test.ts</files>
  <read_first>
    - `tests/listing/crud.test.ts` in full — its existing `createDraftListing` cases, specifically "grandfathered SUCCEEDS (FINDING F-7)" and the D-270 `saveListingStep` case. The three new cases go beside them and follow their setup idiom exactly.
    - `19-RESEARCH.md § 5.6` — the three cases and the ⚠ naming case 2 as the one to watch go red.
    - `19-RESEARCH.md § 5.3` — the two asymmetric failure directions, so each case's failure message can say which one it is protecting against.
    - `src/app/actions/listing-photo.ts:294` — the photo insert, for case 3's setup.
  </read_first>
  <action>
    Add three cases to `tests/listing/crud.test.ts`, beside the existing `createDraftListing` cases.

    **Case 1 — D-02's headline.** Two consecutive `createDraftListing()` calls for one verified host
    return the SAME id, and the `listing` table grows by exactly one row. Assert both: the identical
    id AND the row count, because an assertion on the id alone would pass if the second call somehow
    returned the first id while still inserting.

    **Case 2 — the loose-predicate guard, and the one that protects the host's work.** Call once,
    write a wizard step to that draft, call again; assert a DIFFERENT id and two rows.

    **Case 3 — the photo loophole.** Mint a draft, attach a `listing_photo` row to it without
    otherwise touching the listing, call again; assert a DIFFERENT id. This is the case that goes
    green by accident if someone later "simplifies" the predicate down to the timestamp comparison,
    so its failure message must name that simplification as the thing it is guarding against.

    Each failure message names which failure direction it is protecting: case 2 and case 3 both
    protect against the unacceptable direction — silently adopting a draft the host has touched,
    which is data loss on a host's own content with no error and no way to notice.

    **Then watch case 2 go red** (19-RESEARCH § 5.6's ⚠): temporarily remove the
    `updatedAt = createdAt` conjunct from the predicate, run the file, confirm case 2 fails by name,
    record the message verbatim, restore the conjunct and confirm green. That conjunct is the term
    that carries the meaning, and a test that would pass without it is testing nothing.
  </action>
  <verify>
    <automated>npx vitest run tests/listing/crud.test.ts</automated>
    <fails_when>Exit code is non-zero, or the reported test count did not increase by 3 from HEAD — cases that were written but not registered report success and assert nothing.</fails_when>
    <automated>npx vitest run</automated>
    <fails_when>Exit code is non-zero anywhere in the suite. A red outside `tests/listing/` means the contract change reached a caller nobody expected — `createDraftListing` is supposed to have exactly one.</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `tests/listing/crud.test.ts`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/listing/crud.test.ts` exits 0 and the file's test count is 3 higher than at HEAD.
    - `npx vitest run` exits 0 across the whole suite.
    - Case 1 asserts both the identical returned id and the exact row-count delta of 1.
    - Case 2 asserts a different id and a row count of 2.
    - Case 3 asserts a different id after a `listing_photo` row is attached, and its failure message names the predicate simplification it guards against.
    - The watched-red evidence is in the summary: case 2's verbatim failure message with the timestamp conjunct removed, and confirmation of the restore and the return to green.
  </acceptance_criteria>
  <done>Three cases pin D-02: one that creation is idempotent, and two that it can never adopt work a host has started — with the load-bearing conjunct proven load-bearing by removing it and watching the right case fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → `createDraftListing` | A server action reachable by any signed-in host; the row it returns determines which listing the wizard then writes into. |
| Anonymous HTTP client → host routes | The routing guard deliberately crosses this boundary with no credential, which is what makes its result decisive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-25 | Elevation of Privilege | The reuse read in `createDraftListing` | high | mitigate | Owner-scoped by argument from the session; the action takes zero parameters, so no request value can select the row. An acceptance criterion pins the zero-argument signature. Same property the existing verification read has. |
| T-19-26 | Information Disclosure | Draft existence leaked to an unverified host | high | mitigate | The reuse read sits AFTER the verification gate, so a host in a refusing state never reaches it and cannot learn whether they own a reusable draft. Placement is an acceptance criterion. |
| T-19-27 | Tampering | A host's in-progress listing adopted by a second Create press | high | mitigate | The predicate requires `updated_at = created_at` AND a null title AND zero photos (and zero operating hours). Cases 2 and 3 pin it, and case 2 is watched red with the load-bearing conjunct removed. |
| T-19-28 | Elevation of Privilege | The ownership check at the edit route | high | mitigate | The routing guard asserts routing, not authorization, and its own failure message forbids softening that check (D-10). `git status --porcelain` on that directory producing no output is an acceptance criterion. |
| T-19-29 | Denial of Service | Concurrent creation racing the check | low | accept | Two tabs can both find no reusable draft and both insert. Accepted, and documented at the call site: the cost is one surplus empty draft, the observed defect was sequential, and the race-free alternative is a schema migration that the milestone's zero-migration invariant forbids. |
| T-19-30 | Spoofing | An ambient session turning the guard's 307 into a 200 | medium | mitigate | The guard uses the API request context rather than a browser page, so it carries no cookies, and `maxRedirects: 0` is asserted present. Non-vacuity is proven by removing that option and watching the assertion report 200. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged — zero packages installed. `git status --porcelain package.json package-lock.json` producing no output is a verify gate. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npx vitest run` exits 0 across the whole suite.
3. `npx playwright test e2e/host-route-reachability.spec.ts --project=chromium` exits 0 with 4 tests in under 10 seconds.
4. `ls e2e/*.spec.ts | wc -l` returns 39.
5. `ls drizzle/*.sql | tail -1` returns `drizzle/0029_listing_review_cascade.sql`.
6. `git status --porcelain package.json package-lock.json` produces no output.
</verification>

<success_criteria>
- A host-facing route that stops resolving goes red in seconds with a message that names the discriminator.
- Two consecutive creations yield one row; a touched draft is never reused.
- `saveListingStep` is unchanged and still ungated.
- Zero migrations, zero dependencies, and nothing under the edit route's directory touched.
</success_criteria>

## Artifacts this phase produces

This plan's slice of the phase-wide index in `19-01-PLAN.md`:

**New files**
- `e2e/host-route-reachability.spec.ts` — D-11's guard. Four tests, one per route, each titled `… resolves to its module (anonymous ⇒ 307)`. No fixture, no seed, no database.

**Modified surface, stated as symbols**
- `src/app/actions/listing.ts`: `createDraftListing` gains a reuse-then-mint branch between the verification gate and the insert; the drizzle import at `:36` gains a descending-order helper. Signature unchanged (zero arguments). `saveListingStep` unchanged.
- `tests/listing/crud.test.ts`: three new cases pinning D-02.

**Spec-count bookkeeping:** `ls e2e/*.spec.ts | wc -l` moves 38 → **39**. This is the final count for
the phase, and it is the number `gate-e2e` runs.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-06-SUMMARY.md` when done.

It must record:
- the routing guard's wall-clock for the whole file (expect under 10 seconds);
- the watched-red line for the guard, produced with the redirect option removed, showing 200 where 307 was expected;
- the watched-red line for D-02 case 2, produced with the timestamp conjunct removed;
- whether the operating-hours conjunct was added, and why (assumption A9);
- the final `ls e2e/*.spec.ts | wc -l` count (expect 39), which is the number plan 19-08 records against the wall-clock;
- confirmation that `saveListingStep` is unchanged and `drizzle/` still ends at `0029_listing_review_cascade.sql`.
</output>
