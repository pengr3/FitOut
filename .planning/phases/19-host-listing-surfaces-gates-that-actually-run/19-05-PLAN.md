---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-prod.txt
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md
autonomous: false
requirements: [HSURF-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "HSURF-02 opens with a REPRODUCTION GATE and not a code change: nothing under src/app/(host)/host/listings/[id]/ is edited at any step of this plan (D-09, D-10)."
    - "The ELEVEN-URL probe set is run anonymously against a clean dev server and its full status matrix is recorded — eleven, not ten; the prose count in the upstream sources is wrong and the list is right."
    - "The same eleven-URL probe is run under a production build (next build then next start with the platform-wallet pair supplied inline), and its matrix is recorded — this is the item the phase CAN close (b)."
    - "UNPROVEN item (a) — that clearing .next and restarting makes it go away — is recorded as permanently OPEN with its reason, because the dev server that exhibited the 404 no longer exists. No task claims to close it (D-11)."
    - "UNPROVEN item (c) is recorded as a REFRAMING with its mtimes and its five candidates, and NO cause is named in any docblock or in the finding (D-11)."
    - "The /ops row stays 404 and byte-identical — it is in the probe set as a regression control, not as a subject."
    - "The written finding exists on disk, so this class of defect leaves an instrument and a record behind rather than being re-diagnosed from scratch a third time (D-11)."
  artifacts:
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-prod.txt
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md
  key_links:
    - "An anonymous request to the edit route can only produce a 307: EditListingPage redirects to /login at edit/page.tsx:30-33, BEFORE the db.select() at :43 and long before the notFound() at :49. A 404 therefore proves the module never ran."
    - "npm start arms every module-scope boot guard because it sets NODE_ENV=production; the platform-wallet pair is exempt only during the build phase, so it must be supplied inline at start or the first route importing src/lib/paymongo.ts 500s."
    - "next build and next dev share .next — the dev server must be killed before the build, because a build racing a dev server is one of the four named candidates for (c)."
---

<objective>
Run HSURF-02's reproduction gate and write down what it found — including what it could not find.

The measured evidence points at the running dev server, not at application code, and the process that
exhibited the 404 is gone. So this plan produces a matrix, a verdict and a written finding, and its
most likely honest outcome is "not reproducible". That is a finding, not a failure of the phase.

Purpose: this class has cost real time twice on this machine and neither occasion left anything
behind, so both were re-diagnosed from scratch. This plan leaves the record; plan 19-06 leaves the
instrument.
Output: two probe matrices on disk, and `19-FINDING-404.md` carrying a disposition for each of the
three unproven items — one closed, two open, and no cause named.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-CONTEXT.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: clear .next, restart, and run the eleven-URL probe against a clean dev server</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt</files>
  <precondition>Plan 19-04's archive is complete — all four files exist under `evidence/` and are non-empty. This task's first command destroys the originals, and there is no second chance. Also: nothing may be listening on :3000 when it starts (`playwright.config.ts` sets `reuseExistingServer: false`, so a stray server is a guaranteed failure later too).</precondition>
  <read_first>
    - `19-RESEARCH.md § 1.1` — the probe set, why every request is ANONYMOUS, the expected matrix per URL, and the two ⚠ notes on row 5 and row 11.
    - `19-RESEARCH.md § 1.2` steps 0 through 4 — the ordered protocol, and the branch rule at step 4.
    - `19-RESEARCH.md § Code Examples § The eleven-URL probe` — the loop, ready to run.
    - `src/app/(host)/host/listings/[id]/edit/page.tsx:30-33` and `:43-51` — the `/login` redirect that precedes the database read, and the `notFound()` behind it. **Read only. Never edit. That `notFound()` is a shipped ownership check (D-10).**
    - `19-CONTEXT.md § D-09`, `§ D-10`, `§ D-11`.
  </read_first>
  <action>
    Follow 19-RESEARCH § 1.2 in order, no steps skipped and none reordered.

    **Step 2** — confirm nothing holds :3000: an anonymous request to the site root must report
    `000` (connection refused). If a server IS running, kill it before touching the build directory.

    **Step 3** — D-09's clear: remove the whole `.next` directory, not just its dev subtree. The
    production manifest lives at the top level and the production half of this protocol rebuilds it
    anyway.

    **Step 4** — start the dev server, wait for ready, then run the ELEVEN-URL probe from
    § 1.1 anonymously (no cookie), capturing status code and redirect target per URL into
    `evidence/probe-dev.txt`.

    ⚠ It is eleven URLs, not ten. `PITFALLS.md § B5` says "ten" in its prose and then lists eleven;
    `ROADMAP.md` and `19-CONTEXT.md` both inherit the wrong count. Use all eleven and say so in the
    summary, so the next reader is not hunting for a twelfth.

    ⚠ The uuid the probe set uses, `e6ca32d0-41c1-4fbf-9cee-79402b962c51`, was DELETED by plan 19-04.
    That is fine and it changes nothing about what the probe measures: `listing.id` is `text`, the
    segment matcher accepts any string, and the request is anonymous so the module redirects to
    `/login` before it ever reads the database. Keep the same literal so the matrix is comparable to
    the 2026-09-03 pass, and note in the file that the row no longer exists.

    Then run the confirming second observation for rows 3 and 4: `grep -c` the running dev server's
    manifest for the edit and availability routes, and stat the compiled artifact.

    **The branch rule, and it is not negotiable:**
    - All `307` on rows 3, 4, 5 and 6 ⇒ NOT REPRODUCED under a clean dev server. Continue to Task 2.
    - Any `404` on rows 3, 4, 5 or 6 ⇒ REPRODUCED. **Stop. Capture everything — the dev server's
      stdout, the manifest before and after the request, the artifact mtime before and after — and
      hand the diagnosis to `/gsd-debug` (D-10).** Do not proceed to the production probe first; a
      live reproduction is the scarcest thing in this phase.
    - ⚠ Row 11 (`/ops`) must stay `404` and byte-identical. It is a regression control, not a
      subject. If it moves, stop and escalate — that is a Phase-20 concern arriving early.
  </action>
  <verify>
    <automated>test -s .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt && wc -l < .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt</automated>
    <fails_when>The file is missing or empty, or the line count is fewer than 11 — a probe set that recorded ten rows is the documented off-by-one and means one URL was silently dropped.</fails_when>
    <automated>grep -c '/ops ' .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt</automated>
    <fails_when>The count is 0 — the `/ops` regression control is missing from the matrix, so nothing confirms D-219's 404 cloak survived this phase.</fails_when>
    <automated>grep '/ops ' .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt</automated>
    <fails_when>The printed line reports any status other than `404` — a 200 or a 307 on `/ops` means the cloak moved, which is out of this phase's scope and is an immediate escalation.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/probe-dev.txt` contains exactly 11 rows, one per URL, each with a status code.
    - Rows 1, 2, 7 and 8 report `307` with a `/login` redirect target.
    - Rows 9 and 10 report `200` — the two public dynamic-segment controls.
    - Row 11 (`/ops`) reports `404`.
    - Rows 3, 4, 5 and 6 each report either `307` (NOT REPRODUCED) or `404` (REPRODUCED), and the summary states which, per row, without generalising.
    - The manifest `grep -c` results for the edit and availability routes on the freshly-built dev server are recorded alongside the matrix.
    - `git status --porcelain "src/app/(host)/host/listings/[id]/"` produces no output — nothing under that directory was touched.
  </acceptance_criteria>
  <reversibility rating="costly">`rm -rf .next` destroys the original artifacts permanently and they are not in git. It is downgraded from one-way to costly only because plan 19-04 archived every one of them first — that archive is the reversal path, and this task's precondition asserts it is complete before the command runs. Without that archive this would be a one-way door and would need its own decision checkpoint.</reversibility>
  <done>A clean dev server has been probed on all eleven URLs anonymously, the matrix is on disk, the manifest observation for the two subject rows is recorded, and no file under the edit route's directory was touched.</done>
</task>

<task type="auto">
  <name>Task 2: the production-build probe — the one unproven item this phase CAN close</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-prod.txt</files>
  <precondition>The dev server from Task 1 is stopped. `next build` and `next dev` share the `.next` directory, and a build racing a dev server is one of the four named candidates for what this phase is trying not to confuse itself about.</precondition>
  <read_first>
    - `19-RESEARCH.md § 1.2` steps 5 and 6 — the build, the start with the platform-wallet pair supplied inline, and the ⚠ about the three further boot guards that can fire.
    - `src/lib/paymongo.ts:25-47` — the module-scope boot guards, and specifically why the platform-wallet guard is exempt during the build phase but fires at start.
    - `.github/workflows/ci.yml:626-641` — the five module-scope boot guards the build step enumerates, and the written argument that softening a security control to make a red check green is the worst trade available.
    - `19-RESEARCH.md § 1.3` row (b) — what evidence closes this item, and what to write if the server cannot be made to boot.
  </read_first>
  <action>
    Build the application, then start it in production mode with the two platform-wallet variables
    supplied **inline for that one command** — visibly fake, self-describing values, exactly as
    19-RESEARCH § 1.2 step 5 writes them. `next start` sets a production environment, which arms
    every module-scope fail-closed boot guard; the wallet guard is exempted only during the build
    phase, so it fires at runtime and 500s the first route that imports the payments module.

    ⚠ Do not write those values into `.env.local`. That file is not in git, so a machine-specific
    fix would not travel, and the research records this trap by name.
    ⚠ Do not weaken any boot guard to make the probe run. If the server 500s on a DIFFERENT guard
    name, supply that name inline too with a visibly fake self-describing value, and **record which
    ones were needed** — that list is itself a useful finding and belongs in the summary. Three of
    the five are not build-phase exempt, so this is a live possibility rather than a hypothetical.

    Re-run the same eleven-URL anonymous probe against the production server and capture it to
    `evidence/probe-prod.txt`. Then confirm the production manifest contains the edit route.

    **Verdict — it is one of exactly three (19-RESEARCH § 1.2 step 7):**
    - **A. Not reproduced in dev, not reproduced in prod** — the most likely outcome and the one the
      surviving evidence points at. NO application file changes for HSURF-02 beyond the idempotent
      creation and the failure sentence, which are their own defect and stand on their own merits.
    - **B. Reproduced in dev, not in prod** — a live dev-server artifact, now reproducible. Hand to
      `/gsd-debug`. This is the only branch that could still close items (a) and (c) properly.
      **Still no edit to the edit route's page.**
    - **C. Reproduced under a production build** — an application/routing bug that ships. Escalate to
      the PM. It is the only branch where a source change under that directory is even on the table,
      and even then not at the ownership check.

    Record which verdict landed, with the matrix that supports it.
  </action>
  <verify>
    <automated>test -s .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-prod.txt && wc -l < .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-prod.txt</automated>
    <fails_when>The file is missing or empty, or the line count is fewer than 11 — the production probe did not cover the full set, most often because the server 500'd on a boot guard before the loop ran and the failure was not noticed.</fails_when>
    <automated>grep -c 'listings/\[id\]/edit' .next/app-path-routes-manifest.json</automated>
    <fails_when>The count is 0 — the freshly built PRODUCTION manifest is missing the edit route, which would be a genuinely new finding and contradicts the 2026-09-04 measurement. Stop and escalate.</fails_when>
    <automated>git status --porcelain "src/app/(host)/host/listings/[id]/"</automated>
    <fails_when>Any output at all — a file under the edit or availability route directory was modified, which D-10 forbids absolutely.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/probe-prod.txt` contains 11 rows with status codes.
    - `grep -c 'listings/\[id\]/edit' .next/app-path-routes-manifest.json` returns 1.
    - The summary names the verdict — A, B or C — and quotes the rows that determine it.
    - The summary lists every environment variable that had to be supplied inline for the server to boot, or states that only the platform-wallet pair was needed.
    - `git status --porcelain "src/app/(host)/host/listings/[id]/"` produces no output.
    - `git status --porcelain .env.local` produces no output, and `.env.local` is not in the commit.
  </acceptance_criteria>
  <done>The application has been probed under a real production build across all eleven URLs, the matrix is on disk, the verdict is named, and every boot guard that fired was supplied around rather than softened.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: the written finding — one item closed, two recorded open, no cause named</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md</files>
  <read_first>
    - `19-RESEARCH.md § 1.0` items 1 and 2 — the exact wording to use for item (a)'s "not closeable" disposition, and the mtime reframing for item (c) with its five candidates.
    - `19-RESEARCH.md § 1.3` — the table of what closes each item and what gets written if it cannot.
    - `19-CONTEXT.md § D-11` — "if it IS confirmed a dev-server artifact, the phase still leaves a guard behind plus the written finding", and the ⚠ forbidding a named cause without evidence.
    - `19-RESEARCH.md § Assumptions Log` rows A4 and A5 — the two assumptions the reframing rests on, both marked UNPROVEN and both forbidden from any docblock as fact.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/probe-dev.txt` and `evidence/probe-prod.txt` — the two matrices this finding reports.
  </read_first>
  <action>
    Write `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md`.

    Structure it as: the verdict (A, B or C) with both eleven-row matrices; then one section per
    unproven item, each opening with an explicit `**Disposition:**` line reading one of
    `OPEN — NOT CLOSEABLE`, `CLOSED`, `REPRODUCED — ESCALATED` or `OPEN — REFRAMED`; then the
    eleven-not-ten correction; then the `/ops` regression control result; then a pointer to the
    routing guard plan 19-06 ships.

    Item **(a)** gets `OPEN — NOT CLOSEABLE`, with 19-RESEARCH § 1.3's wording: the dev server that
    exhibited the 404 was already gone when the phase opened, and a clean restart serving 307 shows
    the current tree routes correctly but is not evidence about the old process.

    Item **(b)** gets its disposition from Task 2's matrix — `CLOSED` if the production build did
    not reproduce it, `REPRODUCED — ESCALATED` if it did.

    Item **(c)** gets `OPEN — REFRAMED`. Carry the three mtimes (compiled artifact `Sep 3 15:18`,
    dev session markers `18:09`, manifest `19:33`), list all five candidates — a swallowed compile
    error, an HMR write race, a build racing a dev server on the shared directory, a bundler bug,
    and the new fifth possibility that nothing removed the entry at all because the session may
    simply never have added it — and **attribute none of them**.

    ⚠ Write the reframing as inference, explicitly labelled. Two claims underpin it and both are
    UNPROVEN (assumptions A4 and A5): that this framework's dev route manifest is an incremental
    per-session compile ledger, and that the compiled artifact is a leftover from an earlier
    session. Both are inferred from mtimes on one machine. Neither may be stated as established.

    ⚠ A finding that names a cause is worse than no finding. This document's job is to record the
    discriminator so the next reader does not start from scratch — not to supply a diagnosis the
    evidence does not support.
  </action>
  <what-built>
    `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md` — the phase's
    written record of the listing-creation 404. It carries the verdict (A, B or C), both eleven-row
    probe matrices, and one section per unproven item, each opening with an explicit
    `**Disposition:**` line. Item (a) is `OPEN — NOT CLOSEABLE` because the dev server that exhibited
    the 404 was gone before the phase opened. Item (b) is closed or escalated by the production-build
    matrix. Item (c) is `OPEN — REFRAMED`, carrying three mtimes and five candidates with none
    attributed.

    This document exists because this defect class has now cost real time twice on this machine and
    left nothing behind either time, so it was re-diagnosed from scratch on both occasions. Plan 19-06
    ships the instrument; this is the record.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md` and read
    it as someone who was not present for the probe. Work through the eight questions in the
    `<human-check>` block below, in order. They are not a formality — questions 4 and 5 are the whole
    reason this is a human gate rather than a grep, because a regular expression over prose cannot
    tell an attribution from a deliberate refusal to attribute.

    The two supporting files are `evidence/probe-dev.txt` and `evidence/probe-prod.txt`; open them if
    a matrix row in the finding looks wrong.
  </how-to-verify>
  <resume-signal>
    Reply **approved** if all eight questions hold. Reply with the failing question numbers otherwise,
    and the executor rewrites the finding and re-presents it — a finding that names an unproven cause
    is worse than no finding at all, so this gate does not pass on a partial.
  </resume-signal>
  <verify>
    <human-check>
      Read `19-FINDING-404.md` and confirm, as a reader who was not present:

      1. Does it record the verdict (A, B or C) with both matrices, all eleven rows each?
      2. Does item **(a)** read as PERMANENTLY OPEN and NOT CLOSEABLE, with the reason stated —
         that the dev server which exhibited the 404 was already gone when the phase opened, and
         that a clean restart serving 307 shows the current tree routes correctly but is not
         evidence about the old process?
      3. Does item **(b)** carry a disposition based on the production matrix — CLOSED if the
         production build did not reproduce it, or REPRODUCED with an escalation if it did?
      4. Does item **(c)** read as a REFRAMING and never as an answer? Does it carry the three
         mtimes (compiled artifact 15:18, session markers 18:09, manifest 19:33), list all five
         candidates including the new fifth one — that nothing necessarily removed the entry —
         and **name none of them as the cause**?
      5. Does the document avoid stating, anywhere, as fact: that clearing the build directory
         fixed it; that the dev manifest is an incremental per-session ledger; or that the
         compiled artifact is a leftover from an earlier session? All three are UNPROVEN
         (assumptions A4 and A5) and must be written as inference, not as finding.
      6. Does it state that the probe set is ELEVEN URLs, and that the "ten" in the upstream prose
         is a miscount?
      7. Does it record that `/ops` stayed 404 as a regression control?
      8. Does it point the next reader at the guard plan 19-06 ships, so the third re-diagnosis
         does not start from scratch?

      **Approve only if all eight hold.** A finding that names a cause is worse than no finding —
      it is the confident-wrong-claim this project's house rules forbid, and it would send the next
      reader down a path this phase deliberately did not prove.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md` exists and is non-empty.
    - It contains a section per unproven item, each with an explicit disposition line reading OPEN, NOT CLOSEABLE, CLOSED or REPRODUCED.
    - It contains both eleven-row matrices, or links to the two evidence files and reproduces their subject rows inline.
    - `grep -c '^\*\*Disposition:\*\*' 19-FINDING-404.md` returns 3 — one explicit disposition line per unproven item, so no item can be left implicitly resolved.
    - Item (a)'s disposition line reads `OPEN — NOT CLOSEABLE`; item (c)'s reads `OPEN — REFRAMED`.
    - It names all five candidates for item (c) and attributes none — confirmed by the human reviewer's question 4, which is the gate for this, because a regex over prose cannot tell an attribution from a refusal to attribute.
    - It states the probe count as eleven and flags the upstream "ten" as a miscount.
    - The human reviewer has answered all eight questions in the affirmative.
  </acceptance_criteria>
  <done>The phase leaves a written record: one unproven item honestly closed, one honestly reframed, one honestly declared unclosable, and no cause named anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous HTTP client → the running application | Every probe carries no cookie; that is precisely what makes the result decisive, because an anonymous caller cannot reach the ownership check. |
| Module-scope boot guards → the local production process | Starting the app in production mode arms fail-closed guards that do not run in development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-20 | Elevation of Privilege | The ownership check at the edit route | high | mitigate | Nothing under `src/app/(host)/host/listings/[id]/` is edited at any step (D-10). `git status --porcelain` on that directory producing no output is an acceptance criterion in two tasks. The probe is anonymous by design so it can never be mistaken for an authorization test. |
| T-19-21 | Tampering | Module-scope fail-closed boot guards | high | mitigate | Guards are supplied around with visibly fake inline values for one command, never softened, never written to `.env.local`, and never committed. The workflow file makes the identical argument at length and is cited in the task. |
| T-19-22 | Information Disclosure | The `/ops` 404 cloak | high | mitigate | `/ops` is in the probe set as a regression control with a hard stop: any status other than 404 halts the plan and escalates. D-219's cloak was measured with body equality and must not be disturbed by this phase. |
| T-19-23 | Repudiation | A finding that names an unproven cause | medium | mitigate | The human checkpoint's questions 4 and 5 exist for this alone, and an acceptance criterion greps the finding for causal claims. Assumptions A4 and A5 are named as inference in the document, never as fact. |
| T-19-24 | Information Disclosure | Fake credentials in the summary | low | accept | The values supplied inline are visibly fake and self-describing (`dev-wallet-not-a-real-account`), carry no secret, and are recorded so the next reader can reproduce the probe. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged — zero packages installed. This plan runs only existing scripts (`npm run build`, `npm start`) and `curl`. |
</threat_model>

<verification>
1. Both probe files exist with 11 rows each.
2. `/ops` reports 404 in both matrices.
3. `grep -c 'listings/\[id\]/edit' .next/app-path-routes-manifest.json` returns 1.
4. `git status --porcelain "src/app/(host)/host/listings/[id]/"` produces no output.
5. `19-FINDING-404.md` exists, names no cause, and has passed the eight-question human check.
</verification>

<success_criteria>
- The eleven-URL probe ran anonymously against a clean dev server and against a real production build.
- The verdict is named and supported by both matrices.
- Item (b) is closed; items (a) and (c) are recorded open with their reasons; no cause is named.
- Nothing under the edit route's directory was touched, and no boot guard was weakened.
</success_criteria>

## Artifacts this phase produces

This plan's slice of the phase-wide index in `19-01-PLAN.md`:

**New files**
- `evidence/probe-dev.txt` — the eleven-URL anonymous matrix against a clean dev server, with the manifest observation for the two subject rows.
- `evidence/probe-prod.txt` — the same eleven URLs under `next build` plus `next start`.
- `19-FINDING-404.md` — the written finding. Sections: the verdict; item (a) NOT CLOSEABLE with reason; item (b) with its disposition; item (c) REFRAMED with its three mtimes and five candidates, none attributed; the eleven-not-ten correction; the `/ops` regression control; and a pointer to the guard plan 19-06 ships.

**Runtime state changed:** `.next/` was cleared and rebuilt. Not in git; disposable once archived.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-05-SUMMARY.md` when done.

It must record:
- both eleven-row matrices, per row, with status and redirect target;
- the verdict (A, B or C) and the rows that determine it;
- every environment variable that had to be supplied inline for `npm start` to boot;
- the manifest `grep -c` results for dev and for production;
- ⚠ that the probe set is ELEVEN URLs and the upstream "ten" is a miscount;
- ⚠ that item (a) is permanently open and item (c) is reframed rather than answered, and that no cause is named anywhere.
</output>
