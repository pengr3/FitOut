---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 12
type: execute
wave: 12
depends_on: ["19-11"]
gap_closure: true
files_modified:
  - scripts/refuse-mail-credential.mjs
  - tests/design/mail-credential-refusal.test.ts
  - .github/workflows/ci.yml
  - scripts/verify-workflows.mjs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14 — SUPERSEDES 19-01-PLAN.md's must_have `gate-e2e refuses to run when a live mail credential is present in its environment, and says so with an ::error:: line before anything boots`, which shipped INERT (19-VERIFICATION truth #8, 19-REVIEW CR-01). What is true after this plan: `gate-e2e`'s first step after `npm ci` reads the job's REAL process environment — every variable the container actually holds — and exits 1 with an `::error::` line naming the measured fourteen outbound sends per suite run when any variable NAME begins with the mail-provider prefix, before the database is migrated, before the catalogue is seeded, and before Playwright boots the app."
    - "D-14 — the runtime half is what closes the `container.env` blind spot, and the header says so instead of implying coverage nobody has. A key spelled in a job's `container.env` block reaches every step's process environment, so the refusal observes it; `services.*.env` sets variables only inside the postgres service container, which runs no application code and is therefore not a mail-send vector. The PARSE half still walks neither key (WR-03, carried forward, not closed here) and the header records that."
    - "D-14 — the two halves cannot drift. The mail-provider prefix is spelled exactly once in workflow-facing code (`MAIL_KEY_PREFIX` in `scripts/verify-workflows.mjs`); the runtime refusal READS it from that declaration rather than holding a copy; and a parse-based invariant goes red the moment the declaration stops matching the pattern the refusal reads it with."
    - "CI-01 — `.github/workflows/` spells no mail-provider token at all. The repository's own cheapest audit is now machine-asserted by the one file allowed to name the token, and it was RED at this plan's start because `ci.yml` carried exactly one occurrence in the inert step's `env:` value."
    - "CI-01 — the refusal is provable without a browser and without a push: a build-blocking design test spawns the real script with the variable set (exit 1, an `::error::` line on stdout) and unset (exit 0), so `npm run build` re-proves it on every build rather than a human trusting one watched CI run."
    - "CI-01 adjacency (spec-less edge probe) — an env KEY declared in a workflow `env:` map and an env VARIABLE present in the job's process are NOT the same thing and are not treated as equal: the parse half answers the first, the runtime half answers the second, both exist, and each is separately asserted."
    - "CI-01 empty (spec-less edge probe) — when the prefix cannot be read (the declaration no longer matches the pattern), the refusal exits 1 with an `::error::` naming the source path, rather than scanning for the empty string. An empty prefix matches every variable or none, and either is a green that means nothing."
    - "CI-01 ordering (spec-less edge probe) — the refusal's position is compared by INDEX within `gate-e2e`'s run-command list, not by presence: a refusal step that exists but runs after migrate, seed or Playwright is red, not green."
    - "`.github/workflows/ci.yml` and `scripts/verify-workflows.mjs` describe the control the tree now has: the claim that the parse half fires ahead of job 5's browser is removed from all three places it is written, replaced by the fact that `gate-db-free` and `gate-e2e` declare no `needs:`, start in parallel, and GitHub cancels neither when the other fails — so the parse half protects the NEXT run and the runtime half is what protects THIS one."
    - "Nothing else in `gate-e2e` moves: the display name, the container image, the service, `--project=chromium`, and the migrate → seed → Playwright ordering are byte-unchanged, and `gate-price-parity` is untouched. Verified by a parsed before/after comparison with only the refusal step's own `run:`/`env:` excluded."
  artifacts:
    - scripts/refuse-mail-credential.mjs
    - tests/design/mail-credential-refusal.test.ts
    - .github/workflows/ci.yml
    - scripts/verify-workflows.mjs
    - .planning/REQUIREMENTS.md
  key_links:
    - "`scripts/refuse-mail-credential.mjs` → `MAIL_KEY_PREFIX` in `scripts/verify-workflows.mjs`, read out of that file's source text with the same pattern the checker asserts on. This is the seam that makes the two halves one value; a copied literal would re-create exactly the drift class this phase has been fighting."
    - "`gate-e2e`'s refusal step → the container's real process environment, via Node's `process.env` (the same source `env |` prints), which IS populated from `container.env` and the runner environment. The Actions `${{ env.X }}` expression context never had this link — that absence is the whole defect."
    - "`tests/design/mail-credential-refusal.test.ts` → `npm run build`, via `test:design` (`package.json:11,33`) and `vitest.design.config.ts`, which has no `globalSetup`/`setupFiles` and therefore needs no database — so this proof runs on a laptop with no Docker."
  prohibitions:
    - statement: "A fail-closed control must never be documented as active while being structurally incapable of firing. An inert guard is worse than no guard, because its presence stops anyone from building the real one — this repository just spent a whole verification round taking one at face value."
      status: unverified
      verification: flagged
    - statement: "A checker and the thing it checks must never each hold their own copy of the value they agree on. A second spelling is a second source of truth wearing the costume of a constant."
      status: unverified
      verification: flagged
    - statement: "A refusal must never degrade to a vacuous pass when its own input is missing. An unreadable prefix is a hard stop, not a silent scan for nothing."
      status: unverified
      verification: flagged
    - statement: "A credential's VALUE must never be printed by the control that detects it. A build log is a wider audience than the mailbox the send would have reached."
      status: unverified
      verification: flagged
    - statement: "A false sentence about WHEN a security control fires must be replaced, not preserved behind a marker. To grep — which is how this repository audits its own prose — a preserved false sentence is indistinguishable from the claim itself."
      status: unverified
      verification: flagged
  assumptions:
    - "SPEC-LESS EDGE PROBE — all seven rows have a stated disposition, zero silent drops. CI-01 adjacency, CI-01 empty and CI-01 ordering are AUTHORED above as explicit truths (verification: explicit). HSURF-01 adjacency, HSURF-01 empty and HSURF-01 ordering are ALREADY-COVERED by executed plans and carried in `19-09-PLAN.md`'s assumptions block — the substance is in `19-UI-SPEC.md`'s `## UI Considerations` (adjacency: rows grouped by `cardTop` within 1px so a second row is never compared against the first; empty: zero → `EmptyState`, one → A1 skipped by row-grouping; ordering: A1 runs per visual row) and in `19-02`/`19-03`'s shipped guards. The HSURF-02 `unclassified` row remains UNRESOLVED and is carried as a flagged planner assumption in `19-09-PLAN.md`; it is not dismissed and it is not re-planned here, because HSURF-02 is VERIFIED and this run is CI-01-only. Accounting: 3 authored here, 4 carried in 19-09, none counted twice, none dropped."
    - "UI-SPEC LIFT — `19-UI-SPEC.md`'s `## UI Considerations` reports 9 covered + 1 backstop + 0 unresolved, every one of them owned by an executed plan (19-02/19-03 for the grid, 19-07/19-10 for the D-03 notice). This gap creates and changes no UI surface, so no item is lifted into this plan's must_haves. The a11y-name backstop (`getByRole(\"button\", { name: \"Delete\" })` after D-07) stays where it is; it is not this plan's to close."
    - "OUT OF SCOPE — CR-02 / verification truth #5 (`gate-e2e` is not a required branch-protection check; the `ci` run is red on every push because of 14 pre-existing e2e failures). This is a recorded PM hold and a human-verification item, not a gap. No task here adds `continue-on-error`, a known-failures allowlist, or a suite repair — and adding `continue-on-error` would additionally turn 19-11's unconditional invariant red."
    - "OUT OF SCOPE — WR-01 (`unlistListing`/`softDeleteListing`/`ConfirmDialog.onConfirm` missing try/catch) and WR-04 (~11 stale `<file>:<line>` citations). Both are real, both WARNING severity, both explicitly carried forward by 19-VERIFICATION rather than closed. No task here touches `src/`."
    - "OUT OF SCOPE — WR-03 / CR-01's closing paragraph (widening the PARSE half to walk `job.container.env` and `job.services.*.env` keys, and adding a `vars.` value scan). It is 'independently worth doing' and it is NOT in the gap's authoritative `missing:` block. The gap is closed by the RUNTIME half observing the real environment, which covers the `container.env` vector completely; Task 3 records in the header WHICH half covers it rather than leaving the reader to assume both do. Carried forward, named, not absorbed."
    - "OUT OF SCOPE — verification truths #1, #2, #3, #4, #6 and #7 are VERIFIED. No task here modifies `src/app/actions/listing.ts`, `src/components/listing/listing-card.tsx`, `src/lib/listing/create-signal.ts`, `src/app/(host)/host/listings/new/page.tsx` or `.../listings/page.tsx`."
    - "OUT OF SCOPE — the two `human_verification` items (the PM's reconfirmation of CI-01's closure state; a human read of the D-03 grid notice copy). These are for people. They are not tasks and are not turned into assertions."
    - "REQUIREMENTS.md — Task 3 corrects exactly ONE table cell as BOOKKEEPING, not as a code fix: `| HSURF-01 | Phase 19 | Gaps Found |`, which 19-VERIFICATION flags as drift in REQUIREMENTS.md itself (HSURF-01 was never a failed must_have in either round). A SECOND, adjacent drift is OBSERVED AND DELIBERATELY NOT CORRECTED: line ~237's `- [ ] **HSURF-01**` checkbox is unticked while HSURF-02 and CI-01 are ticked. The scope fence names the traceability row and only the traceability row; the checkbox is flagged here so it is not lost, and left for whoever owns REQUIREMENTS.md hygiene."
    - "API COVERAGE — the detector was run over this phase's plan scope and returned `detected: false`. No external API/SDK is integrated by this gap (the mail provider is the thing being kept OUT of the environment, not integrated), so no `COVERAGE.md` matrix is produced and no fabricated row is written."
    - "SCHEMA PUSH — no schema-relevant file is in scope (`drizzle/`, `src/lib/db/schema.ts` are untouched), so no `[BLOCKING]` schema-push task is injected. The migration tree still ends at `0029_listing_review_cascade.sql`."
    - "PACKAGE LEGITIMACY — zero packages are installed by this plan. `package.json` is not in `files_modified`; both new files import only `node:` builtins and the already-installed `vitest`. A package proposal inside this gap-closure run is a scope alarm, not a convenience."
---

<assumption_delta_decision>
The `assumption-delta` detector fired (`pluralization`, on the word "second" in the phase section's
"a second-order defect that IS in FitOut's code"). That specific hit is incidental prose, but the
phase DOES carry a real identity question and it is exactly this gap's subject, so it is answered
rather than waved off.

- **The noun that is now primary:** *the mail-credential refusal* — one control, asserted in two
  places over two different subjects.
- **Decision: `promote`.** The half that observes the **real process environment** becomes the
  primary assertion of D-14. The parse half is demoted from "the stronger shape" (its own comment's
  claim, written when the runtime half was believed live) to what it actually is: workflow-file
  hygiene over `env:` KEYS, which protects the NEXT run. Rationale: only one of the two can observe
  the environment the suite runs in, and the header's error was treating them as interchangeable
  halves of one coverage story. Adding the real check alongside while leaving the header's
  "two halves and both are live" framing intact would repeat the defect in a new place.
- **Invariant test accepted:** Task 2's no-drift invariant plus Task 1's build-blocking design test
  encode the generalized intent — the prefix is ONE value, and the refusal is asserted on the
  process environment, not on a workflow expression context.
</assumption_delta_decision>

<objective>
Make D-14's refusal real.

`gate-e2e`'s mail-credential refusal reads the provider key through a `${{ env.… }}` context expression
(the one occurrence of that token in `.github/workflows/`, at the step's `env:` key `MAIL_KEY_UNDER_TEST`)
— and the GitHub Actions **expression** context is
built exclusively from `env:` maps written in the workflow file. It never sees the process
environment, repository or organization secrets, `vars.*`, `container.env`, or `services.*.env`. So the
step can only fire on the one input `scripts/verify-workflows.mjs` already rejects the whole file for,
before the step could run. Net real coverage: **zero**. Worse, `ci.yml`'s header names
`container.env`/`services.*.env` as the parse half's carried-forward blind spot and leans on the runtime
half to cover it — and the runtime half cannot see those either, so a credential injected that way is
caught by **neither**, and the suite sends real mail (measured: 14 sends per run) with no failure of any
kind.

Purpose: this is a fail-closed privacy and cost control documented as active while being structurally
incapable of firing. It is the exact class the file warns about twice in its own prose — a description
that outlives what it describes — applied this time to a security control rather than a job list.

Output: a refusal that reads the environment the job actually has, sourcing its prefix from the one
file allowed to spell it; four parse-based invariants that make the two halves undriftable, each
watched red under a real reverted mutation; a build-blocking design test so the behaviour is re-proven
on every build instead of once in CI; and headers in both files that describe the control that exists.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VERIFICATION.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-11-PLAN.md

**Why `depends_on: ["19-11"]`.** Worktrees are OFF and this phase's plans form a strict chain on `dev`;
the wave number is this plan's position in that chain, not a concurrency claim. Its real artifact
dependency is 19-11, which last touched both `scripts/verify-workflows.mjs` (three `gate-e2e`
invariants) and `.github/workflows/ci.yml` (the header this plan corrects again, for a different
falsehood).

**Do not modify any of `19-01-PLAN.md` … `19-11-PLAN.md`.** They are executed. This plan's headline
must_have SUPERSEDES 19-01's D-14 truth in the register above; the plan file itself stays as shipped.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The refusal reads the environment the job actually has — end to end, proven red locally</name>

  <reversibility rating="costly">The mechanism swap (a shell test of an Actions expression → a Node
  script reading `process.env`) is one commit to revert. What makes it costly rather than freely
  reversible is the removal of the step's `MAIL_KEY_UNDER_TEST` env key: `scripts/verify-workflows.mjs`'s
  parse-scan comment and `ci.yml`'s header both describe that key by name, so a partial revert leaves a
  checker's comment explaining a carve-out for a key that no longer exists. Handled by removing the key
  and its two descriptions in the same plan (this task and Task 3) — both or neither, never one.</reversibility>

  <read_first>
    - `.github/workflows/ci.yml` — the `gate-e2e` mail-refusal step and the two ⚠ comment paragraphs
      directly above it (search for the step name `Refuse to run the suite with a live mail credential
      in the environment`). Read the whole block: the `[17-D28]` measurement paragraph, the paragraph
      explaining why the `env:` key is named `MAIL_KEY_UNDER_TEST`, the `run:` body, and the `env:` map.
      ⚠ Note the sentence in that second paragraph reading `runs, so it catches a violation about a
      minute before any browser starts.` — it is false and this task removes it.
    - `.github/workflows/ci.yml` — the `gate-e2e` job's `steps:` list in order, so you can see the
      refusal is FIRST after `npm ci` and stays there.
    - `scripts/verify-workflows.mjs` around the `MAIL_KEY` / `MAIL_KEY_PREFIX` declarations — read the
      comment above them in full. It states the rule this task must not break: the token is spelled ONCE
      there and never in a workflow file, because the cheapest audit of "this workflow cannot mail real
      people" is a count that a comment would permanently break.
    - `scripts/verify-workflows.mjs` — the parse-based mail-env-key scan and the ⚠ carve-out paragraph
      above it, so you know exactly which comment Task 3 will have to rewrite once the `env:` key is gone.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` § CR-01 in full,
      including the fix sketch and its three numbered consequences.
    - `tests/design/e2e-email-silence.test.ts` — the whole header. It is this repository's existing
      statement of the mail chain (`playwright.config.ts`'s `webServer.env`, `src/lib/email.ts` binding
      at module load, the `[email:dev]` fallback), the house voice for a mail assertion, and the
      precedent for recording watched reds inside the test file itself. Do not restate its chain; the
      new test asserts a different link and says so.
    - `tests/design/gitignore-baselines.test.ts` — the design suite's existing `child_process` idiom
      (shelling out from a design test, and the container caveat recorded around it).
    - `vitest.design.config.ts` — no `globalSetup`, no `setupFiles`, `environment: "node"`,
      `include: ["tests/design/**/*.test.ts", …]`. This is why the new test is DB-free and
      build-blocking, and why it must not reach for `tests/setup.ts`.
  </read_first>

  <behavior>
    - Spawned with a variable whose NAME begins with the mail-provider prefix present in the child's
      environment, `scripts/refuse-mail-credential.mjs` exits 1 and its stdout contains at least one
      line beginning `::error::`.
    - Spawned with no such variable, it exits 0 and its stdout names the prefix it scanned, the number
      of environment variables it scanned, and the source path it read the prefix from.
    - A variable whose name merely CONTAINS the prefix but does not START with it is not a hit —
      identical semantics to the parse half's `startsWith`, so the two halves agree on what a violation is.
    - Pointed at a source file that does not carry the prefix declaration, it exits 1 with an `::error::`
      naming that path — it never falls back to a default and never scans for an empty prefix.
    - On a hit, the offending variable NAMES appear in the output and no variable VALUE appears anywhere
      in it.
  </behavior>

  <files>scripts/refuse-mail-credential.mjs, tests/design/mail-credential-refusal.test.ts, .github/workflows/ci.yml</files>

  <action>
Write the test first, watch it fail for the right reason (the script does not exist yet), then write the
script, then rewire the workflow step.

**1. `tests/design/mail-credential-refusal.test.ts` (new).** Five assertions, one per `<behavior>` bullet.
Spawn the real script with `spawnSync` from `node:child_process`, invoking `process.execPath` with the
script path, `encoding: "utf8"`, and an explicitly constructed `env` object — never `process.env`
spread wholesale plus an override, because the property under test is what the CHILD's environment
contains. Derive the prefix inside the test by reading `scripts/verify-workflows.mjs` with the same
declaration pattern the script uses, and build the violating variable name as the prefix followed by
`_API_KEY`; do not type the provider's token into this file. Use a temporary file under the OS temp
directory for the missing-declaration case and delete it afterwards. Assert on `status` and on
`stdout`, and assert the hit case's stdout does NOT contain the sentinel value you set the variable to.
Header comment: why the file lives in `tests/design/` (it runs inside `npm run build` via `test:design`,
and pays no Postgres preflight), why it spawns rather than imports (the contract `ci.yml` consumes is a
process exit code plus stdout, not a function's return value), and the CR-01 history in one sentence.

**2. `scripts/refuse-mail-credential.mjs` (new).** Node ESM, `node:` builtins only, no dependencies.

  - Resolve the prefix source as `new URL("./verify-workflows.mjs", import.meta.url)` so the working
    directory never matters; accept an optional first positional argument overriding that path, used
    ONLY by the design test's missing-declaration case. `ci.yml` will invoke it with no arguments and
    Task 2's invariant asserts that.
  - Extract the prefix with the literal pattern `/^const MAIL_KEY_PREFIX = "([A-Z_]+)";$/m` — the exact
    characters Task 2 will assert this file and the checker share. If the pattern does not match, or the
    capture is empty, print an `::error::` line naming the source path and the pattern and `exit 1`.
    No default, no fallback, no scan for the empty string: an empty prefix matches everything or nothing
    and both are a green that means nothing.
  - Collect `Object.keys(process.env)` whose name starts with the prefix, sorted.
  - On a hit: print `::error::` lines stating that a live mail credential is present in this job's real
    process environment and not merely in the workflow's `env:` maps; that a full suite run with it set
    was measured at fourteen real outbound sends per run (finding `[17-D28]`); and that the fix is to
    remove it from the job, container or repository environment and never to weaken this check. Print
    the offending variable NAMES. ⚠ Never print a variable's VALUE — a credential echoed into a build
    log is a wider disclosure than the send this control exists to prevent. Then `exit 1`.
  - On no hit: print the prefix, the number of environment variables scanned, and the source path the
    prefix came from, and `exit 0` — this file keeps the repository's standing "it prints what it
    checked" property, so a green says what it looked at.
  - ⚠ Do not spell the prefix's own value anywhere in this file. It is READ, never copied; a copy is a
    second source of truth wearing the costume of a constant.
  - Header comment: what this file is, why the prefix is read rather than copied, and the one-paragraph
    CR-01 history — the previous incarnation of this control tested a GitHub Actions expression context
    that is built only from workflow/job/step `env:` maps, so it could not observe a credential from any
    other channel and added zero coverage over the parse half.

**3. Rewire the `gate-e2e` step in `.github/workflows/ci.yml`.**

  - Keep the step's `name:` byte-identical — Task 2's invariant and the file header both locate it by
    that string.
  - Do not move the step. It stays first after `npm ci`.
  - Replace the `run:` body with an invocation of `scripts/refuse-mail-credential.mjs` under `node`,
    with no arguments.
  - DELETE the step's entire `env:` map, including the `MAIL_KEY_UNDER_TEST` key and the Actions
    expression that made this control inert. That expression is the file's only occurrence of the
    mail-provider token, and removing it is what restores the repository's own cheapest audit to zero —
    Task 2 turns that audit into an invariant.
  - Rewrite the two ⚠ comment paragraphs above the step. Keep the `[17-D28]` measurement and the
    `instrumentation.ts` argument (the key IS the switch for that provider) unchanged — they are still
    true and still the reason this control exists. Falsify the `MAIL_KEY_UNDER_TEST` paragraph IN PLACE
    in the file's established style, using the marker text `FALSIFIED 2026-09-04 BY PLAN 19-12 (review
    finding CR-01)`, keeping its true historical content (the two sites were deliberately spelled
    differently, and each said so) and stating what replaced it: the step now runs a script that reads
    the job's real process environment and reads the prefix out of the checker that owns it, so there is
    no second spelling to keep apart. ⚠ The stale ordering sentence quoted in `<read_first>` item 1 is
    the one exception to "keep the old claim": DELETE it, do not preserve it behind the marker. A false
    sentence about WHEN a control fires is, to grep, indistinguishable from the claim itself, and grep is
    how this repository audits its own prose. Replace it with the fact that `gate-db-free` and `gate-e2e`
    declare no `needs:` — they `start in parallel, and GitHub cancels neither` when the other fails — so
    the parse half `protects the NEXT run, never the current one`, and this step is what protects THIS one.
  - ⚠ Do not spell the mail-provider prefix or the provider's variable name anywhere in this file, in a
    comment or otherwise.
  - Change no other job, step, `run:` command, `env:` key, service, container option or timeout.
  </action>

  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/mail-credential-refusal.test.ts</automated>
    <fails_when>non-zero exit; or the reporter prints `0 passed`; or fewer than 5 tests are reported passing; or the run reports `No test files found`</fails_when>
    <automated>node scripts/refuse-mail-credential.mjs</automated>
    <fails_when>non-zero exit in a clean shell; or stdout does not name both the number of environment variables scanned and the source path the prefix was read from</fails_when>
    <automated>node -e "const y=require('yaml');const d=y.parse(require('fs').readFileSync('.github/workflows/ci.yml','utf8'));const s=d.jobs['gate-e2e'].steps.find(x=>typeof x.name==='string'&&x.name.startsWith('Refuse to run the suite'));console.log(JSON.stringify({run:s&&s.run,env:s?(s.env===undefined?null:s.env):'STEP-MISSING'}));if(!s||s.env!==undefined||!String(s.run).includes('scripts/refuse-mail-credential.mjs'))process.exit(1)"</automated>
    <fails_when>non-zero exit; or the printed `env` is anything other than `null`; or the printed `run` does not name `scripts/refuse-mail-credential.mjs`; or it prints `STEP-MISSING`</fails_when>
    <automated>node -e "const y=require('yaml'),cp=require('child_process'),fs=require('fs');const norm=(t)=>{const d=y.parse(t);const s=d.jobs['gate-e2e'].steps.find(x=>typeof x.name==='string'&&x.name.startsWith('Refuse to run the suite'));if(s){delete s.run;delete s.env;}return JSON.stringify(d);};const head=cp.execSync('git show HEAD:.github/workflows/ci.yml',{encoding:'utf8',maxBuffer:100000000});const now=fs.readFileSync('.github/workflows/ci.yml','utf8');const same=norm(head)===norm(now);console.log(same?'IDENTICAL':'DIFFERS');if(!same)process.exit(1)"</automated>
    <fails_when>it prints `DIFFERS`, or exits non-zero — any parsed change outside the refusal step's own `run:` and `env:` means another job or step was touched. Run this BEFORE committing the task, while `HEAD` is still the pre-task commit.</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line begins with `FAIL`; or the HARD STOP banner is printed; or the summary line does not still report 44 invariants (baselines=11, ci=26, cross=7) — Task 1 adds no invariant, and a changed count here means the checker was edited out of turn</fails_when>
  </verify>

  <acceptance_criteria>
    - `scripts/refuse-mail-credential.mjs` exists, is git-tracked (`git ls-files -- scripts/refuse-mail-credential.mjs` is non-empty), and its `import`/`require` statements name only `node:` builtins.
    - Its source contains the exact text `^const MAIL_KEY_PREFIX = "([A-Z_]+)";$` and the string `verify-workflows.mjs`.
    - It contains no copy of the prefix VALUE: `node -e "const fs=require('fs');const p=/^const MAIL_KEY_PREFIX = \"([A-Z_]+)\";$/m.exec(fs.readFileSync('scripts/verify-workflows.mjs','utf8'))[1];const s=fs.readFileSync('scripts/refuse-mail-credential.mjs','utf8');console.log('occurrences='+s.split(p).length-1);process.exit(s.includes(p)?1:0)"` exits 0 and prints `occurrences=0`.
    - `tests/design/mail-credential-refusal.test.ts` exists and reports exactly 5 passing tests under `vitest.design.config.ts`.
    - The `gate-e2e` refusal step declares NO `env:` map at all, and its `run:` invokes `scripts/refuse-mail-credential.mjs` with no arguments.
    - The step's `name:` is byte-identical to its value at `HEAD`, and it is still the first run command after `npm ci` in the job's step list.
    - `grep -c "browser starts" .github/workflows/ci.yml` returns `1` after this task — the step-comment occurrence is gone, the header occurrence remains and is Task 3's.
    - `git grep -c -- "MAIL_KEY_UNDER_TEST" .github/workflows/ci.yml` returns at most `1` — only the header's historical mention (Task 3's to falsify) survives; the live `env:` key is gone.
    - The parsed before/after comparison prints `IDENTICAL`.
    - The watched reds are recorded verbatim in the SUMMARY: (a) the design test's failure output before the script existed, and (b) the script's own stdout and exit code when run locally with a violating variable set, captured line for line rather than paraphrased.
  </acceptance_criteria>

  <done>`gate-e2e`'s first step after `npm ci` reads the container's real process environment, refuses with an `::error::` line and a non-zero exit when a variable named for the mail provider is present, sources the prefix from the one file allowed to spell it, and a build-blocking design test proves both directions on a machine with no Docker and no browser.</done>
</task>

<task type="auto">
  <name>Task 2: Four invariants that make the two halves undriftable — each watched RED</name>

  <reversibility rating="reversible">Four `check()` calls and three module-scope constants in one
  script; deleting them restores today's behaviour exactly. No workflow behaviour changes.</reversibility>

  <read_first>
    - `scripts/verify-workflows.mjs` — the `MAIL_KEY` / `MAIL_KEY_PREFIX` declarations and their full
      comment, plus the `CI_E2E_JOB` / `CI_E2E_CONTEXT` constants and the comment explaining why they
      are separate. The new constants go beside these and must read as siblings.
    - `scripts/verify-workflows.mjs` — the `check`, `hardStop`, `stepsOf` and `runsOf` helpers and the
      `counts` map `check` increments, so the new invariants land in the right section total.
    - `scripts/verify-workflows.mjs` — the whole `gate-e2e` block: the hard stop for a missing job, the
      display-name check, and 19-11's three checks (`runs the functional project BY NAME`,
      `is unconditional`, `seeds the demo catalogue BEFORE it runs the suite`). The third of those is
      the index-comparison idiom Invariant C copies; note it binds `e2eRuns` once and prints indices.
    - `scripts/verify-workflows.mjs` — the parse-based mail-env-key scan and its ⚠ carve-out paragraph.
      ⚠ THIS TASK DOES NOT TOUCH EITHER. The scan's behaviour is unchanged and Task 3 owns the comment.
    - `scripts/verify-workflows.mjs` — the `cross` section's snapshot-flag carrier check near the end of
      the file: `readdirSync(WORKFLOW_DIR)`, the per-file `readFileSync`, and the shape of its `check()`
      call. Invariant D is a sibling of exactly that idiom and belongs beside it.
    - `scripts/refuse-mail-credential.mjs` as written by Task 1, in full — Invariant B asserts on its
      source text, so the assertion must match what is actually there.
    - `.github/workflows/ci.yml` — the `gate-e2e` refusal step as rewired by Task 1.
  </read_first>

  <files>scripts/verify-workflows.mjs</files>

  <action>
Add three module-scope constants beside the existing mail constants, then four `check()` calls. Change
nothing else in the file — in particular do not edit the parse scan, the addressing rule, the
supply-chain scan, any `baselines` invariant, or any comment (Task 3 owns the comments).

**Constants.** `MAIL_KEY_PREFIX_DECL` holding the regular expression literal
`/^const MAIL_KEY_PREFIX = "([A-Z_]+)";$/m` — the exact pattern `scripts/refuse-mail-credential.mjs`
reads the declaration with; `MAIL_REFUSAL_SCRIPT` holding `scripts/refuse-mail-credential.mjs`; and
`CI_E2E_MAIL_STEP` holding the refusal step's exact `name:` string. Comment on the first: this pattern
is the seam between the two halves, and the invariants below assert the pattern against the declaration
and the declaration against the script that reads it, so neither can move without a named red.

**Invariant A — the refusal reads the REAL process environment** (`ci` section, in the `gate-e2e` block,
immediately after 19-11's seeds-before-suite check). Locate the step in `stepsOf(e2e)` by exact `name`
equality with `CI_E2E_MAIL_STEP`. True when the step exists, its `run` includes `MAIL_REFUSAL_SCRIPT`,
its `run` contains no `${{` expression, and the step declares no `env` map at all. Evidence prints the
step's index, its `run` text and its `env` value. Comment: state the measured defect this replaces — an
Actions `env` expression is built only from workflow/job/step `env:` maps, so the previous form of this
step could fire only on the exact input the parse scan already rejects the file for, and could not
observe `container.env` at all.

**Invariant B — the two halves cannot drift** (`ci` section, next). Read this script's own source via
`import.meta.url` and the refusal script's source. True when all three hold: applying
`MAIL_KEY_PREFIX_DECL` to this file's own source yields exactly `MAIL_KEY_PREFIX`; the refusal script's
source contains `MAIL_KEY_PREFIX_DECL.source` verbatim; and the refusal script's source does NOT contain
`MAIL_KEY_PREFIX`'s value anywhere (a copy). Evidence names which of the three conjuncts held, in the
existing evidence voice — the prefix itself may be printed, as the neighbouring scan already prints it.

**Invariant C — the refusal runs before anything boots** (`ci` section, next), compared by INDEX over
`e2eRuns` in the shape 19-11's ordering check established. Find the index of the run command naming
`MAIL_REFUSAL_SCRIPT`, and the indices of the migrate, seed and Playwright commands. True when the
refusal index is non-negative, every run command before it is `npm ci`, and the refusal index is
strictly less than each of the other three. Evidence prints all four indices and the list length.
Comment: presence is not the property — the whole value of this control is that it costs seconds
instead of a suite of real mail, and a refusal that runs after the suite has already sent it.

**Invariant D — the workflow directory spells no mail-provider token** (`cross` section, beside the
snapshot-flag carrier check, reusing that block's `workflowFiles` enumeration and per-file read). True
when the raw text of every file in the workflow directory contains zero occurrences of
`MAIL_KEY_PREFIX`. Evidence prints the files scanned and any file:count pairs that are non-zero.
Comment: this is the repository's own cheapest audit of "this workflow cannot mail real people", made
machine-checked in the one file allowed to name the token, exactly as the snapshot flag's carrier check
is. State the measurement: it was RED at the start of plan 19-12, because `ci.yml` carried one
occurrence in the inert step's `env:` value — this invariant is what stops that occurrence coming back.

**Then prove all four by watching them red.** For each mutation: apply it, run the script, capture the
exact FAIL line, revert, and confirm the script is green again before applying the next.

  1. replace the refusal step's `run:` in `ci.yml` with a trivial `echo` — Invariant A;
  2. re-add an `env:` map to that step — Invariant A again, for the other conjunct;
  3. reformat the `MAIL_KEY_PREFIX` declaration in this script (single quotes, or extra spacing) so the
     pattern no longer matches — Invariant B;
  4. edit the pattern inside `scripts/refuse-mail-credential.mjs` to a different but equivalent regular
     expression — Invariant B again, for the shared-source conjunct;
  5. move the refusal step after the seed step — Invariant C;
  6. add a comment line to `baselines.yml` containing the prefix — Invariant D.

After the last revert, confirm `git status --porcelain .github/workflows/ scripts/refuse-mail-credential.mjs`
produces no output. A mutation is a measurement, and a measurement left in the tree is a defect. Record
all six FAIL lines verbatim in the SUMMARY; a paraphrase is not evidence that the check fired.
  </action>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line begins with `FAIL`; or the HARD STOP banner is printed; or the summary line does not read `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8)` — a count of 44 means the checks were written but never registered through `check(...)`</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=ci</automated>
    <fails_when>non-zero exit; or the printed output does not contain all three new `gate-e2e` invariant names; or the final line does not report `ci=29`</fails_when>
    <automated>node scripts/verify-workflows.mjs --section=cross</automated>
    <fails_when>non-zero exit; or the final line does not report `cross=8`</fails_when>
    <automated>git status --porcelain .github/workflows/ scripts/refuse-mail-credential.mjs</automated>
    <fails_when>any output at all — a mutation used to watch a red was left in the tree</fails_when>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-workflows.mjs` declares `MAIL_KEY_PREFIX_DECL`, `MAIL_REFUSAL_SCRIPT` and `CI_E2E_MAIL_STEP` at module scope, beside the existing mail constants.
    - Three new `check()` calls sit inside the `ci` section's `gate-e2e` block, after 19-11's seeds-before-suite check and before the `gate-visual` hard stop; one new `check()` call sits in the `cross` section beside the snapshot-flag carrier check.
    - Invariant A's `ok` expression requires ALL of: the step exists by exact name, its `run` names the refusal script, its `run` contains no `${{`, and its `env` is `undefined` — not any one of them alone.
    - Invariant B's `ok` expression requires all three conjuncts (pattern-matches-declaration, script-shares-pattern-source, script-holds-no-copy), and its evidence names which conjunct failed.
    - Invariant C compares INDICES and requires the refusal index to be non-negative and strictly less than the migrate, seed and Playwright indices — a missing refusal step is red, not green.
    - Invariant D counts occurrences across every file in the workflow directory and its evidence lists the files it scanned.
    - `node scripts/verify-workflows.mjs` prints `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8)`.
    - Six FAIL lines — one per mutation listed in the action — are recorded verbatim in the SUMMARY, each with confirmation the script returned green after the revert.
    - `git status --porcelain .github/workflows/ scripts/refuse-mail-credential.mjs` is empty at the end of this task.
    - The parse-based mail-env-key scan's code is byte-unchanged: `git diff HEAD -- scripts/verify-workflows.mjs` shows no modification inside its `for` loops or its `check()` call.
  </acceptance_criteria>

  <done>Detaching the runtime refusal — by replacing what it runs, by re-introducing an Actions-expression `env:` map, by drifting either half's spelling of the prefix, by moving the step after the suite, or by pasting the provider's token back into a workflow file — now fails the checker that runs on every push, and each of those six was watched failing rather than reasoned about.</done>
</task>

<task type="auto">
  <name>Task 3: Both files describe the control the tree now has — and one bookkeeping cell</name>

  <reversibility rating="reversible">Comment text in two files and one table cell in a planning
  document. No job, step, command, check or code path changes, so behaviour is identical before and
  after.</reversibility>

  <read_first>
    - `.github/workflows/ci.yml` — the header block from the note that begins `⚠ THE LIST ABOVE PREDATES
      JOBS 4 AND 5` through the end of the D-14 bullet. Read every line. Three passages in it are false
      or stale after Tasks 1 and 2:
        (a) the note's closing sentence, which claims every invariant below is asserted `on job 1, before
            any` / `browser starts.` (it spans two comment lines);
        (b) the D-14 bullet's RUNTIME-half description, which says the step reads the key through the
            `MAIL_KEY_UNDER_TEST` `env:` key — a key Task 1 deleted;
        (c) the D-14 bullet's PARSE-half sentence ending `on job 1, about a minute into a run.`, and the
            ⚠ paragraph after it, which names `container.env`/`services.*.env` as a carried-forward hole
            while implying the runtime half covers it.
    - `.github/workflows/ci.yml` — the two passages this file has ALREADY falsified in place, both
      carrying the marker form `⚠ FALSIFIED 2026-09-04 BY PLAN 19-11 (review finding WR-05).` and
      `⚠ QUANTIFIER FALSIFIED 2026-09-04 BY PLAN 19-11 (WR-05).` This is the house style: keep the old
      claim, mark it with the date and the plan, state what replaced it. Do not invent a new style.
    - ⚠ `.github/workflows/ci.yml` — the paragraph explaining that the snapshot-update flag is
      deliberately not spelled anywhere in this file, comments included, because the cheapest audit of
      that rule is a count a comment would permanently break. Do not introduce that token while editing
      nearby prose. The same rule now binds the mail-provider token by Invariant D.
    - `scripts/verify-workflows.mjs` — the comment block above the parse-based mail scan, in full. Two
      things in it are now false: the sentence claiming the scan `fires about a minute before any
      browser starts`, and the whole ⚠ carve-out paragraph describing `gate-e2e`'s step as naming
      `MAIL_KEY_UNDER_TEST` and reading the provider key through a `${{ env.… }}` context expression in
      its VALUE. Neither is true after Task 1.
    - `scripts/verify-workflows.mjs` — the four invariants added by Task 2, so the header's list names
      what the parser actually asserts rather than what this plan intended.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` § CR-01,
      consequence 3 — the parallel-jobs argument, stated with its evidence.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VERIFICATION.md` — the
      Requirements Coverage note recording that REQUIREMENTS.md's `HSURF-01 | Phase 19 | Gaps Found`
      row is stale, flagged rather than silently corrected.
    - `.planning/REQUIREMENTS.md` — the traceability table's three Phase 19 rows.
  </read_first>

  <files>.github/workflows/ci.yml, scripts/verify-workflows.mjs, .planning/REQUIREMENTS.md</files>

  <action>
Three edits. In the two source files, nothing outside a comment line may change.

**1. `.github/workflows/ci.yml`'s header.**

  - The `⚠ THE LIST ABOVE PREDATES JOBS 4 AND 5` note: its closing timing claim is FALSE and is DELETED
    rather than preserved — a false sentence about when a control fires reads, to grep, exactly like the
    claim itself. Replace it with the truth: these invariants run on `gate-db-free`, which declares no
    `needs:` and neither does `gate-e2e`, so the two `start in parallel, and GitHub cancels neither` when
    the other fails. Note in the same breath what that means: a parse-based invariant `protects the NEXT
    run, never the current one`.
  - Extend the `gate-e2e` invariant list from four entries to seven, adding Task 2's three `ci` checks in
    the same register as the four beside them: the refusal asserted to read the real process environment
    with no Actions expression and no `env:` map; the two halves asserted undriftable by pattern and by
    shared source; and the refusal asserted to precede migrate, seed and the suite by INDEX. Attribute
    each to plan 19-12, as the existing entries attribute themselves to 19-08 and 19-11.
  - Rewrite the D-14 bullet. Falsify the RUNTIME-half description IN PLACE with the marker text
    `FALSIFIED 2026-09-04 BY PLAN 19-12 (review finding CR-01)`, keeping the old claim and stating what
    replaced it: the step runs a committed script that reads the job's real process environment and
    reads the prefix out of the checker that owns it, so the two sites no longer hold two spellings.
  - The bullet must now state, in these words, which half covers what:
      · `the RUNTIME half is the one that observes container.env` — a container-level `env:` block
        reaches every step's process environment, which is exactly what the step now reads;
      · `services.*.env` sets variables only inside the service container, which runs no application
        code and is therefore not a mail-send vector;
      · the PARSE half still walks neither key — review finding WR-03, carried forward and NOT closed by
        plan 19-12 — and its job is workflow-file hygiene over `env:` KEYS, which is a different question
        from what the job's process holds.
    Delete the ⚠ paragraph's implication that this hole is covered by nobody; it now names the covering
    half explicitly, and names what genuinely remains open.
  - Add one entry to the invariant list for Invariant D: across the workflow directory the mail-provider
    token appears zero times, asserted in the checker rather than by a human running a count, and RED at
    plan 19-12's start.
  - ⚠ Do not spell the mail-provider prefix or the provider's variable name anywhere in this file. Do not
    introduce the snapshot-update flag's spelling either.

**2. `scripts/verify-workflows.mjs`'s mail-scan comment block.** Comment-only.

  - Delete the false timing sentence, as in edit 1, and replace it with the parallel-jobs fact and its
    consequence — this scan protects the next run; the runtime half protects this one.
  - Rewrite the ⚠ carve-out paragraph. Falsify it in place with the same marker text, keeping the
    historical claim (the step deliberately named its key differently so this scan would not fire on a
    correct file) and stating what replaced it: the step declares no `env:` map at all, so there is
    nothing here to carve out, and Invariants A/B/C are what now assert the runtime half.
  - State plainly what this scan is and is not: it answers whether a workflow FILE declares a
    provider-named `env:` KEY. It does not and cannot answer what the job's process environment holds.
    Both questions are now asked, by different assertions, and neither substitutes for the other.
  - Change no executable line in this file.

**3. `.planning/REQUIREMENTS.md` — BOOKKEEPING, NOT A CODE FIX.** In the traceability table, change the
status cell of the row whose first cell is `HSURF-01` from `Gaps Found` to `Complete`, matching the
HSURF-02 and CI-01 rows. This corrects documentation drift that 19-VERIFICATION flagged rather than
silently fixed: HSURF-01 was never a failed must_have in either verification round; both rounds' gaps
were against HSURF-02 and CI-01 only. Change nothing else in the file — in particular do NOT touch the
CI-01 row, and do NOT tick the `- [ ] **HSURF-01**` checkbox in the requirement body above; that is a
second, adjacent drift recorded in this plan's assumptions and deliberately left for whoever owns
REQUIREMENTS.md hygiene. Say in the commit message that this edit is bookkeeping.
  </action>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line begins with `FAIL`; or the HARD STOP banner is printed; or the summary line does not still read `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8)`</fails_when>
    <automated>grep -c "browser starts" .github/workflows/ci.yml scripts/verify-workflows.mjs || true</automated>
    <fails_when>either printed line ends in a count other than `:0` — the false timing claim survives somewhere in the two files it was written in</fails_when>
    <automated>grep -c "FALSIFIED 2026-09-04 BY PLAN 19-12 (review finding CR-01)" .github/workflows/ci.yml scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or either printed line ends in `:0` — a correction that is not marked as a correction is a silent rewrite, which this file's own style forbids</fails_when>
    <automated>grep -c "the RUNTIME half is the one that observes container.env" .github/workflows/ci.yml</automated>
    <fails_when>the printed count is `0`, or the command exits non-zero — the header does not name which half closes the blind spot, which is half of what the gap asked for</fails_when>
    <automated>grep -c "start in parallel, and GitHub cancels neither" .github/workflows/ci.yml scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit, or either printed line ends in `:0` — the parallel-jobs correction is missing from one of the two files that carried the false claim</fails_when>
    <automated>git diff -U0 -- .github/workflows/ci.yml scripts/verify-workflows.mjs</automated>
    <fails_when>any added or removed line — excluding `@@` hunk headers and the `+++`/`---` file headers — whose first non-whitespace character after the leading `+`/`-` is neither `#` nor `/`. This edit is documentation-only; a changed YAML or JavaScript line means a job or a check was touched.</fails_when>
    <automated>grep -n "^| HSURF-01 \|^| HSURF-02 \|^| CI-01 " .planning/REQUIREMENTS.md</automated>
    <fails_when>the HSURF-01 row still reads `Gaps Found`, or the CI-01 or HSURF-02 row's status text differs from its value at `HEAD`</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "browser starts" .github/workflows/ci.yml scripts/verify-workflows.mjs` reports `:0` for both files (it reported `2` and `1` at this plan's start).
    - `.github/workflows/ci.yml` contains the literal `the RUNTIME half is the one that observes container.env`.
    - `.github/workflows/ci.yml` and `scripts/verify-workflows.mjs` each contain the literal `start in parallel, and GitHub cancels neither` and the literal `protects the NEXT run, never the current one`.
    - Each of the two source files contains at least one occurrence of `FALSIFIED 2026-09-04 BY PLAN 19-12 (review finding CR-01)`.
    - The `gate-e2e` invariant list in `ci.yml`'s header enumerates SEVEN checks and attributes the three new ones to plan 19-12, and a separate entry records the workflow-directory token count as an invariant.
    - The D-14 bullet states that `services.*.env` variables live only inside the service container and that the PARSE half walks neither container nor service `env:` — naming WR-03 as carried forward, not closed.
    - `git diff -U0` over the two source files shows only comment lines changed.
    - `node scripts/verify-workflows.mjs` still prints `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8)`.
    - Invariant D still passes, proving no provider token was introduced into a workflow file by this prose edit.
    - `.planning/REQUIREMENTS.md`'s HSURF-01 traceability row reads `Complete`; the CI-01 and HSURF-02 rows and the `- [ ] **HSURF-01**` checkbox are byte-unchanged from `HEAD`.
  </acceptance_criteria>

  <done>A reader auditing this repository's mail control from either file's own prose learns which half observes the process environment, which half observes the workflow text, which one protects the run in front of them, and what is still known to be uncovered — and the requirement table no longer reports a gap that never existed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository / organization / container environment → the `gate-e2e` job's process | A live mail credential reaching this job was MEASURED at fourteen real outbound sends per suite run, to real addresses, on every pull request. This is the boundary the whole plan is about. |
| a pull request → the CI workflow file | Contributor-authored YAML crosses here. The parse-based half inspects the FILE; it cannot inspect the process the file produces. |
| `scripts/verify-workflows.mjs` ↔ `scripts/refuse-mail-credential.mjs` | Two files that must agree on one value. Any second spelling is a silent divergence with no observer. |
| the refusal's own output → a public build log | A control that detects a credential is one line away from disclosing it more widely than the send it prevented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-12-01 | Information Disclosure | `gate-e2e` process environment (D-14, CR-01) | high | mitigate | Task 1 replaces the Actions-expression test with `scripts/refuse-mail-credential.mjs`, which reads `process.env` — the same source `env |` prints, and the only place a container-level or runner-level key is observable. Task 2's Invariant A asserts the step keeps that shape; Task 1's design test asserts both directions on every build. |
| T-19-12-02 | Tampering | drift between the two halves of D-14 | high | mitigate | The prefix is READ, never copied. Invariant B asserts three conjuncts: the declaration matches the pattern, the refusal script shares that pattern's source text, and the refusal script holds no copy of the value. Watched red under two separate reformatting mutations. |
| T-19-12-03 | Information Disclosure | the refusal's own stdout | medium | mitigate | The script prints offending variable NAMES and never VALUES; the design test asserts the sentinel value it sets does not appear in the output. Recorded as a prohibition so a later "helpful" debug line cannot be added innocently. |
| T-19-12-04 | Repudiation | `ci.yml` and `verify-workflows.mjs` header prose | medium | mitigate | The header's own text is why this blind spot survived a full verification round: it named a hole and leaned on a half that could not cover it. Task 3 states which half covers what, deletes the false timing claim from all three places it was written, and marks each correction in the file's established falsification style. |
| T-19-12-05 | Information Disclosure | a mail key spelled in `services.*.env` | low | accept | Service-container `env:` reaches only the postgres container, which runs no application code, so it is not a mail-send vector. The PARSE half still walks neither container nor service `env:` (WR-03). Accepted with the residual NAMED in the header by Task 3 rather than left implicit, and with the real vector (`container.env`) now covered by the runtime half. |
| T-19-12-06 | Spoofing | an env key that reaches for the provider through `vars.` under an unrelated NAME (CR-01's unnamed second gap) | low | accept | The runtime half is name-based and would miss it; the parse half matches `secrets.` literals, not `vars.`. Out of the gap's authoritative `missing:` block and carried forward with WR-03 rather than absorbed. Recorded here so the next reader does not have to rediscover it. |
| T-19-12-07 | Elevation of Privilege | `gate-e2e` as a merge gate (D-15, CR-02) | medium | transfer | The gate reports but cannot block: branch protection returns a plan-upgrade refusal on this repository. Transferred to the PM as a recorded visibility/billing decision and re-raised as a human-verification item. No task here works around it, and none adds `continue-on-error` — which would also turn 19-11's unconditional invariant red. |
| T-19-12-SC | Tampering | npm/pip/cargo installs and third-party actions | high | mitigate | This plan installs nothing and adds no `uses:` entry. Both new files import only `node:` builtins and the already-installed `vitest`. `19-RESEARCH.md`'s Package Legitimacy Audit records zero packages for this phase, so no legitimacy checkpoint is required; a package proposal inside this gap-closure run is a scope alarm. |
</threat_model>

<verification>
1. `npx vitest run --config vitest.design.config.ts tests/design/mail-credential-refusal.test.ts` — 5 passed.
2. `node scripts/verify-workflows.mjs` — exit 0, `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8)`.
3. `grep -c "browser starts" .github/workflows/ci.yml scripts/verify-workflows.mjs` — `:0` on both.
4. The parsed before/after comparison of `ci.yml` prints `IDENTICAL` with only the refusal step's `run:`/`env:` excluded.
5. The SUMMARY records EIGHT observed reds verbatim: the design test before the script existed, the script's local refusal output, and one FAIL line per Task 2 mutation.
</verification>

<success_criteria>
- `gate-e2e` refuses to run when a variable named for the mail provider is present in the job's REAL process environment — including one injected through `container.env` — with an `::error::` line, before migrate, before seed, before the suite.
- The mail-provider prefix is spelled exactly once in workflow-facing code, read by the runtime half, and asserted undriftable by a parse-based invariant that was watched red.
- Zero occurrences of the provider token across `.github/workflows/`, asserted by the checker rather than by a human running a count.
- Both files' prose names which half observes the process, which half observes the file, and which one protects the run in front of the reader.
- No job, step, run command, service, container option or timeout in `ci.yml` changed, and `gate-price-parity` is untouched.
</success_criteria>

<artifacts_this_phase_produces>
This plan's slice of the phase-wide index in `19-01-PLAN.md`. Every symbol below is created or first
named by plan 19-12.

**New files**
| Path | What it is |
|---|---|
| `scripts/refuse-mail-credential.mjs` | The runtime half of D-14, rebuilt: reads the job's real process environment, reads the prefix from `scripts/verify-workflows.mjs`, `::error::` + `exit 1` on a hit or on an unreadable prefix. Node ESM, `node:` builtins only. |
| `tests/design/mail-credential-refusal.test.ts` | Build-blocking, DB-free proof of both directions of that refusal, spawning the real script. 5 tests. |

**New module-scope constants** (`scripts/verify-workflows.mjs`)
| Symbol | What it holds |
|---|---|
| `MAIL_KEY_PREFIX_DECL` | The regular expression the refusal script reads the prefix declaration with — the seam between the two halves. |
| `MAIL_REFUSAL_SCRIPT` | `scripts/refuse-mail-credential.mjs` |
| `CI_E2E_MAIL_STEP` | The refusal step's exact `name:` string, which is how the invariants locate it. |

**New workflow invariants** (`scripts/verify-workflows.mjs`; `ci` 26 → 29, `cross` 7 → 8, total 44 → 48)
- A — `gate-e2e`'s mail refusal reads the REAL process environment: it runs the refusal script, carries no `${{` expression, and declares no `env:` map. (`ci`)
- B — the two halves cannot drift: the declaration matches the shared pattern, the refusal script carries that pattern's source text, and it holds no copy of the value. (`ci`)
- C — the refusal precedes migrate, seed and the suite, compared by INDEX. (`ci`)
- D — across the workflow directory, the mail-provider token appears zero times. (`cross`)

**Removed** — `gate-e2e`'s `env:` key `MAIL_KEY_UNDER_TEST` and its `${{ env.… }}` value: the only
occurrence of the provider token in `.github/workflows/`, and the whole of the inert control. The
name survives only inside falsification markers in both files' prose.

**New verify commands entering the phase's proven set**
- `npx vitest run --config vitest.design.config.ts tests/design/mail-credential-refusal.test.ts`
- `node scripts/refuse-mail-credential.mjs`
- `node scripts/verify-workflows.mjs --section=cross`
- The parsed before/after `ci.yml` comparison with one step's `run:`/`env:` excluded, as a
  "nothing else moved" gate.

**Deliberately NOT produced:** no new runtime dependency, no `package.json` change, no `drizzle/`
migration, no change to any file under `src/`, no widening of the parse half to `container.env` /
`services.*.env` (WR-03, carried forward), no `continue-on-error`, no branch-protection change.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-12-SUMMARY.md` when done.

It must record, verbatim:
- the design test's failure output from before `scripts/refuse-mail-credential.mjs` existed;
- the script's own stdout and exit code when run locally with a violating variable set, line for line;
- one FAIL line per Task 2 mutation (six), each with confirmation the checker returned green after the revert;
- the `node scripts/verify-workflows.mjs` summary line before (44) and after (48);
- the `grep -c "browser starts"` counts before (`ci.yml:2`, `verify-workflows.mjs:1`) and after;
- the exact wording used for each falsification marker, so a later reader can find them by search;
- that WR-01, WR-03, WR-04 and CR-02 are carried forward and were NOT closed by this plan.
</output>
