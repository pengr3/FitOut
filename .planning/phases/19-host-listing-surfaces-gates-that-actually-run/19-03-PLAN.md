---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/components/listing/listing-card.tsx
  - tests/design/listing-card-merge-order.test.ts
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt
autonomous: true
requirements: [HSURF-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CardFooter at src/components/listing/listing-card.tsx:434 carries gap-2 mt-auto flex-wrap, appended in that order, and the footer band sits flush with the card's bottom edge at every band (D-05)."
    - "Card at :352 is byte-unchanged at gap-0 pt-0 — a full-height utility was NOT added, because the grid wrapper sets no alignment and the items already stretch (D-05)."
    - "The Delete control renders as an icon-only button whose accessible name is still Delete, and its ConfirmDialog is unchanged (D-07)."
    - "src/components/ui/card.tsx and src/components/ui/button.tsx are byte-unchanged — every class lands at the call site (D-04)."
    - "Both HSURF-01 guards are GREEN at 320x800, 700x900 and 1280x900, and their pre-fix red messages are the same assertions now passing."
    - "e2e/overflow-320.spec.ts still passes on /host/listings, including its 24px expectTargets scan — icon-sm is 28px, and the 44px bar was NOT imported (D-07)."
    - "A standing check proves cn() keeps all of p-4, gap-2, mt-auto and flex-wrap, so the append could not have deleted an earlier class (D-06)."
    - "loading (list-collection) — the shipped loading.tsx composes PageHeader plus CardGridSkeleton with role=status, reading the same RESULT_GRID_GAP the real grid uses; unchanged by this phase, and the Create listing CTA stays deliberately omitted."
    - "populated (list-collection) — cards in a row end at the same bottom edge WITH the footer flush to the card bottom, at 320x800, 700x900 and 1280x900."
    - "overflow (list-item footer control cluster) — [data-slot=card-footer] satisfies scrollWidth at most clientWidth at all three bands via flex-wrap at the call site; the controls are clipped rather than spilled, so the assertion measures the footer's own box."
    - statement: "getByRole(\"button\", { name: \"Delete\" }) still resolves on /host/listings after the control goes icon-only; no test locates this control by its text today, so nothing currently proves the name survives."
      verification: backstop
  artifacts:
    - src/components/listing/listing-card.tsx
    - tests/design/listing-card-merge-order.test.ts
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt
  key_links:
    - "cn(base, className) places the call-site string LAST (ui/card.tsx:14-18, :76, :86-91), which is the position tailwind-merge keeps — the structural reason D-06's hazard is absent here."
    - "listing-card.tsx has exactly one call site, (host)/host/listings/page.tsx, so the search grid cannot regress; / renders SearchResultCard instead."
    - "size=icon-sm resolves to size-7 = 28px, the same height as its h-7 siblings, so the footer row's baseline does not move and the 24px expectTargets scan still clears."
---

<objective>
Land HSURF-01's fix: three class strings and one control shape, all at the call site, and turn plan
19-02's two red guards green.

Purpose: the footer band floats mid-card because `Card` is a top-packed column with no growing child,
and the four controls are clipped rather than spilled because `Button` can neither shrink nor wrap
inside an `overflow-hidden` card. These are two defects, and fixing either does not fix the other.
Output: an aligned grid whose action controls are all fully visible and pressable inside their own
card, at 320px and at both grid bands, with the guards that prove it now passing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-PATTERNS.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-UI-SPEC.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the call-site edit — footer flush, footer wraps, Delete goes icon-only</name>
  <files>src/components/listing/listing-card.tsx</files>
  <read_first>
    - `src/components/listing/listing-card.tsx:38` — the existing `lucide-react` import (`PencilIcon, CalendarClock, CheckCircle2, type LucideIcon`); this is where the trash icon joins.
    - `src/components/listing/listing-card.tsx:270-279` — the docblock recording that this component has ONE call site and that `/` renders `SearchResultCard` instead.
    - `src/components/listing/listing-card.tsx:352` — `<Card className="gap-0 pt-0">`. **Do not change it.**
    - `src/components/listing/listing-card.tsx:368` — `<CardContent className="space-y-1 py-4">`. **Do not change it.**
    - `src/components/listing/listing-card.tsx:433-480` — the `{hasActions && …}` fork, `<CardFooter className="gap-2">`, the Edit and Availability buttons, the `div.ml-auto`, the Unlist `ConfirmDialog` and the Delete `ConfirmDialog` with its `confirmLabel="Delete listing"` / `confirmVariant="destructive"`.
    - `src/components/ui/card.tsx:14-18`, `:76`, `:86-91` — `cn(base, className)` with the call-site string last, and `Card`'s base carrying `flex flex-col`, `overflow-hidden` and `has-data-[slot=card-footer]:pb-0`. **Read only. Never edit (D-04 / D-129.)**
    - `src/components/ui/button.tsx:74`, `:104`, `:117-119` — the base's `shrink-0` and `whitespace-nowrap`, `size="sm"` = `h-7`, and `size="icon-sm"` = `size-7`. **Read only.**
    - `src/app/(host)/host/listings/page.tsx:180` — the grid wrapper, `cn(RESULT_GRID_GAP, "grid sm:grid-cols-2 lg:grid-cols-3")`, with no alignment declared.
    - `src/components/listing/photo-lightbox.tsx:365-373` — the shipped icon-only-button idiom: an `aria-hidden` icon plus an `sr-only` span carrying the name.
    - `e2e/overflow-320.spec.ts:3144-3150` — the written `touch: []` argument. **The bar for this cluster is 24px, and asserting 44 would be red against reviewed code.**
    - `19-RESEARCH.md § 2.2`, `§ 2.3`, `§ 2.5`; `19-UI-SPEC.md § Surface Contract B`.
  </read_first>
  <behavior>
    - After the change, at 320x800, 700x900 and 1280x900: for every rendered card, `cardBottom - footerBottom` is at most 1px (guard A clause 2 in `e2e/host-listing-grid.spec.ts` goes green).
    - At the same three bands: `[data-slot="card-footer"]`'s `scrollWidth` is at most its `clientWidth`, with an empty offenders list (guard B goes green).
    - `getByRole("button", { name: "Delete" })` still resolves on `/host/listings` — the visible label is gone, the accessible name is not.
    - The Delete confirmation dialog still opens, still reads "Delete this listing?" and still confirms with "Delete listing" at `confirmVariant="destructive"`.
    - The Unlist control still renders its visible "Unlist" label on a published listing, and renders not at all on a draft.
    - `e2e/overflow-320.spec.ts`'s `/host/listings` row still passes, including its 24px hit-area scan.
  </behavior>
  <action>
    Three edits, all in `src/components/listing/listing-card.tsx`, all at the call site (D-04).

    **(1) `:434` — the footer.** `className="gap-2"` becomes `className="gap-2 mt-auto flex-wrap"`,
    **appended in that order** (D-06). `mt-auto` absorbs the column's free space above the footer,
    restoring what `Card`'s base `has-data-[slot=card-footer]:pb-0` already assumes: the footer is
    flush with the card's bottom edge. `flex-wrap` is a SEPARATE fix for a SEPARATE defect —
    `CardFooter` is the flex container that cannot wrap, while `Button` carries both `shrink-0` and
    `whitespace-nowrap`, so the row overruns its content box and is cut at the card's rounded edge.

    ⚠ Do NOT add a full-height utility to `Card` at `:352`. It is a measured no-op (D-05): the grid
    wrapper at `page.tsx:180` declares no alignment, a grid container's alignment defaults to
    stretch for an auto-height item, and the card boxes therefore already stretch. Adding it would be
    a no-op dressed as a fix — the ticket closes and the defect returns with a new explanation.
    ⚠ Do NOT add a growth utility to `CardContent` at `:368`. The footer alternative is the
    lower-risk of the two D-05 permits, because it changes one property on one element and is inert
    on the branch where `hasActions` is false.

    **(2) `:465` — the Delete trigger goes icon-only** (D-07). `size="sm"` becomes
    `size="icon-sm"`; the visible text is dropped; the button gains a trash icon marked
    `aria-hidden="true"` and a `<span className="sr-only">Delete</span>` carrying the accessible
    name, following `photo-lightbox.tsx:370-373`. Keep `variant="ghost"` and
    `className="text-destructive"` exactly as they are.

    ⚠ The `sr-only` span IS the name. Delete it and `getByRole("button", { name: "Delete" })` stops
    resolving — silently, and only for screen-reader users, because no test locates this control by
    its text today.
    ⚠ The ConfirmDialog is not optional and must not be simplified. Icon-only is safe from mis-taps
    BECAUSE the destructive act stays double-gated behind `confirmLabel="Delete listing"` /
    `confirmVariant="destructive"`.
    ⚠ Only Delete goes icon-only. Unlist at `:451` keeps its label (D-07), and the `div.ml-auto` at
    `:449` is unchanged — `margin-left: auto` resolves to 0 once free space is negative, so it
    neither causes nor worsens the overflow, and with the footer wrapping it right-aligns the
    destructive pair on its own line.
    ⚠ The hit-area bar for this cluster is 24px. `size="icon-sm"` is `size-7` = 28px, the same height
    as its `h-7` siblings, so the row's baseline does not move and the scan clears with room. Do not
    import a 44px requirement — `e2e/overflow-320.spec.ts:3144-3150` argues in as many words that
    asserting 44 on any control in this cluster would be red against reviewed code.

    **(3) `:38` — the import.** Add the `lucide-react` trash icon to the existing named import,
    aliased in the file's established style, so the icon child at `:465` resolves.

    Each edit gets a docblock carrying its argument and naming what pins it, per the repository's
    standing comment pattern — 19-RESEARCH § 2.2 has all three written out; copy them rather than
    paraphrase. The footer docblock must state that the classes are APPENDED and never prepended,
    and why: `cn(base, className)` puts the call-site string last, which is the position
    tailwind-merge keeps, and Phase 17's WR-04 measured a hoisted constant deleting a padding class
    outright.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `src/components/listing/listing-card.tsx` — most likely an unresolved icon import.</fails_when>
    <automated>npx vitest run tests/listing/listing-card.test.tsx</automated>
    <fails_when>Exit code is non-zero, or any test in that file reports a failure — the existing card unit tests must not regress.</fails_when>
    <automated>git diff HEAD --stat -- src/components/ui/card.tsx src/components/ui/button.tsx</automated>
    <fails_when>Any output at all — a vendored shadcn primitive was edited, which D-04/D-129 forbid because `npx shadcn add` re-violates a fork on the next install and because it would change every Card in the app.</fails_when>
    <automated>grep -cE 'className="[^"]*h-full[^"]*"' src/components/listing/listing-card.tsx</automated>
    <fails_when>The count is greater than 0 — a full-height utility was added to a rendered element's class attribute, which D-05 identifies as a measured no-op and forbids as the fix. The pattern is deliberately scoped to the inside of a `className` string so that the docblocks, which must argue about that utility by name, cannot invalidate their own gate.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c 'className="gap-2 mt-auto flex-wrap"' src/components/listing/listing-card.tsx` returns 1.
    - `grep -c 'className="gap-0 pt-0"' src/components/listing/listing-card.tsx` returns 1 — the `Card` call site is byte-unchanged.
    - `grep -c 'className="space-y-1 py-4"' src/components/listing/listing-card.tsx` returns 1 — the `CardContent` call site is byte-unchanged.
    - `grep -c 'size="icon-sm"' src/components/listing/listing-card.tsx` returns 1.
    - `grep -c 'sr-only' src/components/listing/listing-card.tsx` returns at least 1, and the span's text content is `Delete`.
    - `grep -c 'confirmLabel="Delete listing"' src/components/listing/listing-card.tsx` returns 1 and `grep -c 'confirmVariant="destructive"' src/components/listing/listing-card.tsx` returns 1.
    - `grep -c '>Unlist<' src/components/listing/listing-card.tsx` returns 1 — Unlist kept its visible label.
    - `grep -cE 'className="[^"]*h-full[^"]*"' src/components/listing/listing-card.tsx` returns 0 (scoped to class attributes, so the docblocks may name the utility they forbid).
    - `git diff HEAD --stat -- src/components/ui/card.tsx src/components/ui/button.tsx` produces no output.
    - `npx vitest run tests/listing/listing-card.test.tsx` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Three class/markup edits in one file; `git revert` restores the prior render exactly, and the guards from plan 19-02 would go red again and say why.</reversibility>
  <done>The footer sits flush with the card's bottom edge and wraps rather than clipping, Delete is icon-only with its accessible name and its confirmation intact, Unlist keeps its label, and both vendored primitives are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: the standing merge-order check, and the build-blocking design suite</name>
  <files>tests/design/listing-card-merge-order.test.ts</files>
  <read_first>
    - `tests/design/clearance-merge-order.test.ts:1-30` — the mechanism, in the repository's own words: `cn` is `clsx` plus `extendTailwindMerge`, and tailwind-merge resolves a conflict by deleting the EARLIER class. This is the file to mirror.
    - `src/lib/utils.ts:41-47` — `extendTailwindMerge({ extend: { theme: { text: [...TYPE_ROLES] } } })`. It extends only the type-role theme; no layout group is touched, so stock grouping applies unmodified.
    - `src/components/ui/card.tsx:86-91` — `CardFooter`'s base string, `flex items-center rounded-b-xl border-t bg-muted/50 p-4`, which is the first argument to the merge under test.
    - `19-RESEARCH.md § 2.4` — why D-06's hazard is structurally absent from these three edits, and the exact standing check to write.
    - `19-UI-SPEC.md § ⚠ Append, never prepend (D-06 / WR-04)`.
    - `tests/design/status-vocab.test.ts:184` and `tests/design/card-pattern-coverage.test.ts` — the two design gates that scan `listing-card.tsx`. Neither should redden, but the suite is build-blocking so it settles the question in seconds.
  </read_first>
  <action>
    Create `tests/design/listing-card-merge-order.test.ts` carrying one standing assertion (D-06):
    passing `CardFooter`'s base string and the new call-site string through `cn` must yield a result
    containing ALL FOUR of `p-4`, `gap-2`, `mt-auto` and `flex-wrap`. If any one is missing, the
    append silently deleted a class and the render is worse than the defect being fixed — which is
    exactly what Phase 17's WR-04 measured.

    Write the failure message to name the mechanism and the precedent, not the fact that a string
    differs: state that tailwind-merge deletes the EARLIER class on a conflict, that WR-04 measured a
    hoisted constant destroying a padding class outright, and that the call-site string must stay in
    the last argument position.

    Then run the build-blocking design suite in full. `npm run build` is `lint`, then `test:design`,
    then the Next compile, so a red here blocks the build regardless — running it now settles
    assumption A8 (whether the status-vocabulary scan reddens from an `sr-only` "Delete") in seconds
    rather than at deploy time.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/listing-card-merge-order.test.ts</automated>
    <fails_when>Exit code is non-zero, or the reported test count is 0 — a design file that collects no tests reports success and asserts nothing.</fails_when>
    <automated>npm run test:design</automated>
    <fails_when>Exit code is non-zero. A red in `tests/design/status-vocab.test.ts` means the `sr-only` name added status vocabulary; a red in `tests/design/card-pattern-coverage.test.ts` means its allow-list entries for this file were treated as class pins rather than as a status; a red in `tests/design/brand-recipe.test.ts` means a brand-variant call site was added, which this task does not do.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --config vitest.design.config.ts tests/design/listing-card-merge-order.test.ts` exits 0 and reports at least 1 passing test.
    - `npm run test:design` exits 0 with zero failures.
    - The new test's assertion covers all four of `p-4`, `gap-2`, `mt-auto`, `flex-wrap` — `grep -c "mt-auto" tests/design/listing-card-merge-order.test.ts` returns at least 1 and `grep -c "p-4" tests/design/listing-card-merge-order.test.ts` returns at least 1.
    - The test imports `cn` from the project's own utility module rather than reimplementing the merge.
  </acceptance_criteria>
  <done>A build-blocking assertion proves the appended classes all survive the merge, and the whole design suite is green against the edited card.</done>
</task>

<task type="auto">
  <name>Task 3: both guards GREEN at three bands, and the reviewed 320px spec still green</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt</files>
  <precondition>The local Postgres container is up and migrated, and nothing is listening on :3000 — `playwright.config.ts` sets `reuseExistingServer: false` unconditionally, so a stray dev server is a guaranteed failure.</precondition>
  <read_first>
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt` — the recorded red, so the green run can be stated as the same assertions now passing rather than as a different set.
    - `e2e/host-listing-grid.spec.ts` — the spec written in plan 19-02; run it, do not modify it. Changing an assertion to make it pass is the failure mode this whole ordering exists to prevent.
    - `e2e/overflow-320.spec.ts:3129-3150` — the `/host/listings` row, its `tell` selector `a[href^="/host/listings/"][href$="/availability"]` (the Availability anchor, untouched by D-07), and its `touch: []` argument.
    - `19-VALIDATION.md § Per-Requirement Signals § HSURF-01`.
  </read_first>
  <action>
    Run `e2e/host-listing-grid.spec.ts` against the fixed tree and capture the output to
    `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt`.
    Both guards must be green at all three bands.

    Then run `e2e/overflow-320.spec.ts` as a regression check — not as proof. It passed against the
    broken tree and it must still pass now; what it proves is that the icon-only Delete did not
    break the 24px hit-area scan and that the Availability anchor its row keys on is untouched.

    ⚠ If guard A clause 2 is still red, the recommended `mt-auto` did not produce the intended
    rendering (assumption A1). D-05 permits the alternative — a growth utility on `CardContent` —
    and the guard is precisely the instrument that settles which one works. Try it, re-run, and
    record which one landed and what the numbers were.
    ⚠ If guard B is still red at 320px even with the wrap and the icon-only Delete (assumption A2),
    that is an ESCALATION, not a widening. The next lever named in the research is a deferred overflow
    menu, which is explicitly out of scope. Stop and raise it.

    Record in the summary, side by side, the pre-fix numbers and the post-fix numbers for both
    guards, at each band.
  </action>
  <verify>
    <automated>npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --reporter=list > .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt 2>&1</automated>
    <fails_when>Exit code is non-zero, or `guards-post-fix.txt` contains the string `failed` — the fix did not land the geometry the guards measure.</fails_when>
    <automated>npx playwright test e2e/overflow-320.spec.ts --project=chromium</automated>
    <fails_when>Exit code is non-zero. A red naming a hit-area target means the icon-only control fell below the 24px scan; a red naming the row's tell means the Availability anchor was disturbed, which this phase does not touch.</fails_when>
    <automated>git diff HEAD --stat -- e2e/host-listing-grid.spec.ts e2e/overflow-320.spec.ts</automated>
    <fails_when>Any output at all — an assertion was changed to make the run green, which invalidates the watched-red evidence from plan 19-02.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/guards-post-fix.txt` exists and reports zero failing tests across all three bands.
    - `npx playwright test e2e/overflow-320.spec.ts --project=chromium` exits 0.
    - `git diff HEAD --stat -- e2e/host-listing-grid.spec.ts e2e/overflow-320.spec.ts` produces no output — neither spec was edited to reach green.
    - The summary tabulates, per band, guard A clause 2's pre-fix gap in pixels against its post-fix gap, and guard B's pre-fix `scrollWidth`/`clientWidth` pair against its post-fix pair.
    - The summary names which of D-05's two permitted fixes actually landed.
  </acceptance_criteria>
  <done>The same two assertions that were red against the shipped defect are green against the fixed tree, no assertion was edited to get there, and the reviewed 320px spec is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored `ui/*` primitives → application call sites | The shadcn primitives are treated as upstream; a fork silently changes every consumer, including the public search grid. |
| Icon-only control → assistive technology | Removing a visible label moves the entire accessible name onto DOM text that no current test asserts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Tampering | `src/components/ui/card.tsx`, `src/components/ui/button.tsx` | high | mitigate | Every class lands on a `className` prop at the call site (D-04/D-129). A `git diff --stat` on both files producing no output is a task acceptance criterion, so the fork cannot pass silently. |
| T-19-11 | Repudiation | The Delete control's accessible name | high | mitigate | The `sr-only` span carrying "Delete" is an explicit acceptance criterion, and the UI contract marks the resolving `getByRole` query as a held-out backstop assertion for verification — its absence at verify time is `insufficient_spec`, never a silent pass. |
| T-19-12 | Elevation of Privilege | Destructive action without confirmation | high | mitigate | The `ConfirmDialog` wrapping Delete is unchanged: `confirmLabel="Delete listing"`, `confirmVariant="destructive"`. Both strings are pinned by acceptance criteria. Icon-only is safe from mis-taps only because the act stays double-gated. |
| T-19-13 | Tampering | The reviewed 24px hit-area exemption | medium | mitigate | `size="icon-sm"` is 28px, above the 24px bar the reviewed spec scans. No 44px requirement is imported; `e2e/overflow-320.spec.ts` is asserted byte-unchanged. |
| T-19-14 | Tampering | Class deletion under tailwind-merge | medium | mitigate | Classes are appended, never prepended, and a build-blocking design test asserts all four survive the merge (D-06 / WR-04). |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged — zero packages installed. `lucide-react` is already imported at `listing-card.tsx:38`; `sr-only`, `mt-auto` and `flex-wrap` are stock Tailwind utilities. `npx shadcn add` must NOT be run against `card` or `button`. A package proposal here is a milestone scope alarm. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run test:design` exits 0.
3. Both guards green at three bands, captured in `evidence/guards-post-fix.txt`.
4. `e2e/overflow-320.spec.ts` green.
5. `git diff HEAD --stat -- src/components/ui/card.tsx src/components/ui/button.tsx` produces no output.
</verification>

<success_criteria>
- Every card in a row ends at the same bottom edge with the footer flush to it, at all three bands.
- Every action control is fully visible and pressable inside its own card at all three bands.
- The Delete control is icon-only and still named "Delete" to assistive technology, behind its unchanged confirmation dialog.
- Neither vendored primitive was edited; no full-height no-op was added.
</success_criteria>

## Artifacts this phase produces

This plan's slice of the phase-wide index in `19-01-PLAN.md`:

**New files**
- `tests/design/listing-card-merge-order.test.ts` — the D-06 standing append-order check, build-blocking via `npm run test:design`.
- `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-post-fix.txt` — the green capture, to be read against `guards-pre-fix.txt`.

**Modified surface, stated as symbols**
- `src/components/listing/listing-card.tsx`: `CardFooter` call-site class becomes `gap-2 mt-auto flex-wrap`; the Delete trigger becomes `size="icon-sm"` with an `aria-hidden` trash icon and an `sr-only` name; the `lucide-react` import at `:38` gains the trash icon.

**Held-out for verification (backstop):** `getByRole("button", { name: "Delete" })` resolving on
`/host/listings`. Not written by this plan — its absence at verify time is `insufficient_spec`.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-03-SUMMARY.md` when done.

It must record:
- a per-band table of guard A clause 2 and guard B, pre-fix numbers against post-fix numbers;
- which of D-05's two permitted fixes landed (`mt-auto` on the footer, or the growth utility on the content), and why;
- confirmation that both vendored primitives show no diff;
- confirmation that `e2e/overflow-320.spec.ts` is green and byte-unchanged;
- the `npm run test:design` result, which settles assumption A8.
</output>
