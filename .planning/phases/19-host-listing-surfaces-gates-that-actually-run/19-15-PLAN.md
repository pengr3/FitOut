---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 15
type: execute
wave: 15
depends_on: ["19-14"]
gap_closure: true
files_modified:
  - scripts/verify-workflows.mjs
  - .github/workflows/ci.yml
  - tests/design/workflow-invariants.test.ts
  - scripts/refuse-mail-credential.mjs
  - tests/design/mail-credential-refusal.test.ts
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CI-01 / SC4 — the requirement's literal text is now protected by an assertion, not merely true of today's file. A `ci` section check requires `triggersOf(doc)` to include BOTH `push` and `pull_request`. Deleting `pull_request:` from `.github/workflows/ci.yml` — a one-line edit that detaches EVERY gate in the file from pull requests — was independently reproduced this session at all 48 invariants green, and is now red with a named FAIL line. SC4's wording is literally *\"opening a pull request runs the repository's functional Playwright specs\"*, so this is the one trigger whose absence falsifies the requirement the phase claims."
    - "CI-01 — the trigger check is a SUPERSET test, deliberately, and the `baselines` section's is an EXACT test, deliberately, and the two opposite shapes each carry their reason at the site. On `baselines.yml` an ADDED trigger is the single most dangerous edit in the repository (the write path regenerating itself on every commit), so the set is pinned exactly. On `ci.yml` an added `workflow_dispatch` is harmless and a MISSING trigger is the danger, so membership is asserted and additions are not."
    - "D-14 / CR-01 — the refusal step's interpreter is pinned one level out as well. A new `ci` section check requires that NEITHER `.github/workflows/ci.yml` at workflow level NOR the `gate-e2e` job declares a `defaults:` block at all. The whole block is forbidden rather than one key inside it: that is the allow-list shape 19-14 established one level in, applied to the level `grep -n \"shell\\|defaults\" scripts/verify-workflows.mjs` proved was read nowhere in the checker. The scope is the workflow and `gate-e2e`, not every job, because a `defaults:` block on an unrelated job cannot change how `gate-e2e` executes and a wider assertion would fire against a correct file."
    - "CI-01 — the invariant total moves from 48 to 50 and EVERY sentence documenting it moves in the same commit. `node scripts/verify-workflows.mjs` ends with `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`; `.github/workflows/ci.yml`'s invariant list becomes EIGHT `gate-e2e` entries plus a new file-level bullet for the trigger assertion; the paragraph asserting the fixed list length and total quotes the checker's summary line verbatim so the documented count and the printed count are the same bytes; and `tests/design/workflow-invariants.test.ts`'s control constant carries the new line. A stale count sentence is a false claim of exactly the class this phase exists to delete."
    - "CI-01 — both increments trace to a named gap remediation and nothing rides along. The `ci` section goes 29 → 31: +1 for the execution-defaults invariant (19-VERIFICATION.md gap 1, `missing:` bullet 1, and 19-REVIEW.md CR-01's second half) and +1 for the trigger invariant (19-VERIFICATION.md gap 2, `missing:` bullet 1, and 19-REVIEW.md WR-01). No third check is added."
    - "CI-01 — both new invariants have standing cases the day they land. `tests/design/workflow-invariants.test.ts` grows from nine cases to twelve: a workflow-level `defaults:` block, a job-level one, and a deleted `pull_request:` trigger, each spawning the real checker against a mutated copy and each asserting a non-zero exit with a `FAIL` line naming the right invariant. No predicate in this plan is proven only by a hand-applied mutation."
    - "D-14 / WR-03 — the refusal script's failure message stops claiming coverage the control cannot have. Repository, organization and environment secrets do NOT populate a step's process environment; they are reachable only through `${{ secrets.* }}` interpolated into an `env:` or `with:` map, which is exactly the input the sentence said this scan is *not* limited to. The message now names the two input classes this scan genuinely adds over the parse half — a `container.env` block and the runner environment — and names the one case invisible to BOTH halves, a secret mapped under a non-provider-prefixed key, together with the assertion in the other file that does cover it."
    - "D-14 / WR-04 — the file's own rule binds its own diagnostics. `scripts/refuse-mail-credential.mjs:40-41` states that this file prints variable NAMES and never variable VALUES, because a credential echoed into a build log is a wider disclosure than the send the control prevents. The argument-refusal branch echoed unfiltered caller-controlled content twenty-eight lines later. It now prints the argument COUNT and states that the values are deliberately withheld, so a developer probing the control by pasting a credential as an argument does not get it framed back as a workflow annotation."
    - "CI-01 / IN-02 — a build-blocking test can no longer fail for a reason unrelated to its property. `tests/design/mail-credential-refusal.test.ts`'s `withScriptCopy` compared a `tmpdir()`-built path against the realpath-derived path the script prints, which differ on any platform where the temp directory is a symlink (macOS `/var` → `/private/var`). The temp directory is now resolved with `realpathSync`, matching the harness 19-14 wrote next door — a repository that writes the correct idiom in a new file while leaving the broken one beside it is the inconsistency this phase keeps paying for."
    - "CI-01 adjacency (spec-less edge probe) — two trigger assertions that look adjacent are deliberately opposite, and each states why at its own site. Exact-set on the write path, membership on the compare path. A future reader who \"harmonises\" them breaks one of the two properties, so the divergence is documented as a decision rather than left to look like drift."
    - "CI-01 empty (spec-less edge probe) — emptiness is red where absence is the failure and green where absence IS the property, and the two are distinguished at the site. `triggersOf(doc)` returns `[]` for an absent or null `on:` block, so the trigger check is FALSE rather than vacuously true. The defaults check is the mirror image: `undefined` is the ONLY passing value, so a present-but-empty `defaults:` block is red, because a block that exists is a block a later edit can fill."
    - "CI-01 ordering (spec-less edge probe) — the trigger verdict is order-independent by construction and its evidence is not. Membership is asserted with `includes`, so reordering the `on:` block changes no verdict; the evidence line prints the parsed trigger list in the document's own key order, rendering `(none)` when empty, so a red is diagnosable against the file as written."
    - "Nothing executable in `.github/workflows/ci.yml` moves. No job, step, `run:`, `env:` key, service, container option, trigger or timeout changes in this plan — the only bytes that change in that file are comment lines, proven by a `git diff -U0` in which every added and removed line begins with `#`. In particular the `pull_request:` trigger is asserted, never edited; mutations exist only inside `mkdtemp` copies."
  artifacts:
    - scripts/verify-workflows.mjs
    - .github/workflows/ci.yml
    - tests/design/workflow-invariants.test.ts
    - scripts/refuse-mail-credential.mjs
    - tests/design/mail-credential-refusal.test.ts
  key_links:
    - "`.github/workflows/ci.yml`'s `on:` block → a `ci` section `check()`. Before this plan `triggersOf(doc)` was read in the `ci` section ONLY for the `parsed values (ci)` printout at `:929` and never compared, so deleting `pull_request:` left all 48 invariants green — independently reproduced this session."
    - "`.github/workflows/ci.yml`'s workflow-level and `gate-e2e`-level `defaults:` blocks → a `ci` section `check()`. Before this plan neither was read anywhere in the checker, confirmed by a grep for both terms returning no matches in the whole file."
    - "the checker's printed summary line → `.github/workflows/ci.yml`'s count paragraph → `tests/design/workflow-invariants.test.ts`'s control constant. All three now carry the SAME BYTES, and the control case is what makes a future count change impossible to land silently: adding a `check()` without updating the constant turns the design suite red inside `npm run build`."
    - "`scripts/refuse-mail-credential.mjs`'s failure messages → what the scan actually reads. This is the link WR-03 measured broken: the message named repository and environment secrets, which never reach `Object.keys(process.env)` at all."
    - "`scripts/refuse-mail-credential.mjs:40-41`'s stated rule → every diagnostic line in the same file. WR-04 measured this link broken twenty-eight lines below the rule."
  prohibitions:
    - "A documented invariant COUNT must never outlive the count it documents. A stale total is a false claim of exactly the class this phase exists to delete, and unlike the ones that took three rounds to find, this one is cheap to detect — so shipping it would be the least defensible instance of the pattern."
    - "A control's failure message must never name an input the control cannot see. A remedy sentence pointing at the wrong input costs the next reader the whole investigation, and it reads as coverage to everyone who does not open the code beside it."
    - "A rule a file states about itself binds every line of that file, including diagnostics added later by a different plan. A control that prints caller-controlled content into a build log has widened the disclosure it exists to prevent."
    - "A new file must never ship the correct idiom while the file beside it keeps the broken one. Two spellings of the same helper in the same directory is a second source of truth wearing the costume of a convention."
  assumptions:
    - "SPEC-LESS EDGE PROBE — all seven rows have a stated disposition and zero silent drops. IN SCOPE and AUTHORED above as explicit truths (verification: explicit): CI-01 adjacency, CI-01 empty, CI-01 ordering — each re-derived for THIS plan's subject (the two deliberately opposite trigger-check shapes; emptiness being red for triggers and green for defaults; membership being order-independent while its evidence is not) rather than copied from 19-13's or 19-14's. OUT OF SCOPE and FLAGGED, not dropped: HSURF-01 adjacency, HSURF-01 empty and HSURF-01 ordering are covered by executed plans 19-02/19-03 (substance in `19-UI-SPEC.md`'s `## UI Considerations`) and both HSURF truths are VERIFIED regressions untouched by this round; the HSURF-02 `unclassified` row remains UNRESOLVED and is carried forward as a flagged planner assumption from 19-09-PLAN.md. Accounting: 7 surfaced = 3 authored + 4 flagged."
    - "PROHIBITION PROBE — Stage 1 over-produced nine raw must-NOT candidates. Stage 2 dropped the routine-engineering ones (must not leave a temp directory behind; must not depend on the host's temp-path shape; must not throw on an absent `on:` block) and kept FOUR bespoke values/safety items, authored descriptor-less above. Two canon referrals, breadcrumbed rather than minted: (a) log injection and secret redaction generally are canon — covered by `/gsd-secure-phase` and the eslint security rules; WR-04's fix is the specific instance, not a new general rule; (b) 'a credential's VALUE must never be printed by the control that detects it' is already minted in 19-12-PLAN.md and re-asserted by `mail-credential-refusal.test.ts` case 6 — this plan extends its reach to `argv` rather than re-minting it."
    - "UI-SPEC LIFT — `19-UI-SPEC.md`'s `## UI Considerations` reports 9 covered + 1 backstop + 0 unresolved, every item owned by an executed plan (19-02/19-03 for the grid, 19-07/19-10 for the D-03 notice). This plan creates and changes NO UI surface: it touches two Node scripts, two Vitest design tests and one comment block in a workflow file. No item is lifted into these must_haves. Said explicitly rather than dropped silently."
    - "OUT OF SCOPE — the two `human_verification` items in 19-VERIFICATION.md, carried unchanged across three rounds: (a) the PM's reconfirmation of CI-01's closure state, including D-15's branch-protection flip being unreachable on this GitHub plan (403 — Free private repo) and the `ci` run being red on every push from 14 pre-existing e2e failures with no expected-failure boundary; (b) a human read of the D-03 creation-failure notice copy. Both are recorded PM/UX calls. They are not tasks and are not turned into assertions."
    - "OUT OF SCOPE, AND SAID SO RATHER THAN QUIETLY OMITTED — 19-REVIEW.md's IN-03 (nothing pins that the refusal script's argument guard PRECEDES its file and environment reads; the stated ordering is documented intention, and moving the guard below the reads leaves all eight cases green) and IN-04 (the prefix match is case-sensitive against a `process.env` that is case-insensitive on Windows, so the design suite exercises different semantics than the Linux container it guards). Both are Info-grade, neither appears in any gap's authoritative `missing:` block, and both are named here so the next reader does not have to rediscover them. IN-01 was folded into 19-14 Task 3; WR-05 into 19-14 Task 2."
    - "OUT OF SCOPE — 19-VERIFICATION.md's carried-forward anti-patterns that live under `src/`: `ConfirmDialog`'s handlers lacking `try`/`catch` in `src/components/listing/listing-card.tsx`, and ~11 stale `<file>:<line>` citations in guard failure messages. No task here touches `src/`."
    - "REQUIREMENTS.md IS NOT MODIFIED. 19-VERIFICATION.md flagged for the third consecutive round that CI-01's traceability row already reads `Complete` while CI-01 was only PARTIALLY SATISFIED, and separately that the HSURF-01 body checkbox at `:237` is unticked while its table row at `:313` reads `Complete`. Closing these gaps is what earns the CI-01 cell; flipping it and back inside one plan would be churn, and the checkbox inconsistency predates this round and is a documentation-consistency issue for whoever next edits that file. Both are left byte-unchanged and the earning is recorded in this plan's SUMMARY instead."
    - "HISTORICAL COUNT SENTENCES ARE NOT EDITED. `.github/workflows/ci.yml:239` and `:256` and `scripts/verify-workflows.mjs:737` and `:801` record what was MEASURED at the time — that a mutation left all 48 invariants green. Those are true statements about a past measurement and rewriting them would destroy the record. Only PRESENT-TENSE assertions of the current total change in this plan, and the grep gate below is scoped narrowly enough to distinguish the two."
    - "API COVERAGE — the detector was run over this phase's plan scope and returned `detected: false`. No external API/SDK/service is integrated; GitHub Actions is the subject being PARSED, not an API being called. No `COVERAGE.md` matrix is produced and no row is fabricated for a capability that does not exist."
    - "ASSUMPTION DELTA — no new identity question. 19-13 promoted the gate-bearing unit from the job to every unit that can carry a condition; 19-14 promoted the step's permitted shape from a forbidden-key set to a permitted-key set. This plan applies the SAME already-decided promotion one level out (the whole `defaults:` block is forbidden, not one key inside it) and adds one assertion about a property that had none. Decision: `no-change`."
    - "SCHEMA PUSH — no schema-relevant file is in scope. `drizzle/`, `src/lib/db/schema.ts` and `supabase/` are untouched and absent from `files_modified`. No `[BLOCKING]` schema-push task is injected."
    - "PACKAGE LEGITIMACY — zero packages are installed. `package.json` is not in `files_modified`; both scripts import only `node:` builtins and both tests import only `node:` builtins plus the already-installed `vitest`. 19-RESEARCH.md's `## Package Legitimacy Audit` records `Not applicable — this phase installs zero external packages`, with no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is required."
---

<objective>
Assert the trigger the requirement is literally about, forbid the block that changes how the gate runs,
and move every documented count in the same commit that moves the count.

Plan 19-14 changed the SHAPE of three predicates and left the total at 48 on purpose, so the whole of it
could be measured against one unchanged summary line. This plan adds the two assertions that genuinely
did not exist, and pays the count debt in full:

- **19-REVIEW.md WR-01 / 19-VERIFICATION.md gap 2.** Nothing in the `ci` section asserts `ci.yml`'s
  trigger set. `triggersOf(doc)` is read there only for the diagnostic printout. Deleting
  `pull_request:` — one line — detaches every gate in the file from pull requests while the checker
  reports all 48 invariants hold. It was independently reproduced this session. CI-01's requirement text
  is literally about opening a pull request, so this is the one trigger whose absence falsifies the
  requirement the phase claims to have satisfied.
- **19-REVIEW.md CR-01, second half.** A `defaults: { run: { shell: … } }` block at workflow or job level
  changes the interpreter for every step below it, including the refusal, and nothing in the checker
  reads either level. 19-14's allow-list closed the STEP's attribute surface; this closes the two levels
  that reach into it from outside.

Purpose: the count is load-bearing, not bookkeeping. This phase's entire failure mode is a documented
guarantee outliving the thing that implements it, and a stale total is the cheapest possible instance of
it. So the two new checks, the header's list, its fixed-count paragraph and the standing test's control
constant all move together, in one commit, and the paragraph quotes the checker's summary line verbatim
so the documented count and the printed count are the same bytes.

Output: `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`, an eight-entry
`gate-e2e` invariant list plus a new file-level bullet, twelve standing mutation cases, and three
carried-forward review warnings — a false coverage claim, a build-log disclosure and a cross-platform
test flake — closed in the D-14 script family they belong to.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VERIFICATION.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-14-PLAN.md

**Why `depends_on: ["19-14"]`.** Worktrees are OFF and this phase's plans form a strict chain on `dev`;
the wave number is this plan's position in that chain, not a concurrency claim. Task 1 edits the standing
test file 19-14 creates and the invariant list 19-14 restructures, so it cannot start before that plan is
committed.

**Do not modify, renumber, supersede or delete any of `19-01-PLAN.md` … `19-14-PLAN.md`.** They are all
executed and all have SUMMARYs. This plan is purely additive.

**Line-ending note, measured:** `.gitattributes` declares `* text=auto` and this working tree checks out
`.github/workflows/ci.yml` with CRLF. The new mutation builders inherit 19-14's `eolOf()` discipline; the
trigger-deletion builder must remove a whole line including its line terminator, whichever it is, and
must not leave a blank line behind that changes the `on:` block's shape for a reason unrelated to the
property under test.

**Expected baseline entering this plan (verify before starting):** `node scripts/verify-workflows.mjs`
exits 0 ending with `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8).`, and
`npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts` reports
`9 passed`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two invariants that did not exist, and every sentence that documents the total, in one commit</name>

  <precondition>Plan 19-14 is fully committed, `git status --porcelain` is empty, `node scripts/verify-workflows.mjs` ends with `All 48 invariants hold across 3 section(s) (baselines=11, ci=29, cross=8).`, and `tests/design/workflow-invariants.test.ts` reports `9 passed`.</precondition>

  <reversibility rating="reversible">Two `check()` calls, two local bindings, three mutation cases, one
  test constant and one comment paragraph. Deleting them restores 19-14's behaviour exactly and returns
  the total to 48. No workflow behaviour changes and no job runs differently.</reversibility>

  <read_first>
    - `scripts/verify-workflows.mjs:301-310` — the `baselines` section's trigger assertion, including the
      comment explaining that `on:` parses to an object whose KEYS are the triggers and that this is a
      SET comparison rather than a containment test, because an ADDED trigger is the danger there. The
      new `ci` check is the mirror image and must say so at its own site: on the compare path a MISSING
      trigger is the danger and an added one is harmless, so it asserts membership rather than the set.
    - `scripts/verify-workflows.mjs:428-468` — the top of the `ci` section, where the file-level checks
      live (workflow permissions, no job holds write, the concurrency group, the supply chain). The
      trigger check belongs in this neighbourhood, NOT in the `gate-e2e` block, because it is a property
      of the whole file.
    - `scripts/verify-workflows.mjs:202-203` — `triggersOf`. Read what it returns for a `null` `on:` and
      for a string `on:`; the empty-input behaviour is an authored must-have of this plan.
    - `scripts/verify-workflows.mjs:926-939` — the `parsed values (ci)` printout, where `triggersOf(doc)`
      is currently read and never compared. That read is the whole of WR-01.
    - `scripts/verify-workflows.mjs` — the `gate-e2e` block as 19-14 left it: the unconditional check, its
      comment, Invariant A with its allow-list conjunct, and Invariant C with its predecessor allow-list.
      The new execution-defaults check goes immediately after the unconditional check, because it answers
      the same family of question — what can change how this gate runs — one level out.
    - `scripts/verify-workflows.mjs:1116-1126` — the summary block. This is where the string every count
      sentence must match is produced.
    - `.github/workflows/ci.yml:219-290` — the invariant-list comment block as 19-14 Task 3 left it:
      seven `gate-e2e` entries with the new falsification markers, and the paragraph asserting the list
      length and the total. Both change here.
    - `.github/workflows/ci.yml:526-529` — the `on:` block. This task ASSERTS it and never edits it.
    - `.github/workflows/ci.yml` — confirm by reading that no `defaults:` block exists at workflow level
      or on `gate-e2e`, so the new check is green against the committed file the moment it is written.
      (A repository-wide grep confirms neither workflow file carries one.)
    - `tests/design/workflow-invariants.test.ts` as 19-14 left it — `EXPECTED_GREEN`, `eolOf`,
      `withMutatedWorkflows`, `expectRed`, `withRefusalStepKey`, `withJobKey`, `withStepBeforeRefusal`,
      and the nine cases. This task adds two builders and three cases and edits one constant.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` § WR-01 in full,
      including its fix sketch and its note that the addition moves the count; and § CR-01's second half,
      the `defaults` paragraph and its sketch.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VERIFICATION.md` — the second
      `gaps:` entry in full, its `missing:` bullet, and the `pull_request` row of the Behavioral
      Spot-Checks table recording `CHECKER-EXIT=0` with the summary line unchanged.
  </read_first>

  <behavior>
    - A copy of the workflow tree whose `on:` block no longer declares a pull-request trigger makes the
      spawned checker exit non-zero with a `FAIL` line naming the trigger invariant. Before this task
      that mutation was invisible to every invariant.
    - A copy carrying a workflow-level `defaults:` block makes the checker exit non-zero with a `FAIL`
      line naming the execution-defaults invariant, and so does a copy carrying one on `gate-e2e`.
    - Against the real, unmutated tree the checker exits 0 and its final line is
      `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`
    - The standing test's control case pins that same new line, so a future `check()` added without a
      matching constant update turns the design suite red inside `npm run build`.
  </behavior>

  <files>scripts/verify-workflows.mjs, tests/design/workflow-invariants.test.ts, .github/workflows/ci.yml</files>

  <action>
Tests first: add the three cases, watch all three fail against the current checker because it exits 0,
then add the two checks, then move every count sentence in the same commit.

**1. `tests/design/workflow-invariants.test.ts` — two builders and three cases.**

  - Add `withWorkflowLevelBlock(blockLines)` — inserts a complete zero-indented block on its own lines
    immediately BEFORE the `jobs:` key line, CRLF-aware via the existing `eolOf`.
  - Add `withoutPullRequestTrigger()` — returns a `mutate` that removes the pull-request trigger line
    from the `on:` block, including its line terminator, leaving no blank line behind. ⚠ Anchor it on the
    trigger key as it appears in the file with its exact indentation, and rely on the harness's
    differs-from-input assertion to catch an anchor drift rather than assuming the removal applied.
  - Case 10: `withWorkflowLevelBlock` writing a `defaults:` block that sets a custom interpreter for
    every step in the file. `expectRed` against a distinctive fragment of the execution-defaults
    invariant's name. Comment: this reaches into `gate-e2e` from outside the job entirely, so 19-14's
    step-level allow-list — which reads only the keys ON the step — cannot see it.
  - Case 11: `withJobKey` writing the same `defaults:` block on `gate-e2e` itself. Same fragment.
    Comment: the job level is the second of the two ways in, and a check that closed one and not the
    other would be the same partial answer this round exists to stop shipping.
  - Case 12: `withoutPullRequestTrigger`. `expectRed` against a distinctive fragment of the trigger
    invariant's name. Comment: the one-line edit 19-VERIFICATION.md reproduced at `CHECKER-EXIT=0` with
    the summary line unchanged; CI-01's requirement text is literally about opening a pull request, so
    this mutation falsifies the requirement while the checker reports full green.

**2. Watch cases 10, 11 and 12 FAIL against the unchanged checker.** All three must fail because the
spawned checker exited 0. Record all three failure messages verbatim in the SUMMARY.

**3. `scripts/verify-workflows.mjs` — the trigger check.** Place it in the `ci` section's file-level
neighbourhood, near the concurrency check.

  - Bind the parsed trigger list once and assert it INCLUDES both the push trigger and the pull-request
    trigger. Membership, not set equality — a later `workflow_dispatch` addition on this file is harmless
    and must not redden a correct file.
  - Name the invariant so it states the requirement it protects: `ci.yml` runs on BOTH, and CI-01's
    requirement text is literally about pull requests.
  - Evidence: the parsed trigger list, rendered `(none)` when empty.
  - Comment it in the file's voice, and make it carry the three things a reader needs: the measurement
    (deleting the pull-request trigger left all 48 invariants green, reproduced this session); the
    consequence (EVERY gate in the file, `gate-e2e` included, detaches from pull requests in one line);
    and the deliberate asymmetry with the `baselines` section's EXACT trigger comparison, with the reason
    for each shape stated so a future reader does not "harmonise" them and break one.

**4. `scripts/verify-workflows.mjs` — the execution-defaults check.** Place it immediately after the
unconditional check, inside the `gate-e2e` block.

  - Assert that the workflow document declares no `defaults` block AND that `gate-e2e` declares none.
    ⚠ Forbid the WHOLE BLOCK, not one key inside it. That is the allow-list shape 19-14 established one
    level in: the failure class is "an attribute the checker does not know about", so a check that named
    one key inside the block would be the deny-list shape reasserting itself at a new level. `undefined`
    is the only passing value at each level, so a present-but-empty block is red — a block that exists is
    a block a later edit can fill.
  - Scope it to the workflow and to `gate-e2e`, and say why at the site: a `defaults:` block on an
    unrelated job cannot change how `gate-e2e` executes, and asserting over every job would redden a
    correct file the day somebody legitimately sets a working directory elsewhere.
  - Name the invariant so a green run states the property: `gate-e2e` runs its steps with the runner's
    default interpreter.
  - Evidence: both parsed values, JSON-quoted, each rendering `null` when absent.
  - Comment it with the measurement: a one-line interpreter override on the refusal step left the `run:`
    string, the `env:` absence, the `if:` absence and the step's position untouched while the script was
    printed instead of executed, with every invariant green; a grep for the two relevant terms across the
    whole checker returned no matches, so neither level was read anywhere. Note explicitly that this is
    the OUTER half of that finding and 19-14's step-key allow-list is the inner half, and that neither
    substitutes for the other.

**5. Move EVERY count sentence, in this same task and this same commit.** This is the load-bearing half
of the task and it is not bookkeeping.

  - `tests/design/workflow-invariants.test.ts`: update the control constant to the checker's new summary
    line, in full. The constant is the only place the total is spelled in that file.
  - `.github/workflows/ci.yml`, the `gate-e2e` invariant list: it becomes EIGHT entries. Add entry 8 for
    the execution-defaults invariant, in the list's established voice — what it asserts, why the whole
    block rather than one key, and the measurement that bought it.
  - `.github/workflows/ci.yml`: add a NEW top-level bullet, beside the existing file-level bullets rather
    than inside the `gate-e2e` list, for the trigger assertion. It is a property of the whole file, and
    filing it under the job's list would misstate its subject — which is this file's own failure mode.
    State what it asserts, why membership rather than an exact set, and the measurement.
  - `.github/workflows/ci.yml`: rewrite the paragraph that asserts the list length and the invariant
    total. It must now state EIGHT entries, quote the checker's new summary line VERBATIM so the
    documented count and the printed count are the same bytes, attribute the change to this plan, and
    explain that both increments are genuinely new assertions rather than tightenings — naming which gap
    each closes. Keep the reader instruction that a higher count means an invariant added out of turn and
    a lower one means one deleted, updated to the new number.
  - ⚠ DO NOT touch the four HISTORICAL sentences that record what was measured at the time — two in
    `ci.yml`'s invariant list and two in `verify-workflows.mjs`'s comment blocks — which say a past
    mutation left all 48 invariants green. Those are true statements about a past measurement; rewriting
    them destroys the record. The grep gate in `<verify>` is scoped narrowly enough to distinguish a
    present-tense assertion of the total from a historical one, and it will tell you if you cut too wide.

**6. Re-run everything.** The design test must report twelve passing cases; the checker must end with the
new summary line; and the `ci.yml` diff must be comments-only.
  </action>

  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line whose first non-space token is `FAIL` — a red here means one of the two new checks fires against the CORRECT committed file, most likely the defaults check reading a block that does exist or the trigger check spelling a trigger key differently from the parsed one; or the final line is not exactly `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).` — a total of 51 means a third check rode along, and 49 means only one of the two landed</fails_when>

    <automated>npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts</automated>
    <fails_when>non-zero exit; or the reporter does not report `12 passed`; or the control case fails, which after this task means the checker's printed total and the constant in the test file have drifted apart — the exact defect this plan exists to make impossible</fails_when>

    <automated>node -e "const fs=require('fs');const need='All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8)';for(const f of ['.github/workflows/ci.yml','tests/design/workflow-invariants.test.ts']){const n=fs.readFileSync(f,'utf8').split(need).length-1;console.log(f+'='+n);if(n<1)process.exit(1)}"</automated>
    <fails_when>either file reports `=0` — the documented count and the printed count are not the same bytes, which is the stale-count-sentence defect this task exists to prevent; the exit is non-zero on the first such file</fails_when>

    <automated>node -e "const fs=require('fs');let bad=0;for(const f of ['.github/workflows/ci.yml','scripts/verify-workflows.mjs','tests/design/workflow-invariants.test.ts']){for(const l of ['STAYS 48','ci=29']){const n=fs.readFileSync(f,'utf8').split(l).length-1;if(n){console.log(f+' :: '+l+'='+n);bad+=n}}}console.log('STALE-COUNT-HITS='+bad);process.exit(bad===0?0:1)"</automated>
    <fails_when>it prints anything other than `STALE-COUNT-HITS=0`; each offending file and phrase is named on its own line above that total. Both patterns are deliberately narrow so neither matches the four historical sentences recording that a past mutation left all 48 invariants green — those must NOT be edited, and a hit on one of them means the rewrite cut too wide</fails_when>

    <automated>git diff -U0 -- .github/workflows/ci.yml | grep -E "^[+-]" | grep -v "^[+-][+-][+-]" | grep -v "^[+-]#" | wc -l | tr -d ' '</automated>
    <fails_when>it prints anything other than `0` — a non-zero count is an added or removed line that does not begin with `#`, i.e. an executable byte of the workflow changed. In particular the pull-request trigger must be asserted and never edited. `wc -l` is used rather than `grep -c` so the success path exits 0 instead of grep's no-match exit 1</fails_when>

    <automated>git status --porcelain -- .github/workflows/</automated>
    <fails_when>any output after the commit — every mutation in this task lives inside a `mkdtemp` directory and the tracked workflow is only read</fails_when>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-workflows.mjs`'s `ci` section contains exactly TWO new `check()` calls versus 19-14: one asserting trigger membership, one asserting the absence of a `defaults:` block at workflow level and on `gate-e2e`. `git diff -U0` for this task shows no third `check(` added.
    - The trigger check asserts MEMBERSHIP (both triggers present) and not set equality, and its comment states why its shape is deliberately opposite to the `baselines` section's exact comparison.
    - The defaults check forbids the whole block at both levels, with `undefined` as the only passing value at each, and names no individual key inside the block.
    - `node scripts/verify-workflows.mjs` ends with exactly `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`
    - That exact string appears verbatim in `.github/workflows/ci.yml` and in `tests/design/workflow-invariants.test.ts`, proven by the three-way byte gate above, so the documented count, the pinned count and the printed count are the same bytes.
    - `.github/workflows/ci.yml`'s `gate-e2e` invariant list has EIGHT entries, and the trigger assertion is a separate top-level bullet rather than a ninth entry in that list.
    - The paragraph asserting the fixed list length and total names both increments and the gap each closes, and no other check is added alongside them.
    - The stale-count gate above reports `STALE-COUNT-HITS=0` across all three count-bearing files, and the four historical sentences recording a past all-48-green measurement are byte-unchanged (confirm with `git diff` and record the confirmation in the SUMMARY).
    - `tests/design/workflow-invariants.test.ts` reports exactly `12 passed`, and cases 10, 11 and 12 were each observed RED against the pre-change checker with the checker exiting 0 — each failure message recorded verbatim in the SUMMARY.
    - Every added or removed line in `git diff -U0 -- .github/workflows/ci.yml` begins with `#`.
  </acceptance_criteria>

  <done>Deleting the pull-request trigger or declaring a `defaults:` block at either level turns the checker red with a named FAIL line, and the total, the documented total and the pinned total are one string in three files.</done>
</task>

<task type="auto">
  <name>Task 2: Three carried-forward warnings closed in the D-14 script family</name>

  <precondition>Task 1 is committed, `git status --porcelain` is empty, the checker ends with `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`, and both design test files pass (`12 passed` and `8 passed`).</precondition>

  <reversibility rating="reversible">Six lines of `::error::` text in one script and one call wrapped in
  `realpathSync` in one test. No predicate, no invariant, no exit code and no control flow changes.</reversibility>

  <read_first>
    - `scripts/refuse-mail-credential.mjs` — the WHOLE file. Read `:40-41` FIRST: it states the file's
      governing rule, that this file prints variable NAMES and never variable VALUES, because a
      credential echoed into a build log is a wider disclosure than the send the control exists to
      prevent. Then read the argument-refusal branch at `:65-77` and the environment-hit branch at
      `:118-128` against that rule. Two lines break it or misstate it, and both are this task's subject.
    - `scripts/refuse-mail-credential.mjs:8-19` — the `WHY THIS FILE EXISTS AT ALL` paragraph, which
      already states CORRECTLY that the Actions `env` expression context never contains the process
      environment, repository or organization secrets, `vars.*`, `container.env` or `services.*.env`.
      The failure message at `:119-121` contradicts that paragraph three screens down, in the same file.
    - `scripts/verify-workflows.mjs:22-23` — the sentence 19-REVIEW.md WR-03 cites as the CORRECT
      statement of the same fact, one file over. The replacement message must agree with it.
    - `scripts/verify-workflows.mjs` — Invariant B as this plan leaves it. ⚠ It asserts that
      `scripts/refuse-mail-credential.mjs` carries `MAIL_KEY_PREFIX_DECL`'s source text VERBATIM and
      holds NO COPY of the prefix VALUE. Both constrain every edit in this task: do not reformat the
      declaration literal, and do not spell the provider's token anywhere in that file, comment included.
    - `scripts/verify-workflows.mjs`'s `secretHitsIn` (`:214-238`) — the assertion in the OTHER file that
      genuinely covers a secret mapped under a non-provider-prefixed key. The replacement message names
      it, so read what it actually scans before writing a sentence about it.
    - `tests/design/mail-credential-refusal.test.ts:150-170` — `withScriptCopy`, specifically the
      `mkdtempSync` call at `:157` and the two `expect(result.stdout).toContain(paths.sibling)` assertions
      at `:207` and `:218`. Read 19-REVIEW.md § IN-02 for why those three lines together can fail on a
      contributor's macOS for a reason unrelated to the property.
    - `tests/design/mail-credential-refusal.test.ts:229-253` — cases 7 and 8, which exercise the argument
      refusal. Confirm for yourself that neither asserts on the echoed argument content, so the WR-04 edit
      does not require a test change. If either does, the test is updated in this task rather than the
      message being left as-is.
    - `tests/design/workflow-invariants.test.ts` as Task 1 leaves it — the `realpathSync(mkdtempSync(...))`
      idiom 19-14 wrote. The old file is being brought into line with the new one, not the reverse.
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-REVIEW.md` §§ WR-03, WR-04 and
      IN-02 in full, including all three fix sketches. Follow the sketches literally; they were endorsed
      by the verifier.
  </read_first>

  <files>scripts/refuse-mail-credential.mjs, tests/design/mail-credential-refusal.test.ts</files>

  <action>
Three small, independent corrections in one commit. None changes an exit code, a control-flow branch or
an invariant; all three are the same defect class this phase exists to delete, at the smallest scale it
occurs at.

**1. WR-03 — the environment-hit message stops naming inputs the scan cannot see.**

The current message says the credential is in the real process environment and not merely in the
workflow's `env:` maps, and then lists three input classes that reach the scan. One of those three is
wrong: repository, organization and environment secrets do NOT populate a step's process environment.
They are reachable only through an Actions expression interpolated into an `env:` or `with:` map — which
is exactly the input the sentence says this is *not* limited to. The line is a coverage claim the
adjacent code does not implement, in the file whose header spends twenty lines on why that is worse than
no control.

  - Replace it with the two input classes this scan genuinely adds over the parse half: a container-level
    environment block, and the runner environment. Both are true and both are what `verify-workflows.mjs`
    says, correctly, one file over.
  - Add a ⚠ line naming the case invisible to BOTH halves — a secret mapped under a key whose name does
    not begin with the provider prefix, because both halves key off the NAME — and name the assertion
    that DOES cover it: the checker's zero-`secrets.`-in-any-value invariant, which is a different
    assertion in a different file. A control that names its own blind spot and points at what covers it
    is the opposite of the artifact being deleted here.
  - ⚠ Do not spell the provider's token in any of these lines. Invariant B asserts this file holds no
    copy of it, and it is red the moment one appears — including in a comment.

**2. WR-04 — the argument refusal stops echoing caller-controlled content into the build log.**

The branch prints the received arguments back into the log with an annotation prefix. `argv` is
unfiltered caller-controlled content, and the script is invoked by hand as well as by the workflow: the
verifier's own reproduction passed a decoy path, and a developer probing this control by pasting a
credential as an argument gets it printed back framed as a workflow error annotation. The COUNT is the
diagnostic; the CONTENT is not, and `:40-41`'s rule binds `argv` exactly as it binds the environment.

  - Delete the line that echoes the received argument strings. Replace it with two lines: one stating how
    many arguments were received, and one stating that their values are deliberately not echoed because
    the rule this file states about variable values binds `argv` too.
  - Keep every other line of the branch, including the measured reason and the `Fix:` line. The branch's
    argument is correct; only its disclosure is not.
  - Confirm cases 7 and 8 still pass unchanged. If either asserted on the echoed content, update the
    assertion to the count-and-shape form in this task rather than weakening the message.

**3. IN-02 — the sibling test's temp directory is resolved to its real path.**

`withScriptCopy` builds its expected path from `tmpdir()` while the string the script prints comes from a
realpath-derived resolution, so on any platform where the temp directory is a symlink the two differ and
two build-blocking assertions fail for a script that behaved correctly. Linux CI and Windows are
unaffected; macOS is the concrete case.

  - Wrap the `mkdtempSync` call in `realpathSync`, matching the idiom 19-14 wrote in
    `tests/design/workflow-invariants.test.ts`. Add the import.
  - Add one comment line naming the platform case, so the wrapper is not read as decoration and removed.
  - Change nothing else in that file. ⚠ In particular its header sentence recording that a past mutation
    left all 48 invariants green is a HISTORICAL measurement and stays byte-unchanged.

**4. Read the two new message blocks back against the code beside them.** For each line, name the code
that makes it true — the `Object.keys(process.env)` scan for the two input classes, `secretHitsIn` in the
other file for the blind spot, `process.argv.length` for the count. Enumerate that mapping in the SUMMARY.
If a line cannot be made true, delete it rather than shipping it.
  </action>

  <verify>
    <automated>npx vitest run --config vitest.design.config.ts tests/design/mail-credential-refusal.test.ts</automated>
    <fails_when>non-zero exit; or the reporter does not report `8 passed` — in particular case 6 failing would mean a message edit introduced a path by which a value reaches stdout, and cases 4 or 5 failing would mean the `realpathSync` wrapper changed which path the assertions compare</fails_when>

    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>non-zero exit; or any line whose first non-space token is `FAIL` — a red on the prefix-spelled-once invariant would mean an edit reformatted the declaration literal or introduced a copy of the provider token into the refusal script; or the final line is not exactly `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`</fails_when>

    <automated>node -e "const t=require('fs').readFileSync('scripts/refuse-mail-credential.mjs','utf8');const a=t.split('all reach here').length-1;const b=t.split('argv.slice').length-1;console.log('WR03-CLAIM-HITS='+a);console.log('WR04-ECHO-HITS='+b);process.exit(a===0&&b===0?0:1)"</automated>
    <fails_when>it prints anything other than `WR03-CLAIM-HITS=0` — the sentence claiming three input classes reach the process-environment scan is still present, and one of those three cannot reach it; or anything other than `WR04-ECHO-HITS=0` — caller-controlled argument content is still being echoed into the build log, against the rule the same file states at its lines 40-41</fails_when>

    <automated>node -e "const{readFileSync}=require('fs');const s=readFileSync('tests/design/mail-credential-refusal.test.ts','utf8');const ok=/realpathSync\(\s*mkdtempSync/.test(s);console.log('REALPATH-WRAPPED='+ok);process.exit(ok?0:1)"</automated>
    <fails_when>it prints `REALPATH-WRAPPED=false` — the temp directory is still built without resolving symlinks, so the two path assertions can fail on macOS for a reason unrelated to the property under test</fails_when>

    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/refuse-mail-credential.mjs','utf8');const p=/^const MAIL_KEY_PREFIX = \"([A-Z_]+)\";$/m.exec(fs.readFileSync('scripts/verify-workflows.mjs','utf8').replace(/\r\n/g,'\n'))[1];console.log('PREFIX-COPIES='+(s.split(p).length-1));process.exit(s.split(p).length-1===0?0:1)"</automated>
    <fails_when>it prints anything other than `PREFIX-COPIES=0` — a message edit spelled the mail-provider token into the refusal script, which turns the prefix-spelled-once invariant red and re-creates the second-source-of-truth this phase spent three plans removing</fails_when>

    <automated>git status --porcelain -- .github/workflows/ src/</automated>
    <fails_when>any output at all — this task touches neither the workflow files nor application source</fails_when>
  </verify>

  <acceptance_criteria>
    - The WR-03 gate above reports `WR03-CLAIM-HITS=0`, and the replacement message names exactly the two input classes the process-environment scan genuinely adds over the parse half.
    - The replacement message carries a ⚠ line naming the blind spot both halves share — a credential mapped under a key whose name does not begin with the provider prefix — and names the assertion in `scripts/verify-workflows.mjs` that covers it.
    - The WR-04 gate above reports `WR04-ECHO-HITS=0`, and the argument-refusal branch prints how many arguments were received together with a statement that their values are deliberately withheld.
    - The rest of the argument-refusal branch — the measured reason, the no-redirect statement and the `Fix:` line — is byte-unchanged.
    - `scripts/refuse-mail-credential.mjs` still carries `MAIL_KEY_PREFIX_DECL`'s source text verbatim and still holds ZERO copies of the prefix VALUE, proven by the gate above returning `PREFIX-COPIES=0` and by the prefix-spelled-once invariant passing in the same run.
    - `tests/design/mail-credential-refusal.test.ts` wraps its `mkdtempSync` call in `realpathSync`, carries a comment naming the macOS symlink case so the wrapper is not read as decoration, and still reports `8 passed`.
    - That file's header sentence recording a past all-48-green measurement is byte-unchanged (`git diff` confirms; recorded in the SUMMARY).
    - `node scripts/verify-workflows.mjs` still ends with `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`
    - The SUMMARY enumerates every line of both new message blocks beside the code that makes it true — the environment scan for the two input classes, the checker's zero-`secrets.` assertion for the blind spot, and the argument count for the count line.
  </acceptance_criteria>

  <done>Neither message in `scripts/refuse-mail-credential.mjs` claims an input the scan cannot see or prints content the file's own rule forbids, and the sibling design test compares paths that can actually be equal on every platform it runs on.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pull request → `.github/workflows/ci.yml`'s `on:` block | Nothing in the `ci` section crossed this boundary before this plan. One deleted line detaches every gate in the file from pull requests — the event CI-01's requirement text is literally about. |
| a workflow-level or job-level `defaults:` block → how every step below it executes | The outer half of the custom-shell finding. It reaches into `gate-e2e` from outside the job, so a step-key allow-list cannot see it. |
| the checker's printed total → the count documented in `ci.yml` and pinned in the design test | New in this plan. Three copies of one number, deliberately made byte-identical so drift between them is a build-blocking red rather than a sentence nobody re-reads. |
| the refusal script's stdout → a public build log | A control that detects a credential is one line away from disclosing it more widely than the send it prevented; the argument branch was doing exactly that with caller-controlled content. |
| repository / organization / container environment → the `gate-e2e` job's process | Unchanged from 19-12/19-13/19-14 and still the reason D-14 exists: a live mail credential here was measured at fourteen real outbound sends per suite run. The WR-03 fix corrects which parts of this boundary the control can actually observe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15-01 | Tampering | `ci.yml`'s trigger set vs. the `ci` section (WR-01) | high | mitigate | Task 1 adds a membership assertion over `triggersOf(doc)` requiring both the push and pull-request triggers, with a standing case that deletes the pull-request trigger from a mutated copy and asserts a named FAIL line. Reproduced at `CHECKER-EXIT=0` before the fix. |
| T-19-15-02 | Tampering | workflow-level and job-level `defaults:` blocks (CR-01, outer half) | high | mitigate | Task 1 forbids the whole block at both levels — allow-list shape, `undefined` the only passing value — with two standing cases. Neither level was read anywhere in the checker before this plan. |
| T-19-15-03 | Repudiation | a documented invariant total outliving the count it documents | high | mitigate | Task 1 moves the checker's total, `ci.yml`'s count paragraph, the invariant list's length and the design test's control constant in ONE commit, with the paragraph quoting the checker's summary line verbatim. Two grep gates assert no present-tense statement of the old total survives, scoped narrowly enough to preserve the four historical measurement sentences. |
| T-19-15-04 | Repudiation | a control failure message naming an input the control cannot see (WR-03) | high | mitigate | Task 2 replaces the claim with the two input classes the scan genuinely adds, and names the blind spot both halves share together with the assertion in the other file that covers it. Same defect class as the two blockers this round closes, at the smallest scale it occurs at. |
| T-19-15-05 | Information Disclosure | caller-controlled `argv` echoed into a build log (WR-04) | high | mitigate | Task 2 prints the argument COUNT and withholds the values, per the rule the file states about itself at its lines 40-41. The verifier's own reproduction passed a path here, and a developer probing by pasting a credential got it framed back as a workflow annotation. |
| T-19-15-06 | Tampering | a message edit introducing a second spelling of the mail-provider prefix | medium | mitigate | A gate asserts zero copies of the prefix VALUE in the refusal script, and the prefix-spelled-once invariant is re-run in the same task. The declaration literal is named read-only in the task's own constraints. |
| T-19-15-07 | Denial of Service | a build-blocking test failing for a platform reason unrelated to its property (IN-02) | medium | mitigate | Task 2 wraps the temp-directory creation in `realpathSync`, matching 19-14's harness. A gate asserts the wrapper is present so a later edit cannot quietly remove it as decoration. |
| T-19-15-08 | Tampering | a mutation escaping into the tracked tree | medium | mitigate | Every mutation lives inside a `mkdtemp` directory removed in a `finally`; the tracked `ci.yml` is read but never written, and the pull-request trigger is asserted rather than edited. `git status --porcelain` is a verify gate in both tasks. |
| T-19-15-09 | Spoofing | a mail credential reaching the job under a name that does not begin with the provider prefix, or through a variables context | low | accept | The runtime half is name-prefix-based and the parse half matches `secrets.` literals. Accepted rather than closed — but no longer accepted SILENTLY: Task 2's WR-03 fix makes the control name this blind spot in its own failure message and point at the assertion that covers the mapped case. |
| T-19-15-10 | Elevation of Privilege | `gate-e2e` as a merge gate (D-15) | medium | transfer | The gate reports but cannot block: branch protection returns a plan-upgrade refusal (403) on this repository. Transferred to the PM as a recorded visibility/billing decision and carried as a human-verification item. No task here works around it; the trigger assertion makes the gate harder to detach, not blocking. |
| T-19-15-SC | Tampering | npm/pip/cargo installs and third-party actions | high | mitigate | This plan installs nothing and adds no `uses:` entry to any workflow. `package.json` is not in `files_modified`; both scripts import only `node:` builtins and both tests import only `node:` builtins plus the already-installed `vitest`. 19-RESEARCH.md's Package Legitimacy Audit records zero packages for this phase with no `[ASSUMED]`/`[SUS]`/`[SLOP]` rows, so no legitimacy checkpoint is required. A package proposal inside this gap-closure run is a scope alarm. |
</threat_model>

<verification>
1. `node scripts/verify-workflows.mjs` — exit 0, final line exactly `All 50 invariants hold across 3 section(s) (baselines=11, ci=31, cross=8).`
2. `npx vitest run --config vitest.design.config.ts tests/design/workflow-invariants.test.ts` — `12 passed`.
3. `npx vitest run --config vitest.design.config.ts tests/design/mail-credential-refusal.test.ts` — `8 passed`, unregressed.
4. `npx vitest run --config vitest.design.config.ts` — the whole design suite green.
5. The new summary line appears verbatim in BOTH `.github/workflows/ci.yml` and `tests/design/workflow-invariants.test.ts`; the stale-count gate reports `STALE-COUNT-HITS=0` across the three count-bearing files.
6. `WR03-CLAIM-HITS=0`, `WR04-ECHO-HITS=0`, `REALPATH-WRAPPED=true` and `PREFIX-COPIES=0`.
7. `git diff -U0 -- .github/workflows/ci.yml` shows only `#` lines changed across the whole plan.
8. `git status --porcelain -- .github/workflows/ src/` empty after every task.
9. The SUMMARY records THREE observed reds against the pre-change checker — cases 10, 11 and 12, each with the observed exit code — plus the per-line enforcer mapping for both rewritten message blocks, plus the `git diff` confirmation that the four historical all-48-green sentences are byte-unchanged.
</verification>

<success_criteria>
- Deleting `pull_request:` from `ci.yml` turns `node scripts/verify-workflows.mjs` red with a named FAIL line — the one-line edit that was independently reproduced this session at all 48 invariants green, and the one that falsifies CI-01's literal requirement text.
- Declaring a `defaults:` block at workflow level or on `gate-e2e` turns the checker red, closing the outer half of the custom-shell finding at the level nothing in the checker read.
- Both properties are proven by standing cases in `tests/design/workflow-invariants.test.ts`, which runs inside `npm run build`.
- The invariant total is 50, the `ci` section is 31, and the same summary line appears byte-identically in the checker's output, in `ci.yml`'s count paragraph and in the design test's control constant — so a future count change cannot land silently.
- Both increments trace to a named gap remediation; no third check rode along.
- No message in `scripts/refuse-mail-credential.mjs` names an input the scan cannot see or prints caller-controlled content into a build log, and the sibling design test compares paths that can be equal on every platform it runs on.
- No job, step, `run:`, `env:` key, trigger, service, container option or timeout changed in `ci.yml`; nothing under `src/` is touched; no package is installed; `.planning/REQUIREMENTS.md` is byte-unchanged.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol below is created, renamed or first named by plan 19-15. This is the additive slice of the
phase-wide index; nothing here supersedes an entry from 19-01 … 19-14 except where marked.

**`scripts/verify-workflows.mjs`**
- The TRIGGER invariant — new `check()` in the `ci` section's file-level neighbourhood. Asserts `triggersOf(doc)` includes both the push and the pull-request trigger, by MEMBERSHIP rather than by set equality, with the reason for that shape and its deliberate asymmetry with the `baselines` section's exact comparison stated at the site. `ci` count +1.
- The EXECUTION-DEFAULTS invariant — new `check()` immediately after the unconditional check in the `gate-e2e` block. Asserts neither the workflow document nor `gate-e2e` declares a `defaults` block at all; `undefined` is the only passing value at each level. `ci` count +1.
- Two new local bindings for the parsed trigger list and for the two `defaults` reads, both printed in their invariants' evidence.
- The section totals move: `ci` 29 → 31, overall 48 → 50. No other `check()` is added, renamed or removed.

**`.github/workflows/ci.yml`**
- Invariant-list entry 8 — NEW, for the execution-defaults invariant. The `gate-e2e` list becomes EIGHT entries (was seven, restructured by 19-14 Task 3).
- A NEW top-level bullet for the trigger assertion, filed beside the other file-level bullets rather than inside the `gate-e2e` list, because its subject is the whole file.
- The fixed-count paragraph — REWRITTEN. States EIGHT entries, quotes the checker's new summary line verbatim, attributes both increments to their gaps, and updates the reader instruction's number. Supersedes the version 19-14 Task 3 re-stated.
- No new YAML symbol. No job, step, key, trigger, service or option is created, renamed or removed.

**`tests/design/workflow-invariants.test.ts`**
- `withWorkflowLevelBlock(blockLines)` — new mutation builder, inserts a zero-indented block before the `jobs:` key.
- `withoutPullRequestTrigger()` — new mutation builder, removes the pull-request trigger line and its terminator.
- Cases 10, 11 and 12 — NEW: a workflow-level `defaults:` block, a job-level one, and a deleted pull-request trigger. Case total: 12 (was 9).
- `EXPECTED_GREEN` — CHANGED to the new summary line. This constant is the pin that makes a silent count change impossible.

**`scripts/refuse-mail-credential.mjs`**
- The environment-hit failure message — REWRITTEN (WR-03). Names the two input classes the process-environment scan genuinely adds over the parse half, and names the blind spot both halves share together with the assertion in the other file that covers it. Supersedes 19-12's wording.
- The argument-refusal message — AMENDED (WR-04). The line echoing received argument content is DELETED and replaced by an argument count plus a statement that values are deliberately withheld. Supersedes 19-13's wording.
- UNCHANGED and load-bearing: `MAIL_KEY_PREFIX_DECL`'s literal, the absence of any copy of the prefix VALUE, every exit code, and every control-flow branch.

**`tests/design/mail-credential-refusal.test.ts`**
- `withScriptCopy`'s temp-directory creation — CHANGED to `realpathSync(mkdtempSync(...))` (IN-02), with a comment naming the macOS symlink case. Case count unchanged at 8.

**Not produced, deliberately:** no third `check()`; no `--workflow-dir` flag, environment-variable input
or second entry point on the checker; no edit to `vitest.design.config.ts` or `package.json`; no edit to
`.planning/REQUIREMENTS.md`; no new package; no schema object; no `COVERAGE.md`; no change to any of the
four historical sentences recording a past all-48-green measurement.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-15-SUMMARY.md` when done.

It MUST contain, verbatim rather than paraphrased:
- cases 10, 11 and 12 failing against the PRE-change checker, with the observed exit code in each message;
- the checker's summary line before and after Task 1, showing 48 → 50;
- the three-way byte gate's output proving the new summary line appears in the checker, in `ci.yml` and in
  the design test constant;
- the stale-count gate's full output, and the `git diff` confirmation that the four historical
  all-48-green sentences are byte-unchanged;
- the per-line enforcer mapping for both rewritten message blocks in `scripts/refuse-mail-credential.mjs`;
- the `WR03-CLAIM-HITS=0`, `WR04-ECHO-HITS=0`, `PREFIX-COPIES=0` and `REALPATH-WRAPPED=true` gate outputs;
- the comments-only `git diff -U0` count for `.github/workflows/ci.yml`;
- the empty `git status --porcelain -- .github/workflows/ src/` after every task;
- a `## Deviations from Plan` ledger in 19-13-SUMMARY.md's format, including auto-fixed issues;
- an explicit statement of what CI-01's traceability row in `.planning/REQUIREMENTS.md` has now earned,
  recorded here rather than by editing that file.
</output>
