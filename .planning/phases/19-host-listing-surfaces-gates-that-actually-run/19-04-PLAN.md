---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-paths-manifest.19-33.json
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-path-routes-manifest.prod.json
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/dev-artifact-mtimes.txt
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt
autonomous: false
requirements: [HSURF-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The .next/dev evidence — the stale dev manifest, the complete production manifest and the artifact mtimes — is archived under the phase's evidence/ directory BEFORE any clean, because rm -rf .next destroys every row of it (D-09)."
    - "Each of the four orphan draft rows is re-verified EMPTY per row at execution time — not trusted from the research table and not trusted from the creation window (D-01)."
    - "The four rows' full contents are recorded on disk before the delete, so the record survives the rows (D-01)."
    - "Exactly four listing rows are deleted, scoped by host id and by the UTC creation window, and the count is confirmed before and after."
    - "The open PM question is recorded, not assumed: D-01's window and host id are LOCAL facts, so a production database may hold orphan drafts this phase does not touch."
  artifacts:
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-paths-manifest.19-33.json
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-path-routes-manifest.prod.json
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/dev-artifact-mtimes.txt
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt
  key_links:
    - "The archive must run before plan 19-05's rm -rf .next; that ordering is the whole reason this plan is a separate wave."
    - "listing.created_at is timestamptz (schema.ts:263), so the window literals stay UTC and are not translated to local time."
    - "A hard DELETE cascades to listing_photo, listing_amenity, listing_activity_tag, operating_hours and availability_block — which is harmless only BECAUSE the per-row check proves those are empty."
---

<objective>
Preserve the only subject HSURF-02 has left, then clear the defect's residue from the database.

The dev server that exhibited the phantom 404 is gone (measured 2026-09-04: nothing on :3000). Its
`.next/dev` artifacts survive and are the entire remaining evidence base — and D-09's first command,
`rm -rf .next`, destroys all of it. So the archive happens first.

Purpose: D-01 deletes four real rows in a real database. The window is how they were FOUND, not proof
of what they CONTAIN, so emptiness is re-verified per row at execution time and the contents are
written down before the rows stop existing.
Output: an evidence archive, a per-row verification record, four fewer orphan drafts, and one open
question raised rather than absorbed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: archive the .next/dev evidence, before anything clears it</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-paths-manifest.19-33.json, .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-path-routes-manifest.prod.json, .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/dev-artifact-mtimes.txt</files>
  <precondition>`.next/dev/server/app-paths-manifest.json` and `.next/app-path-routes-manifest.json` still exist on this machine. If either is already gone, STOP and record which — the archive cannot be reconstructed and plan 19-05's finding must say so rather than describe evidence it does not have.</precondition>
  <read_first>
    - `19-RESEARCH.md § 1.0` — the seven measured probes, the three consequences, and the four archive commands. This is the only section that matters for this task.
    - `19-RESEARCH.md § Runtime State Inventory` — the row headed "Live service config", which states plainly that these artifacts are not in git and are destroyed by the clean.
    - `19-VALIDATION.md § Manual-Only Verifications` — the row "The `.next/dev` evidence archive", and its "one-shot capture of a state `rm -rf .next` destroys".
  </read_first>
  <action>
    Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/` and run the
    four archive commands from 19-RESEARCH § 1.0 item 3, verbatim:

    (1) copy `.next/dev/server/app-paths-manifest.json` to
    `evidence/app-paths-manifest.19-33.json`;
    (2) copy `.next/app-path-routes-manifest.json` to
    `evidence/app-path-routes-manifest.prod.json`;
    (3) write a recursive `ls -la --time-style=full-iso` of
    `.next/dev/server/app/(host)/host/listings` into `evidence/dev-artifact-mtimes.txt`;
    (4) append a `ls -la --time-style=full-iso` of `.next/dev` to the same file.

    Then re-take, into the summary, the seven measurements 19-RESEARCH § 1.0 tabulates, so the
    archive is accompanied by the numbers that make it readable:
    - the entry count of the dev manifest, and its `grep -c` result for the edit and availability
      routes (expected 16 entries, 0 and 0);
    - whether the compiled `edit/page.js` artifact exists and its mtime (expected present, `Sep 3
      15:18`), and whether the availability artifact exists (expected absent);
    - the production manifest's `grep -c` for the edit route (expected 1) and its mtime;
    - the `.next/dev` session-marker mtimes (expected `18:09` for `package.json`,
      `prerender-manifest.json`, `routes-manifest.json` and `types/`; `19:33` for `server/`,
      `build-manifest.json` and `trace`).

    ⚠ If a number differs from the research table, record the difference rather than the expectation.
    The whole value of this archive is that it is a measurement, and a mismatch is itself a finding.
    ⚠ Do not run any clean, build or dev-server command in this task. Plan 19-05 owns that ordering,
    and a `next build` racing a `next dev` on the shared `.next` is one of the four candidates the
    phase must NOT accidentally trigger before the evidence is safe.
  </action>
  <verify>
    <automated>ls -la .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/</automated>
    <fails_when>Fewer than three files are listed, or any of `app-paths-manifest.19-33.json`, `app-path-routes-manifest.prod.json`, `dev-artifact-mtimes.txt` is missing or zero bytes.</fails_when>
    <automated>node -e "const j=require('./.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-paths-manifest.19-33.json'); console.log('entries='+Object.keys(j).length)"</automated>
    <fails_when>The command throws (the archived file is not valid JSON, i.e. the copy was truncated), or the printed entry count is 0.</fails_when>
    <automated>grep -c 'listings/\[id\]/edit' .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/app-path-routes-manifest.prod.json</automated>
    <fails_when>The count is 0 — the archived PRODUCTION manifest is missing the edit route, which would contradict the measurement the whole reframing rests on and must be reported rather than smoothed over.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four archive outputs exist under `evidence/` and none is zero bytes.
    - `evidence/app-paths-manifest.19-33.json` parses as JSON and its key count is recorded in the summary.
    - `grep -c 'listings/\[id\]/edit' evidence/app-paths-manifest.19-33.json` returns 0 and `grep -c 'listings/\[id\]/availability' evidence/app-paths-manifest.19-33.json` returns 0.
    - `grep -c 'listings/\[id\]/edit' evidence/app-path-routes-manifest.prod.json` returns 1.
    - `evidence/dev-artifact-mtimes.txt` contains full-ISO timestamps for both the `.next/dev/server/app/(host)/host/listings` tree and the `.next/dev` directory.
    - The summary tabulates all seven measurements against the research's expected values, naming any that differ.
    - No clean, build or dev-server command was run in this task.
  </acceptance_criteria>
  <reversibility rating="reversible">This task only copies files into the phase directory; it changes nothing and deletes nothing. It exists to make plan 19-05's one-way clean survivable — capturing the state IS the reversal path for that later step, which is why the two are in different waves.</reversibility>
  <done>Every artifact `rm -rf .next` would destroy is on disk under `evidence/`, accompanied by the numbers that make it readable, and nothing has been cleaned, built or started.</done>
</task>

<task type="auto">
  <name>Task 2: re-verify the four orphan drafts EMPTY, per row, and record what they contain</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt</files>
  <precondition>The local Postgres container `fitout-db-1` is running and reachable (`docker ps` shows it up), and the `fitout` database is the one the research measured. If the container is not up, start it with `npm run db:up` before proceeding; do NOT point this at any other database.</precondition>
  <read_first>
    - `19-CONTEXT.md § D-01` in full — including its own warning: "Verify emptiness per row before deleting, not just by window — the window is how they were found, not proof of what they contain."
    - `19-RESEARCH.md § 5.1` — the four uuids, their UTC creation timestamps, the owning host id `AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy`, and the ⚠ that this table is a research-time read that the plan must re-take.
    - `19-RESEARCH.md § Code Examples § The per-row emptiness verification D-01 requires` — the exact SQL, with its `untouched` / `empty_columns` / `photos` / `amenities` / `bookings` projections and its "DELETE only if" rule.
    - `src/db/schema.ts:199-295` — the `listing` table, and specifically `:262` `deleted_at`, `:263` `created_at` as `timestamp with time zone`, and the cascade declarations at `:295`, `:314`, `:325`, `:478`, `:1035`.
  </read_first>
  <action>
    Run the per-row verification query from 19-RESEARCH § Code Examples against the local `fitout`
    database, scoped by `host_id = 'AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy'`, `deleted_at IS NULL`, and
    `created_at BETWEEN '2026-09-03 10:51:56Z' AND '2026-09-03 10:52:40Z'`.

    ⚠ The window literals are UTC because `created_at` is `timestamptz`. Do not translate them to
    local time — the project's own rule is that all times are stored UTC and converted only at the
    edges.

    Capture the full result set, plus the four rows' complete column contents, into
    `evidence/orphan-drafts-before-delete.txt`. D-01 requires the record to survive the rows, so this
    file is the reversal path: it must contain enough to re-insert an equivalent row if that were
    ever wanted.

    Then evaluate the delete condition PER ROW, not over the window: a row is deletable only if
    `untouched` is true AND `empty_columns` is true AND `photos = 0` AND `amenities = 0` AND
    `bookings = 0`.

    ⚠ The per-row check is not optional and it has two independent jobs. First, it is what D-01
    demands. Second, a hard DELETE cascades to `listing_photo`, `listing_amenity`,
    `listing_activity_tag`, `operating_hours` and `availability_block` — harmless here ONLY because
    the check proves those are empty.

    ⚠ If the query returns a row count other than 4, or if ANY row fails the condition, STOP. Do not
    delete a subset silently and do not widen the window. Record what was found and escalate — a
    fifth row in that window, or a row with content, means the window is no longer describing what
    the research measured.
  </action>
  <verify>
    <automated>test -s .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt && echo "record present"</automated>
    <fails_when>The command prints nothing or exits non-zero — the row record is missing or empty, so the delete would destroy the only copy of what those rows contained.</fails_when>
    <automated>grep -c '3e224ec0-f015-4d18-a58b-ad08ce165d18\|3b22e548-762b-484e-a828-5d0b52b4904f\|485e4843-8b5c-4059-aec0-43b30971e40c\|e6ca32d0-41c1-4fbf-9cee-79402b962c51' .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt</automated>
    <fails_when>The count is less than 4 — at least one of the four measured uuids is absent from the record, so either the query scope drifted or a row is already gone and that must be reported.</fails_when>
    <automated>docker exec fitout-db-1 psql -U fitout -d fitout -tAc "SELECT count(*) FROM listing l WHERE l.host_id = 'AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy' AND l.deleted_at IS NULL AND l.created_at BETWEEN '2026-09-03 10:51:56Z' AND '2026-09-03 10:52:40Z' AND NOT (l.updated_at = l.created_at AND l.title IS NULL AND l.description IS NULL AND l.city IS NULL AND l.hourly_rate_cents IS NULL AND l.day_rate_cents IS NULL AND l.per_head_price_cents IS NULL AND l.cancellation_policy IS NULL AND l.published_at IS NULL AND (SELECT count(*) FROM listing_photo p WHERE p.listing_id = l.id) = 0 AND (SELECT count(*) FROM listing_amenity a WHERE a.listing_id = l.id) = 0 AND (SELECT count(*) FROM booking b WHERE b.listing_id = l.id) = 0)"</automated>
    <fails_when>The printed count is anything other than `0`. A non-zero count names how many rows in that window FAIL the emptiness condition — those rows carry a host's real content and are NOT deletable under D-01. Halt and escalate; do not delete a subset.</fails_when>
    <automated>docker exec fitout-db-1 psql -U fitout -d fitout -tAc "SELECT count(*) FROM listing WHERE host_id = 'AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy' AND deleted_at IS NULL AND created_at BETWEEN '2026-09-03 10:51:56Z' AND '2026-09-03 10:52:40Z'"</automated>
    <fails_when>The printed count is anything other than `4` — a fifth row appeared in the window, or one is already gone. Either way the window no longer describes what the research measured, and that must be reported rather than smoothed over.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/orphan-drafts-before-delete.txt` exists, is non-empty, and contains all four measured uuids.
    - The file records, per row: `untouched`, `empty_columns`, `photos`, `amenities`, `bookings`, and the full column dump.
    - Every row reports `untouched = true`, `empty_columns = true`, `photos = 0`, `amenities = 0`, `bookings = 0`. If any does not, the task halts and the summary records what was found instead.
    - The result set contains exactly 4 rows. A different count halts the task.
    - The window literals in the executed SQL are UTC, byte-identical to `'2026-09-03 10:51:56Z'` and `'2026-09-03 10:52:40Z'`.
  </acceptance_criteria>
  <done>Four rows are proven empty per row and per cascade table, their contents are on disk, and nothing has been deleted yet.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: delete the four orphan drafts, and raise the production-scope question</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt (read); no source file is modified — the change is four rows in the local fitout database</files>
  <read_first>
    - `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/orphan-drafts-before-delete.txt` — Task 2's record. Read it before deleting; the delete is authorised by that file, not by the research table.
    - `19-CONTEXT.md § D-01` — "Reversibility: costly — a delete of real rows in a real database. Take the row contents into the plan's summary before deleting so the record survives the rows."
    - `19-RESEARCH.md § Open Questions` item 2 — "Is there a production database with orphan drafts of the same shape?" and its recommendation: a PM question, not a code question.
    - `19-VALIDATION.md § Negative Space` — the last bullet, which states that this phase deliberately does NOT validate that the delete is safe in any production database.
  </read_first>
  <decision>
    D-01 deletes four real rows from a real database. It is a hard `DELETE`, not the soft delete the
    application uses — the `listing` table has `deleted_at` and `softDeleteListing` writes it, but
    D-01 says the rows are DELETED and rates the reversibility **costly**.

    **What is being decided:** whether to proceed with the hard delete now that Task 2's per-row
    verification is on disk.

    **Evidence in favour:** all four rows report `untouched`, no title, no description, no city, no
    pricing of any kind, no cancellation policy, no `published_at`, zero photos, zero amenities, zero
    bookings. No host intentionally created them; they are the defect's residue. Their full contents
    are recorded at `evidence/orphan-drafts-before-delete.txt`, which is the reversal path.

    **What is NOT being decided, and must be raised rather than absorbed:** D-01's creation window
    and host id are **LOCAL facts**. A production database, if one exists, may hold orphan drafts of
    the same shape that this phase does not touch — and its window and host id would be different,
    so the scope would have to be re-derived there, never copied. The research found no deployment
    configuration in the tree and records the same absence independently.

    **Options:**
    1. Proceed with the scoped hard delete of exactly these four rows (D-01 as written).
    2. Soft-delete instead (set `deleted_at`), diverging from D-01.
    3. Defer, leaving the residue in place.

    Present all three, note that option 1 is the locked decision, and record the answer to the
    production-scope question in the summary either way.
  </decision>
  <options>
    1. **Proceed with the scoped hard DELETE of exactly these four rows.** This is D-01 as written and
       as locked. Reversal path: `evidence/orphan-drafts-before-delete.txt`.
    2. **Soft-delete instead** — set `deleted_at` on the four rows rather than removing them. Diverges
       from D-01's literal text; keeps the rows recoverable in place; leaves them excluded from every
       normal read, so the grid looks the same either way.
    3. **Defer** — leave the residue in place. The idempotent creation in plan 19-06 stops the class
       at its source regardless, so nothing downstream depends on this delete.

    Separately, and needed either way: **does a production database exist?** If yes, D-01's scope must
    be re-derived there — the window and the host id are local facts and must never be copied.
  </options>
  <resume-signal>
    Reply with the chosen option number. On **1** or **2**, the executor runs the statement inside a
    transaction, confirms the affected-row count is exactly 4 before committing, and rolls back and
    escalates on any other count. On **3**, the executor records the deferral and its reason in the
    summary and moves on. In all three cases, answer the production-database question so it can be
    recorded as answered rather than left open.
  </resume-signal>
  <action>
    On approval of option 1: execute a `DELETE FROM listing` scoped by
    `host_id = 'AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy'`, `deleted_at IS NULL`, and the same UTC window,
    inside a transaction. Confirm the reported row count is exactly 4 before committing; if it is
    anything else, roll back and escalate.

    After committing, re-run Task 2's verification query and confirm it now returns zero rows.

    Then record the open PM question in the summary verbatim, as an open item rather than a resolved
    one: **D-01's window and host id are LOCAL facts, so a production database may hold orphan drafts
    this phase does not touch; if a deployed environment exists, the scope must be re-derived there,
    not copied.**

    ⚠ Do not extend the window "while we are in here". Eleven further untouched empty drafts owned by
    other, e2e-minted accounts exist in this database and are deliberately OUTSIDE D-01's scope,
    which is host-scoped and window-scoped.
  </action>
  <verify>
    <automated>docker exec fitout-db-1 psql -U fitout -d fitout -tAc "SELECT count(*) FROM listing WHERE host_id = 'AcW4AhUfkMexEvvEUa8KjsngD7ubMoZy' AND deleted_at IS NULL AND created_at BETWEEN '2026-09-03 10:51:56Z' AND '2026-09-03 10:52:40Z'"</automated>
    <fails_when>The printed count is anything other than `0` — the delete did not remove all four rows, or it was scoped differently than verified.</fails_when>
    <automated>docker exec fitout-db-1 psql -U fitout -d fitout -tAc "SELECT count(*) FROM listing WHERE id IN ('3e224ec0-f015-4d18-a58b-ad08ce165d18','3b22e548-762b-484e-a828-5d0b52b4904f','485e4843-8b5c-4059-aec0-43b30971e40c','e6ca32d0-41c1-4fbf-9cee-79402b962c51')"</automated>
    <fails_when>The printed count is anything other than `0` — at least one of the named rows survives.</fails_when>
    <automated>ls drizzle/*.sql | tail -1</automated>
    <fails_when>The printed filename is anything other than `drizzle/0029_listing_review_cascade.sql` — a migration file appeared, which violates the v1.2 zero-schema-migrations invariant. D-01 is a DELETE, not DDL.</fails_when>
  </verify>
  <acceptance_criteria>
    - The delete reported exactly 4 rows affected, and that number is recorded in the summary.
    - Re-running Task 2's verification query returns zero rows.
    - `ls drizzle/*.sql | tail -1` still returns `drizzle/0029_listing_review_cascade.sql`.
    - `git status --porcelain package.json` produces no output — zero dependency changes.
    - The summary carries the production-scope question as an OPEN item, in the PM-question wording above, not as a resolved one.
    - The summary states that the eleven other untouched drafts owned by e2e-minted accounts were deliberately left alone.
  </acceptance_criteria>
  <reversibility rating="costly">A hard delete of real rows. The reversal path is `evidence/orphan-drafts-before-delete.txt`, which records every column of every row — which is exactly why D-01 requires the record to be taken first, and why this task cannot run before Task 2.</reversibility>
  <done>The four orphan drafts are gone, their contents survive on disk, exactly four rows were affected, no migration appeared, and the production-scope question is raised rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan → the live development database | This plan issues a destructive statement against real rows. There is no application layer in front of it. |
| Build artifacts → the phase's evidence base | `.next/` is not in git; once cleared, nothing reconstructs it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15 | Tampering | `listing` rows in the local database | high | mitigate | Per-row emptiness verification precedes the delete, the delete is scoped by host id AND the UTC window, it runs in a transaction with an affected-row-count check before commit, and the full row contents are archived first. |
| T-19-16 | Denial of Service | Cascade deletion of related rows | high | mitigate | The hard delete cascades to five child tables. The per-row check proves `photos = 0`, `amenities = 0` and `bookings = 0` first, which is the second, independent reason that check is not optional. |
| T-19-17 | Repudiation | Loss of the `.next/dev` evidence | medium | mitigate | The archive runs in wave 4, before plan 19-05's clean in wave 5. The wave split exists for this ordering alone. |
| T-19-18 | Information Disclosure | Archived manifests in the repository | low | accept | The archived files are route path lists and directory listings from a local build. They contain no credential, no user data and no host identity beyond a route shape already present in `src/`. |
| T-19-19 | Elevation of Privilege | Widening the delete scope | high | mitigate | Eleven further empty drafts owned by other accounts exist in the same database and are explicitly out of scope. The acceptance criteria pin the affected-row count at exactly 4. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged — zero packages installed. `git status --porcelain package.json` producing no output is an acceptance criterion. |
</threat_model>

<verification>
1. All four archive outputs exist under `evidence/` and are non-empty.
2. `evidence/orphan-drafts-before-delete.txt` contains all four uuids with their full column dumps.
3. The post-delete count query returns 0.
4. `ls drizzle/*.sql | tail -1` still returns `drizzle/0029_listing_review_cascade.sql`.
5. `git status --porcelain package.json` produces no output.
</verification>

<success_criteria>
- The `.next/dev` evidence is archived before anything cleans it.
- Emptiness was re-verified per row at execution time, not trusted from the research window.
- Exactly four rows were deleted and their contents survive on disk.
- The production-scope question is on the record as OPEN.
</success_criteria>

## Artifacts this phase produces

This plan's slice of the phase-wide index in `19-01-PLAN.md`:

**New files, all under `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/`**
- `app-paths-manifest.19-33.json` — the stale dev route manifest, 16 entries, missing the edit and availability routes.
- `app-path-routes-manifest.prod.json` — the complete production manifest, for contrast.
- `dev-artifact-mtimes.txt` — the full-ISO listing of the `(host)/host/listings` compiled tree and of `.next/dev`.
- `orphan-drafts-before-delete.txt` — the four rows' per-row verification and full column dump; the reversal path for the delete.

**Runtime state changed (recorded because no file in the repo records it):** four `listing` rows
deleted from the local `fitout` database. Eleven further untouched empty drafts owned by other
accounts are deliberately untouched.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-04-SUMMARY.md` when done.

It must record:
- the seven `.next/dev` measurements, re-taken, against the research's expected values, naming any that differ;
- the four rows' contents, per row, per column, and their `untouched` / `empty_columns` / `photos` / `amenities` / `bookings` values;
- the affected-row count reported by the delete (expect exactly 4) and the post-delete count (expect 0);
- ⚠ the OPEN PM question, verbatim: D-01's window and host id are LOCAL facts, so a production database may hold orphan drafts this phase does not touch;
- confirmation that the eleven e2e-minted drafts were left alone, and that `drizzle/` still ends at `0029_listing_review_cascade.sql`.
</output>
