---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - scripts/verify-workflows.mjs
autonomous: true
requirements: [CI-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fifth CI job named gate-e2e exists in .github/workflows/ci.yml and runs the functional Playwright set by project name, not by spec path (D-12)."
    - "gate-e2e is a NEW job — gate-price-parity's run command is byte-unchanged and still names exactly one spec file (D-12)."
    - "gate-e2e refuses to run when a live mail credential is present in its environment, and says so with an ::error:: line before anything boots (D-14)."
    - "scripts/verify-workflows.mjs refuses any ci.yml where a workflow, job or step env KEY begins with the mail-provider prefix, and that refusal has been watched red (D-14)."
    - "node scripts/verify-workflows.mjs exits 0 with gate-e2e present in both its parsed jobs list and its parsed containerized list."
    - "gate-e2e carries a wall-clock stop (D-13) rather than an estimate, because the number does not exist yet."
  artifacts:
    - .github/workflows/ci.yml
    - scripts/verify-workflows.mjs
  key_links:
    - "gate-e2e's DATABASE_URL addresses the postgres service by LABEL, which verify-workflows.mjs:489-537 enforces for containerized jobs."
    - "gate-e2e pins mcr.microsoft.com/playwright:v1.60.0-noble, the same string baselines.yml's capture job pins (verify-workflows.mjs cross section :664-680)."
    - "The D-14 step's env KEY is deliberately NOT the mail-provider key name, so the step and the new parser invariant do not fight each other."
---

<objective>
Ship CI-01's fifth job. `gate-e2e` runs the repository's functional Playwright specs on every pull
request, in the same pinned container the existing jobs use, holding nothing.

This plan LEADS the phase because `CI-01` is what makes every guard the rest of the phase writes
capable of running — shipping a guard before the gate would ship a guard nothing runs.

Purpose: close the remaining half of D-24. Two of four jobs already run Playwright; the functional
`e2e/*.spec.ts` set is what is left, and per D-12 it arrives as a **new** job, never as a widening of
`gate-price-parity`.
Output: `gate-e2e` in `ci.yml`, and D-14's fail-closed mail-credential refusal in two places — a
runtime step in the job and a parse-based invariant in `scripts/verify-workflows.mjs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: gate-e2e end-to-end — one new job, checkout to a real Playwright run</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — read the file header's mutation table first, then `gate-price-parity` at `:766-841` in full. That job is the skeleton; copy it, do not invent one.
    - `.github/workflows/ci.yml:598-599` — `gate-db-free` already runs the workflow verifier; that is where Task 2's invariant will fire.
    - `.github/workflows/ci.yml:626-641` — the five module-scope boot guards `gate-db-free`'s build step enumerates, and the written argument against softening a guard to make a check green.
    - `19-RESEARCH.md § 4.1` — the full `gate-e2e` YAML with its four docblocks. Copy the comments; they carry the argument.
    - `19-RESEARCH.md § 4.2` — the three deltas from job 3, each with its measurement.
    - `19-RESEARCH.md § 4.3` — the eight existing invariants the new job must satisfy, with the `verify-workflows.mjs` line numbers that enforce each.
    - `19-PATTERNS.md § 1` — the same skeleton stated as a pattern assignment.
    - `playwright.config.ts` — `projects[0].name` is `chromium`, `testMatch: "e2e/*.spec.ts"`, `retries: process.env.CI ? 2 : 0`, `reuseExistingServer: false`, `webServer` is `npm run dev`.
  </read_first>
  <action>
    Append a fifth job `gate-e2e` to `.github/workflows/ci.yml`, copying `gate-price-parity`
    (`:766-841`) as the skeleton per D-12. `name: gate-e2e (functional Playwright suite)`,
    `runs-on: ubuntu-latest`, `container.image: mcr.microsoft.com/playwright:v1.60.0-noble` with
    `options: --ipc=host`, one `postgres` service on `postgis/postgis:18-3.6` carrying the four
    `POSTGRES_*` values and the `pg_isready -U fitout -d fitout` health block, and job-level
    `env.DATABASE_URL: postgres://fitout:fitout@postgres:5432/fitout`.

    Exactly three deltas from job 3, and no others (19-RESEARCH § 4.2):
    (1) add a `npm run db:seed` step after `npm run db:migrate` — five specs fail against an empty
    catalogue and say so in their own failure messages (`axe-sweep.spec.ts:1166`,
    `overflow-320.spec.ts:941`, `reduced-motion.spec.ts:259`, `shell.spec.ts:1131`,
    `skeleton-geometry.spec.ts:504`);
    (2) add `timeout-minutes: 45` — D-13's wall-clock does not exist yet, so this is a stop, not an
    estimate, and the comment must say so;
    (3) the run command becomes `npx playwright test --project=chromium` with no spec path — naming
    the project rather than using the `test:e2e` script, because that script also collects the
    `visual` project which job 4 owns.

    Add the D-14 refusal as the FIRST step after `npm ci`, named
    `Refuse to run the suite with a live mail credential in the environment`. Its `env` key is
    `MAIL_KEY_UNDER_TEST`, whose value is the `${{ env.RESEND_API_KEY }}` context expression; the
    step `exit 1`s with an `::error::` line naming the measured cost when that variable is non-empty.
    ⚠ The step's env KEY is deliberately different from the mail-provider variable name so it cannot
    trip Task 2's invariant — say so in the step comment at both sites.

    The job's negative space is load-bearing and each absence gets a comment: no `permissions:` block
    (it inherits the workflow default), no browser-provisioning step (the pinned image ships them,
    and `ci.yml:770-774` records that adding one is the most common way a container job ends up
    comparing against a browser nobody pinned), no port-mapping key on the service (a containerized
    job reaches a service by label), no repository-secret reference, and no compile step of any kind
    (a job that compiles the app would join `verify-workflows.mjs`'s T-11-DBFREE set at `:552-561`
    and redden it, and it would also boot the app in a mode that prunes `instrumentation.ts`'s
    network seam).

    ⚠ Do not spell the snapshot-update flag anywhere in `ci.yml`, not even in prose — the header
    records that prose about a forbidden token is still the token.
    ⚠ Do not touch `gate-price-parity`. Its run command must stay byte-identical.
  </action>
  <verify>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>Exit code is non-zero, or the `parsed values (ci)` block's `jobs` line does not contain `"gate-e2e"`, or its `containerized` line does not contain `"gate-e2e"`. A red on the T-11-DBFREE invariant means a compile step was added to this job; a red on the addressing invariant means the service was addressed by host instead of by label.</fails_when>
    <automated>git diff HEAD --numstat -- .github/workflows/ci.yml | awk '{print "deleted="$2}'</automated>
    <fails_when>The printed `deleted=` count is anything other than 0 — any removed line means an existing job was modified rather than a fifth job appended, which is exactly the D-12 mutation `scripts/verify-workflows.mjs:600` exists to catch.</fails_when>
    <automated>sed -n '/^  gate-e2e:/,$p' .github/workflows/ci.yml | grep -v '^\s*#' | grep -c 'playwright install'</automated>
    <fails_when>The count is greater than 0 — a browser-provisioning step inside the `gate-e2e` block means the job can compare against a browser nobody pinned (D-12).</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-workflows.mjs` exits 0 and its `parsed values (ci)` `jobs` line reads `["gate-db-free","gate-db","gate-price-parity","gate-visual","gate-e2e"]`.
    - The same printout's `containerized` line ends with `"gate-e2e"` and its `service images` list gains a `{"job":"gate-e2e","svc":"postgres","image":"postgis/postgis:18-3.6"}` entry.
    - `git diff HEAD --numstat -- .github/workflows/ci.yml` reports a deletion count of 0.
    - `sed -n '/^  gate-e2e:/,$p' .github/workflows/ci.yml` contains the literal `--project=chromium` and contains no spec path after it.
    - The same extracted block contains `timeout-minutes: 45` and a `npm run db:seed` step positioned after `npm run db:migrate`.
    - `grep -c 'e2e/price-parity.spec.ts' .github/workflows/ci.yml` still returns 2 (its measured value at HEAD — one comment mention, one run command), proving job 3 was neither widened nor renamed.
  </acceptance_criteria>
  <reversibility rating="reversible">A workflow job addition is one commit to revert, and it is merged non-required (D-15), so it cannot block a PR until the flip in plan 19-08.</reversibility>
  <done>`ci.yml` carries five jobs, the fifth runs the whole functional Playwright set by project name against a seeded ephemeral database, it holds one `DATABASE_URL` and nothing else, and the workflow verifier sees it and passes all 20 existing invariants.</done>
</task>

<task type="auto">
  <name>Task 2: the mail-credential invariant in the parser, watched red</name>
  <files>scripts/verify-workflows.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - `scripts/verify-workflows.mjs:81-89` — `CI_VISUAL_JOB` and `SNAPSHOT_UPDATE_FLAG`: the established discipline of spelling a forbidden token in the CHECKER, never in the workflow.
    - `scripts/verify-workflows.mjs:428-437` — the snapshot-flag check, the exact idiom to mirror.
    - `scripts/verify-workflows.mjs:441-451` — the existing repository-secret scan, so the new check reads as a sibling of it rather than as a new kind of thing.
    - `19-RESEARCH.md § 4.4` — both shapes, and the measured reason the runtime step alone is not the strongest one.
    - `19-PATTERNS.md § 2` — the assignment: same file, same check idiom.
    - `instrumentation.ts` — its header's written refusal to fold the mail provider into the PayMongo mock, and why (the client binds at module load, so the key is the switch).
  </read_first>
  <action>
    Add D-14's shape 2 to the `ci` section of `scripts/verify-workflows.mjs`, beside the existing
    secret scan at `:441-451`, following `SNAPSHOT_UPDATE_FLAG`'s discipline: declare a
    module-scope constant holding the mail-provider variable name and its prefix, spelled ONCE here
    and never in `ci.yml`, with a comment stating that reason.

    The check walks `Object.entries(doc.jobs)`, and for each job collects any `job.env` key, any
    step `env` key from `stepsOf(job)`, and any workflow-level `doc.env` key that begins with the
    mail-provider prefix. It asserts the collected list is empty, and its failure detail prints the
    hits and the number of jobs scanned — `verify-workflows.mjs`'s standing "it prints what it
    checked" property.

    The comment must carry the measurement, not a warning: finding `[17-D28]` measured fourteen real
    outbound sends per full suite run whenever the key is set, `instrumentation.ts` deliberately does
    not mock that origin, and nothing else stands between an unrelated secret addition and mail to
    real addresses on every pull request.

    Then WATCH IT RED (Pattern 4, the project's standing rule): temporarily add a job-level env entry
    whose key is the mail-provider variable name to any job in `ci.yml`, run
    `node scripts/verify-workflows.mjs --section=ci`, confirm the new check fails BY NAME and that
    its detail names the offending `job.env.KEY`, record the failure line verbatim in the summary,
    then revert the temporary edit and confirm the section returns to green.

    ⚠ Do not rename the plan 19-01 Task 1 step's `MAIL_KEY_UNDER_TEST` env key to the mail-provider
    name. That would make this new check red against a correct file. The two sites are deliberately
    spelled differently and each says so.
  </action>
  <verify>
    <automated>node scripts/verify-workflows.mjs --section=ci</automated>
    <fails_when>Exit code is non-zero, or the summary line does not report a count of 21 ci invariants (20 existing plus the new one). If it still reports 20, the new check was declared but never registered through `check(...)`.</fails_when>
    <automated>node scripts/verify-workflows.mjs</automated>
    <fails_when>Exit code is non-zero on any of the three sections — the `cross` section compares container images across `ci.yml` and `baselines.yml`, and a red there means `gate-e2e` pinned a different image string.</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-workflows.mjs --section=ci` exits 0 and its final line reports `ci=21`.
    - The new invariant's `ok` line names the mail-provider variable and prints a scanned-jobs count of 5.
    - The watched-red evidence is recorded in the plan summary: the verbatim failure line produced with a temporary mail-provider env key added, and confirmation the section returned green after reverting.
    - `git diff HEAD --numstat -- .github/workflows/ci.yml` still reports a deletion count of 0 after the watched-red revert.
    - `node scripts/verify-workflows.mjs` exits 0 across all three sections.
  </acceptance_criteria>
  <reversibility rating="reversible">One added check in one script; deleting it restores the prior behaviour exactly.</reversibility>
  <done>A parse-based invariant in `gate-db-free` — the cheap job that always runs — refuses any `ci.yml` where a workflow, job or step env key names the mail provider, it has been seen failing for the right reason with the offending key named, and the runtime step in `gate-e2e` and this invariant do not collide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party network | A CI job that holds a credential can reach the outside world on every pull request, including from a fork-authored branch. |
| Workflow file → runner environment | Any `env:` entry in `ci.yml` becomes a process environment variable inside a container this repository does not own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Information Disclosure | `gate-e2e` job env | high | mitigate | The job holds exactly one input, `DATABASE_URL`, pointing at an ephemeral service container. Zero repository-secret references — asserted by `verify-workflows.mjs:441-451`, which runs on every push. |
| T-19-02 | Information Disclosure | Outbound mail from CI | high | mitigate | D-14 in two places: a runtime `exit 1` step that runs before anything boots, and a parse-based invariant in `gate-db-free` that catches it about a minute earlier. Measured exposure: 14 real sends per suite run ([17-D28]). |
| T-19-03 | Tampering | Browser provenance | medium | mitigate | The pinned `mcr.microsoft.com/playwright:v1.60.0-noble` image supplies browsers; no provisioning step, so the job cannot silently compare against an unpinned browser. Enforced by `verify-workflows.mjs:454-461`. |
| T-19-04 | Tampering | Committed visual baselines | medium | mitigate | The job names `--project=chromium`, so it never collects the `visual` project; `playwright.config.ts` additionally sets `updateSnapshots: "none"` unconditionally, so a baseline cannot be minted even by accident. |
| T-19-05 | Denial of Service | Runner occupancy | low | mitigate | `timeout-minutes: 45` bounds a wedged suite. Documented as a stop, not an estimate — D-13's real number replaces it in plan 19-08. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged. `19-RESEARCH.md § Package Legitimacy Audit` records zero packages installed by this phase; `npm ci` is unchanged and `package.json` is not in `files_modified`. A package proposal here is a milestone scope alarm. |
</threat_model>

<verification>
1. `node scripts/verify-workflows.mjs` exits 0 across all three sections.
2. The `parsed values (ci)` printout names five jobs, four containerized-plus-one, and four service images.
3. `git diff HEAD -- .github/workflows/ci.yml` shows additions only.
4. The watched-red failure line for the mail invariant is recorded verbatim in the summary.
</verification>

<success_criteria>
- `gate-e2e` exists as a fifth job and runs `npx playwright test --project=chromium`.
- `gate-price-parity` is byte-unchanged.
- The mail-credential refusal exists in both the job and the parser, and the parser half has been watched red.
- Zero `package.json` changes and zero `drizzle/` changes.
</success_criteria>

## Artifacts this phase produces

The complete phase-wide index. Every symbol below is created or first named by phase 19; each later
plan repeats only its own slice.

**New files**
| Path | Plan | What it is |
|---|---|---|
| `e2e/host-listing-grid.spec.ts` | 19-02 | HSURF-01's two guards, three viewport bands |
| `e2e/host-route-reachability.spec.ts` | 19-06 | D-11's routing guard, anonymous, no fixture |
| `src/lib/listing/create-signal.ts` | 19-07 | D-03's copy module |
| `tests/design/listing-card-merge-order.test.ts` | 19-03 | the `cn()` append-order standing check (D-06) |
| `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/` | 19-04, 19-05 | one-shot `.next/dev` archive, the D-01 row record, the probe matrices |
| `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-FINDING-404.md` | 19-05 | HSURF-02's written finding, with (a)/(b)/(c) dispositions |

**New exported symbols**
| Symbol | File | Plan |
|---|---|---|
| `LISTING_CREATE_FAILED_STATE` | `src/lib/listing/create-signal.ts` | 19-07 |
| `LISTING_CREATE_FAILED_REASON` | `src/lib/listing/create-signal.ts` | 19-07 |
| `LISTING_CREATE_FAILED_CTA` | `src/lib/listing/create-signal.ts` | 19-07 |
| `LISTING_CREATE_FAILED_PARAM` | `src/lib/listing/create-signal.ts` | 19-07 |
| `composeListingCreateFailedSentence()` | `src/lib/listing/create-signal.ts` | 19-07 |
| `seedHostGridFixture()` (name at executor's discretion) | `e2e/helpers/booker-seed.ts` | 19-02 |

**New CI job name:** `gate-e2e` — display name `gate-e2e (functional Playwright suite)` (19-01).

**New spec names (Playwright test titles):**
- `host-listing-grid.spec.ts` — one test per band: `320x800`, `700x900`, `1280x900`, each asserting guard A clause 1 (skipped at 320), guard A clause 2, and guard B (19-02).
- `host-route-reachability.spec.ts` — one test per route: `/host/listings`, `/host/listings/new`, `/host/listings/route-reachability/edit`, `/host/listings/route-reachability/availability`, each titled `… resolves to its module (anonymous ⇒ 307)` (19-06).

**Spec-count bookkeeping:** `ls e2e/*.spec.ts | wc -l` is **37** at phase start and **39** at phase
end. `gate-e2e` runs all of them. Record the number in every summary that touches it rather than
letting it drift.

**Deliberately NOT produced:** no new runtime dependency, no `drizzle/` migration (the tree still
ends at `0029_listing_review_cascade.sql`), no edit to `src/components/ui/card.tsx` or
`src/components/ui/button.tsx`, no edit to `src/app/(host)/host/listings/[id]/edit/page.tsx`, and no
change to `deriveBookable`.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-01-SUMMARY.md` when done.

It must record, verbatim:
- the `parsed values (ci)` `jobs` and `containerized` lines after the change;
- the watched-red failure line for the mail invariant, and confirmation of the revert;
- the current `ls e2e/*.spec.ts | wc -l` count (expect 37 at this wave);
- that `gate-e2e` is merged NON-REQUIRED, and that the required-check flip is plan 19-08's (D-15).
</output>
