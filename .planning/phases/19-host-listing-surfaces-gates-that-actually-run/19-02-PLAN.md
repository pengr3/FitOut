---
phase: 19-host-listing-surfaces-gates-that-actually-run
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - e2e/helpers/booker-seed.ts
  - e2e/host-listing-grid.spec.ts
  - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt
autonomous: true
requirements: [HSURF-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A seed helper in e2e/helpers/booker-seed.ts produces a host owning at least three listings whose CardContent heights genuinely differ, and at least one of them is PUBLISHED."
    - "e2e/host-listing-grid.spec.ts measures guard A (card bottom minus footer bottom, at most 1px) and guard B (scrollWidth at most clientWidth on the footer element itself) at 320x800, 700x900 and 1280x900 (D-08)."
    - "Both guards have been WATCHED RED against the pre-fix tree and their failure messages are recorded verbatim in the plan summary."
    - "Each guard's failure message names the mechanism and the file:line, not merely that a number moved."
    - "partial (list-item ListingCard) — missing cover, title, pricing and weekly hours each render their shipped fallback, and the fixture deliberately exploits that, because differing CardContent heights are what make the footer-flush claim able to fail."
    - "zero-one-many (list-collection) — 0 listings renders EmptyState; 1 renders a single card with the equal-bottoms clause skipped by row grouping; 2 or more runs the clause per visual row grouped by cardTop within 1px, so a second row at lg is never compared against the first."
    - "long-text (list-item title) — a long title wraps to two lines at 320px and is one of the three named height-variation sources; TitleTag stays font-medium leading-snug with no truncation."
    - "zero-one-many (list-item footer control cluster) — the ml-auto div holds ONE control on a draft and TWO on a published listing, and only the four-control published case can make guard B fail, which is why the fixture must include a published listing."
  artifacts:
    - e2e/helpers/booker-seed.ts
    - e2e/host-listing-grid.spec.ts
    - .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt
  key_links:
    - "The spec joins the chromium project automatically via playwright.config.ts testMatch e2e/*.spec.ts, so it rides gate-e2e with no workflow change."
    - "The fixture extends booker-seed.ts rather than opening a seventh postgres() client, which deferred-items.md warns against."
    - "e2e/overflow-320.spec.ts's /host/listings row stays byte-unchanged — the new guards live in a new file for the three reasons in 19-RESEARCH § 3.5."
---

<objective>
Build the instrument before the fix, and prove it can fail.

HSURF-01's whole difficulty is that the three obvious assertions are ALREADY GREEN against the
shipped defect. This plan writes the two that are not, and watches both go red against the current
tree so the next plan's change is measured rather than asserted.

Purpose: D-08 requires two guards because fixing either defect does not fix the other. A guard that
has never been seen failing is a rubber stamp, and a fixture without genuine height variation makes
guard A vacuous no matter how the assertion is written.
Output: a reusable seed fixture, a new three-band Playwright spec, and a recorded pre-fix red.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-RESEARCH.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-PATTERNS.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-UI-SPEC.md
@.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the fixture — a host whose cards are genuinely different heights, one of them published</name>
  <files>e2e/helpers/booker-seed.ts</files>
  <read_first>
    - `e2e/helpers/booker-seed.ts:190-240` — `SeedOptions` / `SeededListing` and `seedBookableListing`, the `withClient` open-write-close idiom this addition must follow.
    - `e2e/helpers/booker-seed.ts:611` — `seedApprovedHostVerification(email)`, and its docblock explaining why a host with no verification row cannot mint a draft at all.
    - `e2e/axe-sweep.spec.ts:294-315` — `mintDraftListing`, including the ⚠ in its docblock. **Its contract changes under D-02**: after plan 19-06 lands, calling it twice returns the SAME id, so a fixture that mints N drafts by repeating it gets one.
    - `e2e/host-headings.spec.ts:203-223` — `signUpHost` and `resumeSession`, and the `SessionCookies` type at `:182`.
    - `src/components/listing/listing-card.tsx:368-430` — the conditionally-rendered space-type line (`:377-381`), hours notice (`:396-405`) and review notice (`:418-429`). These are the height-variation sources.
    - `src/app/(host)/host/listings/page.tsx:118-121` — `loadPublishedListingsMissingHours`, which is what drives the two-line hours notice.
    - `19-RESEARCH.md § 3.4` and `19-UI-SPEC.md § The fixture the state contract depends on`.
  </read_first>
  <action>
    Extend `e2e/helpers/booker-seed.ts` with one exported seed helper for the `/host/listings` grid.
    It must produce, for ONE host, at least three listings whose rendered `CardContent` heights
    genuinely differ, and **at least one of them PUBLISHED**.

    The three cards, and what makes each a different height (19-RESEARCH § 3.4, cheapest first):
    (1) a **draft with no title** — renders "Untitled listing" and no space-type line;
    (2) a **published listing with no weekly hours** — renders the two-line hours notice, and this is
    also the card that renders FOUR footer controls because `Unlist` is gated on
    `status === "published"`;
    (3) a **long title** that wraps to two lines at 320px.

    ⚠ The published card is not a nicety. A draft renders three footer controls and a published
    listing renders four, and only the four-control case can make guard B fail. A drafts-only fixture
    leaves guard B green against the broken tree — the same vacuity trap repeating on the second
    guard. This was confirmed against the PM's screenshot: the draft card's Delete rendered fully
    while the published cards clipped Unlist and Delete.

    Follow the file's own pattern: a short-lived `withClient` connection inside the helper, so the
    spec holds no `postgres()` client of its own. Six e2e specs already hold one and
    `deferred-items.md` warns against a seventh — extend this file, do not open a new client.

    ⚠ Do not build the fixture by calling `mintDraftListing` in a loop. Under D-02 that returns one
    row, not N. Seed the listings directly through this helper.

    Reuse `seedApprovedHostVerification` for the host, or the wizard route cannot be reached at all.
    Give the helper a teardown that removes exactly the rows it wrote, matching how the existing
    seeders in this file clean up.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `e2e/helpers/booker-seed.ts` — a type error in the new helper's options or return shape.</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('e2e/helpers/booker-seed.ts','utf8'); const n=(s.match(/postgres\(/g)||[]).length; console.log('postgres( occurrences:',n); process.exit(0)"</automated>
    <fails_when>The printed count is higher than it was before this task — a new long-lived client was opened instead of reusing the file's `withClient` idiom.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "export async function" e2e/helpers/booker-seed.ts` returns a count one higher than at HEAD.
    - The new helper's source contains a status value of `published` for at least one of the seeded listings, and a title-less listing among them.
    - The helper does not call `mintDraftListing`, and `grep -c "mintDraftListing" e2e/helpers/booker-seed.ts` returns 0.
    - The helper's docblock states, in prose, why a published listing is required (three footer controls versus four) and why repeating the wizard mint would not work under D-02.
  </acceptance_criteria>
  <done>One exported helper seeds a host with at least three grid cards of visibly different content heights, at least one published, with a teardown, and the file still holds no additional database client.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/host-listing-grid.spec.ts — guard A and guard B, three bands</name>
  <files>e2e/host-listing-grid.spec.ts</files>
  <read_first>
    - `19-RESEARCH.md § 3.1` — guard A's assertion body verbatim, including why `getBoundingClientRect` rather than `offsetHeight` and why the row grouping by `cardTop`.
    - `19-RESEARCH.md § 3.2` — guard B's assertion body verbatim, including the `offenders` list and the argument for `el.children` granularity.
    - `19-RESEARCH.md § 3.3` — the three bands and why 700 and 1280 rather than 640 and 1024.
    - `19-RESEARCH.md § 3.5` — why this is a new file and not a row in `overflow-320.spec.ts`.
    - `e2e/overflow-320.spec.ts` — copy its table-driven shape and its named-offender diagnostic. Do NOT copy its single-viewport premise or its document-level assertion. **Leave its `/host/listings` row (`:3129-3150`) byte-unchanged.**
    - `e2e/host-headings.spec.ts:704-710` — the resize-don't-renavigate `open()` helper with its `document.fonts.ready` settle.
    - `src/components/ui/card.tsx:15` — `overflow-hidden` and `has-data-[slot=card-footer]:pb-0`. Read only; never edit (D-04).
    - `src/components/ui/button.tsx:74` — `shrink-0` and `whitespace-nowrap`. Read only.
    - `tests/design/selector-contract.test.ts:20,50-51` — `.locator(` is recorded, not gated, and `data-slot` is a vendored-primitive attribute rather than a test id, so nothing new needs declaring.
  </read_first>
  <action>
    Create `e2e/host-listing-grid.spec.ts` carrying D-08's two guards at three viewport bands.

    Structure: ONE navigation to `/host/listings` with the Task 1 fixture's session, then three
    `setViewportSize` reads — `320x800`, `700x900`, `1280x900` — each followed by
    `await page.evaluate(() => document.fonts.ready)` before measuring. Resize, do not re-navigate;
    the grid is server-rendered and its content does not depend on the viewport.

    **Vacuity gate first**, before any geometry claim: assert at least two `[data-slot="card"]`
    elements rendered, with a message telling the reader to seed a host with listings. An empty grid
    satisfies every assertion below it.

    **Guard A**, per band, from `$$eval` over `[data-slot="card"]`, reading
    `getBoundingClientRect()` for the card and for its `[data-slot="card-footer"]` child:
    - clause 1 — cards sharing a visual row (grouped by `cardTop` within 1px) report one distinct
      rounded `cardBottom`. **Skip this clause at 320px**, where there is one column and nothing to
      compare. It is already true today; it is kept as a vacuity companion and as a regression guard
      against anyone adding an items-start alignment to the grid wrapper.
    - clause 2 — **this is the one that is red today**: `cardBottom - footerBottom` is at most 1px,
      for every card in the measured row, at every band including 320px. `Card`'s base carries
      `has-data-[slot=card-footer]:pb-0`, so ZERO is the designed gap; the 1px is sub-pixel tolerance
      and must not be widened.

    **Guard B**, per band, from `$$eval` over `[data-slot="card-footer"]`: `scrollWidth` is at most
    `clientWidth` on the footer element ITSELF. This is the one signal the ancestor's clipping cannot
    mask. Collect an `offenders` list from the footer's direct children whose right edge exceeds the
    footer's right edge by more than 0.5px, and print it in the failure message so a red names WHICH
    control is clipped.

    Failure messages must name the mechanism, the file and the line — the standard every instrument
    in this repository already meets. Guard A clause 2's message states that the footer band is
    floating mid-card, that `Card` is a top-packed column at the call site, and that the fix is a
    top-margin-auto on the footer rather than a full-height class on the card, which is a measured
    no-op because the grid already stretches its items. Guard B's message states that the controls
    are CLIPPED rather than spilled, names `ui/card.tsx:15` and `ui/button.tsx:74` as the reason, and
    says that a document-level scan reports green against this defect.

    ⚠ The new file must not import the shared document-overflow helper module — that helper's
    assertion is exactly the one that is green against the defect, and mixing it in would make a
    reader believe the document scan is part of the proof.
    ⚠ Do not touch `e2e/overflow-320.spec.ts`. Its `touch: []` argument is reviewed code and is the
    binding precedent that the hit-area bar for this control cluster is 24px, not 44px.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit code is non-zero, or the output names `e2e/host-listing-grid.spec.ts`.</fails_when>
    <automated>npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --list</automated>
    <fails_when>Exit code is non-zero, or the listing does not enumerate one test per band naming 320, 700 and 1280 — a spec that collects zero tests reports success and measures nothing.</fails_when>
    <automated>git diff HEAD --stat -- e2e/overflow-320.spec.ts</automated>
    <fails_when>Any output at all — the reviewed `/host/listings` row and its `touch: []` argument must be byte-unchanged.</fails_when>
    <automated>grep -cE '^\s*import .*helpers/overflow' e2e/host-listing-grid.spec.ts</automated>
    <fails_when>The count is greater than 0 — the document-level overflow helper was imported here, which dresses a vacuous assertion as proof. The pattern is scoped to import statements so the spec's docblocks may name the helper they deliberately do not use.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --list` collects at least three tests, and their titles contain `320`, `700` and `1280`.
    - `grep -c 'data-slot="card-footer"' e2e/host-listing-grid.spec.ts` returns at least 2 (guard A reads the footer's rect; guard B reads its scroll box).
    - `grep -c "getBoundingClientRect" e2e/host-listing-grid.spec.ts` returns at least 2 — one read for the card box and one for the footer box. Positions, not heights: the offset parent of a static card inside a grid is not the card.
    - `grep -cE '^\s*import .*helpers/overflow' e2e/host-listing-grid.spec.ts` returns 0.
    - `grep -c "document.fonts.ready" e2e/host-listing-grid.spec.ts` returns at least 3 — one settle per band.
    - `git diff HEAD --stat -- e2e/overflow-320.spec.ts` produces no output.
    - `ls e2e/*.spec.ts | wc -l` returns 38.
  </acceptance_criteria>
  <done>A new spec measures both HSURF-01 guards at three bands, gated on a non-empty grid, with failure messages that name the mechanism rather than a moved number, and the reviewed single-viewport spec is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: watch both guards go RED against the pre-fix tree, verbatim</name>
  <files>.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt</files>
  <precondition>The local Postgres container is up and `npm run db:migrate` has been applied; Playwright boots its own dev server (`playwright.config.ts` sets `reuseExistingServer: false`, so any stray server on :3000 must be killed first or the run fails).</precondition>
  <read_first>
    - `19-VALIDATION.md § ⚠ Read this before writing any HSURF-01 assertion` — the three signals that are already green, and why a green guard here is a FIXTURE defect rather than a satisfied requirement.
    - `19-RESEARCH.md § 3.4` last paragraph — the project's standing vacuity rule, with its three shipped precedents (`selector-contract.test.ts`, `axe.ts`'s `MIN_SCANNED_NODES`, `overflow.ts`'s `MIN_EXAMINED_ELEMENTS`).
    - `tests/design/e2e-email-silence.test.ts:34-53` — four watched reds recorded in one file, and the stated reason: an absence assertion cannot notice its own subject is gone.
  </read_first>
  <action>
    Run the new spec against the CURRENT, unfixed tree and capture the output to
    `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt`.

    Both guard A clause 2 and guard B must FAIL. Copy their failure messages verbatim into the plan
    summary — the numbers matter: the measured pixel gap below the footer per card, and the
    `scrollWidth` against `clientWidth` pair with the named offenders.

    If guard A clause 2 comes back GREEN, that is a fixture defect and not a satisfied requirement:
    the three cards are rendering at the same content height. Go back to Task 1 and widen the
    variation — a title-less draft against a published listing with the two-line hours notice is the
    cheapest pair that works.

    If guard B comes back GREEN, check first that the fixture actually contains a PUBLISHED listing.
    A draft renders three footer controls and three fit where four do not.

    Record which bands each guard failed at. The `lg` band and the 320px floor are the two the
    arithmetic predicts as tightest; if only one band goes red, say which and say so plainly rather
    than generalising.
  </action>
  <verify>
    <automated>npx playwright test e2e/host-listing-grid.spec.ts --project=chromium --reporter=list > .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt 2>&1; test $? -ne 0</automated>
    <fails_when>The command exits non-zero, which here means Playwright exited 0 — every guard passed against the unfixed tree. That is a vacuous instrument, not a working one: re-check the fixture's height variation and its published listing.</fails_when>
    <automated>grep -c "dead card BELOW its footer" .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt</automated>
    <fails_when>The count is 0 — guard A clause 2 did not fire, so the fixture is producing no height variation and clause 2 is measuring nothing.</fails_when>
    <automated>grep -c "overflows its own content box" .planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt</automated>
    <fails_when>The count is 0 — guard B did not fire, most likely because the fixture contains no published listing and every card rendered only three footer controls.</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/guards-pre-fix.txt` exists, is non-empty, and contains at least one failing test.
    - The file contains guard A clause 2's message with a concrete pixel gap greater than 1.
    - The file contains guard B's message with a `scrollWidth` value greater than its `clientWidth` value, and a non-empty offenders list.
    - The plan summary quotes both failure messages verbatim, including the numbers and the offender names.
    - The summary names which bands each guard failed at, rather than saying "all bands" without evidence.
  </acceptance_criteria>
  <done>Both guards are proven capable of failing against the shipped defect, with numbers, at named bands, and the evidence is on disk rather than in a claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → local database | The seed helper writes real rows to the development database and must remove exactly what it wrote. |
| Playwright browser → the app under test | The spec drives a real session; an ambient or leaked session would change what the grid renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-06 | Tampering | Development database rows | medium | mitigate | The helper follows `booker-seed.ts`'s `withClient` open-write-close idiom and ships a teardown that removes exactly its own rows, matching the existing seeders in that file. |
| T-19-07 | Denial of Service | Database connection pool | low | mitigate | No seventh long-lived `postgres()` client is opened; `deferred-items.md` records the contention hazard and the acceptance criteria pin the client count. |
| T-19-08 | Information Disclosure | Seeded host identity | low | accept | The fixture creates a throwaway host with a tagged email in a local development database. No real user data is involved and nothing is logged beyond the test reporter. |
| T-19-09 | Spoofing | Ambient session in the guard run | low | mitigate | The spec resumes an explicit fixture session rather than relying on whatever cookie the browser context happens to hold, following `host-headings.spec.ts:203-223`. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not engaged — zero packages installed. `@playwright/test` is already pinned at 1.60.0 and `postgres` is already a dependency. A package proposal here is a milestone scope alarm. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `ls e2e/*.spec.ts | wc -l` returns 38.
3. `git diff HEAD --stat -- e2e/overflow-320.spec.ts` produces no output.
4. `evidence/guards-pre-fix.txt` contains both failure messages, with numbers.
</verification>

<success_criteria>
- The fixture produces genuine height variation and includes at least one published listing.
- Both guards exist, at three bands, in a new file.
- Both guards have been watched red and the messages are recorded verbatim.
- `e2e/overflow-320.spec.ts` is byte-unchanged.
</success_criteria>

## Artifacts this phase produces

This plan's slice of the phase-wide index in `19-01-PLAN.md`:

**New files**
- `e2e/host-listing-grid.spec.ts` — HSURF-01's two guards at three bands. Test titles contain `320`, `700`, `1280`.
- `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/evidence/guards-pre-fix.txt` — the watched-red capture.

**New exported symbol**
- one seed helper in `e2e/helpers/booker-seed.ts` (name at the executor's discretion; `seedHostGridFixture` is the suggested one), plus its teardown.

**Spec-count bookkeeping:** `ls e2e/*.spec.ts | wc -l` moves 37 → 38 here, and 38 → 39 in plan 19-06.

<output>
Create `.planning/phases/19-host-listing-surfaces-gates-that-actually-run/19-02-SUMMARY.md` when done.

It must record, verbatim:
- guard A clause 2's failure message, with the measured pixel gap per card;
- guard B's failure message, with the `scrollWidth`/`clientWidth` pair and the offenders list;
- which viewport bands each guard failed at;
- the fixture's composition (which listing is published, which is title-less, which has a long title);
- the new `ls e2e/*.spec.ts | wc -l` count (expect 38).
</output>
