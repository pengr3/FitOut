# Phase 17.1 — probe evidence

**This file is this phase's permanent probe-transcript record.** It plays for 17.1 the role
`.planning/quick/260830-r4b-sticky-bar-footer-clearance/baseline-evidence.md` plays for that quick:
every number a plan in this phase measures lands here **verbatim, as a number on a named route at a
named width in a named build**, not paraphrased into a SUMMARY sentence (D-14).

Two rules this file is written under, both inherited from `baseline-evidence.md` §7a:

1. **The prediction is written BEFORE the reading, in the same section.** A prediction written after
   a measurement is a description. Each `§ P*` states what was expected and why, then what was read.
2. **Corrections are APPENDED, never substituted.** If a later drive contradicts a reading recorded
   here, the original stays byte-identical and the correction is added beneath it with its date. A
   record that can be silently rewritten stops being evidence.

| § | Probe | Item | Written by | Status |
|---|---|---|---|---|
| **P1** | The production status-line probe — does the shipped layout assert on `/listings/[id]` survive `next build && next start`? | 2 (soft-404) | plan **17.1-01** | **recorded** |
| P2 | The checkout route's 640–1023px sticky-bar band — is a control actually occluded? | 1b | a later plan | pending |
| P3 | The PayMongo endpoint census — method + full URL + count per run, across both specs | 3 | a later plan | pending |
| P4 | The seven-width computed-padding read on the repaired checkout `<main>` | 1b | a later plan | pending |
| **W1** | The **watched red** — does deleting `(detail)/not-found.tsx` actually red the four instruments that name it? | 4 (dead boundary) | plan **17.1-02** | **recorded** |

Later plans append their own section. `P*` sections are **probes** — a reading taken of the running
product. `W*` sections are **watched reds** — an instrument driven into failure on purpose, to prove
it is enforced rather than remembered. Both are transcripts and both live here.

---

## § P1 — `/listings/[id]` under `next build && next start` (item 2, assumption `[A6]`)

**Driven by:** plan **17.1-01**, Task 1, on **2026-08-30**. Manual-only by design — no harness in this
repo boots `next start` (17.1-RESEARCH § Validation Architecture). The three `curl` codes below **are**
the deliverable.

### The question, and why it had to be asked in production

Every reading of `/listings/[id]`'s soft-404 to date is a `next dev` reading. `[17-D2]` measured the
identical mechanism in a **production** build, but on `(host)` — a different segment. So `[A6]` — *that
the soft-404 behaves the same in a production build as in `next dev`* — was never driven on this route.

### The prediction, written before the drive

**404 / 404 / 200** (draft / nonexistent / published control), per 17.1-RESEARCH **C1**.

C1's argument, restated so this section stands alone: `[17-D1]` claims
`git log d24b212..HEAD -- 'src/app/listings/[id]/(detail)/'` is empty. It is not — it contains
`89fb451 fix(260826-l1o): /listings/[id] answers a real 404 again`, which landed **72 minutes after**
`[16-D6]` measured the 200. The question therefore **inverted**: this probe asks *does the shipped
layout-assert hold under a production build?*, and

> **a 200 on the draft is a REGRESSION of `89fb451`, not a reproduction of a known defect.**

**The published control is mandatory.** A probe that only reads 404 cannot distinguish "the guard
works" from "the whole app is 500ing". A 5xx on any of the three would be the `src/lib/paymongo.ts`
boot guard or a build failure — Pitfall 5 — and **not** a status-line finding.

### The mechanism under test (already shipped)

`src/app/listings/[id]/(detail)/layout.tsx` awaits `assertPublicListing(id)`. `loading.tsx` creates an
implicit `<Suspense>` boundary and Next flushes the shell — committing `200` — before the page body
reaches `notFound()`. A **layout** renders in that shell, above the boundary, so awaiting there blocks
the flush and the status line is still settable. `assertPublicListing` is `cache()`d, so the layout's
lookup and the page's are one query.

### Build provenance

| | |
|---|---|
| commit the build was made from | **`d69f1e43ceed20a76b9a92fe36435f937db06b58`** (`d69f1e4`, `docs(17.1): add the pattern map`) |
| branch | `dev` |
| working tree at build time | no source file modified — only `.planning/STATE.md` (M) and two untracked paths (`.claude/`, `e2e/tmp-checkout-band.spec.ts`). The build is `d69f1e4`'s source. |
| `.next/` | **cleared** (`rm -rf .next`) before building — a stale `.next/dev/types` is the recorded cause of a class of false reds |
| build command | `npm run build` (= `npm run lint && npm run test:design && next build`) — **exit 0** |
| server version line, verbatim | `▲ Next.js 16.2.7` |
| server ready | `✓ Ready in 279ms` |

### The exact `next start` invocation

```
PLATFORM_WALLET_NUMBER=09170000000 PLATFORM_WALLET_NAME="local" \
  node ./node_modules/next/dist/bin/next start -p 3100
```

Three operational facts carried, each with the reason it is not cosmetic:

- **The wallet values are throwaway literals** (`09170000000` / `local`), passed **inline on that one
  process**. `src/lib/paymongo.ts:39-48` refuses to boot in production without them and is exempted
  only for `NEXT_PHASE=phase-production-build` — which is exactly why `npm run build` is green while
  `npm start` is not. Both vars are **absent from `.env.local` and stayed absent**
  (`grep -c "PLATFORM_WALLET" .env.local` → **0**; `git diff --name-only -- .env.local .env.example`
  → empty). No payout path is exercised by a status-line read.
- **Port 3100, never 3000.** `playwright.config.ts:113` sets `reuseExistingServer: !process.env.CI`,
  so a production server left on :3000 is adopted by every later Playwright run — the `[17-D24]` trap.
  3100 is the port `[17-D2]` used.
- **`node ./node_modules/next/dist/bin/next` rather than `npx next`,** so the shell owns the real
  server PID directly. Killing an `npm`/`npx` wrapper leaves `next start` alive holding the port.

### The listing ids — QUERIED on this run, not copied from any planning document

```
$ docker exec fitout-db-1 psql -U fitout -d fitout -tAc "select id from listing where status = 'draft' limit 1;"
0454c21f-4f18-44cc-9cee-5f376f1470c6

$ docker exec fitout-db-1 psql -U fitout -d fitout -tAc "select id from listing where status = 'published' limit 1;"
uat_listing_notpayable
```

Seed state on this DB today: **22 draft / 16 published**. Both ids verified `deleted_at IS NULL`, so
the published one is genuinely publicly viewable under `isPubliclyViewable` (which folds soft-deletion
into the rule) and is a valid control.

### THE READING — three status codes on three named routes

```
$ for u in "listings/$DRAFT" "listings/a-listing-that-must-never-exist-17-1" "listings/$PUB"; do
    printf '%-60s -> ' "/$u"; curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:3100/$u"
  done

/listings/0454c21f-4f18-44cc-9cee-5f376f1470c6               -> 404
/listings/a-listing-that-must-never-exist-17-1               -> 404
/listings/uat_listing_notpayable                             -> 200
```

| # | Route | Kind | Status | Predicted |
|---|---|---|---|---|
| 1 | `/listings/0454c21f-4f18-44cc-9cee-5f376f1470c6` | **draft** listing | **404** | 404 ✔ |
| 2 | `/listings/a-listing-that-must-never-exist-17-1` | **nonexistent** id | **404** | 404 ✔ |
| 3 | `/listings/uat_listing_notpayable` | **published** listing — the CONTROL | **200** | 200 ✔ |

**Second pass, same server, immediately after — identical: 404 / 404 / 200.** No 5xx appeared on any
route on either pass, so Pitfall 5 does not apply and these are status-line readings.

Bodies were **never captured** (`curl -o /dev/null`, status line only), so no draft listing's content
can have reached this document — T-17.1-01's mitigation, driven rather than asserted.

### The reading against the prediction

**Match. `404 / 404 / 200`, exactly as C1 predicted.**

- **`[A6]` is settled POSITIVELY on `/listings/[id]`.** The shipped layout assert survives a
  production build. The soft-404 on this route is *fixed*, not *dormant*.
- **`89fb451` is not regressed.** 17-01's `next dev` non-reproduction was measuring a **repaired**
  route, not a lucky environment — which is precisely what C1 argued from the git history and what
  this probe confirms from the wire.
- **No repair is licensed on this route.** Item 2 closes as: `[A6]` measured, `[17-D1]` corrected and
  re-scoped, `[17-D2]` folded in.
- **`[17-D2]` keeps its meaning.** It remains the production measurement of the **unfixed** mechanism,
  on `(host)`, whose group-level `loading.tsx` is untouched by this phase. The two rows now read as a
  matched pair: same mechanism, same build type, one segment repaired and one not.
- **Task 3 of plan 17.1-01 is NOT REACHED.** Its entry condition is a 200 on the draft.

### Server teardown

| | |
|---|---|
| shell job PID | `4837` |
| **real Windows listener PID on :3100** | **`3188`** (`netstat -ano` while serving) |
| after kill | `tasklist /FI "PID eq 3188"` → *No tasks are running which match the specified criteria* |
| port 3100 | `curl --max-time 3 http://localhost:3100/` → **exit 7, connection refused**. Only client-side `TIME_WAIT` sockets remain; **no LISTENING socket**. |

**No production server was left holding a port.** The `[17-D24]` adoption trap is not armed.

### One operational finding, recorded because it is the same trap from the other side

Clearing `.next/` before the build broke a **pre-existing, long-running `next dev` server on :3000**
(Windows PID `11696`, `next/dist/server/lib/start-server.js`, ~2.1 GB RSS, started before this
session). It began answering **500 on every route**, and because `reuseExistingServer: !process.env.CI`
makes Playwright **adopt** whatever is on :3000 rather than start its own, the acceptance-criterion e2e
run would have read as a wall of false reds attributable to nothing in this phase.

It was terminated (`taskkill /PID 11696 /T /F`) so Playwright would boot a clean `next dev`. Recorded
because it is `[17-D24]` arriving from the *dev* side: **any drive that clears `.next/` must also
account for a dev server already running against it.** The stale server's own state was already
unrecoverable — it was 500ing — so nothing was lost by killing it.

### Dev-side control, unchanged by this task

```
$ npx playwright test e2e/public-listing.spec.ts --project=chromium --workers=1
  8 passed (35.5s)
```

Including `:385 › a draft listing 404s to the public` and `:390 › an unlisted listing 404s to the
public`. The dev half of the same assertion still holds, so the production reading above is not a
production-only artefact — **both** environments now agree that the route answers a real 404.

---

## § W1 — item 4's watched red (the intermediate state of one commit)

**Driven by:** plan **17.1-02**, Task 1, on **2026-08-30**, at `d69f1e4` + one deletion. The subject is
`src/app/listings/[id]/(detail)/not-found.tsx`, `git rm`'d and **not committed**; every instrument
below was then run against a tree where the file is gone and every reference to it still stands. The
deletion and all six edits land as ONE commit in Task 2, so this state exists for a few minutes
whether it is watched or not — **the red is free, and only the transcript costs anything.**

### The honest limit, stated first

**The deletion itself cannot be watched red from the product side.** The file is unreachable in every
state (`§ P1` and `[17-D3]`: `(detail)/layout.tsx` raises `notFound()` from a **layout**, so the parent
segment's boundary wins, and `src/app/listings/[id]/` has none — it falls to the root). An unreachable
file's presence and its absence render **identically**, so no assertion that drives a browser can tell
them apart. There is no version of this drive that produces a red from a rendered document.

What CAN be watched is the **inventory machinery** — the four disk-walked gates that claim to know
which surfaces exist. That is a narrower claim than "the deletion is proven safe", and it is the one
made here: **the coupling between a deleted file and the instruments naming it is ENFORCED, not
remembered.** Three of the four reds below were driven; the fourth is the mirror of one of them and
is accounted for at the end without being manufactured.

### The prediction, written before the drive

| # | Instrument | Predicted failure | Build-blocking? |
|---|---|---|---|
| 1 | `tests/design/empty-state-adoption.test.ts` | the `ADOPTERS` row names a file the walk cannot find, **and** the two count pins move | **YES** — `test:design` runs inside `npm run build` and inside CI's `gate-db-free` |
| 2 | `e2e/axe-sweep.spec.ts` AC#2 | `extra: ["src/app/listings/[id]/(detail)/not-found.tsx"]` | no |
| 3 | `e2e/overflow-320.spec.ts` `inventory to disk` | the entry names a surface that is gone | no |
| 4 | `e2e/overflow-320.spec.ts` `inventory to tables` | the mirror — reds only if the entry goes and the AC#29 row stays | no |

---

### DRIVE 1 — `tests/design/empty-state-adoption.test.ts` · **BUILD-BLOCKING** · THREE failures, not two

```
$ npm run test:design
```

> ⚠ **THE PLAN'S POINTER TO THIS GATE IS WRONG, AND THE TREE IS STRICTER THAN THE POINTER.**
> `17.1-02-PLAN.md` § interfaces and `17.1-PATTERNS.md` § 6a both name `:912-919` — *"declared here but
> the walk never found it"* — as the assertion that reds if the `ADOPTERS` row stays. It is not: that
> loop iterates **`POSITIVE_SITES`**, a different and much smaller inventory that never contained this
> file, and it **passed** in this run. The `ADOPTERS` row is caught by **three** assertions at
> `:791-816` instead. Corrected here rather than propagated (S5), and the correction is in the
> favourable direction — the coupling is enforced by three clauses where the plan expected one.

Failure 1 of 3 — `:799`, the **import** clause. This is the one an absence assertion could never make:

```
 FAIL  tests/design/empty-state-adoption.test.ts > AC#23 forward — the twelve surfaces composing the shell still compose it > has every adopter importing EmptyState from the pattern module
AssertionError: a surface plan 11-16 converted no longer imports EmptyState. An absence assertion cannot see this: a deleted empty state has no border either.
src/app/listings/[id]/(detail)/not-found.tsx — imports nothing from @/components/patterns/empty-state: expected [ Array(1) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "src/app/listings/[id]/(detail)/not-found.tsx — imports nothing from @/components/patterns/empty-state",
+ ]

 ❯ tests/design/empty-state-adoption.test.ts:799:7
```

Failure 2 of 3 — `:809`, the per-row **site count**, which names the row and the arithmetic together:

```
 FAIL  tests/design/empty-state-adoption.test.ts > AC#23 forward — the twelve surfaces composing the shell still compose it > has every adopter rendering the declared number of blocks
AssertionError: an adopter's <EmptyState> call-site count moved:
src/app/listings/[id]/(detail)/not-found.tsx: declared 1, measured 0: expected [ Array(1) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "src/app/listings/[id]/(detail)/not-found.tsx: declared 1, measured 0",
+ ]

 ❯ tests/design/empty-state-adoption.test.ts:809:96
```

Failure 3 of 3 — `:816`, **the count pin**, in the `expected 17 to be 18` shape the plan predicted:

```
 FAIL  tests/design/empty-state-adoption.test.ts > AC#23 forward — the twelve surfaces composing the shell still compose it > holds the pinned total of `<EmptyState>` call sites across the tree
AssertionError: the tree's total <EmptyState> call-site count moved. Expected 18 across 15 surfaces.: expected 17 to be 18 // Object.is equality

- Expected
+ Received

- 18
+ 17

 ❯ tests/design/empty-state-adoption.test.ts:816:7
```

Counts for the run:

```
 Test Files  1 failed | 66 passed (67)
      Tests  3 failed | 1252 passed | 3 skipped (1258)
   Duration  36.77s
```

**The fourth pin at `:820` (`ADOPTERS.length === EXPECTED_ADOPTER_FILES`) did NOT fire, and that is
correct:** the row is still declared in this state, so the inventory is still 15 rows long. It is the
*tree* that moved to 17 sites, not the inventory — which is exactly the asymmetry the pin exists to
surface. It goes red in the opposite intermediate state (constants moved, row left), and Task 2 moves
both together so neither is ever seen alone on a committed tree.

**This gate is build-blocking.** `package.json`'s `build` script is
`npm run lint && npm run test:design && next build`, and CI's `gate-db-free` runs it too. In this
state `npm run build` cannot reach `next build` at all — the deletion is not merely observed by an
instrument, it is **stopped by one**.

---

### DRIVE 2 — `e2e/axe-sweep.spec.ts` AC#2 · the `extra: [...]` the plan asked for, verbatim

```
$ npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1
```

```
  1) [chromium] › e2e\axe-sweep.spec.ts:899:7 › AC#2 — no surface can be silently absent from the axe table › the table's row set equals the declared surface set

    Error: the axe table's row set is not the declared surface set. `missing` lists surfaces that exist on disk and are absent from the table — an absent surface is the one failure this assertion exists to make impossible, because a surface nobody scanned and a surface that scanned clean look identical in a green run. `extra` lists rows naming a file that is not on disk (a rename, or a row left behind by a deleted route). `duplicated` lists a file claimed by two rows, which would let one of them be quietly dropped. A new route is NOT a number to bump: add a row with a measurement, or a row with a named `skip` paragraph saying what fixture it needs.

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 3

      Object {
        "duplicated": Array [],
    -   "extra": Array [],
    +   "extra": Array [
    +     "src/app/listings/[id]/(detail)/not-found.tsx",
    +   ],
        "missing": Array [],
      }

      at C:\Users\Admin\Roaming\FitOut\e2e\axe-sweep.spec.ts:917:7
```

```
  1 failed
  36 skipped
  59 passed (2.7m)
```

---

### DRIVE 3 — `e2e/overflow-320.spec.ts` `inventory to disk` · the stale surface named

```
$ npx playwright test e2e/overflow-320.spec.ts --project=chromium --workers=1
```

```
  1) [chromium] › e2e\overflow-320.spec.ts:3961:7 › D-201 / AC#2 — every surface on disk is in this file's tables, or excluded by name › inventory to disk: no entry claims a surface that is gone

    Error: 1 inventory entr(ies) name a surface that is not on disk:
      src/app/listings/[id]/(detail)/not-found.tsx
    This direction matters as much as the other one: a map that keeps entries for deleted routes grows into a list of things the app used to have while still reading like coverage, and the next reader counts it as evidence. Delete the entry, and delete its rows in the tables above in the same commit.

    expect(received).toEqual(expected) // deep equality

    - Expected  - 1
    + Received  + 3

    - Array []
    + Array [
    +   "src/app/listings/[id]/(detail)/not-found.tsx",
    + ]

      at C:\Users\Admin\Roaming\FitOut\e2e\overflow-320.spec.ts:3974:7
```

```
  1 failed
  9 skipped
  99 passed (3.1m)
```

**The gate's own remedy sentence is this plan's shape, word for word:** *"Delete the entry, and delete
its rows in the tables above in the same commit."*

---

### DRIVE 4 — `inventory to tables`: the mirror, NOT manufactured

`:4005` **passed** in DRIVE 3 (`ok 106 … inventory to tables: every coveredBy name resolves to a real
row`), and that is the correct reading of this state: the entry and the AC#29 row it names are both
still present, so no `coveredBy` name dangles. It is the mirror of DRIVE 3 and reds from the opposite
direction — delete the `SURFACE_INVENTORY` entry alone and the AC#29 row is orphaned; delete the AC#29
row alone and this clause reds on the dangling name.

Task 1 deliberately did not manufacture it: driving it would mean editing an instrument outside a
commit, which is the one thing the S4 rule forbids. **It was not met in Task 2 either** — both edits
were made before the file was re-run, which is the behaviour the pair of gates is designed to force.
Together the two clauses make the one-commit rule mechanical rather than advisory: **there is no order
in which the entry and the row can be separated that leaves this file green.**

---

### THE UNPLANNED FINDING — the `axe-sweep` row was GREEN WHILE ITS FILE DID NOT EXIST

RESEARCH inferred, from reading the `tell`, that the deleted row had been auditing the wrong document.
It was **driven** instead, in this same intermediate state, and it is stronger than the inference:

```
$ npx playwright test e2e/axe-sweep.spec.ts --project=chromium --workers=1 -g "not found"

  ok 1 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /listings/[id] · not found · court · 320px (3.1s)
  ok 2 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /listings/[id] · not found · court · 1280px (1.7s)
  ok 3 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /invite/[token] · not found · court · 320px (2.8s)
  ok 4 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /invite/[token] · not found · court · 1280px (1.6s)
  ok 5 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /bookings/[id] · not found · court · 320px (3.2s)
  ok 6 [chromium] › e2e\axe-sweep.spec.ts:1005:11 › AC#16 — zero axe violations in court at 320 and 1280px › /bookings/[id] · not found · court · 1280px (1.8s)

  6 passed (30.5s)
```

**The row named a file that had already been deleted from disk, and passed in both viewports.** Its
`path` (`/listings/a-listing-id-that-must-never-exist-17-07`) 404s to the ROOT boundary, and its `tell`
(`[data-testid="empty-state"]`) is rendered by the root boundary too — so the row has never once
audited the file in its `file:` column. It was a **green cell for a document nobody chose to audit**,
and only the disk-walked AC#2 could see it, because AC#2 is the one clause that compares the table
against reality instead of against itself.

This is the phase's own key insight arriving as a measurement rather than a maxim: *the failure mode in
this codebase is never "no gate", it is "a gate that reads the wrong thing and is green"*. It is also
why deleting the row costs **zero** real coverage: the document it actually visited is still audited,
by the `root not-found` row that legitimately owns it.

---

### Every other failure in these runs, against the declared denominator

`e2e-baseline-reds.md` declares **10** rows. Across all three drives, **the only failures were the
three predicted above** — `1 failed` in each Playwright run and `3 failed` in `test:design`, every one
of them naming the deleted path. **Zero** failures needed the denominator, and **zero** rows were added
to it:

```
$ git diff --name-only -- '.planning/phases/17-*/e2e-baseline-reds.md'
(no output)
```

D-14 holds: no assertion was weakened, no row was minted, and nothing in this section is a paraphrase.

---
