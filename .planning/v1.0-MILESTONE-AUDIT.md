---
milestone: v1.0
audited: 2026-08-01T12:00:00Z
reconciled: 2026-08-01T12:55:00Z
reconciled_2: 2026-08-05T03:40:00Z
reconciled_3: 2026-08-10T04:32:30Z
status: tech_debt
scores:
  requirements: 49/49
  phases: 9/9
  integration: 7/7
  flows: 7/7
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-auth-accounts
    items:
      - "**PHASE STATUS IS NOW `passed` (flipped 2026-08-10, quick 260810-hd6). 6/6 human-verification items discharged; ZERO open human items.** The three that were outstanding on 2026-08-05 all closed: item 3 (Google OAuth) and item 4 (Cloudinary avatar) were WALKED LIVE once the credentials were supplied, and item 2 (password-reset) went partial → passed on evidence that had been in hand since 2026-08-05. Items 1, 5 and 6 were already closed. Note this changes the phase's *verification* status only — 9/9 phases were already verified; what changed is that Phase 1 now has nothing outstanding against its 4/4."
      - "CLOSED 2026-08-10 — live Google OAuth round trip. Was OPEN since 2026-06-03 on absent GOOGLE_CLIENT_ID/SECRET. Creds configured; the per-boot `Social provider google is missing clientId or clientSecret` warning is GONE; POST /api/auth/sign-in/social went **HTTP 500 → 200** (i.e. 'Continue with Google' had been a DEAD BUTTON shipping on /login and /signup, not merely unproven); round trip completed to the callback with PKCE S256 + state + the exact redirect_uri. The D-08 auto-link was observed for the first time: providers `credential,google` on ONE row, count(*)=1 for the email, email_verified still true. ACCEPTED CONSEQUENCE, recorded not deferred: trustedProviders:['google'] means a Google account arrives emailVerified:true and bypasses the publish email-verification gate at src/app/actions/listing.ts:375 — the operator took this knowingly in exchange for a working button."
      - "CLOSED 2026-08-10 — real Cloudinary AVATAR upload, on its own surface (no Phase-2 listing-photo evidence borrowed). avatar_public_id fitout/avatars/LTzAEbLxKpeSXM4PgOhrZjniVZT51PCg, avatar_url on res.cloudinary.com/da8uglpk6/, and the asset itself fetches HTTP 200 / image/png / 60,933 bytes. Closing it uncovered that avatar upload had been DEAD SINCE PHASE 1: avatar.ts exported a number from a `use server` module, so Next rejected the module at evaluation and uploadAvatarAction never ran; avatar.test.ts passed throughout because it never crosses the `use server` boundary. Fixed in quick 260807-fc6."
      - "CLOSED 2026-08-10 — password-reset email DELIVERY to a real inbox. Carried as PARTIAL since 2026-08-05, which was over-cautious rather than wrong: that walkthrough delivered BOTH emails to a real inbox, and the dev log shows `GET /api/auth/reset-password/OdCLwmNLMSa2LiFWYfykacCj?callbackURL=%2Freset-password 302` — token as a URL SEGMENT plus callbackURL is the EMAILED LINK's shape, not the `?token=` shape the spec drives after reading Postgres. Real-inbox delivery was therefore genuinely proven on 2026-08-05. STANDING CAVEAT (a fixture limitation, not a gap): e2e/password-reset.spec.ts still reads its token from the Postgres `verification` table because Resend 422s example.com recipients."
      - "CORRECTED 2026-08-05 — the first pass claimed the signup intent default (intent-less signup → canBook) 'has never had recorded product sign-off'. That was FALSE. 01-HUMAN-UAT.md item 6 records: `passed — user accepted \"default to booker\" (approved 2026-06-03)`. The sign-off existed in the record the whole time; the audit had not read it. Item 6 is CLOSED, not debt."
      - "STILL OPEN (code debt, never human-gated) — IN-01: duplicate-email detection in src/app/actions/auth.ts regexes the upstream error message string; brittle to a Better Auth message change."
      - "STILL OPEN (code debt, never human-gated) — repeated `as typeof session.user & { canHost?: boolean; ... }` cast duplicated across 4 layout files."
  - phase: 02-listings-host-onboarding
    items:
      - "Phase status is `human_needed` on ONE item after the 2026-08-05 reconciliation (was two). PAY-04's live PayMongo hosted Linked-Accounts KYC round trip is BLOCKED on PayMongo's sales-gated Platforms beta — standing since 2026-07-10, the same external gate Phase 5 carries. The derived bookability gate itself is fully proven. OPEN."
      - "Real Cloudinary listing-photo upload: DISCHARGED 2026-08-05 on historical evidence. 02-UAT.md test 8 ('Photo upload + reorder | upload >=3, drag-reorder, first = cover') is recorded pass, and that UAT's verdict line states 'Verified against real Postgres + real Cloudinary' — a human did walk it on 2026-07-10, which is exactly what the item asked. It was being carried as outstanding only because the current environment lacks the server-side secret to RE-exercise it. Discharged on the record, not re-observed."
      - "02-UAT punch-list follow-up (low): no regression test covering the Photon-autocomplete → publish path that produced the HIGH-severity publish dead-end fixed in bc6a057."
      - "02-UAT punch-list follow-up (low): address fields are not manually editable — the host depends entirely on autocomplete field-mapping."
      - "photo-uploader.tsx hard-crashes rather than degrading when NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME is absent."
      - "src/lib/search/query.ts:203-210 inlines the deriveBookable predicate as SQL rather than sharing bookability.ts. Deliberate and annotated 'KEEP IN SYNC' at both ends — a pure function cannot run inside a SQL WHERE — but it is a real duplication of the sell gate."
  - phase: 04-booking-core-search-no-payment
    items:
      - "tests/auth/secret-config.test.ts is order-dependent under the full parallel vitest run (cross-file module/env pollution). Not observed in any of this audit's runs (1087/1087 green, twice) but the isolation weakness is unfixed."
  - phase: 05-payments-payouts
    items:
      - "Phase status remains `human_needed` on THREE external items, none of which is a code gap: (1) real hosted checkout per rail, (2) real inhouse payout transfer + wallet correlation, (3) the QRPh operator-alert manual-refund workflow. Reconciled 2026-08-05: item 1 is PARTIAL, not discharged. The first pass said it was 'discharged in substance', which over-stated it — the item's own wording is 'on each rail (card, GCash, Maya, QR Ph)', and only CARD (Phase 6, pay_C4PW6fRGtUTNm6GsKCpt4P36) and QR Ph (Phase-7 Plan-16 probe, pay_ru6sXqhRJto1NW3T83cqak4q on cs_809b1190ba4c3d44b7a77cdc) were hand-paid, plus three further real confirms 2026-07-31 (evt_oGtPa9Vd7ZqWFiPRntuSjacm / evt_zBVdHpjZL1N6hmVtasiZ5K6X / evt_BRQR1Xx9FQcJhy2Dgvmti6m4). GCash and Maya were NEVER individually walked. Items 2 and 3 remain fully OPEN; item 2 is the standing blocker below."
      - "REAL HOST PAYOUTS HAVE NEVER MOVED REAL MONEY. PayMongo's /v2 money-movement beta (batch_transfers, wallet enumeration, transfer polling) is sales-gated and not enabled on the account. Sweep and reconcile are proven only against mockPayMongo. This is the single largest residual risk in the milestone and it is external, not fixable in code."
      - "The QRPh alert path is console.error + an audit row — no alerting/paging integration exists. Verifying 'never silently retain money' operationally needs a human process, not a code check."
  - phase: 06-full-booking-payment-integration
    items:
      - "None outstanding. The stale BOOK-05/HOST-01 requirements rows were corrected 2026-08-01; the 2026-07-20 live re-UAT that closed the phase is recorded in 06-HUMAN-UAT.md."
  - phase: 07-bookings-management-cancellation-notifications
    items:
      - "None outstanding — every item in the phase's deferred-items.md is annotated RESOLVED and 07-HUMAN-UAT.md status is `passed`."
  - phase: 08-group-bookings
    items:
      - "T-08-74 accepted residual: an already-captured qrph overcharge."
      - "LW-01: expireCheckoutSession's already-expired tolerance matches on unstructured PayMongo error text. Fails SAFE (a provider reword re-opens a recovery livelock — a denial, never a double-charge), but it is string-matching a third party."
  - phase: 09-open-capacity-bookings
    items:
      - "Item 1 (copy gap, OPEN): the `Drop-in` badge on the search card carries the whole mental model with no room to define it — a first-time booker cannot classify the card. Conversion risk, not correctness."
      - "Item 2 (ACCEPTED DESIGN, operator decision 2026-07-31): lowStockThreshold never fires early on a cap of 2-3, so a booker on a cap-3 listing sees no change crossing 3 → 2 spots. Anti-invented-urgency by design; asserted and mutation-measured."
      - "The phase's one human-verification item (DatePassPicker first-click reliability) was discharged 2026-08-01 by the ORCHESTRATOR against a production build (4×6/6, then 22/22 across all 8 e2e specs), explicitly NOT by a human. Evidence is deterministic; the signature is not human."
  - phase: milestone-wide
    items:
      - "publishListing does not require operating-hours rows to exist, so deriveBookable can return true for a listing with an empty calendar. Graceful (no crash, no double-booking risk) but a host can go Live with nothing bookable and no signal. Found by the cross-phase integration check; not attributable to a single phase."
      - "REVISED 2026-08-10 — **TWO** phases (02, 05) sit at `human_needed`, not three. Phase 1 is `passed` with zero open human items. The open set is **THREE** items, all external, none unfinished engineering: PayMongo hosted Linked-Accounts KYC (Ph2, sales-gated beta), real host payout transfer (Ph5, /v2 money-movement beta not enabled), QRPh operator-alert process (Ph5, needs a human operational routine) — plus ONE remaining PARTIAL (Ph5 hosted checkout: card + QR Ph hand-paid, GCash and Maya never individually walked). Phase 1's two credential items closed because the credentials were supplied; Phase 2's and Phase 5's cannot close the same way — they are gated on a third party granting beta access and on real money moving, which no amount of local configuration reaches."
nyquist:
  compliant_phases: [01, 02, 03, 04, 05, 06, 07, 08, 09]
  partial_phases: []
  missing_phases: []
  overall: 9/9
reconciliation:
  closed:
    - "Phase 2 had no VERIFICATION.md — the milestone's one blocking gap. Closed by a retroactive 02-VERIFICATION.md (4/4 success criteria, 7/7 requirements)."
    - "LIST-01..06 + PAY-04 were orphaned (in no VERIFICATION.md anywhere). Now covered."
    - "AVAIL-01..05 were absent from every SUMMARY's requirements-completed. Recorded in 03-05-SUMMARY.md (the phase's last plan)."
    - "BOOK-05 + HOST-01 were absent from every SUMMARY's requirements-completed. Recorded in 06-10-SUMMARY.md (the plan that closed the phase)."
    - "06-VERIFICATION.md's BOOK-05/HOST-01 rows read NEEDS HUMAN, contradicting its own passed frontmatter. Corrected, original evidence preserved."
    - "01-VALIDATION.md was status:planned / wave_0_complete:false — a pre-execution draft never refreshed. Closed; every row re-run (14 files / 39 tests) and all four Wave-0 items confirmed present."
    - "03-VALIDATION.md was status:draft / nyquist_compliant:false — same shape, on the phase that owns the double-booking guarantee. Closed; 18 files / 168 tests + 4/4 e2e re-run, all eight Wave-0 items confirmed."
    - "Phase 1 human items 1, 2 and 5 discharged by real-browser e2e specs re-run first-hand (4 passed). REVISED 2026-08-05: items 1 and 5 are closed; item 2 is PARTIAL (flow proven, real-inbox delivery not) — the first pass over-stated it."
    - "SECOND PASS 2026-08-05 — the three phase human-verification RECORDS (01-HUMAN-UAT.md, 02-VERIFICATION.md, 05-HUMAN-UAT.md) were themselves stale: 01 had not been touched since 2026-06-03 and did not reflect the 2026-08-01 reconciliation at all. All three brought current against evidence that already existed. Net: Ph1 item 5 skipped→passed and item 2 pending→partial; Ph2 Cloudinary item discharged on historical evidence; Ph5 item 1 pending→partial. No phase status flipped off human_needed."
    - "The audit's own Phase-1 tech-debt was WRONG about item 6 and is corrected 2026-08-05 — see the Reconciliation Log (second pass)."
    - "THIRD PASS 2026-08-10 — **Phase 1 is fully clear: 6/6 human items discharged, `human_needed` → `passed`, zero open.** Items 3 and 4 were WALKED LIVE (Google OAuth round trip with the D-08 auto-link on ONE row; a real Cloudinary avatar whose asset fetches HTTP 200 / 60,933 bytes) after the credentials were supplied; item 2 went partial → passed on 2026-08-05 evidence that had been read too conservatively. Two of the three were repairs, not just proofs — the OAuth button was returning HTTP 500 and avatar upload had never once executed (a `use server` export bug, fixed in quick 260807-fc6). Phases 2 and 5 are deliberately UNTOUCHED and remain `human_needed`."
    - "12 unticked v1 requirement checkboxes in REQUIREMENTS.md. All ticked."
    - "MANAGE-03's traceability status read In progress while its own body concluded COMPLETE. Corrected."
    - "ROADMAP Phase 9 checkbox was unticked; Phase 8's Wave 10/11 list had 08-18/19/20 unticked and omitted 08-21/08-22. All corrected."
    - "STATE.md read status:executing / completed_phases:8 / 99%. Now verified / 9 / 100%."
  corrected_against_source:
    - "All five Phase-5 WARNINGs (CR-01 stranded payout, WR-01/WR-02 refund handling, WR-03 webhook boot guard, WR-05 booking status default) are CLOSED in current source — the documents were stale."
    - "Phase 4's build-env fail-closed guards and the address-autocomplete lint error are fixed (NEXT_PHASE exemption; eslint clean)."
    - "Phase 7's root-relative email href was resolved by 07-11 with regression assertions."
    - "Phase 8's deferred-items.md items 5/6/7 are already struck through and annotated CLOSED with commits — the note in 08-VERIFICATION.md flagging them as reading OPEN is itself outdated."
---

# Milestone v1.0 — Audit Report

**Project:** FitOut — Airbnb-style marketplace for fitness & recreational spaces
**Milestone:** v1.0 (9 phases, 107 plans)
**Audited:** 2026-08-01 · **Reconciled:** 2026-08-01 · **Reconciled again:** 2026-08-05 (second pass — human-verification records) · **2026-08-10** (third pass — Phase 1 fully discharged)
**Status:** `tech_debt` — no blockers; accumulated deferred items warrant a read before archiving

## Verdict

**49/49 requirements satisfied. 9/9 phases verified. 7/7 cross-phase seams and 7/7 E2E flows wired.
Nyquist 9/9. No blocking gaps.**

The first pass of this audit returned `gaps_found` for one reason: Phase 2 — the phase that owns
listings and the payout-onboarding bookability gate — had shipped without ever running its verification
gate, so seven requirements (LIST-01…06, PAY-04) existed in the traceability table and in working,
independently-traced code but in **no VERIFICATION.md anywhere in the milestone**. That, plus a set of
bookkeeping drifts, has been closed. The characterization held up: it was a **verification-artifact
gap, not a product gap** — the retroactive Phase 2 verification found 4/4 success criteria and 7/7
requirements satisfied, with 144 Phase-2 tests and 3/3 LIST-06 browser E2E re-run first-hand.

What remains is genuine tech debt, and one item on it deserves to be read before anyone calls v1.0
shippable: **real host payouts have never moved real money.** PayMongo's `/v2` money-movement beta is
sales-gated and not enabled on the account, so the sweep and reconcile that pay hosts are proven only
against mocks. The booker-side money path is proven against the real API — real `sk_test_` hosted
checkouts were paid by hand in Phases 6, 8 and 9 — but the host-side leg is not. That is an external
dependency, not unfinished work, and it has been correctly and continuously disclosed since Phase 5.

---

## Requirements Coverage — 3-Source Cross-Reference

49 v1 requirements, each cross-referenced against REQUIREMENTS.md traceability, the owning phase's
VERIFICATION.md, and the SUMMARY `requirements-completed` frontmatter.

| Group | Count | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|---|---|---|---|---|---|
| AUTH-01…05 | 5 | Ph1, all rows SATISFIED | listed | `[x]` | **satisfied** |
| LIST-01…06 | 6 | Ph2 (retroactive), all SATISFIED | listed | `[x]` | **satisfied** |
| PAY-04 | 1 | Ph2 (retroactive), SATISFIED | listed | `[x]` | **satisfied** (external KYC gate) |
| AVAIL-01…05 | 5 | Ph3 `passed`, all VERIFIED | listed (03-05) | `[x]` | **satisfied** |
| SEARCH-01…05 | 5 | Ph4 `passed` | listed (04-05) | `[x]` | **satisfied** |
| BOOK-01, 02, 03 | 3 | Ph4 `passed` | listed (04-01, 04-07) | `[x]` | **satisfied** |
| PAY-01, 02, 03, HOST-03 | 4 | Ph5, all SATISFIED | listed | `[x]` | **satisfied** (external payout gate) |
| BOOK-04, 05, 06, PAY-05, HOST-01 | 5 | Ph6 `passed` | listed (06-10) | `[x]` | **satisfied** |
| BOOK-07, PAY-06, HOST-02, MANAGE-01…03 | 6 | Ph7 `passed`, human UAT `passed` | listed | `[x]` | **satisfied** |
| GROUP-01…05 | 5 | Ph8 `passed` | listed | `[x]` | **satisfied** |
| OPEN-01…04 | 4 | Ph9 `passed` 12/12 | listed | `[x]` | **satisfied** |

**Score: 49/49 satisfied · 0 partial · 0 unsatisfied · 0 orphaned**

Machine-checked after reconciliation: 49 distinct REQ-IDs appear across the nine VERIFICATION.md files;
49 appear inside SUMMARY `requirements-completed` **arrays** (comments excluded); 0 checkboxes remain
unticked. All three sources agree.

---

## Phase Verification Roll-Up

| Phase | VERIFICATION.md | Status | Score | Open human items |
|---|---|---|---|---|
| 01 Auth & Accounts | ✓ | **`passed`** *(was `human_needed`)* | 4/4 | **none — 0 of 6 open.** All six discharged; the last three closed **2026-08-10** (OAuth walked live, Cloudinary avatar walked live, reset-email delivery recognised as already proven) |
| 02 Listings & Host Onboarding | ✓ **added 2026-08-01** | `human_needed` | 4/4 | **1** (PayMongo Platforms beta) — *revised 2026-08-05; the Cloudinary listing-photo item is discharged on the 2026-07-10 UAT record* |
| 03 Availability & Double-Booking | ✓ | `passed` | 12/12 | none |
| 04 Booking Core & Search | ✓ | `passed` | 8/8 | none |
| 05 Payments & Payouts | ✓ | `human_needed` | 5/5 | **2 open** (PayMongo /v2 beta, operator-alert process) + 1 partial (hosted checkout: card + QR Ph hand-paid; GCash + Maya never walked) — *revised 2026-08-05* |
| 06 Full Booking + Payment | ✓ | `passed` | 4/4 | discharged by live re-UAT 2026-07-20 |
| 07 Bookings Mgmt & Notifications | ✓ | `passed` | 4/4 + 7/7 + 3/3 | discharged (07-HUMAN-UAT `passed`) |
| 08 Group Bookings | ✓ | `passed` | 5/5 | none (1 optional recommendation) |
| 09 Open-Capacity Bookings | ✓ | `passed` | 12/12 | discharged 2026-08-01 (by orchestrator, not a human) |

**9/9 phases verified** — unchanged, and worth being precise about: all nine phases have been *verified*
since 2026-08-01. What moved on 2026-08-10 is that **Phase 1 no longer has anything outstanding against
its verification.** **Two** phases (02, 05) now sit at `human_needed`, down from three, and every
remaining item across those two is a third-party beta or an operational process — none is unfinished
engineering.

> **Revised 2026-08-10 (third reconciliation pass).** **Phase 1 is fully clear.** Its last three human
> items closed today: Google OAuth and the Cloudinary avatar were **walked live** once the credentials
> were supplied, and the password-reset item went `partial` → `passed` on evidence already in hand from
> 2026-08-05. The open set across the milestone is now **three** items — PayMongo hosted KYC (Ph2), the
> real host payout transfer (Ph5), the QRPh operator-alert process (Ph5) — plus one `partial` (Ph5
> GCash/Maya rails never individually walked).
>
> Why Phase 1 could close and Phases 2 and 5 cannot: Phase 1's blockers were **missing local
> credentials**, which a human can supply in an afternoon. Phase 2's and Phase 5's are **a third party
> withholding beta access** and **real money that has never moved**. Those are not the same kind of
> obstacle, and Phase 1 closing says nothing about them.
>
> One uncomfortable finding is worth carrying up to this level: two of the three items that closed were
> **repairs, not proofs**. "Continue with Google" was returning HTTP 500 on `/login` and `/signup`, and
> `uploadAvatarAction` had never executed once since Phase 1 because of a `"use server"` export bug. Both
> shipped dead through nine phases behind a "blocked on credentials" label. The label was true and also
> concealing breakage — an argument for un-blocking credential-gated items rather than reasoning about
> them.

> **Revised 2026-08-05 (second reconciliation pass).** The open set across those three phases is **five
> items**, not the eight the first pass implied, and two further items are `partial` rather than
> pending: Ph1 password-reset (flow proven, inbox delivery not) and Ph5 hosted checkout (card + QR Ph
> hand-paid; GCash + Maya unwalked). The wording "or a product sign-off" above was load-bearing on a
> claim that turned out to be false — Phase 1's signup-intent default *was* signed off on 2026-06-03 —
> so the category is dropped. **No phase status changed**; each of 01, 02 and 05 still has at least one
> genuinely open item.

### Phase 5's five documented WARNINGs are all CLOSED

`05-VERIFICATION.md` (2026-07-16) recorded CR-01, WR-01, WR-02, WR-03 and WR-05 as "confirmed present,
unfixed". This audit checked all five against current source rather than trusting the document:

| Finding | Then | Now |
|---|---|---|
| CR-01 payout stranded in `held` on a post-claim throw | unfixed | **CLOSED** — `listWalletAccounts()` is inside the try (`payout-sweep.ts:203-205`); the catch marks `failed`, and `ON CONFLICT … DO UPDATE … WHERE state='failed'` gives a bounded re-claim |
| WR-01 refund overwrites a `processing`/`paid` ledger row | unfixed | **CLOSED** — the UPDATE is guarded `eq(hostPayoutLedger.state, "held")` with the rationale written in |
| WR-02 refund handler reads event type, not refund status | unfixed | **CLOSED** — `if (!["succeeded","refunded"].includes(refundStatus)) return` |
| WR-03 no prod boot guard on `PAYMONGO_WEBHOOK_SECRET` | unfixed | **CLOSED** — `webhook/route.ts:36` |
| WR-05 `booking.status` defaults to `'confirmed'` | unfixed | **CLOSED** — `schema.ts:674` defaults `'pending'` |

Phase 4's build-env guards are fixed too (`NEXT_PHASE !== "phase-production-build"` in both
`paymongo.ts` and the Inngest route), as is the `address-autocomplete.tsx` lint error and Phase 7's
root-relative email `href`.

---

## Cross-Phase Integration — 7/7 WIRED

| # | Seam | Status | Evidence |
|---|---|---|---|
| 1 | Ph2 bookability gate → Ph4/6/9 booking entry points | WIRED | `deriveBookable` (`src/lib/bookability.ts:16`) re-derived server-side at `placeHold` (`booking.ts:174`), `placeOpenHold` (`booking.ts:411`), search Stage-1 (`search/query.ts:203-210`), listing page (`listings/[id]/page.tsx:122`). `payoutsEnabled` has **exactly two writers in the whole `src/` tree**, both in the signature-verified webhook (`webhook/route.ts:464`, `:471`) |
| 2 | Ph3 availability read model → search Stage-2, hold path, Ph9 day panel | WIRED | Single `getAvailability` (`read-model.ts:165`) consumed by listing page, search Stage-2, availability action and booking detail; open/exclusive fork lives inside it. No divergent SQL availability predicate |
| 3 | hold → checkout → webhook confirm → instant/request fork → notification | WIRED | `confirmBooking` (`booking.ts:673-932`) → `checkout_session.payment.paid` (`webhook/route.ts:414-453`) is the sole writer of `status='confirmed'` for BOTH arms → `emitBookingConfirmed` → `emitNotify` → Inngest `notify` |
| 4 | Ph7 cancellation/refund vs Ph6/8/9 booking origins | WIRED | `cancel-booking.ts` is origin-agnostic; forks on `booking.openCapacity` for the window guard (`607-651`, `1055-1163`) and on `booking_group` existence (`voidGroupAndNotifyAttendees`, `420-483`) |
| 5 | Ph8 group vs Ph9 open-capacity mutual exclusivity | WIRED | `createGroup` refuses `occupancyMode !== 'exclusive'` in BOTH the pre-write gate (`group.ts:278`) and the INSERT's own WHERE (`group.ts:333`) — defense in depth |
| 6 | Ph5 payout sweep/reconcile covers all booking origins | WIRED | `queryDuePayouts` (`payout-sweep.ts:99-135`) has no origin filter — group and open-capacity rows are ordinary `booking` rows, so all four origins flow through one sweep |
| 7 | Ph1 capability/ownership gates on later-phase host surfaces | WIRED | `canHost`/`canBook` are `input:false`; every host mutation re-derives ownership server-side (`loadOwnedRequest`, `loadHostOwnedBooking`, `assertOwnership`) rather than trusting the `(host)` route group |

**One WARNING (not a break):** `publishListing`'s `publishSchema` does not require operating-hours rows,
so `deriveBookable` can be true for a listing with an empty calendar. Graceful — it renders
`hasHours:false` and offers no slots — but a host can go "Live" with nothing bookable and no signal.

---

## End-to-End Flows — 7/7 complete

| Flow | Status |
|---|---|
| F1 signup → host → listing → availability → payout → bookable | WIRED (with the empty-calendar warning above) |
| F2 search → listing → hold → pay → webhook confirm → confirmation + email | WIRED |
| F3 request-to-book: hold → request → approve → pay → confirmed; decline/expiry frees slot | WIRED — `request-expiry.ts` terminal mapping matches `units.ts`'s in-tx sweep byte-for-byte |
| F4 booker cancels → tier-snapshot refund → refund issued → notifications → slot freed | WIRED — reads `booking.cancellationPolicy` (creation-time snapshot), never the listing's current tier |
| F5 organizer pays → group → invites → guest RSVP without account → headcount vs capacity | WIRED — `claimSeat` is sole arbiter via `SELECT … FOR UPDATE` (D-112, no count-then-insert race) |
| F6 drop-in: two independent bookers, atomic cap, spots-left, existing pay rail | WIRED — transaction-scoped advisory lock per `(listing, date)`; webhook explicitly untouched |
| F7 session ends → sweep → net transfer → reconcile → /host/earnings | WIRED in code; **live transfer externally gated on PayMongo /v2 beta** |

---

## Nyquist Validation Coverage — 9/9

| Phase | VALIDATION.md | `nyquist_compliant` | doc status | Classification |
|---|---|---|---|---|
| 01 | exists | true | `validated` | COMPLIANT *(closed 2026-08-01)* |
| 02 | exists | true | `validated` | COMPLIANT |
| 03 | exists | true | `validated` | COMPLIANT *(closed 2026-08-01)* |
| 04–07 | exists | true | `validated` | COMPLIANT |
| 08 | exists | true | `verified` | COMPLIANT |
| 09 | exists | true | `complete` | COMPLIANT |

Phases 1 and 3 were `PARTIAL` in the first pass because their VALIDATION.md files were **pre-execution
drafts that were never refreshed** — 01 sat at `status: planned` / `wave_0_complete: false` and 03 at
`status: draft` / `nyquist_compliant: false`. Phase 3 mattered most: it owns the double-booking
guarantee, so it read as the one load-bearing phase with unfinished validation. Neither was a real
coverage gap. Every row in both contracts was re-run first-hand before flipping the flags.

---

## Build & Test Signal (all run first-hand during this audit, 2026-08-01)

| Check | Command | Result |
|---|---|---|
| Typecheck | `npx tsc --noEmit` | exit 0 |
| Full unit/integration suite | `npx vitest run` | **1087 passed / 4 skipped, 0 failures** (120 files) |
| Phase 1 suites | `npx vitest run tests/auth tests/profile` | 14 files / **39 passed** |
| Phase 2 suites | `npx vitest run tests/listing tests/paymongo/{onboarding,webhook-signature,webhook-merchant-activated} …` | 17 files / **144 passed** |
| Phase 3 suites | `npx vitest run tests/availability` | 18 files / **168 passed** (21.65s — inside the contract's 30s latency budget) |
| Phase 1 human-item E2E | `npx playwright test e2e/{login-persistence,password-reset,mode-switch}.spec.ts` | **4 passed (33.7s)** |
| Phase 2 LIST-06 E2E | `npx playwright test e2e/public-listing.spec.ts` | **3 passed** |
| Phase 3 AVAIL E2E | `npx playwright test e2e/availability.spec.ts` | **4 passed (22.1s)** |
| Live DB extensions | `psql -c "SELECT extname FROM pg_extension …"` | `postgis` + `btree_gist` present |
| Live `listing` schema | `psql \d listing` | 31 columns incl. `geometry(Point,4326)`, integer cents, pg enums, `currency='php'` |
| `payoutsEnabled` writer count | `grep -rn "payoutsEnabled:" src/` | exactly 2 writers, both in the webhook |

Reconciliation changed **documentation only** — `git status` shows zero `src/` modifications.

---

## Tech Debt Summary

**Blocking: none.**

Ranked by consequence:

1. **Real host payouts have never moved real money.** PayMongo's `/v2` money-movement beta is
   sales-gated. Sweep and reconcile are proven only against `mockPayMongo`. The booker-side rail *is*
   proven against the real API (hand-paid `sk_test_` checkouts in Phases 6, 8 and 9). External.
   - *Qualified 2026-08-05:* "the booker-side rail is proven" holds **per rail only for card and QR Ph**
     (`pay_C4PW6fRGtUTNm6GsKCpt4P36`; `pay_ru6sXqhRJto1NW3T83cqak4q`). **GCash and Maya have never been
     individually hand-paid.** They ride the same hosted Checkout Session and the same
     `checkout_session.payment.paid` branch — a reason to expect them to work, not evidence that they
     do. Two test-mode checkouts close it, and it is why Phase 5 item 1 is `partial`, not passed.
2. **PayMongo hosted Linked-Accounts KYC (PAY-04) has never been walked.** The gate it produces is
   fully proven structurally; the third party's hosted flow is not reachable. External.
3. **Two phases at `human_needed`** (02, 05) — entirely third-party-beta or operational-process items.
   *(Revised 2026-08-10 from "three phases (01, 02, 05)": **Phase 1 is now `passed` with zero open human
   items** — its two credential-blocked items were walked live and its password-reset item was recognised
   as already proven. Revised 2026-08-05: "product-sign-off" was struck from this list — the one item it
   referred to had been signed off since 2026-06-03.)*
   The two that remain are qualitatively different from the ones that closed: they are not waiting on
   configuration anyone here can supply.
4. **`publishListing` allows a live listing with no operating hours.** The one genuine product-
   completeness finding in the whole audit, and the only one that is fixable today.
5. **Phase 8 accepted residuals** — **STILL OPEN: the already-captured `qrph` overcharge (T-08-74).**
   A captured QRPh payment is **not refundable through the PayMongo API at all** — it is a rail
   limitation, not a code gap — so an overcharge needs an out-of-band operator refund. Nothing in this
   item's partial closure below touches it, and it is tracked for a separate follow-up quick task.
   **AR-08-01 stands. This item as a whole is NOT closed — only its LW-01 half is.**
   *(Revised 2026-08-10 from "the already-captured qrph overcharge (T-08-74) and `expireCheckoutSession`'s
   string-matched already-expired tolerance (LW-01, fails safe)": **the LW-01 half is now CLOSED** by quick
   task `260810-i0v` (`2130ff2` source, `cd10687` tests). The string match on PayMongo's prose is gone —
   `expireCheckoutSession` now re-probes the provider once with `getCheckoutSession(id)` and tolerates an
   expire error only when the provider itself reports `status === "expired"`, so the postcondition is read
   back as fact instead of inferred from an error sentence; `paid`, `active`, unknown/empty, and a failed
   probe all rethrow the ORIGINAL error into the caller's `needs_attention` refusal. Five mutations, five
   recorded REDs; confirmed live against the `sk_test_` API the same day. `src/app/actions/booking.ts` is
   byte-unchanged. **The one that remains is T-08-74**, and it is qualitatively different from the half that
   closed: LW-01 was our own inference from a third party's wording, fixable by us; T-08-74 is a PayMongo
   rail that offers no refund API at all.)*
   *(T-08-79, the truly concurrent double-click on Confirm & pay, was re-opened and CLOSED
   2026-08-01 by quick task `260801-kv2` — a compare-and-swap checkout lease that holds no DB lock
   across either PayMongo round-trip. AR-08-02 is retired; AR-08-01 stands.)*
6. **Phase 9 product copy** — "Drop-in" is unexplained on the search card (conversion risk); the
   small-cap scarcity chip behaviour is an explicitly accepted design decision, not a defect.
7. ~~**Absent dev credentials** — `GOOGLE_CLIENT_*` and server-side `CLOUDINARY_*` are missing from
   `.env.local`, which is why **two** Phase-1 human items cannot be exercised locally (Google OAuth,
   avatar upload).~~ **RESOLVED 2026-08-10.** Both credential sets were supplied and both items were
   walked live — Google OAuth produced the D-08 auto-link on ONE user row, and a real avatar landed on
   Cloudinary (`fitout/avatars/LTzAEbLxKpeSXM4PgOhrZjniVZT51PCg`, asset fetches HTTP 200 / 60,933 bytes).
   **Doing so exposed two dead surfaces the missing credentials had been hiding:** `POST
   /api/auth/sign-in/social` was returning HTTP 500 (a dead "Continue with Google" button on `/login`
   and `/signup`), and `uploadAvatarAction` had never run at all since Phase 1 because `avatar.ts`
   exported a number from a `"use server"` module (fixed in quick `260807-fc6`). One accepted trade came
   with it: `trustedProviders: ["google"]` lets a Google signup bypass the publish email-verification
   gate at `src/app/actions/listing.ts:375`, taken knowingly.
   *(Revised 2026-08-05 from "three Phase-1/2 items": the Phase-2 listing-photo item is
   not blocked-and-unproven, it is proven-and-unrepeatable — a human walked it against real Cloudinary
   on 2026-07-10. The missing secret blocks re-running it, not the discharge.)*

---

## What this audit changed its mind about

Findings this audit checked against source and found **no longer true** — reporting them as open debt
would have been wrong:

- 05-VERIFICATION.md's five open WARNINGs, **including CR-01, a real money-safety gap** that would
  have stranded a host payout in `held` with no recovery path. All five closed.
- Phase 4's `deferred-items.md` build-env and lint failures — both fixed.
- Phase 7's root-relative email `href` — resolved by 07-11 with regression assertions.
- Phase 8's `deferred-items.md` reading "OPEN" for items 5/6/7 — the file has since been struck
  through and annotated with closing commits; the note flagging it is itself outdated.

And the thing the documents **under**-stated: Phase 2 having no VERIFICATION.md was invisible from
ROADMAP.md, STATE.md and REQUIREMENTS.md, all three of which reported Phase 2 as complete.

---

## Reconciliation Log (2026-08-01)

Twelve artifact gaps closed. Documentation only — no `src/` change.

| # | Gap | Closure |
|---|---|---|
| 1 | Phase 2 had no VERIFICATION.md (the blocking gap) | Retroactive `02-VERIFICATION.md` — 4/4 success criteria, 7/7 requirements, 144 tests + 3/3 E2E re-run first-hand, live schema/PostGIS check |
| 2 | LIST-01…06 + PAY-04 orphaned | Covered by the above |
| 3 | AVAIL-01…05 in no SUMMARY frontmatter | Recorded in `03-05-SUMMARY.md`, original note preserved |
| 4 | BOOK-05 + HOST-01 in no SUMMARY frontmatter | Recorded in `06-10-SUMMARY.md` |
| 5 | `06-VERIFICATION.md` rows read NEEDS HUMAN vs its own `passed` frontmatter | Status column corrected; evidence preserved verbatim with the correction appended |
| 6 | `01-VALIDATION.md` `status: planned` / `wave_0_complete: false` | Closed — 14 files / 39 tests + 4 E2E re-run; all Wave-0 items confirmed |
| 7 | `03-VALIDATION.md` `status: draft` / `nyquist_compliant: false` | Closed — 18 files / 168 tests + 4/4 E2E re-run; all 8 Wave-0 items confirmed |
| 8 | Phase 1 human items 1, 2, 5 | Discharged by `e2e/{login-persistence,password-reset,mode-switch}.spec.ts` — 4 passed |
| 9 | 12 unticked v1 checkboxes in REQUIREMENTS.md | All ticked |
| 10 | MANAGE-03 traceability read "In progress" | Corrected to Complete |
| 11 | ROADMAP Phase 9 unticked; Phase 8 Wave 10/11 list stale | Phase 9 ticked; 08-18/19/20 ticked and 08-21/08-22 added |
| 12 | STATE.md `executing` / 8 phases / 99% | Now `verified` / 9 / 100%, with a current `stopped_at` |

**Deliberately NOT closed:** Phases 1, 2 and 5 remain `human_needed`. Their open items depend on
credentials this environment does not have and on a PayMongo beta that has not been granted. Flipping
them to `passed` would have been a fabrication, and the largest of them — real host payouts — is
exactly the risk a milestone audit exists to keep visible.

> *Superseded for Phase 1 on 2026-08-10 — and superseded in the only legitimate way: the credentials
> this environment did not have were obtained, and the items were walked. Phase 1 is `passed`. Phases 2
> and 5 stand exactly as written above; the PayMongo beta has still not been granted and real host
> payouts have still never moved real money.*

### Second pass — human-verification records (2026-08-05)

The first pass reconciled the milestone's *artifacts*. It did not reconcile the three phases' own
**human-verification records**, which turned out to be stale in both directions — carrying items as
open that the evidence already closed, and carrying one as closed-in-substance that the evidence only
half closes. `01-HUMAN-UAT.md` had not been touched since **2026-06-03** and did not reflect the
2026-08-01 reconciliation at all.

| # | Finding | Action |
|---|---|---|
| 1 | **The audit was wrong about Phase 1 item 6.** It asserted the signup-intent default "has never had recorded product sign-off". `01-HUMAN-UAT.md` item 6 has read `passed — user accepted "default to booker" (approved 2026-06-03)` since the day it was written. | Tech-debt bullet corrected and marked as a correction. Item 6 is **closed, not debt** — it was never open. |
| 2 | **The audit was wrong about which Phase-1 items are open.** It said "items 3, 4 and 6 remain open". | The open set is **3 and 4**. Item 2 is `partial`; items 1, 5 and 6 are closed. Corrected in the tech-debt list and the roll-up table. |
| 3 | Phase 1 item 5 (mode-switch) sat at `skipped` / "recommend a manual pass" while `e2e/mode-switch.spec.ts` covers both of its halves. | **skipped → passed**, discharged by the spec re-run 2026-08-05 (`4 passed (29.5s)` across the three Phase-1 specs). Recorded plainly: deterministic real-browser evidence, but the signature is the spec, not a human. Original 2026-06-03 result preserved in place. |
| 4 | Phase 1 item 2 (password-reset) sat at bare `[pending]` although the flow is fully proven. | **pending → partial**, with the reason stated: the spec reads the token from the Postgres `verification` table because Resend refuses the recipient domain (`422 validation_error`, "Please use our testing email address instead of domains like `example.com`", re-observed 2026-08-05). Flow proven; inbox delivery not. |
| 5 | Phase 1 items 3 and 4 were bare `[pending]` with no recorded reason. | Reasons recorded concretely: `GOOGLE_CLIENT_ID`/`SECRET` absent (`WARN [Better Auth]: Social provider google is missing clientId or clientSecret` on every boot), and all three server-side `CLOUDINARY_*` vars absent (only `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` set). Both stay **OPEN**. |
| 6 | Phase 2's Cloudinary human item was carried as outstanding, when `02-UAT.md` test 8 already records a human uploading and drag-reordering photos with a verdict line reading "Verified against real Postgres + real Cloudinary". | **Discharged on historical evidence**, with the qualification written in: it cannot be re-exercised today, and it does not cover the Phase-1 avatar surface. Phase 2's PayMongo hosted-KYC item stays **OPEN**, so the phase stays `human_needed`. |
| 7 | **The audit over-stated Phase 5 item 1**, calling it "discharged in substance". | **pending → partial**, not passed. Card (`pay_C4PW6fRGtUTNm6GsKCpt4P36`) and QR Ph (`pay_ru6sXqhRJto1NW3T83cqak4q` on `cs_809b1190ba4c3d44b7a77cdc`, ₱100.00) were genuinely hand-paid, plus three further real confirms on 2026-07-31. **GCash and Maya were never individually walked**, so the item's own "on each rail (card, GCash, Maya, QR Ph)" wording is not satisfied. |

**Nothing was flipped off `human_needed`, and nothing open was marked passed.** Phase 1 still has Google
OAuth and Cloudinary avatar creds open; Phase 2 still has PayMongo hosted KYC; Phase 5 still has the real
payout transfer and the QRPh operator-alert process. Two items moved to `partial` precisely *because*
"passed" would have been an inflation.

> *True as of 2026-08-05. Superseded for Phase 1 by the third pass below (2026-08-10), where the OAuth and
> Cloudinary creds were obtained and both items walked live. Phase 2 and Phase 5 are unchanged.*

Worth stating directly, since it is the least comfortable line in this document: **this pass found the
audit itself wrong twice** — once claiming a product sign-off had never happened when it was recorded in
the file the audit was summarising, and once calling a rail-by-rail item discharged when half its rails
had never been touched. Both errors ran in the same direction the first time (understating one item,
overstating another) and both are corrected above rather than quietly edited out. An audit that records
its own misreads is worth more than one that reads as consistent.

Documentation only — `git diff --name-only` for this pass touches nothing outside `.planning/`.

### Third pass — Phase 1 fully discharged (2026-08-10)

**2026-08-10 — Phase 1 moves `human_needed` → `passed`. 6 of 6 human-verification items discharged, zero
open.** Recorded by quick task `260810-hd6`; the underlying work was done and verified first-hand the
same day. This is the first time in the milestone that a phase has come **off** `human_needed`.

| # | Item | Was | Now | Evidence |
|---|---|---|---|---|
| 1 | Google OAuth live round trip (Ph1 item 3) | OPEN since 2026-06-03 (creds absent) | **PASSED** | Creds configured; the per-boot `Social provider google is missing clientId or clientSecret` warning is **gone**; `POST /api/auth/sign-in/social` **500 → 200**, now returning an authorize URL with PKCE `S256` + `state` + the exact `redirect_uri`; round trip completed to `/api/auth/callback/google`. **The D-08 auto-link observed for the first time:** providers `credential,google` on **ONE** row, `count(*) = 1` for the email, `email_verified` still `true`. |
| 2 | Cloudinary avatar upload (Ph1 item 4) | OPEN since 2026-06-03 (secrets absent) | **PASSED** | `avatar_public_id = fitout/avatars/LTzAEbLxKpeSXM4PgOhrZjniVZT51PCg`, `avatar_url` on `res.cloudinary.com/da8uglpk6/`, and the asset itself fetches **HTTP 200 · image/png · 60,933 bytes**. Own surface — no Phase-2 listing-photo evidence borrowed. |
| 3 | Password-reset real-inbox delivery (Ph1 item 2) | PARTIAL since 2026-08-05 | **PASSED** | The partial was **over-cautious, not wrong on facts**. The 2026-08-05 walkthrough delivered **both emails to a real inbox**, and its log line `GET /api/auth/reset-password/OdCLwmNLMSa2LiFWYfykacCj?callbackURL=%2Freset-password 302` is the **emailed link's** shape (token as URL segment + `callbackURL`), not the `?token=` shape the spec drives from a DB read. Delivery was proven that day; only the reading of it was conservative. |

**Two of those three were repairs, not proofs.** This is the finding worth carrying forward. Behind the
"blocked on credentials" label, "Continue with Google" was returning **HTTP 500** — a dead button shipping
on both `/login` and `/signup` — and `uploadAvatarAction` had **never executed once** since Phase 1,
because `src/app/actions/avatar.ts` exported a plain number from a `"use server"` module and Next rejected
the whole module at evaluation (fixed in quick `260807-fc6`). `avatar.test.ts` passed the entire time
because it imports the function directly and never crosses that boundary. The credential block was a true
statement that was also concealing breakage for nine phases. That is the case for un-blocking such items
rather than reasoning about them from unit tests.

**One trade accepted, not closed.** `accountLinking.trustedProviders: ["google"]` means a Google account
arrives `emailVerified: true` and therefore **bypasses the publish email-verification gate** at
`src/app/actions/listing.ts:375`. A Google signup can publish a listing having proven inbox control to
Google and never to FitOut. The operator accepted this knowingly, choosing to wire the button rather than
delete it — recorded here so it is not rediscovered as a surprise.

**One caveat carried forward, not a gap.** `e2e/password-reset.spec.ts` still reads its token from the
Postgres `verification` table because Resend refuses `example.com` recipients. That is a property of the
spec's fixture, not of the product.

**Phases 2 and 5 were deliberately NOT touched and remain `human_needed`.** Phase 2's PayMongo hosted
Linked-Accounts KYC round trip is blocked on a **sales-gated beta**. Phase 5 carries three things:
**real host payouts have never moved real money** (the `/v2` money-movement beta is not enabled on the
account), **GCash and Maya were never individually walked** (only card and QR Ph were hand-paid), and the
**QRPh operator routine has no scheduled query, UI or paging** even though the audit sink is now durable.
Phase 1 closing says nothing about any of them: its blockers were missing local credentials, theirs are a
third party withholding access and money that has never moved. Flipping either would be precisely the
fabrication this record-keeping exists to prevent.

Documentation only — `git diff --name-only` for this pass touches nothing outside `.planning/`.

---

_Audited: 2026-08-01 · Reconciled: 2026-08-01, 2026-08-05, 2026-08-10 · Auditor: Claude (gsd-audit-milestone)_
